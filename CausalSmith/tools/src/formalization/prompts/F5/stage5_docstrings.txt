You are documenting the Lean modules behind a machine-verified econometrics paper. The site renders each declaration's docstring as its natural-language statement, so every declaration listed below (currently missing a docstring, or flagged "FIX CROSSLINKS") must get one.

For EACH declaration listed, insert a Lean docstring (/-- ... -/) immediately above the declaration (above its attributes if it has any), or fix the flagged crosslink problem in the existing docstring. Do NOT change any statement, proof, name, attribute, or import — docstring edits only.

Convention (mandatory): the FIRST paragraph of each docstring is a natural-language translation of the formal statement, written for an econometrician with no Lean background — no Lean syntax, no field projections; state the mathematical content directly. One or two sentences suffice; an optional second paragraph may add technical detail. Read each declaration's statement before writing so the translation is faithful.

Crosslinks (mandatory for theorems): in the first paragraph, wrap the phrase translating each hypothesis as [phrase](hyp:name) — name is the binder's name in the signature; a comma list (hyp:h1,h2) lets one phrase cover several binders — and wrap the phrase stating the conclusion as [phrase](goal). Every hypothesis-classified binder and the conclusion must be covered; a theorem with no hypothesis-classified binders still links its conclusion with [phrase](goal). The phrase text itself stays plain readable English (the markup is invisible on the site); write the sentence to read naturally first, then place the brackets around the clauses that carry each hypothesis. Keep brackets inside a phrase balanced; unbalanced interval notation like (0, 1/2] must sit inside backticks or $…$ math, where the parser ignores brackets. Crosslinks live only in the first paragraph — no blank line between a bold title sentence and the annotated translation. A data/object binder the gate classifies as a hypothesis (e.g. an h-named function) is linked on the phrase that introduces the object ("for a design family …") — the link expresses NL↔Lean correspondence, not an assumption claim.

Crosslinks (mandatory for definitions, same markup): wrap the phrase introducing each explicit parameter as [phrase](hyp:paramName) (a section `variable` the definition uses is a parameter too — link it by its name), wrap the phrase that names the object being defined as [phrase](goal), and, when the value is given in several clauses (a run of `let` steps, equation alternatives `| pat => …`, or conjuncts of a `def … : Prop`), wrap the phrase for the N-th clause as [phrase](step:N) —N counts the clauses as the page numbers them: the top-level clauses, or — when the whole value/proposition is one "there exist … such that (1) … (k)" — those k clauses. Every explicit parameter and the object must be covered. An instance is annotated like a definition; an inductive links its parameters, the type as [phrase](goal), and its constructors as [phrase](hyp:ctorName) or [phrase](step:N).

Declarations (file paths relative to {{package_root}}; line numbers are approximate — locate by name if they have drifted):

{{decl_list}}

When finished, reply with DONE and a list of any declarations you could not find.
