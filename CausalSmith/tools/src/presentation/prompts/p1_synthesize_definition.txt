Named mathematical objects are USED in this paper's verified statements but NO
environment defines them — a reader cannot resolve them. Your job: produce faithful
Definition environments covering every listed symbol. Do NOT invent mathematics —
each definition must match how its symbols are used and how the note/Lean describe them.

The orphan symbols, each with the statement bodies that reference it:
{{symbols_block}}

D-stage definitions and symbols (the research stage's own constructions — when an
orphan symbol appears here, TRANSCRIBE its construction into paper notation rather
than reconstructing it from usage):
{{dstage_constructions}}

Research note (the verified mathematical content — the symbols' meanings are here, even
if the note never gave them their own blocks):
{{note_md}}

Lean source: the paper's modules are under `{{lean_subdir}}` (relative to the repo
root, your cwd). You may use lean-lsp / file tools to understand the objects, but the
output is only the paper titles and definition bodies.

GROUPING — one definition per object FAMILY, not per symbol: symbols that belong to a
single mathematical construction (the coordinates or parameter values of one object, a
family of companion polynomials, the pieces of one apparatus) are defined TOGETHER in
one environment with a collective title, exactly as a journal paper would. A symbol
with genuinely standalone content (a classical polynomial, a standard combinatorial
notation) keeps its own environment. Never force unrelated symbols into one body.

For EACH resulting definition:
- A short TITLE naming the object or family (econometrics register; notation may appear
  in it, e.g. "Higher-order Hölder ball $\mathcal H^\beta(L;[0,t_0])$").
- A BODY: self-contained defining statements — "We say <object> when <precise
  property>." Use the SAME notation and the SAME parameter names the statements use
  (if the usages write the radius as $C_\beta$, define it with $C_\beta$, not $L$).
  Pull the precise property from the note / source context; never a vague gloss. No
  `\begin/\end` of structural environments, no `\label`, no Lean identifiers, no
  formalization framing.
- List in "symbols" the exact symbol strings (verbatim from the input) the definition
  covers. Every input symbol must appear in exactly one definition's list, unless it
  genuinely cannot be defined faithfully — then omit it and it becomes a checkpoint
  advisory.

OUTPUT — return ONLY delimited blocks on stdout, one per definition, nothing else (no JSON: a
LaTeX body inside a JSON string is mangled by escape decoding):
@@@DEF@@@
SYMBOLS: <symbol>; <symbol>; ...   (verbatim input symbols, separated by "; " — a symbol never contains ";")
TITLE: <title>
@@@BODY@@@
<the defining statement(s), LaTeX, raw — no escaping>
@@@END@@@
