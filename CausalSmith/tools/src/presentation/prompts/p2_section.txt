Write ONE section of an econometrics working paper. The formal results are already proved and machine-verified in Lean; your job is the surrounding prose.

Paper section/object inventory (for context and ordering):
{{outline}}

This section's brief:
{{section_brief}}

Formal environments that belong in this section (copy each EXACTLY where the exposition calls for it — never edit anything between \begin{...v} and \end{...v}):
{{frozen_envs_for_section}}

Bib keys this section may cite:
{{allowed_bib_keys}}

Assumption guidance (the assumptions this section presents; explain each in prose right after its environment):
{{assumption_notes}}

Per-result notes from the research stage (grounded "why it matters / what it unlocks" for this
section's objects — UNTRUSTED pre-formalization prose: use it to motivate and interpret, but every
claim you state must be supported by the frozen environments, which alone say what is proved):
{{result_notes}}

Published dependencies behind generated theorem-local verification footnotes (the footnotes are
inserted mechanically after drafting; cite these sources where the argument invokes them):
{{cited_dependency_notes}}

Notation table:
{{notation_table}}

Earlier-section inventory, first-link ledger, and bounded continuity excerpt (do NOT reproduce them):
{{prior_sections}}

Referee revision brief (findings on a PREVIOUS draft that may bear on this section):
{{revision_brief}}

Rules:
- REVISION BRIEF: if the brief above lists findings (not the "first draft" sentinel), revise this section's prose to address each one that genuinely applies HERE — fix the flagged claim, qualifier, framing, or imprecision. Ignore items that target other sections or that you cannot fix without changing a frozen environment: NEVER reword, weaken, or strengthen anything between \begin{...v} and \end{...v} to satisfy a finding (the formal statements are immutable; a finding that seems to need a different theorem is out of scope — adjust only the surrounding prose). Write as if this were the first draft: never mention the review, a revision, a "previous version", or "as flagged".
- CONTINUITY: use the earlier-section inventory to see where objects were introduced and the recent prose excerpt for the transition. The excerpt is incomplete: when exact earlier terminology, a definition, or a back-reference needs checking, read ONLY the named section file needed for that question. Do not reread every prior section. Keep notation, symbols, and terminology consistent; introduce a symbol once and afterward refer back. Make back-references real (cite the actual `\cref{obj:...}` / section, never "as shown earlier" with no target), and avoid repeating earlier material.
- Econometrics journal register; complete sentences; motivate assumptions in prose around (not inside) the environments. Use the field's standard vocabulary; avoid imported CS/ML jargon ("frontier", non-statistical "kernel") unless citing a literature that uses it.
- PROSE FLOW (read as continuous exposition, not a checklist): never jump from a paragraph straight into a stack of formal environments — precede any run of environments with a transition sentence that motivates the group ("The analysis rests on the following conditions on …", "We impose three restrictions on the …"). Connect paragraphs and environments with real transitions so the section flows; and VARY sentence openings — consecutive sentences or glosses must not begin with the same stem (a column of "This is …" / "This assumption …" / "This condition …" reads as a template, not prose).
- Cite ONLY the allowed keys, with \citet/\citep.
- EXPLAIN EACH ASSUMPTION as connected prose, not gloss-per-environment: after an assumption, say what it controls and why it is reasonable, but when several related assumptions sit together, weave their glosses into one flowing passage with varied lead-ins rather than a repetitive sentence after each. Per the "Assumption guidance" above, when an assumption is marked STANDARD, name the condition and cite the supplied key with \citep{...}; when it is marked NOVEL, state that it is specific to this analysis and attach no citation. Never invent a citation key for an assumption.
- Every claim about what "we show/prove/establish" must match a frozen environment placed in the paper; do not generalize beyond it.
- INTUITION AFTER MAIN RESULTS: after each headline theorem, add a paragraph explaining the intuition behind the result, when there is one to give. Ground every explanatory sentence in the stated results; heuristic framing is fine, new claims are not.
- CONTRIBUTION FRAMING: explain every result and comparison through what it establishes and why that delivered fact matters. Outside a section explicitly titled Limitations, Future Work, or Open Questions, replace “this paper/result is not...”, “we do not...”, “we provide no...”, and absent-deliverable lists with an accurate affirmative scope statement. Keep all theorem conditions intact.
- No pipeline terminology (stage names, obj_ids outside environment arguments, "the note", "the pipeline").
- NEVER write \begin{proof}...\end{proof} blocks. The paper's only proofs are the separately rendered, Lean-audited theorem proofs supplied by the pipeline appendix; an appendix section presents the auxiliary statements with brief prose on their MATHEMATICAL role, not hand-written proof sketches (those are un-audited content and break the verification story). Do not manually write, duplicate, or soften a verification-scope footnote. For a theorem listed under published dependencies, state it in ordinary paper style and cite the published source at the proof/use site; the pipeline inserts the precise local trust-boundary footnote mechanically.
- Never use a formalization-derived symbol (a parameterized law/rule name) in prose without the cross-reference to the statement that defines it; if no statement defines it, describe the condition in words instead of naming it.
- NOTATION CONVENTIONS (setup section only): if this is the setup/assumptions section, open with a short paragraph fixing the paper's notational conventions (generic constants, norms, asymptotic notation, index ranges) before the first formal environment.
- NO FORMALIZATION FRAMING IN PROSE: never write "Lean-side", "Lean-side inputs", "Lean-verified inputs", or narrate the verification procedure ("after the checks have shown…"). Refer to a theorem's hypotheses as its assumptions/conditions (cite them by `\cref{obj:…}`), exactly as an ordinary econometrics paper would. A multi-word camelCase Lean identifier (e.g. smoothedInverseWeightRegression) must never appear in prose — use the mathematical notation or describe the object.
- SELF-CONTAINED: the paper is the first and only document — never reference the development process ("corrected", "revised", "previous version", "as flagged"); every named object, constant, and bound must be introduced within the paper before use.
- Output ONLY the LaTeX for this section, starting with \section{...}.

REQUIRED OUTPUT FORMAT: your stdout must be the raw LaTeX described above and nothing else — no code fences, no JSON object or receipt, no commentary, no file writes.

CROSS-REFERENCES: every formal environment owns `\label{obj:<obj_id>}`, where `<obj_id>` is the EXACT identifier in its argument. Cite it only with `\cref{obj:<obj_id>}` (or `\Cref` at sentence start); cleveref supplies the kind and number. Never prefix a manual kind word, never use legacy `\ref{...}` for any target, and never substitute note-style P-/T-/A-/L- aliases. Obj_ids may appear only in environment arguments and cleveref commands; never emit `\label` yourself.
NEVER write \label{obj:...} yourself anywhere — the environment macro defines it automatically; an extra one is a LaTeX duplicate-label error and reads as statement drift.
REMARKS PLACEMENT: explanation of a formal environment (why a hypothesis is needed, what a clause rules out, how it is used) belongs in the prose immediately after the environment, never inside it.

INLINE LEAN LINKS (\leanref): at the FIRST prose mention of a from-note object (listed below) that is NOT displayed as a formal environment in the paper — a setup/model notion, an assumption stated only in prose, a defined term used inline — wrap the naming phrase in \leanref{<obj-id>}{<phrase>}, using the EXACT obj-id from the list. The earlier-section link ledger is complete even though the prose excerpt is not: never link a target already in that ledger. Only the first mention; never wrap later mentions, and never wrap an object that already appears as a formal environment (the environment is its link). The obj-id inside \leanref is metadata, like the id inside \cref; it is not a "bare obj_id". Use \leanref for nothing outside this list; if the list is "(none)", do not use it.
From-note objects available for \leanref (obj-id [kind]: statement):
{{leanref_objects}}

INLINE SYMBOL LINKS (\leanref to a realized symbol): the core symbols below are realized in Lean (their `@realizes` tags). At the FIRST prose mention of such a symbol — in RUNNING TEXT — wrap its notation as a self-contained inline-math link: \leanref{sym:<name>}{$<the math>$} — e.g. "the outcome regression \leanref{sym:mu_a}{$\mu_a(x)$}", "the propensity \leanref{sym:e_P}{$e_P(x)$}".
CRITICAL — NEVER put a symbol \leanref inside a \(…\) or \[…\] display. A symbol inside a displayed equation cannot become a clickable link (it renders as part of the equation), and the nesting breaks rendering. So if a symbol's first appearance would be its DEFINING equation, do NOT link it there — link the nearest running-text mention instead. Example: write "the outcome regression \leanref{sym:mu_a}{$\mu_a(x)$} is defined by \(\mu_a(x)=E_P[Y\mid A=a,X=x]\)", NOT "\(\leanref{sym:mu_a}{$\mu_a(x)$}=E_P[Y\mid A=a,X=x]\)".
Only the first mention in the whole paper: never link a target in the earlier-section link ledger, including targets absent from the excerpt. Use the EXACT sym:<name> key from the list; if the list is "(none)", do not add symbol links. Match by meaning, not string: pick the sym: whose description is the symbol you are writing; if none clearly matches your notation, leave it unlinked.
Core symbols available for \leanref (sym:<name> [symbol]: realization description):
{{symbol_leanref_targets}}

VERIFICATION NOTE (appendix only, consolidated): if this section is the appendix/verification section, host a SINGLE short note — at the end of the appendix — summarizing that theorem statements and proofs are machine-checked in Lean 4. Do not replace the generated theorem-local footnotes: those separately identify published conclusions whose source proofs were not formalized. The introduction carries only one factual sentence pointing to the appendix/local notes. If this section is NOT the appendix, do not otherwise discuss formalization scope.
