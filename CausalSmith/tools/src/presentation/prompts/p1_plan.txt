You are planning the structure of an econometrics working paper. The formal content is fixed (machine-verified); do not write prose yet.

Build the strongest defensible reader-first framing from the frozen results and verified literature. Treat the research note as evidence, not as a template: do not inherit its title, hierarchy, process narrative, or emphasis. Use web search when needed to verify the closest prior work and the precise novelty boundary, but cite only keys in the verified citation pool and frame conservatively when evidence is incomplete.

Research note (the verified mathematical content, with P-/L-/T- object ids):
{{note_md}}

Contribution narrative from the research stage (UNTRUSTED pre-formalization prose: it may
advertise results the frozen layer does not deliver. Use it to choose the reader-first framing,
title, and section briefs; the contribution statement itself must rest only on the frozen layer):
{{contribution_narrative}}

Related-work brief:
{{related_work_brief}}

Frozen formal layer (the exact theorem/assumption/definition/lemma environments the paper must contain; each carries its obj_id):
{{frozen_layer_tex}}

Verified citation pool — these are the ONLY bib keys that exist; `bib:` lines must use them verbatim (never invent or rename keys):
{{pool_keys}}

Referee revision brief (structural findings to address on this re-draft; a no-op sentinel on a first draft — act ONLY on what is listed, change nothing else, and never alter a frozen statement):
{{revision_brief}}

Prior outline (when revising, preserve it except where a listed framing, positioning, or structural finding warrants renaming, reordering, or repartitioning; keep unrelated edits local):
{{prior_outline}}

Produce `outline.md` with exactly these sections:

# Title
On the line directly under this heading, put the proposed title as the bold span and nothing else before it: `**<the full title>**` (no "Title." label, no surrounding italics, no leading text). Put the one-sentence contribution statement on a later line (e.g. `**Contribution statement.** ...`). Econometrics journal register — the field's standard vocabulary, no imported CS/ML jargon ("frontier", non-statistical "kernel").

# Notation
A DEFINITION PLAN, not just a rendering map. One row per named symbol used in the frozen layer — every symbol must appear — with four fields:
  `<note symbol>` | `<paper notation>` | `<defining property in one phrase>` | `<home>`
- `<paper notation>`: the mathematical notation (never a Lean identifier).
- `<defining property>`: what the object IS (e.g. for a Hölder ball, "functions on $[0,t_0]$ with $s_\beta$ derivatives bounded by the radius and $\alpha_\beta$-Hölder top derivative"). One CONSISTENT parameter convention per symbol — pick the radius/argument names once and use them everywhere (never `\mathcal H^\beta(L;\cdot)` in one place and `\mathcal H^\beta(C_\beta;\cdot)` in another).
- `<home>`: WHERE a reader resolves it — the `obj_id` of the anchored Definition environment that introduces it (so statements can `\cref` it). Every named operator/class/law/rule used in a frozen statement must have a resolvable anchored environment home; if none exists in the frozen layer, list it on a top-level `notation_gaps: <symbol>=<why>, …` line so synthesis/review can add one (do not invent its mathematical content).
Standard ambient symbols a reader needs no definition for (ℝ, 𝔼, Pr, the covariate space 𝒳, Greek parameters) may be omitted.

The frozen layer is the sole notation inventory. Never copy a symbol from the research note,
revision brief, or prior outline unless that symbol literally occurs in a frozen environment body.
Delete stale prior rows and gaps for unused implementation/internal symbols; do not infer hidden
construction notation from a prose phrase in the frozen layer.
For every row, copy the symbol's full frozen-layer form exactly, including all order indices,
superscripts, arguments, bars, and domains. Do not shorten an indexed symbol into an alias (for
example, never turn `\operatorname{FeasFiber}^{b}_{m,2m+2}(t)` into
`\operatorname{FeasFiber}^{b}_{m}(t)`) unless a frozen definition explicitly introduces that alias.

# Sections
For each paper section, in order: a `## section: <name>` header, a one-paragraph brief of what it covers, a `objs:` line listing the obj_ids whose environments appear in that section (every obj_id in the frozen layer must be placed exactly once; L- lemmas normally go to the appendix), and a `bib:` line listing the bib keys that section may cite (subset of the pool).

Rules:
- Standard working-paper shape: introduction; related work (its own section or an introduction subsection, directly after the introduction material); setup and assumptions; main results; discussion/extensions; appendix with proofs and auxiliary lemmas. Literature positioning lives in the related-work section, not in the closing discussion; its brief must name the closest competing results (from the related-work brief) and the specific comparison the section makes against them. The discussion interprets the paper's own results, extensions, and limitations; it cites prior work only where needed to interpret a result, never to introduce positioning. The appendix ends with a brief verification note consolidating the Lean machine-checking scope (which results are verified, which inputs are only assumed). The introduction must NOT carry a formalized-vs-assumed inventory — only a single factual sentence pointing to that note; the formalization is not a listed contribution.
- APPENDIX-ONLY APPARATUS: an object (definition, assumption, auxiliary construction) whose symbols and content are used only by appendix material belongs in the appendix, placed before its first use there — never in the main body's setup. The main text defines only what its own sections use, and groups related definitions into one environment per object family rather than one per symbol.
- NATURAL PAPER AT EVERY TIER: organize the main text around the scientific question, standard mathematics, intuition, and results. Put declaration names, proof-engineering structure, certified-representation records, execution traces, and other verification machinery in a technical appendix or verification layer unless that machinery is itself scientifically essential. Never make the main exposition a translation of the formalization.
- Abstract and introduction are written LAST in a later stage; still include their planned briefs.
- Do not modify, restate, or paraphrase the frozen environments here.
- HONEST SCOPE. When the contribution narrative carries an honest-scope list, treat it as binding: the outline's contribution statement and section briefs must not frame any disclaimed comparison or strength as a result of the paper.
- REFUTED / DEAD OBJECTS. The note may discuss conjectures its own verdict marks as false/refuted, and quantities no frozen-layer theorem or assumption uses. Do NOT build a section, contribution claim, or interpretive narrative around either. A refuted conjecture and the objects exclusive to it may appear ONLY when explicitly labelled as a refuted/disproved conjecture — never as a property of the results; an object absent from the frozen layer (used by no proved theorem/assumption) gets no airtime as a feature of the paper's results. The contribution and every section brief must rest only on what the frozen layer actually proves.
- CITED DEPENDENCIES: published results used by a theorem are intentionally absent from the frozen environment inventory. Do not invent an assumption/result environment for them. Their consumers carry generated theorem-local verification-scope footnotes, and the proof prose may cite the source. Treat those footnotes as trust-boundary metadata, not as contributions or model assumptions.
- UNDELIVERED REMARKS: place a pre-existing `remarkv` object with status `undelivered` exactly once in a clearly labelled limitations/future-work discussion or appendix note. Frame it affirmatively as an open question or future direction, never as a delivered theorem, assumption, formalized conclusion, or contribution.
- KIND CHECK: an "assumption" that merely constructs or names a quantity (a tuning rule, a threshold sequence, an estimator, a derived object) is mathematically a definition — a human would write "let λ_n be …". For each such object add it to a single top-level line `env_overrides: <obj_id>=definitionv, …` (also usable in the reverse direction). Genuine restrictions on the data-generating process stay assumptions. The allowed override targets are: `definitionv`, `assumptionv`, `propositionv` (a proved result tiered as a Proposition — use to fix a result mis-presented under another head), `algorithmv` (see PROCEDURES), and `remarkv` (NON-load-bearing interpretive or open-question content — e.g. an `oeq:` open question, or a "definition" that is really a theorem-like/open-problem remark). NEVER demote a load-bearing object to `remarkv`: if any theorem or lemma depends on it, it is not a remark. Omit the line if no override is needed.
- REVISION: apply every listed structural or positioning finding, rebuilding the frame when needed; otherwise preserve the prior outline. Preserve every frozen statement verbatim.
- PROCEDURES: a definition that specifies an estimation or selection PROCEDURE — a multi-step construction (sample splitting into folds, candidate generation, evaluation/selection, an output statistic assembled from intermediate quantities) — is assigned `env_overrides: <obj_id>=algorithmv`. It is still a definition for every other purpose (placement, notation home, dependencies); the override only changes its presentation to a boxed, numbered-step algorithm. A definition that merely names a single formula or class stays `definitionv`. The paper's central estimator or procedure (the object the headline theorem is about) is placed in the MAIN BODY, never in the appendix — only its implementation realization (the apparatus that exists solely so a machine can execute or verify it) is appendix material, placed per the APPENDIX-ONLY APPARATUS rule.
- SYNTHESIZED DEFINITIONS: an environment whose obj_id is `synth_<k>` is a presentation definition introducing a named class/operator the statements use. Its placement is mechanical (immediately before its first use); list it or omit it, the pipeline places it either way.

REQUIRED OUTPUT FORMAT: your stdout must be the raw markdown content of outline.md and nothing else — it starts with the line `# Title`. Do NOT wrap it in code fences, do NOT emit a JSON object or receipt, do NOT attempt to write any file.
