You are documenting a Lean 4 library that formalizes causal inference, so that an
econometrician or statistician with ZERO Lean background can audit it. Two duties:

DUTY 1 — declaration docstrings. For EACH declaration listed below, ensure its docstring
(/-- … -/) starts with a natural-language translation of the formal statement as its
FIRST PARAGRAPH, followed by a blank line and then any implementation notes (keep
existing notes; move them below the first paragraph if needed).

The first paragraph must:
- be journal-register mathematical English, self-contained, 1-4 sentences;
- state every quantifier, hypothesis, and side condition of the formal statement —
  do not strengthen, weaken, or omit;
- define or name the objects in words (e.g. "the potential outcome of unit i under
  treatment a"), not Lean identifiers; backtick-quote a Lean name only when referring
  to the code object itself;
- contain NO Lean syntax and NO Lean-internal vocabulary: no ∀/fun/Prop/typeclass
  jargon, no field projections (write "the σ-algebra on the sample space", never
  `B.sigma`; "the probability measure of the bundle", never `B.μ`), no instance-binder
  names (`inst`, `inst_1`), no `Type*`/universe talk. A reader who has never seen Lean
  must not notice the source language. LaTeX math ($…$) is allowed and preferred for
  formulas;
- describe what the declaration itself defines or asserts — ignore `deriving` clauses,
  auto-generated instances, and other compiler-added companions;
- (theorems) carry NL↔Lean crosslinks: wrap the phrase translating each hypothesis as
  [phrase](hyp:binderName) — a comma list (hyp:h1,h2) when one phrase covers several
  binders — and wrap the phrase stating the conclusion as [phrase](goal). Cover EVERY
  hypothesis binder and the conclusion; a theorem with no hypothesis binders still links
  its conclusion with [phrase](goal), and may link a parameter (e.g. an assumption-carrying
  bundle) where a phrase naturally introduces it. Readability comes first: the markup is invisible
  on the rendered page, so write the sentence to read naturally as prose, then place the
  brackets around the clauses that already carry each hypothesis — never contort the
  sentence to fit the markup. Binder names appear ONLY inside (hyp:…), never in the
  visible text. Keep any brackets inside a phrase balanced; unbalanced interval
  notation like (0, 1/2] must sit inside backticks or $…$ math, where the parser
  ignores brackets. Crosslinks live ONLY in the first paragraph — never insert a blank
  line between a bold title sentence and the annotated translation. A structure's
  docstring may likewise link phrases to its field names with [phrase](hyp:fieldName);
- (definitions) carry crosslinks with the same markup: wrap the phrase introducing each
  explicit parameter as [phrase](hyp:paramName) (a section `variable` the definition uses
  is a parameter too — link it by its name), wrap the phrase that names the object being
  defined as [phrase](goal), and when the value is given in several clauses (a run of `let`
  steps, equation alternatives `| pat => …`, or the conjuncts of a `def … : Prop`) wrap the
  phrase for the N-th clause as [phrase](step:N) —N counts the clauses as the page numbers them: the top-level clauses, or — when the whole value/proposition is one "there exist … such that (1) … (k)" — those k clauses. Cover EVERY explicit parameter and the object. The `(goal)` phrase is the one a reader would point at to say what the thing IS
  ("the [observable IPW law](goal) is …"), not the whole sentence;
- (instances, inductives) an `instance` is annotated exactly like a definition (its `(goal)`
  phrase names the structure it provides; `(step:N)` the N-th `where` field or clause). An
  `inductive` links its parameters, wraps the phrase naming the type as [phrase](goal), and
  each constructor's phrase as [phrase](hyp:ctorName) (or [phrase](step:N) by position); a
  constructor's own `/-- … -/` docstring stays with the constructor.

DUTY 2 — module docstrings. For EACH file listed under "Files missing a module
docstring", add a module docstring at the top of the file (immediately after the
`import` lines, before any `open`/`namespace`):

/-! # <short human title>

<1-3 sentences: what this file provides and how it fits the library, in the same
Lean-free register as above.> -/

Rules:
- Every docstring you write or rewrite must be properly closed with `-/` on its last line — an
  unterminated `/--` swallows the declaration below it and breaks the build.
- NEVER change any code outside docstring comments (/-- … -/ and /-! … -/), so every file
  still compiles. Do NOT run `lake build` or any Lean tooling yourself — several of these
  passes run concurrently and verification happens once, after all of them.
- If an existing first paragraph already satisfies the rubric, leave it unchanged.
- Edit the files in place.

Declarations to document (file : line : name : formal statement):
{{targets}}

Files missing a module docstring:
{{module_doc_files}}
