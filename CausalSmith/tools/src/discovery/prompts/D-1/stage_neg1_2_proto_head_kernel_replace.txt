=== MODE: kernel-replace (vN+1, same angle + anchor, NEW kernel) ===

The Stage -0.5 reviewer judged this angle's KERNEL structurally defective, not patchable — a kernel-level `C-definitional-unfold` (the headline reduces to unfolding a definition) or `C-tautological-iff` (the claimed iff is true by definition). Revise cannot save it: the focal object itself is wrong. KEEP this angle's question, seeds, literature anchor, and prior-art framing; SWAP the core kernel for a genuinely non-routine one. Do NOT re-ideate or re-run the literature scan.

Inputs: the PRIOR PROPOSAL CORE is injected below; the PRIOR REVIEWER VERDICT names the kernel-level flag — read its detail to see exactly why the old kernel collapses.

1. Diagnose the collapse in one line (e.g. "the iff held because both sides were the same residual condition by definition").
2. Choose a NEW focal object on the SAME angle that does NOT reduce to a definition: it must expose a sign-flip / regime boundary / impossibility / efficiency frontier / closed-form characterization the anchor's authors would find non-obvious — not a re-encoding of the same identity. Keep the motif unless the motif caused the collapse.
3. Re-state the main statements for the new kernel (demote to `conjecture` anything not strictly stronger than published prior art); keep `symbols`/`assumptions`/`definitions` that survive, replace those tied to the old kernel.
4. Re-aim `related_work` and each statement's `gap` at the NEW kernel. Re-run the WELL-POSEDNESS SELF-CHECK. Re-emit the FULL core.

Terminal status: `completed` (new non-routine kernel authored) OR `needs-pivot` (no non-routine kernel exists on this angle/anchor — do NOT pad with another definitional restatement). Re-emit `literature_checklist` inside the typed core.
