=== MODE: pivot (v1 after the prior cursor exhausted) ===

The previous angle exhausted its current review path. By default you author the FIRST version of a NEW angle, reusing the cold-start ideation substrate — do NOT re-run Step 0 seed generation or the breadth literature map; both carry over.

Priority rule: an injected ORCHESTRATOR ESCALATION LOG may explicitly order a reviewed kernel to carry forward across mechanical cap bookkeeping. That directive overrides the default structural-pivot instruction below: rebuild the directed kernel with its listed repairs and do not call it dead merely because the cursor or revise cap exhausted. Absent such an explicit carry-forward directive, follow the new-angle procedure below.

Inputs (in the brief / injected blocks): the ranked `seed_list` from this run's cold-start, the `exhausted_angles` already tried and rejected, and the rejection note for the dead kernel.

1. Pick a STRUCTURALLY DIFFERENT surviving seed from `seed_list`, skipping any exhausted angle — a different mathematical object / proof route from the dead kernel, not a cosmetic relabel. If every surviving seed is structurally close to the dead kernel, emit `status:"needs-pivot"`.
2. Re-run Step 1 (deepen literature scan) on the chosen seed only: 2–4 adjacent threads, confirm settled-vs-open, confirm no closer paper subsumes it. If it now has fewer than ~3 prior-art candidates, drop it and take the next surviving seed; if none survive, emit `status:"needs-pivot"`.
3. Author the typed core for the new angle per WHAT TO EMIT + the disciplines in the body below; run the WELL-POSEDNESS SELF-CHECK. Write a fresh `cluster`, `novelty_justification`, and `literature_checklist` for the new angle inside the typed core.
