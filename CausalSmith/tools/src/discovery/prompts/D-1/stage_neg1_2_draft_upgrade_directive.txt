=== UPGRADE MODE DIRECTIVE (orchestrator-injected; you are upgrading a banked parent along a declared axis, at a novelty tier at or above the parent's) ===

The orchestrator has supplied an `=== UPGRADE CONTEXT ===` block below containing the parent proposal, derivation, README, and literature map. The rules below OVERRIDE the cold-start ideation steps in the base prompt; everything else (skeleton invariants, output format, soundness self-review, anti-patterns) remains in force.

Upgrade mode contract:

(UM1) **The parent is fixed.** You will NOT propose a structurally-different angle. The kernel of this run is an upgrade of the parent kernel along the declared `upgrade_axis`. Pivots (Step 0b alternative seeds) are NOT available in upgrade mode — if the upgrade axis cannot deliver, you must emit `status: "needs-pivot"` so the orchestrator can drop the upgrade attempt and the user can re-launch with a different parent or axis.

(UM2) **Cluster is locked.** The proposal anchors in the same cluster as the parent (panel / exactid / partialid / stat / experimentation / scm). Cross-cluster pivots are out of scope — they are different questions, not upgrades.

(UM3) **Step 0a is short-circuited.** The parent's literature_map is supplied verbatim below. You MUST:
  - Verify (web search / context7) at least 3 of the parent's named bibkeys still match the claim the parent attributes to them.
  - Add at least ONE new published anchor that pins the upgrade_axis to the active frontier (an open problem the axis resolves, a tension the parent did not address, or a recent paper the upgrade refines).
  - Cite both reused and new anchors in §5 Related work; mark reused entries with `[reused from parent]`.

(UM4) **Step 0b becomes Step 0b'.** Instead of generating ≥10 phenomenon-form seeds, produce 2–5 candidate **upgrade moves** along the declared `upgrade_axis`. Each move must:
  - State the parent claim verbatim or near-verbatim that the move targets.
  - State the upgrade move in one sentence ("parent has X qualitatively; this run delivers X quantitatively with rate r" / "parent assumes A; this run weakens to A' and shows the kernel reduces to parent as the corner" / etc.).
  - Pass the per-axis rubric in the UPGRADE CONTEXT `axis_rubric` field below. If you cannot phrase any move that passes the rubric, emit `status: "needs-pivot"`.

(UM5) **The core prose must lead with the upgrade delta.** In `tldr` and `project_justification.fill`, state what is new vs the parent before what is new vs published literature — name the parent qid+spec, the upgrade axis, and the delta concretely. Put the published-axis positioning in `related_work`.

(UM6) **At least one statement node must carry the upgrade delta.** A pure re-derivation of the parent kernel under a new assumption set without exposing new content is the upgrade-mode failure mode (`N-upgrade-thin` from the reviewer). Add a load-bearing `openendedquestion` node (`oeq:` id plus concrete Handle) for the genuine open content exposed by the upgrade. Its `justification`, `gap`, `consumer`, and core-authored `delta_summary` must make clear why that node is the upgrade delta along the declared `upgrade_axis`.

(UM7) **The typed core must include the parent-reduction check.** Represent "reduces to parent under <named assumption/definition>" as an explicit statement/definition dependency or an `honest_scope` reduction note. This is the corner-case check proving the upgrade is a strict refinement of the parent, not a different theorem. If the upgrade does not reduce, it is a pivot — emit `needs-pivot`.

(UM8) **Typed-core JSON additions.** When upgrade mode is active, the typed proposal core MUST also include the following fields. Do not put them in the stdout disposition receipt:
  - `upgrade_mode: true`
  - `parent_qid: "<echoed from UPGRADE CONTEXT>"`
  - `parent_spec: "<echoed from UPGRADE CONTEXT>"`
  - `upgrade_axis: "computation|estimation|generalization|mechanism"`
  - `delta_summary: "<one paragraph naming the concrete delta the upgrade adds, mapped to the axis rubric>"`
  - `reused_bibkeys: ["<bibkey>", ...]` (array of parent bibkeys you verified and reused)
  - `new_bibkeys: ["<bibkey>", ...]` (array of bibkeys you added beyond the parent's literature_map)

Workflow override: Steps 0a / 0b in the base prompt are REPLACED by UM3 / UM4 above. Step 1a (catalogue duplicate-check) still runs — the upgrade must not duplicate a catalogue entry other than the parent. Step 1b still runs to deepen literature beyond the parent's anchors.

The reviewer (Stage -0.5) will read `upgrade_mode`, `upgrade_axis`, `delta_summary`, and the per-axis rubric. Similarity to the parent kernel is EXPECTED and will not be flagged; the reviewer scores the delta_summary against the axis rubric instead.

=== END UPGRADE MODE DIRECTIVE ===
