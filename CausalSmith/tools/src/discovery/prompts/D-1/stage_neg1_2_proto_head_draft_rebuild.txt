=== MODE: draft-rebuild (vN+1, same kernel, REWRITTEN construction) ===

The Stage -0.5 reviewer judged the KERNEL sound but the DRAFT EXECUTION broken — ≥2 construction-level flags from {`C-sanity` (arithmetic / normalization wrong), `C-coherence` (fields contradict each other), `C-wellposed` (an object used before it is defined), `N-promissory-object` (a headline promises an object the core never exhibits)}. KEEP the focal object and the headline statements; REWRITE the supporting construction, definitions, and downstream statements cleanly on the SAME kernel. Do NOT re-ideate, change the kernel, or re-run the literature scan.

Inputs: the PRIOR PROPOSAL CORE is injected below; the PRIOR REVIEWER VERDICT carries each construction-level flag's detail (e.g. the exact computation that was wrong).

Repair per flag and re-emit the FULL core:
- C-sanity → redo the flagged computation from scratch; make the corrected value / invariant explicit (a sanity-reduction statement that now actually collapses).
- C-coherence → reconcile the contradicting items; make notation, normalization, and definitions consistent end to end.
- C-wellposed → define every object (`def:`/`symbol`) precisely BEFORE any statement uses it; turn promissory phrasing into a well-posed claim.
- N-promissory-object → actually EXHIBIT the promised object (give the explicit `definition.construction`), or demote the headline to match what is genuinely delivered.

Re-run the WELL-POSEDNESS SELF-CHECK over the rewritten items. Terminal status: `completed` (construction rebuilt clean on the same kernel) OR `needs-pivot` (the construction cannot be made sound because the focal object is itself a definitional unfold). Re-emit `literature_checklist` inside the typed core.
