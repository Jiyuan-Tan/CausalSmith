\section{First-order risk and the second-order rate}\label{sec:rates}

The finite orbit reduction in \cref{obj:thm:exact-response-type-game} turns the unrestricted labeled-schedule risk into a finite response-type decision problem. This section first identifies the first-order benchmark in the original labeled game and then refines that benchmark by an explicit second-order construction. Throughout the section, the contrast \(c\) is fixed, nonzero, and zero-sum on the treatment-arm set from \cref{obj:def:labeled-schedule-game}.

The first-order procedure allocates experimental effort in proportion to the absolute contrast weights and estimates the centered contrast by a clipped Horvitz--Thompson rule. The clipping interval is the natural range of the finite-population contrast target.

\begin{definitionv}{def:first-order-procedure}[First-order rule $\widehat\tau_{\mathrm{cHT}}^\star$]
Let
\[
S_c=\{a\in\mathcal A_K:c_a\ne 0\},\qquad
L_c=\sum_{a\in\mathcal A_K}|c_a|,\qquad
q_a^\star=\frac{|c_a|}{L_c}\quad(a\in S_c).
\]
Assign each \(A_i\) independently on \(S_c\) with \(\Pr(A_i=a)=q_a^\star\). The projected contrast-weighted Horvitz--Thompson rule is
\[
\widehat\tau_{\mathrm{cHT}}^\star
=
\operatorname{proj}_{[-L_c/2,L_c/2]}\!\left[
n^{-1}\sum_{i=1}^n\sum_{a\in S_c}
c_a\mathbf 1\{A_i=a\}\frac{Y_i^{\mathrm{obs}}-1/2}{q_a^\star}
\right].
\]
\end{definitionv}

The active support \leanref{sym:S_c}{\ensuremath{S_c}} selects the arms that enter the contrast, while the contrast norm \leanref{sym:L_c}{\ensuremath{L_c}} fixes both the target range and the first-order scale. The assignment probabilities \leanref{sym:q^\star}{\ensuremath{q^\star}} place larger sampling mass on arms with larger absolute contrast weights, matching the variance contribution of the centered Horvitz--Thompson summands.

\begin{definitionv}{synth_5}[Contrast-weighted independent design]
Let \(S_c=\{a\in\mathcal A_K:c_a\ne0\}\), \(L_c=\sum_{a\in\mathcal A_K}|c_a|\), and
\[
q_a^\star=\begin{cases}|c_a|/L_c,&a\in S_c,\\ 0,&a\notin S_c.\end{cases}
\]
The design \(\mathcal D^\star\) is the product assignment law under which \(A_1,\ldots,A_n\) are independent and \(\Pr_{\mathcal D^\star}(A_i=a)=q_a^\star\) for every unit \(i\) and arm \(a\).
\end{definitionv}

\Cref{obj:synth_5} records the same allocation as a design object. This formulation will also be used for the shrinkage rule below, so that the first- and second-order procedures share the same contrast-weighted randomization scheme.

\begin{theoremv}{thm:first-order-saddle}[First-order minimax constant and attaining procedure]
Fix a natural number \(K\) and a nonzero zero-sum contrast \(c\) on the treatment-arm set \(\mathcal A_K\), in the labeled-schedule game of \cref{obj:def:labeled-schedule-game}. Let \(L_c\), \(q_a^\star\), and \(\widehat\tau_{\mathrm{cHT}}^\star\) be the contrast norm, contrast-weighted assignment probabilities, and projected contrast-weighted Horvitz--Thompson rule from \cref{obj:def:first-order-procedure}, and set
\[
C_0(c)=\frac{L_c^2}{4}.
\]
Then the unrestricted labeled-schedule minimax risk satisfies
\[
\lim_{n\to\infty} n\,\rho_n(c)=C_0(c).
\]
Moreover, for every \(n\ge 1\), the independent contrast-weighted design together with \(\widehat\tau_{\mathrm{cHT}}^\star\) attains the finite-sample worst-case bound
\[
\max_{z\in\mathcal T_K^n}
R_n(\mathcal D^\star,\widehat\tau_{\mathrm{cHT}}^\star;z)
\le
\frac{C_0(c)}{n}.
\]
\end{theoremv}

The theorem gives the first-order robustness constant and a finite-sample certificate attaining its envelope. The constant \(C_0(c)=L_c^2/4\) depends on the contrast through its \(\ell_1\) norm, so the allocation benchmark is determined by the signed estimand rather than by a default equal-allocation rule. The bound is stated in the unrestricted labeled-schedule game of \cref{obj:def:labeled-schedule-game}, which places it on the same decision scale as the finite orbit value characterized in \cref{obj:thm:exact-response-type-game}.

The converse input comes from embedding the sharp two-arm difficulty into the multi-arm contrast. This comparison links every fixed contrast to the two-arm contrast \((1,-1)\), and it also gives an exact specialization when the contrast has two active arms.

\begin{theoremv}{thm:embedded-two-arm-converse}[Embedded two-arm converse]
Fix integers \(K\) and \(n\), and a contrast \(c\) on \(\mathcal A_K\), with labeled-schedule minimax risk \(\rho_n(c)\) as in \cref{obj:def:labeled-schedule-game}. Suppose that
\begin{itemize}
\item \textbf{(Arm count.)} \(K\geq 2\).
\item \textbf{(Population size.)} \(n\geq 1\).
\item \textbf{(Contrast.)} \(c\) is nonzero and zero-sum.
\end{itemize}
Let
\[
C_0(c)=\frac{L_c^2}{4},
\qquad
\rho_{n,2}=\rho_n((1,-1)),
\qquad
d_n(c)=\frac{C_0(c)}{n}-\rho_n(c),
\]
where \(\rho_{n,2}\) is the unrestricted two-arm minimax risk at population size \(n\). Then
\[
C_0(c)\rho_{n,2}\leq \rho_n(c)
\qquad\text{and}\qquad
\rho_n(c)\leq \frac{C_0(c)}{n}.
\]
Moreover, if \(n\geq 8\), then
\[
0\leq d_n(c),
\qquad
d_n(c)\leq C_0(c)\{n^{-1}-\rho_{n,2}\},
\qquad
d_n(c)\leq 43\,C_0(c)n^{-4/3}.
\]
Finally, writing \(S_c=\{a\in\mathcal A_K:c_a\neq 0\}\) for the active support, if \(|S_c|=2\), then
\[
\rho_n(c)=C_0(c)\rho_{n,2}
\qquad\text{and}\qquad
d_n(c)=C_0(c)\{n^{-1}-\rho_{n,2}\}.
\]
\end{theoremv}

The embedded comparison supplies the lower side of the second-order scale. It says that any improvement over the first-order envelope for a fixed multi-arm contrast is bounded by the corresponding two-arm improvement, up to the scale factor \(C_0(c)\). When the active support has size two, the comparison becomes an equality, so the two-arm subproblem fully determines the multi-arm risk for that contrast.

The second-order construction keeps the contrast-weighted design and modifies the estimator. The rule first normalizes the centered Horvitz--Thompson score to the interval scale \(h_c=L_c/2\), averages the normalized scores, and then applies a clipped shrinkage toward zero over a bandwidth of order \(n^{-1/3}\).

\begin{definitionv}{synth_16}[Regular variation of a normalizer]
A positive sequence \((a_n)_{n\ge1}\) is regularly varying with index \(\beta\) when, for every \(x>0\), \[ \frac{a_{\lfloor xn\rfloor}}{a_n}\longrightarrow x^\beta \qquad\text{as }n\to\infty . \]
\end{definitionv}

\begin{definitionv}{synth_6}[Clipped-shrinkage estimator]
Put \(h_c=L_c/2\), and let
\[
\lambda_c
=
\min\left\{
\frac{\left|\sum_{a\in\mathcal A_K} c_a t_a\right|}{h_c}
:
t\in\mathcal T_K,\
\sum_{a\in\mathcal A_K} c_a t_a\neq 0
\right\}
\]
be the smallest nonzero normalized response-type contrast magnitude. Under \(\mathcal D^\star\) of \cref{obj:synth_5}, define
\[
W_i=\sum_{a\in S_c}\frac{c_a\mathbf 1\{A_i=a\}}{q_a^\star}\left(Y_i^{\mathrm{obs}}-\frac12\right),
\qquad
V_i=\frac{W_i}{h_c},
\qquad
X_n=\frac1n\sum_{i=1}^nV_i.
\]
For
\[
b_n=n^{-1/3},\qquad
\varepsilon_n=\frac{\sqrt{\lambda_c}}4n^{-1/3},\qquad
g_n(x)=\max\{-b_n,\min(x,b_n)\},
\]
define the clipped-shrinkage rule
\[
\widehat\tau_n^{\mathrm{sh}}
=\operatorname{proj}_{[-h_c,h_c]}\bigl(h_c\{X_n-\varepsilon_ng_n(X_n)\}\bigr).
\]
The projection is never active for \(n\ge1\), because \(|X_n|\le1\), \(0\le b_n\le1\) and \(0\le\varepsilon_n\le1/4\); it is written explicitly only so that the rule takes values in the target interval by construction. This estimator is evaluated as a function of the realized assignment and observed outcomes.
\end{definitionv}

The regular-variation definition in \cref{obj:synth_16} is used to state the uniqueness of the second-order normalizing rate. In \cref{obj:synth_6}, the centered arm score \(W_i\), its normalized version \(V_i\), and their average \(X_n\) are all computed under the same design as in \cref{obj:synth_5}. The constants \(b_n\) and \(\varepsilon_n\) set the clipping width and shrinkage amplitude; their common \(n^{-1/3}\) scale produces an \(n^{-4/3}\) risk correction after averaging.

\begin{theoremv}{thm:universal-second-order-rate}[Universal second-order rate]
For every fixed \(K\geq 2\) and every nonzero zero-sum contrast \(c\), let \(\lambda_c\), the smallest nonzero normalized response-type contrast magnitude, be
\[
\lambda_c
=
\min\left\{
\frac{\left|\sum_{a\in\mathcal A_K} c_a t_a\right|}{L_c/2}
:
t\in\mathcal T_K,\ 
\sum_{a\in\mathcal A_K} c_a t_a\neq 0
\right\}.
\]
Define
\[
C_0(c)=\frac{L_c^2}{4},
\qquad
A_c=\frac{\sqrt{\lambda_c}}{2}-\frac{\lambda_c}{16},
\qquad
B_c=\frac{7\lambda_c}{16},
\qquad
\kappa_c=\frac{1}{2}\min\{A_c,B_c\},
\]
and
\[
d_n(c)=\frac{C_0(c)}{n}-\rho_n(c),
\qquad
s_n=n^{4/3}.
\]
The following assertions hold:
\begin{itemize}
\item \textbf{(Spacing.)} The constants satisfy
\[
0<\lambda_c\leq 1,
\qquad
0<\kappa_c.
\]

\item \textbf{(First-order envelope.)} For every \(n\),
\[
\rho_n(c)\leq \frac{C_0(c)}{n}.
\]

\item \textbf{(Uniform shrinkage bound.)} There is a finite integer \(N_c\) such that, for every integer \(m\geq N_c\),
\[
4\sqrt{\lambda_c}\,m\exp\{-m^{1/3}/8\}
\leq
\frac{\sqrt{\lambda_c}}{2}-\frac{\lambda_c}{16},
\]
and, for every \(n\geq N_c\), the explicit clipped-shrinkage procedure \(\widehat\tau_n^{\mathrm{sh}}\) satisfies
\[
\sup_{z\in\mathcal T_K^n}
R_n(\mathcal D^\star,\widehat\tau_n^{\mathrm{sh}};z)
\leq
C_0(c)\left\{n^{-1}-\kappa_c n^{-4/3}\right\}.
\]

\item \textbf{(Second-order envelope.)} The normalized second-order improvement satisfies
\[
C_0(c)\kappa_c
\leq
\liminf_{n\to\infty} n^{4/3}d_n(c)
\leq
\limsup_{n\to\infty} n^{4/3}d_n(c)
\leq
43C_0(c).
\]

\item \textbf{(Regular-variation index.)} For every positive sequence \(a_n\), every \(\beta\), and every \(C>0\), if \(a_n\) is regularly varying with index \(\beta\) and
\[
a_n d_n(c)\to C,
\]
then \(\beta=4/3\).
\end{itemize}
\end{theoremv}

\Cref{obj:thm:universal-second-order-rate} establishes the \(n^{-4/3}\) order of the improvement for every fixed nonzero zero-sum contrast. The positive spacing constant \(\lambda_c\) is defined over the finite response-type space, so the shrinkage amount can be chosen directly from the contrast. The lower side of the displayed envelope is witnessed by the explicit clipped-shrinkage procedure of \cref{obj:synth_6}, which guarantees an improvement of size \(C_0(c)\kappa_c n^{-4/3}\); the upper side follows from the embedded two-arm comparison in \cref{obj:thm:embedded-two-arm-converse}. The two sides pin the order of the improvement, not its constant. For contrasts with active support of size at least three, the same sandwich applies after the orbit reduction to the genuinely multi-arm response-type count game; support-two contrasts follow the exact two-arm reduction. The regular-variation clause characterizes \(n^{4/3}\) as the unique regularly varying normalization that can yield a positive finite limit for the improvement.

It remains to record the two-arm lower bound used inside the embedded comparison. The result is stated for the unrestricted labeled-schedule minimax risk with the two-arm contrast \((1,-1)\), matching the notation of \cref{obj:thm:embedded-two-arm-converse}.

\begin{theoremv}{thm:coarse-two-arm-minimax-lower}[Coarse two-arm lower bound]
For every integer \(n\ge 8\), let \(\rho_{n,2}:=\rho_n((1,-1))\) denote the unrestricted labeled-schedule minimax risk for the two-arm contrast \((1,-1)\). Then
\[
\frac{(1-n^{-1/3})^2}
{n/(1-n^{-2/3}/4)+40n^{2/3}}
\le \rho_{n,2}
\]
and
\[
n^{-1}-43n^{-4/3}\le \rho_{n,2}.
\]
\end{theoremv}

The bound supplies a fully internal rate input for the two-arm risk. Combined with \cref{obj:thm:embedded-two-arm-converse}, it transfers the \(43C_0(c)n^{-4/3}\) upper bound on the second-order improvement to every fixed multi-arm contrast. Together with the shrinkage construction in \cref{obj:thm:universal-second-order-rate}, the section therefore pins down the second-order minimax scale while retaining the contrast-weighted design identified in \cref{obj:thm:first-order-saddle}.
