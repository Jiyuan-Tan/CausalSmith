{
  "agent": "coordinator",
  "round": 1,
  "callId": "main",
  "model": "codex",
  "prompt": "You are the COORDINATOR. A substrate module has PASSED review — it is correct, sorry-free, and reusable. Your job is to INTEGRATE it into the Causalean library cleanly and permanently: put it in the right place, reuse what already exists instead of reinventing, and document it to the project's standard. You do NOT re-judge the math.\n\n## What was built\nStaging files (namespace `CausalSmith.Substrate.PairwiseAffineSimultaneousCongruenceStability.*`) under: <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability\n- <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability/Definitions.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability/LocalBranch.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability/Main.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability/PairwiseControl.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/PairwiseAffineSimultaneousCongruenceStability/Stability.lean\n\n## Requirement it fulfilled\n# Substrate requirement: pairwise-affine simultaneous congruence stability\n\n## Goal\nExtend the reusable quantitative simultaneous-congruence theory with an explicit operator-norm stability theorem based on pairwise affine separation, allowing the witnessing environment triple to depend on the coordinate pair.\n\n## Provides (API contract)\n- A paper-independent predicate expressing uniform pairwise affine separation: for every distinct coordinate pair `i,j`, some three environments (depending on `i,j`) have the affine `2 × 2` determinant of their shift-coordinate points bounded below in absolute value by a common positive margin `δ`.\n- Coordinate-product and diagonal-branch perturbation lemmas derived directly from one pair's selected triple under approximate simultaneous congruence.\n- A theorem bounding the Euclidean/operator norm between an exact unit-diagonal reference and an approximately feasible unit-diagonal candidate linearly in the residual tolerance, under pairwise affine separation and explicit scale/condition envelopes.\n- A corollary whose displayed constant is no larger than, or can be monotonically weakened to, `16 * (6*M/γ) * sqrt(p*(p-1)*(1+L^2)) * L^3`, where `L = ((p!)*κ^(p-1))^(1/p)`, under the matching dimension, scale, separation, and condition assumptions.\n\n## Statement / milestones\nLet `p > 0`, let `E` be a finite nonempty environment type, and let `s : E → Fin p → ℝ` be bounded diagonal shift vectors. For every `i ≠ j`, assume there exist `e₀,e₁,e₂ : E`—not required to be common across pairs—such that the affine determinant\n\n`(s e₁ i - s e₀ i) * (s e₂ j - s e₀ j) - (s e₂ i - s e₀ i) * (s e₁ j - s e₀ j)`\n\nhas absolute value at least `δ > 0`.\n\nGiven an invertible unit-diagonal reference matrix `B₀` satisfying exact simultaneous congruence diagonalization for the family, and a unit-diagonal candidate `B` whose simultaneous-congruence off-diagonal residuals are uniformly at most `ε ≥ 0`, assume explicit bounds on shift/covariance scale and on condition numbers (or equivalent inverse/norm envelopes) of `B₀` and `B`. For sufficiently small `ε`, prove an explicit linear bound `‖B-B₀‖op ≤ C ε`.\n\nDerive the needed off-diagonal coordinate-product and diagonal-branch controls pair by pair from that pair's own selected triple. Do not replace pairwise separation by a single base plus `p` environments with a nonzero full `p × p` determinant. The API may first prove a Frobenius/L2 bound and convert it to the Euclidean operator norm with explicit constants.\n\nProvide a final specialized/corollary constant expressible or weakenable to\n`16 * (6*M/γ) * sqrt(p*(p-1)*(1+L^2)) * L^3`, `L = ((p!)*κ^(p-1))^(1/p)`, when the generic parameters are instantiated by the corresponding paper-independent scale `M`, pairwise margin `γ`, and determinant/condition envelope `κ`.\n\n## Standard reference\nStandard finite-dimensional perturbation analysis for identifiable simultaneous congruence diagonalization: solve each two-coordinate affine system using a determinant lower bound, aggregate entrywise estimates into Frobenius/operator norm control, and use determinant plus condition estimates to control normalization branches. This is the pairwise analogue of the existing neutral quantitative theory under `Causalean.Discovery.LinearDisentanglement.Quantitative`.\n\n## Intended reuse\nReusable whenever simultaneous-diagonalization identifiability is certified by all pairwise affine minors rather than a single full-dimensional affine determinant. The immediate consumer is a non-effective local contraction theorem, but this module must mention neither that paper nor BACKSHIFT systems, confidence unions, corruption, coverage, or finite-environment application wiring. It must expose generic finite real matrix/shift-family results.\n\n## May assume / must derive\nMay assume: positive finite dimension; finite nonempty environment family; bounded shifts; pairwise affine-separation witnesses and common margin; exact reference feasibility; approximate candidate feasibility; unit-diagonal normalization; invertibility; explicit scale and condition/inverse bounds; nonnegative residual tolerance plus a stated quantitative smallness condition.\n\nMust derive: the two-coordinate affine-system estimates for each pair; off-diagonal product/branch control; aggregation over all ordered or unordered coordinate pairs; the linear operator-norm bound and its explicit parameter dependence; and the stated constant-specialization/weakened corollary. Do not assume full-dimensional affine separation, a common environment tuple for every pair, the final stability estimate, or a local inverse theorem with the desired conclusion.\n\n## Non-goals (optional)\nNo paper-specific `*_Research` imports or types; no BACKSHIFT model, replacement/corruption family, confidence region, finite-sample inference, real-closed-field algorithm, or software. Do not strengthen pairwise witnesses to a global full-dimensional determinant. Do not claim an effective data-driven radius beyond the explicit deterministic smallness condition used by the stability theorem.\n\n## Known building blocks (optional)\nReuse the definitions, norm estimates, transition-matrix algebra, determinant/condition bounds, and residual machinery in `Causalean.Discovery.LinearDisentanglement.Quantitative` wherever faithful. Add a separate pairwise module rather than changing the meaning of `AffineMinorSeparated`. Use Mathlib finite sums/maxima, real inequalities, square roots/powers/factorials, finite-dimensional matrix norms, determinant and inverse estimates. Imports may use only Mathlib, Causalean, and neutral modules in this study staging tree.\n\n\n## Your tools\n- Read the staging files and inspect the Causalean tree (root = the directory containing `Causalean.lean`; sources under `Causalean/`).\n- Project search — run from `CausalSmith/tools`: `npm run search -- \"<concepts>\"` (find existing decls to reuse/dedup against), `npm run search -- --scope module \"<concepts>\"` (find the correct topical HOME file/area — use this for placement), `npm run search -- --type \"<pattern>\"` / `--goal \"<goal>\"`.\n- lean-lsp for signatures / declaration files.\n\n## What to decide\n1. PLACEMENT — merge-first. Find the substrate's natural topical home with `--scope module`. STRONGLY prefer MERGING each decl into an existing `Causalean/.../X.lean`; only CREATE a new topic file inside the narrowest faithful existing top-level subject area. Never invent a generic holding area. Target ≤600 lines; split before ~900 only for an independent topic/proof chain.\n2. NAMESPACE — rewrite every `CausalSmith.Substrate.PairwiseAffineSimultaneousCongruenceStability` reference to the chosen Causalean namespace (the namespace of the file it lands in).\n3. REUSE / DEDUP — for each decl, search Causalean for one that already proves the SAME statement. If it exists, drop your reinvented version and use the existing one (add its import, point references at it). Reuse ONLY on genuine statement-equality — a rewire to a decl that merely looks similar will break the build. Dedup edits stay inside YOUR new content; never delete or alter an existing Causalean decl.\n4. DOCUMENTATION (docstring-canonical) — every declaration that will render as a public library card gets a `/-- … -/` docstring whose FIRST paragraph is a self-contained natural-language translation for an econometrician with no Lean background (no Lean syntax, no field projections like `B.sigma` — say \"the σ-algebra on B\"; no \"For every …\" preamble restating the signature). This includes named local definitions or instances when the index extracts and displays them. `doc:gen` hard-fails if any published card would show “No natural-language translation yet.” Every new file opens with a `/-! … -/` module docstring stating what it provides. Theorem and lemma first paragraphs carry NL↔Lean CROSSLINKS: wrap the phrase translating each hypothesis or explicit binder in `[phrase](hyp:binderName)` (comma list `(hyp:h1,h2)` when one phrase covers several) and the conclusion phrase in `[phrase](goal)`, covering every binder and the conclusion; write the sentence to read naturally first, then bracket. `npm run lint:nl-links` is part of the integration gate and hard-fails on any incomplete or unannotated headline theorem.\n5. HEADLINE + PAGE-DESCRIPTION curation — audit EVERY promoted Lean file and every newly created published namespace path before emitting the manifest. The documentation gate requires every file containing a public `theorem` or `lemma` to have at least one matching declaration in `headline_theorems` in the correct `doc/library_review/<Area>.json`. Curate MAIN results normally (identification theorem, estimand characterization, named decomposition, asymptotic linearity/normality/rate/optimality/efficiency result, sharp bound). If a theorem-bearing file contains only technical/supporting results, still choose its most representative reusable public theorem or lemma as that file's single required headline anchor; additional technical steps remain supporting-tier. Also give every NEW explorer page a short, reader-facing description: use the first paragraph of a roll-up module's `/-! ... -/` docstring when that module exists, otherwise add `namespace_intros[\"<path below Area>\"]` to the area sidecar. Include intermediate directories created by a new subtree (for example, adding `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.*` also requires a description for `AlgebraicGeometry`). Preserve every existing sidecar entry and stage the COMPLETE updated sidecar with a `write_file` op. Before output, cross-check the promoted file list and every new page path against the staged sidecar; never rely on a later failed `doc:gen` run to discover omissions.\n6. API.md — for a NEW module, add a `<!-- GEN:Causalean.<Module> -->` / `<!-- /GEN -->` marker pair in `doc/API.md` where the per-decl table belongs (the system's `doc:gen` fills it — do NOT hand-write the table), plus any cross-cutting narrative for the new file.\n\n## Output — WRITE bodies to staging, emit a TINY manifest\nCRITICAL, read twice: do NOT put file contents inside the JSON. Large Lean bodies inside a JSON string are the #1 cause of a parse failure that aborts the whole integration (a truncated/mis-escaped string cannot be recovered). Instead:\n\n1. WRITE every file body and every insert patch as a SEPARATE FILE under the staging directory:\n   <repo-root>/CausalSmith/doc/study/pairwise-affine-simultaneous-congruence-stability/coordinate_staging/round_1\n   Use short names, e.g. `<repo-root>/CausalSmith/doc/study/pairwise-affine-simultaneous-congruence-stability/coordinate_staging/round_1/foo.lean`, `<repo-root>/CausalSmith/doc/study/pairwise-affine-simultaneous-congruence-stability/coordinate_staging/round_1/merge_x.lean`, `<repo-root>/CausalSmith/doc/study/pairwise-affine-simultaneous-congruence-stability/coordinate_staging/round_1/area.json`. Put the EXACT bytes you want written/inserted into each staged file (namespaced, docstringed, ready to apply).\n2. Emit ONLY a small JSON manifest that REFERENCES those staged files by `from` (relative to the staging dir). It contains no code — just short paths and one short anchor line per merge, so it always parses.\n\n`{ \"ops\": [ … ], \"notes\": \"<brief rationale>\" }`. `target` paths are RELATIVE to the Causalean root. Op kinds:\n\n- `{ \"kind\": \"create_file\", \"target\": \"Causalean/…/Foo.lean\", \"from\": \"foo.lean\", \"newModule\": \"Causalean.….Foo\" }` — write the staged `from` file as a NEW file at `target`. Set `newModule` (the Lean module name) ONLY for a new Lean module, so it is root-wired into `Causalean.lean`. Omit `newModule` for a new non-Lean record file.\n- `{ \"kind\": \"merge_lean\", \"target\": \"Causalean/…/X.lean\", \"anchor\": \"<a line already in X.lean>\", \"from\": \"merge_x.lean\" }` — INSERT the staged `from` content after the first line containing `anchor` (empty anchor → append at end of file). INSERT-ONLY: the system preserves every existing byte and ROLLS BACK if a merge would change existing content. To add an import to an existing file, use a separate `merge_lean` anchored on an existing `import …` line (its `from` file holds just the import line). Multiple `merge_lean` ops may target the same file. If dependent inserts share one file/anchor, combine them into one staged file in dependency order, or anchor each later insert on text introduced by the prior one. Pick a SHORT, UNIQUE anchor line (a section header, a decl name) so it is unambiguous and easy to escape in JSON.\n- `{ \"kind\": \"write_file\", \"target\": \"doc/library_review/<Area>.json\", \"from\": \"area.json\" }` — full-content write of a NON-Lean record file (headline sidecar, `doc/API.md`). Read the current file first, write the COMPLETE updated content to the staged `from` file (add entries/markers, never drop existing ones), and reference it here.\n\n## Hard rules\n- NEVER modify or delete an existing line in an existing Lean file. Integrating must be additive (new files + insert-only merges). If it genuinely requires editing an existing proven decl, do NOT — pick a different placement and explain in `notes`.\n- Final Lean content may import and reference only Mathlib/Causalean modules. Rewrite every temporary staging reference; no `CausalSmith` import or qualified reference may remain in a staged create/merge body.\n- Every `target` stays within the Causalean root; every `from` is a file you wrote under the staging dir.\n- Do NOT run `lake build` / `library_index` / `embed` / `doc:gen` yourself — the system runs the full gate and rolls back on any failure. Emit only the manifest.\n- Keep the manifest JSON small and valid: no code inside it, no trailing commas, all strings closed. If unsure, fewer ops with cleaner anchors.\n",
  "promptBytes": 15313,
  "rawOutput": "{\"ops\":[{\"kind\":\"create_file\",\"target\":\"Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/Definitions.lean\",\"from\":\"definitions.lean\",\"newModule\":\"Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Definitions\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/PairwiseControl.lean\",\"from\":\"pairwise_control.lean\",\"newModule\":\"Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseControl\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/LocalBranch.lean\",\"from\":\"local_branch.lean\",\"newModule\":\"Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.LocalBranch\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/Stability.lean\",\"from\":\"stability.lean\",\"newModule\":\"Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Stability\"},{\"kind\":\"write_file\",\"target\":\"doc/library_review/Discovery.json\",\"from\":\"discovery.json\"},{\"kind\":\"write_file\",\"target\":\"doc/API.md\",\"from\":\"api.md\"}],\"notes\":\"Adds a separate pairwise-affine quantitative stability chain under the existing linear-disentanglement quantitative API, reusing its matrix and congruence primitives without changing the global affine-minor theory.\"}",
  "parsed": {
    "ops": [
      {
        "kind": "create_file",
        "target": "Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/Definitions.lean",
        "from": "definitions.lean",
        "newModule": "Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Definitions"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/PairwiseControl.lean",
        "from": "pairwise_control.lean",
        "newModule": "Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseControl"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/LocalBranch.lean",
        "from": "local_branch.lean",
        "newModule": "Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.LocalBranch"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Discovery/LinearDisentanglement/Quantitative/PairwiseAffine/Stability.lean",
        "from": "stability.lean",
        "newModule": "Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Stability"
      },
      {
        "kind": "write_file",
        "target": "doc/library_review/Discovery.json",
        "from": "discovery.json"
      },
      {
        "kind": "write_file",
        "target": "doc/API.md",
        "from": "api.md"
      }
    ],
    "notes": "Adds a separate pairwise-affine quantitative stability chain under the existing linear-disentanglement quantitative API, reusing its matrix and congruence primitives without changing the global affine-minor theory."
  },
  "ok": true,
  "durationMs": 435884
}
