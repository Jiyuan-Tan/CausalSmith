{
  "agent": "coordinator",
  "round": 1,
  "callId": "main",
  "model": "codex",
  "prompt": "You are the COORDINATOR. A substrate module has PASSED review — it is correct, sorry-free, and reusable. Your job is to INTEGRATE it into the Causalean library cleanly and permanently: put it in the right place, reuse what already exists instead of reinventing, and document it to the project's standard. You do NOT re-judge the math.\n\n## What was built\nStaging files (namespace `CausalSmith.Substrate.UnitCubeFactorizationRnTransport.*`) under: <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport\n- <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport/Clamp.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport/Factorization.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport/Main.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport/ReferenceMeasure.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/UnitCubeFactorizationRnTransport/RnTransport.lean\n\n## Requirement it fulfilled\n# Substrate requirement: unit_cube_factorization_rn_transport\n\n## Goal\nBuild reusable measure-theoretic adapters that turn support-local unit-cube density data into a `Causalean.Graph.FiniteDensity.UnitCubeFactorization` and transport canonical Radon--Nikodym ratios through a measurable equivalence specified only on the measures' supports.\n\n## Provides (API contract)\n- A coordinatewise clamping/retraction map from a finite real product to `Set.pi Set.univ (fun _ => Set.Icc (0 : ℝ) 1)`, with measurability, range, and identity-on-cube lemmas.\n- A constructor for `UnitCubeFactorization` from factors that are measurable or continuous on the cube, parent-local on the cube, nonnegative, and normalized in their own coordinate on the cube. The constructor must use a globally measurable clamped extension and preserve the original factor on the cube.\n- A theorem identifying `Measure.pi (fun _ => unitIntervalReference)` with Lebesgue volume restricted to the finite unit cube, or an equivalent API sufficient to identify the constructed `withDensity` measure with an existing cube-restricted product-density measure.\n- A support-restricted RN transport theorem: if finite measures `μ` and `ν` are supported on `S`, maps `f` and `g` are measurable on the relevant supports and mutually inverse there, then the canonical RN derivative of `map μ f` with respect to `map ν f`, evaluated after `f`, agrees `ν`-a.e. on `S` with the RN derivative of `μ` with respect to `ν`. Expose a pushforward-law corollary for the real-valued RN ratio.\n\n## Statement / milestones\nFor a finite index type, define the coordinate clamp `clampCube`. Prove that it is measurable, maps every point into the unit cube, and equals the identity on the cube. Given cube-local factors `p i` whose values depend only on `i` and its DAG parents and whose own-coordinate integrals equal one, define global factors by `p i (clampCube v)` (with an `ENNReal.ofReal` wrapper when the input is real-valued). Prove the `Measurable`, `parent_local`, and `normalized` fields required by `UnitCubeFactorization`, and prove that its observational and single-target intervention measures equal the corresponding cube-restricted product-density measures.\n\nFor finite measures `μ ≪ ν` concentrated on `S`, and a support-local measurable equivalence `f : α → β`, `g : β → α` between `S` and `T`, prove the RN transport identity without requiring `f` to be a globally measurable embedding. An acceptable proof may pass to subtypes or replace `f` off `S` by a globally measurable embedding, but the final theorem must require only measurability/inverse/injectivity on the support. Deduce equality of `Measure.map (fun x => (μ.rnDeriv ν x).toReal) ν` with the corresponding mapped-measure ratio law after composition with `f`.\n\nAll central declarations must be sorry-free and axiom-clean. The final API must compose with `Causalean.Graph.FiniteDensity.Factorization.targetRatio_map_eq`.\n\n## Standard reference\nThe first component is the standard measurable-extension-by-retraction construction for densities on a compact product cube, together with the finite-product identification of uniform coordinate reference measure. The second component is the standard invariance/change-of-variables property of Radon--Nikodym derivatives under a bimeasurable isomorphism, localized to full-measure supports. Mathlib's global `MeasurableEmbedding.rnDeriv_map` is the reference near-match; the required result is its support-restricted form.\n\n## Intended reuse\nThe immediate consumer is `CausalSmith.ExactID.EID_CrlCoverratioMmdGenericity.ratio_nonancestor_zero`. It must internally construct `FiniteDensityObservedWorldBridge W` from the existing `PositiveNormalizedSmoothMechanisms`, `SharedDiffeomorphicMixing`, and `OnePerfectInterventionPerNode` hypotheses, then combine the bridge with `Factorization.targetRatio_map_eq`. No bridge premise may be added to `ratio_nonancestor_zero` or to the frozen `simultaneous_confidence_edges` theorem.\n\nThe substrate should live in general Mathlib/Causalean-facing modules and be reusable for finite density-factorized causal models on compact product supports and for other support-local changes of variables.\n\n## May assume / must derive\nMay assume finite index types and standard Borel coordinate spaces; local continuity or measurability of factors on the cube; nonnegativity, parent locality, and own-coordinate normalization on the cube; finiteness and absolute continuity of the two measures; measurable support sets; concentration of the measures on those supports; and maps that are measurable and mutually inverse on the supports.\n\nMust derive a globally measurable clamped extension, the `UnitCubeFactorization` fields, equality with the cube-restricted product-density measures, the support-local RN transport identity, and its ratio-law pushforward corollary. Must not assume the desired observational/interventional ratio-law equality, a global embedding not supplied by the support-local hypotheses, or any paper conclusion.\n\nThe study must not import `CausalSmith/*_Research`. Before coordination, main should extract any needed generic aliases/interfaces from the research folder: the finite real product cube, the cube-restricted product-density form of observational/intervention laws, and the support-local mix/unmix inverse conditions. The promoted theorem should depend only on Mathlib and general Causalean modules, including `Causalean.Graph.FiniteDensity`.\n\n## Non-goals (optional)\nDo not reprove finite-DAG ancestral marginal invariance, general disintegration, arbitrary nonsingular transformations, or the paper's decoder theorem. Do not encode the paper's `ObservedWorld`, target permutation, MMD, or canonical observed-law definitions in the promoted substrate.\n\n## Known building blocks (optional)\n- `Causalean.Graph.FiniteDensity.UnitCubeFactorization`, `Factorization.targetRatio_map_eq`, and `unitIntervalReference`.\n- Mathlib `Measure.restrict_pi_pi`, `Measure.withDensity_congr_ae`, and `Measure.withDensity_absolutelyContinuous'`.\n- Mathlib `MeasurableEmbedding.rnDeriv_map` as the global theorem to localize via subtypes or full-measure congruence.\n- Coordinate projection to `Set.Icc`, `Continuous.clamp`, finite-product measurability, `Measure.map_map`, and `Measure.map_congr`.\n\n\n## Your tools\n- Read the staging files and inspect the Causalean tree (root = the directory containing `Causalean.lean`; sources under `Causalean/`).\n- Project search — run from `CausalSmith/tools`: `npm run search -- \"<concepts>\"` (find existing decls to reuse/dedup against), `npm run search -- --scope module \"<concepts>\"` (find the correct topical HOME file/area — use this for placement), `npm run search -- --type \"<pattern>\"` / `--goal \"<goal>\"`.\n- lean-lsp for signatures / declaration files.\n\n## What to decide\n1. PLACEMENT — merge-first. Find the substrate's natural topical home with `--scope module`. STRONGLY prefer MERGING each decl into an existing `Causalean/.../X.lean`; only CREATE a new topic file inside the narrowest faithful existing top-level subject area. Never invent a generic holding area. Target ≤600 lines; split before ~900 only for an independent topic/proof chain.\n2. NAMESPACE — rewrite every `CausalSmith.Substrate.UnitCubeFactorizationRnTransport` reference to the chosen Causalean namespace (the namespace of the file it lands in).\n3. REUSE / DEDUP — for each decl, search Causalean for one that already proves the SAME statement. If it exists, drop your reinvented version and use the existing one (add its import, point references at it). Reuse ONLY on genuine statement-equality — a rewire to a decl that merely looks similar will break the build. Dedup edits stay inside YOUR new content; never delete or alter an existing Causalean decl.\n4. DOCUMENTATION (docstring-canonical) — every declaration that will render as a public library card gets a `/-- … -/` docstring whose FIRST paragraph is a self-contained natural-language translation for an econometrician with no Lean background (no Lean syntax, no field projections like `B.sigma` — say \"the σ-algebra on B\"; no \"For every …\" preamble restating the signature). This includes named local definitions or instances when the index extracts and displays them. `doc:gen` hard-fails if any published card would show “No natural-language translation yet.” Every new file opens with a `/-! … -/` module docstring stating what it provides. Theorem and lemma first paragraphs carry NL↔Lean CROSSLINKS: wrap the phrase translating each hypothesis or explicit binder in `[phrase](hyp:binderName)` (comma list `(hyp:h1,h2)` when one phrase covers several) and the conclusion phrase in `[phrase](goal)`, covering every binder and the conclusion; definition first paragraphs likewise wrap each explicit parameter's phrase in `[phrase](hyp:paramName)`, the phrase naming the defined object in `[phrase](goal)`, and the N-th given-by clause (let-step / equation alternative / conjunct) in `[phrase](step:N)`, covering every explicit parameter and the object; write the sentence to read naturally first, then bracket. `npm run lint:nl-links` is part of the integration gate and hard-fails on any incomplete or unannotated headline theorem.\n5. HEADLINE + PAGE-DESCRIPTION curation — audit EVERY promoted Lean file and every newly created published namespace path before emitting the manifest. The documentation gate requires every file containing a public `theorem` or `lemma` to have at least one matching declaration in `headline_theorems` in the correct `doc/library_review/<Area>.json`. Curate MAIN results normally (identification theorem, estimand characterization, named decomposition, asymptotic linearity/normality/rate/optimality/efficiency result, sharp bound). If a theorem-bearing file contains only technical/supporting results, still choose its most representative reusable public theorem or lemma as that file's single required headline anchor; additional technical steps remain supporting-tier. Also give every NEW explorer page a short, reader-facing description: use the first paragraph of a roll-up module's `/-! ... -/` docstring when that module exists, otherwise add `namespace_intros[\"<path below Area>\"]` to the area sidecar. Include intermediate directories created by a new subtree (for example, adding `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.*` also requires a description for `AlgebraicGeometry`). Preserve every existing sidecar entry and stage the COMPLETE updated sidecar with a `write_file` op. Before output, cross-check the promoted file list and every new page path against the staged sidecar; never rely on a later failed `doc:gen` run to discover omissions.\n6. API.md — for a NEW module, add a `<!-- GEN:Causalean.<Module> -->` / `<!-- /GEN -->` marker pair in `doc/API.md` where the per-decl table belongs (the system's `doc:gen` fills it — do NOT hand-write the table), plus any cross-cutting narrative for the new file.\n\n## Output — WRITE bodies to staging, emit a TINY manifest\nCRITICAL, read twice: do NOT put file contents inside the JSON. Large Lean bodies inside a JSON string are the #1 cause of a parse failure that aborts the whole integration (a truncated/mis-escaped string cannot be recovered). Instead:\n\n1. WRITE every file body and every insert patch as a SEPARATE FILE under the staging directory:\n   <repo-root>/CausalSmith/doc/study/unit_cube_factorization_rn_transport/coordinate_staging/round_1\n   Use short names, e.g. `<repo-root>/CausalSmith/doc/study/unit_cube_factorization_rn_transport/coordinate_staging/round_1/foo.lean`, `<repo-root>/CausalSmith/doc/study/unit_cube_factorization_rn_transport/coordinate_staging/round_1/merge_x.lean`, `<repo-root>/CausalSmith/doc/study/unit_cube_factorization_rn_transport/coordinate_staging/round_1/area.json`. Put the EXACT bytes you want written/inserted into each staged file (namespaced, docstringed, ready to apply).\n2. Emit ONLY a small JSON manifest that REFERENCES those staged files by `from` (relative to the staging dir). It contains no code — just short paths and one short anchor line per merge, so it always parses.\n\n`{ \"ops\": [ … ], \"notes\": \"<brief rationale>\" }`. `target` paths are RELATIVE to the Causalean root. Op kinds:\n\n- `{ \"kind\": \"create_file\", \"target\": \"Causalean/…/Foo.lean\", \"from\": \"foo.lean\", \"newModule\": \"Causalean.….Foo\" }` — write the staged `from` file as a NEW file at `target`. Set `newModule` (the Lean module name) ONLY for a new Lean module, so it is root-wired into `Causalean.lean`. Omit `newModule` for a new non-Lean record file.\n- `{ \"kind\": \"merge_lean\", \"target\": \"Causalean/…/X.lean\", \"anchor\": \"<a line already in X.lean>\", \"from\": \"merge_x.lean\" }` — INSERT the staged `from` content after the first line containing `anchor` (empty anchor → append at end of file). INSERT-ONLY: the system preserves every existing byte and ROLLS BACK if a merge would change existing content. To add an import to an existing file, use a separate `merge_lean` anchored on an existing `import …` line (its `from` file holds just the import line). Multiple `merge_lean` ops may target the same file. If dependent inserts share one file/anchor, combine them into one staged file in dependency order, or anchor each later insert on text introduced by the prior one. Pick a SHORT, UNIQUE anchor line (a section header, a decl name) so it is unambiguous and easy to escape in JSON.\n- `{ \"kind\": \"write_file\", \"target\": \"doc/library_review/<Area>.json\", \"from\": \"area.json\" }` — full-content write of a NON-Lean record file (headline sidecar, `doc/API.md`). Read the current file first, write the COMPLETE updated content to the staged `from` file (add entries/markers, never drop existing ones), and reference it here.\n\n## Hard rules\n- NEVER modify or delete an existing line in an existing Lean file. Integrating must be additive (new files + insert-only merges). If it genuinely requires editing an existing proven decl, do NOT — pick a different placement and explain in `notes`.\n- Final Lean content may import and reference only Mathlib/Causalean modules. Rewrite every temporary staging reference; no `CausalSmith` import or qualified reference may remain in a staged create/merge body.\n- Every `target` stays within the Causalean root; every `from` is a file you wrote under the staging dir.\n- Do NOT run `lake build` / `library_index` / `embed` / `doc:gen` yourself — the system runs the full gate and rolls back on any failure. Emit only the manifest.\n- Keep the manifest JSON small and valid: no code inside it, no trailing commas, all strings closed. If unsure, fewer ops with cleaner anchors.\n",
  "promptBytes": 16089,
  "rawOutput": "{\"ops\":[{\"kind\":\"merge_lean\",\"target\":\"Causalean/Graph/FiniteDensity/Cube.lean\",\"anchor\":\"import Mathlib.MeasureTheory.Measure.Lebesgue.Basic\",\"from\":\"cube_imports.lean\"},{\"kind\":\"merge_lean\",\"target\":\"Causalean/Graph/FiniteDensity/Cube.lean\",\"anchor\":\"end Causalean.Graph.FiniteDensity\",\"from\":\"merge_clamp.lean\"},{\"kind\":\"merge_lean\",\"target\":\"Causalean/Graph/FiniteDensity/Cube.lean\",\"anchor\":\"## Unit-cube reference measure\",\"from\":\"merge_reference.lean\"},{\"kind\":\"merge_lean\",\"target\":\"Causalean/Graph/FiniteDensity/Cube.lean\",\"anchor\":\"## Unit-cube factorization constructors\",\"from\":\"merge_factorization.lean\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Mathlib/MeasureTheory/SupportRnDerivTransport.lean\",\"from\":\"SupportRnDerivTransport.lean\",\"newModule\":\"Causalean.Mathlib.MeasureTheory.SupportRnDerivTransport\"},{\"kind\":\"write_file\",\"target\":\"doc/library_review/Graph.json\",\"from\":\"Graph.json\"},{\"kind\":\"write_file\",\"target\":\"doc/library_review/Mathlib.json\",\"from\":\"Mathlib.json\"},{\"kind\":\"write_file\",\"target\":\"doc/API.md\",\"from\":\"API.md\"}],\"notes\":\"Cube adapters merge into the existing finite-density cube module; support-local RN transport is a new Mathlib-facing measure-theory module. No statement-equal Causalean declaration was found.\"}",
  "parsed": {
    "ops": [
      {
        "kind": "merge_lean",
        "target": "Causalean/Graph/FiniteDensity/Cube.lean",
        "anchor": "import Mathlib.MeasureTheory.Measure.Lebesgue.Basic",
        "from": "cube_imports.lean"
      },
      {
        "kind": "merge_lean",
        "target": "Causalean/Graph/FiniteDensity/Cube.lean",
        "anchor": "end Causalean.Graph.FiniteDensity",
        "from": "merge_clamp.lean"
      },
      {
        "kind": "merge_lean",
        "target": "Causalean/Graph/FiniteDensity/Cube.lean",
        "anchor": "## Unit-cube reference measure",
        "from": "merge_reference.lean"
      },
      {
        "kind": "merge_lean",
        "target": "Causalean/Graph/FiniteDensity/Cube.lean",
        "anchor": "## Unit-cube factorization constructors",
        "from": "merge_factorization.lean"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Mathlib/MeasureTheory/SupportRnDerivTransport.lean",
        "from": "SupportRnDerivTransport.lean",
        "newModule": "Causalean.Mathlib.MeasureTheory.SupportRnDerivTransport"
      },
      {
        "kind": "write_file",
        "target": "doc/library_review/Graph.json",
        "from": "Graph.json"
      },
      {
        "kind": "write_file",
        "target": "doc/library_review/Mathlib.json",
        "from": "Mathlib.json"
      },
      {
        "kind": "write_file",
        "target": "doc/API.md",
        "from": "API.md"
      }
    ],
    "notes": "Cube adapters merge into the existing finite-density cube module; support-local RN transport is a new Mathlib-facing measure-theory module. No statement-equal Causalean declaration was found."
  },
  "ok": true,
  "durationMs": 837972
}
