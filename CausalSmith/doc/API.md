# CausalSmith API

Per-file / per-declaration documentation for the **Lean** source of the CausalSmith package — a sibling of the foundational `Causalean/` library. Each catalogue entry corresponds to one CausalSmith research run (completed or in-flight); the matching natural-language specifications live under `CausalSmith/doc/research/active/<qid>/`, and banked runs under `CausalSmith/doc/research/_bank/<tier>/`. Promotion of an entry from `CausalSmith/` into `Causalean/` is a deliberate human step — `CausalSmith` is the staging area, `Causalean` is the curated foundation.

> **Scope.** This file documents the **Lean library** only. The pipeline that produces these theorems — the `causalsmith` CLI, the D-/F-stages, banking, presentation, and the TypeScript tooling under `tools/` — is documented in [`USER_MANUAL.md`](USER_MANUAL.md). Do not duplicate pipeline or tooling material here; it belongs in the manual.

## Workspace context

`CausalSmith/` is a sibling Lean package: `CausalSmith/lakefile.toml` declares `[[require]] path = ".."` against the Causalean workspace, so the dependency is one-way (`CausalSmith → Causalean`, never the reverse). Top-level Lean source roots inside `CausalSmith/CausalSmith/`:

    Panel/            panel / linear-projection theorems (panel_* runs)
    ExactID/          exact-identification theorems (eid_* runs)
    PartialID/        partial-identification theorems (pid_* runs)
    Stat/             estimation / inference theory for a causal estimand (stat_* runs)
    Experimentation/  design-based / randomization-inference theorems (exp_* runs)
    SCM/              graphical-identification theorems (scm_* runs)
    Mathlib/          Mathlib-shaped helpers staged for promotion to Causalean/Mathlib
    Substrate/        thin re-export umbrellas over Causalean substrate (no own declarations)

Each research run lands under `CausalSmith/<Cluster>/<QidCamel>_Research/` with a run barrel `<QidCamel>_Research.lean` importing every module of that run, so `lake build` of the barrel type-checks the whole run over fresh oleans. The `CausalSmith.Research` umbrella (`CausalSmith/CausalSmith/Research.lean`) imports a subset of completed runs; the full tree is swept by `cd CausalSmith/tools && npm run build:full-tree`, since most run modules sit outside the default build's import graph. 

## 1. Panel theorems

Panel / linear-projection outputs (`panel_*` runs); one entry per completed run.

### 1.1 PPML forbidden comparisons in staggered-adoption panels — pseudo-true weighted-FWL signs and a four-cohort reversal

Panel/Theorems paper-scoped entries under `CausalSmith/Panel/PANEL_PpmlForbiddenComparison_Research/` (`panel_ppml_forbidden_comparison`, v1). The paper characterizes the limiting naive unit-and-time-fixed-effect PPML coefficient under staggered adoption: its local response to a treated-cell log effect has the sign of an endogenous, fitted-mean-weighted FWL residual. It also supplies a concrete four-cohort configuration with strictly positive effects but a negative pooled PPML coefficient, and a primitive frontier diagnostic together with the counterfactual-share PTT alternative.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.unit_fe_collapse` | `(T C) → ValidPanelHorizon T → ValidCohortSupport T C → cohort-share, untreated-exponential-mean, within-cohort-baseline, proportional-effects, and collapsed-rank hypotheses → (finite unit and collapsed PPML maximizers have the same beta) ∧ betaNStar → betaStar ∧ within-cohort baseline invariance` | Unit-FE collapse lemma: reduces the finite unit-and-time-FE PPML problem to the collapsed cohort-time criterion, proves convergence of its coefficient to the limiting pseudo-true coefficient, and shows that only within-cohort baseline averages matter. |
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.pseudo_true_ppml_projection` | `(T C pi barB gamma delta) → CollapsedDesignRank T C pi → unique global maximum of limitingCriterion ∧ nuisance score equations ∧ treatment score equation` | Pseudo-true projection lemma: establishes uniqueness of the limiting collapsed PPML projection and its population score characterization. |
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.sharp_ppml_forbidden_sign` | `(T C) → horizon/support/share/baseline/effect/rank hypotheses → treated cell (k,s) → HasDerivAt betaStar [q_ks B_ks exp(delta_ks) W_ks / energy] ∧ 0 < energy ∧ sign derivative = sign W_ks` | Sharp forbidden-comparison theorem: a treated effect moves the pooled PPML coefficient with exactly the sign of its pseudo-true weighted-FWL residual; negative residuals identify locally perverse comparisons. |
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.four_cohort_sign_reversal` | `four-cohort sign-reversal primitive ∈ signReversalRegion ∧ all treated effects positive ∧ late effect largest ∧ late residual = -1/8 ∧ late derivative = -1/10 ∧ positive-effect neighborhood with negative late derivative` | Four-cohort witness: certifies an all-positive-effect staggered-adoption panel in which increasing the largest late effect lowers the naive PPML coefficient. |
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.homogeneous_effect_reduction` | `(T C Omega P Y b pi barB gamma delta delta0) → untreated-exponential-mean → collapsed-rank → homogeneous treated effects → betaStar = delta0 ∧ fittedMean = untreatedMean · exp(D · delta0)` | Homogeneous-effect sanity theorem: when every treated cell has the same log proportional effect, the limiting pooled PPML coefficient recovers that effect exactly. |
| `CausalSmith.Panel.PANEL_PpmlForbiddenComparison_Research.primitive_global_frontier` | `(T C) → horizon/support/share/baseline/effect/rank/frontier-scope/positive-effect hypotheses → sign betaStar = sign Phi ∧ sign-reversal iff Phi < 0 ∧ four-cohort threshold ∧ positive counterfactual-share PTT` | Primitive frontier theorem: eliminates pseudo-true nuisance multipliers in favor of the finite handle `Phi`, gives the four-cohort threshold, and distinguishes a negative pooled PPML coefficient from the positive counterfactual-share PTT. |

## 2. Mathlib-shaped helpers (promotion targets)

Mathlib-shaped lemmas extracted from CausalSmith research derivations and staged under `CausalSmith/CausalSmith/Mathlib/`. They are model-agnostic (pure analysis / measure theory / concentration) and reusable; promotion to `Causalean/Mathlib/` is gated on a second consumer (per the `causalsmith` skill's "Mathlib helper staging" section). Four earlier helpers (offset peeling, `rpow` arithmetic, `MemLp` from square-integrability, product-kernel conditional distributions) have been promoted and now live under `Causalean/Mathlib/`, documented in `doc/API.md`. Currently staged:

- 2.1 `Concentration/FiniteClassRademacher.lean` — finite-class Massart envelopes (feeds `Causalean/Stat/Concentration/`).
- 2.2 `InformationTheory/ProductChiSquared.lean` — χ² tensorization.
- `Probability/ParameterizedFinitePoissonSample.lean` — turns an atomwise measurable family of probability measures on a finite discrete space into the Markov kernel of finite samples with an independent Poisson sample size.
- `Probability/PoissonUsableOccupancy.lean` — deterministic light/heavy cell aggregation for the Laplace-transform argument behind usable Poisson occupancies (birthday-scale exponent `n² / max n d` with explicit constants).

### 2.1 `CausalSmith/Mathlib/Concentration/FiniteClassRademacher.lean` — finite-class Massart envelopes

| Declaration | Signature (sketch) | Description | Status |
|---|---|---|---|
| `CausalSmith.Mathlib.Concentration.finiteClass_rademacherUpperBound` | `[Fintype ι][Nonempty ι] (F : ι→𝒳→ℝ)(norm)(μ)[IsProb](X){n}(hn)(b)(hb:0≤b)(hbound:∀ i ω,│F i (X ω)│≤b) → ∃ ψ, SubRoot ψ ∧ RademacherUpperBound F norm μ X n ψ ∧ ∀ r, ψ r = (b/√n)·√(2 log(2│ι│))` | **Global** finite-class Massart envelope: a CONSTANT `ψ` (independent of `r`), a genuine sub-root upper bound for every star-hull radius via signed-doubling + `FoML.Massart`. Gives `criticalRadius = √c` (the non-localized / slow rate). **Proved**, 0-sorry, `lean_verify` axioms `[propext, Classical.choice, Quot.sound]`. |
| `CausalSmith.Mathlib.Concentration.finiteClass_rademacherUpperBound_linear` | `… (hscale:∀ r≥0, ∀ p:starHullParam ι, norm(starHullEval F p)≤r → p.1.val≤r)(hunit:∀ i ω,│F i (X ω)│≤1) → ∃ ψ, SubRoot ψ ∧ RademacherUpperBound F norm μ X n ψ ∧ ∀ r, ψ r = r·√(2 log(2│ι│)/n)` | **Sharp LINEAR localized** envelope (`ψ(r)=r·√(2log 2│ι│/n)`): the radius-`r` star-hull zero-out class is dominated by `r·`(finite signed `ι`-proxy) via `empiricalRademacherComplexity_smul_class` homogeneity + the unit Massart bound. Gives `criticalRadius = √(2 log(2│ι│)/n)` — the correct finite-class localized critical radius, i.e. the **margin-fast** rate when fed to `localized_uniform_deviation_sharp`. `hscale` is a satisfiable norm-control regularity condition (holds e.g. for a unit-normalized class), discharged by the consumer. **Proved**, 0-sorry, sound axioms. |

### 2.2 `CausalSmith/Mathlib/InformationTheory/ProductChiSquared.lean` — χ² tensorization

Namespace `CausalSmith.Mathlib.ProductChiSquared`. The χ²-divergence analogue of `Causalean/Mathlib/InformationTheory/ProductKLLeCam.lean`, built on `Causalean.Stat.chiSqDiv`.

| Declaration | Signature (sketch) | Description |
|---|---|---|
| `one_add_chiSqDiv_pi_iid_general` | `μ≪ν → (χ²-integrable) → 1 + χ²(μ^⊗n ‖ ν^⊗n) = (1 + χ²(μ‖ν))^n` | `n`-fold i.i.d. χ² tensorization, no `Fintype` on the observation space. |
| `chiSqDiv_pi_iid_integrable` | i.i.d. product χ²-integrability side condition | Discharges the integrability hypothesis of the tensorization for arbitrary finite products. |
| `chiSqDiv_eq_sum_partition_of_restrict_eq_smul` | finite measurable partition, `μ.restrict (sᵢ) = cᵢ • ν.restrict (sᵢ)` → `χ²(μ‖ν) = ∑ᵢ (cᵢ−1)²·ν(sᵢ)` | χ²-divergence on a finite partition where `μ` is a constant multiple of `ν` per cell. |

## 3. Identification, Stat, and Experimentation catalogues

Completed identification, statistical-inference, and experimentation runs, grouped by the *kind* of result (not merely the directory). Clusters with no completed entry are noted; each is populated one `<QidCamel>_Research/` tree at a time as runs finish.

- **`ExactID/`** (`eid_*`) — a new identifying functional for a causal query in the potential-outcomes (exchangeability) language: backdoor, frontdoor, IV/LATE, DID, DTR, mediation, structure/mechanism ID, …
- **`PartialID/`** (`pid_*`) — a partial-identification / bounds result: Manski-family bounds, Balke–Pearl, IV bounds, sensitivity analysis, shape restrictions, missing-data. 
- **`Stat/`** (`stat_*`) — an **estimation-and-inference** kernel for a *causal* estimand: a minimax convergence rate, a semiparametric efficiency bound, a limit law / CLT, a concentration or coverage result, where that statement IS the contribution (not an identification or bounds claim). The estimand must be causal (ATE/ATT/CATE/LATE/policy value/partial-ID endpoint) and the result must advance the Stat frontier. **A matching minimax converse is not required.** It is *one* eligible frontier advance among several — achievability at a new rate, a semiparametric efficiency bound, a limit law, a coverage or concentration result each qualify on their own — so a converse is never a default expectation, and an entry is not downgraded for lacking one (cf. §3.3, whose certified statements deliberately assert no same-class upper bound). Built on `Causalean/Stat/` (Minimax, CLT, Concentration, EmpiricalProcess, Inference, Bootstrap, GMM, UStatistic, …) and `Causalean/Estimation/` (Efficiency, StructureAgnostic, OrthogonalMoments, OSL, NPIV).
- **`Experimentation/`** (`exp_*`) — a **design-based / randomization-inference** result where the treatment assignment is the only randomness and the design itself is a first-class object: a design-based limit law + Wald coverage for a given design, an optimal chosen design with an optimality certificate, or adaptive inference under a data-dependent design. Built on `Causalean/Experimentation/DesignBased/`.
- **`SCM/`** (`scm_*`) — a **graphical-identification** kernel over a DAG / ADMG / selection diagram in the do / σ-calculus language (a new identifying functional under a stated assumption, or a graphical / data-fusion bound), distinct from ExactID/PartialID which speak potential outcomes. Built on `Causalean/SCM/` + `Causalean/Graph/`.

When promotion to Causalean is appropriate (the theorem matures, dependencies stabilize, and a human curator signs off), entries move from `CausalSmith/ExactID/<QidCamel>/` to `Causalean/PO/ID/Exact/<QidCamel>/`, and likewise for PartialID and Stat (the latter to `Causalean/Stat/` or `Causalean/Estimation/`). Causalean's `doc/API.md` is the canonical record for promoted entries; new CausalSmith additions should be recorded here under sections 3.1, 3.2 etc. as they are written.

### 3.1 Generic LiNGAM direction recovery at minimal cumulant order — apolar exclusion of the full opposite-arrow fiber

ExactID/Theorems entry generated under `CausalSmith/ExactID/EID_LingamDirectionMinOrderV1_Research/` (`eid_lingam_direction_min_order_v1`). The real-feasibility portion of the generic locus is derived internally from the proved truncated-moment-interior result.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.ExactID.EID_LingamDirectionMinOrderV1.generic_apolar_arrow_recovery` | `(m : ℕ) → ValidComplexity m → ∃ Ur Ul : Set (ParamSpace ℂ m), Ur ⊆ genericParameterLocus m (2 * m + 2) ∧ Ul ⊆ genericParameterLocus m (2 * m + 2) ∧ IsZariskiOpenParamIn (2 * m + 2) Ur ∧ IsZariskiDenseParamIn (2 * m + 2) Ur ∧ IsZariskiOpenParamIn (2 * m + 2) Ul ∧ IsZariskiDenseParamIn (2 * m + 2) Ul ∧ …` | Generic apolar arrow-recovery theorem: at cumulant order `2m + 2`, Zariski-open dense forward and reverse parameter loci each meet a real feasible open region, have an empty full opposite-arrow fiber, and recover the squarefree support annihilator whose fixed axis determines the arrow; real feasibility is derived internally. |

### 3.1a Robust BACKSHIFT under adversarial covariance-slot replacement — sharp deletion radius and uniform support frontier

ExactID/Theorems paper-scoped entries under `CausalSmith/ExactID/EID_RobustBackshiftUniformDistance_Research/` (`eid_robust_backshift_uniform_distance`, v1). The paper characterizes when a BACKSHIFT-normalized structural matrix remains uniquely recoverable after up to `c` arbitrary positive-semidefinite covariance-slot replacements: the exact criterion is that the honest shift cloud's affine deletion distance exceeds `c`. It gives a matching positive-definite ambiguity witness on failure, an almost-sure sparse-support incidence frontier, an ex-ante all-deletion budget formula, set-theoretic confidence-union coverage, and a non-effective local contraction result under explicit true-side margins.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.ExactID.RobustBackshiftUniformDistance.sharp_replacement_radius` | `(W : BackshiftSystem p m c) → BackshiftNormalization W → NonnegativeShifts W → HonestCovarianceModel W → W.invariantNoise.PosSemidef → let delta := envDeletionDistance W.honest W.shifts; let gamma := affineSeparation …; (delta > c ↔ gamma > 0) ∧ (delta > c → compatibleSet … = {W.structural}) ∧ (positive-definite interior ambiguity witnesses when delta ≤ c)` | Sharp replacement-radius theorem: affine deletion distance exceeding the corruption budget is exactly positive affine separation and guarantees singleton compatibility under every PSD replacement; failure has a matching ambiguity witness in the positive-definite invariant-noise interior. |
| `CausalSmith.ExactID.RobustBackshiftUniformDistance.generic_support_frontier` | `(μ : Measure Ωsample) → [IsProbabilityMeasure μ] → 2 ≤ p → 1 ≤ m → c < m → (H : Finset (Environment m)) → H.card = honestCount m c → (Z : Environment m → Fin p → Bool) → GenericActiveAmplitudes H Z μ a → ∀ᵐ ω ∂μ, (generic occupancy formula) ∧ (supportFrontierCriterion H Z c ↔ uniform singleton recovery) ∧ (failure ambiguity) ∧ (2 * c + 3 ≤ m necessity and attainment)` | Generic sparse-support frontier: on one probability-one event, the exact four-term support-incidence occupancy formula is equivalent to uniform singleton recovery, with a positive-definite ambiguity witness on failure and a sharp environment-count frontier. |
| `CausalSmith.ExactID.RobustBackshiftUniformDistance.uniform_support_deletion_radius` | `(μ : Measure Ωsample) → [IsProbabilityMeasure μ] → 2 ≤ p → 1 ≤ m → c < m → (Zbar : Environment m → Fin p → Bool) → GenericActiveAmplitudes Finset.univ Zbar μ abar → ∀ᵐ ω ∂μ, (uniformAfterDeletion ↔ fullDesignOccupancy Zbar < m - 2 * c) ∧ (exact integer budget formula) ∧ (all-active occupancy)` | Uniform support-deletion-radius theorem: almost surely, robustness after every `c`-environment deletion holds exactly when full-design forced occupancy is below `m - 2c`; it also gives the integer budget formula and the all-active specialization. |
| `CausalSmith.ExactID.RobustBackshiftUniformDistance.confidence_union_coverage` | `(W : BackshiftSystem p m c) → (V : InferenceWorld p m) → BackshiftNormalization W → NonnegativeShifts W → HonestCovarianceModel W → W.invariantNoise.PosSemidef → coverageEvent W V.currentRegions → W.structural ∈ confidenceUnion c V` | Confidence-union coverage theorem: simultaneous coverage of the honest covariance slots places the true structural matrix in the set-theoretic union over feasible corrupted-BACKSHIFT explanations. |
| `CausalSmith.ExactID.RobustBackshiftUniformDistance.non_effective_four_margin_contraction` | `2 ≤ p → 1 ≤ m → c < m → (gamma0 zeta0 kappabar Mbar : ℚ) → positive affine/slack and conditioning/scale bounds → ∃ r0 > 0, ∀ W V, (four true-side margins and candidate condition bound) → honestRegionRadius … ≤ r0 → (outer-radius and diameter contraction bounds)` | Four-margin contraction theorem: four true-side margins plus a feasible-candidate condition-number bound yield a non-effective positive local radius and a linear bound on both confidence-union outer radius and diameter. |

### 3.1b Generic cover-ratio MMD recovery from one unknown-target perfect intervention per latent node

ExactID/Theorems entry generated under `CausalSmith/ExactID/EID_CrlCoverratioMmdGenericity_Research/` (`eid_crl_coverratio_mmd_genericity`, v1). In the positive compact-cube, shared-diffeomorphic-mixing regime, the result decodes a cover-separated causal-minimal mechanism from its observational and one-perfect-intervention environment laws, recovering the permuted ancestral order, intervention-to-coordinate alignment, exact parent sets, and representation up to relabeling and componentwise `C²` diffeomorphisms.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.ExactID.EID_CrlCoverratioMmdGenericity.exact_ratio_decoder` | `{n : ℕ} → {G : Causalean.DAG (Fin n)} → (s : SignVector n) → (θ : Mechanism n G) → (world : ∀ θ' : Mechanism n G, ObservedWorld G θ') → PositiveNormalizedSmoothMechanisms G θ → CausalMinimality G θ → SharedDiffeomorphicMixing G θ (world θ) → OnePerfectInterventionPerNode G θ (world θ) → FixedOwnDerivativeSign G s θ → ⟨θ, ⟨…, …, …⟩⟩ ∈ coverSeparatedSet (s := s) gaussianFeatureMap (world θ).targetPerm → ExactRatioDecoderConclusion s θ (world θ)` | Exact ratio-decoder theorem: positive smooth causal-minimal mechanisms with one unknown-target perfect intervention per node are exactly decoded from their observed environment laws when every ancestral cover has positive Gaussian-MMD discrepancy; the conclusion recovers the permuted graph and parent sets, aligns intervention labels with recovered coordinates, and gives uniqueness up to componentwise `C²` diffeomorphisms. |

### 3.2 Margin-overlap policy regret — matched minimax welfare-regret rate

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_PolicyRegretMarginOverlap_Research/` (banked accepted, `stat_policy_regret_margin_overlap` v1). The paper characterizes the minimax welfare-regret rate of offline policy learning under a Tsybakov margin (exponent `α`) and one-sided polynomial overlap decay (exponent `γ`): a **Le Cam two-point converse** `M_n ≥ c·n^{-r⋆(α,γ)}` over the baseline observed-law class, and a **conditional clipped-AIPW `1/n`-ERM achievability** bound `n^{-r_feas}(log n)^p` on that class plus explicit estimator/fold/process side-conditions. The world is an `ObservedLaw 𝒳` — a covariate marginal `P_X`, a per-draw observation law on `𝒳 × {0,1} × ℝ`, and the nuisance functionals (propensity, arm regressions, contrast `τ = μ₁−μ₀`); `LawClass` bundles the six member assumptions (margin, overlap-decay, positivity, bounded outcome, well-formedness, zero-effect). The whole module is **0-sorry, axiom-clean** (`[propext, Classical.choice, Quot.sound]`).

The achievability theorem `feasible_upper` (the conditional bracket) rests on the welfare-identity ERM-selection bridge `R_P(π̂) ≤ |G_cf(π̂)| + |Σ_k w_k drift_k(π̂)| + 1/n` (`feasible_erm_welfare_bridge`, from `clip_bias`'s foldwise conditional-mean drift decomposition + the `1/n` empirical near-max inequality), composed with the pooled cross-fit offset control (`crossfit_localized_offset_control`, applied to a pointwise-bounded truncated increment that agrees a.e. with the unbounded clipped-AIPW increment), the localized self-bound (`localized_vc_self_bound`), and the uniform-constant clipped drift bound (`localized_clipped_drift_bound`), with the regret-dependent drift term absorbed by a Young inequality. It carries **two honest substrate-gates**, `ass:vc-localized-envelope` / `ass:vc-localized-offset-envelope` (the localized finite-VC empirical-process envelopes) — these are the note's *deliberately assumed* atomic inputs (`\begin{assumption}`, explicitly "not asserted to follow from the law class"), threaded as hypotheses, and dischargeable from `Causalean.Stat.Concentration.localized_uniform_deviation_expectation` / `localized_offset_expectation` if one wants the stronger unconditional-in-those-envelopes statement (tracked in `SUBSTRATE_DEBT.md`). The strict-gap residual (whether a feasible estimator attains the converse exponent) is recorded as the open question `FeasibleTightQuestion` (`oeq:feasible-tight`, a `Prop` `def`, no proof). The helper substrate is split by topic under `Helpers/` (`CrossfitProcess`, `ClipBias`, `FeasibleERM`, `DriftBound`, `OffsetControl`, `SelfBound`, `MasterBound`) behind a `Helpers.lean` barrel.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.PolicyRegretMarginOverlap.regret_eq_disagreement_integral` | `(P : ObservedLaw 𝒳) (π : Policy 𝒳) → WellFormedLaw P → BoundedOutcome P → Measurable π → lawRegret P π = ∫ x, |P.contrast x| * disagreementIndicator π (lawOptimalPolicy P) x ∂P.PX` | Welfare-regret identity: policy regret equals the covariate integral of treatment-effect magnitude over the set where the policy disagrees with the law-optimal policy. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.margin_localization` | `(Cm α u0 : ℝ) → ∃ C, 0 < C ∧ ∀ (P : ObservedLaw 𝒳) (policySet), MarginTail P Cm α u0 → ZeroEffectRegular P policySet → WellFormedLaw P → BoundedOutcome P → (∀ π ∈ policySet, Measurable π) → ∀ π ∈ policySet, P.PX.real (disagreementSet π (lawOptimalPolicy P)) ≤ C * (lawRegret P π) ^ (α / (1 + α))` | Margin-localization: under the Tsybakov margin the disagreement-set mass is bounded by a power `α/(1+α)` of the regret, with one constant uniform over all laws in the class. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.minimax_lower` | `(α γ u0 cB Cm Co co underlineP : ℝ) (policySet : Set (Policy ℝ)) → MarginWindow u0 → 0 ≤ α → 0 ≤ γ → … (overlap-smallness constant choices, 8·cB < log 5) … → ∃ c, 0 < c ∧ ∀ᶠ n, minimaxRegret {P | LawClass α γ Cm u0 Co co underlineP policySet P} policySet n ≥ c * n ^ (-(rStar α γ))` | Minimax lower bound (Le Cam two-point converse): minimax welfare regret over the baseline observed-law class is eventually at least `c·n^{-r⋆(α,γ)}`, from an explicit two-point witness with a χ²-calibrated overlap-margin construction. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.rate_characterization` | `(α γ u0 cB Cm Co co underlineP : ℝ) (policySet : Set (Policy ℝ)) → … (same hypotheses as minimax_lower) … → ∃ c, 0 < c ∧ ∀ᶠ n, minimaxRegret {P | LawClass … P} policySet n ≥ c * n ^ (-((1 + α) / (2 + α + betaAG α γ)))` | Rate characterization: the converse restated with the closed matched exponent `(1+α)/(2+α+βAG(α,γ))` — the margin-driven / overlap-limited phase boundary read off the calibration, not assumed. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.feasible_upper` | `{K} (α γ Cm u0 Co co underlineP a c CMu CProd q0 uBar : ℝ) (dPi) (assign) (policySet) (enum) (muHat0 muHat1 eHat) (rMu rE) → 0 ≤ γ → PolynomialNuisanceExponents … → (foldwise nuisance measurability / MemLp L²-rate / Bochner regularity) → … → ∃ C p, 0 < C ∧ 0 ≤ p ∧ ∀ᶠ n, upperRisk … ≤ C * n ^ (-(feasibleRate …).r) * (log n) ^ p` | Conditional achievability (`oeq:feasible-upper`): the cross-fit clipped-AIPW `1/n`-ERM, run on the certified `def:feasible-rate` schedule, attains the uniform eventual rate `C·n^{-r_feas}(log n)^p` over the bundled law-class / estimator-side / finite-VC-envelope domain — the upper bracket matching the converse. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.crude_localized_master_bound` | `{K} (policySet) (α γ Cm u0 Co co underlineP a c CMu CProd : ℝ) (dPi) (assign) (qSeq uSeq rMu rE) (enum) (muHat0 muHat1 eHat) → … (VC envelope, dense skeleton, fixed folds, regularity) … → ∃ C p, 0 < C ∧ 0 ≤ p ∧ ∀ᶠ n, ∀ P (in class) … → ∫ R_P(π̂_n) ≤ C·(five-term γ>0 / two-term γ=0 bound)·(log n)^p` | Pooled crude `q^{-2}`-envelope master bound for the cross-fit clipped-AIPW `1/n`-ERM: the per-`n` expected-regret bound (five terms for `γ>0`, two for `γ=0`) that `feasible_upper` optimizes over the deterministic schedule. |
| `CausalSmith.Stat.PolicyRegretMarginOverlap.FeasibleTightQuestion` | `Prop` `def` (open question, no proof) | Open research question (`oeq:feasible-tight`): in the strict-gap branch, whether some genuinely feasible cross-fitted-nuisance estimator attains the converse exponent `r⋆`; the inhabited-domain guard pins the full `upperRisk` side-condition domain so the question is non-vacuous. |

### 3.3 Anisotropic dose-response minimax converse — all-beta lower floor and regime frontier

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_DoseResponseMinimax_Research/` (banked accepted, `stat_dose_response_minimax` / `holder_anisotropic_converse`). The paper formalizes the interior fixed-dose response minimax converse for the anisotropic Holder class `HolderDoseClass`: the genuine two-point construction yields an all-`beta` pointwise lower floor `n^{-2alpha/(2alpha+1)}`, and rate algebra compares that floor with the published Bonvini-Kennedy HOIF benchmark `rho_n`. The certified statements intentionally do **not** assert a same-class upper bound; the remaining full-beta frontier is recorded separately as the open residual `fullBetaFrontierResidual`.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.DoseResponseMinimax.sharp_pointwise_lower_bound` | `{d : ℕ} (alpha beta s M c0 eps0 t0 : ℝ) → 0 < alpha → 0 < beta → 0 < s → RegimeConstants alpha beta s M c0 eps0 t0 → BaselineSubmodelSlack d beta s M c0 eps0 t0 → ∃ c, 0 < c ∧ ∀ᶠ n, c * n ^ (-(2 * alpha / (2 * alpha + 1))) ≤ minimaxRisk M n (HolderDoseClass d alpha beta s M c0 eps0 t0) t0` | Kernel minimax converse: under the strict-slack baseline, every positive treatment-density smoothness `beta` still admits the classical interior pointwise lower floor `n^{-2alpha/(2alpha+1)}` for the fixed-dose causal response risk. |
| `CausalSmith.Stat.DoseResponseMinimax.certifiedPartialBetaFrontier` | `{d : ℕ} (alpha beta s M c0 eps0 t0 : ℝ) → 0 < alpha → 0 < beta → 0 < s → RegimeConstants alpha beta s M c0 eps0 t0 → BaselineSubmodelSlack d beta s M c0 eps0 t0 → ∃ c, 0 < c ∧ (∀ᶠ n, c * n ^ (-(2 * alpha / (2 * alpha + 1))) ≤ minimaxRisk M n (HolderDoseClass d alpha beta s M c0 eps0 t0) t0) ∧ ∀ n, 1 ≤ n → (((d : ℝ) ≤ 4 * s → publishedHoifRate n alpha s d = n ^ (-(2 * alpha / (2 * alpha + 1)))) ∧ (4 * s < (d : ℝ) → publishedHoifRate n alpha s d = n ^ (-(2 / (1 + (d : ℝ) / (4 * s) + 1 / alpha))) ∧ 2 / (1 + (d : ℝ) / (4 * s) + 1 / alpha) < 2 * alpha / (2 * alpha + 1))))` | Certified partial beta-frontier theorem: combines the all-`beta` lower floor with the smooth and deficient covariate rate identities, while deliberately asserting no same-class upper endpoint. |
| `CausalSmith.Stat.DoseResponseMinimax.oracle_regime_reduction` | `{d : ℕ} (alpha beta s M c0 eps0 t0 : ℝ) → 0 < alpha → 0 < beta → 0 < s → RegimeConstants alpha beta s M c0 eps0 t0 → (d : ℝ) ≤ 4 * s → BaselineSubmodelSlack d beta s M c0 eps0 t0 → ∃ c, 0 < c ∧ ∀ᶠ n, publishedHoifRate n alpha s d = n ^ (-(2 * alpha / (2 * alpha + 1))) ∧ c * publishedHoifRate n alpha s d ≤ minimaxRisk M n (HolderDoseClass d alpha beta s M c0 eps0 t0) t0` | Oracle-regime reduction: when covariate smoothness satisfies `s ≥ d/4`, the published benchmark `rho_n` collapses to the oracle exponent, so the certified lower floor is a constant multiple of `rho_n`. |
| `CausalSmith.Stat.DoseResponseMinimax.sharp_minimax_smooth_covariate` | `{d : ℕ} (alpha beta s M c0 eps0 t0 : ℝ) → 0 < alpha → 0 < beta → 0 < s → RegimeConstants alpha beta s M c0 eps0 t0 → (d : ℝ) ≤ 4 * s → BaselineSubmodelSlack d beta s M c0 eps0 t0 → ∃ c, 0 < c ∧ ∀ᶠ n, c * n ^ (-(2 * alpha / (2 * alpha + 1))) ≤ minimaxRisk M n (HolderDoseClass d alpha beta s M c0 eps0 t0) t0 ∧ n ^ (-(2 * alpha / (2 * alpha + 1))) = publishedHoifRate n alpha s d` | Smooth-covariate regime theorem: for `s ≥ d/4`, the all-`beta` converse lands exactly on the same exponent as the published HOIF comparator, certifying sharpness at the exponent without claiming a same-class upper theorem. |
| `CausalSmith.Stat.DoseResponseMinimax.frontier_bracket_deficient` | `{d : ℕ} (alpha beta s M c0 eps0 t0 : ℝ) → 0 < alpha → 0 < beta → 0 < s → RegimeConstants alpha beta s M c0 eps0 t0 → 4 * s < (d : ℝ) → BaselineSubmodelSlack d beta s M c0 eps0 t0 → ∃ c, 0 < c ∧ ∀ᶠ n, c * n ^ (-(2 * alpha / (2 * alpha + 1))) ≤ minimaxRisk M n (HolderDoseClass d alpha beta s M c0 eps0 t0) t0 ∧ publishedHoifRate n alpha s d = n ^ (-(2 / (1 + d / (4 * s) + 1 / alpha))) ∧ 2 / (1 + d / (4 * s) + 1 / alpha) < 2 * alpha / (2 * alpha + 1)` | Deficient-covariate regime theorem: for `0 < s < d/4`, the certified all-`beta` lower floor remains at the oracle exponent while the published comparator has a strictly smaller exponent, leaving the same-class upper frontier unresolved. |

### 3.4 Chebyshev-Lobatto rollout design — TV-envelope minimax over budgeted treatment-fraction schedules

Experimentation/Theorems paper-scoped entries under `CausalSmith/Experimentation/EXP_RolloutChebyshevMinimax_Research/` (banked accepted, `exp_rollout_chebyshev_minimax` / `tv_envelope_rollout_design`). The paper treats rollout measurement fractions as the experimental design object for estimating a beta-order total treatment effect under interference. It proves the sharp total-variation variance-envelope reduction to the polynomial amplification criterion `A_beta(p)`, then gives the matched Chebyshev-Lobatto minimax theorem over budgeted schedules `S_{k,q}`: when `k ≥ c beta` and `q ≤ q_max < 1`, the minimax amplification has low-budget base `rho(q)/q`, with `rho(q) = (1 + sqrt(1-q))^2`, improving the equal-grid `beta/q` benchmark at the exponential-base level. The exact nested-rollout covariance optimizer is deliberately left as the open residual `oeq:exact-nested-minimax`; the certified exact-risk consequence is rate feasibility, not exact optimality.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.RolloutChebyshev.tv_envelope_design` | `(n k beta : ℕ) (q sigma0sq : ℝ) {Ω : Type*} [Fintype Ω] (D : FiniteDesign Ω) (Y : Fin n → (Fin n → Bool) → ℝ) (Z : Fin (k + 1) → Ω → (Fin n → Bool)) (barY : Fin (k + 1) → Ω → ℝ) (m : ℝ → ℝ) (a : ℕ → ℝ) → 1 ≤ beta → beta ≤ k → 0 ≤ sigma0sq → (p : Fin (k + 1) → ℝ) → BudgetedSchedule k q p → RolloutLawClass n k beta D Y Z barY m a sigma0sq p → (unbiased-weight nonemptiness, design-unbiasedness, sharp TV-envelope variance bound, fixed-schedule envelope optimum, minimax envelope reduction, and optimal-schedule characterization)` | TV-envelope rollout design theorem: for any budgeted schedule and beta-order rollout law, linear-unbiased weights exist, every such estimator is design-unbiased for `m 1 - m 0`, its design variance is bounded sharply by `(sigma0sq/n)(sum |w_j|)^2`, and minimizing this envelope over weights and schedules reduces to `A_beta(p)` and `M_{beta,k,q}`. |
| `CausalSmith.Experimentation.RolloutChebyshev.chebyshev_minimax` | `(qmax : ℝ) → 0 < qmax ∧ qmax < 1 → ∃ Cminus, 0 < Cminus ∧ ∀ c, 1 < c → ∃ Cplus, 0 < Cplus ∧ ∀ (beta k : ℕ) (q : ℝ), 1 ≤ beta → (k : ℝ) ≥ c * beta → LowBudgetCap q qmax → 0 < q → (∀ p, BudgetedSchedule k q p → amplification beta k p ≥ Cminus * (((1 + sqrt (1 - q))^2 / q)^(2 * beta))) ∧ amplification beta k (chebyshevSchedule k q) ≤ Cplus * (((1 + sqrt (1 - q))^2 / q)^(2 * beta)) ∧ minimaxAmplification beta k q is trapped between the same two constant multiples` | Chebyshev-Lobatto matched minimax theorem: every budgeted schedule pays the lower amplification base `rho(q)/q`, while the shifted Chebyshev-Lobatto schedule attains the matching upper base, so the minimax value has intrinsic exponent `2 beta` and low-budget pointwise base `rho(q)/q` rather than the equal-spacing `beta/q` factor. |

### 3.5 Plus-minus-one design exactness boundary — implementability gap for two-block homophily

Experimentation/Theorems paper-scoped entries under `CausalSmith/Experimentation/EXP_DesignPm1ExactnessBoundaryV1_Research/` (banked accepted, `exp_design_pm1_exactness_boundary_v1`). The paper resolves when the interference-homophily worst-case-MSE covariance SDP is exactly implementable by a randomized plus-minus-one assignment design on a balanced two-community homophily graph. The block-symmetric elliptope reduces to a three-coordinate slice `T_m`; the implementable plus-minus-one image is the same slice when `m` is even and the parity-truncated slice `y+z ≥ d_m` when `m` is odd. The certified kernel gives exact cut and iid corner regimes, an odd-`m` positive-gap window, and a finite active-set certificate identifying the tight rounding-loss certificate `rho_star` with the implementability gap `Delta_m^pm`; the exact phase frontier `r_star` remains an explicit open object.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.DesignPm1.cut_corner_exactness` | `(m : ℕ) (a b r kappa : ℝ) → TwoBlockHomophily m a b → 0 ≤ r → 0 ≤ kappa → r < rCut m a b kappa → (cutCovariance m is the unique relaxed minimizer over blockElliptope) ∧ implementabilityGap m a b r kappa = 0 ∧ (cutCovariance m is the unique implementable minimizer) ∧ cutDesign m ∈ blockExchangeableDesignClass m ∧ assignmentSecondMoment m (cutDesign m) = cutCovariance m` | Cut-corner exactness theorem: in the low-ratio region `0 ≤ r < r_cut(m,a,b,kappa)`, the cut covariance uniquely minimizes the relaxed objective, the implementability gap is zero, and the two-point cut design realizes the implementable optimum. |
| `CausalSmith.Experimentation.DesignPm1.robust_corner_exactness` | `(m : ℕ) (a b r : ℝ) → TwoBlockHomophily m a b → 0 ≤ r → ((∃ kappa, 0 ≤ kappa ∧ identity covariance is relaxed-optimal) ↔ a + 3*b = 2*m ∧ r = 2*b*(a+b)) ∧ (on this locus every kappa > 0 makes identity covariance uniquely relaxed-optimal) ∧ iidDesign m ∈ blockExchangeableDesignClass m ∧ assignmentSecondMoment m (iidDesign m) = 1 ∧ (off the locus, finite-kappa identity optimality fails while minimizers converge to identity as kappa → ∞)` | Robust iid-corner theorem: finite-kappa iid exactness occurs exactly on the affine-balanced locus `a+3b=2m` and `r=2b(a+b)`, where every positive robustness weight selects the iid covariance realized by `iidDesign`; off-locus the iid covariance is only an infinite-robustness limit. |
| `CausalSmith.Experimentation.DesignPm1.gap_window` | `(m : ℕ) (a b : ℝ) → TwoBlockHomophily m a b → LowScaleTwoBlock m a b → (OddCommunitySize m → ∀ kappa, 0 ≤ kappa → kappa < kappaGap m a b → rCutGap a b kappa < rGapMinus m a b kappa ∧ rGapMinus m a b kappa < rGapPlus m a b kappa ∧ ∀ r, rGapMinus m a b kappa < r → r < rGapPlus m a b kappa → spreadCovariance m is the unique relaxed minimizer ∧ spreadCovariance m ∉ implementableCovarianceClass m ∧ 0 < implementabilityGap m a b r kappa) ∧ (Even m → blockElliptope m a b ⊆ implementableCovarianceClass m)` | Positive-gap window theorem: under low-scale two-block homophily and odd community size, every `r` in the explicit gap interval exposes the non-implementable spread vertex and yields strictly positive implementability gap; for even `m`, the whole block elliptope slice is implementable, so this positive-gap claim is false. |
| `CausalSmith.Experimentation.DesignPm1.sharp_rho_star` | `(m : ℕ) (a b r kappa : ℝ) → TwoBlockHomophily m a b → 0 ≤ r → 0 ≤ kappa → roundingLossCertificate m a b r kappa = implementabilityGap m a b r kappa ∧ 0 ≤ roundingLossCertificate m a b r kappa ∧ 0 ≤ implementabilityGap m a b r kappa ∧ (zero-loss criterion by relaxed argmin intersecting the parity slice) ∧ (for kappa > 0, unique active-set relaxed minimizer and truncation-segment finite case split) ∧ (for kappa = 0, exposed-face minimizer characterization)` | Sharp rounding-loss certificate theorem: identifies `rho_star` with the exact plus-minus-one implementability gap, gives the zero-loss criterion as intersection of the relaxed argmin with the parity-truncated implementable slice, and supplies the finite weighted-simplex active-set computation for the relaxed and truncated optima. |

### 3.6 Minimax-optimal heterogeneous Bernoulli design for bipartite-interference experiments

Experimentation/Theorems paper-scoped entries under `CausalSmith/Experimentation/EXP_BipartiteMinimaxDesign_Research/` (banked accepted, `exp_bipartite_minimax_design` v1). The paper studies graph-only conservative-envelope design for bipartite interference experiments with independently assigned, intervention-unit-specific Bernoulli probabilities. It extends the homogeneous bipartite Hájek benchmark to heterogeneous probabilities, proves convex envelope-optimal design and a bounded-degree surrogate certificate, separates heterogeneous designs from the common-probability design on singleton-exposure graphs, and supplies a heterogeneous Hájek CLT plus conservative post-design Wald coverage under explicit feasible-design, uniform positivity-floor, bounded-dependency, and variance-nondegeneracy conditions. The denominator-tightness step is derived in the current Lean development rather than assumed as a theorem hypothesis.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.homogeneous_reduction` | `(E : BipartiteExperiment I O) (D : FiniteDesign (I → Bool)) (pc : ℝ) → 0 < pc → pc < 1 → (p : I → ℝ) → probability-vector bounds → (∀ k, p k = pc) → IndepHeteroBernoulli D p ... → treated/control overlap loads collapse to the common-`p` formulas and `varScale` equals the homogeneous Hájek overlap expression` | Homogeneous sanity reduction: when every intervention unit uses the same Bernoulli probability, the heterogeneous overlap-load variance formula reduces to the published common-probability bipartite Hájek formula. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.hetero_envelope` | `(E : BipartiteExperiment I O) (D : FiniteDesign (I → Bool)) (p : I → ℝ) → probability-vector and positivity bounds → IndepHeteroBernoulli D p ... → BoundedOutcomes E → pairwise linear-score moment formula ∧ E.varScale D p ≤ E.varEnvelope p` | Heterogeneous envelope theorem: factors the covariance of the Hájek linearization scores through graph overlap loads and bounds the resulting variance scale by the observable graph-only envelope. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.convex_design` | `(E : BipartiteExperiment I O) (ε B : ℝ) → 0 < ε → ε < 1/2 → card I * ε ≤ B → B ≤ card I * (1 - ε) → feasibleSet nonempty ∧ compact ∧ convex ∧ ConvexOn E.varEnvelope ∧ envelope minimizer exists ∧ KKT-style multiplier certificate for every minimizer` | Convex envelope-design theorem: the budgeted positivity-floor feasible set is a compact convex design class, the conservative envelope is convex on it, and an envelope-optimal heterogeneous Bernoulli design exists with a finite-dimensional multiplier certificate. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.surrogate_certificate` | `(E : BipartiteExperiment I O) (ε B dbar : ℝ) → 0 < ε → ε < 1/2 → BoundedOutcomeDegree E dbar → BudgetAdmissible ε B → uniform surrogate/envelope sandwich ∧ approxRatio E ε B ≤ max 1 (ε ^ (-(dbar - 1)))` | Surrogate approximation certificate: under bounded shared-neighborhood degree and budget admissibility, the additive observable surrogate uniformly sandwiches `V_env/4` and yields a nonvacuous approximation-ratio bound. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.heterogeneity_separation` | `(E : BipartiteExperiment I O) (ε B rho : ℝ) → EpsilonAdmissible ε → BudgetAdmissible ε B → rho = B/card I → ε < rho ∧ rho < 1 - ε → rho ≠ 1/2 → homogeneous design data and envelope-line C² data → gradient-spread separation ∧ singleton-gradient formula ∧ singleton degree-spread separation` | Heterogeneity-separation theorem: proves that observable gradient spread gives a strict envelope improvement over the homogeneous design, and derives that trigger from unequal singleton-exposure degree summaries away from `rho = 1/2`. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.hetero_clt` | `(E : ∀ n, BipartiteExperiment (Ix n) (Ox n)) (D : ∀ n, FiniteDesign (Ix n → Bool)) (p : ∀ n, Ix n → ℝ) (ε B : ℕ → ℝ) (dbar Dbar : ℝ) → card (Ox n) = n eventually → heterogeneous Bernoulli, bipartite-interference, bounded-outcome/degree/dependency, feasible optimal-design, uniform ε-floor, and variance-nondegeneracy assumptions → HeteroLinearization E D p ∧ studentized CDF convergence to stdNormalCdf` | Heterogeneous Hájek CLT: along feasible envelope-optimal designs, the Hájek estimator linearizes onto the sum of local scores and the studentized statistic converges to the standard normal in CDF form; its denominator tightness is derived from the stated regularity conditions. |
| `CausalSmith.Experimentation.BipartiteMinimaxDesign.postdesign_wald` | `(E : ∀ n, BipartiteExperiment (Ix n) (Ox n)) (D : ∀ n, FiniteDesign (Ix n → Bool)) (p : ∀ n, Ix n → ℝ) (ε B : ℕ → ℝ) (dbar Dbar : ℝ) → assumptions of hetero_clt → 0 < α → α < 1 → 0 ≤ z → stdNormalCdf z = 1 - α/2 → variance domination ∧ conservative Wald coverage liminf at least 1 - α` | Post-design conservative Wald theorem: uses the graph-only conservative envelope as a variance upper bound and converts the heterogeneous CLT into asymptotic conservative coverage for the optimized design. |

### 3.7 Central-DP Hölder CATE two-point barrier — the ordinary TV-contraction converse is limited to the CATE-smoothness privacy branch

Stat/Theorems entry generated under `CausalSmith/Stat/STAT_DpCateMinimaxV1_Research/` (`stat_dp_cate_minimax_v1`, `holder_central_dp`). The result is a proof-method hardness theorem for pointwise observational Hölder CATE under central approximate differential privacy: the local causal geometry forces a total-variation separation of order `Δ^(1+d/γ)`, so a lower-bound argument certified through the ordinary DP TV-contraction route cannot yield a privacy separation larger than `(n ε_n)^(-γ/(γ+d))`. It does not rule out fuzzy, multi-hypothesis, or other private comparison methods.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.DpCateMinimax.causal_dp_two_point_barrier` | `{d : ℕ} → (alpha beta gamma L e0 f0 f1 r0 : ℝ) → (x0 : Fin d → ℝ) → (eps del : ℕ → ℝ) → RegimeConstants alpha beta gamma L e0 f0 f1 r0 x0 → PrivacyBudget eps del → ∃ cTV, 0 < cTV ∧ (∀ P Q, HolderCateClass … P → HolderCateClass … Q → IidSampling P → IidSampling Q → cTV * |τ_P(x0) - τ_Q(x0)|^(1 + d/gamma) ≤ tvDist P.dataMeasure Q.dataMeasure) ∧ (∀ η ∈ (0,1), ∃ Cη > 0, ∀ n ≥ 1, … → |τ_P(x0) - τ_Q(x0)| ≤ Cη * (n * eps n)^(-gamma/(gamma+d)))` | Causal DP two-point / TV-contraction barrier: every pair of admissible CATE laws has TV separation at least a power of its pointwise CATE gap, and any central-DP two-point certificate based on the standard TV contraction is consequently limited to the `γ`-smoothness privacy branch. |

### 3.8 Discrete-confounder ATE minimax log-gap — matched fixed-overlap rate and universal hybrid

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_DiscreteAteMinimaxLoggap_Research/` (`stat_discrete_ate_minimax_loggap` / `polynomial_upper_match`). The paper establishes the fixed-interior minimax MSE rate for an ATE with an unrestricted finite discrete confounder, using a computable heavy/light ratio-polynomial hybrid; it also supplies a verified confounding witness and an overlap-free-input universal-hybrid envelope. The lower half is the cited `ZengOneArmMinimaxLower` result, formally transferred from the control-zero treated-arm subclass to ATE; its published moment-matching proof is not rederived in Lean. The triangular-array overlap frontier `epsilon = epsilon_n` remains explicitly open.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.DiscreteAteMinimaxLoggap.two_category_confounding` | `(epsilon : ℝ) → 0 < epsilon → epsilon ≤ 1 / 2 → Consistency Q ∧ ConditionalExchangeability Q ∧ Overlap epsilon (observedMarginal Q) ∧ ateFunctional (observedMarginal Q) = 1 / 2 ∧ naiveContrast (observedMarginal Q) = epsilon`, where `Q = twoCategoryWitness epsilon …` | Two-category confounding witness: constructs a full-data law satisfying consistency, conditional exchangeability, and overlap whose true ATE is `1/2` while its naive observed contrast is `epsilon`. |
| `CausalSmith.Stat.DiscreteAteMinimaxLoggap.sharp_minimax_fixed_interior` | `(epsilon : ℝ) → ZengOneArmMinimaxLower epsilon → 0 < epsilon → epsilon < 1 / 2 → ∃ a_epsilon rho_epsilon C_epsilon, ∃ N_epsilon, …` | Matched fixed-interior minimax theorem: uniformly for `d ≤ rho_epsilon n log n`, the hybrid’s worst-case MSE and the minimax risk are bounded above and below by epsilon-dependent multiples of `1/n + d²/(n² log² n)`; it also gives the parametric elbow and consistency frontier. |
| `CausalSmith.Stat.DiscreteAteMinimaxLoggap.overlap_adaptive_universal_hybrid` | `HybridEstimatorComputable ∧ (fixed-interior hybrid rate) ∧ (centered-estimator bound) ∧ (1/(100n) ≤ minimaxRisk n d (1/2) ≤ 1/n)` | Universal-hybrid theorem: one computable estimator independent of `epsilon` attains the fixed-class hybrid rate, combines with a centered estimator for the near-randomization envelope, and gives the exact-order endpoint bracket. |

### 3.9 Sharp minimax Neyman-regret rate for adaptive two-arm experiments — global log-rate and instance-local converse (conditional)

Stat/Theorems entries under `CausalSmith/Stat/STAT_NeymanRegretMinimax_Research/` (namespace `CausalSmith.Stat.NeymanRegretMinimax`; `stat_neyman_regret_minimax`, banked downgraded). The run certifies the gate-free core of a `Θ(log T)` minimax Neyman-regret rate for adaptive two-arm design under a superpopulation law class, with the leading constant the strict-interior local-complexity supremum `sup_{ν ∈ M°} κ_ν`. Both headline theorems compile **sorry-free and axiom-clean but are honest CONDITIONALS**: they assume `LocalNeighborhoodRiskInputs`, the disclosed sequential van Trees / DQM Fisher-tensorization bridge (an external substrate debt tracked in `SUBSTRATE_DEBT.md`); the tilt band-functional continuity is *derived* inside the proof, not assumed. They are not certified unconditional.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.NeymanRegretMinimax.global_log_rate` | `LocalNeighborhoodRiskInputs → ∃ c₀ > 0, ∀ band (0<um<om≤1, 0<ur) with a strict-interior tangent law and i.i.d.-realizable band, ∀ ε>0, ∃ T₀, ∀ T ≥ T₀, c₀·(sup_{ν ∈ M°} κ_ν − ε)·log T ≤ ⨅_Alg ⨆_{ν ∈ MBand} cumulativeNeymanRegret Alg ν T` | Global minimax log-rate converse: every adaptive design pays worst-case cumulative Neyman regret at least `c₀·(sup_{M°} κ_ν)·log T` over the band class, with a single universal constant `c₀` (quantified outside the band parameters) and the strict-interior complexity supremum as leading factor. |
| `CausalSmith.Stat.NeymanRegretMinimax.instance_local_minimax` | `LocalNeighborhoodRiskInputs → ∃ c₀ > 0, ∀ ν ∈ M_tan, ∀ μ with SuperpopulationIID μ ν, ∀ ε>0, ∃ feasible direction u_ε with H_ν·π̇_ν(u_ε)²/J_ν(u_ε) ≥ κ_ν − ε, a linear-tilt local path, radius η>0, and T₀ s.t. ∀ Alg, T ≥ T₀, c₀·(κ_ν − ε)·log T ≤ ⨆_{h ∈ [−η,η]} cumulativeNeymanRegret Alg (p h) T` | Instance-level local minimax converse: near every tangent law `ν`, some `η`-neighborhood of linear tilts forces cumulative Neyman regret `≥ c₀·κ_ν·log T` on every adaptive design. |

### 3.10 Saturation-design skew threshold — quartic moment-program design optimum (in-flight)

Experimentation/Theorems entries under `CausalSmith/Experimentation/EXP_SaturationSkewThreshold_Research/` (namespace `CausalSmith.Experimentation.SaturationSkew`; `exp_saturation_skew_threshold`, v1). The run treats the cluster-saturation law as the experimental-design object and studies when the design-based variance functional — a quartic in the saturation moments, `V0 + V1·m₂ + V3·m₃ + V4·m₄` — is minimized at a symmetric (Dirac) design versus a skewed one, reducing V-optimization to a moment program with an at-most-three-point optimizer.

**Status: banked `downgraded`** (`_bank/downgraded/exp_saturation_skew_threshold_v1`); several headline proofs are still `sorry`. The theorem statements below are scaffolded and the shared substrate is largely proved, but several headline proofs are still `sorry`, resting on three deferred substrate-gates (a Cai-type saturation moment expansion, a CAD / Tarski–Seidenberg semialgebraic solver, and a Winkler extreme-point step). Signatures may still change.

| Declaration | Description |
|---|---|
| `variance_moment_reduction` | Reduces the design-based finite-population variance to the quartic saturation-moment functional `V0 + V1 m₂ + V3 m₃ + V4 m₄`. |
| `sutva_corner` | Under SUTVA the functional is linear in the variance moment, with sign-determined corner minimizers. |
| `support_at_most_three` | An optimal saturation law exists supported on at most three points, collapsing at the budget corners. |
| `positive_penalty_threshold` | The Dirac (symmetric) design minimizes iff the local quadratic penalty `V1 + V3 d + V4 d²` is nonnegative on the centered support domain. |
| `skewed_witness` | A concrete skewed admissible law strictly beats the Dirac design on the variance functional. |
| `explicit_profile_value` / `explicit_two_point_cell` | Closed-form value of the optimization profile, and the least value achievable by a two-point cell. |
| `global_certificate` / `constructive_optimal_design_algorithm` | Global optimality certificate for the minimizer, and a semialgebraic constructive algorithm producing a ≤3-point optimal design. |
| `design_optimal_saturation` / `finite_attainability` / `rounding_loss` | Design-level optimum, exact finite-sample attainability of finitely-supported targets, and the integer-rounding loss bound. |

### 3.11 Bounded-outcome SNIPE degree frontier — matched cancellation-sensitive minimax risk

Experimentation/Theorems entry generated under `CausalSmith/Experimentation/EXP_SnipeDegreeFrontier_Research/` (`exp_snipe_degree_frontier`, v1). The theorem gives the simultaneous bounded-coefficient-mass and uniformly bounded-outcome minimax frontier for total-treatment-effect estimation under product-Bernoulli assignment and low-order neighborhood interference. Its risk scale is `B² min(1, d A_d / n)`, where the exact SNIPE energy `A_d` is comparable to `binom(d, kStar d beta p)`; complete directed blocks attain the sharp unclipped-SNIPE constant.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.SnipeDegreeFrontier.bounded_outcome_degree_frontier` | `(beta : Nat) (p : Real) → 1 ≤ beta → 0 < p → p < 1 → ∃ cLower cUpper > 0, ∀ n D d B, 1 ≤ n → DegreeIndex (Fin n) d → 0 ≤ B → IsProductBernoulli D p → …` | Matched two-class degree frontier: the coefficient-mass model is contained strictly in the bounded-outcome model for `B>0, d≥1`, and both minimax risks are sandwiched at the cancellation-sensitive rate `B² min(1, d A_d/n)`; clipped SNIPE attains the bound, while canonical SNIPE has exact complete-block worst-case risk `B² d A_d/n`. |

### 3.12 Transported LATE strength frontier — matched compact confidence-set length under weak first stages and covariate shift

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_TransportedLateStrengthFrontier_Research/` (`stat_transported_late_strength_frontier`, v1). The paper identifies the effective transported-IV strength as `n μ_n² / κ_n`, where `κ_n` is Kish weight dispersion, and establishes the compact confidence-set-length frontier `min(1, t₀⁻¹/²)`. It includes oracle and unknown-weight attainments, global and finite-cell converses, a fixed-geometry formulation, and the no-shift reduction.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.TransportedLateStrengthFrontier.compact_causal_range` | `(P : TransportedArray 𝒳) (N k : ℕ → ℕ) (c epsilon : ℝ) (n : ℕ) → integrability assumptions → TransportedIVClass P N k c epsilon n → … ∧ transportedOutcomeITT P n / transportedFirstStage P n = targetCACE P n ∧ targetCACE P n ∈ [-1,1]` | Compact causal-range theorem: the transported reduced-form ratio equals the target complier causal effect and lies in the forced bounded parameter space. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.fixed_geometry_frontier` | `(N k : ℕ → ℕ) (c epsilon alpha : ℝ) (g : Geometry 𝒳) → rate, admissibility, and slice-assumption hypotheses → ∀ t₀ > 0, cα·min(1,t₀⁻¹/²) ≤ fixedGeometryValue … t₀ ∧ fixedGeometryValue … t₀ ≤ Cα·min(1,t₀⁻¹/²)` | Fixed-geometry frontier: every admissible deterministic transport geometry has conditional honest expected-length order `min(1,t₀⁻¹/²)`, uniformly in the geometry. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.no_shift_reduction` | `(N k : ℕ → ℕ) (c epsilon alpha : ℝ) (g : Geometry 𝒳) → standing assumptions → w = 1 a.e. → (∀ n, geometryKish g n = 1 ∧ targetX n = sourceX n) ∧ …` | No-shift reduction: unit transport weight makes source and target covariate laws coincide, sets Kish dispersion to one, and recovers the single-population weak-ratio frontier. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.oracle_converse` | `(N k : ℕ → ℕ) (c epsilon alpha : ℝ) → standing and class-assumption hypotheses → ∃ c₀ t_c > 0, ∀ t₀ > 0, c₀·min(1,t₀⁻¹/²) ≤ oracleValue … t₀ ∧ …` | Oracle converse: even when transport weights and propensities are known, global and finite-cell honest procedures have a common lower confidence-set-length frontier determined only by effective strength. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.oracle_score_inversion_attainment` | `(N k : ℕ → ℕ) (c epsilon alpha : ℝ) → standing and class-assumption hypotheses → ∃ C : OracleProcedure 𝒳 N k c epsilon, … ∧ OracleHonest … C ∧ ∀ t₀ > 0, frontierRisk … C ⟨t₀, _⟩ ≤ C₀·min(1,t₀⁻¹/²)` | Oracle score-inversion attainment: one honest transported-score inversion sequence simultaneously achieves the compact frontier at every positive strength threshold. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.finite_cell_unknown_weight_attainment` | `(N k : ℕ → ℕ) (c epsilon alpha : ℝ) → standing, measurable-cell, and finite-cell-class hypotheses → ∃ C : FiniteCellProcedure 𝒳 N k, … ∧ FiniteCellHonest … C ∧ …` | Finite-cell unknown-weight attainment: a sample-only collision-scaled inversion rule is honest and attains the oracle order without being given the transport-weight vector. |
| `CausalSmith.Stat.TransportedLateStrengthFrontier.regular_cell_unknown_weight_attainment` | `(N k : ℕ → ℕ) (c epsilon alpha cminus cplus : ℝ) → standing, measurable-cell, and regular-cell-class hypotheses → ∀ L, … → ∃ C : RegularCellProcedure 𝒳 N k, …` | Regular-cell unknown-weight attainment: with known regular source-cell probabilities and propensity, a cross-averaged rule that does not use the transport weight matches the oracle frontier order. |

### 3.13 Logarithmic uniform-risk penalty for distance-based boundary estimation — point-indexed and CTY common-map converses

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_BddUniformLogPenalty_Research/` (`stat_bdd_uniform_log_penalty`, v1). On the exact CTY nonparametric law class for integer `q ≥ 1` and `L ≥ 4`, the paper proves that the coordinatewise distance architecture has a necessary `(log n / n)^(1/4)` uniform-risk penalty. The point-indexed result permits arbitrary law-independent Borel fixed-point sections and uses outer expectation; the inherited CTY common-map result transfers the same logarithmic converse to completed expectation. Both are converse-only statements and assert neither a matching upper bound nor an exact asymptotic constant.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.BddUniformLogPenalty.point_indexed_distance_log_converse` | `(q : ℕ) (L : ℝ) → 1 ≤ q → 4 ≤ L → ∃ cPI > 0, liminf (scaledRisk (pointIndexedDistanceRisk · q L)) = liminf (normalizedRisk (pointIndexedDistanceRisk · q L)) ∧ ENNReal.ofReal cPI ≤ liminf (normalizedRisk (pointIndexedDistanceRisk · q L))` | Point-indexed logarithmic converse: uniformly over the exact CTY law class, every law-independent distance-rule family with Borel fixed-point sections has positive normalized outer-expected minimax liminf at the `(log n / n)^(1/4)` rate. |
| `CausalSmith.Stat.BddUniformLogPenalty.cty_same_class_log_converse` | `(q : ℕ) (L : ℝ) → 1 ≤ q → 4 ≤ L → ∃ cCTY > 0, liminf (scaledRisk (ctyDistanceRisk · q L)) = liminf (normalizedRisk (ctyDistanceRisk · q L)) ∧ ENNReal.ofReal cCTY ≤ liminf (normalizedRisk (ctyDistanceRisk · q L))` | CTY common-map same-class converse: transfers the positive logarithmic lower bound to CTY’s literal common-map decision class, whose completed expectation agrees with the relevant outer expectation. |

### 3.14 Fixed-separation non-Gaussian partially linear estimation via transform zeros — exact identification, root-n risk, and Gaussian diagnostics

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_SaPlmCumulantConverse_Research/` (`stat_sa_plm_cumulant_converse` / `nongaussian_spectral_annihilation`). The paper develops a fixed-separation theory for a non-Gaussian partially linear model using zeros of the treatment-noise transform: it establishes exact residual identities and contour identification, then proves an explicit fixed-record spectral procedure has matched `n⁻¹` minimax MSE bounds. It also aligns the result with the published finite-order ACE comparison class, isolates a symmetric-mixture benchmark, and records the bounded-outcome Gaussian case as a degeneracy diagnostic rather than a Gaussian-versus-non-Gaussian minimax claim. The shrinking-cumulant local frontier remains an open question.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.SaPlmCumulantConverse.known_zero_instrument` | `(p : Parameters) (m : Model p) → 1 ≤ n → NonGaussianClass p n m → 1 ≤ ell → treatmentMGF p m z0 = 0 → analyticOrderNatAt (treatmentMGF p m) z0 = ell → …` | Known-zero instrument theorem: a treatment-transform zero yields a conditionally centered residual instrument, an exact residual moment factorization, and—when the denominator is nonzero—a ratio formula for the PLM coefficient. |
| `CausalSmith.Stat.SaPlmCumulantConverse.exact_contour_identification` | `(p : Parameters) (m : Model p) → 1 ≤ n → NonGaussianClass p n m → (pStar : CertifiedBankInputs p) → (j : Fin …) → … → θ₀ = contourFunctional … ∧ contourFunctional … = normalized circleIntegral …` | Exact contour identification: on a certified zero-free contour with a nonzero residual zero count, the target coefficient equals the contour functional and its normalized circular-integral representation. |
| `CausalSmith.Stat.SaPlmCumulantConverse.adaptive_rootn_minimax` | `(r : ℕ) (delta Ctheta Cg Cq psieta psixi : ℝ) → … → ∃ c C > 0, ∀ …, c / n ≤ minimaxRisk … ∧ minimaxRisk … ≤ C / n ∧ …` | Fixed-separation minimax theorem: a common certified spectral experiment has an `n⁻¹` minimax MSE lower and upper bracket, a measurable ordinary estimator, generalized-quantile control, and a conditional represented-execution guarantee. |
| `CausalSmith.Stat.SaPlmCumulantConverse.jms_ace_alignment` | `∀ (r : ℕ) (delta Ctheta Cg Cq psieta psixi : ℝ), … → ∃ Cspec > 0, ∀ …` | ACE alignment: relates the spectral model class to the published finite-order ACE class, preserves the cited ACE quantile upper bound, and gives the stated asymptotic upper-versus-upper comparison. |
| `CausalSmith.Stat.SaPlmCumulantConverse.common_experiment_dichotomy` | `(r : ℕ) (delta Ctheta Cg Cq psieta psixi : ℝ) → … → ∀ …` | Common-experiment theorem: along fixed-record parameter sequences, the non-Gaussian spectral risk has an `n⁻¹` bracket, while the bounded-outcome Gaussian branch has a zero target; it also retains the conditional compiled-execution clause. |
| `CausalSmith.Stat.SaPlmCumulantConverse.symmetric_mixture_reduction` | `(Ctheta Cg Cq psixi : ℝ) → ∃ C > 0, ∀ p m, … → fourthCumulant p m = -2 ∧ treatmentMGF p m (iπ/2) = 0 ∧ …` | Symmetric-mixture reduction: the stipulated two-Gaussian mixture has fourth cumulant `-2`, a transform zero at `iπ/2`, a sine-instrument denominator lower bound, and an `n⁻¹` MSE bound for the sine estimator. |
| `CausalSmith.Stat.SaPlmCumulantConverse.local_to_gaussian_partial_benchmarks` | `(deltaSeq : ℕ → ℝ) → (∀ n, 0 < deltaSeq n) → Antitone deltaSeq → Tendsto deltaSeq atTop (nhds 0) → …` | Partial local-to-Gaussian benchmarks: supplies a Gaussian-Rademacher path bound and a conditional comparison between the published ACE bound and the better of two supplied upper bounds, without resolving the local minimax frontier. |
| `CausalSmith.Stat.SaPlmCumulantConverse.bounded_outcome_gaussian_degeneracy` | `(p : Parameters) → (∀ m, GaussianClass p p.n m → m.theta0 = 0) ∧ …` | Bounded-outcome Gaussian degeneracy: the simultaneous Gaussian-class assumptions force the PLM coefficient to vanish, so the corresponding Gaussian minimax MSE and generalized-quantile risks are zero on a nonempty fixed-code class. |

### 3.15 Discrete-confounder ATE heterogeneity frontier — all-alphabet same-class minimax bracket

Stat/Theorems entry generated under `CausalSmith/Stat/STAT_DiscreteAteHeterogeneityFrontier_Research/` (`stat_discrete_ate_heterogeneity_frontier`, v1). Under fixed overlap, arbitrary unknown cell masses, bounded conditional means and conditional second central moments, the result combines a rare-cell polynomial estimator with an occupancy-weighted estimator selected using the known heterogeneity radius. It gives an all-alphabet upper and lower minimax bracket on the same real-outcome class, matches in the established endpoint and fixed-radius regimes, and leaves only the identified shrinking-radius wedge potentially unresolved.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.DiscreteAteHeterogeneityFrontier.two_sided_minimax_bracket_all_d` | `(epsilon : ℝ) → 0 < epsilon → epsilon < 1 / 2 → ∃ c_epsilon C_epsilon : ℝ, 0 < c_epsilon ∧ c_epsilon ≤ 1 ∧ 1 ≤ C_epsilon ∧ c_epsilon ≤ C_epsilon ∧ ∀ n d : ℕ, ∀ M sigma : ℝ, 0 < n → 0 < d → 1 ≤ M → 0 ≤ sigma → sigma ≤ 2 → c_epsilon * M ^ 2 * converseRate n d sigma ≤ minimaxRisk n d epsilon M sigma ∧ minimaxRisk n d epsilon M sigma ≤ C_epsilon * M ^ 2 * frontierRate n d sigma` | All-alphabet heterogeneity-radius bracket: on one arbitrary-cell-mass real-outcome model class, the minimax ATE risk lies between the capped exact-homogeneity-plus-radius converse rate and the known-radius selector rate, without claiming uniform equivalence throughout the residual shrinking-radius wedge. |

### 3.16 Multi-arm binary minimax second-order frontier — universal four-thirds exponent and exact finite certificates

Experimentation/Theorems entry generated under `CausalSmith/Experimentation/EXP_MultiarmSecondorderMinimaxFrontier_Research/` (`exp_multiarm_secondorder_minimax_frontier`, v1). For a fixed multi-arm binary finite population and a prespecified zero-sum contrast, the development identifies the labeled minimax problem with an explicit finite response-type orbit game, proves the contrast-weighted first-order saddle value, and establishes a positive second-order improvement of order `n⁻⁴ᐟ³` with a matching-order internal converse. Exact rational finite programs give procedure and prior certificates for rational contrasts, while risk continuity transfers scale-matched brackets to real contrasts. The paper-local observation-dependent Bayesian information argument makes the product-measure almost-everywhere measurability and integrability fields needed for Fubini and weighted-L² steps explicit. The sharp normalized constant, convergence, optimizer/prior limits, and a limiting face-corner operator remain open.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.MultiarmSecondorderMinimaxFrontier.second_order_rate_and_certificate_frontier` | `(K : ℕ) (c : Contrast ℝ K) → AdmissibleArmCount K → 3 ≤ (Sc c).card → let _openFrontier := secondOrderRateAndCertificateOpenFrontier; 0 < liminf (fun n => secondOrderScale n * dN K c n) atTop ∧ liminf … ≤ limsup … ∧ limsup … ≤ 43 * C0 c ∧ (nonempty compact cluster set) ∧ (every convergent positive regularly varying normalization has exponent 4/3) ∧ (slow-variation characterization) ∧ (exact rational and transferred real-contrast certificate conclusions) ∧ (three-arm full-data risk is strictly below the scalar-compression minimax value)` | Second-order rate and certificate frontier: for contrasts with at least three active arms, the normalized minimax improvement has a nonempty compact cluster set bounded strictly above zero and by `43·C₀(c)`; any regularly varying normalization yielding a positive finite limit must have exponent `4/3`, exact finite-program certificates track the same scale for rational and real contrasts, and the certified three-arm example shows scalar score compression is not minimax-preserving. |

### 3.17 Sharp survivor-complier benefit bounds by partial transport

PartialID/Theorems entry generated under `CausalSmith/PartialID/PID_SlateBenefitPartialtransport_Research/` (`pid_slate_benefit_partialtransport`, v1). For finite ordered outcomes under conditional IV independence, exclusion, overlap, no defiers, and covariate-specific weak selection monotonicity, the paper identifies survivor-complier outcome capacities and gives the exact branch-free partial-transport projection of compatible full laws. Its threshold-cut formulas give sharp lower and upper bounds for the survivor-complier probability of benefit, including unequal selection directions, zero-gap tie faces, and zero-survivor cells; compatible full laws attain both endpoints.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.PartialID.SlateBenefitPartialTransport.sharp_exact_mass_threshold_interval` | `(S : POSlateSystem P 𝒳 K) → (εZ : ℝ) → (d : 𝒳 → Bool) → TieSafeSurvivorModel S εZ d → (compatible full-law survivor couplings equal the branch-free exact-mass capacity polytope) ∧ (sign and tie-face reductions) ∧ (threshold-flow lower and upper extrema) ∧ (identified benefit interval equals the closed aggregate interval) ∧ (endpoint-attaining flows)` | Sharp exact-mass threshold-interval theorem: every compatible IV-selection law projects to precisely the capacity-constrained survivor-complier transport polytope; its threshold flows attain the sharp conditional probability-of-benefit endpoints, including increasing, decreasing, and zero-gap selection faces. |

### 3.18 Target-span proxy transport — exact identification beyond full source rank

SCM/Theorems entry generated under `CausalSmith/SCM/SCM_ProxyTargetspanTransport_Research/` (`scm_proxy_targetspan_transport`, v1). For a positive finite latent-shift SCM with an injective proxy channel, the target intervention probability is point identified exactly when the target proxy law lies in the treatment-specific source proxy span; every balancing vector then yields the same observable outcome-moment functional. Off the span, the companion positive full-law perturbation construction supplies observationally equivalent compatible models with different intervention probabilities.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.SCM.ProxyTargetspanTransport.target_span_iff` | `(PO : E → W → X → Y → ℝ) → (bvec : W → ℝ) → compatibleFiber PO bvec is nonempty → M0 ∈ compatibleFiber PO bvec → ProxyChannelInjectivity M0 → (x : X) → (y : Y) → (IdentifiedInterval (interventionalProb · x y) (· ∈ compatibleFiber PO bvec) is subsingleton ↔ balancingFiber (condProxyMatrix M0 x) bvec is nonempty) ∧ (every balancing vector gives the common identified value) ∧ (all balancing vectors give the same observable functional)` | Target-span identification theorem: within the positive compatible finite-SCM fiber, the transported intervention probability is constant exactly when the target proxy vector can be balanced by the observed treatment-specific proxy matrix; any such weights compute the common causal value, independently of the chosen solution. |

### 3.19 Propensity-hinge completeness frontier for divergence-based causal bounds

SCM/Theorems entry generated under `CausalSmith/SCM/SCM_PropensityLvSharpnessFrontier_Research/` (`scm_propensity_lv_sharpness_frontier`, v1). The paper characterizes exactly which calibrated convex divergence generators recover the mutual-support bow-SCM compatible-law class: universal exactness is equivalent to exactness on full-support binary laws, and both hold precisely for the propensity-cap hinge frontier.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.SCM.PropensityLvSharpnessFrontier.generator_exactness_iff_hinge` | `(e : ℝ) (f : ℝ → ℝ) → StrictPositivity e → AdmissibleGenerator f → (UniversalMutualExact f e ↔ BinaryMutualExact f e) ∧ (BinaryMutualExact f e ↔ f ∈ hingeClassSet (1 / e))` | Propensity-hinge frontier theorem: for a positive nondegenerate treatment propensity and an admissible convex generator, exact mutual-support divergence-ball recovery over every outcome law is equivalent to recovery on full-support binary laws, and this occurs exactly when the generator lies in the affine hinge class at likelihood-ratio cap `1 / e`. |

### 3.20 Honest minimax risk and confidence-length frontier for noninvertible threshold modified-treatment policies under polynomial generalized-propensity thinning

Stat/Theorems entry generated under `CausalSmith/Stat/STAT_LmtpThresholdAtomFrontier_Research/` (`stat_lmtp_threshold_atom_frontier`, v1). For a finite-stratum continuous-treatment clamp policy with polynomially thinning generalized propensity, the paper gives a matched observed-data minimax absolute-risk frontier. The primary result combines a total-Gram stabilized estimator with global-shift and localized-Bernoulli witnesses, yielding the regular empirical-mean term plus the threshold-atom local-regression term.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.LmtpThresholdAtomFrontier.clamp_minimax_risk` | `(J : ℕ) (beta kappa L cminus cplus pmin deltaBar alpha : ℝ) → RegimeConstants J beta kappa L cminus cplus pmin deltaBar alpha → ∃ c C amplitude > 0, c < C ∧ amplitude ≤ 1 / 4 ∧ ∀ deltaSeq, ThresholdSequence deltaBar deltaSeq → ∀ Bseq, ∀ᶠ n, c * clampFrontier n delta kappa h beta ≤ observedMinimaxRisk … ≤ stabilizedWorstRisk … ≤ C * clampFrontier n delta kappa h beta ∧ (global-shift and localized-Bernoulli witnesses with bounded product chi-squared divergence)` | Matched clamp minimax-risk theorem: uniformly over admissible threshold sequences, the observed minimax absolute risk and the total-Gram stabilized estimator’s worst-case risk are of the same order as the sum of the root-`n` natural-course term and the polynomially thinned threshold-atom local-regression term; explicit global and localized Bernoulli alternatives certify both lower-bound components. |

### 3.21 Collision-uniform proxy effect-law frontier — quotient-law estimation, inference, and minimax bounds through eigenvalue collisions

Stat/Theorems paper-scoped entries under `CausalSmith/Stat/STAT_ProxyEffectlawEigencollisionFrontier_Research/` (`stat_proxy_effectlaw_eigencollision_frontier`, v1). The paper studies a uniformly conditioned latent-class proxy model and treats the positive quotient law of latent treatment effects as the target. It establishes a gap-free Lipschitz observable-summary-to-law map, total positive estimators and honest root-`n` confidence sets through effect collisions, cluster-adaptive reporting, and matching two-class minimax frontiers. Ordered latent weights remain gap-sensitive, with the separate clipped inverse-gap rate recorded below; constructive total repair is retained as an open question.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.observed_vmw_margin_inclusion` | `(publishedScope) (publishedMargins) (P : Measure (FullData k dx dz)) [IsProbabilityMeasure P] → dimension, margin, and UCVMWModel hypotheses → observed-moment factorization, singular-value margins, envelopes, potential-outcome bounds, and conditional published-VMW scope` | Model-inclusion proposition: derives the observable factorization, uniform singular margins, and bounded latent potential-outcome/effect envelopes from the uniformly conditioned causal proxy model. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.summary_closure_compact` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and positive-margin hypotheses → bounded admissible summary image ∧ IsCompact summaryClosure ∧ (summaryClosure.Nonempty ↔ Nonempty ModelLaw)` | Compactness proposition: the admissible observable-summary image is uniformly bounded, its closure is compact, and that closure is nonempty exactly when the model class is. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.gap_free_positive_measure_modulus` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and positive-margin hypotheses → ∃ Cmod > 0, quotient-law Lipschitz bound and unique continuous measurable extension to summaryClosure` | Gap-free modulus theorem: the positive quotient effect law is Lipschitz in observable summary distance uniformly across collisions, with a unique positive real-law extension to the compact closure. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.summary_repair_total_borel` | `(k dx dz n : ℕ) (L pi0 sigma0 alpha C0 : ℝ) → domain hypotheses → Measurable empSummary → ∃ R, Measurable (summaryRepair R) ∧ empty-closure zero-law fallback ∧ nonempty theoretical confidence set` | Total-repair proposition: constructs a measurable positive-law repair for every empirical summary, including a zero-law fallback when the admissible closure is empty. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.collision_uniform_root_n` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and margin hypotheses → ∃ C, uniform high-probability W1 root-n bounds for lattice and repaired-summary law estimators` | Collision-uniform estimation theorem: both the explicit lattice estimator and theoretical repaired-summary estimator achieve a uniform root-`n` Wasserstein bound without an eigengap condition. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.honest_root_n_confidence` | `(k dx dz : ℕ) (pi0 sigma0 : ℝ) → dimension and margin hypotheses → ∃ concentration constant, lattice estimator, and repair data giving honest nonempty confidence sets with root-n W1 diameters` | Honest-confidence theorem: supplies simultaneous coverage and root-`n` Wasserstein diameter bounds for the theoretical and computable confidence sets at every collision pattern. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.cluster_adaptive_report` | `(k dx dz : ℕ) (L pi0 sigma0 alpha C0 : ℝ) → model and concentration hypotheses → cluster partition, support intervals, mass intervals, and event probability bound` | Cluster-report theorem: converts the computable law confidence set into collision-adaptive support and aggregate-mass reports, with interval widths controlled by external cluster separation. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.labeled_weight_upper` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and margin hypotheses → ∃ C, ∀ n g P, GapStratum … → expectedWeightRisk … ≤ C * min 1 (sqrt n * g)⁻¹` | Labeled-weight upper bound: ordered latent weights have the expected clipped inverse-gap risk on separated strata, unlike the gap-free quotient-law target. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.two_class_witness_valid` | `(eps : ℝ) → WitnessPerturbationDomain eps → UCVMWModel witnessLaw eps ∧ explicit arm probabilities, proxy determinants, masses, and quotient law` | Collision-witness proposition: verifies an unequal-weight, full-rank two-class proxy law throughout the collision, including its explicit positive effect law. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.matching_local_lower_bounds` | `(cLoc : ℝ) → LocalRadiusDomain cLoc → ∃ a c C > 0, ∀ n, quotient-law and labeled-weight two-point local risk lower bounds` | Local-converse theorem: explicit factorization-preserving two-point paths yield root-`n` quotient-law and clipped inverse-gap labeled-weight lower bounds. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.same_class_quotient_minimax` | `∃ c C : ℝ, 0 < c ∧ 0 < C ∧ c < C ∧ ∀ n ≥ 1, minimax quotient-law risk on the fixed two-class UCVMW model is between c/sqrt n and C/sqrt n` | Same-class quotient minimax proposition: combines the estimator and local converse into a matched root-`n` Wasserstein frontier on one uniformly conditioned proxy class. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.same_class_labeled_minimax` | `∃ c C : ℝ, 0 < c ∧ 0 < C ∧ c < C ∧ ∀ n ≥ 1, ∀ g in the gap domain, labeled minimax risk is between c·min(1,(sqrt n g)⁻¹) and C·min(1,(sqrt n g)⁻¹)` | Same-class labeled minimax proposition: matches the ordered-weight upper and lower bounds on the identical two-class gap-localized model. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.finite_net_law_estimator` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and margin hypotheses → ∃ C, total Borel finite-net estimator with polynomial library/operation bounds and deterministic W1 error control` | Finite-net estimator theorem: a precomputed finite representative library gives a total positive law estimator with polynomial fixed-dimensional search complexity and collision-uniform accuracy. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.published_vmw_converse_transfer` | `(publishedScope) (publishedMargins) → VMW scope gates → ∃ a c > 0, quotient-law and labeled-weight lower bounds for every containing published-VMW comparator class` | Published-converse transfer theorem: lifts only the explicit two-point lower bounds to published VMW comparator classes containing the corresponding witness pairs. |
| `CausalSmith.Stat.ProxyEffectlawEigencollisionFrontier.polynomial_lattice_law_estimator` | `(k dx dz : ℕ) (L pi0 sigma0 : ℝ) → dimension and margin hypotheses → ∃ C, prescribed structured lattice estimator with atom floor, measurability, polynomial candidate count, and uniform root-n tail bound` | Structured-lattice proposition: constructs the explicit no-advice, sample-evaluable positive-law estimator used by the uniform estimation and confidence results. |

### 3.22 Discrete-confounder optimal-policy value — matched minimax frontier via a Jackson–factorial estimator

Stat/Theorems entry generated under `CausalSmith/Stat/STAT_DiscreteOptimalValueMinimaxMatched_Research/` (`stat_discrete_optimal_value_minimax_matched`, `jackson_factorial`). For binary treatment and outcome with an unrestricted high-dimensional categorical confounder and fixed overlap, the result matches an explicit pilot-local Jackson–factorial estimator to an all-measurable-estimator lower bound at the saturated rate `min{1, d / (n log(ed))}`. It also identifies the observed optimal-policy value with its causal counterpart and covers arbitrary cell masses, including null cells.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Stat.DiscreteOptimalValueMinimaxMatched.matched_minimax_frontier` | `CaiLowAbsoluteMomentPriors → JhwPoissonL1Lower → ∃ tuning : JacksonTuning, 2 < tuning.boundedAlphabetCutoff ∧ ∀ epsilon : ℝ, 0 < epsilon → epsilon < 1 / 2 → ∃ cepsilon Cepsilon : ℝ, 0 < cepsilon ∧ cepsilon ≤ Cepsilon ∧ ∀ n d : ℕ, 1 ≤ n → 2 ≤ d → cepsilon * min 1 (d / (n * logAlphabet d)) ≤ minimaxRisk n d epsilon ∧ minimaxRisk n d epsilon ≤ jacksonWorstCaseRisk tuning n d epsilon ∧ jacksonWorstCaseRisk tuning n d epsilon ≤ Cepsilon * min 1 (d / (n * logAlphabet d)) ∧ causalMinimaxRisk n d epsilon = minimaxRisk n d epsilon` | Matched optimal-value minimax theorem: assuming the cited Cai–Low moment-matching prior and Jiao–Han–Weissman Poisson L1 lower bound, the observed minimax risk and the explicit Jackson–factorial estimator’s worst-case risk are uniformly comparable to `min{1, d / (n log(ed))}`, and the causal and observed minimax risks coincide. |

### 3.23 Boolean Möbius variance-bound complete class — exact quartic separation under finite randomization

Experimentation/Theorems entry generated under `CausalSmith/Experimentation/EXP_BinaryTruthboundComplete_Research/` (`exp_binary_truthbound_complete`, v1). For finite randomized designs with binary potential-outcome schedules and assignment-specific observed coordinates, the paper characterizes expected observable variance bounds by their Boolean Möbius expansions, identifies universal conservativeness with finite pointwise domination, and gives the observable-margin dual and Pareto complete class. A certified six-coordinate experiment exhibits an exact nonnegative quartic improvement over every observable quadratic and cubic bound; the result also records the binary Aronow--Samii embedding, product tensorization, and conditional replicated-block studentized inference.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.BinaryTruthbound.expectedRule_mem_observableSpan_iff` | `(E : Setup) → (∃ g : AssignmentRule E, expectedRule E g = b) ↔ b ∈ observableSpan E ⊤`, together with the Möbius vanishing criterion, an assignment-level implementation formula, and pointwise conservativeness | Observable-function characterization: an expected assignment rule is implementable exactly when its Boolean Möbius coefficients vanish off observable coordinate sets; universal conservativeness is exactly pointwise domination of the true variance. |
| `CausalSmith.Experimentation.BinaryTruthbound.optValue_eq_dual_and_paretoCompleteClass` | `(E : Setup) → ∀ q : FullSupportObjective E,` primal attainment, equality with `sSup (dualObjective E '' dualMarginSet E q)`, dual attainment, complementary slackness, and the Pareto complete-class equivalence | Duality and complete-class theorem: every full-support objective has primal and dual optimizers, with tightness on positive dual mass characterizing joint optimality; exactly the Pareto-admissible conservative bounds optimize for some such objective. |
| `CausalSmith.Experimentation.BinaryTruthbound.rational_primal_dual_certificates` | `(E : Setup) → (q : Theta E → ℝ) →` rational assignment probabilities, scores, and objective weights `→ ∃` rational primal coefficients, dual weights, and assignment rule satisfying feasibility, equality, and tightness | Rational-certificate theorem: rational input data admit exact rational observable-bound, dual-margin, and assignment-table certificates. |
| `CausalSmith.Experimentation.BinaryTruthbound.designVariance_estimable_iff` | `(E : Setup) → ObservableScore E → (∃ g, expectedRule E g = trueVarianceFn E) ↔ trueVarianceFn E ∈ observableSpan E ⊤`, equivalent to the pairwise target-coefficient condition | Variance-estimability criterion: the true design variance is observable exactly when every never-jointly-observed coordinate pair has zero product of target coefficients. |
| `CausalSmith.Experimentation.BinaryTruthbound.hms_quadratic_mem_observableSpan_strict` | `(E : Setup) →` HMS-compatible quadratic forms lie in `observableSpan E 2` and, when PSD-conservative, in `conservativeCone E 2`; a conservative Boolean quadratic is not any homogeneous matrix quadratic | HMS inclusion theorem: the published compatible quadratic class embeds in the Boolean quadratic/cubic comparison space, and the Boolean quadratic cone is strictly larger. |
| `CausalSmith.Experimentation.BinaryTruthbound.optValue_odd_degree_collapse` | `(E : Setup) → ComplementInvariantVariance E → ComplementInvariantObjective E q → FullSupportWeight E q →` odd-degree and preceding even-degree optima agree | Complement-symmetry theorem: with complement-invariant variance and objective, cubic terms cannot improve the quadratic optimum; if every observed set has size at most three, quadratic already equals the unrestricted observable optimum. |
| `CausalSmith.Experimentation.BinaryTruthbound.witness_quartic_separation` | `optValue witnessExperiment witnessObjective ⊤ = 107 / 72 ∧ optValue … 2 = optValue … 3 = 55 / 36 ∧` gap `= 1 / 24`, with primal, dual, and implementation certificates | Exact quartic-separation theorem: the certified six-coordinate design has a nonnegative quartic observable bound that strictly improves every observable quadratic and cubic bound by `1/24`. |
| `CausalSmith.Experimentation.BinaryTruthbound.asBinaryCorrection_mem_conservativeCone_two` | `(E : Setup) →` positive singleton observation probabilities and binary HT scores `→` the Aronow--Samii correction is in `conservativeCone E 2`, with its excess identity and the witness separation | Aronow--Samii specialization: the binary zero-pairwise-inclusion correction is a feasible Boolean quadratic conservative bound, while the witness shows the larger all-observable class is strictly better in general. |
| `CausalSmith.Experimentation.BinaryTruthbound.optValue_product_tensorization` | `(B : ℕ) → (comp : Fin B → Setup) → (a : Fin B → ℝ) → (q : ∀ j, FullSupportObjective (comp j)) →` product optimum equals `∑ j, (a j)^2 * optValue (comp j) (q j) r` | Tensorization theorem: independent product experiments preserve the exact full and degree-restricted optimum decomposition despite allowing cross-block observable interactions; identical witness copies retain a `1/(24B)` gap. |
| `CausalSmith.Experimentation.BinaryTruthbound.studentized_wald_coverage` | `(Arr : BlockArray) → (M V α zAlpha : ℝ) →` gated bounded-score CLT, denominator weak law, conservativeness, asymptotic coverage, and feasible studentization conclusions | Studentized-inference theorem: under the stated independent-block, boundedness, nondegeneracy, denominator-LLN, and conservativeness conditions, the Wald interval is asymptotically conservative and has the stated limiting coverage. |
| `CausalSmith.Experimentation.BinaryTruthbound.witness_hms_sdp_optimum` | `sInf (witnessHMSObjective '' {H | witnessHMSFeasible H}) = 7 / 6 + Real.sqrt 17 / 9`, with attainment and exact differences from `107/72` and `55/36` | Direct HMS-program certificate: the witness's compatible semidefinite quadratic program has an attained exact irrational optimum, explicitly exceeding both the all-observable quartic and Boolean quadratic/cubic optima. |

### 3.24 Dense Kneser projection phase transition and CR2 frontier

Experimentation/Theorems paper-scoped entries under `CausalSmith/Experimentation/EXP_DenseGroupPartitionProjectionPhase_Research/` (`exp_dense_group_partition_projection_phase`, v1). For completely randomized equal-size groups drawn without replacement, the paper diagonalizes disjoint-group covariance through the Kneser/Johnson decomposition. It derives exact PAME and CR2 variance identities, isolates the degree-one dense-sampling correction, characterizes the CR2 consistency frontier including a sparse regime beyond birthday coupling, and gives finite and one-realization impossibility certificates.

| Declaration | Signature | Description |
|---|---|---|
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.exact_kneser_identity` | `(n M) → 2 ≤ M → 2M ≤ n → Johnson projections and Kneser-spectrum hypotheses → eigen-action on every Johnson component, disjoint-pair covariance decomposition, and contrast covariance identity` | Exact Kneser identity: diagonalizes the ordered-disjoint-pair covariance operator and expresses the treatment-contrast covariance as signed Johnson projection energies. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.exact_pame_variance` | `(n M G G1) → MG ≤ n → 2 ≤ M → 2 ≤ G1 → 2 ≤ G−G1 → PotentialOutcome n M → unbiased PAME, exact variance, CR2 expectation, and CR2-bias identities` | Exact PAME variance theorem: separates independent-group variance from disjoint-group covariance and proves that expected CR2 minus the exact design variance is minus the contrast cross-covariance. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.dense_projection_limit` | `2 ≤ M → (p rho B) → ∃ C>0, ∀ schedule arrays and Johnson projections, growth/fraction/boundedness and spectral hypotheses → dense variance and covariance limits plus a higher-degree bound` | Dense projection limit: at the group-count scale, only the degree-one Johnson energy survives, while higher degrees and arm-specific cross-covariance terms vanish uniformly for bounded schedules. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.cr2_phase_frontier` | `2 ≤ M → DenseScheduleClass A p rho B cSigma → Johnson/Kneser hypotheses → CR2 centered and variance-gap convergence, conservativeness, and a ratio-consistency equivalence` | CR2 phase-frontier theorem: identifies the degree-one correction governing dense-regime conservativeness and gives the necessary-and-sufficient condition for CR2 ratio consistency. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.sparse_beyond_birthday` | `2 ≤ M → (dense schedules at rho = 0 are CR2 ratio-consistent) ∧ BirthdayBenchmark M ∧ (aligned sparse schedules are CR2 ratio-consistent)` | Sparse-beyond-birthday theorem: establishes CR2 ratio consistency from a vanishing sampling fraction, including the `n^(3/4)` group-count benchmark where whole-design birthday coupling fails. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.clubsandwich_cr2_consumer_identity` | `equal-group binary lm fit and pinned clubSandwich/sandwich contracts → Sigma 1 1 = cr2Var Y ...` | clubSandwich consumer identity: certifies that the treatment-coordinate CR2 output for the specified equal-group binary regression is exactly the paper's group-level CR2 variance statistic. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.eight_unit_witness_moments` | `Johnson projections on the 8-unit, size-2 witness → arm variance = 3/7 ∧ degree-one energy = 3/7 ∧ cross-covariance = −1/7 ∧ variance = 1/7 ∧ expected CR2 = 2/7` | Eight-unit witness: verifies the finite balanced design's exact variance and expected-CR2 discrepancy, providing a concrete regression check for the dense correction. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.rademacher_mixture_separation` | `2 ≤ M → positive sampling fraction → schedule growth/fraction and Johnson/Kneser hypotheses → equal observed expectations, separated prior limits, and scaled-variance nondegeneracy` | Rademacher-mixture separation: constructs observationally indistinguishable same and independent priors whose degree-one energies and scaled variances have a positive dense-regime limit gap. |
| `CausalSmith.Experimentation.DenseGroupPartitionProjectionPhase.qv_diagonal_impossibility` | `2 ≤ M → positive density and bounded schedule assumptions → QVDiagonalCertificate ... ∧ ∃ epsilon>0, every estimator has liminf worst-case ratio error at least 1/2` | One-realization impossibility theorem: uses high-probability diagonal supports of the Rademacher mixtures to rule out uniform exact-variance ratio consistency over the full positive-density class. |
