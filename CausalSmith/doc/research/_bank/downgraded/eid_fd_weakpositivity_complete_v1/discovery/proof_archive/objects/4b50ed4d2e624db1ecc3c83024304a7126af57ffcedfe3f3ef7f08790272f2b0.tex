The proved local spine gives the following exact partial result.  Feasibility is decided by \(F\) functional-map branches in \(O(FJ_T(d+m)h+FR)\) table operations, with a rational original-alphabet witness or a finite polynomial contradiction.  There are at most \(2^K\) support strata, and all observational and interventional cell-positivity guards on a fixed stratum reduce to finite reachability.  Any target-relevant zero-probability parent row yields an original-alphabet separating pair; once any separating pair exists, real-closed-field transfer yields an exactly encodable real-algebraic pair.  Every successful classical \(\mathsf{ID}\) output embeds as a guarded rational certificate under strict observational positivity.  These results do not decide the residual positive-support algebraic strata on which classical \(\mathsf{ID}\) fails and no support-hole row is available.