Start from the exact functional-map feasibility enumerator. On feasible inputs, enumerate deterministic functional-map and row-support strata, discard strata that violate joint-treatment reachability, and compile their induced observable supports into rational-coefficient sign guards in \(p\). Install a success leaf when the one-way classical-\(\mathsf{ID}\) proposition applies or when exact coefficient comparison proves, on every latent stratum compatible with the observable guard, a complete collection of conditional-event-ratio identities for \(q\); license each denominator by the entailed positive observed-event guard. On unresolved strata, enumerate observationally unreachable parent rows and test the intervention-row endpoint-difference polynomials exactly; a nonzero polynomial is evaluated on the fixed rational interpolation grid to return an original-alphabet rational separating pair. If no such pair is found, fix every proved target-inert unreachable row canonically and return the remaining doubled existential semialgebraic fiber formula, together with any already-certified success leaves, without an identification verdict. Generic real quantifier elimination is not used to decide the residual formula or to search for identification; it may be used only to verify an independently supplied exact certificate.