# eid_robust_backshift_uniform_distance — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (A population BACKSHIFT multi-environment covariance system).** A population BACKSHIFT multi-environment covariance system. Fix a dimension \(p\in\{2,3,\ldots\}\) and a finite label set \(E=\{1,\ldots,m\}\) of observed environment slots, a corruption budget \(c\in\{0,\ldots,m-1\}\), and \(h=m-c\). Write \(\mathbb S_{+}^{p}\) and \(\mathbb S_{++}^{p}\) for the real symmetric positive-semidefinite and positive-definite \(p\times p\) cones. An observed input is a family \(\Sigma=(\Sigma_e:e\in E)\in(\mathbb S_{+}^{p})^{E}\). A structural explanation consists of an invertible demixing matrix \(D\in\mathrm{GL}_p(\mathbb R)\) with connectivity target \(B=I_p-D\), an invariant disturbance covariance \(\Omega\in\mathbb S^{p}\) that is permitted to be nondiagonal and singular, and nonnegative diagonal intervention-variance levels \(s=(s_{ek})\) on an unknown honest set \(H\subseteq E\) with \(\lvert H\rvert=h\), linked by the equilibrium covariance equation \(\Sigma_e=D^{-1}(\Omega+\operatorname{diag}(s_e))D^{-\mathsf T}\) for \(e\in H\); the remaining \(c\) slots of \(E\setminus H\) may carry arbitrary elements of \(\mathbb S_{+}^{p}\). Admissibility is the BACKSHIFT normalization \(\mathcal D=\{A\in\mathrm{GL}_p(\mathbb R):\operatorname{diag}(A)=\mathbf 1,\ \operatorname{CP}(I_p-A)<1\}\), where \(\operatorname{CP}\) is the maximum absolute entry product along a directed simple cycle of length at least two. Two competing explanations \(D^{(1)},H_1,s^{(1)}\) and \(D^{(2)},H_2,s^{(2)}\) with \(\lvert H_r\rvert\ge h\) may share only the overlap \(J=H_1\cap H_2\), of size at least \(m-2c=h-c\). The geometry of a shift design is measured by affine lines \(L\subset\mathbb R^2\): the pairwise occupancy \(M_{k\ell}=\max_L\#\{e\in H:(s_{ek},s_{e\ell})\in L\}\), the deletion distance \(\delta_H=\min_{k<\ell}(h-M_{k\ell})\), and the affine-minor separation \(\gamma_H\). The recovery map is the \(c\)-replacement compatibility set \(\mathcal R_c(\Sigma)\subseteq\mathcal D\), with its subset-enumeration presentation \(\text{SubsetConsensus}\). Conditioning margins are \(\kappa_D=\|D\|_{\mathrm{op}}\|D^{-1}\|_{\mathrm{op}}\), \(\zeta_D=1-\operatorname{CP}(I_p-D)\), and the scale bound \(M_H=1+\|\Omega\|_{\mathrm{op}}+\max_{e\in H}\|\Sigma_e\|_{\mathrm{op}}+\max_{e\in H}\|\operatorname{diag}(s_e)\|_{\mathrm{op}}\); no interiority margin is imposed on the covariances, which are only required to be positive semidefinite. The size of a set-valued structural output is measured by \(\operatorname{diam}_{\mathrm{op}}\) and \(\operatorname{outer}_{\mathrm{op}}\), both valued in \([0,+\infty]\). — bypass-justified (ABSTRACTION MISMATCH against the whole declared cluster substrate root). The paper's ambient world carries no graph, no d-separation, no do-operator, no potential outcome, and no probability space at the population layer: an environment is a covariance matrix, an intervention is an unobserved diagonal variance shift, and identification is congruence-diagonalizer uniqueness under a normalization. Surveyed and rejected, one line each: `Causalean/Graph/SWIG.lean` + `Causalean/SCM/ID/GraphicalThms/*` (SWIG/d-separation graphical criteria; the paper never states a graphical condition and the target is a cyclic system, which `Graph/DAG.lean` forbids); `Causalean/SCM/ID/Identifiable.lean` and `Overlap.lean` (identifiability of an interventional kernel from an observational kernel by do-calculus; here the observable is a second-moment family and the target is a matrix, so `Identifiable`'s kernel-equality shape does not type-fit); `Causalean/SCM/Do/*` (do-calculus rules on measure kernels — no consumer); `Causalean/PO/**` and `Causalean/PO/ID/Exact/*` (`POSystem`, `Adjustment`, `Backdoor.*`, `Frontdoor.*`, `ATE.*`, `DID.*`, `DTR.*`, `LATE.*`, `*.Assumptions` — all are potential-outcome estimand identification under overlap/unconfoundedness; the paper has no treatment variable, no propensity, and no estimand of the form \(E[Y(a)]\)). What IS reused, and it is the highest-leverage reuse in this plan, is Causalean's linear-causal-discovery matrix layer, which sits at exactly this abstraction: `Causalean.Mathlib.LinearAlgebra.genPerm_of_det_ne_zero_of_colSupport` (Causalean/Mathlib/LinearAlgebra/MonomialMatrix.lean:37) is verbatim the monomial-extraction step of `lem:overlap-uniqueness` ('invertibility then makes \(Q\) monomial'), and `Causalean.Mathlib.LinearAlgebra.perm_uniqueness` / `eq_of_genPerm_triangular_unitDiag` (:104,:132) are the unit-diagonal pinning template that the cycle-product argument replaces. Search trace: `grep '^## '` over ../doc/API.md then reads of §9 (Identifiable), §9a (Overlap), §9d/§9e/§9e' (Adjustment/Backdoor), §8h (PO), §17 (LiNGAM), §17b (LinearDisentanglement), §17c (InvariantPrediction), §10j/§10j' (Cholesky, Vandermonde), §10j (OperatorSqrt); `lean_local_search` (project_root=CausalSmith) on `PosSemidef`, `l2_opNorm`, `Collinear`, `cycleFactorsFinset`, `det_eq_prod_eigenvalues`, `powersetCard`; `grep -rn 'genPerm|lingam_reduction|perm_uniqueness|cholesky_unique' Causalean/` under the sibling base `../Causalean/` (NOT a `Causalean/…` glob from the CausalSmith cwd). Result: Causalean has no multi-environment covariance model, no simultaneous-diagonalization-by-congruence predicate, and no BACKSHIFT normalization; `Discovery/LinearDisentanglement/Model.lean` is the nearest neighbour (per-context precision matrices \(\Theta_k=H^\mathsf T B_k^\mathsf T B_kH\) under single-node perfect interventions) but its world fixes a full-row-rank mixing, upper-triangular positive-diagonal \(B_0\), and an acyclic latent DAG, all of which this cyclic unit-diagonal cycle-product world denies. F2 therefore emits a paper-local record `BackshiftSystem` over `Matrix (Fin p) (Fin p) ℝ`, following the established per-paper record pattern, and imports only the Mathlib matrix/analysis layer plus `MonomialMatrix`. REVISED (core revision): the symbol `lambda` (\(\lambda_{\mathrm{cov},H}=\min_{e\in H}\lambda_{\min}(\Sigma_e)\)) is DROPPED from `binds_symbols` and from the world text — the revised core removes the covariance-interiority margin entirely (the contraction theorem now imposes four true-side margins \(\gamma_H,\zeta_D,\kappa_D,M_H\) and only a condition-number bound on feasible candidates, with covariances merely positive semidefinite). No other S1 binding, module, or reuse decision moves: the surveyed-and-rejected cluster substrate and the `MonomialMatrix` reuse are unchanged, since the population layer of the core (records, admissibility, compatibility, occupancy, separation) is untouched by the revision. REVISED AGAIN (F1.5 review, FLAG-missed-reuse on `def:operator-diameter`): the symbol `diamOp` bound here is now realized by REUSED Mathlib `Metric.ediam` rather than by a local functional, so `Mathlib.Topology.EMetricSpace.Diam` joins this block's module list and the operator-norm metric structure on `Matrix (Fin p) (Fin p) ℝ` is taken from the scope `Matrix.Norms.L2Operator` (`Matrix.instL2OpMetricSpace`, `Mathlib/Analysis/CStarAlgebra/Matrix.lean:158,172`) — the same single `open scoped` this development already needs for `‖·‖`. The world text is unchanged (\(\operatorname{diam}_{\mathrm{op}}\) is still the \([0,+\infty]\)-valued operator-norm diameter; only its Lean realization moved from define-local to reuse), and `outerOp` remains a local one-sided functional because Mathlib has no sup-of-distances-to-a-point decl.
**required modules.** Mathlib.LinearAlgebra.Matrix.NonsingularInverse, Mathlib.LinearAlgebra.Matrix.PosDef, Mathlib.Analysis.Matrix.Order, Mathlib.Analysis.Matrix.Spectrum, Mathlib.Analysis.CStarAlgebra.Matrix, Mathlib.GroupTheory.Perm.Cycle.Factors, Mathlib.Data.Finset.Powerset, Mathlib.LinearAlgebra.AffineSpace.FiniteDimensional, Mathlib.Topology.EMetricSpace.Diam, Causalean.Mathlib.LinearAlgebra.MonomialMatrix

**S-2 (A prespecified sparse intervention-design genericity world).** A prespecified sparse intervention-design genericity world. On a probability space carrying the shift design, a deterministic binary support pattern \(Z\in\{0,1\}^{H\times p}\) is fixed before the experiment, and random positive amplitudes \(a\in(0,\infty)^{H\times p}\) produce the realized shift levels \(s_{ek}=Z_{ek}a_{ek}\). The joint law of the active subarray \((a_{ek}:Z_{ek}=1)\) is absolutely continuous with respect to Lebesgue measure on its positive orthant; nothing is assumed about the inactive coordinates, and the amplitudes may be dependent. For each coordinate pair \(k<\ell\) the pattern determines the incidence counts \(n_{uv}^{k\ell}=\#\{e\in H:(Z_{ek},Z_{e\ell})=(u,v)\}\) and the closed-form generic occupancy \(M_{k\ell}^{\mathrm{gen}}=\max\{n_{00}^{k\ell}+n_{10}^{k\ell},\ n_{00}^{k\ell}+n_{01}^{k\ell},\ n_{00}^{k\ell}+\mathbf 1\{n_{11}^{k\ell}>0\},\ \min(2,h)\}\). Points of type \(00\) coincide at the origin and are counted with their full multiplicity as indexed observations, so this is an incidence statement about an indexed family, not about a set of distinct points. — bypass-justified: this is a bare Lebesgue-genericity world, not a causal sampling world. Surveyed `Causalean/Stat/Sample.lean` (`IIDSample`), `Causalean/Stat/EmpiricalProcess/*`, `Causalean/Stat/Concentration/*` via `grep '^## '` of ../doc/API.md §12, §12ep, §12' and reads of the section tables: every Causalean sampling primitive is an i.i.d.-observation layer with empirical means and concentration, whereas this world has no observations and no estimator — it only needs `MeasureTheory.Measure.AbsolutelyContinuous` of one fixed law against the volume measure on a positive orthant and a null-set union over finitely many polynomial equations. Mathlib-side check for the one genuinely mathematical ingredient (`lean_leansearch` "zero set of a nonzero multivariate polynomial has measure zero" and "nonzero polynomial vanishes on a set of Lebesgue measure zero in R^n"): Mathlib supplies the univariate root finiteness `Polynomial.finite_setOf_isRoot` (Mathlib.Algebra.Polynomial.Roots) and `MvPolynomial.support_nonempty` / `zeroLocus` algebra, but NO statement that the real zero set of a nonzero `MvPolynomial (Fin n) ℝ` is Lebesgue-null; Causalean-side check `grep -rln 'MvPolynomial' ../Causalean/` returns only `Causalean/Mathlib/AlgebraicGeometry/PolynomialImageDimension/*` and `Causalean/Mathlib/Analysis/MonomialGram.lean`, and `grep -n 'measure zero|Lebesgue|volume|addHaar'` over that directory returns nothing — that development is Zariski/dimension-theoretic and carries no measure. The gap is therefore a build-inline helper (`Helpers/PolynomialNull.lean`), proved by the induction the note's own proof spells out: expand in the last variable, apply the inductive hypothesis to the coefficient polynomials, and integrate the finite-root sections via `MeasureTheory.Measure.pi` / Fubini.
**required modules.** Mathlib.MeasureTheory.Measure.MeasureSpace, Mathlib.MeasureTheory.Measure.Lebesgue.Basic, Mathlib.MeasureTheory.Constructions.Pi, Mathlib.Algebra.MvPolynomial.Basic, Mathlib.Algebra.Polynomial.Roots

**S-3 (A set-theoretic finite-sample inference world layered on…).** A set-theoretic finite-sample inference world layered on the population system. Fix a sample-size index \(n\geq1\) and a nominal noncoverage level \(\alpha\in(0,1)\). For each slot \(e\in E\) the data supply an ARBITRARY nonempty simultaneous covariance region \(\mathcal C^{(n)}_{e,\alpha}\subseteq\mathbb S_{+}^{p}\); no representation, encoding, membership oracle, or computability property of a region is assumed, and no sampling mechanism producing it is specified. The honest-slot error radius is \(r_{n,\alpha,H}=\max_{e\in H}\sup_{\Gamma\in\mathcal C^{(n)}_{e,\alpha}}\|\Gamma-\Sigma_e\|_{\mathrm{op}}\in[0,+\infty]\), with the value \(+\infty\) permitted, and the identification-robust output is the confidence union \(\mathcal U^{(n)}_{\alpha}=\bigcup\{\mathcal R_c(\Gamma):\Gamma_e\in\mathcal C^{(n)}_{e,\alpha}\ \forall e\in E\}\), the union of the population replacement-compatibility sets over all simultaneous selections. The coverage event is \(G_{n,\alpha,H}=\{\Sigma_e\in\mathcal C^{(n)}_{e,\alpha}\ \forall e\in H\}\). Quantitative statements are made against four fixed rational margins imposed on the TRUE instance — \(\gamma_H\geq\gamma_0\in\mathbb Q_{>0}\), \(\zeta_D\geq\zeta_0\in\mathbb Q\cap(0,1]\), \(\kappa_D\leq\overline\kappa\in\mathbb Q\cap[1,\infty)\), \(M_H\leq\overline M\in\mathbb Q\cap[1,\infty)\) — together with a single declarative condition-number bound \(\|A\|_{\mathrm{op}}\|A^{-1}\|_{\mathrm{op}}\leq\overline\kappa\) on every feasible candidate. No covariance-interiority, candidate affine-separation, candidate normalization-slack, or candidate scale condition is imposed, and the local radius \(r_0\) is a non-effective compactness quantity: its existence is asserted, no procedure evaluates it, and no executable, algorithmic, or software interface is part of this world. — REVISED (core revision, whole-block rewrite): the previous S3 was an ENCODED-REGION world binding `RegionCode`, `lambda0`, `CertificateInferenceHandle` and `AuditedReleaseContraction`, and carrying a rational-semialgebraic syntax layer with a release/abstain certificate handle. The revised core deletes all four of those symbols and the entire symbolic layer: regions are now ARBITRARY nonempty subsets of \(\mathbb S_+^p\), the covariance-interiority margin is gone, and every executable/effective/decision/chart/software item is moved into the unresolved `oeq:certified-uniform-inference`. The whole `Mathlib.Algebra.MvPolynomial.Basic` / `Mathlib.Data.Rat.Defs`-syntax module list, the `RationalSignFormula`/`FOFormula` plan, and the sibling-CAD analogous-module record are therefore DROPPED (they are recorded in `file_plan.bypass_survey` for provenance only). What remains is a plain `Set (Matrix (Fin p) (Fin p) ℝ)`-indexed-family world over `ℝ≥0∞`-valued radii plus four rational margin parameters. bypass-justified (ABSTRACTION MISMATCH, re-surveyed for the revised world): Causalean's set-valued identification substrate is `Causalean/PO/ID/Partial/**` — `IdentifiedInterval`, support functions of random convex sets, and Hausdorff-distance vocabulary — which lives at the real-interval / convex-body abstraction over a potential-outcome estimand, whereas this world's uncertainty object is a family of arbitrary subsets of the PSD matrix cone and its output is a union of matrix solution sets. Search trace for the revised world: `grep '^## '` over ../doc/API.md then reads of §9j2/§9j2sf (`PO/ID/Partial/` abstract bounds engine, random sets) and §12/§12ep (`Stat/Sample.lean`, `Stat/EmpiricalProcess/*` — i.i.d. observation layers with empirical means, no set-valued covariance region); `lean_local_search` (project_root=CausalSmith) on `l2_opNorm` (→ `Matrix.l2_opNorm_def`, `l2_opNorm_mul`, `l2_opNorm_mulVec`, `l2_opNorm_conjTranspose`, scoped `Matrix.Norms.L2Operator` — REVISED (F1.5): the scope name recorded here earlier, `Matrix.L2OpNorm`, is STALE and does not exist in this tree; `Mathlib/Analysis/CStarAlgebra/Matrix.lean:172,183,260,269` puts `Matrix.instL2OpMetricSpace`, `Matrix.instL2OpNormedAddCommGroup`, `instL2OpNormedSpace` and `instL2OpNormedRing` under `scoped[Matrix.Norms.L2Operator]`, and F2 must open that name or nothing resolves; REUSED wholesale as this world's operator norm AND — since the same scope supplies the `MetricSpace`, hence `EMetricSpace`, structure — as the ambient metric for the REUSED `Metric.ediam` that now realizes \(\operatorname{diam}_{\mathrm{op}}\), with `Metric.ediam_le` discharging the contraction theorem's diameter conclusion), on `IsCompact.exists_isMinOn` (→ `IsCompact.exists_isMinOn`, `IsCompact.exists_isMinOn_mem_subset`, Mathlib.Topology.Order.Compact) and on `isCompact_of_isClosed_isBounded` (→ `Metric.isCompact_of_isClosed_isBounded`, Mathlib.Topology.MetricSpace.Bounded) — both confirmed present and named in the two cited-gate nodes' discharge notes; `grep -rn 'diam|opNorm' ../Causalean/` under the sibling base returns only `PO/ID/Partial/RandomSet/` Hausdorff vocabulary and `Mathlib/OperatorSqrt.lean` at the `E →ₗ[ℝ] E` abstraction. No confidence-union or set-valued matrix-compatibility primitive exists in Causalean or Mathlib.
**required modules.** Mathlib.Data.ENNReal.Basic, Mathlib.Data.Rat.Defs, Mathlib.Analysis.SpecialFunctions.Pow.Real, Mathlib.Analysis.CStarAlgebra.Matrix, Mathlib.Topology.Order.Compact, Mathlib.Topology.EMetricSpace.Diam, Mathlib.Data.Finset.Powerset

## Assumptions (A)

**A-1 (assumption).** \(D\in\mathcal D=\{A\in\mathrm{GL}_p(\mathbb R):\operatorname{diag}(A)=\mathbf 1,\ \operatorname{CP}(I_p-A)<1\}\).

**A-2 (assumption).** \(s_{ek}\ge0\) for every \(e\in H\) and \(1\le k\le p\).

**A-3 (assumption).** \(\Sigma_e=D^{-1}(\Omega+\operatorname{diag}(s_e))D^{-\mathsf T}\) for every \(e\in H\).

**A-4 (assumption).** \(s_{ek}=Z_{ek}a_{ek}\) for every \(e\in H\) and \(1\le k\le p\).

**A-5 (assumption).** The joint law of \((a_{ek}:e\in H,1\le k\le p,Z_{ek}=1)\) is absolutely continuous with respect to Lebesgue measure on its positive orthant.

## Definitions (P)

**P-1 (…).** \(\mathcal D=\{A\in\mathrm{GL}_p(\mathbb R):\operatorname{diag}(A)=\mathbf 1,\ \operatorname{CP}(I_p-A)<1\}\).

**P-2 (For \(1\le k<\ell\le p\),…).** For \(1\le k<\ell\le p\), \(M_{k\ell}=\max_{L\subset\mathbb R^2\,\mathrm{affine\ line}}\#\{e\in H:(s_{ek},s_{e\ell})\in L\}\).

**P-3 (\(\delta_H=\min_{1\le k<\ell\le p}(h-M_{k\ell})\).).** \(\delta_H=\min_{1\le k<\ell\le p}(h-M_{k\ell})\).

**P-4 (For \(\Sigma\in(\mathbb S_{+}^{p})^{E}\),…).** For \(\Sigma\in(\mathbb S_{+}^{p})^{E}\), \(\mathcal R_c(\Sigma)=\{A\in\mathcal D:\exists K\subseteq E,\ |K|\ge h,\ \exists\Psi\in\mathbb S_{+}^p,\ \exists t_e\in[0,\infty)^p,\ \Sigma_e=A^{-1}(\Psi+\operatorname{diag}(t_e))A^{-\mathsf T}\ \forall e\in K\}\).

**P-5 (For each \(k<\ell\),…).** For each \(k<\ell\), \(M_{k\ell}^{\mathrm{gen}}=\max\{n_{00}^{k\ell}+n_{10}^{k\ell},n_{00}^{k\ell}+n_{01}^{k\ell},n_{00}^{k\ell}+\mathbf 1\{n_{11}^{k\ell}>0\},\min(2,h)\}\).

**P-6 (Set \(\gamma_H=0\) when \(h-c<3\)).** Set \(\gamma_H=0\) when \(h-c<3\); otherwise set \(\gamma_H=\min_{1\le k<\ell\le p}\min_{S\subseteq H,\,|S|=h-c}\max_{e<f<g\in S}\left|\det\begin{pmatrix}1&s_{ek}&s_{e\ell}\\1&s_{fk}&s_{f\ell}\\1&s_{gk}&s_{g\ell}\end{pmatrix}\right|\).

**P-7 (Definitional implementation note).** Definitional implementation note: given a feasibility oracle that returns the full solution set for each queried semialgebraic system, query every \(K\subseteq E\) with \(|K|=h\) for all \(A\in\mathcal D\) satisfying \(\operatorname{offdiag}(A(\Sigma_e-\Sigma_f)A^{\mathsf T})=0\) for \(e,f\in K\) and admitting one \(\Psi\in\mathbb S_{+}^p\) with nonnegative diagonal shifts for the transformed raw covariances. Define \(\text{SubsetConsensus}(\Sigma,c)\) as the union of the oracle-returned solution sets. This enumeration makes exactly \(\binom{m}{h}\) oracle queries and, by expansion of \(\text{def:compatible-set}\), its returned union equals \(\mathcal R_c(\Sigma)\); neither fact is a theorem contribution or a claim that an encoded terminating solver is supplied.

**P-8 (…).** \(r_{n,\alpha,H}=\max_{e\in H}\sup_{\Gamma\in\mathcal C^{(n)}_{e,\alpha}}\|\Gamma-\Sigma_e\|_{\mathrm{op}}\in[0,+\infty]\), where the supremum and hence the maximum may equal \(+\infty\).

**P-9 (For every nonempty matrix set \(\mathcal A\),…).** For every nonempty matrix set \(\mathcal A\), \(\operatorname{diam}_{\mathrm{op}}(\mathcal A)=\sup_{A_1,A_2\in\mathcal A}\|A_1-A_2\|_{\mathrm{op}}\in[0,+\infty]\).
**reuse.** Metric.ediam

**P-10 (For every nonempty matrix set \(\mathcal A\) and matrix \…).** For every nonempty matrix set \(\mathcal A\) and matrix \(D\), \(\operatorname{outer}_{\mathrm{op}}(\mathcal A,D)=\sup_{A_1\in\mathcal A}\|A_1-D\|_{\mathrm{op}}\in[0,+\infty]\).

**P-11 (For every \(n\geq1\), \(\alpha\in(0,1)\), and indexed fam…).** For every \(n\geq1\), \(\alpha\in(0,1)\), and indexed family of nonempty sets \(\mathcal C^{(n)}_{e,\alpha}\subseteq\mathbb S_{+}^{p}\), define the set-theoretic confidence union \[ \mathcal U^{(n)}_{\alpha} =\bigcup_{\Gamma\in\prod_{e\in E}\mathcal C^{(n)}_{e,\alpha}} \mathcal R_c(\Gamma). \] Thus \(\mathcal U^{(n)}_{\alpha}\) is the union of the population replacement-compatibility sets over all simultaneous selections from the covariance regions. This definition asserts neither computability nor a coverage property.

**P-12 (The set-theoretic simultaneous-coverage implication for a…).** The set-theoretic simultaneous-coverage implication for arbitrary nonempty positive-semidefinite covariance regions is established by \(\text{thm:confidence-union-coverage}\). The result \(\text{thm:non-effective-four-margin-contraction}\) establishes a non-effective existential local radius and an explicit linear modulus under four bounds on the true instance---affine separation, normalization slack, structural conditioning, and scale---and only a condition-number bound on every feasible candidate. Can one additionally build and formally verify an executable real-closed-field quantifier-elimination, sign-determination, and root-isolation substrate which, from finite rational-semialgebraic covariance-region codes, (i) terminates and emits a quantifier-free code whose realization is exactly \(\mathcal U^{(n)}_\alpha\), (ii) decides a release-or-abstain condition, possibly using a conservative universal four-margin audit to certify unknown true-side margins and forcing abstention within that audit whenever it finds a feasible candidate with zero affine separation, while distinguishing that operational sufficient audit from the sole candidate-side condition-number bound required by the analytic contraction theorem, (iii) decides for every rational population covariance family whether \(\mathcal R_c(\Sigma)\) is empty, is a singleton, or contains at least two matrices, and (iv) computes a certified positive lower bound for the compact far-family residual and hence an effective positive radius \(r_0\)? Can the same verified program automate normalization-chart changes without altering the represented matrix set and support a corruption-aware extension of the CRAN \(\texttt{backShift}\) workflow? Every executable, effective, decision, chart-automation, and software item in this question is unresolved future work. A feasible candidate with zero affine separation or a small normalization slack may trigger that conservative operational audit, but neither is an analytic candidate-side obstruction to contraction around a fixed true instance satisfying \(\gamma_H\geq\gamma_0>0\) and \(\zeta_D\geq\zeta_0>0\).

## Lemmas (L)

**L-1 (Let the first explanation \(D^{(1)},H_1,s^{(1)}\) and the…).** Let the first explanation \(D^{(1)},H_1,s^{(1)}\) and the second explanation \(D^{(2)},H_2,s^{(2)}\) fit the same \(\Sigma\), each through a BACKSHIFT covariance equation satisfying the unit-diagonal and cycle-product normalization, with its own invariant positive-semidefinite nuisance, nonnegative shifts, and \(|H_r|\ge h=m-c\). Set \(J=H_1\cap H_2\). Then \(|J|\ge m-2c=h-c\). If, for every \(k<\ell\), the first explanation's cloud \(\{(s^{(1)}_{ek},s^{(1)}_{e\ell}):e\in J\}\) is not contained in an affine line, then \(D^{(1)}=D^{(2)}\).

**L-2 (Let \(I\) be a finite index set of cardinality \(r\), let…).** Let \(I\) be a finite index set of cardinality \(r\), let \(Z^\circ\in\{0,1\}^{I\times p}\), and let \(a^\circ\in(0,\infty)^{I\times p}\). Suppose the subarray \((a^\circ_{ek}:Z^\circ_{ek}=1)\) has a joint distribution absolutely continuous on its positive orthant. Define \(P_e^{k\ell}=(Z^\circ_{ek}a^\circ_{ek},Z^\circ_{e\ell}a^\circ_{e\ell})\), \(N_{uv}^{k\ell}=\#\{e\in I:(Z^\circ_{ek},Z^\circ_{e\ell})=(u,v)\}\), and \(M_I^{k\ell}=\max_{L\text{ affine line}}\#\{e\in I:P_e^{k\ell}\in L\}\). Then, simultaneously for all \(k<\ell\), almost surely \[ M_I^{k\ell}=\max\{N_{00}^{k\ell}+N_{10}^{k\ell},N_{00}^{k\ell}+N_{01}^{k\ell},N_{00}^{k\ell}+\mathbf 1\{N_{11}^{k\ell}>0\},\min(2,r)\}. \]

## Theorems (T)

### T-block: t1 — Fix an honest BACKSHIFT instance \(D,H,s,\Omega\) with…
**Statement.** Fix an honest BACKSHIFT instance \(D,H,s,\Omega\) with \(D\in\mathcal D\), nonnegative shifts, \(\Omega\in\mathbb S_{+}^{p}\), and \(\Sigma_e=D^{-1}(\Omega+\operatorname{diag}(s_e))D^{-\mathsf T}\) for every \(e\in H\). If \(\delta_H>c\), equivalently \(\gamma_H>0\), then \(\mathcal R_c(\Sigma)=\{D\}\) for every assignment of matrices in \(\mathbb S_{+}^{p}\) to the \(c\) slots in \(E\setminus H\). If, in addition, this fixed instance has \(\Omega\in\mathbb S_{++}^{p}\) and \(\delta_H\le c\), then there are positive-definite values for all slots in \(E\setminus H\), a matrix \(D'\in\mathcal D\) with \(D'\ne D\), an invariant \(\Omega'\in\mathbb S_{++}^{p}\), and nonnegative shifts on a set \(H'\subseteq E\) with \(|H'|=h\) such that \(D,D'\in\mathcal R_c(\Sigma)\). Consequently, over the published class allowing \(\Omega\in\mathbb S_{+}^{p}\) and positive-semidefinite covariance slots, the threshold \(\delta_H>c\) is minimax sharp: whenever \(\delta_H\le c\), the class contains an ambiguity witness in its positive-definite invariant-noise and covariance subclass. No ambiguity is asserted for every fixed singular boundary value of \(\Omega\). On the positive-definite invariant-noise interior and at \(c=0\), the equivalence is exactly the BACKSHIFT pairwise noncollinearity criterion.

### T-block: t2 — Under the prespecified support pattern \(Z\) and absolute…
**Statement.** Under the prespecified support pattern \(Z\) and absolutely continuous active amplitudes, simultaneously for every \(k<\ell\), \(M_{k\ell}=M_{k\ell}^{\mathrm{gen}}\) almost surely. Consequently, the pattern guarantees \(\mathcal R_c(\Sigma)=\{D\}\) uniformly for all admissible \(D\in\mathcal D\), all \(\Omega\in\mathbb S_{+}^{p}\), and every positive-semidefinite assignment to the \(c\) replacement slots if and only if, for every \(k<\ell\), \(\max\{n_{00}^{k\ell}+n_{10}^{k\ell},n_{00}^{k\ell}+n_{01}^{k\ell},n_{00}^{k\ell}+\mathbf 1\{n_{11}^{k\ell}>0\},\min(2,h)\}<h-c\). Failure of this inequality has an ambiguity witness with positive-definite invariant noise and positive-definite covariance slots; it is not a claim that every fixed singular boundary instance is ambiguous. In particular, any uniformly robustly identifying support pattern satisfies \(m\ge2c+3\), and this environment-count lower bound is attained by support patterns with no larger forced affine incidence.

### T-block: t3 — Let \(\bar Z\in\{0,1\}^{E\times p}\) be a planned full-en…
**Statement.** Let \(\bar Z\in\{0,1\}^{E\times p}\) be a planned full-environment support array and let \(\bar a\in(0,\infty)^{E\times p}\). Suppose the active subarray \((\bar a_{ek}:\bar Z_{ek}=1)\) has a joint density on its positive orthant, and put \(\bar s_{ek}=\bar Z_{ek}\bar a_{ek}\). For \(u,v\in\{0,1\}\), define \(N_{uv}^{k\ell}=\#\{e\in E:(\bar Z_{ek},\bar Z_{e\ell})=(u,v)\}\), and set \(T=\max_{k<\ell}\max\{N_{00}^{k\ell}+N_{10}^{k\ell},N_{00}^{k\ell}+N_{01}^{k\ell},N_{00}^{k\ell}+\mathbf 1\{N_{11}^{k\ell}>0\},\min(2,m)\}\). Almost surely, the following are equivalent: (i) for every failed set \(F\subseteq E\) with \(|F|=c\), the retained set \(H=E\setminus F\) and shifts \((\bar s_e:e\in H)\) give a design that guarantees \(\mathcal R_c(\Sigma)=\{D\}\) uniformly over all admissible \(D\in\mathcal D\), all \(\Omega\in\mathbb S_+^p\), and all positive-semidefinite replacements; (ii) \(T<m-2c\). If \(T<m\), condition (ii) is equivalent to \(c\le\lfloor(m-T-1)/2\rfloor\); if \(T=m\), no nonnegative corruption budget is certified. For the all-active design and \(m\ge3\), the largest certified budget is \(c_{\max}=\lfloor(m-3)/2\rfloor\).

### T-block: t4 — Fix \(n\geq1\), \(\alpha\in(0,1)\), and an arbitrary inde…
**Statement.** Fix \(n\geq1\), \(\alpha\in(0,1)\), and an arbitrary indexed family of nonempty covariance regions \(\mathcal C^{(n)}_{e,\alpha}\subseteq\mathbb S_{+}^{p}\), \(e\in E\). Under \(\text{ass:backshift-normalization}\), \(\text{ass:nonnegative-shifts}\), and \(\text{ass:honest-covariance-model}\), suppose additionally that \(\Omega\in\mathbb S_+^p\), and define \[ G_{n,\alpha,H} =\{\Sigma_e\in\mathcal C^{(n)}_{e,\alpha}\text{ for every }e\in H\}. \] Then \[ G_{n,\alpha,H}\subseteq\{D\in\mathcal U^{(n)}_\alpha\}. \] Consequently, whenever the displayed events are measurable, \[ \Pr(G_{n,\alpha,H})\geq1-\alpha \quad\Longrightarrow\quad \Pr\{D\in\mathcal U^{(n)}_\alpha\}\geq1-\alpha. \] This is a set-theoretic coverage implication and makes no representation or computability claim.

### T-block: t5 — Fix \(p\geq2\), \(m\geq1\), and \(0\leq c\leq m-1\), put…
**Statement.** Fix \(p\geq2\), \(m\geq1\), and \(0\leq c\leq m-1\), put \(h=m-c\) and \(q=m-2c=h-c\), and fix \(\gamma_0\in\mathbb Q_{>0}\), \(\zeta_0\in\mathbb Q\cap(0,1]\), and \(\overline\kappa,\overline M\in\mathbb Q\cap[1,\infty)\). Define \[ L_0=\bigl(p!\,\overline\kappa^{p-1}\bigr)^{1/p}, \qquad K_0=\frac{6\overline M}{\gamma_0} \sqrt{p(p-1)(1+L_0^2)}, \qquad C_0=16K_0L_0^3. \] Under \(\text{ass:backshift-normalization}\), \(\text{ass:nonnegative-shifts}\), and \(\text{ass:honest-covariance-model}\), suppose that \(\Omega\in\mathbb S_+^p\) and that the true instance satisfies \[ \gamma_H\geq\gamma_0,\qquad \zeta_D\geq\zeta_0,\qquad \kappa_D\leq\overline\kappa,\qquad M_H\leq\overline M. \] Fix \(n\geq1\), \(\alpha\in(0,1)\), and an arbitrary indexed family of nonempty covariance regions \(\mathcal C^{(n)}_{e,\alpha}\subseteq\mathbb S_{+}^p\). Assume declaratively that every tuple consisting of \(K\subseteq E\) with \(|K|=h\), \(A\in\mathcal D\), \(\Psi\in\mathbb S_+^p\), \(t_e\in[0,\infty)^p\), and \(\Gamma_e\in\mathcal C^{(n)}_{e,\alpha}\) for \(e\in K\), satisfying \[ A\Gamma_eA^{\mathsf T}=\Psi+\operatorname{diag}(t_e) \qquad(e\in K), \] also satisfies the single bound \[ \|A\|_{\mathrm{op}}\|A^{-1}\|_{\mathrm{op}} \leq\overline\kappa. \] Then there exists a real number \(r_0>0\), depending only on \(p,m,c,\gamma_0,\zeta_0,\overline\kappa,\overline M\), such that on the event \[ G_{n,\alpha,H} =\{\Sigma_e\in\mathcal C^{(n)}_{e,\alpha}\text{ for every }e\in H\}, \] one has \[ r_{n,\alpha,H}\leq r_0 \quad\Longrightarrow\quad \operatorname{outer}_{\mathrm{op}}(\mathcal U^{(n)}_\alpha,D) \leq C_0r_{n,\alpha,H}, \] \[ \operatorname{diam}_{\mathrm{op}}(\mathcal U^{(n)}_\alpha) \leq2C_0r_{n,\alpha,H}. \] Consequently, along any sequence of covered outcomes satisfying the same declarative candidate-side condition-number bound and \(r_{n,\alpha,H}\to0\), both displayed set radii converge to zero. The existence of \(r_0\) is non-effective: no procedure is asserted for checking the universal candidate premise or for computing \(r_0\), and no executable, algorithmic, or software interface is asserted.
