The proved population transition supplies the corrected deterministic envelope
\[
C_B\{2^{-(2s_y+1)J_y}+\mathbf 1\{d\ge1\}2^{-2s_zJ_z}\},
\]
the finite growing-rank Gaussian-quadratic approximation and forced equality Gaussianization for \(d\ge1\), the fixed-unequal-law studentized central limit theorem, and the separate exact \(d=0\) quadratic-process law.  It does not define a common-multiplier statistic, a conditional critical value, or \(\mathcal I_n^{\mathrm{mix}}\).  In particular, the population bounded-Lipschitz approximation neither consistently identifies the local spectral shift over contiguous alternatives nor proves conditional multiplier-quantile validity as the linear variance vanishes.