Let
\[
E_{R,n}=C_B\left[
\varepsilon_{\mathrm{pil},n}\{2^{-(s_y+1)J_y}+\mathbf 1\{d\ge1\}2^{-s_zJ_z}\}
+\varepsilon_{\mathrm{pil},n}^{2+a_y}
+\rho^{2a_y/(2+3/s_y+d/s_z)}\varepsilon_{\mathrm{pil},n}^2
\right].
\]
For \(d\ge1\), the proved exact-target expansion and the population spectral theorem yield an infeasible population-calibrated interval: under the spectral theorem's score-approximation condition, and after choosing a deterministic sequence that dominates the little-oh remainder in \(L^2\), center at \(\widehat\theta^{\mathrm{an}}_{J_y,J_z,n}\) and use the population Gaussian-quadratic proxy quantile divided by \(n\), enlarged by a fixed multiple of \(E_{R,n}\) and the deterministic remainder sequence. Markov's inequality for the \(L^2\)-controlled remainder and the bounded-Lipschitz population approximation then give asymptotic coverage at least \(1-\alpha\). Its half-width has order
\[
E_{R,n}+\frac{A_{J_y,J_z,n}}n+\sqrt{\frac{V_{L,n}}n},
\]
which becomes the certified corrected Gram scale at \((J_y^*,J_z^*)\). In the separate \(d=0\) branch, the proved quantile-process limit analogously gives population-law critical values at equality and the regular Gaussian limit away from equality. These are not data-driven honest intervals: the conditional \(d\ge1\) proxy uses the population Hessian, spectrum, local shifts, and linear variance, while the \(d=0\) statement does not supply one calibration uniformly connecting its null, local, and regular laws.