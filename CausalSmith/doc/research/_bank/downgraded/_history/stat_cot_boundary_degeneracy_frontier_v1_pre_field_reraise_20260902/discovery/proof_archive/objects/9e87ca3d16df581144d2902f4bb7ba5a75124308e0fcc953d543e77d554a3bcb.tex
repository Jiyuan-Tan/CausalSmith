For \(d\ge1\), inverse ellipticity smooths outcome frequencies while covariate resolution replicates the conditional Hessian spectrum. Retaining the non-orthogonal energy cross term corrects the outcome truncation bias to \(2^{-(2s_y+1)J_y}\), while weighted covariate projection stability gives \(2^{-2s_zJ_z}\). The retuned observable two-fold ridge-Galerkin statistic still attains the local frontier \(\min\{\rho^2,\rho n^{-1/2}+n^{-4s_z/(4s_z+d)}\}\) throughout the exact pilot-feasible region, with a sign mixture proving the zero and quadratic converse and a separate fixed-frequency two-point argument proving the regular converse. A proved growing-kernel contraction lemma yields the finite Gaussian-quadratic approximation and forces Gaussianization at equality for \(d\ge1\); the separate \(d=0\) branch retains its exact Brownian-bridge quadratic law. One confidence interval honest across null, local, critical, and regular regimes remains open.