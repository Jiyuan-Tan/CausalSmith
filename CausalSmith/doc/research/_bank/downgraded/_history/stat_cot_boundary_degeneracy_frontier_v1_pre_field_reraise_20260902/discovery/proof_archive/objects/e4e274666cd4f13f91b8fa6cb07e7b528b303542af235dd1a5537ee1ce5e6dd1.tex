The proved population result supplies the corrected deterministic envelope
\[
C_B\{2^{-(2s_y+1)J_y}+\mathbf 1\{d\ge1\}2^{-2s_zJ_z}\},
\]
the growing-rank Gaussian-quadratic approximation and forced equality Gaussianization for \(d\ge1\), the separate fixed-alternative studentized central limit theorem, and the exact \(d=0\) quadratic-process law.  It does not define a common-multiplier statistic, a conditional critical value, or \(\mathcal I_n^{\mathrm{mix}}\).  In particular, a population bounded-Lipschitz approximation cannot identify the local spectral shift consistently or imply conditional multiplier-quantile validity as the linear variance vanishes.