For a supplied partition \(\pi_0\), admissibility implies membership in \(\mathfrak G_{\pi_0}(P)\), and the cited Göbler--Windisch--Drton corollary identifies the coordinate-labelled block DAG.  Thus the graph-coordinate specialization is exactly the fixed-group GANM conclusion.  On the singleton partition, membership in the common restricted subfiber invokes the cited Peters theorem, so both specializations identify the same labelled scalar DAG.  Finally, when both competing block graphs are empty, \(\mathrm{lem:empty-dag-common-refinement}\) shows that their positive density factorizes over the common-refinement cells and that any unequal partition splits an innovation into independent smooth positive subvectors.  Innovation atomicity forbids that split, leaving the unique finest independent coordinate partition.  These are the three claimed reductions.