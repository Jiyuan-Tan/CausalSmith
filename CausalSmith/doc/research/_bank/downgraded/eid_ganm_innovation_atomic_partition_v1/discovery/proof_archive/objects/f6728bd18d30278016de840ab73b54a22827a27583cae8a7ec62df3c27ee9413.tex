All comparator claims not importing the unresolved headline theorem are established.  For a supplied \(\pi_0\), a term-by-term comparison of Definitions~\ref{def:admissible-class} and \ref{def:supplied-group-class} gives
\[
 \{R\in\mathfrak A(P):\pi_R=\pi_0\}
 \subseteq \mathfrak G_{\pi_0}(P).
\]
Proposition~\ref{prop:atomicity-removal-converse} supplies a law--representation witness showing strictness: for \(P_{\mathrm{merge}}\) and \(\pi_0=\{\{1,2\},\{3\}\}\), the merged empty-DAG representation belongs to \(\mathfrak G_{\pi_0}(P_{\mathrm{merge}})\) but fails innovation atomicity and hence does not belong to \(\mathfrak A(P_{\mathrm{merge}})\).  On the common singleton subfiber, Lemma~\ref{lem:peters-scalar-dag-identifiability} gives literal equality of the labelled scalar DAGs.  For every \(\theta\in(0,\infty)^5\), Lemma~\ref{lem:vector-family-atomicity} gives \(\mathfrak A(P_{\mathrm{vec},\theta})\ne\varnothing\), while Lemma~\ref{lem:vector-law-no-scalar-representation} gives
\[
 \mathfrak S(P_{\mathrm{vec},\theta})=\varnothing.
\]
The only remaining sentences of the comparator theorem assert that the unresolved atomic partition-and-DAG theorem identifies the previously unknown partition and that its graph coordinate specializes to the two published graph conclusions.  Those sentences follow immediately from Theorem~\ref{thm:atomic-partition-and-dag} once it is proved, but they cannot be used as premises while that target remains open.