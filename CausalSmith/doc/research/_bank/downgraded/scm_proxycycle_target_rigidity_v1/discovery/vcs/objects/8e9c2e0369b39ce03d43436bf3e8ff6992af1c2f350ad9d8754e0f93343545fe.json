{
  "node_type": "statement",
  "body": {
    "id": "thm:termination-and-exact-bounds",
    "kind": "theorem",
    "statement": "For fixed \\(k=|\\mathcal Z|\\) and \\(\\ell=|\\mathcal W|\\), the finite structural chart enumeration of \\(\\mathscr A\\) terminates using arithmetic and order tests over \\(\\mathbb K_{\\zeta}\\). There is an explicit computable constant \\(B_{k,\\ell}\\le 2^{2^{C(k\\ell+k+\\ell)}}\\), for a universal constant \\(C\\), bounding the number of labeled charts and the algebraic degree over \\(\\mathbb K_{\\zeta}\\). If every coordinate of \\(\\zeta\\) is an explicitly encoded rational whose numerator and denominator have binary length at most \\(L\\), a Turing implementation terminates; after clearing denominators, every algebraic target certificate uses a square-free integer polynomial of degree at most \\(B_{k,\\ell}\\) and rational isolating endpoints, and every coefficient and endpoint has bit length at most \\(B_{k,\\ell}(L+1)^C\\). The classification is obtained from the structural charts in \\(\\mathscr A\\), not by reporting generic two-copy quantifier elimination.",
    "free_symbols": [
      "Z",
      "W",
      "\\mathscr A",
      "\\mathbb K_{\\zeta}",
      "\\zeta"
    ],
    "depends_on": [
      "oeq:mixed-support-elimination",
      "thm:full-rigidity-atlas"
    ],
    "justification": "The field-relative statement separates symbolic exactness for real data from bit complexity for rational encodings.",
    "gap": "Fixed-dimensional real algebraic decision is known, but no end-to-end field-relative certificate and rational-input size bound exists for this structural response-cycle compiler.",
    "consumer": "Symbolic implementations can certify real-parameter outputs, while exact-arithmetic implementations can validate rational-data certificates with explicit size bounds."
  }
}