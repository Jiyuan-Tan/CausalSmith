Assume that the initial selected d-separation check of published Algorithm 1 passes. Assume also that, for every actual terminal \(\operatorname{sID\text{-}Single}\) failure on the resulting trace, an effective terminal-failure compiler returns rational \(E_0,E_1\in\operatorname{CertSCM}(G^S,\sigma_{\mathrm{bin}})\) and \(x_{\mathrm{bin}}\) with \(\mathsf{Check}_{\mathrm{NONID}}(G^S,X,Y,x_{\mathrm{bin}},E_0,E_1)=\mathrm{accept}\). Then, for every finite augmented ADMG \(G^S\) with childless \(S\), every finite signature \(\sigma\), and every \(x\in\prod_{v\in X}\mathcal X_v\), the corresponding restricted run of \(\mathsf{sID}^{\pm}\) is total. It returns either \(\operatorname{ID}(\Phi_{23,x}^{\sigma})\) or \(\operatorname{NONID}(x_{\mathrm{bin}},E_0,E_1,\mathsf{Check}_{\mathrm{NONID}}=\mathrm{accept};\sigma_{\mathrm{bin}})\). Along the chain rooted at \((D_i,T_i^{(0)})\), the target remains \(C=D_i\); a recursive step replaces \(T\) by the S-component \(T'\subseteq\operatorname{An}_{G^S[T]}(D_i)\subsetneq T\) containing \(D_i\). Thus \(\rho\) decreases strictly, the chain has at most \(|T_i^{(0)}|\) invocations, the recursion depth is at most \(\max_i|T_i^{(0)}|\le|N_S|\), and the total number of single-component invocations is at most \(B_0\). On success the published soundness result gives \(\Phi_{23,x}^{\sigma}(P_{M,\sigma}^S)=\Theta_{x,\sigma}(M)\). On failure, checker acceptance certifies exact graph compatibility, equal strictly positive selected laws, and unequal targets, and checking uses at most \(c\{|V|^2+L(E_0)+L(E_1)+2^{|V|}(R(E_0)+R(E_1))(|V|+|Y|+1)\}\) exact rational arithmetic and table-lookup operations. No totality claim is made here for traces that fail the initial d-separation check.