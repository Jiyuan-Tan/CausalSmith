Because the initial d-separation check is assumed to pass, the restricted run reaches line 8 of published Algorithm 1 and forms the finite ancestral set \(D\), its finite S-component partition \(\mathcal D_S(D)\), and the finite initial family \(\mathcal I_0\). For a fixed root \((D_i,T_i^{(0)})\), the first argument of every call to \(\operatorname{sID\text{-}Single}\) is \(C=D_i\). A nonterminal recursive call is made only in the case
\[
 C\subsetneq A:=\operatorname{An}_{G^S[T]}(C)
 \subsetneq T.
\]
The next enclosure is the S-component \(T'\) of \(A\) containing \(C\). Therefore
\[
 \varnothing\ne C\subseteq T'\subseteq A\subsetneq T,
 \qquad |T'|\le |T|-1.
\]
In the multiset order of \(\mathrm{def:termination\text{-}rank}\), replacing \(|T|\) by the strictly smaller positive integer \(|T'|\) strictly decreases \(\rho\). Since the natural numbers and their finite multiset extension are well founded, no chain is infinite.

If \(|T_i^{(0)}|=m\), the positive integer enclosure size can decrease strictly at most \(m-1\) times. Including the initial invocation gives at most \(m\) invocations on that chain. Hence
\[
 \operatorname{depth}\le\max_i|T_i^{(0)}|le |N_S|
\]
and, by \(\mathrm{def:initial\text{-}call\text{-}budget}\),
\[
 \#\{\text{single-component invocations}\}
 \le\sum_{(D_i,T_i^{(0)})\in\mathcal I_0}|T_i^{(0)}|
 =B_0.
\]

There are now only two terminal outcomes. If every single-component call succeeds, Algorithm 1 returns the displayed Eq. 23 expression. The cited soundness statement \(\mathrm{lem:published\text{-}latent\text{-}sid\text{-}soundness}\) gives, for every \(M\in\mathfrak M_+(G^S,\sigma)\),
\[
 \Phi_{23,x}^{\sigma}(P_{M,\sigma}^S)
 =\Theta_{x,\sigma}(M).
\]
All conditionals in that expression are defined by selected-cell positivity, and \(\Theta_{x,\sigma}(M)\) is defined by \(\mathrm{lem:target\text{-}support}\).

Otherwise a terminal single-component failure occurs. The second premise supplies finite rational certificates and an intervention value for which the exact checker accepts, so the restricted algorithm returns the stated \(\operatorname{NONID}\) object. By \(\mathrm{lem:certificate\text{-}checker\text{-}soundness}\), the realized models belong to \(\mathfrak M_+(G^S,\sigma_{\mathrm{bin}})\), have identical strictly positive selected laws, and have different intervention-before-conditioning targets. The same lemma gives the operation bound
\[
 c\left\{|V|^2+L(E_0)+L(E_1)
 +2^{|V|}(R(E_0)+R(E_1))(|V|+|Y|+1)\right\}.
\]
This proves totality and correctness on the restricted trace class. The argument never enters the initial-failure branch and therefore uses no unproved binary rational \(\mathsf{AncestorPair}\) compiler.