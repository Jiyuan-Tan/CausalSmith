If \(G^S\) has no bidirected edges, every district of every induced subgraph is a singleton. By \(\mathrm{def:s\text{-}component}\), for every \(H\subseteq N_S\),
\[
 \operatorname{SComp}_{G^S}(H)=\{\{h\}:h\in H\}.
\]
If \(\operatorname{SHedge}_{G^S}(C,T)\) held, then \(\operatorname{SComp}_{G^S}(T)=\{T\}\) would force \(|T|=1\). The strict inclusion of the nonempty set \(C\subsetneq T\) in \(\mathrm{def:s\text{-}hedge}\) would then be impossible. Hence no S-Hedge exists and the recursive S-Hedge failure branch cannot be reached.

The cited regression statement \(\mathrm{lem:published\text{-}dag\text{-}sid\text{-}consistency}\) records the published reduction of the latent criterion to the initial selected d-separation condition and its comparison with the fully observed criterion. Finally, if Algorithm 1 succeeds, \(\mathrm{lem:published\text{-}latent\text{-}sid\text{-}soundness}\) gives, pointwise for every finite \(\sigma\), every \(x\), and every compatible positive model \(M\),
\[
 \Phi_{23,x}^{\sigma}(P_{M,\sigma}^S)=\Theta_{x,\sigma}(M).
\]
The cited source asserts consistency of the criteria, not literal equality of every displayed formula, which is why the conclusion is limited as stated.