The projection equivalence and the explicit support-deficient two-edge pair reduce the unresolved step to a precise positivity-preserving realization problem.  For overlapping virtual edges, independently replacing each projected latent by a chain latent changes the posterior coupling of the shared ancestor mechanisms after conditioning on \(S=1\).  The frozen core contains no algebraic argument that the simultaneous selected-atom equations have a rational solution retaining the projected contrast, and the handle definition only describes that desired search; it does not prove that its rational nullspace contains such a point.