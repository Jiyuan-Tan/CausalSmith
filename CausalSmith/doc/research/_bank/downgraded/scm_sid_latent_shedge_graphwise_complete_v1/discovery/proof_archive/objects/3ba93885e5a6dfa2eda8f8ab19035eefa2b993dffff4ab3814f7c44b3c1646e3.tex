For the early-kill target, any affine fair-bit coding whose postintervention map from three independent fair latent bits to \((Y,Z_1,Z_2)\) is bijective over \(\mathbb F_2^3\) has selected target \(P(Y=1\mid\operatorname{do}(X_\star=x_\star),S=1)=1/2\), provided the selector is generated from \((Z_1,Z_2)\) and an independent selector latent and the selection denominator is positive. Thus a pair of such full-rank affine codings cannot separate \(Q_{\star,x_\star}\).