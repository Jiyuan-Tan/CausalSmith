The success implication is complete: published soundness gives \(\Phi_{23,x}^{\sigma}(P_{M,\sigma}^S)=\Theta_{x,\sigma}(M)\), so equality of selected laws implies equality of targets. Conversely, any accepted checker pair refutes \(\operatorname{Ident}^{\mathrm{fin}}_S\) by the established checker-soundness and ambient-inclusion lemmas. A terminal recursive failure yields an S-Hedge and the projected ordinary-hedge forest geometry proved this round. The remaining converse requires two independent compilers: a binary rational exact-graph compiler for an initial d-separation failure, and a positive rational compositional inverse lift plus a noncancelling district extension for a terminal recursive failure. Neither is established by the current construction definitions.