The recursion and verification parts are complete independently of the missing compiler.  After the finite initial graph operations, fix a root \((D_i,T_i^{(0)})\in\mathcal I_0\).  The first argument of every call on this chain is \(C=D_i\).  A nonterminal recursive call occurs only after forming
\[
 A=\operatorname{An}_{G^S[T]}(D_i)
\]
and choosing the S-component \(T'\) of \(A\) containing \(D_i\), in the case
\[
 C\subseteq T'\subseteq A\subsetneq T.
\]
Therefore \(1\le |T'|\le |T|-1\).  By \(\mathrm{def:termination\text{-}rank}\), replacing \(|T|\) by the strictly smaller positive integer \(|T'|\) strictly decreases the finite-multiset rank \(\rho\).  The strict order on \(\mathbb N\) is well founded, and so is its finite-multiset extension; hence no call chain is infinite.

If \(|T_i^{(0)}|=m\), at most \(m-1\) strict decreases are possible before termination.  Including the initial invocation, that chain has at most \(m\) invocations.  Consequently
\[
 \operatorname{depth}\le\max_i|T_i^{(0)}|\le|N_S|
\]
and, by \(\mathrm{def:initial\text{-}call\text{-}budget}\),
\[
 \#\{\text{single-component invocations}\}
 \le\sum_{(D_i,T_i^{(0)})\in\mathcal I_0}|T_i^{(0)}|
 =B_0.
\]
If every call succeeds, \(\mathrm{lem:published\text{-}latent\text{-}sid\text{-}soundness}\) proves that the returned Eq. 23 functional equals \(\Theta_{x,\sigma}\).  If any rational pair is returned with accepted checker evidence, \(\mathrm{lem:certificate\text{-}checker\text{-}soundness}\) proves exact-graph membership, equality and positivity of the selected laws, inequality of the targets, and the operation bound
\[
 c\left\{|V|^2+L(E_0)+L(E_1)
 +2^{|V|}(R(E_0)+R(E_1))(|V|+|Y|+1)\right\}.
\]
These arguments prove termination, success soundness, and certificate verification.  They do not construct a certificate on either failure branch.