The native theorem marks the boundary where terminal failure is already ordinary hedge failure inside the nonancestor graph. There, selection can be a common independent full-support channel, and invertible noisy routing prevents the hedge contrast from disappearing before reaching the outcome. Virtual bidirected edges differ because their realization changes ancestor mechanisms that also determine selection.