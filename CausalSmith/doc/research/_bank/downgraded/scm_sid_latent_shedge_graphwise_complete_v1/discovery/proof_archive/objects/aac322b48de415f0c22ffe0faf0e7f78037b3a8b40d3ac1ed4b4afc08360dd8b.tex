A unit is a recursive augmented SCM on a finite childless-selector ADMG. On a fixed native-trace graph, mutually independent exogenous variables may enter multiple mechanisms, and their incidences and the parent interfaces induce exactly \(G^S\). Native failure witnesses use a finite multistate signature; binary rational certificate compilation is not part of the proved headline.