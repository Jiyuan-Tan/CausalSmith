Population identification is proved only on \(\operatorname{NativeForestTrace}\) graph instances, uniformly over finite signatures and from one exact selected observational law within each signature. Every compatible SCM still induces exactly the fixed augmented ADMG. The result is algebraic and uses no sampling approximation.