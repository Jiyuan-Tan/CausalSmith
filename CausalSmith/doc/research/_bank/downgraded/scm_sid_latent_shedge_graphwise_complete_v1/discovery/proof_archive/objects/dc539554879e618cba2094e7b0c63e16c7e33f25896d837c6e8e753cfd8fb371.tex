Can \(\mathsf{sID}^{\pm}\) be made total by two effective finite-rational compilers: one for failure of the initial selected d-separation check and one for every terminal recursive S-Hedge, each returning an accepted exact-graph \(\operatorname{CertSCM}\) pair? A positive answer requires both the independent \(\mathsf{AncestorPair}\) compiler and the compositional inverse lift with noncancelling outcome contrast; graph-recursion termination and checker verification alone do not construct either pair.