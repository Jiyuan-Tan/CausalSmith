---
qid: scm_sid_latent_shedge_graphwise_complete
spec: v1
topic: "Graphwise completeness of latent subpopulation S-ID on native-forest traces. For every finite childless-selector augmented ADMG G^S and disjoint X,Y satisfying NativeForestTrace(G^S;X,Y)—the initial selected d-separation check passes and every reachable terminal S-Hedge admits nested common-root forests whose directed and bidirected edges are actual edges of G^S[N_S]—work over the full positive recursive augmented-SCM class whose declared observed-parent interfaces and shared-exogenous incidences induce exactly G^S. Prove that Abouei et al.'s Algorithm 1 succeeds if and only if P(Y | do(X=x), S=1) is graphwise identifiable from P(V | S=1) over all finite signatures, comparing models only within one signature. On success return the published Eq. 23 functional. On terminal native failure construct one finite multistate exact-graph counterpair with the same strictly positive selected law and unequal intervention-before-conditioning targets, using a positive ordinary-forest separator, common full-support randomized-response routing along actual treatment-avoiding paths, and inert finite-state exactification. Prove explicit graph-recursion bounds and sound finite checking of any supplied rational certificate; do not claim a binary, rational, or executable failure constructor. Preserve unrestricted virtual-edge graphwise completeness, the positive-binary two-virtual-edge separator, the initial selected-d-separation failure construction, exact-rational inverse lifting, and total success-or-certificate compilation as open questions. Retain the UK Biobank component only as a conditional two-selector feasibility audit."
novelty_target: field
banked_novelty_tier: subfield
tier_at_proposal: ACCEPT
tier_at_derivation: REVISE
proposal_promise_gap: "kernel_substituted"
reusable: not_reusable
reraise_status: re-raise
gap_reasons:
  - "The proved iff applies only to the note-defined NativeForestTrace subclass, whose load-bearing clause assumes that every terminal obstruction already has nested forests made from actual nonancestor edges."
  - "It therefore bypasses the virtual-edge realization problem central to the published open completeness question and excludes every initial selected-d-separation failure."
  - "Complete the compositional inverse lift for terminal projected forests with shared or branching selection ancestors, preserving one strictly positive selected law and a noncancelling outcome-event contrast."
reusable_artifacts:
  - "discovery/core.json — typed NativeForestTrace theorem, exact finite-state model class, OEQs, and literature map"
  - "discovery/writeup.tex — complete D0 derivation and explicit NativeForestTrace boundary proof"
  - "discovery/d0_working.json — accepted proof payloads plus the smallest bow-graph positive countermodel calculation"
  - "orchestrator/decision_log.jsonl — failed binary/virtual-edge routes, no-go calculations, citation repairs, and maximality audits"
seeds_burned:
  - index: 0
    one_liner: "seed:graphwise-sid-iff"
    reason: "Angle 0 exhausted six revisions on universal positive selected-law inverse lifting and exact certificate compilation before the NativeForestTrace pivot."
proof_attempt_summary: |
  The run attacked unrestricted latent S-ID completeness through positive binary inverse lifts, a two-virtual-edge fiber calculation, independent smoothing, affine coding, and a general district extension; these routes either lost positivity/target separation or assumed the common-selected-law crux. It then proved the strongest surviving NativeForestTrace converse using positive finite multistate ordinary-hedge models, common selected-law extension, noncancelling actual-edge routing, and exactification, with all citation and mathematical checks passing. Extending the theorem to projected virtual edges with shared or branching selection ancestors, and handling initial selected-d-separation failures, remains open and is required for field-tier completeness.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 120377064
  pipeline_claude_tokens: 0
  total_tokens_consumed: null
banked_on: "2026-09-05"
---

# scm_sid_latent_shedge_graphwise_complete / v1 — Downgraded

**Topic.** Graphwise completeness of latent subpopulation S-ID on native-forest traces. For every finite childless-selector augmented ADMG G^S and disjoint X,Y satisfying NativeForestTrace(G^S;X,Y)—the initial selected d-separation check passes and every reachable terminal S-Hedge admits nested common-root forests whose directed and bidirected edges are actual edges of G^S[N_S]—work over the full positive recursive augmented-SCM class whose declared observed-parent interfaces and shared-exogenous incidences induce exactly G^S. Prove that Abouei et al.'s Algorithm 1 succeeds if and only if P(Y | do(X=x), S=1) is graphwise identifiable from P(V | S=1) over all finite signatures, comparing models only within one signature. On success return the published Eq. 23 functional. On terminal native failure construct one finite multistate exact-graph counterpair with the same strictly positive selected law and unequal intervention-before-conditioning targets, using a positive ordinary-forest separator, common full-support randomized-response routing along actual treatment-avoiding paths, and inert finite-state exactification. Prove explicit graph-recursion bounds and sound finite checking of any supplied rational certificate; do not claim a binary, rational, or executable failure constructor. Preserve unrestricted virtual-edge graphwise completeness, the positive-binary two-virtual-edge separator, the initial selected-d-separation failure construction, exact-rational inverse lifting, and total success-or-certificate compilation as open questions. Retain the UK Biobank component only as a conditional two-selector feasibility audit.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** REVISE

**Banking reason.** D0.5 math, decision, citation, and framing checks pass, but the honest NativeForestTrace theorem is subfield below the fixed field floor; unrestricted virtual-edge inverse lifting remains open after all distinct bounded routes were exhausted.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
