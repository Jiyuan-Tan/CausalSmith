Experimenters can compute the graph-adaptive bandwidth and report its endpoint-clipped conditional interval; the conservative certificate still transparently marks the unresolved sharp confidence-length rate.