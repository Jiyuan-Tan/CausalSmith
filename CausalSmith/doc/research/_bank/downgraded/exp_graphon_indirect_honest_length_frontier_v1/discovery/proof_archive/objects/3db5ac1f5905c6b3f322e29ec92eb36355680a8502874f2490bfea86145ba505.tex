The leading empirical eigenspace of the observed adjacency matrix is used only in the explicit first-projection correction \(q_h-4\widehat\Pi_r(A)A(Z-\mathbf1_n/2)\).  Its graph-observable residualization and coordinate-oscillation certificates are finite and polynomial-time computable; no \(d/n\) rate is asserted for either feasible principal-component remainder.