The finite-population derivative \(\theta_n\) is the target. LiWager2022RandomGraph Theorem 6 is only an asymptotic benchmark after \({\rm prop:li-wager-bridge}\). Every positive realized degree is handled by its exact support radius; isolates remain visible in the certificate. The reportable interval monotonically clips its two ordered endpoints to \([-B,B]\), which preserves coverage because \(\theta_n\in[-B,B]\) and never increases length. The supported-unit lattice, interior, and graph-floor terms have a proposed matching converse, and the same-class unsupported and low-degree modulus is explicit. A full matched frontier is not claimed because the safe \(B\pi_{0,n}\) upper allowance is not matched by the \(B\sqrt{\pi_{0,n}/n}\) isolate testing term. The open tail-adaptive construction must retain the i.i.d. \((U_i,f_i)\) class and the stated conditioning convention. Unknown rank, unknown \(L\), simultaneous policy bands, and optimal ego-cluster design remain outside scope.