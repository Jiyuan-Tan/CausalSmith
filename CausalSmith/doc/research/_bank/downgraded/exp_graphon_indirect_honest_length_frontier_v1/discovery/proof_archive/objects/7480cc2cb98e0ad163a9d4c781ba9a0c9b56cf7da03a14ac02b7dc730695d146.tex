The original tail-augmented minimax statement does not close from the frozen core.  The exact bump proof above supplies only
\[
 \frac{a_m}{n}\sup_P\mathbb E_P\sqrt{K_m(A)},
\]
whereas \({\rm def:tail-modulus}\) uses \(a_m\sqrt{\pi_{m,n}/n}\); Jensen gives the inequality in the wrong direction for that tail converse, which is therefore removed in the proposed claim.  The remaining lattice, interior, honest-length, and graph-floor pieces require explicit rank-\(r\), nonnegative, spectrally strong graphons and bounded response experiments with parameter separation and experiment-distance calculations.  No such laws or calculations are frozen or proved in this dispatch.  In particular, the compatibility of \((r,\kappa,\underline\lambda)\) is not assumed, so even nonemptiness and an interior least-favorable graphon cannot be silently used.  The certified lower result is the missing-sign Bayes bound in the narrowed \({\rm lem:tail-testing-modulus}\); it does not imply the proposed supported-unit theorem.