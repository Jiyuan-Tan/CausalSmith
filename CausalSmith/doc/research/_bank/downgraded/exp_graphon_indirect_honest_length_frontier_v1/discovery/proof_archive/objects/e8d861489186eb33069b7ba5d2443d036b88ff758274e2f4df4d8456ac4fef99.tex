A Bernoulli one-network experiment reveals the realized midpoint derivative only on each unit's exact degree lattice.  The note constructs the normalized degree-adaptive score \(q_{i,h}\), proves its first two Hoeffding projections, and uses explicit first-projection principal-component balancing to form \(\widehat\theta_h\).  The graph-observable bias and bounded-difference certificates give finite-sample conditional coverage at every realized degree, retain isolates, and remain valid for the graph-only selector \(\widehat h(A)\); endpoint clipping to \([-B,B]\) preserves coverage and cannot increase length.  Same-class linear, local \(C^3\)-bump, and lattice experiments prove the supported analytic lower benchmark, while the operational procedure is bounded only by the conservative envelope \(\mathfrak u_{n,d,L}\).  No comparison \(\mathfrak u_{n,d,L}\lesssim\mathfrak r^{\mathrm{sup}}_{n,d,L}\), sharp minimax rate, or achieved honest expected-length frontier is claimed.