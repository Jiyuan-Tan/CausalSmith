D0 MAXIMALITY VERDICT: the source-class bridge correction landed correctly, but two material statement-faithfulness defects remain. No sharper rate, construction, elbow, or class-transfer theorem is justified.

Bridge verification:

- `ass:at-least-square` now correctly labels \(T\ge2K+2\) as this note’s sufficient specialization.
- `prop:published-source-bridge` is present with a complete proof in both `core.json` and `writeup.tex`.
- It correctly proves mover-a.s. full column rank, eligibility \(=\{X\ne0\}\), \(E[\Pi(M)]\succeq\pi_0I\), finite \(B\)-second moments, \(E[\varepsilon\mid M]=0\), and \(\overline\beta_k=\tau_k\).
- The finite-cutoff normalization difference is explicit; only the limits are equated.
- TL;DR, sampling model, target/functional metadata, comparator row, related work, technical limitation, and honest scope consistently describe \(\mathcal P_\Sigma\) as a strict reduced-form specialization—not the published potential-outcome class.
- The tail atlas and negative converse are not transferred upward. \(R^\star/R^\dagger\) and the open \(\Omega(C^\star)\) transport problem remain intact.

Remaining defects:

1. `lem:published-reduced-form-scope` incompletely states the anchor theorem’s hypotheses. The source theorem assumes no anticipation, i.i.d. sampling, never-treated parallel trends, the random-coefficient model, projector invertibility, and \(E[\Delta D_t^2+\Delta Y_t(\mathbf0_t)^2]<\infty\). The current cited lemma lists only the induced reduced-form equation, exclusion, dimension/moment condition on \(B\), and projector condition, yet immediately attributes identification to Theorem 4.  
   Current \(\mathcal P_\Sigma\) implies the relevant observed reduced-form dimension, \(B/\varepsilon\) moments, exclusion, eligibility, and projector conditions, but it does not impose the source’s potential-outcome no-anticipation or parallel-trends assumptions. The paper must distinguish the algebraic observed-law bridge from application of the source’s full causal-identification theorem.

2. `prop:benchmark-reductions` says only that “eligible mover histories” are separated from \(\mathcal Z\), while its proof assumes \(Q(x)>0\) for every point in the entire finite mover support. As written, rank-deficient ineligible movers may remain and contribute to \(\tau_k=E[B_k\mid X\ne0]\), so the claimed identification of the all-mover mean need not hold. Tighten the premise to require the whole mover support to be separated from \(\mathcal Z\), equivalently \(Q>0\) at every mover-support point.

Scoped directive:

```text
--require-core-target lem:published-reduced-form-scope
--require-core-target prop:benchmark-reductions

Make only two statement-faithfulness repairs.

First, state the full hypotheses attached to the anchor's Theorem 4, including
no anticipation, iid sampling, never-treated parallel trends, the additional
second-moment condition, the random-coefficient model, and design
invertibility. Separate those potential-outcome identification hypotheses
from the reduced-form conditions verified by prop:published-source-bridge.
Continue to assert only an observed-law/target-functional bridge on P_Sigma;
do not claim that P_Sigma is a subclass of the published potential-outcome
class.

Second, replace “the finitely many eligible mover histories are separated
from Z” in prop:benchmark-reductions by the exact premise used in its proof:
the entire finite mover support is separated from Z, equivalently Q(x)>0 for
every mover-support point. Preserve tau_k as the all-mover target and preserve
the O(G^-1) risk and O(G^-1/2) length conclusions.

Synchronize core and prose/source metadata. Make no change to the tail atlas,
Rstar/Rdagger definitions, estimator, phase rates, scalar or K=1,T=5
benchmarks, or either negative theorem.
```

After those repairs, the expected tier remains a strong specialist econometric-theory/algebraic-statistics paper. It is not a flagship matched-minimax result while deterministic-to-feasible transport, a positive rich-subclass converse, and honest-length theory remain open.
