Accept the full atomic post-image; `--all` is safe.

Accepted change selectors:

- `assumption-replace:ass:at-least-square`
- `target-estimand-replace:metadata:target-estimand`
- `estimand-functional-replace:metadata:estimand-functional`
- `comparator-promise-table-replace:metadata:comparator-promise-table`

Accepted proof:

- `proof:prop:published-source-bridge`  
  Operationally, omit `--reject-proof`; proof IDs are not selected through `--id`.

Rejected selectors: none.

The bridge was persisted as a new durable partial node with its exact proof, not omitted. It appears in `d0_working.solved`, the packet’s assembled statements, and `provisional_proofs`; hence there is no separate `statement:prop:published-source-bridge` selector.

All required prose groups landed byte-for-byte: TL;DR, project justification, sampling model, related work, technical limitation, and honest scope. The four proposed edits match the source solve exactly; the comparator replacement changes only the anchor row. The source receipts support every attributed anchor condition, while the proposition correctly limits equivalence to the strict induced reduced-form specialization and preserves the finite-cutoff normalization distinction.
