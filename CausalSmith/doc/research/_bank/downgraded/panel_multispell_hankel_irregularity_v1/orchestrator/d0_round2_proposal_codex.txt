D0 round-2 adjudication — `panel_multispell_hankel_irregularity/v1`

Overall verdict: **REJECT AS BUNDLED; do not apply this atomic post-image.**

Individually acceptable variants:

- `statement:lem:pseudoinverse-leverage`
- `statement:thm:geometry-certification`
- `statement:lem:oracle-fitting-remainder`
- `statement:thm:matched-frontier`
- `definition:def:class-oracle`
- `definition:def:oracle-estimator`
- `symbol-add:\Lambda_\Sigma`
- `symbol-add:c_\Sigma`
- `symbol-add:\Omega_{G,\Sigma,k,s}`
- `symbol-add:R_{G,\Sigma,k}^{\dagger}`
- `symbol-add:C_{G,\Sigma,k}^{\dagger}`
- `definition-add:def:empirical-extrapolation-modulus`
- `statement-replace:lem:pseudoinverse-leverage`
- `statement-replace:thm:geometry-certification`
- `statement-replace:lem:selector-geometry`
- `statement-replace:lem:oracle-fitting-remainder`
- `statement-replace:thm:matched-frontier`
- `definition-replace:def:class-oracle`
- `definition-replace:def:oracle-estimator`
- `bibliography-replace:HirshbergMalekiZubizarreta2021`
- `bibliography-replace:AudibertTsybakov2011`
- `bibliography-replace:Gaiffas2005`
- `estimand-functional-replace:metadata:estimand-functional`
- `comparator-promise-table-replace:metadata:comparator-promise-table`

Rejected variants:

- `statement:prop:benchmark-reductions`
- `statement-replace:prop:benchmark-reductions`

The proposed benchmark is not a narrowing. Its finite-support clause deletes the current requirements of independent groups, bounded moments, positive stayer mass, and eligible histories, yet still concludes \(O(G^{-1})\) squared risk and \(O(G^{-1/2})\) interval length. The provisional proof explicitly invokes independence and “the assumed moments,” neither of which appears in the proposed post-image. It also analyzes the score using the unknown true \(\Gamma\); without positive stayer mass or another identification premise, it does not prove a feasible estimator or interval. An i.i.d. finite-support design with an integrable but infinite-variance score already contradicts the stated risk conclusion.

The other provisional proofs support their post-images. In particular, the minimax-linear estimator is total and measurable: the feasible weight box is compact, the positive quadratic penalty makes the objective strictly convex, and separability of the Whitney ball yields measurable imbalance and weights. Its bound does not hide a conditional coefficient moment: the cap gives
\[
E\sum_i\widehat\gamma_i^2
E[(B_{ik}-\mu_k(X_i))^2\mid X_i]
\lesssim G^{-1}E[B_k^2],
\]
while the observed \(H_k^2\)-penalty controls donor noise. Fold independence and `lem:regular-gamma` control generated-\(\Gamma\) terms. No occupancy or empirical-Gram premise is used; any donor/frontier transport failure remains visibly inside \(\Omega_{G,\Sigma,k,s}\).

The \(R^\star/R^\dagger\) separation is otherwise synchronized correctly. The proposed theorem claims attainable risk only at \(R^\dagger(C^\dagger)\), preserves only a one-sided deterministic pole phase for \(R^\star\), and makes no \(\Omega(C^\star)\lesssim R^\star\), reverse-minimax, or honest-length claim. The TL;DR, project fill, honest scope, estimand-functional replacement, symbols, definitions, bibliography, and comparator table agree with that separation.

Partial application is not safe: leaving the old benchmark statement in place would retain its unsupported two-sided scalar and critical-Hankel equalities and conflict with the accepted matched-frontier narrowing. Re-emit the same bundle after restoring explicit independent-group and bounded-moment premises in the finite-support clause, and either retain positive stayer mass with a proof controlling estimated \(\Gamma\), or state explicitly that the sample-average benchmark uses known \(\Gamma\).

Exact CLI recommendation:

```text
--discard-all --note "Atomic post-image rejected: prop:benchmark-reductions deletes the independence, bounded-moment, and positive-stayer/known-Gamma premises used by its provisional proof, so its finite-support O(G^-1) risk and O(G^-1/2) interval claims are unsupported. Partial apply would retain the stale unsupported benchmark equalities. Re-emit the same 24 acceptable variants plus a benchmark statement/proof with the missing premises and Gamma handling restored."
```
