This directive supersedes the earlier prose-sync directive only for def:oracle-estimator, def:class-oracle, thm:matched-frontier, metadata:estimand-functional, and lem:oracle-fitting-remainder; preserve the other previously adjudicated round-1 deltas. Do not attempt to close the chartwise local-polynomial claim by citing generic boundary adaptivity or by adding uniform A_C-thickness, empirical-Gram, anisotropic-occupancy, active-stratum, tail-index, or bias-index assumptions. The cited local-polynomial theorems require exactly such conditions.

Replace def:oracle-estimator's fitted-tail component by a cross-fitted minimax-linear/Riesz correction over the existing original-coordinate Whitney ball. On a donor fold retain H_k≤C scores. On an independent target fold form the empirical functional of f over H_k>C. Choose lexicographically tie-broken weights minimizing squared worst-case Whitney-ball imbalance plus the observed (1+H_k²)-weighted quadratic variance penalty, subject to a fixed signature-coded coordinatewise bound |γ_i|≤Λ_Σ/G. Specify zero weights for empty donor folds and preserve all existing Moore–Penrose and Γ fallback conventions.

Add an empirical extrapolation modulus Ω_G,Σ,k,s(C), defined as the whole-class supremum of the expected minimized objective. Prove totality/measurability and sup_P ‖τhat_k(C)−τ_k‖₂ ≤ C[{Vbar_Σ,k(C)/G}^{1/2}+Ω_G,Σ,k,s(C)^{1/2}+G^{−1/2}]. Retain separately the already-supported Bbar_Σ,k,s(C) ≤ C L_s D_Σ,k,s(C) ≤ C' L_s J_Σ,k,s(C). Do not assert Ω(C*)≲R* from the present normal-link record. Define the augmented finite-grid oracle Rdagger(C)=Vbar(C)/G+Bbar(C)^2+Ω(C)+G^{−1}, use its least minimizer, and state estimator attainment only for Rdagger. Preserve the pole-sensitive deterministic R* phase theorem separately; the comparison Rdagger≲R* is an explicit open transport/optimal-recovery problem.

Add HirshbergMalekiZubizarreta2021, AudibertTsybakov2011, and Gaiffas2005 to the bibliography/source map. Cite SasakiUra2022 only for its global-series choice and explicit Gram/sieve conditions, not as proof of this multivariate bound. SYNC tldr, project_justification.fill, honest_scope, and the estimand-functional sentence: R* has the proved deterministic pole-sensitive upper phase; the minimax-linear estimator attains augmented Rdagger; showing Ω≤R* remains open; honest-length attainment at the old R* scale remains removed. Do not alter the estimand.
