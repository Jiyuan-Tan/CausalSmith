Preserve τ_k, H_k²=A_k/Q on {Q>0}, target-inactive cancellation, the K=1,T=5 early kill, the bounded-score branch, the external finite-support comparison, and the exact fixed-Σ risk/length objective. Replace def:invariant-indices by an actual local-zeta rule. Define Z_k(z)=∫_{S∩{Q>0,H_k>1}}H_k(x)^z dx and Z_{k,s}(z)=∫_{S∩{Q>0,H_k>1}}H_k(x)^z dist(x,𝒵)^s dx. If H_k is unbounded, define α_{Σ,k} and ζ_{Σ,k,s} as their abscissae of convergence/first positive real poles after certified semialgebraic resolution, with m_{Σ,k}=ord_{z=α}Z_k−1 and ℓ_{Σ,k,s}=ord_{z=ζ}Z_{k,s}−1. If H_k is bounded, retain (∞,0,∞,0). Give the equivalent chart-face rule from c_{kja}=(q_{ja}−a_{kja})/2>0: minimize (b_{ja}+1)/c_{kja}, or (b_{ja}+1+s d_{ja})/c_{kja}, over realizable simultaneous-zero faces; the pole order is the maximum number of tied coordinates within one germ, while ties across distinct charts add positive contributions without raising pole order. Use a disjoint canonical chart-cell refinement or cutoff valuations so overlaps and vanishing weights cannot change the count, and prove resolution invariance.

Replace chartwise Hölder smoothness by the primitive condition that μ_k has a representative with an original-coordinate Whitney extension μ̃_k∈C^{s−1,1}(N_δ(S)), norm ≤L_s. Tag this novel/nonstandard and cite Sasaki–Ura (2022) only as scalar derivative-correction motivation. For finite C define A_C=closure(S∩{Q>0,H_k≤C}) and A_{jC}=A_C∩supp ω_j. When A_C is nonempty, define p_C(x) as the lexicographically least Euclidean argmin over A_C and p_{jC}(x) analogously over A_{jC}, falling back to p_C when A_{jC} is empty; prove these selectors Borel. If A_C is empty, use a fixed signature-coded x_Σ, set the finite-C extension to zero, and exclude that cutoff from the effective oracle grid except as an analyzed fallback. Define the population extension by the explicit partition-of-unity Taylor formula centered at p_{jC}(x), with the identity branch when H_k≤C and E_{Σ,k,s,∞}μ_k=μ_k. Define the sample analogue by a displayed ridge local-polynomial normal equation at every selected anchor, with kernel, bandwidth, training-fold indicators, chart weight, monomial vector, and ridge G⁻²I; zero right-hand side gives the empty-cell value. Prove joint measurability. Do not use the underspecified phrase “fit a polynomial on a chart.”

Prove rather than assume Σ_j∫_{H_k>C}ω_j(x)‖x−p_{jC}(x)‖^s dx ≲ J_{Σ,k,s}(C). If this fails for an admitted signature, redefine the frontier remainder by that selector-distance integral and recompute ζ, ℓ, and all rates. Replace the false claim that real quantifier elimination alone decides numerical Hausdorff link-mass inequalities by a certificate of semialgebraic conic/bi-Lipschitz parametrizations with algebraically checkable angle/Jacobian bounds, or state the additional effective volume-bounding algorithm. Finally, the exact matched-frontier theorem must exhibit a nonempty baseline law and same-Σ coefficient-mean perturbations satisfying Whitney, moment, variance, and stayer restrictions and jointly realizing the variance/bias orders; absent that construction retain the oracle upper bound but downgrade exact minimax-risk and honest-length equivalence. Recheck the scalar s=1 reduction, critical logarithmic case, and K=1,T=5 rate under the repaired pole and selector-distance rules.
