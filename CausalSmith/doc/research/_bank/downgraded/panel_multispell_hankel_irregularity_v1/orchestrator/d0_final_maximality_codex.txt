D0 MAXIMALITY VERDICT: one remaining material carrier-level scope defect; no new mathematical, rate, construction, elbow, class-transfer, or anchor-faithfulness repair is needed.

The two requested repairs landed exactly in both `writeup.tex` and `core.json`:

- `lem:published-reduced-form-scope` now records the anchor’s no-anticipation, full-schedule i.i.d., never-treated parallel-trends, random-coefficient, second-moment, and projector-invertibility hypotheses. The verbatim source receipts support this, including \(K<T-1\), component-specific eligibility, and overidentified no-stayer designs. The paper correctly limits \(\mathcal P_\Sigma\) to an observed-law reduced-form specialization.
- `prop:benchmark-reductions` now assumes \(Q(x)>0\) at every mover-support point. Its proof accordingly establishes full rank and bounded leverage for all movers before identifying the all-mover target and deriving the finite-support risk and interval bounds.

The remaining defect is a stale honest-length overclaim:

- `writeup.tex:33` and `core.json`’s `tldr` say the estimator attains “uniform risk and honest-length upper bounds over \(\mathcal P_\Sigma\).”
- `writeup.tex:1288` and `core.json`’s `honest_scope` list “class-level risk and honest-length upper bounds” as closed.
- This directly contradicts `thm:matched-frontier`, which proves only the augmented-oracle risk upper bound and explicitly says that no honest-length bound is asserted. The only proved \(O(G^{-1/2})\) interval-length result is for the separate finite-support law \(P^{\mathrm{fs}}\), not \(\mathcal P_\Sigma\).

Exact scoped directive:

```text
--require-core-target thm:matched-frontier

Make one carrier-only claim-faithfulness repair. Change only metadata:tldr
and metadata:honest-scope, synchronized with the corresponding TL;DR and
Honest scope prose in writeup.tex.

In the TL;DR, replace the assertion that the estimator attains uniform risk
and honest-length upper bounds over P_Sigma by the exact supported claim:
the estimator attains the augmented class-level risk upper bound
Rdagger(Cdagger); no honest-length upper bound over P_Sigma is proved, and
comparison with the deterministic Rstar phase remains open.

In Honest scope, replace “class-level risk and honest-length upper bounds”
by “the class-level augmented-oracle risk upper bound.” State explicitly
that honest-length rates or upper bounds over P_Sigma remain open, while
the O(G^-1/2) interval belongs only to the separate finite-support benchmark
P^fs.

Preserve project_justification.fill, which already states this correctly.
Make no statement, proof, definition, estimator, oracle, rate, benchmark,
comparator, bibliography, anchor-bridge, or negative-theorem change.
```

After that synchronization, no further material same-assumption strengthening is justified. The maximized contribution is the resolution-invariant, target-specific Hankel/local-zeta tail and distance-weighted bias geometry, including logarithmic multiplicities, boundary faces, coordinate cancellation, the critical \(K=1,T=5\) test, and a total minimax-linear estimator attaining the exposed \(R^\dagger\) scale. Its honest limitations remain \(R^\dagger\not\lesssim R^\star\), no rich-subclass converse, no \(\mathcal P_\Sigma\) honest-length theorem, and no specified across-signature adaptive inference procedure.

Expected tier after the carrier repair: strong specialist econometric-theory/algebraic-statistics paper; not flagship matched-minimax theory until transport to \(R^\star\), a positive converse, and honest inference are closed.
