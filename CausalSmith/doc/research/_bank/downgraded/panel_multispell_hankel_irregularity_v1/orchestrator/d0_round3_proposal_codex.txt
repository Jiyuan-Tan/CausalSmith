Round-3 D0 adjudication — `panel_multispell_hankel_irregularity/v1`

Overall verdict: **ACCEPT THE FULL ATOMIC POST-IMAGE. Recommend `--all`.**

The benchmark repair closes the round-2 defect:

- i.i.d. groups are explicit.
- \(0<P^{\mathrm{fs}}(X=0)<1\) supplies positive stayer and mover probabilities.
- Fixed second-moment bounds on \(B,\varepsilon\), together with finite mover support separated from \(\mathcal Z\), give bounded leverage and finite score variance.
- \(\Gamma\) remains unknown, but the proof uses an independent training-fold positive-stayer/projector estimator, obtains \(\|\widehat\Gamma-\Gamma\|_2=O(G^{-1/2})\), and uses bounded support leverage to show the generated-\(\Gamma\) score contribution is \(O(G^{-1/2})\).
- The evaluation-fold score average consequently has squared risk \(O(G^{-1})\); the displayed MSE-bound interval has coverage by Markov and length \(O(G^{-1/2})\).
- The scalar and \(K=1,T=5\) reductions retain only one-sided upper orders, with explicit richness/frontier conditions before any two-sided interpretation.

All previously accepted content remains synchronized: deterministic \(R^\star\) is separated from attainable \(R^\dagger(C^\dagger)\); \(\Omega(C^\star)\lesssim R^\star\), reverse minimax, and honest length at the old scale remain unclaimed; the minimax-linear estimator, symbols, empirical-modulus definition, source additions, comparator downgrades, estimand-functional text, TL;DR, Fill, limitation, and honest scope are preserved.

Accepted selectors — all 26:

- `statement:lem:pseudoinverse-leverage`
- `statement:thm:geometry-certification`
- `statement:lem:oracle-fitting-remainder`
- `statement:thm:matched-frontier`
- `statement:prop:benchmark-reductions`
- `definition:def:class-oracle`
- `definition:def:oracle-estimator`
- `symbol-add:\Lambda_\Sigma`
- `symbol-add:c_\Sigma`
- `symbol-add:\Omega_{G,\Sigma,k,s}`
- `symbol-add:R_{G,\Sigma,k}^{\dagger}`
- `symbol-add:C_{G,\Sigma,k}^{\dagger}`
- `definition-add:def:empirical-extrapolation-modulus`
- `statement-replace:lem:pseudoinverse-leverage`
- `statement-replace:thm:geometry-certification`
- `statement-replace:lem:selector-geometry`
- `statement-replace:lem:oracle-fitting-remainder`
- `statement-replace:thm:matched-frontier`
- `statement-replace:prop:benchmark-reductions`
- `definition-replace:def:class-oracle`
- `definition-replace:def:oracle-estimator`
- `bibliography-replace:HirshbergMalekiZubizarreta2021`
- `bibliography-replace:AudibertTsybakov2011`
- `bibliography-replace:Gaiffas2005`
- `estimand-functional-replace:metadata:estimand-functional`
- `comparator-promise-table-replace:metadata:comparator-promise-table`

Exact rejected selectors: **none**.

CLI recommendation:

```text
--all --note "Accepted the synchronized round-3 atomic post-image: the finite-support benchmark now restores i.i.d. groups, positive stayer and mover probabilities, bounded second moments, eligible mover support separated from the rank-loss locus, and explicit cross-fitted generated-Gamma control. All previously accepted R*/Rdagger, minimax-linear, prose, metadata, and source deltas remain coherent."
```
