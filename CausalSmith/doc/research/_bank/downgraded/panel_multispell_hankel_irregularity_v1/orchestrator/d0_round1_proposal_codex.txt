D0 round-1 adjudication — `panel_multispell_hankel_irregularity/v1`

Overall verdict: **REJECT AS BUNDLED; do not apply the current packet.**

All 13 submitted variants are mathematically acceptable in substance:

- `statement:lem:pseudoinverse-leverage`
- `statement:thm:geometry-certification`
- `statement:thm:matched-frontier`
- `statement:prop:benchmark-reductions`
- `definition:def:class-oracle`
- `definition:def:oracle-estimator`
- `statement-replace:lem:pseudoinverse-leverage`
- `statement-replace:thm:geometry-certification`
- `statement-replace:thm:matched-frontier`
- `statement-replace:prop:benchmark-reductions`
- `definition-replace:def:class-oracle`
- `definition-replace:def:oracle-estimator`
- `estimand-functional-replace:metadata:estimand-functional`

Rejected submitted IDs on individual mathematical merit: **none**. Existing required-core-edit mandates: **none**.

The provisional proofs support the proposed claims. The simultaneous-germ leverage condition fixes the erroneous single-divisor conclusion. Geometry certification is honestly narrowed to strict-margin semidecision. The matched-frontier and benchmark changes retain the novel resolution-invariant Hankel/local-zeta kernel and its deterministic oracle upper phase while leaving estimator attainment and reverse rates unresolved. The class-oracle cap is finite and contains every balancing power. The query bandwidth is total and mathematically defined; its positive floor and possible zero certified radius do not prove occupancy or the integrated fitting rate, which the proposal correctly leaves open. The estimand-functional replacement preserves the identifying limit and removes only the unsupported attainment claim. No assumption or class redefinition is introduced.

The atomic post-image nevertheless remains internally false because no prose update accompanies the semantic narrowing:

- `tldr` still says the feasible estimator attains uniform risk and honest-length bounds.
- `project_justification.fill` still says the oracle procedure attains those bounds and describes the negatively resolved lower-bound/adaptation questions as open.
- `honest_scope` still lists total oracle fitting and class-level risk/honest-length upper bounds as closed.

Mandatory repair: re-emit the identical 13 variants and provisional proofs, plus synchronized `prose_updates` for `tldr`, `project_justification.fill`, and `honest_scope`. Those fields must say that the closed result is the geometric/deterministic oracle upper phase; feasible fitted-estimator attainment and honest interval length await `lem:oracle-fitting-remainder`; universal reverse/minimax and within-signature adaptation claims have been resolved negatively, while richer subclasses remain possible.

Current CLI recommendation:

```text
--discard-all --note "Atomic post-image incomplete: tldr, project_justification.fill, and honest_scope still assert feasible estimator and honest-length attainment contradicted by the accepted matched-frontier narrowing and unresolved oracle-fitting lemma. Re-emit the same 13 variants with synchronized prose."
```

After that exact prose closure is regenerated and freshly reviewed, the appropriate selector is `--all`.
