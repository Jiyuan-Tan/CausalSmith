D0 MAXIMALITY VERDICT: one material scope correction exists; no sharper same-assumption rate or estimator theorem is currently justified.

The note’s fixed-signature class is not the class published by de Chaisemartin–D’Haultfœuille. It is a strict, highly regular subclass of their reduced-form random-coefficient experiment, and the paper should state that bridge explicitly.

### Class audit

I fetched the current lawful arXiv source:

- `https://export.arxiv.org/e-print/2508.07808`
- Server filename: `arXiv-2508.07808v3.tar.gz`
- Source file: `Vol1_ch11_text.tex`
- SHA-256: `f4171defb15dd6f0fa1ce379b3e703dcd613f3e61e344a488f8b3938f540986e`

Verbatim source receipts:

```tex
% Vol1_ch11_text.tex:500--505
\begin{hyp} (Random coefficients distributed-lag linear model)
...
where $K<T-1$ is known. Moreover, $E[\|B\|^2]<\infty$.
```

```tex
% lines 565--574
\Delta \bm{Y} =  \Gamma+ \bm{M} B + \bm{\eps},
\quad E[\bm{\eps} | \bm{M}]=0.
...
$E[\Pi(\bm{M})]$ is invertible.
```

```tex
% lines 586--588
\overline{\beta}_k:=E[\beta_k |e_k\in \text{Im}(\bm{M}')]
```

```tex
% lines 599--607
Suppose that Assumptions \ref{hyp:no_antic}, \ref{hyp:iid},
\ref{hyp:pt_nevertreated}, \ref{hyp:RC_model} and
\ref{hyp:design_RC} hold ...
```

```tex
\overline{\beta}_k = \lim_{C\uparrow \infty}
E\left[e'_k\bm{M}^+(\Delta Y -\Gamma)
 |e_k\in \text{Im}(\bm{M}'),
 \|\bm{M}'{}^+e_k\|\le C\right].
```

The source explicitly says that when \(T>2(K+1)\), the design condition may hold under continuous treatment “where there is no stayer” (line 614). It also allows component-specific eligibility and only imposes \(K<T-1\), not \(T\ge2K+2\).

By contrast, the note fixes:

```tex
% writeup.tex:159--161
\(T\ge 2K+2\).
...
\emph{Standard} (... \cite{deChaisemartinDHa2025}).
```

and its class at lines 233–236 requires positive stayer mass, an absolutely continuous mover law on one fixed compact semialgebraic support, two-sided density bounds, a certified normal-link geometry, \(q>4\) moments, two-sided conditional noise-variance bounds, and Whitney smoothness. Those are not the anchor’s published class.

The attribution of \(T\ge2K+2\) as the anchor’s “standard” condition is therefore false. It is the note’s sufficient specialization.

### Exact bridge that does hold

For every \(P\in\mathcal P_\Sigma\):

1. \(Q\) is not the zero polynomial: setting \(x_{K+1}=1\) and other increments to zero gives an identity \(p\times p\) row block because \(r\ge p\).
2. Full-dimensional \(S\) and absolute continuity imply \(Q(X)>0\) almost surely among movers.
3. Hence
   \[
   \{e_k\in\operatorname{Im}M(X)'\}=\{X\ne0\}
   \quad\text{a.s.}
   \]
4. The stayer atom gives
   \[
   E[\Pi(M(X))]\succeq \pi_0 I_r,
   \]
   so the anchor’s design condition holds.
5. \(M\) records every coordinate of \(X\) under \(r\ge p\), so the note’s \(E[\varepsilon\mid X]=0\) supplies the anchor’s reduced-form \(E[\varepsilon\mid M]=0\).
6. Therefore
   \[
   \tau_k=E[B_k\mid X\ne0]
   =E[\beta_k\mid e_k\in\operatorname{Im}M']
   =\overline\beta_k.
   \]

There is a finite-cutoff normalization difference: the anchor conditions on \(H_k\le C\), whereas the note uses an indicator while retaining the mover denominator. If \(p_C=P(H_k\le C\mid X\ne0)\), the note’s truncated functional is \(p_C\) times the anchor’s. Since \(p_C\to1\), their limits coincide on \(\mathcal P_\Sigma\).

The note should be called a strict specialization of the anchor’s induced reduced-form experiment, not the published potential-outcome class itself. Its assumptions do not directly include the anchor’s no-anticipation and parallel-trends hypotheses.

### Converse transfer

A positive lower bound proved on a quantitatively rich subclass with the same target would transfer to the larger published experiment by minimax monotonicity. The present note has no such positive converse.

Its zero-coefficient witness and negative “no universal lower bound across all frozen signatures” theorem do not transfer upward: enlarging a degenerate subclass can restore target variation, and \(R^\star\) is not defined on the anchor’s unrestricted class. Upper bounds on \(\mathcal P_\Sigma\) also do not transfer to the larger class.

The note cannot simply restate its atlas over the published class: that class includes finite support, arbitrary non-finite support, no-stayer overidentified designs, and component-specific partial eligibility, none of which supplies the fixed density/tube geometry used by the tail proof.

### Maximality audit

No same-assumption rate, construction, or elbow improvement survived the materiality test:

- The \(\alpha>2\), \(\alpha=2\), and \(0<\alpha<2\) phase split is correct.
- The critical logarithm \((\log G)^{m+1}/G\) is correctly retained.
- The irregular log exponent obtained by balancing
  \[
  G^{-1}C^{2-\alpha}(\log C)^m
  \quad\text{and}\quad
  C^{-2\zeta}(\log C)^{2\ell}
  \]
  is correct.
- The scalar corrected \(G^{-4/5}\), uncorrected \(G^{-2/3}\), and \(K=1,T=5\) critical \(G^{-1}\log G\) statements are correctly one-sided absent a nonvanishing modulus.
- The finite-support result is honestly external to \(\mathcal P_\Sigma\).
- The decision history correctly rejected local-polynomial attainment because the normal-link record does not yield uniform retained-set occupancy or empirical Gram control.
- Nothing in the anchor’s broader assumptions supplies that missing control. Thus \(R^\dagger\lesssim R^\star\) cannot be closed lawfully, and the minimax-linear \(R^\dagger\) construction is the maximal currently supported estimator result.

### One scoped D0 directive

```text
--require-core-target prop:published-source-bridge
--require-core-target ass:at-least-square
--require-core-target def:fixed-signature-class

Add one source-class bridge proposition and make no rate, estimator, or oracle
change. State and prove that, for every P in P_Sigma, Q(X)>0 mover-a.s.,
{e_k in Im M(X)'}={X!=0} a.s., E[Pi(M(X))] >= pi_0 I, and therefore the
induced reduced-form law satisfies de Chaisemartin--D'Haultfoeuille's design
condition and has bar_beta_k=tau_k. Record the finite-C normalization
difference and prove only equality of the limits.

State explicitly that P_Sigma is a strict compact-semialgebraic,
full-rank-mover, positive-stayer, bounded-density/moment/variance,
Whitney-smooth specialization of the published reduced-form experiment, not
the paper's published potential-outcome class. Retag T>=2K+2 as this paper's
sufficient at-least-square specialization; the source assumes only K<T-1 plus
E[Pi(M)] invertible and allows no-stayer overidentified designs and
component-specific eligibility.

Do not restate the tail atlas over the anchor's unrestricted class. Do not
claim that the zero-coefficient negative theorem transfers to that class.
Explain that only a future positive lower bound on a target-equivalent rich
subclass would transfer upward by minimax monotonicity. Preserve the exact
Rstar/Rdagger separation and the open Omega transport problem.
```

Core/prose synchronization required:

- Core: `ass:at-least-square`, `def:fixed-signature-class`, new `prop:published-source-bridge`, `sampling_model`, `target_estimand`, `estimand_functional`, the `deChaisemartinDHa2025` comparator row, `related_work`, `technical_internal_limitation`, and `honest_scope`.
- Writeup: TL;DR first sentence, Related Work’s anchor sentence, Setup’s target/functional paragraph, the justification under `ass:at-least-square`, a paragraph immediately after `def:fixed-signature-class`, and the Technical Limitation/Honest Scope sections.
- No changes to `thm:tail-atlas`, `lem:oracle-fitting-remainder`, `thm:matched-frontier`, the benchmark rates, or either negative theorem.

Expected tier after this correction: a strong specialist econometric-theory/algebraic-statistics paper centered on the target-specific Hankel tail atlas, but not a flagship matched-minimax paper while \(R^\dagger\lesssim R^\star\), a rich-subclass converse, and honest length remain open.
