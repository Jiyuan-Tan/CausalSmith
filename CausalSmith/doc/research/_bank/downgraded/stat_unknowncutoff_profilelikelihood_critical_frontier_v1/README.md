---
qid: stat_unknowncutoff_profilelikelihood_critical_frontier
spec: v1
topic: "Revive parent stat_unknowncutoff_weaklate_frontier by characterizing and attaining the all-strength minimax expected length of connected honest intervals for fuzzy-RD LATE when both the compliance jump and unknown cutoff weaken. Preserve the parent’s binary, uniform-design, separate one-sided Hölder class. Replace its refuted affine moving-window localizer with certified support-wide marginal-likelihood profiling that retains unrestricted D-Y association. Derive the complete s=1 critical cutoff/effect-label experiment and decision value, an all-s total honest profile interval, and matching location-mixture and slope-kink converses. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A legal moving-cutoff continuum with q=1/4, c_t=1/2+t kappa/q, constant treatment-side jump kappa, affine outcome-side branches, independent observed D,Y, and an explicit monotone latent lift yields theta=t and KL(P_t^n,P_0^n)<=32 n kappa^3 |t|. This proves the missing min{1,1/(n kappa^3)} converse and 2[1-alpha-4 sqrt(n kappa^3)]_+ <= L_n* <= 2(1-alpha), so L_n* tends to 2(1-alpha) whenever n kappa^3 tends to zero; hence every fixed-s>1 location-entropy critical window is now exactly nonshrinking. The parent’s denominator-fallback containment was also repaired, and no collision was found. UNRESOLVED BOTTLENECK: Prove a uniform profile-to-decision-value theorem with certified discretization, fallback, and calibration under unrestricted allowed D-Y association; solve the nontrivial s=1 critical window, its truncated mixture and second-order coordinates, and establish full-model least favorability on finite positive n kappa^3 faces. EARLY KILL TEST: Reproduce the continuum causal lift, then solve increasingly fine certified whole-class population profile programs on both the parent’s affine counterexample and the new moving-cutoff family at s=1 and s=2; a persistent false-cutoff optimum or failure of the kappa^2|c-c'| gap stops that route. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_unknowncutoff_profilelikelihood_critical_frontier.md"
novelty_target: field
banked_novelty_tier: incremental
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "Continuum-uniform honesty and L_n^star(kappa_n) asymp r_n attainment on R_cert remain an original multi-crux open program; the delivered paper proves sound lower envelopes and restricted calibration only."
reusable: unknown
reraise_status: true-negative
gap_reasons:
  - "The promised total honest profile interval with all-strength minimax attainment is still an explicit open obligation; the delivered kernel is a computable lower frontier plus grid-aligned calibration, not the headline upper/attainment result."
  - "Proving continuum-uniform honesty and L_n^star(kappa_n) asymp r_n attainment on R_cert with sieve, transfer, stochastic false-split, calibration, and fallback controls is faithful but is the original multi-crux open program, not a bounded D0 repair; a crux-encoding assumption is forbidden."
  - "As defined over the declared strength shell kappa_n<=1/8, c_t=1/2+4t kappa_n is not in [1/4,3/4] (e.g. t=1, kappa_n=1/8 gives c_t=1); qualify this definition/witness by kappa_n<=1/16 or restrict t before calling P_t causal-valid."
reusable_artifacts:
  - discovery/writeup.tex
  - discovery/core.json
  - discovery/solve_thm_integrated_and_bump_lower_frontier.json
  - discovery/solve_thm_flat_face_exact_value.json
  - discovery/solve_prop_regime_exhaustion.json
  - discovery/solve_thm_known_cutoff_reduction.json
  - discovery/solve_oeq_all_strength_profile.json
seeds_burned: []
proof_attempt_summary: |
  The run constructed causal-valid moving- and fixed-cutoff continua, proved finite-sample KL/TV controls, explicit integrated lower envelopes, a global all-strength lower bound, flat-face nonshrinkage, population false-split separation, and singular-cell-safe finite-grid calibration. It did not establish continuum-uniform honesty, stochastic profile and fallback control, an attaining interval, or the exact finite-positive critical experiment; those missing pieces form the original multi-crux upper/attainment program rather than a bounded repair. The delivered lower and structural results are sound but support only an incremental contribution below the field floor.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 33133031
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 33133031
  total_tokens_consumed: null
banked_on: "2026-09-10"
---

# stat_unknowncutoff_profilelikelihood_critical_frontier / v1 — Downgraded

**Topic.** Revive parent stat_unknowncutoff_weaklate_frontier by characterizing and attaining the all-strength minimax expected length of connected honest intervals for fuzzy-RD LATE when both the compliance jump and unknown cutoff weaken. Preserve the parent’s binary, uniform-design, separate one-sided Hölder class. Replace its refuted affine moving-window localizer with certified support-wide marginal-likelihood profiling that retains unrestricted D-Y association. Derive the complete s=1 critical cutoff/effect-label experiment and decision value, an all-s total honest profile interval, and matching location-mixture and slope-kink converses. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A legal moving-cutoff continuum with q=1/4, c_t=1/2+t kappa/q, constant treatment-side jump kappa, affine outcome-side branches, independent observed D,Y, and an explicit monotone latent lift yields theta=t and KL(P_t^n,P_0^n)<=32 n kappa^3 |t|. This proves the missing min{1,1/(n kappa^3)} converse and 2[1-alpha-4 sqrt(n kappa^3)]_+ <= L_n* <= 2(1-alpha), so L_n* tends to 2(1-alpha) whenever n kappa^3 tends to zero; hence every fixed-s>1 location-entropy critical window is now exactly nonshrinking. The parent’s denominator-fallback containment was also repaired, and no collision was found. UNRESOLVED BOTTLENECK: Prove a uniform profile-to-decision-value theorem with certified discretization, fallback, and calibration under unrestricted allowed D-Y association; solve the nontrivial s=1 critical window, its truncated mixture and second-order coordinates, and establish full-model least favorability on finite positive n kappa^3 faces. EARLY KILL TEST: Reproduce the continuum causal lift, then solve increasingly fine certified whole-class population profile programs on both the parent’s affine counterexample and the new moving-cutoff family at s=1 and s=2; a persistent false-cutoff optimum or failure of the kappa^2|c-c'| gap stops that route. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_unknowncutoff_profilelikelihood_critical_frontier.md

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** The promised total honest profile interval with all-strength minimax attainment is still an explicit open obligation; the delivered kernel is a computable lower frontier plus grid-aligned calibration, not the headline upper/attainment result.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

The moving-cutoff witness is used only for `kappa_n <= 1/16`; the fixed-cutoff family handles larger strengths in the proved global lower theorem. A future revival should reuse the integrated-envelope artifacts above and focus exclusively on the continuum-honest upper construction and exact critical experiment.
