---
qid: panel_rarelink_cancellation
spec: v1
topic: "Rare-exceedance cancellation geometry for normal-link distributional DiD. For the fifteen fixed pre/post pairs in the Djuazon--Tsyawo crime-free DTT aggregate, use predeclared disjoint block-role samples and write each of four exceedance probabilities as rho c_j on a fixed comparable-rarity box. Prove that the complete probit-versus-Poisson error factors as rho Q A C_rho, where A=log(c1/c3)log(c2/c3), C_rho<0, and u^2 C_rho=-1+O(u^-2) uniformly including the exact cancellation planes. Derive the pairwise and aggregate drifting-cancellation Gaussian frontier, bounded-count product-Poisson limits, and a zero-count-safe finitely terminating rational outer confidence interval with simultaneous coverage and the stated expected-diameter order. PRESOLVE EVIDENCE REQUIRING VERIFICATION: An explicit inverse-Mills integral remainder yields the uniform mixed-derivative expansion without dividing by A. Exact-plane, opposite-sign, approaching-plane, all-zero, all-full, unequal-share, and fifteen-pair cases were checked. A rational-grid construction with an explicit global Lipschitz certificate preserves coverage and diameter order; bounded searches found no same-theorem collision. The published 37-treated-block sample forces the conservative aggregate interval to include zero, so no finite-sample power claim is made. UNRESOLVED BOTTLENECK: Independently prove the common-cutoff inverse-tail and mixed-derivative constants uniformly over the full coefficient box, closing K4-U on both cancellation planes. EARLY KILL TEST: Certify the mixed derivative as one log ratio tends to zero and verify all-zero retention plus the all-full empty-list output for n_j at most six; failure should pivot or stop. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/panel_rarelink_cancellation.md."
novelty_target: field
banked_novelty_tier: incremental
tier_at_proposal: ACCEPT
tier_at_derivation: REJECT
proposal_promise_gap: "The sound split-role theory remains incremental; field-tier relevance would require a new overlapping-block inference architecture."
reusable: unknown
reraise_status: true-negative
gap_reasons:
  - "Stage 0.5 after the authorized bounded soundness-convergence pass remains below the novelty floor: achieved tier=incremental < floor=field, paper_score_ceiling=6.0 < 7.4, and salvageable=false."
  - "No further D0 repair is authorized or warranted: the bounded soundness pass is consumed, and no in-scope change can raise the incremental result to the field floor."
  - "The tier-lifting route requires a different overlapping-block sampling architecture and is outside this scope."
reusable_artifacts:
  - "discovery/core.json — closed 15-statement theorem graph, including the global signed-rectangle factorization, fixed-compact Gaussian-tail transfer, Gaussian and product-Poisson frontiers, and self-contained Bernoulli CLT."
  - "discovery/paper.tex — sound split-role rare-event derivation and conservative rational outer-interval construction, with the overlapping-block limitation explicit."
seeds_burned:
  - index: 0
    one_liner: "seed:exact-cancellation-coefficient"
    reason: "The sole rare-link cancellation angle was fully strengthened and soundness-repaired; reaching field requires a different overlapping-block sampling architecture outside the claim scope."
proof_attempt_summary: |
  Discovery proved the global two-plane cancellation factorization and a fixed-compact Gaussian-tail transfer, then used them to close the split-role Gaussian frontier, bounded-count product-Poisson transition, and conservative rational confidence construction. A bounded validity repair made all limit and sampling premises explicit, replaced the underspecified CLT citation with a self-contained proof, exposed the observed-law functional, and left all 15 graph statements proved. The result remains incremental because the motivating reused-block panel is not covered; a field-tier advance would require a new overlapping-block sampling and covariance architecture.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 29850022
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 29850022
  total_tokens_consumed: null
banked_on: "2026-09-14"
---

# panel_rarelink_cancellation / v1 — Downgraded

**Topic.** Rare-exceedance cancellation geometry for normal-link distributional DiD. For the fifteen fixed pre/post pairs in the Djuazon--Tsyawo crime-free DTT aggregate, use predeclared disjoint block-role samples and write each of four exceedance probabilities as rho c_j on a fixed comparable-rarity box. Prove that the complete probit-versus-Poisson error factors as rho Q A C_rho, where A=log(c1/c3)log(c2/c3), C_rho<0, and u^2 C_rho=-1+O(u^-2) uniformly including the exact cancellation planes. Derive the pairwise and aggregate drifting-cancellation Gaussian frontier, bounded-count product-Poisson limits, and a zero-count-safe finitely terminating rational outer confidence interval with simultaneous coverage and the stated expected-diameter order. PRESOLVE EVIDENCE REQUIRING VERIFICATION: An explicit inverse-Mills integral remainder yields the uniform mixed-derivative expansion without dividing by A. Exact-plane, opposite-sign, approaching-plane, all-zero, all-full, unequal-share, and fifteen-pair cases were checked. A rational-grid construction with an explicit global Lipschitz certificate preserves coverage and diameter order; bounded searches found no same-theorem collision. The published 37-treated-block sample forces the conservative aggregate interval to include zero, so no finite-sample power claim is made. UNRESOLVED BOTTLENECK: Independently prove the common-cutoff inverse-tail and mixed-derivative constants uniformly over the full coefficient box, closing K4-U on both cancellation planes. EARLY KILL TEST: Certify the mixed derivative as one log ratio tends to zero and verify all-zero retention plus the all-full empty-list output for n_j at most six; failure should pivot or stop. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/panel_rarelink_cancellation.md.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** REJECT

**Banking reason.** Stage 0.5 after the authorized bounded soundness-convergence pass remains below the novelty floor: achieved tier=incremental < floor=field, paper_score_ceiling=6.0 < 7.4, and salvageable=false.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
