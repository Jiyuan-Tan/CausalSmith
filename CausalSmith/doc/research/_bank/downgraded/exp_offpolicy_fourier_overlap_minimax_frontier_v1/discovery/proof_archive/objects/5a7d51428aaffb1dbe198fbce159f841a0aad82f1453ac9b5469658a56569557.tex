The proved finite statements give
\[
 \operatorname{LB}(\nu)\le R^{\star}\le F=\mathcal D\le\operatorname{UB}(a)
\]
for every genuine prior \(\nu\) and feasible linear rule \(a\), and the clipped minimizing incidence rule has worst-case risk at most \(F\).  If the prior-rounding inequality \(\mathcal B(\nu)\ge c_0\mathcal D\) were established with one numerical \(c_0>0\), these displays would immediately yield \(c_0F\le R^{\star}\le F\), uniformly over every row and every admissible triangular array.  No such lower comparison is derived from the frozen primitives.