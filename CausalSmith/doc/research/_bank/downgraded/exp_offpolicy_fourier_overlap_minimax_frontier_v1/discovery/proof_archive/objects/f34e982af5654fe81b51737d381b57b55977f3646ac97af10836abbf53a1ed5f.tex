The proved hub-rate lemma supplies the exact sandwich
\[
 \frac{n-1}{n^2}\bigl((1-p)q^2+p(1-q)^2\bigr)
 \le R_n^{\star}\le F_n
 \le \frac{1+(n-1)\bigl((1-q)^2/(1-p)+q^2/p\bigr)}{n^2},
\]
and therefore \(F_n=\Theta(n^{-1})\) and \(R_n^{\star}=\Theta(n^{-1})\) under fixed interior \(p,q\).  The remaining comparator clause cannot be checked from the frozen core because \(B_n^{\mathrm{LE}}\) and \(D_n^{\mathrm{LE}}\) have only descriptive symbol entries, not the anchor's defining formulas.