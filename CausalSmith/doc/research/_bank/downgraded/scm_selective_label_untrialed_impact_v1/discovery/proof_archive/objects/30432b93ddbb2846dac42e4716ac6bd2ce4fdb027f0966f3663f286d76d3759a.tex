Let \(d_i=b_i-p_\dagger\), and retain the frozen row order.  Exact multiplication of the five displayed generators gives
\[
\frac85d_3+\frac45(d_4+d_{13}+d_{14}+d_{30})
=\frac{-8\mathbf e_7+2\mathbf e_8+2\mathbf e_9+2\mathbf e_{11}+2\mathbf e_{12}}5
=:z_1^\star.
\]
Thus the frozen vector with \(\mathbf e_{11}\), rather than the directed replacement with \(\mathbf e_{10}\), is the vector reached by the proposed tangent representation.  For \(r_1^\star=g_1-z_1^\star\), direct multiplication by every tangent generator gives
\[
\begin{split}
((r_1^\star)^\top d_i)_{i=1}^{32}={}&(-1/2,-1/2,0,0,0,0,-1/2,-1/2,
0,0,-1/2,-1/2,0,0,-1/2,-1/2,\\
&-1/2,-3/10,-1/2,-3/10,-1/2,-1/2,-1/2,-1/2,
-1/2,-1/2,-1/2,-1/2,0,0,-1/2,-1/2),
\end{split}
\]
and \((r_1^\star)^\top z_1^\star=0\).  Since every entry is nonpositive and the coefficients in the displayed representation of \(z_1^\star\) are nonnegative, the elementary projection inequality proves \(z_1^\star=\Pi_{T_{\mathfrak O^{32}}(p_\dagger)}g_1\).

The two signed type-weight directions required by the latest directive satisfy
\[
\begin{split}
\dot w_1^L&=-\frac45\mathbf f_3+\frac45\mathbf f_4-\frac{12}{5}\mathbf f_5
+\frac45(\mathbf f_{13}+\mathbf f_{14}+\mathbf f_{30}),\\
\dot w_1^U&=-\frac45\mathbf f_3+\frac45\mathbf f_4-\frac{12}{5}\mathbf f_9
+\frac45(\mathbf f_{13}+\mathbf f_{14}+\mathbf f_{30}).
\end{split}
\]
They have zero mass, but exact incidence multiplication yields
\[
B^{32}\dot w_1^L=B^{32}\dot w_1^U=z_1^\star,
\]
not the vector with \(\mathbf e_{10}\).  Their objectives are respectively \(12/5\) and \(0\).  With
\[
\lambda^C=-2\mathbf e_1-2\mathbf e_4+2\mathbf e_8+2\mathbf e_{11}+2\mathbf e_{12},
\qquad
(\alpha^D,\lambda^D)=(1,-2\mathbf e_3),
\]
the lower slack is \(-1\) exactly on
\[
\{1,2,9,10,11,12,17,18,19,20,25,26,27,28\}
\]
and zero elsewhere, while the upper slack is zero on the support needed for the upper certificate and nonnegative everywhere.  Both duals attain the endpoint values at \(p_\dagger\), and
\[
(\lambda^C)^\top z_1^\star=\frac{12}{5},
\qquad
(\lambda^D)^\top z_1^\star=0.
\]
The primal--dual sandwich therefore proves \(D_L(g_1)=12/5\), \(D_U(g_1)=0\), and \(H(g_1)=12/5\) for the frozen \(\mathbf e_{11}\) certificate.

By contrast, let
\[
\widetilde z_1=\frac{-8\mathbf e_7+2\mathbf e_8+2\mathbf e_9+2\mathbf e_{10}+2\mathbf e_{12}}5.
\]
Although \((g_1-\widetilde z_1)^\top\widetilde z_1=0\), the fourteenth polar inequality fails:
\[
(g_1-\widetilde z_1)^\top d_{14}=\frac15>0.
\]
Because \(d_{14}\in T_{\mathfrak O^{32}}(p_\dagger)\), the first-order projection inequality rules out \(\widetilde z_1=\Pi_{T_{\mathfrak O^{32}}(p_\dagger)}g_1\).  Moreover, for the directed lower dual
\[
\widetilde\lambda^C=-2\mathbf e_1-2\mathbf e_4+2\mathbf e_8+2\mathbf e_{10}+2\mathbf e_{12},
\]
the slack is \(-1\) exactly on
\[
\{1,2,9,10,11,12,14,16,17,19,25,26,27,28\},
\]
not on the directed set.  Finally, for \(\lambda^A=2\mathbf e_6+2\mathbf e_{11}\), the lower slack is \(-1\) exactly on
\[
\{9,10,11,12,13,15,25,26,27,28,29,30\},
\]
so it is zero, rather than \(-1\), at columns \(14\) and \(16\).  These exact contradictions are independent and prevent certification of the requested \(\mathbf e_{10}\) replacement without first changing the frozen table or correcting the directive.