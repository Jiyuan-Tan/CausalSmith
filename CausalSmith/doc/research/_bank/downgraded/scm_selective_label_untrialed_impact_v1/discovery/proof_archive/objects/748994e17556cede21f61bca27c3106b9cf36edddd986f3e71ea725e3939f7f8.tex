At a fixed \(p\in\mathfrak O\), write \((D_L,D_U)=\Gamma'_p\), and refine the active projection cones and the extreme slopes of \(\Lambda_L(p)\) and \(\Lambda_U(p)\) by their zero and comparison hyperplanes.  Let \(\mathcal R_p\) be the resulting finite pool of nonzero restrictions to \(V_p\), and let \(\mathcal A_p\) be the finite family of all matrices obtained by stacking any ordered subset of \(\mathcal R_p\cup(-\mathcal R_p)\), without repeated rows.  This is closed under every row stacking, row selection, and rowwise sign change needed by the Boolean and inclusion--exclusion lower-orthant events.  For \(M\in\mathcal A_p\), set \(\Omega_M=M\Sigma_pM^{\top}\) and \(r_M=\operatorname{rank}(\Omega_M)\), and use the pointwise constant
\[
C(p)=\max\left\{0,\max_{M\in\mathcal A_p:r_M>0}(42r_M^{1/4}+16)
\sum_{o\in\mathcal O}p_o\|\Omega_M^{\dagger/2}M(\mathbf e_o-p)\|_2^3\right\}.
\]
The implementable schedules are \(m_n=\max\{1,\lfloor n^{1/3}\rfloor\}\) and \(\eta_n=\min\{\alpha/2,n^{-1/12}\}\).  In \(\mathfrak O^{32}\), verify the simultaneous projection/LP tie at
\[
p_\dagger=\tfrac12(B^{32}_{\cdot,3}+B^{32}_{\cdot,5})
=\tfrac14(\mathbf e_2+\mathbf e_3+2\mathbf e_7)
\]
by exact tangent representations, all thirty-two polar products, orthogonality, and matching rational primal/dual endpoint certificates for \((B^{32},h^{32})\) at \(g_0=-\mathbf e_2-3\mathbf e_3+4\mathbf e_7\) and \(g_1=\mathbf e_2+\mathbf e_3-2\mathbf e_7\).  The resulting Gaussian law must have \(0<\Pr\{\max(D_L(G_{p_\dagger}),-D_U(G_{p_\dagger}))=0\}<1\).