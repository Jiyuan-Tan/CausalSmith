Use the row order in \(\text{def:complete-32-type-table}\).  For \(z=0\), neutral invariance makes the \(r=0\) triple constant.  If that constant is zero, counterfactual correctness forces the incorrect \(r=1\) triple to be \(000\); if it is one, incorrect-performance order permits exactly
\[
000,\quad100,\quad110,\quad111.
\tag{19}
\]
The table uses neutral triple \(111\) and all four triples in (19); the omitted neutral-zero case has the same observable column and middle target as its displayed \(000\) representative.  For \(z=1\), the neutral \(r=0\) triple is incorrect and constant.  If it is one, correctness forces the correct \(r=1\) triple to be \(111\); if it is zero, correct-performance order permits exactly
\[
000,\quad001,\quad011,\quad111.
\tag{20}
\]
The table uses neutral triple \(000\) and all four triples in (20); the omitted neutral-one case is quotient-equivalent to the displayed \(111\) representative.  This verifies every shape restriction in \(\text{def:legal-response-types}\) row by row and proves completeness of the outcome quotient.

Only \(A_{(1,q_\ell)}\) and \(A_{(1,q_h)}\) enter the trial law, while the target uses only \(Y_{(1,q_m)}\).  The four table patterns exhaust \((A_{(1,q_\ell)},A_{(1,q_h)})\in\{0,1\}^2\); all other action coordinates are irrelevant to \((B_{\cdot,t},h_e(t))\).  Hence there are exactly \(2\cdot4\cdot4=32\) distinct observable--target quotient classes.  Each arm has two masked \(a=0\) cells and four revealed \(a=1\) cells, and inspection of the table shows that all twelve cells occur.  Thus the listed columns generate the full observable polytope and omit no column that can change any endpoint.

For \(\pi_3=\pi_2\), direct multiplication of \(w^{32}(\pi)\) by the two half-incidence entries in each table column gives
\[
\begin{split}
p(\pi)=\bigl(&\pi_4/6,\pi_4/6,\pi_4/24,\pi_2/2+\pi_4/24,\pi_4/8,
\pi_1/2+\pi_2/2-\pi_4/24,\\
&\pi_4/6,\pi_4/6,\pi_4/8,\pi_2/2-\pi_4/24,\pi_4/24,
\pi_1/2+\pi_2/2+\pi_4/24\bigr)^{\top},
\end{split}
\tag{21}
\]
and
\[
K^{32}p(\pi)=\bigl(\pi_4/6,\pi_4/6,\pi_2/2+\pi_4/12,
\pi_1/2+\pi_2/2+\pi_4/12,\pi_4/6,\pi_4/6,
\pi_2/2+\pi_4/12,\pi_1/2+\pi_2/2+\pi_4/12\bigr)^{\top}.
\tag{22}
\]
In the full and blind row orders define
\[
\lambda_L=(0,0,0,0,0,2,0,0,0,0,2,0)^{\top},
\]
\[
\lambda_U=(0,0,-2,0,0,0,2,2,2,0,2,2)^{\top},
\]
\[
\mu_L=(0,2,0,2,-2,0,-2,0)^{\top},\qquad
\mu_U=(2,2,2,2,0,0,0,0)^{\top}.
\tag{23}
\]
The monotone lists (19)--(20) imply, for every quotient column \(i\),
\[
\lambda_L^{\top}B^{32}_i\leq h^{32}_i\leq\lambda_U^{\top}B^{32}_i.
\tag{24}
\]
The lower expression is the indicator of the low cell \((1,1,1)\) plus the indicator of the high cell \((1,1,0)\).  For \(z=0\), the upper expression equals \(1-\mathbf 1\{A_{(1,q_\ell)}=1,Y_{(1,q_\ell)}=0\}\); for \(z=1\), it equals \(1-\mathbf 1\{A_{(1,q_h)}=1,Y_{(1,q_h)}=0\}\).  Similarly, columnwise,
\[
\mu_L^{\top}K^{32}B^{32}_i
=Y_{(1,q_\ell)}+Y_{(1,q_h)}-1\leq h^{32}_i,
\qquad
h^{32}_i\leq1=\mu_U^{\top}K^{32}B^{32}_i.
\tag{25}
\]
Using mass multiplier \(\alpha=0\), (24)--(25) are feasible certificates for the four dual programs.  Substitution of (21)--(22) gives by weak duality
\[
L\geq\pi_1+\pi_2,\quad U\leq\pi_1+\pi_2+\pi_4,
\quad L_{\mathrm{blind}}\geq\pi_1,\quad U_{\mathrm{blind}}\leq1.
\tag{26}
\]

We give attaining primal certificates.  Notation \(\{i:x,\ldots\}\) lists nonzero coordinates.  Put \(r_1=(1,0,0)\), \(r_2=(0,1,0)\), and \(r_3=(0,1,8)\) in \((\pi_1,\pi_2,\pi_4)\)-coordinates:
\[
\begin{array}{c|c|c|l}
X&v_1^X&v_2^X&v_3^X\\\hline
FL&\{32:1\}&\{20:1,32:1\}&
\{3:2/3,6:2,7:2,14:2/3,18:1/3,21:2/3,22:5/3,23:5/3,31:1/3\}\\
FU&\{32:1\}&\{20:1,32:1\}&
\{3:2/3,10:2,11:2,14:2/3,18:1/3,25:2/3,26:5/3,27:5/3,31:1/3\}\\
BL&\{32:1\}&\{8:1,24:1\}&
\{5:8/3,8:7/3,21:8/3,24:7/3\}\\
BU&\{32:1\}&\{12:1,28:1\}&
\{9:8/3,12:7/3,25:8/3,28:7/3\}.
\end{array}
\tag{27}
\]
Adding the two half-incidence entries of every listed column verifies
\[
B^{32}v_k^{FL}=B^{32}v_k^{FU}=p(r_k),\qquad
K^{32}B^{32}v_k^{BL}=K^{32}B^{32}v_k^{BU}=K^{32}p(r_k).
\tag{28}
\]
The masses for \(k=1,2,3\) are \(1,2,10\), and their target sums are
\[
\begin{array}{c|ccc}
X&h^{\top}v_1^X&h^{\top}v_2^X&h^{\top}v_3^X\\\hline
FL&1&1&1\\ FU&1&1&9\\ BL&1&0&0\\ BU&1&2&10.
\end{array}
\tag{29}
\]
For \(X\in\{FL,FU,BL,BU\}\), set
\[
v^X(\pi)=\pi_1v_1^X+\left(\pi_2-\frac{\pi_4}{8}\right)v_2^X
+\frac{\pi_4}{8}v_3^X.
\tag{30}
\]
The coefficients are nonnegative by \(\pi\in\mathcal P_{32}\), and the total mass is
\[
\pi_1+2(\pi_2-\pi_4/8)+10\pi_4/8
=\pi_1+2\pi_2+\pi_4=1,
\tag{31}
\]
where the last equality uses \(\pi_3=\pi_2\) and \(\sum_{k=1}^4\pi_k=1\).  Linearity and
\[
\pi=\pi_1r_1+(\pi_2-\pi_4/8)r_2+(\pi_4/8)r_3
\tag{32}
\]
give feasibility in (28).  Equations (29)--(32) yield the four reverse inequalities to (26), and hence
\[
L=\pi_1+\pi_2,\qquad U=\pi_1+\pi_2+\pi_4,\qquad
L_{\mathrm{blind}}=\pi_1,\qquad U_{\mathrm{blind}}=1.
\tag{33}
\]
At \(\pi^{32}=(1,1,1,1)^{\top}/4\), (33) gives \([1/2,3/4]\) versus \([1/4,1]\), and (21) gives the stated vector \(p^{32}\).  At \(\pi^{32,\circ}=(198,199,199,204)^{\top}/800\), it gives \([397/800,601/800]\) versus \([99/400,1]\), and (21) gives the stated vector \(p^{32,\circ}\).

Finally,
\[
\varepsilon_{32}=\pi_4/24>0,
\qquad a_{32}=\pi_2-\pi_4/8>0,
\qquad b_{32}=\pi_1+a_{32}>0.
\tag{34}
\]
Thus \(w^{32}(\pi)\) assigns positive mass to every one of the thirty-two generators.  If a supporting affine functional is nonnegative on every generator and vanishes at their strictly positive combination, it vanishes at every generator; hence the combination lies in the relative interior of their convex hull.  Applying this observation to the two displayed choices of \(\pi\) proves \(p^{32},p^{32,\circ}\in\operatorname{relint}(\mathfrak O^{32})\).  Finally, \(\text{thm:finite-response-converse}\) converts every primal certificate above into an attaining legal SCM, so the four computed endpoints are causal sharp endpoints rather than merely regression-program values.