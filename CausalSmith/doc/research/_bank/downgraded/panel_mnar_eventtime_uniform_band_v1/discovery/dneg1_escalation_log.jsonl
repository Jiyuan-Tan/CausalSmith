{"angle":0,"version":2,"directive":"Repair the sole remaining D-0.5 correctness defect without changing the topic or weakening the theorem: replace def:cy-base-class and def:cy-event-class as per-n probability-model classes containing asymptotic arrows by classes of complete triangular arrays with common constants and explicit finite-n rate envelopes. Define the n-th slice of each array class before defining Delta_n^* and before every uniform infimum or supremum, so all finite-n optimization domains and shared constants are well-defined. Propagate that correction through thm:uniform-multiplier-band and oeq:attainable-remainder-frontier. Also recheck the exact primary/supplement support for heteroskedastic and variance-replication assumptions; if the inaccessible source does not substantiate a horizon-uniform augmentation, present it as a novel assumption/result with its own justification rather than attributing it to the source. Preserve the already repaired exact shared-cell covariance condition, the Xia--Yan--Wainwright comparator, and the precise remainder-modulus coverage-failure order. Do not reintroduce a per-n class with asymptotic membership.","note":"angle-action:continue"}
{"angle":0,"version":3,"directive":"D-1 REVISE (retain field tier). Demote `thm:staircase-covariance` to a supporting algebraic lemma: after `c`, `Q^sh`, and `Ω` are defined, the covariance formula, deletion of zero products, and the nonzero-iff signed-sum statement are identities under the already-stated cell-noise conditions, not an independent root theorem. Do not manufacture a “stronger” theorem or add assumptions to preserve its headline status; re-root the substantive chain at the uniform vector influence representation, the uniform feasible covariance-rate theorem, and the growing-`K_n` multiplier-band theorem, keeping the two-horizon `1/3` calculation as a normalization/noncancellation witness. Repair variance estimation by separating the influence-cell set `Q_n^IF` from the residual-training set `V_n={(i,t):t∈H_i}` and defining the joint analysis domain `Q_n^+=Q_n^IF∪V_n`; state the centeredness, joint independence, sub-Gaussian, and variance bounds on every cell actually used, while keeping `Ω` and the multiplier sum over `Q_n^IF` (equivalently, use zero coefficients off `Q_n^IF`), and make the leave-one-cell-out and replication-rate claims use `V_n` explicitly. Rewrite every Xia–Yan–Wainwright comparison to acknowledge their heteroskedastic pointwise/nonasymptotic CIs for general scalar weighted bilinear treatment-effect functionals; claim only the missing joint growing-`K_n` law, shared-cell covariance estimation, and simultaneous multiplier band, and do not claim that a fixed eligible event-time weighted functional is outside their scalar class without a proved non-subsumption result. Then audit the entire core, not only these flags: minimize each theorem’s hypotheses; ensure every stochastic/nuisance/training cell is in the domain of its assumptions; reconcile all `used_by`/`depends_on` edges; recheck cohort/unit-share coefficients, `c→Ω→s,D,a,Σ` studentization, plug-in/multiplier normalizations, the `1/3` witness, finite-slice suprema and common envelopes, and all comparator/novelty sentences against their cited results. Preserve the genuine crux—the uniform event-by-cell expansion, feasible growing covariance rate, and joint multiplier approximation—and do not replace it with a definitional identity or an assumption-restated conclusion.","note":"angle-action:continue"}
