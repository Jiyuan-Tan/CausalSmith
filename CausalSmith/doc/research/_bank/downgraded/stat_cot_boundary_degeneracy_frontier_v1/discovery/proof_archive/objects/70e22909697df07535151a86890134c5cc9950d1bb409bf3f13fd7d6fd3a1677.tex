For \(d\ge1\), conditional squared Wasserstein distance has anisotropic boundary geometry: inverse ellipticity smooths outcome frequencies while covariate resolution replicates the ordered spectrum.  For a nonuniform common marginal, the orthogonal projection requires the finite Gram matrix \(G_{R,J_y,J_z,n}\), and the quadratic statistic must use \(B_{J_y,J_z,n}=G_{R,J_y,J_z,n}^{-1}H^{\mathrm{form}}_{J_y,J_z,n}G_{R,J_y,J_z,n}^{-1}\).  With this correction, the observed two-fold statistic uses clipped-and-normalized opposite-fold pilots, an oversampled ridge Galerkin inverse, and a diagonal-free second-order kernel.  The spectrum has Hilbert--Schmidt norm \(\asymp2^{dJ_z/2}\), outcome bias \(2^{-2(s_y+1)J_y}\), and covariate bias \(2^{-2s_zJ_z}\).  In the pilot-feasible region, balancing these scales yields the matched local frontier \(\min\{\rho^2,\rho n^{-1/2}+n^{-4s_z/(4s_z+d)}\}\) and elbow \(s_z=d/4\).  The local limit is a finite-\(n\) Gaussian-quadratic proxy that Gaussianizes because the theorem forces a diffuse spectrum; no nontrivial fixed-chaos transition is claimed.  The separate \(d=0\) branch retains its exact two-sample quantile-process law.  One confidence procedure honest across all regimes remains open.