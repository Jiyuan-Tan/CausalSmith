The missing object is a primitive smoothness-by-equality frontier that separates outcome elliptic smoothing from replicated covariate rank, uses the correct raw-moment coordinates under a nonuniform shared marginal, and recovers the unconditional \(d=0\) benchmark with its correct estimator and constants.