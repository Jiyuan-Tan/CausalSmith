For \(d\ge1\), define the deterministic remainder envelope
\[
E_{R,n}=C_B\left[
\varepsilon_{\mathrm{pil},n}\{2^{-(s_y+1)J_y}+2^{-s_zJ_z}\}
+\varepsilon_{\mathrm{pil},n}^{2+a_y}
+\rho^{2a_y/(2+3/s_y+d/s_z)}\varepsilon_{\mathrm{pil},n}^2
\right].
\]
Under the score-approximation premise of \(\mathrm{thm{:}mixed\text{-}spectral\text{-}transition}\), suppose a deterministic \(r_n^\circ=o(S_n/n)\) dominates in \(L^2\) the little-oh term in \(\mathrm{thm{:}uniform\text{-}second\text{-}order\text{-}expansion}\).  Let \(q_{n,1-\alpha/2}\) be the \((1-\alpha/2)\)-quantile of the absolute population Gaussian--quadratic proxy.  The interval centered at \(\widehat\theta^{\mathrm{an}}_{J_y,J_z,n}\) with half-width
\[
\frac{q_{n,1-\alpha/2}}{n}+C_\alpha\{E_{R,n}+r_n^\circ\}
\]
has population-calibrated asymptotic coverage at least \(1-\alpha\): Markov's inequality allocates probability at most \(\alpha/2\) to the remainder enlargement, while the bounded-Lipschitz approximation, its diffuse normal approximation, and normal anti-concentration transfer the proxy quantile with error \(o(1)\).  Its half-width is
\[
O\!\left(E_{R,n}+\frac{A_{J_y,J_z,n}}n+\sqrt{\frac{V_{L,n}}n}\right).
\]
At the corrected resolutions this is the certified point-estimation scale.  The construction is infeasible because the quantile uses the population Hessian, spectrum, local shifts, and linear variance.  The separate \(d=0\) theorem supplies the exact equality law and the local minimax rate, but not one observable calibration uniform from equality through regular alternatives.