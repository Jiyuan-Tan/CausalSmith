The current claim silently ranges over the boundary-wavelet bases used by both estimators and hard families without requiring enough regularity to characterize the fixed \(\mathcal C^{\beta_{\mathrm{iso}}}\) slice. The added theorem-level premise restricts only those fixed bases, not the law class or the bracket, and licenses the approximation and H\"older scaling steps for every \(0<\tau<\beta_{\mathrm{iso}}\).