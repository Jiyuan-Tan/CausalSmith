\documentclass{article}
\usepackage[margin=0.8in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{mathrsfs,bm,bbm}
\usepackage{graphicx,booktabs,array,multirow,tabularx,longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm,algpseudocode}
\usepackage{subcaption,tikz}
\usepackage{siunitx,cancel,accents}
\usepackage{microtype}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}
\setlength{\emergencystretch}{2em}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{openendedquestion}{Open-ended Question}
\newtheorem{conjecture}{Conjecture}
\newcommand{\coreref}[1]{\ref{#1}}

\begin{document}

\sloppy

\section*{exp\_\allowbreak{}operational\_\allowbreak{}critical\_\allowbreak{}diffusion}

\noindent\textit{Specialization:} v1\par

\paragraph{TL;DR.} For equal-arm stationary homogeneous marked-history service experiments, the paper derives a compact-uniform joint occupation-and-rate-score Gaussian law from exact finite-chain Poisson martingales. Evaluating the exact stationary product law at estimated rates while holding implemented capacity fixed gives fitted-center variance \(Q=A^{\mathsf T}\Gamma A\), residual variance \(W=V-Q\), and uniformly valid Wald coverage for arbitrary joint divergence of \(N\) and \(T_N\). The proved kink residual makes the diffusion center an \(O(N^{-1})\)-accurate corollary under \(T_N=o(N)\). For capacity choice, the staffing radius \(G\) is an exact nonnegative rational design datum; explicit energy, tail, transcendental, and split-quadrature bounds construct a sound rational objective oracle on \([-G,G]\), and rational dyadic branch-and-bound returns a finite checkable certificate.

\section{Project justification}

\paragraph{Gap.} The operational-dosage anchor permits distinct fixed arm populations and capacity and gives separate fixed-system occupation and many-system fluid results. It does not furnish the joint critical array law, marked-rate covariance, exact fixed-capacity plug-in inference, or certified critical-capacity optimization proved here for the narrower equal-arm stationary homogeneous subclass.

\paragraph{Niche.} The finite-source birth--death product law makes the implemented finite-system effect exactly computable at candidate rates, while critical capacity creates a piecewise-diffusion score correction and a nontrivial kink. The same piecewise-Gaussian structure admits an explicit global energy modulus and a rational point evaluator whose quadrature is split exactly at the moving kink.

\paragraph{Fill.} The paper proves stationary diffusion and Poisson-energy limits, a primitive martingale joint central limit law, the exact product-law fitted-center Wald theorem without a horizon upper bound, the uniform kink residual, and the diffusion corollary under \(T_N=o(N)\). For an exact supplied rational staffing radius, it also constructs a sound model-specific rational objective oracle with finite receipts; the generic dyadic certificate theorem computes its stopping depth by rational comparison, and uniform diffusion-energy transfer gives \(\epsilon+o(1)\) finite-system regret.

\section{Related work}

Boutilier, Jonasson, Li, and Yoeli allow distinct fixed arm populations and service capacity and establish separate fixed-system occupation and many-system fluid results. This paper studies the narrower equal-arm stationary compact homogeneous marked-history critical subclass and derives a joint population-duration limit there; it does not claim that positive results transfer upward to the broader anchor class. Braverman and Dai and Braverman inform the piecewise-diffusion and prelimit-Poisson residual methods. Hafouta informs uniform triangular-array reasoning, although the exact martingale proof here needs no upper horizon exponent. Wang and Plechac and Lok inform score--Poisson covariance and projection interpretations; the model-specific fixed-capacity derivative and exact product-law elbow are derived here. Borst, Mandelbaum, and Reiman motivate square-root staffing. The certification layer here is model-specific: it proves the energy modulus, normalizer and tail bounds, rational elementary-function enclosures, and split interval quadrature needed to instantiate the separately proved generic branch-and-bound semantics.

\section{Setup}

Target (estimand / structural parameter): \(\theta_N=\mathbb E_{\rho_{0,N}}[K_{0,N}/N]-\mathbb E_{\rho_{1,N}}[K_{1,N}/N]\).
Primary functional / estimator / diagnostic: \(\widehat\theta_N=(NT_N)^{-1}\int_0^{T_N}\{K_{0,N}(t)-K_{1,N}(t)\}\,dt,\quad \sqrt{NT_N}(\widehat\theta_N-\theta_N)\Rightarrow\mathcal N(0,V);\quad \widetilde\theta_N=\vartheta_N(\widehat p;M_N),\quad \sqrt{NT_N}(\widetilde\theta_N-\theta_N)\Rightarrow\mathcal N(0,Q),\quad \sqrt{NT_N}(\widehat\theta_N-\widetilde\theta_N)\Rightarrow\mathcal N(0,W)\), with no upper restriction on \(T_N/N\) for the exact centers..
\begin{itemize}
  \item \texttt{N} --- \texttt{positive integer}; \(\mathbb N\); \texttt{participants per arm}
  \item \texttt{lambda} --- \texttt{positive real}; \((0,\infty)\); \texttt{undesirable-\allowbreak{}state entry rate}
  \item \texttt{tau} --- \texttt{positive real}; \((0,\infty)\); \texttt{natural recovery rate}
  \item \texttt{nu} --- \texttt{positive real}; \((0,\infty)\); \texttt{service-\allowbreak{}induced recovery rate}
  \item \texttt{p} --- \texttt{rate vector}; \((0,\infty)^3\)
  \par\smallskip\noindent\emph{Definition.} \(p=(\lambda,\tau,\nu)^{\mathsf T}\)
  \item \texttt{c} --- \texttt{positive real}; \((0,\infty)\); \texttt{lower rate bound}
  \item \texttt{C} --- \texttt{positive real}; \((0,\infty)\); \texttt{upper rate bound}
  \item \texttt{Pbox} --- \texttt{compact parameter set}; \(\mathbb R^3\)
  \par\smallskip\noindent\emph{Definition.} \(\mathcal P=[c,C]^3\)
  \item \texttt{G} --- \texttt{nonnegative rational}; \(\mathbb Q_{\geq0}\); \texttt{exact supplied staffing-\allowbreak{}window radius}
  \item \texttt{gamma} --- \texttt{real}; \(\mathbb R\); \texttt{nominal critical staffing offset}
  \item \texttt{TN} --- \texttt{positive real sequence}; \((0,\infty)^{\mathbb N}\); \texttt{observation horizon}
  \item \texttt{d} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(d=\lambda+\tau\)
  \item \texttt{D} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(D=\lambda+\tau+\nu\)
  \item \texttt{a0} --- \texttt{smooth parameter functional}; \(a_0:(0,\infty)^3\to(0,1)\)
  \par\smallskip\noindent\emph{Definition.} \(a_0(p)=\lambda/d\)
  \item \texttt{a} --- \texttt{smooth parameter functional}; \(a:(0,\infty)^3\to(0,1)\)
  \par\smallskip\noindent\emph{Definition.} \(a(p)=\lambda/D\)
  \item \texttt{MN} --- \texttt{integer}; \(\mathbb Z\); implemented treatment-arm staffing, proved interior for \(N\ge N_0\)
  \par\smallskip\noindent\emph{Definition.} \(M_N=\lceil Na(p)+\gamma\sqrt N\rceil\)
  \item \texttt{gammaN} --- \texttt{real}
  \par\smallskip\noindent\emph{Definition.} \(\gamma_N=(M_N-Na(p))/\sqrt N\)
  \item \texttt{EN} --- \texttt{finite state space}
  \par\smallskip\noindent\emph{Definition.} \(E_N=\{0,\ldots,N\}\)
  \item \texttt{K0N} --- \texttt{stationary continuous-\allowbreak{}time Markov chain}; \(D([0,\infty),E_N)\); \texttt{control undesirable-\allowbreak{}count path}
  \item \texttt{K1N} --- \texttt{stationary continuous-\allowbreak{}time Markov chain}; \(D([0,\infty),E_N)\); \texttt{treatment undesirable-\allowbreak{}count path}
  \item \texttt{Q0N} --- \texttt{generator}; \(\mathcal A_{0,N}:\mathbb R^{E_N}\to\mathbb R^{E_N}\)
  \par\smallskip\noindent\emph{Definition.} \((\mathcal A_{0,N}g)(k)=\lambda(N-k)[g(k+1)-g(k)]+\tau k[g(k-1)-g(k)]\), with unavailable boundary jumps omitted
  \item \texttt{Q1N} --- \texttt{generator}; \(\mathcal A_{1,N}:\mathbb R^{E_N}\to\mathbb R^{E_N}\)
  \par\smallskip\noindent\emph{Definition.} \((\mathcal A_{1,N}g)(k)=\lambda(N-k)[g(k+1)-g(k)]+[\tau k+\nu\min\{k,M_N\}][g(k-1)-g(k)]\), with unavailable boundary jumps omitted
  \item \texttt{rho0N} --- \texttt{probability mass function}; \(\Delta(E_N)\)
  \par\smallskip\noindent\emph{Definition.} \(\rho_{0,N}\mathcal A_{0,N}=0\)
  \item \texttt{rho1N} --- \texttt{probability mass function}; \(\Delta(E_N)\)
  \par\smallskip\noindent\emph{Definition.} \(\rho_{1,N}\mathcal A_{1,N}=0\)
  \item \texttt{thetaN} --- \texttt{real}; \texttt{implemented stationary operational effect}
  \par\smallskip\noindent\emph{Definition.} \(\theta_N=\mathbb E_{\rho_{0,N}}[K_{0,N}/N]-\mathbb E_{\rho_{1,N}}[K_{1,N}/N]\)
  \item \texttt{thetahatN} --- \texttt{real-\allowbreak{}valued statistic}
  \par\smallskip\noindent\emph{Definition.} \(\widehat\theta_N=(NT_N)^{-1}\int_0^{T_N}\{K_{0,N}(t)-K_{1,N}(t)\}\,dt\)
  \item \texttt{q0} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(q_0=\lambda\tau/d\)
  \item \texttt{q} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(q=\lambda(\tau+\nu)/D\)
  \item \texttt{L0} --- \texttt{diffusion generator}
  \par\smallskip\noindent\emph{Definition.} \((\mathcal L_0g)(x)=-dxg'(x)+q_0g''(x)\)
  \item \texttt{Lgamma} --- \texttt{diffusion generator}
  \par\smallskip\noindent\emph{Definition.} \((\mathcal L_\gamma g)(x)=[-dx-\nu\min\{x,\gamma\}]g'(x)+qg''(x)\)
  \item \texttt{pi0} --- \texttt{probability density}; \(\mathcal P(\mathbb R)\)
  \par\smallskip\noindent\emph{Definition.} \(\pi_0=\mathcal N(0,q_0/d)\)
  \item \texttt{pigamma} --- \texttt{probability density}; \(\mathcal P(\mathbb R)\)
  \par\smallskip\noindent\emph{Definition.} \(\pi_\gamma(x)=Z_\gamma^{-1}\exp[-Dx^2/(2q)]\) for \(x\le\gamma\), and \(\pi_\gamma(x)=Z_\gamma^{-1}\exp[-dx^2/(2q)-\nu\gamma x/q+\nu\gamma^2/(2q)]\) for \(x>\gamma\), where \(Z_\gamma\) normalizes the density
  \item \texttt{mgamma} --- \texttt{real}
  \par\smallskip\noindent\emph{Definition.} \(m_\gamma=\int_{\mathbb R}x\pi_\gamma(x)\,dx\)
  \item \texttt{h0} --- \texttt{centered function}
  \par\smallskip\noindent\emph{Definition.} \(h_0\) is the \(\pi_0\)-centered solution of \(-\mathcal L_0h_0=x\)
  \item \texttt{hgamma} --- \texttt{centered function}
  \par\smallskip\noindent\emph{Definition.} \(h_\gamma\) is the \(\pi_\gamma\)-centered solution of \(-\mathcal L_\gamma h_\gamma=x-m_\gamma\)
  \item \texttt{V0} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(V_0=2q_0\int_{\mathbb R}\{h_0'(x)\}^2\pi_0(x)\,dx=2\lambda\tau/d^3\)
  \item \texttt{Vgamma} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(V_\gamma=2q\int_{\mathbb R}\{h_\gamma'(x)\}^2\pi_\gamma(x)\,dx\)
  \item \texttt{V} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(V=V_0+V_\gamma\)
  \item \texttt{Clambda} --- \texttt{counting statistic}
  \par\smallskip\noindent\emph{Definition.} \(C_\lambda\) is the pooled number of undesirable-state entries in both arms on \([0,T_N]\)
  \item \texttt{Ctau} --- \texttt{counting statistic}
  \par\smallskip\noindent\emph{Definition.} \(C_\tau\) is the pooled number of marked natural recoveries in both arms on \([0,T_N]\)
  \item \texttt{Cnu} --- \texttt{counting statistic}
  \par\smallskip\noindent\emph{Definition.} \(C_\nu\) is the number of marked service recoveries in the treatment arm on \([0,T_N]\)
  \item \texttt{Elambda} --- \texttt{nonnegative real statistic}
  \par\smallskip\noindent\emph{Definition.} \(E_\lambda=\int_0^{T_N}(2N-K_{0,N}(t)-K_{1,N}(t))\,dt\)
  \item \texttt{Etau} --- \texttt{nonnegative real statistic}
  \par\smallskip\noindent\emph{Definition.} \(E_\tau=\int_0^{T_N}(K_{0,N}(t)+K_{1,N}(t))\,dt\)
  \item \texttt{Enu} --- \texttt{nonnegative real statistic}
  \par\smallskip\noindent\emph{Definition.} \(E_\nu=\int_0^{T_N}\min\{K_{1,N}(t),M_N\}\,dt\)
  \item \texttt{phat} --- \texttt{rate-\allowbreak{}vector estimator}; constructed in Definition \(\mathrm{rate\mbox{-}estimator}\)
  \item \texttt{H} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(H=\int_{\mathbb R}h_\gamma'(x)\pi_\gamma(x)\,dx\)
  \item \texttt{Gamma} --- \texttt{positive diagonal matrix}
  \par\smallskip\noindent\emph{Definition.} \(\Gamma=\operatorname{diag}\{\lambda/(2-a_0-a),\tau/(a_0+a),\nu/a\}\)
  \item \texttt{A} --- \texttt{three-\allowbreak{}dimensional column vector}
  \par\smallskip\noindent\emph{Definition.} \(A=((1-a_0)/d-(1-a)H,-a_0/d+aH,aH)^{\mathsf T}\)
  \item \texttt{Qproj} --- \texttt{nonnegative real}
  \par\smallskip\noindent\emph{Definition.} \(Q=A^{\mathsf T}\Gamma A\)
  \item \texttt{W} --- \texttt{nonnegative real}
  \par\smallskip\noindent\emph{Definition.} \(W=V-A^{\mathsf T}\Gamma A\)
  \item \texttt{gammhatN} --- \texttt{real-\allowbreak{}valued statistic}; constructed in Definition \(\mathrm{fitted\mbox{-}center}\)
  \item \texttt{thetatildeN} --- \texttt{real-\allowbreak{}valued statistic}; constructed in Definition \(\mathrm{fitted\mbox{-}center}\)
  \item \texttt{Vhat} --- \texttt{positive real-\allowbreak{}valued statistic}; constructed in Definition \(\mathrm{wald\mbox{-}intervals}\)
  \item \texttt{Qhat} --- \texttt{positive real-\allowbreak{}valued statistic}; constructed in Definition \(\mathrm{wald\mbox{-}intervals}\)
  \item \texttt{alpha} --- \texttt{real}; \((0,1)\); \texttt{test size}
  \item \texttt{Phi} --- \texttt{standard normal distribution function}; \(\Phi:\mathbb R\to(0,1)\)
  \item \texttt{zalpha} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(z_\alpha=\Phi^{-1}(1-\alpha/2)\)
  \item \texttt{cM} --- \texttt{positive real}; \texttt{normalized staffing cost}
  \item \texttt{cI} --- \texttt{positive real}; \texttt{interval-\allowbreak{}width cost}
  \item \texttt{Iemp} --- \texttt{random interval}; constructed in Definition \(\mathrm{wald\mbox{-}intervals}\)
  \item \texttt{Ifit} --- \texttt{random interval}; constructed in Definition \(\mathrm{wald\mbox{-}intervals}\)
  \item \texttt{J} --- \texttt{continuous objective}; \(J:\mathcal P\times[-G,G]\to\mathbb R\); constructed in Definition \(\mathrm{design\mbox{-}objective}\)
  \item \texttt{epsilon} --- \texttt{positive rational}; \(\mathbb Q_{>0}\); \texttt{certificate tolerance}
  \item \texttt{gammaepsilon} --- \texttt{real}; \([-G,G]\); \texttt{certified returned staffing offset}
  \item \texttt{Mclass} --- \texttt{class of triangular-\allowbreak{}array service experiments}; constructed in Definition \(\mathrm{model\mbox{-}class}\)
  \item \texttt{beta} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(\beta=\min\{c/(3C),2c/(C+2c)\}\)
  \item \texttt{N0} --- \texttt{positive integer}; \(\mathbb N\)
  \par\smallskip\noindent\emph{Definition.} \(N_0=\left\lceil\{(G+\sqrt{G^2+4\beta})/(2\beta)\}^2\right\rceil\)
  \item \texttt{ell} --- \texttt{arm index}; \(\{0,1\}\); \texttt{control or treatment arm}
  \item \texttt{aell} --- \texttt{arm-\allowbreak{}specific fluid center}
  \par\smallskip\noindent\emph{Definition.} \(a_\ell=a_0(p)\) for \(\ell=0\) and \(a_\ell=a(p)\) for \(\ell=1\)
  \item \texttt{sellN} --- \texttt{scaled-\allowbreak{}state map}; \(s_{\ell,N}:E_N\to\mathbb R\)
  \par\smallskip\noindent\emph{Definition.} \(s_{\ell,N}(k)=(k-Na_\ell)/\sqrt N\)
  \item \texttt{QaN} --- \texttt{stationary arm-\allowbreak{}count process}
  \par\smallskip\noindent\emph{Definition.} \(Q_{0,N}(t)=K_{0,N}(t)\) and \(Q_{1,N}(t)=K_{1,N}(t)\)
  \item \texttt{XaN} --- \texttt{scaled stationary process}
  \par\smallskip\noindent\emph{Definition.} \(X_{\ell,N}(t)=(Q_{\ell,N}(t)-Na_\ell)/\sqrt N=s_{\ell,N}(Q_{\ell,N}(t))\)
  \item \texttt{EaN} --- \texttt{scaled lattice}
  \par\smallskip\noindent\emph{Definition.} \(E_{\ell,N}=s_{\ell,N}(E_N)=\{(k-Na_\ell)/\sqrt N:k\in\{0,\ldots,N\}\}\)
  \item \texttt{AaN} --- \texttt{unscaled arm generator family}
  \par\smallskip\noindent\emph{Definition.} \(\mathcal A_{\ell,N}=\mathcal A_{0,N}\) for \(\ell=0\) and \(\mathcal A_{\ell,N}=\mathcal A_{1,N}\) for \(\ell=1\)
  \item \texttt{GaN} --- \texttt{scaled prelimit generator}; \(G_{\ell,N}:\mathbb R^{E_{\ell,N}}\to\mathbb R^{E_{\ell,N}}\)
  \par\smallskip\noindent\emph{Definition.} \((G_{\ell,N}f)(s_{\ell,N}(k))=(\mathcal A_{\ell,N}(f\circ s_{\ell,N}))(k)\) for \(k\in E_N\)
  \item \texttt{V0N} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(V_{0,N}\) is the exact stationary Poisson energy for \(x-\mathbb E_{\rho_{0,N}}X_{0,N}\) under \((G_{0,N},\rho_{0,N})\)
  \item \texttt{V1N} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(V_{1,N}\) is the exact stationary Poisson energy for \(x-\mathbb E_{\rho_{1,N}}X_{1,N}\) under \((G_{1,N},\rho_{1,N})\)
  \item \texttt{JN} --- \texttt{finite-\allowbreak{}system objective}; \(J_N:\mathcal P\times[-G,G]\to\mathbb R\)
  \par\smallskip\noindent\emph{Definition.} \(J_N(p,\gamma)=c_M\gamma+c_Iz_\alpha\sqrt{V_{0,N}(p)+V_{1,N}(p,\gamma)}\)
  \item \texttt{h1N} --- \texttt{lattice restriction of the diffusion Poisson solution}; \(h_{1,N}:E_{1,N}\to\mathbb R\)
  \par\smallskip\noindent\emph{Definition.} \(h_{1,N}(x)=h_{p,\gamma_N}(x)\) for \(x\in E_{1,N}\); its lift to counts is \(k\mapsto h_{1,N}(s_{1,N}(k))\)
  \item \texttt{X0} --- \texttt{stationary Ornstein-\allowbreak{}-\allowbreak{}Uhlenbeck diffusion}
  \par\smallskip\noindent\emph{Definition.} \(X_0\) is the stationary diffusion with generator \(\mathcal L_0\) and invariant density \(\pi_0\)
  \item \texttt{Xgamma} --- \texttt{stationary piecewise Ornstein-\allowbreak{}-\allowbreak{}Uhlenbeck diffusion}
  \par\smallskip\noindent\emph{Definition.} \(X_\gamma\) is the stationary diffusion with generator \(\mathcal L_\gamma\) and invariant density \(\pi_\gamma\)
  \item \texttt{m} --- \texttt{nonnegative integer}; \(\mathbb N_0\); \texttt{requested rational-\allowbreak{}oracle precision}
  \item \texttt{Ibox} --- \texttt{closed rational interval}; \(I=[a,b]\subseteq[-G,G],\ a,b\in\mathbb Q\)
  \item \texttt{Rcut} --- \texttt{positive rational}; \(\mathbb Q_{>0}\); \texttt{Gaussian-\allowbreak{}tail truncation radius}
  \item \texttt{xstar} --- \texttt{real}
  \par\smallskip\noindent\emph{Definition.} \(x^*=0\) for \(\gamma\ge0\) and \(x^*=-\nu\gamma/d\) for \(\gamma<0\)
  \item \texttt{BR} --- \texttt{positive real}
  \par\smallskip\noindent\emph{Definition.} \(B_R=\sqrt{D/(2\pi q)}\{2q/[d(R-|x^*|)]\}\exp[-d(R-|x^*|)^2/(2q)]\)
  \item \texttt{inputname} --- \texttt{rational interval name}
  \par\smallskip\noindent\emph{Definition.} \(\mathfrak r(m)=\prod_j[r^-_j(m),r^+_j(m)]\) has rational endpoints, contains \(p,c_M,c_I,\alpha,G\), is nested in \(m\), and has maximum width at most \(2^{-m}\)
  \item \texttt{Joracle} --- \texttt{rational interval objective oracle}; \(\mathfrak O(I,m)=[\underline J(I,m),\overline J(I,m)]\in\mathbb Q^2\); sound oracle satisfying Definition \(\mathrm{interval\mbox{-}oracle}\); its model-specific instance and finite receipts are constructed in Theorem \(\mathrm{certified\mbox{-}capacity\mbox{-}design}\)
  \item \texttt{LJ} --- \texttt{supplied positive rational}; \(\mathbb Q_{>0}\); \texttt{certified global modulus in the supplied sound interval-\allowbreak{}oracle contract}
  \item \texttt{Depsilon} --- \texttt{nonnegative integer}
  \par\smallskip\noindent\emph{Definition.} \(D_\epsilon=\min\{r\in\mathbb N_0:8GL_J\leq\epsilon2^r\}\), computed by testing the displayed rational inequality for \(r=0,1,2,\ldots\).
  \item \texttt{mepsilon} --- \texttt{nonnegative integer}
  \par\smallskip\noindent\emph{Definition.} \(m_\epsilon=\left\lceil\log_2\max\{1,8/\epsilon\}\right\rceil\)
  \item \texttt{Lepsilon} --- \texttt{rational}; \(\mathbb Q\); \texttt{certified global-\allowbreak{}objective lower bound}
  \item \texttt{Uepsilon} --- \texttt{rational}; \(\mathbb Q\); \texttt{certified incumbent upper bound}
  \item \texttt{certcover} --- \texttt{finite family of rational dyadic intervals}; \texttt{unpruned cover in the capacity certificate}
  \item \texttt{Cert} --- \texttt{finite rational certificate}; constructed in Definition \(\mathrm{capacity\mbox{-}certificate}\)
  \item \texttt{Mfit} --- \texttt{class of fitted-\allowbreak{}center triangular-\allowbreak{}array experiments}; constructed in Definition \(\mathrm{fitted\mbox{-}model\mbox{-}class}\)
  \item \texttt{Rloc} --- \texttt{positive real}; \((0,\infty)\); \texttt{fixed compact-\allowbreak{}state radius in generator convergence}
  \item \texttt{testf} --- \texttt{test function}; \(C_c^3(\mathbb R)\); \texttt{generator-\allowbreak{}convergence test function}
  \item \texttt{Kres} --- \texttt{positive real}; \((0,\infty)\); compact-uniform residual constant proved to exist in Theorem \(\mathrm{kink\mbox{-}residual\mbox{-}bound}\)
  \item \texttt{varthetaN} --- \texttt{smooth exact finite-\allowbreak{}chain effect map}; \(\vartheta_N:(0,\infty)^3\times\{1,\ldots,N-1\}\to\mathbb R\); \texttt{exact stationary treatment-\allowbreak{}control effect evaluated at a candidate rate vector and fixed implemented capacity}
  \item \texttt{thetadiffN} --- \texttt{real-\allowbreak{}valued statistic}; \texttt{diffusion fitted center retained only for the rate-\allowbreak{}restricted corollary}
  \par\smallskip\noindent\emph{Definition.} \(\overline\theta_N=a_0(\widehat p)-a(\widehat p)-N^{-1/2}m_{\widehat p,\widehat\gamma_N}\)
  \item \texttt{Idiff} --- \texttt{random interval}; diffusion-center Wald interval constructed in Definition \(\mathrm{wald\mbox{-}intervals}\)
\end{itemize}

\section{Assumptions}

\begin{assumption}[ass:rate-bounds]\label{ass:rate-bounds}
\(0<c<C<\infty\).
\par\smallskip\noindent\emph{Standard} (Compact interior parameter condition, \cite{NeweyMcFadden1994}).
\end{assumption}

\begin{assumption}[ass:rate-membership]\label{ass:rate-membership}
\(p\in\mathcal P=[c,C]^3\).
\par\smallskip\noindent\emph{Standard} (Compact interior parameter condition, \cite{NeweyMcFadden1994}).
\end{assumption}

\begin{assumption}[ass:staffing-window]\label{ass:staffing-window}
The staffing offset satisfies \(\gamma\in[-G,G]\), where the staffing radius \(G\in\mathbb Q_{\geq 0}\) is supplied exactly.
\par\smallskip\noindent\emph{Standard} (Square-root staffing window, \cite{BorstMandelbaumReiman2004}).
\end{assumption}

\begin{assumption}[ass:population-diverges]\label{ass:population-diverges}
\(N\to\infty\).
\par\smallskip\noindent\emph{Standard} (Many-system asymptotic regime, \cite{BravermanDai2017}).
\end{assumption}

\begin{assumption}[ass:horizon-diverges]\label{ass:horizon-diverges}
\(T_N\to\infty\).
\par\smallskip\noindent\emph{Novel.} Repeated marked monitoring over a growing trial horizon is natural for adherence services that generate daily or continuous event histories.
\end{assumption}

\begin{assumption}[ass:control-stationary-start]\label{ass:control-stationary-start}
\(K_{0,N}(0)\sim\rho_{0,N}\).
\par\smallskip\noindent\emph{Standard} (Stationary initialization for steady-state simulation, \cite{Whitt1992}).
\end{assumption}

\begin{assumption}[ass:treatment-stationary-start]\label{ass:treatment-stationary-start}
\(K_{1,N}(0)\sim\rho_{1,N}\).
\par\smallskip\noindent\emph{Standard} (Stationary initialization for steady-state simulation, \cite{Whitt1992}).
\end{assumption}

\begin{assumption}[ass:arm-independence]\label{ass:arm-independence}
\(K_{0,N}\perp K_{1,N}\).
\par\smallskip\noindent\emph{Standard} (Independent experimental arms, \cite{BoutilierJonassonLiYoeli2024}).
\end{assumption}

\begin{assumption}[ass:control-dynamics]\label{ass:control-dynamics}
The generator of \(K_{0,N}\) is \(\mathcal A_{0,N}\).
\par\smallskip\noindent\emph{Standard} (Finite-source control birth--death model, \cite{BoutilierJonassonLiYoeli2024}).
\end{assumption}

\begin{assumption}[ass:treatment-dynamics]\label{ass:treatment-dynamics}
The generator of \(K_{1,N}\) is \(\mathcal A_{1,N}\).
\par\smallskip\noindent\emph{Standard} (Capacity-constrained finite-source treatment model, \cite{BoutilierJonassonLiYoeli2024}).
\end{assumption}

\begin{assumption}[ass:marked-history]\label{ass:marked-history}
\texttt{Every observed recovery jump is labeled as natural or service-\allowbreak{}induced.}
\par\smallskip\noindent\emph{Novel.} Digital adherence platforms routinely log whether recovery followed ordinary self-verification or recorded sponsor outreach, so the mark is an operational event field rather than an oracle quantity.
\end{assumption}

\begin{assumption}[ass:staffing-cost-positive]\label{ass:staffing-cost-positive}
\(c_M>0\).
\par\smallskip\noindent\emph{Novel.} A positive staffing shadow price represents the actual resource cost of adding sponsor capacity in a service experiment.
\end{assumption}

\begin{assumption}[ass:information-cost-positive]\label{ass:information-cost-positive}
\(c_I>0\).
\par\smallskip\noindent\emph{Novel.} A positive information shadow price represents the planning value placed on shorter inferential intervals.
\end{assumption}

\begin{assumption}[ass:test-size]\label{ass:test-size}
\(\alpha\in(0,1)\).
\par\smallskip\noindent\emph{Standard} (Fixed nominal test size, \cite{vanDerVaart1998}).
\end{assumption}

\begin{assumption}[ass:large-enough-system]\label{ass:large-enough-system}
\(N\ge N_0\).
\par\smallskip\noindent\emph{Novel.} The explicit enrollment threshold guarantees that the critically scaled integer capacity is positive and below the finite population throughout the maintained rate and staffing boxes.
\end{assumption}

\begin{assumption}[ass:fitted-horizon]\label{ass:fitted-horizon}
\(T_N/N\to0\).
\par\smallskip\noindent\emph{Novel.} The fitted-center regime describes operational panels whose monitoring duration grows more slowly than enrollment; this is exactly the relation that makes an order \(N^{-1}\) diffusion-centering error negligible at the \(\sqrt{NT_N}\) inference scale.
\end{assumption}

\section{Definitions}

\begin{definition}[def:model-class]\label{def:model-class}
\par\smallskip\noindent\emph{Defined object.} \texttt{equal-\allowbreak{}arm stationary homogeneous critical finite-\allowbreak{}source experiment subclass}
\par\smallskip\noindent\emph{Construction.} \(\mathcal M\) is the equal-arm stationary homogeneous marked-history critical subclass
\[
 \{(N,p,\gamma,T_N,K_{0,N},K_{1,N})_{N\geq N_0}:
 \mathrm A_{\mathrm{rate\mbox{-}bounds}}\wedge
 \mathrm A_{\mathrm{rate\mbox{-}membership}}\wedge
 \mathrm A_{\mathrm{staffing\mbox{-}window}}\wedge
 \mathrm A_{\mathrm{population\mbox{-}diverges}}\wedge
 \mathrm A_{\mathrm{horizon\mbox{-}diverges}}\wedge
 \mathrm A_{\mathrm{large\mbox{-}enough\mbox{-}system}}\wedge
 \mathrm A_{\mathrm{control\mbox{-}stationary\mbox{-}start}}\wedge
 \mathrm A_{\mathrm{treatment\mbox{-}stationary\mbox{-}start}}\wedge
 \mathrm A_{\mathrm{arm\mbox{-}independence}}\wedge
 \mathrm A_{\mathrm{control\mbox{-}dynamics}}\wedge
 \mathrm A_{\mathrm{treatment\mbox{-}dynamics}}\wedge
 \mathrm A_{\mathrm{marked\mbox{-}history}}\}.
\]
Both arms contain the same fixed number \(N\) of participants at each array index.
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}rate-\allowbreak{}bounds}; \texttt{ass:\allowbreak{}rate-\allowbreak{}membership}; \texttt{ass:\allowbreak{}staffing-\allowbreak{}window}; \texttt{ass:\allowbreak{}population-\allowbreak{}diverges}; \texttt{ass:\allowbreak{}horizon-\allowbreak{}diverges}; \texttt{ass:\allowbreak{}large-\allowbreak{}enough-\allowbreak{}system}; \texttt{ass:\allowbreak{}control-\allowbreak{}stationary-\allowbreak{}start}; \texttt{ass:\allowbreak{}treatment-\allowbreak{}stationary-\allowbreak{}start}; \texttt{ass:\allowbreak{}arm-\allowbreak{}independence}; \texttt{ass:\allowbreak{}control-\allowbreak{}dynamics}; \texttt{ass:\allowbreak{}treatment-\allowbreak{}dynamics}; \texttt{ass:\allowbreak{}marked-\allowbreak{}history}.
\end{definition}

\begin{definition}[def:diffusion-poisson]\label{def:diffusion-poisson}
\par\smallskip\noindent\emph{Defined object.} \texttt{piecewise diffusion Poisson construction}
\par\smallskip\noindent\emph{Construction.} Set \(F_0(x)=\int_{-\infty}^xu\pi_0(u)\,du\) and \(h_0'(x)=-F_0(x)/(q_0\pi_0(x))\). Set \(F_\gamma(x)=\int_{-\infty}^x(u-m_\gamma)\pi_\gamma(u)\,du\) and \(h_\gamma'(x)=-F_\gamma(x)/(q\pi_\gamma(x))\). Choose each additive constant so its solution has stationary mean zero.
\par\smallskip\noindent\emph{Inputs.} \texttt{L0}; \texttt{Lgamma}; \texttt{pi0}; \texttt{pigamma}.
\end{definition}

\begin{definition}[def:residual-handle]\label{def:residual-handle}
\par\smallskip\noindent\emph{Defined object.} \texttt{kink-\allowbreak{}residual proof handle}
\par\smallskip\noindent\emph{Construction.} Restrict the piecewise-Gaussian solution to \(h_{1,N}\) on \(E_{1,N}\), split Taylor's integral remainder according to whether a lattice jump crosses \(\gamma_N\), and combine the exact product ratio \(\rho_{1,N}(k+1)/\rho_{1,N}(k)\) with compact-uniform polynomial tails.
\par\smallskip\noindent\emph{Inputs.} \texttt{GaN}; \texttt{Lgamma}; \texttt{h1N}; \texttt{pigamma}.
\end{definition}

\begin{definition}[def:rate-estimator]\label{def:rate-estimator}
\par\smallskip\noindent\emph{Defined object.} \texttt{marked exposure-\allowbreak{}rate estimator}
\par\smallskip\noindent\emph{Construction.} \(\widehat p=(C_\lambda/E_\lambda,C_\tau/E_\tau,C_\nu/E_\nu)^{\mathsf T}\), evaluated inside \([c/2,2C]^3\) for plug-in calculations and assigned fixed positive fallbacks when an exposure is zero.
\par\smallskip\noindent\emph{Inputs.} \texttt{K0N}; \texttt{K1N}; \texttt{MN}; \texttt{TN}.
\end{definition}

\begin{definition}[def:fitted-center]\label{def:fitted-center}
\par\smallskip\noindent\emph{Defined object.} \texttt{exact fixed-\allowbreak{}capacity finite-\allowbreak{}chain fitted center}
\par\smallskip\noindent\emph{Construction.} For \(p'=(\lambda',\tau',\nu')^{\mathsf T}\in(0,\infty)^3\) and \(1\leq M\leq N-1\), set \(w_{0,N}^{p'}(0)=w_{1,N}^{p',M}(0)=1\) and, for \(k\geq1\),
\[
 w_{0,N}^{p'}(k)=\prod_{j=0}^{k-1}\frac{\lambda'(N-j)}{\tau'(j+1)},
 \qquad
 w_{1,N}^{p',M}(k)=\prod_{j=0}^{k-1}
 \frac{\lambda'(N-j)}{\tau'(j+1)+\nu'\min\{j+1,M\}}.
\]
Normalize these weights to probability masses \(\rho_{0,N}^{p'}\) and \(\rho_{1,N}^{p',M}\), and define the exact finite-chain effect map
\[
 \vartheta_N(p';M)=\sum_{k=0}^N\frac{k}{N}
 \{\rho_{0,N}^{p'}(k)-\rho_{1,N}^{p',M}(k)\}.
\]
Holding the observed \(M_N\) fixed, set
\[
 \widehat\gamma_N=\frac{M_N-Na(\widehat p)}{\sqrt N},
 \qquad
 \widetilde\theta_N=\vartheta_N(\widehat p;M_N).
\]
For the diffusion-only corollary also define
\[
 \overline\theta_N=a_0(\widehat p)-a(\widehat p)
 -N^{-1/2}m_{\widehat p,\widehat\gamma_N}.
\]
\par\smallskip\noindent\emph{Inputs.} \texttt{phat}; \texttt{MN}.
\end{definition}

\begin{definition}[def:wald-intervals]\label{def:wald-intervals}
\par\smallskip\noindent\emph{Defined object.} \texttt{empirical,\allowbreak{} exact fitted-\allowbreak{}center,\allowbreak{} and diffusion-\allowbreak{}center Wald intervals}
\par\smallskip\noindent\emph{Construction.} Set
\[
 I_{\mathrm{emp}}=[\widehat\theta_N\pm z_\alpha\sqrt{\widehat V/(NT_N)}],
 \qquad
 I_{\mathrm{fit}}=[\widetilde\theta_N\pm z_\alpha\sqrt{\widehat Q/(NT_N)}],
 \qquad
 I_{\mathrm{diff}}=[\overline\theta_N\pm z_\alpha\sqrt{\widehat Q/(NT_N)}],
\]
where \(\widehat V\) and \(\widehat Q\) plug \((\widehat p,\widehat\gamma_N)\) into \(V\) and \(Q\), respectively.  Here \(I_{\mathrm{fit}}\) is centered at the exact finite-chain product-law plug-in effect, whereas \(I_{\mathrm{diff}}\) is reserved for the diffusion approximation.
\par\smallskip\noindent\emph{Inputs.} \texttt{thetahatN}; \texttt{thetatildeN}; \texttt{thetadiffN}; \texttt{phat}; \texttt{gammhatN}.
\end{definition}

\begin{definition}[def:design-objective]\label{def:design-objective}
\par\smallskip\noindent\emph{Defined object.} \texttt{capacity-\allowbreak{}-\allowbreak{}precision objective}
\par\smallskip\noindent\emph{Construction.} For the exact supplied staffing radius \(G\in\mathbb Q_{\geq0}\), define
\[
 J(p,\gamma)=c_M\gamma+c_Iz_\alpha\sqrt{V_0(p)+V_\gamma(p)},
 \qquad \gamma\in[-G,G].
\]
\par\smallskip\noindent\emph{Inputs.} \texttt{p}; \texttt{cM}; \texttt{cI}; \texttt{alpha}; \texttt{G}.
\end{definition}

\begin{definition}[def:certificate-algorithm]\label{def:certificate-algorithm}
\par\smallskip\noindent\emph{Defined object.} \texttt{oracle-\allowbreak{}conditional validated capacity search}
\par\smallskip\noindent\emph{Construction.} Conditional on a supplied interval oracle satisfying Definition \(\mathrm{interval\mbox{-}oracle}\), begin with the rational root interval \([-G,G]\).  Decide the case \(G=0\) by exact rational comparison.  If \(G>0\), enumerate \(r=0,1,2,\ldots\) using rational arithmetic until \(8GL_J\leq\epsilon2^r\); the first such integer is \(D_\epsilon\).  Apply the oracle to a fair dyadic branch-and-bound tree for \(J\).  At depth \(r\), bisect every surviving interval, evaluate every child and its midpoint at precision at least \(m_\epsilon+r\), retain a rational incumbent upper bound and an incumbent point \(\gamma_\epsilon\), and prune only an interval whose certified lower bound exceeds the incumbent.  If pruning has not already certified an objective gap at most \(\epsilon\), retain the full-level fallback partition through depth \(D_\epsilon\) and stop there.
\par\smallskip\noindent\emph{Inputs.} \texttt{Joracle}; \texttt{epsilon}; \texttt{G}.
\end{definition}

\begin{definition}[def:scaled-prelimit]\label{def:scaled-prelimit}
\par\smallskip\noindent\emph{Defined object.} \texttt{scaled state,\allowbreak{} lattice,\allowbreak{} and prelimit generator}
\par\smallskip\noindent\emph{Construction.} For \(\ell\in\{0,1\}\), set \(X_{\ell,N}(t)=s_{\ell,N}(Q_{\ell,N}(t))\), \(E_{\ell,N}=s_{\ell,N}(E_N)\), and \((G_{\ell,N}f)(s_{\ell,N}(k))=(\mathcal A_{\ell,N}(f\circ s_{\ell,N}))(k)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{QaN}; \texttt{AaN}; \texttt{sellN}.
\end{definition}

\begin{definition}[def:lattice-poisson]\label{def:lattice-poisson}
\par\smallskip\noindent\emph{Defined object.} \texttt{lattice restriction of the treatment diffusion Poisson solution}
\par\smallskip\noindent\emph{Construction.} Set \(h_{1,N}=h_{p,\gamma_N}|_{E_{1,N}}\); equivalently, its count-lattice lift is \(k\mapsto h_{p,\gamma_N}((k-Na(p))/\sqrt N)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{hgamma}; \texttt{gammaN}; \texttt{EaN}.
\end{definition}

\begin{definition}[def:fitted-model-class]\label{def:fitted-model-class}
\par\smallskip\noindent\emph{Defined object.} \texttt{exact fitted-\allowbreak{}center experiment class}
\par\smallskip\noindent\emph{Construction.} \(\mathcal M_{\mathrm{fit}}\) is the equal-arm stationary homogeneous marked-history class
\[
 \{(N,p,\gamma,T_N,K_{0,N},K_{1,N})_{N\geq N_0}:
 \mathrm A_{\mathrm{rate\mbox{-}bounds}}\wedge
 \mathrm A_{\mathrm{rate\mbox{-}membership}}\wedge
 \mathrm A_{\mathrm{staffing\mbox{-}window}}\wedge
 \mathrm A_{\mathrm{population\mbox{-}diverges}}\wedge
 \mathrm A_{\mathrm{horizon\mbox{-}diverges}}\wedge
 \mathrm A_{\mathrm{large\mbox{-}enough\mbox{-}system}}\wedge
 \mathrm A_{\mathrm{control\mbox{-}stationary\mbox{-}start}}\wedge
 \mathrm A_{\mathrm{treatment\mbox{-}stationary\mbox{-}start}}\wedge
 \mathrm A_{\mathrm{arm\mbox{-}independence}}\wedge
 \mathrm A_{\mathrm{control\mbox{-}dynamics}}\wedge
 \mathrm A_{\mathrm{treatment\mbox{-}dynamics}}\wedge
 \mathrm A_{\mathrm{marked\mbox{-}history}}\}.
\]
In particular, membership imposes no upper restriction on the relative growth of \(T_N\) and \(N\).
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}rate-\allowbreak{}bounds}; \texttt{ass:\allowbreak{}rate-\allowbreak{}membership}; \texttt{ass:\allowbreak{}staffing-\allowbreak{}window}; \texttt{ass:\allowbreak{}population-\allowbreak{}diverges}; \texttt{ass:\allowbreak{}horizon-\allowbreak{}diverges}; \texttt{ass:\allowbreak{}large-\allowbreak{}enough-\allowbreak{}system}; \texttt{ass:\allowbreak{}control-\allowbreak{}stationary-\allowbreak{}start}; \texttt{ass:\allowbreak{}treatment-\allowbreak{}stationary-\allowbreak{}start}; \texttt{ass:\allowbreak{}arm-\allowbreak{}independence}; \texttt{ass:\allowbreak{}control-\allowbreak{}dynamics}; \texttt{ass:\allowbreak{}treatment-\allowbreak{}dynamics}; \texttt{ass:\allowbreak{}marked-\allowbreak{}history}.
\end{definition}

\begin{definition}[def:input-name]\label{def:input-name}
\par\smallskip\noindent\emph{Defined object.} \texttt{supplied rational interval representation of design inputs}
\par\smallskip\noindent\emph{Construction.} A supplied rational interval name for the design inputs is a nested sequence
\[
 \mathfrak r(m)=\prod_j[r_j^-(m),r_j^+(m)],\qquad m\in\mathbb N,
\]
of boxes with rational endpoints containing \((p,c_M,c_I,\alpha,G)\), where the \(G\)-coordinate is the degenerate interval \([G,G]\) at every precision and \(G\in\mathbb Q_{\geq0}\) is therefore supplied exactly.  The boxes are nested, their maximum coordinate width is at most \(2^{-m}\), they preserve positive lower bounds for the rate and cost coordinates, and they certify \(0<\alpha<1\).  Thus \([-G,G]\) has rational endpoints and the predicate \(G=0\) is decidable by rational comparison.  This definition specifies supplied input data; it does not assert a procedure that constructs names for arbitrary real inputs.
\par\smallskip\noindent\emph{Inputs.} \texttt{p}; \texttt{cM}; \texttt{cI}; \texttt{alpha}; \texttt{G}.
\end{definition}

\begin{definition}[def:interval-oracle]\label{def:interval-oracle}
\par\smallskip\noindent\emph{Defined object.} \texttt{supplied validated objective-\allowbreak{}oracle contract}
\par\smallskip\noindent\emph{Construction.} Relative to a supplied input name whose staffing-radius coordinate is the exact rational interval \([G,G]\), a validated interval oracle consists of a global rational bound \(L_J\in\mathbb Q_{>0}\) and a map which, for every interval \(I=[a,b]\subseteq[-G,G]\) with \(a,b\in\mathbb Q\) and every \(m\in\mathbb N\), returns
\[
 \mathfrak O(I,m)=[\underline J(I,m),\overline J(I,m)]\in\mathbb Q^2.
\]
For every \(\eta\in I\), the return must satisfy
\[
 J(p,\eta)\in\mathfrak O(I,m),
 \qquad
 \overline J(I,m)-\underline J(I,m)
 \leq L_J\operatorname{diam}(I)+2^{-m},
\]
and the returned width for a point interval must be at most \(2^{-m}\).  The receipts and all rational inequalities are part of the oracle contract.  No oracle soundness follows merely from a Cauchy name or an unevaluated series description.
\par\smallskip\noindent\emph{Inputs.} \texttt{inputname}; \texttt{J}.
\end{definition}

\begin{definition}[def:capacity-certificate]\label{def:capacity-certificate}
\par\smallskip\noindent\emph{Defined object.} \texttt{oracle-\allowbreak{}relative checkable global capacity certificate}
\par\smallskip\noindent\emph{Construction.} Relative to a supplied oracle satisfying Definition \(\mathrm{interval\mbox{-}oracle}\) on the exact rational root \([-G,G]\),
\[
 \mathrm{Cert}=(\gamma_\epsilon,L_\epsilon,U_\epsilon,
 \{(I_r,\underline J_r,\overline J_r,m_r,\sigma_r)\}_{r=1}^R),
 \qquad \sigma_r\in\{\mathrm{active},\mathrm{pruned}\},
\]
is a finite rational dyadic certificate when every \(I_r\) has rational endpoints, each recorded receipt encloses \(J\) on \(I_r\), every pruned interval satisfies \(\underline J_r>U_\epsilon\), the active intervals cover every global minimizer, the incumbent point receipt at \(\gamma_\epsilon\) has upper endpoint \(U_\epsilon\), and
\[
 \mathcal C_\epsilon:=\{I_r:\sigma_r=\mathrm{active}\}
\]
is the certificate's unpruned cover.  In addition,
\[
 L_\epsilon=\min_{r:\sigma_r=\mathrm{active}}\underline J_r,
 \qquad U_\epsilon-L_\epsilon\leq\epsilon.
\]
The certificate is checkable relative to the supplied receipt contract; this definition alone does not implement an oracle.
\par\smallskip\noindent\emph{Inputs.} \texttt{Joracle}; \texttt{epsilon}; \texttt{G}.
\end{definition}

\section{Main results}

\begin{lemma}[lem:staffing-interior]\label{lem:staffing-interior}
Let \(\beta=\min\{c/(3C),2c/(C+2c)\}\) and \(N_0=\left\lceil\{(G+\sqrt{G^2+4\beta})/(2\beta)\}^2\right\rceil\). Uniformly for \(p\in\mathcal P\), \(\gamma\in[-G,G]\), and \(N\ge N_0\), \(1\le\lceil Na(p)+\gamma\sqrt N\rceil\le N-1\).
\end{lemma}
\begin{proof}
Put \(x=\sqrt N\).  By \(\mathrm A_{\mathrm{rate\mbox{-}bounds}}\) and \(\mathrm A_{\mathrm{rate\mbox{-}membership}}\),
\[
 a(p)=\frac{\lambda}{\lambda+\tau+\nu}
 \geq \frac{c}{3C}\geq\beta,
 \qquad
 1-a(p)=\frac{\tau+\nu}{\lambda+\tau+\nu}
 \geq\frac{2c}{C+2c}\geq\beta.                       \tag{1}
\]
The positive root of \(\beta x^2-Gx-1=0\) is
\[
 r=\frac{G+\sqrt{G^2+4\beta}}{2\beta}.
\]
By the definition of \(N_0\) and \(\mathrm A_{\mathrm{large\mbox{-}enough\mbox{-}system}}\), \(N\geq N_0\geq r^2\), whence \(x\geq r\) and
\[
 \beta N-G\sqrt N\geq1.                                      \tag{2}
\]
Using \(\mathrm A_{\mathrm{staffing\mbox{-}window}}\), equations (1)--(2) give
\[
 Na(p)+\gamma\sqrt N\geq \beta N-G\sqrt N\geq1              \tag{3}
\]
and
\[
 Na(p)+\gamma\sqrt N
 \leq N(1-\beta)+G\sqrt N
 =N-\{\beta N-G\sqrt N\}\leq N-1.                            \tag{4}
\]
Both endpoints in (3)--(4) are integers.  Therefore taking the ceiling preserves them:
\[
 1\leq \left\lceil Na(p)+\gamma\sqrt N\right\rceil\leq N-1.
\]
Every bound used only \((c,C,G)\), so the conclusion is uniform on the stated boxes.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The deterministic bound makes every finite-chain generator and staffing rule well-defined without hidden clipping. \textit{Gap.} The anchor invokes sufficiently large populations but does not state a compact-uniform enrollment threshold for the entire rate and staffing box. \textit{Consumer.} A certificate checker can reject infeasible enrollment values before evaluating the stochastic model.

\begin{theorem}[thm:diffusion-poisson]\label{thm:diffusion-poisson}
For every fixed compact time interval, uniformly over \(p\in\mathcal P\) and \(\gamma\in[-G,G]\), the stationary paths \(X_{0,N}\) and \(X_{1,N}\) converge in bounded-Lipschitz distance on Skorokhod space to the stationary Ornstein--Uhlenbeck process \(X_0\) and stationary piecewise Ornstein--Uhlenbeck process \(X_\gamma\). For every \(f\in C_c^3(\mathbb R)\) and every fixed \(R_{\mathrm{loc}}<\infty\), the corresponding scaled generators satisfy \(\sup_{x\in E_{0,N}\cap[-R_{\mathrm{loc}},R_{\mathrm{loc}}]}|(G_{0,N}-\mathcal L_0)f(x)|+\sup_{x\in E_{1,N}\cap[-R_{\mathrm{loc}},R_{\mathrm{loc}}]}|(G_{1,N}-\mathcal L_{\gamma_N})f(x)|\to0\). Their invariant laws converge compact-uniformly to \(\pi_0\) and \(\pi_\gamma\), the Poisson construction uniquely determines centered solutions with \(1/D\le h_\gamma'(x)\le1/d\), and \(V_{0,N}\to V_0\), \(V_{1,N}\to V_\gamma\) uniformly.
\end{theorem}
\begin{proof}
Write \(\delta=N^{-1/2}\).  Throughout the proof, a constant denoted by \(C_j\) may depend on \((c,C,G)\) and on a displayed compact state or time interval, but not on \(N,p,\gamma\).  This convention is valid by \(\mathrm A_{\mathrm{rate\mbox{-}bounds}}\), \(\mathrm A_{\mathrm{rate\mbox{-}membership}}\), and \(\mathrm A_{\mathrm{staffing\mbox{-}window}}\).  Lemma \(\mathrm{staffing\mbox{-}interior}\) ensures that the implemented capacity is an interior integer.

For the treatment arm put
\[
 x=\frac{k-Na}{\sqrt N},\qquad g=\gamma_N.
\]
The identities \(M_N=Na+\sqrt N g\) and
\(\lambda(1-a)=(\tau+\nu)a=q\), together with \(\mathrm A_{\mathrm{treatment\mbox{-}dynamics}}\), give the exact scaled drift
\[
 \delta\{\lambda(N-k)-\tau k-\nu\min(k,M_N)\}
 =-dx-\nu\min(x,g)=:b_g(x),                                  \tag{1}
\]
and the exact local second characteristic
\[
 \delta^2\{\lambda(N-k)+\tau k+\nu\min(k,M_N)\}
 =2q+\delta\{(\tau-\lambda)x+\nu\min(x,g)\}.                 \tag{2}
\]
Unavailable boundary jumps have rate zero, so (1)--(2) also hold at the endpoints.  For the control arm the corresponding identities, from \(\mathrm A_{\mathrm{control\mbox{-}dynamics}}\), are \(-dx\) and \(2q_0+\delta(\tau-\lambda)x\).

Taylor's formula with integral remainder, applied separately to the available jumps, now implies that for \(f\in C_c^3(\mathbb R)\) and fixed \(R_{\mathrm{loc}}<\infty\),
\[
 \begin{split}
 &\sup_{x\in E_{0,N}\cap[-R_{\mathrm{loc}},R_{\mathrm{loc}}]}
   |(G_{0,N}-\mathcal L_0)f(x)|\\
 &\quad+
 \sup_{x\in E_{1,N}\cap[-R_{\mathrm{loc}},R_{\mathrm{loc}}]}
   |(G_{1,N}-\mathcal L_g)f(x)|
 \leq C_f\delta.                                               \tag{3}
 \end{split}
\]
The ceiling identity gives
\[
 0\leq g-\gamma<\delta.                                       \tag{4}
\]
Since \(x\mapsto\min(x,g)\) is one-Lipschitz jointly in \(x\) and \(g\), (3)--(4) prove the asserted compact-uniform generator convergence with \(\mathcal L_{\gamma_N}\), and also with \(\mathcal L_\gamma\) after an error of order \(\delta\).

We next prove uniform stationary tightness without using a limit theorem.  Applying the treatment generator to \(x^2\) and using (1)--(2) gives
\[
 G_{1,N}x^2=2xb_g(x)+2q+\delta\{(\tau-\lambda)x+\nu\min(x,g)\}. \tag{5}
\]
By (4), \(|g|\leq G+1\) for \(N\geq1\).  If \(x\leq g\), then \(-2\nu x\min(x,g)=-2\nu x^2\leq0\).  If \(x>g\), its positive part is at most \(2\nu(G+1)|x|\).  The remaining term in (5) is at most \(C\delta(1+|x|)\).  Young's inequality and \(d\geq2c\) therefore yield
\[
 G_{1,N}x^2\leq-c_1x^2+C_1                                  \tag{6}
\]
for constants \(c_1,C_1>0\).  The control calculation is the same without the service term.  Stationarity gives \(\mathbb E_{\rho_{\ell,N}}G_{\ell,N}X_{\ell,N}^2=0\), so
\[
 \sup_{N,p,\gamma,\ell}\mathbb E_{\rho_{\ell,N}}X_{\ell,N}^2<\infty. \tag{7}
\]
More generally, expanding the two jumps in \(x^{2r}\), the term of degree \(2r\) is \(-2rdx^{2r}\) plus a nonpositive service contribution except for a term bounded by \(C_r|x|^{2r-1}\).  All lower jump terms are bounded by \(C_r(1+|x|^{2r-2})\).  Young's inequality and induction on \(r\) thus give
\[
 \sup_{N,p,\gamma,\ell}\mathbb E_{\rho_{\ell,N}}|X_{\ell,N}|^{2r}<\infty
 \quad(r\in\mathbb N).                                      \tag{8}
\]

The invariant law is now identified from detailed balance.  In the treatment arm,
\[
 \frac{\rho_{1,N}(k+1)}{\rho_{1,N}(k)}
 =\frac{\lambda(N-k)}{\tau(k+1)+\nu\min(k+1,M_N)}.            \tag{9}
\]
Uniformly for \(|x|\leq R\), expansion of the two logarithms about the common positive leading term \(qN\) gives
\[
 \log\frac{\rho_{1,N}(k+1)}{\rho_{1,N}(k)}
 =\delta\frac{b_g(x)}q+O_R(\delta^2).                          \tag{10}
\]
The remainder remains valid when \([x,x+\delta]\) crosses \(g\), because the minimum map is one-Lipschitz.  The density in the statement satisfies
\[
 q\{\log\pi_g\}'(x)=b_g(x)                                  \tag{11}
\]
on both sides of \(g\); the density and its first derivative match at \(g\).  Multiplying (10) along lattice edges and comparing the resulting sums with the integral of (11) shows that, for every fixed \(R\),
\[
 \sup_{x_k\in E_{1,N}\cap[-R,R]}
 \left|\delta^{-1}\rho_{1,N}(k)-\pi_g(x_k)\right|\longrightarrow0. \tag{12}
\]
To verify the normalization in this ratio argument, first choose \(R\) so that (7) leaves at most \(C/R^2\) mass outside \([-R,R]\), normalize the Riemann sum inside that interval, and then let \(R\to\infty\).  This also shows that no other subsequential invariant limit is possible.  Bound (8) upgrades (12) to convergence of every fixed polynomial moment.  The control invariant law is binomial with success probability \(a_0\), and the same ratio argument gives \(\pi_0\).  Compactness, (4), and the common Gaussian envelope of the displayed densities make all these conclusions uniform in \((p,\gamma)\), and replace \(\pi_g\) by \(\pi_\gamma\) in the limit.

We next pass from stationary laws to stationary paths.  Both drifts are globally Lipschitz, uniformly on the parameter box.  Let \(\zeta_R\) be the first exit time of the scaled chain from \([-R,R]\).  On the stopped interval, (1)--(2) bound the drift and local second characteristic uniformly.  The martingale decomposition of the coordinate, conditional isometry, and Cauchy--Schwarz therefore give, for every stopping time \(\sigma\leq T\) and \(0\leq u\leq1\),
\[
 \mathbb E\!left[
 |X_{\ell,N}((\sigma+u)\wedge\zeta_R)-X_{\ell,N}(\sigma)|^2
 \right]\leq C_R(u+u^2),                                    \tag{13}
\]
Stationarity and (8), applied to the maximum through the stopped martingale decomposition, imply
\[
 \lim_{R\to\infty}\sup_{N,p,\gamma}
 \mathbb P\!\left(\sup_{t\leq T}|X_{\ell,N}(t)|>R\right)=0.  \tag{14}
\]
Equations (13)--(14), by the elementary dyadic-modulus argument and Markov's inequality, give tightness on \(D([0,T],\mathbb R)\), and the jump size \(\delta\) forces every limit to have continuous paths.

For every compactly supported \(C^3\) function, the compensated generator identity is a martingale.  Uniform integrability follows from (8); hence (3) permits passage to any subsequential limit.  The limit solves the martingale equation for \(\mathcal L_0\) or \(\mathcal L_\gamma\).  Synchronous coupling gives, for two solutions driven by the same Brownian motion,
\[
 |X_t^x-X_t^y|\leq e^{-dt}|x-y|,                              \tag{15}
\]
so solutions with a given Brownian path and initial state are unique.  The invariant initial laws were identified in (12), and therefore every subsequence has the stationary limit stated in the theorem.  If uniform bounded-Lipschitz convergence failed, a failing parameter sequence would have a convergent subsequence by compactness; (3)--(15) would identify its unique limit and yield a contradiction.  This proves uniform path convergence on every fixed time interval.

It remains to identify the Poisson solutions and energies.  For either limiting diffusion write \(b,q_*,\pi\), and \(m=\int x\pi(x)\,dx\).  The stationary equation is \(q_*\pi'=b\pi\).  With
\[
 F(x)=\int_{-\infty}^x(u-m)\pi(u)\,du,
 \qquad h'(x)=-\frac{F(x)}{q_*\pi(x)},                         \tag{16}
\]
we have
\[
 q_*(\pi h')'=-(x-m)\pi,
 \qquad -\mathcal Lh=x-m.                                    \tag{17}
\]
Gaussian tails justify the integrations and the centering constant.  If two stationary-integrable centered solutions existed, their difference \(v\) would satisfy \(\mathcal Lv=0\), hence \((\pi v')'=0\) on both sides of the kink with matching flux.  Thus \(v'=C/\pi\).  If \(C\ne0\), the Gaussian tails make \(|v|\pi\) nonintegrable; therefore \(C=0\), and centering makes the remaining constant zero.

For the treatment diffusion, if \(y>x\), the secant slope of \(b_\gamma\) lies in \([-D,-d]\).  Under synchronous coupling,
\[
 e^{-Dt}(y-x)\leq X_t^y-X_t^x\leq e^{-dt}(y-x).                \tag{18}
\]
The resolvent representation
\[
 h_\gamma(x)=\int_0^\infty\{\mathbb E_xX_t-m_\gamma\}\,dt
\]
is justified by (18).  Divide its difference at \(y,x\) by \(y-x\), use (18), and let \(y\downarrow x\) to obtain
\[
 \frac1D\leq h_\gamma'(x)\leq\frac1d.                       \tag{19}
\]
For the control diffusion, (17) gives \(h_0(x)=x/d\).  Integration by parts in (17), with the Gaussian boundary terms vanishing, yields
\[
 2\int(x-m)h(x)\pi(x)\,dx
 =2q_*\int\{h'(x)\}^2\pi(x)\,dx.                             \tag{20}
\]
Thus \(V_0=2q_0/d^2=2\lambda\tau/d^3\), and (20) is the stated formula for \(V_\gamma\).

Finally, connect (20) with the exact finite systems.  For an arm with birth rates \(B_k\), death rates \(D_k\), invariant masses \(\rho_k\), and \(f_k=(k-\mathbb EK)/\sqrt N\), define
\[
 A_k=\sum_{i=0}^k\rho_i f_i,
 \qquad
 u_N(k+1)-u_N(k)=-\frac{A_k}{\rho_kB_k},
 \qquad \sum_k\rho_ku_N(k)=0.                                \tag{21}
\]
Detailed balance telescopes (21) to \(-\mathcal A_Nu_N=f\).  Its exact stationary energy is
\[
 V_N=2\sum_{k=0}^{N-1}\rho_kB_k\{u_N(k+1)-u_N(k)\}^2
 =2\sum_{k=0}^{N-1}\frac{A_k^2}{\rho_kB_k}.                   \tag{22}
\]
In the ordered basic coupling of chains started one count apart, the discrepancy disappears at rate
\[
 B_k-B_{k+1}+D_{k+1}-D_k\in[d,D].
\]
The finite-chain resolvent consequently gives
\[
 0\leq u_N(k+1)-u_N(k)\leq\frac1{d\sqrt N}.                   \tag{23}
\]
On compact scaled intervals, (12), polynomial-moment convergence, and (21) imply uniformly
\[
 \sqrt N\{u_N(k+1)-u_N(k)\}\longrightarrow h_\gamma'(x_k),   \tag{24}
\]
with limit \(d^{-1}\) in the control arm.  By (23) and the rate bound \(B_k+D_k\leq3CN\), the energy on edges incident to \(|x_k|>R\) is at most a constant times \(\rho_N(|X_N|>R-1)\); (8) makes this uniformly negligible as \(R\to\infty\).  Riemann summation of (22) on \([-R,R]\), followed by \(R\to\infty\), proves
\[
 \sup_{p,|\gamma|\leq G}|V_{0,N}-V_0|\to0,
 \qquad
 \sup_{p,|\gamma|\leq G}|V_{1,N}-V_\gamma|\to0.             \tag{25}
\]
Equations (3), (12), (15), (17), (19), and (25) establish every assertion of the theorem, including support, boundary, kink, and uniformity conditions.
\end{proof}
\par\smallskip\noindent \textit{Justification.} This theorem relates the actual scaled finite generators and stationary paths to the two limiting diffusions and makes the long-run variance evaluable. \textit{Gap.} Boutilier, Jonasson, Li, and Yoeli give a fluid limit but no critical stationary path or Poisson-energy limit; Braverman and Dai study a different many-server system and a static-law metric. \textit{Consumer.} Trial planners can compute the capacity-dependent precision curve without simulating each large finite chain.

\begin{theorem}[thm:uniform-joint-law]\label{thm:uniform-joint-law}
Over \(\mathcal M\), with no upper restriction on the relative growth of \(T_N\) and \(N\), uniformly in \((p,\gamma)\in\mathcal P\times[-G,G]\),
\[
 \left(\sqrt{NT_N}(\widehat\theta_N-\theta_N),
 \sqrt{NT_N}(\widehat p-p)^{\mathsf T}\right)^{\mathsf T}
 \Rightarrow
 \mathcal N\!\left(0,
 \begin{pmatrix}V&A^{\mathsf T}\Gamma\\ \Gamma A&\Gamma\end{pmatrix}\right),
\]
and \(I_{\mathrm{emp}}\) covers \(\theta_N\) with probability \(1-\alpha+o(1)\) uniformly.  This joint-array result is for the equal-arm, stationary, compact homogeneous, marked-history critical subclass.  The cited operational-dosage model permits distinct fixed arm populations and capacity and gives separate fixed-system and fluid limits, so no upward transfer of this positive result to that broader class is asserted.
\end{theorem}
\begin{proof}
All probabilities and expectations are under a member of \(\mathcal M\).  Thus both chains start in their invariant laws and are independent.  Put
\[
 f_{\ell,N}(x)=x-\mathbb E_{\rho_{\ell,N}}X_{\ell,N},
 \qquad
 -G_{\ell,N}u_{\ell,N}=f_{\ell,N},
 \qquad
 \mathbb E_{\rho_{\ell,N}}u_{\ell,N}=0.
\]
Lemma \(\mathrm{finite\mbox{-}chain\mbox{-}poisson\mbox{-}controls}\) supplies existence, edge bounds, and moments.  Dynkin's formula gives the square-integrable martingale
\[
 M_{\ell,N}(t)=u_{\ell,N}(X_{\ell,N}(t))-u_{\ell,N}(X_{\ell,N}(0))
 -\int_0^tG_{\ell,N}u_{\ell,N}(X_{\ell,N}(s))\,ds,
\]
and hence
\[
 \int_0^{T_N}f_{\ell,N}(X_{\ell,N}(t))\,dt
 =M_{\ell,N}(T_N)
 -\{u_{\ell,N}(X_{\ell,N}(T_N))-u_{\ell,N}(X_{\ell,N}(0))\}. \tag{1}
\]
The edge bound makes \(u_{\ell,N}\) uniformly \(d^{-1}\)-Lipschitz on its scaled lattice.  Stationary centering and the uniform second moment of \(X_{\ell,N}\) therefore imply
\[
 \sup_{N,p,\gamma,\ell}
 \mathbb E_{\rho_{\ell,N}}u_{\ell,N}(X_{\ell,N})^2<\infty.    \tag{2}
\]
Since \(T_N\to\infty\), (1)--(2) give
\[
 \sqrt{NT_N}(\widehat\theta_N-\theta_N)
 =T_N^{-1/2}\{M_{0,N}(T_N)-M_{1,N}(T_N)\}+o_{\mathbb P}(1),   \tag{3}
\]
uniformly.

Define the compensated marked counts
\[
 U_{\lambda,N}=C_\lambda-\lambda E_\lambda,
 \quad
 U_{\tau,N}=C_\tau-\tau E_\tau,
 \quad
 U_{\nu,N}=C_\nu-\nu E_\nu.                                  \tag{4}
\]
The two generator assumptions give their predictable intensities, and the marked-history assumption distinguishes natural from service recoveries.  Thus the martingales in (4) are pairwise orthogonal and
\[
 \langle U_{\lambda,N}\rangle=\lambda E_\lambda,
 \qquad
 \langle U_{\tau,N}\rangle=\tau E_\tau,
 \qquad
 \langle U_{\nu,N}\rangle=\nu E_\nu.                        \tag{5}
\]
Set
\[
 s_\lambda=2-a_0-a,
 \qquad s_\tau=a_0+a,
 \qquad s_\nu=a.
\]
The control stationary mean is exactly \(Na_0\).  The invariant-law convergence and uniform moments in Theorem \(\mathrm{diffusion\mbox{-}poisson}\) give \(N^{-1}\mathbb E K_{1,N}\to a\).  The time-average conclusion of Lemma \(\mathrm{finite\mbox{-}chain\mbox{-}poisson\mbox{-}controls}\) then yields
\[
 \frac1{NT_N}(E_\lambda,E_\tau,E_\nu)
 \longrightarrow(s_\lambda,s_\tau,s_\nu)                    \tag{6}
\]
uniformly in probability.  Compact positivity gives \(\inf_{p\in\mathcal P}\min_j s_j>0\).  Hence zero-exposure fallbacks and plug-in clipping are inactive with probability tending to one, and componentwise
\[
 \sqrt{NT_N}(\widehat p_j-p_j)
 =\frac{U_{j,N}/\sqrt{NT_N}}{E_j/(NT_N)}
 =\frac{U_{j,N}}{s_j\sqrt{NT_N}}+o_{\mathbb P}(1).             \tag{7}
\]

We calculate the full predictable bracket before taking a limit.  Define
\[
 \mathcal Z_N(t)=\left(
 \frac{M_{0,N}(t)-M_{1,N}(t)}{\sqrt{T_N}},
 \frac{U_{\lambda,N}(t)}{s_\lambda\sqrt{NT_N}},
 \frac{U_{\tau,N}(t)}{s_\tau\sqrt{NT_N}},
 \frac{U_{\nu,N}(t)}{s_\nu\sqrt{NT_N}}
 \right)^{\mathsf T}.                                        \tag{8}
\]
The exact stationary Poisson-energy identity, arm independence, and the time-average assertion of the finite-chain lemma give
\[
 \langle\mathcal Z_N^{(1)}\rangle(T_N)
 =V_{0,N}+V_{1,N}+o_{\mathbb P}(1)
 \longrightarrow V_0+V_\gamma=V.                             \tag{9}
\]
Equations (5)--(6) give the lower diagonal block
\[
 \operatorname{diag}\left\{\frac\lambda{s_\lambda},
 \frac\tau{s_\tau},\frac\nu{s_\nu}\right\}=\Gamma.       \tag{10}
\]

For the cross bracket, inspect each event.  On an upward control edge, the scaled Poisson increment has stationary edge average tending to \(d^{-1}\); on an upward treatment edge it tends to \(H\).  Downward edges have the negative increments after shifting the index.  Since the first coordinate is \(M_{0,N}-M_{1,N}\), the limiting unnormalized cross-bracket rates are
\[
 \begin{split}
 c_\lambda&=\lambda\{(1-a_0)/d-(1-a)H\}=\lambda A_\lambda,\\
 c_\tau&=\tau\{-a_0/d+aH\}=\tau A_\tau,\\
 c_\nu&=\nu aH=\nu A_\nu.                                  \tag{11}
 \end{split}
\]
The exposure factors in (11) follow from (6), and the edge factors from the finite-chain lemma.  Division by \(s_\lambda,s_\tau,s_\nu\), as in (8), gives \(A^{\mathsf T}\Gamma\).  Therefore
\[
 \langle\mathcal Z_N\rangle(T_N)
 \longrightarrow
 \Sigma:=\begin{pmatrix}V&A^{\mathsf T}\Gamma\\
                         \Gamma A&\Gamma\end{pmatrix}        \tag{12}
\]
uniformly in probability.

We now derive, rather than assume, the Gaussian limit.  Fix \(t\in\mathbb R^4\) and put \(L_N=t^{\mathsf T}\mathcal Z_N\).  The Poisson edge bound makes a normalized occupation jump at most \(d^{-1}/\sqrt{NT_N}\), and a normalized count jump at most \((\min_j s_j)^{-1}/\sqrt{NT_N}\).  Hence
\[
 \sup_s|\Delta L_N(s)|\leq\varepsilon_N\longrightarrow0,
 \qquad
 \langle L_N\rangle(T_N)\longrightarrow t^{\mathsf T}\Sigma t. \tag{13}
\]
Let \(\nu_N(ds,dy)\) be the predictable compensator of the jumps of \(L_N\).  Stop when its quadratic integral first exceeds a fixed \(R>t^{\mathsf T}\Sigma t+1\).  The compensated jump-product identity says that
\[
 \exp\!\left{iL_N(u)-
 \int_{[0,u]\times\mathbb R}(e^{iy}-1-iy)\nu_N(ds,dy)\right} \tag{14}
\]
is a mean-one martingale up to the stopping time.  Taylor's integral remainder gives, for \(|y|\leq\varepsilon_N\),
\[
 \left|e^{iy}-1-iy+\frac{y^2}{2}\right|
 \leq\frac{\varepsilon_N y^2}{6}.                             \tag{15}
\]
Since \(\int y^2\nu_N=\langle L_N\rangle\), equations (14)--(15) show that the stopped characteristic function differs from
\(\exp\{-\langle L_N\rangle(T_N)/2\}\) by at most \(C_R\varepsilon_NR\).  Equation (13) and boundedness then give
\[
 \mathbb E e^{iL_N(T_N)}longrightarrow
 \exp\{-t^{\mathsf T}\Sigma t/2\};                           \tag{16}
\]
the stopping probability tends to zero by (13).  Because (16) holds for every \(t\), the joint characteristic functions converge to that of the centered Gaussian vector with covariance \(\Sigma\).  The limit is continuous at the origin, so Fourier inversion against smooth compactly supported densities, followed by approximation of bounded continuous functions, gives
\[
 \mathcal Z_N(T_N)\Rightarrow\mathcal N(0,\Sigma).            \tag{17}
\]
Combining (3), (7), and (17) proves the displayed joint law.

Uniformity follows by the sequence criterion.  If it failed, there would be a parameter sequence with a fixed positive discrepancy.  Compactness supplies a convergent subsequence.  The moment, exposure, edge, and bracket estimates above use the same constants on that subsequence; \(V,H,A,\Gamma\) are continuous Gaussian integrals.  Equations (12)--(17) then contradict the discrepancy.

It remains to studentize.  Equation (7) implies \(\widehat p-p=o_{\mathbb P}(1)\), and smoothness of \(a\) on the compact positive rate box gives
\[
 \widehat\gamma_N-\gamma_N
 =-\sqrt N\{a(\widehat p)-a(p)\}=O_{\mathbb P}(T_N^{-1/2}),
 \qquad 0\leq\gamma_N-\gamma<N^{-1/2}.                       \tag{18}
\]
The Gaussian integrals defining \(V\) are continuous on compact positive-rate and bounded-staffing sets.  Also
\[
 V\geq V_0=\frac{2\lambda\tau}{d^3},
 \qquad \inf_{p\in\mathcal P}V_0>0.                          \tag{19}
\]
Thus \(\widehat V/V\to1\) uniformly in probability.  Dividing the first coordinate by \(\sqrt{\widehat V}\), using \(\alpha\in(0,1)\), and integrating the limiting normal density over \([-z_\alpha,z_\alpha]\) yields
\[
 \sup_{p\in\mathcal P,\,|\gamma|\leq G}
 \left|\mathbb P_{p,\gamma}\{\theta_N\in I_{\mathrm{emp}}\}-(1-\alpha)\right|
 \longrightarrow0.                                           \tag{20}
\]
Only exact prelimit martingales on \([0,T_N]\) were used, so no upper relation between \(T_N\) and \(N\) entered.  Lemma \(\mathrm{boutilier\mbox{-}anchor\mbox{-}scope}\) supplies exactly the final scope comparison and no class-transfer premise.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Exact prelimit Poisson martingales produce the joint occupation and marked-score covariance without approximating paths over the growing horizon. \textit{Gap.} The result is a joint-array theorem for the stated equal-arm subclass, not a class-wide tightening of the broader anchor. \textit{Consumer.} Investigators obtain dependence-aware empirical intervals and the covariance kernel needed for fitted centering.

\begin{theorem}[thm:fitted-center-wald]\label{thm:fitted-center-wald}
Uniformly over \(\mathcal M_{\mathrm{fit}}\), with no upper restriction on the relative growth of \(T_N\) and \(N\),
\[
 \sqrt{NT_N}(\widetilde\theta_N-\theta_N)\Rightarrow\mathcal N(0,Q),
 \qquad
 \sqrt{NT_N}(\widehat\theta_N-\widetilde\theta_N)\Rightarrow\mathcal N(0,W),
\]
and \(I_{\mathrm{fit}}\) covers \(\theta_N\) with probability \(1-\alpha+o(1)\) uniformly.  Holding the implemented capacity fixed,
\[
 \nabla_{p'}\vartheta_N(p';M_N)\big|_{p'=p}\longrightarrow
 A=\nabla a_0-DH\nabla a,
 \qquad Q=A^{\mathsf T}\Gamma A,
 \qquad W=V-A^{\mathsf T}\Gamma A,
\]
uniformly on the compact rate and staffing set.
\end{theorem}
\begin{proof}
Work over \(\mathcal M_{\mathrm{fit}}\).  Its primitive sampling and dynamics conditions are the same as those used in Theorem \(\mathrm{uniform\mbox{-}joint\mbox{-}law}\), and it imposes only \(N,T_N\to\infty\).  Define
\[
 S_N=\sqrt{NT_N}(\widehat\theta_N-\theta_N),
 \qquad R_N=\sqrt{NT_N}(\widehat p-p).
\]
The joint theorem gives, uniformly,
\[
 (S_N,R_N)\Rightarrow(S,R),
 \quad \operatorname{Var}(S)=V,
 \quad \operatorname{Var}(R)=\Gamma,
 \quad \operatorname{Cov}(R,S)=\Gamma A,                      \tag{1}
\]
and \(\widehat p-p=O_{\mathbb P}((NT_N)^{-1/2})\).

The detailed-balance calculation in Lemma \(\mathrm{exact\mbox{-}fixed\mbox{-}capacity\mbox{-}derivative}\) gives
\[
 \vartheta_N(p;M_N)=\theta_N.                                 \tag{2}
\]
On the event that the line segment between \(p\) and the clipped \(\widehat p\) lies in \([c/2,2C]^3\), Taylor's formula is applied while holding the implemented integer \(M_N\) fixed.  The derivative and Hessian bounds of that lemma yield
\[
 \begin{split}
 \sqrt{NT_N}(\widetilde\theta_N-\theta_N)
 &=[\nabla_{p'}\vartheta_N(p';M_N)|_{p'=p}]^{\mathsf T}R_N+\mathcal R_N,\\
 |\mathcal R_N|
 &\leq\frac{C_*}{2}\sqrt{NT_N}\sqrt N\,\|\widehat p-p\|^2
 =O_{\mathbb P}(T_N^{-1/2})=o_{\mathbb P}(1).                 \tag{3}
 \end{split}
\]
Uniform consistency and the compact interior rate bounds make the complement of this event, including any fallback event, have probability tending to zero.  The derivative convergence now gives
\[
 \sqrt{NT_N}(\widetilde\theta_N-\theta_N)
 =A^{\mathsf T}R_N+o_{\mathbb P}(1)
 \Rightarrow\mathcal N(0,A^{\mathsf T}\Gamma A).             \tag{4}
\]
No comparison between \(T_N\) and \(N\) appears in (3).

The derivative limit has the advertised fixed-capacity form.  Direct differentiation gives
\[
 \nabla a_0=((1-a_0)/d,-a_0/d,0)^{\mathsf T},
 \qquad
 D\nabla a=(1-a,-a,-a)^{\mathsf T},
\]
so
\[
 \nabla a_0-DH\nabla a
 =((1-a_0)/d-(1-a)H,-a_0/d+aH,aH)^{\mathsf T}=A.              \tag{5}
\]
Thus the variance in (4) is \(Q=A^{\mathsf T}\Gamma A\).

Subtracting (4) from the first coordinate in (1) gives
\[
 \sqrt{NT_N}(\widehat\theta_N-\widetilde\theta_N)
 \Rightarrow S-A^{\mathsf T}R.                                \tag{6}
\]
Using every covariance identity in (1),
\[
 \operatorname{Var}(S-A^{\mathsf T}R)
 =V+A^{\mathsf T}\Gamma A-2A^{\mathsf T}\Gamma A
 =V-A^{\mathsf T}\Gamma A=W.                                 \tag{7}
\]
This also proves \(W\geq0\), because it is the variance of the displayed Gaussian linear combination.

For Wald coverage, Theorem \(\mathrm{diffusion\mbox{-}poisson}\) gives \(H\geq1/D\).  The service coordinate is \(A_\nu=aH\), while \(\Gamma_{\nu\nu}=\nu/a\).  Hence
\[
 Q=A^{\mathsf T}\Gamma A\geq\frac\nu a(aH)^2=\nu aH^2,     \tag{8}
\]
whose infimum on the compact positive rate box is positive.  Moreover,
\[
 \widehat\gamma_N-\gamma_N
 =-\sqrt N\{a(\widehat p)-a(p)\}=O_{\mathbb P}(T_N^{-1/2}),
 \qquad 0\leq\gamma_N-\gamma<N^{-1/2}.                       \tag{9}
\]
The explicit Gaussian integrals defining \(H\), and therefore \(A,Q\), are continuous on compact positive-rate and bounded-staffing sets.  Equations (1), (8), and (9) imply \(\widehat Q/Q\to1\) uniformly in probability.  Studentizing (4) and integrating the standard normal density over \([-z_\alpha,z_\alpha]\) proves
\[
 \sup_{p\in\mathcal P,\,|\gamma|\leq G}
 \left|\mathbb P_{p,\gamma}\{\theta_N\in I_{\mathrm{fit}}\}-(1-\alpha)\right|
 \longrightarrow0.                                           \tag{10}
\]
Equations (4)--(10) establish both limits, the fixed-staffing correction, and the absence of an upper horizon restriction.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The exact stationary product-law center equals the finite-system estimand at the true rates and removes the horizon upper bound from fitted inference. \textit{Gap.} The load-bearing new step is the uniform fixed-capacity derivative and Hessian control connecting exact finite-chain sensitivity to \(A\). \textit{Consumer.} A Keheala-like platform can use the same marked event history to estimate rates and report a Wald interval for the implemented stationary effect.

\begin{theorem}[thm:certified-capacity-design]\label{thm:certified-capacity-design}
From a supplied rational interval name \(\mathfrak r\) for \((p,c_M,c_I,\alpha,G)\), construct a positive rational \(L_J\) and, for every admissible rational interval \(I\subseteq[-G,G]\) and \(m\in\mathbb N\), rational endpoints \(\underline J(I,m)\) and \(\overline J(I,m)\), together with finite checkable evaluation witnesses derived from the piecewise-Gaussian Poisson-energy formulas for \(V_0\) and \(V_\gamma\), such that \((L_J,\mathfrak O)\) satisfies every enclosure, width, and point-width requirement of Definition \(\mathrm{interval\mbox{-}oracle}\) for \(J(p,\gamma)\).  In particular, the construction must certify the explicit uniform modulus
\[
 \overline J(I,m)-\underline J(I,m)
 \leq L_J\operatorname{diam}(I)+2^{-m}
\]
and must not take oracle soundness as an input premise.
\end{theorem}
\begin{proof}
We construct the modulus and every oracle return from finite rational receipts.

\emph{Step 1: rational parameter bounds and a global modulus.}
By Definition \(\mathrm{input\mbox{-}name}\), the \(G\)-coordinate is exactly \([G,G]\), while the other coordinate intervals are nested, have widths tending to zero, and preserve the strict positivity imposed by Assumptions \(\mathrm{rate\mbox{-}bounds}\), \(\mathrm{rate\mbox{-}membership}\), \(\mathrm{staffing\mbox{-}cost\mbox{-}positive}\), and \(\mathrm{information\mbox{-}cost\mbox{-}positive}\).  A finite prefix therefore supplies rationals
\[
 0<\lambda_-\leq\lambda\leq\lambda_+,
 \quad 0<\tau_-\leq\tau\leq\tau_+,
 \quad 0<\nu_-\leq\nu\leq\nu_+,
\tag{1}
\]
together with rational upper bounds \(c_M^+\geq c_M\) and \(c_I^+\geq c_I\), and a rational lower bound \(0<\alpha_-\leq\alpha\).  Set
\[
 \begin{gathered}
 d_-:=\lambda_-+\tau_-,\qquad d_+:=\lambda_++\tau_+,
 \qquad D_+:=\lambda_++\tau_++\nu_+,\\
 q_-:=\frac{\lambda_-(\tau_-+\nu_-)}{D_+},\qquad
 q_+:=\frac{\lambda_+(\tau_++\nu_+)}{\lambda_-+\tau_-+\nu_-},
 \qquad v_*:=\frac{2\lambda_-\tau_-}{d_+^3}.
 \end{gathered}
\tag{2}
\]
The definitions \(d=\lambda+\tau\), \(D=\lambda+\tau+\nu\), \(q=\lambda(\tau+\nu)/D\), and \(V_0=2\lambda\tau/d^3\), together with (1), give
\[
 d_-\leq d\leq d_+,
 \qquad D\leq D_+,
 \qquad q_-\leq q\leq q_+,
 \qquad V_0\geq v_*>0.
\tag{3}
\]
Indeed, the lower bound on \(q\) uses the lower bounds for its numerator and the upper bound \(D_+\) for its denominator; the upper bound uses the upper bounds for its numerator and the positive lower bound \(\lambda_-+\tau_-+\nu_-\) for its denominator.  Rational bisection with squared-endpoint checks produces positive rationals \(s_q,s_d,s_0\) satisfying
\[
 s_q^2\geq q_+,
 \qquad s_d^2\leq d_-,
 \qquad s_0^2\leq v_*.
\tag{4}
\]
The already established Lemma \(\mathrm{gamma\mbox{-}energy\mbox{-}lipschitz}\) and (3)--(4) imply, uniformly in the supplied parameter box and in \(\gamma\in[-G,G]\),
\[
 \left|\frac{\partial V_\gamma}{\partial\gamma}\right|
 \leq K_V:=\frac{5\nu_+s_q}{d_-^2s_d}.
\tag{5}
\]

Enumerate positive integers \(H\) until rational exponential enclosures certify
\[
 \frac{e^{-H^2/2}}{H}<\frac{\alpha_-}{2}.
\tag{6}
\]
This enumeration terminates because the left side tends to zero.  Since
\[
 1-\Phi(H)
 =\frac1{\sqrt{2\pi}}\int_H^\infty e^{-x^2/2}\,dx
 \leq\frac{e^{-H^2/2}}{H\sqrt{2\pi}}
 <\frac{\alpha_-}{2}\leq\frac\alpha2,
\tag{7}
\]
where the inequality follows from \(x/H\geq1\) in the integral of \(xe^{-x^2/2}\), monotonicity of \(\Phi\) gives \(z_\alpha<H\).  Moreover, (3)--(4) give \(\sqrt{V_0+V_\gamma}\geq s_0\).  Differentiating Definition \(\mathrm{design\mbox{-}objective}\) and applying (5) yields
\[
 |\partial_\gamma J(p,\gamma)|
 \leq c_M^++\frac{c_I^+HK_V}{2s_0}.
\tag{8}
\]
Consequently
\[
 L_J:=1+\left\lceil c_M^++\frac{c_I^+HK_V}{2s_0}\right\rceil
\tag{9}
\]
is a positive rational global Lipschitz constant for \(J(p,\cdot)\).  Its construction uses only finite rational comparisons and the exponential receipts described below.

\emph{Step 2: an exact nonnested point formula.}
Fix rational \(g\in[-G,G]\), which is meaningful because \(G\) is exact rational.  Let
\[
 w_g(x)=
 \begin{cases}
 \exp\{-Dx^2/(2q)\},&x\leq g,\\
 \exp\{-dx^2/(2q)-\nu gx/q+\nu g^2/(2q)\},&x>g.
 \end{cases}
\tag{10}
\]
Put
\[
 s:=-\frac{\nu g}{d},\qquad
 K:=\frac{\nu Dg^2}{2qd},\qquad
 \sigma_D:=\sqrt{q/D},\qquad \sigma_d:=\sqrt{q/d},
\tag{11}
\]
and define \(y_D(x):=x/\sigma_D\), \(y(x):=(x-s)/\sigma_d\), and \(y_g:=y(g)\).  Completing the square gives
\[
 w_g(x)=e^K\exp\{-(x-s)^2/(2\sigma_d^2)\},\qquad x>g.
\tag{12}
\]
Direct integration of (10)--(12) gives the normalizer \(Z\) and unnormalized first moment \(M\):
\[
 \begin{aligned}
 Z={}&\sqrt{2\pi}\sigma_D\Phi(y_D(g))
 +e^K\sqrt{2\pi}\sigma_d\{1-\Phi(y_g)\},\\
 M={}&-\frac qD e^{-Dg^2/(2q)}
 +e^K\left[s\sqrt{2\pi}\sigma_d\{1-\Phi(y_g)\}
 +\frac qd e^{-y_g^2/2}\right].
 \end{aligned}
\tag{13}
\]
Thus \(m_g=M/Z\).  If
\[
 A_0(x):=\int_{-\infty}^xw_g(u)\,du,
 \qquad A_1(x):=\int_{-\infty}^xuw_g(u)\,du,
\tag{14}
\]
then, for \(x\leq g\),
\[
 A_0(x)=\sqrt{2\pi}\sigma_D\Phi(y_D(x)),
 \qquad A_1(x)=-\frac qD e^{-Dx^2/(2q)},
\tag{15}
\]
whereas for \(x>g\), continuity at \(g\) and the substitution \(v=(u-s)/\sigma_d\) give
\[
 \begin{aligned}
 A_0(x)={}&A_0(g)+e^K\sqrt{2\pi}\sigma_d
 \{\Phi(y(x))-\Phi(y_g)\},\\
 A_1(x)={}&A_1(g)+e^K\left[s\sqrt{2\pi}\sigma_d
 \{\Phi(y(x))-\Phi(y_g)\}
 +\frac qd\{e^{-y_g^2/2}-e^{-y(x)^2/2}\}\right].
 \end{aligned}
\tag{16}
\]
Define \(\Psi(x):=A_1(x)-m_gA_0(x)\).  Definition \(\mathrm{diffusion\mbox{-}poisson}\) gives \(h_g'(x)=-\Psi(x)/(qw_g(x))\), and \(\pi_g=w_g/Z\).  Substitution into \(V_g=2q\int(h_g')^2\,d\pi_g\) proves
\[
 V_g=\frac2{qZ}\int_{\mathbb R}\frac{\Psi(x)^2}{w_g(x)}\,dx.
\tag{17}
\]
Thus no nested improper integral remains.

\emph{Step 3: an explicit tail receipt.}
The mode of \(w_g\) is
\[
 x^*=0\quad\hbox{if }g\geq0,
 \qquad x^*=-\nu g/d\quad\hbox{if }g<0.
\tag{18}
\]
The negative log-density has derivative zero at \(x^*\), is continuously differentiable, and has second derivative between \(d/q\) and \(D/q\) away from its single kink.  Integrating these derivative bounds separately on each side of the kink yields
\[
 w_g(x^*)e^{-D(x-x^*)^2/(2q)}
 \leq w_g(x)
 \leq w_g(x^*)e^{-d(x-x^*)^2/(2q)}.
\tag{19}
\]
After integration of the lower bound and division of the upper bound by \(Z\),
\[
 \frac Z{w_g(x^*)}\geq\sqrt{\frac{2\pi q}{D}},
 \qquad
 \pi_g(x)\leq\sqrt{\frac D{2\pi q}}
 e^{-d(x-x^*)^2/(2q)}.
\tag{20}
\]
For an integer \(R>|g|+|x^*|\), put \(r:=R-|x^*|>0\).  Since
\[
 \int_r^\infty e^{-du^2/(2q)}\,du
 \leq\frac q{dr}e^{-dr^2/(2q)},
\]
(20) implies
\[
 \mathbb P_{\pi_g}(|X|>R)
 \leq B_R:=\sqrt{\frac D{2\pi q}}\frac{2q}{dr}
 e^{-dr^2/(2q)}.
\tag{21}
\]
The bound \(h_g'\leq1/d\) from the already established Theorem \(\mathrm{diffusion\mbox{-}poisson}\) then gives
\[
 0\leq2q\int_{|x|>R}\{h_g'(x)\}^2\pi_g(x)\,dx
 \leq\frac{2q}{d^2}B_R.
\tag{22}
\]
On a rational input box, \(|x^*|\leq\nu_+|g|/d_-\).  Replacing the factors in (21)--(22) by outward rational bounds obtained from (2)--(4) produces a rational upper bound dominated by a constant times \(r^{-1}\exp\{-d_-r^2/(2q_+)\}\).  It tends to zero, so enumeration of integer \(R\) terminates at every positive rational tolerance.

\emph{Step 4: finite receipts for elementary functions.}
The identity
\[
 \pi=16\arctan(1/5)-4\arctan(1/239)
\tag{23}
\]
is verified by the rational tangent-addition formulas together with localization of the resulting angle in \((0,\pi)\).  Integrating the finite geometric identity for \((1+t^2)^{-1}\) gives the alternating arctangent expansion with a first-omitted-term rational remainder, so (23) gives nested rational enclosures of \(\pi\).  Positive square roots are enclosed by rational bisection with squared-endpoint checks.

For rational \(x\), choose an integer \(M>2|x|\).  The Taylor expansion of \(e^{x/M}\) has the rational remainder bound
\[
 |R_n|\leq\frac{2|x/M|^{n+1}}{(n+1)!},
\tag{24}
\]
because \(e^{|x|/M}\leq\sum_{j\geq0}|x/M|^j\leq2\).  Raising the positive enclosing interval to the integer power \(M\) gives arbitrarily narrow rational enclosures of \(e^x\).  For rational \(x\geq0\), termwise integration gives
\[
 \Phi(x)=\frac12+\frac1{\sqrt{2\pi}}
 \sum_{j=0}^\infty\frac{(-1)^jx^{2j+1}}{2^j j!(2j+1)}.
\tag{25}
\]
After the successive absolute terms become decreasing, their ratios continue to decrease, so the alternating remainder is bounded by the first omitted term; symmetry treats negative \(x\).  Hence (23)--(25) give finite rational enclosures of every elementary expression in (10)--(22).

Assumption \(\mathrm{test\mbox{-}size}\) places \(1-\alpha/2\) strictly between \(1/2\) and \(1\), and (7) brackets \(z_\alpha\) in \([0,H]\).  Monotone bisection inverts \(\Phi\) on this interval.  If enclosures at a trial midpoint overlap the current enclosure of \(1-\alpha/2\), use
\[
 \inf_{0\leq x\leq H}\Phi'(x)
 =\frac{e^{-H^2/2}}{\sqrt{2\pi}}>0
\tag{26}
\]
and the integral identity for differences of \(\Phi\) to retain a valid shrinking bracket.  Refining the input and function enclosures makes this bracket arbitrarily narrow even when equality occurs at a tested midpoint.  Every receipt in (23)--(26) is a finite list of rational terms, remainder inequalities, bisection comparisons, and outward-rounded operations.

\emph{Step 5: a terminating point evaluator.}
For rational \(g\) and requested precision \(n\), dovetail refinement of the input prefix, enlargement of \(R\), refinement of the elementary receipts, and dyadic refinement of rational partitions of \([-R,g]\) and \([g,R]\).  On every cell, outward rational interval arithmetic applied to the relevant branch of (15)--(16) encloses \(\Psi^2/w_g\).  The denominator is bounded away from zero on the compact interval by (10) and positivity of the refined rate box; (20) supplies a positive lower bound for \(Z\).  The two branch formulas agree at \(g\), so the split omits no kink contribution.

Cell widths times cell ranges give rational lower and upper Darboux sums for the compact part of (17).  On each closed branch the integrand is jointly continuous in the state and in every parameter over a sufficiently refined compact positive input box.  Uniform continuity makes the compact enclosure width tend to zero with the mesh and input widths.  Equation (22) makes the omitted tail tend to zero with \(R\), and Step 4 makes the elementary-function widths tend to zero.  Therefore the dovetailing search terminates with a finite rational receipt
\[
 P(g,n)=[P_-(g,n),P_+(g,n)]\ni J(p,g),
 \qquad P_+(g,n)-P_-(g,n)\leq2^{-n}.
\tag{27}
\]
Here \(V_0=2\lambda\tau/d^3\) is evaluated directly, \(V_g\) is evaluated through (13)--(17), and \(z_\alpha\) through (26).  The receipt records the input prefix, truncation radius, series orders and remainder bounds, quantile bracket, partition, every cell enclosure, both tail bounds, and every outward rational operation.  Thus (27) is checkable finite data rather than an assumed oracle.

\emph{Step 6: interval returns.}
For \(I=[a,b]\subseteq[-G,G]\) with rational endpoints, set \(g_0:=(a+b)/2\), obtain \(P(g_0,m+1)=[P_-,P_+]\), and return
\[
 \mathfrak O(I,m)=
 \left[P_--\frac{L_J(b-a)}2,
 P_++\frac{L_J(b-a)}2\right].
\tag{28}
\]
For every \(\eta\in I\), (8)--(9) and the fundamental theorem of calculus give
\[
 |J(p,\eta)-J(p,g_0)|
 \leq L_J|\eta-g_0|
 \leq\frac{L_J(b-a)}2.
\tag{29}
\]
Combining (27)--(29) proves that (28) encloses \(J(p,\eta)\), and
\[
 \begin{aligned}
 \overline J(I,m)-\underline J(I,m)
 &=P_+-P_-+L_J(b-a)\\
 &\leq2^{-(m+1)}+L_J\operatorname{diam}(I)\\
 &\leq2^{-m}+L_J\operatorname{diam}(I).
 \end{aligned}
\tag{30}
\]
For a point interval the returned width is at most \(2^{-(m+1)}\leq2^{-m}\).  Equations (9), (27), and (28), together with their finite witnesses, therefore construct the pair \((L_J,\mathfrak O)\) and verify every clause of Definition \(\mathrm{interval\mbox{-}oracle}\).  Oracle soundness is a conclusion, not a premise.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The theorem constructs the missing model-specific sound oracle from a proved energy modulus and finite rational point-evaluation receipts on an exactly represented rational staffing interval. \textit{Gap.} No oracle-soundness or irrational-boundary premise remains; termination is proved without a sharp practical worst-case complexity bound. \textit{Consumer.} Together with the generic certificate theorem and finite-design regret transfer, it yields a checkable staffing recommendation for the stated compact model class with exact rational radius.

\begin{theorem}[thm:finite-design-regret]\label{thm:finite-design-regret}
Conditional on a supplied sound interval oracle and the certificate returned by Theorem \(\mathrm{oracle\mbox{-}conditional\mbox{-}capacity\mbox{-}certificate}\), uniformly for \(p\in\mathcal P\), the certified offset satisfies
\[
 J_N(p,\gamma_\epsilon)-\inf_{|\gamma|\leq G}J_N(p,\gamma)
 \leq\epsilon+o(1).
\]
\end{theorem}
\begin{proof}
Define
\[
 S_N(p,\gamma):=V_{0,N}(p)+V_{1,N}(p,\gamma),
 \qquad S(p,\gamma):=V_0(p)+V_\gamma(p).
\]
Every summand is nonnegative because it is a stationary Poisson energy.  Assumptions \(\mathrm{rate\mbox{-}membership}\) and \(\mathrm{staffing\mbox{-}window}\), with the exact finite radius \(G\), give the compact domain \(\mathcal P\times[-G,G]\).  The uniform energy convergence in the already established Theorem \(\mathrm{diffusion\mbox{-}poisson}\) therefore implies
\[
 \begin{aligned}
 r_N&:=\sup_{p\in\mathcal P,\,|\gamma|\leq G}
 |S_N(p,\gamma)-S(p,\gamma)|\\
 &\leq\sup_{p\in\mathcal P}|V_{0,N}(p)-V_0(p)|
 +\sup_{p\in\mathcal P,\,|\gamma|\leq G}
 |V_{1,N}(p,\gamma)-V_\gamma(p)|
 \longrightarrow0.
 \end{aligned}
\tag{1}
\]
For \(x,y\geq0\), assume first that \(x\geq y\).  Then
\[
 (\sqrt x-\sqrt y)^2=x+y-2\sqrt{xy}\leq x-y=|x-y|,
\]
because \(\sqrt{xy}\geq y\); exchanging \(x,y\) proves
\[
 |\sqrt x-\sqrt y|\leq\sqrt{|x-y|}
\tag{2}
\]
in all cases.  In the definitions of \(J_N\) and \(J\), the term \(c_M\gamma\) is identical and cancels.  Therefore (1)--(2) give
\[
 \Delta_N:=\sup_{p\in\mathcal P,\,|\gamma|\leq G}
 |J_N(p,\gamma)-J(p,\gamma)|
 \leq c_Iz_\alpha\sqrt{r_N}\longrightarrow0.
\tag{3}
\]
The multiplier is finite: Assumption \(\mathrm{information\mbox{-}cost\mbox{-}positive}\) gives \(0<c_I<\infty\), and Assumption \(\mathrm{test\mbox{-}size}\) gives \(z_\alpha=\Phi^{-1}(1-\alpha/2)\in(0,\infty)\).  Thus (3) is uniform in \(p\) and \(\gamma\).

Theorem \(\mathrm{certified\mbox{-}capacity\mbox{-}design}\) constructs a sound model-specific interval oracle from the supplied input representation, including the exact rational radius.  Under this theorem's stated premise that the sound oracle and the certificate returned from it are supplied, Theorem \(\mathrm{oracle\mbox{-}conditional\mbox{-}capacity\mbox{-}certificate}\) gives, for each \(p\in\mathcal P\),
\[
 L_\epsilon\leq\mu(p):=\inf_{|\gamma|\leq G}J(p,\gamma)
 \leq J(p,\gamma_\epsilon)\leq U_\epsilon,
 \qquad U_\epsilon-L_\epsilon\leq\epsilon.
\tag{4}
\]
Hence
\[
 J(p,\gamma_\epsilon)-\mu(p)
 \leq U_\epsilon-L_\epsilon\leq\epsilon.
\tag{5}
\]
By the definition of \(\Delta_N\), for every feasible \(\gamma\),
\[
 J_N(p,\gamma)\geq J(p,\gamma)-\Delta_N.
\]
Taking the infimum over the same nonempty interval gives
\[
 \inf_{|\gamma|\leq G}J_N(p,\gamma)
 \geq\mu(p)-\Delta_N.
\tag{6}
\]
The feasibility \(\gamma_\epsilon\in[-G,G]\) similarly yields
\[
 J_N(p,\gamma_\epsilon)
 \leq J(p,\gamma_\epsilon)+\Delta_N.
\tag{7}
\]
Subtracting (6) from (7) and applying (5),
\[
 \begin{aligned}
 J_N(p,\gamma_\epsilon)-\inf_{|\gamma|\leq G}J_N(p,\gamma)
 &\leq J(p,\gamma_\epsilon)-\mu(p)+2\Delta_N\\
 &\leq\epsilon+2\Delta_N.
 \end{aligned}
\tag{8}
\]
The same \(\Delta_N\) controls every \(p\in\mathcal P\); taking the supremum in (8) and using (3) proves the uniform \(\epsilon+o(1)\) regret bound.  The recommendation remains conditional on the supplied sound receipts and returned certificate, as stated.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Uniform diffusion-energy convergence transfers the limiting-objective certificate on the exact rational staffing interval to the finite integer-capacity system. \textit{Gap.} The transfer retains its stated \(o(1)\) approximation term; the oracle-construction and root-representability gaps are discharged upstream. \textit{Consumer.} The constructed oracle, rational dyadic certificate, and this transfer give the staffing manager a checkable \(\epsilon+o(1)\) finite-system regret guarantee.

\begin{proposition}[prop:sanity-reductions]\label{prop:sanity-reductions}
In the continuous extension \(\nu\downarrow0\), the treatment diffusion reduces to the control Ornstein--Uhlenbeck process, \(m_\gamma\to0\), and \(V_\gamma\to2\lambda\tau/(\lambda+\tau)^3\).  For fixed \(N\) and an interior implemented capacity, the exact prelimit Poisson martingale yields the ordinary finite-state occupation central limit theorem as \(T_N\to\infty\).  This second assertion is an internal reduction for the equal-arm stationary homogeneous subclass; it is compatible with, but does not transfer a positive result to, the cited broader model allowing distinct arm populations and capacity.
\end{proposition}
\begin{proof}
Fix \(\lambda,\tau>0\) and \(\gamma\in\mathbb R\), and let \(\nu\downarrow0\) through the continuous extension.  Write
\[
 d=\lambda+\tau,
 \qquad D_\nu=d+\nu,
 \qquad q_\nu=\frac{\lambda(\tau+\nu)}{d+\nu}.                 \tag{1}
\]
Then \(D_\nu\to d\) and \(q_\nu\to q_0=\lambda\tau/d\).  The treatment generator is
\[
 (\mathcal L_{\nu,\gamma}g)(x)
 =[-dx-\nu\min(x,\gamma)]g'(x)+q_\nu g''(x),                  \tag{2}
\]
which converges on compact sets to \(\mathcal L_0\), and equals \(\mathcal L_0\) at \(\nu=0\).

Let \(r_{\nu,\gamma}\) be the unnormalized density in the definition of \(\pi_\gamma\), with \(D,q\) replaced by \(D_\nu,q_\nu\), and let \(\pi_{\nu,\gamma}=r_{\nu,\gamma}/\int r_{\nu,\gamma}\).  Pointwise,
\[
 r_{\nu,\gamma}(x)\longrightarrow
 r_0(x)=\exp\{-dx^2/(2q_0)\}.                                 \tag{3}
\]
On a fixed interval \(0\leq\nu\leq\bar\nu\), positivity and continuity give \(0<q_-\leq q_\nu\leq q_+\).  On the left density branch,
\[
 r_{\nu,\gamma}(x)\leq e^{-dx^2/(2q_+)},
\]
while on the right branch Young's inequality gives a constant \(C_\gamma\) such that
\[
 -\frac{dx^2}{2q_\nu}-\frac{\nu\gamma x}{q_\nu}
 +\frac{\nu\gamma^2}{2q_\nu}
 \leq-\frac{dx^2}{4q_+}+C_\gamma.                             \tag{4}
\]
Thus \((1+x^2)r_{\nu,\gamma}(x)\) has a common integrable Gaussian envelope.  Dominated convergence for the normalizer and first two moments proves
\[
 \pi_{\nu,\gamma}\to\pi_0
 \quad\hbox{in weighted }L^1(1+x^2),
 \qquad m_{\nu,\gamma}\to0.                                 \tag{5}
\]

To pass the Poisson energy to the limit, note that for \(y>x\),
\[
 -(d+\nu)(y-x)
 \leq b_{\nu,\gamma}(y)-b_{\nu,\gamma}(x)
 \leq-d(y-x).                                                  \tag{6}
\]
Synchronous coupling therefore gives
\[
 e^{-(d+\nu)t}(y-x)
 \leq X_t^y-X_t^x\leq e^{-dt}(y-x).                            \tag{7}
\]
For the centered resolvent
\[
 h_{\nu,\gamma}(x)=
 \int_0^\infty\{\mathbb E_xX_t-m_{\nu,\gamma}\}\,dt
\]
equation (7) implies
\[
 \frac1{d+\nu}\leq h_{\nu,\gamma}'(x)\leq\frac1d.           \tag{8}
\]
It also verifies \(-\mathcal L_{\nu,\gamma}h_{\nu,\gamma}=x-m_{\nu,\gamma}\); uniqueness from Theorem \(\mathrm{diffusion\mbox{-}poisson}\) identifies it with the flux solution.  Hence
\[
 \sup_x\left|h_{\nu,\gamma}'(x)-\frac1d\right|
 \leq\frac\nu{d(d+\nu)}.                                    \tag{9}
\]
Equations (1), (5), and (9) yield
\[
 V_\gamma
 =2q_\nu\int\{h_{\nu,\gamma}'(x)\}^2\pi_{\nu,\gamma}(x)\,dx
 \longrightarrow\frac{2q_0}{d^2}
 =\frac{2\lambda\tau}{(\lambda+\tau)^3}.                    \tag{10}
\]
This proves the first reduction.

For the fixed-system reduction, fix \(N\), an arm \(\ell\), and an interior treatment capacity.  Strict positivity of every available birth and death rate makes the finite chain irreducible.  On its scaled lattice set
\[
 f_{\ell,N}(x)=x-\mathbb E_{\rho_{\ell,N}}X_{\ell,N},
 \quad
 -G_{\ell,N}u_{\ell,N}=f_{\ell,N},
 \quad
 \mathbb E_{\rho_{\ell,N}}u_{\ell,N}=0.                     \tag{11}
\]
The centered solution exists uniquely because the generator's null space consists of constants.  If \(r_N(x,y)\) is the transition rate, put
\[
 c_{\ell,N}(x)=\sum_{y\ne x}r_N(x,y)
 \{u_{\ell,N}(y)-u_{\ell,N}(x)\}^2.                          \tag{12}
\]
Dynkin's identity gives
\[
 M_T=u_{\ell,N}(X_{\ell,N}(T))-u_{\ell,N}(X_{\ell,N}(0))
 +\int_0^Tf_{\ell,N}(X_{\ell,N}(t))\,dt,
 \quad
 \langle M\rangle_T=\int_0^Tc_{\ell,N}(X_{\ell,N}(t))\,dt. \tag{13}
\]
The generator product identity and stationarity show that
\[
 \begin{split}
 V_{\ell,N}
 &:=\mathbb E_{\rho_{\ell,N}}c_{\ell,N}(X_{\ell,N})\\
 &=-2\mathbb E_{\rho_{\ell,N}}
   [u_{\ell,N}G_{\ell,N}u_{\ell,N}]
 =2\mathbb E_{\rho_{\ell,N}}[u_{\ell,N}f_{\ell,N}].         \tag{14}
 \end{split}
\]
Reversibility and the spectral gap \(d\) from Lemma \(\mathrm{finite\mbox{-}chain\mbox{-}poisson\mbox{-}controls}\) give, by finite eigen-expansion,
\[
 \operatorname{Var}\!\left\{T^{-1}\int_0^T c_{\ell,N}(X_{\ell,N}(t))\,dt\right\}
 \leq\frac{2\operatorname{Var}_{\rho_{\ell,N}}(c_{\ell,N})}{dT}. \tag{15}
\]
For fixed \(N\), the variance in the numerator is finite, so
\[
 T^{-1}\langle M\rangle_T\longrightarrow V_{\ell,N}
 \quad\hbox{in probability}.                                  \tag{16}
\]
Every jump of \(M_T/\sqrt T\) is bounded by a fixed constant divided by \(\sqrt T\).  The compensated jump-exponential calculation used in Theorem \(\mathrm{uniform\mbox{-}joint\mbox{-}law}\), now with (16), gives
\[
 M_T/\sqrt T\Rightarrow\mathcal N(0,V_{\ell,N}).             \tag{17}
\]
The endpoint term in (13), divided by \(\sqrt T\), vanishes because the state space is finite.  Therefore
\[
 \frac1{\sqrt T}\int_0^T
 \{X_{\ell,N}(t)-\mathbb E_{\rho_{\ell,N}}X_{\ell,N}\}\,dt
 \Rightarrow\mathcal N(0,V_{\ell,N}),                        \tag{18}
\]
or equivalently
\[
 \sqrt{NT}\left[
 T^{-1}\int_0^T\frac{K_{\ell,N}(t)}N\,dt
 -\mathbb E_{\rho_{\ell,N}}\frac{K_{\ell,N}}N\right]
 \Rightarrow\mathcal N(0,V_{\ell,N}).                        \tag{19}
\]
The energy is positive: if (14) vanished, every edge difference of \(u_{\ell,N}\) would vanish; irreducibility would make \(u_{\ell,N}\) constant, contradicting (11) because the coordinate is nonconstant.

For the smallest interior treatment instance, \(N=2,M_N=1\), the unnormalized invariant weights are
\[
 1,qquad \frac{2\lambda}{\tau+\nu},
 \qquad\frac{2\lambda^2}{(\tau+\nu)(2\tau+\nu)}.            \tag{20}
\]
They are all positive.  If \(A_k=\sum_{j=0}^k\rho_{1,2}(j)f_{1,2}(j)\), exact flux summation gives
\[
 u_{1,2}(x_{k+1})-u_{1,2}(x_k)
 =-\frac{A_k}{\rho_{1,2}(k)B_k},
 \qquad
 V_{1,2}=2\sum_{k=0}^1\frac{A_k^2}{\rho_{1,2}(k)B_k}>0.       \tag{21}
\]
Indeed \(A_0<0\), and centering gives \(A_1=-\rho_{1,2}(2)f_{1,2}(2)<0\).  This checks the claim exactly at the smallest interior chain.  Lemma \(\mathrm{boutilier\mbox{-}anchor\mbox{-}scope}\) supplies only the stated compatibility comparison; none of (11)--(21) transfers this equal-arm result to the broader cited class.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The reductions verify the unconstrained diffusion formula and the internally derived fixed-state-space limit. \textit{Gap.} They are fidelity checks and make no upward transfer claim. \textit{Consumer.} Readers can compare against closed-form Ornstein--Uhlenbeck and finite-state benchmarks.

\begin{theorem}[thm:kink-residual-bound]\label{thm:kink-residual-bound}
There is a finite constant \(K_{\mathrm{res}}\), depending only on
\((c,C,G)\), such that for every \(N\geq N_0\),
\[
 \sup_{p\in\mathcal P,\,|\gamma|\leq G}
 \mathbb E_{\rho_{1,N}}
 \left|\{G_{1,N}-\mathcal L_{\gamma_N}\}
 h_{1,N}(X_{1,N})\right|
 \leq K_{\mathrm{res}}N^{-1/2}.
\]
This bound includes every state for which an available upward or downward
lattice jump crosses \(\gamma_N\).
\end{theorem}
\begin{proof}
Put \(\delta=N^{-1/2}\) and \(g=\gamma_N\).  The ceiling identity gives \(0\leq g-\gamma<\delta\), so \(|g|\leq G+1\).  Constants below are therefore uniform over the positive rate box and the enlarged compact staffing interval \([-G-1,G+1]\).  Write \(h_g=h_{p,g}\) and
\[
 b_g(x)=-dx-\nu\min(x,g).
\]
The synchronous-coupling argument in Theorem \(\mathrm{diffusion\mbox{-}poisson}\) is pointwise for every real kink location and therefore gives, at \(g=\gamma_N\),
\[
 \frac1D\leq h_g'(x)\leq\frac1d,                              \tag{1}
\]
and Definition \(\mathrm{diffusion\mbox{-}poisson}\) gives
\[
 qh_g''(x)=-(x-m_g)-b_g(x)h_g'(x).                             \tag{2}
\]

We first obtain derivative envelopes that remain valid at the kink.  The displayed piecewise-Gaussian invariant density has a common Gaussian tail on the compact parameter set.  Applying the stationary identity to smooth truncations of \(x^2\), then removing the truncation, gives
\[
 0=\int\mathcal L_gx^2\,d\pi_g
 =\int\{-2dx^2-2\nu x\min(x,g)+2q\}\,d\pi_g.                 \tag{3}
\]
Since \(x\min(x,g)\geq-(G+1)|x|\), Young's inequality in (3) proves
\[
 \sup_{p,|g|\leq G+1}\int x^2\pi_g(x)\,dx<\infty,
 \qquad
 \sup_{p,|g|\leq G+1}|m_g|<\infty.                            \tag{4}
\]
The quantities \(q^{-1},d,D,\nu\) are uniformly bounded on the rate box.  Equations (1)--(2) therefore imply
\[
 |h_g''(x)|\leq C_1(1+|x|).                                   \tag{5}
\]
On each open side of \(g\), differentiating (2) gives
\[
 qh_g'''(x)=-1-b_g'(x)h_g'(x)-b_g(x)h_g''(x),                 \tag{6}
\]
where \(b_g'=-D\) on the left and \(b_g'=-d\) on the right.  Thus
\[
 |h_g'''(x)|\leq C_2(1+x^2),\qquad x\ne g.                    \tag{7}
\]
The drift is continuous at \(g\); by (1)--(2), \(h_g''\) is also continuous there.  It is continuously differentiable on either side and its derivative has the local bound (7).  Hence \(h_g''\) is locally absolutely continuous across \(g\).  Taylor's integral remainder is therefore valid even when the increment interval crosses the kink; the value of \(h_g'''\) at the singleton \(\{g\}\) is irrelevant.

At \(x=(k-Na)/\sqrt N\), define the available birth and death rates
\[
 B_N(x)=\lambda(N-k),
 \qquad D_N(x)=\tau k+\nu\min(k,M_N).
\]
Direct algebra, including at the two endpoints where one rate is zero, gives
\[
 \delta\{B_N(x)-D_N(x)\}=b_g(x),                              \tag{8}
\]
and
\[
 \frac{\delta^2}{2}\{B_N(x)+D_N(x)\}
 =q+\frac\delta2\{(\tau-\lambda)x+\nu\min(x,g)\}.            \tag{9}
\]
Taylor expansion of each available lattice increment, using the absolute-continuity conclusion above, yields
\[
 \begin{split}
 G_{1,N}h_g(x)
 ={}&b_g(x)h_g'(x)
 +\left[q+\frac\delta2\{(\tau-\lambda)x+\nu\min(x,g)\}\right]h_g''(x)
 +R_N(x),\\
 |R_N(x)|
 \leq{}&\frac{\delta^3}{6}\{B_N(x)+D_N(x)\}
 \mathop{\operatorname{ess\,sup}}_{|u-x|\leq\delta}|h_g'''(u)|.
                                                                    \tag{10}
 \end{split}
\]
The rate bounds give \(B_N+D_N\leq3CN\).  Equations (5), (7), and \(|\min(x,g)|\leq|x|+G+1\) therefore imply
\[
 |R_N(x)|\leq C_3\delta(1+x^2),
 \qquad
 \left|\frac\delta2\{(\tau-\lambda)x+\nu\min(x,g)\}h_g''(x)\right|
 \leq C_4\delta(1+x^2).                                      \tag{11}
\]
Subtracting \(\mathcal L_gh_g=b_gh_g'+qh_g''\) from (10) proves the pointwise, boundary- and kink-inclusive estimate
\[
 |(G_{1,N}-\mathcal L_g)h_g(x)|
 \leq C_5N^{-1/2}(1+x^2),
 \qquad x\in E_{1,N}.                                        \tag{12}
\]

It remains to average (12) under the exact invariant law.  Applying (8)--(9) to \(x^2\) gives
\[
 G_{1,N}x^2
 =2xb_g(x)+2q+\delta\{(\tau-\lambda)x+\nu\min(x,g)\}.      \tag{13}
\]
The inequalities used above, \(\delta\leq1\), and Young's inequality give a uniform constant \(C_6\) such that
\[
 G_{1,N}x^2\leq-dx^2+C_6.                                    \tag{14}
\]
The finite chain starts in \(\rho_{1,N}\), so stationarity implies \(\mathbb E_{\rho_{1,N}}G_{1,N}X_{1,N}^2=0\).  Hence
\[
 \sup_{N,p,\gamma}\mathbb E_{\rho_{1,N}}X_{1,N}^2\leq C_6/d. \tag{15}
\]
Taking expectations in (12) and using (15) proves the theorem with
\[
 K_{\mathrm{res}}=C_5\left(1+\frac{C_6}{2c}\right),          \tag{16}
\]
after enlarging \(C_5,C_6\) to their compact-uniform values and using \(d\geq2c\).

The proof also performs the requested stress test.  The exact invariant product is
\[
 \rho_{1,N}(k)=Z_N^{-1}\prod_{j=0}^{k-1}
 \frac{\lambda(N-j)}{\tau(j+1)+\nu\min(j+1,M_N)},             \tag{17}
\]
with empty product one.  Estimate (12) holds before weighting by (17), with the same constant at every rate-box corner and every ceiling phase \(g-\gamma\in[0,\delta)\); thus product weighting cannot create a nonvanishing scaled residual.  For \(N=2,M_N=1\), the states \(0,1,2\) are the left endpoint, kink state, and right endpoint, and (8)--(12) cover all four directed available transitions.  For arbitrary \(N\), any jump crossing \(g\) is covered by the integral remainder in (10).

Finally, stationarity and \(-\mathcal L_gh_g=x-m_g\) give the useful consequence
\[
 \begin{split}
 \left|\mathbb E_{\rho_{1,N}}X_{1,N}-m_g\right|
 &=\left|\mathbb E_{\rho_{1,N}}
   \{(G_{1,N}-\mathcal L_g)h_g\}(X_{1,N})\right|\\
 &\leq K_{\mathrm{res}}N^{-1/2}.                              \tag{18}
 \end{split}
\]
Thus the early-kill alternative is excluded at endpoints, rate-box corners, kink-crossing edges, and all ceiling phases.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The pointwise Taylor envelope and stationary moment bound prove the previously isolated residual uniformly through the moving kink. \textit{Gap.} This theorem now closes, rather than merely states, the finite-to-diffusion centering remainder. \textit{Consumer.} It certifies when the simpler diffusion fitted center can replace exact product-law evaluation.

\begin{lemma}[lem:finite-chain-poisson-controls]\label{lem:finite-chain-poisson-controls}
For each arm \(\ell\in\{0,1\}\), let \(u_{\ell,N}\) be the stationary-mean-zero solution of
\[
 -G_{\ell,N}u_{\ell,N}=x-\mathbb E_{\rho_{\ell,N}}X_{\ell,N}.
\]
Uniformly over \(p\in\mathcal P\), \(|\gamma|\leq G\), and \(N\geq N_0\), the two birth--death chains have spectral gap at least \(d\), their centered scaled stationary variables have bounded moments through order six, and
\[
 0\leq \sqrt N\{u_{\ell,N}(x+N^{-1/2})-u_{\ell,N}(x)\}\leq d^{-1}
\]
on every available upward edge.  Moreover, the stationary edge averages of these scaled differences converge uniformly to \(d^{-1}\) in the control arm and to \(H=\int h_\gamma'\pi_\gamma\) in the treatment arm.  The same assertions hold for downward edges after the natural index shift.  Finally, time averages of every bounded normalized exposure or predictable-bracket integrand used by the occupation and marked-count martingales differ from their stationary means by \(o_{\mathbb P}(1)\), uniformly for every joint divergence \(N,T_N\to\infty\).
\end{lemma}
\begin{proof}
Write the birth and death rates in either arm as \(B_k\) and \(D_k\).  Lemma \(\mathrm{staffing\mbox{-}interior}\) and the strictly positive rate bounds make both finite chains irreducible.  Their invariant laws satisfy detailed balance,
\[
 \rho_N(k)B_k=\rho_N(k+1)D_{k+1},
 \qquad
 \rho_N(k)=\rho_N(0)\prod_{j=0}^{k-1}\frac{B_j}{D_{j+1}},    \tag{1}
\]
so both chains are reversible.

We first prove the spectral-gap claim.  Let \(-\mathcal A_N\phi=\kappa\phi\), and set \(g_k=\phi(k+1)-\phi(k)\).  Subtracting the eigenvalue equations at \(k+1\) and \(k\) gives
\[
 \begin{split}
 -\kappa g_k={}&B_{k+1}(g_{k+1}-g_k)+D_k(g_{k-1}-g_k)\\
 &-\{B_k-B_{k+1}+D_{k+1}-D_k\}g_k.                            \tag{2}
 \end{split}
\]
Terms with unavailable indices are absent.  In the treatment arm,
\[
 B_k-B_{k+1}+D_{k+1}-D_k
 =\lambda+\tau+\nu\{\min(k+1,M_N)-\min(k,M_N)\}\geq d;       \tag{3}
\]
in the control arm it equals \(d\).  At a positive maximum of a nonzero gradient, the first two terms on the right of (2) are nonpositive, so \(\kappa\geq d\).  Replacing \(\phi\) by \(-\phi\) handles a negative minimum.  Every nonconstant eigenfunction has a nonzero gradient; reversibility makes the eigenvalues real.  Thus the spectral gap is at least \(d\).

Put \(\delta=N^{-1/2}\).  In the treatment arm, with \(x=(k-Na)/\sqrt N\) and \(g=\gamma_N\), the exact identities are
\[
 \delta(B_k-D_k)=-dx-\nu\min(x,g),
 \qquad
 \frac{\delta^2}{2}(B_k+D_k)
 =q+\frac\delta2\{(\tau-\lambda)x+\nu\min(x,g)\}.            \tag{4}
\]
The control identities have drift \(-dx\) and leading second characteristic \(q_0\).  Since \(|g|\leq G+1\), applying the generator to \(x^{2r}\), for \(r=1,2,3\), and using
\[
 x\min(x,g)\geq-(G+1)|x|
\]
gives, by expansion and Young's inequality,
\[
 G_{\ell,N}x^{2r}\leq-c_r x^{2r}+C_r(1+x^{2r-2}).             \tag{5}
\]
Unavailable boundary jumps have zero rate, so (5) includes both endpoints.  Stationarity and induction yield
\[
 \sup_{N,p,\gamma,\ell}
 \mathbb E_{\rho_{\ell,N}}|X_{\ell,N}|^{2r}<\infty,
 \qquad r=1,2,3.                                               \tag{6}
\]

The ordered basic coupling of chains begun in adjacent count states preserves a discrepancy of one until coalescence.  Its coalescence hazard is the expression in (3), hence at least \(d\).  For the centered resolvent
\[
 u_{\ell,N}(x)=\int_0^\infty
 \mathbb E_x\{X_{\ell,N}(t)-\mathbb E_{\rho_{\ell,N}}X_{\ell,N}\}\,dt,
\]
the coupling therefore gives, on every available upward edge,
\[
 0\leq u_{\ell,N}(x+\delta)-u_{\ell,N}(x)
 \leq\int_0^\infty\delta e^{-dt}\,dt=\frac\delta d.          \tag{7}
\]
This resolvent is finite by the same contraction and solves the stated centered Poisson equation.  Downward edges give the identical assertion after shifting the index.

To identify the limiting edge averages, put
\[
 f_i=x_i-\mathbb E X_{\ell,N},
 \qquad
 \Delta_i=u_{\ell,N}(x_{i+1})-u_{\ell,N}(x_i).
\]
Multiplying the Poisson equation by \(\rho_N(i)\), using (1), and summing from zero through \(k\) gives the exact flux identity
\[
 \rho_N(k)B_k\Delta_k=-\sum_{i=0}^k\rho_N(i)f_i.              \tag{8}
\]
The compact-uniform invariant-law convergence in Theorem \(\mathrm{diffusion\mbox{-}poisson}\) sends the numerator in (8) to the corresponding diffusion flux and sends \(\rho_N(k)B_k/\sqrt N\) to \(q_0\pi_0(x)\) or \(q\pi_\gamma(x)\), locally uniformly.  Consequently
\[
 \sqrt N\Delta_k\longrightarrow h_0'(x)=d^{-1}
 \quad\hbox{or}\quad h_\gamma'(x)                            \tag{9}
\]
locally uniformly.  Bound (7), moment bound (6), and truncation outside a compact interval permit dominated convergence in every stationary edge average.  Since
\[
 \frac{K_{0,N}}N\to a_0,
 \qquad \frac{K_{1,N}}N\to a,
 \qquad \frac{\min(K_{1,N},M_N)}N\to a                       \tag{10}
\]
in \(L^2\), equation (9) proves the asserted control average \(d^{-1}\), treatment average \(H=\int h_\gamma'\pi_\gamma\), and their downward-edge counterparts.

Finally, let \(r_N\) be any bounded normalized exposure or predictable-bracket integrand appearing in the occupation and marked-count martingales.  The rate bounds and (7) give a bound on \(\|r_N\|_\infty\) independent of \(N,p,\gamma\).  Expanding \(r_N-\mathbb E_\rho r_N\) in the finite reversible eigenbasis and using the spectral gap gives
\[
 \begin{split}
 \operatorname{Var}\!\left\{T^{-1}\int_0^T r_N(K(t))\,dt\right\}
 &=\frac2{T^2}\int_0^T(T-s)
   \operatorname{Cov}_\rho\{r_N(K(0)),r_N(K(s))\}\,ds\\
 &\leq\frac{2\operatorname{Var}_\rho(r_N)}{dT}.               \tag{11}
 \end{split}
\]
Assumption \(T_N\to\infty\) and Chebyshev's inequality make (11) \(o_{\mathbb P}(1)\), uniformly and without an upper relation between \(T_N\) and \(N\).  Equations (3), (6), (7), (9), and (11) prove all parts of the lemma.
\end{proof}

\begin{lemma}[lem:exact-fixed-capacity-derivative]\label{lem:exact-fixed-capacity-derivative}
For the exact fixed-capacity effect map \(\vartheta_N\) of Definition \(\mathrm{fitted\mbox{-}center}\),
\[
 \vartheta_N(p;M_N)=\theta_N,
 \qquad
 \sup_{p\in\mathcal P,\,|\gamma|\leq G}
 \left\|\nabla_{p'}\vartheta_N(p';M_N)\big|_{p'=p}-A\right\|\longrightarrow0. \tag{11}
\]
On \([c/2,2C]^3\), for every fixed interior capacity \(M\), the map \(p'\mapsto\vartheta_N(p';M)\) is twice continuously differentiable and
\[
 \sup_{p'\in[c/2,2C]^3,\,1\leq M\leq N-1}
 \|\nabla_{p'}^2\vartheta_N(p';M)\|\leq C_*\sqrt N             \tag{12}
\]
for a finite constant \(C_*\) depending only on \((c,C)\).
\end{lemma}
\begin{proof}
The product weights in Definition \(\mathrm{fitted\mbox{-}center}\) satisfy the detailed-balance ratios of the control and treatment generators.  By Lemma \(\mathrm{staffing\mbox{-}interior}\), both chains are irreducible.  Their normalized product weights are therefore their unique invariant laws.  At \(p'=p\) and \(M=M_N\), this proves
\[
 \vartheta_N(p;M_N)=\theta_N.                                 \tag{1}
\]

Let \(\mu_{1,N}(p';M)=\sum_k(k/N)\rho_{1,N}^{p',M}(k)\).  For fixed \(M\), let \(v_N\) be the centered solution of
\[
 -\mathcal A_{1,N}^{p',M}v_N=k/N-\mu_{1,N}(p';M).             \tag{2}
\]
Differentiate the finite identity \(\rho_{1,N}^{p',M}\mathcal A_{1,N}^{p',M}=0\) in a rate coordinate \(r\), apply the differentiated signed measure to \(v_N\), and use (2).  Ordinary finite-sum algebra gives
\[
 \partial_r\mu_{1,N}(p';M)
 =\mathbb E_{\rho_{1,N}^{p',M}}
 [\{\partial_r\mathcal A_{1,N}^{p',M}\}v_N].                \tag{3}
\]
No interchange with an infinite limit occurs.

At \(p'=p,M=M_N\), equation (2) and the definition of \(u_{1,N}\) in Lemma \(\mathrm{finite\mbox{-}chain\mbox{-}poisson\mbox{-}controls}\) imply
\[
 v_N(k)=N^{-1/2}u_{1,N}(s_{1,N}(k)).                           \tag{4}
\]
Define
\[
 R_N^+(k)=N\{v_N(k+1)-v_N(k)\},
 \qquad
 R_N^-(k)=N\{v_N(k)-v_N(k-1)\}.
\]
Substitution of the three generator derivatives in (3) yields
\[
 \begin{split}
 \partial_\lambda\mu_{1,N}
 &=\mathbb E[(1-K_{1,N}/N)R_N^+(K_{1,N})],\\
 \partial_\tau\mu_{1,N}
 &=-\mathbb E[(K_{1,N}/N)R_N^-(K_{1,N})],\\
 \partial_\nu\mu_{1,N}
 &=-\mathbb E[(\min\{K_{1,N},M_N\}/N)R_N^-(K_{1,N})].        \tag{5}
 \end{split}
\]
The edge-average limits and exposure limits in Lemma \(\mathrm{finite\mbox{-}chain\mbox{-}poisson\mbox{-}controls}\) now give, uniformly,
\[
 \nabla_p\mu_{1,N}(p;M_N)
 \longrightarrow((1-a)H,-aH,-aH)^{\mathsf T}.                \tag{6}
\]
The control product law is binomial with normalized mean \(a_0\), so
\[
 \nabla a_0=((1-a_0)/d,-a_0/d,0)^{\mathsf T}.                 \tag{7}
\]
Subtracting (6) from (7) proves
\[
 \nabla_{p'}\vartheta_N(p';M_N)|_{p'=p}
 \longrightarrow((1-a_0)/d-(1-a)H,-a_0/d+aH,aH)^{\mathsf T}=A. \tag{8}
\]

It remains to prove the Hessian envelope.  For either product law write \(S_r(k)=\partial_r\log w_N(k)\).  On \([c/2,2C]^3\), the one-step increments of \(S_r\) and \(\partial_sS_r\) are bounded by constants depending only on \((c,C)\).  For example,
\[
 \partial_\lambda\log w_N(k)=\frac{k}{\lambda},
 \quad
 \partial_\tau\log w_{1,N}(k)
 =-\sum_{j=1}^k\frac{j}{\tau j+\nu\min(j,M)},
\]
\[
 \partial_\nu\log w_{1,N}(k)
 =-\sum_{j=1}^k\frac{\min(j,M)}{\tau j+\nu\min(j,M)}.        \tag{9}
\]
The same claim for second derivatives follows by differentiating the summands; every denominator is at least \((c/2)j\).

For arbitrary \(1\leq M\leq N-1\), the count-scale drift \(B_k-D_k\) decreases by at least \(\lambda'+\tau'\geq c\) at every step, and the total jump rate is at most \(6CN\).  Let \(k_*\) be a sign-change point of the drift.  Expanding the generator on \((k-k_*)^{2r}\), the first-order term is at most \(-c_1|k-k_*|^{2r}\) outside a fixed multiple of \(\sqrt N\), while all jump remainders are at most \(C_rN(1+|k-k_*|^{2r-2})\).  Stationarity and induction for \(r=1,2,3\) give
\[
 \mathbb E|K-k_*|^{2r}\leq C_rN^r,
 \qquad r=1,2,3.                                               \tag{10}
\]
In particular, \(|\mathbb EK-k_*|\leq C\sqrt N\), so (10) also holds with \(k_*\) replaced by \(\mathbb EK\).  Because the scores in (9) and their parameter derivatives are uniformly Lipschitz in \(k\),
\[
 \|S_r(K)-\mathbb ES_r(K)\|_{L^6}\leq C\sqrt N,
 \quad
 \|\partial_sS_r(K)-\mathbb E\partial_sS_r(K)\|_{L^2}
 \leq C\sqrt N.                                               \tag{11}
\]

Finite-sum differentiation gives, for \(f(K)=K/N\),
\[
 \partial_r\mathbb Ef(K)=\operatorname{Cov}(f(K),S_r(K)),     \tag{12}
\]
and
\[
 \begin{split}
 \partial_s\partial_r\mathbb Ef(K)
 ={}&\mathbb E[(f-\mathbb Ef)(S_r-\mathbb ES_r)(S_s-\mathbb ES_s)]\\
 &+\operatorname{Cov}(f,\partial_sS_r).                        \tag{13}
 \end{split}
\]
Equation (10) gives \(\|f-\mathbb Ef\|_{L^6}\leq C N^{-1/2}\).  H\"older's inequality, (11), and (13) bound the first term by \(C\sqrt N\) and the second by \(C\).  Applying this to both arms proves
\[
 \sup_{p'\in[c/2,2C]^3,\,1\leq M\leq N-1}
 \|\nabla_{p'}^2\vartheta_N(p';M)\|\leq C_*\sqrt N.          \tag{14}
\]
Equations (1), (8), and (14) prove the lemma.
\end{proof}

\begin{lemma}[lem:boutilier-anchor-scope]\label{lem:boutilier-anchor-scope}
\texttt{The model in Boutilier,\allowbreak{} Jonasson,\allowbreak{} Li,\allowbreak{} and Yoeli permits distinct fixed arm populations and a separately specified service capacity,\allowbreak{} and its fixed-\allowbreak{}system occupation limit and many-\allowbreak{}system fluid limit are separate results.}
\end{lemma}

\begin{proposition}[prop:diffusion-fitted-center-corollary]\label{prop:diffusion-fitted-center-corollary}
In the additional subregime \(T_N/N\to0\), uniformly over \(\mathcal M_{\mathrm{fit}}\),
\[
 \sqrt{NT_N}(\overline\theta_N-\theta_N)\Rightarrow\mathcal N(0,Q),
 \qquad
 \mathbb P\{\theta_N\in I_{\mathrm{diff}}\}=1-\alpha+o(1).
\]
Moreover \(\overline\theta_N-\widetilde\theta_N=O_{\mathbb P}(N^{-1})\) uniformly.  Thus the diffusion fitted center is an \(O(N^{-1})\)-accurate corollary, and the available residual bound makes \(T_N/N\to0\) sufficient for its Wald limit; this restriction is not imposed on exact finite-chain fitted-center inference.
\end{proposition}
\begin{proof}
Let \(p'\in[c/2,2C]^3\), \(1\leq M\leq N-1\), and put
\[
 g'=\frac{M-Na(p')}{\sqrt N}.
\]
The control invariant mean divided by \(N\) is exactly \(a_0(p')\).  By the definition of the scaled treatment state, its invariant mean divided by \(N\) is
\[
 a(p')+N^{-1/2}\mathbb E_{\rho_{1,N}^{p',M}}X_{1,N}^{p',M}.
\]
Therefore
\[
 \begin{split}
 &\vartheta_N(p';M)-
 \{a_0(p')-a(p')-N^{-1/2}m_{p',g'}\}\\
 &\qquad=-N^{-1/2}
 \{\mathbb E_{\rho_{1,N}^{p',M}}X_{1,N}^{p',M}-m_{p',g'}\}. \tag{1}
 \end{split}
\]

Apply Theorem \(\mathrm{kink\mbox{-}residual\mbox{-}bound}\) with the compact rate bounds \(c/2,2C\) and staffing radius \(G+1\).  Its enrollment threshold is finite, so \(N\to\infty\) eventually exceeds it.  The centering consequence proved there turns (1) into
\[
 \left|\vartheta_N(p';M)-
 \{a_0(p')-a(p')-N^{-1/2}m_{p',g'}\}\right|
 \leq K N^{-1}                                                \tag{2}
\]
uniformly whenever \(|g'|\leq G+1\).

The rate-score coordinate of Theorem \(\mathrm{uniform\mbox{-}joint\mbox{-}law}\) gives
\[
 \widehat p-p=O_{\mathbb P}((NT_N)^{-1/2}).                   \tag{3}
\]
Smoothness of \(a\) on the compact positive rate box yields
\[
 \widehat\gamma_N-\gamma_N
 =-\sqrt N\{a(\widehat p)-a(p)\}=O_{\mathbb P}(T_N^{-1/2}),
 \qquad 0\leq\gamma_N-\gamma<N^{-1/2}.                       \tag{4}
\]
Since \(T_N\to\infty\), the event \(\widehat p\in[c/2,2C]^3\) and \(|\widehat\gamma_N|\leq G+1\) has probability tending to one uniformly.  Take \(p'=\widehat p,M=M_N\) in (2) on this event.  Definition \(\mathrm{fitted\mbox{-}center}\) then gives
\[
 \overline\theta_N-\widetilde\theta_N=O_{\mathbb P}(N^{-1}) \tag{5}
\]
uniformly.  Under \(T_N/N\to0\),
\[
 \sqrt{NT_N}(\overline\theta_N-\widetilde\theta_N)
 =O_{\mathbb P}(\sqrt{T_N/N})=o_{\mathbb P}(1).               \tag{6}
\]
Theorem \(\mathrm{fitted\mbox{-}center\mbox{-}wald}\) gives
\[
 \sqrt{NT_N}(\widetilde\theta_N-\theta_N)
 \Rightarrow\mathcal N(0,Q),
 \qquad \widehat Q/Q\to1,                                    \tag{7}
\]
uniformly, with \(Q\) bounded away from zero.  Equations (6)--(7) prove
\[
 \sqrt{NT_N}(\overline\theta_N-\theta_N)
 \Rightarrow\mathcal N(0,Q).
\]
Studentization by \(\widehat Q\), \(\alpha\in(0,1)\), and integration of the normal density over \([-z_\alpha,z_\alpha]\) yield
\[
 \sup_{p\in\mathcal P,\,|\gamma|\leq G}
 \left|\mathbb P_{p,\gamma}\{\theta_N\in I_{\mathrm{diff}}\}-(1-\alpha)\right|
 \longrightarrow0.
\]
Equation (5) proves the claimed \(O_{\mathbb P}(N^{-1})\) approximation, and (6) isolates the sole use of the fitted-horizon condition.
\end{proof}
\par\smallskip\noindent \textit{Justification.} This corollary isolates the sole use of the fitted-horizon condition. \textit{Gap.} It quantifies the price of replacing exact product-law centering by the diffusion mean. \textit{Consumer.} Implementers may choose the cheaper diffusion center when monitoring duration grows more slowly than enrollment.

\begin{theorem}[thm:oracle-conditional-capacity-certificate]\label{thm:oracle-conditional-capacity-certificate}
Conditional on a supplied input name and a supplied sound pair \((L_J,\mathfrak O)\) satisfying Definition \(\mathrm{interval\mbox{-}oracle}\) on every call made by Definition \(\mathrm{certificate\mbox{-}algorithm}\), for every rational \(\epsilon>0\) that algorithm terminates and returns a certificate \(\mathrm{Cert}\), an incumbent \(\gamma_\epsilon\in[-G,G]\), and rational numbers \(L_\epsilon,U_\epsilon\) such that
\[
 L_\epsilon\leq \inf_{|\gamma|\leq G}J(p,\gamma)
 \leq J(p,\gamma_\epsilon)\leq U_\epsilon,
 \qquad U_\epsilon-L_\epsilon\leq\epsilon.
\]
Every global minimizer, if one exists, lies in the certificate's unpruned interval cover, and every pruning decision is validated by a recorded oracle lower bound.  For \(G>0\), the full-level fallback makes at most \(2^{D_\epsilon+1}-1\) interval evaluations and fewer than \(3\cdot2^{D_\epsilon+1}\) interval-objective or point-oracle requests, all at precision at least \(m_\epsilon\).  For \(G=0\), one certified point evaluation suffices.  This theorem constructs no model-specific oracle and its certificate is checkable only relative to the supplied sound receipts.
\end{theorem}
\begin{proof}
Let
\[
 \mu:=\inf_{|\gamma|\leq G}J(p,\gamma).
\]
The supplied oracle encloses the finite continuous objective on every interval it evaluates.  Because \(G\in\mathbb Q_{\geq0}\) is supplied exactly, rational comparison decides whether \(G=0\).

Suppose first that \(G>0\).  Definition \(\mathrm{certificate\mbox{-}algorithm}\) determines \(D_\epsilon\) by enumerating nonnegative integers until
\[
 8GL_J\leq\epsilon2^{D_\epsilon}.
\tag{1}
\]
This enumeration terminates because \(\epsilon>0\), and every comparison is between rationals.  Every interval \(I\) at depth \(D_\epsilon\) in the dyadic tree rooted at the rational interval \([-G,G]\) has rational endpoints and
\[
 \operatorname{diam}(I)=2G2^{-D_\epsilon}.
\]
Consequently (1) gives
\[
 L_J\operatorname{diam}(I)
 =2GL_J2^{-D_\epsilon}\leq\frac\epsilon4.
\tag{2}
\]
At depth \(r\), the algorithm uses precision at least \(m_\epsilon+r\).  Thus at the terminal level
\[
 e_\epsilon:=2^{-(m_\epsilon+D_\epsilon)}
 \leq2^{-m_\epsilon}\leq\frac\epsilon8,
\tag{3}
\]
by the definition of \(m_\epsilon\).  The interval-oracle contract and (2)--(3) show that every terminal interval receipt has width at most
\[
 w_\epsilon:=L_J\operatorname{diam}(I)+e_\epsilon
 \leq\frac{3\epsilon}{8},
\tag{4}
\]
and every terminal midpoint receipt has width at most \(e_\epsilon\).

Let \(U_t\) be the incumbent upper endpoint at any finite stage and let \(U_\epsilon\) be the final incumbent.  Since an incumbent is the upper endpoint of a point receipt containing its objective value,
\[
 U_t\geq\mu,
 \qquad U_t\geq U_\epsilon.
\tag{5}
\]
If an interval \(I\) contains a global minimizer \(\gamma^*\), oracle inclusion yields
\[
 \underline J(I,m)\leq J(p,\gamma^*)=\mu\leq U_t.
\tag{6}
\]
The strict pruning rule therefore never removes an interval containing a global minimizer.  This proves the asserted cover property without convexity, uniqueness, or interiority.

We next show that the terminal active family is nonempty and brackets \(\mu\).  If \(U_\epsilon>\mu\), then for every \(0<\delta<U_\epsilon-\mu\), the definition of infimum supplies \(\eta_\delta\in[-G,G]\) such that
\[
 J(p,\eta_\delta)<\mu+\delta<U_\epsilon\leq U_t.
\tag{7}
\]
Every ancestor containing \(\eta_\delta\) has lower receipt no greater than \(J(p,\eta_\delta)\), so (7) prevents its pruning.  If \(U_\epsilon=\mu\), let \(u\) be the evaluated point furnishing the final incumbent.  Its point receipt gives
\[
 \mu\leq J(p,u)\leq U_\epsilon=\mu,
\tag{8}
\]
and the same argument prevents pruning of every ancestor containing \(u\).  Thus the terminal active family is nonempty in both cases.

Define \(L_\epsilon\) as the minimum lower endpoint over active terminal interval receipts, as in Definition \(\mathrm{capacity\mbox{-}certificate}\).  For any active terminal interval \(I\) and any \(\eta\in I\), oracle inclusion and (4) imply
\[
 \underline J(I,m)\geq J(p,\eta)-w_\epsilon\geq\mu-w_\epsilon.
\]
Taking the minimum gives
\[
 L_\epsilon\geq\mu-w_\epsilon.
\tag{9}
\]
If \(U_\epsilon>\mu\), the active interval supplied by (7) has lower endpoint below \(\mu+\delta\) for every sufficiently small \(\delta>0\), hence \(L_\epsilon\leq\mu\).  If \(U_\epsilon=\mu\), the active interval from (8) has lower endpoint at most \(J(p,u)=\mu\).  Therefore
\[
 L_\epsilon\leq\mu.
\tag{10}
\]

If \(U_\epsilon=\mu\), its desired upper bound is immediate.  Otherwise, take \(\eta_\delta\) from (7), let \(I_\delta\) be its active terminal interval, and let \(u_\delta\) be that interval's evaluated midpoint.  Since the interval receipt contains both objective values, (4) gives
\[
 J(p,u_\delta)\leq J(p,\eta_\delta)+w_\epsilon
 <\mu+\delta+w_\epsilon.
\tag{11}
\]
The corresponding point-receipt upper endpoint is at most \(J(p,u_\delta)+e_\epsilon\), and the final incumbent is no larger.  Letting \(\delta\downarrow0\) and using (3)--(4),
\[
 U_\epsilon\leq\mu+w_\epsilon+e_\epsilon
 \leq\mu+\frac\epsilon2.
\tag{12}
\]
Equations (9), (10), and (12) yield
\[
 U_\epsilon-L_\epsilon
 \leq2w_\epsilon+e_\epsilon
 \leq\frac{7\epsilon}{8}<\epsilon.
\tag{13}
\]
Since \(\gamma_\epsilon\) is the incumbent point, its receipt and (10) also give
\[
 L_\epsilon\leq\mu
 \leq J(p,\gamma_\epsilon)\leq U_\epsilon.
\tag{14}
\]

If an interval was pruned when the incumbent was \(U_t\), its recorded lower endpoint exceeds \(U_t\), which is at least \(U_\epsilon\) by (5).  Hence every pruning record satisfies the certificate inequality \(\underline J_r>U_\epsilon\).  Equations (6), (10), (13), and (14), with the retained receipts, verify all remaining semantic clauses of Definition \(\mathrm{capacity\mbox{-}certificate}\).

The full binary tree through depth \(D_\epsilon\) has
\[
 \sum_{r=0}^{D_\epsilon}2^r=2^{D_\epsilon+1}-1
\tag{15}
\]
nodes.  Hence the fallback makes at most that many interval evaluations.  With at most one interval request and one midpoint request per node, the total is at most
\[
 2(2^{D_\epsilon+1}-1)<3\cdot2^{D_\epsilon+1}.
\tag{16}
\]
All requested precisions are at least \(m_\epsilon\), and pruning can only decrease the count.

Finally, if the exact rational comparison gives \(G=0\), the feasible set is the singleton \(\{0\}\).  Query it at precision \(m_\epsilon\), put \(\gamma_\epsilon=0\), and use the rational receipt endpoints as \(L_\epsilon,U_\epsilon\).  Then
\[
 L_\epsilon\leq J(p,0)\leq U_\epsilon,
 \qquad U_\epsilon-L_\epsilon\leq2^{-m_\epsilon}
 \leq\frac\epsilon8<\epsilon.
\]
This one-record certificate completes the proof in the remaining case.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The theorem gives finite certificate semantics and a worst-case dyadic termination bound computed entirely by rational comparisons, including nonconvex objectives and boundary optima. \textit{Gap.} It remains a generic result conditional on sound receipts; the separate model-specific theorem supplies such receipts for this objective. \textit{Consumer.} Instantiating it with the constructed oracle produces a checkable global staffing certificate on the exact rational feasible interval.

\begin{lemma}[lem:stationary-poincare]\label{lem:stationary-poincare}
For every \(p\in\mathcal P\) and \(\gamma\in[-G,G]\), write \(P_{\gamma,t}\) for the stationary semigroup generated by \(\mathcal L_\gamma\).  The potential \(-\log\pi_\gamma\), up to its normalizing constant, is continuously differentiable and piecewise quadratic, with second derivative at least \(d/q\) away from \(\gamma\).  If \(f\) is locally absolutely continuous and \(f,f'\in L^2(\pi_\gamma)\), then
\[
 \operatorname{Var}_{\pi_\gamma}(f)
 \leq \frac qd\int_{\mathbb R}\{f'(x)\}^2\pi_\gamma(x)\,dx .
\]
\end{lemma}
\begin{proof}
Fix \(p\in\mathcal P\) and \(\gamma\in[-G,G]\), and write
\[
 b_\gamma(x)=-dx-\nu\min(x,\gamma).
\]
The displayed density satisfies \(q(\log\pi_\gamma)'=b_\gamma\) on both sides of the kink.  The one-sided derivatives agree at \(x=\gamma\), because both equal \(-D\gamma/q\).  Hence the potential \(U_\gamma=-\log\pi_\gamma\), up to its normalizing constant, is continuously differentiable and
\[
 U_\gamma''(x)=
 \begin{cases}
 D/q,&x<\gamma,\\
 d/q,&x>\gamma.
 \end{cases}                                                   \tag{1}
\]
The rate assumptions give \(D>d>0\) and \(q>0\), proving the curvature assertion.

Let \(X_t^x,X_t^y\) solve the diffusion with the same Brownian path.  Monotonicity of \(u\mapsto\min(u,\gamma)\) gives
\[
 (x-y)\{b_\gamma(x)-b_\gamma(y)\}\leq-d(x-y)^2.              \tag{2}
\]
The Brownian terms cancel in the difference, so integration of (2) yields
\[
 |X_t^x-X_t^y|\leq e^{-dt}|x-y|.                              \tag{3}
\]
For smooth compactly supported \(f\), difference quotients in the initial state and (3) imply
\[
 |(P_{\gamma,t}f)'(x)|
 \leq e^{-dt}P_{\gamma,t}|f'|(x).                             \tag{4}
\]
One way to remove the single nondifferentiability point is to approximate the drift by smooth drifts whose derivatives remain in \([-D,-d]\); (3) is preserved, and dominated convergence gives (4).  Jensen's inequality and invariance of \(\pi_\gamma\) give
\[
 \int\{(P_{\gamma,t}f)'\}^2\,d\pi_\gamma
 \leq e^{-2dt}\int P_{\gamma,t}(f'^2)\,d\pi_\gamma
 =e^{-2dt}\int(f')^2\,d\pi_\gamma.                           \tag{5}
\]

The identity \(q(\log\pi_\gamma)'=b_\gamma\), integrated on the two half-lines, gives
\[
 -\int u\mathcal L_\gamma v\,d\pi_\gamma
 =q\int u'v'\,d\pi_\gamma                                  \tag{6}
\]
for smooth compactly supported \(u,v\); the two boundary terms at \(\gamma\) cancel because \(u,v,\pi_\gamma\) are continuous there.  Apply (6) with \(u=v=P_{\gamma,t}f\).  The semigroup equation gives
\[
 -\frac{d}{dt}\operatorname{Var}_{\pi_\gamma}(P_{\gamma,t}f)
 =2q\int\{(P_{\gamma,t}f)'\}^2\,d\pi_\gamma.                \tag{7}
\]
Couple a process from a fixed initial state to an independent stationary initial state.  Equation (3) and the Gaussian second moment of \(\pi_\gamma\) show that \(P_{\gamma,t}f\to\int f\,d\pi_\gamma\) in \(L^2(\pi_\gamma)\).  Integrating (7) from zero to infinity and using (5),
\[
 \begin{split}
 \operatorname{Var}_{\pi_\gamma}(f)
 &=2q\int_0^\infty\int\{(P_{\gamma,t}f)'\}^2\,d\pi_\gamma\,dt\\
 &\leq2q\int_0^\infty e^{-2dt}\,dt\int(f')^2\,d\pi_\gamma
 =\frac qd\int(f')^2\,d\pi_\gamma.                           \tag{8}
 \end{split}
\]
For a locally absolutely continuous \(f\) with \(f,f'\in L^2(\pi_\gamma)\), truncate and mollify.  The functions and their derivatives converge in \(L^2(\pi_\gamma)\), so both sides of (8) converge.  This proves the lemma with all boundary and integrability conditions explicit.
\end{proof}

\begin{lemma}[lem:gamma-energy-lipschitz]\label{lem:gamma-energy-lipschitz}
For every \(p\in\mathcal P\), the map \(\gamma\mapsto V_\gamma(p)\) is continuously differentiable on the maintained staffing interval, with one-sided derivatives at its endpoints, and
\[
 \left|\frac{\partial V_\gamma(p)}{\partial\gamma}\right|
 \leq \frac{5\nu\sqrt q}{d^{5/2}}.
\]
Consequently, for all \(\gamma_1,\gamma_2\in[-G,G]\),
\[
 |V_{\gamma_1}(p)-V_{\gamma_2}(p)|
 \leq \frac{5\nu\sqrt q}{d^{5/2}}|\gamma_1-\gamma_2|.
\]
\end{lemma}
\begin{proof}
Fix \(p\in\mathcal P\).  Use the unnormalized invariant density
\[
 w_\gamma(x)=
 \begin{cases}
 \exp\{-Dx^2/(2q)\},&x\leq\gamma,\\
 \exp\{-dx^2/(2q)-\nu\gamma x/q+\nu\gamma^2/(2q)\},&x>\gamma.
 \end{cases}                                                   \tag{1}
\]
Its branches and their first derivatives in \(x\) agree at \(x=\gamma\).  For fixed \(x\ne\gamma\),
\[
 r_\gamma(x):=\partial_\gamma\log w_\gamma(x)
 =-\frac\nu q(x-\gamma)_+,
 \qquad
 \dot b_\gamma(x):=\partial_\gamma b_\gamma(x)
 =-\nu\mathbf1\{x>\gamma\}.                                 \tag{2}
\]
On every compact set of \(\gamma\)-values, (1)--(2) and their difference quotients are dominated by a polynomial times a common Gaussian density.  Dominated convergence therefore gives, in weighted \(L^1\),
\[
 \partial_\gamma\pi_\gamma(x)
 =\pi_\gamma(x)\{r_\gamma(x)-\mathbb E_{\pi_\gamma}r_\gamma(X)\}, \tag{3}
\]
and permits differentiation of all expectations below.

We justify differentiation of the Poisson solution.  For \(u\ne0\), subtract
\(-\mathcal L_{\gamma+u}h_{\gamma+u}=x-m_{\gamma+u}\) and
\(-\mathcal L_\gamma h_\gamma=x-m_\gamma\).  With difference quotients \(D_uh=(h_{\gamma+u}-h_\gamma)/u\), \(D_ub=(b_{\gamma+u}-b_\gamma)/u\), and \(D_um=(m_{\gamma+u}-m_\gamma)/u\), one gets
\[
 -\mathcal L_\gamma D_uh=D_ub\,h_{\gamma+u}'-D_um.           \tag{4}
\]
Integration against \(\pi_\gamma\) shows that the right side is centered.  Monotonicity of the kink gives \(|D_ub|\leq\nu\), and Theorem \(\mathrm{diffusion\mbox{-}poisson}\) gives \(0<h_{\gamma+u}'\leq d^{-1}\).  Thus the variance of the right side is at most \(\nu^2/d^2\).  Multiply (4) by the centered version of \(D_uh\), use the integration-by-parts identity in Lemma \(\mathrm{stationary\mbox{-}poincare}\), Cauchy--Schwarz, and that lemma's inequality:
\[
 \begin{split}
 q\mathbb E_{\pi_\gamma}\{(D_uh')^2\}
 &\leq\sqrt{\operatorname{Var}_{\pi_\gamma}(D_uh)}\frac\nu d\\
 &\leq\sqrt{\frac qd\mathbb E_{\pi_\gamma}\{(D_uh')^2\}}\frac\nu d.
                                                                    \tag{5}
 \end{split}
\]
Consequently
\[
 \mathbb E_{\pi_\gamma}\{(D_uh')^2\}
 \leq\frac{\nu^2}{qd^3}.                                     \tag{6}
\]

The flux formula, the Gaussian domination after (2), and (6) imply that the difference quotients converge in the stationary Sobolev norm.  Indeed, every weakly convergent subsequence passes in (4) to the same centered equation, while applying (5) to the difference of two such equations gives strong convergence and uniqueness.  If \(k_\gamma=\partial_\gamma h_\gamma\), the limit satisfies
\[
 -\mathcal L_\gamma k_\gamma
 =\dot b_\gamma h_\gamma'-m_\gamma',
 \qquad
 m_\gamma'=\mathbb E_{\pi_\gamma}(\dot b_\gamma h_\gamma'), \tag{7}
\]
and
\[
 \mathbb E_{\pi_\gamma}\{(k_\gamma')^2\}
 \leq\frac{\nu^2}{qd^3}.                                     \tag{8}
\]
The second identity in (7) follows by integrating the first; it also verifies the centering needed for inversion of \(-\mathcal L_\gamma\).

Since \(r_\gamma'(x)=-(\nu/q)\mathbf1\{x>\gamma\}\) almost everywhere, Lemma \(\mathrm{stationary\mbox{-}poincare}\) gives
\[
 \operatorname{Var}_{\pi_\gamma}(r_\gamma)
 \leq\frac qd\mathbb E_{\pi_\gamma}\{(r_\gamma')^2\}
 \leq\frac{\nu^2}{qd}.                                       \tag{9}
\]
Also \(1/D\leq h_\gamma'\leq1/d\), so the elementary range bound for a variance gives
\[
 \operatorname{Var}_{\pi_\gamma}(h_\gamma'^2)
 \leq\frac1{4d^4}.                                            \tag{10}
\]
Differentiate \(V_\gamma=2q\mathbb E_{\pi_\gamma}(h_\gamma'^2)\), using (3), (7), and the strong Sobolev convergence:
\[
 V_\gamma'
 =4q\mathbb E_{\pi_\gamma}(h_\gamma'k_\gamma')
 +2q\operatorname{Cov}_{\pi_\gamma}(h_\gamma'^2,r_\gamma). \tag{11}
\]
Cauchy--Schwarz, (8)--(10), and \(\mathbb E h_\gamma'^2\leq d^{-2}\) imply
\[
 \begin{split}
 |V_\gamma'|
 &\leq4q\left(\frac1{d^2}\frac{\nu^2}{qd^3}\right)^{1/2}
 +2q\left(\frac1{4d^4}\frac{\nu^2}{qd}\right)^{1/2}\\
 &=\frac{5\nu\sqrt q}{d^{5/2}}.                              \tag{12}
 \end{split}
\]
The common domination in (2) and strong convergence above show that (11) is continuous in the interior of the staffing interval and has the corresponding one-sided limits at its endpoints.  Integrating (12) between \(\gamma_1\) and \(\gamma_2\) proves
\[
 |V_{\gamma_1}(p)-V_{\gamma_2}(p)|
 \leq\frac{5\nu\sqrt q}{d^{5/2}}|\gamma_1-\gamma_2|.
\]
This proves every assertion of the lemma.
\end{proof}

\section{Interpretation}

The exact fitted interval targets the implemented stationary operational effect, not a diffusion estimand: at the true rates the product-law map equals \(\theta_N\) exactly. Same-path rate fitting projects the empirical occupation fluctuation onto the marked rate scores, producing fitted-center variance \(Q\) and empirical-minus-fitted variance \(W=V-Q\). The diffusion center is computationally simpler but inherits \(T_N=o(N)\) from its proved \(O(N^{-1})\) approximation error. For design, an exact rational radius makes the feasible root and its boundary points exactly representable; the constructed rational oracle encloses the limiting capacity--precision objective, the generic certificate theorem terminates after a comparison-defined dyadic depth, and the regret theorem transfers the recommendation to the finite system up to \(\epsilon+o(1)\).

\section*{Technical internal limitation (diagnostic only)}

The floating-point witness at \((\lambda,\tau,\nu,\gamma)=(1,1,1,0)\) remains diagnostic rather than a theorem output. Capacity certification requires the planning radius \(G\) to be supplied exactly as a nonnegative rational; the remaining inputs may be supplied by shrinking rational boxes. The constructive proof may refine those boxes, the truncation radius, series orders, quantile bracket, and Darboux partition without a practical worst-case running-time bound. The kink residual is proved uniformly, including kink-crossing jumps and ceiling phases, and \(K_{\mathrm{res}}\) is a proved finite constant. Marked recovery causes, stationary initialization, independent equal-sized arms, homogeneous compactly positive rates, and a bounded critical staffing window remain substantive restrictions.

\section{Honest scope}

Exact finite-chain fitted centering and empirical inference require only \(N,T_N\to\infty\); no upper relation between duration and enrollment is imposed. The diffusion fitted center differs by \(O_{\mathbb P}(N^{-1})\), so the proved route gives its Wald law under \(T_N/N\to0\). The paper does not claim results for the anchor's unequal-arm class, unmarked histories, common shocks, arbitrary initialization, heterogeneous rates, vanishing-rate boundaries, post-assignment randomization inference, or clinical cure effects. Capacity certification applies to the compact critical-offset objective on \([-G,G]\) when \(G\in\mathbb Q_{\geq0}\) is supplied exactly and the other inputs have rational interval names; it proves termination and finite checkability, not a uniform polynomial complexity bound.

\begin{thebibliography}{99}

  \bibitem{BoutilierJonassonLiYoeli2024} Justin J. Boutilier, Jonas Oddur Jonasson, Hannah Li, and Erez Yoeli (2024), Operational Dosage: Implications of Capacity Constraints for the Design and Interpretation of Experiments, arXiv:2407.21322, version 3 dated 2025.

  \bibitem{BravermanDai2017} Anton Braverman and J. G. Dai (2017), Stein's Method for Steady-State Diffusion Approximations of \(M/Ph/n+M\) Systems, Annals of Applied Probability 27(1), 550--581, doi:10.1214/16-AAP1211.

  \bibitem{Braverman2022} Anton Braverman (2022), The Prelimit Generator Comparison Approach of Stein's Method, Stochastic Systems 12(2), 181--204, doi:10.1287/stsy.2021.0085.

  \bibitem{Hafouta2023} Yeor Hafouta (2023), Convergence Rates in the Functional Central Limit Theorem for Alpha-Mixing Triangular Arrays, Stochastic Processes and their Applications 161, 242--290, doi:10.1016/j.spa.2023.04.008.

  \bibitem{WangPlechac2019} Ting Wang and Petr Plechac (2019), Steady State Sensitivity Analysis of Continuous Time Markov Chains, SIAM Journal on Numerical Analysis 57(1), 192--217, doi:10.1137/18M119402X.

  \bibitem{Lok2024} Judith J. Lok (2024), How Estimating Nuisance Parameters Can Reduce the Variance, Statistics in Medicine 43(23), 4456--4480, doi:10.1002/sim.10164.

  \bibitem{BorstMandelbaumReiman2004} Sem Borst, Avi Mandelbaum, and Martin I. Reiman (2004), Dimensioning Large Call Centers, Operations Research 52(1), 17--34, doi:10.1287/opre.1030.0081.

  \bibitem{CorlissRall1987} George F. Corliss and Louis B. Rall (1987), Adaptive, Self-Validating Numerical Quadrature, SIAM Journal on Scientific and Statistical Computing 8(5), 831--847, doi:10.1137/0908069.

  \bibitem{JanssonKnuppel1995} Christian Jansson and Olaf Knueppel (1995), A Branch and Bound Algorithm for Bound Constrained Optimization Problems without Derivatives, Journal of Global Optimization 7(3), 297--331, doi:10.1007/BF01279453.

  \bibitem{LiJohariKuangWager2026} Shuangning Li, Ramesh Johari, Xu Kuang, and Stefan Wager (2026), Experimenting under Stochastic Congestion, arXiv:2302.12093, version 6.

  \bibitem{HaysRaghavan2025} Chris Hays and Manish Raghavan (2025), Double Machine Learning for Causal Inference under Shared-State Interference, arXiv:2504.08836.

  \bibitem{Whitt1992} Ward Whitt (1992), Asymptotic Formulas for Markov Processes with Applications to Simulation, Operations Research 40(2), 279--291.

  \bibitem{NeweyMcFadden1994} Whitney K. Newey and Daniel McFadden (1994), Large Sample Estimation and Hypothesis Testing, in Handbook of Econometrics, volume 4, 2111--2245.

  \bibitem{vanDerVaart1998} A. W. van der Vaart (1998), Asymptotic Statistics, Cambridge University Press.

  \bibitem{YoeliEtAl2019} Erez Yoeli, Jon Rathauser, Syon P. Bhanot, Mulili K. Kimenye, Elizabeth Mailu, Everlyne Masini, Philip Owiti, and David Rand (2019), Digital Health Support in Treatment for Tuberculosis, New England Journal of Medicine 381(10), 986--987.

  \bibitem{BaekBoutilierFariasJonassonYoeli2023} Jackie Baek, Justin J. Boutilier, Vivek F. Farias, Jonas Oddur Jonasson, and Erez Yoeli (2023), Policy Optimization for Personalized Interventions in Behavioral Health, arXiv:2303.12206.

  \bibitem{JohariLiLiskovichWeintraub2022} Ramesh Johari, Hannah Li, Inessa Liskovich, and Gabriel Y. Weintraub (2022), Experimental Design in Two-Sided Platforms: An Analysis of Bias, Management Science 68(10), 7069--7089, doi:10.1287/mnsc.2021.4247.

\end{thebibliography}

\end{document}