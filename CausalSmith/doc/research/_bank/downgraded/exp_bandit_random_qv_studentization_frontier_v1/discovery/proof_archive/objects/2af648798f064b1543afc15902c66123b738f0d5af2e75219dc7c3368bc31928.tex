Take the experiment constructed in \(\mathrm{prop:endogenous\text{-}qv\text{-}self\text{-}normalization\text{-}obstruction}\), relabel arm \(2\) as control \(0\), and let the deterministic scored set be \(\{1,\ldots,T\}\). Its effective scored sample size is therefore \(T\), which diverges along the even horizons. The full-data arrival at every time is the constant vector with \(Y_t(0)=Y_t(1)=1\), so the arrivals are identically distributed and independent. The executed propensity is the logged predictable number \(p_t=P(A_t=1\mid\mathcal F_{t-1})\): it equals \(1/2\) in the first block and, in the second block, equals either \(1/4\) or \(1/2\) according to the observed sign of \(S_n\). Conditional on \(\mathcal F_{t-1}\), treatment is drawn from this logged distribution independently of the potential outcomes. Thus sequential randomization holds, and \(1/4\le p_t\le3/4\) gives uniform overlap.

In the AIPW notation of the cited theorem, choose the predictable nuisance fits
\[
 m_{t-1,0}=m_{t-1,1}=0
\]
and the target
\[
 \theta_0=E[Y_t(1)-Y_t(0)]=0.
\]
The theorem's scored AIPW increment is then exactly
\[
 \begin{aligned}
 X_t
 &=m_{t-1,1}-m_{t-1,0}-\theta_0
   +\frac{\mathbf1\{A_t=1\}}{p_t}\{Y_t-m_{t-1,1}\}
   -\frac{\mathbf1\{A_t=0\}}{1-p_t}\{Y_t-m_{t-1,0}\}\\
 &=\frac{\mathbf1\{A_t=1\}}{p_t}
   -\frac{\mathbf1\{A_t=0\}}{1-p_t}
 =\xi_t.
 \end{aligned}
\tag{32}
\]
The nuisance fits are deterministic and hence predictable. All outcomes, fits, fit errors, inverse propensities, and increments in (32) are bounded by constants independent of \(T\); therefore every displayed fourth-moment and bounded-nuisance requirement in the theorem is finite uniformly in time. Because every time is scored, the theorem's score sum and realized square sum are respectively
\[
 \sum_{t=1}^T X_t=S_T,
 \qquad
 \sum_{t=1}^T X_t^2=[S]_T.
\tag{33}
\]
Likewise, its predictable variance is
\[
 V_T^2=\sum_{t=1}^T E[X_t^2\mid\mathcal F_{t-1}]
       =\langle S\rangle_T\ge4T
\tag{34}
\]
on every assignment path, where the last inequality is equation~(10) in the proved obstruction. Thus the variance-growth hypothesis holds with room to spare.

All the cited theorem's displayed hypotheses are therefore met by this specialization. Nevertheless, equations~(16)--(17) of \(\mathrm{prop:endogenous\text{-}qv\text{-}self\text{-}normalization\text{-}obstruction}\) give
\[
 \frac{\sum_{t=1}^{2n}X_t}{\sqrt{\sum_{t=1}^{2n}X_t^2}}
 =\frac{S_{2n}}{\sqrt{[S]_{2n}}}
 \Longrightarrow L,
 \qquad
 E[L]\ne0,
\tag{35}
\]
so the limit is not \(N(0,1)\). The source-attribution node \(\mathrm{lem:saco\text{-}pointwise\text{-}rqv\text{-}pivot}\) records that Saco, Theorem~5.14, asserts precisely the contradicted Studentized conclusion. Hence the experiment lies inside the published hypothesis class under the displayed specialization and refutes its variance-growth-only Studentized central limit conclusion.