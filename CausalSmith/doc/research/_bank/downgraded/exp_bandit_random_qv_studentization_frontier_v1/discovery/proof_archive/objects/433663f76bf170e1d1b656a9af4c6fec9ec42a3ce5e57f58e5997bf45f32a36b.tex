The proof first derives the AIPW martingale-difference property in the deterministic scored-set order and an explicit fourth-moment bound from Saco's overlap, outcome, and predictable-nuisance envelopes.  A new early-measurable \(L^4\) martingale lemma normalizes the suffix by \(N_n\Lambda_n\), proves the martingale CLT by an exponential-compensator argument, shows realized minus predictable QV is negligible in \(L^2\), removes the \(o(N_n)\) prefix, and then proves the exact sample-variance identity needed for feasible Studentization and Wald coverage.  The repaired class is exactly Saco's published enumerated class, with uniform envelopes, intersected with early QV stabilization.  A matching obstruction calculates the two possible suffix-QV limits in the existing two-regime witness and proves that an \(o(N_n)\)-history predictor cannot recover the later branch, so no admissible early scale exists.  Alongside this direct repair, the finite-attractor analysis supplies basin-stable causal IPW-Z mixtures, uniform bounded-process realized-QV inference, and exact contrastwise and matrixwise deterministic-sandwich boundaries; the bounded directional-matrix witness keeps those last two frontiers distinct.