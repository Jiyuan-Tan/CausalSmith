The positive result specifies deterministic \(0\le k_T<T\), a positive \(\mathcal F_{k_T}\)-measurable scale \(\Lambda_T\ge\omega_{c,T}\), negligible prefix \(k_T/(T\omega_{c,T})\to0\), relative suffix predictable-QV convergence, and \(T\omega_{c,T}\to\infty\). Bounded IPW scores imply uniform Lindeberg control and realized-to-predictable QV concentration; exact-root localization and square-root comparison make the feasible truncation inactive and deliver uniform Wald coverage. For the finite-attractor class, the explicit early nearest-attractor label with \(k_T=\min\{T-1,\lceil T\max\{T^{-1/2},\sqrt{r_T}\}\rceil\}\) verifies these conditions from the Cesaro attraction modulus. The negative two-regime construction shows why lower QV growth without early measurability cannot support the same conclusion.