The sound contribution is a sufficiency/impossibility frontier for adaptive causal IPW--Z inference. A finite-tail-attractor design yields basin-stable mixed normality. More generally, a prespecified contrast has a uniformly pivotal, everywhere-defined realized-QV statistic when its suffix predictable information is asymptotic to a positive scale \(\Lambda_T\) already measurable at an early time \(k_T\), the prefix is negligible, and \(T\omega_{c,T}\to\infty\). A positive predictable-QV floor alone is not sufficient: the explicit Guo--Xu-motivated two-regime witness lets the early score sign choose the later variance and produces a noncentered self-normalized limit. Deterministic-sandwich validity at every nominal level is contrastwise equivalent to basin-invariant scalar variance, and validity for all contrasts is equivalent to deterministic \(V_J\). The bounded Guo--Xu recursion remains an open dynamics consumer rather than a certified member of the theorem class.