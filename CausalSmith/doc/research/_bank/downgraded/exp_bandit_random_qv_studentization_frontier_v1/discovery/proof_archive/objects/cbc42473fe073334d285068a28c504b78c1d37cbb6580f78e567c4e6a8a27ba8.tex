For the prespecified nonzero \(c\), use the event-truncated sandwich formed with the contrast-specific threshold \(\omega_{c,T}/2\) and define the total statistic
\[
 T_T^{\mathrm{rqv}}(c)=
 \frac{\sqrt T\,c^\top(\widehat\theta_T-\theta^*)}
 {\sqrt{c^\top\widehat V_T(c)c}}.
\]