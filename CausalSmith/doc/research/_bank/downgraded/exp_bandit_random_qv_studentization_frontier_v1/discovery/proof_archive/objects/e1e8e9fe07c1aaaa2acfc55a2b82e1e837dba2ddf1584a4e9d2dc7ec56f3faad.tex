For the prespecified nonzero \(c\), use the horizon-specific event-truncated sandwich in \(\mathrm{def:event\text{-}truncated\text{-}rqv\text{-}sandwich}\) and define the total statistic
\[
 T_T^{\mathrm{rqv}}(c)=
 \frac{\sqrt T\,c^\top(\widehat\theta_T-\theta^*)}
 {\sqrt{c^\top\widehat V_T(c)c}}.
\]