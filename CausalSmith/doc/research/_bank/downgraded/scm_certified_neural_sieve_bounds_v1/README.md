---
qid: scm_certified_neural_sieve_bounds
spec: v1
topic: "Certified mixed-variable neural-SCM identified-set inference via a response-function outer sieve: for a known acyclic ADMG and compact class with finite district response labels, bounded-density continuous district noise, Lipschitz continuous mechanisms, and inverse-CDF categorical mechanisms, construct an exact-multinomial finite-sample outer interval and neural feasible inner interval; prove uniform coverage of the entire restricted identified set, an excess-width decomposition into the graph-query endpoint modulus evaluated at sampling radius plus explicit O(h) sieve error and a rationally verified epsilon global-optimization gap, and finite-h termination of spatial branch-and-bound with independently checkable certificates; include the uniform binary bow witness and the UA-DCM Parents' Labor Supply and Autobounds consumers"
novelty_target: field
banked_novelty_tier: incremental
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: null
reusable: unknown
reraise_status: true-negative
gap_reasons:
  - "From finite cell masses, response intervals, and set-valued boundary routes, the reverse direction does not derive a Lipschitz/measurable selection whose actual kappa-code probabilities equal the recorded masses: (7) is asserted, not constructed."
  - "This is a certificate-program-specific uniform enclosure oracle, not a primitive or derived property of the stated rational grammar; give concrete integration conditions and an enclosure construction with the claimed uniform rate, or rescope computation to an explicitly algorithmic integrable grammar."
  - "ass:semialgebraic-code permits Boolean combinations of weak inequalities, hence negated atoms and strict cells; the proof's claims that every constraint is weak, the feasible set is closed/compact, and empty boxes are excluded by closedness do not follow."
  - "The stated target is Theta(p_0), but the executable I_out brackets only Theta_cert(p_0); make the two deliverables and their targets explicit and do not present the subclass certificate as the functional for the full-class target."
  - "The neural result is only conditional on already possessing independently certified near-extremal witnesses; their existence, construction, and vanishing representation residual remain open, despite the neural-certification framing."
reusable_artifacts:
  - discovery/solve_prop_uniform_binary_bow.json
  - discovery/solve_thm_exact_whole_set_coverage.json
  - discovery/solve_thm_rational_certificate_termination.json
  - discovery/core.json
seeds_burned: []
proof_attempt_summary: |
  The run built a dyadic response-function outer-sieve argument, exact multinomial
  projection coverage, a binary-bow witness, and a proposed rational branch-and-bound
  certificate. The reverse coded-law lifting collapsed because the frozen assumptions do
  not construct the required Lipschitz measurable selections, while strict cells and an
  assumed integration oracle prevent the advertised terminating certificate theorem.
  Forward-only coverage and conditional certificate soundness remain valid incremental
  content, but not the field-tier mixed-variable sharpness or neural endpoint claim.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 17917433
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 17917433
  total_tokens_consumed: null
banked_on: "2026-09-10"
---

# scm_certified_neural_sieve_bounds / v1 — Downgraded

**Topic.** Certified mixed-variable neural-SCM identified-set inference via a response-function outer sieve: for a known acyclic ADMG and compact class with finite district response labels, bounded-density continuous district noise, Lipschitz continuous mechanisms, and inverse-CDF categorical mechanisms, construct an exact-multinomial finite-sample outer interval and neural feasible inner interval; prove uniform coverage of the entire restricted identified set, an excess-width decomposition into the graph-query endpoint modulus evaluated at sampling radius plus explicit O(h) sieve error and a rationally verified epsilon global-optimization gap, and finite-h termination of spatial branch-and-bound with independently checkable certificates; include the uniform binary bow witness and the UA-DCM Parents' Labor Supply and Autobounds consumers

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** The strongest honest result is forward-only exact-multinomial projection coverage and conditional certificate soundness; reverse exact-code lifting and terminating mixed-variable certification remain unproved, so the result is incremental below the field floor.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
