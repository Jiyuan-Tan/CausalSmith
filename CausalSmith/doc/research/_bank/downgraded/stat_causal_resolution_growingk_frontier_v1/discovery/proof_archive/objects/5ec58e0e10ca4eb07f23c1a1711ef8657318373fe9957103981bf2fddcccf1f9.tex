The established covariance theorem and the existing Fano nodes give the \(\sqrt{\log K_n/n}\) standardized lower radius for the declared all-noise subclasses, while the prior noiseless-local proof gives a matching multiplier upper radius under its abstract remainder condition.  The missing positive-noise upper side is not a rate optimization: the current residual-score packing perturbs the outcome regressions through periodic \(B_1\) directions with jumps at the \(j\)- and \((j+1)\)-grids.  Those alternatives are not members of any common fixed-radius Hölder-\(s\) nuisance ball.  A valid common-class theorem therefore requires smoothed score directions together with proofs that smoothing preserves adjacent-score separation, optimizer stability, global basin entry, and the learner-substituted remainder bounds.