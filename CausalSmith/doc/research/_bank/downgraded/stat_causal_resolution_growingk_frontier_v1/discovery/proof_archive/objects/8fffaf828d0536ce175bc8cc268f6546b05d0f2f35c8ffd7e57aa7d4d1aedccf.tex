Conditional on valid uniform linearization and multiplier approximation, the Gaussian moment-generating-function calculation in \(\texttt{thm:exact-covariance-packing}\) gives upper radius \(w_n/\sqrt n\).  The separated set \(J_n\) gives the matching \(\sqrt{\log|J_n|/n}\) information scale once a within-class likelihood family has standardized gain separation and \(O(\log|J_n|)\) product Kullback--Leibler divergence.  The current positive Fano family is outside the class and the current score gauge omits codebook estimation, so these conditional implications do not certify the frozen theorem.