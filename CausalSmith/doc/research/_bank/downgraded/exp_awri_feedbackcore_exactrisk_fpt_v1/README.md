---
qid: exp_awri_feedbackcore_exactrisk_fpt
spec: v1
topic: "Feedback-core exact-risk certification for weighted random isolation. Revive banked parent exp_awri_mixture_minimax_certificate with a different mathematical angle: given its fixed WRI/RDIM network-design model and a supplied size-k feedback vertex set of the conflict graph, prove an evaluated cutoff-forest moment algebra that computes every one/two-tagged greedy-isolated-set count polynomial and dense rational RDIM risk matrix in L n^2 2^{O(k log(k+1))} poly(n,b,lambda) bit operations, with no (nb)^k table or unevaluated integration gate. Use the derived 3^k core inclusion-exclusion reduction, complete vertex-cover oracle, and ordered-ratio nested-moment recurrence as traction; close heterogeneous forest branching, moving boundary integration, all tags, and intermediate bit height. Then return a rational epsilon-minimax primal-dual certificate for the finite rule library; omit the parent's unsupported exact algebraic optimizer. Consumer: exact AWRI rule selection for a rural-China insurance-diffusion follow-up after measuring the conflict graph's feedback number. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A three-state cutoff-forest reduction and exact core inclusion-exclusion identity were derived; vertex-cover cores are FPT, and a directed-realizable feedback-k family with binomial dense support has an O(k! k m^2) nested-ratio evaluation. Exact checks matched all 44 C6 tag polynomials, 288 risk entries, 384 vertex-cover queries, six repeated-star cases, and three heterogeneous cases. Relative core order and scalar activation counts both failed, while no prior-art collision or universal obstruction was found. UNRESOLVED BOTTLENECK: Prove an evaluated moment representation for heterogeneous cutoff-forest messages that preserves future core-priority exponent profiles and closes under branching, component products, moving-endpoint integration, and tagged coefficient extraction in true FPT bit complexity. EARLY KILL TEST: On heterogeneous replicated-star integrals with k<=3, m<=2, and branch weights in {1,2}, match all tags and the private-leaf encoding to exact residual enumeration and prove symbolic arbitrary-k,m complexity; if closure still indexes k degree totals or profile-count vectors with an n-dependent exponent, stop and pivot before general forest or SDP work. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/exp_awri_feedbackcore_exactrisk_fpt.md"
novelty_target: field
banked_novelty_tier: incremental
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: null
reusable: unknown
reraise_status: retry
gap_reasons:
  - "The proposed all-feedback-set FPT oracle is replaced by this evaluated XP theorem: the vertex-cover and identical-star subclasses do not establish the promised heterogeneous forest closure or eliminate the (nb)^{O(k)} state dependence."
  - "The general supplied-feedback-set result is an XP algorithm with an (nb+n+1)^{O(k)} factor, whereas fixed-parameter exact evaluation for arbitrary heterogeneous forest attachments remains explicitly open."
reusable_artifacts:
  - "discovery/core.json — exact L·6^k·poly(n,b,lambda) vertex-cover oracle, evaluated XP general oracle, identical-star recurrence, and rational epsilon-minimax certificate"
  - "discovery/writeup.tex — complete derivations, entered-subset DP, tagged-risk reconstruction, and honest heterogeneous-profile obstruction"
  - "discovery/solve_thm_feedback_core_exact_risk.json — reusable exact-risk and cutoff-forest solve artifact"
  - "reviews/review_math.json — math-pass and cited-source verification receipts"
seeds_burned: []
proof_attempt_summary: |
  The run derived exact tagged WRI/RDIM risks, an evaluated general XP algorithm, a rational epsilon-minimax certificate, and an exact L·6^k·poly(n,b,lambda) algorithm when the supplied feedback core is also a vertex cover. The intended arbitrary-feedback-set FPT closure collapsed because heterogeneous forest attachments retain future exponent profiles whose known exact representation has (nb)^{O(k)} state dependence; scalar, activation-count, and grouped-profile compressions did not preserve the required moments. A future re-raise must supply a genuinely new evaluated operator algebra that closes under branching, products, moving endpoints, and all one/two-tag extractions without n-dependent exponent profiles.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 29883156
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 29883156
  total_tokens_consumed: null
banked_on: "2026-09-11"
---

# exp_awri_feedbackcore_exactrisk_fpt / v1 — Downgraded

**Topic.** Feedback-core exact-risk certification for weighted random isolation. Revive banked parent exp_awri_mixture_minimax_certificate with a different mathematical angle: given its fixed WRI/RDIM network-design model and a supplied size-k feedback vertex set of the conflict graph, prove an evaluated cutoff-forest moment algebra that computes every one/two-tagged greedy-isolated-set count polynomial and dense rational RDIM risk matrix in L n^2 2^{O(k log(k+1))} poly(n,b,lambda) bit operations, with no (nb)^k table or unevaluated integration gate. Use the derived 3^k core inclusion-exclusion reduction, complete vertex-cover oracle, and ordered-ratio nested-moment recurrence as traction; close heterogeneous forest branching, moving boundary integration, all tags, and intermediate bit height. Then return a rational epsilon-minimax primal-dual certificate for the finite rule library; omit the parent's unsupported exact algebraic optimizer. Consumer: exact AWRI rule selection for a rural-China insurance-diffusion follow-up after measuring the conflict graph's feedback number. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A three-state cutoff-forest reduction and exact core inclusion-exclusion identity were derived; vertex-cover cores are FPT, and a directed-realizable feedback-k family with binomial dense support has an O(k! k m^2) nested-ratio evaluation. Exact checks matched all 44 C6 tag polynomials, 288 risk entries, 384 vertex-cover queries, six repeated-star cases, and three heterogeneous cases. Relative core order and scalar activation counts both failed, while no prior-art collision or universal obstruction was found. UNRESOLVED BOTTLENECK: Prove an evaluated moment representation for heterogeneous cutoff-forest messages that preserves future core-priority exponent profiles and closes under branching, component products, moving-endpoint integration, and tagged coefficient extraction in true FPT bit complexity. EARLY KILL TEST: On heterogeneous replicated-star integrals with k<=3, m<=2, and branch weights in {1,2}, match all tags and the private-leaf encoding to exact residual enumeration and prove symbolic arbitrary-k,m complexity; if closure still indexes k degree totals or profile-count vectors with an n-dependent exponent, stop and pivot before general forest or SDP work. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/exp_awri_feedbackcore_exactrisk_fpt.md

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** The general supplied-feedback-set result is an XP algorithm with an (nb+n+1)^{O(k)} factor, whereas fixed-parameter exact evaluation for arbitrary heterogeneous forest attachments remains explicitly open.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
