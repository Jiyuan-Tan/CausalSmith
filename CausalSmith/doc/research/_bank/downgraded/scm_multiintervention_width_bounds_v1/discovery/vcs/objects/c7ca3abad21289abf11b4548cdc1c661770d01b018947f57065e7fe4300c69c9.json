{
  "node_type": "assumption",
  "body": {
    "id": "ass:complete-response-list",
    "kind": "representation",
    "condition": "\\(I\\) contains exactly one record for every element of every \\(R_c\\).",
    "free_symbols": [
      "I",
      "Rc"
    ],
    "novel": {
      "flag": true,
      "justification": "Graph-to-response compilers already generate explicit canonical response columns for small discrete graphs, including the matrices used by symbolic-bound software; this condition makes that existing finite representation auditable."
    }
  }
}