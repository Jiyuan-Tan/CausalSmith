Observed-side regressions are estimated by local constants with score-count-qualified dyadic bandwidths, a deterministic tie break, and the explicit \(h_0\) fallback. Upper proofs use conditional independent-product factorization and the centered score-conditional sub-Gaussian kernel MGF; converses use exact centered-Gaussian submodels.