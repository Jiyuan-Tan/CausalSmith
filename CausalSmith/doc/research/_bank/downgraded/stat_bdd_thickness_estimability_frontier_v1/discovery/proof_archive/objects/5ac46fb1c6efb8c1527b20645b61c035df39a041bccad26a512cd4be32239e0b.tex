Fix \(P\in\mathcal P(m,\mathsf J_-)\), and write \(\mu_{t,P}(E)=P_X(E\cap\mathcal A_t)\).  If \(x\in\mathcal B\), \(r>0\), and \(s=\min\{h_0,r/2\}\), positivity of \(m\) and the declared lower-mass member property give
\[
 \mu_{t,P}\{z:d(z,x)<r\}\ge q_{t,P}(x,s)\ge m(s)>0. \tag{29}
\]
Thus \(x\in\mathcal X_{t,P}\) on both sides.  Lipschitz continuity of the specified representative and the trace definition imply
\[
 \theta_{t,P}(x)=g_{t,P}(x). \tag{30}
\]
The assignment partition and sharp assignment identify the observations in a side average with \(T=t\).  The trace envelope gives \(\tau_P(x)\in[-2L,2L]\), so projection onto this interval cannot enlarge pointwise error and makes the loss at most \(4L\).

For the upper bound, fix \(h\) with \(h,2h\in\mathcal H_n\) and
\[
 nm(h)\ge A\log(e+n/h). \tag{31}
\]
Let \(z_1,\ldots,z_K\) be a maximal \(h\)-separated subset of \(\mathcal B\).  It is an \(h\)-net and global boundary entropy gives \(K\le C_{\mathcal B}h^{-1}\).  Independent sampling and the lower-mass property give
\[
 N_{t,n}(z_j,h)\sim\operatorname{Bin}\{n,q_{t,P}(z_j,h)\},
 \qquad q_{t,P}(z_j,h)\ge m(h). \tag{32}
\]
For \(N\sim\operatorname{Bin}(n,q)\), Markov's inequality applied to \(2^{-N}\), followed by \(1-y\le e^{-y}\), gives
\[
 \Pr(N\le nq/2)\le2^{nq/2}(1-q/2)^n\le e^{-nq/8}. \tag{33}
\]
Let \(H_h\) be the event that every center-side count is at least \(nm(h)/2\).  Then
\[
 P(H_h^c)\le2C_{\mathcal B}h^{-1}e^{-nm(h)/8}. \tag{34}
\]
For each \(x\), a net center within \(h\) satisfies \(B(z_j,h)\subseteq B(x,2h)\), so on \(H_h\),
\[
 \inf_{x,t}N_{t,n}(x,2h)\ge nm(h)/2. \tag{35}
\]
Choose the fixed \(A\) large enough that (31) makes \(2h\) admissible at every \(x,t\).

Condition on \(X_1,\ldots,X_n\).  The selected bandwidths are then fixed.  Under \(\mathrm{ass:iid\text{-}sampling}\), the conditional law of the residual vector is the product of the score-conditional kernels in \(\mathrm{ass:gaussian\text{-}errors}\).  Consequently, for every deterministic index set \(S\), multiplication of the kernel MGF inequalities gives
\[
 \mathbb E_P\!\left[\exp\!\left\{\lambda\sum_{i\in S}\varepsilon_i\right\}\mid X_1,\ldots,X_n\right]
 \le \exp\{\sigma^2\lambda^2|S|/2\}. \tag{36}
\]
Chernoff's argument and a union bound over the finite disk-pattern family therefore give exactly the tail and expectation controls recorded in \(\mathrm{lem:disk\text{-}pattern\text{-}sub\text{-}gaussian\text{-}control}\).  In particular, if \(Z_n\) is its maximum normalized residual sum, then
\[
 \mathbb E_P(Z_n\mid X_1,\ldots,X_n)
 \le\sqrt{2\log\left\{4|\mathcal H_n|\sum_{j=0}^3\binom nj\right\}}+1
 \le C\sqrt{\log(e+n)}. \tag{37}
\]
For any nonempty side average at bandwidth \(b\), (30) and Lipschitz smoothness bound its conditional bias by \(Lb\).  On \(H_h\), all selected bandwidths are admissible.  Since \(\log(e+n/b)\ge\log(e+n)\) up to a fixed numerical factor for \(b\le h_0<1\), (37) bounds the conditional expected supremum of their stochastic errors by a constant times their selected criteria.  Pointwise minimization of that criterion and comparison with the admissible bandwidth \(2h\), using (35), give
\[
 \mathbb E_P\!\left[\sup_{x\in\mathcal B}|\widehat\tau_n(x)-\tau_P(x)|\mid X_1,\ldots,X_n\right]
 \le C\left\{Lh+\sigma\sqrt{\frac{\log(e+n/h)}{nm(h)}}\right\} \tag{38}
\]
on \(H_h\).  Projection, the \(4L\) bound off \(H_h\), and (34) yield
\[
 \sup_{P\in\mathcal P(m,\mathsf J_-)}\mathbb E_P\sup_x|\overline\tau_n(x)-\tau_P(x)|
 \le C\left\{Lh+\sigma\sqrt{\frac{\log(e+n/h)}{nm(h)}}\right\}
 +CLh^{-1}e^{-nm(h)/8}. \tag{39}
\]
After increasing \(A\) so that \(A/8\ge2\), (31) implies \(h^{-1}e^{-nm(h)/8}\le h\), and the last term is absorbed.  Taking the infimum over qualifying dyadic pairs, and combining with the unconditional bound \(4L\), proves the upper display.  The estimator definitions and the fixed projection show directly that the estimator uses neither \(m\) nor \(\mathsf J_-\).

For the converse, choose \(P^\circ\) in the nonempty class and retain its score marginal and geometry.  On an extension carrying independent \(Z_0,Z_1\sim\mathcal N(0,1)\), define \(P_0\) by \(Y(t)=\sigma Z_t\).  All score-dependent member properties are unchanged; the traces vanish; and the residual kernel is exactly centered Gaussian, hence satisfies the assumed MGF bound.  Thus \(P_0\in\mathcal P(m,\mathsf J_-)\).

Let \(M=\mathsf J_-(h,u)\ge1\).  The lower-profile condition and the definition of \(\mathsf J_{P_0}\) supply \(h\)-separated points \(x_1,\ldots,x_M\) with \(\min_tq_{t,P_0}(x_j,h)\le u\).  Choose a minimizing side \(t_j\), and put
\[
 \eta_h=Lh/12,\qquad \psi_{j,h}(z)=(1-3d(z,x_j)/h)_+. \tag{40}
\]
Define \(P_j\) by shifting only the potential outcome on side \(t_j\), adding \(\eta_h\psi_{j,h}(X)\) on side one and subtracting it on side zero.  The perturbation is \(L/4\)-Lipschitz, bounded by \(L\), and changes neither the design conditions nor the exact centered-Gaussian residual kernel.  Therefore \(P_j\in\mathcal P(m,\mathsf J_-)\).  By (29)--(30), the target at \(x_j\) changes by \(\eta_h\); the other tents vanish there.  Hence
\[
 \|\tau_{P_j}-\tau_{P_k}\|_\infty\ge\eta_h,\qquad0\le j<k\le M. \tag{41}
\]
Direct integration of the centered-Gaussian likelihood ratio gives
\[
 \operatorname{KL}(P_j^{\otimes n},P_0^{\otimes n})
 =\frac{n\eta_h^2}{2\sigma^2}\int_{\mathcal A_{t_j}}\psi_{j,h}(z)^2\,dP_X(z)
 \le\frac{nL^2h^2u}{288\sigma^2}. \tag{42}
\]
If the first information condition holds, retain one alternative and choose \(a\) so (42) is at most \(1/8\); the two-law clause of \(\mathrm{lem:finite\text{-}information\text{-}testing}\) yields risk at least \(cLh\).  If \(M\ge2\) and the second condition holds, use \(\log(M+1)/\log M\le\log3/\log2\) and decrease \(a\) by that fixed factor.  Then (42) meets the multiple-law threshold, and (41) plus the same lemma again give
\[
 R_n\{\mathcal P(m,\mathsf J_-)\}\ge cLh. \tag{43}
\]

For a sequence of classes, the displayed sufficient condition makes a qualifying \(h_n\) available eventually, and both terms in (39) tend to zero because \(h_n\downarrow0\).  Conversely, let \(F_n\) be the set of radii for which some \(u\) satisfies either lower-bound information condition.  If \(\sup F_n\not\to0\), then along a subsequence one can choose feasible radii bounded below by a positive constant; (43) keeps minimax risk bounded away from zero.  This proves the stated necessary condition.

For \(m_\alpha\), \(\mathrm{prop:exponential\text{-}horn\text{-}consistency}\) gives \(m_\alpha(h)\asymp h^{\alpha+1}e^{-h^{-\alpha}}\).  Setting \(s_n=\{(\log n)/2\}^{-1/\alpha}\) gives
\[
 \frac{nm_\alpha(s_n)}{\log(e+n/s_n)}
 \asymp\frac{n^{1/2}s_n^{\alpha+1}}{\log n}\longrightarrow\infty. \tag{44}
\]
Rounding upward to a dyadic \(h_n\in\mathcal H_n\) with \(s_n\le h_n<2s_n\) and \(2h_n\in\mathcal H_n\) preserves (44) by monotonicity of \(m_\alpha\).

Finally, with \(u=C_mh^\kappa\), the first information condition at \(M\asymp1\) balances \(nh^{\kappa+2}\asymp1\), giving \(h\asymp n^{-1/(\kappa+2)}\).  With \(M\asymp h^{-1}\), the second balances \(nh^{\kappa+2}\asymp\log(1/h)\), giving \(h\asymp(\log n/n)^{1/(\kappa+2)}\).  These lower rates match \(\mathrm{thm:thickness\text{-}entropy\text{-}separation}\).  For a slack declaration, the circular pervasive witness may legally be paired with \(\mathsf J_-(h,u)=\mathbf1\{u\ge C_mh^\kappa\}\); the lower display then exposes only one site while the global-net upper argument still pays the boundary entropy.  Hence the arbitrary-profile conclusion is the stated honest bracket, not an exact criterion.