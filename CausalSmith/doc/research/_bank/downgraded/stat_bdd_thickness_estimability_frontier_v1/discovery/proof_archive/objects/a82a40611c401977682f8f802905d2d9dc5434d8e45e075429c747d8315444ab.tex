Use the explicit band for finite-sample global coverage on declared unions and its global expected-supremum width as matched on every nonempty polynomial pervasive class and the containing base class, for any fixed \(\gamma\in(0,1)\).