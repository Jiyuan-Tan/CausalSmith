The established theorem \(\mathrm{thm:arbitrary\text{-}modulus\text{-}two\text{-}sided\text{-}bracket}\) supplies one clipped count-adaptive estimator, using neither declared object, with the explicit upper bound
\[
 C\min\!\left[L,\inf_{\substack{h,2h\in\mathcal H_n\\ n m(h)\ge A\log(e+n/h)}}\left\{Lh+\sigma\sqrt{\frac{\log(e+n/h)}{n m(h)}}\right\}\right],
\]
and supplies the same-class lower bound \(cLh\) whenever \(M=\mathsf J_{-}(h,u)\) and \(nL^2h^2u/\sigma^2\) is at most a constant, or at most a constant times \(\log(M+1)\) when \(M\ge2\). These bounds recover the established isolated and pervasive polynomial rates, and \(\mathrm{prop:exponential\text{-}horn\text{-}consistency}\) gives vanishing risk for each fixed positive exponential modulus. The settled lower-profile theorem further proves that, for \(m(h)=c_mh^\kappa\) and the one-site declaration \(\mathsf J_{-}(h,u)=\mathbf 1\{u\ge C_mh^\kappa\}\), the two-argument class retains \(\mathcal P_{\kappa}^{\mathrm{perv}}\) and has the pervasive rate, whose ratio to the isolated rate is \((\log n)^{1/(\kappa+2)}\to\infty\).