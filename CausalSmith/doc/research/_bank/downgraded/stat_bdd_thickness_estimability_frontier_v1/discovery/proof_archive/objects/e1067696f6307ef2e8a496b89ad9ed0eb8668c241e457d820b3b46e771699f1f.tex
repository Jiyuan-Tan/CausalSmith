Condition on the observed scores and calibrate all nonempty side averages over the finite disk-pattern family by the conditional tail radius supplied by \(\mathrm{lem:disk\text{-}pattern\text{-}sub\text{-}gaussian\text{-}control}\), whose moment-generating-function bound is derived from the exact conditional Gaussian assumption; add the Lipschitz bias \(L\widehat h_{t,n}(x)\) at a positive selected count and the trace envelope \(L\) at a zero count, then difference the two sides to form \(\mathcal C_n\). For any two full observed laws, transfer simultaneous coverage between them with \(\operatorname{TV}\) to obtain the two-law expected-half-width obstruction. This handle does not perform local multiplicity calibration and does not replace full-observed-law distinguishability by score-only distinguishability.