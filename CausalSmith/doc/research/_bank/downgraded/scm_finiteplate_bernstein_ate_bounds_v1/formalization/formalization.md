# scm_finiteplate_bernstein_ate_bounds — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (A positive fixed-plate hierarchical observational world).** A positive fixed-plate hierarchical observational world. There are \(n\) independent clusters; cluster \(i\) carries a latent four-cell law \(Q_i\in K_{\epsilon}=\{q\in\Delta_3:\epsilon\le p(q)\le1-\epsilon\}\), where \(p(q)=q_{10}+q_{11}\) and \(0<\epsilon<1/2\), drawn from an unknown mixing measure \(\mu\in\mathcal P(K_{\epsilon})\). Conditionally on \(Q_i=q\) the \(m\) individual causal vectors \((A_{ij},Y_{ij}(0),Y_{ij}(1))\in\{0,1\}^3\) are mutually independent and identically distributed over \(j\), treatment is ignorable given \(Q_i\), outcomes satisfy \(Y_{ij}=A_{ij}Y_{ij}(1)+(1-A_{ij})Y_{ij}(0)\), and the observed pair \(O_{ij}=(A_{ij},Y_{ij})\) has conditional cell probabilities \(\Pr(O_{ij}=(a,y)\mid Q_i)=Q_{i,ay}\) almost surely. The analyst observes only the within-cluster four-cell count \(C_i\in\mathcal I_m\). The causal target is the cluster-equally-weighted ATE \(\tau(\mu)=\int_{K_{\epsilon}}f\,d\mu\) with \(f(q)=q_{11}/p(q)-q_{01}/(1-p(q))\in[-1,1]\). — Encoding: `Cell := Bool × Bool` (the (a,y) pair); `q : Cell → ℝ` with `q ∈ stdSimplex ℝ Cell` (Mathlib `stdSimplex`, verified at Mathlib/Analysis/Convex/StdSimplex.lean, compact + convex lemmas available); `K_epsilon := stdSimplex ℝ Cell ∩ {q | ε ≤ p q ∧ p q ≤ 1-ε}`. Random variables `A Y0 Y1 Y : Fin n → Fin m → Ω → Bool` and `Q : Fin n → Ω → (Cell → ℝ)` on a single probability space; `mu : Measure (Cell → ℝ)` supported on `K_epsilon`. `C i ω : Cell → ℕ` is the within-cluster count. SUBSTRATE SURVEY (bypass-justified, one line each): (a) `Causalean.PO.POSystem` (Causalean/PO/Core/System.lean) — regime/graph-indexed PO operator over dependent value spaces; this note uses no intervention regime and no graph, only a two-arm plate of binary variables, so the regime machinery adds indexing overhead with no reused lemma; (b) `Causalean.SCM.PartialID.CompatibleSCM` / `IsSharp` (Causalean/SCM/PartialID/CanonicalModel.lean, SharpnessCertificate.lean) — require a `SWIGGraph N` and `SCM N Ω` structural-equation model; the hierarchical class here is a latent-mixture plate model with no structural equations over a node set, so the abstraction does not fit; (c) `Causalean/Graph/*`, `Causalean/SCM/Do/*`, `Causalean/SCM/ID/*` (dSep, backdoor, frontdoor, do-calculus, c-components) — graphical identification machinery; this note's identification content is a moment-fiber question at a FIXED, already-ignorable conditioning set, so no do-calculus rule is invoked. Search trace: read `../doc/API.md` §8h (PO/), §9j2 (PO/ID/Partial), §9j2sf (SupportFunction), §9j2rs, §9j2inf, §11 (SCM/ID); globbed `../Causalean/SCM/PartialID/**`; grepped `../Causalean` for `multinomial|pValue|deviance|likelihoodRatio` (11 hits, all unrelated proxy/bootstrap files). Analogous module for F3 to adapt: `CausalSmith/SCM/SCM_SelectiveLabelUntrialedImpact_Research/Basic.lean:293-317` (`observablePolytope`, `responseFiber`, `sharpEndpointPrograms = (sInf, sSup)` over a fiber) — same architecture with a FINITE latent simplex; the gap to bridge is that our latent parameter ranges over `P(K_ε)`, a continuum of Borel measures, not `stdSimplex ℝ T`.
**required modules.** Mathlib.Analysis.Convex.StdSimplex, Mathlib.MeasureTheory.Measure.ProbabilityMeasure, Mathlib.MeasureTheory.Measure.Dirac, Mathlib.MeasureTheory.Integral.Bochner.Basic, Mathlib.Data.Nat.Choose.Multinomial, Causalean.Stat.Sample

**S-2 (The degree-\(m\) Bernstein moment geometry over the overl…).** The degree-\(m\) Bernstein moment geometry over the overlap-restricted simplex. Indexing counts by \(\mathcal I_m=\{c\in\mathbb N_0^4:\sum c_{ay}=m\}\) with \(N_m=|\mathcal I_m|=\binom{m+3}{3}\), the Bernstein coordinates are \(B^{(m)}_c(q)=\frac{m!}{\prod c_{ay}!}\prod q_{ay}^{c_{ay}}\), the moment map is \(g_m=(B^{(m)}_c)_{c\in\mathcal I_m}:K_{\epsilon}\to\mathbb R^{N_m}\), and \(V_m=\operatorname{span}\{B^{(m)}_c\}\subset C(K_{\epsilon})\). The observable object is the count law \(b=\int g_m\,d\mu\), ranging over the compact convex moment body \(\mathcal B_{m,\epsilon}=\operatorname{conv}g_m(K_{\epsilon})\); above each \(b\) sits the moment fiber \(\mathcal F_{m,\epsilon}(b)=\{\nu\in\mathcal P(K_{\epsilon}):\int g_m\,d\nu=b\}\), whose image under \(\nu\mapsto\int f\,d\nu\) is the sharp interval \([L_m(b),U_m(b)]\). Dual to it is the Bernstein majorant--minorant pair \(D_m(b)\), the valid envelope class \(\mathcal E_m\), the scalar-reference limited-pooling family \((\ell_{m,p_*},u_{m,p_*})\) with weights \(w_m\) and its intersection \(I^{\cap}_{LW,m}\), and the uniform approximation modulus \(d_m(\epsilon)=\inf_{v\in V_m}\|f-v\|_{\infty,K_{\epsilon}}\). — Reuse decisions verified by `lean_local_search`: `convexHull` + Carathéodory `convexHull_eq_union` (Mathlib/Analysis/Convex/Caratheodory.lean) realizes \(\mathcal B_{m,\epsilon}\) and the (N+1)-atom compression; `intrinsicInterior` / `intrinsicFrontier` (Mathlib/Analysis/Convex/Intrinsic.lean) realize relint/relbd verbatim — do NOT hand-roll a relative interior; `Nat.multinomial` (Mathlib/Data/Nat/Choose/Multinomial.lean) supplies the Bernstein normalisation; `Metric.infDist` in `C(K_ε, ℝ)` (Mathlib/Topology/ContinuousMap/Compact.lean sup-norm instance + Mathlib/Topology/MetricSpace/HausdorffDistance.lean) realizes \(d_m(\epsilon)\); `Causalean.PartialID.IdentifiedInterval` realizes \(\Theta_{m,\epsilon}(b)\) exactly. Index type `I_m := {c : Cell → ℕ // ∑ c = m}` (a `Fintype`); `N_m := Fintype.card I_m` with the \(\binom{m+3}{3}\) formula proved as a small helper. Search trace for the absent pieces: `lean_local_search "bernstein"` → only Mathlib's UNIVARIATE `bernsteinPolynomial` / `bernsteinApproximation` on `[0,1]` (Mathlib/RingTheory/Polynomial/Bernstein.lean, Mathlib/Analysis/SpecialFunctions/Bernstein.lean) — no multivariate simplex Bernstein basis, no moment body, no moment fiber; `lean_local_search "Caratheodory"` → empty by name, found via `convexHull_eq_union`; `lean_local_search "multinomial"` → `Nat.multinomial` only, no multinomial DISTRIBUTION; grep of `../Causalean` for `multinomial|deviance|likelihoodRatio` → no moment-body or fiber substrate. Analogous module for F3: `CausalSmith/SCM/SCM_SelectiveLabelUntrialedImpact_Research/Basic.lean:284-334` (`targetFunctional`, `observablePolytope`, `responseFiber`, `sharpEndpointPrograms`, `lowerEndpoint`/`upperEndpoint`) and `T2_SharpInterval.lean`, `T4_DualFaceStrictRefinement.lean`, `T5_RelativeOpenStrictRefinement.lean` — the finite-latent analogue of exactly the primal/dual/contact-face/strict-refinement chain needed here; those files contain `sorry`s, so they are a PATTERN reference, not a `reuse` target.
**required modules.** Mathlib.Analysis.Convex.Caratheodory, Mathlib.Analysis.Convex.Combination, Mathlib.Analysis.Convex.Intrinsic, Mathlib.Analysis.Convex.Topology, Mathlib.Analysis.Convex.Exposed, Mathlib.Analysis.Convex.Extreme, Mathlib.Topology.ContinuousMap.Compact, Mathlib.Topology.MetricSpace.HausdorffDistance, Mathlib.Data.Nat.Choose.Multinomial, Causalean.PO.ID.Partial.Basic, Causalean.PO.ID.Partial.SupportFunction.Interval

**S-3 (The exact finite-sample multinomial inversion world).** The exact finite-sample multinomial inversion world. With \(m,n\) and \(0<\alpha<1\) fixed, the aggregate cluster-count vector \(Z\in\mathcal H_n=\{z\in\mathbb N_0^{N_m}:\sum_c z_c=n\}\) is multinomial with size \(n\) and cell law \(b\); \(\widehat b=Z/n\). For a candidate law \(b'\in\Delta_{N_m-1}\) the deviance is \(T_n(z;b')=2\sum_{c:z_c>0}z_c\log\{z_c/(nb'_c)\}\) (extended by \(+\infty\) when \(z_c>0=b'_c\)), and \(P_n(b';z)\) is its exact tie-inclusive upper-tail probability under \(b'\). The nonrandomized test rejects exactly when \(P_n\le\alpha\); its inversion is the count-law region \(\mathcal R_{n,\alpha}(z^{\mathrm{obs}})=\{b':P_n(b';z^{\mathrm{obs}})>\alpha\}\), and the reported causal object is \(\mathcal C_{n,\alpha}(z^{\mathrm{obs}})=\bigcup_{b'\in\mathcal R_{n,\alpha}\cap\mathcal B_{m,\epsilon}}[L_m(b'),U_m(b')]\subseteq[-1,1]\). — `T_n` and `P_n` are total functions into `ℝ≥0∞` / `EReal` so the `+∞` branch is definitional rather than a side condition; `H_n` is a `Fintype` so `P_n` is a genuine `Finset.sum`. Search trace: `lean_local_search "multinomial"` → `Nat.multinomial`, `Finsupp.multinomial` (combinatorics only) and `CausalSmith.SCM.SelectiveLabelUntrialedImpact.multinomialCounts` / `multinomialCovariance` (a CausalSmith research module with `sorry`s, not reusable); `lean_local_search "binomial"` → `ProbabilityTheory.binomial`, `PMF.binomial` (Mathlib/Probability/Distributions/Binomial.lean) — the two-cell case only, no multinomial law and no exact-test p-value; grep `../Causalean` for `pValue|deviance|likelihoodRatio` → nothing statistical. So the multinomial pmf and the exact tie-inclusive p-value are build-inline (both are elementary finite sums). REUSED vocabulary: `Causalean.PartialID.Inference.RandomCoversIcc` (Causalean/PO/ID/Partial/Inference/Basic.lean:92) is the exact set-coverage event `[L,U] ⊆ [A ω, B ω]` used by `prop:whole-set-coverage`; its companion `honest_ci_set_cover` is NOT reusable here because our coverage comes from an exact finite-sample test, not a two-sided concentration union bound. Analogous module: `CausalSmith/SCM/SCM_SelectiveLabelUntrialedImpact_Research/T6_WholeSetConfidence.lean` states the same `Set.Icc L U ⊆ wholeSetConfidenceInterval …` shape.
**required modules.** Mathlib.Analysis.Convex.StdSimplex, Mathlib.Data.Nat.Choose.Multinomial, Mathlib.MeasureTheory.Measure.ProbabilityMeasure, Causalean.PO.ID.Partial.Inference.Basic, Causalean.Stat.Sample

**S-4 (The exact real-algebraic decision world).** The exact real-algebraic decision world. All data (\(\epsilon\), \(\alpha\), the count vectors, the candidate law \(b\)) are rational, and every constraint of the problem — the simplex and overlap inequalities cutting out \(K_{\epsilon}\), the Bernstein moment equations, the objective equation after clearing the strictly positive factor \(p(q)(1-p(q))\), global Bernstein dual feasibility, complementary contact, and the deviance comparisons after exponentiation — is a polynomial equality or inequality with rational coefficients. The objects living in this world are the quantified first-order formulas whose elimination yields the endpoint functionals, the confidence set, and the boundary stratification: the finite-support recovery procedure \(\operatorname{CAD}_m(b,\epsilon)\), the confidence-set procedure \(\operatorname{CAD}^{\mathrm{conf}}_{m,n}(z^{\mathrm{obs}},\epsilon,\alpha)\), and the boundary-atlas handle \(\mathcal A_{m,\epsilon}\). — This world is the one that carries visible substrate debt: real-closed-field quantifier elimination / cylindrical algebraic decomposition is NOT in Mathlib, and building it is a major upstream contribution, so `lem:cad-quantifier-elimination` is a cited SUBSTRATE-GATE (never crux — a crux tag there would only yield a permanently-unbuildable `sorry`). The three procedures are encoded as named `def`s returning the specification they satisfy (endpoint values + witness atoms + certificate/flag), and every theorem about them is stated CONDITIONAL on the gate Prop. Search trace: `lean_local_search "Semialgebraic"` → the single hit `CausalSmith.Experimentation.SaturationSkew.SemialgebraicOptimizationAlgorithms` (CausalSmith/Experimentation/EXP_SaturationSkewThreshold_Research/Helpers/Bridge/SemialgebraicSolver.lean:20), which is itself a threaded `Prop` SUBSTRATE-GATE for the same Tarski–Seidenberg/CAD debt — this note follows that precedent exactly; `lean_leansearch "real closed field quantifier elimination cylindrical algebraic decomposition"`-class queries and `lean_local_search` for `Semialgebraic` return no Mathlib decision procedure. PRECEDENT FILE for F2 to copy the gate shape from: CausalSmith/Experimentation/EXP_SaturationSkewThreshold_Research/Helpers/Bridge/SemialgebraicSolver.lean.
**required modules.** Mathlib.Analysis.Convex.Intrinsic, Mathlib.Analysis.Convex.StdSimplex

## Assumptions (A)

**A-1 (assumption).** \((Q_i,(A_{ij},Y_{ij}(0),Y_{ij}(1))_{j=1}^m)\) are independent and identically distributed across \(i\).

**A-2 (assumption).** \(Q_i\sim\mu\) for every \(1\le i\le n\).

**A-3 (assumption).** Conditional on \(Q_i=q\), the vectors \((A_{ij},Y_{ij}(0),Y_{ij}(1))\) are identically distributed and mutually independent over \(j\).

**A-4 (assumption).** \(\Pr((A_{ij},Y_{ij})=(a,y)\mid Q_i)=Q_{i,ay}\) almost surely for every \(a,y\in\{0,1\}\), \(1\le i\le n\), and \(1\le j\le m\).

**A-5 (assumption).** \((Y_{ij}(0),Y_{ij}(1))\perp A_{ij}\mid Q_i\).

**A-6 (assumption).** \(Y_{ij}=A_{ij}Y_{ij}(1)+(1-A_{ij})Y_{ij}(0)\).

## Definitions (P)

**P-1 (…).** \(\mathfrak M_{m,\epsilon}=\{(\mu,\mathcal M):\mu\in\mathcal P(K_{\epsilon}),\ Q_i\sim\mu,\ (Q_i,(A_{ij},Y_{ij}(0),Y_{ij}(1))_{j=1}^m)_{i=1}^n\text{ satisfies independent cluster sampling, conditional iid sampling, observed-cell calibration, latent ignorability, and consistency}\}\)

**P-2 (…).** \(\mathcal B_{m,\epsilon}=\{b\in\Delta_{N_m-1}:b=\int g_m(q)\,d\nu(q)\text{ for some }\nu\in\mathcal P(K_{\epsilon})\}\)

**P-3 (…).** \(\Theta_{m,\epsilon}(b)=\{\tau(\nu):\nu\in\mathcal F_{m,\epsilon}(b)\}=[L_m(b),U_m(b)]\)
**reuse.** Causalean.PartialID.IdentifiedInterval

**P-4 (…).** \(D_m(b)=(\sup_{\lambda\in\mathbb R^{N_m}}\{\lambda^\top b:\sum_c\lambda_cB_c^{(m)}\le f\text{ on }K_{\epsilon}\},\inf_{\lambda\in\mathbb R^{N_m}}\{\lambda^\top b:\sum_c\lambda_cB_c^{(m)}\ge f\text{ on }K_{\epsilon}\})\)

**P-5 (…).** \(\mathcal E_m=\{(\ell,u)\in V_m^2:\ell(q)\le f(q)\le u(q)\text{ for every }q\in K_{\epsilon}\}\), where \(\ell^\top b=\sum_c\ell_cb_c\) and \(u^\top b=\sum_cu_cb_c\) use the unique degree-\(m\) Bernstein coefficient vectors

**P-6 (For \(b\in\mathcal B_{m,\epsilon}\) and…).** For \(b\in\mathcal B_{m,\epsilon}\) and \(p_*\in[\epsilon,1-\epsilon]\), set \(I_{LW,m,p_*}(b)=[\ell_{m,p_*}^{\top}b,u_{m,p_*}^{\top}b]\) and define the best homogeneous scalar-reference intersection by \(I_{LW,m}^{\cap}(b)=[\max_{p_*\in[\epsilon,1-\epsilon]}\ell_{m,p_*}^{\top}b,\min_{p_*\in[\epsilon,1-\epsilon]}u_{m,p_*}^{\top}b]\), where the dot products use the unique degree-\(m\) Bernstein coefficient vectors of \(\ell_{m,p_*},u_{m,p_*}\in V_m\)

**P-7 (For observed \(z^{\mathrm{obs}}\in\mathcal H_n\), enumera…).** For observed \(z^{\mathrm{obs}}\in\mathcal H_n\), enumerate every \(w\in\mathcal H_n\), compute the exact multinomial upper-tail probability \(P_n(b';z^{\mathrm{obs}})\) using \(T_n(w;b')\ge T_n(z^{\mathrm{obs}};b')\), include every deviance tie, use no auxiliary randomization, and retain \(b'\) exactly when \(P_n(b';z^{\mathrm{obs}})>\alpha\)

**P-8 (…).** \(\mathcal C_{n,\alpha}(z^{\mathrm{obs}})=\{t\in\mathbb R:\exists b'\in\mathcal R_{n,\alpha}(z^{\mathrm{obs}})\cap\mathcal B_{m,\epsilon},\ t\in[L_m(b'),U_m(b')]\}\)

**P-9 (Represent \(t\in\mathcal C_{n,\alpha}(z^{\mathrm{obs}})\)…).** Represent \(t\in\mathcal C_{n,\alpha}(z^{\mathrm{obs}})\) by variables \(b'\), \(1\le s\le N_m+1\), atom locations \(q^1,\ldots,q^s\in K_{\epsilon}\), and masses \(\omega_k\ge0\) satisfying \(\sum_k\omega_k=1\), \(b'_c=\sum_k\omega_kB_c^{(m)}(q^k)\) for every \(c\), \(t=\sum_k\omega_k f(q^k)\), and \(b'\in\mathcal R_{n,\alpha}(z^{\mathrm{obs}})\); split by the finitely many deviance-order cells, clear positive propensity denominators, and apply CAD to eliminate all variables except \(t\)

**P-10 (Use two quantified branches).** Use two quantified branches. On the relative-interior branch, use at most \(N_m\) atoms, impose the rational simplex, overlap, mass, Bernstein-moment, objective, global dual-feasibility, and complementary-contact conditions, clear only the positive factors \(p(q)(1-p(q))\), and return algebraic primal endpoints, endpoint atoms, and matching dual certificates. On the arbitrary rational boundary branch, use at most \(N_m\) atoms and optimize the finite-support primal endpoint formula without any dual variables; after returning both algebraic primal endpoints and endpoint atoms, separately decide existence of matching global dual certificates. If no matching certificate exists, return a boundary/nonattainment flag rather than declaring the primal infeasible or withholding its endpoints and witnesses

**P-11 (Stratify the semialgebraic faces of…).** Stratify the semialgebraic faces of \(\mathcal B_{m,\epsilon}\) by CAD and, on each representing support variety, test membership of the restricted function \(f\) in the affine span of the restricted Bernstein coordinates

**P-12 (…).** \(d_m(\epsilon)=\inf_{v\in V_m}\|f-v\|_{\infty,K_{\epsilon}}\), where \(\|u\|_{\infty,K_{\epsilon}}=\sup_{q\in K_{\epsilon}}|u(q)|\).

## Lemmas (L)

**L-1 (Every \((\mu,\mathcal M)\in\mathfrak M_{m,\epsilon}\) has…).** Every \((\mu,\mathcal M)\in\mathfrak M_{m,\epsilon}\) has \(Q_i\sim\mu\), conditional count law \(C_i\mid Q_i=q\sim\operatorname{Multinomial}(m,q)\), marginal count law \(\Pr(C_i=c)=\int B_c^{(m)}(q)\,d\mu(q)\), and cluster-equally-weighted ATE \(\tau(\mu)=\int\{q_{11}/p(q)-q_{01}/(1-p(q))\}\,d\mu(q)=\int f\,d\mu\). Conversely, for every \(\mu\in\mathcal P(K_{\epsilon})\), draw \(Q_i\sim\mu\); conditional on \(Q_i=q\), draw \(A_{ij}\sim\operatorname{Bernoulli}(p(q))\) and conditionally treatment-independent binary potential outcomes \(Y_{ij}(0),Y_{ij}(1)\) with means \(q_{01}/(1-p(q))\) and \(q_{11}/p(q)\), respectively, independently over \(j\), and set \(Y_{ij}=A_{ij}Y_{ij}(1)+(1-A_{ij})Y_{ij}(0)\). This construction belongs to \(\mathfrak M_{m,\epsilon}\) and has conditional cell probabilities \(\Pr((A_{ij},Y_{ij})=(a,y)\mid Q_i)=Q_{i,ay}\) almost surely.

**L-2 (For every…).** For every \(b\in\operatorname{relint}(\mathcal B_{m,\epsilon})\), there exists \(\nu_b\in\mathcal F_{m,\epsilon}(b)\) with full support on \(K_{\epsilon}\). If \(r_b=f-\Pi_{V_m}^{L^2(\nu_b)}f\), then \(r_b\) is a bounded continuous function, \(\int B_c^{(m)}r_b\,d\nu_b=0\) for every \(c\), and \(\int fr_b\,d\nu_b=\int r_b^2\,d\nu_b\). Whenever \(f\notin V_m\), sufficiently small positive \(s\) makes \(d\nu_b^{\pm}=(1\pm sr_b)d\nu_b\) two distinct members of the same fiber with different ATEs.

**L-3 (For every finite \(m\ge1\) and every \(0<\epsilon<1/2\),…).** For every finite \(m\ge1\) and every \(0<\epsilon<1/2\), \(V_m\) is the restriction to \(K_{\epsilon}\) of polynomials of total degree at most \(m\) on the simplex, whereas \(f(q)=q_{11}/p(q)-q_{01}/(1-p(q))\) does not belong to \(V_m\).

## Theorems (T)

### T-block: t1 — Let…
**Statement.** Let \(H_{m,\epsilon}:=\operatorname{conv}\{(g_m(q),f(q)):q\in K_{\epsilon}\}\). The set \(H_{m,\epsilon}\) is compact and equals \[ \left\{\left(\int g_m\,d\nu,\int f\,d\nu\right): \nu\in\mathcal P(K_{\epsilon})\right\}. \] Every pair in \(H_{m,\epsilon}\) has a representing probability measure supported on at most \(N_m+1\) points of \(K_{\epsilon}\), and therefore has a compatible hierarchical SCM in \(\mathfrak M_{m,\epsilon}\) with at most \(N_m+1\) latent cluster laws. Consequently, for every \(b\in\mathcal B_{m,\epsilon}\), the sharp fiber \(\Theta_{m,\epsilon}(b)\) is a nonempty attained compact interval \([L_m(b),U_m(b)]\), and each endpoint has a representing measure, and hence a compatible hierarchical SCM, supported on at most \(N_m={m+3\choose3}\) latent laws. If \(b\in\operatorname{relint}(\mathcal B_{m,\epsilon})\), then \(D_m(b)=(L_m(b),U_m(b))\) with both dual extrema attained. If an optimal dual polynomial has contact image of affine dimension \(r\), its associated endpoint admits at most \(r+1\) atoms.

### T-block: t2 — For a continuous functional…
**Statement.** For a continuous functional \(h:K_{\epsilon}\to\mathbb R\), \(\int h\,d\nu\) is identical for all \(\nu\in\mathcal F_{m,\epsilon}(b)\) and every feasible \(b\) if and only if \(h\in V_m\). For \(h=f\), universal point identification therefore fails for every finite \(m\), and every \(b\in\operatorname{relint}(\mathcal B_{m,\epsilon})\) satisfies \(L_m(b)<U_m(b)\).

### T-block: t3 — For \(m\ge2\) and any \(q^*\in K_{\epsilon}\), the bounda…
**Statement.** For \(m\ge2\) and any \(q^*\in K_{\epsilon}\), the boundary moment vector \(b=g_m(q^*)\) has \(\mathcal F_{m,\epsilon}(b)=\{\delta_{q^*}\}\) and hence \(L_m(b)=U_m(b)=f(q^*)\).

### T-block: t4 — For every \((\ell,u)\in\mathcal E_m\) and every…
**Statement.** For every \((\ell,u)\in\mathcal E_m\) and every \(b\in\mathcal B_{m,\epsilon}\), \([L_m(b),U_m(b)]\subseteq[\ell^\top b,u^\top b]\). The lower equality \(L_m(b)=\ell^\top b\) holds if and only if \(b\in\operatorname{conv}\{g_m(q):q\in K_{\epsilon},\ \ell(q)=f(q)\}\), and the upper equality \(U_m(b)=u^\top b\) holds if and only if \(b\in\operatorname{conv}\{g_m(q):q\in K_{\epsilon},\ u(q)=f(q)\}\). If \(m\ge2\), each contact hull is a proper closed convex subset of \(\mathcal B_{m,\epsilon}\); consequently, for each endpoint separately there is a nonempty relatively open subset of \(\operatorname{relint}(\mathcal B_{m,\epsilon})\) on which the sharp endpoint strictly improves its valid envelope endpoint.

### T-block: t5 — For rational \(\epsilon\) and rational…
**Statement.** For rational \(\epsilon\) and rational \(b\in\operatorname{relint}(\mathcal B_{m,\epsilon})\), the relative-interior branch of \(\operatorname{CAD}_m(b,\epsilon)\) terminates and returns the algebraic numbers \(L_m(b)\) and \(U_m(b)\) by minimal polynomials and isolating intervals, endpoint measures with at most \(N_m\) atoms, compatible endpoint models in \(\mathfrak M_{m,\epsilon}\), and matching globally feasible Bernstein dual certificates. For arbitrary rational \(b\), the boundary-capable branch first decides primal feasibility using no dual variables. If \(b\notin\mathcal B_{m,\epsilon}\), it certifies infeasibility. If \(b\in\mathcal B_{m,\epsilon}\), it terminates and returns both algebraic primal endpoints, endpoint measures with at most \(N_m\) atoms, and compatible endpoint models, then separately decides whether matching globally feasible dual certificates exist; absence of a matching certificate returns a boundary/nonattainment flag and does not alter the primal endpoints or witnesses.

### T-block: t6 — For fixed \(m,n\), observed…
**Statement.** For fixed \(m,n\), observed \(z^{\mathrm{obs}}\in\mathcal H_n\), and \(0<\alpha<1\), the nonrandomized likelihood-ratio test that rejects \(b'\) when \(P_n(b';z^{\mathrm{obs}})\le\alpha\), with every equality \(T_n(w;b')=T_n(z^{\mathrm{obs}};b')\) included in the upper tail, has rejection probability at most \(\alpha\) under \(Z\sim\operatorname{Multinomial}(n,b')\). Its inversion \(\mathcal R_{n,\alpha}(z^{\mathrm{obs}})\) is semialgebraic: on each zero-pattern cell, every deviance comparison is equivalent after exponentiation and positive-denominator clearing to a monomial polynomial inequality, and \(P_n\) is a finite polynomial sum. If \(\epsilon\), \(\alpha\), and \(z^{\mathrm{obs}}\) are rational, then \(\mathcal C_{n,\alpha}(z^{\mathrm{obs}})\subseteq[-1,1]\) is a one-dimensional semialgebraic set. The procedure \(\operatorname{CAD}^{\mathrm{conf}}_{m,n}\) terminates after reducing each feasible \((b',t)\) to at most \(N_m+1\) atoms and returns either the complete disjoint decomposition into points and intervals with every algebraic endpoint and open-or-closed endpoint flag, or the certified endpoint hull with its attainability flags.

### T-block: t7 — Corollary
**Statement.** Corollary. For every \(n,m\), every \(0<\alpha<1\), and every \((\mu,\mathcal M)\in\mathfrak M_{m,\epsilon}\) with true count law \(b\), exact count-law coverage gives \(\Pr_{\mathcal M}(b\in\mathcal R_{n,\alpha}(Z))\ge1-\alpha\). On this event, the defining union for \(\mathcal C_{n,\alpha}(Z)\) contains \([L_m(b),U_m(b)]\); hence \(\Pr_{\mathcal M}([L_m(b),U_m(b)]\subseteq\mathcal C_{n,\alpha}(Z))\ge1-\alpha\), uniformly over \(\mathfrak M_{m,\epsilon}\), including boundary laws with singleton fibers.

### T-block: t8 — When \(m=1\), \(\epsilon=1/4\), and…
**Statement.** When \(m=1\), \(\epsilon=1/4\), and \(b=(1/4,1/4,1/4,1/4)\), the exact sharp interval is \([-1/3,1/3]\). The upper dual certificate is \(1-\frac43(q_{01}+q_{10})\), the lower certificate is \(\frac13-\frac43(q_{01}+q_{10})\), and each endpoint is attained by a mixing law with at most four atoms.

### T-block: t9 — For every fixed \(m\geq1\) and rational…
**Statement.** For every fixed \(m\geq1\) and rational \(0<\epsilon<1/2\), the relative boundary \(\operatorname{relbd}(\mathcal B_{m,\epsilon})\), taken in \(\operatorname{aff}(\mathcal B_{m,\epsilon})\), admits a finite truth-invariant semialgebraic stratification on each cell of which the indicator of \(L_m(b)=U_m(b)\), the affine dimension \(0\) or \(1\) of \(\Theta_{m,\epsilon}(b)\), and the ordered pair of minimum lower- and upper-endpoint support sizes are constant. Both minimum sizes lie in \(\{1,\ldots,N_m\}\). The construction \(\mathcal A_{m,\epsilon}\), completed by the quantified endpoint and support-size tests in the proof, is a terminating CAD procedure that returns defining formulas, the constant support labels, and one algebraic sample point with endpoint witnesses of the certified minimum sizes for every nonempty cell. It does not return a uniform symbolic endpoint-witness map over a positive-dimensional cell. For any separately supplied algebraic \(b\) on the relative boundary, specialization followed by a fresh terminating QE/CAD run returns endpoint witnesses having the certified minimum support sizes at that \(b\).

### T-block: t10 — Corollary
**Statement.** Corollary. For every \(p_*\in[\epsilon,1-\epsilon]\), \((\ell_{m,p_*},u_{m,p_*})\in\mathcal E_m\). For every \(b\in\mathcal B_{m,\epsilon}\), the extrema defining \(I_{LW,m}^{\cap}(b)\) are attained and \([L_m(b),U_m(b)]\subseteq I_{LW,m}^{\cap}(b)=\bigcap_{p_*\in[\epsilon,1-\epsilon]}I_{LW,m,p_*}(b)\). If \(m\ge2\), there is one nonempty relatively open set \(\mathcal R\subseteq\operatorname{relint}(\mathcal B_{m,\epsilon})\) such that, for every \(b\in\mathcal R\), \(L_m(b)>\max_{p_*\in[\epsilon,1-\epsilon]}\ell_{m,p_*}^{\top}b\) and \(U_m(b)<\min_{p_*\in[\epsilon,1-\epsilon]}u_{m,p_*}^{\top}b\). The optimized interval above is the intersection of the scalar-reference, \(b\)-only transplant; no inclusion comparison with their full observed-covariate, covariate-dependent procedure is asserted.

### T-block: t11 — For every finite \(m\geq1\) and every \(0<\epsilon<1/2\),…
**Statement.** For every finite \(m\geq1\) and every \(0<\epsilon<1/2\), let \(d_m(\epsilon)\) be the uniform approximation modulus in the preceding definition. Put \(\rho=1-2\epsilon\) and \[ E_m(\epsilon)=\frac{1}{T_m(1/\rho)} =\operatorname{sech}\!\left(m\operatorname{arcosh}(1/\rho)\right), \] where \(T_m\) is the degree-\(m\) Chebyshev polynomial of the first kind. Then \[ \max_{b\in\mathcal B_{m,\epsilon}}\{U_m(b)-L_m(b)\} =\sup_{b\in\operatorname{relint}(\mathcal B_{m,\epsilon})}\{U_m(b)-L_m(b)\} =2d_m(\epsilon), \] and \[ 0<d_m(\epsilon)\leq E_m(\epsilon)\leq\rho^m. \] Moreover, \(E_m(\epsilon)<\rho^m\) whenever \(m\geq2\). Consequently every feasible fiber has width at most \[ 2\operatorname{sech}\!\left(m\operatorname{arcosh}\!\left(\frac{1}{1-2\epsilon}\right)\right). \] For every \(0<\delta\leq2\), the integer plate-size condition \[ m\geq \frac{\operatorname{arcosh}(2/\delta)} {\operatorname{arcosh}(1/(1-2\epsilon))} \] guarantees \(U_m(b)-L_m(b)\leq\delta\) simultaneously for all \(b\in\mathcal B_{m,\epsilon}\).
