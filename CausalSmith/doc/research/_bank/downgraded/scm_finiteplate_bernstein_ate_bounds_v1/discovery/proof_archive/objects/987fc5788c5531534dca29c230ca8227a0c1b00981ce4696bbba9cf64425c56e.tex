Fixed-size cluster counts reveal only degree-\(m\) Bernstein moments of latent four-cell exposure--outcome laws, so the cluster-equally-weighted ATE is generally an interval rather than a point. Each hierarchical SCM carries \(\mu\in\mathcal P(K_{\epsilon})\), draws \(Q_i\sim\mu\), and calibrates the observed four-cell law to \(Q_i\). The sharp interval has attained measure and Bernstein-polynomial programs for every relative-interior count law, with compatible endpoint SCMs using at most \({m+3\choose3}\) latent laws. It is contained in the best intersection over all homogeneous scalar-reference Lee--Weidner transplants, and for \(m\geq2\) both sharp endpoints improve strictly on one common nonempty relatively open interior region. Exact multinomial inversion yields a semialgebraic ATE confidence set recoverable by terminating CAD, and a finite semialgebraic atlas classifies the boundary fibers for rational overlap thresholds.