For the worked member \(\mathcal G_\star=\mathcal G(\{C\},1)\), every compatible \(P_{\mathrm{obs}}\) satisfies \[Q_a(y)=\sum_{c}P_{\mathrm{obs}}(c)\sum_bP_{\mathrm{obs}}(b\mid a,c,R_W=0)\sum_{a'}P_{\mathrm{obs}}(y\mid b,a',c,R_W=0)P_{\mathrm{obs}}(a'\mid c,R_W=0)=f_a^\star(P_{\mathrm{obs}})(y).\] The derivation first records \(P(Y\mid\operatorname{do}(A),R_W)\), then selects \(R_W=0\), uses \(B_1\) as the front-door set conditional on \(C\), and averages over the unselected \(P_{\mathrm{obs}}(C)\). No de Aguas Proposition 4 certificate exists: the edge \(A\leftrightarrow Y\) remains an open length-one path in every \(\mathcal H_r[\underline A]\), so condition \(\mathrm{(v)}\) fails for every \(K,H,\{L_r\}_r\). The root-control procedure is inapplicable because \(C\to R_W\), so its required context variable is not a causal root. The TCF partition places every compatible observed law on an identifying cell carrying \(f_a^\star\).