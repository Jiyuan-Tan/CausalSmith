Each of the \(n\) vertices has at most three current statuses and membership in the fixed set has at most two possibilities, so there are at most \(3^n2^n=6^n\) graph-status and fixed-set records. A partial missingness context has at most
\[
c=\prod_{R_Z\in\mathcal R}(|\mathcal X_{R_Z}|+1)
\]
values. No vertex is fixed twice, so a license trace has length at most \(t\leq n\); for length \(j\), each entry has at most \(\ell+1\) label choices. For a fixed operation prefix, its provenance expression is determined by the formal observed cells and the prescribed marginalization, fixing, and selection operations, so it contributes no independent choice. Consequently
\[
B_{\mathrm{state}}=6^nc\sum_{j=0}^{t}(\ell+1)^j
\]
bounds the states even if no two branches are merged. The corrected canonical replay rule can only reduce this number.

At most one doubled test is generated for each state and each \((a,y)\), giving
\[
N_{\mathrm{term}}leq|\mathcal X_A||\mathcal X_Y|B_{\mathrm{state}}.
\]
Each copy \(\theta^i\) has \(g+hK_{\mathcal G}\) coordinates and the free vector has \(|\mathcal X_{\mathcal O}|\) coordinates, so the total number of variables is at most
\[
p=2\{g+hK_{\mathcal G}\}+|\mathcal X_{\mathcal O}|.
\]
Concatenating the at most \(N_{\mathrm{term}}\) formulas with one separator or Boolean connective per formula has binary encoding length at most
\[
S_{\mathrm{tot}}=N_{\mathrm{term}}(s+1).
\]
A signed-subresultant CAD terminates directly: projection removes one variable at each of at most \(p\) levels; every projection set is finite because it is formed from finitely many coefficients, discriminants, and resultants; univariate signed-subresultant sequences strictly decrease degree and therefore isolate finitely many real roots; and lifting performs finitely many sign evaluations over finitely many cells. By the definition of \(\operatorname{CAD}(p,S_{\mathrm{tot}})\), all projection, isolation, lifting steps and output cells are bounded by that number. Adding the state visits yields the proposed bound.

The resulting partition contains finitely many integer polynomials. Rational and real-algebraic inputs admit finite exact sign computation, and the exact-real interface supplies each required sign by definition. Thus specialization terminates after finitely many calls. None of these finite bounds is asserted to be polynomial.