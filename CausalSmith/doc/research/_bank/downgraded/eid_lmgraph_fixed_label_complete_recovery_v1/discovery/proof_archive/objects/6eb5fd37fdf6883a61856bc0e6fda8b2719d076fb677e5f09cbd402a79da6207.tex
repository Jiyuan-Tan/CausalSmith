Whenever \(\operatorname{RC}(\mathcal G,A,Y)\) holds, delayed restriction is unnecessary. For every compatible \(P_{\mathrm{obs}}\), its unique TCF partition cell is identifying exactly when every context-specific DAG is identifying; for every \(a,y\), that cell carries exactly \(f_a^{\mathrm{RC}}(P_{\mathrm{obs}})(y)\).