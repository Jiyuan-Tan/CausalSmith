In a recursive semi-Markovian causal graph, whenever the ordinary \(\operatorname{ID}\) algorithm returns an expression for \(P(Y\mid\operatorname{do}(A=a))\), that expression is correct in every compatible causal model.