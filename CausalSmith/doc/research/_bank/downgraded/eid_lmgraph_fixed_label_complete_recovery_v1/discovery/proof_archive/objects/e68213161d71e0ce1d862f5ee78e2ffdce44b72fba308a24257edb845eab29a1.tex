For every finite positive regular-maximal \(\mathcal G\) and supplied \(A,Y\), two conclusions are logically independent. First, whenever the colored graphical recursion fails graphwise to derive a trace functional for \((A,Y)\), there exists at least one, not necessarily unique, failed target-relevant branch whose licensed prefix-closed trace yields some \(H\in\operatorname{CH}(\mathcal G,A,Y)\); the exhaustive diagnostic compiler returns such an \(H\). Second, for every exact specialization \(\operatorname{Exact}(P_{\mathrm{obs}})\) with nonempty \(\mathfrak F(\mathcal G,P_{\mathrm{obs}})\), every negative cell relation yielding \(\omega=(a,y,\theta^0,\theta^1)\) decodes to \(\mathcal M_0,\mathcal M_1\in\mathfrak F(\mathcal G,P_{\mathrm{obs}})\) with individual-edge-factorized latent variables, strictly positive full-data cells,
\[
P_{\mathcal M_0}(\mathcal O)=P_{\mathcal M_1}(\mathcal O)=P_{\mathrm{obs}},
\qquad
Q_a^{\mathcal M_0}(y)\neq Q_a^{\mathcal M_1}(y).
\]
The graphwise hedge is existential and is not required to be realized by the decoded pair. Conversely, the negative-cell pair is independent of the failed graphical branch. No unique sample-aligned record \(\Delta(\omega,\mathcal T)\) is asserted.