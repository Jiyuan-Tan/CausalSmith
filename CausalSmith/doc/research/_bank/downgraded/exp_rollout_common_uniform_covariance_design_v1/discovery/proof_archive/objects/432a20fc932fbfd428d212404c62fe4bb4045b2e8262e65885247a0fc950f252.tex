A monotone production ramp assigns unit \(i\) at every fraction by the shared threshold \(Z_i(u)=\mathbf 1\{U_i\le u\}\).  Over the declared aggregate-coefficient ellipsoid for a fixed degree-at-most-\(\beta\) schedule, the exact cross-stage covariance is a subset-intersection kernel.  The kernel reduces to small subset-lattice blocks, Nature's worst schedule is an explicit finite-valued leading-eigenvector construction, and the joint node--weight minimum is attained.  Polynomial exactness also yields the degree-sensitive moment barrier \(\Psi_\beta(q)\), which is strictly stronger than the first-moment bound below full rollout when \(\beta\ge2\).  The known-design radius certificate gives finite-sample Chebyshev coverage.  The CEY comparison class is used only as its noise-free union over unknown static graphs: exact covariance and unbiasedness transfer to that union, whereas ellipsoidal minimax equality and radius coverage do not transfer without a common bound.  The sole remaining mathematical gap is a global active-block and node-dual characterization of all minimizers and the Chebyshev--Lobatto win, lose, and tie regions.