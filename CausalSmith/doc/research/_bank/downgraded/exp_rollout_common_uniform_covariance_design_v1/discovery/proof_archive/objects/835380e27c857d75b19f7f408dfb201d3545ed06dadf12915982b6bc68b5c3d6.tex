For every feasible \((n,\beta,k,q,\delta,B,\omega)\) with \(1\le\beta\le n\),
\[
\inf_{p\in\mathcal P_{k,q,\delta}}\inf_{w\in\mathcal W_\beta(p)}\sup_{(Y,c)\in\mathcal C_{n,\beta}(B,\omega)}\operatorname{Var}_U(\widehat\tau_{p,w})=\frac{B^2}{n}\inf_{p\in\mathcal P_{k,q,\delta}}\inf_{w\in\mathcal W_\beta(p)}\max_{0\le h\le\beta}\lambda_{\max}\{H_h(p,w)\}=V^*_{n,\beta,k,q,\delta}.
\]
For every fixed \((p,w)\), if \(v^*\) is a unit leading eigenvector in an active block and \(Q_h\) is its explicit subset-lattice lift, then \(a^*=(B/\sqrt n)\Omega^{1/2}Q_hv^*\) attains the inner supremum. The schedule \(c^*_{i,S}=a^*_S\), \(c^*_{i,\varnothing}=0\), and \(Y_i^*(z)=\sum_{S\in\mathcal S_{n,\beta}}c^*_{i,S}\prod_{j\in S}z_j\) satisfies \(\max_{i\in I_n,z\in\{0,1\}^n}|Y_i^*(z)|\le\sum_{S\in\mathcal S_{n,\beta}}|a_S^*|<\infty\) and is a legal least-favourable member of \(\mathcal C_{n,\beta}(B,\omega)\). For fixed nodes \(p\), the weight problem has a semidefinite epigraph and attains its value after quotienting the common null directions of the covariance blocks. No joint node--weight attainment claim is made.