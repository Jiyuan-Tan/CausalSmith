CortezEichhornYu2022 is the setup and writing anchor.  Its static graph fixes each unit's neighborhood, nonzero low-order coefficients are supported on that neighborhood, interaction degree is bounded, and one common random ordering generates the monotone Bernoulli rollout.  Its finite-schedule bounds use schedule-specific \(Y_{\max}\) and \(L_{\max}\), and its observed responses add independent identically distributed Gaussian noise.  Under the corresponding linear readout, that noise contributes \((\sigma^2/n)\sum_{t=0}^k w_t^2\) in addition to the design variance.  This paper isolates the \(\sigma=0\) potential-outcome projection and takes the union over unknown static graphs.  The complete graph proves that this union is exactly the unrestricted algebraic degree-at-most-\(\beta\) class, so the exact monomial covariance identity and polynomial-exact unbiasedness apply throughout it.  The ellipsoidal minimax equality, leading-eigenvector least-favourable schedule, and radius-based Chebyshev coverage are instead claims only for \(\mathcal C_{n,\beta}(B,\omega)\).  If a genuinely uniform noise-free bound \(\sum_S|c_{i,S}|\le Y_{\max}\) holds and \(\omega_{\min}=\min_d\omega_d\), the algebraic inclusion uses \(B=\sqrt n\,Y_{\max}/\sqrt{\omega_{\min}}\) and yields only a conservative certificate and coverage transfer.  Kallus2021 supplies an ellipsoidal eigenvalue minimax template for two-arm assignment laws, not the nested subset kernel or rollout-node optimization.  ChenLiDengWang2023 optimizes a covariance-based upper bound for a single-stage network estimator, while EichhornKhanUganderYu2024 gives design-dependent low-order variance bounds without the common-uniform cross-stage saddle.  XuChenFernandezSinnoBhasin2015 and LiJamkhandeKochetkovLiu2022 document shared deterministic hashes in production ramps but do not solve the interference-induced cross-threshold design problem.