For every declared feasible tuple, the exact covariance criterion has a jointly attaining pair \((p^*,w^*)\).  Conditional on fixed nodes, the weight problem has the explicit semidefinite epigraph
\[
C(p)w=e,
\qquad
\begin{pmatrix}
\rho I&L_p(w)^{\mathsf T}\\
L_p(w)&I
\end{pmatrix}\succeq0.
\]
The singleton block also gives the sharp universal bound
\[
V^*_{n,\beta,k,q,\delta}\ge
\frac{B^2\omega_1}{n}\frac{1-q}{q}
\quad(0<q<1),
\]
with equality for \(\beta=1\), while \(V^*_{n,\beta,k,1,\delta}=0\).  What remains unresolved is a necessary-and-sufficient global active-block certificate over the coupled node and weight variables and the resulting Chebyshev--Lobatto win, lose, or tie classification.