---
qid: exp_rollout_common_uniform_covariance_design
spec: v1
topic: "PRESOLVE EVIDENCE REQUIRING VERIFICATION: Study nested hash-threshold rollouts generated by common uniforms U_i, with Z_i(p)=1{U_i<=p}, rather than independent assignments at each treated fraction. For N=3, take Y_1=Y_2=0, Y_3(z)=z_1z_2, q=1/2, and interpolation nodes {0,x,1/2}, x in [1/8,3/8]. The exact covariance kernel is Gamma(p,r)={min(p,r)^2-p^2r^2}/9; with the endpoint interpolation weights it gives variance 4(1-x)/{9(1-2x)}, strictly increasing in x, hence 14/27 at x=1/8 versus 2/3 at x=1/4. Use this only as a legal witness, not as a universal node optimum. The accepted target is a robust exact-covariance saddle over a declared aggregate-coefficient ellipsoid for beta-order finite-population schedules, jointly choosing nodes and polynomial-exact linear weights, with an explicit legal least-favourable schedule, joint attainment, subset-lattice block reduction, and a design-computable known-radius finite-sample Chebyshev certificate. The full unbounded noise-free CEY graph-union envelope is a comparator, not the minimax class. A design-based CLT and outcome-estimated covariance are outside the formal claims because the unrestricted schedule class supplies neither a genuine setwise dependency graph nor an identifiable variance estimator. LinkedIn's deterministic common-hash ramp is the direct consumer. The analytic global node-weight optimality conditions, exact leading small-budget constant, and Chebyshev-Lobatto win/loss/tie regime map remain explicitly open. EARLY KILL TEST: Drop if the covariance arithmetic fails, if common-U covariance equals the independent-stage envelope after legal conditioning, or if prior nested-rollout work already proves the same exact aggregate-ellipsoid covariance saddle and least-favourable schedule."
novelty_target: field
banked_novelty_tier: subfield
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "Field-tier exact optimizer and leading-constant characterization remains the declared analytic node-weight frontier."
reusable: not_reusable
reraise_status: true-negative
gap_reasons:
  - "The exact covariance reduction/attained min-sup and fixed-parameter q exponent are valid but field lift requires the exact global node-weight optimizer, matching leading constant, active-block dual certificate, and Chebyshev-Lobatto win/loss/tie regimes."
  - "The required lift is exactly the declared oeq:analytic-node-weight-frontier, a genuine open problem rather than a bounded D0 repair."
reusable_artifacts:
  - "discovery/core.json — sound typed exact common-uniform covariance/min-sup result and dependency graph"
  - "discovery/writeup.tex — complete derivation, legal least-favourable schedule, block reduction, joint attainment, and small-budget exponent"
  - "discovery/open_obligations.json — precise unresolved global node-weight optimizer/dual frontier"
  - "discovery/solve_oeq_analytic_node_weight_frontier.json — attempted optimizer-frontier analysis and obstruction receipts"
seeds_burned: []
proof_attempt_summary: |
  D0 verified the exact common-uniform covariance identity, reduced the robust design problem to an attained spectral min-sup over the declared aggregate-coefficient ellipsoid, and proved finite-sample radius coverage plus the fixed-parameter small-budget exponent. Three carried D0.5 revisions exhausted all bounded improvements while math review remained clean. The field-tier lift would require solving the genuinely open global node-weight optimizer, exact leading constant, active-block dual certificate, and Chebyshev-Lobatto regime map, so the sound result is banked at subfield tier.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 117257336
  pipeline_claude_tokens: 0
  total_tokens_consumed: null
banked_on: "2026-09-03"
---

# exp_rollout_common_uniform_covariance_design / v1 — Downgraded

**Topic.** PRESOLVE EVIDENCE REQUIRING VERIFICATION: Study nested hash-threshold rollouts generated by common uniforms U_i, with Z_i(p)=1{U_i<=p}, rather than independent assignments at each treated fraction. For N=3, take Y_1=Y_2=0, Y_3(z)=z_1z_2, q=1/2, and interpolation nodes {0,x,1/2}, x in [1/8,3/8]. The exact covariance kernel is Gamma(p,r)={min(p,r)^2-p^2r^2}/9; with the endpoint interpolation weights it gives variance 4(1-x)/{9(1-2x)}, strictly increasing in x, hence 14/27 at x=1/8 versus 2/3 at x=1/4. Use this only as a legal witness, not as a universal node optimum. The accepted target is a robust exact-covariance saddle over a declared aggregate-coefficient ellipsoid for beta-order finite-population schedules, jointly choosing nodes and polynomial-exact linear weights, with an explicit legal least-favourable schedule, joint attainment, subset-lattice block reduction, and a design-computable known-radius finite-sample Chebyshev certificate. The full unbounded noise-free CEY graph-union envelope is a comparator, not the minimax class. A design-based CLT and outcome-estimated covariance are outside the formal claims because the unrestricted schedule class supplies neither a genuine setwise dependency graph nor an identifiable variance estimator. LinkedIn's deterministic common-hash ramp is the direct consumer. The analytic global node-weight optimality conditions, exact leading small-budget constant, and Chebyshev-Lobatto win/loss/tie regime map remain explicitly open. EARLY KILL TEST: Drop if the covariance arithmetic fails, if common-U covariance equals the independent-stage envelope after legal conditioning, or if prior nested-rollout work already proves the same exact aggregate-ellipsoid covariance saddle and least-favourable schedule.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** D0.5 exhausted three carried revise rounds: the exact covariance reduction, attained min-sup, and fixed-parameter small-budget exponent are sound, but field lift requires the unresolved global node-weight optimizer, matching leading constant, active-block dual certificate, and Chebyshev-Lobatto regime map.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

The exact covariance/operator derivation is reusable evidence for future nested common-hash rollout work.
Any upgrade should start from `discovery/open_obligations.json` and solve the global optimizer frontier;
repeating local covariance reductions or the already-matched exponent will not address the tier gap.
