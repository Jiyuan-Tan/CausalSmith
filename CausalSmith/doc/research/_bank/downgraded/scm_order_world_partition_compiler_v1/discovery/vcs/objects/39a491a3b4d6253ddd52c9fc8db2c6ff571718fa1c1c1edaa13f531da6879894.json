{
  "node_type": "statement",
  "body": {
    "id": "thm:partition-continuation",
    "kind": "theorem",
    "statement": "Let \\(\\mathcal H\\) be the context set produced by \\(\\mathsf N\\). Fix \\(i\\in\\{0,\\ldots,k\\}\\). Any two feasible partial evaluations through coordinate \\(i\\) that induce the same partition \\(\\pi_i\\) have exactly the same support of subsequent world-value columns subject to all later factual and normalized-query pins. Every legal representative transition is realizable by selecting one natural output per block, and every accepting transition path extends by sparse-response completion to a complete \\(\\widehat f\\in\\mathfrak F\\) with at most \\(kh\\) stored rows and one default per coordinate.",
    "free_symbols": [
      "Nq",
      "GammaN",
      "k",
      "pi",
      "H",
      "E",
      "xprefix",
      "f",
      "fhat",
      "F",
      "h",
      "D"
    ],
    "depends_on": [
      "ass:finite-alphabets",
      "ass:complete-predecessor-functions",
      "def:ordered-scm-class",
      "def:ordered-evaluation",
      "def:query-normalizer",
      "def:prefix-partition",
      "def:sparse-response-completion",
      "lem:query-normalization"
    ],
    "justification": "This is the exact continuation invariant that removes numeric alphabet labels from the persistent state after value-preserving normalization.",
    "gap": "Halpern supplies finite causal certificates and Rossetto--Antonucci quotient completed response signatures; neither proves equality of complete normalized prefixes to be a local right congruence.",
    "consumer": "A production compiler can reuse one canonical state for every numeric history with the same cross-world normalized-prefix equality type.",
    "proof_tex": "The assertion is vacuous for continuation when \\(i=k\\), so first take \\(i<k\\) and write \\(q=i+1\\).  Let a feasible partial evaluation have partition \\(\\pi_i\\).  For a block \\(T\\in\\pi_i\\), split its worlds into\n\\[\n N_T=\\{w\\in T:q\\notin\\operatorname{dom}(w)\\},\n \\qquad\n J_T=T\\setminus N_T.\n\\]\nHere a context \\(w\\) is identified with its intervention.  By \\texttt{def:prefix-partition}, all worlds in \\(T\\) have the same complete predecessor tuple \\(x^w_{<q}\\).  Consequently, \\texttt{def:ordered-evaluation} forces one common value \\(z_T\\in\\mathcal D_q\\) on every world in \\(N_T\\), while each \\(w\\in J_T\\) has the fixed value \\(w(q)\\).  Conversely, different blocks of \\(\\pi_i\\) have different complete predecessor tuples, so \\texttt{ass:complete-predecessor-functions} permits their values \\(z_T\\) to be selected independently.\n\nIt follows that a column \\(y=(y_w)_{w\\in\\mathcal H}\\) is legal at coordinate \\(q\\) exactly when\n\\[\n y_w=w(q)\\quad(w\\in J_T),\n \\qquad\n y_w=z_T\\quad(w\\in N_T)\n\\]\nfor some \\(z_T\\in\\mathcal D_q\\) for every block \\(T\\), and when the entries of \\(y\\) obey the factual and normalized-query pins at coordinate \\(q\\).  These conditions mention the preceding history only through \\(\\pi_i\\).  Given such a column, define the row of \\(f_q\\) at the predecessor tuple belonging to \\(T\\) to be \\(z_T\\).  The tuples for distinct blocks are distinct, so these assignments are consistent; finiteness and nonemptiness in \\texttt{ass:finite-alphabets} allow arbitrary default values on all unlisted tuples.  Therefore every legal column is realizable.\n\nNow take two feasible histories with the same \\(\\pi_i\\).  The preceding characterization gives them the same set of legal numeric columns.  Appending the same column splits each old block according to equality of its new entries, and blocks that were already distinct cannot merge because their complete prefixes remain unequal.  Hence the two histories induce the same \\(\\pi_{i+1}\\).  Induction on \\(q=i+1,\\ldots,k\\) gives identical supports of all later column sequences subject to all later pins.  This proves both inclusions of the asserted continuation-support equality.\n\nFor an accepted transition path, at coordinate \\(q\\) record one pair\n\\[\n (x^w_{<q},z_T)\n\\]\nfor each block \\(T\\) containing at least one natural world, choosing any such \\(w\\in N_T\\).  There are at most \\(h\\) recorded pairs per coordinate and hence at most \\(kh\\) in total.  If the same predecessor tuple occurs for two natural worlds, those worlds are in the same block and the transition rule assigned the same output, so the recorded rows are consistent.  Choose a default \\(d_q\\in\\mathcal D_q\\), which exists by \\texttt{ass:finite-alphabets}, and assign it to every unlisted tuple as in \\texttt{def:sparse-response-completion}.  Induction over coordinates then shows that the completed \\(\\widehat f\\) reproduces every stored world column: intervened coordinates use their constants, and natural coordinates find precisely their recorded row.  Thus \\(\\widehat f\\in\\mathfrak F\\), proving the stated sparse-extension claim as well as realizability of every representative path."
  }
}