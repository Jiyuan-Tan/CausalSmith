%%% FIELD oeq-statement
Can the handle in \(\text{def:legal-relative-handle}\) establish, for \(r_n=a_n/2\),
\[
\limsup_{n\to\infty}\sup_{P_n\in\mathcal M}P_n\!\left[\sqrt n\|\Phi_{\mathrm{ext}}(\widehat p_n)-\Phi(p_n)\|_\infty>Q_{1-\alpha}\{E_{p_n,r_n}(G^*)\mid\widehat p_n\},\ B_n\ne\varnothing,\ M(\widehat p_n)>0\right]\le\alpha,
\]
together with uniformly vanishing probability of the fallback event \(\{B_n=\varnothing\}\cup\{M(\widehat p_n)=0\}\), including arrays with zero cells, simultaneous active-cut ties, and \(Q_{1-\alpha}\{E_{p_n,r_n}(G^*)\mid\widehat p_n\}\to0\), where each displayed conditional quantile uses the lower-quantile convention?
%%% FIELD oeq-partial
The accepted sharp-identification result still gives \(\Theta_I(p)=[L(p),U(p)]\).  The total procedure is definable and measurable.  The fallback event is uniformly negligible and the finite coordinate diameters are \(O_{P_n}(n^{-1/2})\) on its complement.  At each fixed separated regular face, the critical value converges to the exact common-Gaussian max-norm quantile and coverage converges to \(1-\alpha\).  The explicit \(K=4\) diagnostic also has exact limiting coverage while combining an order-\(n^{-1}\) atom, order-\(n^{-1/2}\) support emergence, simultaneous upper-cut ties, and a degenerate lower endpoint.  On the singleton-cell counterexample, every even symmetric-probe reduction is impossible, but the one-sided ray satisfies \(W^{\mathrm{ray}}_{p,r}(g)=0\), \(W^{\mathrm{ray}}_{p,r}(-g)=M_0^{-1}\), and the explicit legal base \(b_\tau\) dominates the opposite-direction finite increment.  This is a single-face oriented repair, not a global comparison or an all-array coverage theorem.
