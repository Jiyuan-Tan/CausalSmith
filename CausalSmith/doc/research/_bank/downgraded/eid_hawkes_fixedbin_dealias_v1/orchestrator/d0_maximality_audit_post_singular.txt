IMPROVE

Fresh checkpoint audit

This is a fresh whole-paper D0 maximality audit at the exact checkpoint
2026-09-05T19:05:33.241Z, after application of the corrected
singular-feedback extension. I read the complete current
discovery/writeup.tex, discovery/core.json, discovery/d0_working.json, the
sealed open-ended-question records, the estimand, comparator, sampling-model,
project-justification, related-work, TL;DR, and honest-scope prose, and every
prior audit/adjudication receipt in orchestrator/. Both current JSON files
parse successfully. The checkpoint reports nineteen working records, no new
target to discharge, deterministic TeX rendering, and no prose warning.

Post-singular theorem and totality audit

The restricted-image theorem is present, proved, and synchronized with the
typed core and durable working state. Its declared domain is the population
image of stable common-exponential representatives satisfying A != 0,
diag(A)=0, and the principal-log strip only on
Delta(A-beta I)|im(A). It does not impose ambient full rank, acyclicity, or an
orientation restriction, and it does not encode the recovered parameter as
an observable gate.

The proof establishes every required domain-totality fact. The stable
semigroup gives S=im(Gamma_1)=im(K)=im(A), rank r>=1, J invertible, and
invariance of S. Consequently R has full row rank and F represents T|S. The
restricted strip makes the principal logarithm exist and equal Delta B|S;
stability makes L and F-I_r invertible. Since im(A)=S, an adapted basis gives
an upper block form for A, and diag(A)=0 yields

  beta=-tr(L)/r.

The proof then recovers H=J_S^{-1}, K, and Z=K-K^T=AD-DA^T. Stationarity
gives D=diag(m/Delta)>0, and W=O^T D^{-1}O is positive definite. With
C_0=AD and P=OO^T, the identities im(C_0)=S,
C_0(I-P)=Z(I-P), and C_0D^{-1}O=OA_S give exactly

  C_0={OA_S-Z(I-P)D^{-1}O}W^{-1}O^T+Z(I-P).

Thus the displayed reconstruction returns AD, then A and mu. The orthogonal
basis-change calculation is correct and makes the ambient output invariant;
applying it to an arbitrary representative proves representative
independence.

Both strict witnesses are valid. The weighted three-cycle plus isolated
label has rank three in dimension four, spectral radius at most 1/2, and
restricted imaginary part at most pi/4 for every Delta under the stated
epsilon_Delta choice. The reciprocal pair plus isolated label has rank two,
spectral radius 1/2, and real restricted spectrum for every Delta. Both have
positive baselines and define stationary locally finite Hawkes laws. They
are outside the DAG regime and outside ambient full rank.

The original all-bin-width DAG theorem remains intact and still covers
singular, disconnected, repeated-pole, diamond, and proportional-row cases
without a logarithm strip. The full-rank stable theorem remains intact and
continues to permit self-excitation, cycles, and reciprocal edges under the
ambient strip. These are genuinely complementary to the new zero-diagonal
positive-rank restricted-strip branch. All Gaussian approximation, HAC,
rank adaptation, support recovery, and graph-confidence assertions remain
confined to the acyclic fixed-margin class. The prose and structured metadata
state those boundaries consistently.

Sealed-open-question audit

resolved_oeqs is empty. Both sealed_open_oeqs entries remain present, and the
corresponding canonical statements remain status to-prove. The one-lag
question now correctly leaves only the rank-two directed-chain and
transitive-triangle loci unresolved; the fork proposition is only a proved
partial result. The boundary question now correctly records only the
delta=0 empty-graph diameter obstruction; the positive-delta_n frontier and
an attaining honest procedure remain unresolved. The singular-feedback
theorem does not settle or rewrite either question.

Comparator, class, and observation audit

There is no class or observation laundering in the current paper. The broad
stable-kernel comparators observe a complete covariance function, a full
spectrum or transform, or a full aggregated-count law. Exact-history
comparators change the sampling experiment. Small-bin comparators change the
asymptotic regime. None transfers a fixed-positive-bin inverse from one mean
and two covariance lags. The comparator table correctly marks the relations
as informed-by, and the current three structural recovery regimes are
described without importing the richer observation operators.

Nevertheless, the current paper is not observation-maximal. Its already
proved substrate yields a material covariance-only extension to both stable
feedback regimes, but the formal covariance-only theorem and its map are
still restricted to the DAG class. This is not merely a prose corollary: it
removes the separately supplied first-order mean from every newly added
feedback recovery theorem and produces two new domain-total identifying
maps on strict non-DAG classes.

Concrete scoped root directive

Extend covariance-only exact recovery from the acyclic branch to the entire
current three-regime population union. Preserve the existing DAG theorem,
and add image-defined covariance domains and branchwise maps for

  (i) the stable full-rank ambient-strip image, and
  (ii) the nonempty stable zero-diagonal restricted-strip image,

where each covariance domain is the population image of the corresponding
structural class under (Gamma_0,Gamma_1,Gamma_2), not a list of arbitrary
observable gates.

For either branch, first use only Gamma_1 and Gamma_2 to reconstruct the
positive-lag covariance density. In the full-rank branch, set

  T=Gamma_2 Gamma_1^{-1},
  B=Delta^{-1}Log(T),
  J=B^{-1}(T-I_p),
  K=J^{-2}Gamma_1,
  C_tilde,+ (t)=exp(Bt)K.

In the restricted-rank branch, use the existing O,R,F,L^sharp,H^sharp steps,
which also do not use m or D, and set

  C_tilde,+ (t)=O exp(L^sharp t)(H^sharp)^2 R.

The proofs already show in the respective image domains that these expressions
equal C_+(t) for every t>0. Now define in either branch

  D^0=Delta^{-1}[Gamma_0
       - integral_0^Delta (Delta-t)
         {C_tilde,+(t)+C_tilde,+(t)^T} dt],
  m^0=Delta diag(D^0).

The zero-lag covariance-measure decomposition used in the existing
thm:covariance-only-recovery is structural, not acyclic:

  Gamma_0=Delta D
    + integral_0^Delta (Delta-t){C_+(t)+C_+(t)^T}dt.

Hence D^0=D=diag(lambda)>0 and m^0=m on each new population image. Feed
(m^0,Gamma_1,Gamma_2) into the corresponding already-proved mean-plus-two-lag
inverse. This immediately gives

  Psi_fr(Gamma_0,Gamma_1,Gamma_2)=(beta,A,mu)

on the full-rank ambient-strip covariance image and

  Psi_0,+,rs(Gamma_0,Gamma_1,Gamma_2)=(beta,A,mu)

on the positive-rank zero-diagonal restricted-strip covariance image. Every
logarithm and inverse is total because the covariance-image domains inherit
exactly the totality facts proved for the corresponding mean-plus-two-lag
images; D is recovered before any formula requiring it. Basis invariance in
the restricted branch is inherited from the existing O -> OU calculation.

This extension is material and strict. The current cycle-plus-isolated and
reciprocal-plus-isolated witnesses certify covariance-only recovery on
singular non-DAG laws, while any admitted self-exciting full-rank witness
such as beta=1, A=(1/2)I_p, and any strictly positive baseline certifies the
separate full-rank branch. Neither conclusion is contained in
the present DAG-only covariance theorem. The result uses the same fixed-bin
second-order substrate and no event times, continuum of lags, full spectrum,
higher cumulants, or aggregated-count likelihood.

Synchronize the covariance-only definition/theorem, symbols, estimand
overview, comparator entry for the covariance-only result, TL;DR,
project-justification, related-work, and honest scope so that the alternative
observation tuple (Gamma_0,Gamma_1,Gamma_2) is stated for all three recovery
regimes. Do not extend any sampling/inference theorem beyond the acyclic
fixed-margin class, do not claim that covariance-only recovery solves the
one-lag OEQ, and do not claim any positive-delta_n boundary frontier.
