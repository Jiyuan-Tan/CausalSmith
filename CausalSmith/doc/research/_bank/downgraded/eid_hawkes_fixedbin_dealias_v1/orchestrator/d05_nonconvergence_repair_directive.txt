Repair the D0.5 non-convergence without adding any primitive assumption or weakening any delivered theorem.

For lem:uniform-bin-eighth-moment, preserve its existing finite-depth Poisson-cluster construction but strengthen the proved moment conclusion explicitly: the same recursion yields every fixed order needed here, in particular sup_theta E_theta ||X_0||_2^16 < infinity uniformly on M_{Delta,p}(kappa). Run the existing cumulant calculation through order 16, using g_{p-1}^{(32)}(0) in the duration-tail Cauchy--Schwarz step and the complete Bell polynomial through order 16. If the formal statement must change, emit the faithful statement-change proposal, all exact dependency/free-symbol metadata, and a complete proof of the proposed statement. Do not add an assumption.

For lem:bin-moment-limit, replace the two asserted “repeated clipping/largest gap” passages by a complete four-point strong-mixing derivation, either inline or through a newly added helper lemma. State and prove the product inequality
|Cov(V,W)| <= C ||V||_p ||W||_q alpha(g)^{1-1/p-1/q},  p^{-1}+q^{-1}<1,
using an elementary truncation/layer-cake argument unless an already verified citation in the core directly supplies it. For ordered s_1<=...<=s_4 and G=max_j(s_{j+1}-s_j), apply it to the j and 4-j products using p=8/j and q=8/(4-j). Explicitly handle the 1|3, 2|2, and 3|1 fourth-cumulant cancellations and derive
|cum(U_{s_1}^{a_1},U_{s_2}^{a_2},U_{s_3}^{a_3},U_{s_4}^{a_4})| <= C rho^{G/2}.
Then derive E|sum_{k in I} a^T U_k|^4 <= C|I|^2 from the moment-cumulant decomposition, absolutely summable pair covariances, and summation over the three ordered gaps. Also prove
sup_{h,h'>=0} sum_{t in Z}|Cov(U_0^a U_h^b,U_t^c U_{t+h'}^d)| < infinity
uniformly by the same cumulant envelope, the bound that at most 6q+1 integers t have largest adjacent gap among {0,h,t,t+h'} at most q, and the two uniformly bounded covariance convolutions. Expand the two finite HAC averages, set t=ell-k, and use this displayed sum to derive the C/N_n bound in the current equation (13), retaining the O(b_n^2/N_n) conclusion under b_n=o(n^{1/2}). Update the lemma’s descriptive phrase to “derived geometric-mixing and sixteenth-bin-count bounds imply.” If a helper lemma is added, wire every exact dependency and symbol.

Synchronize related_work with exactly the supported distinction and no invented theorem number or rate: “JinHuang2026 studies causal discovery with hidden Hawkes nodes through a vanishing-bin autoregressive approximation and covariance-rank conditions. That latent-node, shrinking-bin problem is complementary to, and does not subsume, the present exact recovery for a fully observed labeled process from one mean and two covariance lags at a fixed positive bin width.” Leave the bibliography and comparator-table entry unchanged.

Synchronize technical_internal_limitation by deleting every dangling reference to def:wiener-hopf-numerical-evaluator and the claim that the note supplies a regularized finite-section evaluator. State instead: “The exact final Wiener--Hopf step is infinite dimensional; this note supplies no numerical evaluator or finite-section discretization theorem for it. That operation may be numerically ill-conditioned, and no estimator, rate, or confidence theorem is claimed for the broad branch. The existing rank-adaptive HAC and graph-inference results remain acyclic-only.” Do not recreate that deleted definition.

Preserve without substantive change the broad restricted-strip population recovery theorems and definitions, the acyclic-only scope of all CLT/HAC/support/confidence claims, the principal-log and Bacry--Muzy citation attestations, and both sealed OEQs. Do not alter claims outside the two named proof targets. Emit prose_updates for the two synchronized prose fields. Ensure the final carrier has zero occurrences of def:wiener-hopf-numerical-evaluator and contains the Jin--Huang distinction in Related Work.
