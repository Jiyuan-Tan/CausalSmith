Round-3 adjudication: accept all 9 variants as one atomic post-image.

Exact selectors, in dependency-safe order:

- ACCEPT `symbol-add:sym:DiffEvent`
- ACCEPT `symbol-replace:sym:VHat`
- ACCEPT `symbol-replace:sym:Tmargin`
- ACCEPT `symbol-replace:sym:Iedge`
- ACCEPT `definition:def:hac-estimator`
- ACCEPT `definition-replace:def:hac-estimator`
- ACCEPT `definition:def:graph-confidence-set`
- ACCEPT `definition-replace:def:graph-confidence-set`
- ACCEPT `statement-replace:prop:thresholded-uniform-support`

No rejection selectors are warranted.

Proof decisions:

- RETAIN `lem:bin-moment-limit`
- RETAIN `thm:rank-adaptive-estimation`
- RETAIN `prop:thresholded-uniform-support`
- RETAIN `thm:simultaneous-graph-coverage`

Use no `--reject-proof` flags. These four payloads form the required topological closure: `lem:bin-moment-limit` → `thm:rank-adaptive-estimation` → support/coverage.

Rationale:

- `D_n` is measurable and non-circular. It depends only on the already-measurable `V_n` and finitely many Borel starred signed-jump coordinates. Within `V_n`, nonzero `Z*ij` exactly removes every off-diagonal positive-part kink. It does not depend on `VHat`, `Tmargin`, or any downstream inference object.
- No proof assumes `P(D_n)→1`. This is essential because margin-class graphs may contain absent unordered pairs. `D_n` is used only for the optional full-parameter sandwich and Wald benchmark.
- The generalized-inverse Gaussian-maximum quantile is total for every finite PSD covariance, including singular and zero matrices. The defining set is actually nonempty and the quantile finite because the Gaussian maximum is finite almost surely and its CDF tends to one; `inf empty=+infinity` is a harmless guard.
- Interval and graph-set semantics are unchanged: inference remains on `V_n`, with real-line intervals and all labeled DAGs on `V_n^c`.
- The support proposition's mathematical statement, symbols, and status are unchanged. Its replacement records the missing `lem:covariance-semigroup` proof-spine dependency.
- No full positive-part Gaussian limit returns. The coverage theorem continues to use only the smooth signed jump; `VHat` and `Tmargin` remain optional and quarantined behind `D_n`.
- All downstream claims reopened by the two definition changes have supplied replacement proofs. The graph-coverage proof explicitly covers `D_n` measurability and singular-covariance calibration.

Mechanical risks:

- Apply all nine selectors atomically. Do not split either `definition:*` carrier from its matching `definition-replace:*` carrier.
- Use kind-qualified selectors because both definitions occur in two proposal channels.
- Add `DiffEvent` before processing `VHat`, `Tmargin`, or either definition replacement if the application engine validates sequentially.
- Promote proofs in packet order. Unlike round 2, rejecting the theorem proofs would unnecessarily leave a complete dependency closure stale.
- Keep `statement-replace:prop:thresholded-uniform-support` even though its displayed theorem statement is byte-identical; the dependency update is the substantive change.
- Minor editorial cleanup only: the support proof attributes the skew-jump identity to `thm:two-lag-exact-recovery`, while its newly direct dependency is `lem:covariance-semigroup`. Both support the identity, so this is not rejection-worthy.

Audited against packet hash `0b21778dbb60fe9deff02c0e09028ff90728f3b0a4b0188780753fd408d70030` and the 2026-09-05T16:37:27.725Z checkpoint.
