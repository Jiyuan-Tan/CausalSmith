D0.5 DECISION LOG — 2026-09-05

Classification: REPAIRABLE AT D0.

The decision-referee finding `opaque_inverse@def:wiener-hopf-two-lag-inverse` is not a load-bearing mathematical-identification defect. The current theorem correctly defines a population identifying functional through the unique causal entrywise-L1 Wiener–Hopf solution, and the cited Wiener–Hopf uniqueness result is sufficient for injectivity/identification. The finding is nevertheless valid against the stronger ExactID presentation: an unspecified infinite-dimensional Wiener–Hopf solve is not yet a downstream-evaluable recovery map.

A bounded same-assumption repair exists. No finite-section approximation or numerical convergence theorem is needed.

Let
    P := H^2 R,
    C_+(t) = O exp(Lt) P,
and, for x > 0, define the explicitly evaluable response transform
    Rcal(x) := ∫_0^∞ exp(-xt) C_+(t)^T dt
             = P^T (x I_r - L^T)^(-1) O^T.
Define (beta#, A#) as the unique pair (x,M) ∈ (0,∞) × R^{p×p} satisfying
    M O = O(L + x I_r),
    M [D + Rcal(x)] = K.
Equivalently, on
    Omega := {x > 0 : det[D + Rcal(x)] ≠ 0},
set
    A(x) := K [D + Rcal(x)]^(-1)
and take beta# as the unique x ∈ Omega satisfying
    A(x) O = O(L + x I_r),
with A# := A(beta#).

The true parameters satisfy these equations because
    Rcal(beta) = K^T(2 beta I - A^T)^(-1) = Q A^T,
    D + Rcal(beta) = (2 beta I - A)Q,
    A[D + Rcal(beta)] = K,
and
    A O = O(L + beta I_r).

Conversely, every candidate (x,M) satisfying the two finite-dimensional equations makes
    chi_x(t) := M exp(-xt) 1_{t>0}
solve the original Wiener–Hopf equation exactly. The positive-time convolution contributes
    g_+(t) - exp(-xt) K D^(-1),
while the negative-time contribution is
    exp(-xt) M Rcal(x) D^(-1);
the two displayed candidate equations therefore make the residual vanish. Wiener–Hopf uniqueness then gives
    M exp(-xt) = A exp(-beta t).
Since A ≠ 0 on the broad branch, x = beta and M = A. Thus the broad inverse reduces to a finite-dimensional rational matrix calculation plus a unique positive scalar root. Wiener–Hopf uniqueness remains only the uniqueness certificate, not an unevaluated computational step.

Accordingly, do not narrow the broad theorem to injectivity-only unless this finite-dimensional replacement unexpectedly fails formal checking. Preserve the four explicit algebraic branches and both sealed OEQs.

The maximality receipt remains sound concerning the flagship feedback-inference request, but its “no material room” conclusion is not sound as applied to the opaque population inverse: it missed the finite-dimensional reduction above.

The requested flagship extension—finite-section Wiener–Hopf estimation, approximation error negligible relative to sampling error, root-n inference, and simultaneous graph coverage over cyclic/reciprocal/self-exciting models—is genuine new research and is not a bounded D0 repair. It requires new uniform stability, spectral/rank/root-separation and conditioning assumptions; continuity and differentiation of the inverse/root map; finite-section bias control; perturbation analysis for image/rank/logarithm operations; infinite-depth cyclic-cluster mixing or another dependence argument; and graph-boundary inference. Current sampling theory is acyclic and finite-depth, so this work must not be claimed.

NEXT D0 DIRECTIVE:

1. Replace `def:wiener-hopf-two-lag-inverse` with the finite-dimensional response-transform/root-pair definition above.
2. Rewrite the proof of `thm:stable-nonzero-two-lag-wiener-hopf-recovery` to establish:
   (i) the resolvent formula for Rcal(x);
   (ii) existence of the true pair;
   (iii) the convolution calculation showing every candidate generates a causal L1 Wiener–Hopf solution;
   (iv) uniqueness of the pair by the cited Wiener–Hopf uniqueness theorem;
   (v) recovery of beta, A, and mu = (I - A/beta)lambda.
3. Synchronize the covariance-only wrapper theorem and all descriptions of `PhiBroad`, `PsiBroad`, `ChiWH`, `M0WH`, and `M1WH`. The broad map must no longer claim to numerically solve an infinite-dimensional Wiener–Hopf equation.
4. Update the TLDR, gap/fill language, estimand functional, related-work comparison, honest scope, and rendered writeup wherever the broad inverse is described as an unevaluated causal-L1 solve.
5. Replace the technical limitation with:
   “The broad population inverse reduces to a finite-dimensional matrix-resolvent calculation and a unique positive scalar root. This note proves exact population totality but no uniform conditioning, sample estimator, rate, or confidence theorem for that root on the broad feedback class. Existing inference remains acyclic-only.”
6. Preserve the restricted-strip broad domain, nonzero/stability conditions, all four explicit algebraic recovery branches, the acyclic-only inference theorem, and both sealed open-exception questions.
7. Do not add finite-section or root-n feedback claims in D0. After synchronization, rerun the full checker and both referee passes.

Final disposition: the current population theorem is mathematically sound but underspecified as an ExactID evaluator; apply the bounded finite-dimensional repair and remain at field tier. Do not pursue the flagship inference program in this repair cycle.
