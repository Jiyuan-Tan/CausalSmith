Round-2 adjudication: accept all 15 outstanding variants. The combined post-image is coherent and fixes the prior totality and finite-sample covariance defects without altering either accepted statement narrow, any model class, or the fixed-bin identification kernel.

Exact decisions:

- ACCEPT `definition:def:rank-adaptive-estimator`
- ACCEPT `definition:def:hac-estimator`
- ACCEPT `definition:def:graph-confidence-set`
- ACCEPT `symbol-add:sym:ValidEvent`
- ACCEPT `symbol-add:sym:zHat`
- ACCEPT `symbol-replace:sym:thetaHat`
- ACCEPT `symbol-replace:sym:OmegaHat`
- ACCEPT `symbol-replace:sym:VHat`
- ACCEPT `symbol-replace:sym:WHat`
- ACCEPT `symbol-replace:sym:Iedge`
- ACCEPT `symbol-replace:sym:Cset`
- ACCEPT `symbol-replace:sym:Tmargin`
- ACCEPT `definition-replace:def:rank-adaptive-estimator`
- ACCEPT `definition-replace:def:hac-estimator`
- ACCEPT `definition-replace:def:graph-confidence-set`

No `--reject-proof` or other cancellation is warranted. Both provisional proofs should be retained; under the packet contract they supersede the shorter stale proofs embedded in the current paper.

Rationale:

- `def:rank-adaptive-estimator` now uses a fixed Borel-measurable SVD rule and one shared event `V_n`. The event checks the selected-rank spectral gap, `q∈(0,1)`, every required inverse, positive intensity divisors, and invertibility of `I−A*/β*`. The sentinel is assigned on the entire complement. This makes `theta_hat_n` genuinely total and makes the invalid event common to every downstream object.
- `zHat` is total and the identity `z_hat=zeta(theta_hat_n)` is valid on both branches: on `V_n` it follows from `mu*=(I−A*/beta*)lambda*` plus the added invertibility check; on the complement both sides equal zero under the sentinel.
- The Bartlett estimator is PSD for every sample. With `N_n=n−2`, the stated HAC sum equals the double-sum kernel matrix with entries `(1−|s−t|/b_n)_+`; the displayed sliding-window Gram representation proves PSD even when `b_n>N_n`. Hence both sandwich matrices are PSD wherever computed, and `W_hat_n=0` remains PSD on invalid samples.
- The Gaussian maximum is therefore defined for every sample, including singular and zero `W_hat_n`. On invalid samples the real-line intervals and all-acyclic-graphs fallback give automatic conservative coverage.
- `V_hat_n` and `T_n^fm` are now explicitly optional and quarantined from signed-edge inference. Where the full positive-part derivative or ellipsoid is unavailable, `V_hat_n=0` and `T_n^fm=Theta_bar`; no full-map Gaussian claim is reintroduced.
- The accepted theorem statements are unchanged. The new provisional rank proof establishes that `P(V_n^c)→0` pointwise and uniformly on the margin class, so sentinel use does not disturb consistency or root-n tightness. The new simultaneous-coverage proof explicitly incorporates the fallback event, Bartlett PSD identity, singular-covariance anti-concentration, and plug-in quantile argument.
- No assumptions or class definitions change. Restricting the estimator to the Bartlett window is an estimator-formula correction, not model-class manipulation. The exact two-lag inverse, covariance semigroup, signed-jump recovery, target `(beta,A,mu)`, and graph estimand are preserved.

No additional hardening is required before applying this bundle. Only minor editorial cleanup is advisable later:

- Replace the proof typo `sqrt n,a_n→infinity` with `sqrt n a_n→infinity`.
- Spell the optional full-derivative event as an explicit measurable condition, such as nonzero recovered signed-jump coordinates in addition to `V_n`, rather than the semantic phrase `where the derivative exists`.
- Define the Gaussian quantile by a generalized-inverse convention for complete formal precision when `W_hat_n` is singular.

The packet contains no statement proposals, no assumption proposals, no citation revalidations, and no required core-edit mandates. Both OEQs remain sealed and open; their durable records are still explicitly `partial: true`.
