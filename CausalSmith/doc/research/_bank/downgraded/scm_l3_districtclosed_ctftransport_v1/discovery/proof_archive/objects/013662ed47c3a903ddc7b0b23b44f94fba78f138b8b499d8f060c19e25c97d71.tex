For a saturated compatible family, independent complete district blocks and finite total probability give, for every cell \(C\),
\[
P_{M^s}(C)=
\sum_{\omega}\mathbf 1_C(\omega)
\prod_Dq_D^s(\omega_D)
=\Psi_C^s(\theta^s).
\tag{1}
\]
For a supplied postprocessed output cell, typed-menu realizability gives directly
\[
P_a^s(y)=\mathcal L_{M^s}(f_a(T_a))(y)
=P_{M^s}\{f_a(T_a)=y\}.
\tag{2}
\]
The deterministic preimage identity evaluates the indicator in \((1)\); it does not introduce tuple-cell probabilities as menu generators.  District-kernel invariance supplies every unchanged-district equality, so the induced tuple belongs to \(\Theta(\mathcal P)\).

Conversely, a point of \(\Theta(\mathcal P)\) defines independent latent blocks storing the complete deterministic response maps.  Acyclicity gives unique recursive evaluation, and the complete output-cell equations in the fiber give every supplied law.  The realization stays on \(\mathcal X\).  This proves the saturated joint range equality.  Formula \((1)\) contains at most one probability coordinate from each visited original district, hence is multi-affine and has the stated degree.

For arbitrary \(G\), finite canonical counterfactual equivalence applied in each domain preserves every finite supplied and target counterfactual cell and each induced complete district response law.  It therefore maps the broad compatibility fiber into \(\Theta_G^{\mathrm{can}}(\mathcal P)\).  Conversely, the one-hot structural tables, canonical exogenous laws, graph-compatible within-district restrictions, independence across original districts, cross-domain response-law equalities, and directly imposed output-cell equations define a compatible canonical family.  Thus the sparse canonical joint range equality also holds on the original alphabets.

Each parameter fiber is a closed subset of a finite product of compact simplices and finite one-hot sets, hence compact.  The relevant nonemptiness assumption makes it nonempty.  All constraints are polynomial sign conditions, so each fiber is semialgebraic.  Fix either exact row \((\mathfrak C,\Lambda,\Xi)\).  Continuity on compact \(\Lambda\) gives attained extrema.  Joint range equality makes the displayed minima and maxima sharp causal bounds and supplies original-alphabet endpoint models existentially.  The cell is identified exactly when the extrema agree; if they differ, the two endpoint models have unequal values.  A probability bound improves a logical endpoint exactly under the stated strict inequality.  No connectedness was assumed, so no interval-filling conclusion follows.

Apply the same argument to the nonnegative evidence polynomial.  The three cases
\[
e_+=0,
\qquad e_-=0<e_+,
\qquad e_->0
\tag{3}
\]
are exhaustive.  They respectively give uniform zero, nonuniform definition with zero- and positive-evidence witnesses, and uniform strict positivity.  Only the last permits division.  There the conditional ratio is continuous on compact \(\Lambda\), so it attains sharp lower and upper values.  Moreover,
\[
Q_\sigma^\star-Q_\rho^\star
=\frac{\Xi_{B_\sigma\cap E}^\star-
\Xi_{B_\rho\cap E}^\star}{\Xi_E^\star}.
\tag{4}
\]
The positive denominator and attained extrema prove the exact strict-dominance and opposite-ranking characterizations.

Finally, the broad local inclusion, focal equality, and orthogonal identity are exactly those proved by the one-district projection theorem.  Its fixed-baseline preservation calculation applies to output cells because their recursively evaluated preimage indicators are ordinary finite indicators.  The two separation lemmas give the stronger sufficient predicates.  The local construction never discards a cross-district polynomial equality from either global fiber.  Hence local row-face failure is promoted to nonidentification only under exact focal projection; otherwise unequal optimizers of the appropriate exact global program are the valid paired-model certificate.