Run \(\mathsf{DCTFTR}\) first.  Recursion soundness proves termination and shows that a successful fast-path expression \(F\in\mathscr R(\mathcal P)\) equals the causal query throughout \(\mathcal C(\mathcal P)\).  Relabelling this terminal \(\operatorname{ID\_RAT}\) preserves its derivation.  Every other local terminal invokes the canonical phase.

Set
\[
S=\Theta_G^{\mathrm{can}}(\mathcal P).
\]
The exact global theorem and \(\mathcal C(\mathcal P)\ne\varnothing\) make \(S\) nonempty, compact, semialgebraic, and exactly range equivalent to the broad SCM fiber on the original alphabets.  Its finitely many polynomial coefficients lie in the effectively presented field of real algebraic numbers generated by the supplied values.  This field is real closed.  Therefore the cited Basu--Mohammad-Nezhad theorem applies to every finite first-order formula below and supplies a terminating algorithm returning an equivalent quantifier-free sign formula.

For an unconditional target put \(p_T=\Phi_{T,\mathrm{can}}^\star\).  Apply quantifier elimination to
\[
\varphi_T(t):=\exists x\,[x\in S\wedge t-p_T(x)=0]
\tag{1}
\]
and call the equivalent output \(\chi_T(t)\).  Apply it also to the closed sentence
\[
\exists t_1,t_2\,[\chi_T(t_1)\wedge\chi_T(t_2)
\wedge t_1\ne t_2]
\tag{2}
\]
to decide singleton status.  Finally eliminate the quantified variable in
\[
\chi_T(\ell)\wedge
\forall t\,[\chi_T(t)\Longrightarrow\ell\leq t]
\tag{3}
\]
and in the analogous maximum formula.  The resulting formulas are \(\chi_{\min}\) and \(\chi_{\max}\).  Compactness and nonemptiness of \(S\), together with continuity of \(p_T\), show that each endpoint formula has exactly one satisfying real value.  Thus \(\operatorname{UNCOND\_QE}\) describes the exact range and endpoints and decides identification without outputting either endpoint.

For a conditional target put \(e=\Phi_{E,\mathrm{can}}^\star\) and \(n=\Phi_{B_\rho\cap E,\mathrm{can}}^\star\).  Since \(S\ne\varnothing\) and \(e\geq0\) on \(S\), exactly one of
\[
\begin{array}{ll}
\mathsf Z:&\neg\exists x\,[x\in S\wedge e(x)>0],\\
\mathsf N:&\exists x\,[x\in S\wedge e(x)=0]
\ \wedge\ \exists x\,[x\in S\wedge e(x)>0],\\
\mathsf P:&\neg\exists x\,[x\in S\wedge e(x)=0]
\end{array}
\tag{4}
\]
holds.  Quantifier elimination decides these closed sentences.  Case \(\mathsf Z\) is uniform zero evidence.  Case \(\mathsf N\) is exactly nonuniform definition.  In case \(\mathsf P\), continuity and compactness strengthen pointwise positivity to \(\min_{x\in S}e(x)>0\).  Only then is cross multiplication legal, and
\[
\varphi_Q(t):=
\exists x\,[x\in S\wedge e(x)>0\wedge e(x)t-n(x)=0]
\tag{5}
\]
is exactly the conditional range.  Quantifier elimination yields \(\chi_Q\); repeating \((2)\)--\((3)\) gives its singleton decision and endpoint formulas.  This proves all terminal semantics and zero-denominator rejection.

Only finitely many calls to the terminating quantifier-elimination algorithm are made, so the hybrid procedure terminates.  Exact global range equivalence shows that a singleton is exactly identification and that a non-singleton contains two values attained by original-alphabet same-menu SCM families.  This is an existential mathematical consequence of range equivalence, not an algorithmic witness-extraction claim.  No CAD cells, samples, isolating data, explicit endpoint values, canonical parameter witnesses, or proof-record verifier has been used.