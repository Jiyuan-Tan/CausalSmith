Choose \(\mathbf M^0\in\mathcal C_{\mathrm{sat},D}(\mathcal P)\).  District factorization gives the finite nonfocal product law.  Since every slice belongs to the linear space \(R_D(m_D)\),
\[
\bar\chi_C^{s,\mathbf M^0}=
\sum_{\omega_{-D}}\nu_{-D}^{0,s}(\omega_{-D})\chi_{C,\omega_{-D}}
\in R_D(m_D).
\]
The same baseline works for every cell, proving \(\mathsf{BASep}_D\), and composition gives the hierarchy.

For strictness, take independent binary roots \(X,Z\) feeding binary \(Y=\mathbf1\{X=Z\}\), with \(D=\{X\}\), fair \(Z\), and supply only the complete marginal of \(Y\).  Then \(A_D=\mathbf1^{\mathsf T}\) and \(R_D=\operatorname{span}\{(1,1)\}\).  The two slices of \(\{Y=1\}\) are \((1,0)\) and \((0,1)\), so active separation fails, while their fair average is \((1/2,1/2)\in R_D\); the complementary cell is analogous.  Focal saturation permits every law of \(X\) while the fixed fair \(Z\) and structural table of \(Y\) remain unchanged, so the baseline is compatible.