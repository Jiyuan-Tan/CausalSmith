Write \(q=(q_1,q_2,q_3,q_4)^\top\) in the response-profile order of the binary definition.  The equations \(A_Dq=m_D\) are
\[
q_1+q_2+q_3+q_4=1,
\qquad q_3+q_4=\frac12,
\qquad q_2+q_4=\frac12,
\qquad q_j\geq0.
\]
Setting \(p=q_4\) gives
\[
q_1=p,
\qquad q_2=q_3=\frac12-p,
\qquad 0\leq p\leq\frac12,
\]
and proves the unique parameterization \(q(p)\).  Since \(d_Dq(p)=1\), direct row multiplication gives
\[
Q_\rho(q(p))=q_2+q_3=1-2p,
\qquad
Q_\sigma(q(p))=q_2+q_4=\frac12.
\]
The candidate row gives \(r_D^\dagger q(p)=q_4=p\), whence \(I_D=[0,1/2]\), and
\[
\Gamma_{\rho\prec\sigma}(p;m_D)
=g_{\rho,\sigma,D}q(p)
=-q_3+q_4=2p-\frac12.
\]
Therefore \(\mathcal J_{\rho\prec\sigma}^+=(1/4,1/2]\).

In the augmented row order,
\[
(0,-1,0,2)
\begin{bmatrix}A_D\\ r_D^\dagger\end{bmatrix}
=(0,0,-1,1)=g_{\rho,\sigma,D}.
\]
Thus the displayed vector is dual feasible with equality, and its objective is
\[
(0,-1,0,2)(1,1/2,1/2,p)^\top=2p-\frac12.
\]
At \(p=3/8\) this equals \(1/4\).

The required SCM realizations do not rely on a sparse-district surjectivity claim.  For each \(p\in[0,1/2]\), take one binary latent variable \(U_Y\) with state space \(\Omega_D\) and law \(q(p)\), and set \(Y_t=f_t(U_Y)\) for \(t\in\{0,1\}\).  Use the same \(U_Y\) law in target and source because \(D=\{Y\}\) is unchanged.  Take \(Z=0\) deterministically in the target and \(Z=1\) deterministically in the source, and choose the treatment mechanism independently.  All outputs use the original binary alphabets, the graph is recursive, the singleton districts are mutually independent, district closure holds because \(\Delta_s=\{Z\}\), and unchanged-district response-law invariance holds.  The laws at \(p=1/4\), \(p=0\), and \(p=1/2\) are respectively
\[
(1/4,1/4,1/4,1/4),
\qquad (0,1/2,1/2,0),
\qquad (1/2,0,0,1/2),
\]
with gap values \(0,-1/2,1/2\).  This supplies the asserted original-alphabet boundary and strict-ranking witnesses.

With no experiment, \(p=0\) makes \(\rho\) strictly optimal and \(p=1/2\) makes \(\sigma\) strictly optimal, so
\(
\operatorname{PO}(\varnothing)=\{\rho,\sigma\}
\).
Acquiring the only block imposes \(q_4=3/8\); the unique parameterization yields
\[
\mathcal K_{\{e^\dagger\}}=\{q(3/8)\}.
\]
At this point \(Q_\rho=1/4<1/2=Q_\sigma\).  Since \(\mathcal R_{\mathrm{arm}}=\{\rho,\sigma\}\),
\[
\operatorname{PO}(\{e^\dagger\})
=\mathcal P_{\mathrm{full}}
=\{\sigma\}.
\]