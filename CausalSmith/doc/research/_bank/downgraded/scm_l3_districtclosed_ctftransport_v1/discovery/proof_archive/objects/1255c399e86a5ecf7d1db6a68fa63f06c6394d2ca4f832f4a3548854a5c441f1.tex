Run \(\mathsf{DCTFTR}\) first.  By \(\mathrm{thm:recursion\text{-}soundness}\), this run terminates uniquely.  If it returns an identifying expression \(F\), that theorem gives
\[
F(\mathcal P)=P_{M^\star}(T)
\quad\text{or}\quad
F(\mathcal P)=P_{M^\star}(B_\rho\mid E)
\tag{1}
\]
for every \(\mathbf M\in\mathcal C(\mathcal P)\), with every quotient protected by its stored nonzero or strict-positivity certificate.  The expression grammar in \(\mathrm{def:allowed\text{-}rational\text{-}functional}\) gives \(F\in\mathscr R(\mathcal P)\), so relabelling this terminal as \(\operatorname{ID\_RAT}\) proves the fast-path clause.  Every other local terminal invokes the canonical phase.

Put
\[
S=\Theta_G^{\mathrm{can}}(\mathcal P)\subseteq\mathbb R^N.
\tag{2}
\]
The nonemptiness hypothesis and \(\mathrm{thm:exact\text{-}global\text{-}canonical\text{-}frontier}\) imply that \(S\) is a nonempty compact real semialgebraic set and that its target-cell ranges equal the corresponding ranges over the broad compatibility fiber on the original alphabets.  Let \(\mathbb D\) be the ordered coefficient ring generated by the finitely many supplied rational or real-algebraic output probabilities.  Their effective presentations give exact arithmetic and sign comparison in \(\mathbb D\), and the order-preserving embedding into \(\mathbb R_{\mathrm{alg}}\) gives exact coefficient representations.  It does not restrict the variables in \((2)\).

Fix any real-closed field \(K\) containing an ordered copy of \(\mathbb D\).  Apply \(\mathrm{lem:basu\text{-}mohammad\text{-}nezhad\text{-}quantifier\text{-}elimination}\) with ambient field \(R=K\) and coefficient ring \(D=\mathbb D\).  Its algorithm terminates and uses only arithmetic in \(\mathbb D\).  Because the input syntax, coefficient operations, and exact sign decisions in \(\mathbb D\) are independent of \(K\), the deterministic output is one \(\mathsf{QEFormula}\) syntax whose interpretation is equivalent to the input formula in every such \(K\).  In particular, take \(K=\mathbb R\).  Thus all following realizations include arbitrary real canonical probabilities and real target values, including transcendental ones.

For an unconditional target write \(p_T=\Phi_{T,\mathrm{can}}^\star\).  Quantifier elimination applied to
\[
\varphi_T(t)=\exists x\,[x\in S\wedge t-p_T(x)=0]
\tag{3}
\]
returns \(\chi_T\) satisfying, for every \(t\in\mathbb R\),
\[
\chi_T(t)\quad\Longleftrightarrow\quad
\exists x\in S\;t=p_T(x).
\tag{4}
\]
By the exact-global range equality, the right side of \((4)\) is equivalent to
\[
\exists\mathbf M\in\mathcal C(\mathcal P)\;
t=P_{M^\star}(T).
\tag{5}
\]
Hence the real realization of \(\chi_T\), not merely its algebraic points, is the full broad causal range.  Eliminating
\[
\exists t_1,t_2\,[\chi_T(t_1)\wedge\chi_T(t_2)\wedge t_1\ne t_2]
\tag{6}
\]
therefore decides non-singleton status exactly.  Since \(S\) is nonempty and compact and \(p_T\) is continuous, it attains a real minimum \(\ell_T\) and maximum \(u_T\).  Consequently
\[
\chi_T(\ell)\wedge\forall t\,[\chi_T(t)\Longrightarrow\ell\leq t]
\tag{7}
\]
holds over \(\mathbb R\) exactly at \(\ell=\ell_T\); existence is attainment and uniqueness follows from antisymmetry.  The reversed inequality similarly defines only \(u_T\).  Quantifier elimination preserves these equivalences over \(\mathbb R\), proving the endpoint and unconditional-terminal clauses without asserting that every range point or either endpoint is algebraic.

For a conditional target let
\[
e(x)=\Phi_{E,\mathrm{can}}^\star(x),
\qquad
n(x)=\Phi_{B_\rho\cap E,\mathrm{can}}^\star(x).
\tag{8}
\]
By causal probability semantics, \(e(x)\geq0\) on \(S\).  Since \(S\ne\varnothing\), exactly one of
\[
\begin{array}{ll}
\mathsf Z:&\neg\exists x\in S\;e(x)>0,\\
\mathsf N:&(\exists x\in S\;e(x)=0)\wedge(\exists x\in S\;e(x)>0),\\
\mathsf P:&\neg\exists x\in S\;e(x)=0
\end{array}
\tag{9}
\]
holds: if a zero exists, existence or nonexistence of a positive point selects \(\mathsf N\) or \(\mathsf Z\); if no zero exists, \(\mathsf P\) holds.  Nonnegativity and nonemptiness make the cases pairwise disjoint.  Uniform quantifier elimination and exact evaluation of the three closed outputs therefore give the claimed payload-free statuses.  Under \(\mathsf Z\), \(e=0\) on \(S\).  Under \(\mathsf N\), the conditional is undefined at one compatible law and defined at another, so no functional on the whole fiber may divide.  Under \(\mathsf P\), continuity of \(e\) and compactness of the real set \(S\) yield
\[
\min_{x\in S}e(x)>0.
\tag{10}
\]
Only in this case is the equation \(e(x)t-n(x)=0\) equivalent to \(t=n(x)/e(x)\).  Therefore the real realization of the eliminated formula
\[
\varphi_Q(t)=\exists x\,[x\in S\wedge e(x)>0\wedge e(x)t-n(x)=0]
\tag{11}
\]
is exactly the conditional range.  The ratio is continuous by \((10)\), so the compactness argument used for \((7)\) gives unique real lower- and upper-endpoint realizations, and \((6)\) gives the exact singleton decision.

Only finitely many invocations of the cited terminating algorithm occur, so the canonical phase and hence the hybrid procedure terminate.  If either real range is non-singleton, \((5)\) or its conditional analogue selects two distinct attained real values, and the exact-global range theorem supplies two original-alphabet same-menu compatible SCM families realizing them.  This is an existential consequence of range equality, not algorithmic witness extraction.  No compactness of \(\mathbb R_{\mathrm{alg}}\), algebraicity of all range points, cylindrical decomposition record, sample, isolating datum, explicit endpoint value, or canonical parameter witness has been used.