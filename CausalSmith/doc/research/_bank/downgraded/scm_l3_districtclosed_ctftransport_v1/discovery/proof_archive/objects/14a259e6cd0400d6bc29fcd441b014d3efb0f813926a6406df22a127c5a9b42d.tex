Use the complete carrier \(\Omega_D=\Omega_D^{\mathrm{all}}\) and the corrected stack containing normalization, accepted raw rows, and the separately accepted numerator and context rows.  For every district \(D\),
\[
\mathcal Q_D(\mathcal P)\subseteq\mathcal K_D(m_D).
\]
If \(\mathsf{BASep}_D(\mathcal P)\) holds, then it supplies \(\mathcal C_{\mathrm{sat},D}(\mathcal P)\ne\varnothing\) and
\[
\mathcal Q_D^{\mathrm{sat},D}(\mathcal P)=\mathcal K_D(m_D),\qquad
R_D(m_D)=\operatorname{span}\{q-q':q,q'\in\mathcal K_D(m_D)\}^{\perp}.
\]
For its single baseline \(\mathbf M^0\), every transport-admissible complete supplied cell is preserved when \(q_0\) is replaced by any \(q\in\mathcal K_D(m_D)\), because
\[
(\bar\chi_C^{s,\mathbf M^0})^{\mathsf T}(q-q_0)=0.
\]
Every boundary point is therefore realized on \(\mathcal X\) by a compatible SCM saturated only at \(D\), with every nonfocal graph restriction unchanged.  Nonempty \(\mathcal C_{\mathrm{sat},D}(\mathcal P)\) together with \(\mathsf{ASep}_D(\mathcal P)\), and hence together with \(\mathsf{Sep}_D(\mathcal P)\), is sufficient for \(\mathsf{BASep}_D(\mathcal P)\).  Without the stated focal saturation and baseline-averaged separation, only the broad inclusion is asserted.