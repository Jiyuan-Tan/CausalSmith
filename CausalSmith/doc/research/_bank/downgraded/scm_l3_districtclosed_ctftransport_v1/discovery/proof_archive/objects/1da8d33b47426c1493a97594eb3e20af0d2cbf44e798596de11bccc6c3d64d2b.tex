The hypotheses meet the locality and positivity premises of transported dominance duality.  Dual feasibility and positive value therefore give \(Q_\sigma^\star>Q_\rho^\star\) in every compatible model where the comparison is defined.  Since \(\sigma\) remains available, \(\rho\) is never maximizing, so deleting it cannot remove a maximizing arm.  Recursion soundness classifies uniform zero evidence as undefined before division, rather than assigning the corresponding arm a numerical rank.