Fix a compatible family.  Normalization preserves probability because its exhaustive Boolean trace is pointwise valid.  Ancestor restriction preserves it by repeated finite total probability.  For split, district factorization over the original districts and recursive determinism yield
\[
P_{M^\star}(e)=\sum_{\omega}\mathbf1_e(\omega)\prod_{D\in\mathcal D(G)}q_D^\star(\omega_D),
\tag{1}
\]
exactly the successor DAG.  No induced-subgraph district factorization is invoked.

At extraction, the district-cell lemma proves every accepted raw, numerator, and context row; admissibility changes \(q_D^s\) to \(q_D^\star\).  The rule stacks the numerator and context equations separately, so its literal checker establishes \(A_Dq_D^\star=m_D\).  Rejected candidates contribute nothing.  At face identification, the checked row decomposition and forced zeros give
\[
b_D^{\mathsf T}q_D^\star=\lambda^{\mathsf T}m_D.
\tag{2}
\]
Ready sums preserve denotation by finite additivity, and ready products preserve it by the same original-district factorization as (1).  Condition retains the two root values and divides only under its independent positivity proof.

Operationally each rule fixes all earlier rank coordinates and decreases its own; hence the lexicographic rank falls.  Priority and canonical least-node order select one successor.  Every rejected partial check takes a typed failure branch, so a least unresolved phase can never be stuck.