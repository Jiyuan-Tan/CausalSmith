By definition, \(\mathfrak I_{\mathrm{CTFTR}}\) retains only observational and atomic hard-interventional constructors that are also constructors of the DCTFTR typed-menu grammar. Sending the graph, each domain label, descriptor, supplied law, and query to the identically labelled DCTFTR object is therefore injective and type preserving. This map does not include the conditional or stochastic-policy constructors excluded from the restricted carrier.

Likewise, \(\mathfrak I_{\mathrm{CTFIDU+}}\) is the one-domain intersection for which a constructor map into the DCTFTR partial-copy syntax is declared. Set \(\mathcal S=\{\star\}\), send each action through that declared map, and retain the graph, supplied cells, and target. This is injective and type preserving. No action lacking the constructor map is used.

On either image, recursion soundness proves that every successful DCTFTR expression equals the causal query in every compatible model. The already established CTFTR and CTFIDU+ published-soundness lemmas give the same semantic equality for a successful published expression on its respective input. Thus the successful expressions agree with the same query; no equality of syntax or traces is needed.

Both embeddings are proper. In \(\mathfrak I_{\mathrm{bin}}\),
\[
p=P^s(Y_0=1,Y_1=1)=\frac38
\]
is a joint cross-world cell, not an observational or atomic hard-interventional descriptor, so the first restricted carrier does not contain the instance. For the second carrier, a one-SCM instance has one factual law for \(Z\), whereas the binary family requires simultaneously
\[
P^\star(Z=1)=0,
\qquad P^s(Z=1)=1.
\]
No single probability law satisfies both equations. The two-domain DCTFTR family permits the difference because \(Z\in\Delta_s\). Hence the binary instance lies in \(\mathfrak I_{\mathrm{DCTFTR}}\) but outside each source image, proving properness with exactly the restrictions claimed.