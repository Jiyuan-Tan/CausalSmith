By the recursion definition, the two initial roots denote
\[
\bigl(P_{M^\star}(B_\rho\cap E),P_{M^\star}(E)\bigr)
\]
for every \(\mathbf M\in\mathcal C(\mathcal P)\).  Induction over the transition trace, using the transition-preservation lemma at each nonterminal step, shows that every reached configuration has this same ordered pair of root denotations.

The lexicographic order on \(\mathbb N^8\) is well founded.  Indeed, nested induction on the first coordinate, then the second, and so on, excludes an infinite sequence that fixes every earlier coordinate and strictly decreases the first coordinate at which it changes.  Every nonterminal transition has exactly this property by the transition-preservation lemma.  Hence the run is finite.  Fixed priority and the least-node convention give at most one successor, while the phase analysis in that lemma gives either that successor or a typed terminal.  The run is therefore unique, and \(\operatorname{STUCK}\) is unreachable.

Suppose the terminal is
\[
\operatorname{ID}(\operatorname{Quot}(F_B,F_E,\pi),\delta_B,\delta_E,\pi).
\]
The expression grammar starts from typed-menu cells and rational constants and is closed under the finite sums, products, and certified quotients used by the transition rules.  Induction over \(\delta_B\) and \(\delta_E\) gives \(F_B,F_E\in\mathscr R(\mathcal P)\).  Root preservation gives, for every compatible \(\mathbf M\),
\[
F_B(\mathcal P)=P_{M^\star}(B_\rho\cap E),
\qquad
F_E(\mathcal P)=P_{M^\star}(E).
\]
The independent certificate \(\pi\) proves \(F_E>0\) on the entire fiber.  Division is therefore well typed, and
\[
\frac{F_B(\mathcal P)}{F_E(\mathcal P)}
=\frac{P_{M^\star}(B_\rho\cap E)}{P_{M^\star}(E)}
=P_{M^\star}(B_\rho\mid E)
=Q_\rho^\star(\mathbf M).
\]

If instead the independent zero calculus proves \(z:F_E=0\) throughout the fiber, the evidence event has probability zero in every compatible model, so the conditional is undefined and the declared terminal is \(\operatorname{ZERO\_EVIDENCE}(F_E,z)\).  Every other listed failure is returned where a required typed judgment or certificate is absent and performs no division.  In particular, the observed numerical value of a denominator is not used as a uniform positivity proof.  A face-failure vector varies only \(\mathcal K_D(m_D)\).  It becomes a global paired-model certificate only after the separate district-saturated, district-separated hypotheses of the corrected fiber-projection theorem establish that the relevant global projection equals this row face.