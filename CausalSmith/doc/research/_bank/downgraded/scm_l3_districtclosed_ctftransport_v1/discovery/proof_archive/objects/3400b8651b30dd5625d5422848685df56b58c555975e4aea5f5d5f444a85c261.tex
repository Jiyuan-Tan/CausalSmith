Fix an accepted trace.  Recursive compatibility makes every event in the trace a zero--one function of the complete response-profile tuple, and district factorization gives the product law across original districts.  Finite total probability therefore expresses every event probability as a finite polynomial in the district response-law coordinates.  Each marginalization node is finite additivity, each complement node is subtraction from one, and each product node is ordinary multiplication.  At a conditional node the independently checked context certificate gives \(H>0\), so multiplication by \(H\) and later cancellation are legal.

After multiplying by the finite product of all such positive denominators, the trace checker compares two ordinary polynomials.  Exact rational ring normalization verifies equality coefficient by coefficient.  The cleared-denominator expressions are therefore equal for every compatible response-law tuple, and division by the positive denominator product recovers equality of the original expressions.  Thus the fixing trace preserves probability in every compatible SCM.