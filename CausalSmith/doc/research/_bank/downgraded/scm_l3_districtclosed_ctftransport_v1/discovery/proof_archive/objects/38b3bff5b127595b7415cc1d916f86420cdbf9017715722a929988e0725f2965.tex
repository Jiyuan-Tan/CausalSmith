Using the complete carrier \(\Omega_D=\Omega_D^{\mathrm{all}}\), there are two exact finite frontiers.  If \(\mathcal C_{\mathrm{sat}}(\mathcal P)\ne\varnothing\), then \(\Theta(\mathcal P)\) is nonempty compact semialgebraic and, for every finite target-cell collection \(\mathcal F\),
\[
\{(P_{M^\star}(C):C\in\mathcal F):\mathbf M\in\mathcal C_{\mathrm{sat}}(\mathcal P)\}
=\{(\Psi_C^\star(\theta^\star):C\in\mathcal F):\theta\in\Theta(\mathcal P)\}.
\]
Each \(\Psi_C^s\) is multi-affine with degree at most the number of original districts visited by \(C\), and every right-hand point has an original-alphabet district-saturated realization.  For an arbitrary ADMG, if \(\mathcal C(\mathcal P)\ne\varnothing\), then \(\Theta_G^{\mathrm{can}}(\mathcal P)\) is nonempty compact semialgebraic and
\[
\{(P_{M^\star}(C):C\in\mathcal F):\mathbf M\in\mathcal C(\mathcal P)\}
=\{(\Phi_{C,\mathrm{can}}^\star(\vartheta^\star):C\in\mathcal F):\vartheta\in\Theta_G^{\mathrm{can}}(\mathcal P)\}.
\]
This realization preserves the original alphabets and every sparse graph-compatible restriction.

For either exact row \((\mathfrak C,\Lambda,\Xi)\), continuous target-cell polynomials attain sharp lower and upper bounds on nonempty \(\Lambda\).  A bound is nontrivial exactly when its lower endpoint is positive or its upper endpoint is below one; equality of endpoints is identification, and unequal endpoints give two same-menu original-alphabet models.  Define theorem-locally
\[
e_-(\Lambda)=\min_{x\in\Lambda}\Xi_E^\star(x^\star),\qquad
e_+(\Lambda)=\max_{x\in\Lambda}\Xi_E^\star(x^\star).
\]
If \(e_+=0\), evidence is uniformly zero; if \(e_-=0<e_+\), the conditional is not uniformly defined; both cases reject division.  If \(e_->0\), the minimum and maximum of \(\Xi_{B_\rho\cap E}^\star/\Xi_E^\star\) are attained sharp conditional bounds.  With the same positive denominator, uniform strict dominance is equivalent to a strictly positive attained minimum numerator gap, and opposite rankings occur exactly when its minimum is negative and maximum positive.

Finally \(\mathcal Q_D(\mathcal P)\subseteq\mathcal K_D(m_D)\).  Under \(\mathsf{BASep}_D\), the focal projection equals the row face and
\[
R_D(m_D)=\operatorname{span}\{q-q':q,q'\in\mathcal K_D(m_D)\}^{\perp}.
\]
Nonempty focal saturation plus \(\mathsf{ASep}_D\), hence plus \(\mathsf{Sep}_D\), is sufficient.  Outside this local scope, a face failure is only an outer-relaxation witness; unequal extrema of the appropriate exact global program are the broad-model paired certificate.