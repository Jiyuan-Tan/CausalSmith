Map every observational or atomic hard-interventional CTFTR descriptor to the identically labelled DCTFTR source descriptor, preserve the selection diagram, alphabets, target, and supplied law, and omit constructors that are not in the stated restriction.  This is injective and type preserving by the definition of \(\mathfrak I_{\mathrm{CTFTR}}\).  It is not surjective: the binary instance contains the supplied cross-world cell \(P^s(Y_0=1,Y_1=1)\), which no observational or atomic hard-interventional descriptor denotes.

For a DCTFTR-expressible CTFIDU+ instance, set \(\mathcal S=\{\star\}\) and translate each action through its declared constructor map.  This is again injective and type preserving by the definition of the intersection \(\mathfrak I_{\mathrm{CTFIDU+}}\).  It is proper because the binary instance has two domains with
\[
P^\star(Z=1)=0,
\qquad P^s(Z=1)=1.
\]
One SCM assigns a single law to \(Z\), so it cannot realize both displayed laws; the source-labelled instance is therefore outside the one-SCM grammar.

On the first embedded restriction, the published CTFTR soundness lemma makes every successful published functional equal the causal query, while recursion soundness gives the same conclusion for a successful DCTFTR functional.  The CTFIDU+ published soundness lemma and recursion soundness give the analogous semantic agreement on the second restriction.  Equality to the same causal quantity proves semantic agreement but does not identify syntax or transition traces.  The exclusions in the proposition follow directly from the two restricted grammar definitions.