Fix \(D\).  Enumerate every domain, descriptor, complete output cell, and extraction record canonically.  A candidate is transport admissible only after a checked proof of \(s=\star\) or \(D\cap\Delta_s=\varnothing\).  First retain every admissible complete output cell \(C_{a,y}\) whose exhaustive \(\operatorname{SupportLocal}_D\) table accepts, with row \(r^{\mathrm{raw}}_{s,a,D,y}\) and value \(P_a^s(y)\).  Next, for every successful extraction judgment, retain separately the numerator row-value pair
\[
(r_{s,a,D,h,c},N_{s,a,D,h,c})
\]
and the context row-value pair
\[
(v_{s,a,D,h},H_{s,a,D,h}).
\]
Never stack the quotient \(K_{s,a,D}(c\mid h)\) as a linear row.  Prepend \(\mathbf 1^\top\) with value one and stack precisely these accepted rows as \(A_D\) and their values as \(m_D\).  A failed locality, fixing, expression, positivity, or admissibility check contributes no row.  A rejected complete cell remains, without information loss, among the complete polynomial equalities of the exact global fiber.  A deterministic-postprocessing cell is treated by its explicit finite preimage, so it creates no causal information beyond its declared returned-tuple regime.