Run \(\mathsf{DCTFTR}\) first.  Recursion soundness proves termination and shows that a successful fast-path expression \(F\in\mathscr R(\mathcal P)\) equals the causal query throughout \(\mathcal C(\mathcal P)\).  Relabelling this terminal \(\operatorname{ID\_RAT}\) preserves its derivation.  Every other local terminal invokes the canonical phase.

Set
\[
S=\Theta_G^{\mathrm{can}}(\mathcal P).
\]
The exact global theorem and \(\mathcal C(\mathcal P)\ne\varnothing\) make \(S\) nonempty, compact, semialgebraic, and exactly range equivalent to the broad SCM fiber on the original alphabets.  Let \(\mathbb D\) be the ordered coefficient ring generated by the finitely many supplied real-algebraic values.  Their stipulated effective presentations give exact arithmetic and sign comparison in \(\mathbb D\).  Embed \(\mathbb D\) into \(\mathbb R_{\mathrm{alg}}\), the real closed field of all real algebraic numbers.  Every polynomial defining \(S\), and every target, evidence, or numerator polynomial, is then a polynomial over the ambient real closed field \(\mathbb R_{\mathrm{alg}}\) whose coefficients lie in \(\mathbb D\).  The cited Basu--Mohammad-Nezhad theorem therefore applies with \(R=\mathbb R_{\mathrm{alg}}\); its algorithm operates in \(\mathbb D\) and terminates with an equivalent quantifier-free sign formula.

For an unconditional target put \(p_T=\Phi_{T,\mathrm{can}}^\star\).  Applying that theorem to
\[
\varphi_T(t):=\exists x\,[x\in S\wedge t-p_T(x)=0]
\tag{1}
\]
gives \(\chi_T(t)\), whose realization is exactly the canonical target range.  Applying it to
\[
\exists t_1,t_2\,[\chi_T(t_1)\wedge\chi_T(t_2)
\wedge t_1\ne t_2]
\tag{2}
\]
decides whether the range has two distinct values.  Applying it to
\[
\chi_T(\ell)\wedge
\forall t\,[\chi_T(t)\Longrightarrow\ell\leq t]
\tag{3}
\]
and the analogous maximum formula gives \(\chi_{\min}\) and \(\chi_{\max}\).  Compactness and nonemptiness of \(S\), together with continuity of \(p_T\), show that each endpoint formula has exactly one satisfying value in \(\mathbb R_{\mathrm{alg}}\): existence follows from attainment, and uniqueness follows from antisymmetry of \(\leq\).  Thus \(\operatorname{UNCOND\_QE}\) describes the exact range and endpoints and decides identification without outputting an endpoint value.

For a conditional target put \(e=\Phi_{E,\mathrm{can}}^\star\) and \(n=\Phi_{B_\rho\cap E,\mathrm{can}}^\star\).  Since \(S\ne\varnothing\) and \(e\geq0\) on \(S\), exactly one of
\[
\begin{array}{ll}
\mathsf Z:&\neg\exists x\,[x\in S\wedge e(x)>0],\\
\mathsf N:&\exists x\,[x\in S\wedge e(x)=0]
\ \wedge\ \exists x\,[x\in S\wedge e(x)>0],\\
\mathsf P:&\neg\exists x\,[x\in S\wedge e(x)=0]
\end{array}
\tag{4}
\]
holds.  Indeed, if a zero exists then either a positive point exists, giving \(\mathsf N\), or none exists, giving \(\mathsf Z\); if no zero exists, \(\mathsf P\) holds.  Nonemptiness and nonnegativity make these cases disjoint.  Quantifier elimination produces three closed quantifier-free outputs, and their constant sign tests are evaluated exactly in \(\mathbb D\).  Thus \(\mathsf Z\) gives uniform zero evidence and the payload-free zero-evidence status; \(\mathsf N\) gives exactly the payload-free nonuniform-definition status.  Neither case forms a ratio.  In case \(\mathsf P\), continuity and compactness strengthen pointwise positivity to \(\min_{x\in S}e(x)>0\).  Only then is cross multiplication legal, and
\[
\varphi_Q(t):=
\exists x\,[x\in S\wedge e(x)>0\wedge e(x)t-n(x)=0]
\tag{5}
\]
is exactly the conditional range.  Quantifier elimination yields \(\chi_Q\); repeating \((2)\)--\((3)\) gives its singleton decision and endpoint formulas.

Only finitely many calls to the terminating quantifier-elimination algorithm are made, so the hybrid procedure terminates.  Exact global range equivalence makes singleton status equivalent to identification and makes a non-singleton range contain two values attained by original-alphabet same-menu SCM families.  This is an existential consequence of the range theorem, not algorithmic witness extraction.  No CAD cell, sample, isolating datum, explicit endpoint value, canonical parameter witness, or proof-record verifier is used.