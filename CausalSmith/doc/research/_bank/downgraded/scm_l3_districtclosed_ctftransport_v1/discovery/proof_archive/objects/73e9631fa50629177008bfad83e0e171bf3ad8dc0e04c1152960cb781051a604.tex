The rational fast path terminates by recursion soundness.  A successful identification trace remains valid after relabelling, and a proved uniform-zero terminal is sound.  Every other local terminal invokes the canonical phase.

By the exact global theorem, \(S=\Theta_G^{\mathrm{can}}(\mathcal P)\) is a nonempty compact semialgebraic set with exactly the broad SCM target range.  Its coefficients lie in an effectively presented real-algebraic field.  For polynomials \(p,q\), with \(q>0\) on \(S\), form
\[
\varphi_{p/q}(t)\Longleftrightarrow
\exists x\,[x\in S\wedge q(x)>0\wedge q(x)t-p(x)=0].
\tag{1}
\]
The cited finite CAD/QE algorithm terminates, constructs exact algebraic sample points for sign-invariant cells, and returns a quantifier-free formula equivalent to (1).  Ordering the finitely many one-dimensional cells gives the compact image endpoints; lifting their closed endpoint cells through (1) gives feasible attaining samples.  The hybrid verifier recomputes projection, root isolation, sign tables, truth propagation, and substitutions, so every accepted trace certifies the same semantic endpoint facts although representations may differ.

For an unconditional target take \(p=\Phi_{T,\mathrm{can}}^\star\), \(q=1\).  Equal endpoints are exactly identification by range equality; unequal endpoints lift to two original-alphabet same-menu models.  For a conditional target first use \(p=\Phi_{E,\mathrm{can}}^\star\), \(q=1\).  The three mutually exclusive cases are: maximum zero, giving uniform zero evidence; minimum zero and positive maximum, giving zero- and positive-evidence witnesses; or positive minimum.  Only in the third case use (1) with \(p=\Phi_{B_\rho\cap E,\mathrm{can}}^\star\) and \(q=\Phi_{E,\mathrm{can}}^\star\).  Equal ratio endpoints give identification and unequal endpoints give the paired models.  These cases are exhaustive.  The finite fast path followed by a terminating accepted CAD trace proves termination and soundness of every legal run, without asserting a unique CAD representation.
