Translate each observational or atomic hard-interventional descriptor identically, retaining graph, domains, alphabets, target, and supplied law; the proposed restriction explicitly requires the finite checks to accept.  This is injective and type preserving.  It is not surjective because the binary source cell \(P^s(Y_0=1,Y_1=1)\) is genuinely cross-world.  For the declared CTFIDU+ intersection, set \(\mathcal S=\{\star\}\) and apply its constructor map.  The source-labelled binary instance is outside this image because one SCM cannot have both displayed laws of \(Z\).

Published CTFTR soundness and recursion soundness equate successful functionals with the same causal query on the first intersection; published CTFIDU+ soundness and recursion soundness do so on the second.  Equality to the same query proves semantic, not syntactic, agreement.  The omitted constructors are absent by the restricted grammar definitions.