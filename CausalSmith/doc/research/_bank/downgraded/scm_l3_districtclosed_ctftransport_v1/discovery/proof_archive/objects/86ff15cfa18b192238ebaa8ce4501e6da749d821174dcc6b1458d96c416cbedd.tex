Assume first \(\mathcal C_{\mathrm{sat}}(\mathcal P)\ne\varnothing\), and take \(\mathbf M\) in that fiber.  In domain \(s\), let \(q_D^s\) be the law of the complete block \(U_D^s\in\Omega_D^{\mathrm{all}}\).  Independence across the original districts and finite total probability give, for every counterfactual cell \(C\),
\[
P_{M^s}(C)=
\sum_{\omega\in\prod_D\Omega_D^{\mathrm{all}}}
\mathbf 1\{C(\omega)=1\}\prod_Dq_D^s(\omega_D)
=\Psi_C^s(\theta^s).
\tag{1}
\]
The carrier contains every parent configuration permitted by \(G\) and \(\mathcal X\).  District-kernel invariance gives \(q_D^s=q_D^\star\) in every unchanged source, and typed-menu realizability turns (1) into all complete supplied-cell equations.  Hence \(\theta\in\Theta(\mathcal P)\), proving the forward saturated range inclusion jointly over \(\mathcal F\).

Conversely, given \(\theta\in\Theta(\mathcal P)\), take in every domain independent district blocks with the specified laws on \(\Omega_D^{\mathrm{all}}\), and let each structural equation read its stored response-map component.  Acyclicity gives unique recursive values in the original alphabets.  The saturated class permits every such product law, and its unchanged-district equalities are exactly those in \(\Theta(\mathcal P)\).  Equation (1) and the defining menu equations show that the resulting family belongs to \(\mathcal C_{\mathrm{sat}}(\mathcal P)\) and realizes all displayed target values.  This proves the reverse saturated inclusion.

For the arbitrary-ADMG row, take \(\mathbf M\in\mathcal C(\mathcal P)\).  Apply finite canonical counterfactual equivalence in each domain to the finite joint collection consisting of all complete response-map coordinates, all supplied cells, and all cells in \(\mathcal F\).  The resulting canonical SCMs preserve the diagram, original alphabets, every supplied and target cell, and every complete district response-law.  Thus they preserve the required cross-domain response-law equalities and determine a point of \(\Theta_G^{\mathrm{can}}(\mathcal P)\), proving the forward canonical inclusion.

Conversely, a point \(\vartheta\in\Theta_G^{\mathrm{can}}(\mathcal P)\) supplies one-hot structural tables, simplex-valued exogenous laws, all graph-compatible latent-factor restrictions, and product restrictions across original districts.  These data define recursive compatible canonical SCMs on \(\mathcal X\).  The response-law equations give district-kernel invariance and the complete supplied-cell equations give \(\mathcal P\).  Therefore the realization lies in \(\mathcal C(\mathcal P)\) and attains the displayed \(\Phi_{\mathrm{can}}\)-values, proving the reverse inclusion.

Both fibers are closed bounded semialgebraic subsets of finite products of simplices and finite one-hot table sets, hence compact.  Their assumed SCM fibers and the forward maps prove nonemptiness.  Each monomial in (1) contains at most one coordinate from each visited original district, proving multi-affinity and the degree bound.

Fix either exact row \((\mathfrak C,\Lambda,\Xi)\).  A continuous cell polynomial attains its minimum and maximum on nonempty compact \(\Lambda\).  Exact range equality makes them attained sharp SCM bounds.  They improve on \([0,1]\) exactly when the lower endpoint is positive or the upper is below one, and equality of the extrema is equivalent to constancy and identification.  Unequal endpoint parameters realize two same-menu original-alphabet models with unequal values; no connectedness is used.

Apply the same argument to the nonnegative evidence polynomial.  If its maximum is zero, it vanishes throughout the fiber.  If its minimum is zero and maximum positive, endpoint models respectively have zero and positive evidence, so the conditional is not uniformly defined.  Neither case permits division over all of \(\mathfrak C\).  If the minimum is positive, the continuous ratio
\[
x\longmapsto
\frac{\Xi_{B_\rho\cap E}^\star(x^\star)}{\Xi_E^\star(x^\star)}
\tag{2}
\]
attains sharp lower and upper bounds, whose endpoint parameters again realize compatible original-alphabet SCMs.

For two arms with the same positive denominator,
\[
Q_\sigma^\star-Q_\rho^\star
=\frac{\Xi_{B_\sigma\cap E}^\star-
\Xi_{B_\rho\cap E}^\star}{\Xi_E^\star}.
\tag{3}
\]
Thus uniform strict dominance is equivalent to positivity of the attained minimum numerator gap; negative minimum and positive maximum are equivalent to two attained opposite strict rankings.  This proves the exact safe-pruning statement while keeping the conditional target distinct from its numerator-gap optimization.

Finally, the one-district projection theorem gives \(\mathcal Q_D(\mathcal P)\subseteq\mathcal K_D(m_D)\), and under \(\mathsf{BASep}_D(\mathcal P)\) gives
\[
\mathcal Q_D^{\mathrm{sat},D}(\mathcal P)=\mathcal K_D(m_D),
\qquad
R_D(m_D)=\operatorname{span}\{q-q':q,q'\in\mathcal K_D(m_D)\}^\perp.
\tag{4}
\]
For its single baseline, changing \(q_0\) to \(q\) changes a transport-admissible supplied cell by
\[
(\bar\chi_{C_{a,v}}^{s,\mathbf M^0})^{\mathsf T}(q-q_0)=0
\]
using (4).  The separation hierarchy gives the two baseline-free sufficient checks.  This focal construction preserves every nonfocal graph restriction, whereas the first global construction varies all saturated district blocks independently.  Outside exact focal projection, a local face remains only an outer relaxation; unequal extrema of the appropriate exact global program are the valid broad-model pair certificate.