Suppose \(\mathsf{Local}_D(C)\Downarrow(b_D,\ell_C)\).  For the broad compatibility fiber, membership
\[
b_D\in R_D(m_D)=\operatorname{row}(A_D)+
\operatorname{span}\{e_\omega:\omega\in Z_D(m_D)\}
\]
is sufficient to identify \(P_{M^\star}(C)\), with value \(\lambda^{\mathsf T}m_D\) whenever
\[
b_D=\lambda^{\mathsf T}A_D+
\sum_{\omega\in Z_D(m_D)}\zeta_\omega e_\omega.
\]
If additionally \(\mathsf{BASep}_D(\mathcal P)\) holds, membership is necessary for constancy on \(\mathcal C_{\mathrm{sat},D}(\mathcal P)\); failure yields two same-menu original-alphabet focal-saturated SCM families with unequal target-cell probabilities and unchanged nonfocal restrictions.  Nonempty \(\mathcal C_{\mathrm{sat},D}(\mathcal P)\) plus \(\mathsf{ASep}_D(\mathcal P)\), and hence plus \(\mathsf{Sep}_D(\mathcal P)\), is sufficient for this exact clause.  If locality or exact focal projection is absent, no global paired-model conclusion is asserted.