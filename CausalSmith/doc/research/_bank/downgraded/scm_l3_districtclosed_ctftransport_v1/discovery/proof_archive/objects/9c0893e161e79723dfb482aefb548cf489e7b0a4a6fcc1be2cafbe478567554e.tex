For \(P_a^s\in\Delta(\mathcal X_{W_a})\), write \[\operatorname{Extract}_{s,a,D}(P_a^s)\Downarrow(K_{s,a,D},\kappa)\] exactly when consistency, unnesting, ancestor restriction, fixing, and marginalization define a kernel \(K_{s,a,D}:H_{s,a,D}\to\Delta(C_{s,a,D})\) and \(\kappa\in\mathscr R(\mathcal P)\) certifies every context, marginal, and nonzero divisor used. An undefined fixing, unavailable context, or uncertified quotient has no successful judgment.