By the corrected definition, \(\mathfrak I_{\mathrm{CTFTR}}\) contains only the observational and atomic hard-interventional constructors that are also constructors of the DCTFTR typed-menu grammar.  Sending every graph, domain label, descriptor, supplied law, and query to the identically labelled DCTFTR object is therefore an injective type-preserving map.  The map does not mention the conditional or stochastic-policy constructors of the full published grammar, so it proves only the stated restriction.

Likewise, \(\mathfrak I_{\mathrm{CTFIDU+}}\) is defined as the intersection for which a declared constructor map into the DCTFTR partial-copy syntax is available.  Set \(\mathcal S=\{\star\}\), send each declared Layer-3 action through that map, and retain the graph, supplied cells, and target.  This is injective and type preserving by construction.  No undeclared Layer-2.5 action is used.

On either image, recursion soundness proves that a successful DCTFTR expression equals the causal query in every compatible model.  The already established published-soundness lemmas give the same semantic equality for successful CTFTR and CTFIDU+ expressions on their respective inputs.  This proves semantic agreement with the query, not equality of syntax or transition traces.

Both embeddings are proper.  In \(\mathfrak I_{\mathrm{bin}}\),
\[
p=P^s(Y_0=1,Y_1=1)=\frac38
\]
is a joint cross-world cell and is not an observational or atomic hard-interventional descriptor, so the first restriction does not contain the instance.  For the second restriction, a one-SCM instance has one factual law for \(Z\), whereas the binary family requires simultaneously
\[
P^\star(Z=1)=0,
\qquad
P^s(Z=1)=1.
\]
No single probability law satisfies both equations.  The two-domain family is permitted by DCTFTR because \(Z\in\Delta_s\), but it is outside the one-SCM restriction.  Thus both inclusions are proper.