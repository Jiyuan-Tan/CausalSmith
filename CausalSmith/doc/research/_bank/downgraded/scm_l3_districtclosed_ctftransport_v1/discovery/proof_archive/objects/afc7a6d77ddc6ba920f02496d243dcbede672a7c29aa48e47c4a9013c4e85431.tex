Assume \(\mathcal C(\mathcal P)\ne\varnothing\) and that every supplied menu probability is effectively rational or real algebraic.  For an unconditional finite target cell \(T\) or the conditional query \(Q_\rho^\star=P_{M^\star}(B_\rho\mid E)\), every legal run of \(\mathsf{DCTFTR\text{-}CAN}\) terminates and returns a valid certified \(\mathsf{HybridResult}\).  A fast-path \(\operatorname{ID\_RAT}(F,\delta)\) has \(F\in\mathscr R(\mathcal P)\) and equals the causal query throughout \(\mathcal C(\mathcal P)\).  Otherwise the graph-compatible canonical CAD fallback is exhaustive: for an unconditional query, equal sharp extrema yield \(\operatorname{ID\_CAN}\), while unequal extrema yield \(\operatorname{PAIR\_CAN}\) with two original-alphabet same-menu endpoint SCMs.  For a conditional query, let \(e_-^{\mathrm{can}}\) and \(e_+^{\mathrm{can}}\) be respectively the minimum and maximum of \(\Phi_{E,\mathrm{can}}^\star\) on \(\Theta_G^{\mathrm{can}}(\mathcal P)\).  Then \(e_+^{\mathrm{can}}=0\) yields \(\operatorname{ZERO\_EVIDENCE}\); \(e_-^{\mathrm{can}}=0<e_+^{\mathrm{can}}\) yields \(\operatorname{NOT\_UNIFORMLY\_DEFINED}\) with zero- and positive-evidence witnesses; and only when \(e_-^{\mathrm{can}}>0\) are the sharp conditional-ratio extrema computed, yielding \(\operatorname{ID\_CAN}\) when equal and an endpoint \(\operatorname{PAIR\_CAN}\) when unequal.  Projection polynomials, algebraic isolating data, sign tables, and lifted samples make every fallback output exactly checkable.  No uniqueness of the CAD trace or certificate representation is asserted, and a local face failure is never itself treated as broad-model nonidentification.