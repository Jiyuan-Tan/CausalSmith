The initial roots denote
\[
\bigl(P_{M^\star}(B_\rho\cap E),P_{M^\star}(E)\bigr)
\tag{1}
\]
for every \(\mathbf M\in\mathcal C(\mathcal P)\), by the recursion definition.  Induction on the checked trace, using the transition-preservation lemma at each nonterminal step, proves that (1) is the root denotation of every reached configuration.

The lexicographic order on \(\mathbb N^8\) is well founded: nested induction on its eight coordinates rules out an infinite sequence that fixes all earlier coordinates and strictly decreases the first changing coordinate.  Each nonterminal transition has exactly this property, so the run is finite.  Fixed priority and the least-node convention make the successor unique; the phase analysis in the transition lemma supplies either that successor or a typed terminal and rules out \(\operatorname{STUCK}\).

Suppose the terminal is
\[
\operatorname{ID}(\operatorname{Quot}(F_B,F_E,\pi),\delta).
\]
The expression grammar begins with typed-menu cells and rational constants and is closed under the checked finite sums, products, and certified quotients used by the trace.  Structural induction on \(\delta\) therefore gives
\[
F_B,F_E\in\mathscr R(\mathcal P).
\tag{2}
\]
Root preservation gives, for every compatible \(\mathbf M\),
\[
F_B(\mathcal P)=P_{M^\star}(B_\rho\cap E),
\qquad
F_E(\mathcal P)=P_{M^\star}(E).
\tag{3}
\]
The independent certificate \(\pi\) proves \(F_E>0\) throughout the compatibility fiber.  Division is consequently well typed, and (3) gives
\[
\frac{F_B(\mathcal P)}{F_E(\mathcal P)}
=P_{M^\star}(B_\rho\mid E)
=Q_\rho^\star(\mathbf M).
\]

If the zero calculus instead proves \(F_E=0\) throughout the fiber, (3) shows that evidence is null in every compatible model; the conditional is undefined and \(\operatorname{ZERO\_EVIDENCE}\) is correct.  Every other failure is emitted where a required typed derivation, local row, face identity, or positivity certificate is absent, and none divides.  In particular, a \(\operatorname{FACE\_NOT\_IDENTIFIED}\) vector varies only the relaxation \(\mathcal K_D(m_D)\); without the separate focal-saturation and baseline-averaged projection hypotheses, it is not promoted to a pair of compatible SCMs.