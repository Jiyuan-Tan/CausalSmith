Let the target locality judgment accept.  The district-cell lemma gives \(P_{M^\star}(C)=b_D^{\mathsf T}q_D^\star\).  If
\[
b_D=\lambda^{\mathsf T}A_D+
\sum_{\omega\in Z_D(m_D)}\zeta_\omega e_\omega,
\]
then broad projection gives \(A_Dq_D^\star=m_D\), while the definition of \(Z_D(m_D)\) gives \(q_D^\star(\omega)=0\) on every forced-zero coordinate.  Multiplication yields
\[
P_{M^\star}(C)=\lambda^{\mathsf T}m_D,
\]
an explicit functional of the supplied menu.

Under \(\mathsf{BASep}_D\), exact focal projection and the orthogonal identity show that \(b_D^{\mathsf T}q\) is constant over the focal fiber exactly when \(b_D\in R_D(m_D)\).  If membership fails, there are \(q,q'\in\mathcal K_D(m_D)\) with unequal values.  Exact projection lifts them to two original-alphabet same-menu families while fixing every nonfocal district.  The separation hierarchy gives the stated simpler sufficient checks.  Without target locality the first equation is unavailable, and without focal projection the row-face witnesses need not lift, establishing the final limitation.