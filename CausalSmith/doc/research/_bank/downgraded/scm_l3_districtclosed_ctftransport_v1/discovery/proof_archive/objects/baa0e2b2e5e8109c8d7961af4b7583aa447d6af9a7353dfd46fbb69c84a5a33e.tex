For each \(D\), prepend \(\mathbf1^{\mathsf T}\) with value \(1\).  For every supplied complete cell in domain \(s\), retain \((r^{\mathrm{raw}}_{s,a,D,v},P_a^s(v))\) only after checking both transport admissibility, \(s=\star\) or \(D\cap\Delta_s=\varnothing\), and exhaustive support locality.  For every accepted \(\mathsf{RowEligible}_{s,a,D}(h,c)\) with the same admissibility proof, retain the two separate row--value pairs
\[
(u_{s,a,D,h,c},N_{s,a,D,h,c})
\quad\text{and}\quad
(v_{s,a,D,h},H_{s,a,D,h}).
\]
Stack the rows in canonical order as \(A_D\) and their values as \(m_D\).  Never stack the quotient \(K_{s,a,D}(c\mid h)\).  A changed-district cell, a cross-district raw cell, a nonlocal numerator or context, or a failed fixing or positivity check creates no local row; its complete supplied-cell equality remains in the exact global canonical fiber.