Assume \(\mathcal C(\mathcal P)\ne\varnothing\) and that every supplied menu probability is effectively presented as a rational or real-algebraic number.  For either an unconditional finite target-domain counterfactual cell \(T\) or the conditional query \(Q_\rho^\star=P_{M^\star}(B_\rho\mid E)\), the proof-producing algorithm \(\mathsf{DCTFTR\text{-}CAN}\) has a unique finite run and returns exactly one sound terminal.  A successful fast-path terminal \(\operatorname{ID\_RAT}(F,\delta)\) satisfies \(F\in\mathscr R(\mathcal P)\) and equals the causal query throughout \(\mathcal C(\mathcal P)\).  Otherwise the exact canonical fallback is exhaustive: for an unconditional query it returns \(\operatorname{ID\_CAN}\) exactly when the sharp canonical extrema agree and otherwise returns \(\operatorname{PAIR\_CAN}\) with two original-alphabet same-menu SCM families attaining unequal endpoint probabilities.  For a conditional query it returns \(\operatorname{ZERO\_EVIDENCE}\) exactly when the maximum compatible evidence probability is zero; it returns \(\operatorname{NOT\_UNIFORMLY\_DEFINED}\) when the minimum is zero and the maximum is positive, with respective zero- and positive-evidence witnesses; and only when the minimum evidence probability is strictly positive does it optimize the conditional ratio, returning \(\operatorname{ID\_CAN}\) exactly when its sharp extrema agree and otherwise \(\operatorname{PAIR\_CAN}\) with two original-alphabet same-menu SCM families attaining unequal conditional values.  Every global certificate and witness is checkable by exact real-algebraic arithmetic.  Thus every unresolved local \(\mathsf{DCTFTR}\) trace is completed by an exact fixed-declared-alphabet identification, undefinedness, or paired-model certificate; a local face failure alone is never treated as broad-model nonidentification.