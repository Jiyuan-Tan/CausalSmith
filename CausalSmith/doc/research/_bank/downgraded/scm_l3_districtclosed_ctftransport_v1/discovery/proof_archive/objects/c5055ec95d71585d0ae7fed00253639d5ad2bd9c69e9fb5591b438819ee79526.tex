Dual feasibility and positive value invoke the dominance theorem and give \(Q_\sigma^\star>Q_\rho^\star\) in every compatible model on which the checked local-arm instance is defined.  Thus \(\rho\) never maximizes while \(\sigma\) is available, so deleting it cannot delete a maximizing defined arm.  Missing locality or positivity is outside the typed comparison, and recursion soundness reports uniform zero evidence as undefined rather than assigning a reward.