Run the rational phase first.  Recursion soundness gives a unique finite trace.  A successful identification expression belongs to \(\mathscr R(\mathcal P)\) and equals the causal query throughout \(\mathcal C(\mathcal P)\); relabelling it \(\operatorname{ID\_RAT}\) preserves its derivation.  A certified uniform-zero terminal is likewise sound.  Every other local terminal invokes the canonical phase.

Set \(S=\Theta_G^{\mathrm{can}}(\mathcal P)\).  Exact global canonical range equivalence and \(\mathcal C(\mathcal P)\ne\varnothing\) make \(S\) nonempty, compact, semialgebraic, and exactly range-equivalent to the broad SCM fiber on the original alphabets.  Its coefficients lie in the effectively presented real-algebraic field generated by the supplied values.  The exact-algebraic trace lemma supplies a terminating checked trace for every polynomial or ratio image used below; importantly, sample semantics, truth propagation, endpoint selection, and witness lifting come from that internal lemma, not from either cited source.

For an unconditional target take \(p=\Phi_{T,\mathrm{can}}^\star\).  The trace returns its exact attained minimum and maximum and attaining canonical points.  By range equivalence, equality is exactly identification and returns \(\operatorname{ID\_CAN}\); inequality yields two original-alphabet same-menu SCMs with unequal target probabilities and returns \(\operatorname{PAIR\_CAN}\).

For a conditional target first optimize \(e=\Phi_{E,\mathrm{can}}^\star\).  Nonnegativity makes the following cases exhaustive:
\[
\max_S e=0;\qquad
\min_S e=0<\max_S e;\qquad
\min_S e>0.
\]
The first returns uniform zero evidence with a checked endpoint witness.  The second returns zero- and positive-evidence witnesses and rejects a functional on the whole fiber.  Only in the third case does the algorithm optimize
\[
\frac{\Phi_{B_\rho\cap E,\mathrm{can}}^\star}
{\Phi_{E,\mathrm{can}}^\star}.
\]
The trace lemma applies because the denominator is then strictly positive on compact \(S\).  Equal endpoints return \(\operatorname{ID\_CAN}\); unequal endpoints return \(\operatorname{PAIR\_CAN}\), with checked attaining parameters that range equivalence lifts to same-menu original-alphabet SCMs.

The rational phase terminates, and the fixed QE, decomposition, exact-arithmetic, and trace-checking phase terminates whenever invoked.  Canonical orders give one selected run, without claiming that another valid CAD representation cannot exist.  The terminal cases are mutually exclusive and exhaustive, and every accepted value or witness has the semantics just proved.  Thus every unresolved local trace is completed as stated, and no local face failure is used by itself as a broad nonidentification certificate.