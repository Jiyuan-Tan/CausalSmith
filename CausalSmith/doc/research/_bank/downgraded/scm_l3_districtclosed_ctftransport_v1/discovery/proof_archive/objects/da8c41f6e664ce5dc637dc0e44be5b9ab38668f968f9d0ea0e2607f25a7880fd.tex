Fix \(\mathbf M\in\mathcal C(\mathcal P)\).  Normalization is pointwise Boolean equality; deterministic postprocessing is the disjoint preimage identity \(\mathbf1\{f_a(T_a)=y\}=\sum_{t:f_a(t)=y}\mathbf1\{T_a=t\}\).  Ancestor restriction preserves probability by finite total probability.  Original-district split preserves it because recursive determinism and district factorization give
\[
P_{M^\star}(C)=
\sum_{\omega}\mathbf1_C(\omega)
\prod_{D\in\mathcal D(G)}q_D^\star(\omega_D),
\tag{1}
\]
exactly the successor sum-product DAG; no factorization for induced-subgraph districts is used.

At extraction, the district-cell lemma proves every accepted raw, numerator, and context equation.  Admissibility alone permits replacing the source law by \(q_D^\star\).  Since numerator and context rows are stacked separately and quotients are not rows, literal checker replay proves \(A_Dq_D^\star=m_D\).  Rejected candidates contribute nothing.  At face identification, the checked decomposition and forced zeros give
\[
b_D^{\mathsf T}q_D^\star=\lambda^{\mathsf T}m_D.
\tag{2}
\]
Ready sums preserve denotation by finite additivity; ready products preserve it by the original-district factorization in (1).  Condition retains the two root values and divides only under its independent uniform-positivity proof.

Operationally each nonterminal rule fixes every earlier rank coordinate and decreases its own coordinate.  The fixed priority and least-node order select one successor.  Every failed parser, locality test, extraction, face test, or denominator test takes its typed failure branch, so a least unresolved phase cannot be stuck.  This proves denotation preservation, strict rank decrease, uniqueness, and unreachability of \(\operatorname{STUCK}\).