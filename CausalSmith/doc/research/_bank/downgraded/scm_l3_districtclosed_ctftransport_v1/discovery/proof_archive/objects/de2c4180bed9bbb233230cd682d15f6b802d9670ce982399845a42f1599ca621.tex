For every \(\mathbf M\in\mathcal C(\mathcal P)\), every nonterminal transition of a well-typed configuration produced by the corrected \(\mathsf{DCTFTR}\) recursion preserves the ordered pair of root denotations.  In particular, \(\mathsf{SPLIT}\) uses only the full response-profile expansion over the original districts, and \(\mathsf{EXTRACT}\) uses only accepted raw equations and the separately derived numerator and context equations in \(A_Dq_D^\star=m_D\); no conditional quotient is treated as a row and no nonfocal context is discarded.  The condition rule divides only under an independently checked uniform strict-positivity certificate.  Each rule decreases the corresponding first changeable coordinate of \(\operatorname{rank}_{\mathrm{lex}}\), canonical priority gives a unique successor for the rational fast path, and every failed partial operation has its declared typed failure branch, so \(\operatorname{STUCK}\) is unreachable.