Initialize a finite acyclic work DAG with roots \(\operatorname{Raw}(\mathrm{num},B_\rho\cap E)\) and \(\operatorname{Raw}(\mathrm{den},E)\).  The priority rules are \(\mathsf{NORM}\), \(\mathsf{ANCESTOR}\), \(\mathsf{SPLIT}\), \(\mathsf{EXTRACT}\), \(\mathsf{FACE\_ID}\), \(\mathsf{MARGINALIZE}\), \(\mathsf{PRODUCT}\), and \(\mathsf{CONDITION}\), using \(\operatorname{rank}_{\mathrm{lex}}\) from the configuration grammar and least-node tie breaking.  Normalization includes deterministic-postprocessing preimage expansion and checks pointwise Boolean equality.  Ancestor restriction records its finite marginalization identity.  Split uses the exact original-district expansion
\[
P_{M^\star}(C)=
\sum_{\omega\in\prod_{D\in\mathcal D(G)}\Omega_D}
\mathbf 1_C(\omega)
\prod_{D\in\mathcal D(G)}q_D^\star(\omega_D),
\]
followed only by finite sums and products; it never invokes factorization over newly created induced-subgraph districts.  Extract enumerates every admissible supplied output cell and every constructive extraction derivation, retains a raw row only after exhaustive locality accepts, retains each derived numerator and context row separately, never retains a quotient as a row, and checks every equation in \(A_Dq_D^\star=m_D\) literally.  Rejections remain global constraints and are recorded in the trace.  Face identification uses a checked decomposition
\[
b_D=\lambda^{\mathsf T}A_D+
\sum_{\omega\in Z_D(m_D)}\zeta_\omega e_\omega
\]
or returns a local-relaxation failure witness; when only normalization survives, failure of this same face test is labelled \(\operatorname{NO\_ADMISSIBLE\_FACTOR}\).  Ready sums and products use finite additivity and original-district factorization.  Condition returns zero evidence only with a uniform-zero certificate, divides only with an independent uniform-positive certificate, returns a checked zero-evidence model when one is found, and otherwise returns uncertified positivity.  In rule order, the first seven transitions decrease respectively \(r_N,r_A,r_S,r_X,r_F,r_{\Sigma},r_{\Pi}\), fix every earlier coordinate, and create no earlier-phase node; condition decreases \(r_C\) after the other coordinates vanish.  If no rule applies to a nonterminal, the algorithm returns a typed \(\operatorname{STUCK}\) trace.  Every partial check has its stated failure branch, so this last trace is unreachable from a well-typed initial configuration.