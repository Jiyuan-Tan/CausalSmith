For a saturated compatible family, independent complete district blocks and finite total probability give
\[
P_{M^s}(C)=\sum_{\omega}\mathbf1_C(\omega)\prod_Dq_D^s(\omega_D)=\Psi_C^s(\theta^s).
\tag{1}
\]
Kernel invariance and typed-menu realizability therefore put \(\theta\) in \(\Theta(\mathcal P)\).  Conversely, any \(\theta\) in that fiber defines independent latent blocks storing the complete response maps; recursive evaluation is unique by acyclicity, the menu equations give the supplied laws, and the construction stays on \(\mathcal X\).  This proves joint range equality.  Formula (1) is multi-affine and has one factor per visited original district.

For arbitrary \(G\), finite canonical counterfactual equivalence in every domain preserves all supplied cells, target cells, and complete district response laws, hence maps the broad fiber into \(\Theta_G^{\mathrm{can}}\).  Conversely, the one-hot tables, canonical exogenous laws, sparse factor restrictions, original-district products, cross-domain response-law equalities, and supplied-cell equations define a compatible canonical family.  This proves the second joint range equality.  Both parameter fibers are closed bounded semialgebraic subsets of finite products of simplices and one-hot sets, hence compact and, by the assumed compatible model, nonempty.

Fix either exact row \((\mathfrak C,\Lambda,\Xi)\).  Continuity and compactness give attained extrema; range equality makes them sharp SCM bounds and yields original-alphabet endpoint models.  Equality is constancy, while unequal extrema are a pair certificate.  Applying this to evidence gives the exhaustive cases \(e_+=0\), \(e_-=0<e_+\), and \(e_->0\).  Only in the last case is the ratio continuous on all of \(\Lambda\), so it attains sharp conditional endpoints.  For two arms,
\[
Q_\sigma^\star-Q_\rho^\star=
\frac{\Xi_{B_\sigma\cap E}^\star-\Xi_{B_\rho\cap E}^\star}{\Xi_E^\star},
\]
and the positive denominator proves the exact gap-sign characterizations.

The final local claims are precisely the broad inclusion, exact focal equality, and orthogonal identity proved by the one-district projection theorem.  Its replacement calculation preserves every cross-district cell whose single-baseline averaged row is active.  The separation lemmas give the simpler sufficient checks.  This local argument does not alter the exact global program and does not promote an outer-face witness outside its scope.