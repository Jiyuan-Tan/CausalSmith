The two initial roots denote
\[
\bigl(P_{M^\star}(B_\rho\cap E),P_{M^\star}(E)\bigr)
\tag{1}
\]
for every \(\mathbf M\in\mathcal C(\mathcal P)\).  Induction over the checked trace, using transition preservation at every nonterminal step, shows that (1) remains the root denotation of every reached configuration.

The lexicographic order on \(\mathbb N^8\) is well founded: nested induction on the coordinates rules out an infinite sequence that fixes all earlier coordinates and decreases the first changing coordinate.  Every transition has this form.  Hence the run is finite; canonical priority and the least-node convention make it unique.  The phase analysis in the transition lemma supplies either a successor or a typed terminal, so \(\operatorname{STUCK}\) is unreachable.

Suppose the terminal is \(\operatorname{ID}(\operatorname{Quot}(F_B,F_E,\pi),\delta)\).  Structural induction on \(\delta\) and the expression grammar gives \(F_B,F_E\in\mathscr R(\mathcal P)\).  Root preservation gives
\[
F_B(\mathcal P)=P_{M^\star}(B_\rho\cap E),
\qquad F_E(\mathcal P)=P_{M^\star}(E)
\tag{2}
\]
for every compatible model.  The independent certificate \(\pi\) proves \(F_E>0\) throughout the fiber.  Thus division is legal and
\[
\frac{F_B(\mathcal P)}{F_E(\mathcal P)}
=P_{M^\star}(B_\rho\mid E)=Q_\rho^\star(\mathbf M).
\]
If the zero calculus proves \(F_E=0\) throughout the fiber, (2) proves that the conditional is undefined everywhere and the zero-evidence terminal is correct.  Each remaining failure occurs before a missing locality, extraction, face, or positivity derivation would be used and performs no division.  A local face witness varies only \(\mathcal K_D(m_D)\); it becomes a pair of SCMs only under the separate exact focal-projection hypotheses.