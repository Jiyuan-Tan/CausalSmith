For every instance in the observational and hard-interventional CTFTR input class, if the published CTFTR algorithm returns an expression for a conditional counterfactual query, that expression equals the query in every causal model compatible with the input instance.