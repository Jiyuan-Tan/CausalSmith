Fix \(\mathbf M\in\mathcal C(\mathcal P)\).  We first use structural induction on the checked derivation \(\delta\).  At a source-cell leaf, typed-menu realizability gives
\[
\operatorname{Eval}_{s,a}(P_a^s(v))=P_a^s(v)=P_{M^s}(C_{a,v}).
\]
The contradiction and sure-event leaves have probabilities zero and one.  If the claim holds for \(A\), finite complementarity gives \(P_{M^s}(A^c)=1-P_{M^s}(A)\).  If it holds for the pairwise disjoint events \((A_j:j\in J)\), finite additivity gives
\[
P_{M^s}\!\left(\bigsqcup_{j\in J}A_j\right)
=\sum_{j\in J}P_{M^s}(A_j)
=\sum_{j\in J}F_j(\mathcal P).
\]
The marginalization constructor enumerates a finite partition \((A_x:x\in\mathcal X_W)\), so the same identity proves its case.  A fixing-event node is admitted only with a trace covered by \(\mathrm{lem:finite\text{-}fixing\text{-}calculus\text{-}soundness}\) and a type-correct substitution; that lemma and the induction hypotheses prove the fixing case.  There is no quotient case in an event-probability derivation.  The separate kernel quotient is evaluated only after both event derivations are proved sound and its positive context certificate is checked.  This proves expression soundness.  The positivity verifier uses only exact rational normalization, Farkas certificates on its displayed linear face, or exact real-algebraic sign certificates on its displayed canonical constraints; direct substitution into the checked identity or inequality proves each accepted certificate.

All response sets are finite by recursive finite-state compatibility and \(\Omega_D=\Omega_D^{\mathrm{all}}\).  The support-local checker therefore examines a finite list and terminates.  If it accepts, every compared pair of nonfocal profiles has equal truth value at each \(\omega_D\); hence the indicator is nonfocal-independent and equals the value stored in \(\operatorname{row}_D(A)(\omega_D)\).  Conversely, if the indicator is nonfocal-independent, every comparison made by the exhaustive checker is equal, so it accepts.  On rejection the returned triple has unequal indicators and is an explicit witness to nonlocality.  This proves all three assertions.