Assume \(\mathcal C(\mathcal P)\ne\varnothing\) and effectively presented rational or real-algebraic menu probabilities.  For an unconditional finite target cell or the conditional \(Q_\rho^\star\), every legal run of \(\mathsf{DCTFTR\text{-}CAN}\) terminates and returns a sound checked terminal.  A fast-path \(\operatorname{ID\_RAT}\) is an element of \(\mathscr R(\mathcal P)\) equal to the causal query throughout the broad fiber.  Otherwise the exact canonical fallback is exhaustive: an unconditional query returns \(\operatorname{ID\_CAN}\) exactly when its sharp extrema agree and an original-alphabet \(\operatorname{PAIR\_CAN}\) otherwise.  A conditional query returns zero evidence when the maximum evidence probability is zero, returns \(\operatorname{NOT\_UNIFORMLY\_DEFINED}\) with zero- and positive-evidence witnesses when the minimum is zero and maximum positive, and only under a strictly positive minimum evidence probability optimizes the conditional ratio, returning equal-value identification or unequal endpoint models.  Every accepted global trace is exactly checkable.  CAD representations and endpoint witnesses need not be unique.  Thus every unresolved local trace receives exact fixed-alphabet identification, undefinedness, or a paired-model certificate; a local face failure alone is never promoted to broad nonidentification.