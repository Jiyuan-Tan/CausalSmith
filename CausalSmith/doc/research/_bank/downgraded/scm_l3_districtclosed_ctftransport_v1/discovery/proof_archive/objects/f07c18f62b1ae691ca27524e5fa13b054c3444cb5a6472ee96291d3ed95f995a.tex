For a saturated compatible family, independent complete district blocks and finite total probability give, for every cell \(C\),
\[
P_{M^s}(C)=
\sum_{\omega}\mathbf1_C(\omega)
\prod_Dq_D^s(\omega_D)=\Psi_C^s(\theta^s).
\tag{1}
\]
For a deterministic-postprocessing output cell, the indicator in (1) is the disjoint sum over \(f_a^{-1}(y)\), so typed-menu realizability yields exactly its declared pushforward probability.  Kernel invariance supplies the unchanged-district equalities.  Hence \(\theta\in\Theta(\mathcal P)\).  Conversely, any point of that fiber defines independent latent blocks storing the complete response maps; acyclicity gives unique recursive evaluation, the complete output-cell equations give every supplied law, and the construction stays on \(\mathcal X\).  This proves joint range equality.  Formula (1) is multi-affine and has one probability factor for each visited original district.

For arbitrary \(G\), finite canonical counterfactual equivalence applied separately in every domain preserves all complete supplied and target cells and every induced complete district response law, so it maps the broad fiber into \(\Theta_G^{\mathrm{can}}(\mathcal P)\).  Conversely, the one-hot tables, canonical exogenous laws, within-district graph-compatible restrictions, original-district independence, cross-domain response-law equalities, and supplied output-cell equations define a compatible canonical family.  Thus the sparse canonical joint range equality also holds.  Each parameter fiber is a closed subset of a finite product of compact simplices and finite one-hot sets, hence compact; the assumed compatible family makes it nonempty.  Its constraints are polynomial sign conditions, so it is semialgebraic.

Fix either exact row \((\mathfrak C,\Lambda,\Xi)\).  Continuity on compact \(\Lambda\) gives attained extrema.  Range equality makes them sharp SCM bounds and yields original-alphabet endpoint models.  Equality of endpoints is exactly constancy and hence identification; unequal endpoints give the asserted pair.  A probability bound is nontrivial precisely when its lower endpoint is positive or its upper endpoint is below one.

Apply the same argument to evidence.  Since evidence probabilities are nonnegative, the cases \(e_+=0\), \(e_-=0<e_+\), and \(e_->0\) are exhaustive.  The first is uniform zero, the second is failure of uniform definition with both endpoint witnesses, and only the third permits division on the whole fiber.  There the conditional ratio is continuous and attains sharp endpoints.  Moreover,
\[
Q_\sigma^\star-Q_\rho^\star=
\frac{\Xi_{B_\sigma\cap E}^\star-
\Xi_{B_\rho\cap E}^\star}{\Xi_E^\star}.
\]
The positive denominator and attained compact extrema prove both the exact strict-dominance and opposite-ranking characterizations.

The final local claims are exactly the broad inclusion, focal equality, and orthogonal identity proved by the one-district projection theorem.  Its fixed-baseline calculation applies to postprocessed cells because their indicators are ordinary finite preimage indicators.  The separation lemmas give the simpler sufficient checks.  None of these local arguments replaces the complete global polynomial equations, and no local relaxation witness is promoted outside the focal projection scope.