Map each observational or atomic hard-interventional CTFTR descriptor identically, including graph, domains, alphabets, target, and complete supplied law; choose the identity deterministic output map.  This is an injective type-preserving map into the DCTFTR grammar.  It is not surjective because the binary instance supplies a genuine joint two-world cell.  The full conditional and stochastic-policy constructors were excluded from the source restriction, so no map for them is claimed.

For the declared CTFIDU+ intersection, set \(\mathcal S=\{\star\}\) and apply its stipulated constructor map, again using the identity output map unless the constructor already declares a finite postprocessor.  Properness follows from the binary source-labelled family: one SCM cannot simultaneously induce \(P^\star(Z=1)=0\) and \(P^s(Z=1)=1\).  Published CTFTR soundness and recursion soundness equate successful functionals with the same causal query on the first intersection; published CTFIDU+ soundness and recursion soundness do so on the second.  Equality to one query proves semantic agreement only.  This establishes every asserted inclusion, strictness witness, and limitation.