Assume \(\mathcal C(\mathcal P)\ne\varnothing\) and every supplied output-cell probability is effectively rational or real algebraic.  Let \(\mathbb D\) be the effective ordered coefficient ring they generate, embedded in the real closed field \(\mathbb R_{\mathrm{alg}}\), and interpret the canonical constraints, query polynomials, and \(\mathsf{QEFormula}\) over \(\mathbb R_{\mathrm{alg}}\) with coefficients in \(\mathbb D\).  For either an unconditional finite target-domain counterfactual cell \(T\) or the conditional query \(Q_\rho^\star=P_{M^\star}(B_\rho\mid E)\), \(\mathsf{DCTFTR\text{-}CAN}\) terminates and returns a \(\mathsf{HybridResult}\).  A fast-path \(\operatorname{ID\_RAT}(F,\delta)\) has \(F\in\mathscr R(\mathcal P)\) and equals the causal query throughout \(\mathcal C(\mathcal P)\).  Otherwise, for \(S=\Theta_G^{\mathrm{can}}(\mathcal P)\), the fallback returns exact quantifier-free range and endpoint formulas and an exact singleton decision.  For an unconditional target it returns \(\operatorname{UNCOND\_QE}\); identification holds exactly when its range is a singleton.  For a conditional target it evaluates the three closed sentences \(\mathsf Z\), \(\mathsf N\), and \(\mathsf P\) from \(\mathrm{def:hybrid\text{-}result\text{-}grammar}\): it returns the payload-free status \(\operatorname{ZERO\_EVIDENCE\_QE}\) exactly when \(\mathsf Z\) is true, the payload-free status \(\operatorname{NOT\_UNIFORMLY\_DEFINED\_QE}\) exactly when \(\mathsf N\) is true, and otherwise returns \(\operatorname{COND\_QE}\) after \(\mathsf P\) proves that evidence has no zero on \(S\).  Only in this last case is the conditional range formed, and identification holds exactly when it is a singleton.  Exact arithmetic and sign comparison in \(\mathbb D\) evaluate all closed quantifier-free outputs.  Every returned range or endpoint formula is equivalent over \(\mathbb R_{\mathrm{alg}}\) to its defining first-order formula.  A non-singleton range implies, by exact canonical range equivalence, the existence of two original-alphabet same-menu SCM families with unequal target values, but the algorithm is not claimed to output endpoint values, algebraic samples, canonical parameters, or paired SCMs.  A local face failure alone is never used as broad-model nonidentification.