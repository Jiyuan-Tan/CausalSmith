Suppose the checked locality judgment \(\mathsf{Local}_D(C)\Downarrow b_D\) exists.  For the broad compatibility fiber,
\[
b_D\in R_D(m_D)
=\operatorname{row}(A_D)+\operatorname{span}\{e_\omega:\omega\in Z_D(m_D)\}
\]
is sufficient to identify the causal target \(P_{M^\star}(C)=b_D^{\mathsf T}q_D^\star\), with value \(\lambda^{\mathsf T}m_D\) whenever
\[
b_D=\lambda^{\mathsf T}A_D+
\sum_{\omega\in Z_D(m_D)}\zeta_\omega e_\omega.
\]
If additionally \(\mathsf{BASep}_D(\mathcal P)\) holds, membership is necessary for constancy on \(\mathcal C_{\mathrm{sat},D}(\mathcal P)\).  Failure then yields two original-alphabet compatible focal-district-saturated SCM families with identical supplied laws and unequal target probabilities while every nonfocal district retains its graph-compatible restrictions.  Nonemptiness together with \(\mathsf{ASep}_D(\mathcal P)\), and hence with \(\mathsf{Sep}_D(\mathcal P)\), is sufficient for this exact clause.  Without target locality or baseline-averaged focal projection, a failed local face test is not a broad-model nonidentification certificate.