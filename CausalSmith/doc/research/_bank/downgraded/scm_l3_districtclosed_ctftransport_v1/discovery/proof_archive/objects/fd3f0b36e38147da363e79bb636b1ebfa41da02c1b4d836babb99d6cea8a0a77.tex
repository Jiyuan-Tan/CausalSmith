For the complete carrier and corrected stack,
\[
\{\mathcal C_{\mathrm{sat},D}(\mathcal P)\ne\varnothing\text{ and }\mathsf{ASep}_D(\mathcal P)\}
\Longrightarrow\mathsf{BASep}_D(\mathcal P).
\]
Together with structural separation this gives the stated hierarchy from \(\mathsf{Sep}_D\) to \(\mathsf{ASep}_D\) to \(\mathsf{BASep}_D\), with nonemptiness attached to the first two predicates.  The second implication is strict on original binary alphabets: a three-vertex binary DAG and complete target menu exist for which two focal slices outside \(R_D(m_D)\) average under one compatible nonfocal product law to a row inside it.