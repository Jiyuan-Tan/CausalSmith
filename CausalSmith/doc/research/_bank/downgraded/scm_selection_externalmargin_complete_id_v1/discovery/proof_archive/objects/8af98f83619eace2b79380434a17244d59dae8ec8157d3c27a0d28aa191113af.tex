Assume the three antecedent certificates named in the statement.  Fix a compatible input and suppose first that the cell run returns \(F_{x,y}\).  Theorem \(\mathrm{thm{:}extid\text{-}sound\text{-}termination}\) gives
\[
 F_{x,y}(q_S,q_0)=\psi_{x,y}(M)
 \quad\text{for every }M\in\mathfrak F_{\epsilon}(\mathcal G,T_0;q_S,q_0),
\]
so success identifies that cell on the exact fiber.

Suppose instead that the cell run returns \(\operatorname{FAIL}\).  The assumed affirmative resolution of the margin-anchored hedge question supplies, after this particular input is fixed, a number \(\eta>0\) and a represented baseline
\[
 (M,\mathcal R,U_0)
 \in\mathfrak R_{\epsilon,\eta}^{\circ}(\mathcal G,T_0;q_S,q_0)
 \quad\text{with}\quad
 h_{x,y}\notin\Gamma_{(M,\mathcal R,U_0)}.
\]
Theorem \(\mathrm{thm{:}projective\text{-}nullspace\text{-}certificate}\) then supplies a nonzero admissible perturbation and two induced models satisfying
\[
 (M_i,\mathcal R_i,U_0)
 \in\mathfrak R_{\epsilon,\eta/2}^{\circ}(\mathcal G,T_0;q_S,q_0),
 \qquad i=1,2,
\]
\[
 \operatorname{Obs}_{T_0}(M_1)
 =\operatorname{Obs}_{T_0}(M_2)=(q_S,q_0),
 \qquad
 \psi_{x,y}(M_1)\neq\psi_{x,y}(M_2).
\]
Thus no map of the two input tables can equal the failed query cell throughout the exact fiber.  The first two antecedent audit certificates are part of the stipulated premise ordering and ensure that the universal hedge premise was pursued only after the required finite falsification gates; the implication above then uses the universal hedge certificate itself.

There are finitely many cells in \(\mathcal I\).  If every cell run succeeds, applying the successful-cell equality coordinatewise gives \(\mathbf F(q_S,q_0)=\boldsymbol\psi(M)\) throughout the fiber.  If at least one cell fails, the preceding pair agrees on both input laws but differs in that coordinate, and hence differs in the full table \(\boldsymbol\psi\).  Therefore the finite collection decides recovery of the full table under the three stated antecedents.