Fix \(\epsilon>0\), \((q_S,q_0)\in\mathcal C_{\epsilon}(\mathcal G,T_0)\), \(M\in\mathfrak F_{\epsilon}(\mathcal G,T_0;q_S,q_0)\), and a cell \((x,y)\). The exact transition system has four audited reductions. If \(T_0=V\), initialization stores every scalar cell \(q_0(v)=P_M(V=v)\) at \((V,\varnothing,\varnothing,\mathrm{pop},v,\ast,\ast)\), and population graphical fixing plus probability moves reproduce the ordinary do-calculus functional for \(\psi_{x,y}\). If \(\mathcal G\) entails \(S\perp\!\!\!\perp V\), population storage inserts those full-population scalar keys with annotations \(q_S(v)\). If \(T_0=\varnothing\), the external-margin law is trivial, its scalar unit creates no key, and no margin-extension instance is admissible. The resulting boundary is the selected-source scalar closure augmented by exactly the population kernels inserted from a selected key \((A,B,C,\mathrm{sel},a,b,c)\) when \(A\perp\!\!\!\perp S\mid B,C\) in \(G_{\overline B}\), together with their consequences under the unchanged population graphical-fixing and probability moves; at \(B=C=\varnothing\), the storage predicate is unconditional selection independence. Whenever margin extension reconstructs every cell of \(P_M(V)\), later population moves return adjustment, front-door, and ordinary-ID functionals cell by cell; collecting all \((x,y)\in\mathcal I\) recovers their standard full conditional-kernel tables on the exact fiber.