\documentclass{article}
\usepackage[margin=0.8in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{mathrsfs,bm,bbm}
\usepackage{graphicx,booktabs,array,multirow,tabularx,longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm,algpseudocode}
\usepackage{subcaption,tikz}
\usepackage{siunitx,cancel,accents}
\usepackage{microtype}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}
\setlength{\emergencystretch}{2em}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{openendedquestion}{Open-ended Question}
\newtheorem{conjecture}{Conjecture}
\newcommand{\coreref}[1]{\ref{#1}}

\begin{document}

\sloppy

\section*{eid\_\allowbreak{}blocked\_\allowbreak{}colour\_\allowbreak{}reversal\_\allowbreak{}complete}

\noindent\textit{Specialization:} v1\par

\paragraph{TL;DR.} Equal incoming coefficients encode communities of direct causes. On the published singleton-permitting blocked class, equality of full positive-definite covariance models is characterized by common skeleton and unshielded colliders, common repeated parent blocks, and fixed orientation across unequal endpoint block families; equivalently, states are linked by partition-compatible singleton covered reversals. The canonical CPDAG-plus-background-arrow representation yields exact-once state enumeration after \(O(p^3)\) initialization with \(O(p+m)\) delay, output-linear partition lifting, a sharp \(p!\) bound, generic fixed-\(p\) BIC component recovery, and simultaneous inference for the finite effect set. The revised open question charges the total explicit output length for complete class records, including all memberships, compact circuits, requested shortest reversal certificates, and the exact partition certificate. No deterministic output-polynomial or polynomial-delay compact rational-circuit identity or canonicalization algorithm has been established, so that question remains open.

\section{Project justification}

\paragraph{Gap.} BoegeKubjasMisraSolus2026 proves BPEC structural identifiability only after forbidding singleton edge-colour classes and explicitly leaves graphical and transformational edge-coloured equivalence open. Ordinary covered reversals ignore coefficient partitions, while partial homoscedasticity constrains innovation variances rather than incoming coefficients.

\paragraph{Niche.} Target-local blocked colourings with unrestricted positive noise variances form a natural intermediate class: repeated blocks reveal stable causal communities, but singleton blocks retain exactly the ordinary orientation ambiguity compatible with those communities.

\paragraph{Fill.} The project recovers repeated blocks from the identity tangent space, proves by an exact sparse regression separator that any reversed edge must join equal endpoint families, and combines these facts with monotone covered reversals to obtain the full static and transformational characterization. It then converts the class to a canonical background PDAG: every repeated-block arrow is background knowledge, every consistent extension has a unique path-independent partition lift, and the published linear-delay extension algorithm enumerates the blocked component exactly once. The same quotient supports generic fixed-dimensional BIC recovery and simultaneous possible-effect inference. The residual arithmetic-circuit question now measures time against the input plus complete explicit class records, including memberships, exact identity certificates, and requested shortest certificates.

\section{Related work}

BoegeKubjasMisraSolus2026 is the writing and primary method anchor. Its Definition 5.14 defines blocked edge colourings without excluding singleton classes; properness is separate, and BPEC is the blocked, proper no-singleton subclass. Its Theorem 5.17 proves structural identifiability within BPEC, while Question 5.19 asks for a graphical characterization of edge-coloured model equivalence. The present result agrees with that boundary on the BPEC subclass and gives a complete equivalence characterization on the published blocked superclass; it does not transfer a BPEC converse to arbitrary cross-target colourings. Chickering1995 supplies the disagreement-decreasing sequence of distinct covered reversals, while the tangent and separator arguments prove those reversals legal for the coefficient partitions. WienobstLuttermannBannachLiskiewicz2023, Theorem 5, is used after forming the CPDAG-plus-\(F(C)\) PDAG: cubic Meek-closure initialization and linear-delay consistent-extension enumeration carry over through the canonical partition-lift bijection. That result does not quotient compact rational effect circuits or account for complete functional-class member lists and shortest certificates. Haughton1988 informs the finite smooth-model BIC argument. MaathuisKalischBuhlmann2009, ChangGuoMalinsky2026, and GuoPerkovic2022EfficientLS inform possible-effect estimation and inference but do not provide simultaneous inference over this full-model-equivalence component or deterministic output-sensitive rational-functional canonicalization.

\section{Setup}

Target (estimand / structural parameter): \(\mathcal R(C)=\{D\in\mathfrak S_p:\mathcal M(D)=\mathcal M(C)\}\).
Primary functional / estimator / diagnostic: \(\mathcal R(C)=\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}\), with \(\mathsf S(E)=(\operatorname{skel}(G_E),\operatorname{uc}(G_E),(\mathcal N_j(E))_{j\in V},F(E))\).
\begin{itemize}
  \item \texttt{p} --- \texttt{positive integer}; \(p\in\mathbb N\) with \(p\geq 1\); \texttt{graph size}
  \par\smallskip\noindent\emph{Definition.} \texttt{Number of labelled system variables.}
  \item \texttt{V} --- \texttt{finite labelled vertex set}; \(V=[p]=\{1,\ldots,p\}\); \texttt{variable indices}
  \par\smallskip\noindent\emph{Definition.} \texttt{Common vertex set of every graph under comparison.}
  \item \texttt{pa} --- \texttt{parent-\allowbreak{}set map}; \(\operatorname{pa}_{K}(j)=\{i\in V:i\to j\text{ in }K\}\); \texttt{graph primitive}
  \par\smallskip\noindent\emph{Definition.} Parents of vertex \(j\) in a directed graph \(K\) on \(V\).
  \item \texttt{Kgraph} --- \texttt{directed graph}; \(\mathcal K\) on \(V\); \texttt{class member graph}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic directed graph used in the state-\allowbreak{}class definition.}
  \item \texttt{Xi} --- \texttt{family of set partitions}; \(\Xi=(\Xi_j)_{j\in V}\); \texttt{class member partition}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic family of target-\allowbreak{}specific parent partitions.}
  \item \texttt{Astate} --- \texttt{generic graph-\allowbreak{}partition pair}; \(A=(\mathcal K,\Xi)\); \texttt{class binder}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic candidate state used only as the bound member of the blocked-\allowbreak{}state class.}
  \item \texttt{StateClass} --- \texttt{finite blocked-\allowbreak{}state class}; \(\mathfrak S_p\); \texttt{published blocked edge-\allowbreak{}colouring class with singleton blocks admitted}
  \par\smallskip\noindent\emph{Definition.} All labelled acyclic graphs on \(V\) equipped with a partition of each target's parent set, including singleton blocks.
  \item \texttt{Estate} --- \texttt{generic blocked state}; \(E\in\mathfrak S_p\); \texttt{generic state}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic state used to define model and invariant maps.}
  \item \texttt{GE} --- \texttt{DAG coordinate of a state}; \(G_E\) on \(V\); \texttt{generic causal graph}
  \par\smallskip\noindent\emph{Definition.} Directed acyclic graph coordinate of \(E\).
  \item \texttt{PiE} --- \texttt{parent-\allowbreak{}partition coordinate of a state}; \(\Pi^E=(\Pi^E_j)_{j\in V}\); \texttt{generic coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(E\), with \(\Pi^E_j\) partitioning \(\operatorname{pa}_{G_E}(j)\).
  \item \texttt{PEj} --- \texttt{target parent index set}; \(P_{Ej}=\operatorname{pa}_{G_E}(j)\); \texttt{target-\allowbreak{}local regression index}
  \par\smallskip\noindent\emph{Definition.} Parent set used as the row index of target \(j\)'s constrained regression design.
  \item \texttt{Ip} --- \texttt{identity matrix}; \(I_p\in\mathbb R^{p\times p}\); \texttt{SEM normalization}
  \par\smallskip\noindent\emph{Definition.} Identity matrix indexed by \(V\).
  \item \texttt{betaE} --- \texttt{state block-\allowbreak{}coefficient vector}; \(\beta_E=(\beta_{jB})_{j\in V,\,B\in\Pi^E_j}\in\mathbb R^{b_E}\), where \(b_E=\sum_{j\in V}|\Pi^E_j|\); \texttt{structural coefficient parameter}
  \par\smallskip\noindent\emph{Definition.} \texttt{One unrestricted real structural coefficient for each target-\allowbreak{}parent block.}
  \item \texttt{BE} --- \texttt{structural coefficient matrix}; \(B_E(\beta_E)\in\mathbb R^{p\times p}\); \texttt{blocked structural-\allowbreak{}parameter assembly}
  \par\smallskip\noindent\emph{Definition.} For a vector \(\beta_E=(\beta_{jB})_{j\in V,\,B\in\Pi^E_j}\), the matrix satisfies \( (B_E(\beta_E))_{ji}=\beta_{jB}\) when \(i\in B\in\Pi^E_j\), and is zero when \(i\notin\operatorname{pa}_{G_E}(j)\).
  \item \texttt{omegaE} --- \texttt{positive innovation-\allowbreak{}variance vector}; \(\omega_E=(\omega_{Ej})_{j\in V}\in(0,\infty)^p\); \texttt{noise parameters}
  \par\smallskip\noindent\emph{Definition.} \texttt{Unrestricted target-\allowbreak{}specific innovation variances.}
  \item \texttt{ThetaE} --- \texttt{state parameter space}; \(\Theta_E=\mathbb R^{b_E}\times(0,\infty)^p\), where \(b_E=\sum_{j\in V}|\Pi^E_j|\); \texttt{structural parameter domain}
  \par\smallskip\noindent\emph{Definition.} \texttt{One unrestricted real coefficient per parent block and one unrestricted positive innovation variance per vertex.}
  \item \texttt{PhiE} --- \texttt{covariance parametrization}; \(\phi_E(\beta_E,\omega_E)=(I_p-B_E(\beta_E))^{-1}\operatorname{diag}(\omega_E)(I_p-B_E(\beta_E))^{-\mathsf T}\); \texttt{observational-\allowbreak{}law map}
  \par\smallskip\noindent\emph{Definition.} Linear Gaussian SEM covariance map for state \(E\).
  \item \texttt{Model} --- \texttt{positive-\allowbreak{}definite covariance-\allowbreak{}model map}; \(\mathcal M(E)=\phi_E(\Theta_E)\subseteq\mathbb S^p_{++}\); \texttt{population model}
  \par\smallskip\noindent\emph{Definition.} Full covariance image of state \(E\), including zero and coincident block coefficients.
  \item \texttt{C} --- \texttt{blocked state}; \(C\in\mathfrak S_p\); \texttt{reference state}
  \par\smallskip\noindent\emph{Definition.} \texttt{First state under comparison and the true state in statistical results.}
  \item \texttt{G} --- \texttt{DAG}; \(G=G_C\); \texttt{reference graph}
  \par\smallskip\noindent\emph{Definition.} Graph coordinate of \(C\).
  \item \texttt{Pi} --- \texttt{parent-\allowbreak{}partition family}; \(\Pi=\Pi^C\); \texttt{reference coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(C\).
  \item \texttt{D} --- \texttt{blocked state}; \(D\in\mathfrak S_p\); \texttt{comparison state}
  \par\smallskip\noindent\emph{Definition.} \texttt{Second state under comparison.}
  \item \texttt{H} --- \texttt{DAG}; \(H=G_D\); \texttt{comparison graph}
  \par\smallskip\noindent\emph{Definition.} Graph coordinate of \(D\).
  \item \texttt{Lambda} --- \texttt{parent-\allowbreak{}partition family}; \(\Lambda=\Pi^D\); \texttt{comparison coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(D\).
  \item \texttt{Nfam} --- \texttt{nonsingleton-\allowbreak{}family map}; \(\mathcal N_j(E)=\{B\in\Pi^E_j:|B|\geq2\}\); \texttt{coefficient-\allowbreak{}partition invariant}
  \par\smallskip\noindent\emph{Definition.} Family of repeated-coefficient parent blocks at vertex \(j\).
  \item \texttt{Fset} --- \texttt{family-\allowbreak{}mismatch oriented-\allowbreak{}edge map}; \(F(E)=\{i\to j\in G_E:\mathcal N_i(E)\neq\mathcal N_j(E)\}\); \texttt{coefficient-\allowbreak{}compelled orientations}
  \par\smallskip\noindent\emph{Definition.} \texttt{Edges whose endpoint nonsingleton families differ.}
  \item \texttt{Skel} --- \texttt{skeleton map}; \(\operatorname{skel}(G_E)\); \texttt{ordinary Markov invariant}
  \par\smallskip\noindent\emph{Definition.} \texttt{Undirected skeleton of a state graph.}
  \item \texttt{UC} --- \texttt{unshielded-\allowbreak{}collider map}; \(\operatorname{uc}(G_E)\); \texttt{ordinary Markov invariant}
  \par\smallskip\noindent\emph{Definition.} \texttt{Set of labelled unshielded collider triples of a state graph.}
  \item \texttt{Sig} --- \texttt{static blocked-\allowbreak{}equivalence signature}; \(\mathsf S(E)=(\operatorname{skel}(G_E),\operatorname{uc}(G_E),(\mathcal N_j(E))_{j\in V},F(E))\); \texttt{recovery functional}
  \par\smallskip\noindent\emph{Definition.} \texttt{Static signature combining ordinary Markov information with repeated-\allowbreak{}block and family-\allowbreak{}mismatch orientation information.}
  \item \texttt{Tspace} --- \texttt{off-\allowbreak{}diagonal tangent-\allowbreak{}space map}; \(T_E^{\circ}=\operatorname{proj}_{\mathrm{off}}T_{I_p}\mathcal M(E)\); \texttt{first-\allowbreak{}order model invariant}
  \par\smallskip\noindent\emph{Definition.} \texttt{Projection of the model tangent space at the identity covariance onto unordered off-\allowbreak{}diagonal coordinates.}
  \item \texttt{Rev} --- \texttt{partial state transformation}; \(\operatorname{rev}_{i\to j}(E)\); \texttt{local equivalence move}
  \par\smallskip\noindent\emph{Definition.} \texttt{Singleton-\allowbreak{}block covered reversal with target-\allowbreak{}partition transport.}
  \item \texttt{Rcomp} --- \texttt{legal-\allowbreak{}reversal component}; \(\mathcal R(E)\subseteq\mathfrak S_p\); \texttt{structural equivalence class}
  \par\smallskip\noindent\emph{Definition.} Connected component generated by the partial transformations \(\operatorname{rev}_{i\to j}\).
  \item \texttt{ArrowGap} --- \texttt{arrow-\allowbreak{}disagreement count}; \(\delta(C,D)=|\{\{i,j\}:i\to j\text{ in }G,\ j\to i\text{ in }H\}|\); \texttt{path-\allowbreak{}length lower bound}
  \par\smallskip\noindent\emph{Definition.} Number of common skeleton edges oriented differently by \(G\) and \(H\).
  \item \texttt{dRev} --- \texttt{legal-\allowbreak{}reversal graph distance}; \(d_{\mathrm{rev}}(C,D)\in\mathbb N_0\cup\{\infty\}\); \texttt{component metric}
  \par\smallskip\noindent\emph{Definition.} Minimum number of legal reversals from \(C\) to \(D\), with infinity when none exists.
  \item \texttt{n} --- \texttt{sample size}; \(n\in\mathbb N\); \texttt{sampling index}
  \par\smallskip\noindent\emph{Definition.} \texttt{Number of independent observations.}
  \item \texttt{Xsample} --- \texttt{Gaussian observations}; \(X^{(1)},\ldots,X^{(n)}\in\mathbb R^p\); \texttt{observed sample}
  \par\smallskip\noindent\emph{Definition.} Independent centered Gaussian observations with covariance \(\Sigma_0\).
  \item \texttt{Sigma0} --- \texttt{true covariance matrix}; \(\Sigma_0\in\mathcal M(C)\); \texttt{population law}
  \par\smallskip\noindent\emph{Definition.} \texttt{Population covariance of the observations.}
  \item \texttt{Sn} --- \texttt{sample covariance matrix}; \(S_n=n^{-1}\sum_{r=1}^nX^{(r)}X^{(r)\mathsf T}\); \texttt{sufficient statistic}
  \par\smallskip\noindent\emph{Definition.} \texttt{Centered empirical covariance matrix.}
  \item \texttt{BIC} --- \texttt{global information criterion}; \[\operatorname{BIC}_n(E)=\begin{cases}-2\sup_{\Sigma\in\mathcal M(E)}\ell_n(\Sigma)+d_E\log n,&S_n\in\mathbb S^p_{++},\\ d_E\log n,&S_n\notin\mathbb S^p_{++},\end{cases}\qquad d_E=p+\sum_{j\in V}|\Pi^E_j|.\]; \texttt{component selector}
  \par\smallskip\noindent\emph{Definition.} \texttt{Total Gaussian BIC criterion with the likelihood branch used only for positive-\allowbreak{}definite sample covariance and a deterministic measurable fallback otherwise.}
  \item \texttt{Chat} --- \texttt{BIC-\allowbreak{}selected blocked state}; \(\widehat C_n\in\operatorname*{argmin}_{E\in\mathfrak S_p}\operatorname{BIC}_n(E)\); \texttt{estimated component representative}
  \par\smallskip\noindent\emph{Definition.} \texttt{Any global BIC minimizer under a deterministic tie rule.}
  \item \texttt{Exc} --- \texttt{algebraic exceptional-\allowbreak{}set map}; \(\mathcal E_C\subsetneq\Theta_C\); \texttt{genericity boundary}
  \par\smallskip\noindent\emph{Definition.} Finite union of pullbacks of intersections with nonequivalent blocked covariance models of dimension at most \(d_C\), together with zero-gradient effect strata.
  \item \texttt{Qpairs} --- \texttt{finite ordered intervention-\allowbreak{}response list}; \(\mathcal Q\subseteq\{(a,y)\in V^2:a\neq y\}\); \texttt{causal query menu}
  \par\smallskip\noindent\emph{Definition.} \texttt{Prespecified ordered pairs for which total effects are reported.}
  \item \texttt{Sigma} --- \texttt{generic positive-\allowbreak{}definite covariance matrix}; \(\Sigma\in\mathbb S^p_{++}\); \texttt{covariance argument}
  \par\smallskip\noindent\emph{Definition.} \texttt{Argument of the off-\allowbreak{}model block-\allowbreak{}constrained least-\allowbreak{}squares maps.}
  \item \texttt{BlockInc} --- \texttt{target-\allowbreak{}local block-\allowbreak{}incidence matrix}; \(A_{Ej}\in\{0,1\}^{|P_{Ej}|\times|\Pi^E_j|}\); \texttt{block-\allowbreak{}constrained regression design}
  \par\smallskip\noindent\emph{Definition.} With rows indexed by \(i\in P_{Ej}\) and columns indexed by \(B\in\Pi^E_j\), \((A_{Ej})_{iB}=\mathbf 1\{i\in B\}\).
  \item \texttt{BlockGram} --- \texttt{target-\allowbreak{}local block-\allowbreak{}design Gram map}; \(\Gamma_{Ej}(\Sigma)=A_{Ej}^{\mathsf T}\Sigma_{P_{Ej},P_{Ej}}A_{Ej}\); \texttt{constrained least-\allowbreak{}squares normal matrix}
  \par\smallskip\noindent\emph{Definition.} Gram matrix of the block-sum regressors for target \(j\); it is positive definite whenever \(P_{Ej}\neq\varnothing\) and \(\Sigma\in\mathbb S^p_{++}\).
  \item \texttt{BlockScore} --- \texttt{target-\allowbreak{}local block-\allowbreak{}design covariance vector}; \(h_{Ej}(\Sigma)=A_{Ej}^{\mathsf T}\Sigma_{P_{Ej},j}\); \texttt{constrained least-\allowbreak{}squares right-\allowbreak{}hand side}
  \par\smallskip\noindent\emph{Definition.} Covariance vector between the block-sum regressors and target \(j\).
  \item \texttt{LE} --- \texttt{off-\allowbreak{}model block-\allowbreak{}constrained least-\allowbreak{}squares map}; \(L_E:\mathbb S^p_{++}\to\mathbb R^{p\times p}\); \texttt{state-\allowbreak{}specific off-\allowbreak{}model coefficient projection}
  \par\smallskip\noindent\emph{Definition.} For each target \(j\) with nonempty parent set, solve \(\Gamma_{Ej}(\Sigma)\beta_{Ej}(\Sigma)=h_{Ej}(\Sigma)\), repeat the block coefficient \(\beta_{Ej,B}(\Sigma)\) on every \(i\in B\), and put zero outside the parent set; the empty-parent row is zero.
  \item \texttt{gEffect} --- \texttt{member-\allowbreak{}labelled total-\allowbreak{}effect functional}; \(g_E:\mathbb S^p_{++}\to\mathbb R^{|\mathcal Q|}\), with \(g_E(\Sigma)=(((I_p-L_E(\Sigma))^{-1})_{ya})_{(a,y)\in\mathcal Q}\); \texttt{state-\allowbreak{}specific causal functional}
  \par\smallskip\noindent\emph{Definition.} For state \(E\) and positive-definite \(\Sigma\), apply the explicit target-local block-constrained map \(L_E(\Sigma)\) and take its path-sum total effects. The map is rational and smooth throughout \(\mathbb S^p_{++}\).
  \item \texttt{EffectSet} --- \texttt{finite state-\allowbreak{}component effect-\allowbreak{}set map}; \(\mathcal T_E(\Sigma)=\{g_D(\Sigma):D\in\mathcal R(E)\}\) for \(E\in\mathfrak S_p\) and \(\Sigma\in\mathbb S^p_{++}\); \texttt{set-\allowbreak{}valued causal target}
  \par\smallskip\noindent\emph{Definition.} Finite plug-in effect set on the positive-definite cone; at \(E=C\) and \(\Sigma\in\mathcal M(C)\) it is the component's causal-effect target.
  \item \texttt{alpha} --- \texttt{error probability}; \(\alpha\in(0,1)\); \texttt{inference level}
  \par\smallskip\noindent\emph{Definition.} \texttt{Familywise noncoverage level.}
  \item \texttt{Conf} --- \texttt{simultaneous member-\allowbreak{}labelled confidence collection}; \(\mathfrak I_{n,\alpha}(\widehat C_n)\); \texttt{effect-\allowbreak{}set confidence procedure}
  \par\smallskip\noindent\emph{Definition.} On \(S_n\in\mathbb S^p_{++}\), the Bonferroni collection uses the explicit block-constrained maps \(L_E(S_n)\), the resulting rational effect maps, and their delta-method derivatives on the positive-definite cone; on \(S_n\notin\mathbb S^p_{++}\), it is the deterministic collection of full real-line intervals.
  \item \texttt{Pcanon} --- \texttt{canonical background partially directed acyclic graph}; \(\mathcal P_C\) on \(V\); \texttt{canonical graph-\allowbreak{}extension representation of the blocked component}
  \par\smallskip\noindent\emph{Definition.} The ordinary CPDAG of \(G\) augmented by the background orientations in \(F(C)\), before or after equivalent Meek closure.
  \item \texttt{LiftC} --- \texttt{partial canonical graph-\allowbreak{}to-\allowbreak{}state lift}; \(\mathsf L_C(H)=(H,\Lambda^H)\); \texttt{path-\allowbreak{}independent partition transport}
  \par\smallskip\noindent\emph{Definition.} For each target, retain the fixed nonsingleton family of \(C\) and add singleton blocks for exactly the remaining parents in \(H\).
  \item \texttt{Enum} --- \texttt{canonical-\allowbreak{}PDAG blocked-\allowbreak{}component enumerator}; \(\operatorname{Enum}(C)\); \texttt{complete state enumeration with output-\allowbreak{}linear lifting}
  \par\smallskip\noindent\emph{Definition.} Exact-once enumeration of consistent extensions of \(\mathcal P_C\), followed by \(\mathsf L_C\); legal-reversal breadth-first search is retained as a complete fallback.
  \item \texttt{Henum} --- \texttt{exact distinct-\allowbreak{}effect enumeration handle}; \(\mathsf H_{\mathrm{enum}}\); \texttt{residual output-\allowbreak{}sensitive rational-\allowbreak{}functional quotient problem}
  \par\smallskip\noindent\emph{Definition.} \texttt{Canonical state enumeration followed by deterministic certification of global rational identity of member effect maps on the positive-\allowbreak{}definite cone.}
  \item \texttt{m} --- \texttt{nonnegative integer}; \(m=|\operatorname{skel}(G)|\leq\binom{p}{2}\); \texttt{enumeration edge-\allowbreak{}count parameter}
  \par\smallskip\noindent\emph{Definition.} \texttt{Number of edges in the common skeleton,\allowbreak{} used as the graph-\allowbreak{}input size in enumeration bounds.}
\end{itemize}

\section{Assumptions}

\begin{assumption}[ass:acyclic-member]\label{ass:acyclic-member}
The directed graph \(\mathcal K\) is acyclic.
\par\smallskip\noindent\emph{Standard} (Directed acyclicity, \cite{Chickering1995}).
\end{assumption}

\begin{assumption}[ass:target-parent-partition]\label{ass:target-parent-partition}
For every \(j\in V\), \(\Xi_j\) is a partition of \(\operatorname{pa}_{\mathcal K}(j)\), with singleton blocks permitted.
\par\smallskip\noindent\emph{Standard} (Published blocked target-local edge colouring, with properness not imposed, \cite{BoegeKubjasMisraSolus2026}).
\end{assumption}

\section{Definitions}

\begin{definition}[def:blocked-state-class]\label{def:blocked-state-class}
\par\smallskip\noindent\emph{Defined object.} \texttt{Published singleton-\allowbreak{}permitting blocked state class}
\par\smallskip\noindent\emph{Construction.} The published blocked edge-coloured superclass, specialized to unrestricted target-specific innovation variances, is
\[
\mathfrak S_p=\left\{A=(\mathcal K,\Xi):\mathcal K\text{ is an acyclic directed graph on }V,\ \Xi_j\text{ is a partition of }\operatorname{pa}_{\mathcal K}(j)\text{ for every }j\in V\right\}.
\]
Singleton blocks are permitted. The BPEC class is the proper subclass in which no edge-colour block is a singleton.
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}acyclic-\allowbreak{}member}; \texttt{ass:\allowbreak{}target-\allowbreak{}parent-\allowbreak{}partition}.
\end{definition}

\begin{definition}[def:covariance-model]\label{def:covariance-model}
\par\smallskip\noindent\emph{Defined object.} \texttt{Full blocked covariance model}
\par\smallskip\noindent\emph{Construction.} For \(E\in\mathfrak S_p\), let \(b_E=\sum_{j\in V}|\Pi^E_j|\), \(\Theta_E=\mathbb R^{b_E}\times(0,\infty)^p\), and \[\mathcal M(E)=\left\{(I_p-B_E(\beta_E))^{-1}\operatorname{diag}(\omega_E)(I_p-B_E(\beta_E))^{-\mathsf T}:(\beta_E,\omega_E)\in\Theta_E\right\}.\] Here \(B_E(\beta_E)_{ji}=\beta_{jB}\) for \(i\in B\in\Pi^E_j\), and all real block coefficients are admitted.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:nonsingleton-family]\label{def:nonsingleton-family}
\par\smallskip\noindent\emph{Defined object.} \texttt{Nonsingleton parent-\allowbreak{}block family}
\par\smallskip\noindent\emph{Construction.} \[\mathcal N_j(E)=\{B\in\Pi^E_j:|B|\geq2\},\qquad j\in V.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:family-mismatch-edges]\label{def:family-mismatch-edges}
\par\smallskip\noindent\emph{Defined object.} \texttt{Family-\allowbreak{}mismatch orientation set}
\par\smallskip\noindent\emph{Construction.} \[F(E)=\{i\to j\in G_E:\mathcal N_i(E)\neq\mathcal N_j(E)\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:static-signature]\label{def:static-signature}
\par\smallskip\noindent\emph{Defined object.} \texttt{Static blocked signature}
\par\smallskip\noindent\emph{Construction.} \[\mathsf S(E)=\left(\operatorname{skel}(G_E),\operatorname{uc}(G_E),(\mathcal N_j(E))_{j\in V},F(E)\right).\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:offdiagonal-tangent]\label{def:offdiagonal-tangent}
\par\smallskip\noindent\emph{Defined object.} \texttt{Off-\allowbreak{}diagonal model tangent}
\par\smallskip\noindent\emph{Construction.} At \(I_p\in\mathcal M(E)\), define \[T_E^{\circ}=\operatorname{proj}_{\mathrm{off}}T_{I_p}\mathcal M(E)=\operatorname{span}\{v_{jB}:j\in V,\ B\in\Pi^E_j\},\qquad \operatorname{supp}(v_{jB})=\{\{i,j\}:i\in B\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:legal-reversal]\label{def:legal-reversal}
\par\smallskip\noindent\emph{Defined object.} \texttt{Partition-\allowbreak{}compatible singleton covered reversal}
\par\smallskip\noindent\emph{Construction.} For \(i\to j\) in \(G_E\), \(\operatorname{rev}_{i\to j}(E)\) is defined exactly when \(\operatorname{pa}_{G_E}(j)=\operatorname{pa}_{G_E}(i)\cup\{i\}\), \(\{i\}\in\Pi^E_j\), and \(\Pi^E_j\setminus\{\{i\}\}=\Pi^E_i\). It reverses \(i\to j\), sets \(\Pi'_j=\Pi^E_i\) and \(\Pi'_i=\Pi^E_i\cup\{\{j\}\}\), and leaves every other partition unchanged.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:reversal-component]\label{def:reversal-component}
\par\smallskip\noindent\emph{Defined object.} \texttt{Legal-\allowbreak{}reversal component}
\par\smallskip\noindent\emph{Construction.} \[\mathcal R(E)=\{E'\in\mathfrak S_p:E'\text{ is reached from }E\text{ by a finite sequence of defined }\operatorname{rev}_{i\to j}\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:component-enumerator]\label{def:component-enumerator}
\par\smallskip\noindent\emph{Defined object.} \texttt{Canonical PDAG component enumerator with breadth-\allowbreak{}first fallback}
\par\smallskip\noindent\emph{Construction.} Given \(C=(G,\Pi)\), construct the canonical background PDAG \(\mathcal P_C\) from \(\texttt{def:canonical-background-pdag}\), compute its Meek closure, and apply the consistent-extension enumerator of \(\texttt{lem:pdag-consistent-extension-enumeration}\). For every emitted DAG \(H\), emit the canonical state \(\mathsf L_C(H)\) from \(\texttt{def:canonical-partition-lift}\); the emitted set is \(\operatorname{Enum}(C)\). The primary procedure is exact-once without state memoization. When a shortest certificate is requested, repeatedly scan for a covered arrow that disagrees with \(H\), reverse it, and canonically lift each intermediate graph. As a simple fallback, initialize a queue and exact visited set with \(C\), apply every defined \(\operatorname{rev}_{i\to j}\), append each unvisited state, and stop when the queue is empty.
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{def:\allowbreak{}canonical-\allowbreak{}background-\allowbreak{}pdag}; \texttt{def:\allowbreak{}canonical-\allowbreak{}partition-\allowbreak{}lift}; \texttt{def:\allowbreak{}legal-\allowbreak{}reversal}.
\end{definition}

\begin{definition}[def:off-model-block-ls]\label{def:off-model-block-ls}
\par\smallskip\noindent\emph{Defined object.} \texttt{Off-\allowbreak{}model target-\allowbreak{}local block-\allowbreak{}constrained least-\allowbreak{}squares map}
\par\smallskip\noindent\emph{Construction.} Fix \(E\in\mathfrak S_p\), \(\Sigma\in\mathbb S^p_{++}\), and \(j\in V\). Write \(P_{Ej}=\operatorname{pa}_{G_E}(j)\), and let \(A_{Ej}\) have rows \(i\in P_{Ej}\), columns \(B\in\Pi^E_j\), and entries \((A_{Ej})_{iB}=\mathbf 1\{i\in B\}\). If \(P_{Ej}\neq\varnothing\), define \[\Gamma_{Ej}(\Sigma)=A_{Ej}^{\mathsf T}\Sigma_{P_{Ej},P_{Ej}}A_{Ej},\qquad h_{Ej}(\Sigma)=A_{Ej}^{\mathsf T}\Sigma_{P_{Ej},j},\qquad \beta_{Ej}(\Sigma)=\Gamma_{Ej}(\Sigma)^{-1}h_{Ej}(\Sigma).\] The columns of \(A_{Ej}\) are linearly independent because \(\Pi^E_j\) is a partition into nonempty blocks, so \(\Gamma_{Ej}(\Sigma)\) is positive definite and the solution is unique. Set \((L_E(\Sigma))_{ji}=\beta_{Ej,B}(\Sigma)\) for \(i\in B\in\Pi^E_j\) and zero for \(i\notin P_{Ej}\); if \(P_{Ej}=\varnothing\), take the unique empty coefficient vector and set row \(j\) to zero. Thus \(L_E(\Sigma)\) is defined for every \(\Sigma\in\mathbb S^p_{++}\), is rational in the entries of \(\Sigma\), and is smooth on the positive-definite cone.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}; \texttt{Sigma}.
\end{definition}

\begin{definition}[def:global-bic]\label{def:global-bic}
\par\smallskip\noindent\emph{Defined object.} \texttt{Global blocked-\allowbreak{}state BIC}
\par\smallskip\noindent\emph{Construction.} For each \(E\in\mathfrak S_p\), set \(d_E=p+\sum_{j\in V}|\Pi^E_j|\). On the measurable event \(S_n\in\mathbb S^p_{++}\), compute \(L_E(S_n)\) using the block-design systems in \(\texttt{def:off-model-block-ls}\). For each target set \(\widehat\omega_{Ej}=S_{n,jj}-h_{Ej}(S_n)^{\mathsf T}\Gamma_{Ej}(S_n)^{-1}h_{Ej}(S_n)\), with \(\widehat\omega_{Ej}=S_{n,jj}\) for an empty parent set. Positive definiteness of \(S_n\) makes every nonempty \(\Gamma_{Ej}(S_n)\) positive definite and every \(\widehat\omega_{Ej}\) strictly positive, so the Gaussian likelihood supremum is finite and attained and \[\operatorname{BIC}_n(E)=-2\sup_{\Sigma\in\mathcal M(E)}\ell_n(\Sigma)+d_E\log n.\] On \(S_n\notin\mathbb S^p_{++}\), invoke no Gram inverse, residual-variance formula, or likelihood supremum and set \(\operatorname{BIC}_n(E)=d_E\log n\). For every \(n\in\mathbb N\), let \(\widehat C_n\) be the minimizer over \(\mathfrak S_p\) selected by a fixed deterministic ordering of states.
\par\smallskip\noindent\emph{Inputs.} \texttt{Sn}; \texttt{def:\allowbreak{}off-\allowbreak{}model-\allowbreak{}block-\allowbreak{}ls}.
\end{definition}

\begin{definition}[def:generic-exception]\label{def:generic-exception}
\par\smallskip\noindent\emph{Defined object.} \texttt{Generic component-\allowbreak{}selection exception}
\par\smallskip\noindent\emph{Construction.} For \(C\in\mathfrak S_p\), let \(\mathcal E_C\subsetneq\Theta_C\) be the finite union, over \(D\in\mathfrak S_p\) with \(d_D\leq d_C\) and \(\mathcal M(D)\neq\mathcal M(C)\), of the polynomial pullbacks of \(\mathcal M(C)\cap\mathcal M(D)\), augmented by the polynomial zero-gradient loci of the reported nonconstant effect coordinates.
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{Qpairs}.
\end{definition}

\begin{definition}[def:component-effect-set]\label{def:component-effect-set}
\par\smallskip\noindent\emph{Defined object.} \texttt{Component total-\allowbreak{}effect set}
\par\smallskip\noindent\emph{Construction.} For \(E\in\mathfrak S_p\) and \(\Sigma\in\mathbb S^p_{++}\), compute the explicit block-constrained map \(L_D(\Sigma)\) from \(\texttt{def:off-model-block-ls}\) for every \(D\in\mathcal R(E)\). Define \(g_D(\Sigma)=(((I_p-L_D(\Sigma))^{-1})_{ya})_{(a,y)\in\mathcal Q}\) and \[\mathcal T_E(\Sigma)=\{g_D(\Sigma):D\in\mathcal R(E)\}.\] Because \(G_D\) is acyclic, \(I_p-L_D(\Sigma)\) is invertible. Each \(g_D\) is rational and smooth on \(\mathbb S^p_{++}\), and \(\mathrm Dg_D(\Sigma)\) is defined throughout that cone. The causal target is the specialization \(\mathcal T_C(\Sigma)\) for \(\Sigma\in\mathcal M(C)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{Qpairs}; \texttt{def:\allowbreak{}off-\allowbreak{}model-\allowbreak{}block-\allowbreak{}ls}.
\end{definition}

\begin{definition}[def:simultaneous-region]\label{def:simultaneous-region}
\par\smallskip\noindent\emph{Defined object.} \texttt{Simultaneous component-\allowbreak{}effect confidence collection}
\par\smallskip\noindent\emph{Construction.} For every \(n\in\mathbb N\), first select \(\widehat C_n\) by \(\texttt{def:global-bic}\). On \(S_n\in\mathbb S^p_{++}\), order the finite member-labelled coordinates \(g_E(S_n)\) for \(E\in\mathcal R(\widehat C_n)\), compute every \(L_E(S_n)\) through the block-design Gram system in \(\texttt{def:off-model-block-ls}\), and evaluate the derivative \(\mathrm Dg_E(S_n)\) of the rational map in \(\texttt{def:component-effect-set}\) only on this open positive-definite domain. Use these derivatives with the plug-in Gaussian sample-covariance covariance matrix to obtain delta-method standard errors. Let \(K\) be the number of nonconstant labelled coordinates. If \(K\geq1\), use critical value \(z_{1-\alpha/(2K)}\) for those coordinates and report structurally zero coordinates as singleton intervals; if \(K=0\), report only the singleton intervals and do not evaluate a critical value. On \(S_n\notin\mathbb S^p_{++}\), invoke no Gram inverse, coefficient map, or delta-method derivative and assign the interval \(\mathbb R\) to every member-labelled coordinate. The resulting total measurable collection is \(\mathfrak I_{n,\alpha}(\widehat C_n)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{Sn}; \texttt{Chat}; \texttt{alpha}; \texttt{def:\allowbreak{}global-\allowbreak{}bic}; \texttt{def:\allowbreak{}off-\allowbreak{}model-\allowbreak{}block-\allowbreak{}ls}; \texttt{def:\allowbreak{}component-\allowbreak{}effect-\allowbreak{}set}.
\end{definition}

\begin{definition}[def:enumeration-handle]\label{def:enumeration-handle}
\par\smallskip\noindent\emph{Defined object.} \texttt{Canonical-\allowbreak{}state global-\allowbreak{}rational quotient handle}
\par\smallskip\noindent\emph{Construction.} The handle \(\mathsf H_{\mathrm{enum}}\) first runs \(\operatorname{Enum}(C)\), whose canonical lift removes all path-dependent partition choices. For each emitted state \(E\in\mathcal R(C)\), form a compact rational arithmetic circuit for \(g_E\). Two circuits are declared equivalent only when they define the same rational map on the whole cone \(\mathbb S^p_{++}\): clear their positive block-Gram determinant denominators coordinatewise and produce a deterministic exact polynomial-identity certificate, for example the fully expanded coefficient table. Emit one representative circuit per certified identity class together with the list of all member states and their shortest legal-reversal certificates. Numerical evaluations are never used as identity certificates. This handle names a terminating exact procedure; it does not assert an output-polynomial or polynomial-delay bound in the number of distinct rational maps.
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{Qpairs}; \texttt{def:\allowbreak{}component-\allowbreak{}enumerator}; \texttt{def:\allowbreak{}component-\allowbreak{}effect-\allowbreak{}set}.
\end{definition}

\begin{definition}[def:canonical-background-pdag]\label{def:canonical-background-pdag}
\par\smallskip\noindent\emph{Defined object.} \texttt{Canonical CPDAG with block-\allowbreak{}family background arrows}
\par\smallskip\noindent\emph{Construction.} Let \(\operatorname{CPDAG}(G)\) denote the ordinary completed partially directed acyclic graph whose consistent DAG extensions are the Markov-equivalence class of \(G\). Define \(\mathcal P_C\) by retaining every directed edge of \(\operatorname{CPDAG}(G)\) and, for every adjacency whose orientation belongs to \(F(C)\), inserting that orientation from \(G\); every remaining unoriented adjacency stays undirected. The construction does not require \(\mathcal P_C\) to be maximally oriented; its Meek closure is the corresponding MPDAG with the same consistent extensions.
\par\smallskip\noindent\emph{Inputs.} \texttt{C}.
\end{definition}

\begin{definition}[def:canonical-partition-lift]\label{def:canonical-partition-lift}
\par\smallskip\noindent\emph{Defined object.} \texttt{Canonical partition lift of a DAG extension}
\par\smallskip\noindent\emph{Construction.} Fix \(C\in\mathfrak S_p\). For a DAG \(H\) on \(V\) satisfying \(\bigcup_{B\in\mathcal N_j(C)}B\subseteq\operatorname{pa}_H(j)\) for every \(j\in V\), put
\[
 U_j(C)=\bigcup_{B\in\mathcal N_j(C)}B,
 \qquad
 \Lambda^H_j=\mathcal N_j(C)\cup
 \left\{\{i\}:i\in\operatorname{pa}_H(j)\setminus U_j(C)\right\},
\]
and define the partial canonical lift by \(\mathsf L_C(H)=(H,\Lambda^H)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{H}.
\end{definition}

\section{Main results}

\begin{lemma}[lem:tangent-block-recovery]\label{lem:tangent-block-recovery}
For any \(C,D\in\mathfrak S_p\), equality \(\mathcal M(C)=\mathcal M(D)\) implies equality of their skeletons and \(\mathcal N_j(C)=\mathcal N_j(D)\) for every \(j\in V\). More precisely, the inclusion-minimal nonempty supports in \(T_C^{\circ}\) are exactly \(\{\{i,j\}:i\in B\}\) for \(B\in\Pi_j\); every support of size at least two has a unique center \(j\) and recovers the labelled block \(B\), while a support of size one recovers only the underlying edge. Thus every edge whose orientation differs between \(G\) and \(H\) is a singleton block in both states.
\end{lemma}
\begin{proof}
By \(\texttt{lem:blocked-model-geometry}\), the identity covariance belongs to both models and
\[
 T_C^{\circ}=\operatorname{span}\{v_{jB}:j\in V,\ B\in\Pi_j\},
 \qquad
 \operatorname{supp}(v_{jB})=A_{jB}:=\{\{i,j\}:i\in B\}.
\]
Every unordered edge of \(G\) lies in exactly one set \(A_{jB}\): its orientation supplies its unique head \(j\), and the partition \(\Pi_j\) supplies the unique block \(B\) containing its tail. Therefore the nonempty supports \(A_{jB}\) are pairwise disjoint. If
\[
 w=\sum_{j\in V}\sum_{B\in\Pi_j}a_{jB}v_{jB}\neq0,
\]
then each coordinate belongs to at most one summand, so no coordinate can cancel and
\[
 \operatorname{supp}(w)=\bigcup_{a_{jB}\neq0}A_{jB}.
\]
It follows in both directions that the inclusion-minimal nonempty supports of vectors in \(T_C^{\circ}\) are precisely the sets \(A_{jB}\): a generator support is minimal because every nonzero vector supported inside it is a nonzero scalar multiple of that generator, and a support involving at least two generators properly contains each participating generator support. Their union is \(\operatorname{skel}(G)\).

Suppose \(\mathcal M(C)=\mathcal M(D)\). These are the same embedded subset of \(\mathbb S^p_{++}\), so their tangent spaces at the common point \(I_p\) agree; projection off the diagonal gives \(T_C^{\circ}=T_D^{\circ}\). Hence the two collections of minimal supports just characterized agree. A member of that collection having at least two edges is a labelled star with a unique common endpoint: if \(\{\{i,j\}:i\in B\}\) has at least two distinct edges, their only common vertex is \(j\). Thus its center recovers the target \(j\), and its other endpoints recover the labelled block \(B\). Such supports recover exactly
\[
 \mathcal N_j(C)=\{B\in\Pi_j:|B|\geq2\}
\]
at every target. A one-edge support has two possible centers and recovers only its underlying unordered edge. Applying the reconstruction to \(D\) yields
\[
 \operatorname{skel}(G)=\operatorname{skel}(H),
 \qquad
 \mathcal N_j(C)=\mathcal N_j(D)\quad(j\in V).
\]
Finally suppose \(i\to j\) in \(G\) and \(j\to i\) in \(H\). If the block of \(i\) in \(\Pi_j\) had size at least two, it would be a recovered member \(B\in\mathcal N_j(D)\) containing \(i\), which would force \(i\in\operatorname{pa}_H(j)\), contrary to \(j\to i\). Hence that block is \(\{i\}\). The same argument with \(C,D\) and \(i,j\) interchanged shows that the reversed edge occupies the singleton block \(\{j\}\) at target \(i\) in \(D\).
\end{proof}
\par\smallskip\noindent \textit{Justification.} The tangent support partition is the first orientation-robust observable that distinguishes repeated incoming coefficient blocks from reversible singleton edges. \textit{Gap.} BoegeKubjasMisraSolus2026, Theorem 4.1 and Section 5, gives smoothness and fixed-graph rational recovery but no cross-DAG recovery of target-local blocks from equality of model images. \textit{Consumer.} The lemma lets a covariance-model comparison recover causal communities without knowing which Markov-equivalent orientation generated them.

\begin{lemma}[lem:reversed-edge-separator]\label{lem:reversed-edge-separator}
Let \(C,D\in\mathfrak S_p\) satisfy \(\mathcal M(C)=\mathcal M(D)\), and suppose \(x\to y\) is in \(G\) while \(y\to x\) is in \(H\). Then \(\mathcal N_x(C)=\mathcal N_y(C)\). Specifically, if \(S\in\mathcal N_x(C)\) is split across two blocks of \(\Pi_y\), choosing unit innovation variances, coefficient one on \(x\to y\), coefficient one on a block \(T\in\Pi_y\) meeting but not containing \(S\), and zero elsewhere gives a covariance in \(\mathcal M(C)\) whose regression of \(X_x\) on \(\operatorname{pa}_H(x)\) has coefficient \(-q\mathbf 1\{s\in T\}\) on \(s\in S\), with \(q=(2+|T\setminus(\operatorname{pa}_H(x)\setminus\{y\})|)^{-1}>0\), contradicting the block \(S\) in \(D\).
\end{lemma}
\begin{proof}
By \(\texttt{lem:tangent-block-recovery}\), the reversed edge is a singleton block in both states and
\[
 \mathcal N_j(C)=\mathcal N_j(D)\quad(j\in V).
\]
By \(\texttt{lem:edge-coloured-model-equivalence-markov}\), \(G\) and \(H\) have the same unshielded colliders.

Fix \(S\in\mathcal N_x(C)\) and \(s\in S\). Tangent recovery gives \(S\in\mathcal N_x(D)\), so \(s\to x\) occurs in both graphs. If \(s\) and \(y\) were nonadjacent, then \(s\to x\to y\) in \(G\) would become the unshielded collider \(s\to x\leftarrow y\) in \(H\), contradicting equality of unshielded colliders. Hence \(s\) and \(y\) are adjacent. In \(G\), the orientation \(y\to s\) would create the directed cycle \(y\to s\to x\to y\); acyclicity from \(\texttt{def:blocked-state-class}\) therefore forces \(s\to y\). Since \(s\) was arbitrary,
\[
 S\subseteq\operatorname{pa}_G(y).
\]

Suppose, for a contradiction, that \(S\) is not contained in a single block of \(\Pi_y\). Since \(\Pi_y\) partitions \(\operatorname{pa}_G(y)\), there is a block \(T\in\Pi_y\) with \(T\cap S\neq\varnothing\) and \(S\nsubseteq T\). The block of the arrow \(x\to y\) is \(\{x\}\), so \(x\notin T\). In the model for \(C\), choose every innovation variance equal to one, choose coefficient one on the singleton block \(\{x\}\) at target \(y\), coefficient one on the block \(T\) at target \(y\), and coefficient zero on every other block. These parameters belong to \(\Theta_C\), and \(\texttt{def:covariance-model}\) gives a positive-definite covariance \(\Sigma\in\mathcal M(C)\). All variables other than \(X_y\) are then mutually independent standard Gaussians, and
\[
 X_y=X_x+\sum_{r\in T}X_r+\varepsilon_y,
 \qquad \varepsilon_y\sim N(0,1),
\]
with \(\varepsilon_y\) independent of the other displayed variables.

Put \(P=\operatorname{pa}_H(x)\) and \(Q=P\setminus\{y\}\). Since \(S\in\mathcal N_x(D)\), \(S\subseteq Q\). Projecting \(X_y\) linearly on \(X_Q\) removes exactly the independent summands indexed by \(T\cap Q\); its residual is
\[
 Z=X_x+\sum_{r\in T\setminus Q}X_r+\varepsilon_y,
 \qquad
 \operatorname{Var}(Z)=2+|T\setminus Q|.
\]
The residual of \(X_x\) after projection on \(X_Q\) is \(X_x\). Consequently the normal equations, equivalently the Frisch--Waugh residual calculation, give the coefficient on \(X_y\) in the regression of \(X_x\) on \((X_Q,X_y)\) as
\[
 q=\frac{\operatorname{Cov}(X_x,Z)}{\operatorname{Var}(Z)}
   =\frac{1}{2+|T\setminus Q|}>0.
\]
Before adding \(X_y\), the coefficient of every member of \(X_Q\) is zero. The coefficient of \(X_s\) in the projection of \(X_y\) on \(X_Q\) is \(\mathbf 1\{s\in T\}\), because these regressors are mutually independent with unit variance. Substitution into the normal equations therefore yields
\[
 \operatorname{coef}_{X_s}\{X_x\mid X_P\}
 =-q\,\mathbf 1\{s\in T\},\qquad s\in S.
\]
The conditions \(T\cap S\neq\varnothing\) and \(S\nsubseteq T\) make these coefficients take both values \(-q\) and \(0\) on the one block \(S\). But \(S\in\mathcal N_x(D)\), and the parent-regression recovery formula in \(\texttt{lem:blocked-model-geometry}\) says that every covariance in \(\mathcal M(D)\) has equal regression coefficients on every block of \(\Pi^D_x\). Thus \(\Sigma\notin\mathcal M(D)\), contradicting \(\mathcal M(C)=\mathcal M(D)\). We have proved that every \(S\in\mathcal N_x(C)\) is contained in a member of \(\mathcal N_y(C)\); the containing block is nonsingleton because \(|S|\geq2\).

Apply the same proved argument to the reversed arrow \(y\to x\) in \(D\), with \((D,y)\) in place of \((C,x)\). Using targetwise tangent recovery to identify the two states' families, every member of \(\mathcal N_y(C)\) is contained in a member of \(\mathcal N_x(C)\). If \(S\in\mathcal N_x(C)\), choose \(T\in\mathcal N_y(C)\) and \(U\in\mathcal N_x(C)\) with \(S\subseteq T\subseteq U\). The blocks \(S,U\) belong to the same partition and intersect, so \(S=U\), whence \(S=T=U\). Reversing the roles gives the converse membership, and therefore
\[
 \mathcal N_x(C)=\mathcal N_y(C).
\]
\end{proof}
\par\smallskip\noindent \textit{Justification.} This sparse covariance witness is the load-bearing necessity step because it handles arbitrary extra target parents and simultaneous arrow changes. \textit{Gap.} BoegeKubjasMisraSolus2026, Lemma 5.11 and the proof of Theorem 5.17, separates reversals only in the proper-colouring regime; it does not establish the endpoint-family condition when singleton blocks coexist with repeated blocks. \textit{Consumer.} The separator certifies when a proposed singleton reversal changes the complete covariance model, preventing a learner from merging observationally distinguishable causal-community states.

\begin{theorem}[thm:complete-blocked-equivalence]\label{thm:complete-blocked-equivalence}
For every \(C=(G,\Pi),D=(H,\Lambda)\in\mathfrak S_p\), the following are equivalent: \(\mathcal M(C)=\mathcal M(D)\); \(\mathsf S(C)=\mathsf S(D)\), equivalently \(G,H\) have the same skeleton and unshielded colliders, \(\mathcal N_j(C)=\mathcal N_j(D)\) for all \(j\in V\), and every edge joining vertices with unequal nonsingleton families has the same orientation; and \(D\in\mathcal R(C)\). Every step in the final characterization is a partition-compatible singleton covered reversal with the prescribed transport. Moreover, \(d_{\mathrm{rev}}(C,D)=\delta(C,D)\), and a shortest path reverses each disagreeing arrow exactly once. When no parent block of \(C\) is a singleton, \(\mathcal R(C)=\{C\}\), agreeing with the BPEC structural-identifiability theorem on its proper no-singleton subclass. The preceding equivalence criterion applies to the published blocked superclass that also permits singleton blocks.
\end{theorem}
\begin{proof}
Fix \(C=(G,\Pi)\) and \(D=(H,\Lambda)\) in \(\mathfrak S_p\). By \(\texttt{def:blocked-state-class}\), their graphs are finite labelled DAGs and each target family partitions its parent set. By \(\texttt{def:covariance-model}\), the coefficient on every block ranges over all of \(\mathbb R\), each innovation variance lies in \((0,\infty)\), and \(\mathcal M(E)\) is the full positive-definite covariance image of the corresponding linear Gaussian structural model. Thus the structural target is the state modulo \(\mathcal R(C)\); equality of observable covariance-model images is a separate relation whose equivalence to that target is proved below.

First suppose \(\mathcal M(C)=\mathcal M(D)\). The cited result \(\texttt{lem:edge-coloured-model-equivalence-markov}\) gives
\[
 \operatorname{skel}(G)=\operatorname{skel}(H),
 \qquad
 \operatorname{uc}(G)=\operatorname{uc}(H).
\]
The proved tangent-support lemma gives
\[
 \mathcal N_j(C)=\mathcal N_j(D),\qquad j\in V.
\]
If an adjacency \(\{x,y\}\) is oriented oppositely in \(G\) and \(H\), \(\texttt{lem:reversed-edge-separator}\) gives \(\mathcal N_x(C)=\mathcal N_y(C)\). Contrapositively, every adjacency whose endpoint families differ has the same orientation in the two graphs. With the common skeleton and common vertex-indexed families, \(\texttt{def:family-mismatch-edges}\) then gives \(F(C)=F(D)\). All four coordinates in \(\texttt{def:static-signature}\) agree, hence \(\mathsf S(C)=\mathsf S(D)\).

Conversely suppose \(\mathsf S(C)=\mathsf S(D)\). Equality of the first two signature coordinates and \(\texttt{lem:verma-pearl-markov-equivalence}\) show that \(G\) and \(H\) are Markov equivalent. Abbreviate
\[
 \mathcal N_j:=\mathcal N_j(C)=\mathcal N_j(D),\qquad j\in V.
\]
By \(\texttt{lem:chickering-monotone-covered-reversals}\), there is a sequence
\[
 G=G_0,G_1,\ldots,G_r=H,
 \qquad r=\delta(C,D),
\]
of distinct covered-edge reversals, with every intermediate graph an acyclic member of the same Markov-equivalence class. Because the sequence has exactly as many distinct reversals as the number of initially disagreeing edges and ends at \(H\), it reverses each initially disagreeing edge exactly once and no initially agreeing edge.

We lift this sequence by induction. Let \(E_t=(G_t,\Pi^t)\), starting with \(E_0=C\), and maintain
\[
 \mathcal N_k(E_t)=\mathcal N_k\quad(k\in V).
\]
Suppose the next covered arrow is \(i\to j\). Its underlying edge disagrees between \(G\) and \(H\). If \(\mathcal N_i\neq\mathcal N_j\), the orientation of this edge belongs to \(F(C)\), while equality \(F(C)=F(D)\) forces that orientation to agree in \(G\) and \(H\), a contradiction. Hence
\[
 \mathcal N_i=\mathcal N_j=:\mathcal N.
\]
Let \(B\in\Pi^t_j\) be the unique block containing \(i\). If \(|B|\geq2\), then \(B\in\mathcal N_j(E_t)=\mathcal N\), so equality of endpoint families gives \(B\in\mathcal N_i(E_t)\). A member of the latter family is contained in \(\operatorname{pa}_{G_t}(i)\), but \(i\in B\), which would make \(i\) its own parent. A DAG has no self-loop, so \(B=\{i\}\).

Coveredness gives, for \(P=\operatorname{pa}_{G_t}(i)\),
\[
 \operatorname{pa}_{G_t}(j)=P\cup\{i\}.
\]
Put \(U=\bigcup_{A\in\mathcal N}A\). The nonsingleton blocks at both endpoints are exactly \(\mathcal N\). Any \(k\in P\setminus U\) must therefore occupy the singleton block \(\{k\}\) in both endpoint partitions. It follows that
\[
 \Pi^t_i=\mathcal N\cup\{\{k\}:k\in P\setminus U\},
 \qquad
 \Pi^t_j=\Pi^t_i\cup\{\{i\}\}.
\]
These are precisely the three definedness conditions in \(\texttt{def:legal-reversal}\). Applying \(\operatorname{rev}_{i\to j}\) produces \(E_{t+1}\) with graph \(G_{t+1}\), and \(\texttt{lem:legal-reversal-model-invariance}\) preserves every nonsingleton family, completing the induction.

After \(r\) steps the graph is \(H\). Given a parent set \(P_j\) and a compatible nonsingleton family \(\mathcal N_j\), the only possible partition is
\[
 \mathcal N_j\cup
 \left\{\{k\}:k\in P_j\setminus\bigcup_{B\in\mathcal N_j}B\right\}.
\]
The final lifted state and \(D\) have the same graph and the same nonsingleton families, so this formula makes their partitions equal. The final state is \(D\), proving \(D\in\mathcal R(C)\).

Finally suppose \(D\in\mathcal R(C)\). By \(\texttt{def:reversal-component}\), there is a finite legal-reversal path from \(C\) to \(D\). Repeated use of \(\texttt{lem:legal-reversal-model-invariance}\) gives \(\mathcal M(D)=\mathcal M(C)\). This completes the cycle of implications among model equality, static-signature equality, and legal-reversal connectivity.

The lifted Chickering path has \(\delta(C,D)\) steps and reverses every disagreeing edge exactly once. Conversely, a legal move changes the orientation of only one skeleton edge. Each endpoint-disagreeing edge must be reversed an odd, hence positive, number of times along any path, so every path has at least \(\delta(C,D)\) moves. Therefore
\[
 d_{\mathrm{rev}}(C,D)=\delta(C,D).
\]

For later use, if \(B\in\mathcal N_j(C)\) and \(i\in B\), then \(i\to j\) in \(G\). Equality \(\mathcal N_i(C)=\mathcal N_j(C)\) would put the same set \(B\), which contains \(i\), inside \(\operatorname{pa}_G(i)\), again creating a self-parent. Hence
\[
 B\in\mathcal N_j(C),\ i\in B
 \quad\Longrightarrow\quad
 \mathcal N_i(C)\neq\mathcal N_j(C)
 \quad\Longrightarrow\quad
 i\to j\in F(C).
\]
If no parent block of \(C\) is a singleton, every arrow is covered by the first premise, hence every arrow lies in \(F(C)\) and is fixed by the static criterion. The graph and every nonsingleton family then determine the whole partition, so \(\mathcal R(C)=\{C\}\). This agrees with the cited BPEC conclusion \(\texttt{lem:bpec-proper-identifiability}\) on its proper no-singleton subclass. No coefficient is declared identified merely because its arrow is fixed: the result identifies the graph--partition state modulo the displayed equivalence, whereas numerical coefficients remain parameters of the observational law.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The theorem identifies the exact full-model quotient on the published singleton-permitting blocked class and gives both its static recovery functional and its legal local transformation calculus. \textit{Gap.} BoegeKubjasMisraSolus2026 proves uniqueness only inside the proper no-singleton BPEC subclass and asks for edge-coloured equivalence; Chickering1995 has no coefficient-partition legality condition. \textit{Consumer.} An extension of the colouredDAGs workflow can report the exact covariance-equivalence component while retaining every repeated-coefficient community.

\begin{proposition}[prop:complete-component-enumeration]\label{prop:complete-component-enumeration}
For every \(C\in\mathfrak S_p\),
\[
 \operatorname{Enum}(C)=\mathcal R(C)
 =\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}.
\]
Static equality is decidable using \(O(p^3)\) elementary comparisons with adjacency matrices and canonical block bitsets. The primary procedure takes \(O(p^3)\) Meek-closure initialization, enumerates every consistent extension of \(\mathcal P_C\) exactly once with \(O(p+m)\) worst-case delay, and applies the output-linear canonical partition lift \(\mathsf L_C\). Moreover \(|\mathcal R(C)|\leq p!\), sharply, with equality for the complete-skeleton all-singleton state. If shortest legal-reversal certificates are requested, the constructive covered-reversal scan uses \(O(p^5)\) time per emitted state and returns for each \(D\) a path of length \(\delta(C,D)\). Exact state memoization is required only by the legal-reversal breadth-first fallback, which is complete and also returns shortest certificates.
\end{proposition}
\begin{proof}
The bijection in \(\texttt{prop:canonical-pdag-component-lift}\), applied to \(\texttt{def:canonical-background-pdag}\), \(\texttt{def:canonical-partition-lift}\), and \(\texttt{def:component-enumerator}\), proves that the primary procedure emits every state in \(\mathcal R(C)\) exactly once. Together with \(\texttt{thm:complete-blocked-equivalence}\), this gives
\[
 \operatorname{Enum}(C)=\mathcal R(C)
 =\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}.
\]

For the static decision bound, compare adjacency matrices in \(O(p^2)\) operations. Enumerating labelled triples and testing the two incoming adjacencies together with the absent shielding adjacency compares unshielded-collider sets in \(O(p^3)\) operations. Encode the parent partitions by the Boolean tensor recording, for each \((j,i,k)\), whether \(i\) and \(k\) lie in the same nonsingleton parent block of \(j\); this is constructible and comparable in \(O(p^3)\) elementary operations and determines every labelled nonsingleton family. A final adjacency scan compares the family-mismatch orientations in \(O(p^2)\). Thus equality of static signatures is decidable within \(O(p^3)\) elementary comparisons.

The cited algorithm \(\texttt{lem:pdag-consistent-extension-enumeration}\) enumerates every consistent extension of \(\mathcal P_C\) exactly once after \(O(p^3)\) Meek-closure initialization, with \(O(p+m)\) worst-case delay. The canonical-lift proposition shows that producing \(\mathsf L_C(H)\) scans only the output graph and its parent incidences, takes \(O(p+m)\) time, and creates neither a transport choice nor a duplicate. Hence the same delay and exact-once guarantee hold for blocked states.

The same proposition proves \(|\mathcal R(C)|\leq p!\). For completeness, the injection is obtained by assigning each component DAG its lexicographically first topological order: a fixed skeleton and a topological order determine every arrow by directing each adjacency from the earlier to the later endpoint, so two DAGs assigned the same order coincide. The lift supplies at most one blocked state per DAG. Sharpness holds for the complete-skeleton all-singleton state. Each of the \(p!\) total orders gives a distinct transitive acyclic orientation; the complete skeleton has no unshielded collider, and all such states have \(\mathcal N_j=\varnothing\) and \(F=\varnothing\). The equivalence theorem puts all \(p!\) states in one component.

When shortest certificates are requested, \(\texttt{prop:canonical-pdag-component-lift}\) applies the constructive covered-edge scan to each emitted DAG. All background arrows remain fixed, and canonical lifting makes every intermediate covered reversal legal. The scan costs at most \(O(p^5)\) per output and returns exactly \(\delta(C,D)\) moves; \(\texttt{thm:complete-blocked-equivalence}\) proves that this length is minimal. Only the fallback breadth-first traversal needs an exact visited-state set. The legal-move graph is finite, so that traversal terminates, emits precisely the connected component without duplication, and its predecessor tree also supplies shortest certificates.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The population characterization is operational because the canonical background PDAG and unique partition lift yield exact-once enumeration with explicit delay, sharp size, and shortest-certificate guarantees. \textit{Gap.} WienobstLuttermannBannachLiskiewicz2023 handles PDAG extensions but not target partitions; the canonical-lift bijection proves output-linear lifting, path independence, and legality of the constructive shortest certificates. \textit{Consumer.} Component enumeration supplies every graph-specific adjustment and path functional needed for possible-effect analysis, with complete state labels and optional shortest legal-reversal certificates.

\begin{proposition}[prop:ordinary-and-proper-reductions]\label{prop:ordinary-and-proper-reductions}
If every block of \(C\) is a singleton, then \(\mathcal N_j(C)=\varnothing\) for all \(j\) and \(\mathcal R(C)\) is exactly the ordinary Markov-equivalence class of \(G\), with \(d_{\mathrm{rev}}=\delta\) as in Chickering1995. If no block of \(C\) is a singleton, then every edge is fixed and \(\mathcal R(C)=\{C\}\) as in the BPEC identifiability boundary. If \(G=H\) and \(\mathcal M(C)=\mathcal M(D)\), then \(\Pi=\Lambda\).
\end{proposition}
\begin{proof}
Suppose every block of \(C\) is a singleton. Then \(\mathcal N_j(C)=\varnothing\) for every \(j\), hence \(F(C)=\varnothing\). The static criterion in \(\texttt{thm:complete-blocked-equivalence}\) reduces exactly to equality of skeletons and unshielded colliders. By \(\texttt{lem:verma-pearl-markov-equivalence}\), the graph coordinates in \(\mathcal R(C)\) are precisely the ordinary Markov-equivalence class of \(G\), and their parent partitions are forced to be all-singleton. The distance conclusion of the equivalence theorem gives \(d_{\mathrm{rev}}(C,D)=\delta(C,D)\).

Now suppose no parent block of \(C\) is a singleton. For every edge \(i\to j\), the block \(B\in\mathcal N_j(C)\) containing \(i\) cannot belong to \(\mathcal N_i(C)\), since every member of the latter is contained in \(\operatorname{pa}_G(i)\) and no such set contains \(i\). Thus \(\mathcal N_i(C)\neq\mathcal N_j(C)\), so every edge lies in \(F(C)\). The static criterion fixes every orientation, and it fixes each nonsingleton family, which in this regime is the entire partition. Therefore \(\mathcal R(C)=\{C\}\), consistently with \(\texttt{lem:bpec-proper-identifiability}\) on the proper BPEC subclass.

Finally assume \(G=H\) and \(\mathcal M(C)=\mathcal M(D)\). The tangent-support lemma gives \(\mathcal N_j(C)=\mathcal N_j(D)\) for every \(j\). The common graph gives identical parent sets. At each vertex the partition is uniquely reconstructed by adjoining singleton blocks for all parents outside the union of the common nonsingleton family. Hence \(\Pi_j=\Lambda_j\) for all \(j\), and \(\Pi=\Lambda\).
\end{proof}
\par\smallskip\noindent \textit{Justification.} The two extreme partitions verify that the characterization interpolates exactly between ordinary Markov equivalence and proper blocked identifiability. \textit{Gap.} The two endpoint results are published separately by Chickering1995 and BoegeKubjasMisraSolus2026; their exact interpolation within one full-model criterion is new. \textit{Consumer.} These reductions provide regression tests for software and prevent interpreting every fixed arrow as a numerically identified direct effect.

\begin{theorem}[thm:generic-bic-component-recovery]\label{thm:generic-bic-component-recovery}
Fix \(p\), \(C\in\mathfrak S_p\), and a parameter \(\theta_C\in\Theta_C\setminus\mathcal E_C\) with \(\Sigma_0=\phi_C(\theta_C)\). For each integer \(n\geq p\), let \(X^{(1)},\ldots,X^{(n)}\) be independent \(N_p(0,\Sigma_0)\) observations and let \(\widehat C_n\) be the deterministically tie-broken global minimizer in \(\texttt{def:global-bic}\). Then \(S_n\in\mathbb S^p_{++}\) almost surely. Consequently every statewise block Gram matrix \(\Gamma_{Ej}(S_n)\) is invertible, every constrained residual variance in \(\texttt{def:global-bic}\) is strictly positive, and only the finite attained likelihood branch is used. As \(n\to\infty\) through integers satisfying \(n\geq p\), \[\Pr\{\mathcal R(\widehat C_n)=\mathcal R(C)\}\longrightarrow1.\] The exceptional set is proper algebraic: models of dimension at most \(d_C\) are excluded by their nonzero pulled-back defining polynomials, while larger models lose by the diverging dimension penalty. This is component recovery, not consistency of greedy GECS.
\end{theorem}
\begin{proof}
Write \(d_E=p+\sum_{j\in V}|\Pi^E_j|\). By \(\texttt{lem:blocked-model-geometry}\), each \(\mathcal M(E)\) is a connected, relatively closed, embedded submanifold of \(\mathbb S^p_{++}\) of dimension \(d_E\), cut out there by finitely many polynomial equalities after clearing positive denominators.

We first verify that \(\mathcal E_C\) is proper. Fix \(D\) with \(d_D\leq d_C\) and \(\mathcal M(D)\neq\mathcal M(C)\). If \(d_D<d_C\), an inclusion \(\mathcal M(C)\subseteq\mathcal M(D)\) would inject a \(d_C\)-dimensional tangent space into a \(d_D\)-dimensional one. If \(d_D=d_C\), such an inclusion would make \(\mathcal M(C)\) open in \(\mathcal M(D)\) by equal-dimensional embedded-manifold charts and closed there by relative closedness. Connectedness of \(\mathcal M(D)\) would force equality. Both cases contradict the choice of \(D\); hence \(\mathcal M(C)\nsubseteq\mathcal M(D)\). At least one polynomial equation defining \(\mathcal M(D)\) therefore has a nonzero polynomial pullback through the polynomial map \(\phi_C\), and its zero set contains \(\phi_C^{-1}(\mathcal M(C)\cap\mathcal M(D))\).

For any reported effect coordinate nonconstant on \(\mathcal M(C)\), composition with \(\phi_C\) is a nonconstant rational function on the open parameter domain \(\Theta_C\). In characteristic zero its gradient cannot vanish identically; clearing its positive denominators makes at least one gradient coordinate a nonzero polynomial. There are finitely many states and reported coordinates. The product of all nonzero polynomials obtained above is nonzero and cannot vanish throughout the open set \(\Theta_C\). Hence the finite union \(\mathcal E_C\) is a proper algebraic subset. The assumed exclusion \(\theta_C\notin\mathcal E_C\) in particular gives
\[
 \Sigma_0\notin\mathcal M(D)
 \quad\text{for every }D\text{ with }d_D\leq d_C
 \text{ and }\mathcal M(D)\neq\mathcal M(C).
\]

By \(\texttt{lem:fixed-p-gaussian-covariance-calculus}\), \(S_n\in\mathbb S^p_{++}\) almost surely whenever \(n\geq p\). For every nonempty parent set, the disjoint nonempty columns of \(A_{Ej}\) are linearly independent, and therefore, for \(u\neq0\),
\[
 u^{\mathsf T}\Gamma_{Ej}(S_n)u
 =(A_{Ej}u)^{\mathsf T}S_{n,P_{Ej}P_{Ej}}(A_{Ej}u)>0.
\]
The joint sample covariance of the block-sum regressors and \(X_j\) is the restriction of \(S_n\) under a full-column-rank linear map, hence is positive definite. Its Schur complement is the constrained residual variance, which is strictly positive.

For completeness, acyclicity gives \(\det(I_p-B_E)=1\), and the centered Gaussian negative log likelihood, up to a state-independent constant, separates as
\[
 -2\ell_n(B_E,\omega_E)
 =n\sum_{j\in V}\left\{\log\omega_{Ej}
 +\omega_{Ej}^{-1}n^{-1}\sum_{r=1}^n
 \bigl(X_j^{(r)}-(B_E)_{jP_{Ej}}X_{P_{Ej}}^{(r)}\bigr)^2\right\}.
\]
The positive-definite Gram systems have the unique constrained least-squares solutions in \(\texttt{def:global-bic}\), and minimizing in each \(\omega_{Ej}>0\) gives its strictly positive residual variance. Thus every statewise likelihood supremum on the probability-one positive-definite branch is finite and attained.

Up to an additive constant, the population likelihood contrast from \(\Sigma_0\) to \(\Sigma\) is
\[
 K(\Sigma_0,\Sigma)=\frac12\left\{
 \operatorname{tr}(\Sigma^{-1}\Sigma_0)
 -\log\det(\Sigma^{-1}\Sigma_0)-p\right\}.
\]
Diagonalizing \(\Sigma^{-1/2}\Sigma_0\Sigma^{-1/2}\) and using \(u-\log u-1\geq0\), with equality only at \(u=1\), shows \(K\geq0\), with equality only at \(\Sigma=\Sigma_0\). The same scalar expression diverges at zero and infinity, so its sublevel sets are compact subsets of \(\mathbb S^p_{++}\). Relative closedness and exclusion of \(\Sigma_0\) imply
\[
 \kappa_D:=\inf_{\Sigma\in\mathcal M(D)}K(\Sigma_0,\Sigma)>0
\]
for every nonequivalent \(D\) with \(d_D\leq d_C\).

Since \(S_n\to\Sigma_0\) in probability, it lies with probability tending to one in a fixed compact Loewner neighborhood of \(\Sigma_0\). Coercivity of the eigenvalue contrast uniformly localizes every sample optimizer to a fixed compact sublevel set. On that set the normalized likelihood is continuous, and its only sample-dependent term is affine in \(S_n\), so it converges uniformly to its population counterpart. Consequently
\[
 \sup_{\Sigma\in\mathcal M(D)}\ell_n(\Sigma)
 -\sup_{\Sigma\in\mathcal M(C)}\ell_n(\Sigma)
 \leq -n\kappa_D+o_{\mathbb P}(n)
\]
for each such \(D\). No \(O(\log n)\) penalty difference can offset this order-\(n\) loss.

If \(d_D>d_C\), let \(\ell_n(S_n)\) be the saturated Gaussian maximum. Because \(\Sigma_0\in\mathcal M(C)\),
\[
 \sup_{\mathcal M(D)}\ell_n-\sup_{\mathcal M(C)}\ell_n
 \leq \ell_n(S_n)-\ell_n(\Sigma_0).
\]
Writing \(\Delta_n=S_n-\Sigma_0\), the first-order terms in the likelihood expansion cancel at the saturated optimum, and \(\sqrt n\Delta_n=O_{\mathbb P}(1)\). The second-order Taylor remainder, multiplied by \(n\), is therefore \(O_{\mathbb P}(1)\). Hence
\[
 \operatorname{BIC}_n(D)-\operatorname{BIC}_n(C)
 \geq(d_D-d_C)\log n-O_{\mathbb P}(1)
 \longrightarrow+\infty
\]
in probability.

The class \(\mathfrak S_p\) is finite for fixed \(p\). A union bound shows that, with probability tending to one, every nonequivalent state has criterion strictly larger than that of \(C\). Any remaining minimizer has covariance image \(\mathcal M(C)\); equal embedded images have equal dimension, hence equal BIC penalty as well. By \(\texttt{thm:complete-blocked-equivalence}\), every such minimizer satisfies
\[
 \mathcal R(\widehat C_n)=\mathcal R(C).
\]
This proves consistency of the stated global component selector and makes no claim about greedy GECS.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Singleton blocks change the estimable structural target from one state to an exact quotient component, so selection consistency must quotient identical covariance images. \textit{Gap.} BoegeKubjasMisraSolus2026, Section 6 and Discussion, derives a decomposable BIC and reports GECS simulations but leaves consistency open; Haughton1988 treats finitely many smooth models without the blocked-state equality quotient. \textit{Consumer.} A fixed-dimensional coloured-DAG analysis can consistently report the right component even when several singleton-block orientations have exactly equal likelihood.

\begin{theorem}[thm:simultaneous-component-effect-inference]\label{thm:simultaneous-component-effect-inference}
Under the conditions of \(\texttt{thm:generic-bic-component-recovery}\), fix finite \(\mathcal Q\) and \(\alpha\in(0,1)\). Along integers \(n\geq p\), the event \(S_n\in\mathbb S^p_{++}\) has probability one. Hence every \(L_E(S_n)\) is obtained from the uniquely solvable block-constrained Gram systems in \(\texttt{def:off-model-block-ls}\), and every derivative \(\mathrm D g_E(S_n)\) is evaluated inside the positive-definite cone on which \(g_E\) is rational and smooth. Then \(\mathcal T_{\widehat C_n}(S_n)\) converges in probability to \(\mathcal T_C(\Sigma_0)\) in Hausdorff distance. With \(\mathfrak I_{n,\alpha}(\widehat C_n)\) defined memberwise before removing numerical duplicates, \[\liminf_{\substack{n\to\infty\\ n\geq p}}\Pr\left\{\mathcal R(\widehat C_n)=\mathcal R(C)\text{ and }g_E(\Sigma_0)\text{ belongs to its labelled interval in }\mathfrak I_{n,\alpha}(\widehat C_n)\text{ for every }E\in\mathcal R(C)\right\}\geq1-\alpha.\] The conclusion permits coincident effect values and singular joint covariance because only marginal nonconstant-coordinate variances enter the Bonferroni construction.
\end{theorem}
\begin{proof}
We first connect the observable functional to the causal target. If \(E\in\mathcal R(C)\), then \(\texttt{thm:complete-blocked-equivalence}\) gives \(\mathcal M(E)=\mathcal M(C)\), so \(\Sigma_0\in\mathcal M(E)\). The injectivity in \(\texttt{lem:blocked-model-geometry}\) gives a unique structural parameter \((B_E,\omega_E)\) producing \(\Sigma_0\). In the associated linear Gaussian NPSEM,
\[
 X=B_EX+\varepsilon,
 \qquad \varepsilon\sim N_p(0,\operatorname{diag}(\omega_E)),
\]
with mutually independent innovations of positive variance. For \(P_{Ej}=\operatorname{pa}_{G_E}(j)\), innovation independence gives
\[
 \Sigma_{0,P_{Ej},j}
 =\Sigma_{0,P_{Ej},P_{Ej}}(B_E)_{jP_{Ej}}^{\mathsf T}.
\]
Writing \((B_E)_{jP_{Ej}}^{\mathsf T}=A_{Ej}\beta_{Ej}\), the constrained normal equations are
\[
 h_{Ej}(\Sigma_0)
 =A_{Ej}^{\mathsf T}\Sigma_{0,P_{Ej},P_{Ej}}A_{Ej}\beta_{Ej}
 =\Gamma_{Ej}(\Sigma_0)\beta_{Ej}.
\]
The Gram matrix is positive definite, so uniqueness of the solution in \(\texttt{def:off-model-block-ls}\) gives \(L_E(\Sigma_0)=B_E\).

Under the intervention \(\operatorname{do}(X_a=t)\), let \(r_k\) be the derivative of \(\mathbb E[X_k\mid\operatorname{do}(X_a=t)]\) with respect to \(t\). Acyclicity gives \(r_a=1\), while for \(k\neq a\),
\[
 r_k=\sum_i(B_E)_{ki}r_i.
\]
Every parent of \(a\) is a nondescendant of \(a\), so its derivative is zero and the row \(a\) obeys the same rearranged system. Hence
\[
 (I_p-B_E)r=e_a,
 \qquad
 r_y=((I_p-B_E)^{-1})_{ya}.
\]
For \(a\neq y\), this derivative is the total causal effect. Therefore \(g_E(\Sigma_0)\) equals the member-labelled causal-effect vector, and \(\mathcal T_C(\Sigma_0)\) is the finite set of causal effects compatible with the identified component. This equality has been derived from the NPSEM and is not a definition of the causal target as an observed regression functional.

By \(\texttt{lem:fixed-p-gaussian-covariance-calculus}\), \(S_n\in\mathbb S^p_{++}\) almost surely for \(n\geq p\), and \(S_n\to\Sigma_0\) in probability. Positive definiteness and full column rank of each block-incidence matrix make every \(\Gamma_{Ej}(S_n)\) positive definite. Each \(L_E\), and therefore each \(g_E\), is rational and smooth on this cone; acyclicity makes \(I_p-L_E(\Sigma)\) invertible there.

Let
\[
 A_n=\{\mathcal R(\widehat C_n)=\mathcal R(C)\}.
\]
The BIC theorem gives \(\Pr(A_n)\to1\). On \(A_n\), the selected and true components have the same finite labelled state set. Continuity and finiteness imply
\[
 d_H\{\mathcal T_{\widehat C_n}(S_n),\mathcal T_C(\Sigma_0)\}
 \leq\max_{E\in\mathcal R(C)}
 \|g_E(S_n)-g_E(\Sigma_0)\|
 \xrightarrow{\mathbb P}0.
\]
The probability of \(A_n^c\) vanishes, proving unconditional Hausdorff convergence in probability.

For simultaneous coverage retain state and query labels even when numerical effects coincide. A coordinate of \(g_E\) that is structurally constant is zero: at zero structural coefficients it is zero, and if a directed path exists one can make only the coefficients along one such path nonzero, so the coordinate is not constant. Such zero coordinates have exact singleton intervals. For every remaining coordinate \(f=g_{E,k}\), the assumption \(\theta_C\notin\mathcal E_C\) gives \(\mathrm Df(\Sigma_0)\neq0\). The covariance of the Gaussian sample-covariance coordinates is
\[
 (V_0)_{ij,kl}
 =\Sigma_{0,ik}\Sigma_{0,jl}+\Sigma_{0,il}\Sigma_{0,jk}.
\]
By \(\texttt{lem:fixed-p-gaussian-covariance-calculus}\), \(V_0\) is positive definite on nonzero symmetric directions. Thus every nonconstant coordinate has a strictly positive asymptotic variance, its plug-in variance is consistent, and its studentized error converges marginally to \(N(0,1)\).

Define the oracle labelled intervals by applying the same formula to every \(E\in\mathcal R(C)\), regardless of the selected state. On \(A_n\) they coincide exactly with the labelled intervals in \(\mathfrak I_{n,\alpha}(\widehat C_n)\). Let \(K\) be the fixed number of labelled nonconstant coordinates. If \(K\geq1\), each two-sided interval with critical value \(z_{1-\alpha/(2K)}\) has limiting noncoverage at most \(\alpha/K\). The union bound, which does not require a nonsingular joint effect covariance, gives
\[
 \limsup_{\substack{n\to\infty\\ n\geq p}}
 \Pr\{\text{some oracle labelled coordinate is not covered}\}
 \leq\alpha.
\]
If \(K=0\), every interval is exact and the same assertion holds with probability zero on the left. Finally,
\[
 \Pr\{A_n\text{ and all selected labelled coordinates are covered}\}
 \geq1-\Pr(A_n^c)
 -\Pr\{\text{some oracle labelled coordinate is not covered}\}.
\]
Taking lower limits proves the displayed simultaneous bound. Coincident values remain separately labelled, and singularity of their stacked covariance is harmless because the construction uses only positive marginal variances.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The observationally identified object is a finite set of possible effects, so valid inference must cover all component members rather than one selected graph. \textit{Gap.} MaathuisKalischBuhlmann2009 proves consistency of possible-effect multisets without simultaneous intervals, while ChangGuoMalinsky2026 targets coverage of one fixed true effect after graph screening rather than every member of a full-model-equivalence effect set. \textit{Consumer.} An analyst using the Sachs protein network can report simultaneous uncertainty for every total effect compatible with the recovered coefficient-community component.

\begin{openendedquestion}[oeq:output-sensitive-effect-enumeration]\label{oeq:output-sensitive-effect-enumeration}
Using \(\mathsf H_{\mathrm{enum}}\), can one construct a deterministic output-polynomial or polynomial-delay algorithm for the following explicitly charged output? The output must contain one compact arithmetic circuit for each distinct \(g_E\) with \(E\in\mathcal R(C)\), where identity means global rational identity on \(\mathbb S^p_{++}\), the complete list of member states in each identity class, every requested shortest legal-reversal certificate from \(C\), and an exact encoded certificate that the records partition all states in \(\mathcal R(C)\) by functional identity. Total running time is measured against the input encoding plus the total encoding length of all these records; polynomial delay is measured between complete consecutive records and must also account for the record being written. Canonical state enumeration and path-independent partition lifting are already supplied by \(\mathsf H_{\mathrm{enum}}\). The unresolved requirement is deterministic output-sensitive identity and canonicalization for the compact rational circuits after positive-denominator clearing; numerical agreement at finitely many covariance matrices is not an identity certificate.
\end{openendedquestion}
\par\smallskip\noindent \textit{Justification.} After state enumeration is solved, a meaningful output-sensitive effect problem must charge complete class records, including memberships, certificates, and compact rational representatives. \textit{Gap.} Neither PDAG enumeration nor known-graph effect estimation supplies deterministic output-polynomial or polynomial-delay canonicalization of compact rational circuits under global identity, even after all explicit class-record output is charged. \textit{Consumer.} A solution would let large components feed possible-effect analysis through certified rational-functional classes without hiding member lists or shortest certificates outside the complexity measure.

\begin{lemma}[lem:blocked-model-geometry]\label{lem:blocked-model-geometry}
For every \(E\in\mathfrak S_p\), the map \(\phi_E:\Theta_E\to\mathbb S^p_{++}\) is an injective smooth embedding with connected, relatively closed image \(\mathcal M(E)\) of dimension \(p+\sum_{j\in V}|\Pi^E_j|\). At the identity, its differential satisfies
\[
 \operatorname{proj}_{\mathrm{off}}T_{I_p}\mathcal M(E)
 =\operatorname{span}\{v_{jB}:j\in V,\ B\in\Pi^E_j\},
 \qquad (v_{jB})_{\{i,j\}}=\mathbf 1\{i\in B\}.
\]
Moreover, \(\mathcal M(E)\) is the common zero set, inside \(\mathbb S^p_{++}\), of finitely many rational equalities whose denominators are positive principal minors; after clearing those denominators the equalities are polynomial.
\end{lemma}
\begin{proof}
Fix \(E\in\mathfrak S_p\). A topological ordering of the acyclic graph \(G_E\) makes \(B_E\) strictly triangular, so \(B_E^p=0\) and
\[
 (I_p-B_E)^{-1}=I_p+B_E+\cdots+B_E^{p-1}.
\]
Thus \(\phi_E\) is polynomial in \((\beta_E,\omega_E)\). Its value is positive definite because it is the congruence of the positive-definite diagonal matrix \(\operatorname{diag}(\omega_E)\) by the invertible matrix \((I_p-B_E)^{-1}\).

Write \(P_j=\operatorname{pa}_{G_E}(j)\). Under the structural equations associated with \(\texttt{def:covariance-model}\),
\[
 X_j=(B_E)_{jP_j}X_{P_j}+\varepsilon_j.
\]
Every member of \(P_j\) precedes \(j\) topologically and is a function of innovations other than \(\varepsilon_j\). The diagonal innovation covariance therefore gives \(\operatorname{Cov}(X_{P_j},\varepsilon_j)=0\). Since every principal submatrix of a positive-definite covariance is positive definite,
\[
 (B_E)_{jP_j}=\Sigma_{jP_j}\Sigma_{P_jP_j}^{-1},
 \qquad
 \omega_{Ej}=\Sigma_{jj}-\Sigma_{jP_j}\Sigma_{P_jP_j}^{-1}\Sigma_{P_jj},
\]
with the usual empty-parent convention. These formulas recover every block coefficient and innovation variance. They are rational and smooth on \(\mathbb S^p_{++}\), because their denominators are positive principal minors. They define a smooth left inverse to \(\phi_E\) on its image. Hence \(\phi_E\) is injective, its differential is injective, and it is a homeomorphism onto its image; it is therefore a smooth embedding. Its dimension is
\[
 d_E=p+\sum_{j\in V}|\Pi^E_j|,
\]
and its image is connected because \(\Theta_E=\mathbb R^{b_E}\times(0,\infty)^p\) is connected.

For arbitrary \(\Sigma\in\mathbb S^p_{++}\), define
\[
 \widetilde B_{jP_j}(\Sigma)=\Sigma_{jP_j}\Sigma_{P_jP_j}^{-1},
 \qquad
 \widetilde B_{ji}(\Sigma)=0\quad(i\notin P_j),
\]
and
\[
 \widetilde\Omega(\Sigma)
 =(I_p-\widetilde B(\Sigma))\Sigma(I_p-\widetilde B(\Sigma))^{\mathsf T}.
\]
Then \(\Sigma\in\mathcal M(E)\) if and only if
\[
 \widetilde B_{ji}(\Sigma)=\widetilde B_{jk}(\Sigma)
 \quad\text{for every }i,k\in B\in\Pi^E_j,
\]
and every off-diagonal entry of \(\widetilde\Omega(\Sigma)\) vanishes. Necessity is the recovery formula. Conversely, the first family assembles \(\widetilde B\) from one coefficient per prescribed block. Its zero pattern is acyclic, so \(I_p-\widetilde B\) is invertible. The second family makes \(\widetilde\Omega\) diagonal, and its diagonal entries are strictly positive because it is an invertible congruence of \(\Sigma\). Therefore
\[
 \Sigma=(I_p-\widetilde B)^{-1}\widetilde\Omega(I_p-\widetilde B)^{-\mathsf T}\in\mathcal M(E).
\]
The displayed conditions are finitely many continuous rational equalities on \(\mathbb S^p_{++}\), proving relative closedness. Their denominators are products of positive principal minors, so clearing them gives equivalent polynomial equalities on this cone.

Finally, \(I_p\) is obtained at \(\beta_E=0\) and \(\omega_E=(1,\ldots,1)\). Differentiation there gives
\[
 d\Sigma=dB_E+dB_E^{\mathsf T}+\operatorname{diag}(d\omega_E).
\]
The derivative in coordinate \(\beta_{jB}\) is one exactly on the off-diagonal coordinates \(\{\{i,j\}:i\in B\}\), whereas innovation-variance derivatives are diagonal. Projection off the diagonal yields
\[
 \operatorname{proj}_{\mathrm{off}}T_{I_p}\mathcal M(E)
 =\operatorname{span}\{v_{jB}:j\in V,\ B\in\Pi^E_j\},
 \qquad
 (v_{jB})_{\{i,j\}}=\mathbf 1\{i\in B\}.
\]
\end{proof}

\begin{lemma}[lem:edge-coloured-model-equivalence-markov]\label{lem:edge-coloured-model-equivalence-markov}
For blocked states \(C=(G,\Pi)\) and \(D=(H,\Lambda)\), equality \(\mathcal M(C)=\mathcal M(D)\) implies that \(G\) and \(H\) are Markov equivalent; in particular, they have the same skeleton and the same unshielded colliders.
\end{lemma}

\begin{lemma}[lem:verma-pearl-markov-equivalence]\label{lem:verma-pearl-markov-equivalence}
\texttt{Two directed acyclic graphs on the same labelled vertex set are Markov equivalent if and only if they have the same skeleton and the same unshielded colliders.}
\end{lemma}

\begin{lemma}[lem:chickering-monotone-covered-reversals]\label{lem:chickering-monotone-covered-reversals}
If the directed acyclic graphs \(G\) and \(H\) are Markov equivalent and \(\delta(C,D)\) of their common-skeleton edges have opposite orientations, then \(G\) can be transformed into \(H\) by exactly \(\delta(C,D)\) distinct covered-edge reversals. Every intermediate graph is acyclic and Markov equivalent to \(G\).
\end{lemma}

\begin{lemma}[lem:legal-reversal-model-invariance]\label{lem:legal-reversal-model-invariance}
If \(E' = \operatorname{rev}_{i\to j}(E)\) is defined by \(\texttt{def:legal-reversal}\), then \(E'\in\mathfrak S_p\), the reverse transformation \(\operatorname{rev}_{j\to i}(E')\) is defined and equals \(E\), and \(\mathcal M(E')=\mathcal M(E)\). The nonsingleton families are preserved at every vertex.
\end{lemma}
\begin{proof}
Let \(S=\operatorname{pa}_{G_E}(i)\). Definedness in \(\texttt{def:legal-reversal}\) says
\[
 \operatorname{pa}_{G_E}(j)=S\cup\{i\},
 \qquad \{i\}\in\Pi^E_j,
 \qquad \Pi^E_j\setminus\{\{i\}\}=\Pi^E_i.
\]
The reversal preserves acyclicity. Indeed, if replacing \(i\to j\) by \(j\to i\) created a cycle, the old graph would contain a directed path from \(i\) to \(j\) not using \(i\to j\). If \(k\to j\) were the last edge on that path, coveredness would give \(k\in S=\operatorname{pa}_{G_E}(i)\), so the path segment \(i\leadsto k\) followed by \(k\to i\) would already be a directed cycle. The transported families partition the new parent sets. Thus the reversed state \(E'\) belongs to \(\mathfrak S_p\). Its new parent sets and partitions satisfy
\[
 \operatorname{pa}_{G_{E'}}(j)=S,
 \quad \operatorname{pa}_{G_{E'}}(i)=S\cup\{j\},
 \quad \Pi^{E'}_j=\Pi^E_i,
 \quad \Pi^{E'}_i=\Pi^E_i\cup\{\{j\}\},
\]
so the inverse move \(\operatorname{rev}_{j\to i}(E')\) is defined and equals \(E\).

It remains to prove equality of the full covariance images. Conditional on \(X_S\), write the two affected structural equations in \(E\) as
\[
 X_i=a^{\mathsf T}X_S+\varepsilon_i,
 \qquad
 X_j=bX_i+d^{\mathsf T}X_S+\varepsilon_j,
\]
where \(u=\operatorname{Var}(\varepsilon_i)>0\) and \(v=\operatorname{Var}(\varepsilon_j)>0\). The vectors \(a,d\) are each constant on every block of the common partition \(\Pi^E_i\). Define
\[
 t=v+b^2u>0,
 \qquad q=\frac{bu}{t},
 \qquad \eta_j=b\varepsilon_i+\varepsilon_j,
 \qquad \eta_i=\varepsilon_i-q\eta_j.
\]
Direct substitution gives
\[
 \begin{aligned}
 X_j&=(d+ba)^{\mathsf T}X_S+\eta_j,\\
 X_i&=qX_j+\{a-q(d+ba)\}^{\mathsf T}X_S+\eta_i.
 \end{aligned}
\]
Moreover,
\[
 \operatorname{Var}(\eta_j)=t,
 \qquad
 \operatorname{Var}(\eta_i)=\frac{uv}{t}>0,
 \qquad
 \operatorname{Cov}(\eta_i,\eta_j)=bu-qt=0.
\]
The pair \((\eta_i,\eta_j)\) is jointly Gaussian, hence its zero covariance implies independence. It is a linear transformation of \((\varepsilon_i,\varepsilon_j)\), so it is also independent of all remaining innovations. Both \(d+ba\) and \(a-q(d+ba)\) are constant on each block of \(\Pi^E_i\), and \(q\) occupies the transported singleton block \(\{j\}\). Thus the transformed equations are admissible for \(E'\), proving \(\mathcal M(E)\subseteq\mathcal M(E')\). Applying the same construction to the inverse legal move gives the reverse inclusion.

Removing singleton blocks from \(\Pi^{E'}_j=\Pi^E_i\) and \(\Pi^{E'}_i=\Pi^E_i\cup\{\{j\}\}\), while leaving every other partition unchanged, preserves the nonsingleton family at every vertex.
\end{proof}

\begin{lemma}[lem:bpec-proper-identifiability]\label{lem:bpec-proper-identifiability}
\texttt{Within the BPEC class of properly blocked edge-\allowbreak{}coloured directed acyclic graphs,\allowbreak{} equality of covariance models identifies the entire edge-\allowbreak{}coloured directed acyclic graph.}
\end{lemma}

\begin{lemma}[lem:fixed-p-gaussian-covariance-calculus]\label{lem:fixed-p-gaussian-covariance-calculus}
Fix \(p\) and let \(X^{(1)},\ldots,X^{(n)}\) be independent \(N_p(0,\Sigma_0)\) vectors with \(\Sigma_0\in\mathbb S^p_{++}\). If \(n\geq p\), then \(S_n\in\mathbb S^p_{++}\) almost surely. Let \(\operatorname{vech}\) stack the upper-triangular entries of a symmetric matrix, and let \(V_0\) be indexed by unordered pairs with entries \((V_0)_{ij,kl}=\Sigma_{0,ik}\Sigma_{0,jl}+\Sigma_{0,il}\Sigma_{0,jk}\). Then \(S_n\to\Sigma_0\) in \(L^2\), and
\[
 \sqrt n\,\operatorname{vech}(S_n-\Sigma_0)\ \Longrightarrow\ N(0,V_0),
 \qquad
 \operatorname{Cov}(X_iX_j,X_kX_l)=(V_0)_{ij,kl}.
\]
The covariance \(V_0\) is positive definite on nonzero symmetric directions. Consequently every continuously differentiable scalar functional with nonzero derivative at \(\Sigma_0\) obeys the corresponding studentized plug-in delta limit.
\end{lemma}
\begin{proof}
Let \(Z\) be the \(p\)-by-\(n\) matrix whose columns are \(\Sigma_0^{-1/2}X^{(r)}\). Its entries have a joint Lebesgue density. For \(n\geq p\), failure of row rank \(p\) is the common zero set of all \(p\)-by-\(p\) minors; it has Lebesgue measure zero because at least one such determinant polynomial is nonzero. Hence the data matrix has row rank \(p\) almost surely and
\[
 S_n=n^{-1}XX^{\mathsf T}\in\mathbb S^p_{++}
\]
almost surely.

Differentiating the centered Gaussian moment-generating function \(\exp(t^{\mathsf T}\Sigma_0t/2)\) four times at zero gives
\[
 \mathbb E[X_iX_jX_kX_l]
 =\Sigma_{0,ij}\Sigma_{0,kl}
  +\Sigma_{0,ik}\Sigma_{0,jl}
  +\Sigma_{0,il}\Sigma_{0,jk}.
\]
Subtracting \(\mathbb E[X_iX_j]\mathbb E[X_kX_l]\) proves
\[
 \operatorname{Cov}(X_iX_j,X_kX_l)
 =\Sigma_{0,ik}\Sigma_{0,jl}+\Sigma_{0,il}\Sigma_{0,jk}.
\]
Summing the coordinate variances gives \(\mathbb E\|S_n-\Sigma_0\|_F^2=O(n^{-1})\), so \(S_n\to\Sigma_0\) in \(L^2\).

Put \(W_r=\operatorname{vech}(X^{(r)}X^{(r)\mathsf T}-\Sigma_0)\). These vectors are independent and centered, have covariance \(V_0\), and possess finite third moments. For each fixed vector \(t\), Taylor expansion of the characteristic function, with the third-order remainder bounded using \(\mathbb E|t^{\mathsf T}W_1|^3<\infty\), gives
\[
 \mathbb E\exp\{it^{\mathsf T}W_1/\sqrt n\}
 =1-\frac{t^{\mathsf T}V_0t}{2n}+O(n^{-3/2}).
\]
Taking the \(n\)-th power and applying the Cram\'er--Wold criterion yields
\[
 \sqrt n\,\operatorname{vech}(S_n-\Sigma_0)\Longrightarrow N(0,V_0).
\]

For any nonzero symmetric matrix \(A\), the corresponding quadratic-form direction has variance
\[
 \operatorname{Var}(X^{\mathsf T}AX)
 =2\operatorname{tr}\{(\Sigma_0^{1/2}A\Sigma_0^{1/2})^2\}>0.
\]
Thus \(V_0\) is positive definite on nonzero symmetric directions. If \(f\) is continuously differentiable, Taylor's formula gives
\[
 \sqrt n\{f(S_n)-f(\Sigma_0)\}
 =\mathrm Df(\Sigma_0)[\sqrt n(S_n-\Sigma_0)]+o_{\mathbb P}(1).
\]
The preceding multivariate limit gives the delta-method normal law. The formula for \(V_0\) is polynomial in \(\Sigma_0\), so replacing \(\Sigma_0\) by \(S_n\) is consistent. When \(\mathrm Df(\Sigma_0)\neq0\), positive definiteness makes the limiting variance strictly positive, and division by its plug-in standard error gives the studentized standard normal limit.
\end{proof}

\begin{lemma}[lem:pdag-consistent-extension-enumeration]\label{lem:pdag-consistent-extension-enumeration}
For a partially directed acyclic graph on \(p\) vertices and \(m\) edges, there is an algorithm that enumerates every consistent directed-acyclic extension exactly once after \(O(p^3)\) initialization, with \(O(p+m)\) worst-case delay.
\end{lemma}
\par\smallskip\noindent \textit{Justification.} This published algorithm supplies only the extension-enumeration complexity used after the local canonical-lift theorem. \textit{Gap.} The cited theorem enumerates PDAG extensions but has no coefficient-block state or partition-lift statement. \textit{Consumer.} The canonical-lift proposition transfers the algorithm to blocked covariance-equivalence components.

\begin{proposition}[prop:canonical-pdag-component-lift]\label{prop:canonical-pdag-component-lift}
Fix \(C\in\mathfrak S_p\), let \(\mathcal P_C\) be the canonical background PDAG in \(\texttt{def:canonical-background-pdag}\), and let \(m=|\operatorname{skel}(G)|\). For every \(B\in\mathcal N_j(C)\) and \(i\in B\), \(\mathcal N_i(C)\neq\mathcal N_j(C)\) and \(i\to j\in F(C)\). Consequently every consistent DAG extension \(H\) of \(\mathcal P_C\) is in the domain of the canonical lift \(\mathsf L_C\), and \(H\mapsto\mathsf L_C(H)\) is a bijection from the consistent extensions of \(\mathcal P_C\) onto \(\mathcal R(C)\). The lift is therefore path independent. The canonical procedure in \(\operatorname{Enum}(C)\) emits every component state exactly once after \(O(p^3)\) initialization and with \(O(p+m)\) worst-case delay, including output-linear partition lifting. If a shortest legal-reversal certificate from \(C\) is emitted with each state, a direct covered-edge scan gives the safe bound \(O(p^5)\) per output and returns exactly \(\delta(C,D)\) legal reversals for output \(D\). Moreover, \(|\mathcal R(C)|\leq p!\), and this bound is sharp for the complete-skeleton all-singleton state. The legal-reversal breadth-first search remains a complete fallback and returns the same shortest certificates.
\end{proposition}
\begin{proof}
For \(j\in V\), abbreviate
\[
 \mathcal N_j=\mathcal N_j(C),
 \qquad U_j=\bigcup_{B\in\mathcal N_j}B.
\]
If \(B\in\mathcal N_j\) and \(i\in B\), then \(i\to j\) in \(G\). If \(\mathcal N_i=\mathcal N_j\), then \(B\in\mathcal N_i\); but every member of \(\mathcal N_i\) is contained in \(\operatorname{pa}_G(i)\), whereas \(i\in B\), contradicting acyclicity and the absence of self-loops. Therefore
\[
 \mathcal N_i\neq\mathcal N_j,
 \qquad i\to j\in F(C).
\]

The ordinary CPDAG of \(G\) represents exactly the DAGs Markov equivalent to \(G\). The PDAG \(\mathcal P_C\) additionally fixes each orientation in \(F(C)\) as in \(G\); this background knowledge is compatible because \(G\) itself is an extension. Let \(H\) be a consistent DAG extension of \(\mathcal P_C\). The preceding display forces \(i\to j\) in \(H\) for every \(i\in B\in\mathcal N_j\), so
\[
 U_j\subseteq\operatorname{pa}_H(j).
\]
The members of \(\mathcal N_j\) are pairwise disjoint. Hence
\[
 \Lambda^H_j=\mathcal N_j\cup
 \{\{i\}:i\in\operatorname{pa}_H(j)\setminus U_j\}
\]
is a partition of \(\operatorname{pa}_H(j)\). Since \(H\) is acyclic, \(\mathsf L_C(H)=(H,\Lambda^H)\) lies in \(\mathfrak S_p\) and has nonsingleton family \(\mathcal N_j\) at every vertex. It has the skeleton and unshielded colliders of \(G\) and respects all orientations in \(F(C)\); therefore
\[
 \mathsf S(\mathsf L_C(H))=\mathsf S(C).
\]
The equivalence theorem gives \(\mathsf L_C(H)\in\mathcal R(C)\).

Conversely, if \(D=(H,\Lambda)\in\mathcal R(C)\), the equivalence theorem gives \(\mathsf S(D)=\mathsf S(C)\). Thus \(H\) is Markov equivalent to \(G\), respects all background arrows in \(F(C)\), and is a consistent extension of \(\mathcal P_C\). Its nonsingleton family at \(j\) is \(\mathcal N_j\), while every parent outside \(U_j\) must form a singleton block. Consequently \(\Lambda_j=\Lambda^H_j\), so \(D=\mathsf L_C(H)\). The graph coordinate makes the lift injective, and the two directions make it surjective. The displayed formula depends only on \(H\) and the fixed families, proving path independence.

Apply \(\texttt{lem:pdag-consistent-extension-enumeration}\) to \(\mathcal P_C\). Its \(O(p^3)\) initialization includes passage to the equivalent Meek-closed MPDAG, after which it emits every consistent extension exactly once with \(O(p+m)\) delay. The lift scans the \(p\) vertices and \(m\) parent incidences once, reuses the fixed nonsingleton blocks, and appends the singleton remainder blocks. Its time and representation size are \(O(p+m)\), so composition preserves exact-once output and \(O(p+m)\) delay without state memoization.

For a shortest certificate to an emitted \(D=(H,\Lambda)\), let \(K\) be the current DAG. If \(K\neq H\), apply \(\texttt{lem:chickering-monotone-covered-reversals}\) to \(K,H\). Its first reversal shows that some currently disagreeing arrow is covered; scanning at most \(m\) arrows and comparing parent-incidence rows finds one in \(O(mp)\) time. Reverse it and repeat. Each step reduces the disagreement count by one, so there are exactly \(\delta(C,D)\leq m\) steps and total time \(O(m^2p)=O(p^5)\). Every arrow in \(F(C)\) has the same orientation in the current graph and \(H\), so none is reversed. The legality argument in \(\texttt{thm:complete-blocked-equivalence}\), applied to the canonical lift at each step, makes every reversal a defined legal move. The resulting state path has length \(\delta(C,D)\), which that theorem proves minimal.

For the cardinality bound, assign each component DAG its lexicographically first topological order. A fixed skeleton together with a topological order determines all arrow orientations, so this assignment is injective. The lift gives one state per graph, and there are \(p!\) total orders; hence \(|\mathcal R(C)|\leq p!\). For sharpness take a complete skeleton with all blocks singleton. Each total order produces a distinct transitive tournament. A complete skeleton has no unshielded collider, while \(\mathcal N_j=\varnothing\) and \(F(C)=\varnothing\) at every vertex, so all \(p!\) states have the same static signature and lie in one component.

Finally, the breadth-first fallback applies exactly the reversible legal moves in \(\texttt{def:legal-reversal}\). Its exact visited set is the connected component \(\mathcal R(C)\), so finiteness gives termination and no duplication. Breadth-first predecessors minimize move count, and the equivalence theorem identifies that count with \(\delta(C,D)\).
\end{proof}
\par\smallskip\noindent \textit{Justification.} The static equivalence criterion becomes an exact output-sensitive state enumerator only after consistent graph extensions are shown to possess one canonical partition lift. \textit{Gap.} The cited PDAG algorithm has no partition data; the local proof supplies the lift, legal shortest certificates, and the sharp blocked-component bound. \textit{Consumer.} Component software can enumerate blocked states without testing path-dependent partition transports.

\begin{lemma}[lem:exact-global-rational-effect-identity]\label{lem:exact-global-rational-effect-identity}
For any \(E,E'\in\mathcal R(C)\), equality \(g_E=g_{E'}\) as rational maps on \(\mathbb S^p_{++}\) is decidable by a deterministic exact finite procedure: write each coordinate over products of its positive block-Gram determinants, cross-multiply, and test whether every coefficient of each resulting numerator polynomial is zero. The expanded coefficient tables are exact identity certificates; agreement at finitely many numerical covariance matrices is not such a certificate.
\end{lemma}
\begin{proof}
Fix \(E,E'\in\mathcal R(C)\).  We first construct an exact rational representation of \(g_E\); the same construction applies to \(E'\).

For a vertex \(j\) having nonempty parent set \(P_{Ej}\), write
\[
 \Delta_{Ej}(\Sigma)=\det\Gamma_{Ej}(\Sigma),
 \qquad
 \Gamma_{Ej}(\Sigma)=A_{Ej}^{\mathsf T}
 \Sigma_{P_{Ej},P_{Ej}}A_{Ej}.
\]
The columns of \(A_{Ej}\) are linearly independent.  Indeed, if \(A_{Ej}u=0\), then, for a row indexed by any \(i\in B\in\Pi^E_j\), that row equals the coordinate \(u_B\); since every block is nonempty, every coordinate of \(u\) is zero.  Because \(\Sigma\in\mathbb S^p_{++}\), every principal submatrix \(\Sigma_{P_{Ej},P_{Ej}}\) is positive definite.  Consequently, for every nonzero \(u\),
\[
 u^{\mathsf T}\Gamma_{Ej}(\Sigma)u
 =(A_{Ej}u)^{\mathsf T}
   \Sigma_{P_{Ej},P_{Ej}}(A_{Ej}u)>0.
\]
Thus \(\Gamma_{Ej}(\Sigma)\) is positive definite and
\(\Delta_{Ej}(\Sigma)>0\) throughout \(\mathbb S^p_{++}\).  This proves, rather than assumes, the condition needed for division below.

By \(\texttt{def:off-model-block-ls}\), the constrained coefficient vector is
\[
 \beta_{Ej}(\Sigma)
 =\Gamma_{Ej}(\Sigma)^{-1}h_{Ej}(\Sigma)
 =\frac{\operatorname{adj}(\Gamma_{Ej}(\Sigma))
              h_{Ej}(\Sigma)}{\Delta_{Ej}(\Sigma)}.
\]
Every entry of \(\Gamma_{Ej}\) and \(h_{Ej}\) is a polynomial, in fact a linear polynomial, in the \(p(p+1)/2\) free symmetric entries of \(\Sigma\).  The adjugate formula therefore shows that every entry of row \(j\) of \(L_E(\Sigma)\) is a rational function with polynomial numerator and, when the parent set is nonempty, denominator \(\Delta_{Ej}\).  An empty-parent row is identically zero and introduces no denominator.

Since the nonzero pattern of \(L_E(\Sigma)\) is contained in the edge set of the acyclic graph \(G_E\), a topological permutation makes \(L_E(\Sigma)\) strictly triangular.  Hence \(L_E(\Sigma)^p=0\), and direct multiplication gives
\[
 (I_p-L_E(\Sigma))
 \left(I_p+L_E(\Sigma)+\cdots+L_E(\Sigma)^{p-1}\right)
 =I_p-L_E(\Sigma)^p=I_p.
\]
Thus
\[
 (I_p-L_E(\Sigma))^{-1}
 =I_p+L_E(\Sigma)+\cdots+L_E(\Sigma)^{p-1}.
\]
It follows from \(\texttt{def:component-effect-set}\) that each reported coordinate \(g_{E,k}\) has an exactly computable representation
\[
 g_{E,k}(\Sigma)=\frac{P_{E,k}(\Sigma)}{Q_{E,k}(\Sigma)},
\]
where \(P_{E,k}\) and \(Q_{E,k}\) are polynomials and \(Q_{E,k}\) is a finite product, allowing repeated factors, of the determinants \(\Delta_{Ej}\).  Therefore \(Q_{E,k}(\Sigma)>0\) on \(\mathbb S^p_{++}\).  All additions and multiplications involved are finite because \(p\) and \(\mathcal Q\) are finite.  Applying the same argument to \(E'\), and cross-multiplying, yields
\[
 g_{E,k}(\Sigma)-g_{E',k}(\Sigma)
 =\frac{N_k(\Sigma)}{Q_{E,k}(\Sigma)Q_{E',k}(\Sigma)},
\]
where
\[
 N_k=P_{E,k}Q_{E',k}-P_{E',k}Q_{E,k}
\]
is a polynomial and the displayed denominator is strictly positive on the positive-definite cone.  Hence
\[
 g_{E,k}(\Sigma)=g_{E',k}(\Sigma)
 \text{ for every }\Sigma\in\mathbb S^p_{++}
 \quad\Longleftrightarrow\quad
 N_k(\Sigma)=0
 \text{ for every }\Sigma\in\mathbb S^p_{++}.
\]

We now justify the polynomial-identity step.  Regard a symmetric matrix as a vector in \(\mathbb R^d\), where \(d=p(p+1)/2\).  The cone \(\mathbb S^p_{++}\) is nonempty, since it contains \(I_p\), and open: if \(\Sigma\) is positive definite, continuity of the quadratic form on the unit sphere gives \(c=\min_{\lVert x\rVert=1}x^{\mathsf T}\Sigma x>0\), and every symmetric perturbation \(H\) with operator norm less than \(c\) satisfies \(x^{\mathsf T}(\Sigma+H)x>0\) for every nonzero \(x\).  A polynomial on \(\mathbb R^d\) that vanishes on a nonempty open set is the zero polynomial.  For completeness, choose an open box inside that set and induct on \(d\).  The case \(d=1\) is the elementary fact that a nonzero univariate polynomial has only finitely many roots.  For the induction step, expand in the last variable.  Fixing the first \(d-1\) variables in the corresponding open box gives a univariate polynomial vanishing on an interval, so each coefficient polynomial vanishes throughout an open box in \(\mathbb R^{d-1}\); the induction hypothesis makes every coefficient identically zero.  Applying this fact to \(N_k\) proves
\[
 N_k(\Sigma)=0
 \text{ for every }\Sigma\in\mathbb S^p_{++}
 \quad\Longleftrightarrow\quad
 \text{every monomial coefficient of }N_k\text{ is zero}.
\]

This gives the promised deterministic exact finite procedure.  For every reported coordinate, form the block Gram matrices and score vectors symbolically, use adjugates to form the rational entries of \(L_E\) and \(L_{E'}\), evaluate the finite nilpotent sums, cross-multiply their positive determinant products, expand each \(N_k\) in the standard monomial basis, and test its finitely many exact coefficients.  The maps \(g_E\) and \(g_{E'}\) are identical exactly when all of these coefficient tables are zero.  The expanded tables, together with the displayed symbolic construction, are checkable exact identity certificates; a nonzero listed coefficient certifies nonidentity.  Mere agreement at a finite list of numerical covariance matrices does not imply the required polynomial identity: without an additional proved interpolation argument, a nonzero polynomial can vanish at every point of any prescribed finite list.  No bound polynomial in a compact arithmetic-circuit encoding is asserted.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The residual effect-enumeration question needs an exact equality notion and certificate before its complexity can be posed honestly. \textit{Gap.} Numerical coincidence is not rational identity, while compact-circuit canonicalization complexity is not supplied by the state-enumeration literature. \textit{Consumer.} A finite exact postprocessor can partition all enumerated states by global equality of their total-effect maps.

\section{Interpretation}

Repeated coefficient blocks do two jobs. They survive every observationally equivalent representation, and each of their arrows becomes background knowledge because a parent contained in a repeated block cannot have the same block family as the target. After those arrows are fixed, the remaining graph uncertainty is precisely an ordinary Markov-equivalence extension problem, and the endpoint parent sets determine all residual singleton blocks uniquely. This orientation information is structural: an arrow compelled by the PDAG or by \(F(C)\) does not by itself identify its numerical coefficient.

\section*{Technical internal limitation (diagnostic only)}

The population component \(\mathcal R(C)\) is an equivalence class of complete covariance-model images, not the pointwise fibre of every state fitting one exceptional covariance. Zero edges and coincident coefficients can create additional pointwise representations outside the component. The set \(F(C)\) records orientations forced directly by unequal nonsingleton families, while collider, acyclicity, and Meek-closure constraints can compel further arrows; neither kind of compelled arrow alone identifies a numerical coefficient. Canonical PDAG enumeration is linear-delay in state outputs. Exact global rational identity is decidable by denominator clearing and coefficient expansion, but expansion can be exponentially larger than compact circuits and can undergo cancellation. No deterministic output-polynomial total-time or polynomial-delay guarantee is claimed for complete functional-class records, even though their member lists, exact certificates, and requested shortest paths are now included in the output-size measure.

\section{Honest scope}

The equivalence theorem concerns finite labelled causally sufficient linear Gaussian DAGs, target-local coefficient blocks, unrestricted real block coefficients, unrestricted positive target-specific innovation variances, and full positive-definite covariance images. It excludes cross-target edge colours, vertex-coloured innovation variances, latent confounding, selection, cycles, and pointwise supermodel fibres. State enumeration uses the canonical CPDAG-plus-\(F(C)\) PDAG, with \(O(p^3)\) initialization, \(O(p+m)\) delay, and output-linear partition lifting; attaching a shortest legal-reversal certificate has the safe \(O(p^5)\) per-output bound. The sharp state-count bound is \(p!\), attained by the complete all-singleton state. Exact quotienting of compact rational effect circuits is finite, but no deterministic output-sensitive bound is shown when complexity is measured against the input plus complete class records, including memberships and certificates. BIC recovery and confidence coverage are fixed-\(p\), pointwise, and generic away from a proper algebraic exceptional set; no greedy-search consistency or uniform inference near model intersections is asserted.

\begin{thebibliography}{99}

  \bibitem{BoegeKubjasMisraSolus2026} Tobias Boege, Kaie Kubjas, Pratik Misra, and Liam Solus. Coloured Gaussian Directed Acyclic Graphical Models. Journal of the Royal Statistical Society: Series B, 88, 2026. DOI: 10.1093/jrsssb/qkaf068; arXiv:2404.04024v3.

  \bibitem{Chickering1995} David Maxwell Chickering. A Transformational Characterization of Equivalent Bayesian Network Structures. Proceedings of the Eleventh Conference on Uncertainty in Artificial Intelligence, 87--98, 1995.

  \bibitem{VermaPearl1990} Thomas Verma and Judea Pearl. Equivalence and Synthesis of Causal Models. Proceedings of the Sixth Conference on Uncertainty in Artificial Intelligence, 220--227, 1990.

  \bibitem{WuDrton2023} Jun Wu and Mathias Drton. Partial Homoscedasticity in Causal Discovery with Linear Models. IEEE Journal on Selected Areas in Information Theory, 4:1077--1090, 2023. arXiv:2308.08959.

  \bibitem{MakamReichenbachSeigal2023} Visu Makam, Philipp Reichenbach, and Anna Seigal. Symmetries in Directed Gaussian Graphical Models. Electronic Journal of Statistics, 17:3905--3946, 2023. arXiv:2108.10058.

  \bibitem{AnderssonMadiganPerlman1997} Steen A. Andersson, David Madigan, and Michael D. Perlman. A Characterization of Markov Equivalence Classes for Acyclic Digraphs. Annals of Statistics, 25:505--541, 1997.

  \bibitem{WienobstBannachLiskiewicz2023} Marcel Wienöbst, Max Bannach, and Maciej Liśkiewicz. Polynomial-Time Algorithms for Counting and Sampling Markov Equivalent DAGs. Journal of Machine Learning Research, 24:1--45, 2023. arXiv:2205.02654.

  \bibitem{WienobstLuttermannBannachLiskiewicz2023} Marcel Wienöbst, Malte Luttermann, Max Bannach, and Maciej Liśkiewicz. Efficient Enumeration of Markov Equivalent DAGs. AAAI Conference on Artificial Intelligence, 12313--12320, 2023. arXiv:2301.12212.

  \bibitem{Haughton1988} Dominique M. A. Haughton. On the Choice of a Model to Fit Data from an Exponential Family. Annals of Statistics, 16:342--355, 1988.

  \bibitem{MaathuisKalischBuhlmann2009} Marloes H. Maathuis, Markus Kalisch, and Peter Bühlmann. Estimating High-Dimensional Intervention Effects from Observational Data. Annals of Statistics, 37:3133--3164, 2009. arXiv:0810.4214.

  \bibitem{ChangGuoMalinsky2026} Ting-Hsuan Chang, Zijian Guo, and Daniel Malinsky. Post-Causal-Discovery Inference. Biometrika, 2026. DOI: 10.1093/biomet/asaf073; arXiv:2405.06763.

  \bibitem{GuoPerkovic2022EfficientLS} F. Richard Guo and Emilija Perković. Efficient Least Squares for Estimating Total Effects under Linearity and Causal Sufficiency. Journal of Machine Learning Research, 23:1--41, 2022.

\end{thebibliography}

\end{document}