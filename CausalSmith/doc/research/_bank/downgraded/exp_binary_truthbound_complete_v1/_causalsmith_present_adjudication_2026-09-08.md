# Presentation adjudication — 2026-09-08

## P1 checkpoint

- Accepted the 20 `notation-mutual-definition` advisories. They are stale-home false alarms: the generated Lean-backed setup definitions `synth_1` and `synth_2` introduce the global experiment symbols (`p_z`, `v_z`, `v_{z,i}`, `x_i`, and related carriers) before any audited environment uses them. The formal layer's actual order therefore satisfies definition-before-use even though the original notation-table home rows point to later domain-specific environments.
- Accepted the six `notation-home-is-result` advisories. The generated Lean-backed definitions `synth_9`, `synth_12`, `synth_15`, and `synth_16` themselves define the flagged symbols before the named theorem results; no result is being relied on as a definition.
- The Lean equivalence judge found all 31 paper environments faithful after convergence and froze their bodies onto the bank graph.
- The outline contains no standard-literature theorem or lemma used only as motivation. The school-network handle and exposure-block item are retained solely as a clearly labelled open construction in the discussion, consistent with the accepted scope.
- Acknowledged the P2 ballast gate for `prop:variance-estimability`, `thm:aronow-samii-specialization`, `thm:tensorization`, `thm:studentized-coverage`, `def:product-experiment`, and `def:wald-interval`: the first four are delivered characterization/benchmark/scalability/inference results, and the two definitions are required to state the latter results.
- After promotion round 1 added `lem:numerator-symmetric-band-liminf`, the P1 delta notation review exposed four Lean-oriented renderings. Replaced `Fin(B)` by the ordinary index set `1,...,B`, `rowLaw_B` by the mathematical design-law symbol `P_B`, Lean real-coercion syntax by ordinary division, and the product-score coordinate coercion by an explicit prose convention that binary coordinates are regarded as real numbers. The product amendment changes notation only and preserves the matched Lean statement.
- P2 review: front matter contains contributions and a roadmap, related work is the first substantive section, all twelve proofs open with `Proof of \cref{obj:...}`, and all twelve proof audits are faithful. Moved the product setup and promoted liminf lemma out of the quadratic-benchmark section and into products/studentization, immediately before the coverage result that consumes the lemma.
- P3 manual repairs after two automatic revisions: replaced dropped key `AronowSamii2013` with verified `AronowSamii2017Interference`; changed “characterizes a complement-symmetry mechanism” to the proved “gives complement-symmetry conditions”; inserted the accepted limitations sentence verbatim.
- The first P2 regeneration correctly rewrote sections 04 and 06 but exposed a frozen-layer-order mismatch because the edited outline had not yet been propagated through P1. Re-entered at P1 to rebuild the global formal-layer order before reassembly; no mathematical statement changed.
- The P1 order repair still pulled the promoted lemma and its product setup into the quadratic section because the lemma reused `c`, already the paper's design-mean score vector, as its band radius. Renamed only the paper-level radius to `γ` (an alpha-renaming of the Lean binder), eliminating the notation collision so the lemma and its setup can remain in products/studentization.
- Pipeline-bug stop at P3: the remaining `cite-pool` finding is inside the frozen body of `def:aronow-samii-binary-correction`. Editing the authored section to use `AronowSamii2017Interference` and re-entering with `--from P2 --reassemble --auto` reproduced the same finding because P2 mechanically restored the old frozen body, after which P3 failed with `P3 revision round 1 returned no replacements`. No identical retry was attempted.
- Superseding diagnosis after independent pipeline audit: `AronowSamii2013` is the scientifically correct variance-result citation. The actual defect was P0 accepting a top-ranked no-DOI title-search mismatch; after the audited identity-safe lookup repair, direct P0 replay verified `AronowSamii2013` and the P3 cite-pool failure cleared. The frozen 2013 citation is retained.
- P4 exposed a malformed assumption-provenance injection for Billingsley (author parsed as `Patrick` and the full locator embedded in the title). Replaced it with the exact third-edition book metadata and DOI already present in `references_raw.bib`. Also restored the accepted limitations sentence verbatim after P1 regenerated the outline, and alpha-renamed the liminf proof's paper variable from `c` to `γ` to match its statement.

## Accepted scope caveats (must remain verbatim in the paper limitations)

finite binary schedules; exponential finite program; separation on an engineered six-coordinate witness; scalable school-network computation, connected-interference inference, and finite-sample coverage remain open.

## P5 frozen-layer adjudication

- Corrected six reader-facing environments without changing object IDs, Lean declarations, hypotheses, conclusions, or proof sources: `def:bound-program`, `synth_21`, `prop:rational-certificates`, `thm:aronow-samii-specialization`, `thm:quartic-separation`, and `thm:tensorization`. The set `{b ∈ E : b ≥ f}` is an affine feasible set when `f` is nonzero, so “conservative cone” was replaced by “conservative feasible set.”
- Corrected the centered witness coordinate in `thm:quartic-separation` from the tautology `x_i(theta)=x_i` to its existing Lean definition `x_i(theta)=2 theta_i-1`.
- Synchronized the accepted bank graph, `p1_cache.json` synthesized definition, `formal_layer.json`, and authored section/proof prose. Existing audit keys and verdicts were left untouched; production key-oracle replays predict five P1 statement audits and all twelve P2 proof audits because each proof key includes the full formal layer.
- Citation metadata only: replaced the invalid Billingsley DOI in `Helpers/CitedGates.lean` with the directly verified primary locator A. W. van der Vaart (1998), Section 2.8, Proposition 2.27, pp. 20–21. The `LindebergFellerIndependentArray` declaration type is unchanged.
