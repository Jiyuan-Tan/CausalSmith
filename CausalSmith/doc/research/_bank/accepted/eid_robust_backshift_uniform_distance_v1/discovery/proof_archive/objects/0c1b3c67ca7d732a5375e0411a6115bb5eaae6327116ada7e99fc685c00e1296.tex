The finite enumeration in \(\text{def:subset-consensus}\) was reduced to exactly \(\binom{m}{h}\) calls to an assumed full-solution feasibility oracle, but that definition does not implement the oracle. The analytic constraints can be split into finitely many polynomial branches, yet such a split alone supplies neither verified projection nor exact output codes. A future universal four-margin audit may conservatively certify unknown true-side margins and abstain at candidate zero separation, but the proved local argument shows that candidate affine separation is not needed for contraction around a fixed true instance with \(\gamma_H\geq\gamma_0\).