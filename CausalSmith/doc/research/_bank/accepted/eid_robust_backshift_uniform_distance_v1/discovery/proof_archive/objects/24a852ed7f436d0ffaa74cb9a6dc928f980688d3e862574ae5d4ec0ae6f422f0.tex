The strongest closed result is the following. By \(\text{thm:confidence-union-coverage}\), arbitrary nonempty positive-definite simultaneous covariance regions give the set-theoretic implication \(G_{n,\alpha,H}\subseteq\{D\in\mathcal U^{(n)}_\alpha\}\). By \(\text{thm:non-effective-four-margin-contraction}\), declarative affine-separation, normalization-slack, structural-conditioning, and scale bounds on the true instance and every feasible candidate yield an existential \(r_0>0\) and the explicit implications
\[
r_{n,\alpha,H}\leq r_0
\Longrightarrow
\operatorname{outer}_{\mathrm{op}}(\mathcal U^{(n)}_\alpha,D)
\leq C_0r_{n,\alpha,H},
\qquad
\operatorname{diam}_{\mathrm{op}}(\mathcal U^{(n)}_\alpha)
\leq2C_0r_{n,\alpha,H}.
\]
The radius is a positive compactness minimum and is not effectively computed. The definition \(\text{def:subset-consensus}\) supplies the exact finite count \(\binom{m}{h}\) only for calls to an assumed full-solution feasibility oracle; it does not implement that oracle. No covariance-interiority margin is used or claimed.