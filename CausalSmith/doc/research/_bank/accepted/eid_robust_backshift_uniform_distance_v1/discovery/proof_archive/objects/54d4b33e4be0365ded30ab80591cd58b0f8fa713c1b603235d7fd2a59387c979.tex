Exact projection, finite-sample coverage, universal-audit exactness, and abstention at a zero affine minor are the conclusions of \(\text{thm:certified-inference-representation-obstruction}\).  The two quantitative inequalities, effective positivity of \(r_0\), uniformity, and the restriction to audit-release outcomes are exactly \(\text{thm:audited-release-contraction}\).

It remains to justify the population classification and chart assertions.  When every population covariance has rational entries, define the direct population formula
\[
 \Phi_{\Sigma}(A)\quad\Longleftrightarrow\quad
 \bigvee_{\substack{K\subseteq E\\|K|=h}}
 \exists\Psi\,\exists(t_e)_{e\in K}\,
 \left[
 \begin{array}{l}
 A\in\mathcal D,\ \Psi\in\mathbb S_+^p,\ t_e\in[0,\infty)^p,\\
 A\Sigma_eA^{\mathsf T}=\Psi+\operatorname{diag}(t_e)\quad(e\in K)
 \end{array}
 \right].
\]
After expanding \(A\in\mathcal D\) and the positive-semidefinite cone as in \(\text{thm:certified-inference-representation-obstruction}\), this is a first-order formula with rational-polynomial atoms.  By \(\text{def:compatible-set}\), its realization is exactly \(\mathcal R_c(\Sigma)\), including when a rational covariance is singular.  Its emptiness is decided by
\[
 \neg\exists A\,\Phi_{\Sigma}(A),
\]
its singleton property by
\[
 \exists A\,\Phi_{\Sigma}(A)
 \ \wedge\ 
 \forall A_1,A_2,
 \bigl(\Phi_{\Sigma}(A_1)\wedge\Phi_{\Sigma}(A_2)\bigr)
 \Longrightarrow A_1=A_2,
\]
and the existence of multiple candidates by
\[
 \exists A_1,A_2,
 \quad \Phi_{\Sigma}(A_1)\wedge\Phi_{\Sigma}(A_2)
 \wedge\sum_{i,j}(A_{1,ij}-A_{2,ij})^2>0.
\]
These three mutually exclusive alternatives exhaust every set.  Each display is a closed first-order rational-polynomial sentence, so \(\text{lem:effective-real-quantifier-elimination}\) terminates and decides it.  The quantifier-free output, together with the finite affine-minor sign formula in the representation theorem, is the asserted rational certificate.

Finally, the defining feasibility formula is already global in the entries of \(A\), including unit diagonal, determinant nonvanishing, and every cycle-product inequality, so it invokes no local normalization chart.  If an auxiliary chart has rational transition functions, split its domain into the finitely many sign cells on which every denominator is nonzero, adjoin variables for the chart coordinates and polynomial equations obtained by multiplying by those denominators, and retain their nonzero sign conditions.  Projection back to \(A\) by effective real quantifier elimination gives exactly the graph image on each cell.  Taking the finite disjunction over cells and charts therefore leaves the matrix-space realization unchanged.  This proves the qualified chart-switching assertion.  The final scope sentence merely records that neither cited theorem claims audit passage or software implementation.