It remains open to construct and formally verify a terminating real-closed-field quantifier-elimination, sign-determination, and root-isolation program that emits exact quantifier-free codes for \(\mathcal U^{(n)}_\alpha\), classifies rational inputs as empty, singleton, or multiple, verifies a release-or-abstain policy, computes an effective positive radius, automates normalization-chart changes, and supports a corruption-aware \(\texttt{backShift}\) implementation.