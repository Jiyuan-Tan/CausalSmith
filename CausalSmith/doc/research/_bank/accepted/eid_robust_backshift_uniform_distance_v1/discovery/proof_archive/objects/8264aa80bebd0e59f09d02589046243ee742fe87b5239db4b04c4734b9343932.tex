The finite reduction in \(\text{def:subset-consensus}\) was expanded: it makes exactly \(\binom{m}{h}\) calls to an oracle returning full solution sets, but that oracle is definitional and provides neither a terminating solver nor a quantifier-free output code. The analytic constraints can be decomposed into finitely many polynomial equalities and inequalities after choosing finite cycle and affine-minor branches, but the present core contains no verified elimination substrate that preserves exact set realization, decides the universal candidate-side four-margin premise, or extracts an effective lower bound from the far-family residual. Thus the remaining step requires new formal-algebraic and software work, not another consequence of the existing compactness proof.