For population identification only the covariance family \(\Sigma\) is observed. At each index \(n\), any sampling mechanism may be used to construct the indexed nonempty regions \(\mathcal C^{(n)}_{e,\alpha}\subseteq\mathbb S_+^p\); the coverage theorem assumes only the stated simultaneous honest-slot event and measurability for its probability implication.