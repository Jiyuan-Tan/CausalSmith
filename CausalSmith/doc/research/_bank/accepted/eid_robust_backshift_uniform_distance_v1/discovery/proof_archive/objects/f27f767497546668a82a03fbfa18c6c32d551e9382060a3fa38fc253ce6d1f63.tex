Fix \(n\geq1\), \(\alpha\in(0,1)\), and a sample outcome in \(G_{n,\alpha,H}\). For \(e\in H\), put \(\Gamma_e=\Sigma_e\). For each \(e\in E\setminus H\), choose \(\Gamma_e\in\mathcal C^{(n)}_{e,\alpha}\), which is possible because every encoded region is nonempty. Take
\[
A=D,\qquad K=H,\qquad \Psi=\Omega,\qquad t_e=s_e\quad(e\in H).
\]
The definition of \(H\) gives \(|K|=h\). Assumption \(\text{ass:backshift-normalization}\) gives \(A\in\mathcal D\); the theorem-local premise gives \(\Psi=\Omega\in\mathbb S_+^p\); and \(\text{ass:nonnegative-shifts}\) gives \(t_e\in[0,\infty)^p\). On the coverage event, \(\text{ass:honest-covariance-model}\) gives
\[
\Gamma_e=Sigma_e
=D^{-1}(\Omega+\operatorname{diag}(s_e))D^{-\mathsf T}
=A^{-1}(\Psi+\operatorname{diag}(t_e))A^{-\mathsf T}
\quad(e\in K).
\]
Thus \(D\in\mathcal R_c(\Gamma)\) by \(\text{def:compatible-set}\), and \(D\in\mathcal U^{(n)}_\alpha\) by \(\text{def:confidence-union}\). Therefore
\[
G_{n,\alpha,H}\subseteq\{D\in\mathcal U^{(n)}_\alpha\}.
\]
Monotonicity of probability proves the stated coverage implication.

Now fix any realized encoded region family. For each \(K\subseteq E\) with \(|K|=h\), expand \(F_K\) from \(\text{def:certificate-inference-handle}\). All covariance equations are entrywise polynomial equations. Invertibility is expressed by \((\det A)^2>0\). If \(\mathfrak C_p\) is the finite set of directed simple cycles and \(\pi_C(A)\) is the corresponding entry product in \(I_p-A\), then
\[
\operatorname{CP}(I_p-A)<1
\quad\Longleftrightarrow\quad
1-\pi_C(A)>0\ \text{ and }\ 1+\pi_C(A)>0
\quad(C\in\mathfrak C_p).
\]
Positive semidefiniteness of \(\Psi\) is the first-order condition
\[
\forall v\in\mathbb R^p,\qquad v^{\mathsf T}\Psi v\geq0,
\]
and positive definiteness of \(\Gamma_e\) is
\[
\forall v\in\mathbb R^p,\qquad
\left(\sum_jv_j^2=0\right)\ \vee\ v^{\mathsf T}\Gamma_ev>0.
\]
The region code is a finite Boolean combination of rational-polynomial sign conditions, while unit diagonal, shift nonnegativity, and the transformed covariance equations are polynomial conditions. Hence every \(F_K\) is a first-order ordered-field formula with rational-polynomial atoms.

Let
\[
\Phi_{\mathcal U}(A)
\quad\Longleftrightarrow\quad
\bigvee_{\substack{K\subseteq E\\|K|=h}}
\exists\Psi\,\exists(t_e)_{e\in K}\,\exists(\Gamma_e)_{e\in K}\,F_K.
\tag{20}
\]
If \(A\in\mathcal U^{(n)}_\alpha\), \(\text{def:confidence-union}\) and \(\text{def:compatible-set}\) supply a full region selection and a compatible set \(K_0\) of size at least \(h\). Restricting its witnesses to any \(h\)-element subset \(K\subseteq K_0\) proves (20). Conversely, witnesses for one disjunct in (20) give
\[
\Gamma_e=A^{-1}(\Psi+\operatorname{diag}(t_e))A^{-\mathsf T},
\qquad e\in K,
\]
and therefore put \(A\) in \(\mathcal R_c(\Gamma)\). Choosing arbitrary region members outside \(K\), possible by nonemptiness, completes the covariance family and gives \(A\in\mathcal U^{(n)}_\alpha\). Consequently
\[
\{A:\Phi_{\mathcal U}(A)\}=\mathcal U^{(n)}_\alpha.
\tag{21}
\]
There are exactly \(\binom mh\) disjuncts. Applying \(\text{lem:effective-real-quantifier-elimination}\) to (20) terminates and produces a finite quantifier-free rational-polynomial code whose realization is exactly (21).

Each audit condition is rational semialgebraic. When \(h-c\geq3\), affine separation is exactly
\[
\bigwedge_{k<\ell}\ \bigwedge_{\substack{S\subseteq K\\|S|=h-c}}
\ \bigvee_{e<f<g\in S}
\left(\Delta^{k\ell}_{efg}(t)\geq\gamma_0
\ \vee\ -\Delta^{k\ell}_{efg}(t)\geq\gamma_0\right);
\]
when \(h-c<3\), it is false because \(\gamma(w)=0<\gamma_0\). Cycle slack is the finite conjunction \(|\pi_C(A)|\leq1-\zeta_0\). Under the repaired gauge-invariant definition, covariance interiority is exactly
\[
\Gamma_e-\lambda_0I_p\succeq0\qquad(e\in K),
\tag{22}
\]
with each cone inequality written by a universally quantified quadratic form. There is no condition \(\Psi-\lambda_0I_p\succeq0\): feasibility requires only \(\Psi\succeq0\).

The condition-number bound is equivalent to
\[
\exists q>0\ \forall v\in\mathbb R^p,\qquad
q\|v\|_2^2\leq\|Av\|_2^2
\leq\overline\kappa^2q\|v\|_2^2.
\]
Indeed, the displayed inequalities imply \(\|A\|_{\mathrm{op}}\leq\overline\kappa\sqrt q\) and \(\|A^{-1}\|_{\mathrm{op}}\leq q^{-1/2}\); conversely take \(q=\|A^{-1}\|_{\mathrm{op}}^{-2}\). Finally, \(\|X\|_{\mathrm{op}}\leq r\) is equivalent to
\[
r\geq0,
\qquad
\forall v\in\mathbb R^p,\quad \|Xv\|_2^2\leq r^2\|v\|_2^2.
\]
Introducing three nonnegative bounds for \(\Psi\), the family \((\Gamma_e)\), and the family \((\operatorname{diag}(t_e))\), whose sum plus one is at most \(\overline M\), therefore expresses \(M(w)\leq\overline M\). The converse direction chooses the three actual finite maxima, so this representation is exact.

It follows that the sentence asserting the existence of a feasible tuple violating at least one audit bound is a closed first-order rational-polynomial sentence. Effective real quantifier elimination decides it. It is false exactly when all feasible tuples pass affine separation, cycle slack, covariance interiority (22), structural conditioning, and scale. Thus \(\text{CertificateInferenceHandle}\) returns the exact code (21) after a clean universal audit and returns \(\mathsf{ABSTAIN}\) otherwise. A feasible tuple with \(\gamma(w)=0\) necessarily triggers abstention because \(\gamma_0>0\).

For rational population \(\Sigma\), expand \(\text{def:compatible-set}\) into the first-order rational-polynomial predicate \(\Phi_\Sigma(A)\), using
\[
\Sigma_e=A^{-1}(\Psi+\operatorname{diag}(t_e))A^{-\mathsf T}
\quad\Longleftrightarrow\quad
A\Sigma_eA^{\mathsf T}=\Psi+\operatorname{diag}(t_e).
\]
The three closed formulas
\[
\neg\exists A\,\Phi_\Sigma(A),
\]
\[
\exists A\left[\Phi_\Sigma(A)\wedge
\forall A'\left(\Phi_\Sigma(A')\Rightarrow
\sum_{i,j}(A_{ij}-A'_{ij})^2=0\right)\right],
\]
and
\[
\exists A\,\exists A'\left[\Phi_\Sigma(A)\wedge\Phi_\Sigma(A')\wedge
\sum_{i,j}(A_{ij}-A'_{ij})^2>0\right]
\]
say, respectively, that \(\mathcal R_c(\Sigma)\) is empty, a singleton, or has at least two elements. Exactly one holds by elementary set trichotomy, and each is decidable by \(\text{lem:effective-real-quantifier-elimination}\).

The free coordinates of \(\Phi_{\mathcal U}(A)\) are the entries of \(A\), so no normalization chart is used. On a rational chart cell where \(A_{ij}=P_{ij}(u)/Q_{ij}(u)\) and the sign of the nonzero denominator is fixed, the relation is equivalent to
\[
Q_{ij}(u)A_{ij}-P_{ij}(u)=0
\]
together with that denominator-sign condition. Adjoining these polynomial graph equations and projecting out \(u\) preserves exactly the represented matrix set; effective quantifier elimination and the finite union over sign cells prove the chart assertion.

Finally, \(\text{lem:finite-membership-query-obstruction}\) supplies the claimed finite-query limitation. This theorem is representational: it supplies no inverse joint-diagonalization modulus by itself. The structural target \(B=I_p-D\) was fixed independently of the observed-region functional \(\mathcal U^{(n)}_\alpha\); the first paragraph derived their coverage relation from the structural assumptions rather than defining them to be equal.