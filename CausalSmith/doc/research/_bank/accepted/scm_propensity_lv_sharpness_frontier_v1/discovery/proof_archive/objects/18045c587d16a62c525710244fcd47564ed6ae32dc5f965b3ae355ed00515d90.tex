Intersect the three equal classes in \(\mathrm{thm:conditional\text{-}one\text{-}sided\text{-}bow\text{-}mixture\text{-}completeness}\) with the reverse-support condition \(Q_a(x,\cdot)\ll P_a(x,\cdot)\) for both arms almost everywhere.  The graphical and mixture intersections are respectively \(\mathfrak M_{\mu_X,\boldsymbol e}(\boldsymbol P)\) and \(\mathfrak L_{\mu_X,\boldsymbol e}(\boldsymbol P)\) by \(\mathrm{prop:conditional\text{-}mutual\text{-}support\text{-}intersection}\).  The cap class already has \(P_a(x,\cdot)\ll Q_a(x,\cdot)\), so its intersection is exactly the displayed mutual-absolute-continuity cap class.  The simultaneous construction in the one-sided theorem uses the prescribed \(R_0,R_1\); when each residual is absolutely continuous with respect to its observed kernel, the support-intersection proposition shows that the same construction is a mutual-support witness.