In the conditional setup define \(f_{a,x}(t)=(t-1/e_a(x))_+\).  The map \((a,x,t)\mapsto f_{a,x}(t)\) is jointly Borel, each fiber is finite, continuous, and convex with \(f_{a,x}(1)=0\), and its calibrated radius is \(B_{f_{a,x}}(e_a(x))=0\).  The fiberwise constraints
\[
 P_a(x,\cdot)\ll Q_a(x,\cdot),\qquad
 D_{f_{a,x}}(P_a(x,\cdot)\|Q_a(x,\cdot))\le0
\]
for both arms \(\mu_X\)-almost everywhere cut out exactly \(\mathfrak M^{\to}_{\mu_X,\boldsymbol e}(\boldsymbol P)\); adding \(Q_a(x,\cdot)\ll P_a(x,\cdot)\) cuts out exactly \(\mathfrak M_{\mu_X,\boldsymbol e}(\boldsymbol P)\).  No integrated divergence surrogate is used.  If two nonnull strata have distinct propensity values, no single fixed finite continuous convex generator is universally exact at both, whereas this adaptive family is simultaneously exact in one global SCM.