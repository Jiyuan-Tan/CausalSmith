The class \(\mathfrak L_{\mu_X,\boldsymbol e}(\boldsymbol P)\) consists of the pairs \(\boldsymbol Q\in\mathfrak L^{\to}_{\mu_X,\boldsymbol e}(\boldsymbol P)\) having a residual-kernel representation in which \(R_a(x,\cdot)\ll P_a(x,\cdot)\) for both arms and \(\mu_X\)-almost every \(x\).