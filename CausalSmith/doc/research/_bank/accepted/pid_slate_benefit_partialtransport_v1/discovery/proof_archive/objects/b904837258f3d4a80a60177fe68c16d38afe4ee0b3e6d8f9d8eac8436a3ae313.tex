If a map is Hadamard directionally differentiable at the population value, the centered and scaled plug-in estimator equals the directional derivative applied to the centered and scaled first-stage estimator plus a term converging to zero in probability; hence first-stage weak convergence transfers through that directional derivative.