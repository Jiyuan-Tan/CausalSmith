---
qid: stat_pomdp_latent_overlap_minimax
spec: v1
topic: "The cost of hidden state under uniformly bounded stationary policy overlap. sampling: One stationary trajectory (X_t,A_t,Y_t)_{t=1}^T; joint state S_t=(X_t,H_t) has arbitrary finite state space, with no fixed bound on either cardinality; H_t never observed. All finite observed alphabets and known policies are supplied to the estimator; latent alphabet, transition/reward kernel and stationary distributions are unknown. This is a union of finite models; constants in the requested risk comparison must not depend on cardinalities. causal_kernel: A time-homogeneous joint kernel K(dy,dx_next,dh_next | x,h,a), as Hu–Wager Model 3. Under behavior, A_t is drawn from the known policy b(.|X_t), independently of all past and H_t conditional on X_t. Target e(.|X_t) is known, memoryless in the observed state. Rewards satisfy |Y_t|<=1. policy_overlap: For fixed zeta>0, e(a|x)<=exp(zeta)b(a|x) for all a,x (zero behavior cells therefore have zero target probability). Take a fixed binary action alphabet. Both policies may vary across instances and are revealed to the estimator. mixing: For fixed t0>0 and p in {b,e}, the induced joint-state kernel P_p satisfies ||nu P_p - nu_prime P_p||_TV <= exp(-1/t0)||nu-nu_prime||_TV for every pair of state distributions. Let d_b,d_e be their unique stationary joint-state laws; observed data start from d_b. latent_stationary_overlap: For a fixed C>1 independent of T and all state cardinalities, d_e(s)<=C d_b(s) at every joint state s=(x,h). This is a structural class restriction; neither d_b nor d_e nor their ratio is revealed. Do not replace it by overlap of observed X marginals. estimand: theta(K,e)=sum_s d_e(s) sum_a e(a|x) E_K[Y|s,a], the stationary mean outcome if target policy e were deployed. risk: R_T(t0,zeta,C)=inf over all measurable estimators based only on the observed trajectory and known (b,e,t0,zeta,C), sup over the displayed class, E[(theta_hat-theta)^2]. Supremum includes all finite state cardinalities and known-policy pairs obeying restrictions. Determine the exact asymptotic order of R_T(t0,zeta,C), with an explicit attaining observable-data estimator and a matching lower bound against all measurable estimators, for fixed positive t0,zeta and C>1. Characterize whether bounded latent stationary occupancy removes, reduces, or leaves unchanged the partial-observability penalty present in Hu–Wager. Rate and all boundary/logarithmic factors are outputs, not stipulated answers. All constants may depend on t0,zeta,C but not T or cardinality. A one-sided lower construction or a reuse of the unrestricted-class PHIW upper bound without matching the restricted-class converse does not deliver the kernel. No proxy informativeness, emission-rank, finite-memory, or known latent dynamics assumptions may be inserted. Consumer: Hu–Wager Section5.2 applies PHIW to the Luckett et al. (2019) mobile-health insulin-policy simulator calibrated to Maahs et al. (2012), with dietary intake hidden and ten-day hourly glucose trajectories. This result changes whether a substantive assurance that a candidate insulin policy stays within the logging policy's latent physiological occupancy can justify shorter history weights or faster precision, or whether collecting state/proxy information remains necessary. The paper-shaped deliverable must demonstrate that implication on a finite-state discretized version of this published application; it must not claim the original continuous simulator already satisfies the finite-state uniform contraction/overlap class. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A signed-depth reset chain keeps the same depth occupancy under both policies while treatment preserves a hidden sign and control erases it. Its target-to-behavior stationary ratios remain at most 1+epsilon, so epsilon can be fixed below C-1 independently of history length. A single noisy reward at terminal depth gives a target-value separation proportional to alpha^Q. The observed-filter argument uses the fact that terminal arrival forces the preceding Q rewards to be fair; a two-stage posterior bound suggests observed-path KL of order T alpha^(2Q) p^Q without revealing reset times. Balancing Q suggests the unchanged Hu-Wager risk exponent 2/(2+t0*zeta) for every fixed C>1, whereas C=1 permits parametric immediate weighting. Exhaustive small-path checks support normalization, occupancy and divergence scaling; a refreshed 360-state insulin-policy adaptation shows finite occupancy and nonzero immediate-weight bias. UNRESOLVED BOTTLENECK: Independently verify the uniform terminal-posterior bound and chain-rule KL, including stationary pre-sample signs and every initial window. EARLY KILL TEST: Re-derive this observed-data KL bound before any formalization; a missing inverse-history factor or need to reveal resets invalidates the proposed lower construction. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_pomdp_latent_overlap_minimax.md."
novelty_target: field
banked_novelty_tier: field
tier_at_proposal: ACCEPT
tier_at_derivation: PASS
proposal_promise_gap: null
reusable: unknown
reraise_status: retry
gap_reasons:
  - "None: Stage -0.5 returned ACCEPT at field tier and Stage 0.5 returned PASS; no conjecture collapsed."
reusable_artifacts:
  - Causalean/Mathlib/Probability/FiniteMarkovOscillation.lean
  - Causalean/Mathlib/InformationTheory/FiniteWordChainRule.lean
seeds_burned: []
proof_attempt_summary: |
  The formalization proves matching minimax lower and PHIW upper rates for fixed latent
  stationary overlap, the shrinking-overlap and C = 1 frontiers, and a finite insulin-policy
  certificate. The proof closes the observed-path KL argument through an explicit finite-word
  disintegration and keeps the two positive-horizon assumptions visible; all five headline
  theorems are source-clean and use only standard Lean axioms.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 422365685
  pipeline_claude_tokens: 221300045
  pipeline_tokens_consumed: 643665730
  total_tokens_consumed: null
banked_on: "2026-09-12"
paper_score: 6.4
paper_score_rationale: "The verified all-radius minimax frontier is a substantial contribution, but the manuscript requires major restructuring and several foundational clarifications before its scope and argument are publication-ready."
---

# stat_pomdp_latent_overlap_minimax / v1 — Accepted

**Topic.** The cost of hidden state under uniformly bounded stationary policy overlap. sampling: One stationary trajectory (X_t,A_t,Y_t)_{t=1}^T; joint state S_t=(X_t,H_t) has arbitrary finite state space, with no fixed bound on either cardinality; H_t never observed. All finite observed alphabets and known policies are supplied to the estimator; latent alphabet, transition/reward kernel and stationary distributions are unknown. This is a union of finite models; constants in the requested risk comparison must not depend on cardinalities. causal_kernel: A time-homogeneous joint kernel K(dy,dx_next,dh_next | x,h,a), as Hu–Wager Model 3. Under behavior, A_t is drawn from the known policy b(.|X_t), independently of all past and H_t conditional on X_t. Target e(.|X_t) is known, memoryless in the observed state. Rewards satisfy |Y_t|<=1. policy_overlap: For fixed zeta>0, e(a|x)<=exp(zeta)b(a|x) for all a,x (zero behavior cells therefore have zero target probability). Take a fixed binary action alphabet. Both policies may vary across instances and are revealed to the estimator. mixing: For fixed t0>0 and p in {b,e}, the induced joint-state kernel P_p satisfies ||nu P_p - nu_prime P_p||_TV <= exp(-1/t0)||nu-nu_prime||_TV for every pair of state distributions. Let d_b,d_e be their unique stationary joint-state laws; observed data start from d_b. latent_stationary_overlap: For a fixed C>1 independent of T and all state cardinalities, d_e(s)<=C d_b(s) at every joint state s=(x,h). This is a structural class restriction; neither d_b nor d_e nor their ratio is revealed. Do not replace it by overlap of observed X marginals. estimand: theta(K,e)=sum_s d_e(s) sum_a e(a|x) E_K[Y|s,a], the stationary mean outcome if target policy e were deployed. risk: R_T(t0,zeta,C)=inf over all measurable estimators based only on the observed trajectory and known (b,e,t0,zeta,C), sup over the displayed class, E[(theta_hat-theta)^2]. Supremum includes all finite state cardinalities and known-policy pairs obeying restrictions. Determine the exact asymptotic order of R_T(t0,zeta,C), with an explicit attaining observable-data estimator and a matching lower bound against all measurable estimators, for fixed positive t0,zeta and C>1. Characterize whether bounded latent stationary occupancy removes, reduces, or leaves unchanged the partial-observability penalty present in Hu–Wager. Rate and all boundary/logarithmic factors are outputs, not stipulated answers. All constants may depend on t0,zeta,C but not T or cardinality. A one-sided lower construction or a reuse of the unrestricted-class PHIW upper bound without matching the restricted-class converse does not deliver the kernel. No proxy informativeness, emission-rank, finite-memory, or known latent dynamics assumptions may be inserted. Consumer: Hu–Wager Section5.2 applies PHIW to the Luckett et al. (2019) mobile-health insulin-policy simulator calibrated to Maahs et al. (2012), with dietary intake hidden and ten-day hourly glucose trajectories. This result changes whether a substantive assurance that a candidate insulin policy stays within the logging policy's latent physiological occupancy can justify shorter history weights or faster precision, or whether collecting state/proxy information remains necessary. The paper-shaped deliverable must demonstrate that implication on a finite-state discretized version of this published application; it must not claim the original continuous simulator already satisfies the finite-state uniform contraction/overlap class. PRESOLVE EVIDENCE REQUIRING VERIFICATION: A signed-depth reset chain keeps the same depth occupancy under both policies while treatment preserves a hidden sign and control erases it. Its target-to-behavior stationary ratios remain at most 1+epsilon, so epsilon can be fixed below C-1 independently of history length. A single noisy reward at terminal depth gives a target-value separation proportional to alpha^Q. The observed-filter argument uses the fact that terminal arrival forces the preceding Q rewards to be fair; a two-stage posterior bound suggests observed-path KL of order T alpha^(2Q) p^Q without revealing reset times. Balancing Q suggests the unchanged Hu-Wager risk exponent 2/(2+t0*zeta) for every fixed C>1, whereas C=1 permits parametric immediate weighting. Exhaustive small-path checks support normalization, occupancy and divergence scaling; a refreshed 360-state insulin-policy adaptation shows finite occupancy and nonzero immediate-weight bias. UNRESOLVED BOTTLENECK: Independently verify the uniform terminal-posterior bound and chain-rule KL, including stationary pre-sample signs and every initial window. EARLY KILL TEST: Re-derive this observed-data KL bound before any formalization; a missing inverse-history factor or need to reveal resets invalidates the proposed lower construction. STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_pomdp_latent_overlap_minimax.md.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** PASS

**Banking reason.** F5 clean after current-hash dual-model convergence review; all five headline theorems are complete, source-clean, and use only standard axioms.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
