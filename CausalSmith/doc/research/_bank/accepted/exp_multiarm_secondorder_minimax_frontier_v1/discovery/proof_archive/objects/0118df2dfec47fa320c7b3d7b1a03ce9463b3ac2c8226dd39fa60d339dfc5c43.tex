First take \(K=2\) and \(c=(1,-1)\). Directly from the orbit target,
\[
\tau_c(m)
=\frac1n\sum_{t\in\{0,1\}^2}m_t(t_1-t_2)
=\frac{m_{(1,0)}-m_{(0,1)}}n
=\frac{p_+-p_-}{n}.
\]
The two zero-effect response types contribute zero to this contrast, and \(r_0=m_{(0,0)}+m_{(1,1)}\). This aggregation is valid for the target only. In the orbit likelihood, the row constraints \(\sum_a h_{t,a}=m_t\) and the factorial factors \(m_t!\) remain indexed by each \(t\in\{0,1\}^2\). Hence \(m_{(0,0)}\) and \(m_{(1,1)}\) remain separate coordinates of \(P_m(x\mid r)\); no individual-effect aggregation is being asserted for that kernel.

The exact response-type theorem gives
\[
\rho_n((1,-1))=G_n((1,-1)).
\]
The cited published two-arm theorem identifies this unrestricted minimax value, not the preceding four-count likelihood, with the scalar-experiment value based on
\[
X_2=p_++\operatorname{Binomial}(r_0,1/2)
\]
for estimating \((p_+-p_-)/n\). Here \(L_c=2\), so \(C_0(c)=1\). The embedded-two-arm theorem, equivalently the cited expansion in this normalization, gives
\[
d_n(c)=\frac1n-\rho_n(c)
=C_A n^{-4/3}+o(n^{-4/3}).
\]
This proves the two-arm sanity and regression clause while keeping the orbit likelihood and scalar value representation logically distinct.

For every fixed \(K\ge3\), every nonzero zero-sum contrast \(c\), every assignment law on \(\mathcal A_K^n\), and every possibly biased estimator in the frozen decision class, the exact response-type theorem proves
\[
\rho_n(c)=G_n(c),
\]
where nature ranges over \(m\in\mathcal M_{n,K}\), the orbit simplex generated by all \(2^K\) binary response types. The cited published result concerns only the two-arm problem. Therefore the equality for every fixed \(K\ge3\) is a strict extension in treatment-domain scope. Nothing in this argument excludes a different scalar representation of a multi-arm minimax value, so the final nonrepresentability disclaimer is preserved.