The primary design class is every law \(\mathcal D\in\Delta(\mathcal A_K^n)\) chosen before the fixed response schedule is revealed.  The first-order upper procedure assigns independently with \(q_a^\star=|c_a|/L_c\) on \(S_c\).  For the converse, any such \(K\)-arm design is pushed forward to the binary sign-group assignment and Rao--Blackwellized over latent arm labels; outcomes attached to inactive arms are discarded and replaced by the fixed embedded value zero, so the induced two-arm procedure receives no extra information.