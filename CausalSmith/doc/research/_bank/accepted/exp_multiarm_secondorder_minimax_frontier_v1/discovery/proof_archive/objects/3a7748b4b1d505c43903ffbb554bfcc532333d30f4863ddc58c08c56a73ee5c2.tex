It remains open to prove which regularly varying normalizer, if any, makes \(a_nd_n(c)\) converge to a finite positive limit, and to derive convergence, boundary and corner compatibility, and uniqueness for the corresponding limiting value problem.  For \(c^\dagger\), one must additionally prove whether the finite-information advantage over the scalar score survives at that normalization.