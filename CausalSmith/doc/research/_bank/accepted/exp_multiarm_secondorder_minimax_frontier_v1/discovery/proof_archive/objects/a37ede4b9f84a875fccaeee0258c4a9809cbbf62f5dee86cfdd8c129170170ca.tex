The exact orbit-game theorem guarantees finite minimax procedures abstractly, but it does not expose a matched computable upper--lower pair.  The posterior completion and grid-distance argument provide that missing certificate without assuming the second-order exponent.