The exact response-type game, the embedded two-arm procedure reduction, \(\rho_n(c)\geq C_0(c)\rho_{n,2}\), exact equality for \(|S_c|=2\), the contrast-weighted first-order rule, and the universal \(n^{-4/3}\) ceiling are theorem-level claims.  Support-two contrasts inherit the published Bernoulli/nonlinear-shrinkage rule, least-favorable converse, and Airy correction after rescaling.  For \(|S_c|\geq3\), no positive second-order constant, normalizer, limiting operator, analytic shrinkage rule, or correction to independent \(q^\star\) is claimed.  The \(c^\dagger\) scalar-score result proves only that one data reduction loses finite-sample minimax information, and the \(n\leq5\) rational certificates are finite diagnostics only.  ACTG 175 receives a computable finite game, the first-order benchmark, and the ceiling, not a completed multivariate second-order analysis.