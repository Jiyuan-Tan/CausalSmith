\textbf{Proof.}
The support-two assertion is the first part of \(\mathrm{thm:attainment\text{-}and\text{-}k3\text{-}certified\text{-}converse}\). Indeed, if the active coefficients are \(\alpha\) and \(-\alpha\), then \(\alpha=L_c/2>0\), \(C_0(c)=\alpha^2\), and the definition of \(q^\star\) gives probability \(1/2\) on each active arm. That theorem lifts the balanced two-arm posterior-mean procedure and, separately, the internally constructed complete-schedule least-favorable prior. Its Bayes-risk lower bound applies to every assignment law, including laws dependent across units, and every possibly biased estimator. The matching upper and lower values are \(\alpha^2\rho_n((1,-1))\). Substitution of the published two-arm Airy expansion yields
\[
 d_n(c)
 =C_0(c)\{n^{-1}-\rho_n((1,-1))\}
 =C_0(c)C_A n^{-4/3}+o(n^{-4/3}).
\]
Inactive arms need no positivity because their coefficients vanish and the attaining design never assigns them.

For arbitrary support, the second part of the same theorem invokes the explicit construction and proof in \(\mathrm{thm:universal\text{-}second\text{-}order\text{-}rate}\). It supplies a finite threshold after which the displayed clipped-shrinkage risk bound holds. Combining that upper procedure with the unrestricted embedded two-arm converse gives the displayed positive liminf and finite limsup. Therefore \(d_n(c)=\Theta(n^{-4/3})\), which proves that the exponent is \(4/3\) without assuming convergence of the normalized sequence.

Now suppose \(|S_c|\geq3\). The first part of \(\mathrm{thm:second\text{-}order\text{-}rate\text{-}and\text{-}certificate\text{-}frontier}\) proves from those two-sided bounds that the subsequential-limit set of \(n^{4/3}d_n(c)\) is nonempty and compact in \((0,\infty)\). For rational \(c\), its exact-rational certificate comparison at \(M=n\) gives
\[
 B_{n,c}(\nu_{n,n,c})\leq\rho_n(c)\leq U_{n,n}(c)
 \leq\Lambda_{n,n}(c)
 \leq B_{n,c}(\nu_{n,n,c})+\frac{C_0(c)}{4n^2}.
\]
Multiplication by \(n^{4/3}\) makes the total width at most \(C_0(c)n^{-2/3}/4=o(1)\). Hence the normalized grid, procedure, prior, and minimax improvements are pairwise \(o(1)\) apart and have the same subsequential limits and convergence behavior.

For real \(c\), the same frontier theorem applies the established continuity-transfer certificate with rational zero-sum approximants satisfying \(L_{c-c^{(n)}}/2=o(n^{-5/6})\). With \(M=n\), the transferred risk-bracket width is \(o(n^{-4/3})\). After multiplication by \(n^{4/3}\), each endpoint is therefore \(o(1)\) from the normalized minimax improvement, which proves the asserted subsequential and convergence transfer. The finite-program construction has one constraint for every response-count vector, permits zero mass on allocation orbits, and omits only zero-denominator posterior terms; consequently none of exposure positivity, full allocation support, rank, or overlap is required.

Finally, the \(K=3\), \(c=c^\dagger\) specialization, including \(C_0(c^\dagger)=1\), the gap \(1/(4M^2)\), and the diagnostic calculations through \(n=5\), is the final part of \(\mathrm{thm:attainment\text{-}and\text{-}k3\text{-}certified\text{-}converse}\). Both invoked theorems expressly stop short of a cross-\(n\) optimizer or prior limit and of an analytic limiting equation. Thus the last paragraph records the exact residual frontier and makes no claim that any of those unresolved objects exists. \(\square\)