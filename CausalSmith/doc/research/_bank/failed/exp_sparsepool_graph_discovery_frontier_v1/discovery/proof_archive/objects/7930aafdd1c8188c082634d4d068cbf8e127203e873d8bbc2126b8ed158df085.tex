For the Dantzig upper bound, apply the simultaneous-certificate theorem with its rowwise noise error parameter set to \(\eta/2\) and its pool-construction failure parameter set to \(\eta/2\).  Its union bound then gives graph error at most \(\eta\), while its sample-size expression is bounded by \(U_{\mathrm D}\) after changing the universal constant.  The feasible region of the second Dantzig program is a nonempty polyhedron and its differentiable objective has a finite minimum, so the polyhedral normal-cone optimality condition supplies dual multipliers and zero duality gap at \(\theta_i^{\mathrm D}\).  Choosing that unique exact target is therefore a certified output with zero gap.  Its uniqueness also makes it a measurable function of the finite-dimensional data, so this proves an admissible measurable upper-bound witness without a computational-complexity assertion.

The partially penalized-Lasso lemma supplies a second admissible decoder with graph error at most \(\eta\) whenever \(m\ge U_{\mathrm L}\), again after a universal adjustment of \(C\).  Taking the better of the two legal decoders proves
\[
 m^*\le\left\lceil\min\{U_{\mathrm D},U_{\mathrm L}\}\right\rceil
\]
from the definition of \(m^*\).  In particular, \(U_{\mathrm L}\) contains no factor \(s+1\) multiplying \(\sigma^2/\delta^2\); its separate empirical-dual term is exactly the cost isolated in the proof of that lemma.

The adaptive information lower-bound theorem applies to every admissible recovery-valid design-decoder pair on the Gaussian-noise subclass of \(\mathcal M_s\), and therefore gives
\[
 m^*\ge c\,\frac{\sigma^2\{\log(n-1)+\log(1/\eta)\}}{q\delta^2}.
\]
Its second conclusion gives the stated probability-one noiseless degree-one obstruction.  The fixed-design rank obstruction proved in this round gives \(m\ge2s+2\) whenever \(\kappa_s(Z)>0\).  These three arguments establish every asserted inequality and qualifier.  Because none supplies a recovery-valid adaptive lower bound matching the sparse exploration terms in all regimes, the final sentence records a genuine residual rather than claiming a sharp order not proved by the frozen core.