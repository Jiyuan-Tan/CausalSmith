Apply the balanced-pool uniform concentration lemma.  A vector admitted in the definition of \(\kappa_s(Z)\) has at most \(2s+1\) nonzero coordinates, so the first conclusion of that lemma gives \(\kappa_s(Z)^2\ge c_0q\).  Under the proposed condition \(n\ge9\), the factor-three cone conclusion gives \(\phi_s(Z)^2\ge c_0q\) directly from the definition of \(\phi_s(Z)\).  Renaming the universal constants proves the probability assertion.  To generate one row, draw a uniform random permutation of \(\mathcal U\) and retain its first \(B\) entries; a direct permutation shuffle takes \(O(n)\) real-arithmetic operations, so all \(m\) rows take \(O(mn)\) operations.