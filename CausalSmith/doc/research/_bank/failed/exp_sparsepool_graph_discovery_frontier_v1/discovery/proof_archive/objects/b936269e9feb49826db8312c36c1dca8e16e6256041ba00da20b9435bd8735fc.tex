The proved result is the two-sided bracket in \(\mathrm{thm{:}sharp\text{-}pool\text{-}complexity\text{-}bracket}\): the better of the certified-output Dantzig bound \(U_{\mathrm D}\) and the exact partially penalized-Lasso bound \(U_{\mathrm L}\) is a valid upper bound, while \(c\sigma^2\{\log(n-1)+\log(1/\eta)\}/(q\delta^2)\) is a valid lower bound for every adaptive recovery-valid design.  The Lasso removes \(s+1\) from the noise-to-signal term but retains an \((s+1)q^{-1}\log(8n^3/\eta)\) empirical strict-dual-feasibility cost.  The noiseless degree-one and fixed-design rank obstructions are also proved.  No all-regime matching adaptive converse or certified polynomial-time finite-precision decoder follows from these results.