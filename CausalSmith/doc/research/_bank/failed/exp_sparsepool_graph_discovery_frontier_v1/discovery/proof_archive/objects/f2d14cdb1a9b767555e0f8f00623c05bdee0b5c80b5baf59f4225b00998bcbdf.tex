For each \(i\in\mathcal U\), compute \(v_i=\min_{u\in\mathcal D_i}\lVert u\rVert_1\) by linear programming, and define \(\theta_i^{\mathrm D}=\arg\min\{\lVert u\rVert_2^2/2:u\in\mathcal D_i,\ \lVert u\rVert_1\le v_i\}\), which is unique. In the Blum--Shub--Smale real-arithmetic model with oracle access to the observed real coordinates, return a feasible \(\widehat\theta_i\) when the primal--dual gap for this strongly convex quadratic program is at most \(\varepsilon_{\mathrm{comp}}^2/2\). When \(r_m<\delta/2\), use the explicit coordinate tolerance \(\varepsilon_{\mathrm{comp}}=(\delta-2r_m)/8\).