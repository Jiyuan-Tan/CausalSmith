---
qid: pid_inspection_separable_switch_frontier
spec: v1
topic: "Ordinary new-topic generation after the mandated revival_remaining.sh count printed zero. This is not a revival of the rejected known-generator commutator: the target is a sharp partially identified continuous-time component risk when only grouped factual event laws are observed. Retain the exact active-ceiling finite-piece theorem over every compatible measurable hazard model; inactive-ceiling pulse bounds, interval existence, mesh rates and generic confidence inversion cannot substitute for it. MODEL: Fixed known T>0,L>0, arbitrary finite fixed grid 0=t0<...<tK=T. iid subjects, A randomized Bernoulli(1/2), no covariates or additional censoring. Absorbing first-event system: four deterministic Lebesgue-measurable cause-specific hazards lambda_j^a:[0,T]->[0,L], a in {0,1}, j in {1,2}. Under component intervention (aY,aD), cause1 hazard is lambda1^aY and cause2 hazard lambda2^aD; survival is exp(-integral(lambda1^aY+lambda2^aD)). This hazard-splicing restriction defines the causal model; no latent independence beyond what realizes these intervention laws is asserted. All such quadruples are allowed. Observe only A and first-failure inspection bin plus adjudicated cause, or survival to T; retain neither exact failure time nor within-bin order. The population observation is factual conditional bin triples z_ak=(p_ak,q_ak,s_ak), summing to1. Survival is positive because hazards are bounded. Restrict z to compatible observations. Target psi=integral_0^T exp(-Lambda1^1(t)-Lambda2^0(t)) lambda1^1(t)dt, and SDE=psi-observed arm0 cause1 risk. No sensitivity analysis: the hazard-component model is maintained and coarsening produces partial identification. KERNEL: Conjecture over every L,T,K,grid and compatible z: derive a sharp constructive representation of the full attainable hybrid incidence-survival frontier for ONE bin, sufficient to evaluate its support envelope H_z(v)=sup(r+v*s) and lower envelope h_z(v)=inf(r+v*s), for v in [0,1]. Here (r,s) is the hybrid cause1 incidence and end-survival within that bin, generated by four bounded hazards reproducing both factual triples. These suprema define the object, NOT the promised solution. The hard assertion is that exact envelopes admit a uniformly finite switching representation: there exists a universal finite integer B, independent of z,L,bin width, such that each envelope value is attained by compatible piecewise-constant hazard quadruples with at most B common subintervals; levels may be anywhere in [0,L], not necessarily bang-bang. Derive an explicit valid B and finite parameter optimization with endpoint probability constraints and objective expressed in elementary exponential integrals; characterize the optimizing incidence-survival tradeoff beyond merely invoking general optimal control. Prove global endpoints by backward evaluation U_(K+1)=D_(K+1)=0, U_k=H_zk(U_(k+1)), D_k=h_zk(D_(k+1)), including compatible full-model gluing. Also derive the worst-case identification-width order over compatible z: for each fixed M>0 and LT<=M, sufficiently small L*max(bin width), sup_z(U1-D1) is bounded above and below by positive constants depending only on M times L^2*sum(bin width^2). Constants and the finite-switch bound B are delegated derivation objects; the finite-switch EXISTENCE and matched mesh order are conjectures, not established claims. Sampling rung: invert an explicit simultaneous multinomial confidence region through the endpoint computation to obtain an interval containing the entire population identified set with coverage at least1-alpha uniformly over the class; report computational approximation error separately if used. No one-bin result or generic discretization substitutes for the hard finite-switch frontier theorem. PRESOLVE EVIDENCE REQUIRING VERIFICATION: The presolve derives exact factual-bin compatibility, compactness and interval sharpness in survival-flow coordinates, an exact active-ceiling frontier with one saturated factual arm, and compatible global gluing. For two positive factual triples with survival exp(-1.6), an exact five-piece family has a quartic restricted support optimizer: at continuation zero its hybrid risk is 0.47070698750422113 with survival 0.18947781529999005. Root independently checked the path probabilities; global optimality is not established. Switching equations make two nuisance controls monotone; a proposed simultaneous logistic singular obstruction fails the Goh necessary condition, while single-control singular arcs admit exact pulse replacement. Matched mesh-width bounds and multinomial inversion coverage are derived supporting results. Fixed-grid optimizations supply feasible values only, and current primary checks found no direct solved collision.\nUNRESOLVED BOTTLENECK: Prove parameter-uniform endpoint-preserving compression of optimal alternating bang trajectories when both nuisance hazards are saturated and factual-incidence multipliers are negative; assemble all flat, singular, abnormal and boundary cases into an explicit universal finite B.\nEARLY KILL TEST: Parameterize two full four-corner cycles, compute factual constraints and support objective exactly, and examine the constrained Hessian for an improving cycle-deletion direction or an optimality-certified obstruction. Do not replace the finite-piece theorem with mesh bounds or numerical gridding.\nSTRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/mill/scratch/mill-w10/round093839_inspection_presolve_draft.md."
novelty_target: field
banked_novelty_tier: unknown
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: null
reusable: not_reusable
reraise_status: true-negative
gap_reasons:
  - "kernel_substituted@oeq:universal-finite-switch: the delivered supporting results are a weaker kernel, not the required object"
  - "unevaluable_bound@def:support-envelopes: no computable map, constrained program, or certified evaluation algorithm was supplied"
reusable_artifacts:
  - discovery/writeup.tex
  - discovery/core.json
  - reviews/review_rubric.json
  - reviews/review_math.json
seeds_burned: []
proof_attempt_summary: |
  The derivation established supporting compatibility, interval-attainment,
  recursion, ceiling-sensitivity, mesh-width, and confidence-inversion results.
  It did not prove the headline universal finite-switch bound B or equality with
  a finite evaluable program; those were left open, so the required kernel was
  not delivered and the boundary consult found no distinct faithful retry.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 17666231
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 17666231
  total_tokens_consumed: null
banked_on: "2026-09-09"
---

# pid_inspection_separable_switch_frontier / v1 — Failed

**Topic.** Ordinary new-topic generation after the mandated revival_remaining.sh count printed zero. This is not a revival of the rejected known-generator commutator: the target is a sharp partially identified continuous-time component risk when only grouped factual event laws are observed. Retain the exact active-ceiling finite-piece theorem over every compatible measurable hazard model; inactive-ceiling pulse bounds, interval existence, mesh rates and generic confidence inversion cannot substitute for it. MODEL: Fixed known T>0,L>0, arbitrary finite fixed grid 0=t0<...<tK=T. iid subjects, A randomized Bernoulli(1/2), no covariates or additional censoring. Absorbing first-event system: four deterministic Lebesgue-measurable cause-specific hazards lambda_j^a:[0,T]->[0,L], a in {0,1}, j in {1,2}. Under component intervention (aY,aD), cause1 hazard is lambda1^aY and cause2 hazard lambda2^aD; survival is exp(-integral(lambda1^aY+lambda2^aD)). This hazard-splicing restriction defines the causal model; no latent independence beyond what realizes these intervention laws is asserted. All such quadruples are allowed. Observe only A and first-failure inspection bin plus adjudicated cause, or survival to T; retain neither exact failure time nor within-bin order. The population observation is factual conditional bin triples z_ak=(p_ak,q_ak,s_ak), summing to1. Survival is positive because hazards are bounded. Restrict z to compatible observations. Target psi=integral_0^T exp(-Lambda1^1(t)-Lambda2^0(t)) lambda1^1(t)dt, and SDE=psi-observed arm0 cause1 risk. No sensitivity analysis: the hazard-component model is maintained and coarsening produces partial identification. KERNEL: Conjecture over every L,T,K,grid and compatible z: derive a sharp constructive representation of the full attainable hybrid incidence-survival frontier for ONE bin, sufficient to evaluate its support envelope H_z(v)=sup(r+v*s) and lower envelope h_z(v)=inf(r+v*s), for v in [0,1]. Here (r,s) is the hybrid cause1 incidence and end-survival within that bin, generated by four bounded hazards reproducing both factual triples. These suprema define the object, NOT the promised solution. The hard assertion is that exact envelopes admit a uniformly finite switching representation: there exists a universal finite integer B, independent of z,L,bin width, such that each envelope value is attained by compatible piecewise-constant hazard quadruples with at most B common subintervals; levels may be anywhere in [0,L], not necessarily bang-bang. Derive an explicit valid B and finite parameter optimization with endpoint probability constraints and objective expressed in elementary exponential integrals; characterize the optimizing incidence-survival tradeoff beyond merely invoking general optimal control. Prove global endpoints by backward evaluation U_(K+1)=D_(K+1)=0, U_k=H_zk(U_(k+1)), D_k=h_zk(D_(k+1)), including compatible full-model gluing. Also derive the worst-case identification-width order over compatible z: for each fixed M>0 and LT<=M, sufficiently small L*max(bin width), sup_z(U1-D1) is bounded above and below by positive constants depending only on M times L^2*sum(bin width^2). Constants and the finite-switch bound B are delegated derivation objects; the finite-switch EXISTENCE and matched mesh order are conjectures, not established claims. Sampling rung: invert an explicit simultaneous multinomial confidence region through the endpoint computation to obtain an interval containing the entire population identified set with coverage at least1-alpha uniformly over the class; report computational approximation error separately if used. No one-bin result or generic discretization substitutes for the hard finite-switch frontier theorem. PRESOLVE EVIDENCE REQUIRING VERIFICATION: The presolve derives exact factual-bin compatibility, compactness and interval sharpness in survival-flow coordinates, an exact active-ceiling frontier with one saturated factual arm, and compatible global gluing. For two positive factual triples with survival exp(-1.6), an exact five-piece family has a quartic restricted support optimizer: at continuation zero its hybrid risk is 0.47070698750422113 with survival 0.18947781529999005. Root independently checked the path probabilities; global optimality is not established. Switching equations make two nuisance controls monotone; a proposed simultaneous logistic singular obstruction fails the Goh necessary condition, while single-control singular arcs admit exact pulse replacement. Matched mesh-width bounds and multinomial inversion coverage are derived supporting results. Fixed-grid optimizations supply feasible values only, and current primary checks found no direct solved collision.
UNRESOLVED BOTTLENECK: Prove parameter-uniform endpoint-preserving compression of optimal alternating bang trajectories when both nuisance hazards are saturated and factual-incidence multipliers are negative; assemble all flat, singular, abnormal and boundary cases into an explicit universal finite B.
EARLY KILL TEST: Parameterize two full four-corner cycles, compute factual constraints and support objective exactly, and examine the constrained Hessian for an improving cycle-deletion direction or an optimality-certified obstruction. Do not replace the finite-piece theorem with mesh bounds or numerical gridding.
STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/mill/scratch/mill-w10/round093839_inspection_presolve_draft.md.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** kernel_substituted@oeq:universal-finite-switch: the delivered supporting results are a weaker kernel, not the required object

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
