{
  "node_type": "definition",
  "body": {
    "id": "def:two-regimes",
    "name": "mutually exclusive assignment regimes",
    "construction": "\\(\\mathsf B(p)\\) draws iid categorical labels and conditionally independent unit assignments, whereas \\(\\mathsf{CR}(\\alpha)\\) draws one fixed-count label vector and conditionally uniform exact-slice assignments; every result invokes exactly one regime.",
    "free_symbols": [
      "\\mathsf B(p)",
      "p",
      "\\mathsf{CR}(\\alpha)",
      "\\alpha"
    ],
    "inputs": [
      "p",
      "\\alpha",
      "C_{C,k}"
    ]
  }
}