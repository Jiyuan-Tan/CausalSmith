For every fixed finite \(K\), the handle \(\mathfrak C_K\) can be completed into an arbitrary-\(\varepsilon\) validated certificate in the following extended-value sense.  Fix any face \(A\subseteq\mathcal K\) with \(|A|\geq2\) and any compact rational cell of positive \((v_A,r_A)\) on which the induced quotient covariance \(\Omega_A\) is uniformly positive definite.  A deterministic algorithm enumerates all nonempty subsets \(S\subseteq A\), invokes the compact certificates at explicitly computed radii, and returns a represented randomized selector together with outward-rounded numbers \(L^{\infty}_{\varepsilon,A}\) and \(U^{\infty}_{\varepsilon,A}\) satisfying throughout every terminal covariance cell
\[
L^{\infty}_{\varepsilon,A}
\leq G_A(v_A,r_A)
\leq \sup_{h\in\mathbb R^A}
\mathbb E_h\!\left[\max_{k\in A}h_k-h_{\delta(X)}\right]
\leq U^{\infty}_{\varepsilon,A},
\qquad
U^{\infty}_{\varepsilon,A}-L^{\infty}_{\varepsilon,A}\leq\varepsilon.
\]
The algorithm uses an independent Gaussian information split, retains coordinates within a fixed threshold of the screening maximum, and applies the certified compact selector for the realized retained subset.  Thus partial-tie limits such as \((0,-a,-M)\) are handled by their retained lower-dimensional game rather than by radial truncation.

The same routine gives certified outer-design computations.  Over the Bernoulli feasible set \(p\in\Delta_K\), it returns a risk interval of width at most \(\varepsilon\) and a collection of rational simplex cells containing every minimizer of \(p\mapsto G_A(v_A,(Bp)_A)\), together with a feasible \(p\) whose certified risk is within \(\varepsilon\) of the minimum.  Over the exact-count feasible set \(a\in\Delta_A\), it returns the analogous objects for \(a\mapsto G_A(v_A,a)\).  A zero action-relevant coordinate is assigned the proved value \(+\infty\), never a positive-rate Gaussian formula; a computable two-arm lower bound excludes a whole neighborhood of each such boundary before the finite interior calculation.

Finally, implement the selector in the primitive cluster experiments by an independent covariance pilot whose size diverges and whose sample fraction vanishes, a fixed positive screening fraction, and the remaining decision fraction, with covariance snapping as in the compact oracle transfers.  Uniformly on the Bernoulli outer simplex and on each compact active-interior exact-count allocation set, its asymptotic scaled simple regret is bounded by the certified Gaussian upper value.  This transfer is uniform over the finitely many partial-tie faces and over triangular schedule-law arrays whose calibrated covariance stays in the certified positive-definite cells.  Policies separated at a fixed primitive scale are screened with vanishing scaled regret, while policies separated at the \(C^{-1/2}\) scale enter the appropriate retained compact game.  At a vanishing action-relevant exact-count share, only the extended value \(+\infty\) is asserted.  Hence a diagonal choice of certificate tolerances tending to zero transfers the certified noncompact Gaussian value and the certified outer design to the calibrated primitive experiments on precisely these domains.