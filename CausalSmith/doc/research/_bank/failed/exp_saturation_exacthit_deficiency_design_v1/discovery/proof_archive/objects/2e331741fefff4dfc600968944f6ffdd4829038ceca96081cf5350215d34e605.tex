Under \(\texttt{ass:cr-exact-slices}\), \(Z_c\in\mathcal S_{L_c}\) almost surely.  Since the counts \(m_1,\ldots,m_K\) are distinct,
\[
 L_c=k\quad\Longleftrightarrow\quad \sum_{j=1}^nZ_{c,j}=m_k. \tag{8}
\]
Thus the deleted label vector is a deterministic measurable function of the retained assignment vectors.  Conversely the reduced record is obtained from the full record by forgetting the label vector.  The two experiments are isomorphic through parameter-free measurable kernels for every fixed label-count vector and every schedule law.  Composing an arbitrary randomized rule on any standard-Borel decision space with either kernel preserves its pointwise risk.  Taking the supremum over \(P\) and the infimum over rules proves the displayed minimax equality, including finite ranking actions.

The causal target is unchanged: by \(\texttt{ass:cr-label-schedule-independence}\) and slice uniformity,
\[
 \mathbb E_P\{W(Z_c)\mid L_c=k\}
 =M_k^{-1}\sum_{z\in\mathcal S_k}\mathbb E_PW(z)=U_k. \tag{9}
\]