With a prespecified within-stratum split whose pilot count tends to infinity and is \(o(C_{C,k})\) for every active \(k\), active covariance estimates are consistent and the remaining triangular array has the same Gaussian covariance.  Thus any fixed almost-everywhere continuous Gaussian selector transfers uniformly over bounded \(h\).  The frozen definition does not supply the selector's covariance-stability and global certified-risk inequalities, so the asserted \(U_\varepsilon\) conclusion and the diagonal \(\varepsilon\downarrow0\) attainment remain dependent on the unresolved Gaussian certificate.