\(\mathfrak C_K\) is the certified hierarchical noncompact routine on a declared candidate face \(A\).  Choose \(0<\lambda<1\); split a Gaussian observation into independent screening and decision observations with covariances \(\Sigma(v_A,r_A)/\lambda\) and \(\Sigma(v_A,r_A)/(1-\lambda)\); retain arms within threshold \(T\) of the screening maximum; multiply the decision observation by \(\sqrt{1-\lambda}\); and apply the compact quotient certificate separately on every realized nonempty subset \(S\subseteq A\).  Optimize the resulting certified value over \(B\Delta_K\) or \(\Delta_A\) by rational branch-and-bound, excluding action-relevant zero-rate neighborhoods with the certified two-arm lower bound.  The routine adapts among retained subsets of the declared face.  Global candidate adaptation means taking \(A=\mathcal K\) with positive information for every candidate; at an action-relevant zero rate its value is \(+\infty\).