\(\mathfrak C_K\) is the certified hierarchical noncompact routine on a declared candidate face \(A\): choose \(0<\lambda<1\) and \(T>0\), split Gaussian information independently into a screening observation with covariance \(\Sigma/\lambda\) and a decision observation with covariance \(\Sigma/(1-\lambda)\), retain arms within threshold \(T\) of the screening maximum, and feed \(\sqrt{1-\lambda}\) times the decision observation to the compact certificate for the realized nonempty subset \(S\subseteq A\).  Enumerate all such subsets, choose \(\lambda,T\), and the compact tolerances by outward rational bounds, then branch-and-bound over \(B\Delta_K\) or \(\Delta_A\).  Zero action-relevant rates take the extended value \(+\infty\).  Primitive implementation uses separate covariance-pilot, screening, and decision cluster subsamples with the same asymptotic covariance normalization.  Uniformity is over retained subsets inside \(A\); a global all-candidate implementation takes \(A=\mathcal K\) and requires positive information for every candidate.