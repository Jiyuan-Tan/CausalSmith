# eid_blocked_colour_rational_separation_complete — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (A finite labelled variable population \(V\) of size…).** A finite labelled variable population \(V\) of size \(p=|V|\ge1\), carrying finite directed acyclic graphs on \(V\): parent sets \(\operatorname{pa}_{\mathcal K}(j)\), undirected skeletons \(\operatorname{skel}\), unshielded-collider (immorality) triples \(\operatorname{uc}\), covered edges, and the arrow-disagreement count \(\delta\) between two orientations of a common skeleton. — Highest-leverage reuse decision of this plan. `Causalean.DAG V` ([DecidableEq V] [Fintype V]; fields `edge`, `decEdge`, `acyclic : forall v, not Relation.TransGen edge v v`) is exactly the graph coordinate of a blocked state, and `Causalean.DAG.ofAcyclic` (Causalean/Graph/AcyclicConstruct.lean:35) materializes it from a raw relation + acyclicity witness. Confirmed available and signature-checked by reading source: `Causalean.DAG.parents` (Finset V), `Causalean.DAG.UAdj` (Causalean/Graph/DSep/ActivePath.lean:58) = skeleton adjacency, `Causalean.DAG.IsImmorality` (MarkovEquiv/Defs.lean:42) = unshielded collider, `Causalean.SameSkeleton` / `Causalean.SameImmoralities` (Defs.lean:52,55), `Causalean.MarkovEquiv` (Defs.lean:61), `Causalean.DAG.IsCoveredEdge` (CoveredReversal.lean:42), `Causalean.DAG.flipEdge` (CoveredReversal.lean:125) + `Causalean.DAG.flipRel_acyclic` (73) + `Causalean.DAG.flipEdge_edge` (130) + `Causalean.DAG.flipEdge_sameSkeleton` (136) + `Causalean.DAG.flipEdge_sameImmoralities` (156) + `Causalean.DAG.markovEquiv_flipEdge` (1726), `Causalean.edgeDiff` / `Causalean.edgeDiffCount` (Decompose.lean:59,65), `Causalean.exists_covered_reversed_edge` (Decompose.lean:91, AMP Lemma 3.2 existence), `Causalean.edgeDiffCount_flipEdge_lt` (Decompose.lean:184), `Causalean.markovEquiv_of_sameSkeleton_sameImmoralities` (Decompose.lean:237), and the flagship `Causalean.markovEquiv_iff_sameSkeleton_sameImmoralities` (Causalean/Graph/MarkovEquiv.lean:59). `ArrowGap` = `Causalean.edgeDiffCount` restricted to reversed common-skeleton edges (helper `arrowGap` = card of the reversed-pair Finset; the substrate's `edgeDiff` is the same construction shape). Surveyed and BYPASS-JUSTIFIED from the exactid reuse list: `Causalean.SCM.ID.Identifiable`, `Causalean.SCM.ID.Overlap`, `Causalean.SCM.ID.Adjustment`, `Causalean.SCM.ID.Backdoor*`, `Causalean.SCM.ID.Frontdoor*`, `Causalean.SCM.ID.GraphicalThms.*`, `Causalean.Graph.SWIG` / `SWIGGraph`, `Causalean.PO.*`, `Causalean.SCM.Do.*` -- all of these express do/PO-language identification of an interventional functional from an observational kernel, whereas every node here is a STRUCTURE-identification claim about equality of two covariance IMAGES of coloured graphs; no interventional kernel, no treatment, no overlap. Search trace: grep '^## ' ../doc/API.md (section index) then read sections 1 (Graph/DAG.lean) and 4b (Graph/MarkovEquiv/); Glob Causalean/Graph/MarkovEquiv/*.lean -> 7 files, all read or outlined; grep '(CPDAG|essentialGraph|Meek|consistent extension)' over Causalean/ -> only a prose mention in Graph/MarkovEquiv.lean, so no CPDAG module exists.
**required modules.** Causalean.Graph.DAG, Causalean.Graph.AcyclicConstruct, Causalean.Graph.DSep.ActivePath, Causalean.Graph.MarkovEquiv, Causalean.Graph.MarkovEquiv.Defs, Causalean.Graph.MarkovEquiv.CoveredReversal, Causalean.Graph.MarkovEquiv.Decompose

**S-2 (Singleton-permitting blocked edge-coloured linear Gaussia…).** Singleton-permitting blocked edge-coloured linear Gaussian DAG models over \(V\). A state \(E_0=(G_{E_0},\Pi^{E_0})\) pairs an acyclic graph on \(V\) with, for every target \(j\), a partition \(\Pi^{E_0}_j\) of \(\operatorname{pa}_{G_{E_0}}(j)\) into nonempty coefficient blocks, singletons permitted. Its parameter space is \(\Theta_{E_0}=\mathbb R^{b_{E_0}}\times(0,\infty)^p\) with \(b_{E_0}=\sum_j|\Pi^{E_0}_j|\): one unrestricted real coefficient per block and one positive innovation variance per vertex. The block-constant matrix \(B_{E_0}(\beta)\) has \((B_{E_0}(\beta))_{ji}=\beta_{jB}\) for \(i\in B\in\Pi^{E_0}_j\) and zero at nonparents, and the covariance map is \(\phi_{E_0}(\beta,\omega)=(I_p-B_{E_0}(\beta))^{-1}\operatorname{diag}(\omega)(I_p-B_{E_0}(\beta))^{-\mathsf T}\), whose full image \(\mathcal M(E_0)\subseteq\mathbb S^p_{++}\) is the observational model. Population objects live over an arbitrary \(\Sigma\in\mathbb S^p_{++}\): parent regressions \(r_j(P;\Sigma)=\Sigma_{P,P}^{-1}\Sigma_{P,j}\), block-constrained regressions, determinant and cleared-regression certificates, unit-noise binary-star covariances, and legal singleton covered reversals with partition transport. No probability space, no faithfulness assumption, no restriction to nonzero coefficients. — The blocked (target-local edge-coloured) layer is genuinely new: no Causalean or Mathlib object carries a per-target partition of parent sets with block-constant structural coefficients. It is built ON TOP of S1 (`BlockedState.toDAG`) so the graph coordinate is never rebuilt. Search trace: grep '(coloured|colored|edgeColour|edgeColor|Finpartition)' over Causalean/ -> 2 hits, both in Stat/Nonparametric/MomentProblems (set-partition combinatorics for cumulants, unrelated to edge colouring), so no edge-colouring substrate exists; grep '(linear[- ]Gaussian|LinearGaussian|linearSEM|trek)' over Causalean/ -> Causalean/Discovery/InvariantPrediction/LinearGaussian/{Model,Regression,Completeness}.lean and Causalean/Discovery/LiNGAM/*, Causalean/Discovery/LinearDisentanglement/*. BYPASS-JUSTIFIED per module: `Causalean.Discovery.InvariantPrediction.LinearGaussian.Model` (`ObsSEM`, Model.lean:94) is a random-variable-level Gaussian SEM bundling a probability space, measurable coordinates, a.e. structural equations and `iIndepFun` noises -- a strictly heavier abstraction than this node's purely algebraic covariance IMAGE in `Set (Matrix V V R)`, and its `hEdge : dag.edge k j <-> beta j k /= 0` forbids the zero coefficients this paper's `honest_scope` explicitly admits; `Causalean.Discovery.LiNGAM.*` is non-Gaussian cumulant identification; `Causalean.Discovery.LinearDisentanglement.*` (SigmaSolutions, Rowspan, Uniqueness) is latent-factor covariance factorisation with no parent-block colouring. Mathlib side reused rather than rebuilt: `Matrix.PosDef`, `Matrix.det`, `Matrix.adjugate`, `Matrix.inv`, `Matrix.det_fromBlocks_one_11` / `Matrix.det_fromBlocks_22` and `Matrix.schur_complement_eq_22` (confirmed via leansearch 'determinant of block matrix Schur complement fromBlocks'), `Finpartition.ofExistsUnique` / `Finpartition.sup_parts` (confirmed via leansearch), `Relation.ReflTransGen`, `Nat.clog`, `Nat.choose`, `MvPolynomial.funext` (confirmed via loogle) for the real proper-algebraic-subset step. Also surveyed for the certificate arithmetic: `Causalean.Stat.Nonparametric.LocalPoly.DesignMatrixPosDef` (`designMatrix_posDef`, line 87) -- bypass-justified, it is Vandermonde/weight specific, not the generic `A^T Sigma A > 0` for a column-injective incidence `A`, which is a two-line build-inline over `Matrix.PosDef`.
**required modules.** Causalean.Graph.DAG, Causalean.Graph.AcyclicConstruct, Causalean.Graph.MarkovEquiv.Defs, Causalean.Graph.MarkovEquiv.CoveredReversal, Mathlib.Order.Partition.Finpartition, Mathlib.LinearAlgebra.Matrix.PosDef, Mathlib.LinearAlgebra.Matrix.SchurComplement, Mathlib.LinearAlgebra.Matrix.Adjugate, Mathlib.Logic.Relation, Mathlib.Algebra.MvPolynomial.Funext

**S-3 (Independent centred \(p\)-variate Gaussian sampling with…).** Independent centred \(p\)-variate Gaussian sampling with known zero mean: observations \(X^{(1)},\ldots,X^{(n)}\) i.i.d. \(N_p(0,\Sigma_0)\) with \(\Sigma_0\in\mathcal M(C)\), the sample covariance \(S_n=n^{-1}\sum_r X^{(r)}X^{(r)\mathsf T}\) as Gaussian sufficient statistic, a deterministic fallback on the event \(S_n\notin\mathbb S^p_{++}\), the fixed-\(p\) asymptotic regime \(n\to\infty\), a global information criterion over the finite state class with a fixed deterministic tie rule, and a familywise noncoverage level \(\alpha\in(0,1)\). — The sampling model is `Causalean.Stat.IIDSample Omega X mu P` (Causalean/Stat/Sample.lean:44; fields `Z : Nat -> Omega -> X`, `meas`, plus i.i.d./law fields) instantiated at `X = EuclideanSpace R V` with `P` the centred Gaussian law of covariance `Sigma_0`; `S.sampleMean` gives `S_n` entrywise. Confirmed reusable by reading source: `Causalean.Stat.IIDSample.sampleCov_entry_tendsto_inProb` (VarianceEstimation.lean:140) delivers exactly the entrywise `S_n -> Sigma_0` in probability the BIC proof needs, `Causalean.Stat.Tendsto_inProb` / `Tendsto_dist` / `IsBigOp` / `IsLittleOp` (Stat/Limit/Convergence.lean:57,90,161,174) are the project's convergence wrappers, `Causalean.Stat.gaussianMeasure` (Stat/CLT/AsymptoticLinearity.lean:53) the real Gaussian limit wrapper, `Causalean.Stat.deltaMethod_scalar` (Stat/Inference/DeltaMethod.lean:63) the scalar delta method for each labelled effect coordinate, `Causalean.Stat.Tendsto_dist.wald_coverage_Iic` (Stat/Inference/WaldVec.lean:81) the coverage-from-portmanteau step, and `Causalean.PartialID.RandomSet.hausdorffDist` / `directedHausdorff` (Causalean/PO/ID/Partial/RandomSet/Hausdorff.lean:73,64) the set-valued metric for the Hausdorff conclusion. Search trace: grep '(BIC|bayesianInformation|Bonferroni|sampleCovariance|sampleCov|Wishart)' over Causalean/ -> Stat/Inference/VarianceEstimation.lean, PO/ID/Partial/Inference/{ImbensManski,IntervalCI}.lean (Bonferroni only as prose in the interval-endpoint context, not a reusable multiplicity lemma; no BIC and no Wishart anywhere); grep '(logDet|gaussianLogLik|logLikelihood|loglik)' over Causalean/ -> no matches, so the Gaussian log-likelihood / `log det K + tr(K^{-1} T)` discrepancy is build-inline; grep 'Hausdorff' over Causalean/ -> the RandomSet/Hausdorff.lean and CriterionSet/Consistency.lean pair (the latter's `tendsto_hausdorffDist_levelSet` is level-set specific, bypass-justified, but is the ANALOGOUS module F3 should read for the set-convergence bookkeeping pattern). Mathlib side: leansearch 'quantile of standard normal distribution' -> only `ProbabilityTheory.cdf`; no inverse-CDF, hence decision P6.
**required modules.** Causalean.Stat.Sample, Causalean.Stat.Inference.VarianceEstimation, Causalean.Stat.Limit.Convergence, Causalean.Stat.CLT.AsymptoticLinearity, Causalean.Stat.Inference.DeltaMethod, Causalean.Stat.Inference.WaldVec, Causalean.PO.ID.Partial.RandomSet.Hausdorff

## Assumptions (A)

**A-1 (assumption).** The directed graph \(\mathcal K\) is acyclic.

**A-2 (assumption).** For every \(j\in V\), \(\Xi_j\) is a partition of \(\operatorname{pa}_{\mathcal K}(j)\), with singleton blocks permitted.

## Definitions (P)

**P-1 (…).** \[\mathfrak S_p=\left\{A=(\mathcal K,\Xi):\mathcal K\text{ is an acyclic directed graph on }V,\ \Xi_j\text{ is a partition of }\operatorname{pa}_{\mathcal K}(j)\text{ for every }j\in V\right\}.\]

**P-2 (For \(E_0\in\mathfrak S_p\), let…).** For \(E_0\in\mathfrak S_p\), let \(b_{E_0}=\sum_{j\in V}|\Pi^{E_0}_j|\), \(\Theta_{E_0}=\mathbb R^{b_{E_0}}\times(0,\infty)^p\), and \[\mathcal M(E_0)=\left\{(I_p-B_{E_0}(\beta_{E_0}))^{-1}\operatorname{diag}(\omega_{E_0})(I_p-B_{E_0}(\beta_{E_0}))^{-\mathsf T}:(\beta_{E_0},\omega_{E_0})\in\Theta_{E_0}\right\}.\] The entry \((B_{E_0}(\beta_{E_0}))_{ji}\) equals \(\beta_{jB}\) for \(i\in B\in\Pi^{E_0}_j\), and all real block coefficients are admitted.

**P-3 (…).** \[\mathcal N_j(E_0)=\{B\in\Pi^{E_0}_j:|B|\geq2\},\qquad j\in V.\]

**P-4 (…).** \[F(E_0)=\{i\to j\in G_{E_0}:\mathcal N_i(E_0)\neq\mathcal N_j(E_0)\}.\]

**P-5 (…).** \[\mathsf S(E_0)=\left(\operatorname{skel}(G_{E_0}),\operatorname{uc}(G_{E_0}),(\mathcal N_j(E_0))_{j\in V},F(E_0)\right).\]

**P-6 (For a rejected state \(J\), an ordered conditional-indepe…).** For a rejected state \(J\), an ordered conditional-independence certificate chooses a topological order \(\pi_J\), a nonadjacent pair \(u,v\) with \(u\) preceding \(v\), and \(S=\operatorname{Pred}_{\pi_J}(v)\setminus\{u\}\), and evaluates \(f_{uvS}(\Sigma)=\det(\Sigma_{[u,S],[v,S]})\). A blocked-regression certificate chooses \(j\in V\) and distinct \(u,v\) in one block of \(\Pi^J_j\), puts \(P=\operatorname{pa}_{G_J}(j)\), and evaluates \(L_{j;u,v}(\Sigma)=(e_u-e_v)^{\mathsf T}\operatorname{adj}(\Sigma_{P,P})\Sigma_{P,j}\).

**P-7 (For \(E_0\in\mathfrak S_p\), choose \(z\in V\) and…).** For \(E_0\in\mathfrak S_p\), choose \(z\in V\) and \(\mathcal A_z\subseteq\Pi^{E_0}_z\) with \(1\leq|\mathcal A_z|\leq2\), set \(A_z=\bigcup_{B\in\mathcal A_z}B\), \(B^{\star}=e_z\mathbf 1_{A_z}^{\mathsf T}\), and \[\Sigma^{\star}=(I_p+B^{\star})(I_p+B^{\star})^{\mathsf T}=I_p+e_z\mathbf 1_{A_z}^{\mathsf T}+\mathbf 1_{A_z}e_z^{\mathsf T}+|A_z|e_ze_z^{\mathsf T}.\] The set of all such covariances is \(\mathcal W_2(E_0)\).

**P-8 (Order vertices, blocks, and pairs lexicographically).** Order vertices, blocks, and pairs lexicographically. For \(\mathsf S(C)\neq\mathsf S(D)\), \(\mathsf{Route}(C,D)\) applies the first available rule: a skeleton mismatch activates the source block containing the differing arrow tail and returns the rejected ordered conditional-independence certificate; a collider mismatch activates the source block or two source blocks containing the collider parents and returns the rejected ordered conditional-independence certificate; a nonsingleton-family mismatch chooses crossing edge-support partition atoms, activates the source atom, and returns the rejected within-block regression certificate; an \(F\)-mismatch chooses a reversed \(x\to y\) with \(\mathcal N_x\neq\mathcal N_y\), selects a direction in which a block \(S\in\mathcal N_x\) is split by \(\Pi^E_y\), activates \(\{x\}\) and the first block \(T\in\Pi^E_y\) meeting but not containing \(S\), and returns the rejected regression difference between the first elements of \(S\cap T\) and \(S\setminus T\).

**P-9 (For \(i\to j\) in \(G_{E_0}\),…).** For \(i\to j\) in \(G_{E_0}\), \(\operatorname{rev}_{i\to j}(E_0)\) is defined exactly when \(\operatorname{pa}_{G_{E_0}}(j)=\operatorname{pa}_{G_{E_0}}(i)\cup\{i\}\), \(\{i\}\in\Pi^{E_0}_j\), and \(\Pi^{E_0}_j\setminus\{\{i\}\}=\Pi^{E_0}_i\). It reverses \(i\to j\), sets \(\Pi'_j=\Pi^{E_0}_i\) and \(\Pi'_i=\Pi^{E_0}_i\cup\{\{j\}\}\), and leaves every other partition unchanged.

**P-10 (…).** \[\mathcal R(E_0)=\{E'\in\mathfrak S_p:E'\text{ is reached from }E_0\text{ by a finite sequence of defined }\operatorname{rev}_{i\to j}\}.\]

**P-11 (Form the ordinary CPDAG of \(G\), orient every arrow in \…).** Form the ordinary CPDAG of \(G\), orient every arrow in \(F(C)\), apply closure under the standard consistent-extension rules, and enumerate every acyclic extension \(K\) with the same unshielded colliders. For each extension emit the unique lift \(E_K=(K,\Pi^K)\) with \[\Pi^K_j=\mathcal N_j(C)\cup\left\{\{a\}:a\in\operatorname{pa}_K(j)\setminus\bigcup\mathcal N_j(C)\right\},\qquad j\in V.\] The emitted set is \(\operatorname{Enum}(C)\).

**P-12 (Fix \(E_0\in\mathfrak S_p\),…).** Fix \(E_0\in\mathfrak S_p\), \(\Sigma\in\mathbb S^p_{++}\), and \(j\in V\). Put \(P_{E_0j}=\operatorname{pa}_{G_{E_0}}(j)\). Let \(A_{E_0j}\) have rows \(i\in P_{E_0j}\), columns \(B\in\Pi^{E_0}_j\), and entries \((A_{E_0j})_{iB}=\mathbf 1\{i\in B\}\). For a nonempty parent set define \(\Gamma_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},P_{E_0j}}A_{E_0j}\), \(h_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},j}\), and \(\beta_{E_0j}(\Sigma)=\Gamma_{E_0j}(\Sigma)^{-1}h_{E_0j}(\Sigma)\). Repeat each block coefficient on its member parents and put zero outside the parent set to obtain \(L_{E_0}(\Sigma)\); use a zero row for an empty parent set.

**P-13 (For \(E_0\in\mathfrak S_p\), set…).** For \(E_0\in\mathfrak S_p\), set \(d_{E_0}=p+\sum_{j\in V}|\Pi^{E_0}_j|\). If \(S_n\in\mathbb S^p_{++}\), compute the attained statewise Gaussian likelihood using the target-local systems in \(\texttt{def:off-model-block-ls}\) and set \(\operatorname{BIC}_n(E_0)=-2\sup_{\Sigma\in\mathcal M(E_0)}\ell_n(\Sigma)+d_{E_0}\log n\). If \(S_n\notin\mathbb S^p_{++}\), set \(\operatorname{BIC}_n(E_0)=d_{E_0}\log n\). Let \(\widehat C_n\) be the minimizer under a fixed ordering of \(\mathfrak S_p\).

**P-14 (For \(E_0\in\mathfrak S_p\),…).** For \(E_0\in\mathfrak S_p\), \(\Sigma\in\mathbb S^p_{++}\), and finite \(\mathcal Q\), define \(g_{E'}(\Sigma)=(((I_p-L_{E'}(\Sigma))^{-1})_{ya})_{(a,y)\in\mathcal Q}\) for each \(E'\in\mathcal R(E_0)\), and set \[\mathcal T_{E_0}(\Sigma)=\{g_{E'}(\Sigma):E'\in\mathcal R(E_0)\}.\]

**P-15 (For \(C\in\mathfrak S_p\), \(\mathcal E_C\) is the finite…).** For \(C\in\mathfrak S_p\), \(\mathcal E_C\) is the finite union, over \(D\in\mathfrak S_p\) with \(d_D\leq d_C\) and \(\mathcal M(D)\neq\mathcal M(C)\), of the polynomial pullbacks to \(\Theta_C\) of \(\mathcal M(C)\cap\mathcal M(D)\), together with the polynomial zero-gradient loci of the nonconstant coordinates reported by \(\mathcal T_C\).

**P-16 (Select \(\widehat C_n\) by \(\texttt{def:global-bic}\)).** Select \(\widehat C_n\) by \(\texttt{def:global-bic}\). On \(S_n\in\mathbb S^p_{++}\), retain member labels, evaluate every rational map \(g_E(S_n)\) and its derivative, estimate its variance from the Gaussian covariance limit, and use Bonferroni critical value \(z_{1-\alpha/(2K)}\) for the \(K\) nonconstant labelled coordinates; assign exact singleton intervals to structurally zero coordinates. On \(S_n\notin\mathbb S^p_{++}\), assign \(\mathbb R\) to every coordinate. The resulting collection is \(\mathfrak I_{n,\alpha}(\widehat C_n)\).

**P-17 (Apply \(\mathsf H_{\mathrm{opt}}\)).** Apply \(\mathsf H_{\mathrm{opt}}\): restrict universal separator schemes to minimal crossing-partition and unequal-family reversal subinstances, translate active-block support into regression support rank, and use exact determinant divisibility and rational-height counting to lower-bound jointly the number of active blocks and the largest numerical height.

**P-18 (Using \(\mathsf H_{\mathrm{opt}}\), characterize the Pare…).** Using \(\mathsf H_{\mathrm{opt}}\), characterize the Pareto frontier of universal exact separator schemes for \(\mathfrak S_p\) in the two coordinates consisting of the maximum number of active target blocks and the maximum rational height. In particular, determine for which \(p\) the achieved pair \((2,\lceil\log_2p\rceil)\) can be improved in either coordinate without worsening the other, while every scheme must still separate every pair with unequal static signatures by an observable polynomial certificate.

## Lemmas (L)

**L-1 (Fix \(z\in V\) and \(A_z\subseteq V\setminus\{z\}\), and…).** Fix \(z\in V\) and \(A_z\subseteq V\setminus\{z\}\), and let \(\Sigma^{\star}=I_p+e_z\mathbf 1_{A_z}^{\mathsf T}+\mathbf 1_{A_z}e_z^{\mathsf T}+|A_z|e_ze_z^{\mathsf T}\). For \(j\in V\) and \(P\subseteq V\setminus\{j\}\), write \(r=r_j(P;\Sigma^{\star})\). If \(j=z\), then \(r_q=\mathbf 1\{q\in A_z\}\) for \(q\in P\). If \(j\neq z\) and either \(z\notin P\) or \(j\notin A_z\), then \(r=0\). If \(j\in A_z\) and \(z\in P\), put \(Q=P\setminus\{z\}\) and \(d=1+|A_z\setminus Q|\); then \(r_z=1/d\), \(r_q=-1/d\) for \(q\in A_z\cap Q\), \(r_q=0\) for \(q\in Q\setminus A_z\), and \(\det(\Sigma^{\star}_{P,P})=d\).

## Theorems (T)

### T-block: t1 — For all \(C,D\in\mathfrak S_p\) with…
**Statement.** For all \(C,D\in\mathfrak S_p\) with \(\mathsf S(C)\neq\mathsf S(D)\), \(\mathsf{Route}(C,D)\) returns \(E\in\{C,D\}\), \(J\in\{C,D\}\setminus\{E\}\), \(z\in V\), and \(\mathcal A_z\subseteq\Pi^E_z\) with \(1\leq|\mathcal A_z|\leq2\). With every innovation variance equal to one, every block coefficient in \(E\) equal to one on \(\mathcal A_z\) and zero elsewhere, and \(A_z=\bigcup_{B\in\mathcal A_z}B\), the resulting covariance satisfies \[B^{\star}=e_z\mathbf 1_{A_z}^{\mathsf T},\qquad (B^{\star})^2=0,\qquad \Sigma^{\star}=(I_p+B^{\star})(I_p+B^{\star})^{\mathsf T}\in\mathcal M(E)\setminus\mathcal M(J).\] Every entry of \(\Sigma^{\star}\) is an integer in \(\{0,1,\ldots,p\}\), \(\det(\Sigma^{\star})=1\), and the router returns either an ordered certificate \(f_{uvS}(\Sigma^{\star})\) or a blocked-regression certificate \(L_{j;u,v}(\Sigma^{\star})\) whose value belongs to \(\{-2,-1,1,2\}\). Every recovered regression coefficient in the certificate has numerator in \(\{-1,0,1\}\) and reduced denominator at most \(p\), so all supplied numerical data have height at most \(B(p)=\lceil\log_2p\rceil\). The route uses polynomially many graph and rational-arithmetic operations in \(p\). For \(p=1\), the implication is vacuous.

### T-block: t2 — For all \(C,D\in\mathfrak S_p\), the following are equiva…
**Statement.** For all \(C,D\in\mathfrak S_p\), the following are equivalent: \(\mathcal M(C)=\mathcal M(D)\); \(\mathsf S(C)=\mathsf S(D)\); and \(D\in\mathcal R(C)\). Every legal reversal preserves the full covariance image, including zero coefficients. If these conditions hold, then \(d_{\mathrm{rev}}(C,D)=\delta(C,D)\), and a shortest path reverses every initially disagreeing arrow exactly once. Thus the signature is an exact recovery functional for the covariance-model component, not merely a sufficient invariant.

### T-block: t3 — For every \(C\in\mathfrak S_p\),…
**Statement.** For every \(C\in\mathfrak S_p\), \[\operatorname{Enum}(C)=\mathcal R(C)=\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}.\] Each consistent extension of the ordinary CPDAG with the background arrows \(F(C)\) has exactly one lifted partition, so the enumeration is duplicate-free and terminates after at most \(p!\) outputs. A shortest legal-reversal path from \(C\) to each emitted \(D\) has length \(\delta(C,D)\).

### T-block: t4 — If every block of \(C\) is a singleton, then…
**Statement.** If every block of \(C\) is a singleton, then \(\mathcal N_j(C)=\varnothing\) for every \(j\in V\), and graph projection is a bijection from \(\mathcal R(C)\) onto the ordinary Markov-equivalence class of \(G\); the inverse sends each equivalent DAG to its unique all-singleton lift. If every nonempty block of \(C\) is nonsingleton, then \(\mathcal R(C)=\{C\}\). The first conclusion is the singleton lift of the Chickering1995 covered-reversal component, and the second is the target-local proper-blocked structural-identifiability conclusion covered by BoegeKubjasMisraSolus2026.

### T-block: t5 — Fix \(p\), \(C\in\mathfrak S_p\), and…
**Statement.** Fix \(p\), \(C\in\mathfrak S_p\), and \(\theta_C\in\Theta_C\setminus\mathcal E_C\), and put \(\Sigma_0=\phi_C(\theta_C)\). Let \(X^{(1)},\ldots,X^{(n)}\) be independent \(N_p(0,\Sigma_0)\) observations, and let \(\widehat C_n\) minimize \(\operatorname{BIC}_n\) over \(\mathfrak S_p\) under the fixed tie rule. The set \(\mathcal E_C\) is a proper algebraic subset of \(\Theta_C\), and as \(n\to\infty\) with fixed \(p\), \[\Pr\{\mathcal R(\widehat C_n)=\mathcal R(C)\}\longrightarrow1.\] The result concerns the global criterion and makes no claim about greedy-search consistency.

### T-block: t6 — Under \(\texttt{thm:generic-bic-component-recovery}\), fi…
**Statement.** Under \(\texttt{thm:generic-bic-component-recovery}\), fix finite \(\mathcal Q\) and \(\alpha\in(0,1)\). Then \(\mathcal T_{\widehat C_n}(S_n)\) converges in probability to \(\mathcal T_C(\Sigma_0)\) in Hausdorff distance. Retaining state labels before removing coincident numerical values, the confidence collection satisfies \[\liminf_{n\to\infty}\Pr\left\{\mathcal R(\widehat C_n)=\mathcal R(C)\text{ and every }g_E(\Sigma_0),\ E\in\mathcal R(C),\text{ belongs to its labelled interval in }\mathfrak I_{n,\alpha}(\widehat C_n)\right\}\geq1-\alpha.\] The conclusion permits duplicate effect values and singular joint effect covariance.

### T-block: t7 — For every \(C,D\in\mathfrak S_p\), if…
**Statement.** For every \(C,D\in\mathfrak S_p\), if \(\mathsf S(C)\neq\mathsf S(D)\), then \[ \mathcal W_2(C)\not\subseteq\mathcal M(D) \qquad\text{or}\qquad \mathcal W_2(D)\not\subseteq\mathcal M(C). \] Moreover, for every \(E_0\in\mathfrak S_p\), writing \(b_j=|\Pi^{E_0}_j|\), \[ |\mathcal W_2(E_0)| =\sum_{j\in V}\left(b_j+\binom{b_j}{2}\right) =\sum_{j\in V}\binom{b_j+1}{2} \leq\binom{p+1}{3} \leq\frac{p^2(p-1)}{2}. \] The bound \(\binom{p+1}{3}\) is sharp for every \(p\geq1\): equality is attained by a transitively oriented complete directed acyclic graph whose parent partitions consist entirely of singleton blocks.
