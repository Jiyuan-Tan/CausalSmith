\documentclass{article}
\usepackage[margin=0.8in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{mathrsfs,bm,bbm}
\usepackage{graphicx,booktabs,array,multirow,tabularx,longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm,algpseudocode}
\usepackage{subcaption,tikz}
\usepackage{siunitx,cancel,accents}
\usepackage{microtype}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}
\setlength{\emergencystretch}{2em}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{openendedquestion}{Open-ended Question}
\newtheorem{conjecture}{Conjecture}
\newcommand{\coreref}[1]{\ref{#1}}

\begin{document}

\sloppy

\section*{eid\_\allowbreak{}blocked\_\allowbreak{}colour\_\allowbreak{}rational\_\allowbreak{}separation\_\allowbreak{}complete}

\noindent\textit{Specialization:} v1\par

\paragraph{TL;DR.} Target-local equality constraints among structural coefficients encode causal communities, but permitting singleton communities restores observational ambiguity. Every static-signature discrepancy is nevertheless exposed by a unit-noise covariance generated from at most two coefficient-one blocks at one target, with integer entries bounded by \(p\) and an exact determinant or cleared-regression value in \(\{-2,-1,1,2\}\). The resulting universal binary-star library has the exact size \(\sum_j[|\Pi^{E_0}_j|+\binom{|\Pi^{E_0}_j|}{2}]\), at most the sharp worst-case value \(\binom{p+1}{3}\). Covariance-model equality is equivalent to signature equality and to connectivity by legal singleton covered reversals, enabling exact component enumeration and a qualified fixed-\(p\) rung for generic global-BIC component recovery and simultaneous member-labelled total-effect inference.

\section{Project justification}

\paragraph{Gap.} BoegeKubjasMisraSolus2026 proves identifiability only when every blocked edge-colour class is nonsingleton and leaves graphical equivalence open. Its rational constraints and the determinant machinery of SullivantTalaskaDraisma2010 and DrtonRobevaWeihs2020 do not provide a uniformly small observable witness, an exactly counted universal witness library, or a sharp worst-case library bound when singleton blocks are admitted.

\paragraph{Niche.} The target-local blocked class has enough structure for every skeleton, collider, block-family, or unequal-family orientation mismatch to be routed to one sparse star calculation. Partition disjointness makes the star parametrization injective, while acyclicity bounds the exact library size through a topological order, and the class still contains ordinary Markov equivalence and the proper BPEC boundary as opposite special cases.

\paragraph{Fill.} The project proves the four-case binary-star router and logarithmic numerical-height guarantee, counts its universal library exactly and obtains the sharp bound \(\binom{p+1}{3}\), lifts Chickering's disagreement-decreasing sequence to the exact legal blocked move graph, enumerates the resulting component, and establishes pointwise generic selection and effect-set inference without claiming uniformity at model intersections.

\section{Related work}

BoegeKubjasMisraSolus2026 provides the model exemplar: its Introduction moves from equality-constrained causal coefficients to causal communities, Theorem 19 proves proper BPEC identifiability, and Question 20 isolates general edge-coloured equivalence; the present strict advance is confined to target-local blocked colours with singleton blocks and adds bounded constructive separators together with an exactly counted binary-star dictionary whose sharp worst-case size is \(\binom{p+1}{3}\). Chickering1995, Theorems 1 and 2, supplies the ordinary skeleton-collider criterion and the disagreement-decreasing covered-reversal induction; the new step is proving that equal blocked signatures force every selected move to be singleton and partition-compatible. SullivantTalaskaDraisma2010 supplies the determinant-expansion viewpoint, while DrtonRobevaWeihs2020 supplies the cleared rational-constraint analogue; both inform the certificate proof but are not claimed to be sharpened in their general graph classes. WuDrton2023 gives the nearest complete partition-restricted equivalence theorem for innovation variances, a different parameter restriction. MakamReichenbachSeigal2023 supplies the target-local pooled-regression likelihood construction. WienobstLuttermannBannachLiskiewicz2023 informs consistent-extension enumeration with background arrows. Haughton1988 informs the smooth finite-model BIC argument. NandyMaathuisRichardson2017 and StriederDrton2023 inform the simultaneous effect-set inference rung. Targeted searches through September 2026 found no paper occupying the bounded binary-star target-method pair, the exact sharp dictionary count, or the stated singleton-permitting blocked equivalence criterion.

\section{Setup}

Target (estimand / structural parameter): \(\mathcal R(C)=\{D\in\mathfrak S_p:\mathcal M(D)=\mathcal M(C)\}\).
Primary functional / estimator / diagnostic: \(\mathcal R(C)=\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}\), certified on inequality by \(\mathsf{Route}(C,D)\).
\begin{itemize}
  \item \texttt{p} --- \texttt{positive integer}; \(p\in\mathbb N\) with \(p\geq 1\); \texttt{graph size}
  \par\smallskip\noindent\emph{Definition.} \texttt{Number of labelled system variables.}
  \item \texttt{V} --- \texttt{finite labelled vertex set}; \(V=[p]=\{1,\ldots,p\}\); \texttt{variable indices}
  \par\smallskip\noindent\emph{Definition.} \texttt{Common vertex set of every graph under comparison.}
  \item \texttt{pa} --- \texttt{parent-\allowbreak{}set map}; \(\operatorname{pa}_{K}(j)=\{i\in V:i\to j\text{ in }K\}\); \texttt{graph primitive}
  \par\smallskip\noindent\emph{Definition.} Parents of vertex \(j\) in a directed graph \(K\) on \(V\).
  \item \texttt{Kgraph} --- \texttt{directed graph}; \(\mathcal K\) on \(V\); \texttt{class member graph}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic directed graph in the blocked-\allowbreak{}state class.}
  \item \texttt{Xi} --- \texttt{family of set partitions}; \(\Xi=(\Xi_j)_{j\in V}\); \texttt{class member partition}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic target-\allowbreak{}indexed family of parent partitions.}
  \item \texttt{Astate} --- \texttt{generic graph-\allowbreak{}partition pair}; \(A=(\mathcal K,\Xi)\); \texttt{class binder}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic member used in the blocked-\allowbreak{}state class definition.}
  \item \texttt{StateClass} --- \texttt{finite blocked-\allowbreak{}state class}; \(\mathfrak S_p\); \texttt{singleton-\allowbreak{}permitting blocked edge-\allowbreak{}colouring class}
  \par\smallskip\noindent\emph{Definition.} Labelled acyclic graphs on \(V\) with one partition of each target parent set, including singleton blocks.
  \item \texttt{Estate} --- \texttt{generic blocked state}; \(E_0\in\mathfrak S_p\); \texttt{generic state}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic state used in model and invariant maps.}
  \item \texttt{GE} --- \texttt{DAG coordinate of a state}; \(G_{E_0}\) on \(V\); \texttt{generic causal graph}
  \par\smallskip\noindent\emph{Definition.} Directed acyclic graph coordinate of \(E_0\).
  \item \texttt{PiE} --- \texttt{parent-\allowbreak{}partition coordinate of a state}; \(\Pi^{E_0}=(\Pi^{E_0}_j)_{j\in V}\); \texttt{generic coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(E_0\), with \(\Pi^{E_0}_j\) partitioning \(\operatorname{pa}_{G_{E_0}}(j)\).
  \item \texttt{Ip} --- \texttt{identity matrix}; \(I_p\in\mathbb R^{p\times p}\); \texttt{SEM normalization}
  \par\smallskip\noindent\emph{Definition.} Identity matrix indexed by \(V\).
  \item \texttt{betaE} --- \texttt{state block-\allowbreak{}coefficient vector}; \(\beta_{E_0}=(\beta_{jB})_{j\in V,\,B\in\Pi^{E_0}_j}\in\mathbb R^{b_{E_0}}\); \texttt{structural coefficient parameter}
  \par\smallskip\noindent\emph{Definition.} One unrestricted real coefficient for each target-parent block, where \(b_{E_0}=\sum_{j\in V}|\Pi^{E_0}_j|\).
  \item \texttt{BE} --- \texttt{structural coefficient matrix}; \(B_{E_0}(\beta_{E_0})\in\mathbb R^{p\times p}\); \texttt{blocked structural-\allowbreak{}parameter assembly}
  \par\smallskip\noindent\emph{Definition.} The entry \((B_{E_0}(\beta_{E_0}))_{ji}\) equals \(\beta_{jB}\) when \(i\in B\in\Pi^{E_0}_j\), and equals zero for a nonparent.
  \item \texttt{omegaE} --- \texttt{positive innovation-\allowbreak{}variance vector}; \(\omega_{E_0}\in(0,\infty)^p\); \texttt{noise parameters}
  \par\smallskip\noindent\emph{Definition.} \texttt{Unrestricted target-\allowbreak{}specific innovation variances.}
  \item \texttt{ThetaE} --- \texttt{state parameter space}; \(\Theta_{E_0}=\mathbb R^{b_{E_0}}\times(0,\infty)^p\); \texttt{structural parameter domain}
  \par\smallskip\noindent\emph{Definition.} \texttt{Parameter space containing one real coefficient per block and one positive innovation variance per vertex.}
  \item \texttt{PhiE} --- \texttt{covariance parametrization}; \(\phi_{E_0}(\beta_{E_0},\omega_{E_0})=(I_p-B_{E_0}(\beta_{E_0}))^{-1}\operatorname{diag}(\omega_{E_0})(I_p-B_{E_0}(\beta_{E_0}))^{-\mathsf T}\); \texttt{observational-\allowbreak{}law map}
  \par\smallskip\noindent\emph{Definition.} Linear Gaussian SEM covariance map for state \(E_0\).
  \item \texttt{Model} --- \texttt{positive-\allowbreak{}definite covariance-\allowbreak{}model map}; \(\mathcal M(E_0)=\phi_{E_0}(\Theta_{E_0})\subseteq\mathbb S^p_{++}\); \texttt{population observational model}
  \par\smallskip\noindent\emph{Definition.} Full covariance image of state \(E_0\), including zero and coincident block coefficients.
  \item \texttt{Sigma} --- \texttt{generic positive-\allowbreak{}definite covariance matrix}; \(\Sigma\in\mathbb S^p_{++}\); \texttt{observable population argument}
  \par\smallskip\noindent\emph{Definition.} \texttt{Generic covariance argument for regression and effect maps.}
  \item \texttt{RegCoef} --- \texttt{population regression-\allowbreak{}coefficient map}; \(r_j(P;\Sigma)=\Sigma_{P,P}^{-1}\Sigma_{P,j}\); \texttt{certificate arithmetic}
  \par\smallskip\noindent\emph{Definition.} Coefficient vector from regressing coordinate \(j\) on coordinates indexed by \(P\subseteq V\setminus\{j\}\).
  \item \texttt{C} --- \texttt{blocked state}; \(C\in\mathfrak S_p\); \texttt{reference state}
  \par\smallskip\noindent\emph{Definition.} \texttt{First state under comparison and the true state in the statistical results.}
  \item \texttt{G} --- \texttt{DAG}; \(G=G_C\); \texttt{reference graph}
  \par\smallskip\noindent\emph{Definition.} Graph coordinate of \(C\).
  \item \texttt{Pi} --- \texttt{parent-\allowbreak{}partition family}; \(\Pi=\Pi^C\); \texttt{reference coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(C\).
  \item \texttt{D} --- \texttt{blocked state}; \(D\in\mathfrak S_p\); \texttt{comparison state}
  \par\smallskip\noindent\emph{Definition.} \texttt{Second state under comparison.}
  \item \texttt{H} --- \texttt{DAG}; \(H=G_D\); \texttt{comparison graph}
  \par\smallskip\noindent\emph{Definition.} Graph coordinate of \(D\).
  \item \texttt{Lambda} --- \texttt{parent-\allowbreak{}partition family}; \(\Lambda=\Pi^D\); \texttt{comparison coefficient blocks}
  \par\smallskip\noindent\emph{Definition.} Partition coordinate of \(D\).
  \item \texttt{Nfam} --- \texttt{nonsingleton-\allowbreak{}family map}; \(\mathcal N_j(E_0)=\{B\in\Pi^{E_0}_j:|B|\geq2\}\); \texttt{coefficient-\allowbreak{}partition invariant}
  \par\smallskip\noindent\emph{Definition.} Family of repeated-coefficient parent blocks at target \(j\).
  \item \texttt{Fset} --- \texttt{family-\allowbreak{}mismatch oriented-\allowbreak{}edge map}; \(F(E_0)=\{i\to j\in G_{E_0}:\mathcal N_i(E_0)\neq\mathcal N_j(E_0)\}\); \texttt{coefficient-\allowbreak{}compelled orientations}
  \par\smallskip\noindent\emph{Definition.} \texttt{Arrows whose endpoint nonsingleton families differ.}
  \item \texttt{Skel} --- \texttt{skeleton map}; \(\operatorname{skel}(G_{E_0})\); \texttt{ordinary Markov invariant}
  \par\smallskip\noindent\emph{Definition.} \texttt{Undirected skeleton of the state graph.}
  \item \texttt{UC} --- \texttt{unshielded-\allowbreak{}collider map}; \(\operatorname{uc}(G_{E_0})\); \texttt{ordinary Markov invariant}
  \par\smallskip\noindent\emph{Definition.} \texttt{Set of labelled unshielded collider triples of the state graph.}
  \item \texttt{Sig} --- \texttt{static blocked-\allowbreak{}equivalence signature}; \(\mathsf S(E_0)=(\operatorname{skel}(G_{E_0}),\operatorname{uc}(G_{E_0}),(\mathcal N_j(E_0))_{j\in V},F(E_0))\); \texttt{structure-\allowbreak{}recovery functional}
  \par\smallskip\noindent\emph{Definition.} \texttt{Static signature combining ordinary Markov information with repeated-\allowbreak{}block families and their compelled orientations.}
  \item \texttt{E} --- \texttt{source blocked state}; \(E\in\{C,D\}\); \texttt{certificate source}
  \par\smallskip\noindent\emph{Definition.} \texttt{State selected by the certificate router as the source of a separating covariance.}
  \item \texttt{J} --- \texttt{rejected blocked state}; \(J\in\{C,D\}\setminus\{E\}\); \texttt{certificate target}
  \par\smallskip\noindent\emph{Definition.} \texttt{State rejected by the separating certificate.}
  \item \texttt{z} --- \texttt{active target vertex}; \(z\in V\); \texttt{star center}
  \par\smallskip\noindent\emph{Definition.} \texttt{Unique target at which the router activates coefficient blocks.}
  \item \texttt{Ablocks} --- \texttt{active block subfamily}; \(\mathcal A_z\subseteq\Pi^E_z\); \texttt{sparse certificate support}
  \par\smallskip\noindent\emph{Definition.} One or two parent blocks activated at target \(z\).
  \item \texttt{Aunion} --- \texttt{active parent set}; \(A_z=\bigcup_{B\in\mathcal A_z}B\); \texttt{star leaves}
  \par\smallskip\noindent\emph{Definition.} Union of the active blocks at target \(z\).
  \item \texttt{Bstar} --- \texttt{binary-\allowbreak{}star coefficient matrix}; \(B^{\star}=e_z\mathbf 1_{A_z}^{\mathsf T}\); \texttt{bounded structural witness}
  \par\smallskip\noindent\emph{Definition.} Coefficient matrix with value one on every active arrow into \(z\) and zero elsewhere.
  \item \texttt{SigmaStar} --- \texttt{binary-\allowbreak{}star covariance matrix}; \(\Sigma^{\star}=(I_p+B^{\star})(I_p+B^{\star})^{\mathsf T}\); \texttt{separating observable witness}
  \par\smallskip\noindent\emph{Definition.} \texttt{Unit-\allowbreak{}innovation covariance generated by the binary-\allowbreak{}star coefficient matrix.}
  \item \texttt{hgt} --- \texttt{rational bit-\allowbreak{}height map}; \(h(a/b)=\lceil\log_2\max\{1,|a|,b\}\rceil\); \texttt{certificate complexity measure}
  \par\smallskip\noindent\emph{Definition.} \texttt{Bit height of a reduced rational number with positive denominator,\allowbreak{} extended entrywise by maxima to finite numerical objects.}
  \item \texttt{Bp} --- \texttt{explicit height bound}; \(B(p)=\lceil\log_2 p\rceil\); \texttt{arithmetic guarantee}
  \par\smallskip\noindent\emph{Definition.} \texttt{Proposed uniform upper bound for every numerical datum in the binary-\allowbreak{}star certificate.}
  \item \texttt{CertCI} --- \texttt{ordered conditional-\allowbreak{}independence certificate}; \(f_{uvS}(\Sigma)=\det(\Sigma_{[u,S],[v,S]})\); \texttt{skeleton-\allowbreak{}or-\allowbreak{}collider rejection}
  \par\smallskip\noindent\emph{Definition.} \texttt{Determinant certificate indexed by a topological predecessor set in the rejected graph.}
  \item \texttt{CertReg} --- \texttt{cleared blocked-\allowbreak{}regression certificate}; \(L_{j;u,v}(\Sigma)=(e_u-e_v)^{\mathsf T}\operatorname{adj}(\Sigma_{P,P})\Sigma_{P,j}\); \texttt{block-\allowbreak{}or-\allowbreak{}orientation rejection}
  \par\smallskip\noindent\emph{Definition.} Cleared difference between two parent-regression coefficients forced to agree by one rejected block, where \(P=\operatorname{pa}_{G_J}(j)\).
  \item \texttt{Router} --- \texttt{deterministic certificate algorithm}; \(\mathsf{Route}(C,D)=(E,J,z,\mathcal A_z,\mathsf{cert})\); \texttt{constructive separation map}
  \par\smallskip\noindent\emph{Definition.} \texttt{Lexicographic four-\allowbreak{}case router that first tests skeletons,\allowbreak{} then unshielded colliders,\allowbreak{} then nonsingleton target-\allowbreak{}block families,\allowbreak{} and finally unequal-\allowbreak{}family reversed arrows.}
  \item \texttt{W2} --- \texttt{finite binary-\allowbreak{}star library map}; \(\mathcal W_2(E_0)=\{\Sigma^{\star}:1\leq|\mathcal A_z|\leq2,\ z\in V,\ \mathcal A_z\subseteq\Pi^{E_0}_z\}\); \texttt{finite separating dictionary}
  \par\smallskip\noindent\emph{Definition.} All unit-noise stars formed by activating one or two whole blocks at a common target in state \(E_0\).
  \item \texttt{Rev} --- \texttt{partial state transformation}; \(\operatorname{rev}_{i\to j}(E_0)\); \texttt{local equivalence move}
  \par\smallskip\noindent\emph{Definition.} \texttt{Singleton-\allowbreak{}block covered reversal with endpoint partition transport.}
  \item \texttt{Rcomp} --- \texttt{legal-\allowbreak{}reversal component}; \(\mathcal R(E_0)\subseteq\mathfrak S_p\); \texttt{structural equivalence class}
  \par\smallskip\noindent\emph{Definition.} \texttt{Connected component generated by legal singleton covered reversals.}
  \item \texttt{ArrowGap} --- \texttt{arrow-\allowbreak{}disagreement count}; \(\delta(C,D)=|\{\{i,j\}:i\to j\text{ in }G,\ j\to i\text{ in }H\}|\); \texttt{path-\allowbreak{}length lower bound}
  \par\smallskip\noindent\emph{Definition.} Number of common skeleton edges oriented differently by \(G\) and \(H\).
  \item \texttt{dRev} --- \texttt{legal-\allowbreak{}reversal graph distance}; \(d_{\mathrm{rev}}(C,D)\in\mathbb N_0\cup\{\infty\}\); \texttt{component metric}
  \par\smallskip\noindent\emph{Definition.} Minimum number of legal reversals from \(C\) to \(D\), with infinity when no path exists.
  \item \texttt{Enum} --- \texttt{background-\allowbreak{}oriented extension enumerator}; \(\operatorname{Enum}(C)\); \texttt{exact component enumeration}
  \par\smallskip\noindent\emph{Definition.} Consistent-extension enumeration after orienting the ordinary CPDAG by \(F(C)\), followed by canonical restoration of fixed nonsingleton blocks and residual singleton blocks.
  \item \texttt{n} --- \texttt{sample size}; \(n\in\mathbb N\); \texttt{sampling index}
  \par\smallskip\noindent\emph{Definition.} \texttt{Number of independent observations.}
  \item \texttt{Xsample} --- \texttt{Gaussian observations}; \(X^{(1)},\ldots,X^{(n)}\in\mathbb R^p\); \texttt{observed sample}
  \par\smallskip\noindent\emph{Definition.} Independent centered Gaussian observations with covariance \(\Sigma_0\).
  \item \texttt{Sigma0} --- \texttt{true covariance matrix}; \(\Sigma_0\in\mathcal M(C)\); \texttt{population law}
  \par\smallskip\noindent\emph{Definition.} \texttt{Population covariance of the observations.}
  \item \texttt{Sn} --- \texttt{sample covariance matrix}; \(S_n=n^{-1}\sum_{r=1}^nX^{(r)}X^{(r)\mathsf T}\); \texttt{Gaussian sufficient statistic}
  \par\smallskip\noindent\emph{Definition.} \texttt{Empirical covariance matrix under known zero mean.}
  \item \texttt{BlockInc} --- \texttt{target-\allowbreak{}local block-\allowbreak{}incidence matrix}; \(A_{E_0j}\in\{0,1\}^{|P_{E_0j}|\times|\Pi^{E_0}_j|}\); \texttt{block-\allowbreak{}constrained regression design}
  \par\smallskip\noindent\emph{Definition.} For \(P_{E_0j}=\operatorname{pa}_{G_{E_0}}(j)\), rows are parents, columns are blocks, and \((A_{E_0j})_{iB}=\mathbf 1\{i\in B\}\).
  \item \texttt{BlockGram} --- \texttt{target-\allowbreak{}local block Gram map}; \(\Gamma_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},P_{E_0j}}A_{E_0j}\); \texttt{constrained least-\allowbreak{}squares matrix}
  \par\smallskip\noindent\emph{Definition.} Normal matrix for the block-sum regressors at target \(j\).
  \item \texttt{BlockScore} --- \texttt{target-\allowbreak{}local block covariance vector}; \(h_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},j}\); \texttt{constrained least-\allowbreak{}squares right-\allowbreak{}hand side}
  \par\smallskip\noindent\emph{Definition.} Covariance vector between the block-sum regressors and target \(j\).
  \item \texttt{LE} --- \texttt{off-\allowbreak{}model block-\allowbreak{}constrained coefficient map}; \(L_{E_0}:\mathbb S^p_{++}\to\mathbb R^{p\times p}\); \texttt{state-\allowbreak{}specific coefficient projection}
  \par\smallskip\noindent\emph{Definition.} Matrix obtained from the unique target-local block-constrained population regressions for state \(E_0\).
  \item \texttt{BIC} --- \texttt{global information criterion}; \(\operatorname{BIC}_n(E_0)=-2\sup_{\Sigma\in\mathcal M(E_0)}\ell_n(\Sigma)+d_{E_0}\log n\); \texttt{component selector}
  \par\smallskip\noindent\emph{Definition.} Gaussian BIC with \(d_{E_0}=p+\sum_{j\in V}|\Pi^{E_0}_j|\) and a deterministic fallback when \(S_n\) is singular.
  \item \texttt{Chat} --- \texttt{BIC-\allowbreak{}selected blocked state}; \(\widehat C_n\in\operatorname*{argmin}_{E_0\in\mathfrak S_p}\operatorname{BIC}_n(E_0)\); \texttt{estimated component representative}
  \par\smallskip\noindent\emph{Definition.} \texttt{Global BIC minimizer under a fixed deterministic tie rule.}
  \item \texttt{Qpairs} --- \texttt{finite ordered intervention-\allowbreak{}response list}; \(\mathcal Q\subseteq\{(a,y)\in V^2:a\neq y\}\); \texttt{causal query menu}
  \par\smallskip\noindent\emph{Definition.} \texttt{Prespecified ordered pairs for total-\allowbreak{}effect reporting.}
  \item \texttt{gEffect} --- \texttt{member-\allowbreak{}labelled total-\allowbreak{}effect functional}; \(g_{E_0}(\Sigma)=(((I_p-L_{E_0}(\Sigma))^{-1})_{ya})_{(a,y)\in\mathcal Q}\); \texttt{state-\allowbreak{}specific causal functional}
  \par\smallskip\noindent\emph{Definition.} \texttt{State-\allowbreak{}specific vector of linear-\allowbreak{}SEM total effects obtained from the block-\allowbreak{}constrained coefficient map.}
  \item \texttt{EffectSet} --- \texttt{finite component effect-\allowbreak{}set map}; \(\mathcal T_{E_0}(\Sigma)=\{g_{E'}(\Sigma):E'\in\mathcal R(E_0)\}\); \texttt{set-\allowbreak{}valued causal target}
  \par\smallskip\noindent\emph{Definition.} \texttt{Member-\allowbreak{}labelled total-\allowbreak{}effect vectors over a legal-\allowbreak{}reversal component.}
  \item \texttt{Exc} --- \texttt{algebraic exceptional-\allowbreak{}set map}; \(\mathcal E_C\subseteq\Theta_C\); \texttt{genericity boundary}
  \par\smallskip\noindent\emph{Definition.} \texttt{Union of nonequivalent-\allowbreak{}model intersection pullbacks and zero-\allowbreak{}gradient loci used in the pointwise generic statistical results.}
  \item \texttt{alpha} --- \texttt{error probability}; \(\alpha\in(0,1)\); \texttt{inference level}
  \par\smallskip\noindent\emph{Definition.} \texttt{Familywise noncoverage level.}
  \item \texttt{Conf} --- \texttt{simultaneous member-\allowbreak{}labelled confidence collection}; \(\mathfrak I_{n,\alpha}(\widehat C_n)\); \texttt{effect-\allowbreak{}set confidence procedure}
  \par\smallskip\noindent\emph{Definition.} \texttt{Bonferroni delta-\allowbreak{}method intervals for every selected-\allowbreak{}component member and query,\allowbreak{} with exact singleton intervals for structurally zero coordinates.}
  \item \texttt{Hopt} --- \texttt{certificate lower-\allowbreak{}bound handle}; \(\mathsf H_{\mathrm{opt}}\); \texttt{open optimality route}
  \par\smallskip\noindent\emph{Definition.} \texttt{Support-\allowbreak{}rank and determinant-\allowbreak{}divisibility analysis on crossing partitions and unequal-\allowbreak{}family reversed-\allowbreak{}edge subinstances.}
\end{itemize}

\section{Assumptions}

\begin{assumption}[ass:acyclic-member]\label{ass:acyclic-member}
The directed graph \(\mathcal K\) is acyclic.
\par\smallskip\noindent\emph{Standard} (Directed acyclicity, \cite{Chickering1995}).
\end{assumption}

\begin{assumption}[ass:target-parent-partition]\label{ass:target-parent-partition}
For every \(j\in V\), \(\Xi_j\) is a partition of \(\operatorname{pa}_{\mathcal K}(j)\), with singleton blocks permitted.
\par\smallskip\noindent\emph{Standard} (Blocked target-local edge colouring with a singleton-permitting variant, \cite{BoegeKubjasMisraSolus2026}).
\end{assumption}

\section{Definitions}

\begin{definition}[def:blocked-state-class]\label{def:blocked-state-class}
\par\smallskip\noindent\emph{Defined object.} \texttt{Singleton-\allowbreak{}permitting blocked-\allowbreak{}state class}
\par\smallskip\noindent\emph{Construction.} \[\mathfrak S_p=\left\{A=(\mathcal K,\Xi):\mathcal K\text{ is an acyclic directed graph on }V,\ \Xi_j\text{ is a partition of }\operatorname{pa}_{\mathcal K}(j)\text{ for every }j\in V\right\}.\]
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}acyclic-\allowbreak{}member}; \texttt{ass:\allowbreak{}target-\allowbreak{}parent-\allowbreak{}partition}.
\end{definition}

\begin{definition}[def:covariance-model]\label{def:covariance-model}
\par\smallskip\noindent\emph{Defined object.} \texttt{Full blocked covariance model}
\par\smallskip\noindent\emph{Construction.} For \(E_0\in\mathfrak S_p\), let \(b_{E_0}=\sum_{j\in V}|\Pi^{E_0}_j|\), \(\Theta_{E_0}=\mathbb R^{b_{E_0}}\times(0,\infty)^p\), and \[\mathcal M(E_0)=\left\{(I_p-B_{E_0}(\beta_{E_0}))^{-1}\operatorname{diag}(\omega_{E_0})(I_p-B_{E_0}(\beta_{E_0}))^{-\mathsf T}:(\beta_{E_0},\omega_{E_0})\in\Theta_{E_0}\right\}.\] The entry \((B_{E_0}(\beta_{E_0}))_{ji}\) equals \(\beta_{jB}\) for \(i\in B\in\Pi^{E_0}_j\), and all real block coefficients are admitted.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:nonsingleton-family]\label{def:nonsingleton-family}
\par\smallskip\noindent\emph{Defined object.} \texttt{Nonsingleton parent-\allowbreak{}block family}
\par\smallskip\noindent\emph{Construction.} \[\mathcal N_j(E_0)=\{B\in\Pi^{E_0}_j:|B|\geq2\},\qquad j\in V.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:family-mismatch-edges]\label{def:family-mismatch-edges}
\par\smallskip\noindent\emph{Defined object.} \texttt{Family-\allowbreak{}mismatch orientation set}
\par\smallskip\noindent\emph{Construction.} \[F(E_0)=\{i\to j\in G_{E_0}:\mathcal N_i(E_0)\neq\mathcal N_j(E_0)\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:static-signature]\label{def:static-signature}
\par\smallskip\noindent\emph{Defined object.} \texttt{Static blocked signature}
\par\smallskip\noindent\emph{Construction.} \[\mathsf S(E_0)=\left(\operatorname{skel}(G_{E_0}),\operatorname{uc}(G_{E_0}),(\mathcal N_j(E_0))_{j\in V},F(E_0)\right).\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:certificate-formats]\label{def:certificate-formats}
\par\smallskip\noindent\emph{Defined object.} \texttt{Observable rejection certificates}
\par\smallskip\noindent\emph{Construction.} For a rejected state \(J\), an ordered conditional-independence certificate chooses a topological order \(\pi_J\), a nonadjacent pair \(u,v\) with \(u\) preceding \(v\), and \(S=\operatorname{Pred}_{\pi_J}(v)\setminus\{u\}\), and evaluates \(f_{uvS}(\Sigma)=\det(\Sigma_{[u,S],[v,S]})\). A blocked-regression certificate chooses \(j\in V\) and distinct \(u,v\) in one block of \(\Pi^J_j\), puts \(P=\operatorname{pa}_{G_J}(j)\), and evaluates \(L_{j;u,v}(\Sigma)=(e_u-e_v)^{\mathsf T}\operatorname{adj}(\Sigma_{P,P})\Sigma_{P,j}\).
\par\smallskip\noindent\emph{Inputs.} \texttt{J}; \texttt{Sigma}.
\end{definition}

\begin{definition}[def:binary-star-library]\label{def:binary-star-library}
\par\smallskip\noindent\emph{Defined object.} \texttt{One-\allowbreak{}target binary-\allowbreak{}star library}
\par\smallskip\noindent\emph{Construction.} For \(E_0\in\mathfrak S_p\), choose \(z\in V\) and \(\mathcal A_z\subseteq\Pi^{E_0}_z\) with \(1\leq|\mathcal A_z|\leq2\), set \(A_z=\bigcup_{B\in\mathcal A_z}B\), \(B^{\star}=e_z\mathbf 1_{A_z}^{\mathsf T}\), and \[\Sigma^{\star}=(I_p+B^{\star})(I_p+B^{\star})^{\mathsf T}=I_p+e_z\mathbf 1_{A_z}^{\mathsf T}+\mathbf 1_{A_z}e_z^{\mathsf T}+|A_z|e_ze_z^{\mathsf T}.\] The set of all such covariances is \(\mathcal W_2(E_0)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:four-case-router]\label{def:four-case-router}
\par\smallskip\noindent\emph{Defined object.} \texttt{Four-\allowbreak{}case binary-\allowbreak{}star router}
\par\smallskip\noindent\emph{Construction.} Order vertices, blocks, and pairs lexicographically. For \(\mathsf S(C)\neq\mathsf S(D)\), \(\mathsf{Route}(C,D)\) applies the first available rule: a skeleton mismatch activates the source block containing the differing arrow tail and returns the rejected ordered conditional-independence certificate; a collider mismatch activates the source block or two source blocks containing the collider parents and returns the rejected ordered conditional-independence certificate; a nonsingleton-family mismatch chooses crossing edge-support partition atoms, activates the source atom, and returns the rejected within-block regression certificate; an \(F\)-mismatch chooses a reversed \(x\to y\) with \(\mathcal N_x\neq\mathcal N_y\), selects a direction in which a block \(S\in\mathcal N_x\) is split by \(\Pi^E_y\), activates \(\{x\}\) and the first block \(T\in\Pi^E_y\) meeting but not containing \(S\), and returns the rejected regression difference between the first elements of \(S\cap T\) and \(S\setminus T\).
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{D}; \texttt{def:\allowbreak{}static-\allowbreak{}signature}; \texttt{def:\allowbreak{}certificate-\allowbreak{}formats}; \texttt{def:\allowbreak{}binary-\allowbreak{}star-\allowbreak{}library}.
\end{definition}

\begin{definition}[def:legal-reversal]\label{def:legal-reversal}
\par\smallskip\noindent\emph{Defined object.} \texttt{Partition-\allowbreak{}compatible singleton covered reversal}
\par\smallskip\noindent\emph{Construction.} For \(i\to j\) in \(G_{E_0}\), \(\operatorname{rev}_{i\to j}(E_0)\) is defined exactly when \(\operatorname{pa}_{G_{E_0}}(j)=\operatorname{pa}_{G_{E_0}}(i)\cup\{i\}\), \(\{i\}\in\Pi^{E_0}_j\), and \(\Pi^{E_0}_j\setminus\{\{i\}\}=\Pi^{E_0}_i\). It reverses \(i\to j\), sets \(\Pi'_j=\Pi^{E_0}_i\) and \(\Pi'_i=\Pi^{E_0}_i\cup\{\{j\}\}\), and leaves every other partition unchanged.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:reversal-component]\label{def:reversal-component}
\par\smallskip\noindent\emph{Defined object.} \texttt{Legal-\allowbreak{}reversal component}
\par\smallskip\noindent\emph{Construction.} \[\mathcal R(E_0)=\{E'\in\mathfrak S_p:E'\text{ is reached from }E_0\text{ by a finite sequence of defined }\operatorname{rev}_{i\to j}\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}.
\end{definition}

\begin{definition}[def:component-enumerator]\label{def:component-enumerator}
\par\smallskip\noindent\emph{Defined object.} \texttt{Background-\allowbreak{}oriented consistent-\allowbreak{}extension enumerator}
\par\smallskip\noindent\emph{Construction.} Form the ordinary CPDAG of \(G\), orient every arrow in \(F(C)\), apply closure under the standard consistent-extension rules, and enumerate every acyclic extension \(K\) with the same unshielded colliders. For each extension emit the unique lift \(E_K=(K,\Pi^K)\) with \[\Pi^K_j=\mathcal N_j(C)\cup\left\{\{a\}:a\in\operatorname{pa}_K(j)\setminus\bigcup\mathcal N_j(C)\right\},\qquad j\in V.\] The emitted set is \(\operatorname{Enum}(C)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{def:\allowbreak{}nonsingleton-\allowbreak{}family}; \texttt{def:\allowbreak{}family-\allowbreak{}mismatch-\allowbreak{}edges}.
\end{definition}

\begin{definition}[def:off-model-block-ls]\label{def:off-model-block-ls}
\par\smallskip\noindent\emph{Defined object.} \texttt{Off-\allowbreak{}model target-\allowbreak{}local block-\allowbreak{}constrained least-\allowbreak{}squares map}
\par\smallskip\noindent\emph{Construction.} Fix \(E_0\in\mathfrak S_p\), \(\Sigma\in\mathbb S^p_{++}\), and \(j\in V\). Put \(P_{E_0j}=\operatorname{pa}_{G_{E_0}}(j)\). Let \(A_{E_0j}\) have rows \(i\in P_{E_0j}\), columns \(B\in\Pi^{E_0}_j\), and entries \((A_{E_0j})_{iB}=\mathbf 1\{i\in B\}\). For a nonempty parent set define \(\Gamma_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},P_{E_0j}}A_{E_0j}\), \(h_{E_0j}(\Sigma)=A_{E_0j}^{\mathsf T}\Sigma_{P_{E_0j},j}\), and \(\beta_{E_0j}(\Sigma)=\Gamma_{E_0j}(\Sigma)^{-1}h_{E_0j}(\Sigma)\). Repeat each block coefficient on its member parents and put zero outside the parent set to obtain \(L_{E_0}(\Sigma)\); use a zero row for an empty parent set.
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}; \texttt{Sigma}.
\end{definition}

\begin{definition}[def:global-bic]\label{def:global-bic}
\par\smallskip\noindent\emph{Defined object.} \texttt{Global blocked-\allowbreak{}state BIC}
\par\smallskip\noindent\emph{Construction.} For \(E_0\in\mathfrak S_p\), set \(d_{E_0}=p+\sum_{j\in V}|\Pi^{E_0}_j|\). If \(S_n\in\mathbb S^p_{++}\), compute the attained statewise Gaussian likelihood using the target-local systems in \(\texttt{def:off-model-block-ls}\) and set \(\operatorname{BIC}_n(E_0)=-2\sup_{\Sigma\in\mathcal M(E_0)}\ell_n(\Sigma)+d_{E_0}\log n\). If \(S_n\notin\mathbb S^p_{++}\), set \(\operatorname{BIC}_n(E_0)=d_{E_0}\log n\). Let \(\widehat C_n\) be the minimizer under a fixed ordering of \(\mathfrak S_p\).
\par\smallskip\noindent\emph{Inputs.} \texttt{Sn}; \texttt{def:\allowbreak{}off-\allowbreak{}model-\allowbreak{}block-\allowbreak{}ls}.
\end{definition}

\begin{definition}[def:component-effect-set]\label{def:component-effect-set}
\par\smallskip\noindent\emph{Defined object.} \texttt{Component total-\allowbreak{}effect set}
\par\smallskip\noindent\emph{Construction.} For \(E_0\in\mathfrak S_p\), \(\Sigma\in\mathbb S^p_{++}\), and finite \(\mathcal Q\), define \(g_{E'}(\Sigma)=(((I_p-L_{E'}(\Sigma))^{-1})_{ya})_{(a,y)\in\mathcal Q}\) for each \(E'\in\mathcal R(E_0)\), and set \[\mathcal T_{E_0}(\Sigma)=\{g_{E'}(\Sigma):E'\in\mathcal R(E_0)\}.\]
\par\smallskip\noindent\emph{Inputs.} \texttt{Estate}; \texttt{Sigma}; \texttt{Qpairs}; \texttt{def:\allowbreak{}off-\allowbreak{}model-\allowbreak{}block-\allowbreak{}ls}.
\end{definition}

\begin{definition}[def:generic-exception]\label{def:generic-exception}
\par\smallskip\noindent\emph{Defined object.} \texttt{Generic statistical exception set}
\par\smallskip\noindent\emph{Construction.} For \(C\in\mathfrak S_p\), \(\mathcal E_C\) is the finite union, over \(D\in\mathfrak S_p\) with \(d_D\leq d_C\) and \(\mathcal M(D)\neq\mathcal M(C)\), of the polynomial pullbacks to \(\Theta_C\) of \(\mathcal M(C)\cap\mathcal M(D)\), together with the polynomial zero-gradient loci of the nonconstant coordinates reported by \(\mathcal T_C\).
\par\smallskip\noindent\emph{Inputs.} \texttt{C}; \texttt{Qpairs}.
\end{definition}

\begin{definition}[def:simultaneous-region]\label{def:simultaneous-region}
\par\smallskip\noindent\emph{Defined object.} \texttt{Simultaneous component-\allowbreak{}effect confidence collection}
\par\smallskip\noindent\emph{Construction.} Select \(\widehat C_n\) by \(\texttt{def:global-bic}\). On \(S_n\in\mathbb S^p_{++}\), retain member labels, evaluate every rational map \(g_E(S_n)\) and its derivative, estimate its variance from the Gaussian covariance limit, and use Bonferroni critical value \(z_{1-\alpha/(2K)}\) for the \(K\) nonconstant labelled coordinates; assign exact singleton intervals to structurally zero coordinates. On \(S_n\notin\mathbb S^p_{++}\), assign \(\mathbb R\) to every coordinate. The resulting collection is \(\mathfrak I_{n,\alpha}(\widehat C_n)\).
\par\smallskip\noindent\emph{Inputs.} \texttt{Sn}; \texttt{Chat}; \texttt{alpha}; \texttt{def:\allowbreak{}global-\allowbreak{}bic}; \texttt{def:\allowbreak{}component-\allowbreak{}effect-\allowbreak{}set}.
\end{definition}

\begin{definition}[def:certificate-optimality-handle]\label{def:certificate-optimality-handle}
\par\smallskip\noindent\emph{Defined object.} \texttt{Certificate Pareto-\allowbreak{}lower-\allowbreak{}bound handle}
\par\smallskip\noindent\emph{Construction.} Apply \(\mathsf H_{\mathrm{opt}}\): restrict universal separator schemes to minimal crossing-partition and unequal-family reversal subinstances, translate active-block support into regression support rank, and use exact determinant divisibility and rational-height counting to lower-bound jointly the number of active blocks and the largest numerical height.
\par\smallskip\noindent\emph{Inputs.} \texttt{def:\allowbreak{}certificate-\allowbreak{}formats}; \texttt{def:\allowbreak{}binary-\allowbreak{}star-\allowbreak{}library}.
\end{definition}

\section{Main results}

\begin{lemma}[lem:binary-star-regression-calculus]\label{lem:binary-star-regression-calculus}
Fix \(z\in V\) and \(A_z\subseteq V\setminus\{z\}\), and let \(\Sigma^{\star}=I_p+e_z\mathbf 1_{A_z}^{\mathsf T}+\mathbf 1_{A_z}e_z^{\mathsf T}+|A_z|e_ze_z^{\mathsf T}\). For \(j\in V\) and \(P\subseteq V\setminus\{j\}\), write \(r=r_j(P;\Sigma^{\star})\). If \(j=z\), then \(r_q=\mathbf 1\{q\in A_z\}\) for \(q\in P\). If \(j\neq z\) and either \(z\notin P\) or \(j\notin A_z\), then \(r=0\). If \(j\in A_z\) and \(z\in P\), put \(Q=P\setminus\{z\}\) and \(d=1+|A_z\setminus Q|\); then \(r_z=1/d\), \(r_q=-1/d\) for \(q\in A_z\cap Q\), \(r_q=0\) for \(q\in Q\setminus A_z\), and \(\det(\Sigma^{\star}_{P,P})=d\).
\end{lemma}
\begin{proof}
Put \(a=\mathbf 1_{A_z}\).  By \(\texttt{def:binary-star-library}\),
\[
 \Sigma^{\star}_{qq}=1\ (q\ne z),\qquad
 \Sigma^{\star}_{zz}=1+|A_z|,\qquad
 \Sigma^{\star}_{zq}=\Sigma^{\star}_{qz}=a_q,
\]
and all other off-diagonal entries vanish.  If \(j=z\), then \(P\subseteq V\setminus\{z\}\), so
\[
 \Sigma^{\star}_{P,P}=I_{|P|},\qquad \Sigma^{\star}_{P,z}=a_P.
\]
The definition of \(r_j\) gives \(r_q=\mathbf 1\{q\in A_z\}\).

Suppose \(j\ne z\).  If \(z\notin P\), then \(\Sigma^{\star}_{P,j}=0\), because \(j\notin P\) and distinct noncentral coordinates are uncorrelated.  If \(j\notin A_z\), the same conclusion holds even when \(z\in P\), because \(\Sigma^{\star}_{zj}=0\).  Positive definiteness of \(\Sigma^{\star}_{P,P}\) therefore gives \(r=0\) in both cases.

It remains that \(j\in A_z\) and \(z\in P\).  Set \(Q=P\setminus\{z\}\), \(c=\mathbf 1_{A_z\cap Q}\), and order \(P\) as \(Q,z\).  Then
\[
 \Sigma^{\star}_{P,P}=
 \begin{pmatrix}I_{|Q|}&c\\ c^{\mathsf T}&1+|A_z|\end{pmatrix},
 \qquad
 \Sigma^{\star}_{P,j}=\binom{0}{1}.
\]
For
\[
 d=1+|A_z|-|A_z\cap Q|=1+|A_z\setminus Q|,
\]
direct multiplication gives
\[
 \begin{pmatrix}I_{|Q|}&c\\ c^{\mathsf T}&1+|A_z|\end{pmatrix}
 \binom{-c/d}{1/d}=\binom{0}{1}.
\]
Thus \(r_z=1/d\), \(r_q=-1/d\) on \(A_z\cap Q\), and \(r_q=0\) on \(Q\setminus A_z\).  The Schur-complement determinant identity gives
\[
 \det(\Sigma^{\star}_{P,P})=1+|A_z|-c^{\mathsf T}c=d.
\]
Here \(j\in A_z\setminus Q\), so \(2\le d\le1+|A_z|\le p\); in particular, every division above is valid.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The exact regression table turns each combinatorial mismatch into an integer-valued observable certificate and controls every denominator. \textit{Gap.} SullivantTalaskaDraisma2010 expands covariance minors by trek systems, and DrtonRobevaWeihs2020 clears rational constraints through nested determinants; neither derives this one-target block-colour regression table. \textit{Consumer.} The four-case router uses the table to verify nonmembership without Gröbner-basis computation or numerical tolerance choices.

\begin{theorem}[thm:bounded-binary-star-separation]\label{thm:bounded-binary-star-separation}
For all \(C,D\in\mathfrak S_p\) with \(\mathsf S(C)\neq\mathsf S(D)\), \(\mathsf{Route}(C,D)\) returns \(E\in\{C,D\}\), \(J\in\{C,D\}\setminus\{E\}\), \(z\in V\), and \(\mathcal A_z\subseteq\Pi^E_z\) with \(1\leq|\mathcal A_z|\leq2\). With every innovation variance equal to one, every block coefficient in \(E\) equal to one on \(\mathcal A_z\) and zero elsewhere, and \(A_z=\bigcup_{B\in\mathcal A_z}B\), the resulting covariance satisfies \[B^{\star}=e_z\mathbf 1_{A_z}^{\mathsf T},\qquad (B^{\star})^2=0,\qquad \Sigma^{\star}=(I_p+B^{\star})(I_p+B^{\star})^{\mathsf T}\in\mathcal M(E)\setminus\mathcal M(J).\] Every entry of \(\Sigma^{\star}\) is an integer in \(\{0,1,\ldots,p\}\), \(\det(\Sigma^{\star})=1\), and the router returns either an ordered certificate \(f_{uvS}(\Sigma^{\star})\) or a blocked-regression certificate \(L_{j;u,v}(\Sigma^{\star})\) whose value belongs to \(\{-2,-1,1,2\}\). Every recovered regression coefficient in the certificate has numerator in \(\{-1,0,1\}\) and reduced denominator at most \(p\), so all supplied numerical data have height at most \(B(p)=\lceil\log_2p\rceil\). The route uses polynomially many graph and rational-arithmetic operations in \(p\). For \(p=1\), the implication is vacuous.
\end{theorem}
\begin{proof}
We first prove that the certificate formats vanish on the rejected model.  Fix a rejected state \(J\) and a topological order \(\pi_J\).  If \(u,v\) are nonadjacent, \(u\) precedes \(v\), and \(S=\operatorname{Pred}_{\pi_J}(v)\setminus\{u\}\), then \(\operatorname{pa}_{G_J}(v)\subseteq S\).  In the acyclic SEM of \(\texttt{def:covariance-model}\), the innovation of \(X_v\) is independent of all predecessor variables and its structural mean is measurable with respect to \(X_S\).  Hence
\[
 \operatorname{Cov}(X_u,X_v\mid X_S)=0.
\]
Positive definiteness and the Schur-complement formula yield
\[
 f_{uvS}(\Sigma)=\det(\Sigma_{S,S})
 \bigl(\Sigma_{uv}-\Sigma_{uS}\Sigma_{S,S}^{-1}\Sigma_{Sv}\bigr)=0,
\]
where \(\det(\Sigma_{\varnothing,\varnothing})=1\).  Likewise, for \(P=\operatorname{pa}_{G_J}(j)\), the innovation of \(X_j\) is independent of \(X_P\), so \(r_j(P;\Sigma)\) is its structural parent-coefficient vector.  The block constraint gives \(r_u=r_v\) for \(u,v\) in one block, whence
\[
 L_{j;u,v}(\Sigma)=\det(\Sigma_{P,P})(r_u-r_v)=0.
\]
These arguments use acyclicity, positive innovation variances, and the parent-block restriction, with no faithfulness assumption.

Assume now that \(\mathsf S(C)\ne\mathsf S(D)\).  The cases are disjoint because the router uses the first mismatch.

\emph{Skeleton mismatch.}
Choose an adjacency \(\{a,z\}\) present only in a source state \(E\), oriented \(a\to z\), and activate the block of \(\Pi^E_z\) containing \(a\).  Let \(J\) be the other state.  Order the nonadjacent endpoints as \(u,v\) in a topological order of \(J\), and put \(S=\operatorname{Pred}_{\pi_J}(v)\setminus\{u\}\).  Neither endpoint is in \(S\), and \(\Sigma^{\star}_{S,S}=I\).  Distinct active leaves are independent, so conditioning on active leaves in \(S\) does not change \(\operatorname{Cov}(X_a,X_z)=1\).  Therefore
\[
 f_{uvS}(\Sigma^{\star})=1.
\]

\emph{Collider mismatch.}
With equal skeletons, choose \(a\to z\leftarrow c\) as an unshielded collider only in \(E\), and activate the one or two blocks at \(z\) containing \(a,c\).  In any topological order of the noncollider state \(J\), the center \(z\) precedes the later endpoint \(v\) of \(a,c\), in each possible fork or chain orientation.  Let \(u\) be the earlier endpoint, \(S=\operatorname{Pred}_{\pi_J}(v)\setminus\{u\}\), and \(R=S\setminus\{z\}\).  Then \(a,c\notin S\), \(z\in S\), and projection of \(X_z\) on \(X_R\), using \(\texttt{lem:binary-star-regression-calculus}\), gives
\[
 d=1+|A_z\setminus R|,\qquad
 \det(\Sigma^{\star}_{S,S})=d,\qquad
 \operatorname{Cov}(X_a,X_c\mid X_S)=-d^{-1}.
\]
Thus \(f_{uvS}(\Sigma^{\star})=-1\).

\emph{Nonsingleton-family mismatch.}
For \(B\in\Pi^E_j\), define the edge atom
\[
 \mathscr E_j(B)=\{\{i,j\}:i\in B\}.
\]
These atoms partition the skeleton edge set.  An atom with at least two edges has a unique common center, so it recovers its target and nonsingleton block.  Thus equal skeletons but unequal vertex-indexed nonsingleton families give unequal edge-atom partitions.  For two unequal finite partitions, after possibly interchanging their roles, there are a source atom \(U\) and rejected atom \(K\) with
\[
 U\cap K\ne\varnothing,\qquad K\setminus U\ne\varnothing;
\]
otherwise the two partitions refine one another.  Activate the single source block represented by \(U\), with center \(z\), and let \(K\) have center \(j\).

If \(j=z\), choose rejected-block parents \(u,v\) represented by edges in \(U\cap K\) and \(K\setminus U\).  Then the regression lemma gives \(r_u=1\), \(r_v=0\), and \(\det(\Sigma^{\star}_{P,P})=1\), so \(L_{j;u,v}(\Sigma^{\star})=1\).  If \(j\ne z\), the two star atoms meet only in \(\{j,z\}\).  Hence \(j\to z\) in \(E\), \(z\to j\) in \(J\), and \(z\) is in \(K\).  For any other parent \(v\in K\), the regression lemma gives
\[
 r_z=d^{-1},\qquad r_v\in\{0,-d^{-1}\},\qquad
 \det(\Sigma^{\star}_{P,P})=d,
\]
so \(L_{j;z,v}(\Sigma^{\star})\in\{1,2\}\).

\emph{Family-mismatch orientation.}
The preceding invariants now agree, but \(F(C)\ne F(D)\).  There is an adjacency oriented \(x\to y\) in one state and \(y\to x\) in the other with \(\mathcal N_x\ne\mathcal N_y\).  This edge is a singleton block at both receiving endpoints; a nonsingleton occurrence would contradict equality of the corresponding vertex-indexed family.

Take the state with \(x\to y\).  If \(S\in\mathcal N_x\) and \(s\in S\), then \(s\to x\) occurs in both states.  If \(s,y\) were nonadjacent, reversing \(x-y\) would change \(s\to x\to y\) into the unshielded collider \(s\to x\leftarrow y\), contrary to equality of collider sets.  They are adjacent, and acyclicity rules out \(y\to s\) because \(s\to x\to y\to s\) would be a cycle.  Hence
\[
 S\subseteq\operatorname{pa}_{G_E}(y).
\]
Interchanging states and endpoints gives \(T\subseteq\operatorname{pa}_{G_J}(x)\) for every \(T\in\mathcal N_y\).

Some tail block is split by the source partition at the head in at least one direction.  Otherwise every \(S\in\mathcal N_x\) lies in a \(T\in\mathcal N_y\), and every such \(T\) lies in a \(U\in\mathcal N_x\).  Then \(S\subseteq T\subseteq U\); since \(S,U\) intersect and are blocks of the same partition, \(S=U\), so \(S=T=U\).  Applying the symmetric argument to every \(T\) gives \(\mathcal N_x=\mathcal N_y\), a contradiction.

Rename the selected direction so that \(x\to y\) is in \(E\), \(S\in\mathcal N_x\) is split by \(\Pi^E_y\), and \(T\in\Pi^E_y\) meets but does not contain \(S\).  Activate the distinct blocks \(\{x\}\) and \(T\) at \(y\).  In \(J\), regress \(x\) on \(P=\operatorname{pa}_{G_J}(x)\), set \(Q=P\setminus\{y\}\), and choose \(s\in S\cap T\), \(t\in S\setminus T\).  The common block \(S\) is contained in \(Q\).  Since \(A_y=\{x\}\cup T\), the regression lemma yields
\[
 d=1+|A_y\setminus Q|=2+|T\setminus Q|,\qquad
 r_s=-d^{-1},\qquad r_t=0,\qquad
 \det(\Sigma^{\star}_{P,P})=d.
\]
Thus \(L_{x;s,t}(\Sigma^{\star})=-1\).  The term \(T\setminus Q\) explicitly covers extra parents, and other reversed arrows have zero coefficient in this star.

The four cases exhaust unequal signatures and activate at most two whole blocks at one target.  Set all other source coefficients to zero and all innovation variances to one.  Since \(z\notin A_z\),
\[
 (B^{\star})^2=e_z(\mathbf 1_{A_z}^{\mathsf T}e_z)\mathbf 1_{A_z}^{\mathsf T}=0,
 \qquad (I_p-B^{\star})^{-1}=I_p+B^{\star}.
\]
Thus the constructed covariance is the displayed \(\Sigma^{\star}\in\mathcal M(E)\).  Its nonzero certificate and the universal vanishing proved above imply \(\Sigma^{\star}\notin\mathcal M(J)\).  In a topological order \(I_p+B^{\star}\) is unit triangular, so
\[
 \det(\Sigma^{\star})=\det(I_p+B^{\star})^2=1.
\]
Its entries are \(0,1\), and the center variance \(1+|A_z|\le p\).  Every recovered coefficient is \(0\), \(1\), or \(\pm1/d\), with \(2\le d\le p\) whenever division occurs; the cleared certificate values above lie in \(\{-1,1,2\}\).  Hence every regression numerator is in \(\{-1,0,1\}\), every denominator and every other numerical magnitude is at most \(p\), and the height is at most \(B(p)=\lceil\log_2p\rceil\).  For \(p=1\) the implication is vacuous.

Finally, skeleton comparison, collider enumeration, atom comparison, containment scans, topological sorting, and elimination on matrices of order at most \(p\) all use polynomially many graph or exact rational-arithmetic operations.  This proves the runtime assertion.
\end{proof}
\par\smallskip\noindent \textit{Justification.} This is the citation-independent necessity bridge: every inequivalent signature receives a sparse, bounded, exactly checkable observable witness. \textit{Gap.} BoegeKubjasMisraSolus2026, Theorem 19 and Question 20, gives proper-blocked identifiability and an ideal-based equivalence test but no bounded witness theorem; SullivantTalaskaDraisma2010 and DrtonRobevaWeihs2020 give determinant-vanishing machinery without coefficient-colour height guarantees. \textit{Consumer.} A coloured-DAG solver can return a short certificate explaining why two fitted causal-community states are observationally inequivalent.

\begin{theorem}[thm:complete-blocked-equivalence]\label{thm:complete-blocked-equivalence}
For all \(C,D\in\mathfrak S_p\), the following are equivalent: \(\mathcal M(C)=\mathcal M(D)\); \(\mathsf S(C)=\mathsf S(D)\); and \(D\in\mathcal R(C)\). Every legal reversal preserves the full covariance image, including zero coefficients. If these conditions hold, then \(d_{\mathrm{rev}}(C,D)=\delta(C,D)\), and a shortest path reverses every initially disagreeing arrow exactly once. Thus the signature is an exact recovery functional for the covariance-model component, not merely a sufficient invariant.
\end{theorem}
\begin{proof}
If \(\mathcal M(C)=\mathcal M(D)\) but \(\mathsf S(C)\ne\mathsf S(D)\), the bounded separation theorem supplies \(\Sigma^{\star}\) in exactly one of the two models, a contradiction.  Thus model equality implies signature equality.

Assume \(\mathsf S(C)=\mathsf S(D)\).  By \(\texttt{def:static-signature}\), \(G,H\) have the same skeleton and colliders.  The cited \(\texttt{lem:chickering-covered-disagreement-sequence}\) gives
\[
 G=G_0,G_1,\ldots,G_m=H,\qquad m=\delta(C,D),
\]
where each step reverses a covered edge that disagreed between \(G,H\), and no such edge is reversed twice.  Write \(\mathcal N_j=\mathcal N_j(C)=\mathcal N_j(D)\).  Every arrow \(a\to j\) with \(a\in B\in\mathcal N_j\) occurs at both endpoints, so it is never reversed.  Lift each \(G_r\) by
\[
 \Pi^{(r)}_j=\mathcal N_j\cup
 \{\{a\}:a\in\operatorname{pa}_{G_r}(j)\setminus\bigcup\mathcal N_j\}.
\]
This is a valid partition, and at \(r=0,m\) it equals the partition of \(C,D\), respectively.

Suppose the next covered disagreement is \(i\to j\).  Equality of \(F(C),F(D)\) forces \(\mathcal N_i=\mathcal N_j\), since otherwise the disagreeing orientation would be recorded in \(F\).  Coveredness says
\[
 \operatorname{pa}_{G_r}(j)=\operatorname{pa}_{G_r}(i)\cup\{i\}.
\]
The parent \(i\) cannot belong to a block in \(\mathcal N_j=\mathcal N_i\), for that would make a parent block of \(i\) contain \(i\).  Hence \(\{i\}\in\Pi^{(r)}_j\).  Removing the common nonsingleton blocks from the covered parent-set equality leaves identical singleton blocks, so
\[
 \Pi^{(r)}_j\setminus\{\{i\}\}=\Pi^{(r)}_i.
\]
The move is legal and its prescribed transport produces \(\Pi^{(r+1)}\).  Induction proves \(D\in\mathcal R(C)\).

It remains to prove that one legal reversal preserves the full image.  Put \(S=\operatorname{pa}(i)\), so \(\operatorname{pa}(j)=S\cup\{i\}\), and write
\[
 X_i=a^{\mathsf T}X_S+\varepsilon_i,\qquad
 X_j=bX_i+d^{\mathsf T}X_S+\varepsilon_j,\qquad
 \operatorname{Var}(\varepsilon_i)=u>0,\quad
 \operatorname{Var}(\varepsilon_j)=v>0.
\]
Legality makes \(a,d\) block-constant on the same partition.  Define
\[
 t=v+b^2u>0,\quad q=\frac{bu}{t},\quad
 \eta_j=b\varepsilon_i+\varepsilon_j,\quad
 \eta_i=\varepsilon_i-q\eta_j.
\]
Substitution gives
\[
 X_j=(d+ba)^{\mathsf T}X_S+\eta_j,\qquad
 X_i=qX_j+\{a-q(d+ba)\}^{\mathsf T}X_S+\eta_i.
\]
Moreover,
\[
 \operatorname{Var}(\eta_j)=t,\quad
 \operatorname{Cov}(\eta_i,\eta_j)=bu-qt=0,\quad
 \operatorname{Var}(\eta_i)=u-\frac{b^2u^2}{t}=\frac{uv}{t}>0.
\]
The transformed innovations are jointly Gaussian, hence their zero covariance gives independence; they remain independent of all other innovations.  The new vectors are linear combinations of block-constant vectors, and \(q\) belongs to the new singleton block.  This is a valid parameter of the reversed state with the same law.  The reverse move is legal as well, proving equality of images in both directions, including \(b=0\).  Iteration proves \(D\in\mathcal R(C)\Rightarrow\mathcal M(C)=\mathcal M(D)\).

The constructed path has length \(\delta(C,D)\), so \(d_{\mathrm{rev}}(C,D)\le\delta(C,D)\).  Every path must reverse each initially disagreeing adjacency an odd number of times, giving the opposite inequality.  Hence equality holds, and the constructed shortest path reverses each disagreement once.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The theorem converts the observable separator into both a static criterion and a complete local transformation calculus. \textit{Gap.} BoegeKubjasMisraSolus2026, Question 20, asks for graphical edge-coloured equivalence and Theorem 19 excludes singleton classes; Chickering1995, Theorem 2, permits every covered reversal and has no coefficient-partition legality condition. \textit{Consumer.} Singleton-permitting causal-community discovery can report the exact observational component rather than either forcing proper colours or claiming a unique DAG.

\begin{proposition}[prop:exact-component-enumeration]\label{prop:exact-component-enumeration}
For every \(C\in\mathfrak S_p\), \[\operatorname{Enum}(C)=\mathcal R(C)=\{D\in\mathfrak S_p:\mathsf S(D)=\mathsf S(C)\}.\] Each consistent extension of the ordinary CPDAG with the background arrows \(F(C)\) has exactly one lifted partition, so the enumeration is duplicate-free and terminates after at most \(p!\) outputs. A shortest legal-reversal path from \(C\) to each emitted \(D\) has length \(\delta(C,D)\).
\end{proposition}
\begin{proof}
If \(B\in\mathcal N_j(C)\) and \(i\in B\), then \(\mathcal N_i(C)\ne\mathcal N_j(C)\); equality would make \(B\) a parent block of \(i\) containing \(i\).  Thus every arrow supporting a nonsingleton block belongs to \(F(C)\).  Every extension \(K\) in \(\texttt{def:component-enumerator}\) retains these arrows, so
\[
 \Pi^K_j=\mathcal N_j(C)\cup
 \{\{a\}:a\in\operatorname{pa}_K(j)\setminus\bigcup\mathcal N_j(C)\}
\]
is a valid partition.  It is the unique partition with nonsingleton family \(\mathcal N_j(C)\), since every residual parent must be singleton.

Each emitted \(K\) has the skeleton and colliders of \(G\), retains \(F(C)\), and has the same endpoint families.  Therefore its lifted state \(E_K\) has \(\mathsf S(E_K)=\mathsf S(C)\).  Conversely, a state with this signature is an acyclic extension with the required colliders and background arrows, and uniqueness of the lift makes it an output.  The complete-equivalence theorem now gives
\[
 \operatorname{Enum}(C)=\{D:\mathsf S(D)=\mathsf S(C)\}=\mathcal R(C).
\]
The unique lift makes the output duplicate-free.  Choose the lexicographically first topological order of every emitted DAG.  Two orientations of a fixed skeleton cannot share a topological order, because every edge points from its earlier to its later endpoint.  This injects the outputs into the \(p!\) total orders, proving termination after at most \(p!\) outputs.  Finally, the complete-equivalence theorem supplies a shortest legal path of length \(\delta(C,D)\) to each output.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Exact equivalence becomes usable only when every compatible state and a shortest transformation certificate can be emitted without partition ambiguity. \textit{Gap.} WienobstLuttermannBannachLiskiewicz2023 gives linear-delay consistent-extension enumeration with background knowledge but does not prove the blocked-partition lift or its equality with a coefficient-coloured covariance component. \textit{Consumer.} The enumerated component supplies every member-labelled causal effect and every admissible singleton orientation for downstream analysis.

\begin{proposition}[prop:ordinary-and-proper-reductions]\label{prop:ordinary-and-proper-reductions}
If every block of \(C\) is a singleton, then \(\mathcal N_j(C)=\varnothing\) for every \(j\in V\), and graph projection is a bijection from \(\mathcal R(C)\) onto the ordinary Markov-equivalence class of \(G\); the inverse sends each equivalent DAG to its unique all-singleton lift.  If every nonempty block of \(C\) is nonsingleton, then \(\mathcal R(C)=\{C\}\).  The first conclusion is the singleton lift of the Chickering1995 covered-reversal component, and the second is the target-local proper-blocked structural-identifiability conclusion covered by BoegeKubjasMisraSolus2026.
\end{proposition}
\begin{proof}
If every block of \(C\) is singleton, then \(\texttt{def:nonsingleton-family}\) gives \(\mathcal N_j(C)=\varnothing\) for all \(j\), and \(\texttt{def:family-mismatch-edges}\) gives \(F(C)=\varnothing\).  Every DAG \(H\) ordinarily Markov equivalent to \(G\) has a unique all-singleton lift \(D_H\); it has the same skeleton and colliders and empty nonsingleton families and \(F\)-set.  Thus \(\mathsf S(D_H)=\mathsf S(C)\), so the complete-equivalence theorem places \(D_H\) in \(\mathcal R(C)\).  Conversely, every member of \(\mathcal R(C)\) has that signature, hence is an all-singleton lift of a DAG ordinarily equivalent to \(G\).  This proves the claimed bijection, and the cited Chickering lemma identifies its graph image with the covered-reversal component.

If every nonempty block of \(C\) is nonsingleton, a legal move out of \(C\) would require a singleton block, contrary to the hypothesis.  A legal move cannot enter \(C\), either, because its endpoint partition contains the new singleton specified by \(\texttt{def:legal-reversal}\).  Thus \(C\) is isolated and \(\mathcal R(C)=\{C\}\), agreeing with the target-local specialization of \(\texttt{lem:boege-proper-blocked-identifiability}\).
\end{proof}
\par\smallskip\noindent \textit{Justification.} The endpoint reductions verify that the new component interpolates exactly between ordinary observational ambiguity and proper-blocked uniqueness. \textit{Gap.} Chickering1995 and BoegeKubjasMisraSolus2026 establish the two endpoints separately but do not characterize the singleton-permitting interpolation. \textit{Consumer.} The reductions provide exact regression tests for implementations and clarify when coefficient communities do or do not orient an arrow.

\begin{theorem}[thm:generic-bic-component-recovery]\label{thm:generic-bic-component-recovery}
Fix \(p\), \(C\in\mathfrak S_p\), and \(\theta_C\in\Theta_C\setminus\mathcal E_C\), and put \(\Sigma_0=\phi_C(\theta_C)\). Let \(X^{(1)},\ldots,X^{(n)}\) be independent \(N_p(0,\Sigma_0)\) observations, and let \(\widehat C_n\) minimize \(\operatorname{BIC}_n\) over \(\mathfrak S_p\) under the fixed tie rule. The set \(\mathcal E_C\) is a proper algebraic subset of \(\Theta_C\), and as \(n\to\infty\) with fixed \(p\), \[\Pr\{\mathcal R(\widehat C_n)=\mathcal R(C)\}\longrightarrow1.\] The result concerns the global criterion and makes no claim about greedy-search consistency.
\end{theorem}
\begin{proof}
Fix \(E_0\).  The columns of \(A_{E_0j}\) are nonzero indicators of disjoint blocks, hence linearly independent.  Thus for \(c\ne0\) and \(\Sigma\in\mathbb S^p_{++}\),
\[
 c^{\mathsf T}\Gamma_{E_0j}(\Sigma)c
 =(A_{E_0j}c)^{\mathsf T}\Sigma_{P,P}(A_{E_0j}c)>0.
\]
Every inverse in \(\texttt{def:off-model-block-ls}\) exists.  Its constrained residual variance is positive because
\[
 \min_b\operatorname{Var}\{X_j-b^{\mathsf T}A_{E_0j}^{\mathsf T}X_P\}
 \ge\Sigma_{jj}-\Sigma_{jP}\Sigma_{P,P}^{-1}\Sigma_{Pj}>0.
\]

On \(\mathcal M(E_0)\), innovation independence makes parent regression recover every structural row and innovation variance.  These quantities form a smooth rational inverse to \(\phi_{E_0}\).  Therefore \(\mathcal M(E_0)\) is a connected smooth embedded manifold of dimension \(d_{E_0}=p+\sum_j|\Pi^{E_0}_j|\).  In a topological order, membership is equivalent to requiring, in each regression on all predecessors, zero coefficients for nonparents and equal coefficients within each block.  Necessity follows from the triangular SEM; conversely, the ordered Gaussian conditional densities yield such an SEM with positive conditional variances.  Clearing the positive principal-minor denominators makes these finitely many polynomial equations.  Consequently \(\mathcal M(E_0)\) is relatively closed in \(\mathbb S^p_{++}\).

If \(d_D<d_C\), the \(d_C\)-dimensional manifold \(\mathcal M(C)\) cannot lie in \(\mathcal M(D)\).  If \(d_D=d_C\) and containment held, equal-dimensional charts would make \(\mathcal M(C)\) open in \(\mathcal M(D)\); relative closedness makes it closed, and connectedness of \(\mathcal M(D)\) would force equality.  Thus every nonequivalent \(D\) with \(d_D\le d_C\) has a defining polynomial whose pullback through \(\phi_C\) is nonzero.  A nonconstant rational effect coordinate has some nonzero partial derivative; clearing positive denominators makes its zero-gradient locus proper algebraic.  Finiteness of the state and query sets, and multiplication of one nonzero polynomial for each locus, show that \(\mathcal E_C\) is a proper algebraic subset of \(\Theta_C\).

Let \(\theta_C\notin\mathcal E_C\) and \(\Sigma_0=\phi_C(\theta_C)\).  For \(K\in\mathbb S^p_{++}\), put
\[
 q_T(K)=\log\det K+\operatorname{tr}(K^{-1}T).
\]
Up to a state-independent constant, \(-2n^{-1}\ell_n(K)=q_{S_n}(K)\).  Gaussian fourth moments give \(\mathbb E\|S_n-\Sigma_0\|_F^2=O(n^{-1})\); hence
\[
 S_n\to\Sigma_0,\qquad \|S_n-\Sigma_0\|_F=O_{\mathbb P}(n^{-1/2}).
\]
For \(n\ge p\), the Gaussian data matrix has full row rank almost surely, because every rank-deficient matrix lies in the zero set of its nonzero \(p\)-minor polynomials.  Thus the singular fallback is eventually irrelevant.

For nonequivalent \(D\) with \(d_D\le d_C\), exclusion from \(\mathcal E_C\) gives \(\Sigma_0\notin\mathcal M(D)\).  If \(\lambda_r\) are the eigenvalues of \(K^{-1}\Sigma_0\), then
\[
 q_{\Sigma_0}(K)-q_{\Sigma_0}(\Sigma_0)
 =\sum_{r=1}^p(\lambda_r-\log\lambda_r-1)\ge0,
\]
with equality only at \(K=\Sigma_0\).  The summand diverges at zero and infinity, so sublevel sets are compact.  Relative closedness yields
\[
 \Delta_D=\inf_{K\in\mathcal M(D)}
 \{q_{\Sigma_0}(K)-q_{\Sigma_0}(\Sigma_0)\}>0.
\]
Uniform continuity on a compact sublevel set and \(S_n\to\Sigma_0\) make the empirical gap at least \(\Delta_D/2\) with probability tending to one.  Since \(\Sigma_0\in\mathcal M(C)\),
\[
 \operatorname{BIC}_n(D)-\operatorname{BIC}_n(C)
 \ge n\Delta_D/2-(d_C-d_D)\log n\to+\infty.
\]

If \(d_D>d_C\), then
\[
 \sup_{\mathcal M(D)}\ell_n-\sup_{\mathcal M(C)}\ell_n
 \le\sup_{\mathbb S^p_{++}}\ell_n-\ell_n(\Sigma_0).
\]
The saturated maximizer is \(S_n\).  A second-order expansion at \(\Sigma_0\) and the preceding \(n^{-1/2}\) bound make the right side \(O_{\mathbb P}(1)\).  Therefore
\[
 \operatorname{BIC}_n(D)-\operatorname{BIC}_n(C)
 \ge-O_{\mathbb P}(1)+(d_D-d_C)\log n\to+\infty.
\]
If \(\mathcal M(D)=\mathcal M(C)\), the complete-equivalence theorem connects the states by legal reversals.  Each move removes one singleton block and adds one, so \(d_D=d_C\), and the BIC values tie exactly.  Since \(\mathfrak S_p\) is finite for fixed \(p\), a union bound over nonequivalent states proves
\[
 \Pr\{\mathcal R(\widehat C_n)=\mathcal R(C)\}\longrightarrow1.
\]
Only the global criterion was used.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Singleton blocks change the estimable structure from one state to an exact image quotient, so likelihood ties must be handled at the component level. \textit{Gap.} BoegeKubjasMisraSolus2026, Section 6 and Discussion, derives decomposable BIC and reports GECS simulations but leaves consistency open; Haughton1988 treats a fixed finite collection of smooth models without this equality quotient and its algebraic intersections. \textit{Consumer.} A fixed-dimensional coloured-DAG analysis can consistently report the correct observational component despite exact likelihood ties among its members.

\begin{theorem}[thm:simultaneous-component-effect-inference]\label{thm:simultaneous-component-effect-inference}
Under \(\texttt{thm:generic-bic-component-recovery}\), fix finite \(\mathcal Q\) and \(\alpha\in(0,1)\). Then \(\mathcal T_{\widehat C_n}(S_n)\) converges in probability to \(\mathcal T_C(\Sigma_0)\) in Hausdorff distance. Retaining state labels before removing coincident numerical values, the confidence collection satisfies \[\liminf_{n\to\infty}\Pr\left\{\mathcal R(\widehat C_n)=\mathcal R(C)\text{ and every }g_E(\Sigma_0),\ E\in\mathcal R(C),\text{ belongs to its labelled interval in }\mathfrak I_{n,\alpha}(\widehat C_n)\right\}\geq1-\alpha.\] The conclusion permits duplicate effect values and singular joint effect covariance.
\end{theorem}
\begin{proof}
Fix \(E\in\mathcal R(C)\).  Complete equivalence gives \(\Sigma_0\in\mathcal M(E)\).  If the structural parent vector at \(j\) is \(A_{Ej}\beta_{Ej}\), innovation independence gives
\[
 \Sigma_{P,j}=\Sigma_{P,P}A_{Ej}\beta_{Ej},\qquad
 h_{Ej}(\Sigma_0)=\Gamma_{Ej}(\Sigma_0)\beta_{Ej}.
\]
The positive-definite Gram matrix is invertible, so \(L_E(\Sigma_0)=B_E\).  Acyclicity makes \(B_E\) nilpotent.  For \(a\ne y\), differentiating the intervened SEM after replacing the equation for \(X_a\) by \(X_a=x\) gives
\[
 \tau^E_{a\to y}=\sum_{k=1}^{p-1}(B_E^k)_{ya}
 =((I_p-B_E)^{-1})_{ya}=g_E(\Sigma_0)_{(a,y)}.
\]
Thus equality of the causal effect and observed-law functional is proved from the structural equations; it is not definitional.  Since the observed covariance identifies only \(\mathcal R(C)\), the member-labelled effects form the set in \(\texttt{def:component-effect-set}\).

For arbitrary positive-definite \(\Sigma\), every block Gram matrix remains positive definite, its inverse is rational and smooth, and
\[
 (I_p-L_E(\Sigma))^{-1}=\sum_{k=0}^{p-1}L_E(\Sigma)^k.
\]
Thus every \(g_E\) is smooth near \(\Sigma_0\).

We derive the covariance limit used by the intervals.  For symmetric \(T\), direct Gaussian integration gives
\[
 \mathbb E\exp\{isX^{\mathsf T}TX\}
 =\det(I_p-2is\Sigma_0T)^{-1/2}.
\]
Independence and \(\log\det(I-U)=-\operatorname{tr}U-\tfrac12\operatorname{tr}(U^2)+O(\|U\|^3)\) yield
\[
 \mathbb E\exp\!\left[i\operatorname{tr}\{T\sqrt n(S_n-\Sigma_0)\}\right]
 \longrightarrow\exp\{-\operatorname{tr}((\Sigma_0T)^2)\}.
\]
This is the characteristic function of a centered Gaussian symmetric matrix \(Z\) with
\[
 \operatorname{Cov}(Z_{ij},Z_{k\ell})
 =\Sigma_{0,ik}\Sigma_{0,j\ell}+\Sigma_{0,i\ell}\Sigma_{0,jk}.
\]
For nonzero symmetric \(T\), the variance is
\[
 2\operatorname{tr}\{(\Sigma_0^{1/2}T\Sigma_0^{1/2})^2\}>0.
\]

Let \(g\) be a nonconstant labelled coordinate.  Since \(\theta_C\notin\mathcal E_C\), \(Dg(\Sigma_0)\ne0\).  Taylor expansion gives
\[
 \sqrt n\{g(S_n)-g(\Sigma_0)\}
 =Dg(\Sigma_0)[\sqrt n(S_n-\Sigma_0)]+o_{\mathbb P}(1)
 \Longrightarrow N(0,\sigma_g^2),
\]
with \(\sigma_g^2>0\).  The plug-in variance is consistent by continuity, so its studentized interval has limiting coverage \(1-\alpha/K\).  A structurally zero coordinate has no directed path, is identically zero by the finite path expansion, and its singleton interval covers exactly.  There are no other constant coordinates: zero block coefficients give effect zero, while positive coefficients on a directed path give a positive effect.

A union bound over the \(K\) nonconstant labels gives simultaneous limiting coverage at least \(1-\alpha\), without a nonsingular joint effect covariance.  Generic BIC recovery makes the probability of selecting the wrong component \(o(1)\), proving the stated liminf after intersecting the two events.  Labels are retained, so duplicate values are harmless.

On the correct-component event, continuity and finiteness give
\[
 \max_{E\in\mathcal R(C)}\|g_E(S_n)-g_E(\Sigma_0)\|\longrightarrow0
\]
in probability.  The Hausdorff distance of the corresponding unlabelled finite sets is bounded by this maximum.  Adding the \(o(1)\) selection-error probability proves the asserted Hausdorff convergence.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The observationally recovered object is a finite set of member-labelled causal effects, so inference must cover every component member. \textit{Gap.} NandyMaathuisRichardson2017 derives joint effect inference across ordinary equivalence classes, and StriederDrton2023 uses test inversion under equal variances; neither treats equality-constrained coefficient blocks after exact blocked-component selection. \textit{Consumer.} A causal-community analysis can report simultaneous uncertainty for all total effects compatible with its recovered singleton-permitting component.

\begin{openendedquestion}[oeq:certificate-pareto-optimality]\label{oeq:certificate-pareto-optimality}
Using \(\mathsf H_{\mathrm{opt}}\), characterize the Pareto frontier of universal exact separator schemes for \(\mathfrak S_p\) in the two coordinates consisting of the maximum number of active target blocks and the maximum rational height. In particular, determine for which \(p\) the achieved pair \((2,\lceil\log_2p\rceil)\) can be improved in either coordinate without worsening the other, while every scheme must still separate every pair with unequal static signatures by an observable polynomial certificate.
\end{openendedquestion}
\par\smallskip\noindent \textit{Justification.} The upper bound is operationally sharp only after the unavoidable support and arithmetic costs of universal separation are understood. \textit{Gap.} BoegeKubjasMisraSolus2026 supplies no witness-complexity notion, while general rational-identifiability arguments do not resolve minimum support or height on this blocked class. \textit{Consumer.} A lower frontier would certify whether any exact equivalence tester can use a smaller numerical alphabet or fewer activated communities than the binary-star library.

\begin{lemma}[lem:chickering-covered-disagreement-sequence]\label{lem:chickering-covered-disagreement-sequence}
Let \(G_0\) and \(G_1\) be finite directed acyclic graphs on the same labelled vertex set.  They have the same skeleton and the same unshielded colliders if and only if they are Markov equivalent.  If they are equivalent and \(m\) adjacencies have opposite orientations, then there is a sequence of exactly \(m\) distinct covered-edge reversals from \(G_0\) to \(G_1\); every intermediate graph is acyclic and equivalent, and each reversal corrects one original orientation disagreement.
\end{lemma}

\begin{lemma}[lem:boege-proper-blocked-identifiability]\label{lem:boege-proper-blocked-identifiability}
\texttt{For proper blocked edge-\allowbreak{}coloured directed acyclic graphs,\allowbreak{} in which every edge-\allowbreak{}colour block has at least two members,\allowbreak{} equality of the associated linear Gaussian covariance models implies equality of the coloured directed acyclic graphs.}
\end{lemma}

\begin{proposition}[prop:sharp-finite-star-dictionary]\label{prop:sharp-finite-star-dictionary}
For every \(C,D\in\mathfrak S_p\), if \(\mathsf S(C)\neq\mathsf S(D)\), then
\[
\mathcal W_2(C)\not\subseteq\mathcal M(D)
\qquad\text{or}\qquad
\mathcal W_2(D)\not\subseteq\mathcal M(C).
\]
Moreover, for every \(E_0\in\mathfrak S_p\), writing \(b_j=|\Pi^{E_0}_j|\),
\[
|\mathcal W_2(E_0)|
=\sum_{j\in V}\left(b_j+\binom{b_j}{2}\right)
=\sum_{j\in V}\binom{b_j+1}{2}
\leq\binom{p+1}{3}
\leq\frac{p^2(p-1)}{2}.
\]
The bound \(\binom{p+1}{3}\) is sharp for every \(p\geq1\): equality is attained by a transitively oriented complete directed acyclic graph whose parent partitions consist entirely of singleton blocks.
\end{proposition}
\begin{proof}
Suppose first that \(\mathsf S(C)\neq\mathsf S(D)\).  By the bounded binary-star separation theorem, there are distinct states \(E,J\in\{C,D\}\), a target \(z\in V\), and a subfamily \(\mathcal A_z\subseteq\Pi^E_z\) with \(1\leq|\mathcal A_z|\leq2\) such that the corresponding covariance \(\Sigma^{\star}\) belongs to \(\mathcal M(E)\setminus\mathcal M(J)\).  By the binary-star-library definition, the same choices imply \(\Sigma^{\star}\in\mathcal W_2(E)\).  Hence, if \(E=C\), then \(\mathcal W_2(C)\not\subseteq\mathcal M(D)\), while if \(E=D\), then \(\mathcal W_2(D)\not\subseteq\mathcal M(C)\).  This proves the separating-dictionary assertion.

It remains to count the library exactly.  Fix \(E_0\in\mathfrak S_p\), and put \(b_j=|\Pi^{E_0}_j|\).  For a target \(z\) and an admissible active subfamily \(\mathcal A_z\), let \(A_z=\bigcup_{B\in\mathcal A_z}B\).  Every block in a partition is nonempty, so \(A_z\neq\varnothing\).  Also \(z\notin A_z\), because \(A_z\subseteq\operatorname{pa}_{G_{E_0}}(z)\) and an acyclic directed graph has no self-loop.  The binary-star-library formula gives
\[
\Sigma^{\star}_{kk}
=
\begin{cases}
1+|A_z|,&k=z,\\
1,&k\neq z,
\end{cases}
\qquad
\Sigma^{\star}_{zi}=\mathbf 1\{i\in A_z\}\quad(i\neq z).
\]
Consequently \(z\) is the unique index whose diagonal entry exceeds one, and, after \(z\) is recovered,
\[
A_z=\{i\in V\setminus\{z\}:\Sigma^{\star}_{zi}=1\}.
\]
Thus the covariance recovers both its target and its active union.  Finally, because the members of \(\Pi^{E_0}_z\) are pairwise disjoint nonempty atoms, equality
\[
\bigcup_{B\in\mathcal A_z}B
=
\bigcup_{B\in\mathcal A'_z}B
\]
forces \(\mathcal A_z=\mathcal A'_z\): if \(B\in\mathcal A_z\) and \(i\in B\), then the unique partition atom containing \(i\) on the right is \(B\), so \(B\in\mathcal A'_z\), and the reverse inclusion is symmetric.  Hence the map from admissible pairs \((z,\mathcal A_z)\) to binary-star covariances is injective.  There are exactly \(\binom{b_z}{1}+\binom{b_z}{2}\) admissible subfamilies at target \(z\), including zero choices when \(b_z=0\).  Summing the disjoint, injectively represented target contributions yields
\[
|\mathcal W_2(E_0)|
=\sum_{z\in V}\left(\binom{b_z}{1}+\binom{b_z}{2}\right)
=\sum_{z\in V}\left(b_z+\binom{b_z}{2}\right)
=\sum_{z\in V}\binom{b_z+1}{2}.
\]

We next prove the uniform bound.  A finite acyclic directed graph has a topological order: indeed, every nonempty induced subgraph has a source, since otherwise repeatedly following an incoming arrow in the finite vertex set would repeat a vertex and create a directed cycle; deleting a source and iterating gives the order.  Write this order as \(v_1,\ldots,v_p\), with every arrow directed from an earlier to a later vertex.  Then
\[
|\operatorname{pa}_{G_{E_0}}(v_k)|\leq k-1.
\]
Because \(\Pi^{E_0}_{v_k}\) partitions that parent set into nonempty blocks,
\[
b_{v_k}\leq|\operatorname{pa}_{G_{E_0}}(v_k)|\leq k-1.
\]
The map \(m\mapsto\binom{m+1}{2}=m(m+1)/2\) is nondecreasing on the nonnegative integers.  Therefore
\[
|\mathcal W_2(E_0)|
=\sum_{k=1}^{p}\binom{b_{v_k}+1}{2}
\leq\sum_{k=1}^{p}\binom{k}{2}
=\binom{p+1}{3},
\]
where the last equality follows by induction from Pascal's identity \(\binom{k+1}{3}=\binom{k}{3}+\binom{k}{2}\), with both sides zero at \(p=1\).  Since \(p+1\leq3p\) for \(p\geq1\),
\[
\binom{p+1}{3}
=\frac{p(p-1)(p+1)}{6}
\leq\frac{p^2(p-1)}{2},
\]
so the former coarse bound is retained as a consequence.

For sharpness, choose any ordering \(v_1,\ldots,v_p\) of \(V\), include every arrow \(v_a\to v_b\) with \(a<b\), and let every \(\Pi^{E_0}_{v_k}\) be the singleton partition of \(\{v_1,\ldots,v_{k-1}\}\).  The graph is acyclic, these are valid parent partitions, and hence the resulting state belongs to \(\mathfrak S_p\).  It satisfies \(b_{v_k}=k-1\), so the exact identity already proved gives
\[
|\mathcal W_2(E_0)|
=\sum_{k=1}^{p}\binom{k}{2}
=\binom{p+1}{3}.
\]
Thus the global bound is attained, including the case \(p=1\), when both sides are zero.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The pointwise separator becomes a finite universal test dictionary with an exact pre-data size and a sharp worst-case combinatorial bound. \textit{Gap.} BoegeKubjasMisraSolus2026 decides equality using rational model constraints, whereas this proposition reduces inequality detection on the blocked class to an exactly counted finite library of binary stars. \textit{Consumer.} Exact-arithmetic implementations can allocate and precompute the full separating library, with the sharp worst-case size known before observing data.

\section{Interpretation}

The causal target is the covariance-model equivalence component, whereas the static signature and the finite star dictionary are observable-law recovery devices whose correctness is proved rather than assumed. A signature mismatch can be rejected by searching a finite source-model library, and the exact count shows the computational certificate budget is determined by the parent-block counts. The sharp worst case occurs for a complete acyclic graph with singleton parent blocks; coarser blocks or missing arrows can only reduce the library.

\section*{Technical internal limitation (diagnostic only)}

Exact-arithmetic enumeration through \(p=5\) remains a diagnostic audit of the constructive proof, not a premise of it: the all-\(p\) router, exact dictionary count, and sharp bound are established symbolically. The unresolved certificate Pareto question is whether a different universal scheme can improve on two active blocks or height \(\lceil\log_2p\rceil\) without worsening the other coordinate. The statistical rung remains restricted to a global optimizer, fixed \(p\), pointwise parameters outside the stated algebraic exceptional set, and simultaneous inference over a finite recovered component.

\section{Honest scope}

Population equivalence allows zero coefficients and all positive innovation variances and makes no faithfulness assumption. The bounded separator, exact sharp dictionary count, complete equivalence, and finite component enumeration hold for all \(p\) in the singleton-permitting target-local blocked class. No Pareto optimality of the two-block or height guarantees is claimed. Statistical conclusions are global-optimizer, fixed-\(p\), pointwise, and generic. Greedy GECS consistency, growing dimension, uniform inference near model intersections, cross-target edge-colour classes, constrained innovation variances, and cyclic graphs are outside scope.

\begin{thebibliography}{99}

  \bibitem{BoegeKubjasMisraSolus2026} Tobias Boege, Kaie Kubjas, Pratik Misra, and Liam Solus. Coloured Gaussian Directed Acyclic Graphical Models. Journal of the Royal Statistical Society: Series B, 88:819--875, 2026. DOI: 10.1093/jrsssb/qkaf068; arXiv:2404.04024v3.

  \bibitem{Chickering1995} David Maxwell Chickering. A Transformational Characterization of Equivalent Bayesian Network Structures. Proceedings of the Eleventh Conference on Uncertainty in Artificial Intelligence, 87--98, 1995.

  \bibitem{SullivantTalaskaDraisma2010} Seth Sullivant, Kelli Talaska, and Jan Draisma. Trek Separation for Gaussian Graphical Models. Annals of Statistics, 38:1665--1685, 2010. DOI: 10.1214/09-AOS760; arXiv:0812.1938.

  \bibitem{DrtonRobevaWeihs2020} Mathias Drton, Elina Robeva, and Luca Weihs. Nested Covariance Determinants and Restricted Trek Separation in Gaussian Graphical Models. Bernoulli, 26:2503--2543, 2020. DOI: 10.3150/19-BEJ1179; arXiv:1807.07561.

  \bibitem{WuDrton2023} Jun Wu and Mathias Drton. Partial Homoscedasticity in Causal Discovery with Linear Models. IEEE Journal on Selected Areas in Information Theory, 4:1077--1090, 2023. arXiv:2308.08959.

  \bibitem{MakamReichenbachSeigal2023} Visu Makam, Philipp Reichenbach, and Anna Seigal. Symmetries in Directed Gaussian Graphical Models. Electronic Journal of Statistics, 17:3905--3946, 2023. arXiv:2108.10058.

  \bibitem{WienobstLuttermannBannachLiskiewicz2023} Marcel Wienöbst, Malte Luttermann, Max Bannach, and Maciej Liśkiewicz. Efficient Enumeration of Markov Equivalent DAGs. AAAI Conference on Artificial Intelligence, 12313--12320, 2023. arXiv:2301.12212.

  \bibitem{RadhakrishnanSolusUhler2018} Adityanarayanan Radhakrishnan, Liam Solus, and Caroline Uhler. Counting Markov Equivalence Classes for DAG Models on Trees. Discrete Applied Mathematics, 244:170--185, 2018. DOI: 10.1016/j.dam.2018.03.015; arXiv:1706.06091.

  \bibitem{Haughton1988} Dominique M. A. Haughton. On the Choice of a Model to Fit Data from an Exponential Family. Annals of Statistics, 16:342--355, 1988. DOI: 10.1214/aos/1176350709.

  \bibitem{NandyMaathuisRichardson2017} Preetam Nandy, Marloes H. Maathuis, and Thomas S. Richardson. Estimating the Effect of Joint Interventions from Observational Data in Sparse High-Dimensional Settings. Annals of Statistics, 45:647--674, 2017. DOI: 10.1214/16-AOS1462.

  \bibitem{StriederDrton2023} David Strieder and Mathias Drton. Confidence in Causal Inference under Structure Uncertainty in Linear Causal Models with Equal Variances. Journal of Causal Inference, 11, 2023. DOI: 10.1515/jci-2023-0030; arXiv:2309.04298.

\end{thebibliography}

\end{document}