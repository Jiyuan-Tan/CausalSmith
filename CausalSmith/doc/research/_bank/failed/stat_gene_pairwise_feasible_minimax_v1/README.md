---
qid: stat_gene_pairwise_feasible_minimax
spec: v1
topic: "Feasible GENE margins and minimax simultaneous orientation of supplied independent cause-effect pairs. Work directly in a positive-density Peters restricted-ANM domain under one explicit fixed regularity envelope and fixed Gaussian kernels. Characterize a local attainable rectangle for the joint population GENE navigation and all-query residual-HSIC margins, and prove the corrected all-procedure frontier N* proportional to log((d+1)/alpha)/lambda there, with a computable simultaneous graph confidence set and matching product-testing converse. Credit Wornbard et al.; novelty must be the F_d geometry and uniform minimax theorem. This is a new attempt at parent eid_gene_population_landscape, not its failed general-landscape classification. Presolve evidence and the exact unresolved uniform-nuisance kill test are embedded in the queue anchor."
novelty_target: field
banked_novelty_tier: unknown
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "A faithful field-level continuation requires a new full-class density-adaptive or directly orthogonal residual-HSIC estimator, reverse-regression regularity, unbounded-envelope concentration, and an all-query deviation theorem; these are the unresolved research kernel, not a bounded correction."
reusable: not_reusable
reraise_status: retry
gap_reasons:
  - "The promised all-procedure frontier N^*(d,alpha,lambda) on the separated restricted-ANM envelope is replaced by a matched upper/lower frontier only for the strict Hermite-chart subclass M_d^H(lambda); the full class receives only a lower bound."
  - "No faithful same-topic bounded repair is presently available."
  - "Proving these is the unresolved research kernel, not a bounded correction."
reusable_artifacts:
  - "discovery/solve_thm_attainable_margin_rectangle.json"
  - "discovery/solve_thm_simultaneous_graph_confidence_set.json"
  - "discovery/solve_thm_fixed_local_polynomial_uniform_expansion_impossible.json"
  - "discovery/core.json"
seeds_burned: []
proof_attempt_summary: |
  The run constructed and audited an attainable GENE-inspired/HSIC margin rectangle, a matched
  orientation frontier and simultaneous confidence set on a sub-half Hermite chart, and a
  fixed-local-polynomial protocol counterexample. It collapsed at D0.5 because the advertised
  field contribution required the matching upper bound and confidence procedure uniformly over
  the broader separated restricted-ANM envelope, whereas only the lower bound transferred there.
  A future retry must supply a genuinely full-class density-adaptive or orthogonal residual-HSIC
  construction with reverse-regression regularity, unbounded-envelope concentration, and uniform
  all-query deviation control.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 34985616
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 34985616
  total_tokens_consumed: null
banked_on: "2026-09-12"
---

# stat_gene_pairwise_feasible_minimax / v1 — Failed

**Topic.** Feasible GENE margins and minimax simultaneous orientation of supplied independent cause-effect pairs. Work directly in a positive-density Peters restricted-ANM domain under one explicit fixed regularity envelope and fixed Gaussian kernels. Characterize a local attainable rectangle for the joint population GENE navigation and all-query residual-HSIC margins, and prove the corrected all-procedure frontier N* proportional to log((d+1)/alpha)/lambda there, with a computable simultaneous graph confidence set and matching product-testing converse. Credit Wornbard et al.; novelty must be the F_d geometry and uniform minimax theorem. This is a new attempt at parent eid_gene_population_landscape, not its failed general-landscape classification. Presolve evidence and the exact unresolved uniform-nuisance kill test are embedded in the queue anchor.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** terminal:laundering — the promised full separated restricted-ANM all-procedure frontier was replaced by a matched frontier only on the strict Hermite-chart subclass; the full class receives only a lower bound.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
