{
  "node_type": "statement",
  "body": {
    "id": "thm:rank-feasible-local-hoif",
    "kind": "theorem",
    "statement": "For every compact \\(K\\subset(0,\\infty)^3\\), there are \\(C_K<\\infty\\) and \\(n_K\\) such that, for every \\((\\alpha,\\beta,\\gamma)\\in K\\), every \\(n\\geq n_K\\) with \\(G_n=1\\), and the estimator \\(\\widehat{\\tau}^{G}_n(x_0)\\) derived from \\(\\mathfrak H_G\\), \\[\\sup_{P\\in\\mathcal{P}_{\\mathrm{band}}}\\mathbb{E}_P\\left|\\widehat{\\tau}^{G}_n(x_0)-\\tau_P(x_0)\\right|\\leq C_K\\left[(h_n^*)^\\gamma+\\left\\{\\frac{h_n^*}{(k_n^*)^{1/d}}\\right\\}^{\\alpha+\\beta}+\\left(\\frac{1}{N_n^*}\\left\\{1+\\frac{k_n^*}{N_n^*}\\right\\}\\right)^{1/2}\\right]=C_K r_n^*.\\] The estimator is an everywhere-defined same-sample M\\\"obius-stabilized empirical-Gram local HOIF and uses no estimate or smoothness condition for \\(f_P\\). On the explicitly restricted class \\(\\mathcal P_{\\mathrm{band}}\\), the construction therefore attains the KBRW three-term objective on the \\(G_n\\) branch without the density-estimation or density-ratio condition used in KBRW Theorem 2. No inclusion relation between the two theorem classes is asserted.",
    "free_symbols": [
      "\\alpha",
      "\\beta",
      "\\gamma",
      "n",
      "G_n",
      "\\widehat{\\tau}^{G}_n(x_0)",
      "x_0",
      "\\mathfrak{H}_{G}",
      "P",
      "\\mathcal{P}_{\\mathrm{band}}",
      "\\tau_P",
      "(h_n^*,k_n^*)",
      "d",
      "N_n^*",
      "r_n^*",
      "f_P"
    ],
    "depends_on": [
      "ass:iid-sampling",
      "ass:density-band",
      "ass:overlap",
      "ass:bounded-outcomes",
      "ass:holder-propensity",
      "ass:holder-control-regression",
      "ass:holder-cate",
      "def:holder-ball",
      "def:band-class",
      "def:optimizer",
      "def:regime-partition",
      "def:rank-feasible-handle",
      "lem:cate-identification",
      "lem:optimizer-partition"
    ],
    "justification": "It localizes same-sample stabilization at the random effective size \\(N_n^*\\) and, on the explicitly restricted fixed positive-band class, avoids a density-estimation remainder without a logarithmic risk loss.",
    "gap": "Liu et al. prove stabilization for global bilinear forms with deterministic sample size, while the KBRW upper theorem uses a local covariate-law condition; neither result supplies density-free control of weighted occupancy, leverage, and collisions in the same local sample under the fixed positive-band restriction.",
    "consumer": "It supplies density-free optimal pointwise CATE estimation whenever the optimizer-selected local rank is empirically feasible.",
    "obligation": {
      "what_is_open": "An explicit same-sample M\\\"obius kernel, its finite truncation order, and a uniform proof that its growing-rank nuisance-product bias and every collision component are bounded by the two nonoracle terms in \\(r_n^*\\) on \\(G_n=1\\).",
      "obstruction": "The handle specifies no basis, preliminary nuisance maps, inverse-Gram expansion, partition-lattice kernel, truncation rule, or spectral threshold. Those choices determine both the estimator and the collision remainder. The density band does control a fixed-dimensional local Gram, but it does not by itself prove the missing growing-rank cancellation.",
      "attempted": "I derived the oracle empirical-Gram localization and screening bound directly. A naive same-sample plug-in or one-step series fit leaves a diagonal own-observation term of order \\(k/N_n^*\\), which can exceed the required \\(\\sqrt{k_n^*}/N_n^*\\); merely naming M\\\"obius inversion would assume the theorem's crux.",
      "partial_result": "The completed oracle calculation above proves a density-free local empirical-Gram screening result: for every fixed target-polynomial dimension, the population local Gram has eigenvalues bounded away from zero and infinity using only \\(f_-\\le f_P\\le f_+\\), its screen fails with probability \\(O((nh^d)^{-1})\\), and the clipped oracle local \\(R\\)-estimator has risk \\(O(h^\\gamma+(nh^d)^{-1/2})\\).  Thus arbitrary nonsmooth variation of \\(f_P\\) is not itself an obstruction.  What remains unproved is the growing-rank, same-sample nuisance-product cancellation that must produce \\((h/k^{1/d})^{\\alpha+\\beta}+\\sqrt{k}/(nh^d)\\)."
    }
  }
}