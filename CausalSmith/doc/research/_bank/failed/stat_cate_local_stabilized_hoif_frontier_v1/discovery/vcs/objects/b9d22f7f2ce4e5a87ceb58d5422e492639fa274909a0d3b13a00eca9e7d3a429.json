{
  "node_type": "statement",
  "body": {
    "id": "lem:optimizer-partition",
    "kind": "lemma",
    "statement": "For every \\(n\\geq2\\), the minimizer \\((h_n^*,k_n^*)\\) exists, is unique after lexicographic selection, and satisfies \\(G_n+P_n=1\\) and \\(G_nP_n=0\\). Moreover, \\(r_n^*\\) equals the exact infimum of the three-term KBRW objective over \\(\\mathcal T_n\\).",
    "free_symbols": [
      "n",
      "(h_n^*,k_n^*)",
      "G_n",
      "P_n",
      "r_n^*",
      "\\Phi_n(h,k)",
      "\\mathcal{T}_n"
    ],
    "depends_on": [
      "def:tuning-domain",
      "def:kbrw-objective",
      "def:optimizer",
      "def:regime-partition"
    ],
    "justification": "The deterministic split must cover every sample size without an unanalysed tuning boundary.",
    "gap": "KBRW optimize asymptotic choices; the exact lexicographic optimizer and exhaustive branch selector are new bookkeeping required by a single total estimator.",
    "consumer": "The unified estimator can switch without data-dependent model selection or uncovered equality cases."
  }
}