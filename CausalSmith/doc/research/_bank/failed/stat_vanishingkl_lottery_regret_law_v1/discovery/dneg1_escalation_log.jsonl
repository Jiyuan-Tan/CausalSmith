{"angle":0,"version":1,"directive":"Revise the proposal without changing the field-tier novelty kernel. (1) Remove prop:model-inclusion and prop:fixed-penalty-reduction from the root contribution/solve list; retain them only as explicitly labeled sanity/reduction checks to Fang–Ridder–Xie, with prose and comparator table synchronized so no novelty is claimed for them. (2) Repair thm:certified-calibration and def:certified-algorithm by specifying effective inputs (rational data or named interval-oracle representations for p, b, v_min, v_max and all optimized parameters), a machine-checkable certificate/output format, directed-rounding/error semantics, and an explicit finite termination/complexity bound. (3) Do not present the K=3 commitment-savings statement as a mere definitional specialization: either prove a substantive application-specific step such as finite-stratification adequacy or a strict certified comparison with the fixed-c frontier in Fang–Ridder–Xie, or demote it to an illustration outside the root theorem list. Preserve the genuinely new uniform compactified path law, atom-safe selector-valid envelope, field novelty target, and all novelty/comparator prose consistency.","note":"angle-action:continue"}
{"angle":0,"version":2,"directive":"Repair the sole remaining D-0.5 soundness defect without altering the field-tier kernel. Define an observed, computable plug-in regret/frontier path built only from the sample and the declared estimators (not from unknown Δ_n or E_n); define the observed-data σ-field explicitly; require every selected temperature t-hat to be measurable with respect to that observed-data σ-field (or the σ-field generated by the plug-in path); and state the precise uniform approximation/coverage bridge from the plug-in path to the oracle normalized regret path. Rewrite thm:atom-safe-envelope and every consumer/prose field so post-selection validity is claimed only for this operational selector. Preserve the effective certified-computation contract, one-sided atom-safe semantics, novelty/comparator fields, and field novelty target.","note":"angle-action:continue"}
