# eid_sourcemin_cyclic_effect_rankfrontier — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (A normalized overcomplete independent-component observati…).** A normalized overcomplete independent-component observational world. Fix an observed dimension \(p\in\{2,3,\ldots\}\) and a source dimension \(n\in\{p,p+1,\ldots\}\), with index sets \(\mathsf O=\{1,\ldots,p\}\), \(\mathsf S=\{1,\ldots,n\}\) and \(\mathsf L=\{p+1,\ldots,n\}\), and an ordered pair of observed coordinates \(x\in\mathsf O\), \(y\in\mathsf O\setminus\{x\}\). The data-generating objects are a mixing matrix \(C=[c_1\;\cdots\;c_n]\in\mathbb R^{p\times n}\) and a source vector \(\varepsilon=(\varepsilon_1,\ldots,\varepsilon_n)^\top\in\mathbb R^n\) with mutually independent, nondegenerate, non-Gaussian coordinates; the observed vector is \(X=C\varepsilon\in\mathbb R^p\) and the observational law is \(P_X=\mathcal L(C\varepsilon)\in\mathcal P(\mathbb R^p)\). For a lifting degree \(d\ge1\), a contraction degree \(q\ge1\) and the common order \(r=2d+q\), the source cumulant vector is \(\lambda_j=\operatorname{cum}_r(\varepsilon_j)\), the observational cumulant tensor is \(\mathcal T_r=\operatorname{cum}_r(X)=\sum_{j=1}^n\lambda_jc_j^{\otimes r}\in\operatorname{Sym}^r(\mathbb R^p)\), and the lifted direction matrix is \(V_d=[c_1^{\otimes d}\;\cdots\;c_n^{\otimes d}]\in\mathbb R^{p^d\times n}\); two unit probes \(u,v\in\mathbb S^{p-1}\) are fixed, and \(\zeta=(P_X,C,\varepsilon,\lambda)\) indexes the representation. The law-level direction object is \(\mathcal D(P_X)\subseteq\mathbb P^{p-1}\), the union of the unordered projective column sets of every irreducible, all-non-Gaussian independent-component representation of \(P_X\). — Reuse inside this block: `Causalean.Stat.MomentProblems.sourceCumulant` for \(\lambda_j=\operatorname{cum}_r(\varepsilon_j)\); `Causalean.Stat.MomentProblems.IsGaussianLaw` for the non-Gaussianity atom; `ProbabilityTheory.iIndepFun` for mutual independence; `Matrix.rank` / `Matrix.det` / `Matrix.submatrix` for the rank and deletion-minor vocabulary. NEW here: the order-\(r\) MULTIVARIATE cumulant tensor -- Causalean's `jointCumulant` is bivariate only (`Causalean/Stat/Nonparametric/MomentProblems/Cumulant.lean:60`), so the tensor \(\mathcal T_r\) is built inline per D-A as a symmetric array `(Fin r -> Fin p) -> Real` defined by the same set-partition Moebius formula. Search trace: grep -i 'cumulant' over ../Causalean -> 8 files (`Stat/Nonparametric/MomentProblems/{Cumulant,MomentCumulantInversion,TruncatedCumulantInterior,SymmetricAtomSolve,AtomicLaw}.lean`, `Discovery/LiNGAM/{Kurtosis,LiNGAMKurtosis}.lean`, `Stat/Nonparametric.lean`); only univariate/bivariate cumulants and a 4th-order kurtosis identity, no order-\(r\) tensor. Mathlib-side: `lean_leansearch` 'cumulant tensor of a random vector' / 'moment' -> only `ProbabilityTheory.moment`, `cgf`, `complexMGF`; no multivariate cumulant. Bypass-justified for `Causalean/Graph/SWIG.lean` and `Causalean/SCM/Model/SCM.lean`: this world has no DAG and no kernel-valued mechanism, only a linear map \(C\) and an independent source vector.
**required modules.** Mathlib.LinearAlgebra.Matrix.Rank, Mathlib.LinearAlgebra.Matrix.NonsingularInverse, Mathlib.Probability.Independence.Basic, Mathlib.Probability.Distributions.Gaussian.Real, Mathlib.Probability.Moments.Variance, Causalean.Stat.Nonparametric.MomentProblems.Cumulant

**S-2 (The source-minimal cyclic structural-completion world att…).** The source-minimal cyclic structural-completion world attached to the observational world. A completion of the fixed representation is a pair \(\mathcal M=(Q,H)\) of real \(n\times n\) matrices whose endogenous equilibrium \(V\in\mathbb R^n\) solves \(QV=H\varepsilon\), with \(Q=I_n-B\) unit-diagonal, \(H\) monomial (exactly one nonzero entry in each row and each column, so exactly one disturbance per equation), \(\det(Q)\ne0\), the fixed-law match \([Q^{-1}H]_{\mathsf O,:}=C\), and \(\det(Q_{-x,-x})\ne0\); these completions form the fiber \(\mathfrak F_x(C,\varepsilon)\). Each \(\mathcal M\) has an assigned source \(s_{\mathcal M}(i)=j\) determined by \(H_{ij}\ne0\) and a hard-intervention equilibrium effect \(\theta_{y\leftarrow x}(\mathcal M)=(Q^{-1})_{yx}/(Q^{-1})_{xx}\). At the law level a completion \(\widetilde{\mathcal M}=(m_{\widetilde{\mathcal M}},Q^{\widetilde{\mathcal M}},H^{\widetilde{\mathcal M}},\xi^{\widetilde{\mathcal M}})\) with \(m_{\widetilde{\mathcal M}}\ge p\) has equilibrium \(V^{\widetilde{\mathcal M}}\), observed mixing matrix \(A^{\widetilde{\mathcal M}}=[(Q^{\widetilde{\mathcal M}})^{-1}H^{\widetilde{\mathcal M}}]_{\mathsf O,:}\), law-assigned source \(s^{\mathrm{law}}_{\widetilde{\mathcal M}}\), reference-aligned source \(\bar s_{\widetilde{\mathcal M}}\), and observed response vector \(\rho_x(\widetilde{\mathcal M})\in\mathbb R^{p-1}\); the source-minimal observational-law fiber \(\mathfrak F^{\mathrm{law}}_x(P_X)\) collects those completions that reproduce \(P_X\) with irreducible \(A^{\widetilde{\mathcal M}}\) and independent nondegenerate non-Gaussian coordinates of \(\xi^{\widetilde{\mathcal M}}\). No spectral-radius or dynamical-stability restriction is imposed. — Bypass-justified against the whole Causalean causal substrate, one line per surveyed module: `Causalean/SCM/Model/SCM.lean` (generalized SCM = node-indexed kernels over a DAG) -- this world is a single linear system \(QV=H\varepsilon\) that is deliberately CYCLIC, so no DAG exists; `Causalean/Graph/SWIG.lean` and `Causalean/SCM/Do/*` (do-calculus, SWIG splits) -- the hard intervention here is the deletion of row/column \(x\) from \(Q\), an algebraic minor condition, not a graph surgery; `Causalean/SCM/ID/Identifiable.lean` (`Identifiable G Phi` quantifies over `Causalean.SCM` models sharing a `SWIGGraph`) -- the identification statements here quantify over a fiber of MATRIX TUPLES sharing an observational law, with no shared graph; `Causalean/SCM/ID/{Adjustment,Backdoor,Frontdoor,BackdoorCriterion}.lean` and `GraphicalThms/*` -- no adjustment set, backdoor set or c-component appears anywhere in the note; `Causalean/SCM/ID/Overlap.lean` -- the note's sampling model states treatment overlap is not applicable. Confirmed reuse inside this block: `Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal` (../Causalean/Discovery/LinearDisentanglement/Quantitative/Definitions.lean:90, `forall i, B i i = 1` on `SqMatrix d = Matrix (Fin d) (Fin d) Real`) is the unit-diagonal normalization of \(Q\) verbatim and is imported rather than re-declared. Positive analogue recorded for F3: `Causalean/Discovery/LiNGAM/{PermutationUniqueness,GenPerm}.lean` carries exactly the monomial-matrix bookkeeping used here (`perm_uniqueness`, `genPerm_of_invertible_of_colSupport`: invertible + at most one nonzero per column implies a generalized permutation) but only in the SQUARE, ACYCLIC, unit-diagonal case; the gap to bridge is the rectangular observed block \([\cdot]_{\mathsf O,:}\) and the absence of a topological order. Search trace: grep -n '^(def|structure|abbrev) ' over ../Causalean/SCM/ID/Identifiable.lean -> `Identifiable`, `IdentifiableUnder`, `NonIdentifiable`, `obsEquiv`, all `SWIGGraph`-indexed; lean_local_search 'Identifiable' -> the same two decls only. Mathlib-side: `Matrix.nondegenerate_iff_det_ne_zero`, `Matrix.adjugate`, `Matrix.det_apply` (Leibniz expansion) supply the cofactor/adjugate identities used throughout.
**required modules.** Mathlib.LinearAlgebra.Matrix.NonsingularInverse, Mathlib.LinearAlgebra.Matrix.Adjugate, Mathlib.LinearAlgebra.Matrix.Rank, Causalean.Discovery.LinearDisentanglement.Quantitative.Definitions

**S-3 (The deletion-rank frontier and its constructive certificate).** The deletion-rank frontier and its constructive certificate. For the fixed mixing matrix \(C\) and treatment coordinate \(x\), the deletion-rank-admissible source set is \(J_x(C)=\{j\in\mathsf S:C_{xj}\ne0,\ \operatorname{rank}(C_{-x,-j})=p-1\}\); the induced scalar and vector frontiers are \(\mathcal R_{xy}(C)=\{C_{yj}/C_{xj}:j\in J_x(C)\}\subseteq\mathbb R\) and \(\mathcal V_x(C)=\{(C_{ij}/C_{xj})_{i\in\mathsf O\setminus\{x\}}:j\in J_x(C)\}\subseteq\mathbb R^{p-1}\). The certificate fixes one basis extension \(T_0\in\operatorname{GL}_n(\mathbb R)\) of the rows of \(C\) (so \((T_0)_{\mathsf O,:}=C\)) with inverse \(W_0=T_0^{-1}\), reads admissibility off the kernel block \(K_x=(W_0)_{:,\{x\}\cup\mathsf L}\), and, for each \(j\in J_x(C)\), produces by one latent-row shear and one alternating-path flip of a single column-saturating matching a completion \(\operatorname{Cert}(C,x)_j=\mathcal M_j=(Q_j,H_j)\in\mathfrak F_x(C,\varepsilon)\) whose equation \(x\) is driven by source \(j\). — Search trace: grep -i 'deletion|admissible|frontier' over ../Causalean and CausalSmith/CausalSmith -> the only hits are `CausalSmith/ExactID/EID_RobustBackshiftUniformDistance_Research/{TUniformSupportDeletionRadius,TGenericSupportFrontier}.lean`, which speak of support deletion for a SQUARE `RealMatrix p` backShift system with environment-indexed shifts (`Basic.lean:19 abbrev RealMatrix (p : Nat) := Matrix (Fin p) (Fin p) Real`) -- bypass-justified: no rectangular \(p\times n\) overcomplete mixing, no per-source column deletion, no environments here; but its `Helpers/MatrixMargins.lean` and `Helpers/ConfidenceUnion.lean` are the closest structural template and are recorded as F3 references. Mathlib-side: `lean_leansearch` 'nonzero determinant gives a permutation with all matrix entries nonzero; bipartite perfect matching from Hall' -> `SimpleGraph.exists_isPerfectMatching_of_forall_ncard_le`, `SimpleGraph.exists_bijective_of_forall_ncard_le` (Hall), `Matrix.nondegenerate_iff_det_ne_zero`; the note's proof extracts matchings from a nonzero Leibniz term, so `Matrix.det_apply` + `Finset.exists_ne_zero_of_sum_ne_zero` is the direct route and Hall is the fallback. `Matrix.vandermonde` supplies the worst-case dense-output instance of D-B.
**required modules.** Mathlib.LinearAlgebra.Matrix.Rank, Mathlib.LinearAlgebra.Matrix.Adjugate, Mathlib.LinearAlgebra.Vandermonde, Mathlib.Combinatorics.Hall.Basic, Mathlib.Combinatorics.SimpleGraph.Hall

**S-4 (The separated common-order i.i.d. inference world).** The separated common-order i.i.d. inference world. Fix a miscoverage level \(\alpha\in(0,1)\) and margins \(a,\delta,\sigma\in(0,1]\), \(0<\kappa\le\Lambda\), \(M\ge1\). The separated class \(\mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)\) collects the tuples \(\zeta=(P_X,C,\varepsilon,\lambda)\) with \(d\ge1\), \(q\ge1\), \(r=2d+q\), \(n\le\binom{p+d-1}{d}\) satisfying the source, normalization, cumulant-magnitude, probe-orientation, lifted-rank, pencil-gap, moment-envelope, mixing-singular, loading-selection and deletion-rank conditions. The explicit local-inversion constants are \(\eta=\kappa\sigma^{q+2}\), \(\chi=\sqrt n/\sigma\), \(h=2/\eta+2n\Lambda/\eta^2\), \(\epsilon_0=\min\{\eta/2,\sigma/(6\chi h)\}\), \(L_z=nh\{2\chi+6n\Lambda\chi^2/(\eta\sigma)\}\), \(L_C=2\sqrt{np}L_z\), and the tensor-pencil inverse maps \(\mathcal T_r\) back to \(\{c_1,\ldots,c_n\}\) through the contractions \(M_w\), the compressions \(G_w=A_wA_u^{-1}\) and the spectral projectors of \(G_v\). The data are \(N\) independent draws \(\mathbf X_N=(X^{(1)},\ldots,X^{(N)})\) from \(P_X\); from them one forms the clipped empirical cumulant tensor \(\widehat{\mathcal T}_r\) at clipping level \(B_m=1+H\) with \(H=n^{2r}\max\{1,M\}\) and \(m=\binom{p+r}{r}-1\), the minimum-distance fit \(\widehat C\), the estimated set \(\widehat J_x=J_x(\widehat C)\), and, with \(e_N=\sqrt{m(1+H)/(N\alpha)}\), \(\tau_N=L_me_N\), \(b_N=2L_C\tau_N\), \(w_N=(a^{-1}+a^{-2})b_N\) and \(w_N^{\mathrm{vec}}=\sqrt{p-1}\,w_N\), the scalar confidence union \(\mathcal U_{xy}\subseteq\mathbb R\) and the response-vector confidence union \(\mathcal U_x^{\mathrm{vec}}\subseteq\mathbb R^{p-1}\). — This is the block with the most confirmed reuse. `Causalean.Mathlib.Probability.iid_mean_euclidean_abs_le` (../Causalean/Mathlib/Probability/IidMeanVariance.lean:318) and `memLp_two_iid_mean_euclidean` give the vector empirical-mean L2/L1 deviation used at (14)-(15); `Causalean.Mathlib.Analysis.abs_singularValues_add_sub_singularValues_le_opNorm` (SingularValueWeyl.lean:212) and its `ContinuousLinearMap` form (line 228) are exactly the Weyl step (20) `|s_min(G') - s_min(G)| <= ||G'-G||_op`; `Causalean.Mathlib.Analysis.Matrix.singularValues_mul_mul_transpose_lower_bound` (RectangularSignalSingularValues.lean:179) is exactly the `s_min(S D S^T) >= s_min(S)^2 s_min(D)` step (4) of the local-inverse lemma; The minimum-distance fit does NOT reuse `Causalean.Mathlib.MeasureTheory.borelMeasurable_compact_argmin_selector` (CompactArgminSelection.lean:134): that decl returns an arbitrary measurable feasible minimizer with ties unresolved, whereas def:minimum-distance-fit is the lexicographically selected minimizer, which this block constructs and verifies locally by successive coordinate argmin over the compact feasible set (`IsCompact.exists_isMinOn`, Mathlib.Topology.Order.Compact). `Metric.hausdorffDist` (Mathlib.Topology.MetricSpace.HausdorffDistance) is the `d_H` of the theorem. Analogous module for the perturbation layer: `Causalean/Discovery/LinearDisentanglement/Quantitative/` -- `opNorm_sub_le_of_approximate_simultaneous_congruence`, `transition_coordinate_product_control`, `admissibleRadius`, `stabilityConstant` carry the SAME proof pattern (explicit local operator-norm modulus + admissible residual radius for an approximate simultaneous congruence) at a second-moment/congruence abstraction; the gap to bridge is that the present pencil is an order-\(r\) tensor contraction with a Riesz-projector eigenvector step rather than a family of congruences. Search trace: grep -i 'confidence|coverage|hausdorff' over CausalSmith/CausalSmith -> `EID_RobustBackshiftUniformDistance_Research/Helpers/ConfidenceUnion.lean` (square `RealMatrix p`, environment-indexed, `honestRegionRadius`/`confidenceUnion`) -- bypass-justified at this abstraction (rectangular mixing, source-indexed balls, no environments) but retained as the closest template.
**required modules.** Mathlib.MeasureTheory.Constructions.Pi, Mathlib.Analysis.CStarAlgebra.Matrix, Mathlib.Analysis.Matrix.Spectrum, Mathlib.Topology.MetricSpace.HausdorffDistance, Causalean.Mathlib.Probability.IidMeanVariance, Causalean.Mathlib.Analysis.SingularValueWeyl, Causalean.Mathlib.Analysis.RectangularSignalSingularValues, Mathlib.Topology.Order.Compact

**S-5 (The heterogeneous-order population-oracle world).** The heterogeneous-order population-oracle world. Given the observational law \(P_X\), the known source count \(n\) and an integer cap \(K\ge2n-1\), the coupled recovery handle \(\mathcal H_{\mathrm{multi}}(P_X,n;K)\) processes each order \(\nu\in\{2n-1,\ldots,K\}\): it forms \(\mathcal T_\nu(P_X)=\operatorname{cum}_\nu(X)\), reshapes it into the three blocks of degrees \((n-1,n-1,\nu-2n+2)\), takes the least \(R\in\{0,\ldots,n\}\) for which the reshaped tensor admits a decomposition into \(R\) nonzero terms \(\gamma_h\operatorname{vec}(z_h^{\otimes\ell_1})\otimes\operatorname{vec}(z_h^{\otimes\ell_2})\otimes\operatorname{vec}(z_h^{\otimes\ell_3})\), retains the projective classes \([z_h]\) of any minimizer, unions them across processed orders by exact projective equality, and returns once that union has cardinality \(n\). This is a population-oracle description with exact cumulants, not a finite-sample algorithm. — Search trace: grep -i 'tensor|rank decomposition|CP rank' over ../Causalean -> nothing at the tensor-decomposition abstraction (`Data.Holor` in Mathlib gives `CPRankMax` but no uniqueness). Mathlib-side: `lean_leansearch` 'uniqueness of tensor rank decomposition Kruskal rank three factor matrices' -> only `PiTensorProduct.lift.unique'`, `Holor.CPRankMax1.*`, `rank_tensorProduct'`; no Kruskal-type uniqueness, which is why lem:kruskal-three-factor-uniqueness stays a cited gate. The handle is defined set-theoretically (least feasible \(R\) via `Nat.find` over a decidable-in-principle existence predicate, minimizer direction sets collected as a `Set`), so no executable content is claimed.
**required modules.** Mathlib.LinearAlgebra.Matrix.Rank, Mathlib.Data.Set.Card

## Assumptions (A)

**A-1 (assumption).** \(\operatorname{rank}(C)=p\)

**A-2 (assumption).** \(c_j\ne0\) for every \(j\in\mathsf S\)

**A-3 (assumption).** \(c_j\notin\operatorname{span}(c_k)\) for all distinct \(j,k\in\mathsf S\)

**A-4 (assumption).** \(\lVert c_j\rVert_2=1\) for every \(j\in\mathsf S\)

**A-5 (assumption).** \(\varepsilon_1,\ldots,\varepsilon_n\) are mutually independent

**A-6 (assumption).** \(\operatorname{Var}(\varepsilon_j)>0\) for every \(j\in\mathsf S\)

**A-7 (assumption).** \(\varepsilon_j\) is non-Gaussian for every \(j\in\mathsf S\)

**A-8 (assumption).** For every source \(j\in\mathsf S\) and every integer \(k\ge1\), \(\mathbb E|\varepsilon_j|^k<\infty\)

**A-9 (assumption).** \(Q_{ii}=1\) for every \(i\in\mathsf S\)

**A-10 (assumption).** \(H\) is monomial, meaning that every row and every column has exactly one nonzero entry

**A-11 (assumption).** \(\det(Q)\ne0\)

**A-12 (assumption).** \([Q^{-1}H]_{\mathsf O,:}=C\)

**A-13 (assumption).** \(\det(Q_{-x,-x})\ne0\)

**A-14 (assumption).** \(X^{(1)},\ldots,X^{(N)}\) are independent draws from \(P_X\)

**A-15 (assumption).** \(\kappa\le |\lambda_j|\le\Lambda\) for every \(j\in\mathsf S\)

**A-16 (assumption).** \(u^\top c_j\ge\sigma\) for every \(j\in\mathsf S\)

**A-17 (assumption).** \(s_{\min}(V_d)\ge\sigma\)

**A-18 (assumption).** \(\left|v^\top c_j/(u^\top c_j)-v^\top c_k/(u^\top c_k)\right|\ge\sigma\) for all distinct \(j,k\in\mathsf S\)

**A-19 (assumption).** \(\mathbb E|\varepsilon_j|^{2r}\le M\) for every \(j\in\mathsf S\)

**A-20 (assumption).** \(s_{\min}(C)\ge\delta\)

**A-21 (assumption).** \(|C_{ij}|=0\) or \( |C_{ij}|\ge a\) for every \(i\in\mathsf O\) and \(j\in\mathsf S\)

**A-22 (assumption).** \(s_{\min}(C_{-i,-j})=0\) or \(s_{\min}(C_{-i,-j})\ge\delta\) for every \(i\in\mathsf O\) and \(j\in\mathsf S\)

**A-23 (assumption).** \(\mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)\ne\varnothing\)

## Definitions (P)

**P-1 (…).** \(\mathfrak F_x(C,\varepsilon)=\{\mathcal M=(Q,H):QV=H\varepsilon,\ Q,H\in\mathbb R^{n\times n},\ Q_{ii}=1\ (i\in\mathsf S),\ H\text{ is monomial},\ \det(Q)\ne0,\ [Q^{-1}H]_{\mathsf O,:}=C,\ \det(Q_{-x,-x})\ne0\}\)

**P-2 (For \(\mathcal M=(Q,H)\in\mathfrak F_x(C,\varepsilon)\),…).** For \(\mathcal M=(Q,H)\in\mathfrak F_x(C,\varepsilon)\), set \(s_{\mathcal M}(i)=j\) exactly when \(H_{ij}\ne0\)

**P-3 (…).** \(\theta_{y\leftarrow x}(\mathcal M)=\partial\,\operatorname{Eq}_{\mathcal M}(V_y\mid\operatorname{do}(V_x=t))/\partial t=(Q^{-1})_{yx}/(Q^{-1})_{xx}\)

**P-4 (…).** \(J_x(C)=\{j\in\mathsf S:C_{xj}\ne0\text{ and }\operatorname{rank}(C_{-x,-j})=p-1\}\)

**P-5 (\(\mathcal R_{xy}(C)=\{C_{yj}/C_{xj}:j\in J_x(C)\}\)).** \(\mathcal R_{xy}(C)=\{C_{yj}/C_{xj}:j\in J_x(C)\}\)

**P-6 (Extend the rows of \(C\) to…).** Extend the rows of \(C\) to \(T_0\in\operatorname{GL}_n(\mathbb R)\), set \(W_0=T_0^{-1}\), and put \(K_x=(W_0)_{:,\{x\}\cup\mathsf L}\). Determine \(J_x(C)\) by retaining exactly the indices \(j\) for which \(C_{xj}\ne0\) and the \(j\)-th row of \(K_x\) is nonzero. Form the bipartite nonzero-pattern graph of \((W_0)_{:, -x}\), with row vertices \(\mathsf S\) and column vertices \(\mathsf S\setminus\{x\}\). Compute once a matching that saturates all column vertices, its unique unmatched row, and an alternating-reachability forest rooted at that row. For each \(j\in J_x(C)\), flip the matching along a stored alternating path to \(j\), obtaining a matching that saturates all columns other than \(x\) and leaves row \(j\) unmatched. Retain \(T_j=T_0\) and \(W_j=W_0\) if \((W_0)_{jx}\ne0\); otherwise choose \(\ell\in\mathsf L\) with \((W_0)_{j\ell}\ne0\) and set \(T_j=(I_n+e_\ell e_x^\top)T_0\) and \(W_j=W_0(I_n-e_\ell e_x^\top)\). Augment the matching by assigning row \(j\) to column \(x\), and let \(\Pi_j\) be the row-permutation matrix whose row indexed by a column selects the row assigned to that column. With \(D_j=\operatorname{diag}(\Pi_jW_j)\), return \(Q_j=D_j^{-1}\Pi_jW_j\), \(H_j=D_j^{-1}\Pi_j\), and \(\mathcal M_j=(Q_j,H_j)\).

**P-7 (…).** \(\mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)=\{\zeta=(P_X,C,\varepsilon,\lambda):X=C\varepsilon\in\mathbb R^p,\ C\in\mathbb R^{p\times n},\lambda_j=\operatorname{cum}_r(\varepsilon_j),\ d\ge1,\ q\ge1,\ r=2d+q,\ n\le\binom{p+d-1}{d},\text{ and the listed source, normalization, cumulant, tensor, moment, loading, and deletion-margin properties hold for the indices }p,n,r,d,q\}\)

**P-8 (For each \(w\in\{u,v,e_1,\ldots,e_p\}\), contract the las…).** For each \(w\in\{u,v,e_1,\ldots,e_p\}\), contract the last \(q\) modes of \(\mathcal T_r\) against \(u^{\otimes(q-1)}\otimes w\) and matricize the remaining \(2d\) modes as \(M_w=V_d\operatorname{diag}(\lambda_j(u^\top c_j)^{q-1}(w^\top c_j))V_d^\top\). Let \(U\) be an orthonormal basis for \(\operatorname{range}(M_u)\), set \(A_w=U^\top M_wU\) and \(G_w=A_wA_u^{-1}\), form the spectral projector \(P_j\) of \(G_v\), and set \(z_{ij}=\operatorname{tr}(G_{e_i}P_j)\). Return \(c_j=z_j/\lVert z_j\rVert_2\), oriented by \(u^\top c_j>0\), up to a common permutation

**P-9 (\(\eta=\kappa\sigma^{q+2}\), \(\chi=\sqrt n/\sigma\),…).** \(\eta=\kappa\sigma^{q+2}\), \(\chi=\sqrt n/\sigma\), \(h=2/\eta+2n\Lambda/\eta^2\), \(\epsilon_0=\min\{\eta/2,\sigma/(6\chi h)\}\), \(L_z=nh\{2\chi+6n\Lambda\chi^2/(\eta\sigma)\}\), and \(L_C=2\sqrt{np}L_z\)

**P-10 (Compute every nonconstant raw empirical moment through to…).** Compute every nonconstant raw empirical moment through total degree \(r\) from \(\mathbf X_N\), clip each coordinate to \([-B_m,B_m]\), and substitute the clipped vector into the moment-cumulant partition polynomial to obtain \(\widehat{\mathcal T}_r\)

**P-11 (Let \((\widehat C,\widehat\lambda)\) be the lexicographic…).** Let \((\widehat C,\widehat\lambda)\) be the lexicographically selected minimizer of \(\lVert\widehat{\mathcal T}_r-\sum_{j=1}^n\lambda'_j(c'_j)^{\otimes r}\rVert_F\) over normalized pairs \((C',\lambda')\) satisfying the numerical restrictions defining \(\mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)\)

**P-12 (…).** \(\widehat J_x=\{j\in\mathsf S:\widehat C_{xj}\ne0\text{ and }\operatorname{rank}(\widehat C_{-x,-j})=p-1\}\)

**P-13 (If \(2\tau_N<\epsilon_0\) and \(b_N<\min\{a,\delta\}/2\),…).** If \(2\tau_N<\epsilon_0\) and \(b_N<\min\{a,\delta\}/2\), set \(\mathcal U_{xy}=\bigcup_{j\in\widehat J_x}[\widehat C_{yj}/\widehat C_{xj}-w_N,\widehat C_{yj}/\widehat C_{xj}+w_N]\); otherwise set \(\mathcal U_{xy}=[-a^{-1},a^{-1}]\)

**P-14 (Given \(P_X\), the known source count \(n\), and an integ…).** Given \(P_X\), the known source count \(n\), and an integer cap \(K\ge2n-1\), define \(\mathcal H_{\mathrm{multi}}(P_X,n;K)\) as follows. For every \(\nu\in\{2n-1,\ldots,K\}\), compute \(\mathcal T_\nu(P_X)=\operatorname{cum}_\nu(X)\), set \(\ell_{1,\nu}=\ell_{2,\nu}=n-1\) and \(\ell_{3,\nu}=\nu-2n+2\), and reshape the tensor according to these three blocks as \(\widetilde{\mathcal T}_\nu\). Solve the explicit rank minimization \[ R_\nu^\star= \min\left\{ R\in\{0,\ldots,n\}: \widetilde{\mathcal T}_\nu =\sum_{h=1}^{R}\gamma_h \operatorname{vec}(z_h^{\otimes\ell_{1,\nu}})\otimes \operatorname{vec}(z_h^{\otimes\ell_{2,\nu}})\otimes \operatorname{vec}(z_h^{\otimes\ell_{3,\nu}}),\ \gamma_h\in\mathbb R\setminus\{0\},\ z_h\in\mathbb R^p\setminus\{0\} \right\}, \] where \(R=0\) denotes the empty sum. Retain the projective classes \([z_h]\) from any minimizer, union them over the processed orders using exact projective equality, and return once this union has cardinality \(n\); if it has not reached cardinality \(n\) by \(K\), increase the cap.

**P-15 (For an integer \(m_{\widetilde{\mathcal M}}\ge p\), an ob…).** For an integer \(m_{\widetilde{\mathcal M}}\ge p\), an observational-law structural completion is a tuple \(\widetilde{\mathcal M}=(m_{\widetilde{\mathcal M}},Q^{\widetilde{\mathcal M}},H^{\widetilde{\mathcal M}},\xi^{\widetilde{\mathcal M}})\), where \(Q^{\widetilde{\mathcal M}},H^{\widetilde{\mathcal M}}\in\mathbb R^{m_{\widetilde{\mathcal M}}\times m_{\widetilde{\mathcal M}}}\), \(\xi^{\widetilde{\mathcal M}}\in\mathbb R^{m_{\widetilde{\mathcal M}}}\), and the endogenous equilibrium satisfies \(Q^{\widetilde{\mathcal M}}V^{\widetilde{\mathcal M}}=H^{\widetilde{\mathcal M}}\xi^{\widetilde{\mathcal M}}\). Its observed mixing matrix is \[ A^{\widetilde{\mathcal M}} =\bigl[(Q^{\widetilde{\mathcal M}})^{-1}H^{\widetilde{\mathcal M}}\bigr]_{\mathsf O,:} \in\mathbb R^{p\times m_{\widetilde{\mathcal M}}}. \]

**P-16 (For \(P_X=\mathcal L(C\varepsilon)\), let…).** For \(P_X=\mathcal L(C\varepsilon)\), let \(\mathfrak F_x^{\mathrm{law}}(P_X)\) be the set of all observational-law completions \(\widetilde{\mathcal M}\) such that: \(Q^{\widetilde{\mathcal M}}_{ii}=1\) for \(1\le i\le m_{\widetilde{\mathcal M}}\); \(H^{\widetilde{\mathcal M}}\) is monomial; \(\det(Q^{\widetilde{\mathcal M}})\ne0\); \(\det(Q^{\widetilde{\mathcal M}}_{-x,-x})\ne0\); the coordinates of \(\xi^{\widetilde{\mathcal M}}\) are mutually independent, nondegenerate, and non-Gaussian; \(A^{\widetilde{\mathcal M}}\) has no zero column and no two proportional columns; and \[ \mathcal L\bigl(A^{\widetilde{\mathcal M}}\xi^{\widetilde{\mathcal M}}\bigr)=P_X. \] No spectral-radius or dynamical-stability restriction is imposed.

**P-17 (For…).** For \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\) and \(i\in\mathsf O\), define \(s_{\widetilde{\mathcal M}}^{\mathrm{law}}(i)\) to be the unique \(k\in\{1,\ldots,m_{\widetilde{\mathcal M}}\}\) such that \(H^{\widetilde{\mathcal M}}_{ik}\ne0\).

**P-18 (For…).** For \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\) and \(i\in\mathsf O\), define the aligned source \(\bar s_{\widetilde{\mathcal M}}(i)\) to be the unique \(j\in\mathsf S\) such that \[ A^{\widetilde{\mathcal M}}_{:,s_{\widetilde{\mathcal M}}^{\mathrm{law}}(i)} \in\operatorname{span}(c_j). \]

**P-19 (For…).** For \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\), define its observed hard-intervention equilibrium response vector by \[ \rho_x(\widetilde{\mathcal M}) =\left( \frac{\partial}{\partial t}\operatorname{Eq}_{\widetilde{\mathcal M}} \bigl(V_i^{\widetilde{\mathcal M}}\mid\operatorname{do}(V_x^{\widetilde{\mathcal M}}=t)\bigr) \right)_{i\in\mathsf O\setminus\{x\}} \in\mathbb R^{p-1}. \]

**P-20 (Define the response-vector frontier…).** Define the response-vector frontier \[ \mathcal V_x(C) =\left\{ \left(\frac{C_{ij}}{C_{xj}}\right)_{i\in\mathsf O\setminus\{x\}} :j\in J_x(C) \right\}\subseteq\mathbb R^{p-1}. \]

**P-21 (With \(w_N^{\mathrm{vec}}=\sqrt{p-1}\,w_N\), let…).** With \(w_N^{\mathrm{vec}}=\sqrt{p-1}\,w_N\), let \(\overline B_2(z,t)=\{v\in\mathbb R^{p-1}:\lVert v-z\rVert_2\le t\}\). If \(2\tau_N<\epsilon_0\) and \(b_N<\min\{a,\delta\}/2\), define \[ \mathcal U_x^{\mathrm{vec}} =\bigcup_{j\in\widehat J_x} \overline B_2\!\left( \left(\frac{\widehat C_{ij}}{\widehat C_{xj}}\right)_{i\in\mathsf O\setminus\{x\}}, w_N^{\mathrm{vec}} \right). \] Otherwise define \[ \mathcal U_x^{\mathrm{vec}} =\overline B_2\!\left(0,\frac{\sqrt{p-1}}a\right). \]

**P-22 (Let \(\mathbb P^{p-1}\) denote real projective space).** Let \(\mathbb P^{p-1}\) denote real projective space. Define \[ \mathcal D(P_X) =\bigcup\left\{ \{[a_k]:1\le k\le m\}: \begin{array}{l} m\ge p,\ A=[a_1\;\cdots\;a_m]\in\mathbb R^{p\times m},\ \\ a_k\ne0\text{ and }a_k\notin\operatorname{span}(a_\ell)\text{ for }k\ne\ell,\\ \xi_1,\ldots,\xi_m\text{ are mutually independent, nondegenerate, and non-Gaussian},\\ \mathcal L(A\xi)=P_X \end{array} \right\}. \] Thus \(\mathcal D(P_X)\) is defined set-theoretically from the observational law by unioning the unordered projective column sets of all irreducible, all-non-Gaussian independent-component representations. Its asserted uniqueness and equality to the directions of a particular representation are theorem conclusions, not parts of this definition, and the definition does not prescribe an evaluation algorithm.

## Lemmas (L)

**L-1 (For every…).** For every \(\mathcal M=(Q,H)\in\mathfrak F_x(C,\varepsilon)\), if \(j=s_{\mathcal M}(x)\), then \(C_{xj}\ne0\) and \(\theta_{y\leftarrow x}(\mathcal M)=C_{yj}/C_{xj}\)

**L-2 (For every \(\mathcal M\in\mathfrak F_x(C,\varepsilon)\),…).** For every \(\mathcal M\in\mathfrak F_x(C,\varepsilon)\), its assigned source satisfies \(s_{\mathcal M}(x)\in J_x(C)\)

**L-3 (For every \(j\in J_x(C)\), \(\operatorname{Cert}(C,x)\) r…).** For every \(j\in J_x(C)\), \(\operatorname{Cert}(C,x)\) returns a completion \(\mathcal M_j\in\mathfrak F_x(C,\varepsilon)\) with \(s_{\mathcal M_j}(x)=j\). If \(C\) is rational, the returned \(Q_j\) and \(H_j\) are rational.

**L-4 (Let \(R\ge1\), let \(z_1,\ldots,z_R\) be nonzero pairwise…).** Let \(R\ge1\), let \(z_1,\ldots,z_R\) be nonzero pairwise nonproportional vectors in a finite-dimensional real vector space, and let \(s\ge R-1\). Then the columns \(z_1^{\otimes s},\ldots,z_R^{\otimes s}\) have Kruskal rank \(R\); equivalently, every subcollection is linearly independent.

**L-5 (For every integer \(K\ge3\) and every \(\rho>0\), there e…).** For every integer \(K\ge3\) and every \(\rho>0\), there exists a centered, variance-one, non-Gaussian, moment-determinate probability law \(F\) on \(\mathbb R\) such that \(d_{\mathrm{TV}}(F,\mathcal N(0,1))<\rho\) and the first \(K\) raw moments, hence the cumulants through order \(K\), agree with those of \(\mathcal N(0,1)\).

**L-6 (For every…).** For every \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\), the response vector is well defined and \[ \rho_x(\widetilde{\mathcal M}) =\left( \frac{((Q^{\widetilde{\mathcal M}})^{-1})_{ix}} {((Q^{\widetilde{\mathcal M}})^{-1})_{xx}} \right)_{i\in\mathsf O\setminus\{x\}}. \]

**L-7 (Let \(C'=C\Pi D\), where \(\Pi e_k=e_{\pi(k)}\) is a perm…).** Let \(C'=C\Pi D\), where \(\Pi e_k=e_{\pi(k)}\) is a permutation matrix and \(D=\operatorname{diag}(d_1,\ldots,d_n)\) has \(d_k\ne0\) for every \(k\). Then \[ k\in J_x(C')\quad\Longleftrightarrow\quad \pi(k)\in J_x(C), \] and consequently \(\mathcal V_x(C')=\mathcal V_x(C)\) and \(\mathcal R_{xy}(C')=\mathcal R_{xy}(C)\).

**L-8 (Let…).** Let \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\) and suppose \(m_{\widetilde{\mathcal M}}=n\) and \(A^{\widetilde{\mathcal M}}=C\Pi D\), where \(\Pi e_k=e_{\pi(k)}\) and \(D\) is nonsingular diagonal. Define \[ H^C=H^{\widetilde{\mathcal M}}D^{-1}\Pi^\top, \qquad \mathcal M^C=(Q^{\widetilde{\mathcal M}},H^C). \] Then \(\mathcal M^C\in\mathfrak F_x(C,\varepsilon)\). For every \(i\in\mathsf O\), if \(s_{\widetilde{\mathcal M}}^{\mathrm{law}}(i)=k\), then \(s_{\mathcal M^C}(i)=\pi(k)=\bar s_{\widetilde{\mathcal M}}(i)\). The inverse structural matrix, its intervention minor, and every equilibrium response ratio are unchanged by this canonicalization.

**L-9 (For every…).** For every \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\), one has \(m_{\widetilde{\mathcal M}}=n\), the aligned source \(j=\bar s_{\widetilde{\mathcal M}}(x)\) belongs to \(J_x(C)\), and \[ \rho_x(\widetilde{\mathcal M}) =\left(\frac{C_{ij}}{C_{xj}}\right)_{i\in\mathsf O\setminus\{x\}} \in\mathcal V_x(C). \] In particular, \(C_{xj}\ne0\) and \(\operatorname{rank}(C_{-x,-j})=p-1\).

**L-10 (Let \(C,C'\in\mathbb R^{p\times n}\) and…).** Let \(C,C'\in\mathbb R^{p\times n}\) and \(\lambda,\lambda'\in\mathbb R^n\). Suppose \(d\ge1\), \(q\ge1\), \(r=2d+q\), and \(n\le\binom{p+d-1}{d}\). For every \(j\in\mathsf S\), suppose \[ \lVert c_j\rVert_2=\lVert c'_j\rVert_2=1, \qquad \kappa\le |\lambda_j|,|\lambda'_j|\le\Lambda, \qquad u^\top c_j,u^\top c'_j\ge\sigma. \] Suppose also that both lifted matrices \([c_1^{\otimes d}\;\cdots\;c_n^{\otimes d}]\) and \([(c'_1)^{\otimes d}\;\cdots\;(c'_n)^{\otimes d}]\) have least singular value at least \(\sigma\), and that both ratio collections \[ \left\{\frac{v^\top c_j}{u^\top c_j}:j\in\mathsf S\right\}, \qquad \left\{\frac{v^\top c'_j}{u^\top c'_j}:j\in\mathsf S\right\} \] have pairwise gaps at least \(\sigma\). Define \[ \mathcal T(C,\lambda)=\sum_{j=1}^n\lambda_jc_j^{\otimes r}, \qquad \mathcal T(C',\lambda')=\sum_{j=1}^n\lambda'_j(c'_j)^{\otimes r}. \] If \(\lVert\mathcal T(C',\lambda')-\mathcal T(C,\lambda)\rVert_F<\epsilon_0\), then there is a permutation \(\pi\) of \(\mathsf S\) such that \[ \lVert C'_{\pi}-C\rVert_F \le L_C\lVert\mathcal T(C',\lambda')-\mathcal T(C,\lambda)\rVert_F. \] In particular, this bound applies to two numerical factor pairs in the feasible set of the separated tensor minimum-distance fit; no source law for the fitted pair is required.

**L-11 (Suppose \(\operatorname{rank}(C)=p\), and run the shared-…).** Suppose \(\operatorname{rank}(C)=p\), and run the shared-kernel and shared-matching construction in \(\operatorname{Cert}(C,x)\). Then \(K_x=(W_0)_{:,\{x\}\cup\mathsf L}\) is a basis matrix for \(\ker(C_{-x,:})\), and \[ j\in J_x(C) \quad\Longleftrightarrow\quad C_{xj}\ne0\ \text{ and the \(j\)-th row of \(K_x\) is nonzero}. \] For every \(j\in J_x(C)\), the construction returns \(\mathcal M_j=(Q_j,H_j)\in\mathfrak F_x(C,\varepsilon)\) with \(s_{\mathcal M_j}(x)=j\). Conditional on an exact input matrix \(C\), it computes \(J_x(C)\), all ratio frontiers, and all at most \(n\) dense witness pairs \((Q_j,H_j)\) in \(O(n^3)\) exact real-arithmetic operations. This order is optimal for dense all-witness output in the worst case. If \(C\) is rational, every returned \(Q_j\) and \(H_j\) is rational.

**L-12 (For every \(j\in J_x(C)\), let \(\mathcal M_j=(Q_j,H_j)\)…).** For every \(j\in J_x(C)\), let \(\mathcal M_j=(Q_j,H_j)\) be the fixed-law completion returned by \(\operatorname{Cert}(C,x)\), and set \[ \widetilde{\mathcal M}_j=(n,Q_j,H_j,\varepsilon). \] Then \(\widetilde{\mathcal M}_j\in\mathfrak F_x^{\mathrm{law}}(P_X)\), \(\bar s_{\widetilde{\mathcal M}_j}(x)=j\), and \[ \rho_x(\widetilde{\mathcal M}_j) =\left(\frac{C_{ij}}{C_{xj}}\right)_{i\in\mathsf O\setminus\{x\}}. \] If \(C\) is rational, \(Q_j\) and \(H_j\) are rational.

## Theorems (T)

### T-block: t1 — Let \(P_X=\mathcal L(C\varepsilon)\)
**Statement.** Let \(P_X=\mathcal L(C\varepsilon)\). Under the population full-row-rank, nonzero distinct-direction, and independent nondegenerate non-Gaussian source assumptions, the set-valued functional \(\mathcal D(P_X)\) is the unique unordered projective column set common to every irreducible, all-non-Gaussian independent-component representation of \(P_X\), and \[ \mathcal D(P_X)=\{[c_j]:j\in\mathsf S\}. \] This is a full-law identification statement, not an evaluation algorithm. Moreover, \(J_x(C)\ne\varnothing\), every \(\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\) has \(m_{\widetilde{\mathcal M}}=n\), and the sharp law-fiber response-vector set is \[ \left\{\rho_x(\widetilde{\mathcal M}):\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\right\} =\mathcal V_x(C) =\left\{\left(\frac{C_{ij}}{C_{xj}}\right)_{i\in\mathsf O\setminus\{x\}}:C_{xj}\ne0,\ \operatorname{rank}(C_{-x,-j})=p-1\right\}. \] For every ordered \(y\ne x\), its scalar coordinate projection is exactly \[ \left\{[\rho_x(\widetilde{\mathcal M})]_y:\widetilde{\mathcal M}\in\mathfrak F_x^{\mathrm{law}}(P_X)\right\} =\mathcal R_{xy}(C). \] The frontiers are independent of the representative of \(\mathcal D(P_X)\), because column permutation and nonzero rescaling leave them invariant. The entire response vector is identified if and only if \(\mathcal V_x(C)\) is a singleton, and the \(y\)-coordinate effect is identified if and only if \(\mathcal R_{xy}(C)\) is a singleton. If the latter is not a singleton, two indices with distinct ratios yield two exact same-law SCM countermodels via the canonical embeddings \(\widetilde{\mathcal M}_j=(n,Q_j,H_j,\varepsilon)\) of the shared-kernel and shared-matching certificate construction. Conditional on any exact irreducible mixing representative \(C\), all frontier values and one exact witness per value are computable in \(O(n^3)\) exact real-arithmetic operations. This order is optimal for dense all-witness output in the worst case. This is not an end-to-end complexity claim for obtaining \(C\) from \(P_X\). No stability assumption is imposed.

### T-block: t2 — Let \(P_X=\mathcal L(C\varepsilon)\)
**Statement.** Let \(P_X=\mathcal L(C\varepsilon)\). Suppose that \(\operatorname{rank}(C)=p\), every column of \(C\) is nonzero, no two columns of \(C\) are proportional, and the coordinates of \(\varepsilon\) are mutually independent, nondegenerate, and non-Gaussian. The law-fiber response vector \(\rho_x\) is point identified over \(\mathfrak F_x^{\mathrm{law}}(P_X)\) if and only if \(|\mathcal V_x(C)|=1\), equivalently \(|J_x(C)|=1\). If this vector criterion fails, there are distinct \(j,k\in J_x(C)\) for which the canonical embeddings of the completions returned by \(\operatorname{Cert}(C,x)\) are two exact same-observational-law members \(\widetilde{\mathcal M}_j,\widetilde{\mathcal M}_k\in\mathfrak F_x^{\mathrm{law}}(P_X)\), both satisfying postintervention solvability, such that \(\rho_x(\widetilde{\mathcal M}_j)\ne\rho_x(\widetilde{\mathcal M}_k)\). They can be constructed in \(O(n^3)\) additional arithmetic operations after the shared basis extension and inverse. For every \(y\in\mathsf O\setminus\{x\}\), the scalar coordinate \([\rho_x]_y\) is point identified over the same law fiber if and only if \[ |\mathcal R_{xy}(C)|=1, \qquad\text{equivalently}\qquad \operatorname{rank}\!\left(C_{\{x,y\},J_x(C)}\right)=1. \] If this scalar criterion fails, two canonical witnesses have distinct \(y\)-coordinate responses. Each scalar frontier is the corresponding coordinate projection of \(\mathcal V_x(C)\); the claim does not combine coordinates arising from different source indices into a response vector.

### T-block: t3 — When \(n=p\), the latent-row shear is unnecessary
**Statement.** When \(n=p\), the latent-row shear is unnecessary. Section 4 of LacerdaSpirtesRamseyHoyer2008 constructs candidate models from admissible row permutations, and its Theorem 1 states that the output of LiNG-D is a distribution-entailment equivalence class. Among those square admissible row-permutation candidates, restrict to the candidates satisfying \(\det(Q_{-x,-x})\ne0\). The projection of this postintervention-solvable subset onto \(\theta_{y\leftarrow x}\) equals \(\mathcal R_{xy}(C)\).

### T-block: t4 — For every \(N\) and every nonempty class…
**Statement.** For every \(N\) and every nonempty class \(\mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)\) with \(d\ge1\), \(q\ge1\), and \(n\le\binom{p+d-1}{d}\), the scalar and response-vector confidence unions computed from \(\mathbf X_N\) satisfy \[ \inf_{\zeta=(P_X,C,\varepsilon,\lambda)\in \mathfrak P_{p,n,r}(a,\delta,\sigma,\kappa,\Lambda,M)} P_X^{\otimes N}\!\left\{ \begin{array}{l} \mathcal V_x(C)\subseteq\mathcal U_x^{\mathrm{vec}} \text{ for every }x\in\mathsf O,\quad \mathcal R_{xy}(C)\subseteq\mathcal U_{xy} \text{ for every }x\ne y,\\[2pt] \text{and, if }2\tau_N<\epsilon_0 \text{ and }b_N<\min\{a,\delta\}/2,\text{ then after one common permutation},\\[2pt] \widehat J_x=J_x(C) \text{ and }d_H(\mathcal U_x^{\mathrm{vec}},\mathcal V_x(C)) \le2w_N^{\mathrm{vec}} \text{ for every }x\in\mathsf O,\\[2pt] d_H(\mathcal U_{xy},\mathcal R_{xy}(C))\le2w_N \text{ for every }x\ne y \end{array} \right\}\ge1-\alpha. \] Writing \(\operatorname{pr}_{xy}:\mathbb R^{p-1}\to\mathbb R\) for the \(y\)-coordinate projection associated with treatment \(x\), one has \[ \mathcal R_{xy}(C)=\operatorname{pr}_{xy}(\mathcal V_x(C)), \qquad \mathcal U_{xy}\subseteq \operatorname{pr}_{xy}(\mathcal U_x^{\mathrm{vec}}). \] Thus the scalar results are coordinatewise reports for the same response coordinates; no Cartesian product of scalar unions is asserted. By the sharp effect-frontier theorem, \(\mathcal V_x(C)\) and \(\mathcal R_{xy}(C)\) are exactly the response-vector and scalar causal targets over \(\mathfrak F_x^{\mathrm{law}}(P_X)\). Uniformly over the same representation-indexed tuples in the informative regime, \(2w_N^{\mathrm{vec}}=O(N^{-1/2})\) and \(2w_N=O(N^{-1/2})\).

### T-block: t5 — For a fixed normalized full-row-rank representation…
**Statement.** For a fixed normalized full-row-rank representation \(X=C\varepsilon\) with mutually independent, nondegenerate, non-Gaussian sources having finite absolute moments of every positive integer order and with pairwise nonproportional mixing columns, put \[ A_\nu=\{j:\operatorname{cum}_\nu(\varepsilon_j)\ne0\}, \qquad R_\nu=|A_\nu|. \] The handle \(\mathcal H_{\mathrm{multi}}(P_X,n;K)\), run with exact population cumulants and the known source count, is a terminating pointwise population-oracle procedure: it processes \(\nu=2n-1,2n,\ldots\); for each processed order it solves the stated finite polynomial feasibility systems for \(R=0,\ldots,n\), selects the least feasible \(R\), and deduplicates the returned directions by exact projective equality until their union has cardinality \(n\). At every \(\nu\ge2n-1\), reshaping into blocks of degrees \((n-1,n-1,\nu-2n+2)\) makes the least feasible decomposition recover exactly \(\{[c_j]:j\in A_\nu\}\). The procedure stops after a finite representation-dependent order and returns \[ \mathcal D(P_X)=\{[c_1],\ldots,[c_n]\}, \] without cross-order label alignment, and hence evaluates \(\mathcal V_x(C)\) and every \(\mathcal R_{xy}(C)\) from any representatives of those directions. For \(R_\nu\ge2\), recovery at a processed order has the ex-post adaptive certificate \(\nu\ge2R_\nu-1\) using block degrees \((R_\nu-1,R_\nu-1,\nu-2R_\nu+2)\); the universally executable schedule nevertheless begins at \(2n-1\). The cases \(R_\nu=0\) and \(R_\nu=1\) are respectively the zero-tensor and rank-one cases. This is neither a uniform finite-cutoff claim nor a finite-sample algorithm. No finite cumulant cutoff is uniform over the qualitative all-finite-moments class. The negative conclusion holds even on the stricter subclass in which every source law is moment determinate. Already for \(p=n=2\), that stricter subclass contains two fixed separated direction and frontier targets and triangular sequences of observational laws whose \(N\)-sample total-variation distance tends to zero. Consequently neither the direction set nor the causal frontier admits a uniformly consistent estimator on that subclass, and no frontier-containing confidence-union sequence with uniform coverage strictly greater than one half can have uniformly vanishing Hausdorff error. The frontier map is also discontinuous at vanishing treatment loadings and deletion-rank boundaries. Thus a uniform root-\(N\) heterogeneous-order frontier law does not follow from the qualitative assumptions without quantitative non-Gaussianity, moment, Veronese-conditioning, angular, probe/eigen-gap, deletion-rank, and denominator margins. The existing common-order separated theorem retains its simultaneous coverage and root-\(N\) conclusion.
