---
qid: scm_prox_recant_pid
spec: v1
topic: "Proximal partial identification of the simple-mediation natural/separable direct effect (and the nested recanting-witness path-specific effect, delegated) when proximal completeness fails: the exact fixed-cardinality factorization-constrained proximal response-function program gives a valid, observed-data-computable negative-control-proxy bound that strictly refines the no-proxy Breum/Sjolander floor; anchor 2605.09462 (proximal point-ID under completeness), refine 2512.11549 no-proxy floor, non-reduction vs Ghassami 2304.04374 single-world ATE"
novelty_target: field
tier_at_proposal: REVISE
tier_at_derivation: NA
proposal_promise_gap: "The requested field-tier kernel never cleared D-1 after 16 review attempts; the final gluing-frontier reframe remained at REVISE when stopped."
reusable: unknown
reraise_status: retry
gap_reasons:
  - "This is a restricted re-labelling of pid_mediation_proxy_v1, Conjecture 2 (\"strict proxy tightening over no-proxy bounds\"), which already asks for weak/incomplete proxies to exclude no-proxy natural-effect endpoints."
  - "The recanting identified sets quantify over lambda^rw in F^rw although that feasible set contains (lambda^rw,q^Z,q^W), and no positive-cell nonempty recanting domain is defined to rule out vacuous endpoint exclusion."
  - "The final angle-3 gluing-frontier reframe remained at REVISE because the program masses were not yet bound to the declared compatible-SCM fiber and the delegated recanting feasible set remained undefined."
reusable_artifacts:
  - "discovery/gaps.json — six-paper/open-problem harvest and literature map"
  - "discovery/proto_core_angle2_rejected.json — graph-native fixed-cardinality SCM program and strict-contraction attempt"
  - "discovery/proto_core.json — final exact endpoint-lift/gluing-frontier reframe"
  - "discovery/dneg1_escalation_log.jsonl — persistent scope, SCM-routing, and gluing-frontier directives"
  - "reviews/angle2_v4.json and reviews/angle3_v1.json — hard-reject and final-revise receipts"
seeds_burned: []
proof_attempt_summary: |
  The run never cleared the D-1 proposal gate, so no D0 derivation or Lean proof was attempted.
  Across 16 proposal-review attempts it repaired the completeness-failure domain, restored the
  simple-mediation headline, and made the construction SCM-native, but angle 2 was rejected as
  repo-subsumed.  A final non-reduction/gluing-frontier reframe recovered field-tier novelty but
  still had two model-to-program typing defects when the user stopped the run.
banked_on: "2026-07-17"
---

# scm_prox_recant_pid / v1 — Failed

**Topic.** Proximal partial identification of the simple-mediation natural/separable direct effect (and the nested recanting-witness path-specific effect, delegated) when proximal completeness fails: the exact fixed-cardinality factorization-constrained proximal response-function program gives a valid, observed-data-computable negative-control-proxy bound that strictly refines the no-proxy Breum/Sjolander floor; anchor 2605.09462 (proximal point-ID under completeness), refine 2512.11549 no-proxy floor, non-reduction vs Ghassami 2304.04374 single-world ATE

**Novelty target.** field

**Stage -0.5 verdict.** REVISE

**Stage 0.5 verdict.** NA

**Banking reason.** Stopped by user after the D-1 proposal gate failed to converge: angle 2 received a hard not-publishable repo-subsumption reject, and the angle-3 gluing-frontier reframe remained unaccepted.

## Key files

- `scm_prox_recant_pid_v1_state.json` — pipeline state at banking (`banked: true`).
- `scm_prox_recant_pid_v1_proposal.tex` — final proposal version.
- `scm_prox_recant_pid_v1.tex` — derivation note (if Stage 0 ran).
- `scm_prox_recant_pid_v1_reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

The bank status is `failed` because the proposal gate was never accepted, not because the final
gluing-frontier theorem was mathematically refuted.  `reraise_status: retry` records that distinction.
