---
qid: stat_counterfactual_wasserstein_rough_nuisance_frontier
spec: v1
topic: "The rough-nuisance Wasserstein frontier for marginal counterfactual generation. Fix integers d>=1 and m>=3, smoothness alpha,beta,s>0, radius L>=10. Observe n iid (X,A,Y), X uniform on [0,1]^d with this design law known, A binary, Y in [0,1]^m. Consistency and ignorability Y(a) independent of A given X hold. The unknown propensity e(x)=P(A=1|X=x) belongs to H_d^alpha(L) and lies in [1/4,3/4]. The conditional density eta(y|x) of Y given A=1,X=x integrates to 1 in y for every x and satisfies 1/2<=eta<=3/2. For every y its x-section lies in H_d^beta(L); for every x its y-section lies in H_m^s(L); no mixed derivative condition is imposed. The A=0 conditional density is fixed uniform, carrying no additional restrictions linking arm1 to arm0. H_k^t(L) uses r=ceil(t)-1: all partial derivatives through order r bounded by L, and order-r partial derivatives Holder of exponent t-r with constant L (integer t means Lipschitz derivatives of order t-1). The target probability law Q1 has density q1(y)=integral eta(y|x) dx, the law of Y(1). Risk is inf over all measurable probability-valued estimators Qhat of sup over this class E[W1(Qhat,Q1)], with Euclidean transport cost. Constants may depend on d,m,alpha,beta,s,L, never n. Each fixed parameter tuple has asymptotic n scope; no adaptation to unknown smoothness is required. KERNEL: Determine, up to positive constants including every unavoidable critical logarithmic factor, the minimax W1 risk R_n(d,m,alpha,beta,s) on the specified class; construct one explicit finite-data probability-valued estimator attaining it and a matching same-class all-estimator converse. Derive the necessary-and-sufficient smoothness/dimension boundary for R_n having the oracle order n^{-(s+1)/(2s+m)} and derive the sharp rate in every non-oracle region. The exact formula and whether candidate interaction regimes exist are answer-open; a guessed phase diagram is not imposed. The hard theorem is the full matching frontier, with a nonempty non-oracle region derived or, if none exists, a uniform constructive oracle-attainment theorem that explains why rough nuisance does not impose a cost on this whole class. The latter alternative must independently survive the prior-art and flagship gate; no scalar lower bound or oracle-only upper theorem counts as completion. Estimator algebra, projection orders, least-favorable mixtures and proof mechanics are delegated. Explicitly position which subfamilies embed into the published DoubleGen target model; do not claim class containment of its full nuisance framework. COMPUTATION: Construct empirical multiresolution outcome coefficients with finite-order propensity/outcome bias corrections in a known-design covariate basis, then a finite-dimensional minimum-distance projection onto nonnegative unit-mass measures on a refining outcome grid. The output is a discrete probability measure with an executable categorical sampler. Select the finite resolutions and correction order by the known class inputs. This is a proposed concrete algorithm family, not an assumption that its unproved error certificate holds; the solver may derive another explicit algorithm if necessary. CONSUMER: The public DoubleGen implementation and its counterfactual image-generation experiment (Luedtke–Fukumizu 2025, arXiv2509.16842, Section 6.1 and Appendix M.1) and Dance–Xi–Orbanz–Bloem-Reddy DecFM (arXiv2605.07665) generate precisely marginal counterfactual outcome distributions from confounded observations. The frontier tells these methods when increasing generator capacity cannot compensate for rough nuisance learning and supplies a positive reconstruction benchmark/correction alternative. The theorem is for a smooth compact positive-density model and is a theoretical calibration of continuous-output generation, not a claim that raw CelebA pixels satisfy every Holder assumption. npcausal and the published HIV CD4 density application in Kennedy et al Biometrika2023 are additional distributional consumers; the scalar case is not claimed covered by m>=3. PRIOR-ART CONSTRAINT: DoubleGen Section 7 explicitly leaves rough-nuisance optimality open, but Kennedy ACIC 2023 already announces counterfactual-density minimax rates. Establish a substantive new marginal W1 theorem; do not claim first counterfactual-density minimax theory, substitute averaged conditional Wasserstein loss, or present an unmatched bracket as completion.\n\nPRESOLVE EVIDENCE REQUIRING VERIFICATION: The draft constructs smooth, positive, normalized conditional densities with one propensity sign per covariate cell shared across outcome cells. Mixing these signs leaves a deterministic marginal product perturbation. Its displayed all-count mixture calculation gives a proposed Hellinger bound proportional to n^2 h^d delta^m b^2(a^2+b^2), where a scales as h^alpha and b as min(h^beta,delta^s). For d=1,m=6,s=4,alpha=beta=27/200, the resulting Wasserstein lower exponent 243/697 is strictly smaller than the scalar comparator 27/77 and oracle exponent 5/14. Exact exponent arithmetic and a finite normalization/leakage diagnostic were independently checked; the continuous proof remains unverified. An exact weighted Galerkin identity supplies a scale-dependent bias reduction, and finite Kantorovich–Rubinstein projection supplies a probability reconstruction route.\nUNRESOLVED BOTTLENECK: Prove the high-rank higher-order coefficient variance certificate, then match the full asymmetric frontier and critical logarithms. The ACIC2023 density announcement remains a priority-comparison obligation. The isolated lower bound is not completion.\nEARLY KILL TEST: Independently audit the balanced interaction family and full-sample bound, then test a concrete truncated correction at that tuple. Stop the novelty claim if exact prior art subsumes it; do not infer impossibility from failure of one naive correction.\nSTRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_counterfactual_wasserstein_rough_nuisance_frontier.md."
novelty_target: field
banked_novelty_tier: incremental
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "Prove the joint high-rank negative-Besov coefficient-risk certificate, derive a matching upper rate and same-class multiscale converse in every regime, resolve critical logarithms, and characterize the iff oracle boundary without assuming the crux."
reusable: unknown
reraise_status: retry
gap_reasons:
  - "kernel_substituted@oeq:sharp-frontier — The promised Stat deliverable is a matched explicit minimax frontier with critical logarithms, an iff oracle boundary, and an attaining estimator, but the closed kernel supplies only an interaction lower envelope, one non-oracle tuple, and an unmatched loose bracket; the theorem-level rate object therefore remains open."
  - "attainer_missing@oeq:high-rank-coefficient-certificate — The stated finite probability-valued higher-order estimator has no uniform high-rank coefficient-risk certificate, so the note does not yet deliver the estimator-and-rate advance required for its asserted Stat frontier."
  - "under the bar; no bounded fix in scope."
reusable_artifacts:
  - "discovery/core.json — typed graph containing the proved interaction lower envelope, representative non-oracle exponent, finite KR probability-projection lemma, oracle randomized reduction, and GANICE loss-separation witness."
  - "discovery/writeup.tex — checked derivations plus explicit high-rank covariance and multiscale-frontier obstructions."
  - "reviews/review_math.json — clean mathematical audit of the delivered partial results."
  - "reviews/review_general.json — achieved-tier assessment and the precise research program needed for a future re-raise."
seeds_burned: []
proof_attempt_summary: |
  Discovery proved a same-class nuisance-sensitive interaction lower envelope, a strict non-oracle tuple, an oracle submodel reduction, a finite-grid KR projection result, and a comparator-separation witness; the mathematical reviewer reported no errors in those results. The attempted finite-order higher-order estimator did not obtain joint negative-Besov coefficient control once the covariate projection rank exceeds the sample size. Consequently the full matched rate, attaining estimator, critical logarithms, and necessary-and-sufficient oracle boundary remain open, so the sound partial paper cannot stand in for the fixed theorem kernel.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 39330559
  pipeline_claude_tokens: 0
  pipeline_tokens_consumed: 39330559
  total_tokens_consumed: null
banked_on: "2026-09-10"
---

# stat_counterfactual_wasserstein_rough_nuisance_frontier / v1 — Failed

**Topic.** The rough-nuisance Wasserstein frontier for marginal counterfactual generation. Fix integers d>=1 and m>=3, smoothness alpha,beta,s>0, radius L>=10. Observe n iid (X,A,Y), X uniform on [0,1]^d with this design law known, A binary, Y in [0,1]^m. Consistency and ignorability Y(a) independent of A given X hold. The unknown propensity e(x)=P(A=1|X=x) belongs to H_d^alpha(L) and lies in [1/4,3/4]. The conditional density eta(y|x) of Y given A=1,X=x integrates to 1 in y for every x and satisfies 1/2<=eta<=3/2. For every y its x-section lies in H_d^beta(L); for every x its y-section lies in H_m^s(L); no mixed derivative condition is imposed. The A=0 conditional density is fixed uniform, carrying no additional restrictions linking arm1 to arm0. H_k^t(L) uses r=ceil(t)-1: all partial derivatives through order r bounded by L, and order-r partial derivatives Holder of exponent t-r with constant L (integer t means Lipschitz derivatives of order t-1). The target probability law Q1 has density q1(y)=integral eta(y|x) dx, the law of Y(1). Risk is inf over all measurable probability-valued estimators Qhat of sup over this class E[W1(Qhat,Q1)], with Euclidean transport cost. Constants may depend on d,m,alpha,beta,s,L, never n. Each fixed parameter tuple has asymptotic n scope; no adaptation to unknown smoothness is required. KERNEL: Determine, up to positive constants including every unavoidable critical logarithmic factor, the minimax W1 risk R_n(d,m,alpha,beta,s) on the specified class; construct one explicit finite-data probability-valued estimator attaining it and a matching same-class all-estimator converse. Derive the necessary-and-sufficient smoothness/dimension boundary for R_n having the oracle order n^{-(s+1)/(2s+m)} and derive the sharp rate in every non-oracle region. The exact formula and whether candidate interaction regimes exist are answer-open; a guessed phase diagram is not imposed. The hard theorem is the full matching frontier, with a nonempty non-oracle region derived or, if none exists, a uniform constructive oracle-attainment theorem that explains why rough nuisance does not impose a cost on this whole class. The latter alternative must independently survive the prior-art and flagship gate; no scalar lower bound or oracle-only upper theorem counts as completion. Estimator algebra, projection orders, least-favorable mixtures and proof mechanics are delegated. Explicitly position which subfamilies embed into the published DoubleGen target model; do not claim class containment of its full nuisance framework. COMPUTATION: Construct empirical multiresolution outcome coefficients with finite-order propensity/outcome bias corrections in a known-design covariate basis, then a finite-dimensional minimum-distance projection onto nonnegative unit-mass measures on a refining outcome grid. The output is a discrete probability measure with an executable categorical sampler. Select the finite resolutions and correction order by the known class inputs. This is a proposed concrete algorithm family, not an assumption that its unproved error certificate holds; the solver may derive another explicit algorithm if necessary. CONSUMER: The public DoubleGen implementation and its counterfactual image-generation experiment (Luedtke–Fukumizu 2025, arXiv2509.16842, Section 6.1 and Appendix M.1) and Dance–Xi–Orbanz–Bloem-Reddy DecFM (arXiv2605.07665) generate precisely marginal counterfactual outcome distributions from confounded observations. The frontier tells these methods when increasing generator capacity cannot compensate for rough nuisance learning and supplies a positive reconstruction benchmark/correction alternative. The theorem is for a smooth compact positive-density model and is a theoretical calibration of continuous-output generation, not a claim that raw CelebA pixels satisfy every Holder assumption. npcausal and the published HIV CD4 density application in Kennedy et al Biometrika2023 are additional distributional consumers; the scalar case is not claimed covered by m>=3. PRIOR-ART CONSTRAINT: DoubleGen Section 7 explicitly leaves rough-nuisance optimality open, but Kennedy ACIC 2023 already announces counterfactual-density minimax rates. Establish a substantive new marginal W1 theorem; do not claim first counterfactual-density minimax theory, substitute averaged conditional Wasserstein loss, or present an unmatched bracket as completion.

PRESOLVE EVIDENCE REQUIRING VERIFICATION: The draft constructs smooth, positive, normalized conditional densities with one propensity sign per covariate cell shared across outcome cells. Mixing these signs leaves a deterministic marginal product perturbation. Its displayed all-count mixture calculation gives a proposed Hellinger bound proportional to n^2 h^d delta^m b^2(a^2+b^2), where a scales as h^alpha and b as min(h^beta,delta^s). For d=1,m=6,s=4,alpha=beta=27/200, the resulting Wasserstein lower exponent 243/697 is strictly smaller than the scalar comparator 27/77 and oracle exponent 5/14. Exact exponent arithmetic and a finite normalization/leakage diagnostic were independently checked; the continuous proof remains unverified. An exact weighted Galerkin identity supplies a scale-dependent bias reduction, and finite Kantorovich–Rubinstein projection supplies a probability reconstruction route.
UNRESOLVED BOTTLENECK: Prove the high-rank higher-order coefficient variance certificate, then match the full asymmetric frontier and critical logarithms. The ACIC2023 density announcement remains a priority-comparison obligation. The isolated lower bound is not completion.
EARLY KILL TEST: Independently audit the balanced interaction family and full-sample bound, then test a concrete truncated correction at that tuple. Stop the novelty claim if exact prior art subsumes it; do not infer impossibility from failure of one naive correction.
STRONGEST-FORM DRAFT (unverified, gaps listed inside): <repo-root>/internal/presolve_drafts/stat_counterfactual_wasserstein_rough_nuisance_frontier.md.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** kernel_substituted@oeq:sharp-frontier — The promised Stat deliverable is a matched explicit minimax frontier with critical logarithms, an iff oracle boundary, and an attaining estimator, but the closed kernel supplies only an interaction lower envelope, one non-oracle tuple, and an unmatched loose bracket; the theorem-level rate object therefore remains open.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
