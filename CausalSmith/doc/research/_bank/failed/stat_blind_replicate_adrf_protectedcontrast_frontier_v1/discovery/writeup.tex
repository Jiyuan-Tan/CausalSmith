\documentclass{article}
\usepackage[margin=0.8in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{mathrsfs,bm,bbm}
\usepackage{graphicx,booktabs,array,multirow,tabularx,longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{algorithm,algpseudocode}
\usepackage{subcaption,tikz}
\usepackage{siunitx,cancel,accents}
\usepackage{microtype}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}
\setlength{\emergencystretch}{2em}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{openendedquestion}{Open-ended Question}
\newtheorem{conjecture}{Conjecture}
\newcommand{\coreref}[1]{\ref{#1}}

\begin{document}

\sloppy

\section*{stat\_\allowbreak{}blind\_\allowbreak{}replicate\_\allowbreak{}adrf\_\allowbreak{}protectedcontrast\_\allowbreak{}frontier}

\noindent\textit{Specialization:} v1\par

\paragraph{TL;DR.} The observed law supplies an unmarked probability channel and an outcome-marked signed channel. Continuous integrated control on the moment-protected square, quantitative analytic continuation, and Sobolev recovery yield a theoretical exact-profile simultaneous ADRF band with minimax expected maximal width \(r_n\asymp(\log\log n/\log n)^{\beta-1/2}\) for every fixed \(\beta>1/2\). Compactness and measurable-extremum arguments make the exact-profile endpoints well defined. A fixed-density Bernoulli marked experiment based on a weighted Jacobi packet and fixed asymmetric sinc-fourth-mixture errors supplies the matching lower bound.

\section{Project justification}

\paragraph{Gap.} Existing error-free ADRF work gives uniform inference, known-error methods give pointwise ADRF inference, and blind repeated-measurement work treats an unmarked density. None combines an honest whole-curve ADRF band, a sharp expected-width modulus, and a fixed-nuisance marked converse when two replicate errors are unknown, nonidentical, asymmetric, and allowed spectral zeros.

\paragraph{Niche.} Compact dose support makes both latent transforms entire, while the common error second-moment radius supplies a uniformly nonvanishing protected neighborhood. The inferential object is a paired nonlinear quotient, and the band is formed by measurable extrema over the exact compact continuous-channel profile.

\paragraph{Fill.} The project proves the paired protected inverse at exponent \(\beta-1/2\), embeds it in a continuously constrained exact confidence profile, and bounds the profile diameter at the matching rate. A Bernoulli marked weighted-Jacobi construction with fixed asymmetric sinc-fourth-mixture errors leaves the unmarked replicate law fixed and matches the upper rate.

\section{Related work}

Colangelo and Lee (2025), especially Supplementary Theorems S11 and S12, are the closest uniform-in-treatment ADRF comparator, but treatment is observed without error. Huang and Zhang (2023) derive pointwise ADRF inference with a known error transform. Gao, Bondell, Huang, and Gong (2024) estimate a latent-treatment ADRF through a variational neural model without a blind-replicate honest-band theorem. Capitao-Miniconi, Gassiat, and Lehericy (2025) provide the closest blind-replicate method exemplar through a continuous integrated analytic-polynomial contrast and compact-support density rates; the present lower experiment instead uses a weighted Jacobi reproducing-kernel packet in the Bernoulli marked channel together with two fixed nonidentical asymmetric mixtures of shifted and scaled sinc-fourth densities. Kato, Sasaki, and Ura (2021) invert complex Kotlarski moment tests for an unmarked density. Kato and Sasaki (2018, 2019) study density and errors-in-variables regression bands under auxiliary, symmetric, or conventional spectral information, while Neumann (2007) motivates integrated characteristic-function coordinates. Lounici and Nickl (2011), Genovese and Wasserman (2008), and Low (1997) inform sup-norm recovery and confidence-band lower reductions. These are informed-by comparisons; the separate error-free reduction is the only equivalence claim.

\section{Setup}

Target (estimand / structural parameter): \(\mu(\cdot)=E_P[Y(\cdot)]\in C(I)\).
Primary functional / estimator / diagnostic: \(\mu(a)=\sum_{x\in\mathcal X}p_xq_x(a)/f_x(a),\quad a\in I\).
\begin{itemize}
  \item \(n\) --- \texttt{positive integer}; \(\mathbb N\); \texttt{sample size}
  \item \(\alpha\) --- \texttt{scalar}; \((0,1/4)\); \texttt{miscoverage level}
  \item \(\eta\) --- \texttt{scalar}; \((0,1-2\alpha)\); \texttt{testing-\allowbreak{}affinity constant}
  \item \(\mathcal X\) --- \texttt{finite set}; \texttt{covariate-\allowbreak{}stratum space}
  \item \(p_{\min}\) --- \texttt{positive scalar}; \((0,|\mathcal X|^{-1})\); \texttt{stratum-\allowbreak{}probability lower bound}
  \item \(B_A\) --- \texttt{positive scalar}; \texttt{dose-\allowbreak{}support radius}
  \item \(\mathcal A\) --- \texttt{compact interval}; \texttt{latent-\allowbreak{}dose support}
  \par\smallskip\noindent\emph{Definition.} \(\mathcal A=[-B_A,B_A]\)
  \item \(I\) --- \texttt{nondegenerate compact interval}; \(I\subset\operatorname{int}(\mathcal A)\); \texttt{band target interval}
  \item \(\beta\) --- \texttt{smoothness index}; \((1/2,\infty)\); \texttt{Sobolev regularity}
  \item \(f^{\circ}\) --- \texttt{function}; \(H^{\beta}(\mathbb R)\); \texttt{strict-\allowbreak{}interior density scaffold for the lower experiment}
  \par\smallskip\noindent\emph{Definition.} \(f^{\circ}(a)=\dfrac{\exp\{-1/(B_A^2-a^2)\}\mathbf 1\{|a|<B_A\}}{\int_{-B_A}^{B_A}\exp\{-1/(B_A^2-u^2)\}\,du}\)
  \item \(\kappa\) --- \texttt{positive scalar}; \((0,\inf_{a\in I}f^{\circ}(a))\); \texttt{dose-\allowbreak{}density lower bound}
  \item \(R_f\) --- \texttt{positive scalar}; \((\|f^{\circ}\|_{H^{\beta}},\infty)\); \texttt{density Sobolev radius}
  \item \(R_q\) --- \texttt{positive scalar}; \((\|f^{\circ}\|_{H^{\beta}}/2,\infty)\); \texttt{marked-\allowbreak{}density Sobolev radius}
  \item \(B_Y\) --- \texttt{positive scalar}; \([1,\infty)\); \texttt{outcome bound}
  \item \(M_e\) --- \texttt{positive scalar}; \texttt{replicate-\allowbreak{}error second-\allowbreak{}moment radius}
  \item \(\nu_*\) --- \texttt{positive scalar}; \texttt{moment-\allowbreak{}derived protected frequency radius}
  \par\smallskip\noindent\emph{Definition.} \(\nu_*=(2\sqrt{M_e})^{-1}\)
  \item \(\mathcal C_*\) --- \texttt{compact square}; \texttt{continuous protected frequency cube}
  \par\smallskip\noindent\emph{Definition.} \(\mathcal C_*=[-\nu_*,\nu_*]^2\)
  \item \(A\) --- \texttt{random variable}; \(\mathbb R\); \texttt{latent continuous treatment}
  \item \(X\) --- \texttt{random variable}; \(\mathcal X\); \texttt{observed covariate stratum}
  \item \(Y(\cdot)\) --- \texttt{potential-\allowbreak{}outcome process}; \(\mathcal A\to\mathbb R\); \texttt{causal response schedule}
  \item \(Y\) --- \texttt{random variable}; \(\mathbb R\); \texttt{observed outcome}
  \item \(U_1\) --- \texttt{random variable}; \(\mathbb R\); \texttt{first replicate error}
  \item \(U_2\) --- \texttt{random variable}; \(\mathbb R\); \texttt{second replicate error}
  \item \(S_1\) --- \texttt{random variable}; \texttt{first observed treatment replicate}
  \par\smallskip\noindent\emph{Definition.} \(S_1=A+U_1\)
  \item \(S_2\) --- \texttt{random variable}; \texttt{second observed treatment replicate}
  \par\smallskip\noindent\emph{Definition.} \(S_2=A+U_2\)
  \item \(O\) --- \texttt{random vector}; \texttt{observed unit}
  \par\smallskip\noindent\emph{Definition.} \(O=(Y,X,S_1,S_2)\)
  \item \(P\) --- \texttt{probability law}; laws of \(A,X,Y(\cdot),Y,U_1,U_2,S_1,S_2\); \texttt{full-\allowbreak{}data law}
  \item \(Q\) --- \texttt{probability law}; laws of \(A,X,Y(\cdot),Y,U_1,U_2,S_1,S_2\); \texttt{candidate full-\allowbreak{}data law}
  \item \(P_{\mathrm{obs}}\) --- \texttt{probability law}; \texttt{observed-\allowbreak{}data law}
  \par\smallskip\noindent\emph{Definition.} law of \(O\) induced by \(P\)
  \item \(p_x\) --- \texttt{scalar}; \texttt{stratum mass}
  \par\smallskip\noindent\emph{Definition.} \(p_x=P(X=x)\)
  \item \(f_x\) --- \texttt{function}; \(H^{\beta}(\mathbb R)\); \texttt{latent-\allowbreak{}dose density}
  \par\smallskip\noindent\emph{Definition.} conditional density of \(A\) given \(X=x\)
  \item \(m_x\) --- \texttt{function}; \texttt{latent outcome regression}
  \par\smallskip\noindent\emph{Definition.} \(m_x(a)=E_P[Y\mid A=a,X=x]\)
  \item \(q_x\) --- \texttt{function}; \(H^{\beta}(\mathbb R)\); \texttt{signed marked density}
  \par\smallskip\noindent\emph{Definition.} \(q_x(a)=m_x(a)f_x(a)\)
  \item \(\varphi_1\) --- \texttt{function}; \texttt{first error characteristic function}
  \par\smallskip\noindent\emph{Definition.} \(\varphi_1(t)=E_P[e^{itU_1}]\)
  \item \(\varphi_2\) --- \texttt{function}; \texttt{second error characteristic function}
  \par\smallskip\noindent\emph{Definition.} \(\varphi_2(t)=E_P[e^{itU_2}]\)
  \item \(\varphi_{f,x}\) --- \texttt{entire function}; \texttt{latent-\allowbreak{}density transform}
  \par\smallskip\noindent\emph{Definition.} \(\varphi_{f,x}(z)=\int_{\mathcal A}e^{iza}f_x(a)\,da\)
  \item \(\varphi_{q,x}\) --- \texttt{entire function}; \texttt{marked-\allowbreak{}channel transform}
  \par\smallskip\noindent\emph{Definition.} \(\varphi_{q,x}(z)=\int_{\mathcal A}e^{iza}q_x(a)\,da\)
  \item \(F_x\) --- \texttt{function}; \texttt{observable probability channel}
  \par\smallskip\noindent\emph{Definition.} \(F_x(s,t)=E_{P_{\mathrm{obs}}}[\mathbf 1\{X=x\}e^{i(sS_1+tS_2)}]\)
  \item \(G_x\) --- \texttt{function}; \texttt{observable signed marked channel}
  \par\smallskip\noindent\emph{Definition.} \(G_x(s,t)=E_{P_{\mathrm{obs}}}[\mathbf 1\{X=x\}Ye^{i(sS_1+tS_2)}]\)
  \item \(\mu\) --- \texttt{function}; \texttt{average dose-\allowbreak{}response function}
  \par\smallskip\noindent\emph{Definition.} \(\mu(a)=E_P[Y(a)]\)
  \item \(\mathcal M\) --- \texttt{class of full-\allowbreak{}data laws}; \texttt{blind-\allowbreak{}replicate ADRF model class}
  \item \(p_{x,Q}\) --- \texttt{scalar}; \texttt{candidate-\allowbreak{}law stratum mass}
  \par\smallskip\noindent\emph{Definition.} \(p_{x,Q}=Q(X=x)\)
  \item \(f_{x,Q}\) --- \texttt{function}; \texttt{candidate-\allowbreak{}law latent-\allowbreak{}dose density}
  \par\smallskip\noindent\emph{Definition.} conditional density of \(A\) given \(X=x\) under \(Q\)
  \item \(q_{x,Q}\) --- \texttt{function}; \texttt{candidate-\allowbreak{}law signed marked density}
  \par\smallskip\noindent\emph{Definition.} \(q_{x,Q}(a)=E_Q[Y\mid A=a,X=x]f_{x,Q}(a)\)
  \item \(F_{x,Q}\) --- \texttt{function}; \texttt{candidate-\allowbreak{}law probability channel}
  \par\smallskip\noindent\emph{Definition.} \(F_{x,Q}(s,t)=E_Q[\mathbf 1\{X=x\}e^{i(sS_1+tS_2)}]\)
  \item \(G_{x,Q}\) --- \texttt{function}; \texttt{candidate-\allowbreak{}law signed marked channel}
  \par\smallskip\noindent\emph{Definition.} \(G_{x,Q}(s,t)=E_Q[\mathbf 1\{X=x\}Ye^{i(sS_1+tS_2)}]\)
  \item \(\mu_Q\) --- \texttt{function}; \texttt{candidate-\allowbreak{}law ADRF}
  \par\smallskip\noindent\emph{Definition.} \(\mu_Q(a)=\sum_{x\in\mathcal X}p_{x,Q}q_{x,Q}(a)/f_{x,Q}(a)\)
  \item \(\widehat F_{x,n}\) --- \texttt{random function}; \texttt{empirical probability channel}
  \par\smallskip\noindent\emph{Definition.} \(\widehat F_{x,n}(s,t)=n^{-1}\sum_{i=1}^n\mathbf 1\{X_i=x\}e^{i(sS_{i1}+tS_{i2})}\)
  \item \(\widehat G_{x,n}\) --- \texttt{random function}; \texttt{empirical signed marked channel}
  \par\smallskip\noindent\emph{Definition.} \(\widehat G_{x,n}(s,t)=n^{-1}\sum_{i=1}^n\mathbf 1\{X_i=x\}Y_i e^{i(sS_{i1}+tS_{i2})}\)
  \item \(\widehat p_x\) --- \texttt{random scalar}; \texttt{empirical stratum mass}
  \par\smallskip\noindent\emph{Definition.} \(\widehat p_x=n^{-1}\sum_{i=1}^n\mathbf 1\{X_i=x\}\)
  \item \(C_{\alpha}\) --- \texttt{positive scalar}; \texttt{uniform continuous-\allowbreak{}channel concentration constant}
  \item \(\epsilon_n\) --- \texttt{positive scalar}; \texttt{continuous-\allowbreak{}channel confidence radius}
  \par\smallskip\noindent\emph{Definition.} \(\epsilon_n=C_{\alpha}\sqrt{\log(en/\alpha)/n}\)
  \item \(c_K\) --- \texttt{positive scalar}; \texttt{fixed analytic-\allowbreak{}continuation tuning constant}
  \item \(K_n\) --- \texttt{positive integer}; \texttt{analytic continuation order}
  \par\smallskip\noindent\emph{Definition.} \(K_n=\max\{1,\lfloor c_K\log(en)/\log\log(e^e n)\rfloor\}\)
  \item \(r_n\) --- \texttt{positive scalar}; \texttt{candidate sharp maximal-\allowbreak{}width rate}
  \par\smallskip\noindent\emph{Definition.} \(r_n=K_n^{1/2-\beta}\)
  \item \(\mathscr D_n\) --- \texttt{functional}; \texttt{continuous integrated paired-\allowbreak{}channel discrepancy}
  \item \(\Theta_{n,\alpha}\) --- \texttt{random set}; \texttt{exact continuous outer profile}
  \item \(L_n\) --- \texttt{random function}; \(I\to[-B_Y,B_Y]\); \texttt{lower band endpoint}
  \item \(U_n\) --- \texttt{random function}; \(I\to[-B_Y,B_Y]\); \texttt{upper band endpoint}
  \item \(W_n\) --- \texttt{nonnegative random scalar}; \texttt{maximal band width}
  \par\smallskip\noindent\emph{Definition.} \(W_n=\sup_{a\in I}\{U_n(a)-L_n(a)\}\)
  \item \(\mathfrak B_{n,\alpha}\) --- \texttt{class of procedures}; \texttt{uniformly honest simultaneous ADRF bands}
  \item \(\mathfrak R_n\) --- \texttt{nonnegative scalar}; \texttt{minimax expected maximal width}
  \par\smallskip\noindent\emph{Definition.} \(\mathfrak R_n=\inf_{(L_n,U_n)\in\mathfrak B_{n,\alpha}}\sup_{P\in\mathcal M}E_{P_{\mathrm{obs}}^{\otimes n}}[W_n]\)
  \item \(P_{0,n}\) --- \texttt{sequence of full-\allowbreak{}data laws}; \texttt{first fixed-\allowbreak{}nuisance marked alternative derived by Theorem 7}
  \item \(P_{1,n}\) --- \texttt{sequence of full-\allowbreak{}data laws}; \texttt{second fixed-\allowbreak{}nuisance marked alternative derived by Theorem 7}
  \item \(\mathfrak H^{\mathrm{up}}\) --- \texttt{method handle}; \texttt{protected-\allowbreak{}contrast and certified-\allowbreak{}profile route}
  \item \(\mathfrak H^{\mathrm{low}}\) --- \texttt{method handle}; \texttt{fixed-\allowbreak{}density Bernoulli marked-\allowbreak{}packet route}
\end{itemize}

\section{Assumptions}

\begin{assumption}[ass:iid]\label{ass:iid}
\(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P_{\mathrm{obs}}\).
\par\smallskip\noindent\emph{Standard} (i.i.d. superpopulation sampling, \cite{HuangZhang2023}).
\end{assumption}

\begin{assumption}[ass:strata-positivity]\label{ass:strata-positivity}
\(\min_{x\in\mathcal X}p_x\ge p_{\min}\).
\par\smallskip\noindent\emph{Standard} (uniform finite-stratum positivity, \cite{HuangZhang2023}).
\end{assumption}

\begin{assumption}[ass:consistency]\label{ass:consistency}
\(P\{Y=Y(A)\}=1\).
\par\smallskip\noindent\emph{Standard} (consistency, \cite{HuangZhang2023}).
\end{assumption}

\begin{assumption}[ass:unconfoundedness]\label{ass:unconfoundedness}
\(\forall a\in\mathcal A,\ A\perp Y(a)\mid X\).
\par\smallskip\noindent\emph{Standard} (weak unconfoundedness for continuous treatments, \cite{HuangZhang2023}).
\end{assumption}

\begin{assumption}[ass:causal-mean-continuity]\label{ass:causal-mean-continuity}
\(a\mapsto E_P[Y(a)\mid X=x]\in C(I)\) for every \(x\in\mathcal X\).
\par\smallskip\noindent\emph{Standard} (continuous conditional dose-response, \cite{KennedyMaMcHughSmall2017}).
\end{assumption}

\begin{assumption}[ass:error-latent-independence]\label{ass:error-latent-independence}
\((U_1,U_2)\perp(A,X,Y(\cdot),Y)\).
\par\smallskip\noindent\emph{Standard} (classical repeated-measurement independence, \cite{CapitaoMiniconiGassiatLehericy2025}).
\end{assumption}

\begin{assumption}[ass:error-coordinate-independence]\label{ass:error-coordinate-independence}
\(U_1\perp U_2\).
\par\smallskip\noindent\emph{Standard} (independent replicate errors, \cite{CapitaoMiniconiGassiatLehericy2025}).
\end{assumption}

\begin{assumption}[ass:center-second-error]\label{ass:center-second-error}
\(E_P[U_2]=0\).
\par\smallskip\noindent\emph{Standard} (Kotlarski location normalization, \cite{KatoSasakiUra2021}).
\end{assumption}

\begin{assumption}[ass:error-second-moment]\label{ass:error-second-moment}
\(\max_{j\in\{1,2\}}E_P[U_j^2]\le M_e\).
\par\smallskip\noindent\emph{Standard} (uniform replicate-error second-moment bound, \cite{CapitaoMiniconiGassiatLehericy2025}).
\end{assumption}

\begin{assumption}[ass:compact-dose]\label{ass:compact-dose}
\(P(A\in\mathcal A)=1\).
\par\smallskip\noindent\emph{Standard} (compact latent-signal support, \cite{CapitaoMiniconiGassiatLehericy2025}).
\end{assumption}

\begin{assumption}[ass:bounded-outcome]\label{ass:bounded-outcome}
\(P\{\sup_{a\in\mathcal A}|Y(a)|\le B_Y\}=1\).
\par\smallskip\noindent\emph{Standard} (uniformly bounded potential outcomes, \cite{HuangZhang2023}).
\end{assumption}

\begin{assumption}[ass:density-sobolev]\label{ass:density-sobolev}
\(\sup_{x\in\mathcal X}\|f_x\|_{H^{\beta}(\mathbb R)}\le R_f\).
\par\smallskip\noindent\emph{Standard} (Sobolev density ball, \cite{CapitaoMiniconiGassiatLehericy2025}).
\end{assumption}

\begin{assumption}[ass:marked-sobolev]\label{ass:marked-sobolev}
\(\sup_{x\in\mathcal X}\|q_x\|_{H^{\beta}(\mathbb R)}\le R_q\).
\par\smallskip\noindent\emph{Standard} (Sobolev regression-numerator ball, \cite{KatoSasaki2019}).
\end{assumption}

\begin{assumption}[ass:dose-overlap]\label{ass:dose-overlap}
\(\inf_{x\in\mathcal X}\inf_{a\in I}f_x(a)\ge\kappa\).
\par\smallskip\noindent\emph{Standard} (continuous-treatment overlap on an interior interval, \cite{HuangZhang2023}).
\end{assumption}

\section{Definitions}

\begin{definition}[def:model-class]\label{def:model-class}
\par\smallskip\noindent\emph{Defined object.} \(\mathcal M\)
\par\smallskip\noindent\emph{Construction.} \(\mathcal M=\{P:P\text{ satisfies every condition indexed by }\mathrm{by\_member\_properties}\}\).
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}strata-\allowbreak{}positivity}; \texttt{ass:\allowbreak{}consistency}; \texttt{ass:\allowbreak{}unconfoundedness}; \texttt{ass:\allowbreak{}causal-\allowbreak{}mean-\allowbreak{}continuity}; \texttt{ass:\allowbreak{}error-\allowbreak{}latent-\allowbreak{}independence}; \texttt{ass:\allowbreak{}error-\allowbreak{}coordinate-\allowbreak{}independence}; \texttt{ass:\allowbreak{}center-\allowbreak{}second-\allowbreak{}error}; \texttt{ass:\allowbreak{}error-\allowbreak{}second-\allowbreak{}moment}; \texttt{ass:\allowbreak{}compact-\allowbreak{}dose}; \texttt{ass:\allowbreak{}bounded-\allowbreak{}outcome}; \texttt{ass:\allowbreak{}density-\allowbreak{}sobolev}; \texttt{ass:\allowbreak{}marked-\allowbreak{}sobolev}; \texttt{ass:\allowbreak{}dose-\allowbreak{}overlap}.
\end{definition}

\begin{definition}[def:observable-channels]\label{def:observable-channels}
\par\smallskip\noindent\emph{Defined object.} \texttt{probability and signed marked channels}
\par\smallskip\noindent\emph{Construction.} \(F_x(s,t)=E[\mathbf 1\{X=x\}e^{i(sS_1+tS_2)}]\) and \(G_x(s,t)=E[\mathbf 1\{X=x\}Ye^{i(sS_1+tS_2)}]\).
\par\smallskip\noindent\emph{Inputs.} \(P_{\mathrm{obs}}\).
\end{definition}

\begin{definition}[def:adrf-functional]\label{def:adrf-functional}
\par\smallskip\noindent\emph{Defined object.} \texttt{ADRF identifying functional}
\par\smallskip\noindent\emph{Construction.} \(\mu(a)=\sum_{x\in\mathcal X}p_xq_x(a)/f_x(a)\), \(a\in I\).
\par\smallskip\noindent\emph{Inputs.} \(p_x\); \(q_x\); \(f_x\).
\end{definition}

\begin{definition}[def:continuous-protected-profile]\label{def:continuous-protected-profile}
\par\smallskip\noindent\emph{Defined object.} \(\mathscr D_n\) and \(\Theta_{n,\alpha}\)
\par\smallskip\noindent\emph{Construction.} For a candidate law \(Q\in\mathcal M\), let \(F_{x,Q}\), \(G_{x,Q}\), and \(p_{x,Q}\) denote its channels and stratum masses, and set \(\mathscr D_n(Q)=\sum_{x\in\mathcal X}\{\int_{\mathcal C_*}(|\widehat F_{x,n}-F_{x,Q}|^2+B_Y^{-2}|\widehat G_{x,n}-G_{x,Q}|^2)\,ds\,dt+|\widehat p_x-p_{x,Q}|^2\}\) and \(\Theta_{n,\alpha}=\{Q\in\mathcal M:\mathscr D_n(Q)\le\epsilon_n^2\}\).
\par\smallskip\noindent\emph{Inputs.} \(O\); \(n\); \(\alpha\).
\end{definition}

\begin{definition}[def:profiled-band]\label{def:profiled-band}
\par\smallskip\noindent\emph{Defined object.} \(L_n\) and \(U_n\)
\par\smallskip\noindent\emph{Construction.} If \(\Theta_{n,\alpha}\neq\varnothing\), set
\[
L_n(a)=\max\left\{-B_Y,\inf_{Q\in\Theta_{n,\alpha}}\mu_Q(a)\right\},
\qquad
U_n(a)=\min\left\{B_Y,\sup_{Q\in\Theta_{n,\alpha}}\mu_Q(a)\right\},
\qquad a\in I.
\]
If \(\Theta_{n,\alpha}=\varnothing\), set \(L_n(a)=U_n(a)=0\) for every \(a\in I\).
\par\smallskip\noindent\emph{Inputs.} \(\Theta_{n,\alpha}\).
\end{definition}

\begin{definition}[def:honest-band-class]\label{def:honest-band-class}
\par\smallskip\noindent\emph{Defined object.} \(\mathfrak B_{n,\alpha}\)
\par\smallskip\noindent\emph{Construction.} \(\mathfrak B_{n,\alpha}=\{(L_n,U_n):L_n,U_n\text{ are jointly measurable},\ L_n\le U_n,\ \inf_{P\in\mathcal M}P_{\mathrm{obs}}^{\otimes n}(L_n(a)\le\mu(a)\le U_n(a)\text{ for all }a\in I)\ge1-\alpha\}\).
\par\smallskip\noindent\emph{Member properties.} \texttt{ass:\allowbreak{}iid}.
\end{definition}

\begin{definition}[def:upper-handle]\label{def:upper-handle}
\par\smallskip\noindent\emph{Defined object.} \(\mathfrak H^{\mathrm{up}}\)
\par\smallskip\noindent\emph{Construction.} Use the continuous integrated probability and signed marked contrasts on \(\mathcal C_*\), derive their nearly parametric forward radius, pass both compactly supported entire transforms through the quantitative analytic-continuation argument at order \(K_n\), recover the protected quotient \(q_x/f_x\) on \(I\), and take measurable extrema over the exact compact profile \(\Theta_{n,\alpha}\).
\par\smallskip\noindent\emph{Inputs.} \(\widehat F_{x,n}\); \(\widehat G_{x,n}\); \(\widehat p_x\).
\end{definition}

\begin{definition}[def:lower-handle]\label{def:lower-handle}
\par\smallskip\noindent\emph{Defined object.} \(\mathfrak H^{\mathrm{low}}\)
\par\smallskip\noindent\emph{Construction.} Freeze \(p_x\), \(f_x=f^{\circ}\), and two fixed nonidentical asymmetric error densities formed as distinct mixtures of shifted and scaled \(\operatorname{sinc}^4\) densities.  Put a compactly supported weighted Jacobi reproducing-kernel, moment-annihilating packet into a Bernoulli conditional mark, while leaving the latent density and both error laws fixed.  Choose its amplitude to preserve strict \(H^\beta\) slack and the Bernoulli range, use its factorial attenuation in the observed signed marked channel, and finish with the honest-band testing reduction.
\par\smallskip\noindent\emph{Inputs.} \(K_n\); \(r_n\).
\end{definition}

\section{Main results}

\begin{lemma}[lem:paired-channel-factorization]\label{lem:paired-channel-factorization}
For every \(P\in\mathcal M\), \(x\in\mathcal X\), and \((s,t)\in\mathbb R^2\), \(F_x(s,t)=p_x\varphi_{f,x}(s+t)\varphi_1(s)\varphi_2(t)\) and \(G_x(s,t)=p_x\varphi_{q,x}(s+t)\varphi_1(s)\varphi_2(t)\); moreover \(\inf_{|u|\le\nu_*}|\varphi_j(u)|\ge1/2\) for \(j\in\{1,2\}\).
\end{lemma}
\begin{proof}
Fix \(P\in\mathcal M\), \(x\in\mathcal X\), and \((s,t)\in\mathbb R^2\).  Membership in \(\mathcal M\), error--latent independence, coordinate independence, and \(S_j=A+U_j\) give
\[
\begin{aligned}
F_x(s,t)
&=E_P\!\left[\mathbf 1\{X=x\}e^{i(s+t)A}
 E_P(e^{isU_1})E_P(e^{itU_2})\right]\\
&=p_x\varphi_{f,x}(s+t)\varphi_1(s)\varphi_2(t).
\end{aligned}
\]
The model-class consistency and bounded-potential-outcome conditions imply \(|Y|\le B_Y\) almost surely, so the marked expectation is integrable.  The same conditioning, retaining the mark, yields
\[
\begin{aligned}
G_x(s,t)
&=E_P\!\left[\mathbf 1\{X=x\}Y e^{i(s+t)A}\right]
  \varphi_1(s)\varphi_2(t)\\
&=p_x\left\{\int_{\mathcal A}e^{i(s+t)a}m_x(a)f_x(a)\,da\right\}
  \varphi_1(s)\varphi_2(t)\\
&=p_x\varphi_{q,x}(s+t)\varphi_1(s)\varphi_2(t),
\end{aligned}
\]
where the second equality uses the definitions of \(m_x\) and \(f_x\), and the last uses \(q_x=m_xf_x\).

For protection, \(|e^{iv}-1|\le |v|\), Cauchy--Schwarz, and the error second-moment condition imply, for \(|u|\le\nu_*=(2\sqrt{M_e})^{-1}\),
\[
\begin{aligned}
|\varphi_j(u)|
&\ge 1-E_P|e^{iuU_j}-1|\\
&\ge 1-|u|E_P|U_j|\\
&\ge 1-|u|\{E_P(U_j^2)\}^{1/2}
\ge \frac12,
\end{aligned}
\qquad j\in\{1,2\}.
\]
This proves both channel factorizations and the protected-frequency lower bound.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The common nuisance factor makes the probability and marked channels comparable on one protected cube, while the moment bound derives protection rather than assuming it. \textit{Gap.} Capitao-Miniconi, Gassiat, and Lehéricy derive the unmarked factorization; the signed outcome-marked factorization and its joint use for a causal quotient are not part of their result. \textit{Consumer.} The paired factorization is the input to the sharp inverse modulus and the exact continuous-profile restrictions.

\begin{theorem}[thm:adrf-identification]\label{thm:adrf-identification}
The observed law \(P_{\mathrm{obs}}\) identifies \(p_x\), \(f_x\), and \(q_x\) for every \(x\in\mathcal X\), and for every \(a\in I\) it identifies the causal curve by \(\mu(a)=\sum_{x\in\mathcal X}p_xq_x(a)/f_x(a)\).
\end{theorem}
\begin{proof}
The observed law gives \(p_x=P_{\mathrm{obs}}(X=x)\).  Fix \(x\in\mathcal X\) and put
\[
\rho=\min\{\nu_*/4,(8B_A)^{-1}\}>0.
\]
For \(|s|,|t|\le2\rho\), compact dose support and \(|e^{iv}-1|\le |v|\) imply
\[
|\varphi_{f,x}(s+t)|
\ge 1-E_P|e^{i(s+t)A}-1|
\ge 1-B_A|s+t|\ge\frac12.
\]
The paired factorization, stratum positivity, and the protected-frequency bound therefore give
\[
|F_x(s,t)|\ge p_{\min}/8,
\qquad |s|,|t|\le2\rho. \tag{1}
\]

The second-moment condition implies \(E_P|U_2|<\infty\), and compact dose support implies \(E_P|A|<\infty\), so differentiation under the expectation is justified by domination.  Differentiating the factorization with respect to \(t\) at \((s,0)\), using \(\varphi_2(0)=1\) and the centering condition \(\varphi_2'(0)=iE_PU_2=0\), gives
\[
\frac{\partial_tF_x(s,0)}{F_x(s,0)}
=\frac{\varphi_{f,x}'(s)}{\varphi_{f,x}(s)},
\qquad |s|\le2\rho. \tag{2}
\]
The scalar differential equation in (2), with \(\varphi_{f,x}(0)=1\), has the unique solution
\[
\varphi_{f,x}(s)=
\exp\!\left\{\int_0^s
\frac{\partial_tF_x(u,0)}{F_x(u,0)}\,du\right\},
\qquad |s|\le2\rho. \tag{3}
\]
Thus the observed law identifies \(\varphi_{f,x}\) on a nondegenerate interval.  Because \(f_x\) is integrable and supported on \(\mathcal A\), differentiation under its defining integral at every complex argument shows that \(\varphi_{f,x}\) is entire.  If two candidate transforms agree in (3), their difference is entire and vanishes on a real interval.  Expanding at an interior point and repeatedly taking difference quotients shows that every Taylor coefficient is zero; hence the transforms agree on \(\mathbb C\).  Finally, because \(f_x\in H^\beta\) and \(\beta>1/2\), Cauchy--Schwarz gives
\[
\int_{\mathbb R}|\varphi_{f,x}(u)|\,du
\le
\left\{\int_{\mathbb R}(1+u^2)^{-\beta}\,du\right\}^{1/2}
\|f_x\|_{H^\beta}<\infty. \tag{4}
\]
Convolution with Gaussian approximate identities and passage to the limit in (4) gives the pointwise Fourier inversion formula.  Consequently the continuous version of \(f_x\) is identified on \(\mathbb R\).

On the same interval, (1) permits cancellation of the common nuisance factor:
\[
\frac{G_x(s,0)}{F_x(s,0)}
=\frac{\varphi_{q,x}(s)}{\varphi_{f,x}(s)}. \tag{5}
\]
Equations (3)--(5) identify \(\varphi_{q,x}\) locally.  The model-class outcome bound gives \(|q_x|\le B_Yf_x\), so \(q_x\) is integrable and supported on \(\mathcal A\).  Its transform is therefore entire.  The same analytic identity argument, followed by the Fourier inversion argument using \(q_x\in H^\beta\), identifies the pointwise continuous version of \(q_x\).

It remains to relate this observed-law functional to the causal target.  Consistency and conditional unconfoundedness give, for almost every dose under the conditional density,
\[
E_P\{Y(a)\mid X=x\}
=E_P(Y\mid A=a,X=x)
=m_x(a)=\frac{q_x(a)}{f_x(a)}. \tag{6}
\]
Division on \(I\) is valid because dose overlap gives \(f_x(a)\ge\kappa>0\).  Both \(f_x\) and \(q_x\) are continuous by (4), so the quotient in (6) is continuous on \(I\); the causal-mean-continuity condition supplies a continuous version of the left side.  Since \(f_x\ge\kappa\), an equality holding for conditional-dose almost every \(a\in I\) holds for Lebesgue almost every \(a\in I\), and equality of the two continuous versions extends it to every \(a\in I\).  Averaging over the finite strata now proves
\[
\mu(a)=E_PY(a)
=\sum_{x\in\mathcal X}p_xE_P\{Y(a)\mid X=x\}
=\sum_{x\in\mathcal X}p_x\frac{q_x(a)}{f_x(a)},
\qquad a\in I.
\]
Every quantity on the right has already been recovered from \(P_{\mathrm{obs}}\), which proves identification without confusing the causal curve with its identifying functional.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The theorem separates the qualitative causal identifying step from the quantitative band-width problem. \textit{Gap.} Huang and Zhang identify the ADRF with a known error transform, while Capitao-Miniconi, Gassiat, and Lehéricy identify only an unmarked latent signal under blind replicates. \textit{Consumer.} Applied analyses with replicate dietary or exposure measurements obtain a whole-curve causal target before inference is imposed.

\begin{proposition}[prop:continuous-channel-concentration]\label{prop:continuous-channel-concentration}
There is a constant \(C_{\alpha}\) depending only on the fixed class constants such that, uniformly over \(P\in\mathcal M\), \(P_{\mathrm{obs}}^{\otimes n}(\mathscr D_n(P)\le\epsilon_n^2)\ge1-\alpha\) for all sufficiently large \(n\).
\end{proposition}
\begin{proof}
For the true law \(P\), each empirical channel is an average of independent complex variables.  At every \((s,t)\),
\[
 E|\widehat F_{x,n}(s,t)-F_x(s,t)|^2
 =\frac1n\operatorname{Var}\{\mathbf 1(X=x)e^{i(sS_1+tS_2)}\}
 \le\frac{p_x}{n}.
\]
Consistency and bounded potential outcomes give \(|Y|\le B_Y\) almost surely, and hence
\[
 B_Y^{-2}E|\widehat G_{x,n}(s,t)-G_x(s,t)|^2\le\frac{p_x}{n}.
\]
Likewise, \(E|\widehat p_x-p_x|^2\le p_x/n\).  Tonelli's theorem is applicable because every integrand is nonnegative.  Since \(|\mathcal C_*|=4\nu_*^2\) and \(\sum_xp_x=1\), summing and integrating gives the uniform bound
\[
 \sup_{P\in\mathcal M}E_{P_{\mathrm{obs}}^{\otimes n}}\mathscr D_n(P)
 \le\frac{8\nu_*^2+1}{n}. \tag{22}
\]
Markov's inequality and the definition of \(\epsilon_n\) now yield
\[
 \sup_{P\in\mathcal M}P_{\mathrm{obs}}^{\otimes n}
 \{\mathscr D_n(P)>\epsilon_n^2\}
 \le\frac{8\nu_*^2+1}{C_\alpha^2\log(en/\alpha)}.
\]
Choose a fixed \(C_\alpha>0\).  The last expression is at most \(\alpha\) for all sufficiently large \(n\), proving the proposition.  The second-moment condition is used to make \(\nu_*\) a fixed positive radius; no frequency grid is invoked.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Continuous integrated control is the stochastic replacement for the refuted empirical-frequency grid. \textit{Gap.} Kato, Sasaki, and Ura control finitely many self-normalized complex moments; the present claim controls both marked and unmarked channels continuously over a fixed square. \textit{Consumer.} The result supplies a coverage-valid continuous feasible profile without assuming between-grid transform behavior.

\begin{theorem}[thm:honest-upper-band]\label{thm:honest-upper-band}
There exists \(C<\infty\) such that the exact profiled band \((L_n,U_n)\) belongs to \(\mathfrak B_{n,\alpha}\) and satisfies \(\sup_{P\in\mathcal M}E_{P_{\mathrm{obs}}^{\otimes n}}[W_n]\le C r_n\) for all sufficiently large \(n\).
\end{theorem}
\begin{proof}
For \(P\in\mathcal M\), let
\[
E_P=\{\mathscr D_n(P)\le\epsilon_n^2\}.
\]
The established continuous-channel concentration proposition, under the i.i.d. sampling condition, gives
\[
\inf_{P\in\mathcal M}P_{\mathrm{obs}}^{\otimes n}(E_P)\ge1-\alpha \tag{15}
\]
for all sufficiently large \(n\).  On \(E_P\), the true law belongs to the exact profile \(\Theta_{n,\alpha}\), so that profile is nonempty.  The model-class outcome and consistency conditions give \(|\mu_Q(a)|\le B_Y\) for every \(Q\in\mathcal M\) and \(a\in I\).  Therefore the clipping in the exact-profile definition cannot remove \(\mu_P\), and
\[
L_n(a)\le\mu_P(a)\le U_n(a)
\quad\text{for every }a\in I \quad\text{on }E_P. \tag{16}
\]
The exact-profile compactness and measurability lemma makes \(L_n\) and \(U_n\) jointly measurable.  If the profile is nonempty, its infimum is no larger than its supremum and both lie in \([-B_Y,B_Y]\); if it is empty, both endpoints are zero.  Thus \(L_n\le U_n\).  Equations (15)--(16) prove \((L_n,U_n)\in\mathfrak B_{n,\alpha}\).

For width, view \(\mathscr D_n(Q)^{1/2}\) as the norm of the difference between the empirical channel-and-mass vector and the corresponding vector under \(Q\) in the Hilbert direct sum specified by the definition of \(\mathscr D_n\).  If \(Q,R\in\Theta_{n,\alpha}\), the Hilbert-space triangle inequality gives
\[
\begin{aligned}
&\left[\sum_{x\in\mathcal X}\left\{
\int_{\mathcal C_*}\left(|F_{x,Q}-F_{x,R}|^2
+B_Y^{-2}|G_{x,Q}-G_{x,R}|^2\right)\,ds\,dt
+|p_{x,Q}-p_{x,R}|^2\right\}\right]^{1/2}\\
&\hspace{35mm}\le \mathscr D_n(Q)^{1/2}+\mathscr D_n(R)^{1/2}
\le2\epsilon_n. 
\end{aligned} \tag{17}
\]
The paired protected inverse theorem, applied with \(\delta=2\epsilon_n\), therefore yields
\[
\sup_{Q,R\in\Theta_{n,\alpha}}
\|\mu_Q-\mu_R\|_{L^\infty(I)}
\le C\left\{\frac{\log\log(1/\epsilon_n)}{\log(1/\epsilon_n)}\right\}^{\beta-1/2}. \tag{18}
\]
Since \(\epsilon_n=C_\alpha\sqrt{\log(en/\alpha)/n}\), fixed \(\alpha\) implies
\[
\log(1/\epsilon_n)\asymp\log n,
\qquad
\log\log(1/\epsilon_n)\asymp\log\log n. \tag{19}
\]
The definition of \(K_n\), with fixed \(c_K>0\), then turns (18)--(19) into
\[
\sup_{Q,R\in\Theta_{n,\alpha}}
\|\mu_Q-\mu_R\|_{L^\infty(I)}\le C K_n^{1/2-\beta}=Cr_n. \tag{20}
\]
For a nonempty compact profile, the endpoint lemma makes the extrema attainable; even without choosing optimizers, (20) directly bounds every pointwise diameter and hence gives \(W_n\le Cr_n\).  For an empty profile, \(W_n=0\).  Thus the bound holds for every realized sample, and consequently
\[
\sup_{P\in\mathcal M}E_{P_{\mathrm{obs}}^{\otimes n}}[W_n]\le Cr_n.
\]
This establishes the theoretical exact-profile upper band without any computational certification premise.
\end{proof}
\par\smallskip\noindent \textit{Justification.} This is the exact-profile achievability half of the simultaneous-band frontier. \textit{Gap.} Existing ADRF and deconvolution bands do not control the exact continuous-profile diameter under blind asymmetric replicates with spectral zeros. \textit{Consumer.} Studies with two nonidentical replicate measurements obtain a uniformly valid theoretical ADRF band with worst-case expected maximal width.

\begin{theorem}[thm:fixed-nuisance-marked-pair]\label{thm:fixed-nuisance-marked-pair}
There exist \(c>0\) and \(P_{0,n},P_{1,n}\in\mathcal M\) with the same \(p_x\), the same \(f_x=f^{\circ}\), and the same two fixed nonidentical asymmetric error laws, such that \(Y\mid(A=a,X=x)\) is Bernoulli under both laws, \(\|\mu_{1,n}-\mu_{0,n}\|_{L^\infty(I)}\ge c r_n\), and \(\|P_{1,n,\mathrm{obs}}^{\otimes n}-P_{0,n,\mathrm{obs}}^{\otimes n}\|_{\mathrm{TV}}\le\eta\) for all sufficiently large \(n\).
\end{theorem}
\begin{proof}
Apply the fixed-kernel Bernoulli packet lemma with
\[
k_n=\left\lceil\frac{\log(en)}{\log\log(e^e n)}\right\rceil
\]
and set \(P_{j,n}=P_{j,k_n}\).  That lemma constructs members of \(\mathcal M\), with common fixed stratum masses, common density \(f^\circ\), common fixed nonidentical asymmetric error laws, and conditionally Bernoulli marks.  Since \(c_K>0\) is fixed,
\[
k_n\asymp K_n,
\qquad
\|\mu_{1,n}-\mu_{0,n}\|_{L^\infty(I)}
\ge c k_n^{1/2-\beta}\ge c r_n. \tag{21}
\]

Write \(\chi_n^2=\chi^2(P_{1,n,\mathrm{obs}},P_{0,n,\mathrm{obs}})\).  Direct multiplication of likelihood ratios gives the product identity
\[
1+\chi^2(P_{1,n,\mathrm{obs}}^{\otimes n},P_{0,n,\mathrm{obs}}^{\otimes n})
=(1+\chi_n^2)^n. \tag{22}
\]
The packet lemma and \(m!\ge(m/e)^m\) imply
\[
n\chi_n^2
\le Cn\frac{C_0^{4k_n}}{\{(2k_n-4)!\}^2}\longrightarrow0, \tag{23}
\]
because the logarithm of the right side is at most
\[
\log n-4k_n\log k_n+O(k_n)\longrightarrow-\infty.
\]
Using \((1+x)^n\le e^{nx}\), equations (22)--(23) show that the product chi-square divergence tends to zero.  For any likelihood ratio \(Z\), Cauchy--Schwarz gives
\[
\|P-Q\|_{\mathrm{TV}}
=\frac12E_Q|Z-1|
\le\frac12\{E_Q(Z-1)^2\}^{1/2},
\]
and hence
\[
\|P_{1,n,\mathrm{obs}}^{\otimes n}-P_{0,n,\mathrm{obs}}^{\otimes n}\|_{\mathrm{TV}}
\le\frac12\left\{\chi^2(P_{1,n,\mathrm{obs}}^{\otimes n},P_{0,n,\mathrm{obs}}^{\otimes n})\right\}^{1/2}
\longrightarrow0. \tag{24}
\]
It is therefore at most the prescribed \(\eta>0\) for all sufficiently large \(n\).  Combining (21) and (24) proves the theorem.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Freezing the entire unmarked replicate law isolates ADRF uncertainty and prevents the lower bound from being driven by latent-density estimation. \textit{Gap.} The lower experiment of Capitao-Miniconi, Gassiat, and Lehéricy changes the latent density and separates in \(L^2\); Lounici and Nickl likewise perturb the deconvolved density rather than a bounded outcome mark. \textit{Consumer.} The construction certifies that no honest causal band can evade the logarithmic width by estimating the design density or error laws more accurately.

\begin{theorem}[thm:minimax-width-frontier]\label{thm:minimax-width-frontier}
There exist constants \(0<c<C<\infty\) such that \(c r_n\le\mathfrak R_n\le C r_n\) for all sufficiently large \(n\); equivalently, \(\mathfrak R_n\asymp\{\log\log n/\log n\}^{\beta-1/2}\).
\end{theorem}
\begin{proof}
The exact-profile upper-band theorem supplies one data-dependent element of \(\mathfrak B_{n,\alpha}\).  Therefore the definition of minimax expected maximal width gives
\[
\mathfrak R_n
\le\sup_{P\in\mathcal M}E_{P_{\mathrm{obs}}^{\otimes n}}[W_n]
\le Cr_n. \tag{25}
\]

For the converse, take an arbitrary \((L_n,U_n)\in\mathfrak B_{n,\alpha}\) and the two laws from the fixed-nuisance marked-pair theorem.  Define
\[
A_j=\{L_n(a)\le\mu_{j,n}(a)\le U_n(a)
\text{ for every }a\in I\},
\qquad j\in\{0,1\}.
\]
Uniform honesty gives
\[
P_{j,n,\mathrm{obs}}^{\otimes n}(A_j)\ge1-\alpha. \tag{26}
\]
The defining variational inequality for total variation gives
\[
P_{0,n,\mathrm{obs}}^{\otimes n}(A_1)
\ge P_{1,n,\mathrm{obs}}^{\otimes n}(A_1)
-\|P_{1,n,\mathrm{obs}}^{\otimes n}-P_{0,n,\mathrm{obs}}^{\otimes n}\|_{\mathrm{TV}}
\ge1-\alpha-\eta. \tag{27}
\]
The union bound and (26)--(27) yield
\[
P_{0,n,\mathrm{obs}}^{\otimes n}(A_0\cap A_1)
\ge1-2\alpha-\eta>0, \tag{28}
\]
where the strict positivity is exactly the declared condition \(\eta<1-2\alpha\).  On \(A_0\cap A_1\), both causal curves lie pointwise in the same band, so
\[
W_n\ge\|\mu_{1,n}-\mu_{0,n}\|_{L^\infty(I)}\ge cr_n. \tag{29}
\]
Taking expectation under \(P_{0,n,\mathrm{obs}}^{\otimes n}\), then the supremum over \(P\in\mathcal M\), and finally the infimum over all honest bands proves
\[
\mathfrak R_n\ge c(1-2\alpha-\eta)r_n. \tag{30}
\]
For fixed \(c_K>0\) and \(\beta>1/2\),
\[
K_n\asymp\frac{\log n}{\log\log n},
\qquad
r_n=K_n^{1/2-\beta}
\asymp\left\{\frac{\log\log n}{\log n}\right\}^{\beta-1/2}. \tag{31}
\]
Equations (25), (30), and (31) prove the matching frontier.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The matching expected-maximal-width frontier is the theorem-level statistical contribution and holds throughout the Sobolev embedding range \(\beta>1/2\). \textit{Gap.} No cited work matches an honest exact-profile ADRF sup-band upper bound to a fixed-nuisance marked lower bound under fully blind asymmetric replicates with spectral zeros. \textit{Consumer.} Study designers can translate the frontier into sample-size calculations for resolving dose-response features.

\begin{theorem}[thm:error-free-reduction]\label{thm:error-free-reduction}
On the submodel \(U_1=U_2=0\) almost surely, the factorization becomes \(F_x(s,t)=p_x\varphi_{f,x}(s+t)\) and \(G_x(s,t)=p_x\varphi_{q,x}(s+t)\), while the identified ADRF remains \(\mu(a)=\sum_{x\in\mathcal X}p_xE_P[Y\mid A=a,X=x]\), the standard continuous-treatment g-formula.
\end{theorem}
\begin{proof}
On the stated submodel, \(U_1=U_2=0\) almost surely, so \(\varphi_1(s)=\varphi_2(t)=1\) for every \(s,t\).  Substitution into the paired-channel factorization gives
\[
F_x(s,t)=p_x\varphi_{f,x}(s+t),
\qquad
G_x(s,t)=p_x\varphi_{q,x}(s+t).
\]
The identification theorem remains applicable and, using \(q_x=m_xf_x\) and overlap on \(I\), yields
\[
\mu(a)=\sum_{x\in\mathcal X}p_x\frac{q_x(a)}{f_x(a)}
=\sum_{x\in\mathcal X}p_xE_P[Y\mid A=a,X=x].
\]
Consistency and conditional unconfoundedness identify the last conditional regression with \(E_P\{Y(a)\mid X=x\}\) for almost every \(a\); causal-mean continuity and the continuous quotient extend the equality to every \(a\in I\).  Averaging over \(X\) gives \(E_PY(a)\), which is exactly the standard continuous-treatment g-formula asserted in the theorem.
\end{proof}
\par\smallskip\noindent \textit{Justification.} The reduction checks that the blind-replicate construction preserves the established causal target when measurement error vanishes. \textit{Gap.} The result is a required sanity reduction rather than a novelty claim. \textit{Consumer.} Implementations can be unit-tested against the ordinary error-free ADRF functional.

\begin{theorem}[thm:paired-protected-inverse]\label{thm:paired-protected-inverse}
There are constants \(c,C>0\), depending only on the fixed constants of \(\mathcal M\), such that, for every sufficiently small \(\delta>0\), all \(P,Q\in\mathcal M\) satisfying
\[
 \sum_{x\in\mathcal X}\left\{\int_{\mathcal C_*}\left(
 |F_{x,P}-F_{x,Q}|^2+B_Y^{-2}|G_{x,P}-G_{x,Q}|^2\right)\,ds\,dt
 +|p_{x,P}-p_{x,Q}|^2\right\}\le\delta^2
\]
satisfy
\[
 \|\mu_P-\mu_Q\|_{L^\infty(I)}
 \le C\left\{\frac{\log\log(1/\delta)}{\log(1/\delta)}\right\}^{\beta-1/2}.
\]
Conversely, for every sufficiently small \(\delta\), there are two members of a submodel with fixed \(p_x\), fixed \(f_x=f^\circ\), fixed nonidentical asymmetric error laws, and conditionally Bernoulli outcomes, whose paired-channel discrepancy is at most \(\delta^2\) while their ADRFs are separated by at least
\[
 c\left\{\frac{\log\log(1/\delta)}{\log(1/\delta)}\right\}^{\beta-1/2}.
\]
Thus the exponent \(\beta-1/2\) is unimprovable on that fixed-nuisance marked submodel.
\end{theorem}
\begin{proof}
Let \(d(P,Q)\) be the square root of the displayed paired-channel discrepancy and suppose \(d(P,Q)\le\delta\).  Put
\[
\rho=\min\{\nu_*/4,(8B_A)^{-1}\}.
\]
Then \([-2\rho,2\rho]^2\subset\mathcal C_*\).  Under either law, the paired factorization, stratum positivity, compact dose support, and the protected-frequency bound give
\[
|F_x(s,t)|
\ge p_{\min}\left(1-B_A|s+t|\right)\frac12\frac12
\ge p_{\min}/8,
\qquad |s|,|t|\le2\rho. \tag{32}
\]

We first turn integrated channel control into uniform local control.  Every probability channel is uniformly Lipschitz because
\[
E|S_j|\le B_A+\sqrt{M_e},
\]
and every marked channel has the same Lipschitz bound multiplied by \(B_Y\).  If \(H\) is \(L\)-Lipschitz on the outer square and its maximum modulus on the inner square is \(M\), then on the intersection of the outer square with a disk of radius \(M/(2L)\) about a maximizer, \(|H|\ge M/2\).  The inner square has a fixed positive margin, so for sufficiently small \(\|H\|_2\), integration on that disk gives
\[
\|H\|_2^2\ge cM^4/L^2,
\qquad M\le C\|H\|_2^{1/2}. \tag{33}
\]
Applying (33) to each channel difference and using finiteness of \(\mathcal X\) yields
\[
\max_{x\in\mathcal X}\left(
\|F_{x,P}-F_{x,Q}\|_{\infty,[-\rho,\rho]^2}
+B_Y^{-1}\|G_{x,P}-G_{x,Q}\|_{\infty,[-\rho,\rho]^2}
\right)\le C\delta^{1/2}. \tag{34}
\]

The second \(t\)-derivatives of the probability channels are uniformly bounded, since
\[
E S_2^2\le2(B_A^2+M_e).
\]
For a twice differentiable scalar function \(h\), Taylor's formula at \(v\) and \(-v\) gives
\[
|h'(0)|\le \frac{\|h\|_\infty}{v}+\frac v2\|h''\|_\infty.
\]
Choosing \(v\) proportional to \(\|h\|_\infty^{1/2}\), within the fixed outer-square margin, and applying (34) to \(h(t)=F_{x,P}(s,t)-F_{x,Q}(s,t)\), gives
\[
\sup_{|s|\le\rho}
|\partial_tF_{x,P}(s,0)-\partial_tF_{x,Q}(s,0)|
\le C\delta^{1/4}. \tag{35}
\]

Centering of the second error and factorization imply, under each law,
\[
\frac{\partial_tF_x(s,0)}{F_x(s,0)}
=\frac{\varphi_{f,x}'(s)}{\varphi_{f,x}(s)}.
\]
The lower bound (32), together with (34)--(35), bounds the difference of the two logarithmic derivatives by \(C\delta^{1/4}\).  Integrating from zero to \(s\), using \(\varphi_{f,x,P}(0)=\varphi_{f,x,Q}(0)=1\), and using the Lipschitz property of the exponential map on the resulting bounded logarithms yields
\[
\sup_{|s|\le\rho}
|\varphi_{f,x,P}(s)-\varphi_{f,x,Q}(s)|
\le C\delta^{1/4}. \tag{36}
\]
Also, within each law the common nuisance factor cancels:
\[
\frac{G_x(s,0)}{F_x(s,0)}
=\frac{\varphi_{q,x}(s)}{\varphi_{f,x}(s)}.
\]
Using (32), (34), (36), and \(|\varphi_{q,x}(s)|\le\int|q_x|\le B_Y\) gives
\[
\sup_{|s|\le\rho}
|\varphi_{q,x,P}(s)-\varphi_{q,x,Q}(s)|
\le C\delta^{1/4}. \tag{37}
\]

Apply the continuous analytic Sobolev modulus lemma to \(f_{x,P}-f_{x,Q}\) and \(q_{x,P}-q_{x,Q}\).  Their supports lie in \(\mathcal A\), their Sobolev norms are at most \(2R_f\) and \(2R_q\), and their \(L^1\) norms are at most \(2\) and \(2B_Y\), respectively.  Since replacing \(\delta\) by a fixed multiple of \(\delta^{1/4}\) changes the following logarithmic expression only by fixed constants, (36)--(37) imply
\[
\max_{x\in\mathcal X}\left(
\|f_{x,P}-f_{x,Q}\|_\infty+
\|q_{x,P}-q_{x,Q}\|_\infty\right)
\le C\omega(\delta), \tag{38}
\]
where
\[
\omega(\delta)=
\left\{\frac{\log\log(1/\delta)}{\log(1/\delta)}\right\}^{\beta-1/2}.
\]
The discrepancy also directly gives \(\max_x|p_{x,P}-p_{x,Q}|\le\delta\).  Dose overlap and \(|q_{x,Q}|\le B_Yf_{x,Q}\) imply on \(I\)
\[
\left|\frac{q_{x,P}}{f_{x,P}}-
\frac{q_{x,Q}}{f_{x,Q}}\right|
\le \kappa^{-1}|q_{x,P}-q_{x,Q}|
+B_Y\kappa^{-1}|f_{x,P}-f_{x,Q}|. \tag{39}
\]
Combining (38)--(39), the finite identifying sum, and the mass difference proves
\[
\|\mu_P-\mu_Q\|_{L^\infty(I)}\le C\omega(\delta).
\]

For sharpness, apply the fixed-kernel Bernoulli packet lemma with
\[
k(\delta)=\left\lceil c_0
\frac{\log(1/\delta)}{\log\log(1/\delta)}\right\rceil,
\]
where \(c_0\) is a sufficiently large fixed constant.  Since \(m!\ge(m/e)^m\),
\[
C\frac{C_0^{4k(\delta)}}{\{(2k(\delta)-4)!\}^2}\le c_1\delta^2 \tag{40}
\]
for all sufficiently small \(\delta\), with \(c_1\) made as small as needed by increasing \(c_0\).  The packet pair has identical probability channels and identical masses.  If \(H\) is a complex random variable with \(|H|\le1\), the variational definition of total variation gives \(|E_PH-E_QH|\le2\|P-Q\|_{\mathrm{TV}}\); Cauchy--Schwarz gives \(4\|P-Q\|_{\mathrm{TV}}^2\le\chi^2(P,Q)\).  Apply these facts to \(Y e^{i(sS_1+tS_2)}/B_Y\).  Because \(\mathcal C_*\) has finite volume and \(\mathcal X\) is finite, (40) makes the full paired-channel discrepancy at most \(\delta^2\).  The packet separation is
\[
ck(\delta)^{1/2-\beta}\ge c\omega(\delta). \tag{41}
\]
All stratum masses, latent densities, and error laws are fixed across this pair, and its marks are conditionally Bernoulli.  Equations (40)--(41) prove the asserted converse and unimprovability.
\end{proof}

\begin{lemma}[lem:continuous-analytic-sobolev-modulus]\label{lem:continuous-analytic-sobolev-modulus}
Let \(\widehat h(t)=\int_{\mathcal A}e^{ita}h(a)\,da\).  For every fixed \(\rho>0\) there are constants \(c,C,C_0>0\), depending only on \(B_A,\beta,\rho\), such that every \(h\in H^\beta(\mathbb R)\) supported in \(\mathcal A\), with \(\|h\|_{H^\beta}\le R\), \(\|h\|_1\le M\), and \(\|\widehat h\|_{L^2[-\rho,\rho]}\le\varepsilon\), satisfies, for every integer \(k\ge1\),
\[
 \|h\|_\infty\le C\left[Rk^{1/2-\beta}+k(C_0k)^k\varepsilon+Mk e^{-ck}\right].
\]
Consequently, for all sufficiently small \(\varepsilon\),
\[
 \|h\|_\infty\le C(R+M)\left\{\frac{\log\log(1/\varepsilon)}{\log(1/\varepsilon)}\right\}^{\beta-1/2}.
\]
\end{lemma}
\begin{proof}
Put
\[
H(z)=\int_{-B_A}^{B_A}e^{iza}h(a)\,da.
\]
Absolute convergence on compact subsets of \(\mathbb C\) justifies differentiation under the integral and gives
\[
H^{(j)}(z)=\int_{-B_A}^{B_A}(ia)^je^{iza}h(a)\,da,
\qquad |H^{(j)}(0)|\le MB_A^j. \tag{42}
\]
Let \(T_k(z)=\sum_{j=0}^kH^{(j)}(0)z^j/j!\).  Taylor's formula inside the integral gives
\[
|H(t)-T_k(t)|
\le M\frac{(B_A|t|)^{k+1}}{(k+1)!},
\qquad t\in\mathbb R. \tag{43}
\]

We need one elementary polynomial extrapolation bound.  If \(p\) has degree at most \(k\), rescale \([-\rho,\rho]\) to \([-1,1]\) and expand in the orthonormal Legendre basis.  Cauchy--Schwarz bounds every coefficient by \(\|p\|_{L^2[-\rho,\rho]}\), while the three-term recursion, by induction, gives \(|P_j(x)|\le(2|x|+1)^j\) for \(|x|\ge1\).  Summing over \(0\le j\le k\) yields
\[
\sup_{|t|\le T}|p(t)|
\le C\rho^{-1/2}
\left\{\frac{C(1+T)}{\rho}\right\}^k
\|p\|_{L^2[-\rho,\rho]},
\qquad T\ge\rho. \tag{44}
\]
On \([-\rho,\rho]\), (43) and the hypothesis imply
\[
\|T_k\|_2
\le\varepsilon+(2\rho)^{1/2}M
\frac{(B_A\rho)^{k+1}}{(k+1)!}. \tag{45}
\]
Choose a fixed \(a_0>0\), depending only on \(B_A\) and \(\rho\), sufficiently small and set \(T=a_0k\).  The elementary factorial bound \((k+1)!\ge((k+1)/e)^{k+1}\) shows that both
\[
\left\{\frac{C(1+T)}{\rho}\right\}^k
\frac{(B_A\rho)^{k+1}}{(k+1)!}
\quad\text{and}\quad
\frac{(B_AT)^{k+1}}{(k+1)!}
\]
are at most \(Ce^{-ck}\).  Combining (43)--(45) therefore gives
\[
\sup_{|t|\le T}|H(t)|
\le (C_0k)^k\varepsilon+CMe^{-ck}. \tag{46}
\]
Because \(\beta>1/2\), Cauchy--Schwarz in Fourier space gives
\[
\int_{|t|>T}|H(t)|\,dt
\le
\left\{\int_{|t|>T}(1+t^2)^{-\beta}\,dt\right\}^{1/2}
\|h\|_{H^\beta}
\le CRT^{1/2-\beta}. \tag{47}
\]
Fourier inversion is valid because the right side of (47), together with (46), shows \(H\in L^1\).  Splitting the inversion integral at \(T=a_0k\), bounding its inner part by \(2T\) times (46), and using (47) proves
\[
\|h\|_\infty
\le C\left[Rk^{1/2-\beta}
+k(C_0k)^k\varepsilon+Mk e^{-ck}\right]. \tag{48}
\]

For the consequence, let \(L=\log(1/\varepsilon)\) and choose
\[
k=\left\lfloor c_1\frac{L}{\log(e+L)}\right\rfloor
\]
with \(c_1>0\) sufficiently small.  Then
\[
k(C_0k)^k\varepsilon+Mk e^{-ck}
\le C(R+M)k^{1/2-\beta}
\]
for all sufficiently large \(L\): after taking logarithms, the first term has leading exponent \(-(1-c_1+o(1))L\), and the second decays exponentially in \(k\), whereas \(k^{1/2-\beta}\) is polynomial.  Since \(k\asymp L/\log L\), substitution in (48) gives
\[
\|h\|_\infty
\le C(R+M)
\left\{\frac{\log\log(1/\varepsilon)}{\log(1/\varepsilon)}\right\}^{\beta-1/2}.
\]
\end{proof}

\begin{lemma}[lem:fixed-kernel-bernoulli-packet]\label{lem:fixed-kernel-bernoulli-packet}
There are fixed, nonidentical, asymmetric error densities \(g_1,g_2\), constants \(c,C,C_0>0\), and, for every sufficiently large integer \(k\), two laws \(P_{0,k},P_{1,k}\in\mathcal M\) with common stratum masses, common latent density \(f_x=f^\circ\), and common error densities \(g_1,g_2\), such that the second error is centered, both error second moments are at most \(M_e\), and the outcomes are conditionally Bernoulli.  Moreover,
\[
 \|\mu_{1,k}-\mu_{0,k}\|_{L^\infty(I)}\ge ck^{1/2-\beta}
\]
and
\[
 \chi^2(P_{1,k,\mathrm{obs}},P_{0,k,\mathrm{obs}})
 \le C\frac{C_0^{4k}}{\{(2k-4)!\}^2}.
\]
The probability channels and the complete unmarked observed laws are identical under the two laws.
\end{lemma}
\begin{proof}
Choose a closed nondegenerate interval \(J\subset\operatorname{int}(I)\), map it affinely to \([-1,1]\) by \(a\mapsto z(a)\), fix an integer \(r>\beta+4\), and put \(w(z)=(1-z^2)^r\).  Let \(\{\pi_j:j\ge0\}\) be the orthonormal Jacobi polynomials in \(L^2(w(z)\,dz)\), and define
\[
K_k(z,0)=\sum_{j=2k}^{4k}\pi_j(0)\pi_j(z),
\qquad
\widetilde h_k(a)=w(z(a))K_k(z(a),0)\mathbf 1\{a\in J\}. \tag{49}
\]
Every polynomial of degree below \(2k\) is in the span of \(\pi_0,\ldots,\pi_{2k-1}\).  Orthogonality in (49) therefore gives
\[
\int_J a^\ell\widetilde h_k(a)\,da=0,
\qquad 0\le\ell<2k. \tag{50}
\]

We record the estimates needed below and derive their orders from the Jacobi formula.  Rodrigues' formula and the exact squared norm give, for even degrees,
\[
P_{2m}^{(r,r)}(0)
=(-1)^m\frac{\Gamma(2m+r+1)}{2^{2m}m!\,\Gamma(m+r+1)},
\]
and the ratio of consecutive gamma factors shows that the normalized values \(|\pi_{2m}(0)|\) are bounded above and below by positive constants; odd values vanish.  Hence
\[
c k\le K_k(0,0)\le Ck. \tag{51}
\]
Repeated integration by parts in Rodrigues' formula gives, for every fixed integer \(m\le r-1\),
\[
\|\partial_z^m\{wK_k(\cdot,0)\}\|_2^2
\le C_m\sum_{j=2k}^{4k}(1+j)^{2m}|\pi_j(0)|^2
\le C_m k^{2m+1}. \tag{52}
\]
The same factorial-product bounds in the Christoffel--Darboux identity give
\[
\sup_{z\in[-1,1]}|w(z)K_k(z,0)|\le Ck. \tag{53}
\]
Because \(w\) has a zero of order \(r\) at each endpoint, extension by zero introduces no boundary distribution through derivative order \(r-1\).  After the fixed affine change of variables, (51)--(53) imply
\[
\|\widetilde h_k\|_2\le Ck^{1/2},
\qquad
\|\widetilde h_k\|_{H^m}\le C_mk^{m+1/2},
\qquad
\|\widetilde h_k\|_\infty\le Ck. \tag{54}
\]
If \(m>\beta\), Hölder's inequality applied to
\((1+t^2)^\beta|\widehat{\widetilde h_k}(t)|^2\) gives the interpolation inequality
\[
\|\widetilde h_k\|_{H^\beta}
\le\|\widetilde h_k\|_2^{1-\beta/m}
\|\widetilde h_k\|_{H^m}^{\beta/m}
\le Ck^{\beta+1/2}. \tag{55}
\]
After multiplication by one fixed sufficiently small constant, set
\[
h_k=k^{-\beta-1/2}\widetilde h_k.
\]
Equations (51), (54), and (55), together with Cauchy--Schwarz on the fixed interval \(J\), yield
\[
\|h_k\|_{H^\beta}\le1,
\quad \|h_k\|_1\le Ck^{-\beta},
\quad \|h_k\|_\infty\le Ck^{1/2-\beta},
\quad |h_k(a_0)|\ge ck^{1/2-\beta}, \tag{56}
\]
where \(a_0\) corresponds to \(z=0\).  The moment cancellations (50) are unchanged.

We next construct two fixed error laws.  Define
\[
\psi(u)=\left\{\frac{\sin(u/2)}{u/2}\right\}^4,
\qquad
g_{a,v}^{\sharp}(u)=c_{a,v}\{\psi(u)+v\psi(u-a)\}, \tag{57}
\]
where \(a\notin2\pi\mathbb Z\), \(v>0\), \(v\ne1\), and \(c_{a,v}\) normalizes the integral.  This is a nonnegative asymmetric density.  The Fourier transform of \(\psi\) is the fourfold convolution of compact interval indicators, so the characteristic function of (57) has compact support.  The functions \(\sin(u/2)\) and \(\sin((u-a)/2)\) have no common zero.  Their squared distance from their zero lattices has a positive minimum over one period, and their denominators are comparable to \((1+|u|)^4\); hence
\[
c(1+|u|)^{-4}\le g_{a,v}^{\sharp}(u)
\le C(1+|u|)^{-4}. \tag{58}
\]
Thus the second moment is finite.  Choose two different parameter pairs in (57), center both densities, and apply a common sufficiently small scale factor.  The resulting fixed densities \(g_1,g_2\) remain asymmetric and nonidentical; their characteristic functions are supported in one fixed interval \([-T_*,T_*]\), their second moments are at most \(M_e\), and the second density is centered.  The two-sided tail comparison (58) is preserved up to fixed constants.

Take \(p_x=|\mathcal X|^{-1}\) and \(f_x=f^\circ\) for every \(x\).  The declared restrictions on \(p_{\min}\), \(R_f\), and \(\kappa\) make these choices satisfy stratum positivity, the density Sobolev bound, compact support, and overlap.  Define
\[
m_{0,k}(a)=\frac12,
\qquad
m_{1,k}(a)=\frac12+\gamma\frac{h_k(a)}{f^\circ(a)},
\qquad
q_{j,k}=m_{j,k}f^\circ. \tag{59}
\]
Choose fixed \(\gamma>0\) smaller than \(R_q-\|f^\circ\|_{H^\beta}/2\).  The support of \(h_k\) is contained in the interior interval \(J\), where \(f^\circ\) has a positive minimum.  Since \(\beta>1/2\), (56) implies \(\|h_k\|_\infty\to0\), so, after reducing \(\gamma\) once, \(m_{1,k}\in[1/4,3/4]\) for all sufficiently large \(k\).  Equations (56) and (59) also give \(\|q_{j,k}\|_{H^\beta}\le R_q\).

Let \(V\) be uniform on \([0,1]\), independently of \((A,X,U_1,U_2)\), and define
\[
Y_j(a)=\mathbf 1\{V\le m_{j,k}(a)\},
\qquad Y_j=Y_j(A).
\]
This verifies consistency, bounded outcomes because \(B_Y\ge1\), conditional unconfoundedness, conditional Bernoulli marks, and causal-mean continuity.  Together with the preceding checks it proves \(P_{0,k},P_{1,k}\in\mathcal M\).  Their ADRFs satisfy
\[
\|\mu_{1,k}-\mu_{0,k}\|_{L^\infty(I)}
=\gamma\|h_k/f^\circ\|_{L^\infty(I)}
\ge ck^{1/2-\beta}. \tag{60}
\]

It remains to control the observed divergence.  Put
\[
d(s_1,s_2)=\int f^\circ(a)g_1(s_1-a)g_2(s_2-a)\,da,
\qquad
H_k(s_1,s_2)=\int h_k(a)g_1(s_1-a)g_2(s_2-a)\,da. \tag{61}
\]
Compact support of \(f^\circ\) and (58) imply
\[
d(s_1,s_2)
\ge c(1+|s_1|)^{-4}(1+|s_2|)^{-4}. \tag{62}
\]
The two-dimensional Fourier transform of \(H_k\) is
\[
\widehat H_k(s,t)=\widehat h_k(s+t)\varphi_1(s)\varphi_2(t), \tag{63}
\]
and it vanishes outside \([-T_*,T_*]^2\).  Differentiating \(\widehat h_k\), subtracting from \(e^{iua}\) its Taylor polynomial of order \(2k-j-1\), and using (50) gives, for \(0\le j\le4\) and \(|u|\le2T_*\),
\[
|\widehat h_k^{(j)}(u)|
\le C\|h_k\|_1\frac{C_0^{2k-j}}{(2k-j)!}
\le C\frac{C_0^{2k}}{(2k-4)!}. \tag{64}
\]
The concrete error densities in (58) have square-integrable products \(u^jg_\ell(u)\) for \(0\le j\le2\).  Thus their characteristic functions have the square-integrable weak derivatives needed below.  Apply Plancherel to (63), and to every mixed derivative through order two in each coordinate.  Leibniz' rule and (64) then give
\[
\int H_k(s_1,s_2)^2(1+|s_1|)^4(1+|s_2|)^4\,ds_1\,ds_2
\le C\frac{C_0^{4k}}{\{(2k-4)!\}^2}. \tag{65}
\]
Under the baseline law, conditional on any stratum, the observed densities for marks \(Y=1\) and \(Y=0\) are both \(d/2\).  Under the alternative their changes are respectively \(\gamma H_k\) and \(-\gamma H_k\).  Summing the two mark contributions and the common finite-stratum probabilities, then using (62)--(65), yields
\[
\chi^2(P_{1,k,\mathrm{obs}},P_{0,k,\mathrm{obs}})
=4\gamma^2\int\frac{H_k^2}{d}
\le C\frac{C_0^{4k}}{\{(2k-4)!\}^2}. \tag{66}
\]
Finally, the law of \((X,S_1,S_2)\) is unchanged because only the conditional mark was perturbed.  Hence all probability channels and the complete unmarked observed laws coincide.  Equations (60) and (66) prove every assertion.
\end{proof}

\begin{theorem}[thm:paired-protected-modulus-characterization]\label{thm:paired-protected-modulus-characterization}
There are constants \(0<c<C<\infty\) such that, for every sufficiently small \(\delta>0\),
\[
\begin{aligned}
c\left\{\frac{\log\log(1/\delta)}{\log(1/\delta)}\right\}^{\beta-1/2}
&\le
\sup\Bigg\{\|\mu_P-\mu_Q\|_{L^\infty(I)}:P,Q\in\mathcal M,\\
&\quad \sum_{x\in\mathcal X}\left[\int_{\mathcal C_*}
\left(|F_{x,P}-F_{x,Q}|^2+B_Y^{-2}|G_{x,P}-G_{x,Q}|^2\right)\,ds\,dt
+|p_{x,P}-p_{x,Q}|^2\right]\le\delta^2\Bigg\}\\
&\le C\left\{\frac{\log\log(1/\delta)}{\log(1/\delta)}\right\}^{\beta-1/2}.
\end{aligned}
\]
The lower inequality remains true when the supremum is restricted to pairs with fixed common \(p_x\), fixed common \(f_x=f^\circ\), fixed common nonidentical asymmetric error laws, and conditionally Bernoulli outcomes.
\end{theorem}
\begin{proof}
The upper inequality is the first conclusion of the paired protected inverse theorem, applied to every admissible pair and then followed by a supremum.  The converse clause of that same theorem supplies, for every sufficiently small \(\delta\), an admissible pair in the stated fixed-nuisance Bernoulli submodel with separation at least a fixed positive multiple of \(\{\log\log(1/\delta)/\log(1/\delta)\}^{\beta-1/2}\).  Taking the restricted supremum over a set containing that pair proves the lower inequality.  Hence the question has the positive sharp characterization stated here.
\end{proof}
\par\smallskip\noindent \textit{Justification.} This theorem gives the computable two-sided modulus that answers the protected-inverse question. \textit{Gap.} The upper and fixed-nuisance lower moduli are proved at the same exponent. \textit{Consumer.} The exact-profile diameter bound uses its upper side, while the statistical converse uses the fixed-nuisance construction.

\begin{lemma}[lem:exact-profile-compact-measurability]\label{lem:exact-profile-compact-measurability}
Endow the image of \(\mathcal M\) under \(Q\mapsto((p_{x,Q},f_{x,Q},q_{x,Q})_{x\in\mathcal X},\mathcal L_Q(U_1),\mathcal L_Q(U_2))\) with the product of the Euclidean topology for the stratum masses, the uniform topology on \(\mathcal A\) for the density coordinates, and the weak topology for the two error laws.  This reduced-coordinate image is compact.  For every realized sample, \(Q\mapsto\mathscr D_n(Q)\) and \((Q,a)\mapsto\mu_Q(a)\) are continuous on that image.  The exact profile \(\Theta_{n,\alpha}\), interpreted through reduced coordinates, is a measurable compact-valued correspondence, possibly empty, and the functions \((O_1,\ldots,O_n,a)\mapsto\inf_{Q\in\Theta_{n,\alpha}}\mu_Q(a)\) and \((O_1,\ldots,O_n,a)\mapsto\sup_{Q\in\Theta_{n,\alpha}}\mu_Q(a)\), with both set equal to zero when the profile is empty, are jointly measurable on the sample space times \(I\).
\end{lemma}
\begin{proof}
We first prove compactness rather than assume it.  Consider any sequence of reduced coordinates arising from laws in \(\mathcal M\).  The stratum-mass vectors lie in the closed simplex cut out by \(p_x\ge p_{\min}\), hence have a convergent subsequence.  For any function \(h\) supported on \(\mathcal A\) with \(\|h\|_{H^\beta}\le R\), Fourier inversion and Cauchy--Schwarz give the common bound
\[
\|h\|_\infty
\le C R\left\{\int_{\mathbb R}(1+t^2)^{-\beta}\,dt\right\}^{1/2}<\infty. \tag{7}
\]
Moreover, for \(a,a'\in\mathcal A\),
\[
|h(a)-h(a')|
\le CR\left[\int_{\mathbb R}
\min\{4,t^2|a-a'|^2\}(1+t^2)^{-\beta}\,dt\right]^{1/2}. \tag{8}
\]
The integral in (8) tends to zero with \(|a-a'|\) by dominated convergence, since \(\beta>1/2\).  Thus both the density and marked-density balls are uniformly bounded and equicontinuous on \(\mathcal A\).  A diagonal subsequence on a countable dense subset, followed by (8), converges uniformly.  Boundedness in the Hilbert space \(H^\beta\) also gives a weakly convergent subsubsequence.  Its weak limit equals the uniform limit as a distribution, and weak lower semicontinuity preserves the respective \(H^\beta\) bounds.  Nonnegativity, unit integral, support in \(\mathcal A\), overlap on \(I\), and the pointwise relation \(|q_x|\le B_Yf_x\) are closed under uniform convergence.

For either error coordinate, Markov's inequality gives uniformly
\[
Q(|U_j|>T)\le M_eT^{-2}. \tag{9}
\]
Choosing successively convergent distribution functions on rational continuity points and using (9) to prevent loss of mass produces a weakly convergent subsequence.  The function \(u\mapsto u^2\) is the increasing limit of bounded continuous truncations, so the limit law has second moment at most \(M_e\).  In addition,
\[
E_Q\{|U_j|\mathbf 1(|U_j|>T)\}\le M_e/T, \tag{10}
\]
so first moments are uniformly integrable.  Hence the weak limit of the second error remains centered.  This proves sequential compactness of all coordinate factors; they are metrizable, so it proves compactness once closedness of the image is checked.

For that check, let a convergent coordinate sequence have limit \((p_x,f_x,q_x,\nu_1,\nu_2)\).  Put \(m_x=q_x/f_x\) where \(f_x>0\), and put \(m_x=0\) where \(f_x=0\).  The preserved inequality \(|q_x|\le B_Yf_x\) gives \(|m_x|\le B_Y\), and overlap makes \(m_x\) continuous on \(I\).  Construct \(X\) with masses \(p_x\), draw \(A\mid X=x\) with density \(f_x\), draw independent errors with laws \(\nu_1,\nu_2\), independently of \((A,X)\), and define the potential-outcome schedule deterministically by \(Y(a)=m_X(a)\), with \(Y=Y(A)\).  This construction satisfies consistency, conditional unconfoundedness, bounded outcomes, causal-mean continuity on \(I\), both error-independence conditions, centering, all support and Sobolev restrictions, and both overlap restrictions.  Its marked density is \(q_x\).  Thus the limiting coordinates are again the image of a member of \(\mathcal M\), proving compactness of the reduced image.

We next prove continuity.  Weak convergence of error laws implies pointwise convergence of their characteristic functions.  The common bound \(E|U_j|\le\sqrt{M_e}\) makes these characteristic functions uniformly Lipschitz, so a finite-net argument upgrades pointwise convergence to uniform convergence on every compact frequency interval.  Uniform convergence of \(f_x\) and \(q_x\) on \(\mathcal A\) gives uniform convergence of their Fourier transforms on compact frequency intervals.  The paired channel factorization therefore gives uniform convergence of \(F_{x,Q}\) and \(G_{x,Q}\) on \(\mathcal C_*\).  Integration over the finite-volume cube and convergence of the mass coordinates prove continuity of \(Q\mapsto\mathscr D_n(Q)\) for each fixed sample.  Finally, overlap gives, uniformly on \(I\),
\[
\left|\frac{q_x}{f_x}-\frac{\widetilde q_x}{\widetilde f_x}\right|
\le \kappa^{-1}|q_x-\widetilde q_x|
+B_Y\kappa^{-1}|f_x-\widetilde f_x|, \tag{11}
\]
so the finite identifying sum proves joint continuity of \((Q,a)\mapsto\mu_Q(a)\).

Let the compact reduced image just proved be denoted temporarily by \(K\) inside this proof.  The map \((O_1,\ldots,O_n,z)\mapsto\mathscr D_n(z)\) is measurable in the sample and continuous in \(z\): this follows directly from the finite empirical sums and the preceding continuity argument.  Hence the sublevel set \(\Theta_{n,\alpha}=\{z\in K:\mathscr D_n(z)\le\epsilon_n^2\}\) is compact for every sample.  To verify correspondence measurability directly, let \(V\) be open in \(K\), and define the compact sets
\[
C_m(V)=\{z\in K:\operatorname{dist}(z,K\setminus V)\ge m^{-1}\}.
\]
Then \(\Theta_{n,\alpha}\cap V\ne\varnothing\) exactly when, for some \(m\),
\[
\inf_{z\in C_m(V)}\mathscr D_n(z)\le\epsilon_n^2. \tag{12}
\]
For a fixed countable dense subset of each \(C_m(V)\), continuity makes the infimum in (12) the infimum over that countable set, hence measurable.  A countable base for the compact metric space \(K\) completes the correspondence-measurability claim.

It remains to establish joint endpoint measurability without invoking an unstated selection theorem.  For positive integers \(m\), define
\[
v_m^-(o,a)=\inf_{z\in K}\left[\mu_z(a)+m\{\mathscr D_n(o,z)-\epsilon_n^2\}_+\right],
\]
\[
v_m^+(o,a)=\sup_{z\in K}\left[\mu_z(a)-m\{\mathscr D_n(o,z)-\epsilon_n^2\}_+\right]. \tag{13}
\]
Each extremum in (13) equals the corresponding extremum over one fixed countable dense subset of \(K\), because the bracketed objectives are continuous in \(z\).  Thus \(v_m^-\) and \(v_m^+\) are jointly measurable in \((o,a)\).  If the profile is nonempty, compactness and a convergent-subsequence argument for optimizing points show
\[
v_m^-(o,a)\uparrow\inf_{z\in\Theta_{n,\alpha}(o)}\mu_z(a),
\qquad
v_m^+(o,a)\downarrow\sup_{z\in\Theta_{n,\alpha}(o)}\mu_z(a). \tag{14}
\]
Indeed, boundedness of \(\mu_z(a)\) forces every subsequential optimizer with bounded penalized value to have \(\mathscr D_n(o,z)\le\epsilon_n^2\), and continuity then gives the two limiting extrema.  If the profile is empty, compactness makes \(\min_{z\in K}\mathscr D_n(o,z)>\epsilon_n^2\), so this case is itself measurable, and the stipulated zero value is measurable.  The pointwise limits in (14), combined with that empty-profile convention, prove joint measurability of both exact endpoint functions.
\end{proof}
\par\smallskip\noindent \textit{Justification.} Compact reduced coordinates make exact-profile extrema attained and measurable without an effective finite cover. \textit{Gap.} The lemma supplies the non-effective topological foundation needed by the exact-profile statistical procedure. \textit{Consumer.} The exact profiled band uses the lemma for well-defined measurable endpoints.

\begin{openendedquestion}[oeq:finite-profile-certification]\label{oeq:finite-profile-certification}
Under exactly the model class \(\mathcal M\), can one construct from the sample and fixed class constants a terminating algorithm that returns a finite, finitely checkable witness cover of the exact profile \(\Theta_{n,\alpha}\), together with rigorous endpoint enclosures whose deterministic error is \(o(r_n)\), without deciding nonemptiness of an abstract infinite-dimensional decoder?  Such an effective certification result is not established here and remains an open computational question; the statistical band and its rate use the exact profile directly.
\end{openendedquestion}
\par\smallskip\noindent \textit{Justification.} This records the computational guarantee not supplied by compactness and measurable extrema. \textit{Gap.} A terminating finitely checkable witness cover under the full unchanged model is not proved. \textit{Consumer.} Future work must resolve it before advertising auditable finite endpoint computation.

\section*{Technical internal limitation (diagnostic only)}

The compactness and measurable-extremum proof is non-effective. Under the unchanged Sobolev and second-moment model, a terminating finitely checkable witness cover of the exact profile, with rigorously negligible endpoint error and without an oracle decision of abstract decoder nonemptiness, remains open as \(\text{oeq:finite-profile-certification}\). Thus the delivered frontier is a statistical existence and exact-profile result, not an effective numerical certification theorem. On the converse side, the weighted Jacobi packet must preserve Bernoulli range, Sobolev slack, fixed nuisances, and product-law affinity.

\section{Honest scope}

The frontier fixes \(\beta>1/2\), the support interval, Sobolev radii, overlap constants, outcome bound, and error second-moment radius. Error laws remain unknown, nonidentical, asymmetric, and may have zeros outside the protected neighborhood. Band endpoints are exact extrema of the continuous integrated profile. Adaptation to unknown smoothness or drifting channel severity is outside the theorem. The error-free reduction remains a separate specialization.

\begin{thebibliography}{99}

  \bibitem{HuangZhang2023} W. Huang and Z. Zhang (2023), Nonparametric estimation of the continuous treatment effect with measurement error, Journal of the Royal Statistical Society: Series B 85, 1196--1216, doi:10.1093/jrsssb/qkad013.

  \bibitem{CapitaoMiniconiGassiatLehericy2025} J. Capitao-Miniconi, E. Gassiat, and L. Lehéricy (2025), Deconvolution of repeated measurements corrupted by unknown noise, Electronic Journal of Statistics 19, 4141--4173, doi:10.1214/25-EJS2415.

  \bibitem{KatoSasakiUra2021} K. Kato, Y. Sasaki, and T. Ura (2021), Robust inference in deconvolution, Quantitative Economics 12, 109--142, doi:10.3982/QE1643.

  \bibitem{KatoSasaki2018} K. Kato and Y. Sasaki (2018), Uniform confidence bands in deconvolution with unknown error distribution, Journal of Econometrics 207, 129--161, doi:10.1016/j.jeconom.2018.07.001.

  \bibitem{KatoSasaki2019} K. Kato and Y. Sasaki (2019), Uniform confidence bands for nonparametric errors-in-variables regression, Journal of Econometrics 213, 516--555.

  \bibitem{Neumann2007} M. H. Neumann (2007), Deconvolution from panel data with unknown error distribution, Journal of Multivariate Analysis 98, 1955--1968, doi:10.1016/j.jmva.2006.09.012.

  \bibitem{EvdokimovWhite2012} K. Evdokimov and H. White (2012), Some extensions of a lemma of Kotlarski, Econometric Theory 28, 925--932, doi:10.1017/S0266466611000831.

  \bibitem{LouniciNickl2011} K. Lounici and R. Nickl (2011), Global uniform risk bounds for wavelet deconvolution estimators, Annals of Statistics 39, 201--231, doi:10.1214/10-AOS836.

  \bibitem{GenoveseWasserman2008} C. Genovese and L. Wasserman (2008), Adaptive confidence bands, Annals of Statistics 36, 875--905, doi:10.1214/07-AOS500.

  \bibitem{Low1997} M. G. Low (1997), On nonparametric confidence intervals, Annals of Statistics 25, 2547--2554.

  \bibitem{Bull2012} A. D. Bull (2012), Honest adaptive confidence bands and self-similar functions, Electronic Journal of Statistics 6, 1490--1516, doi:10.1214/12-EJS720.

  \bibitem{KennedyMaMcHughSmall2017} E. H. Kennedy, Z. Ma, M. D. McHugh, and D. S. Small (2017), Non-parametric methods for doubly robust estimation of continuous treatment effects, Journal of the Royal Statistical Society: Series B 79, 1229--1245, doi:10.1111/rssb.12212.

  \bibitem{ColangeloLee2025} K. Colangelo and Y.-Y. Lee (2025), Double debiased machine learning nonparametric inference with continuous treatments, Journal of Business \& Economic Statistics, published online, doi:10.1080/07350015.2025.2505487.

  \bibitem{BelomestnyGoldenshluger2021} D. Belomestny and A. Goldenshluger (2021), Density deconvolution under general assumptions on the distribution of measurement errors, Annals of Statistics 49, 615--649, doi:10.1214/20-AOS1969.

  \bibitem{Babii2020} A. Babii (2020), Honest confidence sets in nonparametric IV regression and other ill-posed models, Econometric Theory 36, 658--706.

  \bibitem{GaoBondellHuangGong2024} E. Gao, H. Bondell, W. Huang, and M. Gong (2024), A variational framework for estimating continuous treatment effects with measurement error, International Conference on Learning Representations.

\end{thebibliography}

\end{document}