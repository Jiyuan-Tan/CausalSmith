Conditionally on an accepted covering family \((\gamma_G:G\in\mathfrak G_3)\), finiteness would give the bound
\[
B=\max_{G\in\mathfrak G_3}\operatorname{cost}_3(\gamma_G)+b_{\mathrm{transport}},
\]
where \(b_{\mathrm{transport}}\) is the fixed cost of canonical lookup and relabelling.  Certificate soundness would then give the forward implication, while an accepted rational twin would contradict \(\operatorname{UID}_X(G)=1\) and give the converse.  This is only a conditional reduction: the frozen core supplies neither the \(34\) residual certificates nor their operation counts, so it does not determine a numerical \(B\).  In addition, the current theorem's final assertion is false as written.  The arrows of \(G_{\mathrm{OR}}\) are \(R_2\to R_1\), \(R_3\to R_2\), \(X_3\to R_1\), \(X_1\to R_2\), and \(X_1\to R_3\).  None is a self-censoring arrow, and neither response arrow has its same-index substantive companion into the same child.  Hence \(G_{\mathrm{OR}}\) lies inside, not outside, the no-self-censoring/no-colluder subdomain.