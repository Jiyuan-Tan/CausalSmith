Conditionally on a covering accepted certificate family \((\gamma_G:G\in\mathfrak G_3)\), finiteness gives
\[
B=\max_{G\in\mathfrak G_3}\operatorname{cost}_3(\gamma_G)+b_{\mathrm{transport}},
\]
where \(b_{\mathrm{transport}}\) is the fixed canonical-lookup and relabelling cost. Certificate soundness then proves the forward implication, and every accepted rational twin proves the converse by contradicting \(\operatorname{UID}_X(G)=1\). The missing \(496\)-mask no-colluder transcript and the missing \(34\)-row residual ledger prevent a numerical value of \(B\).