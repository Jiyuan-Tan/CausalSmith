For residual case \(6\), the nonexceptional stratum has an explicit rational inverse.  Suppress \(X_2\) temporarily and write
\[
p(r\mid x)=a_{x_3}(r_1)b_{x_1,r_1}(r_2)c_{x_1,r_2}(r_3).
\]
For \(u,z\in\{0,1\}\), the terminal kernel is observed on the \(R_1=1\) strata:
\[
c_{us}=q(R_3=1\mid X_1^*=u,R_1=1,R_2=s),\qquad s\in\{0,1\}.
\]
Define the observed quantities
\[
H_{uz}=
\frac{q(X_1^*=u,X_3^*=z,R=(1,1,1))}{c_{u1}}
+
\frac{q(X_1^*=u,X_3^*=z,R=(1,0,1))}{c_{u0}}.
\]
Factorization gives \(H_{uz}=p(X_1=u,X_3=z)a_z(1)\).  Put
\[
d_u=b_{u,0}(1),\qquad k_z=\frac{a_z(0)}{a_z(1)},
\]
and let
\[
e_z=q(X_3^*=z,R=(0,1,1)),\qquad
g_z=q(X_3^*=z,R=(0,0,1)),
\]
where unshown observed coordinates are summed out.  Exact summation of the graph factorization yields
\[
e_z=k_z\sum_uH_{uz}d_uc_{u1},\qquad
g_z=k_z\sum_uH_{uz}(1-d_u)c_{u0}.
\]
After cross multiplication, \(d=(d_0,d_1)^{\mathsf T}\) solves the observed linear system \(Md=h\), where
\[
M_{zu}=H_{uz}(g_zc_{u1}+e_zc_{u0}),
\qquad
h_z=e_z\sum_uH_{uz}c_{u0}.
\]
Thus, on the stratum
\[
\Delta=M_{00}M_{11}-M_{01}M_{10}\ne0,
\]
one has the denominator-safe rational formulas
\[
d=M^{-1}h,\qquad
k_z=\frac{e_z}{\sum_uH_{uz}d_uc_{u1}},\qquad
p(X_1=u,X_3=z)=H_{uz}(1+k_z).
\]
The last displayed denominator is positive throughout the model.  Since the response law does not depend on \(X_2\),
\[
p(X_2=v\mid X_1=u,X_3=z)
=
\frac{q(X^*=(u,v,z),R=\mathbf1)}
{\sum_{v'}q(X^*=(u,v',z),R=\mathbf1)},
\]
and multiplying this conditional by \(H_{uz}(1+k_z)\) recovers all eight target cells.  Every conditional and quotient used above expands into observed event marginals.

The unresolved early-kill stratum is exactly \(\Delta=0\).  There the two displayed equations have rank at most one.  The remaining \(X_2\)-specific versions of the \(R=(0,1,1)\) equations and the aggregated \(R_3=0\) equations are available, but the frozen core contains neither a proof that they select a unique rational value of \(k_z\) on every positive stratum nor a positive rational twin when they do not.  The other residual representatives likewise have no ledger rows.  Consequently no \(34\)-row dichotomy or exceptional-stratum \(\operatorname{AlgTest}_3\) audit can yet be certified.