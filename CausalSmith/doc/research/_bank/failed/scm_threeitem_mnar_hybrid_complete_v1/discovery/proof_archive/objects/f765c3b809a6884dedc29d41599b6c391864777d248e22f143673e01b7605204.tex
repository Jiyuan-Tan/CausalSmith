The numerical partition can be derived up to the still-uncompiled identification certificates.  There are \(25(2^9-2^6)=11200\) graphs with at least one self-censoring arrow.  For any such arrow \(X_i\to R_i\), one positive rational twin has \(p(X_i=1)=1/2\) and response probabilities \((1/2,1/2)\), while the other has \(p(X_i=1)=3/5\) and response probabilities \((5/8,5/12)\) at \(X_i=0,1\).  In both laws the observed one-item probabilities are \((1/4,1/4,1/2)\).  Taking the other four variables independent Bernoulli \((1/2)\) gives equal \(27\)-cell coarsenings, minimum full cells \(1/64\) and \(3/320\), and target masses \(1/8\) and \(3/20\) at the cell with \(X_i=1\) and the other substantive coordinates zero.  Extra graph parents are ignored by the kernels, so the construction factorizes over every supergraph containing the self-censoring arrow.

Among the \(1600\) no-self-censoring graphs, fix a response DAG whose undirected skeleton has \(m\) edges.  Requiring a criss-cross pair on a specified skeleton edge fixes two of the six substantive arrows.  Inclusion--exclusion gives \(0,16,28,37\) criss-cross substantive masks for \(m=0,1,2,3\).  Since the corresponding numbers of response DAGs are \(1,6,12,6\), the total is
\[
6\cdot16+12\cdot28+6\cdot37=654.
\]
For an oriented obstruction \(R_1\to R_2\), \(X_2\to R_1\), \(X_1\to R_2\), the two substantive tables
\[
\begin{pmatrix}1/3&1/6\\1/6&1/3\end{pmatrix},
\qquad
\begin{pmatrix}3/10&11/60\\3/20&11/30\end{pmatrix}
\]
are paired, respectively, with
\[
\begin{aligned}
&p_A(R_1=1\mid X_2=0,1)=(1/2,1/2),\\
&p_A(R_2=1\mid X_1=0,1,R_1=1)=(1/2,1/2),\\
&p_A(R_2=1\mid X_1=0,1,R_1=0)=(1/3,2/3),
\end{aligned}
\]
and
\[
\begin{aligned}
&p_B(R_1=1\mid X_2=0,1)=(5/9,5/11),\\
&p_B(R_2=1\mid X_1=0,1,R_1=1)=(1/2,1/2),\\
&p_B(R_2=1\mid X_1=0,1,R_1=0)=(35/54,10/27).
\end{aligned}
\]
Multiplication and summation give the same nine observed cells
\[
q_{11}=\begin{pmatrix}1/12&1/24\\1/24&1/12\end{pmatrix},\quad
q_{10}=(1/8,1/8),\quad q_{01}=(1/9,5/36),\quad q_{00}=1/4.
\]
Adjoining independent fair \(X_3,R_3\) gives positive rational \(64\)-cell twins with minima \(1/144\) and \(1/162\), and target values \(1/6\) and \(3/20\) at \((0,0,0)\).  Relabelling and ignoring extra parents cover all \(654\) graphs.

The finite mask arithmetic behind the remaining counts is also exact.  For a response DAG with \(m\) arrows, absence of a colluder forbids the \(m\) corresponding substantive arrows, so
\[
1\cdot2^6+6\cdot2^5+12\cdot2^4+6\cdot2^3=496.
\]
For direct associational recovery, an arrow \(X_j\to R_i\) is allowed only when \(R_j\) is not a response descendant of \(R_i\).  Across the \(25\) response DAGs, the numbers of allowed substantive arrows have multiplicities
\[
(6,1),\ (5,6),\ (4,6),\ (3,12),
\]
and hence there are \(2^6+6\cdot2^5+6\cdot2^4+12\cdot2^3=448\) direct-recovery masks.  Intersecting this condition with no-colluder gives
\[
2^6+6\cdot2^4+6\cdot2^2+6\cdot2+6=202,
\]
so the direct branch adds \(448-202=246\) graphs.  The criss-cross, no-colluder, and direct-recovery-only classes are disjoint, leaving
\[
1600-654-496-246=204.
\]
A direct check of the five nonidentity simultaneous permutations shows that none fixes a residual mask, so Burnside gives \(204/6=34\) residual classes.

What is not established by these calculations is the theorem's assertion that all \(496\) inherited full-law formulas and all \(246\) direct formulas have already been compiled into accepted \(\mathcal L_3\) traces under the unusually restrictive raw-observed-marginal denominator grammar.  No durable list of the \(742\) accepted \(\mathrm{ID}\) certificates or checker transcript is present in the frozen core.