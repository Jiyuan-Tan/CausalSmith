At each block, condition on \(\mathcal F_{b-1}\), softly tilt the uniform fixed-half saturation assignment by \(f_{\eta,\kappa}(W_b)\), then randomize fixed treatment counts independently within groups and hold both stages fixed for two periods.  This assignment mechanism is part of the structural design class; covariance-limit conditions enter only the theorems that use them.