The finite population consists of \(G\) mutually isolated groups of common size \(n\), observed at the endpoints of \(B\) two-period blocks; potential outcomes are fixed.  The inference regime sends \(B\to\infty\) with even \(G\) fixed or varying arbitrarily, while the projection comparison separately sends \(G\to\infty\).