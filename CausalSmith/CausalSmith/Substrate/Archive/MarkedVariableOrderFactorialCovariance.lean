module
-- pending-child: import CausalSmith.Substrate.MarkedVariableOrderFactorialCovariance.Bounds

/-!
NOTE (2026-08-26): child modules are NOT yet written — this study's build phase
has not run. The imports above are commented out so the tree builds; restore
them when the children land (full-tree export gate caught the dangling imports).

# Marked variable-order factorial covariance substrate

This umbrella module exports finite partial matchings, normalized ordered
finite-coordinate statistics, their mixed-order product/covariance expansion,
and the associated falling-factorial normalization bounds.
-/
