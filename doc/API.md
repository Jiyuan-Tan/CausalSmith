# Causalean Lean 4 Library — API Reference

This document is **docstring-canonical**: each declaration's plain-English description is authored in its Lean docstring (first paragraph = the NL translation), and the per-declaration tables below are a *derived* view of those docstrings (signatures come from the compiled types). Do not hand-edit a per-declaration description or signature here — edit the declaration's docstring, then regenerate (`lake build && lake exe library_index` to refresh the index, then `cd CausalSmith/tools && npm run doc:gen` to re-splice the tables). Per-decl tables live inside `<!-- GEN:<module> -->` … `<!-- /GEN -->` markers; everything outside them (per-submodule narrative, conceptual grouping, usage examples drawn from `SCM/Examples/IV.lean`) is hand-written. CI runs `npm run doc:check`. For per-declaration lookup, prefer `npm run search` (`--scope module` for file-level orientation) over reading this file.

---

## 1. `Graph/DAG.lean` — Directed Acyclic Graphs

### Structure: `DAG V`

A DAG on a finite vertex type `V` with decidable equality.

| Field | Type | Description |
|---|---|---|
| `edge` | `V → V → Prop` | Directed edge relation |
| `decEdge` | `DecidableRel edge` | Decidability instance (auto-registered) |
| `topoOrder` | `V → ℕ` | Topological ordering witness |
| `topoOrder_lt` | `∀ u v, edge u v → topoOrder u < topoOrder v` | Edges respect the ordering (witnesses acyclicity) |

**Construction pattern:**

```lean
inductive MyNode | A | B | C deriving DecidableEq, Repr
instance : Fintype MyNode where ...

def myDAG : DAG MyNode where
  edge     | .A, .B => True | .B, .C => True | _, _ => False
  decEdge  := inferInstance  -- after proving DecidableRel
  topoOrder | .A => 0 | .B => 1 | .C => 2
  topoOrder_lt := by intro u v h; cases u <;> cases v <;> simp_all [...]
```

See `SCM/Examples/IV.lean` for a full 4-node example.

**Term-mode ancestry proofs:**

```lean
-- One edge: use .edge
example : ivDAG.isAncestor Z D :=
  DAG.isAncestor.edge (show ivDAG.edge Z D from trivial)

-- Transitivity: use .trans (ancestor_proof) (edge_proof)
example : ivDAG.isAncestor Z Y :=
  DAG.isAncestor.trans
    (DAG.isAncestor.edge (show ivDAG.edge Z D from trivial))
    (show ivDAG.edge D Y from trivial)
```

### Declarations

_Generated from docstrings — do not hand-edit; run `npm run doc:gen` (see CLAUDE.md). Edit the Lean docstring to change a description._

<!-- GEN:Causalean.Graph.DAG -->
| Decl | Signature | Description |
|---|---|---|
| `DAG` | `(V : Type u_2) → [DecidableEq V] → [Fintype V] → Type u_2` | A Directed Acyclic Graph on a finite vertex type: a decidable edge relation together with the condition that no vertex is connected to itself by a directed path — the transitive closure of the edge relation is irreflexive. Irreflexivity of the transitive closure is exactly the statement that the graph has no directed cycle. |
| `DAG.parents` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the parent set is the finite set of all vertices having a directed edge into that vertex. |
| `DAG.children` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the child set is the finite set of all vertices to which that vertex has a directed edge. |
| `DAG.mem_parents` | `u ∈ G.parents v ↔ G.edge u v` | Membership characterization for `parents`: `u ∈ G.parents v ↔ G.edge u v`. |
| `DAG.mem_children` | `w ∈ G.children v ↔ G.edge v w` | Membership characterization for `children`: `w ∈ G.children v ↔ G.edge v w`. |
| `DAG.isAncestor` | `V → V → Prop` | For a finite vertex population with decidable equality and a directed acyclic graph on that population, the ancestor relation holds from one vertex to another exactly when there is a directed path from the former to the latter. It is established either by a directed edge from the former vertex to the latter or by an existing ancestor path followed by a directed edge. |
| `DAG.isAncestor_iff_transGen` | `G.isAncestor u v ↔ Relation.TransGen G.edge u v` | The inductive ancestor relation coincides with `Relation.TransGen` of the edge relation: both are the transitive closure of the edge relation. |
| `DAG.irrefl` | `¬G.edge v v` | No vertex has an edge to itself (a directed self-loop would be a length-one cycle). |
| `DAG.asymm` | `G.edge u v → ¬G.edge v u` | If there is an edge from `u` to `v`, then there is no edge from `v` to `u` (a two-cycle is forbidden by acyclicity). |
| `DAG.isAncestor_irrefl` | `¬G.isAncestor v v` | Ancestor relation is irreflexive: no vertex is its own ancestor (this is acyclicity, restated for the inductive ancestor relation). |
| `DAG.isAncestor_trans` | `G.isAncestor u v → G.isAncestor v w → G.isAncestor u w` | Ancestor relation is transitive. |
| `DAG.isAncestor_child` | `G.isAncestor u v → G.edge u v ∨ ∃ c, G.edge u c ∧ G.isAncestor c v` | First-step decomposition: if `u` is an ancestor of `v`, then either `edge u v` or there exists a child `c` of `u` such that `c` is an ancestor of `v`. |
| `DAG.isDescendant` | `V → V → Prop` | For a finite directed acyclic graph on a vertex population and two vertices, the first and the second, the descendant relation holds precisely when there is a directed path from the second vertex to the first. |
| `DAG.ancStep` | `Finset V → Finset V` | For a finite directed acyclic graph on a vertex population and a finite vertex set, one backward ancestor-expansion step returns that set together with every parent of every member of the set. |
| `DAG.ancClosure` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the strict-ancestor set is obtained by starting with the vertex’s parents and applying backward ancestor expansion once for each vertex in the population. |
| `DAG.subset_iterate_ancStep` | `S ⊆ G.ancStep^[k] S` | Any finite set of graph nodes remains contained after applying the graph's ancestor-step operation any number of times. |
| `DAG.le_card_iterate` | `(∀ j < k, (f^[j] S₀).card < (f^[j + 1] S₀).card) → (f^[0] S₀).card + k ≤ (f^[k] S₀).card` | If each of a specified number of successive applications of a function on finite sets strictly increases cardinality, the final set has grown by at least that number. |
| `DAG.ancClosure_closed` | `x ∈ G.ancClosure v → G.parents x ⊆ G.ancClosure v` | Every parent of a vertex in its computed ancestor set also belongs to that ancestor set. |
| `DAG.isAncestor_mem_of_closed` | `(∀ x ∈ T, G.parents x ⊆ T) → ∀ {u w : V}, G.isAncestor u w → w ∈ T → u ∈ T` | A finite set that contains every parent of each of its vertices contains every ancestor of each vertex it contains. |
| `DAG.mem_ancClosure` | `u ∈ G.ancClosure v ↔ G.isAncestor u v` | Membership in the backward-reachability fixpoint is exactly ancestry: a vertex lies in `G.ancClosure v` iff it is an ancestor of `v`. This makes the ancestor relation decidable using only the (decidable) edge relation, with no reference to any topological order. |
| `DAG.decIsAncestor` | `(G : Causalean.DAG V) → DecidableRel G.isAncestor` | For a finite vertex population with decidable equality and a directed acyclic graph on that population, the decision procedure for the ancestor relation determines, for every ordered pair of vertices, whether the first is an ancestor of the second. |
| `DAG.ancestors` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the ancestor set is the finite set of all vertices from which a directed path reaches that vertex. |
| `DAG.descendants` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the descendant set is the finite set of all vertices reachable from that vertex by a directed path. |
| `DAG.mem_ancestors` | `u ∈ G.ancestors v ↔ G.isAncestor u v` | Membership characterization for `ancestors`: `u ∈ G.ancestors v ↔ G.isAncestor u v`. |
| `DAG.mem_descendants` | `w ∈ G.descendants v ↔ G.isAncestor v w` | Membership characterization for `descendants`: `w ∈ G.descendants v ↔ G.isAncestor v w`. |
| `DAG.parents_subset_ancestors` | `G.parents v ⊆ G.ancestors v` | Parents are a subset of ancestors. |
| `DAG.children_subset_descendants` | `G.children v ⊆ G.descendants v` | Children are a subset of descendants. |
| `DAG.ancestorsSet` | `Finset V → Finset V` | For a finite directed acyclic graph on a vertex population and a finite vertex set, the set of its strict ancestors contains exactly the vertices from which a directed path reaches at least one member of the given set. |
| `DAG.ancestralSet` | `Finset V → Finset V` | For a finite directed acyclic graph on a vertex population and a finite vertex set, its ancestral closure is that set together with every vertex from which a directed path reaches one of its members. |
| `DAG.descendantsSet` | `Finset V → Finset V` | For a finite directed acyclic graph on a vertex population and a finite vertex set, the set of its strict descendants contains exactly the vertices reachable by a directed path from at least one member of the given set. |
| `DAG.nonDescendants` | `V → Finset V` | For a finite directed acyclic graph on a vertex population and a vertex, the non-descendant set contains exactly the vertices other than that vertex which cannot be reached from it by a directed path. |
| `DAG.ancestorRank` | `V → ℕ` | For a finite directed acyclic graph on a vertex population and a vertex, its ancestor rank is the number of that vertex’s strict ancestors. |
| `DAG.ancestorRank_lt_of_edge` | `G.edge a b → G.ancestorRank a < G.ancestorRank b` | Along an edge the strict-ancestor count strictly increases. |
| `DAG.topoOrder` | `V → ℕ` | For a finite directed acyclic graph on a vertex population and a vertex, the derived topological number is its number of strict ancestors times the population size, plus a fixed tie-breaking enumeration number. This number is injective across vertices and strictly increases along every directed edge. |
| `DAG.topoOrder_injective` | `Function.Injective G.topoOrder` | The derived topological order is injective, so it provides a canonical total order on the finite vertex type. |
| `DAG.topoOrder_lt` | `G.edge u v → G.topoOrder u < G.topoOrder v` | The derived topological order is edge-consistent: if there is an edge from `u` to `v`, then `topoOrder u < topoOrder v`. This witnesses acyclicity. |
| `DAG.isAncestor_topoOrder_lt` | `G.isAncestor u v → G.topoOrder u < G.topoOrder v` | Ancestors respect the topological order: if `u` is an ancestor of `v` then `G.topoOrder u < G.topoOrder v`, so ancestor pairs are strictly ordered by `topoOrder`. |
| `DAG.isRoot` | `V → Prop` | For a finite directed acyclic graph on a vertex population and a vertex, the root condition holds exactly when no directed edge enters that vertex. |
| `DAG.decIsRoot` | `(G : Causalean.DAG V) → (v : V) → Decidable (G.isRoot v)` | For a finite vertex population with decidable equality, a directed acyclic graph on that population, and a vertex, the decision procedure for the root condition determines whether no directed edge enters that vertex. |
| `DAG.roots` | `Finset V` | For a finite directed acyclic graph on a vertex population, the root set is the finite set of all vertices with no incoming directed edge. |
| `DAG.acyclic_of_topoOrder` | `(∀ (u v : V), e u v → r (τ u) (τ v)) → ∀ (v : V), ¬Relation.TransGen e v v` | Acyclicity from a topological ranking. Given an edge relation `e` on `V` and a ranking function `τ` into a type equipped with a transitive, irreflexive relation `r`, if `τ` strictly increases (with respect to `r`) along every edge of `e`, then `e` has no directed cycle: no vertex is reachable from itself via the transitive closure of `e`. |
| `DAG.isAncestor_has_parent` | `G.isAncestor u v → G.parents v ≠ ∅` | Every vertex reached by a nonempty directed path has an incoming edge, namely the final edge of that path. |
<!-- /GEN -->

---

## 2. `Graph/DSep/` — d-Separation (Bayes Ball)

**Files:** `DSep/BayesBall.lean` (core BFS algorithm), `DSep/ActivePath.lean` (path correctness), `DSep/Separation.lean` (dSep definition + structural lemmas), `DSep/Ancestral.lean` (ancestral reduction), `DSep/InduceTransport.lean` (induced-SWIG d-separation transport), `DSep/BackdoorBridges.lean` (ID theorem helpers).

### Bayes Ball internals

| Definition | Signature | Description |
|---|---|---|
| `DAG.BBDir` | `inductive` | `.fromParent` or `.fromChild` |
| `DAG.BBState V` | `V × BBDir` | A vertex with arrival direction |
| `DAG.bbZAncestors` | `(Z : Finset V) → Finset V` | `Z` ∪ ancestors of `Z` (for collider activation) |
| `DAG.bbStep` | `(Z : Finset V) → BBState V → Finset (BBState V)` | One Bayes Ball transition step |
| `DAG.bbReachable` | `(Z X : Finset V) → Finset (BBState V)` | Fixed-point BFS from `X` given `Z` |
| `DAG.bbReachableVertices` | `(Z X : Finset V) → Finset V` | Projection to vertices |

### Monotonicity

| Theorem | Signature | Description |
|---|---|---|
| `DAG.bbReachable_mono_source` | `X' ⊆ X → bbReachable Z X' ⊆ bbReachable Z X` | BFS reachability monotone in source (proven) |
| `DAG.bbReachableVertices_mono_source` | `X' ⊆ X → bbReachableVertices Z X' ⊆ bbReachableVertices Z X` | Vertex reachability monotone in source (proven) |
| `DAG.bbReachableVertices_iff_activePath` | `v ∈ bbReachableVertices Z X ↔ ∃ x ∈ X, ∃ p, p.length ≥ 2 ∧ IsActivePath Z p ∧ p.head? = some x ∧ p.getLast? = some v` | Bayes Ball reachability is equivalent to existence of an active path from the source set | **Proved** |
| `DAG.dSep_subset_left` | `X' ⊆ X → dSep X Y Z → dSep X' Y Z` | d-separation monotone in the source set | **Proved** |
| `DAG.bbZAncestors_union_eq` | `bbZAncestors (Z ∪ S) = bbZAncestors Z ∪ bbZAncestors S` | Ancestral set distributes over union (used to split collider-activation witnesses) | **Proved** |
| `DAG.activePath_transfer_cond_to_source` | active path from `x ∈ X` to `w` given `Z ∪ S` → active path from some `x' ∈ X ∪ S` to `w` given `Z` | Path-level surgery lemma underlying source-to-cond transfer; proof sketch in docstring (last-`S` suffix + directed detour at `S`-only-activated colliders) | **Proved** |
| `DAG.dSep_source_to_cond` | `dSep (X ∪ S) Y Z → dSep X Y (Z ∪ S)` | Moves a source set into the conditioning set in d-separation; reduces to `activePath_transfer_cond_to_source` via the BFS↔active-path equivalence | **Proved** |
| `DAG.dSep_mono_conditioningSet` | `(G' : DAG V) → (∀ u v, G'.edge u v → G.edge u v) → G.dSep X Y Z → G'.dSep X Y Z` | d-sep transfers from a supergraph `G` to any subgraph `G'`: fewer edges cannot create new active paths; used for backdoor / Rule 3 to move d-sep from `G(x)` to the split graph `G(x,z)` | **Proved** |
| `DAG.ancestralSet_idem` | `ancestralSet (ancestralSet S) = ancestralSet S` | Ancestral closure is idempotent; used to certify post-intervention ancestral query supports as ancestrally closed | **Proved** |
| `SWIGGraph.dSep_union_fixed_of_induce_dSep` | observed `X,Y,Z ⊆ R ∩ observed` + ancestral closure of `R` + d-sep in `G.induce R` → ambient d-sep given `Z ∪ fixed` | Transport bridge from an ancestral induced SWIG to the ambient SWIG after conditioning on fixed roots; isolates the fixed-node active-path bookkeeping needed by the Markov bridge | **Sorry** |

### d-Separation

| Definition | Signature | Description |
|---|---|---|
| `DAG.dSep` | `(X Y Z : Finset V) → Prop` | `Disjoint (bbReachableVertices Z X) Y` |
| `DAG.decDSep` | `Decidable (G.dSep X Y Z)` | Decidable instance |

**Usage (all decidable by `decide`):**

```lean
-- Z and Y are NOT d-separated by ∅ (active causal path)
example : ¬ivDAG.dSep {Z} {Y} ∅ := by decide

-- Conditioning on collider D opens the backdoor path
example : ¬ivDAG.dSep {Z} {Y} {D} := by decide

-- Conditioning on {D, U} blocks all paths
example : ivDAG.dSep {Z} {Y} {D, U} := by decide

-- Instrument independence: Z ⊥ U | ∅
example : ivDAG.dSep {Z} {U} ∅ := by decide
```

### Backdoor bridges — `Graph/DSep/BackdoorBridges.lean`

Graph-theoretic helpers consumed by `SCM/ID/Backdoor.lean`.

| Theorem | Signature | Description | Status |
|---|---|---|---|
| `DAG.dSep_union_roots_right` | `dSep X Y Z → (∀ r ∈ R, ∀ u, ¬ edge u r) → Disjoint R X → Disjoint R Y → dSep X Y (Z ∪ R)` | Adjoining root vertices (no incoming edges) to the conditioning set preserves d-separation; the Disjoint hypotheses are currently unused (retained for clarity of intent) | **Proved** |

---

## 3. `Graph/SWIG.lean` — SWIG Graph (Definition 4)

### SWIG Node Type

| Definition | Signature | Description |
|---|---|---|
| `SWIGNode` | `inductive` with `random` / `fixed` | Node-level split for each base variable |
| `swigΩ` | `(Ω : N → Type*) → SWIGNode N → Type _` | Shared value space for random/fixed versions |
| `iotaMap` | `SWIGNode N → SWIGNode N` | `fixed n ↦ random n` link map |

### SWIG DAG Construction

| Definition | Signature | Description |
|---|---|---|
| `swigEdge` | `DAG N → Finset N → SWIGNode N → SWIGNode N → Prop` | Edge rewiring under node-splitting |
| `swigTopo` | `DAG N → SWIGNode N → ℕ` | Interleaved topological order |
| `swigDAG` | `DAG N → Finset N → DAG (SWIGNode N)` | SWIG DAG constructor |
| `initialSWIG` | `DAG N → DAG (SWIGNode N)` | No-intervention SWIG (fixed nodes isolated) |

### Structure: `SWIGGraph N` (Definition 4)

A SWIG Graph `G = (S, V, U, E, ι)` — the graph-level causal structure with a three-way
partition and root constraints, factored out as its own structure and later extended
by `Causalean.SCM`.

| Field | Type | Description |
|---|---|---|
| `dag` | `DAG (SWIGNode N)` | The underlying SWIG DAG |
| `fixed` | `Finset (SWIGNode N)` | Fixed (intervention) nodes S |
| `observed` | `Finset (SWIGNode N)` | Observed (endogenous) random nodes V |
| `unobserved` | `Finset (SWIGNode N)` | Unobserved (exogenous) random nodes U |
| `fixed_is_fixed` | `∀ s ∈ fixed, ∃ n, s = SWIGNode.fixed n` | Shape invariant for fixed nodes |
| `observed_is_random` | `∀ v ∈ observed, ∃ n, v = SWIGNode.random n` | Shape invariant for observed nodes |
| `unobserved_is_random` | `∀ u ∈ unobserved, ∃ n, u = SWIGNode.random n` | Shape invariant for unobserved nodes |
| `obs_unobs_disjoint` | `Disjoint observed unobserved` | V and U are disjoint |
| `dag_edges_classified` | `∀ u v, dag.edge u v → u, v ∈ fixed ∪ observed ∪ unobserved` | Every edge endpoint is classified; preserved under `induce` (replaces the older `obs_unobs_cover_random`) |
| `fixed_image_in_observed` | `∀ s ∈ fixed, iotaMap s ∈ observed` | Graph-level image of ι lands in V |
| `fixed_are_roots` | `∀ s ∈ fixed, dag.parents s = ∅` | S are root nodes |
| `unobs_are_roots` | `∀ u ∈ unobserved, dag.parents u = ∅` | U are root nodes |
| `fixed_outside_fixed_isolated` | `∀ n, fixed n ∉ fixed → parents,children = ∅` | Non‑listed fixed nodes are isolated |
| `all_children_in_observed` | `∀ u ∈ U ∪ S ∪ V, children u ⊆ observed` | Children of non‑random nodes lie in V |

### Graph-level ι map

| Definition | Signature | Description |
|---|---|---|
| `SWIGGraph.iota` | `SWIGGraph N → {s // s ∈ fixed} → {v // v ∈ observed}` | Canonical ι : S → V built from `iotaMap` |
| `SWIGGraph.iotaNode` | `SWIGGraph N → {s // s ∈ fixed} → SWIGNode N` | Forgetful version of ι |
| `SWIGGraph.iotaN` | `SWIGGraph N → {n // fixed n ∈ fixed} → {n // random n ∈ observed}` | ι at the base type level |
| `SWIGGraph.isStandard` | `SWIGGraph N → Prop` | `fixed = ∅` (no interventions) |

---

## 3c. `Graph/SWIGSplitMono.lean` — Monolithic Multi-target Split (one-shot)

**Monolithic** multi-target split on a `SWIGGraph`.  Reroutes every
`.random D → w` edge (for `D ∈ X`) to `.fixed D → w` in a single pass rather
than by iterating a single-target split over `X.toList`.

**Why monolithic.**  An iterated single-target form would produce parent sets
that agree with the base graph only *propositionally* at non-targeted vertices,
blocking `rfl`-level reductions in the cross-SCM bridge
`SCM/Do/Rule3.lean:fixSet_evalMap_nonAnc_compat`.
Under the monolithic form, parent sets at non-`.fixed`-targeted vertices
coincide with the base graph's parents *definitionally* (modulo a direct
`splitMono_parents_eq_of_no_fixed_parent` rewrite), which is exactly the
hypothesis of the Rule 3 non-ancestor bridge.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SWIGGraph.splitMonoEdgeRel` | `(_ : SWIGNode N → SWIGNode N → Prop) → Finset N → SWIGNode N → SWIGNode N → Prop` | Edge relation after monolithic split: for each `D ∈ X`, outgoing edges of `.random D` are rerouted to `.fixed D`; all other edges pass through unchanged | Defined |
| `SWIGGraph.splitMonoTopo` | `SWIGGraph N → Finset N → SWIGNode N → ℕ` | Topological order for the monolithic split: `.fixed D` (`D ∈ X`) gets even value `2 · topoOrder (.random D)`; all others get odd values; even/odd separation guarantees injectivity | Defined |
| `SWIGGraph.splitMonoDAG` | `SWIGGraph N → (X : Finset N) → (hIso : ∀ D ∈ X, parents (.fixed D) = ∅) → DAG (SWIGNode N)` | DAG on `SWIGNode N` after monolithic split, built from `splitMonoEdgeRel` and `splitMonoTopo` | Defined |
| `SWIGGraph.splitMono G X hObs hFix` | `SWIGGraph N` | Monolithic multi-target split: applies `splitMonoDAG`; preserves `observed`, `unobserved`; enlarges `fixed` by `X.image SWIGNode.fixed` | Defined |
| `SWIGGraph.splitMono_observed` *(@[simp])* | `(G.splitMono X _ _).observed = G.observed` | Monolithic split preserves the observed node set | `rfl` |
| `SWIGGraph.splitMono_unobserved` *(@[simp])* | `(G.splitMono X _ _).unobserved = G.unobserved` | Monolithic split preserves the unobserved node set | `rfl` |
| `SWIGGraph.splitMono_fixed` *(@[simp])* | `(G.splitMono X _ _).fixed = G.fixed ∪ X.image SWIGNode.fixed` | Fixed set after monolithic split is the original fixed set union the image of `X` under `.fixed` | `rfl` |
| `SWIGGraph.splitMono_parents_char` | `x ∈ (G.splitMono X _ _).dag.parents v ↔ (x ∈ G.dag.parents v ∧ ∀ D ∈ X, x ≠ SWIGNode.random D) ∨ (∃ D ∈ X, x = SWIGNode.fixed D ∧ SWIGNode.random D ∈ G.dag.parents v)` | Characterizes parents in the split graph: either a non-targeted original parent, or a new `.fixed D` replacing `.random D` | **Proved** |
| `SWIGGraph.splitMono_parents_eq_of_no_fixed_parent` | `(∀ D ∈ X, SWIGNode.fixed D ∉ (G.splitMono X _ _).dag.parents v) → (G.splitMono X _ _).dag.parents v = G.dag.parents v` | Parent-set coincidence at non-`.fixed`-targeted vertices; key prerequisite for the Rule 3 non-ancestor `evalMap` bridge | **Proved** |

Reference: Basic Concepts.tex, Definition 8 (multi-target generalized intervention).

---

## 4. `Graph/CComponents.lean` — C-Components (on SWIGGraph)

C-components are defined on `SWIGGraph`, matching Definition 5 from the tex:
"The fixed nodes S play no role in this definition."

### Confounding

| Definition | Signature | Description |
|---|---|---|
| `SWIGGraph.directlyConfounded` | `(v₁ v₂ : SWIGNode N) → Prop` | Share an unobserved parent; decidable |
| `SWIGGraph.bidirectedNeighbors` | `(v : SWIGNode N) → Finset (SWIGNode N)` | All observed variables directly confounded with `v` |
| `SWIGGraph.bidirectedReachable` | `SWIGNode N → SWIGNode N → Prop` (inductive) | Transitive closure of directly confounded |

### C-component computation

| Definition | Signature | Description |
|---|---|---|
| `SWIGGraph.bidirectedBFS` | `(start : SWIGNode N) → Finset (SWIGNode N)` | BFS on bidirected projection |
| `SWIGGraph.cComponentOf` | `(v : SWIGNode N) → Finset (SWIGNode N)` | C-component containing `v` |
| `SWIGGraph.cComponents` | `Array (Finset (SWIGNode N))` (noncomputable) | All C-components (ordered) |
| `SWIGGraph.cComponentSet` | `Finset (Finset (SWIGNode N))` (noncomputable) | All C-components as `observed.image cComponentOf` — the canonical, order-independent partition index used by `c_component_factorization` |

### Partition / reachability results (all proven, axiom-clean)

| Result | Statement | Status |
|---|---|---|
| `bidirectedReachable_symm` / `_trans` / `_head` / `_observed_left` / `_observed_right` | bidirected reachability is an equivalence relation on `observed` | **Proved** |
| `mem_bidirectedBFS_iff_reachable` | `w ∈ cComponentOf start ↔ bidirectedReachable start w` (BFS correctness: the fuel-bounded search computes exactly the bidirected-reachable set) | **Proved** (fuel-saturation closure) |
| `cComponentSet_biUnion` | `cComponentSet.biUnion id = observed` — the c-components cover the observed nodes | **Proved** |
| `cComponentSet_pairwise_disjoint` | distinct c-components are disjoint | **Proved** |
| `cComponentSet_subset_observed`, `cComponentOf_subset_observed`, `mem_cComponentOf_self` | each component ⊆ observed; every observed node is in its own component | **Proved** |

**Usage:**

```lean
-- D and Y are in the same C-component (confounded via U)
example : SWIGNode.random Y ∈ ivSWIGGraph.cComponentOf (SWIGNode.random D) := by native_decide

-- Z is in its own singleton C-component
example : ivSWIGGraph.cComponentOf (SWIGNode.random Z) = {SWIGNode.random Z} := by native_decide
```

---

## 4b. `Graph/MarkovEquiv/` — Markov Equivalence of DAGs (Verma–Pearl)

Formalization of the Verma–Pearl (1990) characterization: two DAGs declare the same
d-separations iff they share a skeleton and the same v-structures. Umbrella:
`Graph/MarkovEquiv.lean`. Reuses the d-separation engine (`Graph/DSep/`) and the global
Markov property (`SCM/Do/GlobalMarkov.lean`).

### `Graph/MarkovEquiv/Defs.lean` — core relations

| Declaration | Description |
|---|---|
| `DAG.IsImmorality G a b c` | v-structure (immorality) `a → b ← c`: edges `a→b`, `c→b` with `a,c` non-adjacent and distinct. Decidable. |
| `SameSkeleton G₁ G₂` | the two DAGs have the same undirected adjacency (`UAdj`). Decidable. |
| `SameImmoralities G₁ G₂` | the two DAGs have the same v-structures. Decidable. |
| `MarkovEquiv G₁ G₂` | the two DAGs declare the same d-separations on pairwise-disjoint triples: `∀ X Y Z, Disjoint X Y → Disjoint X Z → Disjoint Y Z → (G₁.dSep X Y Z ↔ G₂.dSep X Y Z)` (the standard conditional-independence setting). |
| `MarkovEquiv.refl/symm/trans` | `MarkovEquiv` is an equivalence relation. |

### `Graph/MarkovEquiv/Readoff.lean` — easy direction (read off d-sep)

| Declaration | Description |
|---|---|
| `DAG.not_dSeparable_of_uAdj` | adjacent vertices are not d-separated by any conditioning set (the edge is an always-active path). |
| `DAG.dSeparable_of_not_uAdj` | non-adjacent vertices are d-separated by some set (the parents of the topologically later one). |
| `DAG.adjacent_iff_not_dSeparable` | skeleton read-off: `UAdj a b ↔ ¬ ∃ Z, dSep {a} {b} Z`. |
| `DAG.immorality_iff_colliderSep` | collider read-off: for an unshielded triple, `a→b←c` is a v-structure iff `b` is in no separating set of `a,c` (the PC-algorithm rule). |
| `DAG.immorality_iff_colliderSep_disjoint` | endpoint-disjoint collider read-off: same, restricted to separators containing `b` and excluding `a,c` (the form transported across Markov-equivalent graphs). |
| `DAG.dSeparable_disjoint_of_not_uAdj` | non-adjacent distinct vertices are d-separated by a set disjoint from both (parents of the topologically later one). |
| `DAG.not_edge_self`, `DAG.not_uAdj_self` | no self-loops / self-adjacency in a DAG. |
| `sameSkeleton_of_markovEquiv` | Markov equivalence ⇒ same skeleton. |
| `sameImmoralities_of_markovEquiv` | Markov equivalence ⇒ same v-structures. |
| `sameSkeleton_sameImmoralities_of_markovEquiv` | easy direction of Verma–Pearl (both at once). |

### `Graph/MarkovEquiv/Moralization.lean` — moral graph + moralization criterion

| Declaration | Description |
|---|---|
| `DAG.MoralAdj G S u v` | moral adjacency inside a ground set `S`: distinct `u,v ∈ S` that are skeleton-adjacent (`UAdj`) or share a common child inside `S` ("married parents"). |
| `DAG.MoralStep G S Z u v` | one moral step inside `S` with both endpoints outside the conditioning set `Z`. |
| `DAG.MoralConn G S Z u v` | moral connectivity: a (reflexive-transitive) chain of moral steps inside `S` avoiding `Z`. |
| `DAG.MoralSep G X Y Z` | moral separation: no `x ∈ X` is moral-connected to any `y ∈ Y` inside `An(X∪Y∪Z)` while avoiding `Z`. |
| `DAG.moralAdj_symm` | moral adjacency is symmetric. |
| `DAG.dSep_iff_moralSep` | **the criterion** (Lauritzen–Dawid–Larsen–Speed): for pairwise-disjoint `X,Y,Z`, d-separation equals moral separation in the ancestral set. |
| `moralAdj_congr` | moral adjacency is a skeleton + v-structure invariant for any fixed ground set `S`. |
| `moralStep_congr`, `moralConn_congr` | moral steps / connectivity agree across same-skeleton + same-immorality DAGs, **for a fixed ground set**. |

### `Graph/MarkovEquiv/Transfer.lean` — hard direction (via covered-edge reversals)

| Declaration | Description |
|---|---|
| `markovEquiv_of_sameSkeleton_sameImmoralities` | same skeleton + same v-structures ⇒ Markov equivalent. The proof uses the AMP covered-edge route: same-skeleton/same-immorality DAGs differ by covered-edge reversals, and each reversal preserves every d-separation. |

### `Graph/MarkovEquiv.lean` — umbrella + flagship

| Declaration | Description |
|---|---|
| `markovEquiv_iff_sameSkeleton_sameImmoralities` | **Flagship (Verma–Pearl).** Two DAGs are Markov equivalent iff they have the same skeleton and the same v-structures. Both directions are proved through the read-off and covered-edge reversal routes. |

### `Graph/MarkovEquiv/Distributional.lean` — distributional layer

| Declaration | Description |
|---|---|
| `SCM.IsGlobalIMap G M μ` | the measure `μ` is a global I-map of DAG `G`: every d-separation of `G` (on disjoint `X,Y,Z`) is a conditional independence of `μ`. |
| `SCM.IsFaithful G M μ` | converse: every conditional independence of `μ` reflects a d-separation of `G`. |
| `SCM.DistMarkovEquiv Ω G₁ G₂` | the two DAGs are global I-maps of exactly the same distributions (over value spaces `Ω`). |
| `SCM.isGlobalIMap_dag_self` | the bridge, restated: every SCM is a global I-map of its own DAG (= `full_globalMarkov`). |
| `SCM.distMarkovEquiv_of_markovEquiv` | easy half: graph-level Markov equivalence ⇒ distributional Markov equivalence. The abandoned faithfulness-existence converse is no longer part of the public module. |

---

## 5. `SCM/Model/EdgeType.lean` — Edge Type Hierarchy

### Inductive: `MonotonicityKind`

```
nonDecreasing | nonIncreasing | strictlyIncreasing | strictlyDecreasing
```

### Inductive: `EdgeType`

```
nonparametric | monotonic (kind : MonotonicityKind) | linear | parametric
```

### Refinement relation

| Definition | Signature | Description |
|---|---|---|
| `EdgeType.refinesBool` | `EdgeType → EdgeType → Bool` | Computable refinement check |
| `EdgeType.refines` | `EdgeType → EdgeType → Prop` | `e₁.refinesBool e₂ = true` |
| `EdgeType.decRefines` | `Decidable (e₁.refines e₂)` | Decidability instance |

### Structure: `EdgeTypeAssignment G`

| Field | Type | Description |
|---|---|---|
| `edgeType` | `V → V → EdgeType` | Type of each edge (meaningful when `G.edge u v`) |

---

## 6. `SCM/Model/SCM.lean` — Generalized Structural Causal Model (Definition `def:scm`)

### Structure: `SCM N Ω`

A generalized structural causal model (gSCM) extending `SWIGGraph N` with
deterministic structural functions per observed node and one probability
measure per latent root. Randomness lives only in the latent roots; the
joint / observational kernels are derived via the evaluation map (see
`SCM/Model/Evaluation.lean`, `SCM/Model/Kernel.lean`).

#### Value-space infrastructure (file-local, shared with the rest of the SCM stack)

| Definition | Signature | Description |
|---|---|---|
| `ValuesOn I Ω` | `Finset M → (M → Type*) → Type*` | Dependent product of value spaces over a finite index set |
| `valuesProjection hJI` | `ValuesOn I Ω → ValuesOn J Ω` | Restrict an indexed assignment to a subset `J ⊆ I` |
| `coordinateFamily I` | `∀ i : {i // i ∈ I}, ValuesOn I Ω → Ω i.val` | Coordinate family on `ValuesOn I Ω` |
| `valuesEquivOfEq h` | `ValuesOn I Ω ≃ᵐ ValuesOn J Ω` (where `h : I = J`) | `MeasurableEquiv` canonical transport along an index-`Finset` equality; `toFun` is `valuesProjection (le_of_eq h.symm)` so call sites can rewrite the projection into the equiv by `rfl` |
| `measurePreserving_valuesEquivOfEq h μ` | `MeasurePreserving (valuesEquivOfEq h) (Measure.pi μ) (Measure.pi μ')` | Companion: `Measure.pi` transports along `valuesEquivOfEq`, with target per-coord measure `μ' j = μ ⟨j.val, h ▸ j.property⟩`.  Proved via `subst h`; collapses the two `Measure.pi`'s to the same source after the substitution |

#### Fields (beyond the `SWIGGraph N` parent)

| Field | Type | Description |
|---|---|---|
| `edgeTypes` | `EdgeTypeAssignment dag` | Edge type assignment, orthogonal to semantics |
| `iota_valueSpace` | `∀ s ∈ fixed, swigΩ Ω s = swigΩ Ω (iotaMap s)` | Matching value spaces `X_d = X_{ι(d)}` |
| `structFun v` | `(∀ w ∈ parents v, swigΩ Ω w) → swigΩ Ω v` | Deterministic structural function per observed node |
| `structFun_measurable` | `∀ v, Measurable (structFun v)` | Each structural map is measurable |
| `latentDist L` | `Measure (swigΩ Ω L)` | One probability measure per latent root `L ∈ U` |
| `isProbability_latent` | `∀ L, IsProbabilityMeasure (latentDist L)` | Total mass 1 on each latent |

#### Derived latent product measure

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.latentProduct` | `Measure (LatentValues M)` | Product measure over all latent roots, `Measure.pi (fun u => latentDist u)` | *(sorry — TODO(evaluation))* |
| `instProbabilityLatentProduct` | `IsProbabilityMeasure latentProduct` | Total-mass-1 instance for `latentProduct` | *(sorry — TODO(evaluation))* |

#### Type aliases and basic definitions

| Name | Expands to / Signature | Description |
|---|---|---|
| `FixedValues M` | `ValuesOn M.fixed (swigΩ Ω)` | Values of intervention nodes |
| `ObservedValues M` | `ValuesOn M.observed (swigΩ Ω)` | Values of observed nodes |
| `LatentValues M` | `ValuesOn M.unobserved (swigΩ Ω)` | Values of latent (unobserved) nodes |
| `UnobservedValues M` | alias of `LatentValues M` | Deprecated alias preserved for compatibility |
| `RandomValues M` | `ValuesOn M.randomVars (swigΩ Ω)` | Values over `V ∪ L` |
| `randomVars M` | `M.observed ∪ M.unobserved` | Index set of non-fixed nodes |
| `isStandard M` | `Prop` | `M.fixed = ∅` — no intervention |
| `not_unobs_of_obs` | `n ∈ observed → n ∉ unobserved` | Disjointness helper |

#### Topological-order utilities

| Name | Signature | Description |
|---|---|---|
| `topoLinearOrder M` | `LinearOrder (SWIGNode N)` | Canonical linear order lifted from `dag.topoOrder` (noncomputable) |
| `observedAt M i` | `Fin observed.card → {v // v ∈ observed}` | `i`-th observed node in canonical topological order |
| `observedIndex M v` | `{v // v ∈ observed} → Fin observed.card` | Canonical index of an observed node |
| `observedAt_observedIndex` | Round-trip equality | `(M.observedAt (M.observedIndex v)).val = v.val` |
| `observed_parent_index_lt` | `edge p (observedAt ⟨n, _⟩) → observedIndex p < ⟨n, _⟩` | Observed parents appear strictly earlier in the prefix |

#### Structural equivalence

| Name | Signature | Description |
|---|---|---|
| `SCM.Equiv M₁ M₂` | `Prop` | Conjunction of `SWIGGraph.Equivalent` on the graph layer, edgewise agreement on `edgeTypes`, and `HEq` on `structFun` / `latentDist` |
| `Equiv.refl` / `.symm` / `.trans` | Equivalence closure | Refl and symm are explicit; trans chains the graph-equivalence |
| `instSetoidSCM` | `Setoid (SCM N Ω)` | Registered setoid for `SCM.Equiv` |

---

## 6a. `SCM/Model/Evaluation.lean` — Evaluation Map (Definition `def:scm-eval`)

Evaluation map `φ_M : FixedValues M × LatentValues M → RandomValues M` computed
by strong recursion along `M.topoLinearOrder` via the helper `evalObservedAux`
(observed nodes) plus a direct latent-root projection. The recursion at each
observed index applies `M.structFun` to a parent tuple assembled by the helper
`parentMap`, which classifies each parent via `M.dag_edges_classified` into
fixed / latent / observed-recursion branches.

`parentMap`, `parentMap_{unobserved,fixed,observed}`, `evalObservedAux`,
`evalObservedAux_eq`, `evalMap_observed`, and `evalMap_unobserved` are exposed
as **non-private** (formerly private) so that `Induced.lean`'s bridge lemma
`induce_evalMap_compat` can reuse them without duplication.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.parentMap` | `(s, ℓ, hn : n < M.observed.card, prev : ∀ m < n, …) → ∀ w ∈ parents (observedAt ⟨n,hn⟩), swigΩ Ω w.val` | One recursion step: given a strong-induction hypothesis `prev` supplying values at every strictly earlier observed index, returns the parent-value tuple to feed into `M.structFun` at the `n`-th observed node. Each parent `w` is classified via `dag_edges_classified` into unobserved (read from `ℓ`), fixed (read from `s`), or observed (recurse through `prev` at the parent's topological index, strictly `< n` by `observed_parent_index_lt`) | **Defined** |
| `SCM.parentMap_unobserved` | `w.val ∈ M.unobserved → parentMap … w = ℓ ⟨w.val, huo⟩` | Branch-unfold lemma: rewrites `parentMap … w` to the latent value `ℓ ⟨w.val, huo⟩` under the hypothesis that `w.val ∈ M.unobserved`. Used coordinate-wise in every downstream structural induction | **Proved** via `dif_pos` |
| `SCM.parentMap_fixed` | `w.val ∉ M.unobserved → w.val ∈ M.fixed → parentMap … w = s ⟨w.val, hfix⟩` | Branch-unfold lemma: rewrites `parentMap … w` to the fixed value `s ⟨w.val, hfix⟩` on the fixed branch. Requires both the negation of the unobserved branch and fixed-membership | **Proved** via `dif_neg`/`dif_pos` |
| `SCM.parentMap_observed` | `w.val ∉ M.unobserved → w.val ∉ M.fixed → w.val ∈ M.observed → parentMap … w = (observedAt_observedIndex) ▸ prev (observedIndex w).val …` | Branch-unfold lemma for the observed-recursion branch: rewrites `parentMap … w` to the transported `prev` value at `w`'s topological index. The `▸` cast moves `swigΩ Ω (observedAt (observedIndex w)).val` to `swigΩ Ω w.val` | **Proved** via `dif_neg`/`dif_neg` |
| `SCM.evalObservedAux` | `(s, ℓ, n : ℕ) → ∀ hn : n < M.observed.card, swigΩ Ω (observedAt ⟨n,hn⟩).val` | Strong-recursive workhorse indexed by the *natural number* `n` (where strong recursion is available) rather than by a Subtype. Returns the value of the `n`-th observed node as `structFun (observedAt ⟨n,hn⟩)` applied to the tuple assembled by `parentMap` from earlier observed values | **Defined** via `Nat.strongRecOn'` |
| `SCM.evalObservedAux_eq` | `evalObservedAux M s ℓ n hn = structFun (observedAt ⟨n,hn⟩) (fun w => parentMap M s ℓ hn (fun m _ hm => evalObservedAux M s ℓ m hm) w)` | Canonical β-reduced unfold equation for `evalObservedAux` at a given `n`. Every proof that does structural induction over the topological order (`evalObservedAux_measurable`, `evalObservedAux_agree_anc`, `evalObservedAux_eq_structFunAt`) starts with a rewrite by this lemma | **Proved** via `Nat.strongRecOn'_beta` |
| `SCM.evalMap_observed` | `w.val ∈ M.observed → evalMap s ℓ w = (observedAt_observedIndex) ▸ evalObservedAux M s ℓ (observedIndex w).val …` | Unfolds `evalMap s ℓ w` on the *observed* branch to a transported `evalObservedAux` value at `observedIndex w`. The `▸` cast along `observedAt_observedIndex : (observedAt (observedIndex w)).val = w.val` brings the type `swigΩ Ω (observedAt (observedIndex w)).val` back to `swigΩ Ω w.val` | **Proved** via `dif_pos` |
| `SCM.evalMap_unobserved` | `w.val ∈ M.unobserved → evalMap s ℓ w = ℓ ⟨w.val, huo⟩` | Unfolds `evalMap s ℓ w` on the *latent* branch to `ℓ ⟨w.val, huo⟩`. Uses `not_obs_of_unobs` to discharge the observed-branch hypothesis of `evalMap`'s definitional `dif_neg` | **Proved** via `dif_neg` |
| `SCM.evalMap` | `FixedValues M → LatentValues M → RandomValues M` | The top-level evaluation map. Pointwise over `w : {w // w ∈ M.randomVars}`: observed branch calls `evalObservedAux` at `observedIndex w` (with `▸` cast); latent branch projects `ℓ` at `w` | **Proved** |
| `SCM.evalMap_measurable` | `Measurable (Function.uncurry M.evalMap)` | Joint measurability of `evalMap` in `(s, ℓ)`, needed to define `jointKernel` in `Kernel.lean`. Proven by structural induction on the observed topological index using the private helper `evalObservedAux_measurable`; relies on `structFun_measurable` and the three `parentMap_*` unfold lemmas | **Proved** |
| `parentDispatch` *(private)* | `(v : {v // v ∈ M.observed}) → (∀ w ∈ parents v.val, swigΩ Ω w.val)` | Named extraction of the three-way if-else parent tuple. Avoids inlining the chain in the helpers below | — |
| `evalObservedAux_eq_structFunAt` *(private)* | `evalObservedAux M s ℓ j.val j.isLt = M.structFun (M.observedAt j) (parentDispatch M s ℓ (M.observedAt j))` for a *free* `j : Fin M.observed.card` | Cast-free intermediate used on the way to `evalMap_observed_unfold`: states `evalObservedAux = structFun ∘ parentDispatch` at a *free* `Fin` index `j`, so no dependent-motive `▸` cast blocks the rewrite | **Proved** — observed-parent branch closes by `rfl` since both `parentMap_observed` and `evalMap_observed` produce the *same* `▸`-form |
| `evalObservedAux_cast_eq_structFunAt` *(private)* | `hcast ▸ evalObservedAux M s ℓ k.val k.isLt = M.structFun (M.observedAt j) (parentDispatch …)` given `(hkj : k = j)` | Cast-navigation helper: lifts the free-index equation above to a pair `(j, k)` of Fin indices with `k = j`, through a `.val`-level cast `hcast` | **Proved.** Uses `subst k` to eliminate the Fin mismatch, then `Subsingleton.elim` to replace the reflexive cast proof with `rfl`, collapsing the `▸` |
| `SCM.evalMap_observed_unfold` | `M.evalMap s ℓ ⟨v.val, _⟩ = M.structFun v (fun w => if-else dispatch with recursive `M.evalMap` on observed parents)` for `v : {v // v ∈ M.observed}` | Cast-free "recursive form" of `M.evalMap` at observed nodes, stated directly in terms of `M.evalMap` on parents rather than `evalObservedAux`. Consumed by `Induced.induce_evalMap_compat` as a black box | **Proved (Layer 4).** Uses a `suffices` with `subst hw` to eliminate the circular `v ↔ M.observedIndex v` dependency, then the cast-navigation helper above |
| `SCM.ancestralFactorization` | `∀ (T : Finset (SWIGNode N)), T ⊆ M.observed → (s, s', ℓ, ℓ' agree on fixed/latent ancestors of T via `DAG.isAncestor`) → ∀ v ∈ T, evalMap s ℓ v = evalMap s' ℓ' v` | Congruence form of `lem:scm-ancestral-factor`: `evalMap` at any `v ∈ T` depends on `(s, ℓ)` only through the fixed/latent ancestors of `T`. Uses the inductive `DAG.isAncestor` directly (the Finset `DAG.ancestorsSet` is sorry-tainted via `decIsAncestor`) | **Proved (Layer 4).** Strong recursion via the private helper `evalObservedAux_agree_anc` on the topological index, chaining ancestor witnesses through `isAncestor.edge` + `isAncestor_trans` at each parent branch |
| `SCM.observedIndex_observedAt` | `M.observedIndex (M.observedAt k) = k` for `k : Fin M.observed.card` | Round-trip lemma companion to `observedAt_observedIndex`; needed to collapse `observedIndex` after destructuring through the topological enumeration | **Proved (Layer 4)** via the `orderIsoOfFin` round-trip |
| `SCM.evalMap_topo_indep` | `True` | Placeholder for `prop:scm-evalmap` (independence of `evalMap` from the choice of topological extension). Downstream (`GlobalMarkov`, `DoCalculus`) only depends on the canonical `evalMap` tied to `M.topoLinearOrder`, so the refactor is safely deferred | *(deferred, Layer 5+)* — requires first refactoring `evalMap` to take an explicit linear extension of `M.dag` |

*Factorization lemmas (§5–§6) are in `EvalFactorization.lean`; latent-restricted factorization (§7) is in `EvalLatent.lean`; cross-SCM transport (§8) is in `EquivKernel.lean`.*

---

## 6a'. `SCM/Model/EvalFactorization.lean` — Parent and Ancestor Factorization

Imports `Evaluation.lean`. Provides two families of factorization theorems for `evalMap`:
* §5 — **parent factorization**: the value at an observed node depends on latents only through parent-restricted random coordinates. Consumed by `LocalMarkov`.
* §6 — **ancestor factorization** (existence form): upgrades `ancestralFactorization` to a two-argument curried `g` form needed by `CondIndepFun` machinery in `GlobalMarkov`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.evalMap_factors_through_parents` | `∀ v ∈ M.observed, ∃ g : ValuesOn (parents v ∩ randomVars) (swigΩ Ω) → swigΩ Ω v, Measurable g ∧ ∀ ℓ, evalMap s ℓ ⟨v,_⟩ = g (valuesProjection _ (evalMap s ℓ))` | Parent factorization: `(evalMap s ℓ) v` is a measurable function of parent-restricted random coordinates. Witness `g` applies `structFun v` to parents read from `s` (fixed) or the projection (random). Consumed by `LocalMarkov` | **Proved** |
| `SCM.latentAncestorsOfNode` | `SCM N Ω → SWIGNode N → Finset (SWIGNode N)` | `{u ∈ M.unobserved ∣ u = v ∨ M.dag.isAncestor u v}`, using `Classical.decPred` to avoid `decIsAncestor` | **Defined** |
| `SCM.fixedAncestorsOfNode` | `SCM N Ω → SWIGNode N → Finset (SWIGNode N)` | `{d ∈ M.fixed ∣ d = v ∨ M.dag.isAncestor d v}`, analogous to `latentAncestorsOfNode` | **Defined** |
| `SCM.mem_latentAncestorsOfNode` | `u ∈ M.latentAncestorsOfNode v ↔ u ∈ M.unobserved ∧ (u = v ∨ isAncestor u v)` | Membership characterisation for `latentAncestorsOfNode` | **Proved** |
| `SCM.mem_fixedAncestorsOfNode` | `d ∈ M.fixedAncestorsOfNode v ↔ d ∈ M.fixed ∧ (d = v ∨ isAncestor d v)` | Membership characterisation for `fixedAncestorsOfNode` | **Proved** |
| `SCM.latentAncestorsOfNode_subset` | `M.latentAncestorsOfNode v ⊆ M.unobserved` | Subset lemma | **Proved** |
| `SCM.fixedAncestorsOfNode_subset` | `M.fixedAncestorsOfNode v ⊆ M.fixed` | Subset lemma | **Proved** |
| `SCM.evalMap_factors_through_ancestors` | `[∀ n, Nonempty (Ω n)] → ∀ v ∈ M.observed, ∃ g : ValuesOn (fixedAncestorsOfNode v) → ValuesOn (latentAncestorsOfNode v) → swigΩ Ω v, Measurable (uncurry g) ∧ ∀ s ℓ, evalMap s ℓ ⟨v,_⟩ = g (proj s) (proj ℓ)` | Ancestor factorization (existence form): two-argument curried witness `g` over fixed-ancestor and latent-ancestor projections. Built via extend-and-evalMap; pointwise equation via `ancestralFactorization` at `T := {v}`. Consumed by `GlobalMarkov` | **Proved** |

---

## 6a''. `SCM/Model/EvalLatent.lean` — Latent-Restricted Factorization

Imports `Evaluation.lean`. Provides §7: for a latent root `a` and a non-descendant set `T`, the projection of `evalMap s` to `T` factors through all latent coordinates *except* `a`. Used by `GlobalMarkov.full_local_markov_latent` to establish independence between the `{a}`-projection and the `T`-projection of `evalMap s`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.evalMap_factors_excluding_latent` | `∀ (a : SWIGNode N) (ha : a ∈ M.unobserved) (T ⊆ randomVars), (∀ v ∈ T, ¬isAncestor a v ∧ v ≠ a) → ∃ g : (∀ i ∈ univ.erase ⟨a,ha⟩, swigΩ Ω i.val.val) → ValuesOn T, Measurable g ∧ ∀ ℓ, valuesProjection hT (evalMap s ℓ) = g (fun i => ℓ i.val)` | Latent-restricted factorization: `valuesProjection hT ∘ evalMap s` factors through latent coordinates excluding `a`. Build: measurable `extend` fills in a default at `a`, copies elsewhere; for observed `v ∈ T` close via `ancestralFactorization` (no `a`-ancestor), for unobserved `v ∈ T` via `evalMap_unobserved` | **Proved** |

---

## 6a''''. `SCM/Model/CutsetLatent.lean` — Latent cutset and structural override factorization

Imports `EvalOverrideC.lean`. Defines the latent cutset `C_W` (latent roots reaching a target `Y` along a directed path whose interior avoids an override block `C`) and proves the keystone structural factorization for the continuous-backdoor witness kernel: the override evaluation `evalMap_overrideC` with override block `C` depends on the latent assignment only through its values on the cutset. No d-separation is needed — latents outside the cutset reach `Y` only through the short-circuited block `C`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `DAG.isAncestorAvoiding` | `DAG V → Finset V → V → V → Prop` (inductive) | A directed path from `u` to `v` whose strictly interior nodes all avoid `C`; built by extending at the target end (`edge`, `trans`) | **Defined** |
| `DAG.isAncestorAvoiding.toIsAncestor` | `isAncestorAvoiding C u v → isAncestor u v` | Forgets interior-avoidance: an avoiding path is an ordinary ancestor | **Proved** |
| `DAG.isAncestorAvoiding.cons` | `edge u w → w ∉ C → isAncestorAvoiding C w v → isAncestorAvoiding C u v` | Prepend an edge at the source end (new interior node `w` avoids `C`) | **Proved** |
| `DAG.isAncestorAvoiding.exists_path` | `isAncestorAvoiding C u v → ∃ q, len ≥ 2 ∧ head = u ∧ last = v ∧ (directed edges) ∧ (interior nodes ∉ C)` | Materialises a concrete forward-directed path list with interior nodes avoiding `C` | **Proved** |
| `SCM.cutsetLatent` | `SCM N Ω → Finset (SWIGNode N) → Finset (SWIGNode N) → Finset (SWIGNode N)` | `C_W := {u ∈ unobserved ∣ ∃ y ∈ Y, u = y ∨ isAncestorAvoiding C u y}` (latent roots driving `Y` while avoiding the override block `C`) | **Defined** |
| `SCM.mem_cutsetLatent` | `u ∈ cutsetLatent Y C ↔ u ∈ unobserved ∧ ∃ y ∈ Y, u = y ∨ isAncestorAvoiding C u y` | Membership characterisation | **Proved** |
| `SCM.cutsetLatent_subset` | `cutsetLatent Y C ⊆ M.unobserved` | Subset lemma | **Proved** |
| `SCM.evalMap_overrideC_agree_cutset` | `valuesProjection (cutsetLatent_subset) ℓ₁ = valuesProjection (cutsetLatent_subset) ℓ₂ → evalMap_overrideC hY hC s c ℓ₁ = evalMap_overrideC hY hC s c ℓ₂` | **Keystone (A):** the override evaluation on `Y` depends on the latent vector only through its cutset projection. Strong recursion on the topological index, classifying each parent (latent in cutset / fixed / in `C` / observed-recurse) | **Proved** |
| `SCM.exists_evalMap_overrideC_factors_cutset` | `[∀ n, Nonempty (Ω n)] → ∃ h : ValuesOn (cutsetLatent Y C) → ValuesOn Y, Measurable h ∧ ∀ ℓ, evalMap_overrideC hY hC s c ℓ = h (valuesProjection (cutsetLatent_subset) ℓ)` | Genuine factorization form of (A): a single measurable map of the cutset projection. Built by extend-and-evaluate + the keystone agreement | **Proved** |

---

## 6a'''''. `SCM/Do/CutsetDSep.lean` — Concatenation d-separation for the latent cutset

Imports `CutsetLatent.lean` and `GlobalMarkov.lean`. Transfers d-separation from a target `Y` to the latent cutset that drives it: if `Y ⊥ Zr ∣ (W ∪ F)` then `cutsetLatent Y (Zr ∪ W) ⊥ Zr ∣ (W ∪ F)`. The proof concatenates an active `Zr → c` path with the directed cutset arm `c → … → Y` at the latent fork `c`; the seam is a fork (so the glued path is active) and the arm's interior avoids `W` (via the cutset) and `F` (interior nodes have parents, so they are not fixed roots), contradicting the assumed separation.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.cutsetLatent_dSep_of_dSep` | `(W ⊆ observed) → (F ⊆ fixed) → M.dag.dSep Y Zr (W ∪ F) → M.dag.dSep (cutsetLatent Y (Zr ∪ W)) Zr (W ∪ F)` | **Concatenation d-sep (B):** d-separation transfers from `Y` to its latent cutset. Active-path + directed-arm fork concatenation via `DAG.bbReachable_extend_directed_arm`, contradicting `dSep Y Zr (W∪F)` | **Proved** |
| `SCM.cutsetLatent_dSep_of_fixSet_dSep` | `(W ⊆ observed) → (∀ D∈Z, ∀ w∈W, ¬ ancestor (random D) w) → (fixSet Z).dag.dSep Y Zr (W ∪ (fixSet Z).fixed) → M.dag.dSep (cutsetLatent Y (Zr ∪ W)) Zr (W ∪ M.fixed)` | **Cross-model concatenation d-sep:** post-intervention separation of `Y` from `Zr` in `fixSet Z`, plus backdoor criterion (i) for `W`, yields base-graph latent-cutset separation. A minimal base-graph active `Zr ⤳ Y` path is built (cutset node `c` is a latent root/fork; minimality + the root-fork forward-run argument rule out every treatment out-edge), then transported edge-by-edge into `fixSet Z` | **Proved** |

---

## 6a''''''. `Graph/DSep/OrderedLocalSG.lean` (addendum) — active+directed-arm join

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `DAG.bbReachable_extend_directed_arm` | `IsActivePath Z pa (a→c) → (forward-directed q : c→b, interior ∉ Z) → c ∉ Z → b ∈ bbReachableVertices Z {a}` | Public wrapper extending an active path by a directed arm at a non-collider (fork/chain) seam, gluing via the file-private `chain_join_active`. Used by `cutsetLatent_dSep_of_dSep` | **Proved** |

---

## 6a'''. `SCM/Model/CounterfactualLemmas.lean` — Pathwise CF identities for the PO bridge

Imports `Evaluation.lean` and `InterventionSet.lean`. Provides one-step observed-node unfold lemmas for do-intervened SCMs, plus two pathwise pushforward identities used by `PO/Bridge/FromSCM.lean` to discharge `POSystem.ofSCM_consistency`. The pushforward identities correspond to natural-language propositions in `doc/Basic Concepts.tex` (lines 475–487) and are proved by pathwise induction along the topological order in `Evaluation.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.fixMono_structFun_apply` *(@[simp])* | `(M.fixMono X hObs hFix).structFun v ξ = M.structFun ⟨v.val, _⟩ (fixMonoParentMap M.toSWIGGraph X hObs hFix v.val ξ)` | Exposes the definitional structural equation of a monolithic intervention: the post-intervention node uses the original structural equation with parent values reindexed through the do-parent map | `rfl` |
| `SCM.fixSet_structFun_apply` *(@[simp])* | `(M.fixSet X hObs hFix).structFun v ξ = M.structFun ⟨v.val, _⟩ (fixMonoParentMap M.toSWIGGraph X hObs hFix v.val ξ)` | `fixSet` wrapper for the same structural-equation exposure, used by consumers that work through the public intervention interface | `rfl` |
| `SCM.evalMap_fixSet_observed_apply` | for `v ∈ M.observed`, one observed evaluation step of `(M.fixSet X _ _)` equals `M.structFun v` applied to the `fixMonoParentMap`-reindexed recursive parent tuple | Packaged one-step SCM→do bridge: combines `evalMap_observed_unfold` with `fixSet_structFun_apply`, so downstream proofs can expose the original structural equation without unfolding `fixSet`/`fixMono` manually | **Proved** |
| `SCM.evalMap_fixSet_factual_eq` | for `M`, `X ⊆ N`, `s`, `sx`, `ℓ`, `v ∈ M.observed`, `v ∉ X` (pointwise): `(∀ v ∈ M.fixed, sx v = s v) → (∀ D ∈ X, M.evalMap s ℓ ⟨.random D, _⟩ = sx ⟨.fixed D, _⟩) → (M.fixSet X _ _).evalMap sx ℓ ⟨v, _⟩ = M.evalMap s ℓ ⟨v, _⟩` | prop:scm-cf-consistency: on the event the natural value of `X` equals the intervention assignment, evaluating `M.fixSet X` agrees pathwise with evaluating `M` on every observed node disjoint from `X` | **Proved** |
| `SCM.evalMap_fixSet_union_eq` | for `M`, disjoint `X₁`, `X₂`, `s`, `sx₁`, `sxU`, `ℓ`, `v ∈ M.observed`, `v ∉ X₁ ∪ X₂` (pointwise): with old/new compatibility hypotheses + intermediate factual: `(M.fixSet (X₁ ∪ X₂) _ _).evalMap sxU ℓ ⟨v, _⟩ = (M.fixSet X₁ _ _).evalMap sx₁ ℓ ⟨v, _⟩` | prop:scm-cf-commute: intervening on `X₁ ∪ X₂` matches first intervening on `X₁` then on `X₂`, provided the natural value of `X₂` after the `X₁` intervention already equals the `X₂` assignment | **Proved** |

Reference: `Basic Concepts.tex`, prop:scm-cf-consistency (L475–480), prop:scm-cf-commute (L482–487).

---

## 6b. `SCM/Model/Kernel.lean` — Joint and observational kernels (core)

Given a gSCM `M`, the joint kernel sends each fixed-value assignment `s` to
the pushforward of `M.latentProduct` along `fun ℓ ↦ M.evalMap s ℓ`. It is
built as `(Kernel.const _ M.latentProduct ⊗ₖ Kernel.deterministic (uncurry evalMap) …).map Prod.snd` — i.e. the `(ℓ, evalMap s ℓ)` joint law, projected to
the second factor. The observational kernel is then the `randomToObserved`
pushforward of `jointKernel`.

**This file is intentionally minimal:** do-calculus Rule 2 / Rule 3 kernel
statements and their value-space helpers live in `SCM/Do/Rule2.lean`
and `SCM/Do/Rule3.lean` (see §6b1–§6b2).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.jointKernel` | `Kernel (FixedValues M) (RandomValues M)` | Kernel mapping each fixed-value assignment to the law of `evalMap s` pushed forward through `latentProduct` | noncomputable |
| `SCM.randomToObserved` | `RandomValues M → ObservedValues M` | Projects a full random assignment to its observed-only coordinates | Defined |
| `SCM.measurable_randomToObserved` | `Measurable randomToObserved` | Measurability of the observed-projection map | **Proved** |
| `SCM.jointKernel_apply_eq` | `M.jointKernel s = M.latentProduct.map (fun ℓ => M.evalMap s ℓ)` | Collapses the `compProd`-of-`const`-and-`deterministic` definition to a direct `Measure.map` | **Proved (Layer 4)** |
| `SCM.obsKernel` | `Kernel (FixedValues M) (ObservedValues M)` | Observational kernel: pushforward of `jointKernel` along the observed projection | noncomputable |
| `SCM.jointKernel_map_commute` | `M.obsKernel = (const latentProduct ⊗ₖ deterministic (uncurry evalMap) _).map (randomToObserved ∘ Prod.snd)` | Rewrites `obsKernel` as a single-step push-and-project form via `map_comp_right` | **Proved (Layer 4)** |

Reference: Basic Concepts.tex, Definition `def:scm-joint`; `rem:scm-docalculus-lean` bullet 3 for the Step B escalation.

> **Archive note.** The old kernel-primitive sections `8a. Causal/Kernel/PoSet.lean`, `8b. Causal/Kernel/KernelOps.lean`, and `8e. Causal/Kernel/CrossModel.lean` have been removed. Their contents — iterated Po fold, kernel projection / marginal utilities, cross-model `compProd` analyses — live in `archive/Causal/Kernel/` for historical reference and will be recovered against the SCM primitives in a later pass.

---

## 6b'. `SCM/Model/EquivKernel.lean` — HEq transport of kernels across `SCM.Equiv`

All `SCM.Equiv`-related transport lemmas, from the fundamental `evalMap`-level agreement (§8) through the kernel-level HEq wrappers. Imports `Kernel.lean` (which transitively imports `Evaluation.lean`).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.structFun_apply_eq_of_equiv` *(private)* | `SWIGGraph.Equivalent M₁ M₂ → HEq structFun₁ structFun₂ → (parent tuples agree pointwise) → M₁.structFun ⟨v,_⟩ ξ₁ = M₂.structFun ⟨v,_⟩ ξ₂` | HEq-application equality for `structFun` across `Equiv`-related SCMs. Uses `dcongr_heq` + `Function.hfunext` + `Subtype.heq_iff_coe_eq` + `Equivalent.parents_eq` | **Proved** |
| `SCM.evalMap_eq_of_equiv` | `Equiv M₁ M₂ → (s, ℓ) agree on Finsets → M₁.evalMap s₁ ℓ₁ ⟨w, _⟩ = M₂.evalMap s₂ ℓ₂ ⟨w, _⟩` | Cross-SCM pointwise equality of `evalMap`. Strong induction on M₁-topological index, unfolding via `evalMap_observed_unfold` on both sides, closing via `structFun_apply_eq_of_equiv` + IH | **Proved** |
| `SCM.Equiv.heq_latentProduct` | `Equiv M₁ M₂ → HEq M₁.latentProduct M₂.latentProduct` | Latent product measures match after subst of `unobserved` Finset equality and collapsing HEq `latentDist` to Eq | **Proved** |
| `SCM.Equiv.heq_jointKernel` | `Equiv M₁ M₂ → HEq M₁.jointKernel M₂.jointKernel` | Joint kernels match via `Kernel.ext` + `jointKernel_apply_eq` + pointwise `evalMap_eq_of_equiv` via `funext` | **Proved** |
| `SCM.Equiv.heq_obsKernel` | `Equiv M₁ M₂ → HEq M₁.obsKernel M₂.obsKernel` | `obsKernel = jointKernel.map randomToObserved`; HEq via `heq_jointKernel` + HEq of `randomToObserved` | **Proved** |
| `SCM.Equiv.heq_obsCondKernel` | `Equiv M₁ M₂ + shared Y, CC Finsets → HEq obsCondKernels` | Kernel-native conditional: `obsCondKernel = (obsCondPairKernel).condKernel`. HEq via destructure + `subst` on SWIG-graph equality + latent-index equality; after `subst`, the two `letI`-local `IsFiniteKernel` witnesses elaborate against identical data, so `congr 1` handles instance transport and `rw [h_ok_eq]` closes the `Kernel.condKernel` argument | **Proved** |

---

## 6b0. `SCM/Factored/*.lean` — Recursive Markov factorization of `jointKernel`

Sequential-`compProd` reconstruction of `jointKernel` along the topological order of observed nodes, realizing `P(V, U ∣ S) = P(U) · ∏ P(V_i ∣ Pa(V_i))` with each step a deterministic `Dirac(f_{V_i}(Pa(V_i)))`. Block 3 of the roadmap for `fullCondIndep_singleton_of_dSep`; the observed-node step is deterministic, so step kernels are `Kernel.deterministic` (not `condDistrib`).

Seven files under `Causalean/SCM/Factored/`:

### `PrefixState.lean` — prefix value types

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.ObservedPrefixValues n hn` | `Type _` — recursive product of values at the first `n` observed nodes in topo order | Value type for the observed prefix; base = `PUnit`, step = previous prefix × next node's value | Defined |
| `SCM.OrderedLatentPrefixValues n hn` | `LatentValues M × ObservedPrefixValues M n hn` | Full state at prefix length `n`: all latents plus the first `n` observed values | `abbrev` |
| `SCM.observedPrefixValue hn ξ i` | `swigΩ Ω (observedAt ⟨i.1, _⟩).val` | Read slot `i : Fin n` from a prefix state | Defined |
| `SCM.measurable_observedPrefixValue` | `Measurable` of the reader | Measurability per slot | **Proved** |
| `SCM.extendOrderedLatentPrefix hn` | `OrderedLatentPrefixValues n _ × swigΩ Ω v_n.val → OrderedLatentPrefixValues (n+1) hn` | Append the next observed value to a state | Defined |
| `SCM.measurable_extendOrderedLatentPrefix` | `Measurable (extendOrderedLatentPrefix _)` | Measurability | **Proved** |

### `ParentLookup.lean` — parent-value reader

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.parentValuesFromPrefix hn` | `(FixedValues × OrderedLatentPrefixValues n _) → (∀ p ∈ parents v_n, swigΩ Ω p.val)` | Looks up every parent of `v_n = observedAt ⟨n, hn⟩` from the appropriate component: fixed parents from `s`, latent parents from `ℓ`, observed parents from the prefix via `observedPrefixValue` at index `< n` (by `observed_parent_index_lt`) | Defined |
| `SCM.measurable_parentValuesFromPrefix` | `Measurable (parentValuesFromPrefix _)` | Compositional measurability | **Proved** |

### `StepKernel.lean` — deterministic step kernel

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.stepFun hn` | `(FixedValues × OrderedLatentPrefixValues n _) → swigΩ Ω v_n.val` | `structFun v_n ∘ parentValuesFromPrefix hn` — the structural equation applied to parent values read from the state | Defined |
| `SCM.measurable_stepFun` | `Measurable (stepFun _)` | Composition of `structFun_measurable` with `measurable_parentValuesFromPrefix` | **Proved** |
| `SCM.stepKernel hn` | `Kernel (FixedValues × OrderedLatentPrefixValues n _) (swigΩ Ω v_n.val)` | `Kernel.deterministic (stepFun hn) _` — emits the deterministic value of `v_n` given its parents | Defined |
| `SCM.isMarkov_stepKernel` | `IsMarkovKernel (stepKernel hn)` | Instance; deterministic kernels are Markov | **Proved** |

### `PrefixKernel.lean` — recursive assembly

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.latentKernelOnFixed` | `Kernel (FixedValues M) (LatentValues M)` | `Kernel.const _ latentProduct` — latents as a Markov kernel ignoring fixed input | Defined |
| `SCM.isMarkov_latentKernelOnFixed` | instance | Markov | **Proved** |
| `SCM.jointKernelPrefixZero` | `Kernel (FixedValues M) (OrderedLatentPrefixValues M 0 _)` | Base prefix: pushes `latentKernelOnFixed` through `ℓ ↦ (ℓ, PUnit.unit)` | Defined |
| `SCM.isMarkov_jointKernelPrefixZero` | instance | Markov | **Proved** |
| `SCM.jointKernelPrefix n hn` | `Kernel (FixedValues M) (OrderedLatentPrefixValues M n hn)` | Recursive: `0 → jointKernelPrefixZero`; `k+1 → ((jointKernelPrefix k _) ⊗ₖ stepKernel hn).map (extendOrderedLatentPrefix hn)` — the Markov factorization assembled step by step | Defined |
| `SCM.isMarkov_jointKernelPrefix` | instance | Markov at every `n` by induction | **Proved** |

### `EvalMapCorrespond.lean` — prefix ↔ `evalMap` bridge (block 3.5)

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.partialEvalMap n hn s ℓ` | `OrderedLatentPrefixValues M n hn` | Deterministic partial evaluator mirroring `jointKernelPrefix`: `0 → (ℓ, PUnit.unit)`; `k+1 → extendOrderedLatentPrefix hn (prev, stepFun hn (s, prev))`. This is the function whose `Dirac`-pushforward is the prefix kernel | Defined |
| `SCM.measurable_partialEvalMap` | `Measurable (fun sℓ => partialEvalMap n hn sℓ.1 sℓ.2)` | Induction on `n` using `measurable_stepFun` and `measurable_extendOrderedLatentPrefix` | **Proved** |
| `SCM.partialEvalMap_latent` | `(partialEvalMap n hn s ℓ).1 = ℓ` | Latent component is preserved by every step | **Proved** |
| `SCM.partialEvalMap_observedPrefixValue` | `observedPrefixValue hn (partialEvalMap n hn s ℓ).2 i = evalObservedAux s ℓ i.1 _` | Bridge to the existing `evalObservedAux`: reading slot `i` of the prefix reproduces the recursive evaluator (proof by structural recursion on `n` with `Fin.lastCases`; last case matches `stepFun` to `evalObservedAux_eq` via per-parent cast reconciliation) | **Proved** |
| `SCM.jointKernelPrefix_apply_eq` | `(jointKernelPrefix n hn) s = latentProduct.map (partialEvalMap n hn s)` | Core factorization: prefix kernel = pushforward of `partialEvalMap`; induction on `n` using the local helper `compProd_deterministic_apply` + `Measure.map_map` | **Proved** |

### `Factorization.lean` — final theorem (block 3.6)

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.orderedLatentPrefixFullToRandom` | `OrderedLatentPrefixValues M observed.card _ → RandomValues M` | Reindex the full prefix state back to `RandomValues`: observed coords via `observedPrefixValue` + `cast` through `observedAt_observedIndex`; unobserved coords from the `LatentValues` component | Defined |
| `SCM.measurable_orderedLatentPrefixFullToRandom` | `Measurable` of the reindex | Per-coordinate measurability assembled via `measurable_pi_lambda` | **Proved** |
| `SCM.partialEvalMap_full_eq` | `orderedLatentPrefixFullToRandom (partialEvalMap observed.card _ s ℓ) = evalMap s ℓ` | Coordinate-wise equality: observed coords match via `partialEvalMap_observedPrefixValue`; unobserved coords match via `partialEvalMap_latent` | **Proved** |
| `SCM.jointKernel_factored` | `jointKernel s = ((jointKernelPrefix observed.card _) s).map orderedLatentPrefixFullToRandom` | Pointwise factorization: combines `jointKernel_apply_eq`, `partialEvalMap_full_eq`, `Measure.map_map`, `jointKernelPrefix_apply_eq` | **Proved** |
| `SCM.jointKernel_eq_factored_kernel` | `jointKernel = (jointKernelPrefix observed.card _).map orderedLatentPrefixFullToRandom` | Kernel-level restatement via `Kernel.ext` and `Kernel.map_apply` | **Proved** |

### `ObsChainKernel.lean` — observational chain-rule product

| Declaration | Signature / meaning | Description | Status |
|---|---|---|---|
| `SCM.prefixNodes n` | `Finset (SWIGNode N)` | First `n` observed nodes in the canonical topological order, characterized by `observedIndex < n` | Defined |
| `SCM.observedPredecessors_observedAt` | `M.toSWIGGraph.observedPredecessors (observedAt n) = prefixNodes n` | Tian's full-history predecessor set agrees with the first `n` observed nodes | **Proved** |
| `SCM.obsStepCondKernel hn` | `Kernel (FixedValues M × ValuesOn (prefixNodes n)) (swigΩ Ω v_n)` | One-node conditional kernel `P(V_n | V_0, …, V_{n-1})`, built from `obsCondKernel {v_n} (prefixNodes n)` and mapped out of the singleton value tuple | Defined; Markov/finite instances proved |
| `SCM.obsChainKernel n hn` | `Kernel (FixedValues M) (ValuesOn (prefixNodes n))` | Recursive chain-rule kernel: base Dirac on the empty prefix; successor uses `compProd` with `obsStepCondKernel` and maps through prefix extension | Defined; Markov instance proved |
| `SCM.qFactorProduct` | `Kernel (FixedValues M) (ObservedValues M)` | Full-length observational chain-rule product, reindexed along `prefixNodes observed.card = observed` | Defined |
| `SCM.obsKernel_map_prefixNodes` / `SCM.obsKernel_eq_qFactorProduct` | Prefix projection equality; `M.obsKernel s = M.qFactorProduct s` | Real kernel equality for the observational chain rule, proved by prefix induction using disintegration of `obsCondPairKernel` and transport through the prefix-union equivalence | **Proved** |

**Status of block 3.** The structural `jointKernel` factorization files (3.1–3.6) and the observational chain-rule product in `ObsChainKernel.lean` are closed fully. Block 4 (ordered Markov) consumes `jointKernel_eq_factored_kernel`.

Reference: Basic Concepts.tex, Lemma `lem:scm-ancestral-factor` and the recursive definition around `def:scm-eval`.

---

## 6b1. `SCM/Do/Rule2.lean` — Rule 2 value-space plumbing (aggregator)

`Rule2.lean` is now a **plumbing aggregator**: it re-exports the value-space
/ global-Markov / overlap helpers (`RectIdentity`, `ObsMarkov`,
`ID.Overlap`) consumed by the Rule 2 a.e. statement and downstream
identification.  The kernel-native Rule 2 *statement* itself is the
posterior witness-kernel a.e. theorem `obsCondKernel_fixSet_eq_ae_witness` in
`Rule2AE.lean` (surfaced as `do_rule2_kernel` in `DoCalculus.lean`).

The earlier pointwise/`fillZrW` form `obsCondKernel_fixSet_eq` and its
rectangle-identity core `obsKernel_fixSet_rect_eq` were **retired**: they
pinned `obsCondKernel` on the `μ_C`-null `{Z.random = ζ_s}` slice, which is
ill-posed for continuous treatment (see §6b1b).  The Rule-2-specific
value-space helpers (`zFixedAsRandom`, `valuesUnionMk`, `fillZrW`, …) live
in `SCM/Do/Rule2Kernel/` (`Helpers.lean`, `InterSingleton.lean`,
`LevelsetCompat.lean`, `WMarginal.lean`) and are documented below.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.zFixedAsRandom z` | `ValuesOn (Z.image .fixed) → ValuesOn (Z.image .random)` | Reindexes `.fixed D`-keyed values to `.random D` keys via the shared value space `swigΩ Ω (.fixed D) = Ω D` | Defined |
| `SCM.valuesUnionMk a b` | `ValuesOn A → ValuesOn B → ValuesOn (A ∪ B)` | Piecewise union of two `ValuesOn` slices, with `A` taking priority; defined via `dite` | Defined |
| `SCM.valuesUnionMk_apply_left` / `valuesUnionMk_apply_right` *(@[simp])* | `valuesUnionMk a b ⟨v, hv⟩ = a/b ⟨v, _⟩` under `v ∈ A` / `v ∉ A` | `valuesUnionMk a b` projects to `a` on `A` and to `b` on `B \ A` | **Proved** (`dif_pos`/`dif_neg`) |
| `SCM.measurable_valuesUnionMk_right a` | `Measurable (fun b => valuesUnionMk a b)` | Measurability of `fun b => valuesUnionMk a b` | **Proved** |
| `SCM.valuesUnionEquiv hDisj` | `ValuesOn (A ∪ B) ≃ᵐ ValuesOn A × ValuesOn B` (when `Disjoint A B`) | Canonical measurable equivalence splitting a value on a disjoint union into its two slices; forward is `(valuesProjection subset_union_left, valuesProjection subset_union_right)`, inverse is `valuesUnionMk`. Disjointness is used only for the `left_inv` on the `B`-slot. | **Proved** |
| `SCM.measurable_zFixedAsRandom` | `Measurable zFixedAsRandom` | Measurability of `zFixedAsRandom` | **Proved** |
| `SCM.fillZrW M' Z … W s'` | `ValuesOn W → ValuesOn (Z.image .random ∪ W)` | Rule 2 filler (single-intervention form): inserts the `do(Z)` fixed-node values of `M'` (re-keyed to `.random Z` via `zFixedAsRandom`) into a `W`-indexed tuple | Defined |
| `SCM.measurable_fillZrW M' Z … W s'` | `Measurable (M'.fillZrW Z … W s')` | Measurability of `fillZrW` | **Proved** |
| `SCM.fixSet_evalMap_levelset_compat M' Z … s' ℓ hLevelSet hv` | On the M'-level set (`M'.evalMap (fixSetProj s') ℓ_M' ⟨.random D, _⟩ = s' ⟨.fixed D, _⟩ ∀ D ∈ Z`): `(M'.fixSet Z).evalMap s' ℓ ⟨v, _⟩ = M'.evalMap (fixSetProj s') ℓ_M' ⟨v, _⟩` for every `v ∈ (M'.fixSet Z).observed` | M'-direction cross-SCM `evalMap` bridge (Claim A). Strong recursion on `(M'.fixSet Z).observedIndex`; at the new `u ∈ Z` parent case (via `fixMonoParentMap_apply_random`) `hLevelSet` collapses M's recursive call at `.random u` to `z_u`, matching `(M'.fixSet Z).fixed` at `u` | **Proved** |
| `SCM.fixSet_evalMap_levelset_compat_M2 M' Z … s' ℓ hLS_M2 hv` | On the `(M'.fixSet Z)`-level set: same conclusion as `fixSet_evalMap_levelset_compat` | Other-direction cross-SCM `evalMap` bridge (Claim B). Strong recursion on `M'.observedIndex` (not `(M'.fixSet Z)`'s — the latter's DAG lacks the `.random D → v` edges for `D ∈ Z` since they are rewired, but `M'`'s retains them); at `u ∈ Z` parent: IH at `.random u` combined with `hLS_M2` derives `M'` at `.random u = z_u` | **Proved** |
| `SCM.obsKernel_inter_singleton_Zrand_eq M' Z … W hZrW hDisj_ZrW s' w hS` | `(M'.fixSet Z).obsKernel s' (S ∩ π_{Z.rand∪W}⁻¹ {fillZrW s' w}) = M'.obsKernel (fixSetProj s') (S ∩ π_{Z.rand∪W}⁻¹ {fillZrW s' w})` for any measurable `S ⊆ M'.ObservedValues` | **Z.random-level-set joint kernel agreement** (single-intervention form). Core lemma for `hC1`: on the event `π_{Z.rand ∪ W} = fillZrW s' w`, the `do(Z)`-intervened and base obsKernels agree on any additional measurable `S`-condition. Proof: unfold `obsKernel` to `latentProduct.map (rando ∘ evalMap)`, bridge via `fixSet_latentProduct_compat`; preimage sets agree via Claims A and B | **Proved** (no d-sep used) |
| `SCM.obsKernel_disintegrate_rect M Y CC hY hCC s hD hB` | `M.obsKernel s (π_CC⁻¹ D ∩ π_Y⁻¹ B) = ∫⁻ c in D, obsCondKernel Y CC (s, c) B d((M.obsKernel s).map π_CC)` | Generic M1 disintegration in rectangle form (`InterSingleton.lean`). Direct application of Mathlib's `ProbabilityTheory.setLIntegral_condKernel_eq_measure_prod` to `M.obsCondPairKernel Y CC`, combined with `Kernel.fst_map_prod`. | **Proved** |

---

## 6b1b. `SCM/Do/Rule2Kernel/Structural/*.lean` + `WitnessBridge.lean` — continuous-`Z` cross-SCM bridge

The **posterior witness-kernel proof** for continuous-treatment Rule 2 (sorry-free).  The earlier
structural-kernel route (`obsCondKernel_struct` — the prior-pushforward
representative) was **retired**: it pushed the *prior* latent law forward and so
did not equal the W-confounded posterior conditional.  The current proof instead
builds a *posterior* witness kernel by conditioning the cut-set latent block on
the treatment and pushing it through the structural map; `WitnessBridge.lean`
assembles it and `Rule2AE.lean` exposes the cross-SCM bridge.  The
`Structural/*.lean` files now contribute only the pointwise `evalMap_overrideC`
agreement lemmas consumed by the posterior witness-kernel bridge.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.evalMap_overrideC_dropZr_on_fillZrW M' Z … Y W … s ℓ w` | pointwise: in the post-intervention model, adding the filled `Z.random` coordinates to the override assignment does not change the `Y` output | Post-intervention `evalMap_overrideC` equality for the `fillZrW` assignment (`StructPointwise.lean`). | **Proved** |
| `SCM.evalMap_overrideC_fixSet_compat_on_fillZrW M' Z … W … s w ℓ` | pointwise: the original and post-intervention SCMs agree on the overridden observed evaluation at the same `fillZrW` assignment | Cross-SCM `evalMap_overrideC` compatibility for filled treatment assignments (`StructCrossSCM.lean`). | **Proved** |
| `SCM.cutset_factor_pointwise M' …` | the cut-set latent factorization data for the witness kernel | Pointwise factorization of the override evaluation through the cut-set latent block (`WitnessBridge.lean`). | **Proved** |
| `SCM.obsSide_eq_witness` / `SCM.obsCondKernel_union_eq_witness M' …` | `M1.obsCondKernel Y (Zr∪W)` equals the posterior witness kernel, a.e. | Obs-side identification of the base conditional with the witness kernel (`WitnessBridge.lean`). | **Proved** (sorry-free) |
| `SCM.doSide_eq_witness M' Z … Y W … s0 t …` | `∀ᵐ w ∂μW, M2.obsCondKernel Y W (fixSetExtend s0 t, w) = (witness kernel) w` | Do-side per-slice identification with the *same* posterior witness kernel; M2 witness chain + cross-SCM connect aligned on `μW` by Rule 3 (`WitnessBridge.lean`). | **Proved** (sorry-free) |
| `SCM.obsCondKernel_fixSet_M1_eq_ae_product M' Z … s0 hPositivity_ae` | `∀ᵐ p ∂(νZ ⊗ₘ const μW), M2.obsCondKernel Y W (fixSetExtend s0 p.1, p.2) = M1.obsCondKernel Y (Zr∪W) (s0, valuesUnionMk p.1 p.2)` | Product assembly: LHS=witness (`doSide_eq_witness`), RHS=witness (`obsCondKernel_union_eq_witness`@M1) transported onto the product via `hPositivity_ae`. Consumed by `condDistrib_fixSet_cross_SCM_bridge`. | **Proved** (sorry-free) |
| `SCM.condDistrib_fixSet_cross_SCM_bridge M' Z … s0 hPositivity_ae` | `∀ᵐ p ∂(νZ ⊗ₘ const μW), M2.obsCondKernel Y W (fixSetExtend s0 p.1, p.2) = condDistrib π_Y π_{Zr∪W} (M'.obsKernel s0) (valuesUnionMk p.1 p.2)` | **Continuous-`Z` cross-SCM condDistrib bridge** (`Rule2AE.lean`). Chains the do-side reduction with the obs-side AC transport. | **Proved** (sorry-free) |
| `SCM.obsCondKernel_fixSet_eq_ae_witness M' Z … s0 hOverlap hPositivity_ae` | `∀ᵐ p ∂(νZ ⊗ₘ const μW), M2.obsCondKernel Y W (fixSetExtend s0 p.1, p.2) = M1.obsCondKernel Y (Zr∪W) (s0, valuesUnionMk p.1 p.2)` | **Rule 2, kernel-native witness form (the canonical statement).** A.e. over the supported product marginal — sound for continuous treatment (never pins `obsCondKernel` on a null slice). Surfaced as `do_rule2_kernel`. | **Proved** (sorry-free) |

---

## 6b2. `SCM/Do/Rule3.lean` — Rule 3 bridges and marginal equality

Imports `SCM/Model/Kernel.lean`.  Supplies the latent-product and `evalMap`
compatibility lemmas used in the proof of `condDistrib_intervention_ancestral_eq`,
and hosts that theorem itself.  All three declarations are stated in the
**single-intervention form** on a generic SCM `M'` with one extra do-target
set `Z` — Pearl's two-layer `do(X); do(Z)` reading is recovered at the call
site by instantiating `M' := M.fixSet X …`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.fixSet_latentProduct_compat M' Z hZ_obs hZ_fixed` | `(M'.fixSet Z _ _).latentProduct.map (valuesProjection (fixSet_unobserved).ge) = M'.latentProduct` | Single-intervention latent-product transport: pushing `(M'.fixSet Z).latentProduct` through `valuesProjection` along `fixSet_unobserved` recovers `M'.latentProduct`. Proof via `measurePreserving_valuesEquivOfEq` + `fixSet_latentDist` (both measures are literally the same `Measure.pi` on the same per-coordinate measures). | **Proved** |
| `SCM.fixSet_evalMap_nonAnc_compat M' Z hZ_obs hZ_fixed s' ℓ hv hNoDesc` | `(M'.fixSet Z _ _).evalMap s' ℓ ⟨v,_⟩ = M'.evalMap (fixSetProj s') (valuesProjection (fixSet_unobserved).ge ℓ) ⟨v,_⟩` | Cross-SCM `evalMap` bridge: at an observed `v` with no `SWIGNode.fixed z` (`z ∈ Z`) as ancestor in `(M'.fixSet Z).dag`, the `(M'.fixSet Z)` evaluation agrees with `M'`'s evaluation after projecting the fixed-value argument via `fixSetProj` and transporting the latent argument along `fixSet_unobserved`. Strong recursion on `(M'.fixSet Z).observedIndex`; parent-set coincidence via `fixSet_parents_eq_of_no_fixed_parent`; `fixMonoParentMap` collapses at every parent under the no-fixed-parent hypothesis. | **Proved** |
| `SCM.condDistrib_intervention_ancestral_eq M' Z hZ_obs hZ_fixed T hT hNoDesc s'` | `Measure.map` equality: `T`-marginal of `(M'.fixSet Z _ _).obsKernel s'` equals `T`-marginal of `M'.obsKernel (fixSetProj s')` | `T`-marginal equality of `(M'.fixSet Z).obsKernel` vs `M'.obsKernel` when no `z ∈ Z` has `SWIGNode.fixed z` an ancestor of any `v ∈ T` in `(M'.fixSet Z).dag`. Closes by unfolding `obsKernel` to `latentProduct.map (evalMap ≫ randomToObserved ≫ π)`, bridging the latent source via `fixSet_latentProduct_compat`, and discharging pointwise via `fixSet_evalMap_nonAnc_compat`. | **Proved** |

---

## 7a. `SCM/Model/InterventionMono.lean` — Monolithic multi-target do (`fixMono`)

**Monolithic** multi-target SWIG-split intervention on a gSCM, parallel to the graph-level `SWIGGraph.splitMono`.  This is the SCM-level `do` operation; `fixSet X` with a singleton `X = {D}` recovers a single-target intervention.

Graph layer is `SWIGGraph.splitMono`; `latentDist` and `isProbability_latent` inherited from `M`; `structFun` built from a single monolithic parent reindex `fixMonoParentMap` (no list iteration).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.fixMonoParentMap G X hObs hFix v ξ` | `∀ w : {w // w ∈ G.dag.parents v}, swigΩ Ω w.val` | Monolithic parent reindexer: reads `.fixed D` for each `.random D ∈ X`, copies all other coordinates; public (consumed by `Rule3.lean`) | Defined |
| `SCM.fixMonoParentMap_apply_fixed` | `fixMonoParentMap … ⟨.fixed d, hw⟩ = ξ ⟨.fixed d, …⟩` | Unfolds `fixMonoParentMap` at a `.fixed d` input | `rfl` |
| `SCM.fixMonoParentMap_apply_random_notMem` | `u ∉ X → fixMonoParentMap … ⟨.random u, hw⟩ = ξ ⟨.random u, …⟩` | Unfolds `fixMonoParentMap` at `.random u` with `u ∉ X` | **Proved** |
| `SCM.fixMonoParentMap_apply_random` | `D ∈ X → fixMonoParentMap … ⟨.random D, h⟩ = ξ ⟨.fixed D, …⟩` | Unfolds `fixMonoParentMap` at `.random D` with `D ∈ X`: reads from `.fixed D` | **Proved** |
| `SCM.measurable_fixMonoParentMap` | `Measurable (fixMonoParentMap G X hObs hFix v)` | Measurability of the monolithic parent reindexer | **Proved** |
| `SCM.fixMono M X hObs hFix` | `SCM N Ω` | Monolithic multi-target generalized do: applies `splitMono` on the graph; `latentDist := M.latentDist`; `structFun` reindexed via `fixMonoParentMap` | noncomputable |
| `SCM.fixMono_observed` *(@[simp])* | `(M.fixMono X _ _).observed = M.observed` | `fixMono` preserves the observed node set | `rfl` |
| `SCM.fixMono_unobserved` *(@[simp])* | `(M.fixMono X _ _).unobserved = M.unobserved` | `fixMono` preserves the unobserved node set | `rfl` |
| `SCM.fixMono_fixed` *(@[simp])* | `(M.fixMono X _ _).fixed = M.fixed ∪ X.image SWIGNode.fixed` | Fixed set after `fixMono` is the original fixed set union `X.image SWIGNode.fixed` | `rfl` |
| `SCM.fixMono_latentDist` *(@[simp])* | `(M.fixMono X _ _).latentDist u = M.latentDist u` | `fixMono` inherits `latentDist` unchanged | `rfl` |
| `SCM.fixMono_fixed_subset` | `M.fixed ⊆ (M.fixMono X _ _).fixed` | Original fixed set is a subset of the post-`fixMono` fixed set | **Proved** |
| `SCM.fixMono_image_fixed_subset` | `X.image SWIGNode.fixed ⊆ (M.fixMono X _ _).fixed` | `X.image SWIGNode.fixed` is a subset of the post-`fixMono` fixed set | **Proved** |
| `SCM.fixMono_parents_eq_of_no_fixed_parent` | `(∀ D ∈ X, .fixed D ∉ (M.fixMono X _ _).dag.parents v) → (M.fixMono X _ _).dag.parents v = M.dag.parents v` | Parent-set coincidence at non-`X`-targeted vertices; delegates to `splitMono_parents_eq_of_no_fixed_parent`; key for Rule 3 | **Proved** |

Reference: Basic Concepts.tex, Definition 8 (multi-target generalized intervention); `rem:scm-do-standard-lean`.

---

## 7a2. `SCM/Model/InterventionSet.lean` — Public API (`fixSet`, `fixSetProj`, `fixSetZSlice`)

Thin public-API wrapper: `fixSet` is a definitional alias for `fixMono`, plus the `FixedValues` projection `fixSetProj` consumed by single-intervention `DoCalculus.do_rule2_kernel` / `do_rule3`.

* `fixSetProj : (M.fixSet X _ _).FixedValues → M.FixedValues` — projects away the newly-added `X`-fixed slice; used by `do_rule2_kernel` / `do_rule3` to project a `(M'.fixSet Z).FixedValues` back down to `M'.FixedValues`;
* `fixSetZSlice : ((M.fixSet X).fixSet Z).FixedValues → ValuesOn (Z.image .fixed) (swigΩ Ω)` — extracts the `do(Z)` slice from a two-layer `FixedValues` (retained for downstream use).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.fixSet M X hObs hFix` | `SCM N Ω` | Standard (Pearl) multi-target do: definitional alias for `fixMono` | noncomputable |
| `SCM.fixSet_observed` *(@[simp])* | `(M.fixSet X _ _).observed = M.observed` | `fixSet` preserves the observed node set | `rfl` |
| `SCM.fixSet_unobserved` *(@[simp])* | `(M.fixSet X _ _).unobserved = M.unobserved` | `fixSet` preserves the unobserved node set | `rfl` |
| `SCM.fixSet_fixed` *(@[simp])* | `(M.fixSet X _ _).fixed = M.fixed ∪ X.image SWIGNode.fixed` | Fixed set after `fixSet` is the original fixed set union `X.image SWIGNode.fixed` | `rfl` |
| `SCM.fixSet_latentDist` *(@[simp])* | `(M.fixSet X _ _).latentDist u = M.latentDist u` | `fixSet` inherits `latentDist` unchanged | `rfl` |
| `SCM.fixSet_fixed_subset` | `M.fixed ⊆ (M.fixSet X _ _).fixed` | Original fixed set is a subset of the post-`fixSet` fixed set | **Proved** |
| `SCM.fixSet_image_fixed_subset` | `X.image SWIGNode.fixed ⊆ (M.fixSet X _ _).fixed` | `X.image SWIGNode.fixed` is a subset of the post-`fixSet` fixed set | **Proved** |
| `SCM.fixed_mem_fixSet` | `D ∈ X → SWIGNode.fixed D ∈ (M.fixSet X _ _).fixed` | `SWIGNode.fixed D` is in the post-`fixSet` fixed set when `D ∈ X` | **Proved** |
| `SCM.fixSet_parents_eq_of_no_fixed_parent` | `(∀ D ∈ X, .fixed D ∉ (M.fixSet X _ _).dag.parents v) → (M.fixSet X _ _).dag.parents v = M.dag.parents v` | Parent-set coincidence at non-`X`-targeted vertices; key for Rule 3 non-ancestor bridge | **Proved** |
| `SCM.fixSet_empty_parents` | `(M.fixSet ∅ _ _).dag.parents v = M.dag.parents v` | Empty-set specialization of `fixSet_parents_eq_of_no_fixed_parent` (vacuous hypothesis); used by `fixSet_empty_equiv` and downstream single-intervention bridges | **Proved** |
| `SCM.fixSet_empty_edge` | `(M.fixSet ∅ _ _).dag.edge u v ↔ M.dag.edge u v` | Edge-level coincidence at `X = ∅` (splitMonoEdgeRel reduces when `X = ∅`) | **Proved** |
| `SCM.fixSet_empty_equiv` | `SCM.Equiv (M.fixSet ∅ _ _) M` | `fixSet ∅` is structurally equivalent to `M` (SWIG graph edges, fixed/observed/unobserved, edge types, `HEq` of `structFun` and `latentDist`). The literal equality `M.fixSet ∅ _ _ = M` is **not** provable because `splitMonoTopo` at `X = ∅` rewrites node topoOrders; `SCM.Equiv` ignores topoOrder and is the right invariant for d-sep + kernel semantics. Proof uses the `swigIntervention_comm` recipe: `Function.hfunext` over observed + parent-tuple subtypes, reducing `HEq` to `Eq` via `Equivalent.parents_eq` | **Proved** |
| `SCM.fixSetProj M X _ _` | `(M.fixSet X _ _).FixedValues → M.FixedValues` | Projects `(M.fixSet X).FixedValues` down to `M.FixedValues` by restricting to the original fixed nodes | noncomputable |
| `SCM.measurable_fixSetProj` | `Measurable (M.fixSetProj X _ _)` | Measurability of `fixSetProj` | **Proved** |
| `SCM.fixSetZSlice M X Z …` | `((M.fixSet X).fixSet Z).FixedValues → ValuesOn (Z.image SWIGNode.fixed) (swigΩ Ω)` | Extracts the `do(Z)` fixed-node slice from a double-intervention `FixedValues`; consumed by Rule 2 | noncomputable |
| `SCM.measurable_fixSetZSlice` | `Measurable (M.fixSetZSlice X Z _ _ _ _)` | Measurability of `fixSetZSlice` | **Proved** |

Reference: Basic Concepts.tex, Definition 8 (multi-target generalized intervention); `rem:scm-do-standard-lean`.

---

## 7a3. `SCM/Model/InterventionAncestry.lean` — Ancestry bridges for `fixSet`

Bridges between `isAncestor` in the post-intervention DAG `(M.fixSet X).dag`
and the base `M.toSWIGGraph.dag`.  Consumed by
`SCM/ID/Backdoor.lean` to translate the backdoor criterion's
non-descendant condition (condition (i), stated on the original graph) into
the Rule 3 hypothesis (stated on the post-intervention graph, in terms of
`.fixed D` roots).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `DAG.not_isAncestor_of_root'` | `(∀ u, ¬ G.edge u r) → ∀ u, ¬ G.isAncestor u r` | A vertex with no incoming edges has no proper ancestors | **Proved** |
| `SCM.fixSet_isAncestor_fixed_forward` | `D ∈ X → (M.fixSet X _ _).dag.isAncestor (.fixed D) v → M.toSWIGGraph.dag.isAncestor (.random D) v` | Forward direction: a `.fixed D`-ancestor in the split DAG lifts to a `.random D`-ancestor in the base DAG. Interior vertices of the split path cannot be `.random d` (`d ∈ X`, no outgoing in split) nor `.fixed d` (`d ∈ X`, isolated in the base); all other edges lift `1:1` | **Proved** |

**Note on the iff form.** The backward direction is **false in general**:
if `M.dag` contains `random D → random D' → v` with `D, D' ∈ X`, `D ≠ D'`,
then `G.isAncestor (random D) v` holds but no corresponding `.fixed D → v`
path exists in the split DAG (because `random D'` has no outgoing edges
after splitting). The forward direction is all that's needed for the
backdoor argument (contrapositive of criterion (i)).

---

## 7b. `SCM/Model/Induced.lean` — Induced sub-SCM (Definition `def:scm-induced-sub`)

Full implementation of the induced sub-SCM construction. Imports
`SCM/Model/Kernel.lean` so that the Layer 4 marginal-compatibility statement
can reference `obsKernel` directly.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.isAncestrallyClosedSCM M R` | `Prop` — conjunction of (a) observed-ancestor closure and (b) pairing closure under `iotaMap` | SCM-level ancestral closure: strictly stronger than graph-level `isAncestrallyClosed` | Defined |
| `induce_parents_eq_of_ancClosed` *(private)* | Under `isAncestrallyClosedSCM`, `(M.toSWIGGraph.induce R).dag.parents v = M.dag.parents v` for every `v ∈ (M.toSWIGGraph.induce R).observed` | Under ancestral closure, parent sets of induced-SCM observed nodes equal those in `M` | **Proved** |
| `SCM.induce M R hR` | `SCM N Ω` | Induced sub-SCM on `R ⊆ observed`: restricts the graph, inherits structural functions and latent distribution | **Proved** |
| `SCM.induce_evalMap_compat` | `∀ {v} (hvI : v ∈ (M.induce R hR).randomVars) (hvM : v ∈ M.randomVars), (M.induce R hR).evalMap (sTilde \| _) ℓ ⟨v, hvI⟩ = M.evalMap sTilde ℓ ⟨v, hvM⟩` | `evalMap` of the induced SCM at any `v ∈ (M.induce R).randomVars` agrees pointwise with `M.evalMap` | **Proved (Layer 4)** |
| `SCM.induce_marginal_compat M R hR sTilde` | `(M.induce R hR).obsKernel (sTilde \| (M.induce R hR).fixed) = (M.obsKernel sTilde).map (valuesProjection : ObservedValues M → ObservedValues (M.induce R hR))` | `obsKernel` of the induced SCM is the `valuesProjection` pushforward of `M.obsKernel` | **Proved (Layer 4)** |

References: Basic Concepts.tex, Definitions `def:scm-anc-closed`, `def:scm-induced-sub`; Proposition `prop:scm-induced-marginal`.

---

## 8c. `SCM/Do/SemiGraphoid.lean` — Semi-Graphoid Axioms

Semi-graphoid axioms on observational conditional independence for a gSCM,
stated against an arbitrary finite measure `μ` on `ObservedValues M` using
Mathlib's `ProbabilityTheory.CondIndepFun` (via `valuesProjection` and
`comap_valuesProjection_le`).  The canonical choice downstream is
`Causalean.SCM.obsKernel M s` at `s : M.FixedValues`.  Weak union and contraction
are SCM-level wrappers around graph-independent projection lemmas in
`Mathlib/CondIndep/SemiGraphoid.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `SCM.ObsCondIndep M X Y Z hX hY hZ μ` | `Prop` over `μ : Measure (ObservedValues M)` | Observational conditional independence `X ⊥ Y \| Z` as `CondIndepFun` on the projected σ-algebras | Defined (requires `StandardBorelSpace M.ObservedValues`, `[IsFiniteMeasure μ]`) |
| `SCM.valuesProjection_comp` | composition identity for nested `valuesProjection` | Used in `obsCondIndep_subset_right` | **Proved** |
| `SCM.comap_valuesProjection_mono` | monotonicity of `comap (valuesProjection …)` in the projected index set | Generic projection σ-algebra helper for CI plumbing on finite products | **Proved** |
| `Causalean.condIndep_valuesProjection_symm` | projection-form `CondIndepFun` symmetry | Graph-independent symmetry helper over `ValuesOn I` projections | **Proved** |
| `Causalean.condIndep_valuesProjection_subset_right` | projection-form subset-right | Graph-independent subset-right helper over `ValuesOn I` projections | **Proved** |
| `Causalean.condIndep_valuesProjection_decomposition` | projection-form decomposition | Graph-independent decomposition helper over `ValuesOn I` projections | **Proved** |
| `Causalean.condIndep_valuesProjection_weak_union_axiom` | projection-form weak union | Graph-independent weak-union wrapper around `Mathlib/CondIndep/SemiGraphoid.lean` | **Proved** |
| `Causalean.condIndep_valuesProjection_contraction_axiom` | projection-form contraction | Graph-independent contraction wrapper around `Mathlib/CondIndep/SemiGraphoid.lean` | **Proved** |
| `obsCondIndep_symm` | symmetry in `X` and `Y` | Delegates to `CondIndepFun.symm` | **Proved** |
| `obsCondIndep_subset_right` | `Y' ⊆ Y` inherits CI from `X ⊥ Y \| Z` | SCM-level wrapper via `Causalean.condIndep_valuesProjection_subset_right` | **Proved** |
| `obsCondIndep_decomposition` | `X ⊥ (Y ∪ W) \| Z → X ⊥ Y \| Z` | SCM-level wrapper via `Causalean.condIndep_valuesProjection_decomposition` | **Proved** |
| `obsCondIndep_weak_union` | `X ⊥ (Y ∪ W) \| Z → X ⊥ Y \| (Z ∪ W)` | SCM-level wrapper via `Causalean.condIndep_valuesProjection_weak_union_axiom` | **Proved** |
| `obsCondIndep_contraction` | standard contraction axiom | SCM-level wrapper via `Causalean.condIndep_valuesProjection_contraction_axiom` | **Proved** |

The hard measure-theoretic obligations are factored into
`Mathlib/CondIndep.lean`; this file now exposes the SCM-facing wrappers and
keeps all graph-independent CI obligations in one place.

---

## 8d. `SCM/Do/{FullCondIndep, LocalMarkov, GlobalMarkov, ObsMarkov}.lean` — Global Markov Property (split)

The central bridge between d-separation and observational conditional
independence. Uses d-separation in the **full graph** (including latent
nodes) to derive CI at the observational level.  Originally one file;
split in this session into four focused modules:

* **`FullCondIndep.lean`** — `FullCondIndep` definition + semi-graphoid axioms + congruence helper.
* **`LocalMarkov.lean`** — `condIndepFun_of_map` bridge + `full_local_markov` (observed) + `full_local_markov_latent` (unobserved).
* **`GlobalMarkov.lean`** — `fullCondIndep_singleton_of_dSep` auxiliary + `full_globalMarkov` Verma–Pearl induction.
* **`ObsMarkov.lean`** — `obs_condIndep_of_full` projection + `globalMarkov` + `globalMarkov_with_fixed`.

Imports flow `FullCondIndep → LocalMarkov → GlobalMarkov → ObsMarkov`.

### `FullCondIndep.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FullCondIndep` | CI on `RandomValues M` (full joint distribution including latents) | Conditional independence on the full joint distribution `jointKernel M s`, including latent variables | Definition |
| `fullCondIndep_symm` / `fullCondIndep_subset_right` / `fullCondIndep_decomposition` | Semi-graphoid lemmas for `FullCondIndep` | Thin wrappers around generic projection-form CI lemmas from `SemiGraphoid.lean` | **Proved** |
| `fullCondIndep_weak_union` / `fullCondIndep_contraction` | Semi-graphoid axioms | Also wrappers around the shared projection-form CI layer | **Proved** (depends on Mathlib stubs) |
| `fullCondIndep_congr_left` | `FullCondIndep M X Y Z … μ → X = X' → FullCondIndep M X' Y Z … μ` | Transport `FullCondIndep` along a Finset equality on the first argument; used in the Verma–Pearl induction to convert `{a} ∪ A'` to `insert a A'` | **Proved** (`subst heq`) |

### `LocalMarkov.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `observed_subset_randomVars` | `M.observed ⊆ M.randomVars` | Trivial containment of observed in `M.randomVars = observed ∪ unobserved` | **Proved** |
| `condIndepFun_of_map` | Pushforward bridge: `CondIndepFun` under `ν` → under `ν.map φ` | Transfers `CondIndepFun` through a measurable pushforward via `condDistrib` characterization | **Proved** |
| `full_local_markov` | `FullCondIndep M {v} (NonDesc v ∩ randomVars) (Pa v ∩ randomVars) ... (jointKernel M s)` for `v ∈ M.observed` | Local Markov on the full joint: under `jointKernel M s`, each observed `v` is conditionally independent of its non-descendants given all parents (including latent ones). Proved via `evalMap_factors_through_parents` (LatentValues-level CI built from `condIndepFun_of_measurable_left`, then transported to `RandomValues` via `condIndepFun_of_map`) | **Proved** |
| `full_local_markov_latent` | `FullCondIndep M {a} (NonDesc a ∩ randomVars) ∅ ... (jointKernel M s)` for `a ∈ M.unobserved` | Latent-root analogue of `full_local_markov`: since latents have no parents, the conditioning set collapses to `∅` and we get *unconditional* independence of `{a}` from its non-descendants. Proved in four phases: (A) identify `jointKernel s = latentProduct.map (evalMap s)`; (B)–(C) collapse the `∅`-indexed conditioning σ-algebra to `⊥` via `comap_eq_bot_of_subsingleton` and reduce to `IndepFun … (jointKernel s)` via `condIndepFun_bot_of_indepFun`; (D) pushforward bridge `indepFun_of_map` to `latentProduct`-level `IndepFun`; (E) factor the LHS as a measurable wrap of the latent coord at `⟨a, ha⟩` (via `evalMap_unobserved`) and the RHS as a measurable function of latents *excluding* `⟨a, ha⟩` (via `evalMap_factors_excluding_latent`), then close via `indepFun_pi_of_disjoint` + `IndepFun.comp` at `S = {⟨a,ha⟩}`, `T = univ.erase ⟨a,ha⟩` | **Proved** |

### `GlobalMarkov.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `fullCondIndep_singleton_of_dSep` | `Disjoint {a} Y → dSep {a} Y W → FullCondIndep M {a} Y W ... (jointKernel M s)` | Singleton-source d-sep ⟹ full CI, now with the necessary non-overlap assumption `a ∉ Y`. The body is refactored around three named SCM/product-space bridges: a latent-overlap lemma `latentAncestorsOfSet_inter_subset_of_dSep_with_fixed`, a raw-base factorization `evalMap_valuesProjection_factors_through_latentAncestorsOfSet`, and the corrected latent-base/residual factorization `evalMap_valuesProjection_factors_through_latent_base_and_residual`. The previously stronger claim factoring through the realized `W`-values was false in general and has been removed. The remaining blockers are the two factorization helpers together with the generic product-space CI bridge `condIndepFun_of_shared_base_valuesProjection_pi` | **sorry** (isolated to named helpers) |
| `full_globalMarkov` | `Disjoint X Y → dSep X Y Z → FullCondIndep M X Y Z ... (jointKernel M s)` | Verma–Pearl induction. The disjointness hypothesis rules out the degenerate `X ∩ Y ≠ ∅` case, which is not excluded by the current Bayes-ball `dSep` definition. Body sorry-free; transitively depends on `fullCondIndep_singleton_of_dSep` (step ii) and `fullCondIndep_contraction` (step iii) | **Proved** (modulo named auxiliaries) |

### `ObsMarkov.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `obs_condIndep_of_full` | `FullCondIndep ... (jointKernel s) → ObsCondIndep ... (obsKernel s)` for `X,Y,Z ⊆ observed` | Lifts `FullCondIndep` to `ObsCondIndep` via `randomToObserved` and `condIndepFun_of_map` | **Proved** |
| `globalMarkov` | `Disjoint X Y → dSep X Y Z → ObsCondIndep M X Y Z ... (M.obsKernel s)` for `X,Y,Z ⊆ observed` | `full_globalMarkov` then `obs_condIndep_of_full`; inherits the necessary `Disjoint X Y` side condition from the full theorem | **Proved** (modulo `fullCondIndep_singleton_of_dSep` + `fullCondIndep_contraction`) |
| `globalMarkov_with_fixed` | `Disjoint X Y → dSep X Y (Z_obs ∪ Z_fix) → ObsCondIndep M X Y Z_obs ... (M.obsKernel s)` for `Z_fix ⊆ M.fixed` | Global Markov allowing fixed nodes in the d-sep condition; consumed by `do_rule1`; proved modulo the singleton-source full theorem | **Proved** (modulo `fullCondIndep_singleton_of_dSep_with_fixed`) |

**Corrected in Session 2:** The old `local_markov` (conditioning on
`Pa(v) ∩ V` only) was incorrect for gSCMs with shared latent confounders.
Replaced with `full_local_markov` conditioning on all parents `Pa(v)`
including latent ones, at the full distribution `jointKernel M s`. The
observational `globalMarkov` is *intended* to follow via `obs_condIndep_of_full`
once `full_globalMarkov` is complete.

---

## 8f. `SCM/Do/DoCalculus.lean` — do-Calculus (Pearl 1995 / MST 2019)

The three rules of do-calculus stated in **single-intervention form**
against the SCM observational kernel: every rule takes a generic SCM
`M'` and, for Rules 2 and 3, at most one extra intervention target
`Z : Finset N` applied on top.  Pearl's classical two-layer form
`do(X); do(Z)` is recovered purely as an accounting identity at the
call site by instantiating `M' := M.fixSet X …` — once `M'` is chosen,
the rule's conclusion is the same as Pearl's, with no outer-`X` layer
in the statement itself.

d-Separation is taken directly on the split SWIG graph of the relevant
SCM (`M'.dag` for Rule 1, `(M'.fixSet Z).dag` for Rule 2/3) — the
classical Pearl mutilation graphs `G_{\overline X}`, `G_{\underline Z}`
are **not** used; the former collapses into split-with-conditioning-on
the fixed nodes, the latter into split-of-`Z` (whose `random_Z` has no
outgoing).

**Pearl → split translation.**  Pearl's d-sep conditioning set
`W ∪ X` in `G_{\overline X}` becomes `W ∪ M'.fixed` in the single-
intervention setup: the fixed nodes are kernel parameters (constants)
under `obsKernel s`, so conditioning on them is free.  Callers who
want Pearl's two-layer reading pick `M' := M.fixSet X`; then
`M'.fixed ⊇ X.image .fixed` absorbs the outer-`do(X)` conditioning.

| Declaration | Shape | d-sep / non-ancestor condition | Description | Status |
|---|---|---|---|---|
| `SCM.do_rule1 M' Y Z W … hdSep s` | CI on `M'` | `dSep Y Z (W ∪ M'.fixed)` in `M'.dag`; pairwise disjointness of `Y`, `Z`, `W` | **Rule 1 (single-SCM form):** a d-sep premise on the base SCM `M'` implies `ObsCondIndep M' Y Z W` against `M'.obsKernel s`. Pearl's two-layer `(Y ⊥ Z \| W, X)_{G_{\overline X}}` is obtained by taking `M' := M.fixSet X`. | **sorry** — one-line delegation to `M'.globalMarkov_with_fixed`, which is still `sorry` |
| `SCM.do_rule2_kernel M' Z … Y W … hdSep hWNonDesc hWNonDescM1 s0 hOverlap hPositivity_ae` | a.e.-over-product equality of `obsCondKernel` values | `dSep Y (Z.image .random) (W ∪ (M'.fixSet Z).fixed)` in `(M'.fixSet Z).dag`; disjointness; `hWNonDesc`/`hWNonDescM1` (W not downstream of the intervention); `hOverlap : Rule2JointOverlap`; `hPositivity_ae` (product `νZ ⊗ μW ≪ μ_{Zr∪W}`) | **Rule 2 (action/observation exchange, single-SCM, kernel-native, witness form):** for `(νZ ⊗ₘ μW)`-a.e. treatment/conditioning pair `(t, w)`, the `Y | W`-conditional of the model intervened at `t` equals the `Y | (Z.rand ∪ W)`-conditional of `M'` at `valuesUnionMk t w`. **A.e. over the supported product marginal** — sound for continuous `Z` (never pins `obsCondKernel` on a `μ_C`-null slice; the old pointwise/`fillZrW` form `obsCondKernel_fixSet_eq` was retired for exactly that defect). Pearl's two-layer `G_{\overline X, \underline Z}` reading is recovered via `M' := M.fixSet X`. Delegates to `SCM.obsCondKernel_fixSet_eq_ae_witness`. | **Proved** (sorry-free) |
| `SCM.do_rule3 M' Z hZ_obs hZ_fixed Y W … hNoDesc s'` | `(Y ∪ W)`-marginal equality | `hNoDesc : ∀ v ∈ Y ∪ W, ∀ d ∈ Z, ¬ (M'.fixSet Z).dag.isAncestor (.fixed d) v` (plus unused `_hdSep` reserved for the full Rule 3) | **Rule 3 (insertion/deletion of actions, Rule 3\* form):** `(Y ∪ W)`-marginal of `(M'.fixSet Z).obsKernel s'` equals `(Y ∪ W)`-marginal of `M'.obsKernel (fixSetProj s')`. A single hypothesis covers both the marginal and the conditional forms (dividing by equal `W`-marginals recovers the conditional). Pearl's two-layer `G(x,z)` reading is recovered via `M' := M.fixSet X`. | **Proved** — delegates to `SCM.condDistrib_intervention_ancestral_eq` in `SCM/Do/Rule3.lean` |

**Conditional Rule 3** (`SCM/Do/Rule3Conditional.lean`).  The conditional
form `p(Y | do(z), W) = p(Y | W)` promised by the `do_rule3` row above is
formalized as `do_rule3_conditional`, in the honest a.e. `obsCondKernel`
form (the conditional counterpart of `do_rule2_kernel`).  It needs **no**
positivity/ratio infrastructure: `condDistrib` depends only on the joint
pushforward `μ.map (X, Y)`, and Rule 3\* makes the two `(W, Y)` joints
literally equal, so the conditionals coincide (there is no do-side pinning,
unlike Rule 2).  The core disintegration fact is `condDistrib_eq_of_map_prod_eq`;
the literal (non-a.e.) `condDistrib` form is `do_rule3_conditional_condDistrib`.

**Single-intervention vs. two-layer framing.**  The previous formulation
hard-wired a `(M, X, hX_obs, hX_fixed, Z, …)` shape, doubling up
every `hZ_obs`/`hZ_fixed`-style hypothesis and requiring the user to
reason about the *double-intervention* SCM `(M.fixSet X).fixSet Z`
directly.  The single-intervention shape cleanly separates "the SCM
we're reasoning on" (`M'`) from "the intervention we're comparing
against" (`Z`).  All structural content about `do(X)` lives in the
caller's choice of `M'` — the rule statements never mention `X`.

**Conclusions are mathematically the same:**

* Rule 1: `(Y ⊥ Z | W)` against `M'.obsKernel s`, extending the
  conditioning set by `M'.fixed` at the d-sep level for free (fixed
  nodes are constants under `obsKernel s`).
* Rule 2: `P(y | z, w)_{M'} = P(y | do(z), w)_{M'.fixSet Z}` — stated as
  an `=ᵐ` equality of `obsCondKernel` kernels after aligning the
  `(Z.random ∪ W)`-indexed LHS with the `W`-indexed RHS via `fillZrW`.
  Kernel-native (via Mathlib's `ProbabilityTheory.Kernel.condKernel`);
  carries `[StandardBorelSpace (ValuesOn Y (swigΩ Ω))]`,
  `[Nonempty …]`, `[IsFiniteMeasure (obsKernel …)]`, and
  `[CountableOrCountablyGenerated …]` typeclasses.
* Rule 3: `P(y, w)_{M'.fixSet Z} = P(y, w)_{M'}` — the `(Y ∪ W)`-marginal
  equality (Option B in the design note).  Simpler than the tex's
  conditional-on-`W` form but strictly implies it under the
  non-ancestry hypothesis `hNoDesc` (which covers `Y ∪ W`, not just
  `Y`).  Works for the backdoor step 3 (`Z = X_treatment`, `Y = Z_backdoor`, `W = ∅`).

**Deleted in the SCM migration:** `condDistrib_ci_invariant`,
`obs_sub_transport`, `po_rule1/2/3` (kernel-primitive Po-calculus
theorems, archived).  The earlier two-layer-only helper file
`SCM/Do/RuleSingle.lean` is also deleted — the single-intervention
shape subsumes the single-target specialization it was carrying.

References: Basic Concepts.tex, Proposition `prop:scm-docalculus`;
Pearl (2009), *Causality*, Chapter 3; Malinsky, Shpitser & Tchetgen
Tchetgen (2019).

---

## 8g. `Graph/Induce.lean` — Induced Subgraph of a SWIG Graph

Defines `SWIGGraph.induce`, the graph-level restriction to an observed
subset `R` (Basic Concepts.tex Definition 2.11). Since the April 2026
unification, the induced graph is itself a `SWIGGraph` — the old
`SubSWIGGraph` sibling structure has been deleted. This works because
the weakened `dag_edges_classified` invariant (replacing the stronger
`obs_unobs_cover_random`) is trivially preserved under edge removal.

### Restricted DAG and induce

| Definition | Signature | Description |
|---|---|---|
| `SWIGGraph.inducedEdge` | `Finset (SWIGNode N) → SWIGNode N → SWIGNode N → Prop` | `G.dag.edge u v ∧ u ∈ active ∧ v ∈ active` |
| `SWIGGraph.inducedDag` | `Finset (SWIGNode N) → DAG (SWIGNode N)` | Restricted DAG reusing `G.dag.topoOrder` |
| `SWIGGraph.induce` | `SWIGGraph N → Finset (SWIGNode N) → SWIGGraph N` | Restrict `observed` to `R ∩ observed`; drop `fixed` whose `iotaMap` image was removed; filter edges to the new active set |

### Helper lemmas

| Lemma | Statement |
|---|---|
| `inducedDag_edge_iff` | `(G.inducedDag A).edge u v ↔ G.dag.edge u v ∧ u ∈ A ∧ v ∈ A` |
| `inducedDag_parents_subset` | `(G.inducedDag A).parents v ⊆ G.dag.parents v` |
| `inducedDag_children_subset` | `(G.inducedDag A).children u ⊆ G.dag.children u` |

### Notes

- All invariants of `SWIGGraph` (including `fixed_outside_fixed_isolated`
  and `all_children_in_observed`) are re-established by `induce`; the
  proof lives entirely inside `Induce.lean` with no `sorry`s.
- Graph operations (`parents`, `children`, `descendants`, `cComponentOf`,
  `bidirectedBFS`, `directlyConfounded`) are inherited directly from
  `SWIGGraph`/`CComponents.lean` without duplication.

---

## 8h. `PO/` — Potential Outcome Framework

Namespace: `Causalean.PO`.  The potential-outcome framework is organized by
role: `PO/Core/` for graph-free regimes, systems, variables, and
counterfactual distributions; `PO/Assumptions/` for consistency and
counterfactual-independence assumptions; `PO/Conditioning/` for conditional
expectation workhorses; `PO/Bridge/` for restriction and SCM-to-PO bridges; and
`PO/Analysis/` for regression, residualization, and quantile tools.
Spec: `Basic Concepts.tex` §8 lines 812–1263 (def:po-system through
prop:po-consistency).

Current status is skeleton: types and statements are in place; proofs are
`sorry` where noted below.  See each file's spec-label tags for the
corresponding definition/proposition in the .tex.

### `PO/Core/Regime.lean` — Intervention regimes

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Regime V X` | `structure { target : Finset V, assign : ∀ v : V, v ∈ target → X v }` | Pair `(X, x)` of a target set and an assignment on it | **Defined** |
| `Regime.empty` | `Regime V X` | Empty regime `r_∅` | **Defined** |
| `Regime.Disjoint r₁ r₂` | `Prop` | Disjoint target sets | **Defined** |
| `Regime.sqcup r₁ r₂ h` | `Regime V X` | Disjoint union `r₁ ⊔ r₂` | **Defined** |
| `Regime.empty_disjoint_right` / `_left` | — | `empty` is disjoint from every regime | **Proved** |
| `Regime.sqcup_target` *(@[simp])* | `(r₁.sqcup r₂ h).target = r₁.target ∪ r₂.target` | Target of the disjoint union | `rfl` |
| `Regime.single v x` | `Regime V X` | Singleton regime `({v}, v ↦ x)`; the building block for `{Z ← z}`, `{D ← d}` style one-node interventions | **Defined** |
| `Regime.single_target` *(@[simp])* | `(single v x).target = {v}` | Target of a singleton regime is `{v}` | `rfl` |
| `Regime.single_assign_self` | `(single v x).assign v (mem_singleton_self _) = x` | Evaluating the singleton assignment at `v` returns the supplied value | `rfl` |
| `Regime.single_disjoint_single` | `v ≠ w → (single v x).Disjoint (single w y)` | Two singleton regimes on distinct nodes are disjoint | **Proved** |
| `Regime.single_disjoint_of_not_mem` | `v ∉ r.target → (single v x).Disjoint r` | Left-disjointness of a singleton with any regime missing `v` | **Proved** |
| `Regime.disjoint_single_of_not_mem` | `v ∉ r.target → r.Disjoint (single v x)` | Right-disjointness counterpart; used to chain `sqcup` of several singletons | **Proved** |
| `Regime.listLookup` | `(l : List ((v : V) × X v)) → (v : V) → v ∈ l.map Sigma.fst → X v` | Recursive lookup of the value associated with `v` in a dependent list of `(variable, value)` pairs; defined without `Nodup` so the recursion is definitional | **Defined** |
| `Regime.ofList` | `(l : List ((v : V) × X v)) → (l.map Sigma.fst).Nodup → Regime V X` | Build a regime from a list of `(variable, value)` pairs with nodup keys; target is the toFinset of the keys, assignment via `listLookup`. Used to construct multi-stage DTR regimes uniformly | **Defined** |
| `Regime.ofList_target` *(@[simp])* | `(ofList l h).target = (l.map Sigma.fst).toFinset` | Target of `ofList` is the toFinset of its keys | `rfl` |
| `Regime.ofList_nil` *(@[simp])* | `ofList [] h = empty` | The empty-list builder reduces to `Regime.empty` | **Proved** |
| `Regime.ofList_cons_target` *(@[simp])* | `(ofList (⟨v,x⟩ :: rest) h).target = insert v (rest.map Sigma.fst).toFinset` | Target of a cons unfolds as an `insert` | **Proved** |
| `Regime.listLookup_cons_self` | `listLookup (⟨v,x⟩ :: rest) v hv = x` | Looking up the head key returns the head value | **Proved** |
| `Regime.listLookup_cons_of_ne` | `v ≠ w → listLookup (⟨w,x⟩ :: rest) v hv = listLookup rest v hv'` | Lookup skips a non-matching head and recurses into the tail | **Proved** |

Reference: `Basic Concepts.tex`, def:po-system.

### `PO/Core/System.lean` — Bare PO system and PO operator

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POSystem` | `structure { V, X, Ω, μ, eval, measurable_eval }` | Bare PO system of def:po-system | **Defined** |
| `POSystem.component P r v` | `P.Ω → P.X v` | Per-coordinate potential-outcome variable `v(r)` | **Defined** |
| `POSystem.measurable_component` | `Measurable (P.component r v)` | — | **Proved** |
| `POSystem.poVariable P r Y` | `P.Ω → ValuesOn Y P.X` | Subset-valued `Y(r)` | **Defined** |
| `POSystem.measurable_poVariable` | `Measurable (P.poVariable r Y)` | — | **Proved** |
| `POSystem.poOperator P r Y` | `Measure (ValuesOn Y P.X)` | `Po^P_r(Y) := (Y(r))_# μ` | **Defined** |
| `instance …poOperator… IsProbabilityMeasure` | — | `poOperator` is a probability measure | **Proved** |

Reference: `Basic Concepts.tex`, def:po-operator.

### `PO/Core/Counterfactual.lean` — Cross-world distribution

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POSystem.crossWorldEval P qs` | `P.Ω → ∀ i : Fin qs.length, ValuesOn (qs[i].2) P.X` | Tuple of `Y_i(r_i)(ω)` | **Defined** |
| `POSystem.measurable_crossWorldEval` | — | — | **Proved** |
| `POSystem.counterfactualDist P qs` | `Measure (∀ i, ValuesOn (qs[i].2) P.X)` | Pushforward of `μ` under `crossWorldEval` | **Defined** |
| `instance …counterfactualDist… IsProbabilityMeasure` | — | — | **Proved** |
| `POSystem.counterfactualDist_marginal` | single-coord marginal = `poOperator` | rem:po-reading | **Proved** |

Reference: `Basic Concepts.tex`, def:po-counterfactual, rem:po-reading.

### `PO/Assumptions/Consistency.lean` — Consistency axiom

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POSystem.FactualAgrees P r ω` | `Prop` | Factual value of `r.target` matches `r.assign` at `ω` | **Defined** |
| `POSystem.IntermediateAgrees P r₁ r₂ ω` | `Prop` | Post-`r₁` value of `r₂.target` matches `r₂.assign` | **Defined** |
| `POSystem.Consistency P` | `structure { factual, composition }` | Two-clause consistency predicate | **Defined** |

Reference: `Basic Concepts.tex`, def:po-consistency.

Also adds, in `PO/Core/Regime.lean`:

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Regime.sqcup_assign_pos` | `v ∈ r₁.target → (r₁.sqcup r₂ h).assign v hv = r₁.assign v _` | Compute `sqcup` assignment on the left side | **Proved** |
| `Regime.sqcup_assign_neg` | `v ∉ r₁.target → v ∈ r₂.target → (r₁.sqcup r₂ h).assign v hv = r₂.assign v _` | Compute `sqcup` assignment on the right side | **Proved** |
| `Regime.ext` | `r₁.target = r₂.target → (∀ v h₁ h₂, r₁.assign v h₁ = r₂.assign v h₂) → r₁ = r₂` | Extensionality for `Regime` (subst-based, both membership proofs supplied) | **Proved** |

### `PO/Bridge/Induce.lean` — Sub-PO system (def:po-restrict)

Implements the *restricted sub-system* `P|_R` (def:po-restrict): given a PO
system `P` and `R : Finset P.V`, build a PO system whose variable type is
`↥R`, with the same probability space `(Ω, μ)` and `eval` inherited from
`P` by lifting the sub-regime back to the ambient `P.V`.  Subtype indexing
automatically enforces the spec clause that regimes target only `R`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POSystem.liftRegime P R r'` | `Regime ↥R (fun v => P.X v.val) → Regime P.V P.X` | Lift a sub-regime to the ambient regime type via `Subtype.val` embedding | **Defined** |
| `POSystem.liftRegime_target` *(@[simp])* | `(P.liftRegime R r').target = r'.target.map ⟨Subtype.val, _⟩` | Target reduction | `rfl` |
| `POSystem.liftRegime_assign` | `(P.liftRegime R r').assign w.val hv = r'.assign w hw` | Reading the lifted assignment at a known sub-regime member returns the original sub-assignment | **Proved** |
| `POSystem.liftRegime_empty` *(@[simp])* | `P.liftRegime R Regime.empty = Regime.empty` | Empty-regime preservation | **Proved** |
| `POSystem.restrict P R` | `POSystem` | The sub-PO system `P|_R` of def:po-restrict | **Defined** |
| `POSystem.restrict_V` / `_X` / `_Ω` / `_μ` *(@[simp])* | structural reductions | `↥R` / `P.X v.val` / `P.Ω` / `P.μ` | `rfl` |
| `POSystem.restrict_eval` *(@[simp])* | `(P.restrict R).eval r' ω v = P.eval (P.liftRegime R r') ω v.val` | Eval reduction via lift | `rfl` |
| `POSystem.restrict_component` *(@[simp])* | per-coord agreement with `P.component` after subtype coercion | — | `rfl` |
| `POSystem.liftRegime_disjoint` | `r₁'.Disjoint r₂' → (P.liftRegime R r₁').Disjoint (P.liftRegime R r₂')` | Disjointness lifts (subtype-injectivity) | **Proved** |
| `POSystem.liftRegime_sqcup_target` | `(P.liftRegime R (r₁'.sqcup r₂' h)).target = (P.liftRegime R r₁').target ∪ (P.liftRegime R r₂').target` | Lift commutes with `sqcup` on targets | **Proved** |
| `POSystem.liftRegime_sqcup` | `P.liftRegime R (r₁'.sqcup r₂' h) = (P.liftRegime R r₁').sqcup (P.liftRegime R r₂') (P.liftRegime_disjoint R h)` | Lift commutes with `sqcup` (full regime equality, via `Regime.ext` and `sqcup_assign_pos`/`_neg`) | **Proved** |
| `POSystem.restrict_consistency` | `P.Consistency → (P.restrict R).Consistency` | Consistency propagates to the sub-system; both clauses reduce to the ambient ones via `restrict_eval` and the lift lemmas | **Proved** |

Reference: `Basic Concepts.tex`, def:po-restrict, rem:po-restrict.

### `PO/Bridge/FromSCM.lean` — SCM → PO bridge

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `ObsIdx M` | `{v : SWIGNode N // v ∈ M.observed ∧ ∀ n, v = .random n → .fixed n ∉ M.fixed}` | Observed-node index type for the induced PO; refined to exclude already-intervened nodes so PO regimes are statically well-formed | **Defined** |
| `obsValue M v` | `Type` | Per-variable value space `swigΩ Ω v.val` | **Defined** |
| `regimeTargetN M r` | `Finset N` | Underlying `Finset N` for a regime of observed nodes | **Defined** |
| `regimeTargetN_obs` | `∀ D ∈ regimeTargetN M r, .random D ∈ M.observed` | `fixSet`-obligation: regime targets are observed | **Proved** |
| `regimeTargetN_notFixed` | `∀ D ∈ regimeTargetN M r, .fixed D ∉ M.fixed` | `fixSet`-obligation: regime targets aren't already intervened (from `ObsIdx` refinement) | **Proved** |
| `combinedFixed M s r` | `SCM.FixedValues (M.fixSet (regimeTargetN M r) …)` | Combined background + regime fixed assignment `s ⊔ x`; pinned by uniqueness via a single `Classical.choose` of `v' ∈ r.target` with `.fixed (obsName v') = v.val` | **Proved** |
| `combinedFixed_exists` *(private)* | `v.val ∉ M.fixed → ∃ v' ∈ r.target, .fixed (obsName M v') = v.val` | Existence helper for the `Classical.choose` in `combinedFixed`'s else-branch | **Proved** |
| `inducedEval M s r ℓ` | `∀ v : ObsIdx M, obsValue M v` | World-eval map of the induced PO system: project `(M.fixSet …).evalMap (combinedFixed M s r) ℓ` onto observed | **Proved** |
| `inducedEval_measurable` | `Measurable (inducedEval M s r)` | Composition of `evalMap_measurable` with `measurable_pi_apply` | **Proved** |
| `POSystem.ofSCM M s` | `POSystem` | Induced PO system `PO(M; s)` of def:po-from-scm | **Proved** |
| `obsName M v` / `obsName_spec` | `ObsIdx M → N` / `v.val = .random (obsName M v)` | Reads the underlying `N`-name of an observed `ObsIdx` element via `Classical.choose` on `observed_is_random` | **Proved** |
| `obsName_injective` | `Function.Injective (obsName M)` | Globally injective: same `obsName` ⇒ same `.val` ⇒ same `ObsIdx` | **Proved** |
| `combinedFixed_old` | `combinedFixed M s r ⟨v, mem_union_left _ hv⟩ = s ⟨v, hv⟩` | `combinedFixed` agrees with `s` on pre-existing fixed coords | **Proved** |
| `combinedFixed_new` | `combinedFixed M s r ⟨.fixed D, …⟩ = cast … (r.assign v' hv'tgt)` | `combinedFixed` reads regime intervention values on new coords; uniqueness pins the internal `Classical.choose` to `v'`, the cast collapses via `cast_heq` + `proof_irrel_heq` | **Proved** |
| `regimeTargetN_mem_val` | `D ∈ regimeTargetN M r → ∃ v' ∈ r.target, v'.val = .random D` | Inverse of `regimeTargetN`: recover the regime variable from a target name | **Proved** |
| `obsIdx_val_injective` | `v.val = w.val → v = w` | Injectivity of the `ObsIdx` coercion | **Proved** |
| `sqcup_assign_left` / `sqcup_assign_right` | `(r₁.sqcup r₂ h).assign v _ = r₁.assign v _` (resp. r₂) | `Regime.sqcup` agrees with the appropriate side on its target | **Proved** |
| `inducedEval_empty_eq_evalMap` | `inducedEval M s Regime.empty ℓ v = M.evalMap s ℓ ⟨v.val, _⟩` | Empty-regime reduction to the bare SCM `evalMap` | **Proved** |
| `regimeTargetN_empty` | `regimeTargetN M Regime.empty = ∅` | `simp` lemma | **Proved** |
| `evalMap_fixSet_transport` *(private)* | Transport `(M.fixSet X₁ _ _).evalMap` across set equality `X₁ = X₂` (via `subst` + `evalMap_eq_of_equiv` with `Equiv.refl`) | Bridges `M.fixSet (regimeTargetN M (r₁.sqcup r₂))` and `M.fixSet (regimeTargetN M r₁ ∪ regimeTargetN M r₂)` for the composition proof | **Proved** |
| `POSystem.ofSCM_consistency` | `(POSystem.ofSCM M s).Consistency` | Induced system satisfies consistency, prop:po-consistency. **factual** closes against `SCM.evalMap_fixSet_factual_eq`; **composition** transports via `evalMap_fixSet_transport` then closes against `SCM.evalMap_fixSet_union_eq` | **Proved** (modulo upstream `evalMap_fixSet_factual_eq` / `evalMap_fixSet_union_eq` `sorry`s in `CounterfactualLemmas.lean`) |

Reference: `Basic Concepts.tex`, def:po-from-scm, rem:po-induced, prop:po-consistency, rem:po-vs-do.

### `PO/Core/Variable.lean` — PO variables and regimed variables

Abstracts the `S.hXbool.measurable.comp ((measurable_pi_apply _).comp (P.measurable_eval _))`
chain used throughout IV/LATE-style proofs.  A `POVar P α` bundles a node
`v : P.V` with a measurable equivalence `P.X v ≃ᵐ α`, so factual and
counterfactual realisations land in a fixed measurable type.  `RegimedVar`
further attaches a regime and exposes a uniform `value : P.Ω → α`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POVar P α` | `structure { v : P.V, equiv : P.X v ≃ᵐ α }` | PO system variable identified with a fixed measurable value type `α` | **Defined** |
| `POVar.cf a r` | `P.Ω → α` | Counterfactual value `a` under regime `r`: `ω ↦ a.equiv (P.eval r ω a.v)` | **Defined** |
| `POVar.factual a` | `P.Ω → α` | Factual value `a.cf Regime.empty` | **Defined** |
| `POVar.measurable_cf` | `Measurable (a.cf r)` | One-liner measurability of the counterfactual map | **Proved** |
| `POVar.measurable_factual` | `Measurable a.factual` | Specialisation of `measurable_cf` to the empty regime | **Proved** |
| `POVar.event a x` | `Set P.Ω` | Preimage `a.factual ⁻¹' {x}` — the event `{a = x}` | **Defined** |
| `POVar.measurableSet_event` | `MeasurableSet (a.event x)` | Measurability of the factual event (needs `MeasurableSingletonClass α`) | **Proved** |
| `POVar.cfUnder a w y` | `P.Ω → α` | Counterfactual `a` under the single-node intervention `{w ← y}`, i.e. `a.cf (Regime.single w.v (w.equiv.symm y))`; replaces hand-rolled `DofZ z`, `YofD d` patterns | **Defined** |
| `POVar.measurable_cfUnder` | `Measurable (a.cfUnder w y)` | Measurability of the single-intervention counterfactual | **Proved** |
| `RegimedVar P α` | `structure { var : POVar P α, regime : Regime P.V P.X }` | PO variable bundled with an intervention regime | **Defined** |
| `RegimedVar.value` | `P.Ω → α` | `rv.var.cf rv.regime` — uniform evaluator used by `IndepCF` and `POCFBundle.jointValue` | **Defined** |
| `RegimedVar.measurable_value` | `Measurable rv.value` | Measurability of the uniform evaluator | **Proved** |
| `RegimedVar.ofFactual a` | `RegimedVar P α` | Factual bundling `⟨a, Regime.empty⟩` — the standard shape for "factual Z is independent of the cf bundle" | **Defined** |
| `RegimedVar.ofSingle a w x` | `RegimedVar P α` | Bundling under the single-node intervention `{w ← x}` — shortcut for `dUnderZ`/`yUnderD` style variables | **Defined** |
| `POVar.indicator a x` | `P.Ω → ℝ` | Real-valued indicator `1_{a = x}` (defined as `(a.event x).indicator 1`); subsumes per-system `indD`/`indA`/`indM` boilerplate | **Defined** |
| `POVar.indicator_eq_event_indicator` | `a.indicator x = (a.event x).indicator (fun _ => 1)` | Set-indicator form (definitional `rfl`) | **Proved** |
| `POVar.indicator_apply_eq_one` | `a.factual ω = x → a.indicator x ω = 1` | Pointwise value on `{a = x}` | **Proved** |
| `POVar.indicator_apply_eq_zero` | `a.factual ω ≠ x → a.indicator x ω = 0` | Pointwise value off `{a = x}` | **Proved** |
| `POVar.measurable_indicator` | `Measurable (a.indicator x)` | Measurability of the indicator | **Proved** |
| `POVar.stronglyMeasurable_indicator_comap` | `StronglyMeasurable[comap a.factual _] (a.indicator x)` | Strong measurability w.r.t. the `a.factual` σ-algebra | **Proved** |
| `POVar.integrable_indicator` | `[IsFiniteMeasure P.μ] → Integrable (a.indicator x) P.μ` | Indicator is bounded by 1, hence integrable | **Proved** |
| `POVar.indicator_eq_one_or_zero` | `a.indicator x ω = 1 ∨ a.indicator x ω = 0` | Binary value | **Proved** |
| `POVar.indicator_add_indicator_not` | `a.indicator true ω + a.indicator false ω = 1` | Binary-treatment complementarity (`POVar P Bool`) | **Proved** |

### `PO/Assumptions/ConsistencyLemmas.lean` — Pointwise consistency for `POVar`

Generalises the two pointwise consistency specialisations of `LATE.lean`
(`DofZ_eq_factualD_on_zEvent`, `factualY_eq_YofD_factualD`) to arbitrary
`POVar` pairs on distinct nodes.  Each lemma discharges the `Consistency.factual`
clause by reducing it to a singleton-regime agreement check.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POSystem.factualAgrees_empty` | `∀ ω, P.FactualAgrees Regime.empty ω` | Trivial base case: every `ω` factually agrees with the empty regime (target is `∅`) | **Proved** |
| `POSystem.factualAgrees_sqcup` | `{r₁ r₂ : Regime}`, `h : r₁.Disjoint r₂`, `P.FactualAgrees r₁ ω` → `P.FactualAgrees r₂ ω` → `P.FactualAgrees (r₁.sqcup r₂ h) ω` | Combinator: `FactualAgrees` for a disjoint union reduces componentwise. Lets theorem files assemble multi-target consistency hypotheses from per-variable pieces without hand-rolling a `Finset.mem_union` case split | **Proved** |
| `POVar.factualAgrees_single` | `(a : POVar P α) (x : α)`, `a.factual ω = x` → `P.FactualAgrees (Regime.single a.v (a.equiv.symm x)) ω` | Combinator: turns a factual equality `a.factual ω = x` into `FactualAgrees` for the singleton regime `{a.v ← a.equiv.symm x}`. Base case for building compound `FactualAgrees` via `factualAgrees_sqcup` | **Proved** |
| `POVar.cf_eq_factual_on_event` | `hC : P.Consistency`, `a w : POVar`, `a.v ≠ w.v`, `ω ∈ w.event y` → `a.cfUnder w y ω = a.factual ω` | Consistency of `a` under `{w ← y}`: on the event `{w = y}`, the `w`-intervened counterfactual of `a` equals the factual | **Proved** |
| `POVar.factual_eq_cfUnder_self_selected` | `hC : P.Consistency`, `a w : POVar`, `a.v ≠ w.v`, `∀ ω` → `a.factual ω = a.cfUnder w (w.factual ω) ω` | Pointwise: the factual equals the counterfactual under `{w ← factual w(ω)}` — the identity used to rewrite factual outcomes in terms of cf outcomes on the natural treatment choice | **Proved** |
| `POVar.factual_mul_indicator_eq_cfUnder_mul_indicator` | `hC : P.Consistency`, `a : POVar P ℝ`, `w : POVar P β`, `a.v ≠ w.v` → `(fun ω ↦ a.factual ω * 1_{w=y} ω) = (fun ω ↦ a.cfUnder w y ω * 1_{w=y} ω)` | Integrated form of consistency: multiplying by the indicator `1_{w=y}` lets factual and cf values be freely interchanged; workhorse of backdoor-style Step-1 rewrites | **Proved** |
| `POVar.factual_mul_indicator_eq_cfUnder_mul_indicator_fn` | `hC`, `y : POVar P ℝ`, `w : POVar P β`, `[MeasurableSingletonClass β]`, `y.v ≠ w.v` → `(fun ω ↦ y.factual ω * w.indicator x ω) = (fun ω ↦ y.cfUnder w x ω * w.indicator x ω)` | Pointwise-function variant matching the new `POVar.indicator` shape; one-line reduction to the `Set.indicator` form above. Used by backdoor/Manski/frontdoor/DTR Step-1 rewrites | **Proved** |
| `POVar.cf_eq_factual_of_factualAgrees` | `hC : P.Consistency`, `a : POVar P α`, `r : Regime`, `a.v ∉ r.target`, `P.FactualAgrees r ω` → `a.cf r ω = a.factual ω` | **Multi-target** consistency: pointwise, `a`'s counterfactual under any regime `r` (not naming `a.v`) coincides with its factual at every `ω` that factually agrees with `r`. Generalises `cf_eq_factual_on_event` from singleton to arbitrary regimes | **Proved** |
| `POVar.factual_mul_indicator_eq_cf_mul_indicator` | `hC : P.Consistency`, `a : POVar P ℝ`, `r : Regime`, `a.v ∉ r.target`, `E : Set P.Ω`, `(∀ ω ∈ E, P.FactualAgrees r ω)` → `(fun ω ↦ a.factual ω * 1_E ω) = (fun ω ↦ a.cf r ω * 1_E ω)` | **Multi-target** integrated consistency: on any event `E` of full factual agreement with `r`, the factual outcome and the multi-target counterfactual outcome are interchangeable inside `a · 1_E`. Generalises `factual_mul_indicator_eq_cfUnder_mul_indicator` to arbitrary regimes; consumed directly by `cdtr_backdoor` Step 1 | **Proved** |
| `POVar.eventCondExp_cfUnder_eq_factual_on_event` | `hC : P.Consistency`, `y : POVar P ℝ`, `a : POVar P β`, `y.v ≠ a.v` → `eventCondExp μ (a.event a₀) (y.cfUnder a a₀) = eventCondExp μ (a.event a₀) y.factual` | Consistency-on-event for `eventCondExp`: on the stratum `{a = a₀}`, the `{a ← a₀}`-counterfactual and the factual have the same event-level conditional mean. Shared rewrite in `Manski/{MTR,MTS}` and (via the `Fintype` total law below) the integrated MIV bounds; lives in `EventCondExp.lean` alongside the other `eventCondExp` tooling | **Proved** |

### `PO/Assumptions/IndepCF.lean` — Independence of PO counterfactual bundles

Named shapes for `IndepFun` / `CondIndepFun` over a heterogeneous bundle of
regimed PO variables.  Replaces bespoke product-tuple encodings such as the
`cfTuple` pattern in `PO/ID/Exact/LATE.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POCFBundle P` | `structure { n : ℕ, type : Fin n → Type*, inst : ∀ i, MeasurableSpace (type i), vars : ∀ i, RegimedVar P (type i) }` | Heterogeneous finite-length bundle of regimed PO variables over a fixed PO system | **Defined** |
| `POCFBundle.instMeasurableSpaceType` | `MeasurableSpace (B.type i)` | Per-component measurable-space instance derived from the `inst` field (for typeclass inference) | **Instance** |
| `POCFBundle.jointValue B` | `P.Ω → (∀ i : Fin B.n, B.type i)` | Joint evaluator returning the dependent tuple of cf values | **Defined** |
| `POCFBundle.measurable_jointValue` | `Measurable B.jointValue` | Measurability of the joint evaluator (via `measurable_pi_lambda`) | **Proved** |
| `POCFBundle.nil P` | `POCFBundle P` | Empty bundle (length 0) | **Defined** |
| `POCFBundle.cons a B` | `POCFBundle P` | Prepend a `RegimedVar` to a bundle (length `B.n + 1`); used to build the `(D(1), D(0), Y(1), Y(0))` bundle in LATE | **Defined** |
| `POSystem.IndepCF` | `RegimedVar P α → POCFBundle P → Measure P.Ω → Prop` | `IndepFun a.value B.jointValue μ` — uniform shape for "regimed variable ⟂ bundle" | **Defined** |
| `POSystem.CondIndepCF` | `RegimedVar P α → POCFBundle P → RegimedVar P γ → Measure P.Ω → Prop` | Conditional independence of `a` from `B` given σ(`c.value`); requires `StandardBorelSpace P.Ω` | **Defined** |
| `IndepCF.toIndepFun` / `IndepCF.ofIndepFun` | `P.IndepCF a B μ ↔ IndepFun a.value B.jointValue μ` | Trivial bridges between `IndepCF` and the underlying `IndepFun` | **Proved** (`id`) |
| `CondIndepCF.toCondIndepFun` | `P.CondIndepCF a B c μ → CondIndepFun (comap c.value _) _ a.value B.jointValue μ` | Unfolding bridge to Mathlib's `CondIndepFun` with the comap σ-algebra | **Proved** (`id`) |
| `POSystem.condIndepCF_congr_cond` | `comap c.value = comap c'.value → P.CondIndepCF a B c μ → P.CondIndepCF a B c' μ` | Transports conditional independence across conditioning variables that generate the same σ-algebra | **Proved** |
| `IndepCF.project` | `P.IndepCF a B μ → Measurable ψ → IndepFun a.value (ψ ∘ B.jointValue) μ` | Push `IndepCF` through an arbitrary measurable projection `ψ : (∀ i, B.type i) → β` — replaces open-coded `.comp measurable_id` dances in ATE/LATE | **Proved** |
| `IndepCF.component` | `P.IndepCF a B μ → (i : Fin B.n) → IndepFun a.value (fun ω ↦ B.jointValue ω i) μ` | Specialisation of `project` to single-coordinate extraction (e.g. pick out `Y(d)` from a joint cf bundle) | **Proved** |
| `CondIndepCF.project` | `P.CondIndepCF a B c μ → Measurable ψ → CondIndepFun (comap c.value _) _ a.value (ψ ∘ B.jointValue) μ` | Analogue of `IndepCF.project` at the conditional-independence level | **Proved** |
| `CondIndepCF.component` | `P.CondIndepCF a B c μ → (i : Fin B.n) → CondIndepFun (comap c.value _) _ a.value (fun ω ↦ B.jointValue ω i) μ` | Analogue of `IndepCF.component` at the conditional-independence level | **Proved** |

### `PO/Conditioning/Bundle.lean` — Bundle-conditioned conditional expectation

Lifts the single-`POVar` conditioning machinery in `CondExpTooling.lean` to a
`POCFBundle`, i.e. conditioning on the *joint* cf-value map of a tuple of
regimed variables.  The conditioning σ-algebra `B.sigma` is
`MeasurableSpace.comap B.jointValue inferInstance`.  Used by the multi-stage
DTR / sequential-backdoor identification arguments (`PO/ID/Exact/DTR/`)
where the conditioning σ-algebra is generated by a *bundle* of covariates rather
than a single one; mirrors all the lemmas of `POVar.condExpGiven` /
`POVar.condExpRatio` at the bundle level.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POCFBundle.sigma B` | `MeasurableSpace P.Ω` | `MeasurableSpace.comap B.jointValue inferInstance` — the σ-algebra generated by the bundle's joint cf-value map | **Defined** |
| `POCFBundle.sigma_le` | `B.sigma ≤ inferInstance` | `B.sigma` is a sub-σ-algebra of the ambient σ-algebra on `P.Ω` | **Proved** |
| `POCFBundle.condExpGiven B g μ` | `POCFBundle P → (P.Ω → ℝ) → Measure P.Ω → (P.Ω → ℝ)` | `μ[g \| B.sigma]` — Mathlib `condExp` on the bundle's comap σ-algebra (bundle analogue of `POVar.condExpGiven`) | **Defined** |
| `POCFBundle.stronglyMeasurable_condExpGiven_comap` | `StronglyMeasurable[B.sigma] (B.condExpGiven g μ)` | `condExpGiven g` is strongly measurable w.r.t. the sub-σ-algebra `B.sigma` | **Proved** |
| `POCFBundle.stronglyMeasurable_condExpGiven` | `StronglyMeasurable (B.condExpGiven g μ)` | `condExpGiven g` is strongly measurable w.r.t. the ambient σ-algebra | **Proved** |
| `POCFBundle.integrable_condExpGiven` | `Integrable (B.condExpGiven g μ) μ` | `condExpGiven g` is integrable | **Proved** |
| `POCFBundle.condExpGiven_tower_of_le` | `m ≤ B.sigma → [SigmaFinite (μ.trim _)] → μ[B.condExpGiven g μ \| m] =ᵐ[μ] μ[g \| m]` | Tower property against an arbitrary smaller sub-σ-algebra `m ≤ B.sigma`; bundle analogue of `POVar.condExpGiven_tower_of_le` | **Proved** |
| `POCFBundle.condExpGiven_mul_of_stronglyMeasurable_left` | `StronglyMeasurable[B.sigma] f → Integrable (f*g) μ → Integrable g μ → B.condExpGiven (f*g) μ =ᵐ[μ] f * B.condExpGiven g μ` | Pull-out-left: a `B.sigma`-measurable factor pulls outside `μ[·\|B.sigma]` | **Proved** |
| `POCFBundle.condExpGiven_mul_of_stronglyMeasurable_right` | Symmetric right-pull-out | Pull-out-right form of the same identity | **Proved** |
| `POCFBundle.condExpGiven_indicator_mul` | `MeasurableSet[B.sigma] s → … → B.condExpGiven (1_s * g) μ =ᵐ[μ] 1_s * B.condExpGiven g μ` | Specialisation of pull-out-left to indicators of `B.sigma`-measurable sets | **Proved** |
| `POCFBundle.condExpGiven_congr_ae` | `f =ᵐ[μ] g → B.condExpGiven f μ =ᵐ[μ] B.condExpGiven g μ` | a.e. congruence: equal-a.e. integrands give equal-a.e. bundle conditional expectations | **Proved** |
| `POCFBundle.condExpRatio B g h μ` | `POCFBundle P → (P.Ω → ℝ) → (P.Ω → ℝ) → Measure P.Ω → (P.Ω → ℝ)` | Pointwise ratio `B.condExpGiven g μ / B.condExpGiven h μ` — bundle analogue of `POVar.condExpRatio`, used to package the inner/outer adjusted regressions in DTR | **Defined** |
| `POCFBundle.measurable_condExpRatio` | `Measurable (B.condExpRatio g h μ)` | Pointwise-division measurability of `condExpRatio` | **Proved** |
| `POCFBundle.stronglyMeasurable_condExpRatio` | `StronglyMeasurable (B.condExpRatio g h μ)` | Strong measurability of `condExpRatio` | **Proved** |
| `POCFBundle.condExpRatio_eq_of_mul` | `B.condExpGiven g μ =ᵐ B.condExpGiven h μ * target → (∀ᵐ ω, B.condExpGiven h μ ω ≠ 0) → B.condExpRatio g h μ =ᵐ[μ] target` | Product→ratio characterisation; the exact shape consumed by `cdtr_backdoor` | **Proved** |
| `POSystem.CondIndepCFBundle` | `RegimedVar P α → POCFBundle P → POCFBundle P → Measure P.Ω → Prop` | Bundle-conditioned conditional independence: `CondIndepFun C.sigma C.sigma_le a.value B.jointValue μ` — bundle analogue of `POSystem.CondIndepCF`; requires `StandardBorelSpace P.Ω` | **Defined** |
| `CondIndepCFBundle.toCondIndepFun` | `P.CondIndepCFBundle a B C μ → CondIndepFun C.sigma C.sigma_le a.value B.jointValue μ` | Trivial unfolding bridge to Mathlib's `CondIndepFun` against `C.sigma` | **Proved** (`id`) |
| `CondIndepCFBundle.project` | `P.CondIndepCFBundle a B C μ → Measurable ψ → CondIndepFun C.sigma C.sigma_le a.value (ψ ∘ B.jointValue) μ` | Push `CondIndepCFBundle` through an arbitrary measurable projection of the bundle joint value — analogue of `CondIndepCF.project` for bundle-conditioned CI | **Proved** |

### `PO/Conditioning/EventCondExp.lean` — Event-level conditional expectation and drop-of-conditioning

Thin wrapper around `∫_A g ∂μ / (μ A).toReal` paired with a **drop-of-conditioning**
lemma: when the event-defining variable is `IndepCF` of a bundle `B`,
conditioning on a `value = x` event has no effect on `∫ h ∘ B.jointValue`.
Both multiplied and quotient forms are provided.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `eventCondExp μ A g` | `ℝ`, defined as `(∫ ω in A, g ω ∂μ) / (μ A).toReal` | Event-level conditional expectation, packaging the hand-rolled `condExpDZ` / `condExpYZ` idiom as a standalone operator | **Defined** |
| `eventCondExp_mul_measure_toReal` | `[IsFiniteMeasure μ]`, `A : Set Ω`, `f : Ω → ℝ` → `eventCondExp μ A f * (μ A).toReal = ∫ ω in A, f ω ∂μ` | Quotient-to-set-integral conversion (including the zero-measure case, where both sides collapse to `0`). Domain-agnostic; consumed by the Fintype total law below and by the Manski MTR/MTS set-integral manipulations | **Proved** |
| `integral_eq_sum_measure_mul_eventCondExp` | `[Fintype ι]`, `[IsFiniteMeasure μ]`, `A : ι → Set Ω` measurable / pairwise disjoint / covering `univ`, `f` integrable → `∫ f = ∑ i, (μ (A i)).toReal * eventCondExp μ (A i) f` | **Finite-partition total law.** Instantiates at `ι := Bool` for `Manski/{MTR,MTS}` binary decompositions and at `ι := α` (discrete IV value space) for the integrated MIV bounds. Domain-agnostic — an upstream-ready identity | **Proved** |
| `eventCondExp_eq_sum_condProb_mul_eventCondExp` | `[Fintype ι]`, `[IsFiniteMeasure μ]`, `A` measurable, `C : ι → Set Ω` measurable / pairwise disjoint / covering `univ`, `f` integrable → `eventCondExp μ A f = ∑ i, (μ (A ∩ C i)).toReal / (μ A).toReal * eventCondExp μ (A ∩ C i) f` | **Conditional finite-partition total law (law of iterated expectations).** `E[f∣A] = ∑ i P(C i∣A)·E[f∣A ∩ C i]`. The measure-theoretic core of the finite→population bridge for cell-indexed estimands: a finite covariate-weighted average of within-cell means equals the population conditional expectation `E[f∣G=g]` when the weights are the conditional cell probabilities. Domain-agnostic | **Proved** |
| `eventCondExp_congr_ae` | `f =ᵐ[μ] g → eventCondExp μ A f = eventCondExp μ A g` | a.e.-equal integrands give equal `eventCondExp`; shared congruence used across DID / Manski consistency rewrites | **Proved** |
| `eventCondExp_congr_on` | `MeasurableSet A → (∀ ω ∈ A, f ω = g ω) → eventCondExp μ A f = eventCondExp μ A g` | Equal-on-`A` integrands give equal `eventCondExp`; specialisation of the a.e. version to a pointwise identity on a measurable set | **Proved** |
| `eventCondExp_mono_ae` | `IntegrableOn f A μ → IntegrableOn g A μ → f ≤ᵐ[μ] g → eventCondExp μ A f ≤ eventCondExp μ A g` | Monotonicity of `eventCondExp` under an a.e. inequality of integrable functions; workhorse of the Manski stratum-sandwich bounds | **Proved** |
| `eventCondExp_add` | `IntegrableOn g₁ A μ`, `IntegrableOn g₂ A μ` → `eventCondExp μ A (g₁ + g₂) = eventCondExp μ A g₁ + eventCondExp μ A g₂` | Additivity | **Proved** |
| `eventCondExp_sub` | Analogous subtraction identity | Subtraction | **Proved** |
| `eventCondExp_smul` | `eventCondExp μ A (c • g) = c * eventCondExp μ A g` | Scalar linearity | **Proved** |
| `eventCondExp_of_ae_eq_IndepFun` | `IndepFun z B μ`, measurable `z`, `B`, `h`, `factualF =ᵐ[μ.restrict (z ⁻¹' {x})] h ∘ B`, `0 < (μ (z ⁻¹' {x})).toReal` → `eventCondExp μ (z ⁻¹' {x}) factualF = ∫ h (B ω) ∂μ` | Plain-`IndepFun` consistency-on-cell + drop-of-conditioning helper, reusable when the event variable is not packaged as a `POVar`; used by Angrist-Imbens factual Wald reductions | **Proved** |
| `POSystem.integral_restrict_value_eq_mul_of_IndepCF` | `P.IndepCF rv B μ`, `Measurable h` → `∫ ω in rv.value ⁻¹' {x}, h (B.jointValue ω) ∂μ = (μ (rv.value ⁻¹' {x})).toReal * ∫ ω, h (B.jointValue ω) ∂μ` | Drop-of-conditioning (multiplied form): the core identity, direct application of `IndepFun.integral_restrict_preimage_eq_mul` | **Proved** |
| `POSystem.integral_event_eq_mul_of_IndepCF` | Same identity with `A = a.event x` for factual `POVar a` | Specialisation to the factual event `{a = x}` | **Proved** |
| `POSystem.eventCondExp_eq_integral_of_IndepCF` | Under `IndepCF` and `μ A ∉ {0, ⊤}`, `eventCondExp μ (rv.value ⁻¹' {x}) (h ∘ B.jointValue) = ∫ h ∘ B.jointValue ∂μ` | Quotient form of drop-of-conditioning | **Proved** |
| `POSystem.eventCondExp_event_eq_integral_of_IndepCF` | Quotient form specialised to `a.event x` | Specialisation used directly in `late_wald`-style derivations | **Proved** |
| `POSystem.eventCondExp_of_consistency_IndepCF` | `P.IndepCF (ofFactual a) B μ`, `Measurable h`, `(∀ ω ∈ a.event x, factualF ω = h (B.jointValue ω))`, `μ (a.event x) ∉ {0, ⊤}` → `eventCondExp μ (a.event x) factualF = ∫ h (B.jointValue ω) ∂μ` | Workhorse combo: consistency-on-event rewrite + drop-of-conditioning in one call; collapses the repeated `hCE` blocks of `first_stage_identity` / `reduced_form_identity` | **Proved** |

### `PO/Conditioning/EventCondExpBundle.lean` — bundle analogue of `eventCondExp_of_consistency_IndepCF`

Lifts the single-period `EventCondExp` workhorse to the bundle-conditional setting (`P.CondIndepCFBundle a B C`) used by the Dynamic LATE bridges.  Per-stage workhorse: under bundle-conditional independence + consistency-on-event, the bundle conditional expectation of `factualF · 1_{a=x}` factors into the conditional expectation of `h ∘ B.jointValue` times the conditional expectation of `1_{a=x}`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POCFBundle.condExpGiven_mul_of_consistency_CondIndepCFBundle` | `P.CondIndepCFBundle (ofFactual a) B C μ`, `Measurable h`, `(factualF · 1_{a=x}) =ᵐ (h ∘ B.jointValue) · 1_{a=x}` → `C.condExpGiven (factualF · 1_{a=x}) =ᵐ C.condExpGiven (h ∘ B.jointValue) · C.condExpGiven (1_{a=x})` | Product form of the bundle workhorse.  Proof reduces to `Causalean.condExp_mul_of_condIndep` with `f := a.factual`, `g := B.jointValue`, `u y := ({x} : Set α).indicator 1`, `v := h`, exploiting `(RegimedVar.ofFactual a).value = a.factual` (rfl). | **Proved** |
| `POCFBundle.condExpRatio_of_consistency_CondIndepCFBundle` | Same hypotheses + `∀ᵐ ω, C.condExpGiven (1_{a=x}) ω ≠ 0` → `C.condExpRatio (factualF · 1_{a=x}) (1_{a=x}) =ᵐ C.condExpGiven (h ∘ B.jointValue)` | Ratio form, derived from the product form via `condExpRatio_eq_of_mul`. | **Proved** |

### `PO/Conditioning/CondExpTooling.lean` — σ-algebra-based conditional expectation

Thin wrapper around `MeasureTheory.condExp` keyed by the factual map of a
`POVar`.  Continuous-`X` companion to the event-based `eventCondExp` in
`EventCondExp.lean`; used by the backdoor ATE argument under the PO
framework, where the conditioning covariate may be continuous and the
integrand has the shape `Y(d)·1{D=d}` or `1{D=d}`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POVar.condExpGiven c g μ` | `POVar P γ → (P.Ω → ℝ) → Measure P.Ω → (P.Ω → ℝ)` | `μ[g \| σ(c.factual)]` — Mathlib `condExp` on the comap σ-algebra generated by `c.factual` | **Defined** |
| `POVar.comap_factual_le` | `MeasurableSpace.comap c.factual inferInstance ≤ mΩ` | The conditioning σ-algebra `σ(c.factual)` is a sub-σ-algebra of the ambient one on `P.Ω` | **Proved** |
| `POVar.condExpGiven_add` | `Integrable f μ → Integrable g μ → c.condExpGiven (f+g) μ =ᵐ[μ] c.condExpGiven f μ + c.condExpGiven g μ` | Additivity of `condExpGiven` (a.e.) | **Proved** |
| `POVar.condExpGiven_sub` | Analogous subtraction identity | Subtraction linearity of `condExpGiven` (a.e.) | **Proved** |
| `POVar.condExpGiven_smul` | `c.condExpGiven (k • g) μ =ᵐ[μ] k • c.condExpGiven g μ` | Scalar homogeneity of `condExpGiven` (a.e.) | **Proved** |
| `POVar.stronglyMeasurable_condExpGiven_comap` | `StronglyMeasurable[σ(c.factual)] (c.condExpGiven g μ)` | `condExpGiven g` is strongly measurable w.r.t. the sub-σ-algebra `σ(c.factual)` | **Proved** |
| `POVar.stronglyMeasurable_condExpGiven` | `StronglyMeasurable (c.condExpGiven g μ)` | `condExpGiven g` is strongly measurable w.r.t. the ambient σ-algebra | **Proved** |
| `POVar.integrable_condExpGiven` | `Integrable (c.condExpGiven g μ) μ` | `condExpGiven g` is integrable | **Proved** |
| `POVar.integrable_mul_indicator` | `Integrable f P.μ → Measurable f → Integrable (fun ω => f ω * a.indicator x ω) P.μ` | Multiplication by a factual `{0,1}`-valued `POVar` indicator preserves integrability; removes repeated bounded-indicator proofs in ATE, DTR, and Manski arguments | **Proved** |
| `POVar.condExpGiven_mul_of_stronglyMeasurable_left` | `StronglyMeasurable[σ(c.factual)] f → Integrable (f*g) μ → Integrable g μ → c.condExpGiven (f*g) μ =ᵐ[μ] f * c.condExpGiven g μ` | Pull-out-left: a `σ(c.factual)`-measurable factor pulls outside `μ[·\|σ(c.factual)]` | **Proved** |
| `POVar.condExpGiven_mul_of_stronglyMeasurable_right` | Symmetric right-pull-out | Pull-out-right form of the same identity | **Proved** |
| `POVar.condExpGiven_indicator_mul` | `MeasurableSet[σ(c.factual)] s → … → c.condExpGiven (1_s * g) μ =ᵐ[μ] 1_s * c.condExpGiven g μ` | Specialisation of pull-out-left to indicators of `σ(c.factual)`-measurable sets | **Proved** |
| `POVar.condExpGiven_tower_of_le` | `m ≤ σ(c.factual) → [SigmaFinite (μ.trim …)] → μ[c.condExpGiven g μ \| m] =ᵐ[μ] μ[g \| m]` | Tower property against an arbitrary smaller sub-σ-algebra `m ≤ σ(c.factual)` | **Proved** |
| `POVar.condExpGiven_tower` | Between two POVars with `σ(c₁.factual) ≤ σ(c₂.factual)`: `c₁.condExpGiven (c₂.condExpGiven g μ) μ =ᵐ[μ] c₁.condExpGiven g μ` | Two-POVar tower property | **Proved** |
| `POVar.condExpRatio c g h μ` | `POVar P γ → (P.Ω → ℝ) → (P.Ω → ℝ) → Measure P.Ω → (P.Ω → ℝ)` | Pointwise ratio `c.condExpGiven g μ / c.condExpGiven h μ` — the adjusted-regression functional used in backdoor ATE | **Defined** |
| `POVar.measurable_condExpRatio` | `Measurable (c.condExpRatio g h μ)` | Pointwise-division measurability of `condExpRatio` | **Proved** |
| `POVar.stronglyMeasurable_condExpRatio` | `StronglyMeasurable (c.condExpRatio g h μ)` | Strong measurability of `condExpRatio` | **Proved** |
| `POVar.condExpRatio_eq_of_mul` | `c.condExpGiven g μ =ᵐ c.condExpGiven h μ * target → (∀ᵐ ω, c.condExpGiven h μ ω ≠ 0) → c.condExpRatio g h μ =ᵐ[μ] target` | Product→ratio characterisation; the exact shape consumed by `cate_backdoor` Step 4 | **Proved** |
| `POVar.integral_sub_eq_integral_sub_of_condExpGiven_ae_eq` | `Integrable f μ`, `Integrable g μ`, `c.condExpGiven f μ =ᵐ f'`, `c.condExpGiven g μ =ᵐ g'` → `∫ (f - g) ∂μ = ∫ (f' - g') ∂μ` | CATE-to-ATE integrator: lifts an a.e. identity of conditional expectations to an identity of unconditional integrals of differences; closes `ate_backdoor` in one line | **Proved** |

---

### `PO/Analysis/Quantile.lean` — Distribution, cdf, and quantile of a real potential outcome

Distributional reading of a real-valued potential outcome: for a real `POVar a`
and regime `r`, the counterfactual `a.cf r : P.Ω → ℝ` has a law, cdf, and
quantile. Gives meaning to "the `τ`-quantile of `Y(d)`"; input to the QTE
(`PO/ID/Exact/QTE/QuantileEffect.lean`). Builds on `Stat/Quantile/Quantile.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POVar.cfLaw a r μ` | `Measure ℝ` | Law of the potential outcome `a(r)`: `μ.map (a.cf r)`. | **Defined** |
| `POVar.instIsProbabilityMeasureCfLaw` | instance | `cfLaw` is a probability measure when `μ` is. | **Proved** |
| `POVar.cfCDF a r μ` | `StieltjesFunction ℝ` | Distributional potential outcome `F_{a(r)} = cdf (a.cfLaw r μ)`. | **Defined** |
| `POVar.cfQuantile a r μ τ` | `ℝ` | `τ`-quantile of `a(r)`: `Stat.quantile (a.cfLaw r μ) τ`. | **Defined** |
| `POVar.cfCDF_eq_measureReal` | `a.cfCDF r μ y = (a.cfLaw r μ).real (Iic y)` | The cdf at `y` is `P(a(r) ≤ y)`. | **Proved** |
| `POVar.cfUnderLaw a w y μ` | `Measure ℝ` | Law of `a` under single intervention `{w ← y}` (the `Y(d)` shape). | **Defined** |
| `POVar.cfUnderQuantile a w y μ τ` | `ℝ` | `τ`-quantile of `a` under `{w ← y}` — the `τ`-quantile of `Y(d)`. | **Defined** |
| `POVar.cfUnderQuantile_eq` | `= Stat.quantile (a.cfUnderLaw w y μ) τ` | Unfolds `cfUnderQuantile` to the law-quantile. | `rfl` |

## 9. `SCM/ID/Identifiable.lean` — Identifiability

Namespace: `Causalean.SCM.ID`.

### Identifiability definitions

Identifiability is phrased in terms of **measure-theoretic observational
equivalence** against the SCM observational kernel: `obsEquiv M₁ M₂` is
`HEq` of `Causalean.SCM.obsKernel M₁` and `Causalean.SCM.obsKernel M₂` (the `HEq`
is necessary because `obsKernel` has dependent domain/codomain via
`FixedValues M` and `ObservedValues M`).

| Definition | Signature | Description |
|---|---|---|
| `CausalQuery` | `SCM N Ω → α` | Functional of a gSCM (ATE, LATE, interventional law, etc.) |
| `obsEquiv` | `SCM N Ω → SCM N Ω → Prop` | `HEq (SCM.obsKernel M₁) (SCM.obsKernel M₂)` |
| `Identifiable` | `SWIGGraph N → CausalQuery N Ω α → Prop` | Same SWIG graph (`M.toSWIGGraph = G`, pinning observed/latent/fixed + edges) + `obsEquiv` ⟹ same query value. Faithful to `def:scm-identifiability` (model class shares a SWIG graph, not merely a DAG) |
| `NonIdentifiable` | `SWIGGraph N → CausalQuery N Ω α → Prop` | Negation of `Identifiable` |
| `IdentifiableUnder` | `SWIGGraph N → Af → As → CausalQuery N Ω α → Prop` | Identifiable given functional (`Af`) and structural (`As`) assumption predicates over `SCM N Ω`, within the model class sharing SWIG graph `G` |

### Theorems

| Theorem | Statement | Description |
|---|---|---|
| `nonIdentifiable_iff` | Non-identifiability iff there exist two models with the same SWIG graph and `obsEquiv` but different query values | Equivalence characterization of non-identifiability |
| `identifiable_eq_identifiableUnder_true` | Identifiability without assumptions is the special case `Af = As = fun _ => True` | Links unconditional identifiability to `IdentifiableUnder` with trivial assumptions |
| `identifiableUnder_mono` | Stronger assumptions `(Af₂, As₂)` that imply `(Af₁, As₁)` preserve identifiability | Monotonicity: identifiability under weaker assumptions follows from identifiability under stronger ones |

---

## 9a. `SCM/ID/Overlap.lean` — Rule 2 / backdoor overlap predicate

The kernel-level absolute-continuity overlap predicate consumed by the
kernel-native do-calculus Rule 2 and the backdoor / frontdoor identification
theorems.  (The quantitative small-value/weak-overlap rate analysis — polynomial
lower-tail inverse moments — is developed separately under `Stat/PolynomialTail/`.)

| Definition | Signature | Description | Status |
|---|---|---|---|
| `Rule2JointOverlap M' Z hZ_obs hZ_fixed W hZrW s'` | `Prop` | **Canonical Rule 2 overlap.** Absolute continuity `((M'.fixSet Z).obsKernel s').map π_{Zr∪W} ≪ (M'.obsKernel ∘ fixSetProj s').map π_{Zr∪W}` — a kernel-level AC predicate with no pointwise singleton positivity requirement. Holds trivially in the discrete case; holds in the continuous case whenever the SCM's structural functions are measurable and latents match. Hypothesis of `obsCondKernel_fixSet_eq_ae_witness` (canonical posterior witness-kernel Rule 2) and through it of `do_rule2_kernel` and backdoor / frontdoor identification. | Complete |

Reference: Basic Concepts.tex, Definition 2.12 (Overlap assumption).

---

## 9b. Structural Assumption Ownership

Causalean no longer carries a standalone IV/DID/RDD/proxy assumption catalogue.
Structural assumptions are owned by the theorem module that consumes them
(`PO/ID/Exact/*`, `PO/ID/Partial/*`, or downstream CausalSmith artifacts), while
`SCM/ID/Identifiable.lean` still provides generic `Prop`-valued assumption slots
for abstract identifiability statements.

---

## 9c. `SCM/ID/GraphicalThms/` — Graphical Identification Theorems

Formalizes Basic Concepts.tex §4.1.  Several live files:

* `InducedSubgraph.lean` — graph-only utilities (ancestral closure,
  proper-descendant / non-descendant sets in `G_R`).
* `CComponentFactor.lean` — c-factor definition (`qFactor`),
  c-component parent helpers, and the per-node chain-rule factorization
  theorem `M.obsKernel s = M.qFactorProduct s` (Tian full-history
  convention).
* `QFactorIdentity.lean` — Tian's Q-factor identity (Prop 2.19),
  intervention-target simplification (`fact4`), and the district-id
  corollary.  The Q-factor identity is currently formalized as the
  real marginal fixing identity obtained from Rule 3 and
  `induce_marginal_compat`; the final conditional-`qFactor` reindexing
  remains future work.

The old kernel-era files (`Fixable.lean`, `FixingStep.lean`,
`CFactor.lean`) are **archived** under
`archive/SCM/ID/GraphicalThms/`.  The Shpitser–Pearl c-component
recursion (the chosen design path for the Session-5 ID algorithm) does
not consume the fixing-step machinery, so it is no longer in the build.

### `InducedSubgraph.lean`

| Definition | Description |
|---|---|
| `SWIGGraph.isAncestrallyClosed` | Every observed parent of `v ∈ G.observed` is itself in `G.observed`; `Decidable` |
| `InducedFrom G R` | `G.induce R` — the `G_R` notation from the tex |
| `SWIGGraph.properDescIn G R v₀` | `D := (G.induce R).dag.descendants v₀` — proper descendants of `v₀` in `G_R`; `DAG.descendants` is already irreflexive |
| `SWIGGraph.nonDescIn G R v₀` | `T := (R.erase v₀) \ (G.induce R).dag.descendants v₀` — non-descendants of `v₀` in `G_R`, matching the tex convention that `De(v₀)` includes `v₀` |
| `v₀_not_mem_properDescIn` | `v₀ ∉ G.properDescIn R v₀` — irreflexivity |
| `v₀_not_mem_nonDescIn` | `v₀ ∉ G.nonDescIn R v₀` — by the `erase` in the definition |
| `properDescIn_subset_erase` | `G.properDescIn R v₀ ⊆ R.erase v₀` — **sorry** (needs `descendants_induce_subset`) |
| `nonDescIn_subset_erase` | `G.nonDescIn R v₀ ⊆ R.erase v₀` — proven via `Finset.sdiff_subset` |
| `nonDescIn_subset` | `G.nonDescIn R v₀ ⊆ R` |
| `properDescIn_disjoint_nonDescIn` | `Disjoint (properDescIn R v₀) (nonDescIn R v₀)` |
| `properDescIn_union_nonDescIn_eq_erase` | `D ∪ T = R.erase v₀` (for `v₀ ∈ R`) — **sorry** (needs the subset helper) |

### `CComponentFactor.lean` — c-component factorization (Tian, 2002)

Formalizes `thm:scm-c-factor` (Basic Concepts.tex:662–672) against
`obsKernel`.  The Pa⁺ semantics is the standard Tian (2002) convention:
*topological predecessors*, i.e. observed nodes preceding `v` in a fixed
topological order — not the direct-parent set.

| Definition / Statement | Description |
|---|---|
| `SWIGGraph.observedPredecessors v` | `Pa⁺_G(v) := {w ∈ G.observed | topoOrder w < topoOrder v}` |
| `SWIGGraph.qFactorParents C` | `(⋃_{v ∈ C} Pa⁺_G(v)) \ C` — conditioning set of `Q[C]` |
| `observedPredecessors_subset_observed` / `qFactorParents_subset_observed` | sanity lemmas (proven) |
| `SCM.qFactor M C hC s` | Tian's c-component factor `Q[C]` as a slice of `obsCondKernel M C (qFactorParents C)` at `s` (via `Kernel.comap`); requires `StandardBorelSpace`, `Nonempty`, `IsFiniteMeasure`, `CountableOrCountablyGenerated` instances |
| `SCM.c_component_factorization` | `M.obsKernel s = M.qFactorProduct s`; per-node chain-rule factorization over observed nodes, now sorry-free via `obsKernel_eq_qFactorProduct`; c-component grouping is deferred to a later theorem |

### `QFactorIdentity.lean`

Hypothesis-quantified frames for Tian's identity, fact4, and the
district-id corollary.  The statements are non-vacuous: Tian's identity is the
conditional q-factor equality (a.e. under the induced parent marginal), the
marginal fixing identity is retained as a proved helper, and fact4 is an
`SCM.Equiv` statement.

| Statement | Description | Status |
|---|---|---|
| `QFactorMarginalFixingConclusion M R T Wn` | Under ancestral closure, `T ⊆ (M.induce R).observed`, `Wn.image .random = R \ T`, and the Rule-3 non-descendant condition on `T`, the `T`-marginal of `(M.fixSet Wn).obsKernel` equals the `T`-marginal of `(M.induce R).obsKernel` at the projected fixed slice | **Proved** marginal fixing identity |
| `q_factor_marginal_fixing` | Proves the marginal fixing form by composing `condDistrib_intervention_ancestral_eq` with `induce_marginal_compat` and projection composition | **Proved** |
| `QFactorIdentityConclusion M R T Wn` | Conditional Tian identity: the induced `qFactor (M.induce R) T` equals a.e. the `T | qFactorParents T` conditional extracted from `(M.fixSet Wn).obsKernel`, with the non-descendant condition strengthened to `T ∪ qFactorParents T` for the needed joint identity | Real conditional statement |
| `q_factor_identity` | Proves the conditional Q-factor identity by first deriving the joint law identity on `T ∪ qFactorParents T`, reindexing it to the `(qFactorParents T, T)` pair law, then applying `Kernel.condKernel` uniqueness/disintegration under the common parent marginal | **Proved** |
| `InterventionTargetSimpConclusion M Dn Yn T` | `SCM.Equiv ((M.fixSet Dn).fixSet Yn) (M.fixSet (Dn ∪ Yn))` under observed/fixed/disjointness well-formedness hypotheses | Real `SCM.Equiv` statement |
| `intervention_target_simp` | Proves `fact4` by Finset induction over `Yn`, using the singleton-insert composition helper | Proven modulo `swigInterventionSet_insert_equiv` |
| `SCM.swigInterventionSet_insert_equiv` | Structural equivalence between intervening on `X` then fresh `{y}` and intervening on `insert y X` in one shot | **Proved** |
| `DistrictIdConclusion M T Wn` | Specialization of `QFactorIdentityConclusion` at `R := M.observed` | `:= QFactorIdentityConclusion M M.observed T Wn` |
| `district_id` | Specializes `q_factor_identity` at `R := M.observed` | Direct specialization of the conditional Q-factor identity; **proved** |

### `Density/ReferenceMeasure.lean` — dominated-model density foundation

Namespace `Causalean.SCM`. Foundation for the **density-assisted** c-component
factorization: Tian's assembly `P(v) = ∏_C Q[C]` is a commutative regrouping of
scalar density factors with no kernel-composition analogue, so the
`doKernelY` identification (obligation 2) is routed through the joint density.
Unifies discrete (counting reference) and continuous (Lebesgue reference) models.

| Definition / Theorem | Signature / Statement | Description |
|---|---|---|
| `ReferenceMeasures Ω` | structure: `μ : ∀ v, Measure (swigΩ Ω v)` + per-node `SigmaFinite` | A σ-finite reference measure per SWIG-node value space (counting for discrete, Lebesgue for continuous) |
| `ReferenceFaithful ref` | `∀ v x, ref.μ v {x} ≠ 0` | Full-support condition for finite references; counting reference measures satisfy it |
| `jointRef ref I` | `Measure (ValuesOn I (swigΩ Ω))` | Finite product `Measure.pi` of the per-node references over a node set `I` |
| `DominatedObs M ref` | `∀ s, M.obsKernel s ≪ jointRef ref M.observed` | The gSCM's observational law is absolutely continuous w.r.t. the joint reference (admits a density) |
| `obsDensity M ref s` | `ValuesOn M.observed (swigΩ Ω) → ℝ≥0∞` | Joint observational density `(obsKernel s).rnDeriv (jointRef …)` |
| `withDensity_obsDensity_eq` | `DominatedObs M ref → (jointRef …).withDensity (obsDensity …) = obsKernel s` | Density determines the law (Proved) |
| `obsKernel_eq_of_obsDensity_ae_eq` | a.e.-equal `obsDensity` slices ⟹ equal `obsKernel` slices (single model) | Density injectivity used downstream for the soundness transport (Proved) |

### `Density/FiniteReference.lean` — finite discrete reference infrastructure

Namespace `Causalean.SCM`. Instances and helpers for the finite/discrete density
path: when every base-node value space is finite and has measurable singletons,
the SWIG value spaces and finite product references inherit the corresponding
finite/measurable structure needed by the finite-reference RN chain rule.

| Definition / Theorem | Signature / Statement | Description |
|---|---|---|
| `instFintypeSwigΩ` / `instMeasurableSingletonClassSwigΩ` | `[∀ n, Fintype (Ω n)]` / `[∀ n, MeasurableSingletonClass (Ω n)]` ⟹ the same instances for `swigΩ Ω sn` | Transfers finite and singleton-measurable structure from base nodes to random/fixed SWIG nodes |
| `isFiniteMeasure_of_finite_measurableSingleton` | a σ-finite measure on a finite measurable-singleton type is finite | Generic finite-space measure helper, proved by covering the space with finitely many measurable singletons |
| `instIsFiniteMeasure_refMu` / `instIsFiniteMeasure_jointRef` | finite node spaces ⟹ `IsFiniteMeasure (ref.μ v)` and `IsFiniteMeasure (jointRef ref I)` | Supplies the finite fibre and finite product-reference instances used by discrete density proofs |
| `jointRef_singleton_ne_zero` / `absolutelyContinuous_jointRef_of_faithful` | `ReferenceFaithful ref` ⟹ every product singleton has nonzero `jointRef` mass, hence every measure on the finite coordinate product is `≪ jointRef ref I` | Full-support infrastructure used to prove do-law ancestral marginal domination |
| `aemeasurable_fiber_rnDeriv_of_finite` | any raw fibre RN selector `(κ p.1).rnDeriv ρ p.2` is a.e.-measurable on a finite measurable-singleton product | Discharges the explicit product a.e.-measurability hypothesis of the finite-reference composition-product RN lemma |

### `Density/PiUnion.lean` — `Measure.pi` over a disjoint union

Namespace `Causalean.SCM`. The reference-splitting foundation for the density chain
rule: `Measure.pi` over a disjoint union of coordinates factors as a product.

| Definition / Theorem | Signature / Statement | Description |
|---|---|---|
| `unionSumEquiv hDisj` | `({a // a ∈ A} ⊕ {b // b ∈ B}) ≃ {i // i ∈ A ∪ B}` | Index equivalence for disjoint `A`, `B` |
| `measurePreserving_valuesUnionEquiv` | `MeasurePreserving (valuesUnionEquiv hDisj) (Measure.pi μ_{A∪B}) ((Measure.pi μ_A).prod (Measure.pi μ_B))` | **`Measure.pi` over a disjoint union = the product measure.** Fully proved, axiom-clean; routes the project's union equiv through Mathlib `sumPiEquivProdPi` + `piCongrLeft` |

### `Density/ChainRuleDensity.lean` — observational chain-rule density

Namespace `Causalean.SCM`. Density analogue of the proven kernel chain rule:
the observational density is reduced to the scalar product of the
Radon--Nikodym derivatives of the one-node conditional kernels
`obsStepCondKernel`, evaluated along the observed topological order.

| Definition / Theorem | Signature / Statement | Description |
|---|---|---|
| `obsStepCondDensity M ref s i` | `ValuesOn M.observed (swigΩ Ω) → ℝ≥0∞` | One observed-coordinate conditional density: the RN derivative of `obsStepCondKernel i.isLt` against the node reference measure, evaluated at the prefix and coordinate read from a full observed assignment |
| `qFactorDensityProduct M ref s` | `ValuesOn M.observed (swigΩ Ω) → ℝ≥0∞` | Product over `Fin M.observed.card` of the per-node conditional density factors in observed topological order |
| `qFactorProduct_rnDeriv_eq_qFactorDensityProduct` | with finite measurable-singleton node spaces, `DominatedObs M ref → (M.qFactorProduct s).rnDeriv (jointRef ref M.observed) =ᵐ … qFactorDensityProduct …` | Analytic RN-derivative chain rule. **The `DominatedObs` hypothesis is essential** — false without it (singular part ⟹ vanishing rnDeriv vs nonzero conditional-density product). Fully proved in the finite-reference setting |
| `obsChainKernel_rnDeriv_eq_prefixDensityProduct` | with finite measurable-singleton node spaces, general-`k` prefix induction: `(obsChainKernel k s).rnDeriv (jointRef prefix k) =ᵐ prefixDensityProduct k` | **Proved.** Base on `ValuesOn ∅`; successor peels `extendObsPrefix` via `MeasurableEmbedding.rnDeriv_map`, splits via `jointRef_extendObsPrefix`, derives joint product domination from base and fibre domination, and uses finite-space measurability for the raw fibre derivative |

| `obsChainKernel_absolutelyContinuous_jointRef_prefix` | `M.obsChainKernel k hk s ≪ jointRef ref (M.prefixNodes k)` | **Proved (no Mathlib gap).** Chain = prefix marginal of the dominated `obsKernel`; the reference marginal is a scalar multiple of the prefix reference via the disjoint-union split (`measurePreserving_valuesUnionEquiv`) + `Measure.map_fst_prod` |
| `rnDeriv_compProd_prod_sigmaFinite` | `(μ ⊗ₘ κ).rnDeriv (ν.prod ρ) =ᵐ fun p => f p.1 * (κ p.1).rnDeriv ρ p.2`, for finite `μ`/`ρ`/`κ`, σ-finite `ν`, `μ ≪ ν`, **joint domination** `μ ⊗ₘ κ ≪ ν.prod ρ`, product a.e.-measurability of the raw fibre derivative, and `μ.rnDeriv ν =ᵐ f` | Finite-reference composition-product RN identity in `Causalean/Mathlib/MeasureTheory/RnDerivCompProdSigmaFinite.lean`. Finiteness of `ρ` makes `Kernel.const _ ρ` a finite kernel, so Mathlib extracts μ-a.e. fibre domination from joint domination. The raw `Measure.rnDeriv` fibre representative still carries an explicit product-measurability hypothesis; Mathlib's measurable representative is `Kernel.rnDeriv κ (Kernel.const _ ρ)` |
| `map_pi_valuesEquivOfEq`, `jointRef_prefix_card_map`, `singletonValues_map_ref_eq_jointRef`, `jointRef_extendObsPrefix`, `rnDeriv_compProd_same_left`, `absolutelyContinuous_of_map_measurableEmbedding`, `qFactorProduct_rnDeriv_eq_obsChainKernel_card_pullback_of_jointRef`, `obsChainKernel_card_rnDeriv_pullback_eq_qFactorDensityProduct_of_prefix` | reference reindex / singleton split / per-step `extendObsPrefix` reference split / embedding-pullback of `≪` / `rnDeriv_map` peels / a.e. transport | The transport + reference-split sub-lemmas of the chain rule — **all proved, axiom-clean** |
| `obsDensity_eq_qFactorDensityProduct` | with finite measurable-singleton node spaces, `M.obsDensity ref s =ᵐ[jointRef ref M.observed] M.qFactorDensityProduct ref s` | Dominated observational density factors as the product of one-node conditional densities; D1 is now sorry-free in the finite-reference setting |

### `Density/CComponentDensity.lean` — c-component regrouping of the density

Namespace `Causalean.SCM`. The commutative regrouping with **no kernel analogue**:
the scalar density product is collected by c-component. (Composed kernels can't
be permuted to gather a non-contiguous c-component's factors; scalar densities
can.)

| Definition / Theorem | Signature / Statement | Description |
|---|---|---|
| `cComponentDensityFactor M ref s C` | `ValuesOn M.observed (swigΩ Ω) → ℝ≥0∞` | Product of the one-node conditional density factors over the observed indices whose node lies in c-component `C` (density analogue of Tian's `Q[C]`) |
| `qFactorDensityProduct_eq_prod_cComponentFactor` | `M.qFactorDensityProduct ref s x = ∏ C ∈ cComponentSet, cComponentDensityFactor … C x` | The chain-rule density product regroups as a product over c-components. **Fully proved, axiom-clean** (pure `Finset.prod_fiberwise` regrouping; the kernel-impossible step) |

### Archived (under `archive/SCM/ID/GraphicalThms/`)

* `Fixable.lean` — `SWIGGraph.isFixable`, `isFixableSeq` (kernel-era; no
  longer used by the chosen Shpitser–Pearl recursion).
* `FixingStep.lean` — Lemma 1 skeleton.
* `CFactor.lean` — kernel-era c-factor (replaced by `CComponentFactor.lean`).

---

## 10. `SCM/Examples/IV.lean` — IV DAG Example

Demonstrates the full API on the standard instrumental variable DAG:

```
    U
   ↙ ↘
  Z → D → Y
```

**What it tests:**

| Module | Tests |
|---|---|
| `Graph/DAG.lean` | `parents`, `children`, `isRoot`, `isAncestor`, `isAncestor_irrefl/trans` |
| `Graph/DSep.lean` | `dSep` with various conditioning sets; collider bias; instrument independence |
| `Graph/SWIG.lean` | `SWIGGraph` construction; initial SWIG; SWIG edge tests |
| `Graph/CComponents.lean` | `directlyConfounded` on `SWIGGraph`; `cComponentOf` |
| `SCM/Model/EdgeType.lean` | `EdgeTypeAssignment`; monotonic vs nonparametric; `refines` |
| `SCM/Model/SCM.lean` | Trivial `Causalean.SCM` instance `ivSCM` with `ΩIV := fun _ => Unit`; `structFun` constant; `latentDist := Measure.dirac ()`; `iota_valueSpace` discharged via `Finset.notMem_empty`. Verifies `ivSCM.isStandard` |

---

## 10b. `SCM/Examples/BackDoor.lean` — Backdoor Adjustment Example

Demonstrates the backdoor criterion DAG:

```
    Z
   ↙ ↘
  D → Y
```

Observed: D (treatment), Y (outcome), Z (observed confounder). Latent roots U₁, U₂, U₃ added to satisfy `unobs_are_roots`.

**What it tests:**

| Module | Tests |
|---|---|
| `Graph/DAG.lean` | `parents`, `isRoot`, `isAncestor` (irreflexivity via `isAncestor_topoOrder_lt`) |
| `Graph/DSep.lean` | `dSep` for the observational graph; no d-separation by {Z} (direct edge D → Y remains open) |
| `Graph/SWIG.lean` | `SWIGGraph` construction with three latent roots; `swig_random_root_of_root` for each |
| `Graph/SWIGSplitMono.lean` | Backdoor condition (ii): `splitMonoDAG {bdD}` d-separates Y from D given Z, verified by `native_decide` on the computable `splitMonoDAG` |
| `Graph/CComponents.lean` | Each observed node is its own singleton C-component (no confounding) |
| `SCM/Model/EdgeType.lean` | `allNonparametric` edge type assignment |

The file now additionally demonstrates the full `bdSWIG.backdoorCriterion` on this concrete 3-node DAG.  Condition (i) (no `z ∈ Z` is a descendant of any `random D ∈ X`) is discharged by `native_decide` + `simp`.  Condition (ii) is routed through `splitMonoDAG` (since `splitMono` itself is `noncomputable`) and also closed by `native_decide`.  No full SCM is built — only the graphical criterion is exercised.

---

## 10c. `SCM/Examples/ContinuousBackdoor.lean` — Linear-Gaussian backdoor (continuous treatment)

Validation of the posterior witness-kernel backdoor pipeline: a 3-node SCM with continuous value spaces (`ℝ`) on every observed node, exercising `backdoor_completeness_ae` end-to-end on a treatment variable that is *not* discrete.

DAG: `Z → X`, `Z → Y`, `X → Y` (so `Z` is a backdoor-admissible confounder for the effect of `X` on `Y`).  All three observed nodes have value space `ℝ` (via `CBΩ : Fin 3 → Type := fun _ => ℝ`).  The latent set is empty, so the SCM is fully deterministic — the kernel-native pipeline does not need probabilistic latents to demonstrate the continuous-treatment regime; what matters is the continuity of the value spaces.

The example uses a *computable* SWIGGraph (`cbSWIGGraph`) so the backdoor criterion can be discharged via `native_decide` even though the parent `SCM` is `noncomputable` (carries `Measure`s).  `continuousBackdoorSCM.toSWIGGraph := cbSWIGGraph` definitionally.

| Declaration | Description |
|---|---|
| `CBNode`, `CBΩ` | Node set `Fin 3` (`Zidx = 0`, `Xidx = 1`, `Yidx = 2`) with continuous value spaces `ℝ` |
| `cbEdgeBool`, `cbEdge`, `cbDAG` | Edge relation (`Bool`-lifted for `decide`-friendly `Decidable`) and underlying DAG |
| `cbSWIGGraph` | Computable SWIGGraph (empty latents, `observed = {Z, X, Y}` as `.random` nodes), used for graphical criterion proofs |
| `continuousBackdoorSCM` | The full SCM: extends `cbSWIGGraph`, constant-`0` structural functions, empty `latentDist`.  Marked `noncomputable` only because of the `Measure` API in the SCM structure |
| `cb_Xrand_obs`, `cb_Xfixed` | Witnesses that `random X` is observed and `fixed X` is not in `fixed`; supplied to `backdoor_completeness_ae`'s subset-hypotheses |
| `cb_backdoor_criterion` | Discharges `cbSWIGGraph.backdoorCriterion {X} _ _ {Y} {Z}`.  Condition (i) (`Z` not descendant of `X`) by `native_decide`; condition (ii) (d-separation in splitMono) by `native_decide` on the computable `splitMonoDAG` |

**What this validates.**  Continuous value spaces (`ℝ`) flow through the kernel-native pipeline (`obsKernel`, `obsCondKernel`, `Rule2JointOverlap`, `backdoor_completeness_ae`), and `SWIGGraph.backdoorCriterion` discharges by `native_decide` on the continuous SCM.  This example is sorry-free.

---

## 9d. `SCM/ID/Adjustment.lean` — Adjustment Functionals

Graph-level adjustment functionals that turn an SCM's observational
kernel into a post-intervention `Y`-marginal kernel under the backdoor
or frontdoor criterion.  The split into a *single-SCM completeness*
lemma plus a *cross-SCM congruence* lemma factors the cross-SCM HEq
plumbing out of the do-calculus assembly in `SCM/ID/Backdoor.lean` /
`PO/ID/Exact/Frontdoor.lean`.

| Definition / Statement | Description |
|---|---|
| `SCM.backdoorAdjustment M X hX_obs hX_fixed Y Z hY hZ` | Kernel `Kernel (M.fixSet X).FixedValues (ValuesOn Y _)` implementing `∫_z P(Y \| X, Z=z) dP(z)` as a kernel-native `((zMarginalPost ⊗ₖ M.obsCondKernel Y (X.image .random ∪ Z)).comap ...).map Prod.snd` chain.  The conditional's `(X.image .random ∪ Z)`-argument is assembled via `M.fillZrW X hX_obs hX_fixed Z s_post z`, which fills the `X.image .random` slice from `s_post` (reindexed from `X.image .fixed` through `zFixedAsRandom`) and pairs it with the outer `Z`-value `z` — routing through `fillZrW` (rather than an inline `valuesUnionMk ∘ zFixedAsRandom ∘ valuesProjection`) removes a reindexing-bridge gap in the downstream backdoor completeness proof.  Signature carries `[StandardBorelSpace (ValuesOn Y (swigΩ Ω))]`, `[Nonempty (ValuesOn Y (swigΩ Ω))]`, `[∀ s : M.FixedValues, IsFiniteMeasure (M.obsKernel s)]`, and a `CountableOrCountablyGenerated` instance for the conditional kernel |
| `SCM.frontdoorAdjustment M X hX_obs hX_fixed Y Z hY hZ` | **Proved** (sorry-free).  Kernel `Kernel (M.fixSet X).FixedValues (ValuesOn Y _)` implementing the frontdoor integral as a chain of `⊗ₖ` legs: outer X-marginal `(M.obsKernel.map projXr).comap fixSetProj`, middle `M.obsCondKernel Z (X.image .random)` (Rule 2 leg, `P(Z|X)`), inner `M.obsCondKernel Y (X.image .random ∪ Z)` (Rule 2 leg on inner `do(Z)`, with `X` as adjustment set).  The three kernels compose to `((xMarginal ⊗ₖ zCondX) ⊗ₖ yCondXZ).map Prod.snd`.  Signature requires `[StandardBorelSpace (ValuesOn Y _)]`, `[StandardBorelSpace (ValuesOn Z _)]`, `[Nonempty (ValuesOn Y _)]`, `[Nonempty (ValuesOn Z _)]`, `[StandardBorelSpace (ValuesOn (X.image .random) _)]`, `[StandardBorelSpace (ValuesOn (X.image .random ∪ Z) _)]`, `[∀ s, IsFiniteMeasure (M.obsKernel s)]`, and two `CountableOrCountablyGenerated` instances for the conditional kernels |
| `SCM.backdoorAdjustment_invariant` | **Proved** (sorry closed).  HEq cross-SCM invariance: two SCMs sharing `toSWIGGraph` and `obsKernel` produce the same `backdoorAdjustment` kernel.  Proof destructs both SCMs via `obtain`, `cases h_swig` unifies the SWIGGraph fields definitionally, then `apply heq_of_eq` reduces HEq to Eq. `eq_of_heq _h_obs` strips the obsKernel HEq; `unfold SCM.backdoorAdjustment` + nested `congr 1` peels off `.map Prod.snd` and `⊗ₖ`, leaving the `zMarginalPost` branch (closed by `rw [h_ok]`) and the `condPost` branch (closed by unfolding `obsCondKernel`/`obsCondPairKernel` and `rw [h_ok]`). Does not require `heq_obsCondKernel` — `cases h_swig` already aligns the kernel arguments |
| `SCM.frontdoorAdjustment_invariant` | **Proved** (sorry-free).  Same shape as `backdoorAdjustment_invariant`.  After destructure + `cases h_swig`, `unfold SCM.frontdoorAdjustment` exposes the `⊗ₖ` chain; unfolding `obsCondKernel`/`obsCondPairKernel` exposes the `condKernel`-of-`obsKernel.map`-joint pattern; `simp_rw [h_ok]` rewrites every `obsKernel` mention to the M₂-side, then `rfl` closes the remaining proof-irrelevance noise on `fixSetProj` (only SWIGGraph fields are used in `fixSetProj`; all unified after `cases h_swig`) |

---

## 9e. `SCM/ID/BackdoorCriterion.lean` — Backdoor criterion + Rule-3 leg

The graphical backdoor criterion (Basic Concepts.tex:636–645, Pearl 2009
Theorem 3.3.2) and the single regime-independent do-calculus leg the backdoor
identification proof consumes.  The identification *theorems* live in
`SCM/ID/Backdoor.lean` (§9e').

**Design note on condition (ii).**  `Graph/Mutilation.lean` is not yet
implemented, so the lower-bar mutilation `G_{X̲}` is encoded via
`SWIGGraph.splitMono X hX_obs hX_fix`: splitting `X` reroutes outgoing
edges of `random D` (for `D ∈ X`) to root nodes `fixed D`, which is
exactly the effect of lower-bar mutilation for d-separation purposes.

| Declaration | Description | Status |
|---|---|---|
| `SWIGGraph.backdoorCriterion G X hX_obs hX_fix Y Z` | Two-condition criterion: (i) no `z ∈ Z` is a descendant of any `random D` with `D ∈ X` in `G`; (ii) `Z ∪ X.image .fixed` d-separates `Y` from `X.image .random` in `G.splitMono X hX_obs hX_fix` (encodes `G_{X̲}` d-sep; fixed_X conditioning is vacuous in splitMono but carried to match `do_rule2`'s shape) | Defined; type-checks without sorry |
| `SCM.backdoor_rule3_Z_marginal` | **Rule-3 leg**: under criterion (i), `((M.fixSet X).obsKernel s_post).map proj_Z = (M.obsKernel (fixSetProj s_post)).map proj_Z`. Applies `do_rule3` with `Y_param := ∅`, `W_param := Z` and bridges `hNoDesc` via `fixSet_isAncestor_fixed_forward`. Consumed by `backdoor_completeness_ae`. | **Proved** |

---

## 9e'. `SCM/ID/Backdoor.lean` — Backdoor Identification (continuous, a.e.)

The sound **continuous-treatment** backdoor results, stated a.e. in the
treatment value (so they never read `obsCondKernel`/`backdoorAdjustment` on a
`{X = x}` null slice).  Built on the posterior witness-kernel Rule 2
(`condDistrib_fixSet_cross_SCM_bridge`).  Headline theorems
`backdoor_completeness_ae` and `backdoor_identifiable_ae`.

| Declaration | Description | Status |
|---|---|---|
| `SCM.treatmentMarginal M X hXr s0` | The observational treatment marginal `νX = (M.obsKernel s0).map π_{X.random}` — the supported measure the a.e. statements are anchored at | Defined |
| `SCM.BackdoorPositivityAE M X Z … s0` | Product positivity / overlap `Prop`: `νX ⊗ μZ ≪ μ_{X.rand ∪ Z}` (the standard continuous-treatment positivity; vacuous-free, covers both atomic and non-atomic regimes) | Defined |
| `SCM.doKernelY M X … Y hY s0` | Post-intervention `Y`-marginal as a kernel in the treatment value `t`: `t ↦ ((M.fixSet X).obsKernel (extend s0 t)).map π_Y`. Type `Kernel (ValuesOn (X.image .random)) (ValuesOn Y)` — independent of `M`'s fixed values | Defined |
| `SCM.adjustmentKernelY M X … Y Z hY hZ s0` | Backdoor-adjustment `Y`-marginal as a kernel in the treatment value: `(M.backdoorAdjustment …).comap (fixSetExtend s0)` | Defined |
| `SCM.backdoor_completeness_ae_compProd M X … s0 hOverlap hPositivity` | `νX ⊗ₘ doKernelY = νX ⊗ₘ adjustmentKernelY` at base `s0`, under backdoor criterion + joint overlap + `BackdoorPositivityAE`. The compProd (joint) primary form — well-posed for continuous treatment (never reads `obsCondKernel` on a null slice) | **Proved** (sorry-free) |
| `SCM.backdoor_completeness_ae M X … s0 hOverlap hPositivity` | **Continuous backdoor completeness (a.e.).** For `νX`-a.e. treatment value `t`, `doKernelY … t = adjustmentKernelY … t`. Derived from the compProd form via `Kernel.ae_eq_of_compProd_eq`. **Headline.** | **Proved** (sorry-free) |
| `SCM.backdoor_identifiable_ae M₁ M₂ h_swig X Y Z … h_obs h_s0` | **Continuous backdoor identifiability (a.e., cross-SCM).** Two SCMs sharing the SWIG graph (`h_swig`) and observational kernel (`h_obs : HEq …`) with aligned base points (`h_s0 : HEq s0₁ s0₂`) produce `doKernelY M₁ s0₁ =ᵐ[treatmentMarginal M₁ s0₁] doKernelY M₂ s0₂`. A plain `=ᵐ` (not HEq) since `doKernelY` is `M`-fixed-value-independent. Proof: `backdoor_completeness_ae` on each side + cross-SCM `adjustmentKernelY` invariance (`backdoorAdjustment_invariant` after destructure + `cases h_swig`/`h_s0`). **Headline.** | **Proved** (sorry-free) |

---

## 9f. `PO/ID/Exact/LATE.lean` — IV/LATE Identification Theorem

Formalizes the Wald identification of LATE in the PO framework (Basic Concepts.tex: def:po-iv-system, def:po-iv-assumptions, def:po-late, prop:po-late, rem:po-late).

**Implementation note (2026-04-23 refactor).** The file now consumes the PO
abstraction layer introduced in `PO/`: counterfactual maps are built
as `POVar` (`zVar`, `dVar`, `yVar`), single-node interventions as
`RegimedVar.ofSingle` / `RegimedVar` (`dUnderZ`, `yUnderD`), joint bundles as
`POCFBundle` (`cfBundle`), and the IV independence premise as `P.IndepCF`.
A subsequent pass (2026-04-23) further collapsed the two `hCE` blocks in
`first_stage_identity` / `reduced_form_identity` via
`POSystem.eventCondExp_of_consistency_IndepCF`, deleting the product-form
`cfTuple` / `measurable_cfTuple` / `indepFun_factualZ_cfTuple` /
`integral_zEvent_of_cfTuple` scaffolding in favour of direct
`POCFBundle.jointValue` usage.
Net reduction: 429 → 388 lines.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POIVSystem P` | `structure { Z, D, Y, hZbool, hDbool, hYreal, distinct }` | IV subsystem over a PO system | **Defined** |
| `POIVSystem.zVar`, `dVar`, `yVar` | `POVar P Bool` / `POVar P Bool` / `POVar P ℝ` | `POVar` wrappers around `Z`, `D`, `Y` using the `hZbool`/`hDbool`/`hYreal` equivalences; abstract away the hand-rolled measurability chains | **Defined** |
| `POIVSystem.instrumentRegime S z` | `Regime P.V P.X` | Regime `r_z := ({Z}, z)` | **Defined** |
| `POIVSystem.treatmentRegime S d` | `Regime P.V P.X` | Regime `r_d := ({D}, d)` | **Defined** |
| `POIVSystem.DofZ S z`, `YofD S d` | `P.Ω → Bool` / `P.Ω → ℝ` | Counterfactual treatment `D(z)` and outcome `Y(d)` | **Defined** |
| `POIVSystem.factualZ`, `factualD`, `factualY` | `P.Ω → Bool` / `Bool` / `ℝ` | Factual instrument / treatment / outcome | **Defined** |
| `POIVSystem.complierEvent` | `Set P.Ω` | `{ω | D(1)(ω)=1 ∧ D(0)(ω)=0}` | **Defined** |
| `POIVSystem.zEvent S z` | `Set P.Ω` | `{ω | Z(ω) = z}`, defined as `S.zVar.event z` | **Defined** |
| `POIVSystem.measurable_{DofZ,factualZ,factualD,factualY,YofD}` | `Measurable …` | Measurability of the counterfactual/factual variables (now one-line delegations to `POVar.measurable_cf` / `POVar.measurable_factual`) | **Proved** |
| `POIVSystem.measurableSet_complierEvent` / `measurableSet_zEvent` | `MeasurableSet …` | Measurability of the two event sets | **Proved** |
| `POIVSystem.dUnderZ S z` | `RegimedVar P Bool` | `⟨S.dVar, Regime.single S.Z (S.hZbool.symm z)⟩` — regimed form of `D(z)` used to populate `cfBundle` | **Defined** |
| `POIVSystem.yUnderD S d` | `RegimedVar P ℝ` | `⟨S.yVar, Regime.single S.D (S.hDbool.symm d)⟩` — regimed form of `Y(d)` used to populate `cfBundle` | **Defined** |
| `POIVSystem.cfBundle` | `POCFBundle P` | The bundle `(D(1), D(0), Y(1), Y(0))` built via iterated `POCFBundle.cons`; target of the IV independence hypothesis | **Defined** |
| `POIVSystem.YofDofZ S z` | `P.Ω → ℝ` | `Y(D(z)) := 1_{D(z)=1} Y(1) + 1_{D(z)=0} Y(0)` | **Defined** |
| `POIVSystem.condExpDZ S z`, `condExpYZ S z` | `ℝ` | Event-level `E[D | Z=z]`, `E[Y | Z=z]` | **Defined** |
| `POIVSystem.Assumptions S` | `structure { consistency, instrumentIndep, monotonicity, relevance }` where `instrumentIndep : P.IndepCF (RegimedVar.ofFactual S.zVar) S.cfBundle P.μ` | def:po-iv-assumptions; the instrument-independence field is now phrased against the bundle-based `IndepCF` shape | **Defined** |
| `POIVSystem.LATE` | `ℝ` | `(∫_C (Y(1)-Y(0)) ∂μ) / (μ C).toReal` — def:po-late | **Defined** |
| `POIVSystem.DofZ_eq_factualD_on_zEvent` | `ω ∈ zEvent z → DofZ z ω = factualD ω` | Pointwise D-consistency on `{Z = z}` | **Proved** |
| `POIVSystem.factualY_eq_YofD_factualD` | `factualY ω = YofD (factualD ω) ω` | Pointwise Y-consistency everywhere | **Proved** |
| `POIVSystem.first_stage_identity` | `condExpDZ 1 - condExpDZ 0 = (μ C).toReal` | Step 1 of rem:po-late | **Proved** |
| `POIVSystem.reduced_form_identity` | `condExpYZ 1 - condExpYZ 0 = ∫ (YofDofZ 1 - YofDofZ 0) ∂μ` | Step 2 of rem:po-late | **Proved** |
| `POIVSystem.pointwise_monotonicity` | `∀ᵐ ω, Y(D(1))-Y(D(0)) = (Y(1)-Y(0))·1_C` | Step 3 of rem:po-late | **Proved** |
| `POIVSystem.event_conditioning_identity` | `∫ (Y(1)-Y(0))·1_C ∂μ = (μ C).toReal · LATE` | Step 4 of rem:po-late | **Proved** |
| `POIVSystem.late_wald` | `(condExpYZ 1 - condExpYZ 0) / (condExpDZ 1 - condExpDZ 0) = LATE` | Wald identification of LATE, prop:po-late | **Proved** |

Drop-of-conditioning and consistency-on-event rewrites are delegated to
`POSystem.eventCondExp_of_consistency_IndepCF` from `PO/Conditioning/EventCondExp.lean`;
no private helper remains in this file.

---

## 9f'. `PO/ID/Exact/HeckmanRoy/{Setup,Wald}.lean` — Heckman–Vytlacil IV / Generalized Roy Selection Model (PO framework)

Formalises the Heckman–Vytlacil IV / generalized Roy selection model from `Basic Concepts.tex` (def:po-iv-heckman-roy-system, def:po-iv-heckman-roy-assumptions, def:po-iv-heckman-roy-late, prop:po-iv-heckman-roy-wald, rem:po-iv-heckman-roy-lean). Generalises `LATE.lean` to:

* an arbitrary measurable instrument value space `α` (with `MeasurableSingletonClass`), instead of `Bool` — same parametrization pattern as `Manski/Setup.lean`;
* a latent uniform rank `U : Ω → ℝ` (`U ~ Unif[0,1]`) plus a propensity map `p : α → ℝ`, replacing the binary `D` potentials and monotonicity by threshold crossing `D(z) = 1_{U ≤ p(z)}`;
* a **pairwise** Wald identity: for any `z₀, z₁ : α` with `p z₀ < p z₁` and positive measure on `{Z = z₀}, {Z = z₁}`,
  `(E[Y|Z=z₁] − E[Y|Z=z₀]) / (E[D|Z=z₁] − E[D|Z=z₀]) = LATE(z₀, z₁)`.

**Out of scope (follow-ups):** the marginal treatment effect `MTE` (def:po-iv-heckman-roy-mte) and the continuous-Z / regression-representative formulation (rem:po-iv-heckman-roy-local-iv); both need the `IsRegressionFunction` infrastructure of `PO/Analysis/Regression.lean` to extend to non-singleton instruments.

### `HeckmanRoy/Setup.lean` — data layer

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POHeckmanRoySystem P α` | `structure { Z, D, Y, U, hZ, hDbool, hYreal, hUreal, distinctness pairs, p, hp_mem }` | Heckman–Roy IV subsystem over `P`, parametrised by instrument value space `α` with `MeasurableSingletonClass` | **Defined** |
| `POHeckmanRoySystem.{zVar, dVar, yVar, uVar}` | `POVar P α / Bool / ℝ / ℝ` | `POVar` wrappers around `Z, D, Y, U` | **Defined** |
| `POHeckmanRoySystem.instrumentRegime z` | `Regime P.V P.X` | Single-node regime `{Z ← z}` | **Defined** |
| `POHeckmanRoySystem.treatmentRegime d` | `Regime P.V P.X` | Single-node regime `{D ← d}` | **Defined** |
| `POHeckmanRoySystem.DofZ z` | `P.Ω → Bool` | Counterfactual treatment `D(z)` | **Defined** |
| `POHeckmanRoySystem.YofD d` | `P.Ω → ℝ` | Counterfactual outcome `Y(d)` | **Defined** |
| `POHeckmanRoySystem.factualZ / D / Y / U` | `P.Ω → α / Bool / ℝ / ℝ` | Factual realisations | **Defined** |
| `POHeckmanRoySystem.zEvent z` | `Set P.Ω` | `{Z = z}` as a `POVar.event` | **Defined** |
| `POHeckmanRoySystem.intervalComplierEvent z₀ z₁` | `Set P.Ω` | `{ω | p z₀ < U ω ≤ p z₁}` (def:po-iv-heckman-roy-late) | **Defined** |
| `POHeckmanRoySystem.measurable_*` | `Measurable …` / `MeasurableSet …` | One-line delegations to `POVar.*` for `DofZ, YofD, factualZ, factualD, factualY, factualU, YofDofZ` and the events `zEvent`, `intervalComplierEvent` | **Proved** |
| `POHeckmanRoySystem.YofDofZ z` | `P.Ω → ℝ` | `Y` composed with `D(z)`: `if D(z) ω then Y(1) ω else Y(0) ω` | **Defined** |
| `POHeckmanRoySystem.condExpDZ z`, `condExpYZ z` | `ℝ` | Event-level `E[D | Z=z]`, `E[Y | Z=z]` | **Defined** |
| `POHeckmanRoySystem.yUnderD d` | `RegimedVar P ℝ` | Regimed outcome under `{D ← d}`, used for the bundle | **Defined** |
| `POHeckmanRoySystem.cfBundle` | `POCFBundle P` | The bundle `(U, Y(1), Y(0))`; target of the instrument-independence assumption.  `D(z)` is omitted — it is determined by `U` via threshold crossing | **Defined** |
| `POHeckmanRoySystem.Assumptions S` | `structure { consistency, instrumentIndep, thresholdCrossing, uniformU }` | def:po-iv-heckman-roy-assumptions; `instrumentIndep : P.IndepCF (RegimedVar.ofFactual S.zVar) S.cfBundle P.μ`, `thresholdCrossing : ∀ z, ∀ᵐ ω, (D(z) ω = true ↔ U ω ≤ p z)`, `uniformU : ∀ q ∈ [0,1], μ {U ≤ q} = ENNReal.ofReal q` | **Defined** |
| `POHeckmanRoySystem.LATE z₀ z₁` | `ℝ` | Latent interval ATE: `(∫_C (Y(1)-Y(0)) ∂μ) / (μ C).toReal` (def:po-iv-heckman-roy-late) | **Defined** |

### `HeckmanRoy/Wald.lean` — proof layer

Mirrors the five-step decomposition of rem:po-iv-heckman-roy-lean.  All `theorem`/`lemma` proofs below are filled (Codex-assisted).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `DofZ_eq_factualD_on_zEvent` | `ω ∈ zEvent z → DofZ z ω = factualD ω` | Step 1: pointwise D-consistency on `{Z = z}`; one-line via `POVar.cf_eq_factual_on_event` | **Proved** |
| `factualY_eq_YofD_factualD` | `factualY ω = YofD (factualD ω) ω` | Step 1: pointwise Y-consistency; one-line via `POVar.factual_eq_cfUnder_self_selected` | **Proved** |
| `interval_indicator_sub` | `1_{U ≤ p z₁} ω - 1_{U ≤ p z₀} ω = (intervalComplierEvent z₀ z₁).indicator 1 ω` (for `p z₀ ≤ p z₁`) | Step 5 (algebra): pointwise interval-subtraction identity | **Proved** |
| `complier_measure` | `(μ (intervalComplierEvent z₀ z₁)).toReal = p z₁ - p z₀` (for `p z₀ < p z₁`) | Step 4: uniform-threshold lemma applied at `p z₀, p z₁` plus measure subtraction | **Proved** |
| `first_stage_identity` | `condExpDZ z₁ - condExpDZ z₀ = p z₁ - p z₀` | Step 1+3 (analogue of LATE `first_stage_identity`); uses `POSystem.eventCondExp_of_consistency_IndepCF` | **Proved** |
| `reduced_form_identity` | `condExpYZ z₁ - condExpYZ z₀ = ∫ (YofDofZ z₁ - YofDofZ z₀) ∂μ` | Step 1+3 (analogue of LATE `reduced_form_identity`); same drop-of-conditioning lemma | **Proved** |
| `pointwise_threshold_identity` | `∀ᵐ ω, YofDofZ z₁ ω - YofDofZ z₀ ω = (Y(1)-Y(0))(ω) · 1_{intervalComplierEvent z₀ z₁}(ω)` | Step 2: replaces LATE `pointwise_monotonicity`; case-split on `U ω ≤ p zⱼ` using `thresholdCrossing` | **Proved** |
| `event_conditioning_identity` | `∫ (Y(1)-Y(0)) · 1_C ∂μ = (μ C).toReal · LATE z₀ z₁` | Direct copy of LATE `event_conditioning_identity` with `intervalComplierEvent` | **Proved** |
| `wald_pairwise` | `(condExpYZ z₁ - condExpYZ z₀) / (condExpDZ z₁ - condExpDZ z₀) = LATE z₀ z₁` | Main theorem (prop:po-iv-heckman-roy-wald); stitches the helpers via `field_simp` | **Proved** |

---

## 9f''. `PO/ID/Exact/DynamicLATE/{Setup,Consistency,Bridges,WhenToTreat}.lean` — Two-Period Dynamic LATE (PO framework)

Formalises the two-period dynamic IV/LATE identification of Sojitra (2025) in the bare potential-outcome framework: `subsec:po-dynamic-late`, `def:po-dynamic-late-system`, `def:po-dynamic-late-assumptions`, `def:po-dynamic-late`, `def:po-dynamic-late-observable-functionals`, `prop:po-dynamic-late-when-to-treat`, `rem:po-dynamic-late-bridge`, `rem:po-dynamic-late-lean`. Generalises `LATE.lean` to two periods with sequential encouragements `Z₁,Z₂`, noncompliant treatments `D₁,D₂`, baseline state `S₀ : POVar P γ₀`, intermediate state `S₁ : POVar P γ₁`, and real outcome `Y`.

**Phase A scope.** Definitions, observable functionals, the assumption bundle, the four bridge identities of `rem:po-dynamic-late-bridge`, and the algebraic ratio identifications `whenToTreat_wald`, `mixtureLATE_wald`, plus the heterogeneous (`S₀`-conditional) versions. The bridge proofs and the structural composition-consistency rewrites are filled.

**Out of scope (Phase B):** `prop:po-dynamic-late-always-treat` and `cor:po-dynamic-late-staggered` (always-treat LATE under cross-period mean-independence / staggered-compliance). Will live in a future `AlwaysTreat.lean`.

### `DynamicLATE/Setup.lean` — data layer

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODynLATESystem P γ₀ γ₁` | `structure { S0, S1, Z1, D1, Z2, D2, Y, hZ1bool, hD1bool, hZ2bool, hD2bool, hYreal, vars_inj }` | Two-period dynamic IV subsystem over a PO system (def:po-dynamic-late-system). Pairwise distinctness packaged as `Function.Injective` on a `Fin 7 → P.V` accessor. | **Defined** |
| `PODynLATESystem.{z1Var, z2Var, d1Var, d2Var}` | `POVar P Bool` | `POVar` wrappers around `Z₁,Z₂,D₁,D₂` | **Defined** |
| `PODynLATESystem.yVar` | `POVar P ℝ` | `POVar` wrapper around `Y` | **Defined** |
| `PODynLATESystem.{factualS0, factualS1, factualZ1, factualZ2, factualD1, factualD2, factualY}` | `P.Ω → ·` | Factual realisations of the seven nodes | **Defined** |
| `PODynLATESystem.{Z1_ne_Z2, D1_ne_D2, D1_ne_Y, D2_ne_Y, Z1_ne_Y, Z2_ne_Y, Z1_ne_D1, Z1_ne_D2, Z2_ne_D1, Z2_ne_D2}` | `S.Z1 ≠ S.Z2` etc. | Pairwise distinctness extracted from `vars_inj` (the most-used pairs) | **Proved** |
| `PODynLATESystem.encouragementRegime z` | `Regime P.V P.X` | Two-target regime `{Z₁ ↦ z 0, Z₂ ↦ z 1}` via `Regime.ofList` | **Defined** |
| `PODynLATESystem.treatmentRegime d` | `Regime P.V P.X` | Two-target regime `{D₁ ↦ d 0, D₂ ↦ d 1}` via `Regime.ofList` | **Defined** |
| `PODynLATESystem.encZ2Regime z₂` | `Regime P.V P.X` | Single-target regime `{Z₂ ↦ z₂}`, used in stage-2 ignorability | **Defined** |
| `PODynLATESystem.D1ofZ z`, `D2ofZ z` | `P.Ω → Bool` | Counterfactual stage-1 / stage-2 treatment under encouragement `z` | **Defined** |
| `PODynLATESystem.DofZ z` | `P.Ω → (Fin 2 → Bool)` | Joint counterfactual treatment vector `(D₁(z), D₂(z))` | **Defined** |
| `PODynLATESystem.D2ofZ2 z₂` | `P.Ω → Bool` | `D₂(Z₁, z₂)`: stage-2 treatment when only `Z₂` is fixed | **Defined** |
| `PODynLATESystem.YofD d` | `P.Ω → ℝ` | Counterfactual outcome `Y(d)` under treatment regime fixing both `D`'s | **Defined** |
| `PODynLATESystem.YofDofZ z` | `P.Ω → ℝ` | `Y(D(z))`: outcome under the encouragement regime, i.e. `S.yVar.cf (encouragementRegime z)`. Composition consistency identifies it with the explicit composition `Y(d) ∘ DofZ z` on `{D(z) = d}`. | **Defined** |
| `PODynLATESystem.YofZ2 z₂` | `P.Ω → ℝ` | `Y(D₁, D₂(Z₁, z₂))` realised as `Y` under the regime fixing only `Z₂ = z₂` | **Defined** |
| `PODynLATESystem.measurable_*` | `Measurable …` | Measurability one-line delegations for all CF/factual maps | **Proved** |
| `PODynLATESystem.{yUnderZ z, d1UnderZ z, d2UnderZ z}` | `RegimedVar P …` | Regimed wrappers used to populate `cfBundle1` | **Defined** |
| `PODynLATESystem.{yUnderZ2 z₂, d2UnderZ2 z₂}` | `RegimedVar P …` | Regimed wrappers used to populate `cfBundle2` | **Defined** |
| `PODynLATESystem.historyBundle1` | `POCFBundle P` | Stage-1 history `(S₀,)` — conditioning σ-algebra for the outer regression | **Defined** |
| `PODynLATESystem.historyBundle2` | `POCFBundle P` | Stage-2 history `(S₀, S₁, Z₁, D₁)` — conditioning σ-algebra for the inner regression (stage-2 encouragement `Z₂` is restricted via an indicator, not in the conditioning set) | **Defined** |
| `PODynLATESystem.cfBundle1 z` | `POCFBundle P` | Stage-1 ignorability target `(Y(D(z)), D₁(z), D₂(z))` | **Defined** |
| `PODynLATESystem.cfBundle2 z₂` | `POCFBundle P` | Stage-2 ignorability target `(Y(D₁, D₂(Z₁, z₂)), D₂(Z₁, z₂))` | **Defined** |
| `PODynLATESystem.indD d`, `indZ z` | `P.Ω → ℝ` | Real-valued indicators of `{D = d}`, `{Z = z}` — products of binary `POVar.indicator`s | **Defined** |
| `PODynLATESystem.measurable_indD`, `measurable_indZ` | `Measurable …` | Measurability of the joint indicators | **Proved** |
| `PODynLATESystem.innerCondY z` | `P.Ω → ℝ` | Inner regression `E[Y \| S, D₁, Z = z]` realised as `historyBundle2.condExpRatio (factualY · indZ z) (indZ z)` | **Defined** |
| `PODynLATESystem.innerCondD z d` | `P.Ω → ℝ` | Inner regression `P(D = d \| S, D₁, Z = z)` realised analogously | **Defined** |
| `PODynLATESystem.cObsMean z`, `cObsProb z d` | `P.Ω → ℝ` | Outer regressions of `innerCondY` / `innerCondD` over `(S₀, Z₁ = z₁)`, as functions of `S₀` | **Defined** |
| `PODynLATESystem.obsMean z`, `obsProb z d` | `ℝ` | Unconditional `E[cObsMean]`, `E[cObsProb]` (def:po-dynamic-late-observable-functionals) | **Defined** |
| `PODynLATESystem.DofZEq z d` | `Set P.Ω` | Dynamic complier event `{ω \| D(z) ω = d}` | **Defined** |
| `PODynLATESystem.measurableSet_DofZEq` | `MeasurableSet (DofZEq z d)` | Measurability of the dynamic complier event | **Proved** |
| `PODynLATESystem.LATE z d` | `ℝ` | Dynamic LATE `θ(z, d) := E[Y(d) - Y(0) \| D(z) = d]` totalised as event integral / measure (def:po-dynamic-late) | **Defined** |
| `PODynLATESystem.cLATE z d` | `P.Ω → ℝ` | Heterogeneous (in `S₀`) dynamic LATE via `historyBundle1.condExpRatio` | **Defined** |
| `PODynLATESystem.whenToTreatLATE d`, `cWhenToTreatLATE d` | `ℝ` / `P.Ω → ℝ` | When-to-treat LATE `τ_d := θ(d, d)` and its `S₀`-conditional version | **Defined** |
| `PODynLATESystem.mixtureLATE z`, `cMixtureLATE z` | `ℝ` / `P.Ω → ℝ` | Dynamic mixture LATE `β_z := E[Y(D(z)) - Y(0) \| D(z) ≠ 0]` and its conditional version | **Defined** |
| `PODynLATESystem.Preceq d z` | `Prop` | Coordinate-wise order `d 0 ≤ z 0 ∧ d 1 ≤ z 1` | **Defined** |
| `PODynLATESystem.Assumptions S` | `structure { consistency, ignorability1, ignorability2, overlap1, overlap2, relevance1, relevance2, oneSidedNoncompliance, exclusion, exclusion_D1, exclusion_Z1, integrable_factualY, integrable_YofD, integrable_YofDofZ }` | The dynamic IV/LATE assumption bundle (def:po-dynamic-late-assumptions). Sequential ignorabilities phrased as `CondIndepCFBundle` against the corresponding history bundle. Three primitive-process exclusions: `exclusion` is `Y(D, S)` (Y under encTreatRegime = Y under treatmentRegime); `exclusion_D1` is `D₁(Z₁, S₀)` (D₁ under encouragementRegime z = D₁ under `Regime.single Z₁`); `exclusion_Z1` is `Z₁(S₀)` (P.eval Z₁ under encZ2Regime z₂ = P.eval Z₁ under Regime.empty), needed by the stage-1 composition consistency rewrite to satisfy `IntermediateAgrees`. | **Defined** |

### `DynamicLATE/Consistency.lean` — pointwise consistency-on-event rewrites

Structural pointwise identities used by the bridge proofs.  Each lemma is a direct consequence of PO consistency (def:po-consistency) — single-event `cf_eq_factual_on_event` for stage-2 rewrites and `Consistency.composition` for stage-1 (mirroring the existing `YofDofZ_eq_YofD_on_DofZEq` proof in `WhenToTreat.lean`).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `YofZ2_eq_factualY_on_z2Event` | `(hω : factualZ2 ω = z₂) → YofZ2 z₂ ω = factualY ω` | On `{Z₂ = z₂}`, `Y(D₁,D₂(Z₁,z₂)) = Y` — single-event consistency at `Z₂` for the outcome `Y`. | **Proved** |
| `D2ofZ2_eq_factualD2_on_z2Event` | `(hω : factualZ2 ω = z₂) → D2ofZ2 z₂ ω = factualD2 ω` | On `{Z₂ = z₂}`, `D₂(Z₁,z₂) = D₂` — single-event consistency at `Z₂` for `D₂`. | **Proved** |
| `eval_encouragement_eq_eval_encZ2_on_z1Event` (private) | `(hω : factualZ1 ω = z 0) → v ≠ Z₁ → v ≠ Z₂ → P.eval (encouragementRegime z) ω v = P.eval (encZ2Regime (z 1)) ω v` | Shared helper: composition consistency at any `v ∉ {Z₁, Z₂}`, combining `Consistency.composition` + `Regime.ext` (encouragementRegime ↔ sqcup of two singletons) + `As.exclusion_Z1` (for the `IntermediateAgrees` premise at Z₁). | **Proved** |
| `YofDofZ_eq_YofZ2_on_z1Event` | `(hω : factualZ1 ω = z 0) → YofDofZ z ω = YofZ2 (z 1) ω` | On `{Z₁ = z 0}`, `Y` under `encouragementRegime z` agrees with `Y` under `encZ2Regime (z 1)` — instance of the helper at `v := S.Y`. | **Proved** |
| `D2ofZ_eq_D2ofZ2_on_z1Event` | `(hω : factualZ1 ω = z 0) → D2ofZ z ω = D2ofZ2 (z 1) ω` | Instance of the helper at `v := S.D2`. | **Proved** |
| `D1ofZ_eq_factualD1_on_z1Event` | `(hω : factualZ1 ω = z 0) → D1ofZ z ω = factualD1 ω` | On `{Z₁ = z 0}`, `D₁(z) = D₁` — combines `As.exclusion_D1` (reduces to `Regime.single Z₁ (z 0)`) with `cf_eq_factual_on_event` at `Z₁`. | **Proved** |

### `DynamicLATE/Bridges.lean` — counterfactual / observable bridges

The four bridge identities of `rem:po-dynamic-late-bridge`, plus the one-sided-noncompliance corollary and two stage-2 inner-ratio sub-bridges used by the conditional bridge proofs.  Conditional bridges (`cOutcome_bridge`, `cCompliance_bridge`) are the workhorses; unconditional bridges (`outcome_bridge`, `compliance_bridge`) are short integration corollaries.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `probDofZ_eq_zero_of_not_preceq` | `(h : ¬ Preceq d z) → P.μ (DofZEq z d) = 0` | Under one-sided noncompliance, `D(z) ∈ {d \| d ≼ z}` a.s.; the complement has measure zero. | **Proved** |
| `innerCondY_mul_z1_indicator` | `innerCondY z · 1_{Z₁=z 0} =ᵐ 1_{Z₁=z 0} · historyBundle2.condExpGiven (YofZ2 (z 1))` | Stage-2 outcome ratio bridge multiplied by `1_{Z₁=z₁}`. Single application of `condExpRatio_of_consistency_CondIndepCFBundle` with `a := z2Var`, `B := cfBundle2 (z 1)`, `C := historyBundle2`, plus `historyBundle2`-pullout of the `1_{Z₁=z 0}` factor. | **Proved** |
| `innerCondD_mul_z1_indicator` | `innerCondD z d · 1_{Z₁=z 0} =ᵐ 1_{Z₁=z 0} · 1_{D₁=d 0} · historyBundle2.condExpGiven 1_{D₂(Z₁,z 1)=d 1}` | Stage-2 compliance ratio bridge multiplied by `1_{Z₁=z₁}`.  Same shape as above for the compliance event, factoring out the bundle2-measurable `1_{D₁=d 0}`. | **Proved** |
| `cOutcome_bridge` | `(hd : Preceq d z) → historyBundle1.condExpGiven (YofDofZ z) =ᵐ cObsMean z` | `S₀`-conditional outcome bridge.  Two-stage: stage-2 via `innerCondY_mul_z1_indicator`, stage-1 via `condExpGiven_mul_of_consistency_CondIndepCFBundle` + tower + consistency rewrite at `Z₁`. | **Proved** |
| `cCompliance_bridge` | `(hd : Preceq d z) → historyBundle1.condExpGiven (1_{DofZEq z d}) =ᵐ cObsProb z d` | `S₀`-conditional compliance bridge.  Same shape as `cOutcome_bridge`; the consistency rewrite uses `D2ofZ_eq_D2ofZ2_on_z1Event` and `D1ofZ_eq_factualD1_on_z1Event` (the latter via `As.exclusion_D1`). | **Proved** |
| `outcome_bridge` | `(hd : Preceq d z) → ∫ YofDofZ z = obsMean z` | Unconditional outcome bridge, derived by integrating `cOutcome_bridge` and using `∫ B.condExpGiven f = ∫ f`. | **Proved** |
| `compliance_bridge` | `(hd : Preceq d z) → (P.μ (DofZEq z d)).toReal = obsProb z d` | Unconditional compliance bridge, derived analogously from `cCompliance_bridge` via `integral_indicator_const`. | **Proved** |

### `DynamicLATE/WhenToTreat.lean` — ratio identifications (algebraic from the bridges)

The when-to-treat / mixture Wald identifications of `prop:po-dynamic-late-when-to-treat`. Helper lemmas factor the structural composition-consistency content; the main `whenToTreat_wald` is then assembled algebraically from the bridges plus helpers.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `YofDofZ_eq_YofD_on_DofZEq` | `∀ᵐ ω, DofZ z ω = d → YofDofZ z ω = YofD d ω` | Composition consistency on the encouragement event (uses `As.consistency.composition` + `As.exclusion`) | **Proved** |
| `DofZ_zero_eq_zero` | `∀ᵐ ω, DofZ ![false,false] ω = ![false,false]` | Under one-sided noncompliance, `D(0) = 0` a.s. | **Proved** |
| `YofDofZ_zero_ae_eq_YofD_zero` | `YofDofZ ![false,false] =ᵐ YofD ![false,false]` | A.s. `Y(D(0)) = Y(0)` | **Proved** |
| `DofZ_in_two_values` | `(hd : d ∈ {(1,0),(0,1)}) → ∀ᵐ ω, DofZ d ω = d ∨ DofZ d ω = 0` | Under one-sided + single-period encouragement, `D(d) ∈ {d, 0}` a.s. | **Proved** |
| `YofDofZ_decomposition` | `YofDofZ d =ᵐ YofD d · 1_{D(d)=d} + YofD 0 · 1_{D(d)=0}` | Two-piece pointwise decomposition combining `YofDofZ_eq_YofD_on_DofZEq` + `DofZ_in_two_values` | **Proved** |
| `int_outcome_difference_identity` | `∫ YofDofZ d - ∫ YofDofZ 0 = ∫_{D(d)=d} (Y(d) - Y(0)) dμ` | Numerator rewrite from the decomposition + `YofDofZ_zero_ae_eq_YofD_zero` | **Proved** |
| `whenToTreat_wald` | `(hd : d ∈ {(1,0),(0,1)}) → τ_d = (obsMean d - obsMean 0) / obsProb d d` | **Main when-to-treat identification (prop:po-dynamic-late-when-to-treat).** Combines the outcome / compliance bridges with `int_outcome_difference_identity`. | **Proved** |
| `mixtureLATE_wald` | `β_z = (obsMean z - obsMean 0) / (1 - obsProb z 0)` | **Main mixture identification, totalized at `z = 0` by Lean division.** | **Proved** |
| `cWhenToTreat_wald` | `(hd : d ∈ {(1,0),(0,1)}) → cτ_d =ᵐ (cObsMean d - cObsMean 0) / cObsProb d d` | `S₀`-conditional when-to-treat ratio | **Proved** |
| `cMixtureLATE_wald` | `cβ_z =ᵐ (cObsMean z - cObsMean 0) / (1 - cObsProb z 0)` | `S₀`-conditional mixture ratio, totalized at `z = 0` by Lean division | **Proved** |

**Status as of 2026-04-27 redesign.** The bridges are refactored around a per-stage workhorse (in `PO/Conditioning/EventCondExpBundle.lean`) and a layer of structural pointwise consistency rewrites (in `DynamicLATE/Consistency.lean`).  The Dynamic LATE files in this section typecheck with their local bridge and ratio proofs filled; remaining proof debt, if any, is inherited only from broader project-wide Mathlib/measure-theory stubs already documented elsewhere.

**Setup.lean** also gained:
* `exclusion` field on `Assumptions` (primitive-process clause `Y(D, S)`).
* `exclusion_D1` field on `Assumptions` (primitive-process clause `D₁(Z₁, S₀)`: `D₁` does not depend on `Z₂`); needed to identify `D₁(z)` with the factual `D₁` on the event `{Z₁ = z 0}` in the conditional compliance bridge.
* Helpers `encouragementRegime_disjoint_treatmentRegime` and `encTreatRegime`.

---

## 9g. `PO/ID/Exact/ATE.lean` — Backdoor ATE Identification Theorem (PO framework)

Formalizes the standard backdoor-adjustment identification of the average treatment effect at the potential-outcome level. Mirrors the structure of `LATE.lean` but uses σ-algebra conditional expectations (instead of event-based `eventCondExp`) because the adjustment variable `X` is continuous. The covariate type `γ` is an arbitrary measurable space, so the file simultaneously handles discrete/continuous/vector-valued `X` (e.g. `γ := ℝⁿ`).

Core identity (CATE level):

    μ[Y(d) | σ(X)]  =ᵐ[μ]  μ[Y · 1_{D=d} | σ(X)] / P[D=d | σ(X)].

Integrated corollary: `ATE = adjustedATE`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POBackdoorSystem P γ` | `structure { D, Y, Xvar, hDbool, hYreal, distinct }` | Backdoor subsystem: binary treatment `D`, real outcome `Y`, covariate `POVar P γ` | **Defined** |
| `POBackdoorSystem.{dVar, yVar, xVar}` | `POVar P Bool` / `POVar P ℝ` / `POVar P γ` | `POVar` wrappers around `D`, `Y`, `X` | **Defined** |
| `POBackdoorSystem.YofD S d` | `P.Ω → ℝ` | Counterfactual outcome `Y(d) := yVar.cfUnder dVar d` | **Defined** |
| `POBackdoorSystem.{factualD, factualY, factualX}` | `P.Ω → Bool` / `ℝ` / `γ` | Factual treatment / outcome / covariate | **Defined** |
| `POBackdoorSystem.measurable_{YofD, factualD, factualY, factualX}` | `Measurable …` | Measurability of the counterfactual/factual variables (one-line delegations) | **Proved** |
| `POBackdoorSystem.sigmaX` | `MeasurableSpace P.Ω` | σ-algebra generated by `factualX` (`MeasurableSpace.comap factualX _`) | **Defined** |
| `POBackdoorSystem.sigmaX_le` | `sigmaX ≤ P.measΩ` | σ(X) is a sub-σ-algebra of the ambient one | **Proved** |
| `POBackdoorSystem.yUnderD S d` | `RegimedVar P ℝ` | Regimed form of `Y(d)` used to populate `cfBundle` | **Defined** |
| `POBackdoorSystem.cfBundle` | `POCFBundle P` | Bundle `(Y(1), Y(0))` built via `POCFBundle.cons`; target of the unconfoundedness hypothesis | **Defined** |
| `POBackdoorSystem.propScore S d` | `P.Ω → ℝ` | Propensity score `P[D=d | σ(X)] := μ[dVar.indicator d | sigmaX]` | **Defined** |
| `POBackdoorSystem.CATE S d` | `P.Ω → ℝ` | Conditional ATE `μ[Y(d) | σ(X)]` | **Defined** |
| `POBackdoorSystem.ATE` | `ℝ` | `∫ (Y(1) - Y(0)) ∂μ` — average treatment effect | **Defined** |
| `POBackdoorSystem.adjustedCE S d` | `P.Ω → ℝ` | Observable adjusted-outcome functional `μ[Y · 1_{D=d} | σ(X)] / propScore d` | **Defined** |
| `POBackdoorSystem.adjustedATE` | `ℝ` | `∫ (adjustedCE 1 - adjustedCE 0) ∂μ` — identified side of ATE | **Defined** |
| `POBackdoorSystem.Assumptions S` | `structure { consistency, unconfoundedness, overlap, integrable_{Y1,Y0,factualY} }` | Backdoor assumptions (PO level); `unconfoundedness : P.CondIndepCF (ofFactual dVar) cfBundle (ofFactual xVar) P.μ`; **pointwise** overlap `∀ᵐ ω, 0 < propScore 1 ω ∧ propScore 1 ω < 1` (relaxed from the uniform-`c` strong-overlap form to bare positivity, mirroring DTR) | **Defined** |
| `POBackdoorSystem.cate_backdoor` | `S.CATE d =ᵐ[P.μ] S.adjustedCE d` | CATE-level backdoor adjustment identity; reduces to (a) projecting `hA.unconfoundedness` onto the `d`-th bundle coord via `CondIndepCF.project` and (b) applying `Mathlib.CondIndep.condExp_mul_of_condIndep`; `hne` (nonzero propScore at both `d=true/false`) closed via pointwise overlap — `d=true` directly, `d=false` via `propScore false = 1 - propScore true` from the indicator sum identity. Uses `POVar.indicator` / `POVar.factual_mul_indicator_eq_cfUnder_mul_indicator_fn` throughout | **Proved** (conditional on `condExp_mul_of_condIndep` and `condExp_sup_comap_eq_of_condIndep` Mathlib stubs) |
| `POBackdoorSystem.ate_backdoor` | `S.ATE = S.adjustedATE` | Integrated ATE identification; one-line call to `POVar.integral_sub_eq_integral_sub_of_condExpGiven_ae_eq` fed by `cate_backdoor` at `d = true, false` | **Proved** (conditional on `cate_backdoor`) |
| `POBackdoorSystem.propScore_ne_of_overlap` | `(hov : ∀ᵐ ω, 0 < propScore true ω ∧ propScore true ω < 1) (d) → ∀ᵐ ω, propScore d ω ≠ 0` | Per-arm non-vanishing of the propensity from the (two-sided) **overlap predicate alone** (no full `Assumptions`); reusable by the estimation-layer constructors | **Proved** |

### Regression-adjustment: the adjustment functional is an observable regression

Third beat of the identification story (after `cate_backdoor`, `ate_backdoor`): `adjustedCE d` — the σ(X)-stratified adjustment functional — equals the **observable outcome regression** `E[Y | D=d, X]`, a functional of the observed law of `(X,D,Y)` only (overlap, NO unconfoundedness). Certifies that the back-door nuisance `μ` is a genuine regression problem (the one `ML/Causal/RegressionBridge.lean` solves). Heavy step factored into the reusable Mathlib kernel `condExp_setIndicator_condExp_of_le` (§10k').

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POBackdoorSystem.factualDX` / `measurable_factualDX` | `P.Ω → Bool × γ` | Factual `(D, X)` pair `ω ↦ (factualD ω, factualX ω)` and its measurability | **Defined**/**Proved** |
| `POBackdoorSystem.sigmaDX` / `sigmaDX_le` / `sigmaX_le_sigmaDX` | `MeasurableSpace P.Ω` | σ(D,X) `:= comap factualDX`; sub-σ-algebra of ambient; and `σ(X) ≤ σ(D,X)` (tower) | **Defined**/**Proved** |
| `POBackdoorSystem.outcomeReg` / `stronglyMeasurable_outcomeReg` | `P.Ω → ℝ` | Outcome regression `E[Y | D,X] := μ[factualY | σ(D,X)]` (observable) + its σ(D,X)-strong-measurability | **Defined**/**Proved** |
| `POBackdoorSystem.exists_regFn` / `regFn` / `measurable_regFn` / `outcomeReg_eq_regFn` | `Bool × γ → ℝ` | Doob–Dynkin lift (`Measurable.exists_eq_measurable_comp`) of `outcomeReg` to the value-space regression function `regFn (d,x) = E[Y|D=d,X=x]`, with `outcomeReg = regFn ∘ factualDX` | **Defined**/**Proved** |
| `POBackdoorSystem.exists_propScoreLift` / `eLift` / `measurable_eLift` / `propScore_true_eq_eLift` | `γ → ℝ` | Doob–Dynkin lift of `propScore true` to the value-space propensity `eLift x` (`E[D|X=x]`), with `propScore true = eLift ∘ factualX` | **Defined**/**Proved** |
| `POBackdoorSystem.regression_adjustment` | `(d) (hY : Integrable factualY) (h_ne : ∀ᵐ ω, propScore d ω ≠ 0) → adjustedCE d =ᵐ (fun ω => regFn (d, factualX ω))` | **Regression-adjustment identification (observable).** `adjustedCE d =ᵐ E[Y|D=d,X]`; overlap-only, no unconfoundedness. Proof: `condExpRatio_eq_of_mul` → Mathlib kernel `condExp_setIndicator_condExp_of_le` (σ(X)→σ(D,X) tower) → arm collapse on `{D=d}` → σ(X) pull-out | **Proved** |
| `POBackdoorSystem.regression_adjustment_of_assumptions` | `(hA : Assumptions) (d) → adjustedCE d =ᵐ (fun ω => regFn (d, factualX ω))` | Packaged wrapper: discharges `Integrable factualY` (via `Assumptions.integrable_factualY`) and overlap (via `propScore_ne_of_overlap`) from `hA` | **Proved** |

---

## 9g'. `PO/ID/Exact/ATT.lean` — Backdoor ATT Identification Theorem (PO framework)

Mirrors `PO/ID/Exact/ATE.lean` but identifies the **average treatment effect on the treated** (ATT). Reuses `POBackdoorSystem` (and its `Assumptions`/`adjustedCE`/`propScore`/`CATE`) verbatim. Adds the population-treated-fraction `propTreated`, the PO-level `ATT = E[A·(Y(1)−Y(0))] / π_T`, the observable outcome-regression form `adjustedATT = E[A·(μ₁(X)−μ₀(X))] / π_T`, an extended assumption bundle `ATTAssumptions` (adds `0 < propTreated`), and the AIPW corollary at the σ(X) representative level. The two main theorems delegate the heavy lifting to private helpers:

* **Outcome-regression numerator equality** (per arm `d`): `∫ A · Y(d) dμ = ∫ A · adjustedCE d dμ` — by tower over σ(X) and the unconfoundedness factorization `μ[A·Y(d)|σ(X)] = μ[A|σ(X)] · μ[Y(d)|σ(X)] = e(X)·CATE d`, then `cate_backdoor` to swap `CATE d ↦ adjustedCE d`. The σ(X)-measurable side does the same tower step on the OR side.
* **AIPW IPW-correction vanishing**: `∫ (1−A) · (e/(1−e)) · (Y − adjustedCE false) dμ = 0` — pull `e/(1−e)` (σ(X)-measurable) out of `μ[·|σ(X)]`, rewrite `(1−A)·Y = (1−A)·Y(false)` via consistency, factor by unconfoundedness, then collapse `(1−A)·Y(false) − (1−A)·adjustedCE false` because `μ[Y(false)|σ(X)] =ᵐ adjustedCE false`.
* **Treated-arm consistency rewrite**: `∫ A·(Y − adjustedCE false) dμ = ∫ A·(adjustedCE true − adjustedCE false) dμ` — apply the OR numerator equality at `d = true` to the `A·Y` piece (using `Y = A·Y(1) + (1−A)·Y(0)` consistency-collapsed to `A·Y = A·Y(1)` on the treated set).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POBackdoorSystem.propTreated S` | `ℝ` | Marginal treatment probability `π_T = E[1_{D=1}] = ∫ dVar.indicator true dμ` | **Defined** |
| `POBackdoorSystem.ATT S` | `ℝ` | PO-level ATT `(∫ A · (Y(true) − Y(false)) dμ) / π_T` | **Defined** |
| `POBackdoorSystem.adjustedATT S` | `ℝ` | Observable outcome-regression form `(∫ A · (adjustedCE true − adjustedCE false) dμ) / π_T` | **Defined** |
| `POBackdoorSystem.ATTAssumptions S` | `structure extends Assumptions S { propTreated_pos }` | Extended bundle: `Assumptions` + `0 < propTreated`. The `< 1` half of overlap (already in `Assumptions.overlap`) suffices for the AIPW corollary | **Defined** |
| `POBackdoorSystem.propScore_false_ae` | `S.propScore false =ᵐ[μ] (1 - S.propScore true)` | Indicator-pair sum identity transported through `μ[·|σ(X)]` (private) | **Proved** |
| `POBackdoorSystem.{stronglyMeasurable_adjustedCE_comap, measurable_adjustedCE, integrable_adjustedCE}` | various | σ(X)-strong-measurability, ambient measurability, and a.e. integrability of `adjustedCE d` (private; integrability via `cate_backdoor` transport from `CATE d = condExpGiven Y(d)`) | **Proved** |
| `POBackdoorSystem.att_numerator_arm` | `(hA : Assumptions S) (d) → ∫ A · Y(d) dμ = ∫ A · adjustedCE d dμ` | Per-arm OR numerator equality (private); fed by tower + `condExp_mul_of_condIndep` + `cate_backdoor` | **Proved** |
| `POBackdoorSystem.propScore_ne_zero` | `(hA : Assumptions) (d) → ∀ᵐ ω, propScore d ω ≠ 0` | Both-arm non-vanishing of propensity from `Assumptions.overlap` (mirrors the ATE helper) (private) | **Proved** |
| `POBackdoorSystem.condExp_indicator_residual_adjusted_zero` | `μ[(1−A) · (Y − adjustedCE false) | σ(X)] =ᵐ 0` | Residual zero-conditional-mean for the `A=0` arm using `adjustedCE false` (private) | **Proved** |
| `POBackdoorSystem.weighted_false_residual_integral_zero` | `∫ (1−A) · (e/(1−e)) · (Y − adjustedCE false) dμ = 0` (under `hIPW` integrability + `Assumptions`) | IPW-correction vanishing (private) | **Proved** |
| `POBackdoorSystem.treated_factual_residual_integral_eq_adjusted` | `∫ A·(Y − adjustedCE false) dμ = ∫ A·(adjustedCE true − adjustedCE false) dμ` | Treated-arm consistency rewrite (private) | **Proved** |
| `POBackdoorSystem.ATT_eq_adjustedATT` | `(hA : ATTAssumptions S) → S.ATT = S.adjustedATT` | **Main ATT identification.** Reduces to numerator equality via `congr 1` (denominator `propTreated > 0`) and applies `att_numerator_arm` at `d = true, false` | **Proved** |
| `POBackdoorSystem.adjustedATT_eq_aipwForm` | `(hA : ATTAssumptions S) (hIPW : Integrable IPW-correction) → S.adjustedATT = AIPW-form numerator / propTreated` | **AIPW corollary.** AIPW form uses the σ(X)-measurable representatives `adjustedCE false` (for `μ₀(X)`) and `propScore true` (for `e(X)`). The extra `hIPW` integrability hypothesis is honest about the fact that `e/(1−e)` is unbounded under bare-positivity overlap; this is the standard registered hypothesis for the Hahn (1998) ATT IF | **Proved** |

---

## 9g''. `PO/ID/Exact/QTE/QuantileEffect.lean` — Backdoor Quantile Treatment Effect (Firpo 2007)

Marginal quantile treatment effect `QTE(τ) = F_{Y(1)}⁻¹(τ) − F_{Y(0)}⁻¹(τ)`
under Firpo (2007) ignorability: unconfoundedness + common support (`0 < e_d < 1`,
in `Assumptions`; strict overlap is *not* an identification assumption). Reuses
`POBackdoorSystem` (binary `D`, real `Y`, covariate `X`) and its `Assumptions`
from §9g; covariates enter only in identification, not the estimand (standard
marginal QTE). The quantile layer makes QTE a *functional of the two
potential-outcome laws*: once those laws are identified by observable measures,
QTE is the difference of their quantiles.

| Declaration | Signature | Description | Status |
|---|---|---|---|
The distributional engine (`ipwDensity`, `ipwLaw`, `cfUnderLaw_eq_ipwLaw`) lives
in `DistributionalBackdoor.lean` (§9g''b) and is imported here; this file is the quantile
layer on top.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POBackdoorSystem.qtdQuantile S d μ τ` | `ℝ` | `τ`-quantile of `Y(d)` (`yVar.cfUnderQuantile dVar d`). | **Defined** |
| `POBackdoorSystem.qte S μ τ` | `ℝ` | QTE estimand `F_{Y(1)}⁻¹(τ) − F_{Y(0)}⁻¹(τ)`. | **Defined** |
| `POBackdoorSystem.qtdQuantile_eq_quantile_cfUnderLaw` | `= Stat.quantile (cfUnderLaw …) τ` | Quantile of `Y(d)` = quantile of its law. | `rfl` |
| `POBackdoorSystem.qte_eq_of_law_eq` | `cfUnderLaw … true = ν₁ → cfUnderLaw … false = ν₀ → qte = quantile ν₁ τ − quantile ν₀ τ` | **QTE as a functional of the identified laws** — the payoff of the quantile layer; reduces QTE identification to distributional identification. | **Proved** |
| `POBackdoorSystem.qte_backdoor` | `qte P.μ τ = quantile (ipwLaw 1) τ − quantile (ipwLaw 0) τ` | Backdoor QTE identification; chains `qte_eq_of_law_eq` with the proven `cfUnderLaw_eq_ipwLaw`. | **Proved** |

## 9g''b. `PO/ID/Exact/QTE/DistributionalBackdoor.lean` — Distributional backdoor (Firpo 2007)

Lifts the mean-level backdoor (`ate_backdoor`/`cate_backdoor`, §9g) to the whole
law of `Y(d)`: under Firpo ignorability (unconfoundedness + bare common support
`0 < e_d`), `Law(Y(d))` equals the observable inverse-probability-weighting law.
This is the engine behind §9g'' QTE identification.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POBackdoorSystem.ipwDensity S d` | `P.Ω → ℝ` | IPW density `1_{D=d} / e_d(X)`. | **Defined** |
| `POBackdoorSystem.ipwLaw S d μ` | `Measure ℝ` | Observable IPW law: `factualY` pushed through `(1_{D=d}/e_d)·μ`. | **Defined** |
| `POBackdoorSystem.propScore_pos` | `Assumptions → ∀ᵐ ω, 0 < propScore d ω` | Both arms have a.e.-positive propensity score (bare overlap; false arm via `e_true + e_false = 1`). | **Proved** |
| `POBackdoorSystem.integral_mul_indicator_eq_integral_mul_propScore` | `∫ h·1_{D=d} = ∫ h·e_d` (σX-meas `h`) | Conditional-expectation pull-out of the indicator against a σ(X)-measurable factor. | **Proved** |
| `POBackdoorSystem.ipwDensity_integrable` | `Assumptions → Integrable (ipwDensity d)` | IPW weight integrable under bare overlap, via a truncation / monotone-convergence argument (`∫ 1_{D=d}/e_d = 1`, no uniform lower bound on `e_d`). | **Proved** |
| `POBackdoorSystem.integral_comp_YofD_eq` | `∫ g(Y(d)) = ∫ g(Y)·1_{D=d}/e_d` (bounded meas `g`) | **Core distributional identity** — the `g`-transform of `cate_backdoor`: consistency on `{D=d}`, `Y(d) ⟂ D∣σX` via `condExp_mul_of_condIndep`, then pull `1/e_d` out and cancel. | **Proved** |
| `POBackdoorSystem.cfUnderLaw_eq_ipwLaw` | `cfUnderLaw … d P.μ = ipwLaw d P.μ` | **Distributional backdoor** (Firpo): law of `Y(d)` = observable IPW law. Assembled via `Measure.ext` from `integral_comp_YofD_eq` at `g = 1_s`, bridging `Measure.map`/`withDensity` to the set lintegral. | **Proved** |

## 9g'''. `PO/ID/Exact/PartialLinear/{Setup,Identification}.lean` — Partially Linear Model (continuous treatment, PO framework)

The partially linear regression (PLR) model `Y = g(X) + θ·D + noise` as a causal
model under the **standard backdoor PO framework with a real-valued (continuous)
treatment** `D ∈ ℝ`. `Setup.lean` builds the continuous-treatment substrate and the
homogeneous structural model; `Identification.lean` identifies the structural slope
`θ` as the Robinson partialling-out estimand (the causal bridge). All declarations
**proved** (sorry-free).

| Declaration | Statement | Description |
|---|---|---|
| `POPartialLinearSystem` | structure | Continuous-treatment backdoor subsystem: real treatment node `D`, real outcome `Y`, covariate `X`, value-space equivs, node distinctness. Mirrors `POBackdoorSystem` with `D : ℝ`. |
| `POPartialLinearSystem.YofD d` | `P.Ω → ℝ` | The potential outcome `Y(d)` under dose `d : ℝ` (`yVar.cfUnder dVar d`). |
| `POPartialLinearSystem.sigmaX` / `sigmaXD` | `MeasurableSpace P.Ω` | σ-algebra generated by the covariate, resp. by the covariate and treatment jointly; `sigmaX_le_sigmaXD` records the inclusion. |
| `POPartialLinearModel` | structure (extends system) | Adds the homogeneous linear dose-response `Y(d) = b(X)+θ·d+U`, the backdoor mean-independence `E[U∣σ(X,D)]=0`, and consistency. |
| `POPartialLinearModel.factualY_eq` | `Y =ᵐ b(X)+θ·D+U` | Observed-data form of the structural model, from consistency + the dose-response restriction. |
| `POPartialLinearModel.causal_homogeneity` | `Y(d)−Y(d') =ᵐ θ·(d−d')` | The causal reading: `θ` is the constant per-unit treatment effect. |
| `POPartialLinearModel.mReg` / `lReg` | `P.Ω → ℝ` | Treatment / outcome regressions on the covariate, `E[D∣σX]` and `E[Y∣σX]`. |
| `POPartialLinearModel.condExp_U_sigmaX` | `E[U∣σX] =ᵐ 0` | The structural error has zero mean given the covariate (backdoor + tower). |
| `POPartialLinearModel.condExp_resid_sigmaX` | `E[D−m₀(X)∣σX] =ᵐ 0` | The treatment residual is conditionally mean-zero. |
| `POPartialLinearModel.integral_U_resid` | `E[U·(D−m₀(X))] = 0` | Orthogonality of the structural error to the treatment residual (needs `Integrable U`). |
| `POPartialLinearModel.lReg_eq` | `ℓ₀(X) =ᵐ b(X)+θ·m₀(X)` | Decomposition of the outcome regression. |
| `POPartialLinearModel.robinson_estimand_eq_theta` | `E[(Y−ℓ₀)(D−m₀)] / E[(D−m₀)²] = θ` | **The causal bridge**: the Robinson partialling-out estimand equals the structural slope, given residual treatment variation `E[(D−m₀)²]≠0`. |

## 9h. `PO/ID/Exact/DTR/` — Sequential Backdoor Identification (Dynamic Treatment Regime, general `n`)

Formalizes `prop:po-dynamic-backdoor` from `Basic Concepts.tex` (subsection
`subsec:po-dynamic-regime`) for arbitrary horizon `n ≥ 1`, in the bare
potential-outcome framework.  Reuses the bundle-conditioned conditional
expectation machinery from `PO/Conditioning/Bundle.lean`.

The identification target is the **iterated-conditional-expectation form**

    (historyBundle 0).condExpGiven (Y_of dbar)  =ᵐ[μ]  innerReg dbar (n - 1)

where `innerReg dbar j` is a recursively-defined CE-ratio chain over `n`
stages (innermost level `j = 0` conditions on the full history
`historyBundle (n - 1) = (S 0, D 0, …, S (n - 1))`; outermost level
`j = n - 1` conditions on `historyBundle 0 = (S 0,)`).  Integrated form:
`E[Y(dbar)] = ∫ innerReg dbar (n - 1) ∂μ`.

**Treatment-space and state-space conventions.**  One homogeneous
treatment space `δ : Type` (with `[MeasurableSpace δ] [MeasurableSingletonClass δ]`)
shared by all stages, and per-stage state types `γ : Fin n → Type`.  A
treatment sequence is `dbar : Fin n → δ`.  The `n = 2, δ = Bool`
specialization recovers the legacy two-stage statement (with
`dbar 0 = d₁` and `dbar 1 = d₂`).

**Per-sequence sequential exchangeability.**  Sequential exchangeability is
quantified per `dbar` (mirroring `def:po-dynamic-assumptions` literally),
avoiding the `Fin (|δ|^n)`-wide counterfactual bundle that the n=2
prototype used.  The conditioning σ-algebra at stage `k` is the full
σ-algebra of `historyBundle k` (the `{D̄_{k-1} = d̄_{k-1}}` qualifier from
the tex is dropped per the agreed simplification).

The code is split into five files: Setup / Helpers / Induction /
StrongCancellation / Main.  The dedicated `StrongCancellation.lean` file
houses the unified inductive predicate that merges the cancellation
identity with integrability of `innerReg`, needed under the (relaxed)
*pointwise* overlap hypothesis.

### `DTR/Setup.lean` — Data layer (structure, regimes, bundles, assumptions, observable functionals)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODTRSystem P n δ γ` | `structure { S, D, Y, hDmeas, hYreal, distinctSS, distinctDD, distinctSD, distinctSY, distinctDY }` | General-`n` DTR subsystem: state covariates `S : ∀ k : Fin n, POVar P (γ k)`, treatments `D : Fin n → P.V` with `hDmeas k : P.X (D k) ≃ᵐ δ`, real outcome `Y` with `hYreal : P.X Y ≃ᵐ ℝ`, and five distinctness fields covering the `2n + 1` declared variables (state-state injectivity, treatment-treatment injectivity, S-D, S-Y, D-Y disjointness) | **Defined** |
| `PODTRSystem.dVar S k` | `POVar P δ` | `POVar` wrapper around `S.D k`: `⟨S.D k, S.hDmeas k⟩` | **Defined** |
| `PODTRSystem.yVar S` | `POVar P ℝ` | `POVar` wrapper around `S.Y`: `⟨S.Y, S.hYreal⟩` | **Defined** |
| `PODTRSystem.regimeTarget S k` | `Finset P.V` | The set `{D 0, …, D (k-1)}` (clamped if `k > n`); the `target` of `regimeUpTo dbar k` | **Defined** |
| `PODTRSystem.regimeTarget_mem_iff` | — | `v ∈ regimeTarget k ↔ ∃ i : Fin n, i.val < k ∧ v = S.D i` | **Proved** |
| `PODTRSystem.regimeUpToAux S dbar k h` | `{ r : Regime P.V P.X // r.target = S.regimeTarget k }` | Subtype-bundled regime carrying its target proof; recursion on `k` builds via `Regime.sqcup` of one fresh `Regime.single (S.D ⟨k, _⟩) _` and the smaller regime, with disjointness discharged from `S.distinctDD` and the carried target equation | **Defined** |
| `PODTRSystem.regimeUpTo S dbar k h` | `Regime P.V P.X` | The regime `{D 0 ← dbar 0, …, D (k-1) ← dbar (k-1)}`; thin `.1` projection of `regimeUpToAux` | **Defined** |
| `PODTRSystem.regimeUpTo_target_eq` | `(S.regimeUpTo dbar k h).target = S.regimeTarget k` | `.2` projection from the subtype bundle | **Proved** |
| `PODTRSystem.regime S dbar` | `Regime P.V P.X` | The full-stage regime `regimeUpTo dbar n (le_refl n)` | **Defined** |
| `PODTRSystem.Y_of S dbar` | `P.Ω → ℝ` | Counterfactual outcome `Y(dbar) := S.yVar.cf (S.regime dbar)` | **Defined** |
| `PODTRSystem.S_of S dbar k` | `P.Ω → γ k` | Counterfactual stage-`k` state under the first `k.val` interventions: `(S.S k).cf (S.regimeUpTo dbar k.val _)` | **Defined** |
| `PODTRSystem.{factualD k, factualY, factualS k}` | `P.Ω → δ` / `ℝ` / `γ k` | Factual treatments / outcome / states (each variable's `.factual`) | **Defined** |
| `PODTRSystem.measurable_*` | — | One-liner measurability of the counterfactual / factual maps above | **Proved** |
| `PODTRSystem.indD S dbar k` | `P.Ω → ℝ` | Recursive joint indicator `1_{D 0 = dbar 0 ∧ … ∧ D (k-1) = dbar (k-1)}`: `indD 0 = 1`, `indD (k+1) = indD k · (S.dVar ⟨k, _⟩).indicator (dbar ⟨k, _⟩)` (when `k < n`) | **Defined** |
| `PODTRSystem.measurable_indD` | `Measurable (S.indD dbar k)` | Measurability of the joint indicator (induction on `k`) | **Proved** |
| `PODTRSystem.historyBundle S k h` | `POCFBundle P` (requires `k < n`) | History conditioning bundle at stage `k`: cons-list `(S k, D (k-1), S (k-1), D (k-2), …, S 0)`.  At `k = 0` it is `(S 0,)`; at general `k` it conses `S k` and `D (k-1)` on top of `historyBundle (k - 1)`.  Matches the legacy two-stage convention (`historyBundle 0 = old historyBundle₁`, `historyBundle 1 = old historyBundle₂`). | **Defined** |
| `PODTRSystem.cfYBundle S dbar` | `POCFBundle P` | Singleton CF bundle `[Y(dbar)]` used as the subject of per-sequence sequential exchangeability | **Defined** |
| `PODTRSystem.Assumptions S` | `structure { consistency, exch, overlap, integrable_Y, integrable_factualY }` | Sequential backdoor assumptions (PO level, general `n`): consistency, **per-sequence** sequential exchangeability `∀ dbar, ∀ k : Fin n, P.CondIndepCFBundle (RegimedVar.ofFactual (S.dVar k)) (S.cfYBundle dbar) (S.historyBundle k.val k.isLt) P.μ`, **pointwise** overlap `∀ dbar, ∀ k, ∀ᵐ ω, 0 < (S.historyBundle k.val k.isLt).condExpGiven ((S.dVar k).indicator (dbar k)) P.μ ω` (relaxed from `∃ c > 0, c < condExpGiven ...` to pointwise positivity), integrability of every `Y(dbar)` and `factualY` | **Defined** |
| `PODTRSystem.innerReg S dbar j` | `P.Ω → ℝ` | Recursive iterated-CE-ratio chain.  `j = 0` is the innermost: `(historyBundle (n-1)).condExpGiven (factualY · indD n) / condExpGiven (indD n)`.  `j + 1` adds one outer CE-ratio at `historyBundle (n - j - 2)` with extra factor `(S.dVar ⟨n - j - 2, _⟩).indicator (dbar ⟨n - j - 2, _⟩)`.  For `n = 0`, returns `0`. | **Defined** |
| `PODTRSystem.dtrEffect S dbar` | `ℝ` | Mean potential outcome `E[Y(dbar)] := ∫ Y_of dbar ∂P.μ` | **Defined** |
| `PODTRSystem.adjustedDtr S dbar` | `ℝ` | Integrated adjusted DTR functional `∫ innerReg dbar (n - 1) ∂P.μ` (observable side); `0` when `n = 0` | **Defined** |

### `DTR/Helpers.lean` — Auxiliary measurability, integrability, σ-algebra-comparison lemmas

These helpers are not exposed publicly beyond the proofs in
`DTR/Induction.lean` and `DTR/Main.lean`, but cannot be `private` since
they are split across files.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODTRSystem.hb_step_proj S k h` | `(∀ i, (historyBundle (k+1) h).type i) → ∀ j, (historyBundle k _).type j` | One-step projection from the stage-`(k+1)` joint value to the stage-`k` joint value (forgets the freshly-consed `(S (k+1), D k)` coordinates) | **Defined** |
| `PODTRSystem.measurable_hb_step_proj` | `Measurable (S.hb_step_proj k h)` | Measurability of the projection | **Proved** |
| `PODTRSystem.hb_jointValue_step_eq` | `(historyBundle k _).jointValue = hb_step_proj k h ∘ (historyBundle (k+1) h).jointValue` | Factorisation of the smaller history's joint value through the projection | **Proved** |
| `PODTRSystem.historyBundle_sigma_mono_step` | `(historyBundle k _).sigma ≤ (historyBundle (k+1) h).sigma` | One-step σ-algebra inclusion; needed for the `σ_k → σ_{k+1}` tower in `cdtr_step` | **Proved** |
| `PODTRSystem.historyBundle_sigma_mono` | `(historyBundle j (lt_of_le_of_lt hjk hk)).sigma ≤ (historyBundle k hk).sigma` for `j ≤ k` | Iterated σ-algebra monotonicity | **Proved** |
| `PODTRSystem.indD_eq_zero_or_one` | `∀ k ω, S.indD dbar k ω = 0 ∨ S.indD dbar k ω = 1` | Pointwise: the joint indicator is 0/1-valued; recursion on `k` | **Proved** |
| `PODTRSystem.indD_integrable` | `Integrable (S.indD dbar k) P.μ` | `1_{D 0 = dbar 0 ∧ …}` integrable on a finite measure (bounded by 1) | **Proved** |
| `PODTRSystem.indD_eq_indicator_event` | `S.indD dbar k = ({ω \| ∀ i : Fin n, i.val < k → S.factualD i ω = dbar i}).indicator (fun _ ↦ 1)` | Bridge from the recursive `if`-form to the set-indicator form on the agreement event; required to apply `factual_mul_indicator_eq_cf_mul_indicator` | **Proved** |
| `PODTRSystem.indD_factor_split` | `S.indD dbar (k+1) = fun ω => S.indD dbar k ω * (S.dVar ⟨k, hk⟩).indicator (dbar ⟨k, hk⟩) ω` | Pointwise factorisation `indD (k+1) = indD k · (single-stage indicator)` | **Proved** |
| `PODTRSystem.measurable_factualS_sigma_history` | `Measurable[(S.historyBundle m hm).sigma] (S.factualS k')` for `k'.val ≤ m` | `S k'` lives in the stage-`m` history bundle when `k' ≤ m`; coordinate-projection witness | **Proved** |
| `PODTRSystem.measurable_factualD_sigma_history` | `Measurable[(S.historyBundle m hm).sigma] (S.factualD k')` for `k'.val < m` | `D k'` lives in the stage-`m` history bundle when `k' < m` | **Proved** |
| `PODTRSystem.stronglyMeasurable_indicator_dVar_sigma_history` | `StronglyMeasurable[(S.historyBundle m hm).sigma] ((S.dVar k').indicator x)` for `k'.val < m` | Real-valued indicator pull-out hypothesis used in `cdtr_step` | **Proved** |
| `PODTRSystem.stronglyMeasurable_indD_sigma_history` | `StronglyMeasurable[(S.historyBundle m hm).sigma] (S.indD dbar m')` for `m' ≤ m` | The joint indicator at depth `m'` is strongly measurable w.r.t. the stage-`m` history when `m' ≤ m` | **Proved** |
| `PODTRSystem.factualAgrees_regimeUpTo` | `(∀ i : Fin n, i.val < k → S.factualD i ω = dbar i) → P.FactualAgrees (S.regimeUpTo dbar k h) ω` | Inductive consistency: ω agrees with the partial regime when the first `k` factual treatments match `dbar`; recursion mirrors `regimeUpTo` | **Proved** |
| `PODTRSystem.factualAgrees_regime` | `∀ ω ∈ {ω \| ∀ i : Fin n, S.factualD i ω = dbar i}, P.FactualAgrees (S.regime dbar) ω` | Multi-target consistency hypothesis; corollary of `factualAgrees_regimeUpTo` at `k = n` | **Proved** |
| `PODTRSystem.indD_mul_Y_integrable` | `Integrable (fun ω => S.indD dbar k ω * S.Y_of dbar ω) P.μ` (given `Integrable (S.Y_of dbar)`) | Bound by `\|Y_of dbar\|`; needed by step_3-style pull-outs | **Proved** |
| `PODTRSystem.measurable_innerReg` | `Measurable (S.innerReg dbar j)` | Measurability of `innerReg` at every depth (recursion on `j`) | **Proved** |
| `PODTRSystem.yVar_notMem_regime` | `S.yVar.v ∉ (S.regime dbar).target` | `Y` is not a target of the full regime (which only names the `D` variables); discharged via `regimeUpTo_target_eq` and `distinctDY` | **Proved** |

(Integrability of `innerReg dbar j` is now in `DTR/StrongCancellation.lean`,
derived from the strengthened cancellation identity rather than from a
direct uniform-`c` bound.)

### `DTR/Induction.lean` — Base case and inductive step

The backward-induction identity (★ at depth `j`) is

    innerReg dbar j · indD dbar (n-1-j)
      =ᵐ[μ]
    indD dbar (n-1-j) · (historyBundle (n-1-j)).condExpGiven (Y_of dbar)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODTRSystem.cdtr_base` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `(dbar : Fin n → δ)`, `0 < n` → (★ at `j = 0`) | **Innermost-stage cancellation** (`j = 0`).  Generalizes `step_1` of the legacy n=2 proof.  Chain: consistency rewrite `factualY · indD n =ᵐ Y_of · indD n` (via `indD_eq_indicator_event` + `factualAgrees_regime` + `POVar.factual_mul_indicator_eq_cf_mul_indicator` + `yVar_notMem_regime`); factorisation `indD n = indD (n-1) · (dVar ⟨n-1⟩).indicator` via `indD_factor_split`; pull-out of `indD (n-1)` from the numerator condExp (σ-strongly-measurable via `stronglyMeasurable_indD_sigma_history`); stage-`(n-1)` exchangeability via `CondIndepCFBundle.project` + `condExp_mul_of_condIndep`; pointwise cancellation case-splitting on `indD_eq_zero_or_one`, with overlap_{n-1} discharging the nonzero branch | **Proved** |
| `PODTRSystem.cdtr_step` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `(dbar : Fin n → δ)`, `(j : ℕ)`, `j + 1 < n`, `n - j - 2 < n`, `Integrable (S.innerReg dbar j) P.μ`, IH (★ at `j`) → (★ at `j + 1`) | **Inductive step** (`j → j + 1`).  Generalizes `step_2`–`step_4` of the legacy n=2 proof.  Let `k := n - j - 2`.  Via `condExpRatio_eq_of_mul`: (A) numerator-denominator product identity using IH + `indD_factor_split` + reverse pull-out of `(dVar ⟨k⟩).indicator` inside `σ(historyBundle (k+1))` via `stronglyMeasurable_indicator_dVar_sigma_history` + tower down to `σ(historyBundle k)` via `historyBundle_sigma_mono_step` and `condExpGiven_tower_of_le` + stage-`k` exchangeability via `condExp_mul_of_condIndep`.  (B) Denominator nonvanishing via `overlap_k`.  Multiplied back by `indD k` on both sides.  The extra `Integrable (innerReg j)` hypothesis (added 2026-04-26 along with the pointwise-overlap relaxation) supplies the LHS pull-out integrability previously taken from `innerReg_integrable`. | **Proved** |

### `DTR/StrongCancellation.lean` — Strengthened cancellation identity (a.e. equality)

Under the **pointwise** overlap hypothesis, the proof of integrability of
`innerReg dbar j` is interlocked with the cancellation identity.  This
file proves a single unified inductive predicate

    P j ↔ (innerReg dbar j =ᵐ[μ] indD dbar (n-1-j) · μ[Y_of dbar | σ_{n-1-j}])
          ∧ Integrable (innerReg dbar j) μ

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODTRSystem.cdtr_strong` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `(dbar : Fin n → δ)`, `0 < n`, `∀ j < n` → `(strong identity at j) ∧ Integrable (innerReg j)` | **Strengthened cancellation identity (joint with integrability).**  By induction on `j`: base case `j = 0` reuses `cdtr_base` for the weak form and case-splits on `indD m ∈ {0,1}` to upgrade to the strong form (vanishing on `{indD m = 0}` from numerator/denominator both factoring through `indD m`).  Step case `j → j+1` calls `cdtr_step` with the integrability part of the IH, then upgrades via `indD_factor_split` and the strong IH to show `μ[innerReg j · ind_k | σ_k]` factors through `indD k`, hence `innerReg(j+1) =ᵐ 0` on `{indD k = 0}`. | **Proved** |
| `PODTRSystem.innerReg_integrable` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `j < n` → `Integrable (S.innerReg dbar j) P.μ` | Integrability of every iterated CE-ratio.  Re-derived from `cdtr_strong`'s second conjunct (replacing the previously sorry'd direct-bound proof in `Helpers.lean`). | **Proved** |

### `DTR/Main.lean` — Final theorems

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODTRSystem.cdtr_iter` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `0 < n`, `∀ j < n` → (★ at `j`) | Backward-induction iterate combining `cdtr_base` (`j = 0`) and `cdtr_step` (`j → j + 1`); proved by `Nat.rec` on `j`, with index-bridging via `omega`.  Threads the integrability hypothesis required by `cdtr_step` from `cdtr_strong`. | **Proved** |
| `PODTRSystem.cdtr_backdoor` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `(dbar : Fin n → δ)`, `0 < n` → `(S.historyBundle 0 _).condExpGiven (S.Y_of dbar) P.μ =ᵐ[P.μ] S.innerReg dbar (n - 1)` | **General-`n` CDTR backdoor identification.**  Specialise `cdtr_iter` at `j = n - 1`; the partial indicator `S.indD dbar 0 = fun _ => 1` collapses both `· indD 0` factors via `mul_one`/`one_mul` | **Proved** |
| `PODTRSystem.dtr_backdoor` | `[StandardBorelSpace P.Ω] [IsFiniteMeasure P.μ]`, `S.Assumptions`, `(dbar : Fin n → δ)`, `0 < n` → `S.dtrEffect dbar = S.adjustedDtr dbar` | **General-`n` integrated DTR identification.**  `E[Y(dbar)] = ∫ innerReg dbar (n - 1)`.  Two-line corollary of `cdtr_backdoor` via `MeasureTheory.integral_condExp` and `integral_congr_ae` | **Proved** |

**Status / known gaps.**
- Both `cdtr_backdoor` and `dtr_backdoor` are unconditional **for arbitrary `n ≥ 1`**, modulo the standing project-wide `Mathlib.CondIndep` stubs (`condExp_mul_of_condIndep`) shared with ATE.lean and DID.lean.  No `sorry` in any of the four DTR files.
- The **nested-integral form** that matches `prop:po-dynamic-backdoor` literally in the `.tex` (`∫ ⋯ ∫ μ[Y \| S̄_n, D̄_n=d̄_n] dP(S_n \| S̄_{n-1}, D̄_{n-1}=d̄_{n-1}) ⋯ dP(S_1)`) is *deferred*: it requires the not-yet-available `condDistrib`-as-kernel infrastructure (cf. `Causalean/Mathlib/CondDistrib.lean`, the *condDistrib → Kernel.condKernel migration*).
- The remark in the tex about **history-dependent regimes** `d_k : (S̄_k, D̄_{k-1}) → δ` is not formalized here; the same induction pattern would carry over once the corresponding regime-builder is available.

---

## 9i. `PO/ID/Exact/DID.lean` — Two-Period DID Identification of the ATT (PO framework)

Formalizes `def:po-did-system`, `def:po-did-assumptions`, and `prop:po-did-att`
from `Basic Concepts.tex` (lines 1693–1809): the two-period difference-in-differences
identification of the ATT in the bare potential-outcome framework.  Mirrors
`LATE.lean` / `ATE.lean` in style but is strictly simpler — no σ-algebra
conditional expectations, no independence assumption, no Wald ratio, no
monotonicity.  The whole file lives on the event-level `eventCondExp` layer
plus `POVar` consistency.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `PODIDSystem P` | `structure { D, Y₀, Y₁, hDbool, hY0real, hY1real, hDY0, hDY1 }` | Two-period DID subsystem: binary treatment `D`, real pre/post outcomes `Y₀, Y₁`; `D` distinct from each outcome | **Defined** |
| `PODIDSystem.dVar`, `y0Var`, `y1Var` | `POVar P Bool` / `POVar P ℝ` / `POVar P ℝ` | `POVar` wrappers around `D`, `Y₀`, `Y₁` using the `hDbool` / `hY0real` / `hY1real` equivalences | **Defined** |
| `PODIDSystem.Y0ofD S d`, `Y1ofD S d` | `P.Ω → ℝ` | Counterfactual outcomes `Y₀(d) := y0Var.cfUnder dVar d`, `Y₁(d) := y1Var.cfUnder dVar d` | **Defined** |
| `PODIDSystem.factualD`, `factualY₀`, `factualY₁` | `P.Ω → Bool` / `ℝ` / `ℝ` | Factual treatment and outcomes | **Defined** |
| `PODIDSystem.dEvent S d` | `Set P.Ω` | Factual event `{D = d}`, defined as `S.dVar.event d` | **Defined** |
| `PODIDSystem.measurable_{Y0ofD, Y1ofD, factualD, factualY₀, factualY₁}` | `Measurable …` | One-line delegations to `POVar.measurable_cfUnder` / `POVar.measurable_factual` | **Proved** |
| `PODIDSystem.measurableSet_dEvent` | `MeasurableSet (S.dEvent d)` | Measurability of the treatment event | **Proved** |
| `PODIDSystem.ATT` | `ℝ` | `eventCondExp P.μ (dEvent true) (Y1ofD true - Y1ofD false)` — def:po-did-system | **Defined** |
| `PODIDSystem.Assumptions S` | `structure { consistency, noAnticipation, parallelTrends, posTrue_{ne_zero,ne_top}, posFalse_{ne_zero,ne_top}, intY0ofD_false, intY1ofD_false, intY1ofD_true }` | def:po-did-assumptions; positivity split into four fields, parallel-trends stated directly on `eventCondExp` of `Y₁(0) − Y₀(0)`, integrability required only for the three counterfactuals that actually appear | **Defined** |
| `PODIDSystem.factualDiff_eq_cfDiff_on_dEvent` (private) | `P.Consistency → ∀ ω ∈ dEvent d, factualY₁ ω - factualY₀ ω = Y1ofD d ω - Y0ofD d ω` | Pointwise consistency on `{D = d}`: combines `POVar.cf_eq_factual_on_event` on `y1Var` and `y0Var` | **Proved** |
| `PODIDSystem.att_did` | `S.ATT = eventCondExp (dEvent true) (factualY₁ - factualY₀) - eventCondExp (dEvent false) (factualY₁ - factualY₀)` | DID identification of the ATT, prop:po-did-att; proof is a five-step `calc` on the event-level conditional expectation (no-anticipation split + additivity + two consistency rewrites + parallel-trends bridge) | **Proved** |

---

## 9i*. `PO/ID/Exact/CSDID.lean` — Callaway--Sant'Anna group-time DID identification

Formalizes `def:po-cs-did-system`, `def:po-cs-did-assumptions`, and
`prop:po-cs-did-att` from `Basic Concepts.tex` (lines 2439–2565): the
no-covariate Callaway--Sant'Anna staggered-adoption DID identification of the
group-time ATT(g, t) under irreversibility, no anticipation, and never-treated
parallel trends. Extends two-period `DID.lean` from one cohort + one
post-period to a finite horizon `T` with cohorts `G_g = {D_{g−1}=0, D_g=1}` and
never-treated event `C = ⋂_s {D_s = 0}`. Periods are 0-indexed in Lean
(`Fin T` with `2 ≤ T`), so the .tex bound `2 ≤ g ≤ t ≤ T` becomes
`1 ≤ g.val ≤ t.val < T`. Proof structure mirrors `PODIDSystem.att_did` line by
line; the only genuinely new ingredients are (i) recursive construction of the
treatment-path regimes `regOf g`, `regNT` via iterated `Regime.sqcup`, and
(ii) reduction of `cohortEvent` plus irreversibility to `FactualAgrees (regOf g)`
almost surely.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POCSDIDSystem P` | `structure { T, hT : 2 ≤ T, D Y : Fin T → P.V, hDbool, hYreal, hDinj, hYinj, hDY }` | Staggered-adoption DID subsystem: real-valued outcomes `Y s` and binary treatments `D s` for `s : Fin T`; `D`/`Y` injective and disjoint as node families | **Defined** |
| `POCSDIDSystem.dVar`, `yVar` | `Fin S.T → POVar P Bool` / `Fin S.T → POVar P ℝ` | Per-period `POVar` wrappers around `D s`, `Y s` | **Defined** |
| `POCSDIDSystem.factualD`, `factualY`, `dEvent` | `Fin S.T → P.Ω → Bool` / `Fin S.T → P.Ω → ℝ` / `Fin S.T → Bool → Set P.Ω` | Factual treatment / outcome at period `s`; factual event `{D s = b}` | **Defined** |
| `POCSDIDSystem.measurable_factualD`, `measurable_factualY`, `measurableSet_dEvent` | `Measurable …` / `MeasurableSet …` | One-line delegations to the `POVar` API | **Proved** |
| `POCSDIDSystem.dTargetUpTo` | `POCSDIDSystem P → ℕ → Finset P.V` | Recursive standalone target: image of `S.D` on indices `< k`; lets us prove disjointness for the next `Regime.sqcup` step | **Defined** |
| `POCSDIDSystem.dTargetUpTo_mem_iff` | `v ∈ S.dTargetUpTo k ↔ ∃ i : Fin S.T, i.val < k ∧ v = S.D i` | Membership characterisation of `dTargetUpTo`, used to drive injectivity arguments | **Proved** |
| `POCSDIDSystem.regUpToAux` | `(b : Fin S.T → Bool) → (k ≤ S.T) → { r : Regime // r.target = S.dTargetUpTo k }` | Subtype-valued recursive auxiliary that builds a regime over the first `k` periods together with its target equality (mirrors `PODTRSystem.regimeUpToAux`) | **Defined** |
| `POCSDIDSystem.regimeBy`, `regimeBy_target_eq` | `(Fin S.T → Bool) → Regime P.V P.X` / `(S.regimeBy b).target = S.dTargetUpTo S.T` | Full-horizon regime parameterised by a Bool assignment, with target equality | **Defined / Proved** |
| `POCSDIDSystem.regOf`, `regNT` | `Fin S.T → Regime P.V P.X` / `Regime P.V P.X` | Cohort regime (`D s = 1` for `g.val ≤ s.val`, else `0`) and never-treated regime (`D s = 0` always) | **Defined** |
| `POCSDIDSystem.Y_notin_dTargetUpTo`, `Y_notin_regimeBy`, `yVar_v_notin_regOf_target`, `yVar_v_notin_regNT_target` | `S.Y t ∉ …` / `(S.yVar t).v ∉ …` | The outcome node `Y t` lies outside any `regimeBy`/`regOf`/`regNT` target — required precondition for `cf_eq_factual_of_factualAgrees` | **Proved** |
| `POCSDIDSystem.YofCohort`, `YofNT` | `Fin S.T → Fin S.T → P.Ω → ℝ` / `Fin S.T → P.Ω → ℝ` | Counterfactual outcomes `Y_t(g)` and `Y_t(∞)` as `cf` under the corresponding regimes | **Defined** |
| `POCSDIDSystem.measurable_YofCohort`, `measurable_YofNT` | `Measurable …` | Measurability of the counterfactuals | **Proved** |
| `POCSDIDSystem.predFin` | `(g : Fin S.T) → 1 ≤ g.val → Fin S.T` | Predecessor index `g − 1` as `Fin S.T`; hypothesis is a precondition marker (body uses truncating ℕ subtraction) | **Defined** |
| `POCSDIDSystem.cohortEvent`, `measurableSet_cohortEvent` | `(g : Fin S.T) → 1 ≤ g.val → Set P.Ω` / `MeasurableSet …` | Cohort event `G_g = {D_{g−1}=0} ∩ {D_g=1}`; measurability | **Defined / Proved** |
| `POCSDIDSystem.neverTreatedEvent`, `measurableSet_neverTreatedEvent` | `Set P.Ω` / `MeasurableSet …` | Never-treated event `C = ⋂_s {D_s = 0}`; measurability | **Defined / Proved** |
| `POCSDIDSystem.ATT` | `(g t : Fin S.T) → 1 ≤ g.val → ℝ` | `eventCondExp P.μ (cohortEvent g hg) (Y_t(g) − Y_t(∞))` — def:po-cs-did-system | **Defined** |
| `POCSDIDSystem.Assumptions S` | `structure { consistency, irreversibilityBase, irreversibilityStep, noAnticipation, parallelTrends, posCohort, posNT, intYofCohort, intYofNT }` | def:po-cs-did-assumptions; irreversibility split into base case (`D_0 = 0` a.s.) and absorbing step (`D_s = 1 ⟹ D_{s+1} = 1` a.s.); parallel-trends stated as a long difference on `Y_t(∞) − Y_{g−1}(∞)`; positivity and integrability required for all admissible cohort-period pairs | **Defined** |
| `POCSDIDSystem.factualY_eq_YofCohort_on_cohortEvent` (private) | `S.Assumptions → ∀ g t hg, ∀ᵐ ω ∂P.μ, ω ∈ cohortEvent g hg → YofCohort t g ω = factualY t ω` | A.e. cohort consistency: on `cohortEvent g`, irreversibility forces the factual D-path to agree with `regOf g`, and consistency then gives `Y_t(g) = factual Y_t`. Filled by Codex (gpt-5.5 high) using the `regUpToAux` induction template plus forward/backward irreversibility chains | **Proved** |
| `POCSDIDSystem.factualY_eq_YofNT_on_NT` (private) | `S.Assumptions → ∀ t, ∀ᵐ ω ∂P.μ, ω ∈ neverTreatedEvent → YofNT t ω = factualY t ω` | Pointwise NT consistency: on `neverTreatedEvent`, the D-path matches `regNT` directly (no irreversibility needed); apply consistency. Filled by Codex | **Proved** |
| `POCSDIDSystem.att_csdid` | `S.Assumptions → ∀ g t hg hgt, S.ATT g t hg = eventCondExp (cohortEvent g hg) (factualY t − factualY (predFin g hg)) − eventCondExp neverTreatedEvent (factualY t − factualY (predFin g hg))` | Callaway--Sant'Anna group-time DID identification of `ATT(g, t)`, prop:po-cs-did-att. Five-step proof mirroring `PODIDSystem.att_did` (no-anticipation split, additivity, cohort consistency, parallel-trends bridge, NT consistency). Filled by Codex | **Proved** |

---

## 9i0. `PO/Analysis/Regression.lean` — Regression-Function Representatives

Shared predicate `IsRegressionFunction` and derived lemmas, extracted from the RDD
files so SharpRDD and FuzzyRDD can both use the same interface without duplication.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `IsRegressionFunction μ X g f` | `Measure Ω → (Ω → ℝ) → (Ω → ℝ) → (ℝ → ℝ) → Prop` | `f : ℝ → ℝ` is a regression-function representative of `g` over `X`: measurability of `f`, integrability of `g` and of `f ∘ X`, and the integral identity `∫_{X⁻¹A} g = ∫_{X⁻¹A} f ∘ X` for all measurable `A` | **Defined** |
| `IsRegressionFunction.integrable_pushforward` | `IsRegressionFunction μ X g f → AEMeasurable X μ → Integrable f (μ.map X)` | `f` is integrable under the pushforward of `X` whenever `X` is AE-measurable | **Proved** |
| `IsRegressionFunction.sub` | `IsRegressionFunction μ X g₁ f₁ → IsRegressionFunction μ X g₂ f₂ → IsRegressionFunction μ X (g₁ - g₂) (f₁ - f₂)` | Linearity: difference of representatives is the representative of the difference | **Proved** |
| `IsRegressionFunction.aeEq_of_aeEq_response` | `g₁ =ᵐ[μ] g₂ → IsRegressionFunction μ X g₁ f₁ → IsRegressionFunction μ X g₂ f₂ → f₁ =ᵐ[μ.map X] f₂` | If two response variables are ae-equal, their regression representatives agree ae on the pushforward | **Proved** |

---

## 9i0a. `Panel/Analysis/Residualization.lean` — Population Residualization Core (FWL framework)

Framework primitives for the **population estimand-characterization layer**: a linear `L²` nuisance class, a residualization witness `(V_H, Ṽ)` decomposing a square-integrable variable against the class, the residualized population coefficient `β_H(Y, D) = E[D̃·Ỹ] / E[D̃²]`, and the **population Frisch–Waugh–Lovell theorem**. Pure measure-theoretic; no `POSystem`/`CausalModel`/`SWIGGraph` dependence. Downstream paper-specific files (Słoczyński saturated OLS, TWFE decompositions, event-study, 2SLS, …) instantiate `ResidualizationWitness` for their nuisance class (controls, unit + time fixed effects, cohort effects, …) and reduce to the FWL ratio via this theorem. Specifies `def:po-estimand-residualization-witness`, `def:po-estimand-residualized-coefficient`, and `prop:po-estimand-fwl` from `doc/basic_concepts/po/estimand_characterization/residualization_core.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `LinearL2Class μ` | `structure { mem : (Ω → ℝ) → Prop, memLp, zero_mem, add_mem, smul_mem }` | A linear class of `MemLp 2 μ` real-valued functions on `Ω`, predicate-style: `mem` carves out the underlying set, with closure under scalar multiplication and addition, and every member required to be square-integrable | **Defined** |
| `ResidualizationWitness μ H V` | `structure { VH, Vtilde : Ω → ℝ, VH_mem, Vtilde_memLp, decomp : V =ᵐ[μ] VH + Vtilde, orthogonal }` | Population residualization witness for `V` against `H`: bundles the in-class projection `V_H ∈ H`, the L²-residual `Ṽ`, the a.e. decomposition `V = V_H + Ṽ`, and population orthogonality `∀ h ∈ H, ∫ Ṽ·h dμ = 0` | **Defined** |
| `residualizedCoefficient μ H wY wD` | `noncomputable def : ℝ` | Residualized population coefficient `β_H(Y, D) := (∫ D̃·Ỹ dμ) / (∫ D̃·D̃ dμ)`, the population Frisch–Waugh–Lovell representation of the coefficient on `D` in a linear projection that also includes the nuisance terms `H`. Positivity of the denominator is supplied at theorem-use time | **Defined** |
| `residualizedCoefficient_eq_of_normalEqs` | `(Y_memLp) (D_memLp) (wY) (wD) (hDtilde_pos : 0 < ∫ D̃²) (β) (Hβ) (Hβ_mem) (h_normal_D) (h_normal_H) → β = residualizedCoefficient μ H wY wD` | **Population Frisch–Waugh–Lovell theorem** (`prop:po-estimand-fwl`): if `β` and `Hβ ∈ H` jointly satisfy the population normal equations for projecting `Y` on `D` and `H` (`∫ (Y − βD − Hβ)·D = 0` and `∀ h ∈ H, ∫ (Y − βD − Hβ)·h = 0`), and `E[D̃²] > 0`, then `β` equals the residualized coefficient. Proof: build `h* = wY.VH − β·wD.VH − Hβ ∈ H`, derive `∫ e·D̃ = 0` from the two normal equations and `wD.decomp`, expand `e = h* + (Ỹ − β·D̃)` a.e., use `wD.orthogonal h*` to kill `h*`, divide by `hDtilde_pos.ne'` | **Proved** |

---

## 9i0a-CellBridge. `Panel/CellBridge.lean` — Generic Finite Indicator-Span Bridges

Reusable probability-space infrastructure for finite cell partitions in the estimand-characterization layer. The module hoists the one-axis saturated indicator class used by Słoczyński and the two-axis cohort+period class used by Goodman-Bacon, plus shared cell-mass, cell-mean, finite-partition integral, and indicator-span orthogonality lemmas.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `CellBridge.cellIndicator` | `(X : Ω → ι) → ι → Ω → ℝ` | Real-valued singleton-cell indicator `𝟙{X = i}` | **Defined** |
| `CellBridge.indicatorSpan` | `Measure Ω → (X : Ω → ι) → Measurable X → LinearL2Class μ` | One-axis finite linear `L²` span of singleton indicators `𝟙{X = i}` | **Defined** |
| `CellBridge.twoAxisIndicatorSpan` | `Measure Ω → (X : Ω → ι) → (Z : Ω → κ) → Measurable X → Measurable Z → LinearL2Class μ` | Two-axis finite linear `L²` span of `X`-cell and `Z`-cell indicators, used for cohort+period nuisance classes | **Defined** |
| `CellBridge.cellMass` / `jointCellMass` / `cellMean` | `(μ, X, i)` / `(μ, X, Z, i, k)` / `(μ, F, X, i) → ℝ` | Generic finite cell mass, joint cell mass, and totalized cell mean helpers | **Defined** |
| `CellBridge.indicator_cell_memLp` | `IsProbabilityMeasure μ → Measurable X → MemLp (cell indicator) 2 μ` | Cell indicators are square-integrable under a probability measure | **Proved** |
| `CellBridge.integral_cell_indicator_one_eq_cellMass` | `∫ 𝟙{X=i} dμ = cellMass μ X i` | Integral of a singleton-cell indicator equals its real cell mass | **Proved** |
| `CellBridge.cell_integral_div_mul_cellMass` / `cellMean_mul_cellMass` | `((∫ F·𝟙{X=i}) / cellMass) * cellMass = ∫ F·𝟙{X=i}` | Dividing by cell mass and multiplying back recovers the raw indicator-weighted integral, including zero-mass cells by totalized division | **Proved** |
| `CellBridge.integral_mul_indicatorSpan_eq_zero_of_cell` | `∀ i, ∫ V·𝟙{X=i}=0 → ∫ V·(Σ c_i𝟙{X=i})=0` | Cell-wise orthogonality implies orthogonality to the whole one-axis indicator span | **Proved** |
| `CellBridge.integral_mul_twoAxisIndicatorSpan_eq_zero_of_axes` | `∀ i, ∫ V·𝟙{X=i}=0 → ∀ k, ∫ V·𝟙{Z=k}=0 → ∫ V·h=0` | Per-axis orthogonality implies orthogonality to a two-axis indicator-span representative | **Proved** |
| `CellBridge.integral_eq_sum_cell` | `Integrable F μ → ∫ F dμ = Σ_i ∫ F·𝟙{X=i} dμ` | Integrates an integrable function by summing over finite cells | **Proved** |

---

## 9i0a-AdoptionPath. `Panel/AdoptionPath.lean` — Shared Staggered-Adoption Path Helpers

Paper-agnostic helpers for adoption paths encoded as `WithTop (Fin T)`, with `⊤` representing never-treated. Sun-Abraham's path helpers are compatibility wrappers around these declarations; Goodman-Bacon's `AdoptionDate` namespace uses the same raw `WithTop` encoding for proof stability.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `AdoptionPath.finite` | `Fin T → WithTop (Fin T)` | Embeds a finite adoption period into the shared adoption-path type | **Defined** |
| `AdoptionPath.le` / `lt` | `WithTop (Fin T) → Fin T → Prop` | Treated-by-period and untreated-before-adoption predicates for finite periods | **Defined** |
| `AdoptionPath.isFinite` / `isInfinite` | `WithTop (Fin T) → Prop` | Eventually-treated and never-treated path predicates | **Defined** |
| `AdoptionPath.isNeverTreated` / `isEventuallyTreated` | `WithTop (Fin T) → Prop` | Sun-Abraham-compatible names for infinite and finite adoption paths | **Defined** |
| `AdoptionPath.absorbingTreatment` | `WithTop (Fin T) → Fin T → ℝ` | Absorbing treatment path `1{A ≤ t}`, totalized so `⊤` is untreated in every finite period | **Defined** |
| `AdoptionPath.not_le_of_lt` / `lt_of_isInfinite` | order helpers | `t < A` rules out `A ≤ t`, and never-treated paths are untreated in every finite period | **Proved** |

---

## 9i0a-VariableIntensityIV. `PO/ID/Exact/VariableIntensityIV/{OrderedTreatment,VariableIntensity}.lean` — Variable-Intensity IV ACR (Angrist-Imbens)

Specific-paper scaffold for Angrist-Imbens (1995) variable treatment intensity and average causal response, formulated **inside the potential-outcome framework** (`POSystem`): the instrument, treatment intensity, and outcome are system variables; potential intensities `D(z)` and treatment-indexed potential outcomes `Y(d)` are genuine `POVar.cfUnder` counterfactuals; consistency (`D=D(Z)`, `Y=Y(D)`) is the canonical `POSystem.Consistency` and instrument independence is `POSystem.IndepCF` over a counterfactual bundle (not ad-hoc structure fields). `OrderedTreatment.lean` holds the finite ordered-intensity algebra (adjacent margins, crossing indicators, telescoping identities) directly — its sole consumer is `VariableIntensity.lean` in this folder, so it lives here rather than under `Panel` — and re-exports the shared generic `NormalizedWeights` normalized-weight helpers. `VariableIntensity.lean` packages a directed instrument-cell contrast, crossing events, indicator-weighted ACR, conditional-margin ACR, Wald/2SLS interface definitions, and the headline ACR characterization statements. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/angrist_imbens_variable_intensity.md`.

### `VariableIntensityIV/OrderedTreatment.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `OrderedTreatment.lowerLevel` / `upperLevel` | `Fin J → Fin (J+1)` | Lower and upper treatment levels for the margin `j → j+1` | **Defined** |
| `OrderedTreatment.intensityValue` | `Fin (J+1) → ℝ` | Real-valued numeric representation of an ordered treatment level | **Defined** |
| `OrderedTreatment.marginIncrement` | `(Fin (J+1) → ℝ) → Fin J → ℝ` | Finite-difference increment `f(j+1)-f(j)` on a treatment margin | **Defined** |
| `OrderedTreatment.Crossing` | `Fin (J+1) → Fin (J+1) → Fin J → Prop` | Predicate that a movement from level `a` to level `b` crosses margin `j → j+1` | **Defined** |
| `OrderedTreatment.crossingIndicator` | `Fin (J+1) → Fin (J+1) → Fin J → ℝ` | Real-valued indicator of the crossed-margin predicate | **Defined** |
| `OrderedTreatment.normalizedWeight` | `(ι → ℝ) → ι → ℝ` | Generic finite normalized weight `a_i / Σ_k a_k` | **Defined** |
| `OrderedTreatment.ordered_telescope_indicator` | `a ≤ b → f b - f a = Σ_j marginIncrement f j * crossingIndicator a b j` | Ordered finite telescoping identity over crossed margins, proved by reindexing to the half-open Nat interval `[a.val,b.val)` and applying `Finset.sum_Ico_sub` | **Proved** |
| `OrderedTreatment.ordered_telescope_identity` | `a ≤ b → intensityValue b - intensityValue a = Σ_j crossingIndicator a b j` | Identity-function version of the ordered telescoping lemma, equating intensity movement with crossed-margin count | **Proved** |
| `OrderedTreatment.normalizedWeight_nonneg` | `(∀ i, 0 ≤ a i) → 0 < Σ_i a_i → 0 ≤ normalizedWeight a i` | Nonnegativity of normalized finite weights from nonnegative raw weights and positive total mass | **Proved** |
| `OrderedTreatment.sum_normalizedWeight_eq_one` | `0 < Σ_i a_i → Σ_i normalizedWeight a i = 1` | Normalized finite weights sum to one when the normalizing total is positive | **Proved** |

### `VariableIntensityIV/VariableIntensity.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `VariableIntensityIVSystem P 𝒵 J` | `(P : POSystem) → [Fintype 𝒵] [MeasurableSingletonClass 𝒵] → structure { hJ_pos, Z D Y : P.V, hZ𝒵/hDintensity/hYreal measurable-equivs, distinctness }` | Variable-intensity IV system over a PO system: instrument/treatment/outcome are system variables with value spaces identified with `𝒵`/`Fin (J+1)`/`ℝ`; potential intensities `DofZ` and outcomes `YofD` are `cfUnder` counterfactuals; exclusion is structural (`YofD` fixes only the treatment) | **Defined** |
| `VariableIntensityIVSystem.DofZ` / `YofD` | `𝒵 → P.Ω → Fin (J+1)` / `Fin (J+1) → P.Ω → ℝ` | Potential treatment intensity `D(z)` and treatment-indexed potential outcome `Y(d)`, as single-intervention counterfactuals `dVar.cfUnder zVar z` / `yVar.cfUnder dVar d` | **Defined** |
| `VariableIntensityIVSystem.zEvent` | `𝒵 → Set P.Ω` | Instrument cell event `{ω | Z(ω)=z}`, defined via `POVar.event` | **Defined** |
| `VariableIntensityIVSystem.cfContrastBundle` | `𝒵 → 𝒵 → POCFBundle P` | Contrast-relevant counterfactual bundle `(D(z0), D(z1), Y(0),...,Y(J))` (`D`s at indices `0,1`, `Y(d)` at index `d+2`) carrying the `IndepCF` independence hypothesis | **Defined** |
| `VariableIntensityIVSystem.marginResponse` | `Fin J → P.Ω → ℝ` | Unit causal response `Y(j+1)-Y(j)` on a treatment margin | **Defined** |
| `VariableIntensityIVSystem.crossingEvent` | `𝒵 → 𝒵 → Fin J → Set P.Ω` | Margin-crossing event `C_j(z0,z1)` induced by the directed instrument contrast | **Defined** |
| `VariableIntensityIVSystem.crossingProb` / `totalCrossingProb` | `... → ℝ` | Crossing-event mass and total crossing mass over all margins | **Defined** |
| `VariableIntensityIVSystem.crossingWeight` | `𝒵 → 𝒵 → Fin J → ℝ` | Angrist-Imbens normalized margin-crossing weight | **Defined** |
| `VariableIntensityIVSystem.indicatorWeightedEffect` | `𝒵 → 𝒵 → Fin J → ℝ` | Set integral of the margin response over a crossing event, the unnormalized crossing-effect contribution | **Defined** |
| `VariableIntensityIVSystem.indicatorWeightedACR` / `averageCausalResponse` | `𝒵 → 𝒵 → ℝ` | Indicator-weighted ACR ratio and conditional-margin weighted ACR for a directed contrast | **Defined** |
| `VariableIntensityIVSystem.firstStageContrast` / `reducedFormContrast` | `𝒵 → 𝒵 → ℝ` | Potential first-stage and reduced-form contrasts after consistency, exclusion, and independence reduce the Wald components | **Defined** |
| `VariableIntensityIVSystem.condExpDZ` / `condExpYZ` / `wald` | `𝒵 → ℝ` / `𝒵 → 𝒵 → ℝ` | Event-conditional factual treatment/outcome means and the directed Wald estimand | **Defined** |
| `VariableIntensityIVSystem.ValidContrastAssumptions` | `𝒵 → 𝒵 → Prop` | Assumption bundle for IV validity: `POSystem.Consistency`, `POSystem.IndepCF` instrument independence, directed monotonicity, positive first stage, and outcome integrability (exclusion is structural, not a field) | **Defined** |
| `VariableIntensityIVSystem.integrable_marginResponse` | `ValidContrastAssumptions z0 z1 → Integrable (marginResponse j) μ` | Margin-response integrability derived from treatment-indexed outcome integrability | **Proved** |
| `VariableIntensityIVSystem.firstStage_eq_sum_crossingProb` | `firstStageContrast z0 z1 = Σ_j crossingProb z0 z1 j` | First-stage denominator equals total crossed-margin probability, using monotonicity, ordered-treatment telescoping, and indicator integrals over crossing events | **Proved** |
| `VariableIntensityIVSystem.crossingWeight_nonneg` | `ValidContrastAssumptions z0 z1 → 0 ≤ crossingWeight z0 z1 j` | Nonnegativity of ACR crossing weights using normalized-weight algebra and positive first stage | **Proved** |
| `VariableIntensityIVSystem.sum_crossingWeight_eq_one` | `ValidContrastAssumptions z0 z1 → Σ_j crossingWeight z0 z1 j = 1` | ACR crossing weights sum to one under positive first stage | **Proved** |
| `VariableIntensityIVSystem.reducedForm_eq_sum_crossingEffects` | `reducedFormContrast z0 z1 = Σ_j indicatorWeightedEffect z0 z1 j` | Reduced-form contrast decomposes into indicator-weighted crossed-margin causal responses via pointwise ordered telescoping and finite integral exchange | **Proved** |
| `VariableIntensityIVSystem.indicatorWeightedACR_eq_averageCausalResponse` | `indicatorWeightedACR z0 z1 = averageCausalResponse z0 z1` | Indicator-weighted ACR ratio agrees with the conditional-margin weighted ACR expression; pure algebraic identity requiring no contrast-validity assumption | **Proved** |
| `VariableIntensityIVSystem.wald_eq_averageCausalResponse` | `wald z0 z1 = averageCausalResponse z0 z1` | Headline Angrist-Imbens ACR characterization for a valid directed contrast with positive instrument cells; reduces factual Wald cells to potential first-stage/reduced-form contrasts via `POSystem.eventCondExp_of_consistency_IndepCF` (consistency + `IndepCF` drop-of-conditioning over the counterfactual bundle), then applies the ordered-treatment algebra | **Proved** |
| `VariableIntensityIVSystem.SpecialCases.binaryMargin` | `J = 1 → Fin J` | Canonical single treatment margin obtained by transporting `0 : Fin 1` under the binary-intensity equality | **Defined** |
| `VariableIntensityIVSystem.SpecialCases.wald_eq_late_of_binaryIntensity` | `J = 1 → wald z0 z1 = eventCondExp μ (crossingEvent z0 z1 (binaryMargin h)) (marginResponse (binaryMargin h))` | Binary-treatment single-margin specialization, corresponding to the LATE-style complier event on the unique crossing margin | **Proved** |
| `VariableIntensityIVSystem.SpecialCases.crossingEvent_eq_complianceEvent` | `J = 1 → crossingEvent z0 z1 (binaryMargin h) = {ω \| DofZ z1 ω = Fin.last J ∧ DofZ z0 ω = 0}` | Under `J=1` the unique crossing event equals the LATE complier event `{D(z1)=top ∧ D(z0)=0}` (Fin.last 1 = 1), making `wald_eq_late_of_binaryIntensity` explicitly the Imbens-Angrist (1994) binary LATE | **Proved** |
| `VariableIntensityIVSystem.SpecialCases.wald_eq_constantResponse` | `(∀ j, marginResponse j =ᵐ fun _ => τ) → wald z0 z1 = τ` | Constant marginal response specialization, reducing the indicator-weighted ACR numerator to `τ` times total crossing mass | **Proved** |
| `VariableIntensityIVSystem.SpecialCases.wald_eq_marginResponseAverage` | `(∀ j, m j = conditionalMarginResponse z0 z1 j) → wald z0 z1 = Σ_j crossingWeight z0 z1 j * m j` | Margin-specific response average restatement of the headline ACR characterization | **Proved** |
| `VariableIntensityIVSystem.PopulationTwoSLSScore` | `structure { score, gammaD, centered, denom_nonzero }` | Interface-only finite-score population 2SLS setup for multivalued instruments | **Defined** |
| `PopulationTwoSLSScore.fittedTreatment` / `beta2SLS` | `P.Ω → ℝ` / `ℝ` | First-stage fitted treatment from the score (over factual `Z`/`D`/`Y`) and the population 2SLS ratio | **Defined** |
| `VariableIntensityIVSystem.TwoSLSContrastDecomposition` | `structure { contrastWeight, pairFirstStage, pairReducedForm, ... }` | Deferred interface for expanding multivalued-instrument 2SLS into signed ordered cell contrasts | **Defined** |

---

## 9i0a-MultipleInstrumentIV. `PO/ID/Exact/MultipleInstrumentIV/{FiniteIndex,ResponseTypes,Population,Main,POBridge}.lean` — Multiple-Instrument IV (Mogstad-Torgovitsky-Walters)

Specific-paper finite-support algebra for the MTW multiple-IV characterization.  This layer works with an ordered saturated first-stage index, a finite matrix first-stage projection, support masses, tail coefficients, binary response types, response-type effects, a saturated finite-support population bridge, and an observed measure-backed population 2SLS bridge.  It proves the ordered-tail coefficient nonnegativity, the finite tail-sum interchange identity, the response-type ratio representation, the population-bridge equality from reduced-form/first-stage moments to the finite response-type ratio, the observed `E[h(Z)Y] / E[h(Z)D]` bridge to that ratio, and the positive-weight normalization result under MTW sign alignment.

`POBridge.lean` grounds this algebra in an explicit potential-outcome system (`POMultipleIVSystem`): the response type `G = (D(z⁰),…,D(z^{K-1}))` is a genuine counterfactual, and under `Assumptions` (consistency + per-cell instrument independence `Z ⟂ (D(zᵏ),Y(1),Y(0))`) the two `ObservedBridge` conditional-mean fields are *derived*, not assumed — discharging Gap G5 of the faithfulness audit.  Its flagship `POMultipleIVSystem.observedBeta2SLS_eq_responseTypeWeightedSum` restates the observed 2SLS ratio as the response-type weighted sum `Σ_g ω_g Δ_g` with `Δ_g = E[Y(1)-Y(0) | G=g]`, i.e. entirely in potential-outcome language, via the same drop-of-conditioning + total-law pattern as `PO/ID/Exact/LATE.lean`.  Companion NL doc: `doc/basic_concepts/po/estimand_characterization/mogstad_torgovitsky_walters_multiple_iv.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `MultipleInstrumentIV.Adj K` | `{j : Fin K // 0 < j.val}` | Adjacent ordered-support threshold, representing the source-paper movements `2, ..., K` in zero-based Lean indexing | **Defined** |
| `MultipleInstrumentIV.Adj.lower` / `MultipleInstrumentIV.Adj.upper` | `Adj K → Fin K` | Lower and upper endpoints of an adjacent support movement | **Defined** |
| `MultipleInstrumentIV.FiniteIndex K` | `structure { rho, dhat, rho_nonneg, rho_sum_one, dhat_mono }` | Ordered finite first-stage index: support masses `ρ_k`, ordered index values `dhat_k`, probability-vector constraints, and weak monotonicity in the displayed support order | **Defined** |
| `FiniteIndex.meanIndex` / `centeredIndex` | `FiniteIndex K → ℝ` / `FiniteIndex K → Fin K → ℝ` | First-stage mean `Σ ρ_k dhat_k` and centered index `dhat_k − meanIndex` | **Defined** |
| `FiniteIndex.upperTail` / `tailCoeff` | `FiniteIndex K → Adj K → Finset (Fin K)` / `FiniteIndex K → Adj K → ℝ` | Ordered upper tail `T_j` and MTW tail coefficient `B_j = Σ_{k∈T_j} ρ_k (dhat_k − meanIndex)` | **Defined** |
| `FiniteIndex.centered_weight_sum_zero` | `∑ k, I.rho k * I.centeredIndex k = 0` | Centering identity used to subtract the baseline term in the signed decomposition algebra | **Proved** |
| `FiniteIndex.tailCoeff_nonneg` | `0 ≤ I.tailCoeff j` | Ordered-tail coefficient nonnegativity from nonnegative support masses and weakly increasing first-stage index values | **Proved** |
| `FiniteIndex.tail_sum_interchange` | `Σ_k ρ_k a_k (Σ_j 1{j≤k} x_j) = Σ_j B_j x_j` | Finite upper-tail sum interchange identity used by the MTW signed decomposition | **Proved** |
| `MultipleInstrumentIV.firstStageGram` | `(rho : Fin K → ℝ) → (score : Fin K → Fin L → ℝ) → Matrix (Fin L) (Fin L) ℝ` | Finite second-moment matrix `E[q(Z)q(Z)ᵀ]` for the displayed finite support | **Defined** |
| `MultipleInstrumentIV.MatrixFirstStage K L` | `structure { rho, score, firstStageMoment, rho_nonneg, rho_sum_one, gram_invertible, fitted_mono }` | Source-style finite first-stage projection data: support masses, score vector, moment vector `E[q(Z)D]`, invertible Gram matrix, and ordering of the fitted first stage | **Defined** |
| `MatrixFirstStage.gram` / `projectionCoeff` / `fittedValue` | `Matrix (Fin L) (Fin L) ℝ` / `Fin L → ℝ` / `Fin K → ℝ` | Matrix first-stage construction: Gram matrix, coefficient `(E[q qᵀ])⁻¹ E[qD]`, and fitted index `q(zᵏ)'Π` | **Defined** |
| `MatrixFirstStage.toFiniteIndex` | `MatrixFirstStage K L → FiniteIndex K` | Converts the matrix-inverse first-stage construction into the ordered `FiniteIndex` consumed by the MTW algebra | **Defined** |
| `MultipleInstrumentIV.ResponseType K` | `Fin K → Bool` | Binary response type `g = (D(z¹), ..., D(zᴷ))` over the ordered instrument support | **Defined** |
| `MultipleInstrumentIV.boolToReal` / `MultipleInstrumentIV.typeStep` | `Bool → ℝ` / `ResponseType K → Adj K → ℝ` | Boolean treatment indicator as `0/1` and adjacent response increment `d_j − d_{j−1}` | **Defined** |
| `MultipleInstrumentIV.ResponseTypeStats K` | `structure { mass, effect, mass_nonneg, mass_sum_one }` | Response-type masses `π_g`, type-specific causal effects `Δ_g`, and probability-vector constraints | **Defined** |
| `ResponseTypeStats.unnormTypeWeight` | `FiniteIndex K → ResponseTypeStats K → ResponseType K → ℝ` | MTW unnormalized response-type weight `λ_g = π_g Σ_j B_j (d_j − d_{j−1})` | **Defined** |
| `ResponseTypeStats.typeWeightDenom` / `normalizedTypeWeight` | `ℝ` / `ResponseType K → ℝ` | First-stage denominator `Σ_g λ_g` and normalized response-type weight `ω_g = λ_g / Σ_g λ_g`, implemented via `NormalizedWeights.normalizedWeight` | **Defined** |
| `ResponseTypeStats.responseTypeEstimand` / `beta2SLSFiniteAlgebra` | `ℝ` / `ℝ` | Finite response-type weighted estimand `Σ_g ω_g Δ_g` and finite-algebra ratio form `(Σ_g λ_g Δ_g)/(Σ_g λ_g)` | **Defined** |
| `ResponseTypeStats.PopulationBridge K` | `structure { stats, baseOutcome }` | Saturated finite-support population bridge carrying response-type statistics and baseline outcome means for the centered-index identification step | **Defined** |
| `ResponseTypeStats.PopulationBridge.telescopedTypeStep` | `ResponseType K → Fin K → ℝ` | Telescoped adjacent treatment response `Σ_{j≤k} (d_j − d_{j−1})` used after consistency, exogeneity, exclusion, and baseline subtraction | **Defined** |
| `ResponseTypeStats.PopulationBridge.outcomeAtSupport` / `treatmentAtSupport` | `Fin K → ℝ` / `Fin K → ℝ` | Response-type expansions of the support-point outcome and treatment moments in the saturated finite-support bridge | **Defined** |
| `ResponseTypeStats.PopulationBridge.reducedFormMoment` / `firstStageMoment` / `beta2SLSPopulationBridge` | `ℝ` / `ℝ` / `ℝ` | Finite response-type reduced-form moment, first-stage moment, and their bridge ratio after the support-cell identification reductions | **Defined** |
| `ResponseTypeStats.PopulationBridge.baselineMoment_eq_zero` | baseline component `= 0` | Centered-index cancellation of response-type-specific baseline outcome means | **Proved** |
| `ResponseTypeStats.PopulationBridge.firstStageMoment_eq_typeWeightDenom` / `reducedFormMoment_eq_typeWeightNumerator` | moment equalities | Denominator and numerator halves of the saturated finite-support MTW identification step, proved by finite sum interchange and telescoping | **Proved** |
| `ResponseTypeStats.PopulationBridge.beta2SLSPopulationBridge_eq_beta2SLSFiniteAlgebra` | `beta2SLSPopulationBridge I P = beta2SLSFiniteAlgebra I P.stats` | Saturated finite-support population 2SLS ratio equals the finite response-type ratio | **Proved** |
| `ResponseTypeStats.SignAligned` | `Prop` | MTW sign-alignment condition: every positive-mass response type has nonnegative tail-weighted step contrast | **Defined** |
| `ResponseTypeStats.PopulationBridge.zEvent` | `(Z : Ω → Fin K) → Fin K → Set Ω` | Observed instrument support cell `{ω | Z ω = k}` used by the measure-backed population bridge | **Defined** |
| `ResponseTypeStats.PopulationBridge.observedReducedFormMoment` / `observedFirstStageMoment` / `observedBeta2SLS` | `Measure Ω → (Ω → Fin K) → ... → ℝ` | Observed population moments `E[h(Z)Y]`, `E[h(Z)D]`, and their source population 2SLS ratio, with `h(zᵏ)=dhat_k−Σρ_l dhat_l` | **Defined** |
| `ResponseTypeStats.PopulationBridge.observedReducedFormMoment_eq_sum_eventCondExp` / `observedFirstStageMoment_eq_sum_eventCondExp` | finite total-law identities | Rewrite observed moments into finite sums of support-cell probabilities times support-cell event conditional expectations | **Proved** |
| `ResponseTypeStats.PopulationBridge.ObservedBridge` | `structure { isProbability, rho_eq_zMass, outcome_cell, baseTreatment, treatment_cell }` | Observed-to-finite bridge: the observed law is a probability measure, support masses are observed instrument-cell probabilities, support-cell outcome expectations equal the response-type expansion, and support-cell treatment expectations equal a common baseline plus the telescoped adjacent response-type expansion. NOTE: the `outcome_cell`/`treatment_cell` support-cell conditional-expectation fields are TRANSFERRED assumptions, not DERIVED from a PO system — the A2 algebraic bridge does not derive consistency/exogeneity/exclusion | **Defined** |
| `ObservedBridge.observedReducedFormMoment_eq_reducedFormMoment` / `observedFirstStageMoment_eq_firstStageMoment` | observed moments equal finite bridge moments | Connects `E[h(Z)Y]` and `E[h(Z)D]` to the finite response-type reduced-form and first-stage moments | **Proved** |
| `ObservedBridge.observedBeta2SLS_eq_beta2SLSPopulationBridge` / `observedBeta2SLS_eq_beta2SLSFiniteAlgebra` | observed population 2SLS ratio equals bridge / finite algebra ratio | End-to-end measure-backed population 2SLS bridge from `E[h(Z)Y]/E[h(Z)D]` to the MTW response-type finite algebra | **Proved** |
| `FiniteIndex.tailCoeff_eq_cov` | `B_j = Cov_ρ(dhat(Z), 1_{Z∈T_j})` | Tail coefficient equals the ρ-weighted covariance of the fitted index with the upper-tail indicator | **Proved** |
| `ResponseTypeStats.NegWeightExample.exists_negativeNormalizedTypeWeight` | `∃ I R g, 0 < R.mass g ∧ R.normalizedTypeWeight I g < 0` | Explicit K=2 counterexample (ρ=[3/4,1/4], dhat=[0,1]; 1/4 compliers, 3/4 defiers) showing 2SLS response-type weights can be NEGATIVE without sign alignment — the paper's central message | **Proved** |
| `ResponseTypeStats.PopulationBridge.ObservedBridge.observedBeta2SLS_eq_responseTypeWeightedSum` | `observedFirstStageMoment μ Z D I ≠ 0 → observedBeta2SLS = Σ_g ω_g Δ_g` | End-to-end observed-level corollary taking the observable denominator condition directly | **Proved** |
| `ResponseTypeStats.ComponentwiseMonotoneRestriction` | `structure { allowed, step_nonneg_of_allowed }` | UNUSED PLACEHOLDER: does NOT encode MTW partial/componentwise monotonicity and is connected to no theorem | **Defined** |
| `ResponseTypeStats.unnormTypeWeight_nonneg_of_signAligned` | `SignAligned → 0 ≤ unnormTypeWeight I R g` | Sign alignment and nonnegative type masses imply nonnegative unnormalized response-type weights | **Proved** |
| `ResponseTypeStats.normalizedTypeWeight_nonneg_of_signAligned` | `SignAligned → 0 < typeWeightDenom I R → 0 ≤ normalizedTypeWeight I R g` | Positive denominator preserves nonnegativity after normalization | **Proved** |
| `ResponseTypeStats.normalizedTypeWeight_sum_eq_one_of_pos` | `0 < typeWeightDenom I R → ∑ g, normalizedTypeWeight I R g = 1` | Normalized response-type weights sum to one under positive first-stage denominator | **Proved** |
| `ResponseTypeStats.beta2SLSFiniteAlgebra_eq_responseTypeWeightedSum` | `typeWeightDenom I R ≠ 0 → beta2SLSFiniteAlgebra I R = responseTypeEstimand I R` | Response-type form of the finite MTW ratio (`prop:po-estimand-mtw-response-type-form`, finite algebra layer) | **Proved** |
| `ResponseTypeStats.beta2SLSFiniteAlgebra_eq_positiveResponseTypeAverage` | `SignAligned → 0 < typeWeightDenom I R → beta2SLSFiniteAlgebra = responseTypeEstimand ∧ weights nonnegative ∧ weights sum to one` | Positive-weight characterization under MTW sign alignment (`prop:po-estimand-mtw-positive-weights`, finite algebra layer) | **Proved** |
| `ResponseTypeStats.signedAdjacentNumerator` / `signedAdjacentDenominator` | `ℝ` / `ℝ` | Public facade names for the finite response-type numerator `Σ_g λ_g Δ_g` and denominator `Σ_g λ_g` | **Defined** |
| `ResponseTypeStats.beta2SLSFiniteAlgebra_eq_signedAdjacentRatio` | `signedAdjacentDenominator I R ≠ 0 → beta2SLSFiniteAlgebra I R = signedAdjacentNumerator I R / signedAdjacentDenominator I R` | Facade theorem for the finite response-type ratio | **Proved** |
| `ResponseTypeStats.beta2SLSFiniteAlgebra_eq_responseTypeWeightedSum'` / `beta2SLSFiniteAlgebra_eq_positiveResponseTypeAverage'` | wrappers around the corresponding `ResponseTypes.lean` theorems | Public facade wrappers for the response-type weighted-sum and positive-average finite algebra results | **Proved** |
| `ResponseTypeStats.PopulationBridge.beta2SLSPopulationBridge_eq_signedAdjacentRatio` / `beta2SLSPopulationBridge_eq_responseTypeWeightedSum` / `beta2SLSPopulationBridge_eq_positiveResponseTypeAverage` | wrappers around bridge + finite algebra theorems | Public facade wrappers upgrading the saturated finite-support population bridge to the signed-ratio, response-type weighted-sum, and positive-average conclusions | **Proved** |

---

## 9i0a-UniformTwoWayPanel. `Panel/UniformTwoWayPanel.lean` — Uniform Balanced Two-Way Panel Algebra

Paper-agnostic finite balanced-panel helpers for TWFE and two-way Mundlak arguments under the uniform unit-period measure. Lives directly under `Panel/` (general panel substrate, not estimand-characterization-specific) and exposes bridge lemmas showing it is the equal-unit-weight specialization of `WeightedTwoWayPanel`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `UniformTwoWayPanel.BalancedPanel Unit Time` | `structure { unit_card_ge_two, time_card_ge_two }` | Balanced finite panel side conditions from `def:po-estimand-wooldridge-two-way-population`: at least two units and at least two periods | **Defined** |
| `UniformTwoWayPanel.uniformWeights` | `0 < Fintype.card Unit → WeightedTwoWayPanel.UnitWeights Unit` | Equal unit-weight probability vector `p_i = 1 / |Unit|`, used to instantiate the weighted two-way panel substrate | **Defined** |
| `UniformTwoWayPanel.unitMean` / `timeMean` / `grandMean` | `(Unit → Time → ℝ) → ...` | Uniform finite-panel unit means, time means, and grand mean for a scalar array | **Defined** |
| `UniformTwoWayPanel.ddot` | `(Unit → Time → ℝ) → Unit → Time → ℝ` | Uniform double-demeaned residual `V_it - unitMean V i - timeMean V t + grandMean V` | **Defined** |
| `UniformTwoWayPanel.inner` | `(Unit → Time → ℝ) → (Unit → Time → ℝ) → ℝ` | Unnormalized uniform finite unit-period inner product used for orthogonality statements | **Defined** |
| `UniformTwoWayPanel.ddot_eq_weighted` / `inner_eq_card_smul_weighted` | bridge identities for `uniformWeights` | Uniform double demeaning equals weighted double demeaning with equal unit weights, and the unnormalized uniform inner product is `|Unit|` times the weighted inner product | **Proved** |
| `UniformTwoWayPanel.finiteResidualizedCoefficient` | `(Dtilde Ytilde : Unit → Time → ℝ) → ℝ` | Finite scalar residualized coefficient `inner Dtilde Ytilde / inner Dtilde Dtilde` for supplied residualized regressor and outcome arrays | **Defined** |
| `UniformTwoWayPanel.finite_residualized_coefficient_eq_of_normalEqs` | `normal equations + residual bridges → β = finiteResidualizedCoefficient Dtilde Ytilde` | Paper-agnostic finite scalar FWL handoff: from raw-regressor and nuisance normal equations, an explicitly residualized regressor orthogonal to the nuisance class, an outcome residual bridge, and positive residualized sum of squares, derives the finite residualized-ratio coefficient. Used by Wooldridge `twfe_twm_equivalence` instead of re-deriving scalar FWL algebra locally | **Proved** |
| `UniformTwoWayPanel.IsUnitTimeAdditive` | `(Unit → Time → ℝ) → Prop` | Alias for `Causalean.Panel.Weighted.IsUnitTimeAdditive`, the unit/time fixed-effect span `h_it = a_i + b_t` | **Defined** |
| `UniformTwoWayPanel.unitTimeProjection` | `(Unit → Time → ℝ) → Unit → Time → ℝ` | Removed uniform unit/time component `unitMean + timeMean - grandMean` | **Defined** |
| `UniformTwoWayPanel.ddot_reconstruct` | `ddot V i t + unitMean V i + timeMean V t - grandMean V = V i t` | Pointwise reconstruction identity for uniform double demeaning | **Proved** |
| `UniformTwoWayPanel.unitTimeProjection_additive` | `IsUnitTimeAdditive (unitTimeProjection V)` | The removed component belongs to the unit/time additive nuisance class | **Proved** |
| `UniformTwoWayPanel.sub_ddot_eq_unitTimeProjection` | `V i t - ddot V i t = unitTimeProjection V i t` | Pointwise form showing the part removed by uniform residualization is the unit/time projection | **Proved** |
| `UniformTwoWayPanel.ddot_orthogonal_unit` | `BalancedPanel Unit Time → ∑ i, ∑ t, ddot V i t * a i = 0` | Uniform double-demeaned arrays are orthogonal to arbitrary unit-only functions | **Proved** |
| `UniformTwoWayPanel.ddot_orthogonal_time` | `BalancedPanel Unit Time → ∑ i, ∑ t, ddot V i t * b t = 0` | Uniform double-demeaned arrays are orthogonal to arbitrary time-only functions | **Proved** |
| `UniformTwoWayPanel.ddot_orthogonal_unit_time` | `BalancedPanel Unit Time → IsUnitTimeAdditive h → inner (ddot V) h = 0` | Uniform double-demeaned arrays are orthogonal to the whole unit/time additive nuisance span | **Proved** |

---

## 9i0a-FlexibleDIDMundlak. `Panel/EstimandCharacterization/FlexibleDIDMundlak/{TWFE,VectorTWFE,VectorMundlak,DID,PopulationBridge,PopulationOrigin}.lean` — Wooldridge TWFE/Mundlak and Flexible DID

Specific-paper formalization scaffold for `doc/basic_concepts/po/estimand_characterization/wooldridge_twfe_mundlak_did.md`. `TWFE.lean` proves the scalar finite-panel slice of Wooldridge's TWFE and two-way Mundlak equivalence by instantiating the shared `UniformTwoWayPanel.finite_residualized_coefficient_eq_of_normalEqs` finite FWL handoff. `DID.lean` is a finite-cell composition theorem: it now DERIVES the untreated-prediction recovery from no-anticipation + conditional parallel trends + additivity (no longer an assumed field), eliminates the former opaque `Prop`+`_holds` placeholders, and derives the POLS/ETWFE cell links from finite-cell residual normal equations justified by a saturated block-diagonalization lemma. **G5 de-scaffold (2026-05-30):** `m0`'s exact untreated fit is no longer a posited field — `SaturatedUntreatedRegression` now carries the genuine weighted least-squares projection origin (`untreatedWeight`, `untreatedWeight_pos`, `untreatedNormalEq`: `m0` solves the untreated-only normal equations against the additive class `H0`), and the exact fit `m0 = YgMean` on untreated cells is the DERIVED theorem `untreatedFit` (the projection is exact because no-anticipation + conditional parallel trends place the untreated observed mean inside `H0`). **G9 de-scaffold (2026-05-30):** the finite imputation estimand is now bridged to the population conditional expectation — `PopulationBridge.lean` proves `thetaImp g t = eventCondExp μ cohortEvent Δ` (θ^imp = E[Δ∣G=g]) via the law of iterated expectations over the finite covariate partition, under explicit identification hypotheses (covariate weights are conditional cell probabilities; cell residuals are within-cell conditional means). **G1 de-scaffold (2026-05-30):** the scalar-only restriction is removed — `VectorTWFE.lean` introduces the genuine K-vector TWFE problem with a `Matrix K K ℝ`-valued residualized Gram matrix `Q_ddotX`, the full-rank condition `IsUnit (gram X).det`, and the matrix normal equation; existence and uniqueness of the closed-form coefficient `(gram X)⁻¹ (Σ ddot X ddot Y)` are proved, and `ScalarTWFEProblem` is shown to be the `K = Fin 1` specialization (`ScalarTWFEProblem.toVector` + `toVector_betaTWFE`). **`VectorMundlak.lean` (2026-05-30)** completes Theorem A in vector form: the matrix Frisch-Waugh-Lovell handoff `matrix_fwl_eq_of_normalEqs` (generalizing the scalar finite FWL), the K-vector two-way Mundlak nuisance span `IsVectorTwoWayMundlakNuisance`, the coding-free `VectorTWMFit`, and the equivalence `vec_twfe_twm_equivalence : fit.beta = P.betaTWFE` plus the optional-controls-invariance corollary. The remaining deferred Wooldridge gap is the population conditional-expectation origin of `m0` itself (G5 measure layer).

### `FlexibleDIDMundlak/TWFE.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.ScalarTWFEProblem Unit Time` | `structure { panel, Y, X, ddotX_ss_pos }` | Scalar balanced-panel TWFE problem: outcome, scalar regressor, panel side conditions, and positive residualized sum of squares standing in for full rank of `Q_ddotX` | **Defined** |
| `ScalarTWFEProblem.twfeDenominator` | `ScalarTWFEProblem Unit Time → ℝ` | Residualized-design denominator `∑ ddot X_it^2` | **Defined** |
| `ScalarTWFEProblem.twfeNumerator` | `ScalarTWFEProblem Unit Time → ℝ` | Residualized numerator `∑ ddot X_it * ddot Y_it` | **Defined** |
| `ScalarTWFEProblem.betaTWFE` | `ScalarTWFEProblem Unit Time → ℝ` | Scalar population TWFE coefficient, the finite FWL ratio | **Defined** |
| `ScalarTWFEProblem.twfeNormalEq` | `ScalarTWFEProblem Unit Time → ℝ → Prop` | Scalar double-demeaned normal equation for a proposed TWFE coefficient | **Defined** |
| `ScalarTWFEProblem.betaTWFE_normalEq` | `P.twfeNormalEq P.betaTWFE` | The closed-form scalar TWFE coefficient satisfies the double-demeaned normal equation | **Proved** |
| `ScalarTWFEProblem.betaTWFE_unique` | `P.twfeNormalEq β → β = P.betaTWFE` | Positive residualized sum of squares gives uniqueness of the scalar TWFE normal-equation solution | **Proved** |
| `FlexibleDIDMundlak.IsTwoWayMundlakNuisance` | `(X) → (Z → Unit → ℝ) → (M → Time → ℝ) → (Unit → Time → ℝ) → Prop` | Scalar two-way Mundlak nuisance span: constants, unit means of `X`, time means of `X`, optional unit-only controls, and optional time-only controls | **Defined** |
| `FlexibleDIDMundlak.mundlak_nuisance_unit_time` | `IsTwoWayMundlakNuisance X Zvar Mvar h → IsUnitTimeAdditive h` | Every scalar two-way Mundlak nuisance term is unit/time additive | **Proved** |
| `FlexibleDIDMundlak.ScalarTWMFit` | `structure { beta, nuisance, nuisance_mem, normal_X, normal_H }` | Coding-free scalar two-way Mundlak fit, represented by a coefficient, nuisance function, and normal equations against `X` and the nuisance span | **Defined** |
| `FlexibleDIDMundlak.twfe_twm_residual_common` | `IsUnitTimeAdditive (X - ddot X) ∧ ∀ h ∈ H_twm, inner (ddot X) h = 0` | Residualizing `X` against the two-way Mundlak nuisance span leaves the double-demeaned residual | **Proved** |
| `FlexibleDIDMundlak.twfe_twm_equivalence` | `ScalarTWMFit P Zvar Mvar → fit.beta = P.betaTWFE` | Scalar finite-panel TWFE--two-way Mundlak equivalence (`thm:po-estimand-wooldridge-twfe-twm-equivalence`, scalar slice) | **Proved** |
| `FlexibleDIDMundlak.twfe_twm_optional_controls_invariant` | `fit₁.beta = fit₂.beta` | Adding or removing optional unit-only/time-only controls does not change the scalar coefficient because both fits equal `betaTWFE` | **Proved** |

### `FlexibleDIDMundlak/VectorTWFE.lean` — K-vector TWFE matrix normal equation (audit G1)

Closes G1: the K-vector generalization of the TWFE problem. The vector double-demeaned array is component-wise scalar double demeaning (`ddotVec X i t k = ddot (X ·· k) i t`), so the uniform-panel orthogonality infrastructure is reused.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.ddotVec` | `(Unit → Time → K → ℝ) → Unit → Time → K → ℝ` | Component-wise double-demeaned vector regressor | **Defined** |
| `FlexibleDIDMundlak.gram` | `(Unit → Time → K → ℝ) → Matrix K K ℝ` | Residualized Gram matrix `Q_ddotX = Σ_it ddot(X_it) ddot(X_it)ᵀ` | **Defined** |
| `FlexibleDIDMundlak.numer` | `(Unit → Time → K → ℝ) → (Unit → Time → ℝ) → K → ℝ` | Residualized numerator vector `Σ_it ddot(X_it) ddot(Y_it)` | **Defined** |
| `FlexibleDIDMundlak.VectorTWFEProblem Unit Time K` | `structure { panel, Y, X, gram_unit }` | K-vector balanced-panel TWFE problem; the full-rank condition is `IsUnit (gram X).det`, matching nonsingular `Q_ddotX` in the source | **Defined** |
| `FlexibleDIDMundlak.VectorTWFEProblem.betaTWFE` | `VectorTWFEProblem Unit Time K → K → ℝ` | Closed-form vector TWFE coefficient `(gram X)⁻¹ (Σ ddot X ddot Y)` | **Defined** |
| `FlexibleDIDMundlak.VectorTWFEProblem.vecTwfeNormalEq` | `VectorTWFEProblem Unit Time K → (K → ℝ) → Prop` | Coordinate-wise vector double-demeaned normal equation | **Defined** |
| `FlexibleDIDMundlak.vecNormalEq_iff_mulVec` | `(coordinate-wise normal eq) ↔ (gram X).mulVec β = numer X Y` | The coordinate normal equation equals the matrix normal equation `Q_ddotX β = Σ ddot X ddot Y` | **Proved** |
| `FlexibleDIDMundlak.VectorTWFEProblem.betaTWFE_normalEq` | `P.vecTwfeNormalEq P.betaTWFE` | Existence: the closed-form coefficient solves the matrix normal equation under Gram nonsingularity | **Proved** |
| `FlexibleDIDMundlak.VectorTWFEProblem.betaTWFE_unique` | `P.vecTwfeNormalEq β → β = P.betaTWFE` | Full-rank uniqueness of the matrix normal-equation solution | **Proved** |
| `FlexibleDIDMundlak.ScalarTWFEProblem.toVector` | `ScalarTWFEProblem Unit Time → VectorTWFEProblem Unit Time (Fin 1)` | The scalar problem embeds as the singleton-`K` vector problem; `IsUnit (gram).det` reduces to `ddotX_ss_pos` | **Defined** |
| `FlexibleDIDMundlak.ScalarTWFEProblem.toVector_betaTWFE` | `P.toVector.betaTWFE 0 = P.betaTWFE` | The singleton-coordinate vector coefficient recovers the scalar coefficient, so the scalar theorem is the `K = Fin 1` case | **Proved** |

### `FlexibleDIDMundlak/VectorMundlak.lean` — K-vector two-way Mundlak equivalence (Theorem A, vector form)

Extends `twfe_twm_equivalence` to K-vector regressors via a matrix FWL handoff.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.gramOf` / `numerOf` | `(Unit → Time → K → ℝ) → Matrix K K ℝ` / `… → (Unit → Time → ℝ) → K → ℝ` | Generic residualized Gram matrix / numerator of a supplied residualized regressor | **Defined** |
| `FlexibleDIDMundlak.gram_eq_gramOf` / `numer_eq_numerOf` | `gram X = gramOf (ddotVec X)` / `numer X Y = numerOf (ddotVec X) (ddot Y)` | The TWFE Gram/numerator are the residualized instances of the generic versions | **Proved** |
| `FlexibleDIDMundlak.sum_dotRegressor` | `∑_it Dt·k (∑_j D·j β_j) = ∑_j (∑_it Dt·k D·j) β_j` | A residualized regressor against a `β`-combination factors through the cross-Gram | **Proved** |
| `FlexibleDIDMundlak.matrix_fwl_eq_of_normalEqs` | `(decompositions + per-coordinate orthogonality + IsUnit gram + normal eqs) → β = (gramOf Dtilde)⁻¹ (numerOf Dtilde Ytilde)` | Matrix Frisch-Waugh-Lovell handoff: vector coefficient satisfying pooled normal equations against `D` and nuisance class `H` equals the residualized matrix coefficient | **Proved** |
| `FlexibleDIDMundlak.IsVectorTwoWayMundlakNuisance` | `(X) → (Z → Unit → ℝ) → (M → Time → ℝ) → (Unit → Time → ℝ) → Prop` | K-vector two-way Mundlak nuisance span: constants, unit/time means of every coordinate of `X`, optional unit-only and time-only controls | **Defined** |
| `FlexibleDIDMundlak.vector_mundlak_nuisance_unit_time` | `IsVectorTwoWayMundlakNuisance X Zvar Mvar h → IsUnitTimeAdditive h` | Every vector Mundlak nuisance term is unit/time additive | **Proved** |
| `FlexibleDIDMundlak.sum_ite_one_mul` | `∑_k' (if k'=k then 1 else 0) * f k' = f k` | Coordinate-selection indicator collapse | **Proved** |
| `FlexibleDIDMundlak.VectorTWMFit` | `structure { beta, nuisance, nuisance_mem, normal_X, normal_H }` | Coding-free K-vector two-way Mundlak fit, normal equations against each regressor coordinate and the nuisance span | **Defined** |
| `FlexibleDIDMundlak.vec_twfe_twm_equivalence` | `VectorTWMFit P Zvar Mvar → fit.beta = P.betaTWFE` | K-vector finite-panel TWFE--two-way Mundlak equivalence (`thm:po-estimand-wooldridge-twfe-twm-equivalence`, full vector form) | **Proved** |
| `FlexibleDIDMundlak.vec_twfe_twm_optional_controls_invariant` | `fit₁.beta = fit₂.beta` | Adding or removing optional unit-only/time-only controls does not change the vector coefficient | **Proved** |

### `FlexibleDIDMundlak/DID.lean`

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.StaggeredATTCells Cohort Time Covar` | `structure { cohortShare, covarWeight, treatedCell, untreatedCell, Y0Mean, YgMean, observedMean, ... }` | Finite-cell ATT scaffold with arbitrary treated/untreated cell predicates and primitive conditional means; it does not yet enforce ordered periods, `∞` cohorts, or absorbing treatment internally | **Defined** |
| `StaggeredATTCells.treatedCells` | `StaggeredATTCells Cohort Time Covar → Finset (Cohort × Time)` | Finite support of target treated cohort-time cells selected by `treatedCell` | **Defined** |
| `StaggeredATTCells.untreatedCells` | `StaggeredATTCells Cohort Time Covar → Finset (Cohort × Time)` | Finite untreated design set (support of the weighted projection producing `m0`), selected by `untreatedCell` | **Defined** |
| `StaggeredATTCells.tauCell` | `StaggeredATTCells Cohort Time Covar → Cohort → Time → ℝ` | Covariate-weighted ATT cell `∑_c w_gc (Y_g - Y_0)` | **Defined** |
| `StaggeredATTCells.tauAgg` | `StaggeredATTCells Cohort Time Covar → (Cohort → Time → ℝ) → ℝ` | Weighted aggregate ATT over the treated-cell support | **Defined** |
| `FlexibleDIDMundlak.AggregateWeights P` | `structure { weight, nonneg_on_treated, sum_treated }` | Requested nonnegative treated-cell weights summing to one | **Defined** |
| `FlexibleDIDMundlak.NoAnticipation P` | `Prop` | Finite-cell no-anticipation predicate on `untreatedCell` observations; retained as a named assumption for the causal theorem interface | **Defined** |
| `FlexibleDIDMundlak.ConditionalParallelTrendsAdditive P` | `Prop` | Additive untreated-mean representation `Y0Mean g t c = alpha g c + lambda t c`, the scaffold form of conditional parallel trends | **Defined** |
| `FlexibleDIDMundlak.SaturatedUntreatedRegression P` | `structure { m0, additive, untreatedWeight, untreatedWeight_pos, untreatedNormalEq, design_identifies, target_cell_support }` | Bundled untreated-regression witness. `untreatedNormalEq` (with positive weights `untreatedWeight`/`untreatedWeight_pos`) is the genuine finite-cell projection origin: `m0` solves the weighted least-squares normal equations of the observed outcome on the additive class `H0` over the untreated design. `untreatedFit` (exact fit) and `recovers_target_Y0` are now DERIVED theorems, not fields; `design_identifies` is the concrete connected/full-rank condition. The former opaque `Prop`+`_holds` placeholders (untreated_design_connected, full_rank_after_normalization, covariates_baseline) and the former posited `untreatedFit` field were removed. | **Defined** |
| `FlexibleDIDMundlak.SaturatedUntreatedRegression.untreatedFit` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → untreatedCell g t → m0 g t c = YgMean g t c` | **DERIVED exact untreated fit (G5).** The weighted projection reproduces the cohort-`g` mean on every untreated cell. Proof: no-anticipation + conditional parallel trends make the untreated observed mean additive, so the residual `m0 − Y0Mean` is additive; plugging it into `untreatedNormalEq` forces its positively-weighted squared sum over the untreated design to zero, hence the residual vanishes cellwise | **Proved** |
| `FlexibleDIDMundlak.IsCellAdditive` | `(Cohort → Time → Covar → ℝ) → Prop` | Additive cell function `d g t c = γ g c + δ t c` | **Defined** |
| `FlexibleDIDMundlak.UntreatedDesignIdentifies P` | `Prop` | Connected/full-rank condition: an additive cell function vanishing on all untreated cells vanishes on all treated cells | **Defined** |
| `FlexibleDIDMundlak.SaturatedUntreatedRegression.recovers_target_Y0` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → treatedCell g t → m0 g t c = Y0Mean g t c` | Saturated regression recovers untreated means on treated cells, DERIVED from no-anticipation + conditional parallel trends + additivity + untreated fit + design identifiability | **Proved** |
| `FlexibleDIDMundlak.cellIndicator` | `(g : Cohort) → (t : Time) → Cohort → Time → ℝ` | Saturated treated-cell indicator `1{(g',t')=(g,t)}` | **Defined** |
| `FlexibleDIDMundlak.cellIndicator_normalEq_eq_cellResidual` | `(full saturated indicator normal equation) ↔ cellResidualNormalEq P S theta g t` | Saturated block-diagonalization: the full normal equation summed against a cell's indicator collapses (cross-cell terms vanish by orthogonality) to that cell's residual equation, justifying the POLS/ETWFE per-cell normal-equation fields | **Proved** |
| `FlexibleDIDMundlak.imputationTheta` | `(P) → SaturatedUntreatedRegression P → Cohort → Time → ℝ` | Imputation residual mean using `observedMean - m0` within a treated cell | **Defined** |
| `FlexibleDIDMundlak.cellResidualNormalEq P S theta g t` | `Prop` | Finite-cell residual normal equation `∑_c w_gc (observedMean_gtc - m0_gtc - theta) = 0` for a candidate cell coefficient | **Defined** |
| `FlexibleDIDMundlak.cellResidualNormalEq_eq_imputationTheta` | `cellResidualNormalEq P S theta g t → theta = imputationTheta P S g t` | A saturated treated-cell normal equation identifies the coefficient with the imputation residual mean, using `covarWeight_sum_one` | **Proved** |
| `FlexibleDIDMundlak.FlexibleDIDEstimands P S` | `structure { thetaImp, thetaPOLS, thetaETWFE, thetaImp_eq_imputation, pols_cell_normalEq, etwfe_cell_normalEq }` | Coding-free flexible imputation/POLS/ETWFE cell estimands; POLS and ETWFE coefficients now carry finite-cell residual normal equations instead of direct equality assumptions | **Defined** |
| `FlexibleDIDEstimands.thetaPOLS_eq_imputationTheta` / `thetaETWFE_eq_imputationTheta` | `treatedCell g t → thetaPOLS/ETWFE g t = imputationTheta P S g t` | Derived POLS and ETWFE cell equalities from their residual normal equations; the ETWFE normal equation is the residualized equation supplied by the FWL/TWFE-Mundlak bridge | **Proved** |
| `FlexibleDIDEstimands.pols_cell_eq_imputation` / `etwfe_cell_eq_pols` | `treatedCell g t → thetaPOLS = thetaImp` / `thetaETWFE = thetaPOLS` | Compatibility aliases; both equalities are now consequences of the normal-equation lemmas rather than structure fields | **Proved** |
| `FlexibleDIDMundlak.psiImp` / `psiPOLS` / `psiETWFE` | `(P) → FlexibleDIDEstimands P S → (Cohort → Time → ℝ) → ℝ` | Aggregate estimands formed by weighting the imputation, POLS, and ETWFE cell coefficients over treated cells | **Defined** |
| `FlexibleDIDMundlak.imputationTheta_eq_tauCell` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → treatedCell g t → E.thetaImp g t = P.tauCell g t` | Imputation recovers a treated ATT cell from consistency and the DERIVED `recovers_target_Y0` | **Proved** |
| `FlexibleDIDMundlak.flexible_did_cell_characterization` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → treatedCell g t → thetaImp = thetaPOLS = thetaETWFE = tauCell` | Cell-level theorem: the untreated-prediction witness plus POLS/ETWFE residual normal equations imply all three cell estimands equal the ATT cell | **Proved** |
| `FlexibleDIDMundlak.flexible_did_aggregate_characterization` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → AggregateWeights P → psiImp = tauAgg ∧ psiPOLS = tauAgg ∧ psiETWFE = tauAgg` | Aggregate theorem obtained by summing the normal-equation-derived cell equalities over treated cells | **Proved** |
| `FlexibleDIDMundlak.flexible_did_cell_characterization_of_estimand_equalities` / `flexible_did_aggregate_characterization_of_estimand_equalities` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → …` (same signatures as cell/aggregate characterization respectively) | Compatibility aliases for the earlier scaffold names, now taking the explicit causal hypotheses | **Proved** |
| `FlexibleDIDMundlak.flexible_did_scaffold_characterization` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → cell characterization ∧ aggregate characterization` | Finite-cell Wooldridge DID characterization from saturated untreated prediction plus POLS/ETWFE finite-cell residual normal equations | **Proved** |

### `FlexibleDIDMundlak/PopulationBridge.lean` — finite estimand/projection → population conditional expectation (audit G9 + G5 population half)

Bridges the finite-cell imputation estimand and the saturated projection `m0` to population conditional expectations, reusing the conditional finite-partition total law `Causalean.PO.eventCondExp_eq_sum_condProb_mul_eventCondExp`. Hypothesis-driven: identification hypotheses connect the finite scaffold's primitive means to a probability model — they are exactly the paper's *definitions* (θ^imp = E[Δ∣G=g]; m0 = E[Y(∞)∣G,t,C]), not strengthenings.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.imputationTheta_eq_eventCondExp` | `(μ, P, S, g, t, cohortEvent, covarCell, Δ)`, partition + integrability + `hweight : covarWeight g c = μ(cohortEvent ∩ covarCell c)/μ(cohortEvent)` + `hcell : observedMean − m0 = eventCondExp μ (cohortEvent ∩ covarCell c) Δ` → `imputationTheta P S g t = eventCondExp μ cohortEvent Δ` | **G9 finite→population bridge.** The finite imputation residual mean equals the population conditional expectation `E[Δ∣G=g]` on the cohort event, by the law of iterated expectations over the covariate partition | **Proved** |
| `FlexibleDIDMundlak.thetaImp_eq_eventCondExp` | `treatedCell g t →` (same hypotheses) `→ E.thetaImp g t = eventCondExp μ cohortEvent Δ` | **G9 (θ^imp = E[Δ∣G=g]).** The `FlexibleDIDEstimands` imputation cell estimand equals the population conditional expectation on each treated cell; composes `thetaImp_eq_imputation` with `imputationTheta_eq_eventCondExp` | **Proved** |
| `FlexibleDIDMundlak.m0_eq_eventCondExp_treated` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → treatedCell g t →` `hY0 : Y0Mean g t c = eventCondExp μ (cellEvent g t c) Y0pop` `→ S.m0 g t c = eventCondExp μ (cellEvent g t c) Y0pop` | **G5 population origin (treated cells).** The saturated projection's fitted value equals `E[Y_t(∞)∣G=g,t,C=c]`; causal content (additive extrapolation) carried by `recovers_target_Y0`, `hY0` reads `Y0Mean` as that conditional mean | **Proved** |
| `FlexibleDIDMundlak.m0_eq_eventCondExp_untreated` | `NoAnticipation P → ConditionalParallelTrendsAdditive P → untreatedCell g t →` `hY0 : Y0Mean g t c = eventCondExp μ (cellEvent g t c) Y0pop` `→ S.m0 g t c = eventCondExp μ (cellEvent g t c) Y0pop` | **G5 population origin (untreated cells).** Same conclusion via `untreatedFit` + no-anticipation | **Proved** |

### `FlexibleDIDMundlak/PopulationOrigin.lean` — `ofPopulation` constructor making the m0 conditional-expectation origin definitional (audit G5 measure layer)

Closes the *measure-layer* half of G5: instead of *assuming* `Y0Mean g t c = E[Y(∞)∣cell]` (the `hY0` hypothesis above), it builds a `StaggeredATTCells` whose cell means are *defined* as that `eventCondExp`, so `hY0` holds by `rfl`. Mirrors `DCDHPanel.ofPopulation`. The cell-weight data (`cohortShare`, `covarWeight`) is supplied as a witness (an FWL/partition construction orthogonal to the conditional-expectation origin); the pointwise consistency inputs are the paper's consistency axiom restricted to each cell.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FlexibleDIDMundlak.StaggeredATTCells.ofPopulation` | `(μ : Measure Ω) [IsProbabilityMeasure μ] (cellEvent) (Y0pop Ygpop Yobspop : Ω → ℝ) (treatedCell untreatedCell) (cohortShare covarWeight) (hmeas) (weight side-conditions) (hcons_tr hcons_ut) → StaggeredATTCells Cohort Time Covar` | **G5 (panel from a population model).** Defines `Y0Mean/YgMean/observedMean g t c := eventCondExp μ (cellEvent g t c) {Y0pop/Ygpop/Yobspop}`; derives `consistency_treated`/`consistency_untreated` from pointwise PO consistency via `eventCondExp_congr_on`; weights passed as witnesses | **Defined** |
| `FlexibleDIDMundlak.m0_eq_eventCondExp_treated_ofPopulation` | for `P = ofPopulation μ …`, `hNA → hCPT → treatedCell g t → S.m0 g t c = eventCondExp μ (cellEvent g t c) Y0pop` | **G5 payoff (treated, definitional).** Applies `m0_eq_eventCondExp_treated` with `hY0 := rfl` — the conditional-expectation origin of `m0` is now definitional, not assumed | **Proved** |
| `FlexibleDIDMundlak.m0_eq_eventCondExp_untreated_ofPopulation` | for `P = ofPopulation μ …`, `hNA → hCPT → untreatedCell g t → S.m0 g t c = eventCondExp μ (cellEvent g t c) Y0pop` | **G5 payoff (untreated, definitional).** Untreated-cell analogue, `hY0 := rfl` | **Proved** |

---

## 9i0a-ImputationEventStudy. `Panel/EstimandCharacterization/ImputationEventStudy/Imputation.lean` — Robust Imputation Event Studies (BJS)

Specific-paper finite-cell formalization of the Borusyak-Jaravel-Spiess robust imputation event-study characterization. The module works directly with finite treated and untreated cell index types, arbitrary signed target weights, treated/untreated regressor rows, primitive observed-law means, a nuisance vector, and treated-cell effects. It proves the population imputation identification theorem (taking `PredictionIdentified` as a propositional existence hypothesis and extracting the witness internally) and the intentionally-weak `linear_unbiased_of_imputation_representation` algebra helper. The **source-strength** linear-unbiased representation theorem and the combined iff characterization now live in the companion `PanelBridge.lean` (§9i0a-ImputationEventStudyBridge), where the finite left-null-space adjustment is rebased onto the panel `WeightedSupport`/`Subspace` substrate. A staggered-adoption two-way-FE grounding (closing the abstract-index scope gap) lives in `PanelGrounding.lean` (§9i0a-ImputationEventStudyGrounding). The Gauss-Markov/BLUE efficiency half of `prop:po-estimand-bjs-linear-unbiased-imputation` is in `Efficiency.lean` (§9i0a-ImputationEventStudyEfficiency), built on the finite Gauss-Markov layer §13gm. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/borusyak_jaravel_spiess_imputation.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `ImputationEventStudy.dot` | `(Regressor → ℝ) → (Regressor → ℝ) → ℝ` | Finite regressor dot product used for untreated-outcome linear predictions | **Defined** |
| `ImputationEventStudy.BJSPanel` | `structure { qT, qU, a, EY_T, EY_U, EY0_T, EY0_U, beta0, tau }` | Finite BJS treated/untreated cell design: regressor rows, arbitrary target weights, observed means, untreated potential-outcome means, nuisance vector, and treated-cell effects | **Defined** |
| `BJSPanel.theta` | `BJSPanel Treated Untreated Regressor → ℝ` | Target weighted sum `∑ c, a c * tau c`; weights may be signed and need not sum to one | **Defined** |
| `BJSPanel.targetForTau` | `(Treated → ℝ) → ℝ` | Target functional evaluated at an arbitrary unrestricted treated-effect vector, used in the linear-unbiasedness statement | **Defined** |
| `BJSPanel.UntreatedOutcomeModel` | `Prop` | Deterministic mean version of the untreated-outcome linear model plus untreated-cell no-anticipation / observation equation | **Defined** |
| `BJSPanel.TreatmentEffectFixed` | `Prop` | Treated observed means equal untreated means plus the fixed/design-conditional treated-cell effect | **Defined** |
| `BJSPanel.ImputationWeights` | `structure { weight, row_identity }` | Witness-form prediction-span condition: for every target-relevant treated cell, its regressor row is a weighted combination of untreated rows | **Defined** |
| `BJSPanel.PredictionIdentified` | `Prop` | Propositional existence wrapper (`Nonempty ImputationWeights`) for valid target-relevant imputation weights; natural hypothesis for identification — callers supply existence, not a pre-built witness | **Defined** |
| `BJSPanel.psiImp` | `(Treated → Untreated → ℝ) → ℝ` | Population observed-law imputation functional `∑ a_c (EY_T c - ∑ h_cu EY_U u)` for arbitrary imputation weights | **Defined** |
| `BJSPanel.bjs_imputation_identification` | `UntreatedOutcomeModel → PredictionIdentified → TreatmentEffectFixed → ∃ h : ImputationWeights, psiImp h.weight = theta` | BJS population identification theorem: under the untreated model and prediction-span condition, some imputation weight matrix recovers the weighted treated-cell target; witness extracted internally via `Classical.choice` | **Proved** |
| `BJSPanel.LinearEstimator` | `structure { vT, vU }` | Linear estimator coefficients on treated and untreated observed cell outcomes | **Defined** |
| `BJSPanel.LinearEstimator.value` | `(Treated → ℝ) → (Untreated → ℝ) → ℝ` | Evaluates a treated-plus-untreated linear estimator on arbitrary outcome arrays | **Defined** |
| `BJSPanel.LinearEstimator.observedValue` | `ℝ` | Evaluates a linear estimator on the panel's observed-law treated and untreated means | **Defined** |
| `BJSPanel.LinearEstimator.modelValue` | `(Regressor → ℝ) → (Treated → ℝ) → ℝ` | Model-implied value of the linear estimator for a nuisance vector and unrestricted treated-effect vector | **Defined** |
| `BJSPanel.LinearEstimator.unbiasedForAllTau` | `Prop` | Linear unbiasedness for every nuisance vector and every unrestricted treated-effect vector | **Defined** |
| `BJSPanel.LinearEstimator.vT_eq_a` | `unbiasedForAllTau → ∀ c, vT c = a c` | Hoisted fact: unrestricted treatment-effect heterogeneity forces treated-cell weights to equal target weights (indicator-`tau` test); reused by the efficiency layer | **Proved** |
| `BJSPanel.LinearEstimator.nuisance_coord` | `unbiasedForAllTau → ∀ r, (∑ c, a c * qT c r) + ∑ u, vU u * qU u r = 0` | Hoisted fact: nuisance unbiasedness gives the BJS left-null-space coordinate constraint `aᵀQ_T + vUᵀQ_U = 0`; reused by the efficiency layer | **Proved** |
| `BJSPanel.LinearEstimator.HasImputationRepresentation` | `structure { weights, untreated_weight_representation }` | Explicit witness that a linear estimator's untreated coefficients are represented by valid BJS imputation weights; used as an algebra helper for the source-strength theorem | **Defined** |
| `BJSPanel.linear_unbiased_of_imputation_representation` | `unbiasedForAllTau → HasImputationRepresentation → ∃ H, vT = a ∧ vU = -aᵀH ∧ (∀ YT YU, value YT YU = ∑ c, a c * (YT c - ∑ u, H.weight c u * YU u)) ∧ observedValue = psiImp H.weight` | Intentionally weak algebra helper: given a pre-built imputation representation witness, derives the estimator identity; source-strength derivation lives in `linear_unbiased_of_prediction_identified` | **Proved** |

---

## 9i0a-ImputationEventStudyBridge. `Panel/EstimandCharacterization/ImputationEventStudy/PanelBridge.lean` — Imputation Event Study ↔ panel substrate bridge

Connects the bespoke finite design-matrix algebra of the BJS imputation scaffold to the shared panel inner-product / subspace substrate (`Causalean/Panel/Weighted/`). The OLS imputation estimator is the *uniform-weight* special case of the panel WLS substrate, so the untreated cells carry the uniform `WeightedSupport`; the keystone lemma proves the BJS "left-null-space of `Q_U`" condition **is** `ip`-orthogonality to the regressor column span. The source-strength linear-unbiased theorem and the combined iff characterization (moved here from `Imputation.lean`, statements verbatim) consume that substrate-based left-null-space adjustment; `DecidableEq Untreated` is obtained internally via `Classical.decEq`, so their public signatures are unchanged.

| Declaration | Signature | Description | Status |
| --- | --- | --- | --- |
| `ImputationEventStudy.untreatedSupport` | `(Untreated : Type*) → [Fintype, DecidableEq, Nonempty] → WeightedSupport Untreated` | The untreated cells as the uniform `WeightedSupport` `ω_u ≡ 1/\|U\|` — the OLS = uniform-weight WLS instantiation through which BJS consumes the panel `ip`/`Subspace` API | **Defined** |
| `BJSPanel.regressorColumn` | `Regressor → (Untreated → ℝ)` | The `r`-th regressor column `u ↦ q_{ur}` as an array over untreated cells | **Defined** |
| `BJSPanel.columnSpan` | `Submodule ℝ (Untreated → ℝ)` | Span of all regressor columns; orthogonality to it is the BJS left-null-space condition | **Defined** |
| `BJSPanel.ip_untreatedSupport_regressorColumn` | `ip v (regressorColumn r) = (\|U\|)⁻¹ * ∑ u, v u * qU u r` | The panel inner product of an untreated vector with a regressor column equals the BJS left-null-space sum, rescaled by the uniform weight | **Proved** |
| `BJSPanel.ip_regressorColumn_eq_zero_iff` | `ip v (regressorColumn r) = 0 ↔ ∑ u, v u * qU u r = 0` | `ip`-orthogonality to a single column ⟺ that column's left-null coordinate vanishes | **Proved** |
| `BJSPanel.columnSpan_ip_orthogonal_iff` | `(∀ h ∈ columnSpan, ip v h = 0) ↔ ∀ r, ∑ u, v u * qU u r = 0` | **Keystone bridge**: a vector is `ip`-orthogonal to the regressor column span iff it satisfies the BJS left-null-space condition (the panel-substrate restatement of "`v` lies in the left null space of `Q_U`") | **Proved** |
| `BJSPanel.exists_imputationWeights_of_gap_orthogonal` | `(hc0 : a c0 ≠ 0) → H0 → (gap ip-orthogonal to columnSpan) → ∃ H, ∀ u, ∑ c, a c * H.weight c u = -vU u` | Audit M3 left-null-space row adjustment, phrased through the panel substrate: one nonzero target row absorbs the orthogonal gap, yielding imputation weights representing the untreated coefficients | **Proved** |
| `BJSPanel.linear_unbiased_of_prediction_identified` | `unbiasedForAllTau → PredictionIdentified → (∃ c, a c ≠ 0) → ∃ H, vT = a ∧ vU = -aᵀH ∧ (∀ YT YU, value YT YU = ∑ c, a c * (YT c - ∑ u, H.weight c u * YU u)) ∧ observedValue = psiImp H.weight` | Source-strength BJS linear-unbiased representation theorem; now consumes `exists_imputationWeights_of_gap_orthogonal` (empty-`Untreated` case handled separately) instead of an inline finite-algebra construction | **Proved** |
| `BJSPanel.bjs_linear_unbiased_iff_imputation_form` | `PredictionIdentified → (∃ c, a c ≠ 0) → (unbiasedForAllTau ↔ ∃ H, vT = a ∧ vU = -aᵀH ∧ (∀ YT YU, value = ∑ a_c (YT c - ∑ h_cu YU u)) ∧ observedValue = psiImp H.weight)` | Combined iff characterization (`prop:po-estimand-bjs-linear-unbiased-imputation`, identification half): unbiased for every tau iff BJS imputation form; (⟹) is `linear_unbiased_of_prediction_identified`, (⟸) is pure row-span algebra | **Proved** |

---

## 9i0a-ImputationEventStudyGrounding. `Panel/EstimandCharacterization/ImputationEventStudy/PanelGrounding.lean` — Imputation Event Study staggered-adoption grounding

Closes the abstract-index scope gap (audit G5) *constructively*: exhibits `BJSPanel` as an instance built from genuine panel primitives — units `I`, periods `Fin T`, and a staggered-adoption path `g : I → WithTop (Fin T)` via `Causalean.Panel.AdoptionPath`. Treated cells are those whose treatment has switched on (`g i ≤ t`); untreated cells are those still untreated (`t < g i`), faithfully including every never-treated cell and every pre-adoption cell of a treated unit. The regressor design is the canonical two-way fixed-effect block `I ⊕ Fin T`, so the untreated-outcome model `E[Y(0)] = α_i + λ_t` holds by construction and the BJS hypotheses discharge to a concrete panel.

| Declaration | Signature | Description | Status |
| --- | --- | --- | --- |
| `ImputationEventStudy.TreatedCell` | `(g : I → WithTop (Fin T)) → Type` | Subtype of cells `(i,t)` whose treatment has switched on (`AdoptionPath.le (g i) t`) | **Defined** |
| `ImputationEventStudy.UntreatedCell` | `(g : I → WithTop (Fin T)) → Type` | Subtype of still-untreated cells (`AdoptionPath.lt (g i) t`); includes never-treated and pre-adoption cells | **Defined** |
| `ImputationEventStudy.treated_not_untreated` | `le (g c.1) c.2 → lt (g c.1) c.2 → False` | The cell partition is exclusive: no cell is both adopted-by-`t` and untreated-at-`t` | **Proved** |
| `ImputationEventStudy.treated_or_untreated` | `∀ c, le (g c.1) c.2 ∨ lt (g c.1) c.2` | The cell partition is exhaustive: every cell is treated or untreated | **Proved** |
| `ImputationEventStudy.feRow` | `(I × Fin T) → (I ⊕ Fin T) → ℝ` | Two-way fixed-effect design row for a cell: unit-`i` indicator stacked with time-`t` indicator | **Defined** |
| `ImputationEventStudy.dot_feRow` | `dot (feRow c) (Sum.elim α lam) = α c.1 + lam c.2` | The FE row evaluates the additive fixed-effect model `α_i + λ_t` | **Proved** |
| `ImputationEventStudy.ofStaggeredTWFE` | `(g) → (α : I → ℝ) → (lam : Fin T → ℝ) → (a tau : TreatedCell g → ℝ) → BJSPanel (TreatedCell g) (UntreatedCell g) (I ⊕ Fin T)` | Grounding constructor: builds a `BJSPanel` from an adoption path and two-way fixed effects | **Defined** |
| `ImputationEventStudy.ofStaggeredTWFE_untreatedModel` | `(ofStaggeredTWFE …).UntreatedOutcomeModel` | The grounded panel satisfies the BJS untreated-outcome model (both cell means equal `α_i + λ_t`; untreated cells have no anticipation) | **Proved** |
| `ImputationEventStudy.ofStaggeredTWFE_treatmentFixed` | `(ofStaggeredTWFE …).TreatmentEffectFixed` | The grounded panel satisfies the BJS fixed-effect equation `E[Y_T] = E[Y_T(0)] + τ` | **Proved** |

---

## 9i0a-ImputationEventStudyEfficiency. `Panel/EstimandCharacterization/ImputationEventStudy/Efficiency.lean` — Imputation Event Study Efficiency / BLUE half

The efficiency half of `prop:po-estimand-bjs-linear-unbiased-imputation`: under spherical errors the OLS imputation estimator is the minimum-variance linear unbiased estimator (Gauss-Markov / BLUE). The BJS imputation estimator is recognized as the OLS estimator for the *event-study design* `designFull` (a treated-cell indicator block stacked with the covariate block), so the result is a direct instantiation of the finite Gauss-Markov theorem `GaussMarkov.variance_blue_spherical` (§13gm) at that design. The unbiasedness-to-design-constraint bridge uses the hoisted `LinearEstimator.vT_eq_a` and `LinearEstimator.nuisance_coord`. An additive fixed-effect-block bridge (`designFull_col_mem_indicatorSpan`) identifies the τ-columns of `designFull` with the panel `IndicatorSpan` substrate (`Causalean/Panel/Weighted/IndicatorSpan.lean`), connecting the event-study FE design to the panel indicator-span layer; the BLUE proof itself stays on the finite Gauss-Markov substrate (§13gm), the correct home for the variance argument. No `sorry`/`admit`/`axiom`. Companion NL doc section C3.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `BJSPanel.designFull` | `BJSPanel Treated Untreated Regressor → Matrix (Treated ⊕ Untreated) (Treated ⊕ Regressor) ℝ` | Event-study design: rows are observed cells, columns are treated-cell fixed effects (τ block, cell-indicator on treated rows / zero on untreated rows) stacked with covariates (β block, `qT`/`qU`) | **Defined** |
| `BJSPanel.cFull` | `BJSPanel Treated Untreated Regressor → (Treated ⊕ Regressor → ℝ)` | Target functional in design coordinates: target weights `a` on the τ block, zero on the covariate block | **Defined** |
| `BJSPanel.weightOf` | `LinearEstimator P → (Treated ⊕ Untreated → ℝ)` | A linear estimator's full weight vector over all observed cells (`Sum.elim vT vU`) | **Defined** |
| `BJSPanel.weightOf_vecMul_designFull` | `unbiasedForAllTau → weightOf L ᵥ* designFull P = cFull P` | Unbiasedness bridge: an unbiased estimator's weight satisfies the design constraint (captures both `vT = a` and the nuisance left-null constraint) | **Proved** |
| `BJSPanel.bjs_ols_imputation_min_variance_spherical` | `(∀ i, MemLp (Y i) 2 μ) → SphericalFamily Y μ σ → IsUnit ((designFull P)ᵀ * designFull P).det → unbiasedForAllTau L → Var[∑ olsWeight … * Y] ≤ Var[∑ weightOf L * Y]` | **BJS efficiency theorem (BLUE):** for a full-rank event-study design and spherical cell outcomes, the OLS imputation estimator has variance no larger than any linear unbiased estimator of the target | **Proved** |
| `BJSPanel.treatedClassifier` | `(Treated ⊕ Untreated) → Option Treated` | Classifier on observed cells: each treated cell to its own label, every untreated cell to `none`; its `Some`-indicators are the τ-columns of `designFull` | **Defined** |
| `BJSPanel.designFull_col_eq_cellIndicator` | `(fun i => designFull P i (Sum.inl d)) = cellIndicator treatedClassifier (some d)` | Each treated-cell fixed-effect column of `designFull` is the panel cell indicator of the treated-cell classifier | **Proved** |
| `BJSPanel.designFull_col_mem_indicatorSpan` | `(fun i => designFull P i (Sum.inl d)) ∈ indicatorSpan treatedClassifier` | The treated-cell FE block of `designFull` lies in the panel `IndicatorSpan`: the BJS event-study FE design is the panel indicator-span substrate (additive; BLUE proof unchanged) | **Proved** |

---

## 9i0a-HeterogeneousTWFE. `Panel/EstimandCharacterization/HeterogeneousTWFE/{FinitePanel,FWLBridge,PopulationBridge}.lean` — de Chaisemartin-D'Haultfoeuille (2020) TWFE Signed Weights

Specific-paper finite-cell formalization of the de Chaisemartin-D'Haultfoeuille TWFE heterogeneous-effects warning. `FinitePanel.lean` works over finite group-time cells with positive weights `π_gt`, binary treatment `D_gt`, observed outcomes `Y_gt`, untreated outcomes `Y0_gt`, treatment effects `τ_gt`, and a residualized-treatment witness `D̃_gt` against the group-plus-time fixed-effect span. It proves the finite FWL decomposition `β^twfe = B0 + ∑_{D=1} ω_gt τ_gt`, the zero-untreated-residual-contrast corollary, the fact that treated-cell weights sum to one while inheriting the sign of `D̃`, and the constructive sign-reversal theorem. `FWLBridge.lean` closes audit gaps **G2/G6**: for the uniform balanced panel it builds a genuine `DCDHPanel` whose `D̃` is the *derived* Frisch-Waugh-Lovell residual (double-demeaned treatment `ddot D` from `UniformTwoWayPanel`), with the membership and orthogonality fields PROVED rather than posited — only nonzero double-demeaned treatment variation (`S_D > 0`), binary treatment, and consistency remain as primitives. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/de_chaisemartin_dhaultfoeuille_twfe.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `HeterogeneousTWFE.IsGTFE` | `(G → T → ℝ) → Prop` | Compatibility alias for `AdditiveSpan.IsUnitTimeAdditive`, specialized to group-time fixed effects `a_g + b_t` | **Defined** |
| `HeterogeneousTWFE.DCDHPanel G T` | `structure { pi, D, Y, Y0, tau, Dtilde : G → T → ℝ, pi_pos, pi_sum_one, D_binary, consistency, D_minus_resid_mem, Dtilde_orthogonal, SD_pos }` | Finite DCDH group-time panel plus residualized-treatment witness (`def:po-estimand-dcdh-panel`, `def:po-estimand-dcdh-residualized-treatment`) | **Defined** |
| `DCDHPanel.SD P` | `DCDHPanel G T → ℝ` | Residualized-treatment denominator `S_D = ∑ π_gt D̃_gt²` | **Defined** |
| `DCDHPanel.betaTWFE P` | `DCDHPanel G T → ℝ` | Finite FWL/TWFE coefficient `(∑ π_gt D̃_gt Y_gt) / S_D` (`def:po-estimand-dcdh-twfe-coefficient`) | **Defined** |
| `DCDHPanel.untreatedBias P` | `DCDHPanel G T → ℝ` | Bias term `B0 = (∑ π_gt D̃_gt Y0_gt) / S_D` in the DCDH decomposition | **Defined** |
| `DCDHPanel.omega P g t` | `DCDHPanel G T → G → T → ℝ` | Normalized residualized-treatment cell weight `ω_gt = π_gt D̃_gt / S_D`, interpreted on treated cells | **Defined** |
| `DCDHPanel.treatedCells P` | `DCDHPanel G T → Finset (G × T)` | Finite set of cells with `D_gt = 1`, used to state treated-cell sums without partial weights | **Defined** |
| `DCDHPanel.DWeightedTau P` / `DCDHPanel.treatedWeightedTau P` | `DCDHPanel G T → ℝ` | All-cell `D`-weighted treatment-effect component and equivalent treated-cell weighted sum | **Defined** |
| `DCDHPanel.zeroUntreatedResidualContrast P` | `DCDHPanel G T → Prop` | Exact DCDH bias-free condition `∑ π_gt D̃_gt Y0_gt = 0` (`ass:po-estimand-dcdh-untreated-parallel`) | **Defined** |
| `DCDHPanel.inner_Dtilde_D_eq_SD` | `∑ π_gt D̃_gt D_gt = P.SD` | Denominator identity derived by applying residualized-treatment orthogonality to `D − D̃` | **Proved** |
| `DCDHPanel.zeroUntreatedResidualContrast_of_Y0_mem_gtfe` | `IsGTFE P.Y0 → P.zeroUntreatedResidualContrast` | Additive group-plus-time untreated outcomes are a sufficient, not necessary, condition for zero untreated residual contrast | **Proved** |
| `DCDHPanel.twfe_eq_untreatedBias_add_DWeightedTau` | `P.betaTWFE = P.untreatedBias + P.DWeightedTau` | All-cell version of the DCDH TWFE decomposition after substituting consistency | **Proved** |
| `DCDHPanel.DWeightedTau_eq_treatedWeightedTau` | `P.DWeightedTau = P.treatedWeightedTau` | Binary-treatment filtering lemma: the all-cell effect component equals the treated-cell weighted sum | **Proved** |
| `DCDHPanel.twfe_eq_untreatedBias_add_treated_weighted_tau` | `P.betaTWFE = P.untreatedBias + P.treatedWeightedTau` | Headline decomposition `β^twfe = B0 + ∑_{D=1} ω_gt τ_gt` (`thm:po-estimand-dcdh-twfe-decomposition`) | **Proved** |
| `DCDHPanel.twfe_eq_treated_weighted_tau_of_zeroUntreatedContrast` | `P.zeroUntreatedResidualContrast → P.betaTWFE = P.treatedWeightedTau` | Bias-free DCDH causal weighted-sum corollary under the exact zero untreated residual contrast | **Proved** |
| `DCDHPanel.treated_omega_sum_eq_one` | `∑_{gt ∈ treatedCells} omega P gt.1 gt.2 = 1` | Treated-cell DCDH weights sum to one (`prop:po-estimand-dcdh-weights-sum-one`) | **Proved** |
| `DCDHPanel.treated_omega_neg_iff_Dtilde_neg` | `P.omega g t < 0 ↔ P.Dtilde g t < 0` | Negative-sign direction of `sign(ω_gt)=sign(D̃_gt)`; needs only `π_gt>0`, `S_D>0` | **Proved** |
| `DCDHPanel.treated_omega_pos_iff_Dtilde_pos` | `0 < P.omega g t ↔ 0 < P.Dtilde g t` | Positive-sign direction, completing the sign triplet | **Proved** |
| `DCDHPanel.treated_omega_zero_iff_Dtilde_zero` | `P.omega g t = 0 ↔ P.Dtilde g t = 0` | Zero-sign direction, completing the sign triplet | **Proved** |
| `HeterogeneousTWFE.neg_component_of_exists_Dtilde_neg` | `(∃ g t, P.D g t = 1 ∧ P.Dtilde g t < 0) → ∑_{treated,ω<0} ω < 0` | Bridge: the paper's natural entry condition "some treated cell has D̃_gt<0" implies the negative-weight-sum precondition of the sign-reversal theorem | **Proved** |
| `HeterogeneousTWFE.two_cell_signed_weights_positive_effects_negative_sum` | `0 < c → 0 < ε → ((1+c)ε)/c < M → 0 < ε ∧ 0 < M ∧ (1+c)ε + (-c)M < 0` | Explicit two-cell sign-reversal arithmetic from `prop:po-estimand-dcdh-sign-reversal` | **Proved** |
| `HeterogeneousTWFE.exists_positive_effects_negative_weighted_sum_of_negative_component` | `(∑ w = 1) → (∑_{w<0} w < 0) → ∃ τ, (∀ i, 0 < τ i) ∧ ∑ i, w i τ i < 0` | Reusable finite signed-average construction: a strictly negative total weight component can dominate with positive effects | **Proved** |
| `HeterogeneousTWFE.exists_panel_with_positive_treated_effects_twfe_negative_of_negative_component` | `P.zeroUntreatedResidualContrast → (∑_{treated,ω<0} ω < 0) → ∃ P', same π,D,Y0,D̃, (∀ g t, 0 < P'.tau g t), P'.zeroUntreatedResidualContrast ∧ P'.betaTWFE < 0` | Panel-level DCDH sign-reversal theorem: keeping the treatment/residualization geometry fixed, one can choose globally positive treated effects (for all g t) so TWFE is negative | **Proved** |

### `HeterogeneousTWFE/FWLBridge.lean` — `D̃` as a derived FWL residual (audit G2/G6)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `HeterogeneousTWFE.uniformWeight G T` | `ℝ` | Uniform unit-period cell weight `1/(|G|·|T|)` for the balanced-panel DCDH instance | **Defined** |
| `HeterogeneousTWFE.uniformWeight_pos` | `UniformTwoWayPanel.BalancedPanel G T → 0 < uniformWeight G T` | Positivity of the uniform weight from the `≥2` cardinality side conditions | **Proved** |
| `HeterogeneousTWFE.ofTwoWayPanel` | `BalancedPanel G T → (D Y Y0 tau) → (D binary) → (consistency) → (0 < ∑ uniformWeight·(ddot D)²) → DCDHPanel G T` | **G2/G6:** builds a genuine `DCDHPanel` whose `Dtilde := ddot D` (double-demeaned FWL residual); `D_minus_resid_mem` and `Dtilde_orthogonal` are DERIVED from `UniformTwoWayPanel` (`sub_ddot_eq_unitTimeProjection`, `unitTimeProjection_additive`, `ddot_orthogonal_unit_time`), not posited | **Defined** |
| `HeterogeneousTWFE.ofTwoWayPanel_Dtilde` | `(ofTwoWayPanel …).Dtilde = ddot D` | The residualized treatment of the FWL-derived panel is exactly the double-demeaned treatment | **Proved** |
| `HeterogeneousTWFE.ofTwoWayPanel_betaTWFE` | `(ofTwoWayPanel …).betaTWFE = finiteResidualizedCoefficient (ddot D) Y` | The DCDH TWFE coefficient of the FWL-derived panel equals the uniform-panel FWL residualized coefficient of `Y` on `ddot D` (the uniform weight cancels) | **Proved** |

### `HeterogeneousTWFE/PopulationBridge.lean` — cell consistency from a potential-outcome contrast (audit G10)

Supplies the population origin of the `DCDHPanel.consistency` field and of `tau` as `Y(1) − Y(0)`, at the level of cell-conditional means, reusing `Causalean.PO.eventCondExp_congr_on`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `HeterogeneousTWFE.cellMean_consistency` | `(μ, A measurable, Y Y0 Y1, d ∈ {0,1})`, `hcons : ∀ ω ∈ A, Y ω = Y0 ω + d·(Y1 ω − Y0 ω)` → `eventCondExp μ A Y = eventCondExp μ A Y0 + d·(eventCondExp μ A Y1 − eventCondExp μ A Y0)` | **G10 finite→population bridge.** On a cell where treatment is constant `D ≡ d` and PO consistency holds pointwise, the cell-conditional means satisfy the DCDH consistency identity `Ȳ = Ȳ(0) + d·τ̄` with `τ̄ = E[Y(1)∣A] − E[Y(0)∣A]` a genuine potential-outcome contrast. Proof: case split on `d`, `eventCondExp_congr_on`, `ring` | **Proved** |
| `HeterogeneousTWFE.DCDHPanel.ofPopulation` | `[IsProbabilityMeasure μ]`, cell partition `cellEvent`, outcomes `Yfac Y0pop Y1pop`, binary cell treatment `d`, `Dtilde` witness; partition/positivity + pointwise consistency `hcons` + `Dtilde` orthogonality/`SD_pos` hypotheses → `DCDHPanel G T` | **G10 `DCDHPanel` from a population model.** Builds a genuine panel with `Y/Y0 = E[·∣cell]`, `tau = E[Y(1)∣cell] − E[Y(0)∣cell]` (a real PO contrast), `pi = μ(cell)`. `consistency` is DERIVED via `cellMean_consistency`; `pi_sum_one` from `measure_biUnion_finset` over the partition. `Dtilde` (general-weight FWL residual) is a witness, as in the base panel — the uniform case is `ofTwoWayPanel` | **Defined** |

---

## 9i0a-OLSWeightDecomposition. `Panel/EstimandCharacterization/OLSWeightDecomposition/{FinitePartition,OverlapWeightedATE,GeneralCondExp}.lean` — Słoczyński (2022) Finite-Partition OLS Weights

Specific-paper specialization of the residualization framework to Słoczyński (2022, *Review of Economics and Statistics*). Layer A (`FinitePartition.lean`) is pure finite-cell algebra: given a finite covariate `G : 𝒢` with cell probabilities `π_g`, treated shares `p_g`, and conditional treatment effects `τ_g`, the saturated-OLS estimand decomposes as `β_sat = Σ ω_g τ_g`, where `ω_g ∝ π_g p_g (1−p_g)`. On its own this layer carries no causal meaning (`π, p, τ` are formal reals); the causal content is injected in Layer B. Layer B (`OverlapWeightedATE.lean`, backed by `Support/*.lean`) instantiates the residualization core on a probability space with binary treatment, consistency, and finite-cell CMI, constructs the saturated residualization witnesses, and proves that the residualized coefficient equals the Layer A overlap-weighted ATE — with `τ_g = E[Y(1)−Y(0)∣G=g]` built from real potential outcomes. All declarations live in `namespace Causalean.Panel.EstimandCharacterization.OLSWeightDecomposition`. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/sloczynski_ols_heterogeneous.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FinitePartition 𝒢` | `structure { π, p, τ : 𝒢 → ℝ, π_nonneg, π_sum_one, p_nonneg, p_le_one, overlap_pos }` | Słoczyński finite covariate partition (`def:po-estimand-sloczynski-ols-finite-partition`): cell probabilities `π_g`, treated shares `p_g ∈ [0,1]`, conditional treatment effects `τ_g`, plus the `Σπ=1` probability-vector constraint and the saturated-overlap relevance hypothesis `Σ π_g p_g (1−p_g) > 0` | **Defined** |
| `FinitePartition.cellOverlap P g` | `FinitePartition 𝒢 → 𝒢 → ℝ` | Within-cell treatment variance `p_g (1 − p_g)` | **Defined** |
| `FinitePartition.overlapNumerator P` | `FinitePartition 𝒢 → ℝ` | Numerator `Σ_g π_g · p_g (1−p_g) · τ_g` of the saturated-OLS estimand | **Defined** |
| `FinitePartition.overlapDenominator P` | `FinitePartition 𝒢 → ℝ` | Denominator `Σ_g π_g · p_g (1−p_g)` of the saturated-OLS estimand | **Defined** |
| `FinitePartition.overlapWeight P g` | `noncomputable FinitePartition 𝒢 → 𝒢 → ℝ` | Normalized weight `ω_g = π_g p_g (1−p_g) / Σ_h π_h p_h (1−p_h)` | **Defined** |
| `FinitePartition.overlapWeightedATE P` | `noncomputable FinitePartition 𝒢 → ℝ` | Saturated-OLS estimand `β_sat = (Σ π·p(1−p)·τ) / (Σ π·p(1−p))` in finite-cell form | **Defined** |
| `FinitePartition.overlapDenominator_pos` | `0 < P.overlapDenominator` | Denominator positivity, restated from `overlap_pos` after unfolding `cellOverlap` | **Proved** |
| `FinitePartition.omega_sum_eq_one` | `∑ g, P.overlapWeight g = 1` | The overlap weights sum to one. Proof: pull the common denominator out of the sum via `Finset.sum_div`, then cancel | **Proved** |
| `FinitePartition.finite_weights_eq_sum` | `P.overlapWeightedATE = ∑ g, P.overlapWeight g * P.τ g` | **Finite-partition OLS weights** (`prop:po-estimand-sloczynski-ols-finite-weights`): the saturated-OLS estimand decomposes as `Σ ω_g τ_g`. Proof: distribute the denominator over the numerator sum and rearrange via `ring` | **Proved** |
| `FinitePartition.equal_groups_weight_eq_pi` | `(∀ g, P.p g = 1/2) → ∀ g, P.overlapWeight g = P.π g` | If every `p_g = 1/2`, the normalized overlap weight equals the cell probability. Proof: each `cellOverlap g = 1/4`, denominator becomes `(1/4)·Σπ_g = 1/4`, and the `1/4`'s cancel | **Proved** |
| `FinitePartition.equal_groups_collapses` | `(∀ g, P.p g = 1/2) → P.overlapWeightedATE = ∑ g, P.π g * P.τ g` | **Equal-cell-share collapse** (finite half of `prop:po-estimand-sloczynski-ols-equal-groups`): if every `p_g = 1/2`, then `β_sat = Σ π_g τ_g`. Proof: rewrite via `finite_weights_eq_sum`, then apply `equal_groups_weight_eq_pi` cell by cell | **Proved** |
| `FinitePartition.homogeneous_collapses` | `(∀ g, P.τ g = τ₀) → P.overlapWeightedATE = τ₀` | **Homogeneous-effect collapse** (saturated half of `prop:po-estimand-sloczynski-ols-homogeneous`): if `τ_g = τ₀` for every cell, then `β_sat = τ₀`. Proof: pull `τ₀` out of the numerator, cancel with the denominator | **Proved** |
| `FinitePartition.perTreatedWeight P g` | `noncomputable FinitePartition 𝒢 → 𝒢 → ℝ` | **Per-treated-observation leverage factor** `1 − p_g` in cell `g` (Remark `rem:po-estimand-sloczynski-ols-group-size`): each treated unit in cell `g` contributes weight proportional to the untreated share | **Defined** |
| `FinitePartition.perUntreatedWeight P g` | `noncomputable FinitePartition 𝒢 → 𝒢 → ℝ` | **Per-untreated-observation leverage factor** `p_g` in cell `g`: each untreated unit in cell `g` contributes weight proportional to the treated share | **Defined** |
| `FinitePartition.cellOverlap_eq_perWeights_mul` | `P.cellOverlap g = P.perUntreatedWeight g * P.perTreatedWeight g` | The cell overlap variance factors as the product of the two per-unit leverage factors. Proof: `unfold` + `ring` | **Proved** |
| `FinitePartition.perTreatedWeight_antitone` | `P.p g ≤ P.p h → P.perTreatedWeight h ≤ P.perTreatedWeight g` | **Headline monotonicity** (`rem:po-estimand-sloczynski-ols-group-size`): the per-treated leverage factor `1 − p_g` is antitone in `p_g` — the smaller the treated share, the larger each treated unit's weight. Formalizes the paper's title "Smaller Groups Get Larger Weights" for the treated side. Proof: `linarith` | **Proved** |
| `FinitePartition.perUntreatedWeight_monotone` | `P.p g ≤ P.p h → P.perUntreatedWeight g ≤ P.perUntreatedWeight h` | The per-untreated leverage factor `p_g` is monotone in `p_g` — the larger the treated share, the larger each untreated unit's weight. Symmetrically to `perTreatedWeight_antitone`. Proof: `exact hph` | **Proved** |
| `OppositeGroupRepr` | `structure { ρ, τ_ATT, τ_ATU, β_ols, w₁, w₀ : ℝ, ρ_nonneg, ρ_le_one, twoComponent : β_ols = w₁·τ_ATT + w₀·τ_ATU, weights_sum_one : w₁ + w₀ = 1, equalDispersion : w₁ = 1−ρ }` | Słoczyński opposite-group representation (`ass:po-estimand-sloczynski-ols-opposite-group`). **G2 closed:** the fields are now the paper's genuine structural assumption — the two-component representation `β_ols = w₁·τ_ATT + w₀·τ_ATU` with `w₁+w₀=1` (recorded separately, per `rem:po-estimand-sloczynski-ols-no-covariate-caveat`, NOT derived from the saturated formula, which is a different estimand) — plus the equal-dispersion specialization `w₁ = 1−ρ`. The opposite-group identity is no longer a field: it is the derived theorem `represents`. The general dispersion-based weight formula is not specified by the source and is not formalized | **Defined** |
| `OppositeGroupRepr.tau_ATE R` | `OppositeGroupRepr → ℝ` | ATE in two-component form, `τ_ATE = ρ·τ_ATT + (1−ρ)·τ_ATU` (`def:po-estimand-sloczynski-ols-att-atu`) | **Defined** |
| `OppositeGroupRepr.w0_eq_rho` | `R.w₀ = R.ρ` | The untreated weight equals the treated share in the equal-dispersion case, forced by `weights_sum_one` and `equalDispersion`. Proof: `rw [equalDispersion]` into `weights_sum_one`, `linarith` | **Proved** |
| `OppositeGroupRepr.represents` | `R.β_ols = (1−R.ρ)·R.τ_ATT + R.ρ·R.τ_ATU` | **Słoczyński opposite-group identity** (`prop:po-estimand-sloczynski-ols-opposite-group`), now **derived** (G2) rather than assumed: substitute the equal-dispersion weights `w₁ = 1−ρ`, `w₀ = ρ` into the two-component representation. Proof: `rw [twoComponent, equalDispersion, w0_eq_rho]` | **Proved** |
| `OppositeGroupRepr.equal_groups_eq_ATE` | `R.ρ = 1/2 → R.β_ols = R.tau_ATE` | **Equal-group-size collapse to the ATE** (top half of `prop:po-estimand-sloczynski-ols-equal-groups`): if `ρ = 1/2`, the opposite-group OLS coefficient equals the ATE. Proof: substitute `ρ = 1/2` into both `represents` and `tau_ATE` and `ring` | **Proved** |
| `OppositeGroupRepr.homogeneous_eq_constant` | `R.τ_ATT = τ₀ → R.τ_ATU = τ₀ → R.β_ols = τ₀` | **Homogeneous-effect collapse for the ATT/ATU representation** (ATT/ATU half of `prop:po-estimand-sloczynski-ols-homogeneous`): if `τ_ATT = τ_ATU = τ₀`, then `β_ols = τ₀` for every `ρ`. Proof: substitute into `represents` and `ring` | **Proved** |

### `OLSWeightDecomposition/GeneralCondExp.lean` — general continuous-covariate theorem via σ-algebra condExp (audit G4)

The abstract-conditioning version of `thm:po-estimand-sloczynski-ols-overlap-weighted-ate` for an *arbitrary* covariate σ-algebra `m` (not the finite partition), using Mathlib's `μ[f|m]` conditional expectation. Closes audit G4 (general continuous-X theorem, previously absent). Binary `D`, propensity `p := μ[D|m]`, residual `D̃ := D − p`. CMI is stated in the mean-independence **factoring form** `μ[D·Y(d)|m] =ᵐ μ[D|m]·μ[Y(d)|m]` — a documented *consequence* (weakening) of the paper's full CMI `E[Y(d)|D,X]=E[Y(d)|X]`, hence faithful. All declarations axiom-free.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `denom_identity` | `(hm : m ≤ mΩ) (D) (hDbin) (hD_int) → ∫ (D − μ[D|m])² = ∫ μ[D|m]·(1 − μ[D|m])` | **Denominator (A).** The residualized treatment variance integrates to the overlap factor. Proof: expand `(D−p)² =ᵐ D − 2pD + p²` (binary `D²=ᵐD`), pull `p` out of `μ[·|m]`, integrate via `integral_condExp` — gives `∫p − ∫p² = ∫p(1−p)` | **Proved** |
| `numer_identity` | `(hm) (D Y Y0 Y1) (hDbin) (integrabilities) (hcons) (hCMI1) (hCMI0) → ∫ (D − μ[D|m])·Y = ∫ μ[D|m]·(1−μ[D|m])·(μ[Y1|m] − μ[Y0|m])` | **Numerator (B).** Proof: `(D−p)·Y =ᵐ (1−p)(D·Y1) − p((1−D)Y0)` by consistency+binary; pull out the `m`-measurable `1−p`,`p`; apply CMI factoring → `∫p(1−p)(M1−M0)` | **Proved** |
| `saturated_ols_overlap_weighted_ate` | `(hm) (D Y Y0 Y1) (hDbin) (integrabilities) (hcons) (hCMI1) (hCMI0) (_hoverlap : 0 < ∫ μ[D|m]·(1−μ[D|m])) →` `∫(D−p)Y / ∫(D−p)² = ∫ p(1−p)(μ[Y1|m]−μ[Y0|m]) / ∫ p(1−p)` | **G4 general overlap-weighted ATE.** The saturated FWL estimand equals the overlap-weighted conditional ATE for arbitrary `X`. Proof: rewrite numerator/denominator by (B),(A). The ratio identity is unconditional; `_hoverlap` is the paper's nondegeneracy regime | **Proved** |

### `OLSWeightDecomposition/OverlapWeightedATE.lean` (+ `Support/`) — probability-space bridge (Layer B)

`OverlapWeightedATE.lean` hosts the finite-cell headline theorem; the supporting measure-theoretic machinery lives in the `Support/` subfolder:

| File | Description |
|---|---|
| `Support/Basic.lean` | Saturated-control class, cell statistics, saturated propensity/mean regression, and elementary cell bounds |
| `Support/Integrals.lean` | Reusable finite-cell integral identities and saturated-sum reduction lemmas |
| `Support/Orthogonality.lean` | Per-cell orthogonality facts for treatment/outcome residuals and the observed-outcome `MemLp` bridge |
| `Support/Partition.lean` | Construction of `partitionOf` and the residualization witnesses `residWitnessD` / `residWitnessY` |
| `Support/PerCell.lean` | Per-cell denominator and numerator identities used in the final bridge proof |
| `OverlapWeightedATE.lean` | Public headline theorem `bridge_finite_residualized_eq_overlap` |

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `saturatedClass μ G G_meas` | `LinearL2Class μ` | Finite span of the cell indicators `𝟙{G=g}` used as the saturated-control nuisance class | **Defined** |
| `cellMass` / `cellShare` / `cellTau` | `Measure Ω → … → 𝒢 → ℝ` | Extract cell probability, treated share, and cell treatment effect from a probability-space representation | **Defined** |
| `propensity` / `meanReg` | `Measure Ω → … → Ω → ℝ` | Cell-wise saturated projections for treatment and outcome, represented as finite sums of cell indicators | **Defined** |
| `partitionOf` | `Measure Ω → … → FinitePartition 𝒢` | Builds the Layer A finite partition from the probability-space cell statistics and overlap hypothesis | **Defined** |
| `residWitnessD` / `residWitnessY` | `ResidualizationWitness μ (saturatedClass μ G G_meas) D/Y` | Packages the saturated residualization witnesses; orthogonality is proved by reducing to per-cell indicator moments | **Proved** |
| `denom_per_cell` | `∫ (D−propensity)^2 · 𝟙{G=g} = cellMass g * cellShare g * (1−cellShare g)` | Per-cell denominator identity for the FWL denominator | **Proved** |
| `num_per_cell` | `∫ (D−propensity) · Y · 𝟙{G=g} = cellMass g * cellShare g * (1−cellShare g) * cellTau g` | Per-cell numerator identity under consistency and finite-cell CMI | **Proved** |
| `bridge_finite_residualized_eq_overlap` | `residualizedCoefficient μ (saturatedClass μ G B_meas_G) (residWitnessY … (Y_memLp_of_consistency …)) (residWitnessD …) = FinitePartition.overlapWeightedATE (partitionOf …)` | Layer B bridge: assumes the finite-cell integrated-indicator condition implied by CMI (not an equivalence to the full conditional-mean clause), sums the per-cell numerator and denominator identities over `g`, removes the saturated outcome projection by residual orthogonality, and identifies the resulting ratio with the Layer A overlap-weighted ATE. **G7 fix:** `Y_memLp` is no longer a hypothesis — derived inline via `Y_memLp_of_consistency` from `B_consis+B_binary+B_Y0_L2+B_Y1_L2`. **G6 fix:** `B_Dtilde_pos` is no longer a hypothesis — denominator positivity derived from `B_overlap` via `hDen_sum`. Signature now has 8 hypotheses (was 10). **G4 (deferred):** general continuous-X theorem for arbitrary covariate deferred per `rem:po-estimand-sloczynski-ols-lean` | **Proved** |

---

## 9i0a-StaggeredTWFEDecomposition. `Panel/EstimandCharacterization/StaggeredTWFEDecomposition/{FinitePanel,Pairwise,AlgebraicDecomposition,CausalDecomposition,PopulationBridge,Causal}.lean` — Goodman-Bacon (2021) TWFE staggered-timing decomposition

Specific-paper specialization of the residualization framework to Goodman-Bacon (2021, *Journal of Econometrics*). Layer A (`FinitePanel.lean`, `Pairwise.lean`, `AlgebraicDecomposition.lean`) is pure finite-cell algebra over a balanced cohort-period law: given a `CohortPanel 𝒢 T` carrying cohort shares `p_g`, adoption dates `A_g ∈ WithTop (Fin T)`, and cell outcome means `Y_{gt}`, the population TWFE coefficient `β^twfe := E[D̃·Y]/E[D̃²]` decomposes as a positively-weighted average of three families of 2×2 DID contrasts (treated-vs-never `Δ^TN`, early-vs-late `Δ^EL`, late-vs-early `Δ^LE`). `Pairwise.lean` is the intermediate algebra layer: it first rewrites denominator/numerator quantities into ordered pairwise centered-treatment contributions, then states the local TN/treated-treated window lemmas needed by `AlgebraicDecomposition.lean`. Layer B (`PopulationBridge.lean`) instantiates the residualization core on a probability space with the balanced cohort-period law and span(cohort + period indicators) as the saturated nuisance class. Layer C (`CausalDecomposition.lean`, `Causal.lean`) promotes the algebraic contrasts to causal statements about window-specific ATTs under consistency, no anticipation, and pairwise untreated parallel trends; the `LE` comparison carries the explicit bad-comparison term that motivates Goodman-Bacon's caveat about TWFE under treatment-effect heterogeneity. All declarations live in `namespace Causalean.Panel.EstimandCharacterization.StaggeredTWFEDecomposition`. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/goodman_bacon_twfe_timing.md`.

### `StaggeredTWFEDecomposition/Panel.lean` — cell-statistics record and primitives (Layer A1–A4)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `CompTag` | `inductive { TN, EL, LE }` | Three-valued tag for the type of admissible 2×2 comparison | **Defined** |
| `CohortPanel 𝒢 T` | `structure { p, A, Y, T_pos, p_pos, p_sum_one }` | Staggered-adoption cohort panel: cohort shares `p_g`, adoption dates `A_g : WithTop (Fin T)` (`⊤ = ∞`), cohort-period factual means `Y_{gt}`, plus positivity and probability-vector constraints | **Defined** |
| `AdoptionDate.{le, lt, isFin, isInf}` | `WithTop (Fin T) → Fin T → Prop` (or `→ Prop`) | Goodman-Bacon local helpers using the same `WithTop (Fin T)` convention as `AdoptionPath`: `A_g ≤ t`, `t < A_g`, `A_g ≠ ⊤`, and `A_g = ⊤` | **Defined** |
| `D` / `barD` / `pCohort` | `CohortPanel 𝒢 T → … → ℝ` | Treatment indicator `D_{gt} = 1_{A_g ≤ t}`, cohort treatment share `\overline{D}_g = T⁻¹ ∑_t D_{gt}`, and overall share `E[D] = ∑_g p_g \overline{D}_g` | **Defined** |
| `Dtilde` | `CohortPanel 𝒢 T → 𝒢 → Fin T → ℝ` | Residualized treatment via the explicit double-demeaning formula `D − \overline{D}_g − E[D∣T=t] + E[D]` | **Defined** |
| `VD` / `betaTWFE` | `CohortPanel 𝒢 T → ℝ` | Residualized-treatment variance `V_D = ∑_{g,t} (p_g/T) D̃_{gt}²` and the TWFE estimand `β^twfe = (∑ (p_g/T) D̃ Y) / V_D` (`def:po-estimand-goodman-bacon-twfe`) | **Defined** |
| `Ybar P g S` | `CohortPanel 𝒢 T → 𝒢 → Finset (Fin T) → ℝ` | Window mean `\overline{Y}_{g,S} = \|S\|⁻¹ ∑_{t ∈ S} Y_{gt}` | **Defined** |
| `S0_TN` / `S1_TN` / `S0_EL` / `S1_EL` / `S0_LE` / `S1_LE` | `CohortPanel 𝒢 T → … → Finset (Fin T)` | Six comparison windows for the three 2×2 comparison types (`def:po-estimand-goodman-bacon-comparisons`) | **Defined** |
| `Δ_TN` / `Δ_EL` / `Δ_LE` | `CohortPanel 𝒢 T → 𝒢 → 𝒢 → ℝ` | The three 2×2 DID contrasts (treated-vs-never, early-vs-late, late-vs-early) | **Defined** |
| `lambdaTN` / `q` / `mu` / `lambdaEL` / `lambdaLE` | `CohortPanel 𝒢 T → … → ℝ` | Raw weights `λ^TN, λ^EL, λ^LE` and the timing-pair gap `q_{eℓ} = \overline{D}_e − \overline{D}_ℓ` plus splitting fraction `μ_{eℓ} = (1−\overline{D}_e)/(1−q)` | **Defined** |
| `Lambda` / `w_TN` / `w_EL` / `w_LE` | `CohortPanel 𝒢 T → … → ℝ` | Aggregate raw-weight denominator `Λ` and the three normalized weights `w = λ/Λ` (`def:po-estimand-goodman-bacon-weights`) | **Defined** |
| `admissible` / `𝒦` | `CohortPanel 𝒢 T → CompTag × 𝒢 × 𝒢 → Prop`, `Finset (CompTag × 𝒢 × 𝒢)` | Admissibility predicate and the comparison index set, filtering admissible tagged cohort pairs | **Defined** |
| `weight` / `contrast` / `lambdaWeight` | `CohortPanel 𝒢 T → CompTag × 𝒢 × 𝒢 → ℝ` | Unified normalized weight, 2×2 contrast, and raw weight on the full index, returning `0` outside the admissible set | **Defined** |

### `StaggeredTWFEDecomposition/Pairwise.lean` — pairwise centered-treatment algebra (Layer A intermediate)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `centeredD P g t` | `CohortPanel 𝒢 T → 𝒢 → Fin T → ℝ` | Cohort-demeaned treatment path `D_{gt} − \overline D_g`; the object centered across cohorts at each period before applying the finite weighted covariance/variance identities | **Defined** |
| `vdPairContribution P g u` | `CohortPanel 𝒢 T → 𝒢 → 𝒢 → ℝ` | Ordered-pair contribution `(p_g p_u)/(2T) · Σ_t (centeredD_g(t) − centeredD_u(t))²` in the pairwise representation of `V_D` | **Defined** |
| `numPairContribution P g u` | `CohortPanel 𝒢 T → 𝒢 → 𝒢 → ℝ` | Ordered-pair contribution `(p_g p_u)/(2T) · Σ_t (centeredD_g(t) − centeredD_u(t))·(Y_g(t) − Y_u(t))` in the pairwise representation of the TWFE numerator | **Defined** |
| `Dtilde_eq_centeredD_sub_weighted_mean` | `Dtilde P g t = centeredD P g t − ∑ h, P.p h * centeredD P h t` | Rewrites the explicit double-demeaning formula as cohort-weighted centering of the cohort-demeaned treatment path at each period | **Proved** |
| `VD_eq_pairwise_centeredD` | `VD P = ∑ g, ∑ u, vdPairContribution P g u` | First-stage denominator rewrite via `NormalizedWeights.weighted_center_var`; after this, `Decomposition.lean` only needs adoption-window grouping | **Proved** |
| `twfe_numerator_eq_pairwise_centeredD_Y` | `∑ (p_g/T)·D̃·Y = ∑ g, ∑ u, numPairContribution P g u` | First-stage numerator rewrite via `NormalizedWeights.weighted_center_cov`; after this, `Decomposition.lean` only needs adoption-window grouping | **Proved** |
| `TN_pair_vd_contribution_eq_gap` | pairwise TN denominator contribution equals the treated-vs-never raw gap factor | Local denominator window algebra for a treated-vs-never pair; now factors through `vdPairContribution_add_swap`, `TN_centeredD_gap_eq`, and the proved `TN_time_variance_eq_gap` Bernoulli-variance helper | **Proved** |
| `TT_pair_vd_contribution_eq_gap` | pairwise treated-treated denominator contribution equals the raw timing-pair gap factor | Local denominator window algebra for a treated-treated ordered timing pair; proves the early-late treatment gap is the middle-window indicator and applies the finite binary-variance helper | **Proved** |
| `TN_pair_contribution_eq_lambda_delta` | pairwise TN numerator contribution equals `λ_TN·Δ_TN` | Local numerator window algebra for a treated-vs-never pair; splits the centered binary covariance over `S0_TN`/`S1_TN` and rewrites the restricted sums as `Ybar` means | **Proved** |
| `TT_pair_contribution_eq_lambda_delta_sum` | pairwise treated-treated numerator contribution equals `λ_EL·Δ_EL + λ_LE·Δ_LE` | Local numerator window algebra for a treated-treated timing pair; partitions time into pre-early, middle, and post-late windows, uses the complement mean decomposition, and discharges the `mu` cardinal-share algebra | **Proved** |

### `StaggeredTWFEDecomposition/Decomposition.lean` — five Layer A theorems (A5.1–A5.5)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `weights_nonneg` | `0 ≤ lambdaWeight P k` | All admissible raw weights are nonnegative. Proof: case-split on `CompTag`; uses `0 ≤ p_g`, `0 ≤ \overline{D}_g ≤ 1`, and the cardinality bounds on `q_{eℓ}` | **Proved** |
| `raw_weight_sum_eq_VD` | `Lambda P = VD P` | **Denominator identity** of `thm:po-estimand-goodman-bacon-decomposition`: the residualized-treatment variance equals the sum of all pairwise raw weights (key algebraic content underlying the FWL denominator step) | `sorry` (placeholder) |
| `twfe_numerator_eq_lambda_delta_sum` | `∑ (p_g/T)·D̃·Y = ∑ λ^TN·Δ^TN + ∑ (λ^EL·Δ^EL + λ^LE·Δ^LE)` | **Numerator identity**: TWFE numerator decomposes by unordered cohort pairs into raw-weight × 2×2 DID contributions | `sorry` (placeholder) |
| `weights_sum_one` | `0 < VD P → ∑ k ∈ 𝒦 P, weight P k = 1` | Normalized comparison weights sum to one. Proof: divide `raw_weight_sum_eq_VD` by `Λ = V_D > 0` | **Proved** (modulo `raw_weight_sum_eq_VD`) |
| `twfe_eq_weighted_avg` | `betaTWFE P = ∑ k ∈ 𝒦 P, weight P k * contrast P k` | **Headline theorem** (`thm:po-estimand-goodman-bacon-decomposition`): TWFE coefficient is the weighted average of the 2×2 DID contrasts, with the `VD P = 0` case handled by Lean's totalized division. Proof combines A5.2 + A5.3 with the definition of `weight` | **Proved** (modulo A5.2/A5.3) |

### `StaggeredTWFEDecomposition/Bridge/{Basic,Integrals,Orthogonality,Partition,PerCell}.lean` + `StaggeredTWFEDecomposition/Bridge.lean` — probability-space bridge (Layer B)

Layer B is split across five sub-files (mirroring Sloczynski's Bridge layout) plus a thin facade: `Bridge/Basic.lean` holds the panel class, cell statistics, and projections; `Bridge/Integrals.lean` holds reusable cell integral identities and the per-axis-to-whole-class orthogonality decoupler; `Bridge/Orthogonality.lean` holds per-axis residual orthogonality (under the balanced-cell hypothesis `IsBalancedPanelLaw`); `Bridge/Partition.lean` builds the `panelOf` Layer A panel and the residualization witnesses; `Bridge/PerCell.lean` proves the per-cohort-period denominator and numerator identities; `Bridge.lean` states the headline.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `panelClass μ G T_rv G_meas T_meas` | `LinearL2Class μ` | Linear `L²` class spanning (a.e.) the family of cohort indicators `𝟙{G = g}` and period indicators `𝟙{T_rv = t}`. Membership predicate uses two coefficient maps `cG : 𝒢 → ℝ` and `cT : Fin T → ℝ` (B1) | **Defined** |
| `cellMass` / `cohortMass` / `periodMass` / `cellMean` / `cohortBarD` | `Measure Ω → … → ℝ` | Extract cohort-period cell mass `μ{G=g, T=t}.toReal`, cohort mass, period mass, cell mean of `Y`, and cohort treatment share from a probability-space representation (B2) | **Defined** |
| `panelOf` | `Measure Ω → … → CohortPanel 𝒢 T` | Builds the Layer A `CohortPanel` from the probability-space cell statistics plus an explicit adoption date `A : 𝒢 → WithTop (Fin T)` and the balanced-law side conditions (B2) | **Defined** |
| `panelPropensity` / `panelMeanReg` | `Measure Ω → … → Ω → ℝ` | Cohort + period saturated projections for treatment and outcome (the closed-form residual via two-way demeaning); each lies in `panelClass` (B3) | **Defined** |
| `panelPropensity_mem_panelClass` / `panelMeanReg_mem_panelClass` | `(panelClass μ G T_rv G_meas T_meas).mem (panelPropensity μ D G T_rv)` (resp. `panelMeanReg`) | Membership lemmas for the projections, with explicit `(cG, cT)` coefficient maps | **Proved** |
| `IsBalancedPanelLaw μ G T_rv` | `∀ g t, cellMass = cohortMass · periodMass` | The balanced cohort-period law hypothesis; Lean form of Goodman-Bacon (2021)'s `ℙ(G=g, T=t) = p_g · q_t`. Used as `B_balanced` throughout the Layer B stack | **Defined** |
| `indicator_{cohort,period,panel_cell}_memLp` | `MemLp (𝟙_{cell}) 2 μ` | Cell indicators are square-integrable on a probability measure | **Proved** |
| `integral_{cohort,period,panel_cell}_indicator_one_eq_{cohort,period,cell}Mass` | `∫ 𝟙_{cell} dμ = mass` | Indicator integrates to its cell mass | **Proved** |
| `{cohort,period,panel_cell}_integral_div_mul_{cohort,period,cell}Mass` | `(∫ F·𝟙_{cell} dμ / mass) · mass = ∫ F·𝟙_{cell} dμ` | Division-multiplication cancels even at zero-mass cells (a.e.-vanishing indicator) | **Proved** |
| `cohortBarD_mul_cohortMass` | `cohortBarD g · cohortMass g = ∫ D · 𝟙{G=g} dμ` | Defining identity of `cohortBarD` after multiplying back the cohort mass | **Proved** |
| `panelPropensity_eq_hat_of_mem` / `panelMeanReg_eq_hat_of_mem` | On cell `{G=g ∧ T_rv=t}`, projection equals the closed-form `panelPropensityHat` / `panelMeanRegHat` constant | Cell-constancy identities for the in-class projections; the two-axis analogues of Sloczynski's `propensity_eq_cellShare_of_mem` | **Proved** |
| `integral_mul_panelClass_eq_zero_of_axes` | per-axis orthogonality ⇒ orthogonality against every member of `panelClass` | Decouples the cohort-axis and period-axis orthogonality from the witness-level obligation. Proof: split `V · (Σ_g cG · 𝟙{G=g} + Σ_t cT · 𝟙{T_rv=t})` into two finite sums and apply per-axis vanishing | **Proved** |
| `integral_eq_sum_{cohort,period,panel_cell}` | `∫ F dμ = ∑ over cells ∫ F · 𝟙_{cell} dμ` | Integrate by summing over cells of the appropriate axis(es) | **Proved** |
| `residD_{cohort,period}_orthogonal` / `residY_{cohort,period}_orthogonal` | `∫ Vtilde · 𝟙_{axis} dμ = 0` | Per-axis orthogonality of the `D` / `Y` residual against cohort and period indicators. Cohort axis is automatic from the defining identity of `cohortBarD`; period axis uses `IsBalancedPanelLaw` to make the cross-cohort sum collapse to `∫ D dμ` (resp. `∫ Y dμ`) | **Proved** |
| `residWitnessD_panel` / `residWitnessY_panel` | `ResidualizationWitness μ (panelClass μ G T_rv G_meas T_meas) D/Y` | Full residualization witness against `panelClass`. Orthogonality field discharged via `integral_mul_panelClass_eq_zero_of_axes` plus the four per-axis lemmas, taking `IsBalancedPanelLaw` as input | **Proved** |
| `panelDtilde μ D G T_rv g t` | `(∫ D · 𝟙_{cell} dμ / cellMass) − panelPropensityHat g t` | Cell-level residualized-treatment value; on `panelOf …` panels equals the Layer A `Dtilde` under the side conditions in the bridge theorem | **Defined** |
| `denom_per_cell_panel` | `∫ Vtilde² · 𝟙_{cell g t} dμ = cellMass · panelDtilde²` | Per-cell denominator identity: Vtilde is a.e. constant equal to `panelDtilde` on each cell (by the cell-constancy hypothesis on `D` and `panelPropensity_eq_hat_of_mem`); integrate against the cell indicator | **Proved** |
| `num_per_cell_panel` | `∫ Vtilde · Y · 𝟙_{cell g t} dμ = cellMass · panelDtilde · cellMean` | Per-cell numerator identity: same constancy argument as `denom_per_cell_panel`, then use `panel_cell_integral_div_mul_cellMass` to identify `∫ Y · 𝟙_{cell} dμ = cellMass · cellMean` | **Proved** |
| `bridge_Dtilde_sq_eq_VD` | `∫ wD.Vtilde² dμ = VD (panelOf …)` | Companion B4 identity: the FWL denominator equals the Layer A `V_D`. Proof: `integral_eq_sum_panel_cell` ⊕ `denom_per_cell_panel` per cell ⊕ `hLaw` (`cellMass = cohortMass / T`) ⊕ `hDtilde_eq` (`panelDtilde = Layer A Dtilde`) ⊕ `VD` definition | **Proved** |
| `bridge_VD_pos_iff_Dtilde_sq_pos` | `0 < VD (panelOf …) ↔ 0 < ∫ wD.Vtilde² dμ` | G9 bridge: `VD P > 0` iff the FWL denominator is positive (immediate via `bridge_Dtilde_sq_eq_VD`); lets callers derive `hDtilde_pos` from `hVD_pos` | **Proved** |
| `bridge_finite_residualized_eq_twfe` | `residualizedCoefficient μ panelClass wY wD = betaTWFE (panelOf …)` | **Headline B4**: the population FWL coefficient on the panel class equals the Layer A finite-cell `betaTWFE`. Proof: replace `wY.Vtilde` by `Y` in the numerator using `wD.orthogonal panelMeanReg_mem_panelClass`; expand both numerator and denominator by `integral_eq_sum_panel_cell` ⊕ per-cell identities; substitute `hLaw` / `hDtilde_eq`; identify with `betaTWFE` definition | **Proved** |

### `StaggeredTWFEDecomposition/Causal.lean` — causal characterization corollaries (Layer C)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Ybar0` | `(𝒢 → Fin T → ℝ) → 𝒢 → Finset (Fin T) → ℝ` | Window mean of a potential-outcome map (e.g. `Y(∞)` or `Y(A_g)`); same shape as `Ybar` but operates on a generic potential-outcome map rather than `P.Y` | **Defined** |
| `ATT_window` | `(𝒢 → Fin T → ℝ) → (𝒢 → Fin T → ℝ) → 𝒢 → Finset (Fin T) → ℝ` | Window-specific ATT `\|S\|⁻¹ ∑_{t ∈ S} (Y1_{gt} − Y0_{gt})` in the two-state layer where `Y0 = Y(∞)` and `Y1 = Y(A_g)` (`def:po-estimand-goodman-bacon-window-att`) | **Defined** |
| `CausalAssumptions P Y0 Y1` | `Prop` | Bundles consistency on treated/untreated cells, no anticipation `t < A_g → Y1_{gt} = Y0_{gt}`, and three pairwise untreated-parallel-trends fields (one per comparison type, matching the LaTeX `ass:po-estimand-goodman-bacon-causal`); this is the two-state simplification sufficient for the LE bad-comparison corollary, not the full `Y_gt(a)` adoption-date-indexed family | **Defined** |
| `CausalAssumptions.AdoptionDate.not_le_of_lt` / `lt_of_isInf` | `WithTop`-order helpers | `AdoptionDate.lt` rules out `AdoptionDate.le`; never-treated cohorts are untreated in every period | **Proved** |
| `CausalAssumptions.Ybar_eq_Ybar0_on_S0_TN` / `_of_inf` / `_on_S0_EL` / `_late_on_S1_EL` / `_late_on_S0_LE` | `Ybar P g S = Ybar0 Y0 g S` (under the appropriate side conditions) | Cell-by-cell consistency reduces factual `Ybar` to never-treated `Ybar0` on each cohort-window pair where the cohort is untreated | **Proved** |
| `CausalAssumptions.Ybar_eq_Ybar0_add_ATT_of_treated` | `(∀ t ∈ S, A_g ≤ t) → Ybar P g S = Ybar0 Y0 g S + ATT_window Y0 Y1 g S` | On a treated window, factual `Ybar` decomposes as never-treated `Ybar0` plus the window ATT. Used to extract ATT contributions from each comparison's treated arm | **Proved** |
| `Δ_TN_eq_ATT` | `Δ_TN P g u = ATT_window Y0 Y1 g (S1_TN P g)` | **Layer C corollary 1** (`cor:po-estimand-goodman-bacon-causal-characterization`): under `CausalAssumptions`, the treated-vs-never contrast identifies the treated cohort's window ATT. Proof: reduce `Ybar` of untreated arms to `Ybar0`, apply `parallelTrends_TN` to cancel untreated trends, isolate the treated-arm ATT contribution | **Proved** |
| `Δ_EL_eq_ATT` | `Δ_EL P e ℓ = ATT_window Y0 Y1 e (S1_EL P e ℓ)` | **Layer C corollary 2**: the early-vs-late-before-late contrast identifies the early cohort's window ATT. Proof: same shape as `Δ_TN_eq_ATT`; uses the late-cohort untreated identity on both EL windows and `parallelTrends_EL` | **Proved** |
| `Δ_LE_eq_bad_comparison` | `Δ_LE P e ℓ = ATT_window Y0 Y1 ℓ (S1_LE P e ℓ) − (ATT_window Y0 Y1 e (S1_LE P e ℓ) − ATT_window Y0 Y1 e (S0_LE P e ℓ))` | **Layer C corollary 3**: the late-vs-early-after-early contrast identifies the late cohort's ATT *minus* a bad-comparison term — the change in the already-treated early cohort's ATT across the comparison windows. Proof: decompose both arms' factual `Ybar` into `Ybar0 + ATT_window` (each cell is treated for both `e` and `ℓ` in the appropriate windows), apply `parallelTrends_LE` to cancel untreated trends, isolate the ATT residuals | **Proved** |

---

## 9i0a-EventStudyContamination. `Panel/EstimandCharacterization/EventStudyContamination/{Setup,Conventional,CellGrid,Contamination,InteractionWeighted,Basic}.lean` — Sun-Abraham (2021) Event-Study Contamination and IW

Specific-paper finite-cell formalization for Sun-Abraham (2021). `Setup.lean` defines a finite staggered-adoption event-study system with adoption paths encoded as `WithTop (Fin T)`, admissible cohort-relative-time cells, definitional CATT primitives, and the named causal restrictions. `Conventional.lean` proves the residualized-design weight normalizations, conventional TWFE contamination representation, and apparent-pretrends corollary using finite-cell residualized indicator quantities; it consumes the FWL ratio `D.mu = conventionalMuRatio` and the `ConventionalResidualization` orthogonality conditions as hypotheses. `CellGrid.lean` **derives** both: it builds a genuine weighted-support projection on the cohort-relative-time cell grid (`Causalean/Panel/Weighted/*`) and proves `cellGrid_mu_eq_conventionalMuRatio` (closes gap G3) and `cellGrid_provides_residualization` (closes gaps G4/G9) — replacing the previously-assumed `D.mu = beta` bridge field and the assumed orthogonality fields. `Contamination.lean` exposes the de-scaffolded public theorems that take the genuine `CellGridResidualization` input directly. `InteractionWeighted.lean` keeps the IW DID construction separate and proves the cohort-level DID equality plus convex IW aggregation. `Basic.lean` is the import facade. Companion NL doc: `doc/basic_concepts/po/estimand_characterization/sun_abraham_event_study.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `EventStudyContamination.EventStudySystem` | `structure (T : ℕ) { time, cohorts, cohortShare, cellMass, observedPathMean, observedMean, treatedMean, untreatedMean, untreatedPathMean }` | Finite staggered-adoption event-study system with integer period map, finite cohort support, `WithTop (Fin T)` adoption paths (`⊤` as never treated), cell masses, observed means, treated potential-outcome means, and never-treated potential-outcome means | **Defined** |
| `EventStudySystem.finitePath` / `isNeverTreated` / `isEventuallyTreated` / `relTime` / `absorbingTreatment` | adoption-path and relative-time helpers | Sun-Abraham-compatible wrappers around `AdoptionPath` plus relative event time; encode finite adoption paths, `∞`, eventual treatment, and the absorbing treatment path `1{h ≤ t}` | **Defined** |
| `EventStudySystem.targetPeriods` / `baselinePeriods` / `AdmissibleCell` / `admissibleCells` / `cohortsAtEvent` | finite support helpers | Build finite target/baseline period sets, admissible cohort-relative-time cells, finite cell supports, and `G_e` cohort sets from an explicit event-time support | **Defined** |
| `EventStudySystem.cellMassAtEvent` / `observedCellMean` / `meanCellContrast` / `CATT` / `pathTargetMean` / `pathBaselineMean` | finite-cell averages | Provide the finite mass, observed mean, treated-minus-untreated contrast, definitional CATT `CATT(g,e) = meanCellContrast(g,e)`, and path-level target/baseline means used by conventional and IW statements; this CATT averages over all periods realizing `(g,e)` and coincides with the source point-period CATT when that target-period set is singleton | **Defined** |
| `EventStudySystem.Consistency` / `NoAnticipation` / `MeanParallelUntreated` / `EventStudyCausalRestrictions` | propositions/structure | Named Sun-Abraham causal restrictions: consistency, no anticipation, and mean parallel untreated paths whose additive component is stated using `AdditiveSpan.IsUnitTimeAdditive` | **Defined** |
| `EventStudySystem.CATT_eq_zero_of_noAnticipation` | `NoAnticipation → e < 0 → CATT g e = 0` | No-anticipation implication that pre-treatment CATTs vanish | **Proved** |
| `EventStudySystem.observedCellMean_eq_fixedEffects_add_CATT` | `EventStudyCausalRestrictions → ∃ alpha lambda, observedCellMean = card⁻¹ * ∑ target (alpha g + lambda t) + CATT(g,e)` | Averaged cell-mean decomposition used in the contamination proof | **Proved** |
| `EventStudySystem.ConventionalDesign` | `structure { eventSupport, includedEvents, omittedEvent, displayedEvent, Rdot, mu }` | Finite conventional event-study design for the coefficient on `R^l`, carrying included/omitted relative times, residualized indicator values, and `mu_l` | **Defined** |
| `EventStudySystem.cellAverage` / `IsEventStudyNuisance` / `ConventionalResidualization` / `FiniteIntegrable` | finite residualization hypotheses | Cell-average helper for nuisance functions, finite-cell nuisance span with shared `AdditiveSpan` unit/time base plus Sun-Abraham included event indicators, the residualized-design orthogonality conditions (now derived — see `cellGrid_provides_residualization`), and the finite integrability side condition | **Defined** |
| `EventStudySystem.residualDenom` / `residualNumerator` / `conventionalMuRatio` / `omega` | finite residualized design quantities | Denominator `E[Rdot^l R^l]`, numerator `E[Rdot^l Y]`, residualized coefficient ratio, and Sun-Abraham cell weights `omega(l,g,e)` | **Defined** |
| `EventStudySystem.desired_event_weights_sum_one` | `∑_{g ∈ G_l} omega(l,g,l) = 1` | Desired-event-time weight normalization | **Proved** |
| `EventStudySystem.other_included_event_weights_sum_zero` | `e ∈ L → e ≠ l → ∑_{g ∈ G_e} omega(l,g,e) = 0` | Zero total weight on other included event-time indicators | **Proved** |
| `EventStudySystem.contamination_representation` | `mu_l = ∑_{(g,e)∈C} omega(l,g,e) * CATT(g,e)` | Conventional Sun-Abraham TWFE event-study contamination representation, taking the FWL ratio `D.mu = conventionalMuRatio D` and the `ConventionalResidualization` orthogonality conditions as hypotheses (both now genuinely derivable via `CellGrid.lean`) | **Proved** |
| `EventStudySystem.contamination_representation_split` | desired-event component plus `e ≠ l` contamination component | Split form of the contamination representation | **Proved** |
| `EventStudySystem.apparent_pretrends_from_post_treatment` | lead coefficient equals the sum over cells with `e ≥ 0` under `EventStudyCausalRestrictions` and a negative displayed event time | Apparent-pretrends corollary; derives vanishing pre-treatment CATTs from no anticipation | **Proved** |
| `EventStudySystem.CellIndex` / `cellTotalMass` / `cellSupport` (`CellGrid.lean`) | cell-grid empirical population | The admissible-cell subtype index, total admissible cell mass `Z`, and the `WeightedSupport` over the cell grid with weight `cellMassAtEvent / Z` (requires positive cell masses + nonempty admissible set) | **Defined** |
| `EventStudySystem.ip_cellSupport` (`CellGrid.lean`) | `cellSupport.ip A∘val B∘val = (∑ cellMassAtEvent·A·B)/Z` | The cell-grid weighted inner product reproduces the finite-cell sum (divided by total mass) — the bridge turning weighted-projection facts into the finite-cell orthogonality conditions | **Proved** |
| `EventStudySystem.cellNuisance` / `cellAverage_mem_cellNuisance` / `eventIndicator_mem_cellNuisance` (`CellGrid.lean`) | cell-nuisance subspace + memberships | Span of cell-evaluated event-study nuisance functions; every cell-evaluated nuisance and every non-displayed included relative-time indicator lies in it | **Defined**/**Proved** |
| `EventStudySystem.sum_admissible_mul_eventIndicator` (`CellGrid.lean`) | `∑ admissible f·1{e=e'} = ∑_{G_{e'}} f g e'` | Cell-grid filtering identity; the engine behind `hDisplayedExpansion` and `hOtherIncludedOrthogonal` | **Proved** |
| `EventStudySystem.cellRegressor` / `cellOutcome` / `CellGridResidualization` (`CellGrid.lean`) | genuine residualization input | Displayed-event indicator and observed-cell-mean as cell functions, and the honest hypothesis bundle: positive cell masses, `Rdot` **is** the weighted residual of the regressor against the nuisance span, and `D.mu` solves the weighted normal equations | **Defined** |
| `EventStudySystem.cellGrid_mu_eq_conventionalMuRatio` (`CellGrid.lean`) | `CellGridResidualization → D.mu = conventionalMuRatio D` | **Closes gap G3**: derives the FWL ratio from the weighted normal equations via `WeightedSupport.scalar_fwl_of_normalEqs` (no assumed `D.mu = beta`) | **Proved** |
| `EventStudySystem.cellGrid_provides_residualization` (`CellGrid.lean`) | `CellGridResidualization → ConventionalResidualization D` | **Closes gaps G4/G9**: derives the three finite-cell orthogonality conditions from `residualize_in_orthogonal` + the filtering identity (no assumed orthogonality fields) | **Proved** |
| `EventStudySystem.contamination_representation_of_cellGrid` / `contamination_representation_split_of_cellGrid` / `apparent_pretrends_from_post_treatment_of_cellGrid` (`Contamination.lean`) | genuine public entry points taking `CellGridResidualization` | De-scaffolded contamination theorems: take the genuine cell-grid projection input and derive `ConventionalResidualization` + the FWL ratio internally before applying the finite-cell contamination algebra | **Proved** |
| `EventStudyContamination.PathConsistency` / `pathConsistency_observed_eq_untreated` | `absorbingTreatment h t = 0 → observedPathMean h t = untreatedPathMean h t` | Path-level consistency primitive + lemma converting an untreated comparison path's observed mean to its never-treated potential-outcome mean; now DERIVES the IW observed/untreated bridge (replacing two assumed `IWSupport` fields) | **Defined**/**Proved** |
| `EventStudyContamination.targetPeriods_subsingleton_of_injective` / `CATT_eq_sourceCATT_of_injective` | `Function.Injective P.time → CATT g e = treatedMean g t − untreatedMean g t` at the unique `t∈targetPeriods g e` | Under injective `time`, the cell-averaged CATT collapses to the source point value CATT_{g,e} at period g+e | **Proved** |
| `EventStudyContamination.sum_convex_mem_Icc` | nonneg weights summing to one keep a weighted average in `[lo,hi]` | Finite convex-combination bound helper | **Proved** |
| `EventStudySystem.IWDesign` / `IWSupport` / `IWComparisonParallelTrends` / `IWFiniteIntegrable` | IW DID data and assumptions | Interaction-weighted cohort set, comparison groups, aggregation weights, support restrictions, comparison-group parallel trends, and finite integrability side condition. NOTE: the former observed/untreated bridge fields `hComparisonObservedBaseline`/`hComparisonObservedTarget` were dropped from `IWSupport` — now derived via `PathConsistency` | **Defined** |
| `EventStudySystem.observedTargetMean` / `observedBaselineMean` / `comparisonMass` / `comparisonMeanChange` / `DIDContrast` / `Delta` / `nuIW` | IW estimand primitives | Finite treated-cohort target/baseline means, comparison-group population mass, population-share weighted comparison-group mean change, cohort-specific DID contrast, definitional `Delta(g,l) = DIDContrast(g,l)`, and interaction-weighted event-study estimand `nu_l^IW` | **Defined** |
| `EventStudySystem.IW_Delta_eq_CATT` | `PathConsistency → Delta(g,l) = CATT(g,l)` | Cohort-specific IW DID identification under consistency, no anticipation, IW parallel trends, support, path consistency, and finite integrability | **Proved** |
| `EventStudySystem.IW_convex_characterization` | `PathConsistency → hLo → hHi → (∀ g ∈ G_l^IW, Delta(g,l)=CATT(g,l)) ∧ nu_l^IW = ∑ rho(g,l)*CATT(g,l) ∧ lo ≤ nuIW ∧ nuIW ≤ hi` | Convex IW characterization with nonnegative weights summing to one, no contamination from `e ≠ l`, and a 4-way conjunction: now also takes `PathConsistency` and value bounds `hLo`/`hHi` and concludes `lo ≤ nuIW ∧ nuIW ≤ hi` (IW is genuinely a CONVEX combination of target CATTs — the paper's selling point, using the nonneg/sum-one weight hypotheses) | **Proved** |

---

## 9i0b. `PO/ID/Exact/RDD/RDDLimits.lean` — One-sided Limit Identification Engine

Generic topology + measure-theory engine shared by SharpRDD and FuzzyRDD.
All declarations live in `namespace Causalean.PO.RDDLimits`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `oneSidedLimit_eq_right` | `f =ᵐ[π.restrict (Ici c)] g → ContinuousAt g c → support_right → Tendsto f (𝓝[>] c) (𝓝 L) → L = g c` | If `f` agrees a.e. with a continuous `g` on `Ici c` and there is positive mass to the right of `c`, any right-limit `L` of `f` at `c` equals `g c` | **Proved** |
| `oneSidedLimit_eq_left` | symmetric form for `Iio c` / `𝓝[<] c` | Left-side analogue of `oneSidedLimit_eq_right` | **Proved** |
| `value_eq_of_aeEq_right` | `f =ᵐ[π.restrict (Ici c)] g → ContinuousAt f c → ContinuousAt g c → support_right → f c = g c` | If both functions are continuous at `c` and agree a.e. on `Ici c` with positive right support, they are pointwise equal at `c` | **Proved** |
| `value_eq_of_aeEq_left` | symmetric form | Left-side analogue | **Proved** |

---

## 9i'. `PO/ID/Exact/RDD/SharpRDD.lean` — Sharp RDD Identification (PO framework)

Formalizes the sharp regression discontinuity example from `Basic Concepts.tex`
(`def:po-sharp-rdd-system`, `def:po-sharp-rdd-assumptions`, and
`prop:po-sharp-rdd`) in the bare potential-outcome framework.  This is the
first exact-ID example in the project to expose pointwise regression-function
representatives and one-sided topological limits at a cutoff.  The proof
faithfully derives `ν =ᵐ[π.restrict] μ_d` on each half-line from sharp cutoff +
consistency + the integral characterisation of regression representatives,
then upgrades a.e. agreement to limit equality via two-sided local support.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `IsRegressionFunction` | imported from `PO/Analysis/Regression.lean` | Lightweight primitive for a regression representative `f(x) = E[g \| X=x]`; see `## 9i0. PO/Analysis/Regression.lean` for the full declaration and derived lemmas | **Imported** |
| `POSharpRDDSystem P` | `structure { Xvar, Dvar, Yvar, c, hYD }` | Sharp RDD subsystem: real running variable `X`, binary treatment `D`, real outcome `Y`, cutoff `c`, and distinctness of `Y` from the treatment node (for consistency rewrites) | **Defined** |
| `POSharpRDDSystem.{factualX, factualD, factualY}` | `P.Ω → ℝ` / `P.Ω → Bool` / `P.Ω → ℝ` | Empty-regime factual running variable, treatment, and outcome | **Defined** |
| `POSharpRDDSystem.YofD` | `Bool → P.Ω → ℝ` | Treatment-specific potential outcome `Y(d) := Yvar.cfUnder Dvar d` | **Defined** |
| `POSharpRDDSystem.{measurable_factualX, measurable_factualD, measurable_factualY, measurable_YofD}` | `Measurable …` | Basic measurability lemmas for factual and counterfactual maps | **Proved** |
| `POSharpRDDSystem.dEvent` | `Bool → Set P.Ω` | Factual treatment event `{D = d}` | **Defined** |
| `POSharpRDDSystem.Assumptions S` | `structure { consistency, sharpCutoff, mu, nu, mu_isReg, nu_isReg, mu_continuousAt, support_right, support_left, nu_right_limit_exists, nu_left_limit_exists }` | Sharp RDD assumptions: consistency, a.s. sharp cutoff `D = 1{X ≥ c}`, potential-outcome regressions `μ_d` and observable regression `ν` (each a witnessed `IsRegressionFunction`), continuity of each `μ_d` at `c`, per-`ε > 0` positive `(P.μ.map X)`-mass on every one-sided open neighborhood of `c` (two-sided local support), and existence of both one-sided limits of `ν` at `c` (matching the classical `lim` notation in the textbook statement) | **Defined** |
| `POSharpRDDSystem.tau_RDD` | `S.Assumptions → ℝ` | Cutoff-local estimand `μ_1(c) - μ_0(c)`, by definition of the chosen treatment-specific regression representatives | **Defined** |
| `POSharpRDDSystem.{nu_right_limit, nu_left_limit}` | `S.Assumptions → ℝ` | Chosen witnesses of `lim_{x↓c} ν(x)` and `lim_{x↑c} ν(x)` via `Classical.choose` of the existence fields | **Defined** |
| `POSharpRDDSystem.{tendsto_nu_right_limit, tendsto_nu_left_limit}` | `Tendsto ν (𝓝[>] c) (𝓝 (nu_right_limit hA))` etc. | Spec lemmas: the chosen witnesses are genuine one-sided limits | **Proved** |
| private slice-bridge lemmas | — | Sharp cutoff + consistency: on every measurable slice `X⁻¹ A` with `A ⊆ Ici c` (resp. `A ⊆ Iio c`), `factualY` agrees a.e. with `Y(1)` (resp. `Y(0)`), so the corresponding integrals — and hence the integral characterisations of `ν` and `μ_d` — coincide on these slices | **Proved** |
| private pushforward bridges | `setIntegral_pushforward_eq_right/left` | Translate the slice-level integral identity to the pushforward `(P.μ.map X)` via `setIntegral_map`: integrals of `ν` and `μ_d` agree on every measurable subset of the corresponding half-line | **Proved** |
| private a.e. agreement | `nu_eq_mu_true_ae_restrict_Ici`, `nu_eq_mu_false_ae_restrict_Iio` | Apply `Integrable.ae_eq_of_forall_setIntegral_eq` on the restricted pushforward to upgrade the integral identity to `ν =ᵐ[(P.μ.map X).restrict (Ici c)] μ_1` and the symmetric left version | **Proved** |
| private extraction lemmas | `exists_eq_in_Ioo_right/left` | For every `ε > 0`, the support assumption combined with a.e. agreement yields a point in `(c, c+ε)` (resp. `(c-ε, c)`) where `ν = μ_d`, via `Measure.exists_mem_of_measure_ne_zero_of_ae` | **Proved** |
| private NeBot lemmas | `neBot_right/left` | The agreement-restricted right (resp. left) neighborhood filter `𝓝[>] c ⊓ 𝓟 {ν = μ_d}` is non-trivial | **Proved** |
| `POSharpRDDSystem.nu_right_limit_eq` | `Tendsto ν (𝓝[>] c) (𝓝 L) → L = μ_1 c` | Any right-limit of the observable regression at the cutoff equals `μ_1 c`; proved by uniqueness of limits along the agreement subfilter, using continuity of `μ_1` | **Proved** |
| `POSharpRDDSystem.nu_left_limit_eq` | `Tendsto ν (𝓝[<] c) (𝓝 L) → L = μ_0 c` | Symmetric left-limit identification | **Proved** |
| `POSharpRDDSystem.rdd_identification` | `tau_RDD hA = nu_right_limit hA - nu_left_limit hA` | **Sharp RDD identification at the cutoff** (textbook form of `prop:po-sharp-rdd`): the cutoff-local effect equals the difference of the one-sided observable regression limits, `τ_RDD(c) = lim_{x↓c} ν(x) - lim_{x↑c} ν(x)` | **Proved** |

---

## 9i''. `PO/ID/Exact/RDD/FuzzyRDD.lean` — Fuzzy RDD Identification (PO framework)

Formalizes the fuzzy regression discontinuity example from `Basic Concepts.tex`
(`def:po-fuzzy-rdd-system`, `def:po-fuzzy-rdd-assumptions`, and
`prop:po-fuzzy-rdd`) in the bare potential-outcome framework.  The Lean theorem
uses the regression-representative Wald form described in the implementation
plan: `μ_D(z, ·)` represents `E[D(z) | X=·]`, `μ_Y(z, ·)` represents
`E[Y(D(z)) | X=·]`, and the observable regressions `ν_D`, `ν_Y` are identified
with the corresponding right/left latent regressions at the cutoff.  Local
exclusion is encoded by the `Y(d)` interface; monotonicity is kept in the
assumption bundle to match the classical fuzzy RDD assumption list, though the
representative-level Wald identity itself only uses the bridge from consistency,
cutoff eligibility, continuity, support, and one-sided limit existence.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POFuzzyRDDSystem P` | `structure { Xvar, Zvar, Dvar, Yvar, c, hYD, hZD, hDY }` | Fuzzy RDD subsystem: real running variable `X`, binary cutoff-eligibility instrument `Z`, binary treatment `D`, real outcome `Y`, cutoff `c`, and distinctness witnesses for consistency rewrites | **Defined** |
| `POFuzzyRDDSystem.{factualX, factualZ, factualD, factualY}` | `P.Ω → ℝ` / `P.Ω → Bool` / `P.Ω → Bool` / `P.Ω → ℝ` | Empty-regime factual running variable, eligibility instrument, treatment, and outcome | **Defined** |
| `POFuzzyRDDSystem.DofZ` | `Bool → P.Ω → Bool` | Instrument-specific potential treatment `D(z) := Dvar.cfUnder Zvar z` | **Defined** |
| `POFuzzyRDDSystem.YofD` | `Bool → P.Ω → ℝ` | Treatment-specific potential outcome `Y(d) := Yvar.cfUnder Dvar d` | **Defined** |
| `POFuzzyRDDSystem.YofDofZ` | `Bool → P.Ω → ℝ` | Composed potential outcome `Y(D(z))`, implemented as an if-then-else over the Boolean `D(z)` | **Defined** |
| `POFuzzyRDDSystem.{measurable_factualX, measurable_factualZ, measurable_factualD, measurable_factualY, measurable_DofZ, measurable_YofD, measurable_YofDofZ}` | `Measurable …` | Basic measurability lemmas for factual variables and counterfactual/composed maps | **Proved** |
| `POFuzzyRDDSystem.zEvent` | `Bool → Set P.Ω` | Factual eligibility event `{Z = z}` | **Defined** |
| `POFuzzyRDDSystem.Assumptions S` | `structure { consistency, cutoffEligibility, monotonicity, muD, muY, nuD, nuY, *_isReg, *_continuousAt, support_right, support_left, one-sided limit witnesses, firstStageJump }` | Fuzzy RDD assumptions: consistency, a.s. cutoff rule `Z = 1{X ≥ c}`, monotonicity `D(1) ≥ D(0)`, latent and observable regression representatives with integral characterisations, continuity of latent regressions at `c`, two-sided local support for the law of `X`, existence of observable one-sided limits, and a nonzero latent first-stage jump at the cutoff | **Defined** |
| `POFuzzyRDDSystem.tau_FRD` | `S.Assumptions → ℝ` | Regression-representative cutoff Wald estimand `(μ_Y(1,c)-μ_Y(0,c))/(μ_D(1,c)-μ_D(0,c))`, the formal counterpart used for `prop:po-fuzzy-rdd` | **Defined** |
| `POFuzzyRDDSystem.{nuD_right_limit, nuD_left_limit, nuY_right_limit, nuY_left_limit}` | `S.Assumptions → ℝ` | Chosen witnesses of the four one-sided observable limits via `Classical.choose` | **Defined** |
| `POFuzzyRDDSystem.{tendsto_nuD_right_limit, tendsto_nuD_left_limit, tendsto_nuY_right_limit, tendsto_nuY_left_limit}` | `Tendsto …` | Spec lemmas: the chosen witnesses are genuine one-sided limits of `ν_D` and `ν_Y` | **Proved** |
| private D-side bridge lemmas | — | Cutoff eligibility + consistency: on right/left measurable slices of `X`, factual treatment agrees a.e. with `D(1)`/`D(0)`, yielding integral and pushforward identities between `ν_D` and `μ_D` | **Proved** |
| private Y-side bridge lemmas | — | Consistency rewrites `Y` to `Y(factualD)` and then, on `{Z=z}`, to `Y(D(z))`; combined with the regression representative identities this gives the analogous bridges between `ν_Y` and `μ_Y` | **Proved** |
| private a.e. agreement and extraction lemmas | — | Apply `Integrable.ae_eq_of_forall_setIntegral_eq`, local support, and `Measure.exists_mem_of_measure_ne_zero_of_ae` to obtain agreement points arbitrarily close to `c` on both sides for treatment and outcome regressions | **Proved** |
| `POFuzzyRDDSystem.{nuD_right_limit_eq, nuD_left_limit_eq, nuY_right_limit_eq, nuY_left_limit_eq}` | `Tendsto ν_* (𝓝[>/<] c) (𝓝 L) → L = μ_*(z,c)` | Any one-sided observable regression limit equals the corresponding latent regression representative at the cutoff, by uniqueness of limits along the agreement subfilter | **Proved** |
| `POFuzzyRDDSystem.frd_identification` | `tau_FRD hA = (nuY_right_limit hA - nuY_left_limit hA) / (nuD_right_limit hA - nuD_left_limit hA)` | **Fuzzy RDD identification at the cutoff** (regression-representative form of `prop:po-fuzzy-rdd`): the cutoff Wald estimand equals the Wald ratio of one-sided observable regression limits | **Proved** |
| `POFuzzyRDDSystem.YdiffComplier` | `P.Ω → ℝ` | `(Y(1) - Y(0)) · 1_{D(1)=1 ∧ D(0)=0}`: the Y-difference weighted by the complier indicator; equals `Y(D(1)) - Y(D(0))` a.e. under monotonicity | **Defined** |
| `POFuzzyRDDSystem.Assumptions.{mu_Ydiff_complier, mu_Ydiff_complier_isReg, mu_Ydiff_complier_continuousAt}` | `ℝ → ℝ`, `IsRegressionFunction`, `ContinuousAt` | Regression representative for `E[(Y(1)-Y(0))·1_C \| X=·]`, its `IsRegressionFunction` witness, and continuity at `c`; together they support the LATE bridge | **Defined** |
| `POFuzzyRDDSystem.tau_LATE` | `S.Assumptions → ℝ` | Cutoff-local LATE `E[Y(1)-Y(0) \| X=c, C_c]` in regression-representative form: `mu_Ydiff_complier c / (muD true c - muD false c)` | **Defined** |
| `POFuzzyRDDSystem.tau_late_identification` | `tau_LATE hA = tau_FRD hA` | **Fuzzy RDD identifies cutoff-local LATE** (`prop:po-fuzzy-rdd`): under all fuzzy RDD assumptions including monotonicity, the Wald ratio `τ_FRD` equals the cutoff-local LATE `τ_LATE = E[Y(1)-Y(0)\|X=c, C_c]`; proved via `IsRegressionFunction.sub` + monotonicity case analysis + `aeEq_of_aeEq_response` + continuity at `c` | **Proved** |

---

## 9j. `PO/ID/Partial/Manski.lean` — Manski ATE Bounds under a Discrete IV (PO framework)

Formalizes the **Manski bound family** for the ATE under a discrete IV,
from `Basic Concepts.tex` §`subsec:po-iv-manski`, §`subsec:po-iv-manski-shape`,
and `rem:manski-bound-family` (lines ~1561–2010).  Split into one file per
shape restriction.  Instrument value space `α` is arbitrary with
`MeasurableSingletonClass`; everything lives on the event-level
`eventCondExp` layer plus `POVar` consistency (no `CondIndepFun` stubs).

Layout:

- `Manski/Setup.lean` — data layer (`POManskiIVSystem`) + bound functionals.
- `Manski/Assumptions.lean` — `BaseAssumptions` + shape restrictions.
- `Manski/Helpers.lean` — shared utilities + conditional stratum bounds.
- `Manski/NonAsp.lean` — prop:po-iv-manski (baseline / mean-independence).
- `Manski/MTR.lean` — prop:po-iv-mtr (monotone treatment response).
- `Manski/MTS.lean` — prop:po-iv-mts (monotone treatment selection).
- `Manski/MIV.lean` — prop:po-iv-miv (monotone IV; integrated forms under `[Fintype α]`).
- `Manski/Combined.lean` — cor:po-iv-mtr-mts, cor:po-iv-mtr-miv.
- `Manski.lean` — thin re-export.

### Setup (`Manski/Setup.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `POManskiIVSystem P α` | `structure { Z, D, Y, hZ, hDbool, hYreal, hZD, hZY, hDY }` | Discrete-IV subsystem over a PO system: instrument `Z : α`, binary `D`, real `Y`, pairwise distinctness | **Defined** |
| `zVar` / `dVar` / `yVar` | `POVar P α` / `POVar P Bool` / `POVar P ℝ` | `POVar` wrappers around `Z`, `D`, `Y` | **Defined** |
| `YofD S d` | `P.Ω → ℝ` | Counterfactual outcome `Y(d) := yVar.cfUnder dVar d` | **Defined** |
| `factualZ` / `factualD` / `factualY` | `P.Ω → α / Bool / ℝ` | Factual instrument / treatment / outcome | **Defined** |
| `zEvent S z` / `dEvent S d` | `Set P.Ω` | Factual events `{Z = z}` / `{D = d}` | **Defined** |
| measurability lemmas | `measurable_{YofD, factualZ, factualD, factualY}`, `measurableSet_{zEvent, dEvent}` | One-line delegations to `POVar` | **Proved** |
| `ATE` | `ℝ := ∫ (YofD true - YofD false) ∂P.μ` | Target parameter, average treatment effect | **Defined** |
| `support S` | `Set α := {z \| P.μ (zEvent z) ≠ 0}` | Support of the instrument | **Defined** |
| `boundArm d c z` | `ℝ` | Unified arm bound functional `eventCondExp μ (zEvent z) (factualY · dVar.indicator d + c · dVar.indicator (!d))`; specialises to the four named bounds below | **Defined** |
| `lowerBound1 lo z` | `ℝ := boundArm true lo z` | Observable `L_{1,z}`; now a one-liner on `boundArm` | **Defined** |
| `upperBound1 hi z` | `ℝ := boundArm true hi z` | Observable `U_{1,z}`; one-liner on `boundArm` | **Defined** |
| `lowerBound0 lo z` | `ℝ := boundArm false lo z` | Observable `L_{0,z}`; one-liner on `boundArm` | **Defined** |
| `upperBound0 hi z` | `ℝ := boundArm false hi z` | Observable `U_{0,z}`; one-liner on `boundArm` | **Defined** |

### Assumptions (`Manski/Assumptions.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `BaseAssumptions S` | `structure { consistency, lo, hi, hle, bounded_one, bounded_zero, integrable_{Y1,Y0} }` | Shared hypotheses: consistency, outcome bounds `lo ≤ Y(d) ≤ hi` a.s., integrability of `Y(1)` and `Y(0)`. Factual-`Y` integrability is *derived*, not bundled (see `integrable_factualY`) | **Defined** |
| `BaseAssumptions.bounded hA d` | `∀ᵐ ω ∂P.μ, hA.lo ≤ S.YofD d ω ∧ S.YofD d ω ≤ hA.hi` | Binary-folded form of `bounded_one` / `bounded_zero`: `Y(d)` is a.s. in `[lo, hi]` uniformly in `d : Bool`. Derived by `cases d` | **Proved** |
| `BaseAssumptions.integrable_YofD hA d` | `Integrable (S.YofD d) P.μ` | Binary-folded form of `integrable_Y1` / `integrable_Y0` | **Proved** |
| `BaseAssumptions.integrable_factualY hA` | `Integrable S.factualY P.μ` | Factual outcome integrability, *derived* from arm integrability via consistency (`factualY = Σ_d Y(d)·1{D=d}` a.e.); no longer an assumed field | **Proved** |
| `MeanIndep S` | `Prop { meanIndep_one, meanIndep_zero }` | Mean independence `E[Y(d) \| Z=z] = E[Y(d)]` on `eventCondExp`, for `d ∈ {0,1}` | **Defined** |
| `MTR S` | `Prop { monotone }` | Monotone treatment response: `Y(0) ≤ Y(1)` a.s. | **Defined** |
| `MTS S` | `Prop { mts_one }` | Monotone treatment selection: `eventCondExp μ (dEvent false) (YofD d) ≤ eventCondExp μ (dEvent true) (YofD d)` for `d ∈ {0,1}` | **Defined** |
| `MIV S` | `structure { inst, monotone }` | Monotone instrumental variable: bundled `LinearOrder ↑S.support` and monotonicity of `u ↦ eventCondExp μ (zEvent u) (YofD d)` on the support | **Defined** |

### Helpers (`Manski/Helpers.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| integrability lemmas | `factualY_mul_indD_integrable`, `YofD_mul_indD_integrable`, `const_mul_indD_integrable` | Integrability side conditions under `[IsFiniteMeasure P.μ]` + `BaseAssumptions`; now consume `POVar.indicator` + `integrable_indicator` / `indicator_eq_one_or_zero` | **Proved** |
| pointwise fill-in lemmas | `dVar_inds_at_ω` (private), `YofD_indD_plus_lo_le_YofD`, `YofD_le_YofD_indD_plus_hi` | a.e. "floor/ceiling fill-in" bounds; `dVar_inds_at_ω` packages the binary case split for `(indicator d, indicator (!d))` | **Proved** |
| `boundArm_lo_le_cond_YofD` | `[IsFiniteMeasure], BaseAssumptions, d : Bool, z ∈ support → boundArm d hA.lo z ≤ eventCondExp μ (zEvent z) (YofD d)` | Parametric conditional stratum lower bound; the mean-indep-free core used by `NonAsp` and `MIV` | **Proved** |
| `cond_YofD_le_boundArm_hi` | analogous upper, parametric in `d` | Parametric conditional stratum upper bound | **Proved** |
| `lowerBound1_le_cond_Y1` | — | Legacy corollary: `boundArm_lo_le_cond_YofD hA true` | **Proved** |
| `cond_Y1_le_upperBound1` | — | Legacy corollary: `cond_YofD_le_boundArm_hi hA true` | **Proved** |
| `lowerBound0_le_cond_Y0` | — | Legacy corollary: `boundArm_lo_le_cond_YofD hA false` | **Proved** |
| `cond_Y0_le_upperBound0` | — | Legacy corollary: `cond_YofD_le_boundArm_hi hA false` | **Proved** |

### NonAsp (`Manski/NonAsp.lean`) — prop:po-iv-manski

| Symbol | Type | Description | Status |
|---|---|---|---|
| `lowerBound1_le_integral_Y1` | `[IsFiniteMeasure], BaseAssumptions, MeanIndep, z ∈ support → lowerBound1 hA.lo z ≤ ∫ YofD true ∂μ` | Stratum-level lower bound for `E[Y(1)]` via `lowerBound1_le_cond_Y1` + mean-indep collapse | **Proved** |
| `integral_Y1_le_upperBound1` | analogous upper | Stratum-level upper bound for `E[Y(1)]` | **Proved** |
| `lowerBound0_le_integral_Y0` | — | Stratum-level lower bound for `E[Y(0)]` | **Proved** |
| `integral_Y0_le_upperBound0` | — | Stratum-level upper bound for `E[Y(0)]` | **Proved** |
| `manski_bounds_ATE` | per-stratum-pair sandwich `L_{1,z₁} - U_{0,z₀} ≤ ATE ≤ U_{1,z₁} - L_{0,z₀}` | Manski bounds, per-pair form; strictly stronger than sup/inf form | **Proved** |
| `ciSup_lowerBound1_le_integral_Y1` | `support.Nonempty → ⨆ z, L_{1,z} ≤ ∫ Y(1)` | sup aggregation over support | **Proved** |
| `integral_Y1_le_ciInf_upperBound1` | inf form | inf aggregation of `U_{1,·}` | **Proved** |
| `ciSup_lowerBound0_le_integral_Y0` / `integral_Y0_le_ciInf_upperBound0` | — | sup/inf aggregations for arm 0 | **Proved** |
| `manski_bounds_ATE_ciSup` | `support.Nonempty → (⨆ L_{1}) - (⨅ U_{0}) ≤ ATE ≤ (⨅ U_{1}) - (⨆ L_{0})` | Manski bounds in tex form (sup/inf over support) | **Proved** |

### MTR (`Manski/MTR.lean`) — prop:po-iv-mtr

| Symbol | Description | Status |
|---|---|---|
| `mtr_E_Y_le_E_Y1` | Step 1: `∫ Y ≤ ∫ Y(1)` via a.s. inequality `Y ≤ Y(1)` from consistency + MTR | **Proved** |
| `mtr_E_Y0_le_E_Y` | Step 4: `∫ Y(0) ≤ ∫ Y` symmetrically | **Proved** |
| `mtr_E_Y1_le_upper` | Step 2: `∫ Y(1) ≤ p·E[Y\|D=1] + (1-p)·hi` via total-law decomposition + consistency + ceiling bound | **Proved** |
| `mtr_lower_le_E_Y0` | Step 3: `(1-p)·E[Y\|D=0] + p·lo ≤ ∫ Y(0)` | **Proved** |
| `mtr_nonneg_ATE` | `0 ≤ ATE` from MTR a.s. inequality | **Proved** |
| `mtr_bounds_ATE` | Two-sided ATE sandwich: `0 ≤ ATE ≤ (p·E[Y\|D=1]+(1-p)·hi) - ((1-p)·E[Y\|D=0]+p·lo)` | **Proved** |

### MTS (`Manski/MTS.lean`) — prop:po-iv-mts

| Symbol | Description | Status |
|---|---|---|
| `mts_lower_le_E_Y1` | `p·E[Y\|D=1] + (1-p)·lo ≤ ∫ Y(1)` (MTS lower on arm 1) | **Proved** |
| `mts_E_Y1_le_condY1` | `∫ Y(1) ≤ E[Y\|D=1]` (MTS upper on arm 1) | **Proved** |
| `mts_condY0_le_E_Y0` | `E[Y\|D=0] ≤ ∫ Y(0)` (MTS lower on arm 0) | **Proved** |
| `mts_E_Y0_le_upper` | `∫ Y(0) ≤ p·hi + (1-p)·E[Y\|D=0]` (MTS upper on arm 0) | **Proved** |
| `mts_bounds_ATE` | Two-sided MTS ATE sandwich, obtained by subtracting the four arm bounds | **Proved** |

### MIV (`Manski/MIV.lean`) — prop:po-iv-miv

| Symbol | Description | Status |
|---|---|---|
| `mLower1`, `mUpper1`, `mLower0`, `mUpper0` | Envelope functions `sSup { L_{d,u} \| u ≤ z }` and `sInf { U_{d,u} \| z ≤ u }` on the MIV linear order | **Defined** |
| `miv_mLower1_le_cond_Y1` | `mLower1 z ≤ eventCondExp μ (zEvent z) (YofD true)` on support; chains `Helpers` conditional bound with MIV monotonicity | **Proved** |
| `miv_cond_Y1_le_mUpper1` | analogous upper | **Proved** |
| `miv_mLower0_le_cond_Y0` | lower for arm 0 | **Proved** |
| `miv_cond_Y0_le_mUpper0` | upper for arm 0 | **Proved** |
| `miv_integral_mLower1_le_integral_Y1` | `[Fintype α]`: `∫ mLower1(Z) ≤ ∫ Y(1)`; decomposes each side via `integral_eq_sum_measure_mul_eventCondExp` and compares stratum-wise, exploiting that off-support strata have measure `0` | **Proved** |
| `miv_integral_Y1_le_integral_mUpper1` | `[Fintype α]`: `∫ Y(1) ≤ ∫ mUpper1(Z)`; symmetric to the lower bound | **Proved** |
| `miv_integral_mLower0_le_integral_Y0` | `[Fintype α]`: arm 0 integrated lower | **Proved** |
| `miv_integral_Y0_le_integral_mUpper0` | `[Fintype α]`: arm 0 integrated upper | **Proved** |
| `miv_bounds_ATE` | `[Fintype α]`: `∫ (mLower1(Z) - mUpper0(Z)) ≤ ATE ≤ ∫ (mUpper1(Z) - mLower0(Z))`; follows by linearity from the four integrated bounds | **Proved** |

### Combined (`Manski/Combined.lean`)

| Symbol | Description | Status |
|---|---|---|
| `mtr_mts_bounds_ATE` | cor:po-iv-mtr-mts: `0 ≤ ATE ≤ E[Y\|D=1] - E[Y\|D=0]` under MTR + MTS; combines `mtr_nonneg_ATE` with `mts_E_Y1_le_condY1 ∩ mts_condY0_le_E_Y0` | **Proved** |
| `mtr_miv_bounds_ATE` | `[Fintype α]`: cor:po-iv-mtr-miv: `0 ≤ ATE` (MTR) and `ATE ≤ ∫ (mUpper1(Z) - mLower0(Z))` (MIV); forwards from `miv_bounds_ATE` | **Proved** |

### IntervalForm (`Manski/IntervalForm.lean`) — `Set.Icc` restatements

Pure restatements of the ATE sandwiches as `S.ATE ∈ Set.Icc lo hi`, obtained by
feeding each `L ≤ ATE ∧ ATE ≤ U` pair through the
`Causalean.PartialID.mem_Icc_of_sandwich` bridge (`Basic.lean`). No new
mathematical content; same hypotheses/instances as each source theorem.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `manski_ATE_mem_Icc` | theorem | Closed-interval `Set.Icc` form of the Manski no-assumption per-stratum-pair ATE sandwich (`manski_bounds_ATE`), via the `mem_Icc_of_sandwich` bridge | **Proved** |
| `manski_ATE_mem_Icc_ciSup` | theorem | Closed-interval `Set.Icc` form of the sup/inf-aggregated Manski no-assumption ATE sandwich (`manski_bounds_ATE_ciSup`) | **Proved** |
| `mtr_mts_ATE_mem_Icc` | theorem | Closed-interval `Set.Icc` form of the MTR + MTS ATE sandwich (`mtr_mts_bounds_ATE`): `ATE ∈ [0, E[Y\|D=1] - E[Y\|D=0]]` | **Proved** |
| `mtr_miv_ATE_mem_Icc` | theorem | `[Fintype α]`: closed-interval `Set.Icc` form of the MTR + MIV ATE sandwich (`mtr_miv_bounds_ATE`): `ATE ∈ [0, ∫ (mUpper1(Z) - mLower0(Z))]` | **Proved** |

**Scope note.** Two baseline aggregation forms are exposed in `NonAsp`: the
per-stratum-pair `manski_bounds_ATE` is strictly stronger (pick any
`z₁, z₀ ∈ support`), while `manski_bounds_ATE_ciSup` matches the tex's
sup/inf form but requires `support.Nonempty` (Lean `⨆`/`⨅` fall back to `0`
on empty index sets). The MIV integrated bounds require a `[Fintype α]`
hypothesis on the instrument value space (standard Manski setting with a
discrete IV) and are discharged via the `integral_eq_sum_measure_mul_eventCondExp`
total law in `PO/Conditioning/EventCondExp.lean`; no outstanding `sorry` in the folder.

---

## 9j2. `PO/ID/Partial/` — Abstract Partial-Identification Bounds Engine

Reusable, PO-framework-free partial-identification primitives. Two files, both in
namespace `Causalean.PartialID`:

- `Basic.lean` — the sharp identified interval + a sharp-interval API, including the
  two scalar-sandwich bridge helpers (`mem_Icc_of_sandwich`, `mem_Icc_csInf_csSup`)
  that turn concrete `L ≤ θ ≤ U` bounds into the engine's `Set.Icc` vocabulary.
- `Frechet.lean` — Fréchet–Hoeffding copula bounds + the easy Makarov lower bound.

### `Basic.lean`

| Symbol | Type | Description | Status |
|---|---|---|---|
| `IdentifiedInterval obj feasible` | `Set ℝ` | Sharp identified interval: range of `obj` over all `{x // feasible x}`; the set of all attainable objective values | **Defined** |
| `mem_identifiedInterval hx` | `obj x ∈ IdentifiedInterval obj feasible` | If `x` is feasible then `obj x` is in the identified interval | **Proved** |
| `identifiedInterval_subset_Icc hL hU` | `IdentifiedInterval obj feasible ⊆ Set.Icc L U` | Outer bound: uniform two-sided bounds `L ≤ obj ≤ U` on the feasible set imprison the identified set in `[L, U]` | **Proved** |
| `identifiedInterval_eq_Icc hL hU hLmem hUmem hconn` | `IdentifiedInterval obj feasible = Set.Icc L U` | Sharp bound (order-connected form): outer bound + both endpoints attained + identified set order-connected ⇒ identified set is exactly `[L, U]` | **Proved** |
| `identifiedInterval_param_Icc hfeas hcont hL hU hbound` | `IdentifiedInterval obj feasible = Set.Icc L U` | Mixing-pattern constructor: feasible set = image of `[0,1]` under `γ`, `obj∘γ` continuous, endpoint values `L,U` bracketing `obj` along the path ⇒ sharp interval `[L,U]` (continuous image of a connected set is order-connected) | **Proved** |
| `mem_Icc_of_sandwich hlo hhi` | `θ ∈ Set.Icc L U` | Scalar-sandwich bridge: the literal reading of a two-sided bound `L ≤ θ ≤ U` as `θ ∈ [L, U]`; names the inequality-pair → `Set.Icc` step every concrete bound ends in. Consumers: `Manski`/`Proxy` `IntervalForm` | **Proved** |
| `mem_Icc_csInf_csSup hb ha hy` | `y ∈ Set.Icc (sInf s) (sSup s)` | Worst/best case over a nuisance: every member of a bounded set `s ⊆ ℝ` lies between its infimum and supremum (the `csInf_le`/`le_csSup` step of a sharp bound). Consumers: `Lee/TrimBound`, `BalkePearl/IntervalForm` | **Proved** |

### `Frechet.lean`

Self-contained measure theory over a probability space `P` with two real random
variables `X, Y`; CDF/joint-CDF values written as `.toReal` of event measures.
Only the *easy* (fixed-joint-law) directions are asserted — no sharpness claim.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `frechet_upper u v` | `(P{X≤u ∧ Y≤v}).toReal ≤ min (P{X≤u}).toReal (P{Y≤v}).toReal` | Fréchet–Hoeffding upper bound on the joint CDF (joint event ⊆ each marginal event); needs no measurability — pure measure monotonicity | **Proved** |
| `frechet_lower hY u v` | `max ((P{X≤u}).toReal + (P{Y≤v}).toReal - 1) 0 ≤ (P{X≤u ∧ Y≤v}).toReal` | Fréchet–Hoeffding lower bound on the joint CDF (inclusion–exclusion `P A + P B - 1 ≤ P(A∩B)`); only `Measurable Y` needed | **Proved** |

| `makarov_lower_param hY s a` | `max ((P{X≤a}).toReal - (P{Y<a-s}).toReal) 0 ≤ (P{X-Y≤s}).toReal` | Makarov lower bound on the CDF of the difference `X-Y`, per reference point `a` (from `{X≤a}∩{a-s≤Y} ⊆ {X-Y≤s}` + Fréchet lower); only `Measurable Y` needed | **Proved** |
| `makarov_lower_iSup hY s` | `⨆ a, max ((P{X≤a}).toReal - (P{Y<a-s}).toReal) 0 ≤ (P{X-Y≤s}).toReal` | Sup-convolution envelope form: the supremum over reference points still lower-bounds the CDF of `X-Y` | **Proved** |

### `RandomSet/Interval.lean`

Tier-0 random-set substrate for partial identification (the measure-theoretic
upgrade of `IdentifiedInterval`). An interval random set is a pair of measurable
endpoints `L, U : Ω → ℝ` with `L ≤ U`, viewed as `ω ↦ [L ω, U ω]`. Provides the
two facts a sharpness proof needs — measurable selection and the selection
(Aumann) expectation — both proved *elementarily* (no Kuratowski–Ryll-Nardzewski,
no atomlessness/Lyapunov). Endpoints are reported via `sInf`/`sSup` to match the
`SandwichInterval` convention and keep the support-function generalisation a
drop-in. Namespace `Causalean.PartialID.RandomSet`; all sorry-free + axiom-clean.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `randomInterval L U` | `Ω → Set ℝ` | The interval-valued random closed set `ω ↦ [L ω, U ω]` | **Defined** |
| `IsSelection L U f` | `Prop` | `f` is a measurable everywhere-selection: `Measurable f ∧ ∀ ω, f ω ∈ [L ω, U ω]` | **Defined** |
| `isSelection_left hL hLU` | `IsSelection L U L` | The lower endpoint is always a selection ⇒ the random set has a measurable selection | **Proved** |
| `isSelection_iff_exists_param hL hU hLU f` | `IsSelection L U f ↔ ∃ t, Measurable t ∧ (∀ ω, t ω ∈ [0,1]) ∧ ∀ ω, f ω = L ω + t ω·(U ω−L ω)` | Measurable selection of an interval random set: every selection is `L + t·(U−L)` for a measurable `t : Ω → [0,1]`, and conversely (divide-by-width construction) | **Proved** |
| `selectionExpectation L U μ` | `Set ℝ` | The selection (Aumann) expectation: integrals of integrable measurable selections, `{∫f \| f a selection}` | **Defined** |
| `integral_le_integral_of_le hLint hUint hLU` | `(∫L) ≤ ∫U` | Endpoint integrals are ordered (monotonicity of the Bochner integral) | **Proved** |
| `selectionExpectation_eq_Icc hL hU hLint hUint hLU` | `selectionExpectation L U μ = Set.Icc (∫L) (∫U)` | **Tier-0 core:** the selection expectation is exactly `[∫L, ∫U]` (⊆ by integral monotonicity; ⊇ realised by a *constant* `t ∈ [0,1]`, so no atomlessness needed) | **Proved** |
| `sInf_selectionExpectation …` | `sInf (selectionExpectation L U μ) = ∫L` | Sharp lower endpoint as the infimum over selections | **Proved** |
| `sSup_selectionExpectation …` | `sSup (selectionExpectation L U μ) = ∫U` | Sharp upper endpoint as the supremum over selections | **Proved** |

*Out of scope (flagged for later):* conditional selection expectation
`E[X∣𝒢] = [E[L∣𝒢], E[U∣𝒢]]` (Tier-0.5, for regression envelopes such as
Manski–Tamer interval regression), and the support-function / Artstein /
CLR-inference layers.

### `LP/ConicDuality.lean`

Abstract conic linear-programming duality over a pair of **real Hilbert spaces**,
the value-form backbone for sharp partial-ID bounds posed as
`min ⟪c,x⟫ s.t. A x = b, x ∈ K`. Built on Mathlib's `ProperCone` / `innerDual`.
Hilbert generality is already infinite-dimensional (e.g. `L²(μ)` for proxy/bridge
functions). The *measure*-cone case (weak-∗ duality of measures against `C(X)`)
is **out of scope**: Mathlib gives `FiniteMeasure` only a `Module ℝ≥0` (no signed
`ℝ`-module), no ordered-cone instance, and a non-order weak-∗ topology — so the
nonnegative-measure `ProperCone` needs new infrastructure (a module note in the
file records this). Namespace `Causalean.PartialID`; weak duality + Farkas are
sorry-free, strong duality is a CQ-gated statement awaiting a modelling decision.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `ConicProgram E F` | structure | Primal data `(K, A, b, c)`: cone `K : ProperCone ℝ E`, operator `A : E →L[ℝ] F`, rhs `b : F`, objective `c : E` | **Defined** |
| `ConicProgram.PrimalFeasible x` | `Prop` | `A x = b ∧ x ∈ K` | **Defined** |
| `ConicProgram.DualFeasible y` | `Prop` | reduced cost in the dual cone: `c − Aᵀ y ∈ innerDual K` | **Defined** |
| `ConicProgram.primalValue` | `ℝ` | `sInf { ⟪c,x⟫ : x primal feasible }` | **Defined** |
| `ConicProgram.dualValue` | `ℝ` | `sSup { ⟪b,y⟫ : y dual feasible }` | **Defined** |
| `ConicProgram.weak_duality hx hy` | `⟪b,y⟫ ≤ ⟪c,x⟫` | Pointwise weak duality: any dual-feasible value lower-bounds any primal-feasible value (via `mem_innerDual` + adjoint) | **Proved** |
| `ConicProgram.dualValue_le_primalValue hP hD` | `dualValue ≤ primalValue` | Value form of weak duality (both feasible; no boundedness needed) | **Proved** |
| `ConicProgram.farkas` | `b ∈ ProperCone.map A K ↔ ∀ y, Aᵀ y ∈ innerDual K → 0 ≤ ⟪b,y⟫` | Feasibility alternative / strong-duality engine; restatement of `ProperCone.relative_hyperplane_separation`. `map` bakes in the closure of `A(K)` — exactly the CQ gap | **Proved** |
| `ConicProgram.augmentedImage` | `Set (F × ℝ)` | The augmented image cone `{(A x, ⟪c,x⟫) : x ∈ K}`; its closedness is the primal-attainment CQ | **Defined** |
| `ConicProgram.strong_duality_primal_attained hP hbdd hCQ` | `∃ x, PrimalFeasible x ∧ ⟪c,x⟫ = primalValue` | Strong duality I (closedness CQ): the primal optimum is attained — an extremal feasible point exists. Via the closed value-slice + `IsClosed.csInf_mem` | **Proved** |
| `ConicProgram.strong_duality_zero_gap hP hbdd hCQ` | `primalValue = dualValue` | Strong duality II (closedness CQ — same as I): no duality gap. Closedness suffices (no Slater needed). Proof: for each `ε>0`, separate `(b, primalValue−ε)` from the augmented cone `map Ã K` (`Ã x = (A x, ⟪c,x⟫)`) via `ProperCone.hyperplane_separation_point` on the normed product `F × ℝ`; Riesz-decompose the functional as `⟪y₀,v⟫ + s·t`, rule out `s ≤ 0`, and read off the dual-feasible `−(1/s)•y₀`. | **Proved** |

---

## 9j2inf. `PO/ID/Partial/Inference/` — Confidence intervals for interval-identified parameters

First statistical-inference layer over the partial-identification *geometry*. Where the rest of `PartialID/` produces population sandwiches `L ≤ θ₀ ≤ U`, this folder turns them into finite-sample confidence intervals, formalizing the two coverage notions of the literature (Horowitz–Manski 2000 set coverage vs. Imbens–Manski 2004 parameter coverage). Built on `Stat/Concentration/ConfidenceInterval` (Hoeffding/Bernstein miss bounds) + a union bound.

### `Inference/Basic.lean` — coverage vocabulary + abstract honest-CI lemma (pure measure theory)

| Declaration | Type / statement | Description | Status |
|---|---|---|---|
| `RandomCoversPoint A B θ` | `Set Ω` | Event that the random interval `[A ω, B ω]` contains the fixed value `θ` (Imbens–Manski parameter-coverage notion) | **Defined** |
| `RandomCoversIcc A B L U` | `Set Ω` | Event that `[A ω, B ω]` contains the whole population interval `[L, U]` (Horowitz–Manski set-coverage notion) | **Defined** |
| `randomCoversIcc_subset_randomCoversPoint` | `θ ∈ [L,U] → RandomCoversIcc A B L U ⊆ RandomCoversPoint A B θ` | Set coverage ⟹ point coverage; the easy half of Imbens–Manski (2004, Lemma 1): parameter-coverage CIs are weakly smaller than set-coverage CIs | **Proved** |
| `randomCoversIcc_compl_subset` | `(RandomCoversIcc (lo−wL) (hi+wU) L U)ᶜ ⊆ {wL ≤ |lo−L|} ∪ {wU ≤ |hi−U|}` | Non-coverage is contained in the two one-sided endpoint-failure events; geometric heart of the union bound | **Proved** |
| `honest_ci_set_cover` | `1 − δL − δU ≤ μ.real (RandomCoversIcc (lo−wL) (hi+wU) L U)` | **Abstract honest CI.** Given one-sided miss bounds `μ{wL≤|lo−L|}≤δL`, `μ{wU≤|hi−U|}≤δU`, the widened interval covers `[L,U]` w.p. `≥1−δL−δU` (single union bound; stated in miss probabilities so any concentration inequality plugs in) | **Proved** |
| `honest_ci_point_cover` | `θ ∈ [L,U] → 1 − δL − δU ≤ μ.real (RandomCoversPoint (lo−wL) (hi+wU) θ)` | Parameter-coverage corollary: covering the set suffices to cover any point of it (conservative Horowitz–Manski parameter CI) | **Proved** |

### `Inference/IntervalCI.lean` — finite-sample plug-in instantiations (Hoeffding / Bernstein)

Plug-in Manski sandwich: bounding statistics `f_L, f_U` with population means `L = ∫ f_L`, `U = ∫ f_U`; estimate each endpoint by its sample mean and widen by a concentration half-width.

| Declaration | Statement | Description | Status |
|---|---|---|---|
| `hoeffding_honest_ci_set_cover` | `1−δL−δU ≤ μ.real (RandomCoversIcc [X̄ₙ(f_L)−wL, X̄ₙ(f_U)+wU] L U)` | Honest set CI with Hoeffding half-widths for bounded statistics `f_L∈[aL,bL]`, `f_U∈[aU,bU]`; valid at every `n` | **Proved** |
| `hoeffding_honest_ci_point_cover` | `θ₀∈[∫f_L,∫f_U] → 1−δL−δU ≤ μ.real (RandomCoversPoint … θ₀)` | Parameter-coverage version (Hoeffding) | **Proved** |
| `bernstein_honest_ci_point_cover` | as above with `bernsteinCIHalfWidth` | Variance-adaptive version; far tighter when the bounding statistics have small variance (leading term `2σ√(log(2/δ)/n)`) | **Proved** |

### `Inference/ImbensManski.lean` — asymptotic one-sided sharp interval (Imbens–Manski 2004)

When the identified set has positive width `Δ = U−L > 0`, covering the single true `θ₀` (not the whole set) needs protection on only **one** side: the `O(n^{-1/2})` sampling error is asymptotically negligible vs the fixed `Δ`, so non-coverage is one-sided and a *one-sided* critical value `c = z_{1−α}` already gives `1−α` coverage (vs the two-sided `z_{1−α/2}` the set-coverage interval needs). Normalized endpoint statistics `Sₗ,ₙ = √n(θ̂ₗ−L)/σₗ`, `Sᵤ,ₙ = √n(θ̂ᵤ−U)/σᵤ ⇒ N(0,1)`; offsets `aₙ=√n(θ₀−L)/σₗ≥0`, `bₙ=√n(U−θ₀)/σᵤ≥0`. Sources: Imbens–Manski 2004 (Lemma 1), Stoye 2009, Molinari 2020 §Pointwise-vs-Uniform.

| Declaration | Statement | Description | Status |
|---|---|---|---|
| `gaussianMeasure_zero_one_frontier_Iic c` | `gaussianMeasure 0 1 (frontier (Iic c)) = 0` | Standard normal gives zero mass to the boundary `{c}` of a half-line; continuity-point fact for one-sided portmanteau | **Proved** |
| `farEnd_vanishes_of_tendsto_atBot` | `Sᵤ ⇒ N(0,1) → t ⟶ atBot → (μ{Sᵤ,ₙ < tₙ}).toReal ⟶ 0` | The `Δ>0` mechanism: as the far threshold drifts to `−∞` (`bₙ→∞`), the far-endpoint failure probability vanishes. Bounds the moving tail by a fixed Gaussian tail (`gaussian_tail_small`) + portmanteau | **Proved** |
| `imbensManski_pointwise_coverage` | `… → ∀ε>0, ∀ᶠ n, Φ(c) − ε ≤ (μ{Sₗ,ₙ≤c+aₙ ∧ −(c+bₙ)≤Sᵤ,ₙ}).toReal` | **Core asymptotic guarantee.** With `Sₗ⇒N(0,1)`, `aₙ≥0`, and the far end vanishing, IM coverage of `θ₀` eventually exceeds `Φ(c)−ε` where `Φ(c)=N(0,1)((-∞,c])`. One-sided `c=z_{1−α}` ⟹ `≥1−α`. Pointwise (not uniform; Stoye's superefficiency caveat not addressed) | **Proved** |

Supporting lemma `Causalean.Stat.gaussian_tail_small` (Gaussian half-line tails `<ε` for large cutoff) was factored out of the quantile-Bahadur rate proof into `Causalean/Stat/CLT/GaussianTail.lean` for reuse here.

---

## 9j2sf. `PO/ID/Partial/SupportFunction/` — Support-Function Identified Sets (convex/Hilbert spine)

The multivariate generalization of the scalar `Basic.lean` engine: when the
feasible set `C` is **convex** in a real inner-product space and the target is the
**linear functional** `x ↦ ⟪d, x⟫`, the sharp identified interval is governed by
the *support function* `supportFn C d = sup { ⟪d,x⟫ : x ∈ C }`. Three files, all in
namespace `Causalean.PartialID`. Boundedness/nonemptiness side conditions are carried
explicitly (the real `sSup` convention), matching `SandwichInterval`/`RandomSet`.
This is the abstraction the proximal-L2-bridge, sensitivity-model, and reweighting
partial-ID problems bottom out in.

- `Basic.lean` — `supportFn` definition + the two monotone spine lemmas.
- `Calculus.lean` — the support-function algebra (direction + set operations).
- `Interval.lean` — sharp identified interval `[-h(-d), h(d)]`, width, point-identification, scalar-engine bridge.
- `AffineBall.lean` — closed-form endpoints over a Hilbert affine fiber `{A h = b}` intersected with a ball `‖h‖ ≤ B` (the proximal-L2-bridge identified set).

### `Basic.lean`

| Symbol | Type | Description | Status |
|---|---|---|---|
| `supportFn C d` | `ℝ := sSup ((fun x => ⟪d,x⟫) '' C)` | Support function: best-case value of the linear target `⟪d,·⟫` over the feasible set `C` | **Defined** |
| `supportFn_eq_iSup_image C d` | `supportFn C d = sSup (…)` | Unfolding lemma exposing the `sSup`-of-image form | **Proved** |
| `le_supportFn hx hbdd` | `⟪d,x⟫ ≤ supportFn C d` | Lower sandwich: a feasible point's value is `≤` the support value (needs the image bounded above) | **Proved** |
| `supportFn_le hne hb` | `supportFn C d ≤ b` | Upper sandwich: a uniform upper bound `b` on the functional over nonempty `C` bounds the support value | **Proved** |

### `Calculus.lean`

| Symbol | Type | Description | Status |
|---|---|---|---|
| `supportFn_singleton d x₀` | `supportFn {x₀} d = ⟪d,x₀⟫` | Support function of a point is the functional value | **Proved** |
| `supportFn_nonneg_of_zero_mem h0 hbdd` | `0 ≤ supportFn C d` | `0 ∈ C` forces a nonnegative support value | **Proved** |
| `supportFn_smul_dir ht hne hbdd` | `supportFn C (t•d) = t * supportFn C d` | Positive homogeneity in the direction (`t ≥ 0`) | **Proved** |
| `supportFn_add_dir_le hne hb₁ hb₂` | `supportFn C (d₁+d₂) ≤ supportFn C d₁ + supportFn C d₂` | Subadditivity / sublinearity in the direction | **Proved** |
| `supportFn_mono hCD hne hbdd` | `supportFn C d ≤ supportFn D d` | Monotone in the set (`C ⊆ D`) | **Proved** |
| `supportFn_union hC hD hbC hbD` | `supportFn (C∪D) d = max (supportFn C d) (supportFn D d)` | Union rule | **Proved** |
| `supportFn_inter_le hne hbC hbD` | `supportFn (C∩D) d ≤ min (supportFn C d) (supportFn D d)` | Intersection rule (one-sided) | **Proved** |
| `supportFn_translate x₀ hne hbdd` | `supportFn ((x₀+·) '' C) d = ⟪d,x₀⟫ + supportFn C d` | Translation shifts the support value | **Proved** |
| `supportFn_smul_set ha hne hbdd` | `supportFn (a•C) d = a * supportFn C d` | Positive set-scaling (`a ≥ 0`) | **Proved** |
| `supportFn_minkowski hC hD hbC hbD` | `supportFn (C+D) d = supportFn C d + supportFn D d` | Additivity over Minkowski sums | **Proved** |

### `Interval.lean`

| Symbol | Type | Description | Status |
|---|---|---|---|
| `neg_supportFn_neg_le hx hbdd'` | `-supportFn C (-d) ≤ ⟪d,x⟫` | Lower endpoint `-h(-d)` bounds the functional from below (worst case) | **Proved** |
| `linearImage_subset_Icc hbdd hbdd'` | `(⟪d,·⟫) '' C ⊆ Set.Icc (-supportFn C (-d)) (supportFn C d)` | Outer bound: identified set sits inside the support interval | **Proved** |
| `linearImage_ordConnected hC` | `((⟪d,·⟫) '' C).OrdConnected` | Convex `C` ⇒ identified set has no gaps (linear image of convex is convex in `ℝ`) | **Proved** |
| `linearImage_eq_Icc hC hbdd hbdd' hUmem hLmem` | `(⟪d,·⟫) '' C = Set.Icc (-supportFn C (-d)) (supportFn C d)` | **Sharp** interval (attainment form): convex + both endpoints attained ⇒ identified set is exactly `[-h(-d), h(d)]` | **Proved** |
| `linearImage_eq_Icc_of_isCompact hcomp hC hne` | `(⟪d,·⟫) '' C = Set.Icc (-supportFn C (-d)) (supportFn C d)` | **Sharp** interval (compact convex form): the workhorse — continuity gives bounded + attained automatically | **Proved** |
| `width C d` | `ℝ := supportFn C d + supportFn C (-d)` | Width of the identified set (upper minus lower endpoint) | **Defined** |
| `width_nonneg hne hbdd hbdd'` | `0 ≤ width C d` | The identified width is nonnegative | **Proved** |
| `width_eq_zero_iff hne hbdd hbdd'` | `width C d = 0 ↔ ∀ x ∈ C, ⟪d,x⟫ = supportFn C d` | **Point identification**: zero width iff the target is constant on `C` | **Proved** |
| `identifiedInterval_eq_image obj C` | `IdentifiedInterval obj (· ∈ C) = obj '' C` | Bridge: the scalar `Basic.lean` engine's identified set is the set-image of the objective (so concrete theorems consume either vocabulary) | **Proved** |

### `AffineBall.lean`

Closed-form endpoints over the **bridge fiber** `affineBall A b B = {h | A h = b ∧ ‖h‖ ≤ B}` in a real Hilbert space `H` (operator `A : H →L[ℝ] F`). Decomposing `h = h₀ + v` (min-norm solution `h₀ ⊥ ker A`, free direction `v ∈ ker A`), the Pythagorean split + Cauchy–Schwarz give the sharp value/width in terms of `P c = orthogonalProjection (ker A) c`. This is the `pid_proximal_l2_bridge` identified set.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `opKer A` | `Submodule ℝ H := LinearMap.ker ↑A` | Free-direction subspace `ker A` of the operator (closed; has an orthogonal projection — instance provided) | **Defined** |
| `affineBall A b B` | `Set H := {h | A h = b ∧ ‖h‖ ≤ B}` | The affine-ball feasible fiber: equation solutions inside the norm ball | **Defined** |
| `mem_affineBall` | `h ∈ affineBall A b B ↔ A h = b ∧ ‖h‖ ≤ B` | Membership unfolding (`@[simp]`) | **Proved** |
| `supportFn_affineBall_eq A hsol hperp hfit` | `supportFn (affineBall A b B) c = ⟪c,h₀⟫ + √(B²−‖h₀‖²)·‖P c‖` | Closed-form support value (best case), given min-norm solution `h₀ ⊥ ker A` fitting the ball | **Proved** |
| `width_affineBall_eq A hsol hperp hfit` | `width (affineBall A b B) c = 2·√(B²−‖h₀‖²)·‖P c‖` | Closed-form identified width | **Proved** |
| `affineBall_point_identified_iff A hsol hperp hfit` | `width (affineBall A b B) c = 0 ↔ P c = 0` | **Point identification** (strict ball slack `‖h₀‖<B`): collapse iff `c ⊥ ker A`, i.e. `c` in the closed row space of `A` | **Proved** |

---

## 9j2rs. `PO/ID/Partial/RandomSet/` — Random sets, Hausdorff distance & the support-function bridge (Beresteanu–Molinari 2008)

The estimation/inference layer for set-identified parameters. Beresteanu & Molinari (2008) show the whole inference theory rests on one identity (Hörmander's embedding, their eq. (A.1)): for compact convex `A,B`, `H(A,B) = sup_{‖p‖=1} |s(p,A) − s(p,B)|`. This folder builds that bridge, its `d = 1` (interval) specialization (the scalar interval-data CLT, Theorems 3.1/3.2, in `IntervalCLT.lean`), and the general-`d` finite-grid support-process CLT on genuine random sets (`SetValued.lean` + `SupportProcess.lean`, Theorem A.2). Plan: `doc/beresteanu_molinari_plan.md`. All declarations **proved, sorry-free, axiom-clean**.

### `Interval.lean` — interval random sets & Aumann (selection) expectation (Tier-0, pre-existing)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `randomInterval L U` | `Ω → Set ℝ := fun ω => Icc (L ω) (U ω)` | The interval-valued random closed set `ω ↦ [L ω, U ω]` | **Defined** |
| `IsSelection L U f` | `Prop` | `f` is a measurable everywhere-selection of `[L,U]` | **Defined** |
| `isSelection_iff_exists_param` | selection ↔ `L + t·(U−L)`, `t : Ω→[0,1]` measurable | Elementary measurable selection (no Kuratowski–Ryll-Nardzewski) | **Proved** |
| `selectionExpectation L U μ` | `Set ℝ` | Selection (Aumann) expectation: integrals of integrable selections | **Defined** |
| `selectionExpectation_eq_Icc` | `= Icc (∫L) (∫U)` | **Aumann expectation `= [∫L, ∫U]`** (`⊇` uses a constant `t`, no atomlessness) | **Proved** |
| `sInf_selectionExpectation` / `sSup_selectionExpectation` | `= ∫L` / `= ∫U` | Sharp endpoints as inf/sup over selections | **Proved** |

### `Hausdorff.lean` — directed/symmetric Hausdorff distance + the geometric keystone

| Symbol | Type | Description | Status |
|---|---|---|---|
| `directedHausdorff A B` | `ℝ := sSup ((fun a => infDist a B) '' A)` | One-sided (directed) Hausdorff distance `sup_{a∈A} infDist a B` | **Defined** |
| `hausdorffDist A B` | `ℝ := max (dᴴ A B) (dᴴ B A)` | Symmetric Hausdorff distance | **Defined** |
| `infDist_Icc hcd x` | `infDist x [c,d] = max 0 (max (c−x) (x−d))` | Distance from a real point to an interval (zero inside; signed gap outside) | **Proved** |
| `directedHausdorff_Icc hab hcd` | `dᴴ([a,b],[c,d]) = max 0 (max (c−a) (b−d))` | Directed Hausdorff between intervals (worst over-reach, attained at endpoints) | **Proved** |
| `hausdorffDist_Icc hab hcd` | `H([a,b],[c,d]) = max \|a−c\| \|b−d\|` | **The geometric keystone** (Beresteanu–Molinari eq. (A.1), `d=1`): set distance = larger endpoint gap | **Proved** |

### `Aumann.lean` — support function of an interval & the d=1 Artstein bridge

| Symbol | Type | Description | Status |
|---|---|---|---|
| `supportFn_Icc_one hab` | `supportFn [a,b] (1:ℝ) = b` | Support function of a real interval at `+1`: the upper endpoint | **Proved** |
| `supportFn_Icc_neg_one hab` | `supportFn [a,b] (-1:ℝ) = -a` | Support function at `−1`: the negated lower endpoint | **Proved** |
| `hausdorffDist_Icc_eq_supportFn hab hcd` | `H = max \|Δs(−1)\| \|Δs(1)\|` | **The `d=1` Hörmander identity**: Hausdorff distance = sup over `{±1}` of support-function differences (bridges `Hausdorff` to the `SupportFunction` engine) | **Proved** |
| `artstein_supportFn_one` | `s(+1, E[F]) = ∫ s(+1, F ω) ∂μ` | **Artstein identity** (`d=1`, dir `+1`): support of the Aumann expectation = expectation of the support (`= ∫U`) | **Proved** |
| `artstein_supportFn_neg_one` | `s(−1, E[F]) = ∫ s(−1, F ω) ∂μ` | **Artstein identity** (`d=1`, dir `−1`): `= −∫L` | **Proved** |

### `IntervalCLT.lean` — scalar interval-data CLT (Beresteanu–Molinari Theorem 3.2)

The Tier-1 capstone: `√n · H(Ȳₙ, E[Y]) ⇒ max(|z_L|, |z_U|)`, a continuous-mapping image of the multivariate CLT (`Stat/CLT/GaussianLimit`) through the Hausdorff keystone.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `eucl₂ v` | `EuclideanSpace ℝ (Fin 2)` | Pack a pair `Fin 2 → ℝ` into `ℝ²` (the `PiLp 2` synonym) | **Defined** |
| `maxAbs w` | `ℝ := max \|w 0\| \|w 1\|` | The max-abs functional on `ℝ²` (the Hausdorff statistic in coordinates) | **Defined** |
| `continuous_maxAbs` / `measurable_maxAbs` | `Continuous`/`Measurable maxAbs` | `maxAbs` is continuous (hence measurable) | **Proved** |
| `normalizedSum_maxAbs_clt` | `Tendsto_dist_vec (maxAbs∘normalizedSum) ((gaussianLimit ψ).map maxAbs) μ _` | **Abstract continuous-mapping CLT**: `maxAbs` of the vector normalised sum ⇒ pushforward of the Gaussian limit by `maxAbs` (via `clt_normalizedSum_vec` + `Tendsto_dist_vec.map_continuous`) | **Proved** |
| `sqrt_inv_centered` | `(√n)⁻¹·(s − n·c) = √n·(s/n − c)` | Centered-sum / `√n` algebra (normalised centered sum = `√n × (sample mean − pop mean)`) | **Proved** |
| `sampleMean S y n ω` | `ℝ := (∑_{i<n} y (Zᵢ ω)) / n` | Sample mean of `y` over the first `n` draws | **Defined** |
| `intervalIFVec yL yU P` | `X → ℝ²` | Centered endpoint influence function `ψ(z) = (y_L(z) − E y_L, y_U(z) − E y_U)` | **Defined** |
| `sampleMean_le` | `y_L ≤ y_U ⇒ sampleMean yL ≤ sampleMean yU` | Sample-mean interval is well-ordered | **Proved** |
| `maxAbs_normalizedSum_eq` | `maxAbs (normalizedSum) = √n · H(Ȳₙ, E[Y])` | **The Hausdorff bridge**: the statistic is the scaled Hausdorff distance between the sample-mean interval `Ȳₙ` and `E[Y] = [E y_L, E y_U]` | **Proved** |
| `interval_data_clt` | `Tendsto_dist_vec (fun n ω => √n·H(Ȳₙ,E[Y])) ((gaussianLimit ψ).map maxAbs) μ _` | **Beresteanu–Molinari Theorem 3.2**: `√n · H(Ȳₙ, E[Y]) ⇒ max(\|z_L\|, \|z_U\|)` (CLT ∘ bridge via `congr_ae`). Takes the centered-`ψ` multivariate-CLT hypotheses + statistic measurability | **Proved** |

### `SetValued.lean` — compact-convex set-valued RVs & the Minkowski-mean support bridge (Tier-2 substrate, general `d`)

The general-`d` substrate for the support-process CLT. A set-valued random variable takes **body** values (nonempty compact convex sets, the class `𝒦ₖ(E)`); on a compact set the linear functional `⟪d,·⟫` attains its sup so `supportFn (F ω) d` is well-defined. The one load-bearing fact is the **Minkowski-mean support bridge**, which turns the support value of an empirical Minkowski average into an ordinary sample mean. Pure convex geometry — no probability, no atomlessness.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `IsBody C` | `structure` (`nonempty`, `isCompact`, `convex`) | A body: nonempty compact convex subset of `E` — the value type of an SVRV | **Defined** |
| `bddAbove_inner_image hC d` | `BddAbove ((⟪d,·⟫) '' C)` | On a compact set the linear functional is bounded above (Cauchy–Schwarz on the bounded set) | **Proved** |
| `IsBody.bddAbove h d` | `BddAbove ((⟪d,·⟫) '' C)` | A body's support function is well-defined | **Proved** |
| `isBody_finsetSum s F h` | `IsBody (∑ i ∈ s, F i)` | A finite Minkowski sum of bodies is a body | **Proved** |
| `supportFn_finsetSum s F d h` | `supportFn (∑ᵢ Fᵢ) d = ∑ᵢ supportFn (Fᵢ) d` | Support function commutes with finite Minkowski sums | **Proved** |
| `minkowskiMean s F` | `Set E := (\|s\|)⁻¹ • (∑ i ∈ s, F i)` | The empirical Minkowski average `(1/n) • ∑ᵢ Fᵢ` | **Defined** |
| `supportFn_minkowskiMean s F d h` | `supportFn (minkowskiMean s F) d = (\|s\|)⁻¹ · ∑ᵢ supportFn (Fᵢ) d` | **The keystone**: support of a Minkowski average = average of supports (makes `s(p,F̄ₙ)` an iid sample mean) | **Proved** |

### `SupportProcess.lean` — Tier-2 finite-direction support-process CLT (general `d`)

The general-`d` analogue of `IntervalCLT`, the honest finite-dimensional projection of Beresteanu–Molinari Theorem A.2 (the full `C(Sᵈ⁻¹)` Banach CLT is the deferred Tier-3 gap). Over `k` fixed directions, the centered support process `ψ(z)_j = s(pⱼ,F(z)) − E s(pⱼ,F)` ⇒ a `k`-variate Gaussian; the Hausdorff-over-grid `ℓ^∞` statistic converges accordingly. The `SetValued` section instantiates the abstract shell on genuine random sets.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `maxAbsK w` | `ℝ := Finset.univ.sup' _ (fun j => \|w j\|)` | The `ℓ^∞`/Hausdorff-over-grid functional `supⱼ \|wⱼ\|` on `EuclideanSpace ℝ (Fin k)` (`[NeZero k]`); generalizes `maxAbs` (`Fin 2`) | **Defined** |
| `continuous_maxAbsK` / `measurable_maxAbsK` | `Continuous`/`Measurable maxAbsK` | The grid functional is continuous (hence measurable) | **Proved** |
| `supportProcess_sup_clt` | `Tendsto_dist_vec (maxAbsK ∘ normalizedSum) ((gaussianLimit ψ).map maxAbsK) μ _` | **BM Theorem A.2, finite-grid form (abstract shell)**: `maxAbsK` of the vector normalised process ⇒ `(gaussianLimit ψ).map maxAbsK` = law of `supⱼ \|z(pⱼ)\|`. General-`d` analogue of `normalizedSum_maxAbs_clt` | **Proved** |
| `supportProcess F EF p` | `X → EuclideanSpace ℝ (Fin k)` | The centered **support process** `ψ(x)_j = s(pⱼ, F x) − s(pⱼ, EF)` over a grid of `k` directions; `∫ψ=0` is the carried general-`d` Artstein condition | **Defined** |
| `supportProcess_ofLp` | `(supportProcess F EF p x).ofLp j = s(pⱼ,F x) − s(pⱼ,EF)` | Coordinate access (`rfl`) | **Proved** |
| `supportProcess_normalizedSum_apply` | `normalizedSum (supportProcess ..) n ω j = √n · (s(pⱼ, F̄ₙ) − s(pⱼ, EF))` | **The Minkowski-mean bridge in coordinates**: the `j`-th coordinate of the normalised process is the rescaled support deviation of the empirical Minkowski average `F̄ₙ = minkowskiMean (range n) (F∘Z)`; via `supportFn_minkowskiMean` | **Proved** |
| `setValued_supportProcess_clt` | `Tendsto_dist_vec (maxAbsK ∘ normalizedSum (supportProcess ..)) ((gaussianLimit ψ).map maxAbsK) μ _` | **BM Theorem A.2 on genuine random sets**: the `ℓ^∞`-over-grid statistic of normalised support deviations ⇒ `maxⱼ\|z(pⱼ)\|`; coordinates given by the bridge above (literal form `√n·maxⱼ\|s(pⱼ,F̄ₙ)−s(pⱼ,EF)\|`) | **Proved** |

### `GridTest.lean` — finite-grid specification test (Beresteanu–Molinari Theorem 2.2)

The two-sided test of `H₀ : E[F] = EF` built on `setValued_supportProcess_clt`, rejecting when the rescaled Hausdorff-over-grid statistic exceeds a critical value.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `gridTestStat S F EF p` | `ℕ → Ω → ℝ` | The test statistic `Tₙ = maxAbsK(normalizedSum(supportProcess ..))` = `maxⱼ √n·\|s(pⱼ,F̄ₙ)−s(pⱼ,EF)\|` | **Defined** |
| `gridTestReject S F EF p n c` | `Set Ω := {ω \| c < Tₙ ω}` | Rejection region at critical value `c` | **Defined** |
| `gridTestStat_clt` | `Tendsto_dist_vec (gridTestStat ..) ((gaussianLimit ψ).map maxAbsK) μ _` | Under `H₀`, `Tₙ ⇒ maxⱼ\|z(pⱼ)\|`; thin restatement of `setValued_supportProcess_clt` | **Proved** |
| `gridTest_asymptotic_level` | `μ(gridTestReject .. c) → L(Ioi c)` (`L{c}=0`) | **BM Theorem 2.2 asymptotic level**: at a continuity point `c` of the limit law `L`, the rejection probability → the limit-law tail `L(c,∞)`; at the `(1−α)`-quantile this is level `α`. Via portmanteau (`tendsto_measure_of_null_frontier_of_tendsto'`) on `Ioi c` (frontier `{c}` null) | **Proved** |

Additional results landed in `IntervalCLT.lean` (moment discharge): `measurable_intervalIFVec`, `intervalIFVec_var_integrable`, `intervalIFVec_integrable`, `intervalIFVec_mean_zero`, `intervalIFVec_sum_aemeasurable`, `intervalIFVec_hHmeas` discharge the six analytic side-conditions of `interval_data_clt` from clean hypotheses, yielding the self-contained headline **`interval_data_clt_of_memLp`** (`√n·H(Ȳₙ,E[Y]) ⇒ max(|z_L|,|z_U|)` requiring only `Measurable yL/yU`, `MemLp yL/yU 2 P`, `yL ≤ yU`). All proved, axiom-clean.

### `IntervalInference.lean` — confidence regions & coverage (Beresteanu–Molinari §2–3, Prop 2.7)

Confidence-region geometry for the interval-identified mean + the directed-Hausdorff CLT and its asymptotic coverage.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `dilate A r` | `Set ℝ := A ⊕ [−r,r]` | Minkowski dilation; the BM region `Uₙ = Ȳₙ ⊕ B(0, c/√n)` | **Defined** |
| `dilate_Icc hab hr` | `[a,b] ⊕ [−r,r] = [a−r, b+r]` | Dilation of an interval (`a≤b`, `r≥0`) | **Proved** |
| `subset_dilate_iff_directedHausdorff_le` | `[μL,μU] ⊆ [yl,yu]⊕[−r,r] ↔ dᴴ ≤ r` | **BM Prop 2.7 core**: one-sided coverage ↔ directed Hausdorff within radius | **Proved** |
| `subset_dilate_iff_hausdorff_le` | mutual containment `↔ H ≤ r` | Symmetric (two-sided) coverage characterization | **Proved** |
| `coverage_event_eq` | `{E[Y]⊆Uₙ} = {√n·dᴴ(E[Y],Ȳₙ) ≤ c}` | Coverage event = directed-statistic event (`n≥1`, `c≥0`) | **Proved** |
| `dirStat w` | `ℝ := max 0 (max w₀ (−w₁))` | Directed-Hausdorff functional on `ℝ²` (the `dᴴ` analogue of `maxAbs`) | **Defined** |
| `continuous_dirStat` / `measurable_dirStat` | — | `dirStat` continuous (hence measurable) | **Proved** |
| `normalizedSum_dirStat_clt` | `Tendsto_dist_vec (dirStat∘normalizedSum) ((gaussianLimit ψ).map dirStat) μ _` | Directed continuous-mapping CLT | **Proved** |
| `dirStat_normalizedSum_eq` | `dirStat(normalizedSum) = √n · dᴴ(E[Y], Ȳₙ)` | Directed Hausdorff bridge | **Proved** |
| `directedRegion_coverage` | `μ{E[Y] ⊆ Uₙ} → L(−∞,c]` | **BM Proposition 2.7**: coverage of the identified set converges to the limit-law mass of `Iic c` (`L = (gaussianLimit ψ).map dirStat`); at the `(1−α)`-quantile (`L{c}=0`) this is asymptotic `1−α` coverage. Via the directed CLT + portmanteau `tendsto_measure_of_null_frontier_of_tendsto'` (frontier `{c}` null) on `coverage_event_eq` | **Proved** |

**Frontier (Tier 3, deferred):** the full `C(Sᵈ⁻¹)` Banach CLT (Jain–Marcus) + BLP (Thm 4.3) — same Mathlib gap class as the bracketing-entropy chaining frontier (§12ep.d).

---

## 9j2cs. `PO/ID/Partial/CriterionSet/` — Criterion-function set inference (Chernozhukov–Hong–Tamer 2007)

The second pillar of partial-ID inference, complementary to the support-function/random-set program (§9j2rs): a partially identified parameter is the **argmin set** of a population criterion `Q`, estimated by a **level set** of the sample criterion `Q̂ₙ`. Consistency is phrased in the *same* metric Hausdorff distance (`RandomSet.directedHausdorff`/`hausdorffDist`) that drives Beresteanu–Molinari, tying the two pillars to one geometry. All declarations **proved, sorry-free**.

### `Basic.lean` — criterion, identified set, level-set estimator

| Symbol | Type | Description | Status |
|---|---|---|---|
| `identifiedSet Q` | `Set Θ := {θ \| Q θ = 0}` | The identified set = zero set (argmin of a nonnegative criterion normalised to inf 0) | **Defined** |
| `levelSet Qn c` | `Set Θ := {θ \| Qn θ ≤ c}` | The level-set estimator (sample analogue, relaxed by cutoff `c`) | **Defined** |
| `mem_identifiedSet` / `mem_levelSet` | membership `Iff.rfl` (`@[simp]`) | Unfolding lemmas | **Proved** |
| `levelSet_mono h` | `c₁ ≤ c₂ → levelSet Qn c₁ ⊆ levelSet Qn c₂` | Level set monotone in the cutoff | **Proved** |
| `identifiedSet_eq_levelSet_zero_of_nonneg hQ` | `identifiedSet Q = levelSet Q 0` | For `Q ≥ 0` the identified set is the level-`0` set | **Proved** |
| `identifiedSet_subset_levelSet_self hc` | `identifiedSet Q ⊆ levelSet Q c` (`c≥0`) | Identified set sits inside any nonneg-cutoff level set of the same criterion | **Proved** |

### `Consistency.lean` — Hausdorff consistency of the level-set estimator

The mathematical core is a deterministic rate bound under a linear identifiability minorant; consistency is then a squeeze. Reuses the metric Hausdorff machinery of `RandomSet/Hausdorff.lean`.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `LinearMinorant Q δ` | `Prop := ∀ θ, δ · d(θ, Θ_I) ≤ Q θ` | The CHT polynomial-minorant identifiability condition (degree `γ=1`): `Q` grows ≥ linearly away from `Θ_I` | **Defined** |
| `directedHausdorff_nonneg` / `hausdorffDist_nonneg` | `0 ≤ ·` | Metric Hausdorff distances are nonnegative | **Proved** |
| `directedHausdorff_le_of_forall hM h` | `(∀ a∈A, infDist a B ≤ M) → dᴴ A B ≤ M` | Directed Hausdorff bound from a uniform point-to-set bound | **Proved** |
| `directedHausdorff_eq_zero_of_subset h` | `A ⊆ B → dᴴ A B = 0` | Directed distance vanishes on a subset | **Proved** |
| `hausdorffDist_levelSet_le` | `H(levelSet Qn c, identifiedSet Q) ≤ (c+ε)/δ` | **Deterministic CHT rate bound** under `LinearMinorant Q δ` (`δ>0`), sup-norm error `\|Qn−Q\|≤ε`, cutoff `c≥ε`: estimator within `(c+ε)/δ` of `Θ_I` (minorant pins `Θ̂ₙ`; `Θ_I ⊆ Θ̂ₙ` outright) | **Proved** |
| `tendsto_hausdorffDist_levelSet` | `Tendsto (fun n => H(levelSet (Qn n) (c n), Θ_I)) atTop (𝓝 0)` | **Hausdorff consistency**: with `εₙ→0` (ULLN input) and `εₙ ≤ cₙ → 0`, the level-set estimator is consistent for the identified set (squeeze on the rate bound) | **Proved** |

---

## 9j2sg. `PO/ID/Partial/SupportFunction/Sensitivity.lean` — Reweighting / Sensitivity Models (worst-case mean under a budget)

Sensitivity analysis replaces an unidentified reweighting `w` (the weights that would recover the estimand under unconfoundedness) by an **ambiguity set** `W` and reports the sharp range of the reweighted mean `⟪c, w⟫` over `W`. This is the **applications layer of `SupportFunction/`**: the support-function picture instantiated for sensitivity budgets (general robust-bound vocabulary + the χ²/L² ball), all in one file.

### Reweighting layer — sensitivity vocabulary + structural facts

The general ambiguity-set layer: the robust bounds are the support-interval endpoints, monotone in the budget and collapsing under no ambiguity.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `robustUpper W c` | `ℝ := supportFn W c` | Upper robust bound (worst case from above) of the reweighted mean over ambiguity set `W` | **Defined** |
| `robustLower W c` | `ℝ := -supportFn W (-c)` | Lower robust bound (worst case from below) | **Defined** |
| `robustLower_le_robustUpper` | `robustLower W c ≤ robustUpper W c` | The robust interval is well-ordered (width `≥ 0`) | **Proved** |
| `le_robustUpper` / `robustLower_le` | `⟪c,w⟫ ≤ robustUpper W c` / `robustLower W c ≤ ⟪c,w⟫` | A feasible weight's reweighted mean lies in the robust interval | **Proved** |
| `robustUpper_mono` | `W ⊆ W' → robustUpper W c ≤ robustUpper W' c` | **Budget monotonicity** (upper): a larger ambiguity set raises the upper bound | **Proved** |
| `robustLower_antitone` | `W ⊆ W' → robustLower W' c ≤ robustLower W c` | **Budget monotonicity** (lower): a larger ambiguity set lowers the lower bound (interval widens) | **Proved** |
| `robustUpper_singleton` / `robustLower_singleton` | `robust* {w₀} c = ⟪c,w₀⟫` | **No ambiguity ⇒ point identification** at `⟪c,w₀⟫` (`@[simp]`) | **Proved** |
| `robustInterval_eq_image` | `(⟪c,·⟫)''W = Icc (robustLower W c) (robustUpper W c)` | For compact convex `W` the robust bounds are sharp (the interval is the identified set) | **Proved** |

### χ²/L² divergence-ball model — closed form via the affine-ball engine

The χ²/L² ambiguity set `l2Ball e ρ = {w | ⟪e,w⟫ = 1 ∧ ‖w − e‖ ≤ ρ}` (unit base direction `e`, signed-reweighting relaxation of the χ²-ball). Substituting `v = w − e` reduces it to the affine-ball fiber of `A = innerSL ℝ e`, giving the classic **mean ± `ρ·SD`** band. Reading `e` as the constant and `c = Y` in `L²(P)`: `⟪e,c⟫ = 𝔼[Y]`, `‖c‖²−⟪e,c⟫² = Var(Y)`.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `l2Ball e ρ` | `Set H := {w | ⟪e,w⟫ = 1 ∧ ‖w−e‖ ≤ ρ}` | The χ²/L² ambiguity set: weights normalized against `e`, within an L²-ball of radius `ρ` | **Defined** |
| `mem_l2Ball` | `w ∈ l2Ball e ρ ↔ ⟪e,w⟫ = 1 ∧ ‖w−e‖ ≤ ρ` | Membership unfolding (`@[simp]`) | **Proved** |
| `opKer_innerSL_eq` | `opKer (innerSL ℝ e) = (ℝ ∙ e)ᗮ` | The free-direction kernel is the orthogonal complement of the base direction | **Proved** |
| `norm_orthogonalProjection_opKer_innerSL` | `‖P_{ker} c‖ = √(‖c‖²−⟪e,c⟫²)` | Free-direction projection norm equals the standard deviation (under `‖e‖=1`) | **Proved** |
| `l2Ball_eq_translate` | `l2Ball e ρ = (e + ·)''(affineBall (innerSL ℝ e) 0 ρ)` | Reduction to the affine-ball engine (substitution `v = w−e`) | **Proved** |
| `supportFn_l2Ball_eq` | `supportFn (l2Ball e ρ) c = ⟪e,c⟫ + ρ·√(‖c‖²−⟪e,c⟫²)` | **Closed-form worst case** (`mean + ρ·SD`), `‖e‖=1`, `ρ≥0` | **Proved** |
| `width_l2Ball_eq` | `width (l2Ball e ρ) c = 2ρ·√(‖c‖²−⟪e,c⟫²)` | Closed-form identified width (`2ρ·SD`) | **Proved** |
| `l2Ball_point_identified_iff` | `width (l2Ball e ρ) c = 0 ↔ ‖c‖² = ⟪e,c⟫²` | **Point identification** (`ρ>0`): collapse iff `c` collinear with `e` (outcome constant; equality in Cauchy–Schwarz) | **Proved** |

---

## 9j2msm. `PO/ID/Partial/Sensitivity/MSM/Setup.lean` — Marginal Sensitivity Model for `E[Y(1)]` (Tan / Zhao–Small–Bhattacharya)

The **marginal sensitivity model** in the measure-theoretic backdoor PO framework (`POBackdoorSystem`): partial identification of `E[Y(1)]` when no-unmeasured-confounding is relaxed by a bounded odds ratio `Λ`. The complete propensity `e₀(X,Y(1)) = P[D=1 | σ(X,Y(1))]` (conditioning on the covariate *and* the treated potential outcome) is allowed to differ from the observed propensity `e(X)`, with the discrepancy controlled by `Λ`. Builds on `PO/ID/Exact/ATE.lean` (`propScore`, the condExp machinery); the IPW bridge is the genuine measure-theoretic content.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `OR p₁ p₂` | `ℝ := (p₁/(1−p₁))/(p₂/(1−p₂))` | The odds ratio of two probabilities | **Defined** |
| `completeProp` | `Ω → ℝ := μ[1_{D=1} \| σ(X,Y(1))]` | The complete propensity `e₀(X,Y(1))` — conditions on covariate + treated potential outcome | **Defined** |
| `candMean ẽ` | `ℝ := ∫ 1_{D=1}·Y/ẽ` | Candidate IPW mean `E[A·Y/ẽ]` for a candidate complete propensity `ẽ` | **Defined** |
| `Y1mean` | `ℝ := ∫ Y(1)` | The estimand `μ = E[Y(1)]` | **Defined** |
| `MSMSet Λ` | `Set (Ω→ℝ)` | The MSM ambiguity set: candidates a.e. in `(0,1)` with `1/Λ ≤ OR(ẽ, e(X)) ≤ Λ` a.e. | **Defined** |
| `msmUpper Λ` / `msmLower Λ` | `ℝ := sSup/sInf (candMean '' MSMSet Λ)` | The MSM interval endpoints | **Defined** |
| `candMean_completeProp_eq_Y1mean` | `candMean e₀ = E[Y(1)]` | **IPW / tower bridge** — the candidate mean at the *true* complete propensity recovers the estimand (condExp pull-out + cancellation; needs consistency, `e₀>0` a.e., IPW-integrability of `A·Y/e₀`) | **Proved, axiom-clean** |
| `Y1mean_mem_Icc` | `E[Y(1)] ∈ Icc (msmLower Λ) (msmUpper Λ)` | **The bound** — the estimand lies in the MSM interval (truth is a feasible candidate) | **Proved, axiom-clean** |
| `MSMSet_mono` | `1≤Λ → Λ≤Λ' → MSMSet Λ ⊆ MSMSet Λ'` | The ambiguity set grows with the sensitivity parameter | **Proved, axiom-clean** |
| `msmUpper_mono` / `msmLower_anti` | interval widens in `Λ` | **Budget monotonicity** of the MSM interval | **Proved, axiom-clean** |
| `MSMSet_one_eq` | `ẽ ∈ MSMSet 1 → ẽ =ᵐ e(X)` | **Λ=1 collapse**: no slack forces `OR=1 ⇒ ẽ = e(X)` a.e. (point identification = the NUC/IPW value) | **Proved, axiom-clean** |

Closed form (`Bounds.lean`), with `wMin = 1+(1−e)/(Λe)`, `wMax = 1+Λ(1−e)/e` the box endpoints:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `wMin Λ` / `wMax Λ` | `Ω → ℝ` | The smallest/largest admissible inverse-propensity weight (`OR = 1/Λ` / `OR = Λ` endpoints) | **Defined** |
| `msmUpperForm Λ` / `msmLowerForm Λ` | `ℝ := ∫ A·Y·(wMax/wMin if Y≥0 else wMin/wMax)` | The closed-form upper/lower integrals | **Defined** |
| `msmUpper_eq` / `msmLower_eq` | `msmUpper Λ = msmUpperForm Λ` / `msmLower Λ = msmLowerForm Λ` | **Closed form of the MSM bounds** — the box optimization is pointwise-separable (worst-case weight `wMax` where `Y≥0`, `wMin` where `Y<0`), attained by a feasible boundary `ẽ*`. Needs `1≤Λ`, overlap, envelope-integrability, and a.e.-measurability of candidates | **Proved, axiom-clean** |

Sharp (calibrated) set (`Sharp.lean`) — the Dorn–Guo refinement: data-compatibility adds the calibration constraint `E[Z/ẽ | σ(X)] = 1`:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `Calibrated ẽ` / `MSMSetCalib Λ` | `Prop` / `Set (Ω→ℝ)` | calibration `E[Z/ẽ\|σ(X)] =ᵐ 1`; the box members that are calibrated (data-compatible) | **Defined** |
| `msmUpperCalib Λ` / `msmLowerCalib Λ` | `ℝ := sSup/sInf (candMean '' MSMSetCalib Λ)` | the **sharp** interval endpoints | **Defined** |
| `completeProp_calibrated` | `Calibrated e₀` | **the truth is calibrated** — `E[Z/e₀\|σ(X)]=1` by the tower property (`e₀` conditions on `Y(1)`); same condExp pull-out as the IPW bridge | **Proved, axiom-clean** |
| `Y1mean_mem_Icc_calib` | `E[Y(1)] ∈ Icc (msmLowerCalib Λ) (msmUpperCalib Λ)` | **the sharp interval is valid** (contains the truth) | **Proved, axiom-clean** |
| `msmUpperCalib_le_msmUpper` / `msmLower_le_msmLowerCalib` | `msmUpperCalib Λ ≤ msmUpper Λ`, `msmLower Λ ≤ msmLowerCalib Λ` | **the sharp interval is tighter than the ZSB interval** (`MSMSetCalib ⊆ MSMSet`) | **Proved, axiom-clean** |

Quantile balancing (`QuantileBalance.lean`) — the Dorn–Guo sharp closed form, via the Neyman–Pearson exchange argument:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `cutoffProp Λ c` | `Ω → ℝ := 1/(wMax if Y > c(X) else wMin)` | the quantile-cutoff complete propensity at `σ(X)`-measurable cutoff `c` | **Defined** |
| `cutoff_optimal` | `ẽ ∈ MSMSetCalib Λ → candMean ẽ ≤ candMean (cutoffProp Λ c)` | **optimality of the cutoff weight** — Neyman–Pearson/Dantzig–Wald exchange: `candMean ẽ − candMean(1/w_c) = ∫A·(Y−c)·(w_ẽ−w_c) ≤ 0` (the `c`-term cancels by calibration, the rest is `≤0` pointwise) | **Proved, axiom-clean** |
| `msmUpperCalib_eq_cutoff` | `msmUpperCalib Λ = candMean (cutoffProp Λ c)` | **the sharp upper bound has the quantile-balancing closed form** (given a calibrating cutoff `c`) | **Proved, axiom-clean** |

Cutoff existence — calibration brackets (`CutoffExists.lean`): `wMin_mul_propScore_le_one` (`wMin·e = e+(1−e)/Λ ≤ 1`), `one_le_wMax_mul_propScore` (`1 ≤ e+Λ(1−e) = wMax·e`), and the condExp pull-outs `condExp_treat_wMin_eq` / `condExp_treat_wMax_eq` (`E[Z·wMin\|σ(X)] = wMin·e`, resp. `wMax`). Together: the conditional calibration value at the extreme cutoffs is `E[Z·wMin\|σ(X)] = wMin·e ≤ 1 ≤ wMax·e = E[Z·wMax\|σ(X)]`, so `1` is bracketed and a calibrating cutoff exists. **Proved, axiom-clean.**

Cutoff calibration → survival equation (`CutoffSelection.lean`): `cutoff_calibValue_eq` decomposes `E[Z/cutoffProp Λ c\|σ(X)] = wMin·e + (wMax−wMin)·G(c)` (`G(c) = E[Z·1{Y>c}\|σ(X)]` the conditional treated-survival); `cutoffProp_calibrated_of_survival` ⇒ the cutoff is calibrated iff `G(c) = survTarget := (1−wMin·e)/(wMax−wMin)`; `cutoffProp_mem_MSMSet` (the cutoff is always in the box); and `cutoffProp_mem_MSMSetCalib_of_survival` — **the Phase-4 `hcut_mem` holds as soon as `c` solves `G(c) = survTarget`**. **Proved, axiom-clean.**

Cutoff construction — the sharp bound closed unconditionally (`CutoffConstruct.lean`): constructs the calibrating cutoff and discharges `hcut_mem`. `treatedXYLaw` (treated push-forward of `(X,Y)`), `treatedCondCDF` (its Mathlib-`condCDF`), `calibLevel Λ = 1 − survTarget/e`:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `treatedSurv_const_eq` | `E[Z·1{Y>t}\|σ(X)] =ᵐ e(X)·(1 − F(t\|X))` | **the core bridge** — the weighted conditional survival equals the treated conditional CDF scaled by the propensity (condExp-uniqueness on σ(X)-sets: both sides `= μ(s∩{D=1}∩{Y≤t})`; `setLIntegral_condCDF` + `e=E[Z\|σX]` pull-out + σ(X)-factorization) | **Proved, axiom-clean** |
| `treatedSurv_eq` | `E[Z·1{Y>c(X)}\|σ(X)] =ᵐ e(X)·(1 − F(c(X)\|X))` | the variable-cutoff bridge (frozen σ(X)-measurable `c`; variable-cutoff `condCDF` set-integral via the `condKernel` disintegration) | **Proved, axiom-clean** |
| `exists_calibrating_cutoff` | `∃ c, Measurable[σX] c ∧ treatedSurv c =ᵐ survTarget Λ` | **the calibrating cutoff exists** — the conditional quantile `Q_{calibLevel}(X)` (via `measurable_condQuantile_and_attains`); needs overlap, `1<Λ`, atomless treated outcome law, interior level | **Proved, axiom-clean** |
| `msmUpperCalib_eq_cutoff_unconditional` | `∃ c, … ∧ msmUpperCalib Λ = candMean (cutoffProp Λ c)` | **the Dorn–Guo sharp upper bound, UNCONDITIONALLY** — the quantile-balancing closed form with `hcut_mem` discharged (only overlap / `1<Λ` / atomless / interior-level / envelope-integrability regularity) | **Proved, axiom-clean** |

Lower bound (`LowerBound.lean`) — the `sInf` mirror: the lower cutoff `lowerCutoffProp Λ c = 1/(wMin if Y>c else wMax)` (opposite weights) *minimizes* the candidate mean; `survTargetLower = (wMax·e−1)/(wMax−wMin) = e − survTarget`:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `lowerCutoffProp` / `survTargetLower` / `calibLevelLower` | defs | the minimizing cutoff weight, its calibration target, and quantile level | **Defined** |
| `cutoff_optimal_lower` | `candMean (lowerCutoffProp Λ c) ≤ candMean ẽ` for calibrated `ẽ` | the lower cutoff is the Neyman–Pearson minimizer | **Proved, axiom-clean** |
| `msmLowerCalib_eq_cutoff` / `lowerCutoffProp_mem_MSMSetCalib_of_survival` | closed form given a calibrating cutoff; calibration ⟺ `treatedSurv c = survTargetLower` | mirror of the upper reductions | **Proved, axiom-clean** |
| `exists_calibrating_cutoff_lower` | `∃ c, … ∧ treatedSurv c =ᵐ survTargetLower Λ` | the lower calibrating cutoff exists (`Q_{calibLevelLower}(X)`) | **Proved, axiom-clean** |
| `msmLowerCalib_eq_cutoff_unconditional` | `∃ c, … ∧ msmLowerCalib Λ = candMean (lowerCutoffProp Λ c)` | **the Dorn–Guo sharp LOWER bound, unconditionally** | **Proved, axiom-clean** |

Control-arm cutoff stack (`ControlCutoff.lean`, `ControlQuantileBalance.lean`, `ControlCutoffConstruct.lean`, `ControlLowerBound.lean`) — the exact `D=0` mirror of the treated cutoff development (`wMin0`/`wMax0`/`cutoffProp0`/`lowerCutoffProp0`/`controlSurv`/`survTarget0`/`controlXYLaw`, the control condExp↔condCDF bridge `controlSurv_const_eq`/`controlSurv_eq`, optimality, calibration→survival reduction, construction). Capstones: `msmUpperCalib0_eq_cutoff_unconditional` and `msmLowerCalib0_eq_cutoff_unconditional` — the control sharp bounds in unconditional closed form. **Proved, axiom-clean.**

ATE closed form (`ATEClosedForm.lean`) — combines the four arm closed forms into the sharp ATE interval:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `ate_endpoints_eq_cutoff` | `ateUpperCalib = candMean(cutoffProp cTU) − candMean0(lowerCutoffProp0 cCL)`, `ateLowerCalib = candMean(lowerCutoffProp cTL) − candMean0(cutoffProp0 cCU)` | **the sharp ATE endpoints in closed form** (treated upper − control lower, treated lower − control upper), each a conditional-quantile candidate mean | **Proved, axiom-clean** |
| `ate_mem_Icc_cutoff` | `ate ∈ Icc (…closed lower…) (…closed upper…)` | **the true ATE lies in the sharp closed-form interval** (endpoints + `ate_mem_Icc_calib` validity) | **Proved, axiom-clean** |

Gaussian special case (`Gaussian.lean`) — the Dorn–Guo corollary under a conditional-Gaussian treated outcome law:

| Symbol | Type | Description | Status |
|---|---|---|---|
| `calibLevel_eq` | `calibLevel Λ =ᵐ Λ/(Λ+1)` | **the cutoff quantile level is exactly `Λ/(Λ+1)`**, independent of the propensity (since `survTarget = e/(Λ+1)`) | **Proved, axiom-clean** |
| `GaussianTreatedModel m σ` | structure | the treated conditional law of `Y` given `X` is `N(m(X), σ(X)²)` (`condCDF treatedXYLaw a = Φ((·−m a)/σ a)`) | **Defined** |
| `gaussianCutoff` / `gaussianCutoff_calibrates` | `c(X)=m(X)+σ(X)·Φ⁻¹(Λ/(Λ+1))`; it solves `treatedSurv c =ᵐ survTarget Λ` | **the explicit Gaussian quantile cutoff** calibrates — no measurable-selection needed | **Proved, axiom-clean** |
| `msmUpperCalib_gaussian` | `msmUpperCalib Λ = candMean (cutoffProp Λ (m(X)+σ(X)·Φ⁻¹(Λ/(Λ+1))))` | **the sharp upper bound at the explicit Gaussian cutoff** | **Proved, axiom-clean** |

| `GaussianOutcomeModel` / `msmUpperCalib_gaussian_halfWidth` (`GaussianHalfWidth.lean`) | `msmUpperCalib Λ = ∫ m(X) + (Λ²−1)/Λ·φ(Φ⁻¹(Λ/(Λ+1)))·∫ (1−e(X))·σ(X)` | **the Dorn–Guo Gaussian closed-form half-width** — point-identified mean plus the half-width (`gaussianMSMHalfWidthFactor`); via the conditional Gaussian mean/truncated-mean moments + the per-stratum collapse `wMin·e+(wMax−wMin)e/(Λ+1)=1` | **Proved, axiom-clean** |

(The reusable standard-normal substrate `Φ`, `probit = Φ⁻¹`, the truncated first moment `∫_c^∞ tφ = φ(c)`, and the general Gaussian truncated moment `∫_c^∞ y dN(m,v) = m(1−Φ)+√v·φ` are in `Mathlib/Probability/StdNormalCDF.lean`, `StdNormalMoments.lean`, `GaussianMoments.lean`, §10a'''.)

Scope: Phases 1–7 complete for BOTH arms (`E[Y(1)]`, `E[Y(0)]`) and the **ATE** — the sharp interval has unconditional quantile-balancing closed forms at every endpoint — plus the **Gaussian special case in full closed form** (explicit quantile cutoff + the half-width number). The MSM sharp-bound program is complete. The atomless (continuous-outcome) hypothesis is the Dorn–Guo caveat, faithfully carried; the covariate space `γ` stays a general measurable space. See `doc/msm_sensitivity_plan.md`.

Control arm (`ControlSetup.lean`, `ControlSharp.lean`) — the `D=0` reflection of `Setup.lean`/`Sharp.lean`, bounding `E[Y(0)]` by IPW-weighting `1_{D=0}` against the complete control propensity `P[D=0 | σ(X,Y(0))]` (vs. observed `P[D=0 | σ(X)] = propScore false`). Every declaration mirrors the treated arm with a `0` suffix: `sigmaXY0`, `completeProp0`, `candMean0`, `Y0mean`, `MSMSet0`, `msmUpper0`/`msmLower0`, `Calibrated0`, `MSMSetCalib0`, `msmUpperCalib0`/`msmLowerCalib0`.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `candMean0_completeProp0_eq_Y0mean` | `candMean0 e₀ = E[Y(0)]` | **IPW/tower bridge (control)** — the `D=0` mirror of the treated bridge | **Proved, axiom-clean** |
| `Y0mean_mem_Icc` / `Y0mean_mem_Icc_calib` | `E[Y(0)] ∈ Icc (msmLower0 Λ) (msmUpper0 Λ)` / `… Icc (msmLowerCalib0 Λ) (msmUpperCalib0 Λ)` | **the control ZSB / sharp interval is valid** | **Proved, axiom-clean** |
| `completeProp0_calibrated` | `Calibrated0 e₀` | the true complete control propensity is calibrated (`E[(1−Z)/e₀\|σ(X)]=1`) | **Proved, axiom-clean** |
| `MSMSet0_mono` / `msmUpper0_mono` / `msmLower0_anti` / `MSMSet0_one_eq` | budget monotonicity + `Λ=1` collapse (control) | mirrors the treated structural lemmas | **Proved, axiom-clean** |
| `msmUpperCalib0_le_msmUpper0` / `msmLower0_le_msmLowerCalib0` | sharp control interval tighter than ZSB | mirrors the treated tightness lemmas | **Proved, axiom-clean** |

ATE interval (`ATE.lean`) — combines the two arms into a partial-ID interval for `τ = E[Y(1)] − E[Y(0)]` by *opposing* the arm bounds (`τ⁺ = ψ_T⁺ − ψ_C⁻`, `τ⁻ = ψ_T⁻ − ψ_C⁺`):

| Symbol | Type | Description | Status |
|---|---|---|---|
| `ate` | `ℝ := Y1mean − Y0mean` | the average treatment effect `τ` | **Defined** |
| `ateUpperCalib Λ` / `ateLowerCalib Λ` | `ℝ := msmUpperCalib Λ − msmLowerCalib0 Λ` / `msmLowerCalib Λ − msmUpperCalib0 Λ` | the **sharp** ATE interval endpoints (treated upper paired with control lower, and vice versa) | **Defined** |
| `ateUpper Λ` / `ateLower Λ` | `ℝ := msmUpper Λ − msmLower0 Λ` / `msmLower Λ − msmUpper0 Λ` | the ZSB ATE interval endpoints | **Defined** |
| `sub_mem_Icc_of_mem_Icc` | `a∈[aₗ,aᵤ] → b∈[bₗ,bᵤ] → a−b∈[aₗ−bᵤ, aᵤ−bₗ]` | interval-subtraction arithmetic core | **Proved, axiom-clean** |
| `ate_mem_Icc_calib` / `ate_mem_Icc` | `τ ∈ Icc (ateLowerCalib Λ) (ateUpperCalib Λ)` / `… (ateLower Λ) (ateUpper Λ)` | **the sharp / ZSB ATE interval is valid** (contains the true `τ`), from the two arm-wise validity statements | **Proved, axiom-clean** |
| `ateCalib_subset` | `Icc (ateLowerCalib Λ) (ateUpperCalib Λ) ⊆ Icc (ateLower Λ) (ateUpper Λ)` | the sharp ATE interval nests inside the ZSB ATE interval | **Proved, axiom-clean** |

---

## 9j3. `PO/ID/Partial/BalkePearl/` — Balke-Pearl Sharp ATE Bounds (Binary IV, PO framework)

Formalizes sharp LP bounds on ATE from Balke & Pearl (1994) for a binary IV system.
All three variables Z, D, Y are binary (`Bool`). Split into four files:

- `BalkePearl/Setup.lean` — `POBalkePearlSystem` data layer, `ATE`, `cellProb`, `boolToReal`.
- `BalkePearl/Assumptions.lean` — `BaseAssumptions`, `cfBundle`.
- `BalkePearl/LatentTable.lean` — `latentProb` (16-cell table), partition lemmas, `ATE_eq_sum_latent`, `cellProb_eq_sum_latent`.
- `BalkePearl/Main.lean` — `BPFeasible`, `BPObjective`, `BPIdentifiedInterval`, necessity theorem.
- `BalkePearl/Sharp.lean` — canonical model construction, sharpness theorem.
- `BalkePearl/IntervalForm.lean` — necessity in closed-interval (`Set.Icc`) form via the SandwichInterval engine bridge.
- `BalkePearl/ClosedForm.lean` — the endpoints as explicit formulas in the observed cell probabilities: `bpLower`/`bpUpper` as a max/min of eight affine expressions.
- `BalkePearl/Attainment/{Basic,Lower,Upper}.lean` — cell-probability validity and the sixteen witness tables attaining the individual closed-form expressions.
- `BalkePearl/ClosedFormAttainment.lean` — assembles the witnesses into `sInf = bpLower` and `sSup = bpUpper` (sharpness of the closed form).

### Setup (`BalkePearl/Setup.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `POBalkePearlSystem P` | `structure { Z, D, Y, hZbool, hDbool, hYbool, hZD, hZY, hDY }` | Binary-IV subsystem: three distinct variables with Bool codomains | **Defined** |
| `boolToReal` | `Bool → ℝ` | `false ↦ 0`, `true ↦ 1` | **Defined** |
| `ATE` | `ℝ := ∫ (YofD_real true - YofD_real false) ∂P.μ` | Target parameter, average treatment effect | **Defined** |
| `cellProb y d z` | `ℝ` | Observed joint probability `P(Y=y, D=d \| Z=z)` (as a real) | **Defined** |

### Assumptions (`BalkePearl/Assumptions.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `BaseAssumptions S` | `structure { consistency, exogeneity, ... }` | Structural assumptions: consistency of D and Y, exogeneity of Z | **Defined** |
| `cfBundle S` | auxiliary | Bundle of counterfactual random variables for the latent type partition | **Defined** |

### LatentTable (`BalkePearl/LatentTable.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `dArm z d0 d1` | `Bool` | Treatment arm selector: `d0` if `z=false`, `d1` if `z=true` | **Defined** |
| `yArm d y0 y1` | `Bool` | Outcome arm selector: `y0` if `d=false`, `y1` if `d=true` | **Defined** |
| `latentSet d0 d1 y0 y1` | `Set P.Ω` | Event that unit has latent type `(D(0),D(1),Y(0),Y(1)) = (d0,d1,y0,y1)` | **Defined** |
| `latentProb d0 d1 y0 y1` | `ℝ` | Probability of latent type `(d0,d1,y0,y1)` | **Defined** |
| `latentProb_nonneg` | `0 ≤ latentProb d0 d1 y0 y1` | Nonnegativity of cell probabilities | **Proved** |
| `latentProb_sum_eq_one` | `∑ d0 d1 y0 y1, latentProb = 1` | The 16 latent cells partition Ω | **Proved** |
| `ATE_eq_sum_latent` | `ATE = ∑ d0 d1 y0 y1, (y1-y0) * latentProb` | ATE expressed as weighted sum over latent types | **Proved** |
| `cfCellEvent z d y` | `Set P.Ω` | Cf-cell `{ω | DofZ z ω = d ∧ YofD d ω = y}` — internal helper | **private** |
| `cfTuple` | `P.Ω → Bool⁴` | Product map `(D(0), D(1), Y(0), Y(1))` — independent of `Z` under exogeneity | **private** |
| `indepFun_factualZ_cfTuple hA` | `IndepFun factualZ cfTuple P.μ` | Factual `Z` independent of cf tuple (derived from `IndepCF.project`) | **private** |
| `cellProb_eq_sum_latent hA y d z` | `cellProb y d z = ∑ compatible latentProb` | Observed cell probability = sum over compatible latent types | **Proved** |

### Main (`BalkePearl/Main.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `BPFeasible S hA π` | `structure { nonneg, sum_one, marginal }` | Feasibility predicate: π is nonneg, sums to 1, reproduces all observed cell probabilities | **Defined** |
| `BPObjective π` | `ℝ := ∑ d0 d1 y0 y1, (y1-y0) * π d0 d1 y0 y1` | LP objective functional on a latent table | **Defined** |
| `BPIdentifiedInterval S hA` | `Set ℝ` | Sharp LP identified interval for ATE: `IdentifiedInterval BPObjective (BPFeasible S hA)` | **Defined** |
| `latentProb_feasible hA` | `BPFeasible S hA S.latentProb` | The realized latent table is feasible | **Proved** |
| `ATE_eq_BPObjective` | `S.ATE = BPObjective S.latentProb` | ATE equals the LP objective at the realized table | **Proved** |
| `ATE_mem_BPIdentifiedInterval hA` | `S.ATE ∈ S.BPIdentifiedInterval hA` | NECESSITY: true ATE lies in the identified interval | **Proved** |
| `balkePearl_sharp hA π hπ` (in `Sharp.lean`) | `∃ P' S' hA', (∀ y d z, S'.cellProb y d z = S.cellProb y d z) ∧ S'.ATE = BPObjective π` | SHARPNESS: every feasible latent table is realised by a model that reproduces S's observed cell probabilities | **Proved** |
| `balkePearl_sharp_of_mem hA τ hτ` (in `Sharp.lean`) | `∃ P' S' hA', (∀ y d z, S'.cellProb y d z = S.cellProb y d z) ∧ S'.ATE = τ` | τ-form corollary of sharpness for `BPIdentifiedInterval` membership | **Proved** |

### IntervalForm (`BalkePearl/IntervalForm.lean`)

Restates necessity in the engine's closed-interval vocabulary. `BPObjective` is an
average of `±1`/`0` contrasts against a probability vector, hence always in `[-1,1]`;
this boundedness feeds the bridge `Causalean.PartialID.mem_Icc_csInf_csSup`.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `BPObjective_le_one hnn hsum` | `BPObjective π ≤ 1` | The LP objective is at most 1 on any feasible (nonneg, sum-one) table | **Proved** |
| `neg_one_le_BPObjective hnn hsum` | `-1 ≤ BPObjective π` | The LP objective is at least -1 on any feasible table | **Proved** |
| `bddBelow_BPIdentifiedInterval hA` | `BddBelow (S.BPIdentifiedInterval hA)` | The identified interval is bounded below (by -1) | **Proved** |
| `bddAbove_BPIdentifiedInterval hA` | `BddAbove (S.BPIdentifiedInterval hA)` | The identified interval is bounded above (by 1) | **Proved** |
| `ATE_mem_Icc_csInf_csSup hA` | `S.ATE ∈ Set.Icc (sInf (S.BPIdentifiedInterval hA)) (sSup (S.BPIdentifiedInterval hA))` | NECESSITY, closed-interval form: the true ATE lies between the LP minimum and maximum, via the SandwichInterval bridge | **Proved** |

### Sharp (`BalkePearl/Sharp.lean`)

Canonical-model construction (~750 lines) discharging the sharpness direction.
The model has `V' = Fin 3` (Z, D, Y), `X' v = Bool`, sample space
`Ω' = Bool × (Bool × Bool × Bool × Bool)`, and product measure
`μ' = μ_Z ⊗ π_meas` where `μ_Z` is `S`'s Z-marginal and `π_meas` weights Bool⁴
by π. The eval cascades Z → D → Y, respecting interventions.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `POBalkePearlSharp.SOmega/SV/SX` | `Type`/`Type`/`SV → Type` | Canonical sample space, variable-index, codomain assignment | **Defined** |
| `POBalkePearlSharp.eval r ω` | `∀ v, SX v` | Cascade evaluator for the canonical model | **Defined** |
| `POBalkePearlSharp.zMeasure S` | `Measure Bool` | S's Z-marginal as a discrete measure | **Defined** |
| `POBalkePearlSharp.piMeasure π` | `Measure Bool⁴` | Discrete measure with weights π | **Defined** |
| `POBalkePearlSharp.canonicalMeasure S π` | `Measure SOmega` | Product `zMeasure ⊗ piMeasure` | **Defined** |
| `POBalkePearlSharp.canonicalPOSystem S π hπ_nn hπ_sum` | `POSystem` | Canonical PO system | **Defined** |
| `POBalkePearlSharp.canonicalBP S π hπ_nn hπ_sum` | `POBalkePearlSystem _` | Canonical BP system | **Defined** |
| `POBalkePearlSharp.canonical_consistency` | `Consistency` | Factual + composition consistency on the canonical model | **Proved** |
| `POBalkePearlSharp.canonical_exclusion` | `YofZD z d = YofD d` | Pointwise (and a.e.) equality | **Proved** |
| `POBalkePearlSharp.canonical_exogeneity` | `IndepCF zVar cfBundle μ'` | Z ⊥ cf-bundle, by `indepFun_prod` on the product measure | **Proved** |
| `POBalkePearlSharp.canonical_posZ hA` | `0 < μ'(zEvent z)` | Inherited from `hA.posZ` via Z-marginal identity | **Proved** |
| `POBalkePearlSharp.canonical_baseAssumptions hA` | `S'.BaseAssumptions` | Bundle of the four | **Proved** |
| `POBalkePearlSharp.canonical_latentProb_eq` | `S'.latentProb = π` | Marginal identity | **Proved** |
| `POBalkePearlSharp.canonical_cellProb_eq hA hπ` | `S'.cellProb y d z = S.cellProb y d z` | Reproduces observed cell probabilities | **Proved** |
| `POBalkePearlSharp.canonical_ATE_eq hA` | `S'.ATE = BPObjective π` | LP objective realisation | **Proved** |
| `balkePearl_sharp hA π hπ` | (see Main row above) | The packaged sharpness theorem | **Proved** |

**Outstanding sorrys:** None. Both necessity (`ATE_mem_BPIdentifiedInterval`) and sharpness (`balkePearl_sharp`, `balkePearl_sharp_of_mem`) are fully proved.

### ClosedForm (`BalkePearl/ClosedForm.lean`)

`Main.lean` and `IntervalForm.lean` characterize the endpoints only *implicitly*, as the
infimum and supremum of the LP objective over feasible latent tables. Neither is a formula
an analyst can evaluate on a data set. `ClosedForm.lean` supplies the explicit solution of
the linear program, following Balke & Pearl (1997).

Both endpoints are piecewise-linear in the eight observed cell probabilities
`cellProb y d z = P(Y = y, D = d | Z = z)`. Concretely `bpLower` is the maximum of eight
affine expressions `bpLowerTerm i` and `bpUpper` the minimum of eight `bpUpperTerm i`. The
eight are the vertices of the dual feasible region: four of the "short" form
`p(0,0,z) + p(1,1,z') - 1` over the four instrument pairs `(z, z')`, and four "long" vertices
in which one cell probability enters with weight two. Because each expression is a dual
vertex, it is a nonnegative combination of the feasibility constraints — which is exactly
why every `bpLowerTerm i ≤ BPObjective π` discharges by `linarith` from the eight marginal
equations, normalization, and the sixteen nonnegativity facts.

The analyst-facing statement is `ATE_mem_Icc_bpLower_bpUpper : S.ATE ∈ Set.Icc S.bpLower S.bpUpper`.

Attainment of the endpoints is proved separately, in `ClosedFormAttainment.lean`.

### ClosedFormAttainment (`BalkePearl/ClosedFormAttainment.lean`)

Validity alone leaves open whether the closed form could be loose. This file closes that gap
on the lower side: `csInf_BPIdentifiedInterval_eq_bpLower` shows `bpLower` is *exactly* the
LP minimum, not merely a lower bound.

The proof exhibits, for each of the eight regions (the region where `bpLowerTerm i` is the
largest), a feasible latent table whose objective equals that expression. The eight witnesses
are the primal optima matching the eight dual vertices, located by complementary slackness.
That derivation is offline scaffolding: no duality theory appears in the Lean, each witness is
an explicit table checked directly against nonnegativity, normalization, and the eight
marginal equations.

Two details worth recording. First, four of the eight witnesses carry a free direction along
which the objective is constant; the free variable is pinned at the largest of its lower
bounds (feasible whenever anything is), which is the `max 0 _` in `bpAux*`. Setting those free
variables to zero instead yields *negative* entries and does not work. Second, feasibility of
a witness is not derivable from the observed cells being nonnegative and summing to one per
instrument value — one obligation is Pearl's instrumental inequality (`p(0,0,0) + p(1,0,1) ≤ 1`).
The witness theorems therefore take a feasible table `π₀` as a hypothesis, discharged at the
call site by `latentProb_feasible`.

Note this is a different claim from `balkePearl_sharp`, which shows every feasible objective
value is realized by some PO model but says nothing about the closed-form endpoints. Both are
needed for the closed form to be a sharp bound on the ATE.

Both endpoints are covered: `csInf_BPIdentifiedInterval_eq_bpLower` and
`csSup_BPIdentifiedInterval_eq_bpUpper`, packaged as `bpLower_bpUpper_eq_csInf_csSup`. Together
with `ClosedForm.lean`'s validity result this says the computable interval `[bpLower, bpUpper]`
has both endpoints attained, so it cannot be narrowed without further assumptions.

**Outstanding sorrys:** None.

---

## 9j4. `PO/ID/Partial/Proxy/` — Proximal Partial-Identification Bounds (Ghassami-Shpitser-Tchetgen Tchetgen 2024, Sections 2-3)

Formalises the partial-identification bounds of arXiv 2304.04374 — partial bounds on
ATE / `E[Y(a) | A=¬a]` using proxy variables of an unobserved confounder, **without
the completeness condition** required for exact identification. Reuses
`POProximalSystem` from `PO/ID/Exact/Proximal/Setup.lean` (six POVars X, A, Z, W, Y, U).
Nine files (Helpers and ZBased are split into subdirectories):

- `Proxy/Setup.lean` — additional σ-algebras, target parameters, envelope predicates.
- `Proxy/Assumptions.lean` — three weakened bundles (`WBased`, `ZBased`, `TwoProxy`).
- `Proxy/Helpers.lean` — facade; re-exports `Helpers/{Common,BridgeW,BridgeWZ,CondExpQ}`.
- `Proxy/Helpers/Common.lean` — `meanYofA_eq_strata` + `YofA_essbound_*` clamp lemmas.
- `Proxy/Helpers/BridgeW.lean` — W-only bridge identity `condIntYofA_eq_h_arm`.
- `Proxy/Helpers/BridgeWZ.lean` — two-proxy bridge identity `condIntYofA_eq_hq_armSwap_twoProxy` (paper Theorem 3, identity (★), same-arm form, prob-ratio q convention).
- `Proxy/Helpers/CondExpQ.lean` — q-collapse on `{A=a}` σ_AX (prob-ratio q): `condExp_q_eq_stratumOddsRatio_arm_AX`.
- `Proxy/WBased.lean` — Theorem 1 + Corollary 1 (outcome-confounding-proxy).
- `Proxy/ZBased.lean` — Theorem 2 + Corollary 2 (imports `ZBased/ArmChain`).
- `Proxy/ZBased/ArmChain.lean` — paired arm-chain lemmas (`condIntYofA_le_envelope_arm` / `envelope_le_condIntYofA_arm`), ~600 lines. Both arm chains are now sorry-free in their bodies: the `hStep67` factorisation step is extracted as a private helper `condIntYq_factor_arm` and shared between the upper and lower variants.
- `Proxy/TwoProxy.lean` — Theorem 3 (two conditionally independent invalid proxies).
- `Proxy/IntervalForm.lean` — `Set.Icc` interval restatements of all five sandwich bounds (via the `SandwichInterval` bridge `mem_Icc_of_sandwich`).

### Setup (`Proxy/Setup.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `S.AX`, `S.σ_AX`, `S.σ_X` | tuple maps + σ-algebras | Conditioning targets `(A,X)` and `X` for partial-ID proofs | **Defined** |
| `S.σ_X_le_σ_AX` | `S.σ_X ≤ S.σ_AX` | σ_X is coarser than σ_AX since `X = snd ∘ (A,X)`. Used in `WBased.lean` to lift σ_X-measurable `stratumOddsRatio · Uenv(a,X)` to σ_AX for the pull-out step | **Proved** |
| `S.σ_AX_le_σ_AUX` | `S.σ_AX ≤ S.σ_AUX` | σ_AX is coarser than σ_AUX since `(A,X)` factors through `(A,U,X)` via projection. Used by the σ_AX-tower step in `condExp_Y_eq_condExp_h_arm_AX` | **Proved** |
| `S.σ_X_le_σ_AUX` | `S.σ_X ≤ S.σ_AUX` | Composition `σ_X ≤ σ_AX ≤ σ_AUX`. Used by `ArmChain.hStep9` to lift `Uenv(a,X)` σ_X-meas to σ_AUX for L1 pull-out | **Proved** |
| `S.σ_X_le_σ_UX` | `S.σ_X ≤ S.σ_UX` | `X = snd ∘ (U, X)`. Used by `ArmChain.hStep10` to lift `Uenv(a,X)` σ_X-meas to σ_UX for L2 arm-swap | **Proved** |
| `S.σ_AZX_le_σ_AZUX` | `S.σ_AZX ≤ S.σ_AZUX` | `(A,Z,X) = proj ∘ (A,Z,U,X)`. Reserved for σ_AZX-pull-out steps | **Proved** |
| `S.σ_AUX_le_σ_AZUX` | `S.σ_AUX ≤ S.σ_AZUX` | `(A,U,X) = proj ∘ (A,Z,U,X)`. Reserved for σ_AUX-tower steps | **Proved** |
| `S.meanYofA μ a` | `ℝ := ∫ Y(a) dμ` | Marginal potential outcome mean | **Defined** |
| `S.condMeanYofA μ a` | `ℝ := (μ {A≠a}).toReal⁻¹ * ∫_{A≠a} Y(a) dμ` | Conditional PO mean `E[Y(a) | A=¬a]` | **Defined** |
| `S.ATE μ` | `ℝ := meanYofA μ true - meanYofA μ false` | Average treatment effect | **Defined** |
| `S.stratumOddsRatio μ a` | `P.Ω → ℝ` | σ_X-measurable `p(A=¬a|X)/p(A=a|X)` | **Defined** |
| `S.IsUpperEnvW μ a Uenv` | `Prop` | Integrated upper-envelope predicate for the W-density ratio (Thm 1) | **Defined** |
| `S.IsLowerEnvW μ a Lenv` | `Prop` | Integrated lower-envelope predicate, mirror of above | **Defined** |
| `S.IsUpperEnvZ μ a Uenv` | `Prop` | Pointwise upper bound `(μ[Y \| σ_AZX]) ≤ Uenv(a,X)` on `{A=a}` (Thm 2) | **Defined** |
| `S.IsLowerEnvZ μ a Lenv` | `Prop` | Mirror lower bound | **Defined** |
| `S.IsUpperEnvWZ μ a Uenv` | `Prop` | Same-arm joint-vs-product W-Z envelope (Thm 3): for nonneg measurable `φ_W(W,X)`, `φ_Z(Z,X)`, μ-a.e. on `{A=a}`, `μ[φ_W·φ_Z\|σ_AX] ≤ Uenv(a,X)·μ[φ_W\|σ_AX]·μ[φ_Z\|σ_AX]`. Operational form of `ρ(w,z,x)=p(w,z\|a,x)/(p(w\|a,x)·p(z\|a,x)) ≤ Uenv(a,x)`; canonical sharp choice `Uenv(a,x)=sup_{w,z} ρ(w,z,x)` | **Defined** |
| `S.IsLowerEnvWZ μ a Lenv` | `Prop` | Mirror with inequality reversed; canonical sharp `Lenv(a,x)=inf_{w,z} ρ(w,z,x)` | **Defined** |

### Assumptions (`Proxy/Assumptions.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `WBasedAssumptions S μ` | `structure { consistency, latent_exch, proxy_WA, h, h_nonneg, bridge, …, Y_bdd_below/above }` | Theorem 1 bundle: drops `proxy_YZ`, `completeness`, Z-fields. `h_nonneg : ∀ x, 0 ≤ h x` is bundled (paper Assumption 2: a non-negative bridge is *posited* to exist; not derived from `Y ⊆ [0, ∞)`, which does not force a bridge to be non-negative) | **Defined** |
| `ZBasedAssumptions S μ` | `structure { consistency, latent_exch, proxy_YZ, q, bridge_q, likelihoodRatio_swapA, integrable_likelihoodRatio_swapA, integrable_condExpYofA_mul_L, integrable_condExpY_mul_q, integrable_Y_mul_q, …, overlap_strong }` | Theorem 2 bundle: drops `proxy_WAZ`, `completeness`. **Probability-ratio q convention**: `likelihoodRatio_swapA_spec` operationally encodes `E[𝟙{A=a}\|σ_UX]·L =ᵐ E[𝟙{A=¬a}\|σ_UX]`, i.e. `L = p(¬a\|U,X)/p(a\|U,X)` on the support of `E[𝟙{A=a}\|σ_UX]`. This avoids RN derivatives; differs from the paper's density-ratio q `p(U\|¬a,X)/p(U\|a,X)` by a factor of stratumOddsRatio (both yield the same observable bound). `overlap_strong` encodes paper Assumption 3 positivity `p(A=a\|U,X) > 0`. Four explicit integrability fields cover finiteness gaps not implied by strict overlap (paper's "all integrals finite" convention; Remark 5 forbids a Y-clamp): `integrable_likelihoodRatio_swapA` (L ∈ L¹), `integrable_condExpYofA_mul_L` (`μ[Y(a)\|σ_UX]·L` ∈ L¹), `integrable_condExpY_mul_q` (`μ[Y\|σ_AZX]·q(Z,a,X)` ∈ L¹, used inside the σ_AZX-conditional envelope step), and `integrable_Y_mul_q` (`Y·q(Z,a,X)` ∈ L¹, used inside the proxy_YZ factorisation step that materialises `μ[Y\|σ_AZX]` from `μ[Y\|σ_AUX]·μ[q\|σ_AUX]`) | **Defined** |
| `TwoProxyAssumptions S μ` | `structure { consistency, latent_exch, proxy_WZ_indep, h, h_nonneg, q, bridge_h, bridge_q, likelihoodRatio_swapA, integrable_hq_arm, … }` | Theorem 3 bundle: requires both bridges, `h_nonneg`, and `W ⟂ Z \| (A,X,U)` (paper Assumption 6). No `W ⟂ A \| (U,X)` requirement (does not bundle `proxy_WA`); also drops `proxy_YZ`. Same prob-ratio q convention as `ZBasedAssumptions`. Bundles `integrable_hq_arm : ∀ a, Integrable (h(a,W,X)·q(Z,a,X)) μ` (separate L¹ does not imply product L¹) | **Defined** |

### Helpers (`Proxy/Helpers.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `meanYofA_eq_strata` | `meanYofA = ∫_{A≠a} Y(a) + ∫_{A=a} Y` | Marginalisation identity (consistency on the on-arm); used to derive Cor 1, 2 from Thm 1, 2 | **Proved** |
| `YofA_essbound_above` / `_below` | `Y(a)` inherits `Y`'s a.e. essential bound | Lifts essential bounds on `Y` to the counterfactual `Y(a)` via `latent_exch` + consistency-on-arm; consumed by the trivial-clamp steps in W-side and TwoProxy theorems | **Proved** |
| `condIntYofA_eq_h_arm` | `∫_{A=¬a} Y(a) dμ = ∫_{A=¬a} h(a,W,X) dμ` | Bridge-substitution identity for the W-only bundle | **Proved** |
| `condIntYofA_eq_hq_armSwap_twoProxy` | `∫_{A≠a} Y(a) dμ = ∫_{A=a} h(a,W,X) · q(Z,a,X) dμ` | Two-proxy bridge-substitution identity in **same-arm form** (paper Theorem 3, identity (★), prob-ratio q convention). No `stratumOddsRatio` weight: the off-arm-to-on-arm transport is absorbed into `q` itself via `likelihoodRatio_swapA_spec`. The RHS is a clean `{A=a}` integral that the σ_AX-conditional envelope `IsUpperEnvWZ`/`IsLowerEnvWZ` applies to directly — no σ_AX-fiber transport needed. Proof structured as 4 named `have`s mirroring paper steps (a)–(d): `hStepA` (latent_exch + tower at σ_AUX → ∫_{A≠a} μ[Y(a)\|σ_UX]), `hStepC` (likelihoodRatio_swapA_spec arm-swap via L2 → ∫_{A=a} μ[Y(a)\|σ_UX]·L), `hStepB` (consistency + bridge_h substitution on `{A=a}` → swap μ[Y(a)\|σ_UX] for μ[h(a,W,X)\|σ_AUX]), `hStepD1` (bridge_q → swap L for μ[q(Z,a,X)\|σ_AUX]), `hStepD2` (proxy_WZ_indep + condExp_mul_of_condIndep on (W,X) ⟂ (Z,X) — built by two prodMk-left lifts via condIndepFun_prodMk_of_measurable_left + symm), `hStepD3` (tower {A=a} ∈ σ_AUX). Black-box dependencies: `condExp_mul_of_condIndep`, `condIndepFun_prodMk_of_measurable_left`; `setIntegral_eq_setIntegral_mul_of_likelihoodRatio_swap` (LikelihoodRatioSwap.lean) | **Proved** |
| `condExp_Y_eq_condExp_h_arm_AX_core` | `μ[Y \| σ_AX] =ᵐ[restrict {A=a}] μ[h(a,W,X) \| σ_AX]` | Raw-input collapse identity: takes a bridge function `h` together with `bridge`, `integrable_Y`, `integrable_h`, `integrable_h_arm` directly (no assumption-bundle dependency). Shared core used by both the WBased and TwoProxy wrappers. Proof: bridge equation + indicator-zero argument at σ_AUX, then σ_AX-tower via `σ_AX_le_σ_AUX` and `condExp_condExp_of_le` | **Proved** |
| `condExp_Y_eq_condExp_h_arm_AX` | wrapper for `WBasedAssumptions` | Bundled-input collapse identity for the W-only bundle; specialises `_core` with `HA.h, HA.bridge, HA.integrable_Y, HA.integrable_h, HA.integrable_h_arm` | **Proved** |
| `condExp_Y_eq_condExp_h_arm_AX_twoProxy` | wrapper for `TwoProxyAssumptions` | Same as above, but for the two-proxy bundle (`HA.bridge_h` plays the role of `bridge`); `TwoProxyAssumptions` does not contain `proxy_WA` so it cannot be projected to `WBasedAssumptions` | **Proved** |
| `setIntegral_mul_condExp_of_stronglyMeasurableLeft` | `∫_s f·g dμ = ∫_s f·μ[g\|m] dμ` for m-stronglyMeasurable f, integrable g and f·g, m-measurable s | Generic Mathlib-style helper combining `condExp_mul_of_stronglyMeasurable_left` with `setIntegral_condExp`. Used in `WBased.lean` to swap `h(a,W,X)` for `μ[Y\|σ_AX]` inside `{A=a}` integrals (Fix C) | **Proved** |
| `stratumOddsRatio_spec` (private) | `μ[𝟙_{A=a}\|σ_X] · stratumOddsRatio =ᵐ μ[𝟙_{A=¬a}\|σ_X]` | σ_X projection of `likelihoodRatio_swapA_spec`: algebraic safe-division identity granted σ_X-positivity of `μ[𝟙_{A=a}\|σ_X]`. Closed via `field_simp` after invoking `Causalean.ae_pos_condExp_indicator_of_le` (the σ_X-projection of `overlap_strong`, isolated in `Mathlib/CondIndep.lean`) | **Proved** (modulo the labelled `ae_pos_condExp_indicator_of_le` stub in `Mathlib/CondIndep.lean`) |
| `condExp_q_eq_stratumOddsRatio_arm_AX` | `μ[q(Z,a,X) \| σ_AX] =ᵐ[restrict {A=a}] stratumOddsRatio` | Prob-ratio q-collapse on `{A=a}` σ_AX. Under the codebase's prob-ratio q convention, the σ_AX-conditional expectation of `q(Z,a,X)` collapses on `{A=a}` to the σ_X-measurable stratum-odds ratio. Used in `condMeanYofA_WZ_bounds` to remove the latent bridge `q` from the public statement (parallel to `condExp_Y_eq_condExp_h_arm_AX` for `h`). Proof plan (see docstring): `ae_eq_condExp_of_forall_setIntegral_eq` over `(μ.restrict {A=a}).trim` + σ_AX → σ_X test-set reduction + the L1+L2+L2 chain (`setIntegral_mul_condExp_of_stronglyMeasurableLeft` + `setIntegral_eq_setIntegral_mul_of_likelihoodRatio_swap` twice) + `bridge_q` + `stratumOddsRatio_spec`. All substantive measure-theoretic gaps now isolated upstream — remaining body is mechanical bookkeeping (descent/integrability/measurability) | **Sorry** (mechanical bookkeeping only; no substantive gap remains in this body — substantive gap is now in `Mathlib/CondIndep.lean` `ae_pos_condExp_indicator_of_le`) |

### WBased (`Proxy/WBased.lean`) — Theorem 1 + Corollary 1

| Symbol | Type | Description | Status |
|---|---|---|---|
| `condMeanYofA_W_bounds` | sandwich on `condMeanYofA μ a` | Theorem 1, observable form: `max(essinf Y, env-low) ≤ E[Y(a) \| A=¬a] ≤ min(essup Y, env-up)` where the envelope integrals contain the **observable** factor `μ[Y \| σ_AX]` (not the latent bridge `h`). Removed `h` from the public statement via the collapse identity `condExp_Y_eq_condExp_h_arm_AX` (Fix C). Takes 2 caller-supplied integrability hypotheses `hU_int_h`/`hL_int_h` for `(stratumOddsRatio · Uenv/Lenv(a,X)) · h(a,W,X)` (paper's implicit "all integrals finite" convention; not bundled because `Uenv`/`Lenv` are theorem-level parameters) | **Proved** |
| `meanYofA_W_bounds` | sandwich on `meanYofA μ a` | Corollary 1, observable form with **trivial-clamp** (paper arXiv lines 177-195): `max(essinfY · μ{A≠a}, envelope-lower) + ∫_{A=a} Y dμ ≤ E[Y(a)] ≤ min(essupY · μ{A≠a}, envelope-upper) + ∫_{A=a} Y dμ`. The trivial clamp `essinfY · μ{A≠a}` corresponds to the worst-case potential outcome on the off-arm stratum. Derived via `meanYofA_eq_strata` from the conditional bound, with the off-arm trivial clamp from `YofA_essbound_above`/`_below`. Takes 4 caller-supplied integrability hypotheses for `(stratumOddsRatio · Uenv/Lenv(a,X)) · h(a,W,X)` and the `μ[Y\|σ_AX]` analogues (paper's implicit "all integrals finite" convention; not bundled because `Uenv`/`Lenv` are theorem-level parameters) | **Proved** |

### ZBased (`Proxy/ZBased.lean`) — Theorem 2 + Corollary 2

| Symbol | Type | Description | Status |
|---|---|---|---|
| `condIntYofA_le_envelope_arm` / `envelope_le_condIntYofA_arm` | arm-level upper / lower envelope chain | Internal lemmas reducing the `∫_{A≠a} Y(a)` integral to envelope integrals via `bridge_q`, `proxy_YZ`, `latent_exch_to_condExp'`, `consistency_event'`. Chain restructured to use σ_AUX (not σ_UX) at step (4), avoiding the missing `Y ⟂ A \| σ_UX` lift. All ten steps closed in-body: hStep1/2 (tower at σ_AUX, latent_exch), hArmSwap_Y / hStep10 (L2 arm-swap via `setIntegral_eq_setIntegral_mul_of_likelihoodRatio_swap`), hStep4 (consistency + latent_exch transfer of E[Y(a)\|σ_UX] → E[Y\|σ_AUX] on `{A=a}`), hStep5 (bridge_q substitution), `hStep67` (proxy_YZ-based factorisation: lifts Z⟂Y\|σ_AUX to (Z,X)⟂Y\|σ_AUX via `condIndepFun_prodMk_of_measurable_left`, applies `condExp_mul_of_condIndep` to factor μ[Y·q\|σ_AUX] = μ[Y\|σ_AUX]·μ[q\|σ_AUX], towers back through `setIntegral_condExp` on σ_AUX, then re-towers q·Y → q·μ[Y\|σ_AZX] via `setIntegral_mul_condExp_of_stronglyMeasurableLeft` at m = σ_AZX — extracted as the private helper `condIntYq_factor_arm` shared by upper and lower variants), hStep8 (envelope inequality with q ≥ 0), hStep9 (L1 with m = σ_AUX, f = Uenv(a,X) σ_AUX-meas via σ_X ≤ σ_AUX). Takes three theorem-level integrability hypotheses (`hUInt`, `hU_q`, `hU_L`) for envelope · q and envelope · L pairings (paper's implicit-finiteness convention) | **Proved** |
| `condMeanYofA_Z_bounds` | sandwich on `condMeanYofA μ a` | Theorem 2: `(μ {A≠a})⁻¹ * ∫_{A≠a} Lenv(a,X) ≤ E[Y(a)\|A=¬a] ≤ (μ {A≠a})⁻¹ * ∫_{A≠a} Uenv(a,X)`. No clamp needed (Remark 5). Takes 6 caller-supplied integrability hypotheses (`hLInt`, `hUInt`, `hL_q`, `hU_q`, `hL_L`, `hU_L`) threaded into the arm-chain lemmas | **Proved** (modulo `Mathlib/CondIndep.lean` stubs propagated through the arm chains) |
| `meanYofA_Z_bounds` | sandwich on `meanYofA μ a` | Corollary 2: marginal version via `meanYofA_eq_strata` + `linarith`. Takes the same 6 caller-supplied integrability hypotheses as `condMeanYofA_Z_bounds` | **Proved** (modulo `Mathlib/CondIndep.lean` stubs propagated through the arm chains) |

### TwoProxy (`Proxy/TwoProxy.lean`) — Theorem 3

| Symbol | Type | Description | Status |
|---|---|---|---|
| `condMeanYofA_WZ_bounds` | sandwich on `condMeanYofA μ a` | Theorem 3 (W-shape observable form): `max(essinf Y, (μ{A≠a}).toReal⁻¹·∫_{A=a} stratumOddsRatio·Lenv(a,X)·μ[Y\|σ_AX] dμ) ≤ E[Y(a)\|A=¬a] ≤ min(essup Y, …Uenv version…)`. Public statement contains only `μ[Y\|σ_AX]` — both `h` and `q` are collapsed away via `condExp_Y_eq_condExp_h_arm_AX_twoProxy` and `condExp_q_eq_stratumOddsRatio_arm_AX`, parallel to W-only Theorem 1. Uses the same-arm joint-vs-product envelope `IsLowerEnvWZ`/`IsUpperEnvWZ` and the bridge identity `condIntYofA_eq_hq_armSwap_twoProxy` (now without `stratumOddsRatio` since q is prob-ratio). Both bound integrals live on `{A=a}` — no σ_AX-fiber transport. Takes 2 caller-supplied integrability hypotheses `hU_envInt`/`hL_envInt` for the envelope-bound integrand `Uenv/Lenv(a,X) · μ[h\|σ_AX] · μ[q\|σ_AX]` (paper's implicit "all integrals finite" convention; Phase B decision mirroring WBased) | **Proved** (modulo black-box helpers `condIntYofA_eq_hq_armSwap_twoProxy` and `condExp_q_eq_stratumOddsRatio_arm_AX` which carry their own sorries) |

### IntervalForm (`Proxy/IntervalForm.lean`) — `Set.Icc` interval restatements

Mechanical `Set.Icc` membership corollaries of the five proximal sandwich
theorems: each pushes the source conjunction `L ≤ θ ∧ θ ≤ U` through the adapter
`Causalean.PartialID.mem_Icc_of_sandwich` to get `θ ∈ Set.Icc L U`, matching the
abstract identified-set vocabulary of `PartialID/Basic.lean`. Same binders as the
source theorems; no new mathematical content.

| Symbol | Type | Description | Status |
|---|---|---|---|
| `condMeanYofA_W_mem_Icc` | `condMeanYofA μ a ∈ Set.Icc L U` | Closed-interval `Set.Icc` form of the W-proxy conditional `E[Y(a) \| A=¬a]` bound (`condMeanYofA_W_bounds`), via the `SandwichInterval` bridge | **Proved** |
| `meanYofA_W_mem_Icc` | `meanYofA μ a ∈ Set.Icc L U` | Closed-interval `Set.Icc` form of the W-proxy marginal `E[Y(a)]` bound (`meanYofA_W_bounds`), via the `SandwichInterval` bridge | **Proved** |
| `condMeanYofA_Z_mem_Icc` | `condMeanYofA μ a ∈ Set.Icc L U` | Closed-interval `Set.Icc` form of the Z-proxy conditional `E[Y(a) \| A=¬a]` bound (`condMeanYofA_Z_bounds`), via the `SandwichInterval` bridge | **Proved** |
| `meanYofA_Z_mem_Icc` | `meanYofA μ a ∈ Set.Icc L U` | Closed-interval `Set.Icc` form of the Z-proxy marginal `E[Y(a)]` bound (`meanYofA_Z_bounds`), via the `SandwichInterval` bridge | **Proved** |
| `condMeanYofA_WZ_mem_Icc` | `condMeanYofA μ a ∈ Set.Icc L U` | Closed-interval `Set.Icc` form of the two-proxy conditional `E[Y(a) \| A=¬a]` bound (`condMeanYofA_WZ_bounds`), via the `SandwichInterval` bridge | **Proved** |

**Out of scope (deferred):**
- Section 4 (mediator NIE/NDE; hidden front-door) — needs new mediator counterfactual layer in `Causalean/PO/`.
- Section 5 (LSE smooth approximation, bootstrap CIs) — statistical inference content.
- Sharpness — the paper does not establish sharpness for these bounds.

---

## 9j5. `PO/ID/Partial/Lee/` — Lee Bounds for Sample-Selection ATT (PO framework)

Formalises `prop:po-lee-bounds` from `Basic Concepts.tex`
(§`subsec:po-lee-bounds`): a partial-identification sandwich bound for the
**always-selected ATT** `τ_AS = E[Y(1) - Y(0) | alwaysSelected]` under
sample selection. Hypotheses are random assignment (encoded as the
pair-level independence `A ⫫ (Y(a), Sel(a))` for each `a`), monotone sample
selection `Sel(0) ≤ Sel(1)`, and a finite-support restriction on
`L(Y | A=true, Sel=true)`. The bound takes the form
`underline_m₁ - m₀ ≤ τ_AS ≤ overline_m₁ - m₀`, where
`underline_m₁`, `overline_m₁` are the lower and upper Lee trimmed means and
`m₀ = E[Y | A=false, Sel=true]` is the observable selected-control mean.

The quantile form (rem:po-lee-quantile) is deferred to a follow-on file.

Layout (one file per topic, all proofs complete):

- `Lee/Setup.lean` — `POLeeSystem` data + factual / counterfactual
  accessors + observable cells `selectedTreated`, `selectedControl`.
- `Lee/Assumptions.lean` — `BaseAssumptions` (consistency, pair-level
  random assignment, positivity, integrability) + `MonotoneSelection`.
- `Lee/Trim.lean` — selected-cell probabilities `p0`, `p1`, ratio `rho`,
  observable density `f1`, `LeeTrimWeight`, mean `Mw`, trimmed means
  `lowerTrimMean` / `upperTrimMean`, observable selected-control mean `m0`.
- `Lee/PrincipalStrata.lean` — latent events `alwaysSelected`,
  `helpedSelected`, `harmedSelected`; the pure set equality
  `selOfATrueSet = alwaysSelected ∪ helpedSelected`; under monotone
  selection, the a.s. identities collapsing latent events to observables.
- `Lee/ControlMean.lean` — Step A: `m0_eq_eventCondExp_Y0_alwaysSelected`
  collapses observable `m₀ = E[Y|A=false,Sel=true]` to latent
  `E[Y(0)|alwaysSelected]`.
- `Lee/MixtureIdentity.lean` — selected-treated decomposition (the doc's
  "mixture law"): `selectedTreated_integral_split` (Step B),
  `selectedTreated_measure_split` (Step B'), and the indicator-integrand
  variant `selectedTreated_integral_split_indicator`.
- `Lee/LatentSupport.lean` — `YofA_true_in_finset_ae_alwaysSelected`:
  finite outcome support transfers from observable cell to latent strata.
- `Lee/TrimWeight.lean` — `f1AS` density + the
  `alwaysSelectedTrimWeight` `LeeTrimWeight` witness with all four field
  proofs (`nonneg`, `le_one`, `zero_off`, `sum_eq`).
- `Lee/TrimMean.lean` — `Mw_alwaysSelectedTrimWeight_eq_condExp_Y1_AS`:
  the witness's trimmed mean equals `E[Y(1)|alwaysSelected]`.
- `Lee/TrimBound.lean` — `trimmed_bounds_condExp_Y1_AS`: corresponding
  sandwich `lowerTrimMean 𝒴 ≤ E[Y(1)|alwaysSelected] ≤ upperTrimMean 𝒴`.
  The `csInf`/`csSup` step is now routed through the engine bridge
  `Causalean.PartialID.mem_Icc_csInf_csSup` (see §9j2 `SandwichInterval.lean`);
  the trimmed-mean endpoints are exactly the inf/sup of `Mw` over the
  unidentified trim-weight nuisance.
- `Lee/Main.lean` — `lee_bounds_ATT_AS` (prop:po-lee-bounds): final
  assembly via `eventCondExp_sub` on `alwaysSelected`.
- `Lee.lean` — umbrella re-export.

### Setup (`Lee/Setup.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `POLeeSystem P` | `structure { A, Sel, Y, hAbool, hSelbool, hYreal, hASel, hAY, hSelY }` | Lee sample-selection subsystem over a PO system: binary `A`, binary `Sel` (selection), real `Y`, pairwise distinctness | **Defined** |
| `aVar` / `selVar` / `yVar` | `POVar P Bool` / `POVar P Bool` / `POVar P ℝ` | `POVar` wrappers for the three nodes | **Defined** |
| `SelOfA S a` | `P.Ω → Bool` | Counterfactual selection `Sel(a) := selVar.cfUnder aVar a` | **Defined** |
| `YofA S a` | `P.Ω → ℝ` | Counterfactual outcome `Y(a) := yVar.cfUnder aVar a` | **Defined** |
| `factualA / factualSel / factualY` | `P.Ω → Bool / Bool / ℝ` | Factual treatment / selection / outcome | **Defined** |
| `aEvent S a` / `selEvent S s` | `Set P.Ω` | Factual events `{A = a}` / `{Sel = s}` | **Defined** |
| `selectedTreated` | `Set P.Ω := aEvent true ∩ selEvent true` | Observable selected-treated cell | **Defined** |
| `selectedControl` | `Set P.Ω := aEvent false ∩ selEvent true` | Observable selected-control cell | **Defined** |
| measurability lemmas | `measurable_{SelOfA, YofA, factualA, factualSel, factualY}`, `measurableSet_{aEvent, selEvent, selectedTreated, selectedControl}` | One-line delegations to `POVar` | **Proved** |

### Assumptions (`Lee/Assumptions.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `BaseAssumptions S` | `structure { consistency, randAssign, posSelectedTreated, posSelectedControl, posATrue, posAFalse, posSelTrFinite, posSelCtFinite, integrableY1, integrableY0 }` | Common bundle for prop:po-lee-bounds. Factual-`Y` integrability is *derived*, not bundled (see `integrableY`) | **Defined** |
| `BaseAssumptions.randAssign` | `∀ a : Bool, IndepFun S.factualA (fun ω => (S.YofA a ω, S.SelOfA a ω)) P.μ` | Pair-level random assignment; weaker than the doc's 4-tuple joint indep, but strictly stronger than mean independence — carries the conditional distribution of `(Y(a), Sel(a))` given `A` | **Defined** |
| `BaseAssumptions.integrable_YofA hA a` | `Integrable (S.YofA a) P.μ` | Binary-folded form of `integrableY1` / `integrableY0` | **Proved** |
| `BaseAssumptions.integrableY hA` | `Integrable S.factualY P.μ` | Factual outcome integrability, *derived* from arm integrability via consistency (`factualY = Σ_a Y(a)·1{A=a}` a.e.); no longer an assumed field | **Proved** |
| `MonotoneSelection S` | `Prop { monotone }` | Monotone sample selection: `Sel(0) ≤ Sel(1)` a.s. | **Defined** |

### Trim (`Lee/Trim.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `pSelGivenA S a` | `ℝ` | `eventCondExp μ (aEvent a) (selVar.indicator true)` — conditional selection probability | **Defined** |
| `p0` / `p1` | `ℝ` | Selected-cell probabilities `P(Sel=true | A=false)` / `P(Sel=true | A=true)` | **Defined** |
| `rho` | `ℝ := p0 / p1` | Trimming ratio | **Defined** |
| `f1 S y` | `ℝ` | Observable density `P(Y=y | A=true, Sel=true)` via `eventCondExp` of an `if-then-else` indicator | **Defined** |
| `LeeTrimWeight S 𝒴` | `structure { w, nonneg, le_one, zero_off, sum_eq }` | Lee trim weight on a finite outcome support `𝒴 : Finset ℝ` (def:po-lee-trim) | **Defined** |
| `Mw wt` | `ℝ := rho⁻¹ * ∑ y ∈ 𝒴, y * wt.w y * f1 y` | Mean of a trim weight | **Defined** |
| `lowerTrimMean 𝒴` / `upperTrimMean 𝒴` | `ℝ` | `sInf` / `sSup` of `Set.range (fun wt => Mw wt)` | **Defined** |
| `m0` | `ℝ := eventCondExp μ selectedControl factualY` | Observable selected-control mean | **Defined** |

### PrincipalStrata (`Lee/PrincipalStrata.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `alwaysSelected` | `Set P.Ω := {ω | SelOfA false ω = true ∧ SelOfA true ω = true}` | Latent always-selected stratum | **Defined** |
| `helpedSelected` | `Set P.Ω := {ω | SelOfA false ω = false ∧ SelOfA true ω = true}` | Latent treatment-induced-selected stratum | **Defined** |
| `harmedSelected` | `Set P.Ω := {ω | SelOfA false ω = true ∧ SelOfA true ω = false}` | Latent harmed stratum (a.s. empty under monotone selection) | **Defined** |
| `selOfATrueSet` / `selOfAFalseSet` | `Set P.Ω` | The latent sets `{Sel(1)=true}` / `{Sel(0)=true}` | **Defined** |
| measurability lemmas | `measurableSet_{alwaysSelected, helpedSelected, harmedSelected, selOfATrueSet, selOfAFalseSet}` | One-line delegations to `Measurable.preimage` of singletons | **Proved** |
| `selOfATrueSet_eq_alwaysSelected_union_helpedSelected` | `S.selOfATrueSet = S.alwaysSelected ∪ S.helpedSelected` | Pure set equality (no a.s.) by case analysis on `SelOfA false ω : Bool` | **Proved** |
| `disjoint_alwaysSelected_helpedSelected` | `Disjoint S.alwaysSelected S.helpedSelected` | The two latent strata are disjoint | **Proved** |
| `harmedSelected_ae_empty` | `MonotoneSelection ⇒ μ harmedSelected = 0` | Harmed stratum is null under monotone selection | **Proved** |
| `selOfAFalseSet_ae_eq_alwaysSelected` | `MonotoneSelection ⇒ {Sel(0)=true} =ᵐ alwaysSelected` | Latent `{Sel(0)=true}` collapses (a.s.) to alwaysSelected | **Proved** |

### ControlMean (`Lee/ControlMean.lean`) — Step A

| Symbol | Type | Description | Status |
|---|---|---|---|
| `m0_eq_eventCondExp_Y0_alwaysSelected` | `BaseAssumptions, MonotoneSelection ⇒ m0 = eventCondExp μ alwaysSelected (YofA false)` | Identifies `m₀` (observable selected-control mean) as the latent always-selected mean of `Y(0)`, via consistency on `{A=false, Sel=true}` plus drop-of-conditioning from `randAssign false` plus monotone-selection collapse `{Sel(0)=true} =ᵐ alwaysSelected` | **Proved** |

### MixtureIdentity (`Lee/MixtureIdentity.lean`) — Step B / B' / indicator

| Symbol | Type | Description | Status |
|---|---|---|---|
| `selectedTreated_integral_split` | `BaseAssumptions, MonotoneSelection ⇒ ∫ in selectedTreated, factualY = (μ(aEvent true)).toReal · (∫ in alwaysSelected, YofA true + ∫ in helpedSelected, YofA true)` | Step B: selected-treated integral splits along the latent strata, with the `μ(A=true)` mass factor from drop-of-conditioning | **Proved** |
| `selectedTreated_measure_split` | `BaseAssumptions, MonotoneSelection ⇒ (μ selectedTreated).toReal = (μ(aEvent true)).toReal · ((μ alwaysSelected).toReal + (μ helpedSelected).toReal)` | Step B': mass-level analogue (constant integrand `1`) | **Proved** |
| `selectedTreated_integral_split_indicator y` | `BaseAssumptions, MonotoneSelection ⇒ ∫ in selectedTreated, 1_{factualY = y} = (μ(aEvent true)).toReal · (∫ in alwaysSelected, 1_{YofA true = y} + ∫ in helpedSelected, 1_{YofA true = y})` | Step B for indicator integrand `1_{Y=y}`; used by `TrimWeight` to bound the trim-weight from `f1` | **Proved** |

### LatentSupport (`Lee/LatentSupport.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `YofA_true_in_finset_ae_alwaysSelected` | `BaseAssumptions, MonotoneSelection, 𝒴 : Finset ℝ, hSupp ⇒ (∀ᵐ ω ∂(μ.restrict alwaysSelected), YofA true ω ∈ 𝒴) ∧ (∀ᵐ ω ∂(μ.restrict helpedSelected), YofA true ω ∈ 𝒴)` | Transfers the finite-support hypothesis from observable cell to latent strata via consistency + drop-of-conditioning + monotone selection | **Proved** |

### TrimWeight (`Lee/TrimWeight.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `f1AS y` | `ℝ := eventCondExp μ alwaysSelected (fun ω => if YofA true ω = y then 1 else 0)` | Conditional density of `Y(1)` on `alwaysSelected` at `y` | **Defined** |
| `alwaysSelectedTrimWeight hA hMono 𝒴 hSupp` | `LeeTrimWeight 𝒴` with `w(y) := if y ∈ 𝒴 ∧ S.f1 y ≠ 0 then S.rho * S.f1AS y / S.f1 y else 0` | Lee-trim-weight witness for the always-selected sub-distribution; the four field proofs (`nonneg`, `le_one`, `zero_off`, `sum_eq`) collapse via `selectedTreated_integral_split_indicator` and `YofA_true_in_finset_ae_alwaysSelected` | **Proved** |

### TrimMean (`Lee/TrimMean.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `Mw_alwaysSelectedTrimWeight_eq_condExp_Y1_AS` | `Mw (alwaysSelectedTrimWeight hA hMono 𝒴 hSupp) = eventCondExp μ alwaysSelected (YofA true)` | The trimmed mean of the always-selected witness equals `E[Y(1)|alwaysSelected]`; the `ρ⁻¹` in `Mw` cancels the `ρ` numerator built into `w` | **Proved** |

### TrimBound (`Lee/TrimBound.lean`)

| Symbol | Type | Description | Status |
|---|---|---|---|
| `trimmed_bounds_condExp_Y1_AS` | `lowerTrimMean 𝒴 ≤ eventCondExp μ alwaysSelected (YofA true) ≤ upperTrimMean 𝒴` | Sandwich corollary of `Mw_alwaysSelectedTrimWeight_eq_condExp_Y1_AS`: the witness lies in `Set.range Mw`, so its value sits between the inf and sup | **Proved** |

### Main (`Lee/Main.lean`) — prop:po-lee-bounds

| Symbol | Type | Description | Status |
|---|---|---|---|
| `lee_bounds_ATT_AS` | `[IsFiniteMeasure μ], BaseAssumptions, MonotoneSelection, 𝒴 : Finset ℝ, hSupp ⇒ lowerTrimMean 𝒴 - m0 ≤ eventCondExp μ alwaysSelected (YofA true - YofA false) ≤ upperTrimMean 𝒴 - m0` | **Main theorem** prop:po-lee-bounds; subtracts Step A (`m0 = E[Y(0)|alwaysSelected]`) from Step C (sandwich for `E[Y(1)|alwaysSelected]`) via `eventCondExp_sub` | **Proved** |

### Status

- Lee module is sorry-free; full `lake build Causalean.PO.ID.Partial.Lee` succeeds with zero errors and zero `sorry` warnings on Lee files.
- Quantile form (rem:po-lee-quantile) — deferred follow-on file `Lee/Quantile.lean`.

## 9k. `PO/ID/Exact/Frontdoor.lean` — Frontdoor ATE Identification Theorem (PO framework)

Formalizes `prop:po-frontdoor-ate` from `Basic Concepts.tex`
(§`subsec:po-frontdoor-ate`, lines ~1518–1691): **classical frontdoor
identification of the ATE** under binary treatment `A`, discrete mediator `M`
(finite value space `β`), and real outcome `Y`. The proof pattern relies on the
*composition* clause of `POSystem.Consistency` to collapse the two-variable
counterfactual `Y(a, M(a))` to `Y(a)` on `{M(a) = m}`, plus full mediation and
two exchangeability assumptions.  Event-level style throughout
(`eventCondExp`), matching `Manski.lean` / `DID.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POFrontdoorSystem P β` | `structure { A, M, Y, hAbool, hMequiv, hYreal, hAM, hAY, hMY }` | Frontdoor PO subsystem: binary treatment `A`, discrete mediator `M : β` (finite), real outcome `Y`, pairwise distinctness | **Defined** |
| `POFrontdoorSystem.{aVar, mVar, yVar}` | `POVar P Bool` / `POVar P β` / `POVar P ℝ` | `POVar` wrappers | **Defined** |
| `POFrontdoorSystem.{YofA, MofA}` | `Bool → P.Ω → ℝ` / `Bool → P.Ω → β` | Single-target counterfactuals `Y(a)`, `M(a)` | **Defined** |
| `POFrontdoorSystem.regimeAM S a m` | `Regime P.V P.X` | Two-target intervention `{A ← a, M ← m}` as a `Regime.sqcup` of two singletons (disjointness from `hAM`) | **Defined** |
| `POFrontdoorSystem.YofAM S a m` | `P.Ω → ℝ := yVar.cf (regimeAM a m)` | Two-variable counterfactual `Y(a, m)` | **Defined** |
| `POFrontdoorSystem.{factualA, factualM, factualY}` | `P.Ω → Bool` / `β` / `ℝ` | Factual treatment / mediator / outcome | **Defined** |
| `POFrontdoorSystem.measurable_{YofA, MofA, YofAM, factualA, factualM, factualY}` | `Measurable …` | One-line measurability delegations to `POVar.measurable_{cf, cfUnder, factual}` | **Proved** |
| `POFrontdoorSystem.{aEvent, mEvent}` | `Set P.Ω` | Factual events `{A = a}`, `{M = m}` (indicators are now `POVar.indicator` on the corresponding `POVar`) | **Defined** |
| `POFrontdoorSystem.measurableSet_{aEvent, mEvent}` | — | Measurability of the factual events | **Proved** |
| `POFrontdoorSystem.mUnderA S a` | `RegimedVar P β` | `M(a)` as a regimed variable, for bundling | **Defined** |
| `POFrontdoorSystem.mBundle S` | `POCFBundle P` | Two-entry bundle `[M(true), M(false)]`, used to state `A ⊥ (M(1), M(0))` | **Defined** |
| `POFrontdoorSystem.{pA, pMgivenA, EYgivenAM}` | `ℝ` | Observable `P(A=a)`, `P(M=m \| A=a)`, `E[Y \| A=a, M=m]` (event-level) | **Defined** |
| `POFrontdoorSystem.frontdoorTerm S a` | `ℝ := ∑ m, (EYgivenAM 1 m · pA 1 + EYgivenAM 0 m · pA 0) · pMgivenA m a` | Frontdoor-adjusted functional `φ(a)` | **Defined** |
| `POFrontdoorSystem.{ATE, frontdoorATE}` | `ℝ` | Target parameter `E[Y(1) - Y(0)]` and its observable counterpart `φ(1) - φ(0)` | **Defined** |
| `POFrontdoorSystem.Assumptions S` | `structure { consistency, fullMediation, exch_AM, exch_MY, posA, posAM, indep_Y_M, integrable_{factualY, YofA, YofAM} }` | Full bundle of PO-level frontdoor assumptions from `def:po-frontdoor-ate-assumptions`.  The extra field `indep_Y_M : ∀ a m, IndepFun (MofA a) (YofAM true m) μ` is a cross-world independence assumption needed for the drop-of-conditioning step; it is not derivable from the other fields in the bare PO framework (would follow from d-separation in the graphical frontdoor setup) | **Defined** |
| `POFrontdoorSystem.MofA_eq_factualM_on_aEvent` | `P.Consistency → ω ∈ aEvent a → MofA a ω = factualM ω` | On `{A = a}`, the single-target counterfactual `M(a)` coincides with the factual mediator; one-line shim on `POVar.cf_eq_factual_on_event` | **Proved** |
| `POFrontdoorSystem.YofAM_eq_factualY_on_aMEvent` | `P.Consistency → ω ∈ aEvent a ∩ mEvent m → YofAM a m ω = factualY ω` | On `{A = a} ∩ {M = m}`, the two-variable counterfactual `Y(a,m)` coincides with the factual outcome; built on `POVar.cf_eq_factual_of_factualAgrees` via a `sqcup`-case-split factual-agreement proof | **Proved** |
| `POFrontdoorSystem.YofAM_eq_YofA_on_MofA_event` | `P.Consistency → MofA a ω = m → YofAM a m ω = YofA a ω` | **Composition lemma**: on `{M(a) = m}`, `Y(a,m) = Y(a)` pointwise; reduces the two-target counterfactual to the single-target one via `Consistency.composition` applied to the disjoint singletons `{A ← a}` and `{M ← m}` | **Proved** |
| `POFrontdoorSystem.integral_eq_sum_integral_MofA` | `Integrable f μ → ∫ f ∂μ = ∑ m : β, ∫ ω in {MofA a = ·}, f ω ∂μ` | Finite-partition helper: decompose an integral along the level sets of the counterfactual mediator `MofA a` (valid since `β` is `Fintype`) | **Proved** |
| `POFrontdoorSystem.integral_eq_sum_eventCondExp_aEvent` | `Integrable g μ → ∫ g ∂μ = ∑ a' : Bool, eventCondExp μ (aEvent a') g · μ(aEvent a').toReal` | Bool-partition tower: express `∫ g` as the sum over treatment arms of arm-conditional means weighted by arm probability | **Proved** |
| `POFrontdoorSystem.EofY_eq_frontdoorTerm` | `S.Assumptions → ∀ a, ∫ YofA a ∂P.μ = frontdoorTerm a` | **Frontdoor identification (individual regime):** `E[Y(a)] = φ(a)` for each `a ∈ {0,1}`.  Closes in four blocks: (A) mediator-marginal identity `μ(MofA a = m).toReal = pMgivenA m a` via `exch_AM.component` + consistency; (B) on-slice composition + full-mediation rewrite `∫_{MofA a = m} YofA a = ∫_{MofA a = m} YofAM true m`; (C) drop-of-conditioning via `indep_Y_M`; (D) inner tower `∫ YofAM true m = ∑_{a'} EYgivenAM a' m · pA a'` via `integral_eq_sum_eventCondExp_aEvent` + `exch_MY` + `fullMediation` + `YofAM_eq_factualY_on_aMEvent` | **Proved** |
| `POFrontdoorSystem.ate_frontdoor` | `S.Assumptions → S.ATE = S.frontdoorATE` | **Frontdoor identification of the ATE** `prop:po-frontdoor-ate`: immediate corollary of `EofY_eq_frontdoorTerm` at `a = 1, 0` by linearity of the integral | **Proved** |

---

## 9l. `PO/ID/Exact/Proximal/` — Proximal ATE Identification (PO framework, skeleton)

Proximal causal inference ATE identification under binary treatment `A`,
outcome-side proxy `W`, treatment-side proxy `Z`, latent confounder `U`, and covariate `X`.
Implements def:po-proximal-ate from Basic Concepts.tex §2555–2622.
Split into four files: **Setup** (data layer), **Assumptions** (assumption bundle), **Helpers** (CE manipulations), **Main** (identification theorems).

### `Proximal/Setup.lean` — Data layer

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POProximalSystem P γ_X γ_Z γ_W γ_U` | `structure { Xvar, Avar, Zvar, Wvar, Yvar, Uvar }` | Six `POVar`s: covariate `X`, binary treatment `A`, treatment-side proxy `Z`, outcome-side proxy `W`, real outcome `Y`, latent confounder `U` | **Defined** |
| `S.{X, A, Z, W, Y, U}` | `P.Ω → γ_*` | Factual maps for each variable | **Defined** |
| `S.YofA a` | `P.Ω → ℝ` | Potential outcome `Y(a) := Yvar.cfUnder Avar a` | **Defined** |
| `S.{AZX, AUX, UX, AZUX}` | `P.Ω → Bool × …` | Tuple maps used as conditioning targets | **Defined** |
| `S.{σ_AZX, σ_AUX, σ_UX, σ_AZUX}` | `MeasurableSpace P.Ω` | σ-algebras generated by the respective tuples | **Defined** |
| `S.{σ_AZX_le, σ_AUX_le, σ_UX_le, σ_AZUX_le}` | `S.σ_* ≤ inferInstance` | Sub-σ-algebra proofs for each | **Proved** |

### `Proximal/Assumptions.lean` — Assumption bundle

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `POProximalSystem.Assumptions S μ` | `structure { consistency, latent_exch, proxy_YZ, proxy_WAZ, h, measurable_h, integrable_hAWX, bridge, positivity_arm, completeness, integrable_{Y,YofA0,YofA1,h0WX,h1WX} }` | Full proximal assumption bundle (def:po-proximal-assumptions): consistency, latent exchangeability Y(a) ⟂ A \| (U,X), outcome proxy Y ⟂ Z \| (A,U,X), treatment proxy W ⟂ (A,Z) \| (U,X), outcome bridge function `h` with E[Y − h(A,W,X) \| A,Z,X] = 0, `positivity_arm` (Miao 2018 Asm. 7: each treatment arm has positive conditional probability given (U,X)), stratum-wise Z-completeness for (U,X) functionals on each arm `{A=a}` (Miao 2018 Asm. 8), and integrabilities | **Defined** |

### `Proximal/Helpers.lean` — Conditional-expectation manipulation lemmas

Six helper lemmas for the proximal ATE proof.  The five "drop conditioning" lemmas remain `sorry`'d pending downstream proof cleanup around the now-proved generic drop-conditioning lemma; the sixth (`eq_zero_globally_of_eq_zero_on_arm`) is proved and uses `positivity_arm` to lift a stratum-wise zero σ_UX-measurable function to global μ-zero.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `condExp_drop_Z HA` | `μ[S.Y \| S.σ_AZUX] =ᵐ[μ] μ[S.Y \| S.σ_AUX]` | Drop Z from conditioning on Y: Y ⟂ Z \| (A,U,X) makes Z irrelevant in σ(A,Z,U,X) | **sorry** (needs `condExp_sup_comap_eq_of_condIndep`) |
| `condExp_h_drop_Z HA a` | `μ[h(a,W,X) \| σ_AZUX] =ᵐ[μ] μ[h(a,W,X) \| σ_AUX]` | Drop Z from conditioning on h(a,W,X): via weak union from W ⟂ (A,Z) \| (U,X) | **sorry** (needs `condExp_sup_comap_eq_of_condIndep` + weak union) |
| `condExp_h_drop_A HA a` | `μ[h(a,W,X) \| σ_AUX] =ᵐ[μ] μ[h(a,W,X) \| σ_UX]` | Drop A from conditioning on h(a,W,X): W ⟂ A \| (U,X) (projection from W ⟂ (A,Z) \| (U,X)) | **sorry** (needs `condExp_sup_comap_eq_of_condIndep` + CI projection) |
| `latent_exch_to_condExp HA a` | `μ[S.YofA a \| S.σ_AUX] =ᵐ[μ] μ[S.YofA a \| S.σ_UX]` | Drop A from conditioning on Y(a): Y(a) ⟂ A \| (U,X) makes A irrelevant | **sorry** (needs `condExp_sup_comap_eq_of_condIndep`) |
| `consistency_event HA a hAY` | `μ[S.Y \| S.σ_AUX] =ᵐ[μ.restrict {A=a}] μ[S.YofA a \| S.σ_AUX]` | On {A=a}, Y = Y(a); requires `hAY : S.Avar.v ≠ S.Yvar.v`.  Step 1 (Y =ᵐ Y(a) on restrict) proved via `POVar.cf_eq_factual_on_event` + `ae_restrict_of_forall_mem`.  Step 2 (lift to condExp on restrict) `sorry`'d | **sorry** (step 2 needs condExp-over-restrict lift lemma) |
| `eq_zero_globally_of_eq_zero_on_arm HA a hf_meas hf_zero_on_arm` | `f =ᵐ[μ] 0` (when `f` is σ_UX-measurable and zero on arm `{A=a}`) | Lift a stratum-wise zero σ_UX-measurable function to global μ-zero via `positivity_arm` (Miao 2018 Asm. 7). | **Proved** |

### `Proximal/Main.lean` — Identification theorems

Ties the helper lemmas together into the two main identification results.
`ate_proximal` is fully proved (one-line, reduces to `Eofyofa_eq_Eh`).
`Eofyofa_eq_Eh` is `sorry`'d pending resolution of the helper blockers and
the `g_a` measurable-selection construction.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Eofyofa_eq_Eh HA a hAY` | `∫ ω, S.YofA a ω ∂μ = ∫ ω, HA.h (a, S.W ω, S.X ω) ∂μ` | For each treatment level `a`, counterfactual mean equals bridge mean; requires constructing `g_a : γ_U × γ_X → ℝ` via σ-algebra factorisation and applying completeness | **sorry** (blocked on helpers + needs measurable-selection for `g_a`) |
| `ate_proximal HA hAY` | `∫ YofA true ∂μ − ∫ YofA false ∂μ = ∫ h(true,W,X) ∂μ − ∫ h(false,W,X) ∂μ` | ATE equals difference of bridge integrals; immediate from `Eofyofa_eq_Eh` for both treatment levels | **Proved** |

---

## 10. `Mathlib/AlgebraicGeometry/PolynomialImageDimension/` — affine polynomial-image dimension

This algebraic-geometry substrate turns concrete polynomial parameterizations into exact dimensions of their complex Zariski image closures. It provides the closure and irreducibility language, identifies an image closure's coordinate ring with the coordinate subalgebra, and then derives dimension from either a transcendence-degree certificate or a nonzero Jacobian minor plus a matching upper certificate. It is neutral infrastructure for algebraic statistical models and generic-identifiability arguments.

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.ZariskiClosure -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure` | `Set (ι → ℂ) → Set (ι → ℂ)` | For a coordinate index set and a subset $A$ of the corresponding complex affine space, its affine Zariski closure is the set of all points at which every complex multivariate polynomial that vanishes on every point of $A$ also vanishes. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_extensive` | `A ⊆ Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A` | Every set is contained in its affine Zariski closure. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_mono` | `A ⊆ B → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A ⊆ Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure B` | Affine Zariski closure is monotone. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_idem` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A) = Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A` | Affine Zariski closure is idempotent. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_eq_zeroLocus` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure Z = Z → Z = MvPolynomial.zeroLocus ℂ (MvPolynomial.vanishingIdeal ℂ Z)` | A closed affine set is the zero locus of its vanishing ideal. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_inter` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure B = B → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (A ∩ B) = A ∩ B` | The intersection of two affine-closed sets is affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_zero_of_polynomial` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure {x \| (MvPolynomial.eval x) P = 0} = {x \| (MvPolynomial.eval x) P = 0}` | The zero set of one multivariate polynomial is affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_union` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure B = B → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (A ∪ B) = A ∪ B` | The union of two affine-closed sets is affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_nonvanishing_eq_univ` | `P ≠ 0 → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure {x \| (MvPolynomial.eval x) P ≠ 0} = Set.univ` | For a polynomial `P` in coordinates indexed by `ι`, if `P` is not the zero polynomial, then the set of points where `P` does not vanish is Zariski-dense in the whole affine space. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.Irreducibility -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed` | `Set (ι → ℂ) → Prop` | For a coordinate index set and a subset $Z$ of the corresponding complex affine space, an irreducible affine-closed set is a set that equals its polynomial Zariski closure, is nonempty, and whenever it is the union of two affine-closed subsets, equals one of those two subsets. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.vanishingIdeal_ne_top_of_nonempty` | `Z.Nonempty → MvPolynomial.vanishingIdeal ℂ Z ≠ ⊤` | A nonempty affine set has a proper vanishing ideal. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.nonempty_zeroLocus_of_prime` | `(MvPolynomial.zeroLocus ℂ P).Nonempty` | A prime ideal in a finite complex polynomial ring has a nonempty zero locus. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducibleAffineClosed_iff_isPrime` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure Z = Z → Z.Nonempty → (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Z ↔ (MvPolynomial.vanishingIdeal ℂ Z).IsPrime)` | A nonempty polynomially closed complex set `Z` is irreducible exactly when the polynomial equations that vanish on it form a prime ideal. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducible_zeroLocus_of_prime` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed (MvPolynomial.zeroLocus ℂ P)` | The zero locus of a prime ideal is irreducible affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.vanishingIdeal_strict_anti` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure B = B → A ⊂ B → MvPolynomial.vanishingIdeal ℂ B < MvPolynomial.vanishingIdeal ℂ A` | Strict inclusion of affine-closed sets strictly reverses their vanishing ideals. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.zeroLocus_strict_anti` | `P < Q → MvPolynomial.zeroLocus ℂ Q ⊂ MvPolynomial.zeroLocus ℂ P` | Strict inclusion of prime ideals strictly reverses their complex zero loci. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IrreducibleFiniteRange -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.finiteCoordinateRange_closed` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure {x \| x c ∈ S} = {x \| x c ∈ S}` | The set of complex coordinate vectors whose chosen coordinate belongs to a fixed finite set is closed in the affine Zariski topology. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducible_coordinate_constant_of_finite_range` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Z → ∀ (c : κ) (S : Finset ℂ), (∀ x ∈ Z, x c ∈ S) → ∃ z ∈ S, ∀ x ∈ Z, x c = z` | On an irreducible affine-closed set `Z`, if a fixed coordinate `c` takes values only within a fixed finite set `S` at every point of `Z`, then that coordinate is in fact constant on `Z`, equal to some single value in `S`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.CodimensionOne -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineComponent` | `Set (ι → ℂ) → Set (ι → ℂ) → Prop` | For a coordinate index set, a candidate subset $C$ of the corresponding complex affine space, and a prescribed locus $Z$ in that space, an irreducible affine component of $Z$ is an irreducible affine-closed subset $C$ that is contained in $Z$ and maximal under inclusion among irreducible affine-closed subsets of $Z$: every such subset that contains $C$ equals $C$. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineCodimensionIn` | `ℕ → Set (ι → ℂ) → Set (ι → ℂ) → Prop` | For a coordinate index set, a nonnegative integer $d$, a locus $Z$, and an ambient subset $X$ of the same complex affine space, the statement that $Z$ has affine codimension $d$ in $X$ means that every irreducible affine component of $Z$ is the initial member of a strictly increasing chain of $d+1$ irreducible affine-closed subsets whose final member is $X$, while at least one irreducible affine component of $Z$ is not the initial member of any strictly increasing chain of $d+2$ irreducible affine-closed subsets ending at $X$. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.map_quotient_strictMono` | `I ≤ A → A < B → Ideal.map (Ideal.Quotient.mk I) A < Ideal.map (Ideal.Quotient.mk I) B` | If one ideal is strictly contained in another and both contain a third ideal, their images in the quotient by that third ideal remain strictly ordered. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.no_three_chain_of_minimalPrime_span` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed C → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed X → ∀ (P : MvPolynomial ι ℂ), MvPolynomial.vanishingIdeal ℂ C ∈ (MvPolynomial.vanishingIdeal ℂ X ⊔ Ideal.span {P}).minimalPrimes → ¬∃ chain, StrictMono chain ∧ (∀ (i : Fin 3), Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed (chain i)) ∧ chain 0 = C ∧ chain (Fin.last 2) = X` | A prime affine subvariety whose prime ideal is minimal over one additional equation in the coordinate ring of `X` admits no intermediate irreducible closed set. This is the endpoint-fixed form of Krull's principal ideal theorem needed by `HasAffineCodimensionIn 1`. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.no_intermediate_of_exact_affine_dimensions` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed X → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d C → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension (d + 1) X → ¬∃ Y, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Y ∧ C ⊂ Y ∧ Y ⊂ X` | Exact dimensions differing by one exclude an intermediate irreducible closed set. The proof appends `Y` and `X` to a maximal-length chain inside `C`, contradicting the upper bound for `X`. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducibleAffineComponent_of_no_intermediate` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed C → C ⊆ Z → Z ⊆ X → Z ≠ X → (¬∃ Y, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Y ∧ C ⊂ Y ∧ Y ⊂ X) → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineComponent C Z` | An irreducible closed subset with no irreducible closed set strictly between it and an ambient irreducible variety is a component of every proper closed locus lying between the two. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.vanishingIdeal_mem_minimalPrimes_span_of_no_intermediate` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed C → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed X → C ⊆ X → ∀ P ∈ MvPolynomial.vanishingIdeal ℂ C, P ∉ MvPolynomial.vanishingIdeal ℂ X → (¬∃ Y, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Y ∧ C ⊂ Y ∧ Y ⊂ X) → MvPolynomial.vanishingIdeal ℂ C ∈ (MvPolynomial.vanishingIdeal ℂ X ⊔ Ideal.span {P}).minimalPrimes` | Geometric height one plus one equation through `C` identifies its prime ideal as a minimal prime over that equation in the coordinate ring of `X`. This is the Nullstellensatz converse used to manufacture the explicit minimal-prime certificates from the D0 dimension argument. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.hasAffineCodimensionIn_one_of_minimalPrime_span` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed X → Z ⊆ X → Z ≠ X → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineComponent C Z → ∀ (P : MvPolynomial ι ℂ), MvPolynomial.vanishingIdeal ℂ C ∈ (MvPolynomial.vanishingIdeal ℂ X ⊔ Ideal.span {P}).minimalPrimes → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineCodimensionIn 1 Z X` | For subsets `C`, `Z`, `X` of `ι → ℂ` (with `ι` finite), suppose `X` is irreducible Zariski-closed, `Z` is a proper subset of `X`, `C` is an irreducible component of `Z`, and the vanishing ideal of `C` is a minimal prime over the vanishing ideal of `X` joined with the principal ideal generated by some polynomial `P`. Then `Z` has affine codimension exactly `1` in `X`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.ChainDimension -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension` | `ℕ → Set (ι → ℂ) → Prop` | For a coordinate index set, a nonnegative integer $d$, and a subset $Z$ of the corresponding complex affine space, the statement that $Z$ has exact affine Zariski dimension $d$ means that there exists a strictly increasing chain of $d+1$ irreducible affine-closed subsets, all contained in $Z$, and no strictly increasing chain of $d+2$ irreducible affine-closed subsets, all contained in $Z$, exists. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.AffineSpaceDimension -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.primeOfIrreducible` | `(Z : Set (ι → ℂ)) → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed Z → PrimeSpectrum (MvPolynomial ι ℂ)` | For a finite index set, a subset of the associated complex affine space, and the hypothesis that this subset is irreducible and affine Zariski-closed, the associated prime-spectrum point is the prime ideal consisting of all complex polynomials that vanish on the subset. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.primeOfIrreducible_lt` | `A ⊂ B → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.primeOfIrreducible B hB < Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.primeOfIrreducible A hA` | For two irreducible affine closed subsets of finite-dimensional complex affine space, strict inclusion reverses the strict order of their corresponding vanishing-ideal prime points. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialRing_dimension` | `ringKrullDim (MvPolynomial (Fin d) ℂ) = ↑d` | The Krull dimension of the ring of complex polynomials in d variables is d. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineSpace_hasAffineZariskiDimension` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d Set.univ` | For any natural number `d`, complex affine `d`-space has affine Zariski dimension exactly `d`, in the sense of maximal chains of irreducible affine closed subsets. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.PolynomialMap -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap` | `((ι → ℂ) → κ → ℂ) → Prop` | For a source-coordinate index set, a target-coordinate index set, and a map $f$ from the resulting source complex affine space to the resulting target complex affine space, the statement that $f$ is a polynomial map means that, for every target coordinate, there exists a multivariate complex polynomial in the source coordinates whose value at every source point equals that coordinate of $f$. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.isPolynomialMap_id` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap id` | The identity map is polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap.comp` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap g → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap (g ∘ f)` | A composite of polynomial maps is polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap.eval_comp` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → ∀ (Q : MvPolynomial κ ℂ), ∃ P, ∀ (x : ι → ℂ), (MvPolynomial.eval x) P = (MvPolynomial.eval (f x)) Q` | Substituting a polynomial map into a target polynomial yields a source polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomial_preimage_closed` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → ∀ {A : Set (κ → ℂ)}, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (f ⁻¹' A) = f ⁻¹' A` | The preimage of an affine-closed set under a polynomial map is affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomial_fixedPoints_closed` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure {x \| f x = x} = {x \| f x = x}` | The fixed-point set of a polynomial endomorphism is affine-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomial_range_closed_of_retract` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap g → Function.LeftInverse g f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (Set.range f) = Set.range f` | For maps between the finite-dimensional complex affine spaces `ℂ^ι` and `ℂ^κ`, if `f` is coordinatewise polynomial, an accompanying map `g` back to `ℂ^ι` is coordinatewise polynomial, and `g` is a left inverse of `f`, i.e. `g (f x) = x` for every `x`, then the range of `f` is already Zariski-closed. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomial_image_closed_of_retract` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap g → Function.LeftInverse g f → ∀ {A : Set (ι → ℂ)}, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (f '' A) = f '' A` | A polynomial retract sends affine-closed source subsets to affine-closed images. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.PolynomialRetractDimension -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.image_strictMono_on` | `Set.InjOn f S → ∀ {chain : ι → Set α}, StrictMono chain → (∀ (i : ι), chain i ⊆ S) → StrictMono fun i => f '' chain i` | If a function is injective on a set containing every member of a strictly increasing chain of sets, then the images of those sets form a strictly increasing chain. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducible_image_polynomial_retract` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap g → Function.LeftInverse g f → ∀ {A : Set (ι → ℂ)}, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed A → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed (f '' A)` | A polynomial embedding with a polynomial retraction sends irreducible affine-closed subsets to irreducible affine-closed images. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialRetract_range_dimension` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap g → Function.LeftInverse g f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d (Set.range f)` | For maps between the affine space `ℂ^d` and `ℂ^κ`, if `f` is coordinatewise polynomial, an accompanying map `g` back to `ℂ^d` is coordinatewise polynomial, and `g` is a left inverse of `f`, i.e. `g (f x) = x` for every `x`, then the range of `f` has exact affine (irreducible-chain) dimension `d`, matching the dimension of its source space. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.AffineSubspaceDimension -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.linearMap_isPolynomial` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap ⇑F` | A linear map from a finite-coordinate source is polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineLinearMap_isPolynomial` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap fun x => c + F x` | An affine-linear map from a finite-coordinate source is polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineSubspace_hasAffineZariskiDimension` | `Module.finrank ℂ ↥V = d → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d {x \| x - x₀ ∈ V}` | An affine translate of a `d`-dimensional linear subspace has exact irreducible-chain dimension `d`. For a complex linear subspace `V` of `κ → ℂ` and a base point `x₀`, if `V` has finite rank exactly `d`, then the affine translate `{x \| x - x₀ ∈ V}` has irreducible-chain (Zariski) dimension exactly `d`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.DenseImage -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure_polynomial_image_of_dense` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → ∀ {A : Set (ι → ℂ)}, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = Set.univ → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (f '' A) = Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (Set.range f)` | The closure of the image of a dense set under a polynomial map equals the closure of the full range. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.irreducible_affineClosure_polynomial_image_of_dense` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → ∀ {A : Set (ι → ℂ)}, Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure A = Set.univ → A.Nonempty → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (f '' A))` | The closure of a polynomial image of a nonempty dense subset of finite complex affine space is irreducible. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_isIrreducible` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsPolynomialMap f → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.IsIrreducibleAffineClosed (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure (Set.range f))` | For a map `f` from the affine space `ℂ^ι` (with `ι` finite) to `ℂ^κ` that is coordinatewise polynomial, the Zariski closure of the range of `f` is irreducible as a polynomially closed set. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.Definitions -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateMap` | `(κ → MvPolynomial ι ℂ) → (ι → ℂ) → κ → ℂ` | For two index sets and a family of complex polynomials, one for each output coordinate, the polynomial coordinate map sends every complex-valued assignment to the input indices to the complex-valued assignment whose output-coordinate value is the corresponding polynomial evaluated at that assignment. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialPullback` | `(κ → MvPolynomial ι ℂ) → MvPolynomial κ ℂ →ₐ[ℂ] MvPolynomial ι ℂ` | For two index sets and a family of complex polynomials, one for each output coordinate, the polynomial pullback is the complex-algebra homomorphism that replaces every output-coordinate variable in a polynomial relation by its corresponding coordinate polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra` | `(κ → MvPolynomial ι ℂ) → Subalgebra ℂ (MvPolynomial ι ℂ)` | For two index sets and a family of complex coordinate polynomials, the coordinate subalgebra is the complex subalgebra of the polynomial ring in the input coordinates generated by that family. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure` | `(κ → MvPolynomial ι ℂ) → Set (κ → ℂ)` | For two index sets and a family of complex coordinate polynomials, the polynomial image closure is the smallest complex affine algebraic set containing every output vector obtained by evaluating those polynomials at a complex-valued input vector. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.eval_polynomialPullback` | `(MvPolynomial.eval x) ((Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialPullback f) P) = (MvPolynomial.eval (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateMap f x)) P` | For index types `ι`, `κ`, a polynomial parameterization `f`, a point `x`, and a polynomial `P`, substituting the coordinate polynomials `f` into `P` and evaluating at `x` agrees with evaluating `P` directly at the point obtained by evaluating `f`'s coordinates at `x`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.CoordinateRing -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_vanishingIdeal` | `MvPolynomial.vanishingIdeal ℂ (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure f) = RingHom.ker (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialPullback f)` | A target polynomial relation holds throughout a polynomial image closure exactly when substituting the parameterization's coordinate polynomials makes the relation identically zero. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_coordinateRing` | `(f : κ → MvPolynomial ι ℂ) → (MvPolynomial κ ℂ ⧸ MvPolynomial.vanishingIdeal ℂ (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure f)) ≃ₐ[ℂ] ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f)` | For two index sets and a family of complex coordinate polynomials, the coordinate ring of the closure of the polynomial image is canonically isomorphic, as a complex algebra, to the subalgebra of the polynomial ring in the input coordinates generated by that family. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiDimension_iff_coordinateRingKrullDim` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.affineZariskiClosure Z = Z → (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d Z ↔ ringKrullDim (MvPolynomial ι ℂ ⧸ MvPolynomial.vanishingIdeal ℂ Z) = ↑d)` | For a subset `Z` of `ι → ℂ` (with `ι` finite), if `Z` is Zariski-closed, then `Z` has irreducible-chain (Zariski) dimension `d` exactly when the Krull dimension of its coordinate ring — the polynomial ring on `ι` variables modulo `Z`'s vanishing ideal — equals `d`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.Transcendence -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.coordinateSubalgebraAlgebra` | `(f : κ → MvPolynomial ι ℂ) → Algebra ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f)` | For a source-coordinate index set, a target-coordinate index set, and a family $f$ of complex multivariate polynomials indexed by target coordinates, the complex-algebra structure on the subalgebra generated by the coordinate polynomials of $f$ is the usual one inherited from the surrounding polynomial algebra. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.finiteTypeDomain_ringKrullDim_eq_trdeg` | `Algebra.trdeg K A = ↑d → ringKrullDim A = ↑d` | A finitely generated integral algebra over a field has Krull dimension equal to its finite transcendence degree, the number of algebraically independent coordinates it contains. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_dimension_of_trdeg` | `Algebra.trdeg ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f) = ↑d → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure f)` | For a finite polynomial parameterization `f` of `κ`-many coordinates by variables indexed by a finite set `ι`, if the transcendence degree of the subalgebra of `ℂ` generated by the coordinate polynomials of `f` equals `d`, then the Zariski closure of the image of `f` has exact affine (irreducible-chain) dimension `d`. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.coordinateSubalgebra_trdeg_le_of_surjection` | `Function.Surjective ⇑present → Algebra.trdeg ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f) ≤ ↑(Nat.card τ)` | A surjective presentation by a polynomial algebra in a given number of generators certifies that the coordinate subalgebra has no larger transcendence degree. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.coordinateSubalgebra_trdeg_le_of_polynomial_factorization` | `(∀ (k : κ), (MvPolynomial.bind₁ g) (h k) = f k) → Algebra.trdeg ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f) ≤ ↑(Nat.card τ)` | The algebraic dimension (transcendence degree) of the coordinate algebra generated by a polynomial map is at most `k` whenever each of its coordinates can be written as a polynomial expression in a fixed family of `k` polynomials. This is the factorization-based companion to a Jacobian-minor bound: it certifies an upper bound on dimension from an explicit polynomial reparametrization rather than from a derivative rank. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.Jacobian -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.jacobianCoordinateSubalgebraAlgebra` | `(f : κ → MvPolynomial ι ℂ) → Algebra ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f)` | For two index sets and a family of complex coordinate polynomials, the coordinate subalgebra inherits its complex-algebra structure, namely the structure induced by its inclusion in the polynomial ring in the input coordinates. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialJacobianMinor` | `(κ → MvPolynomial ι R) → (Fin d → κ) → (Fin d → ι) → MvPolynomial ι R` | For a commutative coefficient ring, two index sets, a nonnegative integer determining the minor size, a family of polynomials indexed by the output coordinates, a selection of that many output coordinates, and a selection of that many input coordinates, the polynomial Jacobian minor is the determinant of the square matrix whose entry in each selected row and column is the corresponding formal partial derivative, before evaluation at any point. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.pderiv_polynomialPullback` | `(MvPolynomial.pderiv i) ((Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialPullback f) P) = ∑ j, (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialPullback f) ((MvPolynomial.pderiv j) P) * (MvPolynomial.pderiv i) (f j)` | The partial derivative of a polynomial after polynomial substitution equals the sum of the substituted partial derivatives, each weighted by the corresponding derivative of the substituted polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.totalDegree_pderiv_lt` | `(MvPolynomial.pderiv i) P ≠ 0 → ((MvPolynomial.pderiv i) P).totalDegree < P.totalDegree` | A nonzero partial derivative of a polynomial has strictly smaller total degree than the original polynomial. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.sum_X_mul_pderiv_eq_sum_degree` | `∑ i, MvPolynomial.X i * (MvPolynomial.pderiv i) P = ∑ m ∈ P.support, Finsupp.degree m • (MvPolynomial.monomial m) (MvPolynomial.coeff m P)` | For a polynomial over a commutative semiring in finitely many variables, the sum of each variable times its partial derivative equals the sum of its monomials weighted by their total degrees. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.eq_C_of_forall_pderiv_eq_zero` | `(∀ (i : σ), (MvPolynomial.pderiv i) P = 0) → P = MvPolynomial.C (MvPolynomial.coeff 0 P)` | A polynomial over a characteristic-zero field in finitely many variables whose partial derivatives all vanish equals its constant coefficient. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.algebraicIndependent_of_polynomialJacobianMinor_ne_zero` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialJacobianMinor f rows cols ≠ 0 → AlgebraicIndependent ℂ fun a => f (rows a)` | Over the complex numbers, a nonzero square Jacobian minor proves that the corresponding coordinate polynomials satisfy no nontrivial algebraic relation. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_dimension_of_jacobian` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialJacobianMinor f rows cols ≠ 0 → Algebra.trdeg ℂ ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f) ≤ ↑d → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure f)` | Given a finite polynomial parameterization `f` of `κ`-many coordinates by variables indexed by a finite set `ι`, together with `d` selected output coordinates `rows` and input variables `cols`, if the corresponding `d × d` Jacobian minor of `f`, formed from partial derivatives, is nonzero and the transcendence degree of the coordinate subalgebra generated by `f` is at most `d`, then the Zariski closure of the image of `f` has exact affine (irreducible-chain) dimension `d`. |
| `Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure_dimension_of_jacobian_and_surjection` | `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialJacobianMinor f rows cols ≠ 0 → ∀ (present : MvPolynomial (Fin d) ℂ →ₐ[ℂ] ↥(Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialCoordinateSubalgebra f)), Function.Surjective ⇑present → Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.HasAffineZariskiDimension d (Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.polynomialImageClosure f)` | A nonzero Jacobian minor and a surjective presentation using the same number of generators identify the exact affine dimension of a polynomial image closure. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension -->
_(no documented declarations in Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension)_
<!-- /GEN -->

## 10a. `Mathlib/CondDistrib.lean` — Missing `condDistrib` lemmas (stubs to upstream)

Collects general-purpose conditional-distribution lemmas that are not
specific to this project and should plausibly be upstreamed to Mathlib.
Statements here are independent of the SCM / SWIG infrastructure.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.condDistrib_comp_right_measurableEquiv` | `∀ᵐ x ∂(μ.map X), condDistrib Y (e ∘ X) μ (e x) = condDistrib Y X μ x` (for `e : α ≃ᵐ β`) | Push-forward invariance of `condDistrib` under a measurable equivalence of the conditioning variable; proof via uniqueness of disintegration (`condDistrib_ae_eq_iff_measure_eq_compProd`) applied to the transported kernel `(condDistrib Y X μ).comap e.symm e.symm.measurable`. `Rule2Kernel`'s `hA_extract` step was closed inline without this stub. | **Proved** |
| `Causalean.measure_eq_bind_marginal_condDistrib` | `μ = (μ.map Prod.snd).bind (fun c => (condDistrib Prod.fst Prod.snd μ c).map (fun b => (b, c)))` for finite `μ : Measure (β × γ)`, `β` standard Borel | Measure-theoretic disintegration / chain rule: a finite measure on `β × γ` equals the `bind` of its `γ`-marginal against the regular conditional distribution of `β | γ`; consumed by the backdoor completeness chain-rule step | **Proved** |

## 10a''. `Mathlib/Probability/Kernel/GraphMapProd.lean` — graph push-forward of a product measure

General measure-theoretic core of "exogenous noise + a structural mechanism gives a composition
product": pushing a product measure forward along a graph map preserving the first coordinate is a
`compProd`. Independent of the SCM/SWIG layer; used by Invariant Causal Prediction (`MechanismFactor`).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.GraphMapProd.mechanismKernel` | `(νₗ : Measure γ) (Φ : β × γ → δ) : Kernel β δ`, with `mechanismKernel νₗ Φ o = νₗ.map (Φ (o, ·))` | The structural-mechanism kernel: at each value `o` of the first coordinate, push the second-factor law `νₗ` through the slice `Φ (o, ·)`. Markov when `νₗ` is a probability measure and `Φ` measurable. | **Proved** |
| `Causalean.Mathlib.GraphMapProd.map_graph_prod_eq_compProd` | `Measure.map (fun p => (p.1, Φ p)) (α.prod νₗ) = α.compProd (mechanismKernel νₗ Φ)` (`α` sfinite, `νₗ` probability, `Φ` measurable) | Pushing the product `α ⊗ νₗ` along the graph map `(o,l) ↦ (o, Φ(o,l))` equals the composition product against `mechanismKernel`. | **Proved** |

## 10a''a. `Mathlib/Probability/Kernel/ParameterizedKernelQuantileRealization.lean` — parameterized quantile realization

A Mathlib-shaped kernel-realization primitive: a real-valued Markov kernel with fibers supported
on the closed unit interval is realized by a single uniform variable through a jointly measurable
generalized inverse.  The module supplies the exact fiberwise pushforward law, measurable sections,
and the pointwise unit-interval range guarantee used by parameterized coupling constructions.

<!-- GEN:Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.SupportedOnUnitInterval` | `ProbabilityTheory.Kernel S ℝ → Prop` | For a real-valued Markov kernel, the unit-interval support property says that each parameter-specific probability distribution assigns total mass one to the closed interval from zero to one. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.kernelUnitQuantile` | `ProbabilityTheory.Kernel S ℝ → S → ↑unitInterval → ↑unitInterval` | For a real-valued Markov kernel, a parameter value, and a uniform level in the closed unit interval, the unit-interval-valued parameterized quantile is the supremum of the interval points whose fiber distribution mass below that point is strictly smaller than the supplied level. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization` | `ProbabilityTheory.Kernel S ℝ → S × ℝ → ℝ` | For a real-valued Markov kernel and a parameter--randomization pair, the real-valued quantile realization evaluates the unit-interval quantile after clamping the randomization coordinate to the closed unit interval. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization_mem_unitInterval` | `Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization κ p ∈ Set.Icc 0 1` | For a real-valued Markov kernel and a parameter--randomization pair, the corresponding realization value belongs to the closed unit interval. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.measurable_kernelUnitQuantile` | `Measurable (Function.uncurry (Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.kernelUnitQuantile κ))` | For a real-valued Markov kernel that is Markov, the unit-interval generalized inverse is jointly measurable in the kernel parameter and uniform level. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.measurable_quantileRealization` | `Measurable (Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization κ)` | For a real-valued Markov kernel, the real-valued quantile realization is jointly measurable in its parameter and real randomization input. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.measurable_quantileRealization_section` | `Measurable fun u => Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization κ (s, u)` | For a real-valued Markov kernel and a fixed parameter value, the resulting real-valued function of the randomization input is measurable. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.map_kernelUnitQuantile` | `Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.SupportedOnUnitInterval κ → ∀ (s : S), MeasureTheory.Measure.map (fun u => ↑(Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.kernelUnitQuantile κ s u)) MeasureTheory.volume = κ s` | Given a real-valued Markov kernel, the condition that each fiber is supported on the closed unit interval, and a parameter value, pushing canonical uniform unit-interval volume through the fiber's quantile map exactly recovers that kernel fiber. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.map_quantileRealization` | `Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.SupportedOnUnitInterval κ → ∀ (s : S), MeasureTheory.Measure.map (fun u => Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization κ (s, u)) (MeasureTheory.volume.restrict (Set.Icc 0 1)) = κ s` | Given a real-valued Markov kernel, the condition that each fiber is supported on the closed unit interval, and a parameter value, pushing Lebesgue measure restricted to the closed unit interval through the real quantile section exactly recovers that kernel fiber. |
| `Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.ae_quantileRealization_mem_unitInterval` | `∀ᵐ (u : ℝ) ∂MeasureTheory.volume.restrict (Set.Icc 0 1), Causalean.Mathlib.Probability.Kernel.ParameterizedKernelQuantileRealization.quantileRealization κ (s, u) ∈ Set.Icc 0 1` | For a real-valued Markov kernel and a parameter value, the realization section lies in the closed unit interval almost everywhere under the uniform law. |
<!-- /GEN -->

## 10a'''. `Mathlib/Probability/StdNormalCDF.lean` & `StdNormalMoments.lean` — standard-normal CDF, probit, truncated moment

Self-contained one-dimensional standard-normal analysis (Mathlib gap; upstream-contribution candidate), used by the Gaussian special case of the Dorn–Guo MSM bound. `Φ` reuses Mathlib's `cdf (gaussianReal 0 1)` (inheriting monotonicity + `atBot/atTop` limits); the rest is proven here. All `[propext, Classical.choice, Quot.sound]`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.stdNormalPDF` / `stdNormalCDF` | `ℝ → ℝ` | the standard-normal density `φ = gaussianPDFReal 0 1` and CDF `Φ = cdf (gaussianReal 0 1)` | **Defined** |
| `stdNormalCDF_continuous` | `Continuous stdNormalCDF` | `Φ` continuous — the standard normal is atomless (StieltjesFunction jump = 0) | **Proved, axiom-clean** |
| `stdNormalCDF_strictMono` | `StrictMono stdNormalCDF` | `Φ` strictly increasing — positive mass on every interval | **Proved, axiom-clean** |
| `stdNormalCDF_pos` / `stdNormalCDF_lt_one` | `0 < Φ x` / `Φ x < 1` | full support: `Φ` stays in the open interval `(0,1)` | **Proved, axiom-clean** |
| `probit` | `ℝ → ℝ := sInf {x \| p ≤ Φ x}` | the standard-normal quantile `Φ⁻¹` (generalized inverse) | **Defined** |
| `stdNormalCDF_probit` / `probit_stdNormalCDF` | `Φ(Φ⁻¹ p)=p` on `(0,1)` / `Φ⁻¹(Φ x)=x` | exact two-sided inversion (IVT + strict monotonicity) | **Proved, axiom-clean** |
| `integral_Ioi_stdNormalPDF` | `∫_{c}^∞ φ = 1 − Φ(c)` | the survival integral | **Proved, axiom-clean** |
| `integral_Ioi_id_mul_stdNormalPDF` | `∫_{c}^∞ t·φ(t) dt = φ(c)` | the **truncated first moment** (antiderivative `−φ` + FTC) — the identity behind the closed-form Gaussian MSM half-width | **Proved, axiom-clean** |
| `gaussianMSMHalfWidthFactor` | `ℝ → ℝ := (Λ²−1)/Λ · φ(Φ⁻¹(Λ/(Λ+1)))` | the Dorn–Guo Gaussian half-width scalar | **Defined** |

`Mathlib/Probability/MeasurableCondQuantile.lean` (**proved, axiom-clean**) builds the remaining Dorn–Guo Phase-7 substrate from Mathlib's `condCDF`: `condQuantile ρ τ a = sInf {x | τ a ≤ condCDF ρ a x}` (the conditional generalized inverse) and `measurable_condQuantile_and_attains` — it is measurable in `a` (sublevel-set identity `{q ≤ t} = {a | τ a ≤ condCDF ρ a t}` + `measurable_condCDF`) and, under atomless conditional laws (`Continuous (condCDF ρ a)`), attains the level `condCDF ρ a (condQuantile ρ τ a) = τ a` (right-continuity ⇒ inf attained; IVT ⇒ exact). The conditional analogue of `Causalean.Stat.quantile`; an upstream-Mathlib-contribution candidate. It is consumed by `MSM/CutoffConstruct.lean` (§9j2msm) to construct the calibrating cutoff and close the sharp bound.

## 10a'''''. `Mathlib/Probability/VarianceProd.lean` — law of total variance on a product space

The explicit product-measure form of the variance decomposition for a statistic of two independent coordinates: total variance is the average within-slice variance plus the variance of the slice means. Mathlib's `integral_condVar_add_variance_condExp` states this abstractly via conditional expectation but does not identify its terms with product-coordinate slices, which is what call sites over `μ.prod ν` actually need.

<!-- GEN:Causalean.Mathlib.Probability.VarianceProd -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.variance_prod_eq_integral_variance_add` | `MeasureTheory.MemLp F 2 (μ.prod ν) → (∀ᵐ (t : T) ∂ν, MeasureTheory.MemLp (fun s => F (s, t)) 2 μ) → MeasureTheory.MemLp m 2 ν → (∀ᵐ (t : T) ∂ν, ∫ (s : Ω), F (s, t) ∂μ = m t) → ProbabilityTheory.variance F (μ.prod ν) = ∫ (t : T), ProbabilityTheory.variance (fun s => F (s, t)) μ ∂ν + ProbabilityTheory.variance m ν` | For independent coordinates with laws `μ` and `ν`, and a real-valued statistic `F` on the product that is square-integrable under the product law, suppose that almost every slice of `F` along the first coordinate, at a fixed value of the second coordinate, is square-integrable, that the map `m` of slice means is square-integrable under `ν`, and that `m` records the correct slice means: the integral of `F` over the first coordinate at almost every value of the second coordinate equals `m` there. Then the variance of `F` under the product measure equals the `ν`-average of the within-slice variances of `F` plus the variance, across the second coordinate, of the slice-mean map `m`. |
<!-- /GEN -->

## 10a''''''. `Mathlib/Probability/IidMeanVariance.lean` — moments and deviations of an i.i.d. empirical mean

Scalar and finite-dimensional `L²`/`L¹` deviation bounds for an empirical mean over a finite i.i.d. product measure, together with the exact first two moments of the average (population mean; population variance divided by `n`).

<!-- GEN:Causalean.Mathlib.Probability.IidMeanVariance -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.sqrt_sum_sq_le_sum_abs` | `√(∑ i, v i ^ 2) ≤ ∑ i, \|v i\|` | The Euclidean length of a finite real-valued vector is no greater than the sum of the absolute values of its components. |
| `Mathlib.Probability.integrable_euclidean_of_integrable` | `(∀ (i : ι), MeasureTheory.Integrable (fun ω => v ω i) μ) → MeasureTheory.Integrable (fun ω => √(∑ i, v ω i ^ 2)) μ` | The Euclidean length of a finite family of integrable real functions is itself integrable. |
| `Mathlib.Probability.memLp_two_sqrt_sum_sq` | `(∀ (k : ι), MeasureTheory.MemLp (Y k) 2 μ) → MeasureTheory.MemLp (fun ω => √(∑ k, Y k ω ^ 2)) 2 μ` | The Euclidean length of a finite family of square-integrable real functions is itself square-integrable. |
| `Mathlib.Probability.iid_mean_sq_le_fintype` | `MeasureTheory.MemLp ξ 2 μ → (∫ (s : ι → Ω), ((↑(Fintype.card ι))⁻¹ * ∑ i, ξ (s i) - ∫ (ω : Ω), ξ ω ∂μ) ^ 2 ∂MeasureTheory.Measure.pi fun x => μ) ≤ (∫ (ω : Ω), ξ ω ^ 2 ∂μ) / ↑(Fintype.card ι)` | The mean squared error of a square-integrable scalar sample average from independent, identically distributed observations is at most the population second moment divided by the sample size. |
| `Mathlib.Probability.iid_mean_abs_le_fintype` | `MeasureTheory.MemLp ξ 2 μ → (∫ (s : ι → Ω), \|(↑(Fintype.card ι))⁻¹ * ∑ i, ξ (s i) - ∫ (ω : Ω), ξ ω ∂μ\| ∂MeasureTheory.Measure.pi fun x => μ) ≤ √((∫ (ω : Ω), ξ ω ^ 2 ∂μ) / ↑(Fintype.card ι))` | The mean absolute error of a square-integrable scalar sample average from independent, identically distributed observations is at most the square root of the population second moment divided by the sample size. |
| `Mathlib.Probability.memLp_two_iid_mean_euclidean_fintype` | `(∀ (k : κ), MeasureTheory.MemLp (ξ k) 2 μ) → MeasureTheory.MemLp (fun s => √(∑ k, ((↑(Fintype.card ι))⁻¹ * ∑ i, ξ k (s i) - ∫ (ω : Ω), ξ k ω ∂μ) ^ 2)) 2 (MeasureTheory.Measure.pi fun x => μ)` | The centred coordinatewise sample averages of finitely many square-integrable statistics form a square-integrable Euclidean length on the product sample space. |
| `Mathlib.Probability.iid_mean_euclidean_abs_le_fintype` | `(∀ (k : κ), MeasureTheory.MemLp (ξ k) 2 μ) → (∫ (s : ι → Ω), √(∑ k, ((↑(Fintype.card ι))⁻¹ * ∑ i, ξ k (s i) - ∫ (ω : Ω), ξ k ω ∂μ) ^ 2) ∂MeasureTheory.Measure.pi fun x => μ) ≤ √((∑ k, ∫ (ω : Ω), ξ k ω ^ 2 ∂μ) / ↑(Fintype.card ι))` | The expected Euclidean error of finitely many square-integrable sample averages from the same independent, identically distributed sample is controlled by their summed population second moments and the sample size. |
| `Mathlib.Probability.iid_average_integral_fintype` | `MeasureTheory.Integrable F μ → (∫ (sample : ι → Ω), (↑(Fintype.card ι))⁻¹ * ∑ i, F (sample i) ∂MeasureTheory.Measure.pi fun x => μ) = ∫ (o : Ω), F o ∂μ` | An average of independent, identically distributed observations has the same expectation as the population statistic being averaged. |
| `Mathlib.Probability.iid_average_variance_fintype` | `MeasureTheory.MemLp F 2 μ → ProbabilityTheory.variance (fun sample => (↑(Fintype.card ι))⁻¹ * ∑ i, F (sample i)) (MeasureTheory.Measure.pi fun x => μ) = (↑(Fintype.card ι))⁻¹ * ProbabilityTheory.variance F μ` | The variance of an average of independent, identically distributed observations is the population variance divided by the sample size. |
| `Mathlib.Probability.iid_mean_sq_le` | `0 < n → ∀ (ξ : Ω → ℝ), MeasureTheory.MemLp ξ 2 μ → (∫ (s : Fin n → Ω), ((↑n)⁻¹ * ∑ i, ξ (s i) - ∫ (ω : Ω), ξ ω ∂μ) ^ 2 ∂MeasureTheory.Measure.pi fun x => μ) ≤ (∫ (ω : Ω), ξ ω ^ 2 ∂μ) / ↑n` | The mean squared error of a square-integrable scalar sample average of `n` independent, identically distributed observations is at most the population second moment divided by `n`. |
| `Mathlib.Probability.iid_mean_abs_le` | `0 < n → ∀ (ξ : Ω → ℝ), MeasureTheory.MemLp ξ 2 μ → (∫ (s : Fin n → Ω), \|(↑n)⁻¹ * ∑ i, ξ (s i) - ∫ (ω : Ω), ξ ω ∂μ\| ∂MeasureTheory.Measure.pi fun x => μ) ≤ √((∫ (ω : Ω), ξ ω ^ 2 ∂μ) / ↑n)` | The mean absolute error of a square-integrable scalar sample average of `n` independent, identically distributed observations is at most the square root of the population second moment divided by `n`. |
| `Mathlib.Probability.iid_mean_euclidean_abs_le` | `0 < n → ∀ (ξ : ι → Ω → ℝ), (∀ (k : ι), MeasureTheory.MemLp (ξ k) 2 μ) → (∫ (s : Fin n → Ω), √(∑ k, ((↑n)⁻¹ * ∑ i, ξ k (s i) - ∫ (ω : Ω), ξ k ω ∂μ) ^ 2) ∂MeasureTheory.Measure.pi fun x => μ) ≤ √((∑ k, ∫ (ω : Ω), ξ k ω ^ 2 ∂μ) / ↑n)` | For a strictly positive sample size `n` and finitely many square-integrable real-valued statistics indexed by `k`, each observed on the same `n`-point independent, identically distributed sample, the expected Euclidean norm of the vector of centered sample averages — one coordinate per statistic — is at most the square root of the sum of the population second moments divided by `n`. |
| `Mathlib.Probability.memLp_two_iid_mean_euclidean` | `(∀ (k : ι), MeasureTheory.MemLp (ξ k) 2 μ) → MeasureTheory.MemLp (fun s => √(∑ k, ((↑n)⁻¹ * ∑ i, ξ k (s i) - ∫ (ω : Ω), ξ k ω ∂μ) ^ 2)) 2 (MeasureTheory.Measure.pi fun x => μ)` | The centred coordinatewise sample averages of finitely many square-integrable statistics over an `n`-point independent, identically distributed sample form a square-integrable Euclidean length. |
| `Mathlib.Probability.integrable_iid_mean_euclidean` | `(∀ (k : ι), MeasureTheory.Integrable (ξ k) μ) → MeasureTheory.Integrable (fun s => √(∑ k, ((↑n)⁻¹ * ∑ i, ξ k (s i) - ∫ (ω : Ω), ξ k ω ∂μ) ^ 2)) (MeasureTheory.Measure.pi fun x => μ)` | The centred coordinatewise sample averages of finitely many integrable statistics over an `n`-point independent, identically distributed sample have integrable Euclidean length. |
| `Mathlib.Probability.iid_average_integral` | `0 < m → ∀ (F : Ω → ℝ), MeasureTheory.Integrable F μ → (∫ (sample : Fin m → Ω), (↑m)⁻¹ * ∑ i, F (sample i) ∂MeasureTheory.Measure.pi fun x => μ) = ∫ (o : Ω), F o ∂μ` | An average of `n` independent, identically distributed observations has the same expectation as the population statistic being averaged. |
| `Mathlib.Probability.iid_average_variance` | `MeasureTheory.MemLp F 2 μ → ProbabilityTheory.variance (fun sample => (↑m)⁻¹ * ∑ i, F (sample i)) (MeasureTheory.Measure.pi fun x => μ) = (↑m)⁻¹ * ProbabilityTheory.variance F μ` | The variance of an average of independent, identically distributed observations is the population variance divided by the sample size (including the zero-length case). |
<!-- /GEN -->

## 10a''''''a. `Mathlib/Probability/WeightedProduct.lean` — weighted marked-product residual bounds

Finite-product probability calculus for random-design regression: weights may use the whole
design vector, while conditional centering still yields the sharp one-quarter variance and
one-half L¹ constants.

<!-- GEN:Causalean.Mathlib.Probability.WeightedProduct -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.designVector` | `(Omega → D) → (Fin N → Omega) → Fin N → D` | For a map assigning each observation a design value and a finite sample of observations, the design vector assigns to every sample coordinate the design value of the observation at that coordinate. |
| `Mathlib.Probability.measurable_designVector` | `Measurable design → Measurable (Causalean.Mathlib.Probability.designVector design)` | For a design map that is measurable, the corresponding finite-sample vector of design values is measurable. |
| `Mathlib.Probability.weightedCenteredSum` | `(Omega → D) → (Omega → ℝ) → (D → ℝ) → ((Fin N → D) → Fin N → ℝ) → (Fin N → Omega) → ℝ` | For a map assigning each observation a design value, a real-valued outcome function, a real-valued function of design values, weights that may depend on the complete design vector, and a finite sample of observations, the weighted centered sum is the sum over sample coordinates of each weight times the outcome minus the given function evaluated at that coordinate's design value. |
| `Mathlib.Probability.product_weighted_centered_sq_integral_le` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → (∀ (omega : Omega), 0 ≤ Y omega) → (∀ (omega : Omega), Y omega ≤ 1) → ∀ (mD : D → ℝ), Measurable mD → P[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[P] mD ∘ design → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → MeasureTheory.Integrable (fun z => ∑ i, w (Causalean.Mathlib.Probability.designVector design z) i ^ 2) (MeasureTheory.Measure.pi fun x => P) → MeasureTheory.Integrable (fun z => Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w z ^ 2) (MeasureTheory.Measure.pi fun x => P) → (∫ (z : Fin N → Omega), Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w z ^ 2 ∂MeasureTheory.Measure.pi fun x => P) ≤ 1 / 4 * ∫ (z : Fin N → Omega), ∑ i, w (Causalean.Mathlib.Probability.designVector design z) i ^ 2 ∂MeasureTheory.Measure.pi fun x => P` | For a one-observation probability law, a measurable design map, a measurable outcome confined to the unit interval, a measurable design regression that is its conditional expectation, a measurable array of weights depending on the full design vector, and the two natural square-integrability conditions, the second moment of the weighted centered sum is at most one quarter of the expected sum of squared weights. |
| `Mathlib.Probability.product_weighted_centered_l1_le` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → (∀ (omega : Omega), 0 ≤ Y omega) → (∀ (omega : Omega), Y omega ≤ 1) → ∀ (mD : D → ℝ), Measurable mD → P[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[P] mD ∘ design → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → MeasureTheory.Integrable (fun z => ∑ i, w (Causalean.Mathlib.Probability.designVector design z) i ^ 2) (MeasureTheory.Measure.pi fun x => P) → MeasureTheory.Integrable (fun z => Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w z ^ 2) (MeasureTheory.Measure.pi fun x => P) → (∫ (z : Fin N → Omega), \|Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w z\| ∂MeasureTheory.Measure.pi fun x => P) ≤ 1 / 2 * √(∫ (z : Fin N → Omega), ∑ i, w (Causalean.Mathlib.Probability.designVector design z) i ^ 2 ∂MeasureTheory.Measure.pi fun x => P)` | For a one-observation probability law, a measurable design map, a measurable outcome confined to the unit interval, a measurable design regression that is its conditional expectation, a measurable array of weights depending on the full design vector, and the two natural square-integrability conditions, the expected absolute weighted centered sum is at most one half the square root of the expected sum of squared weights. |
<!-- /GEN -->

## 10a'''''''. `Mathlib/Probability/BernoulliMeasure.lean` — two-point Bernoulli laws

The Bernoulli law on the real line supported on `0` and `1` (`bernoulliLaw`) with its probability, support, integral, absolute-continuity and KL facts, plus the `Bool`-valued sibling `bernoulliBool`. The `Bool` version carries the same two-point law but is valued in `Bool` rather than `{0,1} ⊆ ℝ`, which is what lets it act as a Markov kernel into a `Bool` coordinate of a potential-outcome tuple — the form every binary-treatment witness construction needs.

<!-- GEN:Causalean.Mathlib.Probability.BernoulliMeasure -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.bernoulliLaw` | `ℝ → MeasureTheory.Measure ℝ` | For any real number interpreted as a success parameter, the real-valued Bernoulli measure assigns mass $\max(p,0)$ to $1$ and mass $\max(1-p,0)$ to $0$. |
| `Mathlib.Probability.bernoulliLaw_integral` | `0 ≤ p → p ≤ 1 → ∀ (f : ℝ → ℝ), ∫ (y : ℝ), f y ∂Causalean.Mathlib.Probability.bernoulliLaw p = p * f 1 + (1 - p) * f 0` | Two-point integral for the custom `bernoulliLaw`: since `bernoulliLaw p = ENNReal.ofReal p • dirac 1 + ENNReal.ofReal (1-p) • dirac 0`, its integral splits via `integral_add_measure` / `integral_smul_measure` / `integral_dirac` into the two-point weighted sum, with `0 ≤ p ≤ 1` collapsing the `ENNReal → ℝ` coercions to `p` and `1 - p`. |
| `Mathlib.Probability.bernoulliLaw_isProbabilityMeasure` | `0 ≤ p → p ≤ 1 → MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.bernoulliLaw p)` | The custom `bernoulliLaw` is a probability measure for `0 ≤ p ≤ 1`: its total mass is `p + (1 - p) = 1`. |
| `Mathlib.Probability.bernoulliLaw_ae_nonneg` | `0 ≤ᵐ[Causalean.Mathlib.Probability.bernoulliLaw p] fun y => y` | The Bernoulli law on the real line is almost surely nonnegative, since all of its mass is placed at `0` and `1`. |
| `Mathlib.Probability.bernoulliLaw_ae_le_one` | `(fun y => y) ≤ᵐ[Causalean.Mathlib.Probability.bernoulliLaw p] fun x => 1` | The Bernoulli law on the real line is almost surely at most one, since all of its mass is placed at `0` and `1`. |
| `Mathlib.Probability.bernoulliLaw_ae_zero_or_one` | `∀ᵐ (y : ℝ) ∂Causalean.Mathlib.Probability.bernoulliLaw p, y = 0 ∨ y = 1` | A draw from the real-valued Bernoulli law is almost surely either `0` or `1`. |
| `Mathlib.Probability.bernoulliLaw_ac_of_reference_interior` | `0 < q → q < 1 → (Causalean.Mathlib.Probability.bernoulliLaw p).AbsolutelyContinuous (Causalean.Mathlib.Probability.bernoulliLaw q)` | A Bernoulli law is absolutely continuous with respect to any Bernoulli law whose success probability is strictly between zero and one. |
| `Mathlib.Probability.bernoulliLaw_llr_integrable` | `MeasureTheory.Integrable (MeasureTheory.llr (Causalean.Mathlib.Probability.bernoulliLaw p) (Causalean.Mathlib.Probability.bernoulliLaw q)) (Causalean.Mathlib.Probability.bernoulliLaw p)` | The log-likelihood ratio between any two real-parameter Bernoulli laws is integrable under the first law. |
| `Mathlib.Probability.bernoulliLaw_klDiv_toReal` | `0 ≤ p → p ≤ 1 → 0 < q → q < 1 → (InformationTheory.klDiv (Causalean.Mathlib.Probability.bernoulliLaw p) (Causalean.Mathlib.Probability.bernoulliLaw q)).toReal = p * Real.log (p / q) + (1 - p) * Real.log ((1 - p) / (1 - q))` | Let $p$ be a Bernoulli success probability with $0 \le p \le 1$, and let $q$ be a reference success probability with $0 < q < 1$. Then the Kullback–Leibler divergence from the Bernoulli($p$) law to the Bernoulli($q$) law equals $p \log(p/q) + (1-p)\log((1-p)/(1-q))$, the usual two-point KL formula: a success contribution plus a failure contribution. |
| `Mathlib.Probability.bernoulliLaw_klDiv_le_four_sq_sub` | `1 / 4 ≤ p → p ≤ 3 / 4 → 1 / 4 ≤ q → q ≤ 3 / 4 → InformationTheory.klDiv (Causalean.Mathlib.Probability.bernoulliLaw p) (Causalean.Mathlib.Probability.bernoulliLaw q) ≤ ENNReal.ofReal (4 * (p - q) ^ 2)` | If both success probabilities $p$ and $q$ lie in the middle half of the unit interval, $1/4 \le p \le 3/4$ and $1/4 \le q \le 3/4$, then the Kullback–Leibler divergence from the Bernoulli($p$) law to the Bernoulli($q$) law is at most four times the squared difference of the probabilities, $4(p-q)^2$. |
| `Mathlib.Probability.bernoulliLaw_lintegral_ofReal` | `∫⁻ (y : ℝ), f y ∂Causalean.Mathlib.Probability.bernoulliLaw p = ENNReal.ofReal p * f 1 + ENNReal.ofReal (1 - p) * f 0` | `ℝ≥0∞`/`lintegral` analogue of `bernoulliLaw_integral`: a Bernoulli law integrates an `ℝ≥0∞`-valued function as the two-point weighted sum. |
| `Mathlib.Probability.bernoulliBool` | `ℝ → MeasureTheory.Measure Bool` | For any real number interpreted as a success parameter, the Boolean-valued Bernoulli measure assigns mass $\max(p,0)$ to true and mass $\max(1-p,0)$ to false. |
| `Mathlib.Probability.measurable_bernoulliBool` | `Measurable Causalean.Mathlib.Probability.bernoulliBool` | The Bool-valued Bernoulli distribution varies measurably with its success probability, so a measurable probability parameter can be used to construct a measurable kernel. |
| `Mathlib.Probability.bernoulliBool_isProbabilityMeasure` | `0 ≤ p → p ≤ 1 → MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.bernoulliBool p)` | A Bool-valued Bernoulli distribution is a probability distribution whenever its success probability lies between zero and one. |
| `Mathlib.Probability.bernoulliBool_integral` | `0 ≤ p → p ≤ 1 → ∀ (f : Bool → ℝ), ∫ (z : Bool), f z ∂Causalean.Mathlib.Probability.bernoulliBool p = p * f true + (1 - p) * f false` | The expectation of a real-valued function of a Bool-valued Bernoulli draw is its value at success times the success probability plus its value at failure times the failure probability. |
| `Mathlib.Probability.bernoulliBool_bind` | `(Causalean.Mathlib.Probability.bernoulliBool p).bind K = ENNReal.ofReal p • K true + ENNReal.ofReal (1 - p) • K false` | Drawing a Bool-valued Bernoulli variable and then selecting a distribution according to its value produces the corresponding success-probability mixture of the two distributions. |
| `Mathlib.Probability.bernoulliBool_map` | `MeasureTheory.Measure.map f (Causalean.Mathlib.Probability.bernoulliBool p) = ENNReal.ofReal p • MeasureTheory.Measure.dirac (f true) + ENNReal.ofReal (1 - p) • MeasureTheory.Measure.dirac (f false)` | Transforming a Bool-valued Bernoulli draw produces a two-point distribution concentrated on the transformed success and failure values with their original probabilities. |
| `Mathlib.Probability.binomialWeight` | `ℕ → ℝ → ℕ → ℝ` | For a nonnegative number of trials, a real success parameter, and a nonnegative count, the binomial weight is $\binom{m}{j}p^j(1-p)^{m-j}$. |
| `Mathlib.Probability.sum_bernoulli_eq_binomial` | `∑ b, (∏ i, if b i = true then p else 1 - p) * F {i \| b i = true}.card = ∑ j ∈ Finset.range (Fintype.card ι + 1), Causalean.Mathlib.Probability.binomialWeight (Fintype.card ι) p j * F j` | A Bernoulli-weighted sum of any function of the success count equals the corresponding sum against the binomial mass function. |
| `Mathlib.Probability.totalized_inverse_count_le` | `(if 0 < j then (↑j)⁻¹ else 0) ≤ 2 * (↑j + 1)⁻¹` | The reciprocal of a positive integer, totalized to zero at the origin, is at most twice the reciprocal of its successor. |
| `Mathlib.Probability.binomial_totalized_inverse_count_le` | `0 < p → p ≤ 1 → (∑ j ∈ Finset.range (m + 1), Causalean.Mathlib.Probability.binomialWeight m p j * if 0 < j then (↑j)⁻¹ else 0) ≤ 2 / (↑(m + 1) * p)` | When the success probability is positive and at most one, the binomial expectation of the zero-safe inverse success count is at most twice the reciprocal of the trial count plus one times that probability. |
| `Mathlib.Probability.binomial_inverse_two_arms_interior_le` | `0 < epsilon → epsilon ≤ p → p ≤ 1 - epsilon → (∑ j ∈ Finset.range (m + 1), Causalean.Mathlib.Probability.binomialWeight m p j * if 0 < j ∧ j < m then (↑j)⁻¹ + (↑(m - j))⁻¹ else 0) ≤ 4 / (↑(m + 1) * epsilon)` | If the overlap margin is positive and the success probability lies between that margin and one minus the margin, the binomial expectation of the two inverse arm counts on the interior event is at most four divided by the trial count plus one times the margin. |
<!-- /GEN -->

## 10a''''''''. `Mathlib/Probability/FiniteMarkedPoissonPartition/` — finite marked Poisson experiments

Reusable finite marked-Poisson infrastructure for reductions that split an
observation law across finitely many measurable cells, recombine the cell
experiments, and recover an exact fixed-size independent sample by retaining
the smallest independent marks. The final module supplies the matching
finite-measure relative-entropy identity.

### Count-and-stream construction

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.IIDPoisson -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.iidStreamLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → MeasureTheory.Measure (ℕ → X)` | Given a probability measure on a measurable sample space, the law of an infinite independent and identically distributed stream is the product probability measure on infinite sequences whose every coordinate has that measure as its marginal. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.iidStreamLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.iidStreamLaw P)` | For every measurable sample space and probability measure on that space, the law of the infinite independent and identically distributed stream is a probability measure. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.iidStreamLaw_map_finPrefix` | `MeasureTheory.Measure.map (fun z i => z ↑i) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.iidStreamLaw P) = MeasureTheory.Measure.pi fun x => P` | Every finite prefix of length `n` of an infinite stream whose coordinates are i.i.d. with common law `P` has exactly the corresponding `n`-fold product law. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → NNReal → MeasureTheory.Measure (ℕ × (ℕ → X))` | Given a probability measure on a measurable sample space and a nonnegative Poisson rate, the joint count-and-stream law is the product law of a Poisson count with that rate and an independent infinite stream whose coordinates are independent with the given common distribution. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw P lam)` | For every measurable sample space, probability measure on that space, and nonnegative Poisson rate, the joint law of an independent Poisson count and an independent and identically distributed stream is a probability measure. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw_map_count` | `MeasureTheory.Measure.map Prod.fst (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw P lam) = ProbabilityTheory.poissonMeasure lam` | The count coordinate has the requested scalar Poisson law. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw_map_finPrefix` | `MeasureTheory.Measure.map (fun z i => z.2 ↑i) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.poissonIIDStreamLaw P lam) = MeasureTheory.Measure.pi fun x => P` | Every finite stream prefix remains an exact product sample after pairing the stream with an independent Poisson count. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poissonMeasure_singleton_eq_poissonPMF` | `(ProbabilityTheory.poissonMeasure r) {n} = (ProbabilityTheory.poissonPMF r) n` | The Poisson law with mean `r` gives the single count `n` exactly the Poisson probability mass at `n`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample` | `(X : Type u_2) → [MeasurableSpace X] → Type (max 0 u_2)` | For an observation space, a finite sample is a nonnegative integer sample size together with one observation for each position below that size. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed` | `(n : ℕ) → (Fin n → X) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X` | Given a nonnegative integer sample size and a tuple of observations indexed by its positions, the fixed-size embedding is the finite sample having that size and those observations. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.streamToFiniteSample` | `ℕ × (ℕ → X) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X` | Given a pair consisting of a nonnegative integer and an infinite observation stream, the associated finite sample has that integer as its size and retains exactly the corresponding initial segment of the stream. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_fixedSizeEmbed` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n)` | Embedding a fixed-size tuple into the finite-sequence space is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_finiteSample_count` | `Measurable Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count` | Reading the count of a finite sequence is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_streamToFiniteSample` | `Measurable Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.streamToFiniteSample` | Truncating a count-and-stream outcome to its selected prefix is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → NNReal → MeasureTheory.Measure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X)` | Given a probability measure on the observation space and a nonnegative Poisson mean, the finite Poisson sample law is the distribution obtained by drawing a Poisson count with that mean and an independent infinite sequence of independent observations from that probability measure, then retaining the initial segment selected by the count. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw P lam)` | Let the observation space be equipped with a $\sigma$-algebra. For a probability measure on that space and a nonnegative Poisson mean, the assertion that the finite Poisson sample law is a probability measure holds. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw_map_count` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw P lam) = ProbabilityTheory.poissonMeasure lam` | The count of a finite Poisson sample has scalar Poisson law with the requested mean. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw_restrict_count_eq` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finitePoissonSampleLaw P lam).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' {n}) = (ProbabilityTheory.poissonMeasure lam) {n} • MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n) (MeasureTheory.Measure.pi fun x => P)` | On the fibre where the count equals `n`, the finite Poisson law is the Poisson mass at `n` times the embedded `n`-fold product law. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → (R : MeasureTheory.Measure ℝ) → [MeasureTheory.IsProbabilityMeasure R] → NNReal → MeasureTheory.Measure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ))` | Given a probability measure for observations, a probability measure for real-valued marks, and a nonnegative Poisson mean, the finite marked Poisson sample law is the finite Poisson sample law whose independent observation--mark pairs have the product of those two measures as their common distribution. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam)` | Let the observation space be equipped with a $\sigma$-algebra. For a probability measure on the observation space, a probability measure on real-valued marks, and a nonnegative Poisson mean, the assertion that the finite marked Poisson sample law is a probability measure holds. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw_map_count` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam) = ProbabilityTheory.poissonMeasure lam` | The count of a finite marked Poisson sample has scalar Poisson law with mean `lam`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw_restrict_count_eq` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' {n}) = (ProbabilityTheory.poissonMeasure lam) {n} • MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n) (MeasureTheory.Measure.pi fun x => P.prod R)` | Fix a nonnegative Poisson rate `lam`. For an observation law `P` and an independent mark law `R`, restricting the finite marked Poisson sample law to the event that the observed count equals `n` yields exactly `poissonMeasure lam {n}` times the pushforward, under the fixed-size embedding, of `n` independent draws from the product measure `P.prod R`. |
<!-- /GEN -->

### Partition restriction and cell laws

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Partition.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition` | `(X : Type u_3) → (ι : Type u_4) → [MeasurableSpace X] → [MeasurableSpace ι] → Type (max u_3 u_4)` | A finite measurable partition of the sample space `X` into cells indexed by `ι`, represented by a classifier assigning each observation to its cell — the cells are the fibres of this map — where that classifier is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.ofSets` | `(A : ι → Set X) → (∀ (j : ι), MeasurableSet (A j)) → (Pairwise fun i j => Disjoint (A i) (A j)) → ⋃ j, A j = Set.univ → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition X ι` | Given a family of subsets of the observation space indexed by a finite index set, each of which is measurable, which are pairwise disjoint, and whose union is the whole observation space, the partition constructed from these sets assigns every observation to the unique index of the set containing it. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.ofSets_cellSet` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.ofSets A hA hdis hcover).cell ⁻¹' {j} = A j` | The classifier constructed from a disjoint measurable cover has exactly the supplied sets as its fibres. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellSet` | `ι → Set X` | Given a measurable classifier partition and a cell index, the cell set is the set of observations assigned to that index by the partition. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.measurableSet_cellSet` | `MeasurableSet (p.cellSet j)` | Every classifier cell is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.disjoint_cellSet` | `i ≠ j → Disjoint (p.cellSet i) (p.cellSet j)` | Distinct classifier cells are disjoint. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.iUnion_cellSet` | `⋃ j, p.cellSet j = Set.univ` | The union of all classifier cells is the whole observation space. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellMass` | `MeasureTheory.Measure X → ι → NNReal` | Given a measurable classifier partition, a measure on the observation space, and a cell index, the cell mass is the measure of that cell, represented as a nonnegative real number. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.sum_cellMass` | `∑ j, p.cellMass P j = 1` | Cell masses sum to one under a probability law. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellObservationLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → ι → MeasureTheory.Measure X` | Given a measurable classifier partition, a probability measure on the observation space, and a cell index, the within-cell observation law is the normalized restriction of that probability measure to the cell when the cell has positive probability, and is the original probability measure when the cell has probability zero. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellObservationLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (p.cellObservationLaw P j)` | Let the observation space and the cell-index space each be equipped with a $\sigma$-algebra. For a finite measurable partition of the observation space indexed by the cell-index space, a probability measure on the observation space, and a cell index, the assertion that the associated within-cell observation law is a probability measure holds, including when the selected cell has probability zero. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellObservationLaw_apply_cellSet` | `P (p.cellSet j) ≠ 0 → (p.cellObservationLaw P j) (p.cellSet j) = 1` | A positive-mass within-cell law assigns probability one to its own cell. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellIndices` | `ι → (s : Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)) → Finset (Fin s.count)` | Given a measurable classifier partition, a cell index, and a finite sample of observation--real-mark pairs, the cell indices are precisely the original sample positions whose observations belong to that cell. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.restrictCell` | `ι → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)` | Given a measurable classifier partition, a cell index, and a finite sample of observation--real-mark pairs, the restriction to that cell retains exactly the pairs whose observations belong to the cell, in their original relative order. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.restrictPartition` | `Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ) → ι → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)` | Given a measurable classifier partition and a finite sample of observation--real-mark pairs, the partition-wise restriction assigns to every cell index the sample obtained by retaining exactly the pairs in that cell, in their original relative order. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.measurable_restrictCell` | `Measurable (p.restrictCell j)` | Restriction to one measurable cell is a measurable map on finite marked sequences. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.measurable_restrictPartition` | `Measurable p.restrictPartition` | For a finite measurable partition `p` of the sample space into cells indexed by `ι`, the map sending a finite marked sequence to its family of restrictions to every cell simultaneously is measurable. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Partition.Splitting -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.gatherWord` | `(w : Fin n → ι) → (Fin n → Y) → (j : ι) → Fin (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.wordHistogram✝ w j) → Y` | Given an assignment of each of $n$ positions to a cell index and an $n$-tuple of points, the cell-wise regrouping assigns to each cell the tuple of points at positions assigned to that cell, ordered by their original positions. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.measurable_gatherWord` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.gatherWord w)` | Regrouping an `n`-tuple of points by the cell word `w` is a measurable map. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.fixedPartitionEmbed` | `(c : ι → ℕ) → ((j : ι) → Fin (c j) → Y) → ι → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample Y` | Given a nonnegative integer count for every cell index and for each cell index, a tuple of points having its specified count, the fixed partition embedding assigns to every cell index the finite sample with that count and that tuple of points. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.measurable_fixedPartitionEmbed` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.fixedPartitionEmbed c)` | Embedding one fixed-size point tuple per cell into one finite sample per cell is a measurable map. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.map_restrictPartition_finiteMarkedPoissonSampleLaw` | `MeasureTheory.Measure.map p.restrictPartition (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam) = MeasureTheory.Measure.pi fun j => Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw (p.cellObservationLaw P j) R (lam * p.cellMass P j)` | Partition splitting. Under the marked Poisson sample law with base probability measure `P`, mark distribution `R`, and nonnegative intensity `lam`, restricting the sample to each cell of the finite measurable partition `p` yields, jointly across cells, the product of independent marked Poisson sample laws, one per cell `j`, each with base measure `p.cellObservationLaw P j`, mark distribution `R`, and intensity `lam` times the `P`-mass of cell `j`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Partition.CellLaws -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.map_restrictCell_count_finiteMarkedPoissonSampleLaw` | `MeasureTheory.Measure.map (fun s => (p.restrictCell j s).count) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam) = ProbabilityTheory.poissonMeasure (lam * p.cellMass P j)` | Cell counts are Poisson. Under the marked Poisson sample law with base probability measure `P`, mark distribution `R`, and nonnegative intensity `lam`, the number of marked observations landing in cell `j` of the finite measurable partition `p`, viewed as a random variable, is Poisson distributed with mean equal to `lam` times the `P`-mass of cell `j`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteMeasurablePartition.cellLaw_restrict_count_eq` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw (p.cellObservationLaw P j) R (lam * p.cellMass P j)).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' {n}) = (ProbabilityTheory.poissonMeasure (lam * p.cellMass P j)) {n} • MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n) (MeasureTheory.Measure.pi fun x => (p.cellObservationLaw P j).prod R)` | On its count-`n` fibre, the `j`th cell experiment is the Poisson mass at `n` times `n` independent draws from the normalised restriction and mark law. |
<!-- /GEN -->

### Canonical superposition and retained prefixes

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Superposition.Canonical -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.superpose` | `(ι → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)` | Given one finite sample of observation--real-mark pairs for every member of a finite index set, the superposed finite sample contains all of their pairs, enumerated through the disjoint union of their coordinate positions. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.superpose_count` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.superpose q).count = ∑ j, (q j).count` | The count after finite superposition is the sum of the cell counts. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.pointsOfCount` | `(n : ℕ) → { s // s.count = n } → Fin n → Y` | Given a nonnegative integer and a finite sample known to have exactly that many points, the points at the specified count are the resulting tuple of its $n$ points. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_pointsOfCount` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.pointsOfCount n)` | Reading the `n` points off a finite sample of known size `n` is a measurable map into the space of `n`-tuples. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.fixedCountSuperpose` | `(c : ι → ℕ) → ((j : ι) → Fin (c j) → Y) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample Y` | Given a nonnegative integer count for every member of a finite index set and a tuple of points of the specified count for each member, the fixed-count superposition is the finite sample obtained by concatenating all those tuples. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_fixedCountSuperpose` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedCountSuperpose c)` | Concatenating a family of point tuples of fixed sizes into one finite sample is a measurable map. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.fiberPoints` | `(c : ι → ℕ) → ↑(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.countFiber✝ c) → (j : ι) → Fin (c j) → Y` | Given a nonnegative integer count for every index and a family of finite samples whose member at each index has exactly the specified count, the fixed-count component points are the corresponding family of point tuples. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_fiberPoints` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fiberPoints c)` | Reading off the points of every component sample on the fixed-count event is a measurable map. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_superpose` | `Measurable Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.superpose` | Finite superposition is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.markedKeys` | `(s : Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)) → Finset (Lex (ℝ × Fin s.count))` | Given a finite sample of observation--real-mark pairs, the mark-ordering keys are the finite set of lexicographically ordered pairs consisting of each real mark and its original sample position; the position distinguishes pairs with equal marks. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.markedKeys_card` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.markedKeys s).card = s.count` | The number of lexicographic mark-and-index keys equals the number of marked observations in the finite sample. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.markedKey_decode` | `z ∈ Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.markedKeys s → toLex ((s.points (ofLex z).2).2, (ofLex z).2) = z` | A key belonging to the mark-ordering set is recovered by decoding its stored original index and rebuilding its mark-and-index pair. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks` | `Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)` | Given a finite sample of observation--real-mark pairs, the mark-ordered sample contains the same pairs arranged in increasing order of their real marks, with equal marks ordered by original sample position. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks_count` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks s).count = s.count` | Ordering by marks preserves the sample count. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_orderByMarks` | `Measurable Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks` | The mark-ordering map is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → (R : MeasureTheory.Measure ℝ) → [MeasureTheory.IsProbabilityMeasure R] → NNReal → MeasureTheory.Measure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ))` | Given a probability measure for observations, a probability measure for real-valued marks, and a nonnegative Poisson mean, the canonical marked Poisson sample law is the distribution obtained from the finite marked Poisson sample law by arranging each realized sample in increasing order of its marks, breaking ties by original position. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam)` | Let the observation space be equipped with a $\sigma$-algebra. For a probability measure on the observation space, a probability measure on real-valued marks, and a nonnegative Poisson mean, the assertion that the canonical mark-ordered Poisson sample law is a probability measure holds. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.superposeByMarks` | `(ι → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ)` | Given one finite sample of observation--real-mark pairs for every member of a finite index set, the mark-ordered superposition is the superposition of those samples arranged in increasing order of real mark, with ties broken by original position. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_superposeByMarks` | `Measurable Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.superposeByMarks` | Mark-ordered finite superposition is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw_marks_pairwise_distinct` | `(Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam) {s \| ∀ (i j : Fin s.count), i ≠ j → (s.points i).2 ≠ (s.points j).2} = 1` | Atomless independent marks are pairwise distinct with probability one in the finite marked Poisson sample. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.map_restrictPartition_canonicalMarkedPoissonSampleLaw` | `MeasureTheory.Measure.map p.restrictPartition (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam) = MeasureTheory.Measure.pi fun j => Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw (p.cellObservationLaw P j) R (lam * p.cellMass P j)` | Restricting the canonical global configuration gives exactly the product of independent canonical cell configurations. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.map_superposeByMarks_map_restrictPartition` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.superposeByMarks (MeasureTheory.Measure.map p.restrictPartition (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam)) = Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam` | Restriction followed by mark-ordered superposition is the identity in law on the canonical marked Poisson configuration. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.map_superposeByMarks_canonicalCellLaws` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.superposeByMarks (MeasureTheory.Measure.pi fun j => Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw (p.cellObservationLaw P j) R (lam * p.cellMass P j)) = Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam` | Superposition recovers the canonical global law. Under nonnegative intensity `lam`, and independently for each cell `j` of the finite measurable partition `p`, draw a canonical marked Poisson configuration with base measure `p.cellObservationLaw P j`, mark distribution `R`, and intensity `lam` times the `P`-mass of cell `j`; merging these per-cell configurations by increasing mark (mark-ordered superposition) has exactly the law of the canonical marked Poisson configuration with base measure `P`, mark distribution `R`, and intensity `lam`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks_monotone_marks` | `a ≤ b → ((Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks s).points (Fin.cast (Eq.symm (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks_count s)) a)).2 ≤ ((Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks s).points (Fin.cast (Eq.symm (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.orderByMarks_count s)) b)).2` | Marks in the ordered sequence are nondecreasing. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Superposition.Retention -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.retainedObservations` | `X → (n : ℕ) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ) → Fin n → X` | For a fallback sample value, a nonnegative integer prefix length, and a finite sample of value--mark pairs, the retained-observations vector consists of the values attached to the first $n$ sample points after ordering by their marks when the sample contains at least $n$ points, and otherwise consists entirely of the fallback value. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.prefixPointOr` | `Y → (n : ℕ) → Fin n → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample Y → Y` | For a fallback sample value, a nonnegative integer prefix length, a position among the first $n$ positions, and a finite sample, the prefix point with fallback is the sample point at that position when the sample contains at least $n$ points, and is otherwise the fallback value. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_prefixPointOr` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.prefixPointOr y₀ n k)` | Reading off the `k`-th point of a finite sample, with a fallback value when the sample is too short, is a measurable map. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_retainedObservations` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.retainedObservations x₀ n)` | Retaining and forgetting marks is a measurable map to an `n`-tuple. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.map_retainedObservations_restrict_count_ge` | `MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.retainedObservations x₀ n) ((Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' Set.Ici n)) = (ProbabilityTheory.poissonMeasure lam) (Set.Ici n) • MeasureTheory.Measure.pi fun x => P` | On the event that at least `n` points exist, retaining the `n` smallest atomless independent marks and forgetting marks gives the event probability times the exact product law `P^n`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.normalized_map_retainedObservations_restrict_count_ge` | `(ProbabilityTheory.poissonMeasure lam) (Set.Ici n) ≠ 0 → ((ProbabilityTheory.poissonMeasure lam) (Set.Ici n))⁻¹ • MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.retainedObservations x₀ n) ((Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' Set.Ici n)) = MeasureTheory.Measure.pi fun x => P` | Conditioning on enough points gives i.i.d. draws. Fix a nonnegative intensity `lam` and suppose the Poisson(`lam`) probability of observing at least `n` points is nonzero. Under the marked Poisson sample law with base probability measure `P`, mark distribution `R`, and intensity `lam`, condition on the event that the sample count is at least `n`, retain the `n` mark-smallest points and forget their marks (`x₀` is an irrelevant filler value used only outside this event); the resulting normalised law equals the product of `n` independent copies of `P`. |
<!-- /GEN -->

### Relative entropy

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.KL -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMass` | `(ν : MeasureTheory.Measure X) → [MeasureTheory.IsFiniteMeasure ν] → NNReal` | Given a finite measure on the observation space, the finite-measure mass is its total mass on the whole observation space, represented as a nonnegative real number. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.normalizedFiniteMeasure` | `(ν : MeasureTheory.Measure X) → [MeasureTheory.IsFiniteMeasure ν] → (P₀ : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P₀] → MeasureTheory.Measure X` | Given a finite measure on the observation space and a fallback probability measure on that space, the normalized finite measure is the fallback probability measure when the finite measure is zero, and otherwise is that finite measure divided by its total mass. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.normalizedFiniteMeasure_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.normalizedFiniteMeasure ν P₀)` | For every measurable observation space, finite measure on that space, and probability measure on the same space, the normalized finite measure is a probability measure. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw` | `(ν : MeasureTheory.Measure X) → [MeasureTheory.IsFiniteMeasure ν] → (P₀ : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P₀] → (R : MeasureTheory.Measure ℝ) → [MeasureTheory.IsProbabilityMeasure R] → NNReal → MeasureTheory.Measure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ))` | Given a finite intensity measure on the observation space, a fallback probability measure on that space, a probability measure for real-valued marks, and a nonnegative scalar intensity, the finite-measure marked Poisson law is the finite marked Poisson sample law with observation distribution equal to the normalized intensity measure and Poisson mean equal to the scalar intensity times the total intensity mass. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν P₀ R lam)` | For every measurable observation space, finite intensity measure on that space, fallback probability measure on that space, probability distribution for real-valued marks, and nonnegative scalar intensity, the resulting finite-measure marked Poisson law is a probability measure. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw_map_count` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν P₀ R lam) = ProbabilityTheory.poissonMeasure (lam * Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMass ν)` | The count in the finite-measure marked Poisson experiment is Poisson with mean `lam` times the total mass of the intensity measure. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.klDiv_finiteMeasureMarkedPoissonLaw` | `ν₀ Set.univ = ν₁ Set.univ → InformationTheory.klDiv (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν₀ P₀ R lam) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν₁ P₀ R lam) = ↑lam * InformationTheory.klDiv ν₀ ν₁` | Fix a nonnegative Poisson rate `lam` and suppose the finite intensity measures `ν₀` and `ν₁` have equal total mass. Then, for a common baseline probability measure `P₀` and mark law `R`, the KL divergence between the finite-measure marked Poisson experiments generated by `ν₀` and by `ν₁` equals `lam` times the KL divergence between `ν₀` and `ν₁`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.klDiv_finiteMeasureMarkedPoissonLaw_le` | `ν₀ Set.univ = ν₁ Set.univ → InformationTheory.klDiv ν₀ ν₁ ≤ B → InformationTheory.klDiv (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν₀ P₀ R lam) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw ν₁ P₀ R lam) ≤ ↑lam * B` | A one-point finite-measure KL bound transfers directly to the corresponding marked Poisson experiments after multiplication by the scalar intensity. |
<!-- /GEN -->

## 10a''''. `Mathlib/Analysis/SecondOrderDescent.lean` — one-dimensional smooth descent

Mathlib-shaped scalar optimization helper: a second-derivative upper bound on a real interval
gives a quadratic Taylor upper model, and a strictly negative initial slope gives an explicit
descent gap at the clipped model-optimal step. The module is objective-agnostic; callers supply
the C² regularity and pointwise curvature bound.

<!-- GEN:Causalean.Mathlib.Analysis.SecondOrderDescent -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.descentStep` | `ℝ → ℝ → ℝ → ℝ` | For a real curvature bound, a real slope margin, and a real available interval length, the quadratic-model step is the available interval length when the curvature bound is zero and otherwise the smaller of that length and the slope margin divided by the curvature bound. |
| `Mathlib.Analysis.descentStep_of_eq_zero` | `Causalean.Mathlib.Analysis.descentStep 0 c T = T` | With zero curvature bound, the clipped quadratic-model step is the endpoint. |
| `Mathlib.Analysis.descentStep_of_pos` | `0 < M → ∀ (c T : ℝ), Causalean.Mathlib.Analysis.descentStep M c T = min T (c / M)` | With a positive curvature bound, the clipped quadratic-model step is the smaller of the available interval length and the unconstrained quadratic-model optimizer. |
| `Mathlib.Analysis.descentStep_mem_Icc` | `0 ≤ M → 0 ≤ c → 0 ≤ T → Causalean.Mathlib.Analysis.descentStep M c T ∈ Set.Icc 0 T` | The clipped quadratic-model step is feasible whenever the interval length, slope margin, and curvature bound are nonnegative. |
| `Mathlib.Analysis.second_order_upper_bound` | `ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ M) → ∀ {t : ℝ}, t ∈ Set.Icc 0 T → f t ≤ f 0 + deriv f 0 * t + M / 2 * t ^ 2` | A twice differentiable real function whose second derivative is bounded above on an interval's interior is no larger than its tangent at the left endpoint plus the quadratic curvature allowance. |
| `Mathlib.Analysis.first_order_upper_bound` | `ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ 0) → ∀ {t : ℝ}, t ∈ Set.Icc 0 T → f t ≤ f 0 + deriv f 0 * t` | A real function with nonpositive second derivative in an interval's interior lies below its tangent line at the left endpoint. |
| `Mathlib.Analysis.second_order_descent_gap` | `0 ≤ T → 0 ≤ c → 0 ≤ M → ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ M) → deriv f 0 ≤ -c → Causalean.Mathlib.Analysis.descentStep M c T ∈ Set.Icc 0 T ∧ f 0 - f (Causalean.Mathlib.Analysis.descentStep M c T) ≥ c * Causalean.Mathlib.Analysis.descentStep M c T - M / 2 * Causalean.Mathlib.Analysis.descentStep M c T ^ 2` | Quadratic-model descent gap. For a real function `f` on `[0, T]` where the interval length `T`, the slope margin `c`, and the curvature bound `M` are all nonnegative, `f` is twice continuously differentiable on `[0, T]`, `f` is differentiable at `0`, its second derivative is bounded above by `M` throughout the open interval, and its derivative at `0` is at most `−c`, then the clipped quadratic-model step lies in `[0, T]`, and the resulting decrease `f(0) − f(step)` is at least `c·step − (M/2)·step²`. |
| `Mathlib.Analysis.second_order_descent_gap_min` | `0 ≤ T → 0 ≤ c → 0 < M → ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ M) → deriv f 0 ≤ -c → f 0 - f (min T (c / M)) ≥ c / 2 * min T (c / M)` | With positive curvature, the clipped quadratic-model step gives at least half of the linear descent term. |
| `Mathlib.Analysis.first_order_descent_gap` | `0 ≤ T → ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ 0) → deriv f 0 ≤ -c → f 0 - f T ≥ c * T` | With zero curvature, the endpoint step gives the full linear descent guaranteed by the negative initial slope. |
| `Mathlib.Analysis.second_order_descent_gap_half` | `0 ≤ T → 0 ≤ c → 0 ≤ M → ContDiffOn ℝ 2 f (Set.Icc 0 T) → DifferentiableAt ℝ f 0 → (∀ t ∈ Set.Ioo 0 T, deriv (deriv f) t ≤ M) → deriv f 0 ≤ -c → Causalean.Mathlib.Analysis.descentStep M c T ∈ Set.Icc 0 T ∧ f 0 - f (Causalean.Mathlib.Analysis.descentStep M c T) ≥ c / 2 * Causalean.Mathlib.Analysis.descentStep M c T` | In both zero and positive curvature regimes, the clipped quadratic-model step gives a uniform half-linear decrease. |
<!-- /GEN -->

## 10a'''''. `Mathlib/Analysis/SingularValueWeyl.lean` — singular-value perturbations

Reusable finite-dimensional spectral perturbation infrastructure: every ordered singular value of
a real linear map changes by at most the operator norm of an additive perturbation.  The result is
rectangular-map compatible and retains Mathlib's zero extension beyond the finite-dimensional
range, making it suitable for rank-threshold and weak-identification arguments.

<!-- GEN:Causalean.Mathlib.Analysis.SingularValueWeyl -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.singularValues_le_of_large_subspace` | `j < Module.finrank ℝ V → ∀ (S : Submodule ℝ V), Module.finrank ℝ V ≤ Module.finrank ℝ ↥S + j → ∀ {c : ℝ}, (∀ x ∈ S, ‖A x‖ ≤ c * ‖x‖) → A.singularValues j ≤ c` | Given a linear map, an in-range singular-value index, and a subspace whose codimension is at most that index, if the map is bounded by a scalar multiple of the norm on that subspace, then the indexed singular value is at most that scalar. |
| `Mathlib.Analysis.exists_large_subspace_norm_le_singularValues` | `j < Module.finrank ℝ V → ∃ S, Module.finrank ℝ V ≤ Module.finrank ℝ ↥S + j ∧ ∀ x ∈ S, ‖A x‖ ≤ A.singularValues j * ‖x‖` | Given a linear map and an in-range singular-value index, there is a subspace whose codimension is at most that index and on which the map is bounded by the indexed singular value. |
| `Mathlib.Analysis.singularValues_add_le_add_opNorm` | `(A + D).singularValues j ≤ A.singularValues j + ‖LinearMap.toContinuousLinearMap D‖` | Given a baseline linear map, an additive perturbation, and a singular-value index, the perturbation can increase that indexed singular value by no more than its operator norm. |
| `Mathlib.Analysis.abs_singularValues_add_sub_singularValues_le_opNorm` | `\|(A + D).singularValues j - A.singularValues j\| ≤ ‖LinearMap.toContinuousLinearMap D‖` | Given a baseline linear map, an additive perturbation, and a singular-value index, the absolute change in the indexed singular value is at most the perturbation's operator norm. |
| `Mathlib.Analysis.ContinuousLinearMap.abs_singularValues_add_sub_singularValues_le` | `\|(↑(A + D)).singularValues j - (↑A).singularValues j\| ≤ ‖D‖` | Given a baseline continuous linear map, an additive continuous perturbation, and a singular-value index, the absolute change in the indexed singular value is at most the perturbation's operator norm. |
<!-- /GEN -->

## 10a'''''''. `Mathlib/Analysis/RectangularSignalSingularValues.lean` — rectangular signal singular values

Finite-dimensional real singular-value infrastructure for rectangular identification operators.
It proves a multiplicative lower bound on the rank-sized signal subspace, exact preservation under
orthonormal coordinates for the adjoint range, and monotonicity when compatible operators are
stacked vertically.  The results use zero-extended indices and therefore remain valid at and past
rank boundaries.

<!-- GEN:Causalean.Mathlib.Analysis.RectangularSignalSingularValues -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.le_singularValues_of_subspace` | `0 ≤ c → j < Module.finrank ℝ ↥S → (∀ x ∈ S, c * ‖x‖ ≤ ‖T x‖) → c ≤ T.singularValues j` | Given a real linear map, a candidate signal subspace, a singular-value index, a nonnegative expansion factor, a dimension condition putting that index below the signal-subspace dimension, and a lower expansion bound on the signal subspace, the indexed singular value is at least that factor. |
| `Mathlib.Analysis.least_singularValue_mul_norm_le` | `Function.Injective ⇑T → ∀ (x : E), T.singularValues (Module.finrank ℝ E - 1) * ‖x‖ ≤ ‖T x‖` | Given an injective finite-dimensional real linear map and a vector in its domain, the map expands that vector by at least its last domain-indexed singular value. |
| `Mathlib.Analysis.least_singularValue_mul_norm_le_adjoint_on_range` | `Function.Injective ⇑B → ∀ y ∈ B.range, B.singularValues (Module.finrank ℝ K - 1) * ‖y‖ ≤ ‖(LinearMap.adjoint B) y‖` | Given a full-column-rank real linear map and a vector in its column space, the adjoint expands that signal vector by at least the map's least singular value. |
| `Mathlib.Analysis.singularValues_product_adjoint_lower_bound` | `Function.Injective ⇑A → Function.Injective ⇑D → Function.Injective ⇑B → A.singularValues (Module.finrank ℝ K - 1) * D.singularValues (Module.finrank ℝ K - 1) * B.singularValues (Module.finrank ℝ K - 1) ≤ (A ∘ₗ D ∘ₗ LinearMap.adjoint B).singularValues (Module.finrank ℝ K - 1)` | Given a left rectangular factor, an injective square core, a right rectangular factor, and full-column-rank assumptions for the two rectangular factors, the last signal singular value of their adjoint product is at least the product of the three least signal singular values. |
| `Mathlib.Analysis.Matrix.singularValues_mul_mul_transpose_lower_bound` | `Function.Injective ⇑(Matrix.toEuclideanLin A) → Function.Injective ⇑(Matrix.toEuclideanLin D) → Function.Injective ⇑(Matrix.toEuclideanLin B) → (Matrix.toEuclideanLin A).singularValues (Fintype.card k - 1) * (Matrix.toEuclideanLin D).singularValues (Fintype.card k - 1) * (Matrix.toEuclideanLin B).singularValues (Fintype.card k - 1) ≤ (Matrix.toEuclideanLin (A * D * B.transpose)).singularValues (Fintype.card k - 1)` | Given finite real left and right matrices, an injective finite real square core, and full-column-rank assumptions for the two rectangular matrices, the last signal singular value of their transpose product is bounded below by the product of the three least signal singular values. |
| `Mathlib.Analysis.verticalStack` | `(E →ₗ[ℝ] F₀) → (E →ₗ[ℝ] F₁) → E →ₗ[ℝ] WithLp 2 (F₀ × F₁)` | For a real inner-product domain space, two real inner-product codomain spaces, and two real linear maps from the common domain into those respective codomains, the vertical stack is the real linear map sending each domain vector to its ordered pair of component outputs, equipped with the Hilbert direct-sum norm. |
| `Mathlib.Analysis.verticalStack_adjoint_comp_self` | `LinearMap.adjoint (Causalean.Mathlib.Analysis.verticalStack M₀ M₁) ∘ₗ Causalean.Mathlib.Analysis.verticalStack M₀ M₁ = LinearMap.adjoint M₀ ∘ₗ M₀ + LinearMap.adjoint M₁ ∘ₗ M₁` | Given two real linear maps with a common domain, the Gram operator of their vertical stack is the sum of their two component Gram operators. |
| `Mathlib.Analysis.singularValues_le_verticalStack_left` | `M₀.singularValues j ≤ (Causalean.Mathlib.Analysis.verticalStack M₀ M₁).singularValues j` | Given a first real linear map, a second compatible real linear map, and a singular-value index, the vertical stack's indexed singular value is at least that of the first component. |
| `Mathlib.Analysis.singularValues_le_verticalStack_right` | `M₁.singularValues j ≤ (Causalean.Mathlib.Analysis.verticalStack M₀ M₁).singularValues j` | Given a first compatible real linear map, a second real linear map, and a singular-value index, the vertical stack's indexed singular value is at least that of the second component. |
| `Mathlib.Analysis.singularValues_comp_linearIsometry_of_range_eq_adjoint_range` | `V.range = (LinearMap.adjoint M).range → ∀ (j : ℕ), (M ∘ₗ V.toLinearMap).singularValues j = M.singularValues j` | Given a real linear map, orthonormal signal coordinates, an equality between the coordinate range and the map's adjoint range, and a singular-value index, compressing to those coordinates preserves the indexed singular value. |
| `Mathlib.Analysis.singularValues_comp_linearIsometry_last` | `V.range = (LinearMap.adjoint M).range → (M ∘ₗ V.toLinearMap).singularValues (Module.finrank ℝ K - 1) = M.singularValues (Module.finrank ℝ K - 1)` | Given a real linear map, orthonormal coordinates for its adjoint range, the last coordinate-indexed singular value after compression equals the corresponding singular value before compression. |
<!-- /GEN -->

## 10a''''''''. `Mathlib/Analysis/TwoByTwoSpectralRoots.lean` — ordered roots of a two-by-two matrix

Explicit quadratic-formula roots for real two-by-two matrices, with their symmetric-matrix spectral interpretation and smoothness on the distinct-root region.

<!-- GEN:Causalean.Mathlib.Analysis.TwoByTwoSpectralRoots -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.rootDiscriminant` | `Matrix (Fin 2) (Fin 2) ℝ → ℝ` | Given a real two-by-two matrix, the discriminant governing its two ordered quadratic-formula roots is the squared diagonal difference plus four times the squared upper off-diagonal entry. |
| `Mathlib.Analysis.lambda₁` | `Matrix (Fin 2) (Fin 2) ℝ → ℝ` | Given a real two-by-two matrix, its larger quadratic-formula root is half the trace plus half the nonnegative square root of the discriminant. |
| `Mathlib.Analysis.lambda₂` | `Matrix (Fin 2) (Fin 2) ℝ → ℝ` | Given a real two-by-two matrix, its smaller quadratic-formula root is half the trace minus half the nonnegative square root of the discriminant. |
| `Mathlib.Analysis.strictGapSet` | `Set (Matrix (Fin 2) (Fin 2) ℝ)` | The strict-gap region consists of real two-by-two matrices whose lower quadratic-formula root is strictly below their upper root. |
| `Mathlib.Analysis.rootDiscriminant_nonneg` | `0 ≤ Causalean.Mathlib.Analysis.rootDiscriminant G` | For a real two-by-two matrix, its explicit discriminant is nonnegative. |
| `Mathlib.Analysis.lambda₁_sub_lambda₂` | `Causalean.Mathlib.Analysis.lambda₁ G - Causalean.Mathlib.Analysis.lambda₂ G = √(Causalean.Mathlib.Analysis.rootDiscriminant G)` | For a real two-by-two matrix, the difference between its upper and lower roots is the nonnegative square root of its discriminant. |
| `Mathlib.Analysis.lambda₂_le_lambda₁` | `Causalean.Mathlib.Analysis.lambda₂ G ≤ Causalean.Mathlib.Analysis.lambda₁ G` | For a real two-by-two matrix, its lower explicit root does not exceed its upper explicit root. |
| `Mathlib.Analysis.mem_strictGapSet_iff_discriminant_pos` | `G ∈ Causalean.Mathlib.Analysis.strictGapSet ↔ 0 < Causalean.Mathlib.Analysis.rootDiscriminant G` | For a real two-by-two matrix, having distinct ordered roots is equivalent to strict positivity of its discriminant. |
| `Mathlib.Analysis.lambda₁_add_lambda₂` | `Causalean.Mathlib.Analysis.lambda₁ G + Causalean.Mathlib.Analysis.lambda₂ G = G 0 0 + G 1 1` | For a real two-by-two matrix, the two explicit roots add to its trace. |
| `Mathlib.Analysis.lambda₁_mul_lambda₂` | `G.IsHermitian → Causalean.Mathlib.Analysis.lambda₁ G * Causalean.Mathlib.Analysis.lambda₂ G = G.det` | Given a real two-by-two matrix that is symmetric, the product of its two explicit roots equals its determinant. |
| `Mathlib.Analysis.det_sub_lambda_mul_one_eq_zero` | `G.IsHermitian → ∀ (i : Fin 2), (G - (if i = 0 then Causalean.Mathlib.Analysis.lambda₁ G else Causalean.Mathlib.Analysis.lambda₂ G) • 1).det = 0` | Given a real symmetric two-by-two matrix and one of the two root labels, subtracting that labelled explicit root from the diagonal has zero determinant. |
| `Mathlib.Analysis.eigenvalue_eq_lambda₁_or_lambda₂` | `G.IsHermitian → ∀ {μ : ℝ} {v : Fin 2 → ℝ}, v ≠ 0 → G.mulVec v = μ • v → μ = Causalean.Mathlib.Analysis.lambda₁ G ∨ μ = Causalean.Mathlib.Analysis.lambda₂ G` | Given a real symmetric two-by-two matrix, a real number, a nonzero two-vector, and an eigenvector relation for that number, the number is one of the two explicit ordered roots. |
| `Mathlib.Analysis.contDiffAt_lambda₁` | `G ∈ Causalean.Mathlib.Analysis.strictGapSet → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.lambda₁ G` | Given a real two-by-two matrix with a strict gap between its explicit roots, the upper root varies continuously differentiably near that matrix. |
| `Mathlib.Analysis.contDiffAt_lambda₂` | `G ∈ Causalean.Mathlib.Analysis.strictGapSet → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.lambda₂ G` | Given a real two-by-two matrix with a strict gap between its explicit roots, the lower root varies continuously differentiably near that matrix. |
| `Mathlib.Analysis.isOpen_strictGapSet` | `IsOpen Causalean.Mathlib.Analysis.strictGapSet` | The region of real two-by-two matrices with distinct explicit roots is open. |
| `Mathlib.Analysis.contDiffOn_lambda₁` | `ContDiffOn ℝ 1 Causalean.Mathlib.Analysis.lambda₁ Causalean.Mathlib.Analysis.strictGapSet` | The upper explicit root is continuously differentiable throughout the strict-gap region. |
| `Mathlib.Analysis.contDiffOn_lambda₂` | `ContDiffOn ℝ 1 Causalean.Mathlib.Analysis.lambda₂ Causalean.Mathlib.Analysis.strictGapSet` | The lower explicit root is continuously differentiable throughout the strict-gap region. |
<!-- /GEN -->

## 10a'''''''''. `Mathlib/Analysis/TwoByTwoSpectralProjector.lean` — choice-free upper spectral projector

The algebraic upper projector is expressed directly in the matrix and its two roots, avoiding an arbitrary and potentially discontinuous eigenvector sign choice.

<!-- GEN:Causalean.Mathlib.Analysis.TwoByTwoSpectralProjector -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.outerProjector` | `(Fin 2 → ℝ) → Matrix (Fin 2) (Fin 2) ℝ` | Given a real two-vector, its rank-one outer-product matrix maps a vector to its component in the direction of that two-vector, before normalization. |
| `Mathlib.Analysis.topProjector` | `Matrix (Fin 2) (Fin 2) ℝ → Matrix (Fin 2) (Fin 2) ℝ` | Given a real two-by-two matrix, the algebraic upper spectral-projector candidate is its lower-root-shifted matrix divided by the gap between its explicit roots. |
| `Mathlib.Analysis.IsOrthogonalProjectorOntoTop` | `Matrix (Fin 2) (Fin 2) ℝ → Matrix (Fin 2) (Fin 2) ℝ → Prop` | Given a real two-by-two matrix and a candidate matrix, the property of being the orthogonal projector onto the upper-root eigenspace means that the candidate is symmetric, idempotent, and fixes exactly the upper-root eigenvectors. |
| `Mathlib.Analysis.topProjector_mulVec_eq_iff` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → ∀ (x : Fin 2 → ℝ), (Causalean.Mathlib.Analysis.topProjector G).mulVec x = x ↔ G.mulVec x = Causalean.Mathlib.Analysis.lambda₁ G • x` | Given a real symmetric two-by-two matrix with distinct explicit roots and a two-vector, the algebraic projector fixes that vector exactly when it is an upper-root eigenvector. |
| `Mathlib.Analysis.topProjector_mulVec_eq_zero_iff` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → ∀ (x : Fin 2 → ℝ), (Causalean.Mathlib.Analysis.topProjector G).mulVec x = 0 ↔ G.mulVec x = Causalean.Mathlib.Analysis.lambda₂ G • x` | Given a real symmetric two-by-two matrix with distinct explicit roots and a two-vector, the algebraic projector sends that vector to zero exactly when it is a lower-root eigenvector. |
| `Mathlib.Analysis.topProjector_isHermitian` | `G.IsHermitian → (Causalean.Mathlib.Analysis.topProjector G).IsHermitian` | Given a real two-by-two matrix that is symmetric, its algebraic upper projector is symmetric. |
| `Mathlib.Analysis.topProjector_mul_self` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → Causalean.Mathlib.Analysis.topProjector G * Causalean.Mathlib.Analysis.topProjector G = Causalean.Mathlib.Analysis.topProjector G` | Given a real two-by-two matrix that is symmetric and has distinct explicit roots, its algebraic upper projector is idempotent. |
| `Mathlib.Analysis.topProjector_isOrthogonalProjectorOntoTop` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → Causalean.Mathlib.Analysis.IsOrthogonalProjectorOntoTop G (Causalean.Mathlib.Analysis.topProjector G)` | Given a real two-by-two matrix that is symmetric and has distinct explicit roots, its algebraic quotient is the orthogonal projector onto the upper-root eigenspace. |
| `Mathlib.Analysis.topProjector_eq_outerProjector` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → ∀ {v : Fin 2 → ℝ}, v ⬝ᵥ v = 1 → G.mulVec v = Causalean.Mathlib.Analysis.lambda₁ G • v → Causalean.Mathlib.Analysis.topProjector G = Causalean.Mathlib.Analysis.outerProjector v` | Given a real symmetric two-by-two matrix with distinct explicit roots and a unit upper-root eigenvector, the algebraic upper projector equals that vector's outer-product projector. |
| `Mathlib.Analysis.outerProjector_eq_of_topEigenvectors` | `G.IsHermitian → G ∈ Causalean.Mathlib.Analysis.strictGapSet → ∀ {v w : Fin 2 → ℝ}, v ⬝ᵥ v = 1 → G.mulVec v = Causalean.Mathlib.Analysis.lambda₁ G • v → w ⬝ᵥ w = 1 → G.mulVec w = Causalean.Mathlib.Analysis.lambda₁ G • w → Causalean.Mathlib.Analysis.outerProjector v = Causalean.Mathlib.Analysis.outerProjector w` | Given a real symmetric two-by-two matrix with distinct explicit roots and two unit upper-root eigenvectors, their outer-product projectors coincide. |
| `Mathlib.Analysis.outerProjector_neg` | `Causalean.Mathlib.Analysis.outerProjector (-v) = Causalean.Mathlib.Analysis.outerProjector v` | Given a real two-vector, negating it leaves its outer-product projector unchanged. |
| `Mathlib.Analysis.contDiffAt_topProjector` | `G ∈ Causalean.Mathlib.Analysis.strictGapSet → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.topProjector G` | Given a real two-by-two matrix with distinct explicit roots, its algebraic upper projector varies continuously differentiably near that matrix. |
| `Mathlib.Analysis.contDiffOn_topProjector` | `ContDiffOn ℝ 1 Causalean.Mathlib.Analysis.topProjector Causalean.Mathlib.Analysis.strictGapSet` | The algebraic upper projector is continuously differentiable throughout the strict-gap region. |
<!-- /GEN -->

## 10a''''''''''. `Mathlib/Analysis/LocallyBoundedDerivative.lean` — local derivative bounds

Continuously differentiable maps between real normed spaces have a Fréchet derivative bounded on a neighborhood of each point.

<!-- GEN:Causalean.Mathlib.Analysis.LocallyBoundedDerivative -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.HasLocallyBoundedFDerivAt` | `(E → F) → E → Prop` | Given a function between real normed vector spaces and a point in its domain, the property of having a locally bounded Fréchet derivative means that one neighborhood of the point has a uniform nonnegative bound on derivative norms. |
| `Mathlib.Analysis.ContDiffAt.hasLocallyBoundedFDerivAt` | `ContDiffAt ℝ 1 f x → Causalean.Mathlib.Analysis.HasLocallyBoundedFDerivAt f x` | Given a function between real normed vector spaces, a point in its domain, and continuous differentiability at that point, the function has a locally bounded Fréchet derivative there. |
<!-- /GEN -->

## 10a'''''''''''. `Mathlib/Analysis/RankOneGramPseudoinverse.lean` — Gram-based rank-one pseudoinverse

For real two-row matrices, rank-one truncation and its pseudoinverse are identified with choice-free left-Gram formulas and shown locally continuously differentiable.

<!-- GEN:Causalean.Mathlib.Analysis.RankOneGramPseudoinverse -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.leftGram` | `Matrix (Fin 2) E ℝ → Matrix (Fin 2) (Fin 2) ℝ` | Given a real matrix with two rows, its left Gram matrix records all pairwise inner products of those rows. |
| `Mathlib.Analysis.leftGram_isHermitian` | `(Causalean.Mathlib.Analysis.leftGram H).IsHermitian` | For a real matrix with two rows, its left Gram matrix is symmetric. |
| `Mathlib.Analysis.eigenvectorRankOneTruncation` | `(Fin 2 → ℝ) → Matrix (Fin 2) E ℝ → Matrix (Fin 2) E ℝ` | Given a real two-vector and a real matrix with two rows, the eigenvector-selected rank-one truncation projects the rows of the matrix onto that vector before reconstruction. |
| `Mathlib.Analysis.eigenvectorRankOnePseudoInverse` | `(Fin 2 → ℝ) → Matrix (Fin 2) E ℝ → Matrix E (Fin 2) ℝ` | Given a real two-vector and a real matrix with two rows, the eigenvector-selected rank-one pseudoinverse formula transposes the projected matrix and divides by its upper Gram root. |
| `Mathlib.Analysis.algebraicRankOneTruncation` | `Matrix (Fin 2) E ℝ → Matrix (Fin 2) E ℝ` | Given a real matrix with two rows, the choice-free algebraic rank-one truncation applies the algebraic upper projector of its left Gram matrix. |
| `Mathlib.Analysis.algebraicRankOnePseudoInverse` | `Matrix (Fin 2) E ℝ → Matrix E (Fin 2) ℝ` | Given a real matrix with two rows, the choice-free algebraic rank-one pseudoinverse transposes the algebraically projected matrix and divides by the upper Gram root. |
| `Mathlib.Analysis.eigenvectorRankOneTruncation_eq_algebraic` | `Causalean.Mathlib.Analysis.leftGram H ∈ Causalean.Mathlib.Analysis.strictGapSet → ∀ {u : Fin 2 → ℝ}, u ⬝ᵥ u = 1 → (Causalean.Mathlib.Analysis.leftGram H).mulVec u = Causalean.Mathlib.Analysis.lambda₁ (Causalean.Mathlib.Analysis.leftGram H) • u → Causalean.Mathlib.Analysis.eigenvectorRankOneTruncation u H = Causalean.Mathlib.Analysis.algebraicRankOneTruncation H` | Given a real two-row matrix with a strict left-Gram root gap and a unit upper left-Gram eigenvector, the eigenvector-selected and algebraic rank-one truncations agree. |
| `Mathlib.Analysis.eigenvectorRankOnePseudoInverse_eq_algebraic` | `Causalean.Mathlib.Analysis.leftGram H ∈ Causalean.Mathlib.Analysis.strictGapSet → 0 < Causalean.Mathlib.Analysis.lambda₁ (Causalean.Mathlib.Analysis.leftGram H) → ∀ {u : Fin 2 → ℝ}, u ⬝ᵥ u = 1 → (Causalean.Mathlib.Analysis.leftGram H).mulVec u = Causalean.Mathlib.Analysis.lambda₁ (Causalean.Mathlib.Analysis.leftGram H) • u → Causalean.Mathlib.Analysis.eigenvectorRankOnePseudoInverse u H = Causalean.Mathlib.Analysis.algebraicRankOnePseudoInverse H` | Given a real two-row matrix with a strict positive upper left-Gram root and a unit upper left-Gram eigenvector, the eigenvector-selected and algebraic rank-one pseudoinverse formulas agree. |
| `Mathlib.Analysis.algebraicRankOnePseudoInverse_isMoorePenrose` | `Causalean.Mathlib.Analysis.leftGram H ∈ Causalean.Mathlib.Analysis.strictGapSet → 0 < Causalean.Mathlib.Analysis.lambda₁ (Causalean.Mathlib.Analysis.leftGram H) → have A := Causalean.Mathlib.Analysis.algebraicRankOneTruncation H; have Aplus := Causalean.Mathlib.Analysis.algebraicRankOnePseudoInverse H; A * Aplus * A = A ∧ Aplus * A * Aplus = Aplus ∧ (A * Aplus).IsHermitian ∧ (Aplus * A).IsHermitian` | Given a real two-row matrix with a strict positive upper left-Gram root, the algebraic rank-one pseudoinverse satisfies all four Moore--Penrose equations for the algebraic rank-one truncation. |
| `Mathlib.Analysis.contDiffAt_algebraicRankOneTruncation` | `Causalean.Mathlib.Analysis.leftGram H ∈ Causalean.Mathlib.Analysis.strictGapSet → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.algebraicRankOneTruncation H` | Given a real two-row matrix whose left Gram matrix has distinct roots, the algebraic rank-one truncation varies continuously differentiably near that matrix. |
| `Mathlib.Analysis.contDiffAt_algebraicRankOnePseudoInverse` | `Causalean.Mathlib.Analysis.leftGram H ∈ Causalean.Mathlib.Analysis.strictGapSet → 0 < Causalean.Mathlib.Analysis.lambda₁ (Causalean.Mathlib.Analysis.leftGram H) → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.algebraicRankOnePseudoInverse H` | Given a real two-row matrix whose left Gram matrix has a strict positive upper root, the algebraic rank-one pseudoinverse varies continuously differentiably near that matrix. |
<!-- /GEN -->

## 10a''''''''''''. `Mathlib/Analysis/RankOneWaldSmoothness.lean` — smooth rank-one Wald functional

The induced bilinear Wald functional inherits local continuous differentiability and locally bounded derivative from the choice-free Gram pseudoinverse.

<!-- GEN:Causalean.Mathlib.Analysis.RankOneWaldSmoothness -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.WaldInput` | `Type u_2 → Type u_2` | Given a finite outcome-coordinate type, an input to the rank-one Wald functional consists of a real two-row matrix, an outcome direction, and a two-dimensional target direction. |
| `Mathlib.Analysis.waldRegularSet` | `Set (Causalean.Mathlib.Analysis.WaldInput E)` | The regularity region contains rank-one Wald inputs whose left Gram matrix has distinct roots and a strictly positive upper root. |
| `Mathlib.Analysis.algebraicWaldFunctional` | `ℝ` | Given a rank-one Wald input, the choice-free algebraic Wald functional applies the algebraic rank-one pseudoinverse to the target direction and pairs the result with the outcome direction. |
| `Mathlib.Analysis.isOpen_waldRegularSet` | `IsOpen Causalean.Mathlib.Analysis.waldRegularSet` | The regularity region of the algebraic rank-one Wald functional is open. |
| `Mathlib.Analysis.contDiffAt_algebraicWaldFunctional` | `x ∈ Causalean.Mathlib.Analysis.waldRegularSet → ContDiffAt ℝ 1 Causalean.Mathlib.Analysis.algebraicWaldFunctional x` | Given a rank-one Wald input in the regularity region, the algebraic Wald functional varies continuously differentiably near that input. |
| `Mathlib.Analysis.contDiffOn_algebraicWaldFunctional` | `ContDiffOn ℝ 1 Causalean.Mathlib.Analysis.algebraicWaldFunctional Causalean.Mathlib.Analysis.waldRegularSet` | The algebraic Wald functional is continuously differentiable throughout its regularity region. |
| `Mathlib.Analysis.algebraicWaldFunctional_hasLocallyBoundedFDerivAt` | `x ∈ Causalean.Mathlib.Analysis.waldRegularSet → Causalean.Mathlib.Analysis.HasLocallyBoundedFDerivAt Causalean.Mathlib.Analysis.algebraicWaldFunctional x` | Given a rank-one Wald input in the regularity region, the algebraic Wald functional has a bounded Fréchet derivative on some neighborhood of that input. |
<!-- /GEN -->

## 10a''. `Mathlib/Analysis/ArgumentPrincipleCircle/` — argument principle and Rouché comparison on circles

Reusable complex-analysis infrastructure for certifying a zero count from a
logarithmic-derivative contour integral. The core module establishes the
argument principle for functions analytic near a closed disk; the companion
module gives straight-line homotopy invariance and the standard Rouché
comparison. These facts are deliberately stated only for positively oriented
circles and analytic functions, which is the compact interface needed by
contour-count applications.

### Definitions and local residue

<!-- GEN:Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral` | `(ℂ → ℂ) → ℂ → ℝ → ℂ` | For a complex-valued function, a complex center, and a real radius, the normalized logarithmic-derivative circle integral is the integral of the function's logarithmic derivative around the positively oriented circle with that center and radius, divided by $2\pi i$. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount` | `(ℂ → ℂ) → ℂ → ℝ → ℕ` | For a complex-valued function, a complex center, and a real radius, the zero-multiplicity count is the sum of the analytic multiplicities of all zeros of the function lying strictly inside the open disk with that center and radius, with the standard totalized finite sum used when the support is not finite. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.interiorZeros` | `(ℂ → ℂ) → ℂ → ℝ → Set ℂ` | For a complex-valued function, a complex center, and a real radius, the interior-zero set consists exactly of the complex numbers that are zeros of the function and lie strictly inside the open disk with that center and radius. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.eventuallyEq_logDeriv_add_order_div_sub` | `AnalyticAt ℂ f a → analyticOrderAt f a ≠ ⊤ → ∃ g, AnalyticAt ℂ g a ∧ g a ≠ 0 ∧ ∀ᶠ (z : ℂ) in nhdsWithin a {a}ᶜ, logDeriv f z = ↑(analyticOrderNatAt f a) / (z - a) + logDeriv g z` | Near a finite-order zero of an analytic complex function, its logarithmic derivative is the zero multiplicity divided by displacement from the zero plus the logarithmic derivative of an analytic factor that does not vanish there. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral_centeredMonomial` | `0 < R → a ∈ Metric.ball c R → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral (fun z => (z - a) ^ n) c R = ↑n` | For a positive radius `R` such that the point `a` lies strictly inside the open disk of radius `R` centered at `c`, the normalized logarithmic-derivative integral around that circle of the monomial `z ↦ (z - a)^n` equals `n`. |
<!-- /GEN -->

### Circle argument principle

<!-- GEN:Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.ArgumentPrinciple -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.ArgumentPrincipleCircle.finite_interiorZeros` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → (Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.interiorZeros f c R).Finite` | A complex function analytic on a neighborhood of a closed disk and nonzero on its boundary has only finitely many zeros strictly inside that disk. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.finiteSupport_orderWithinBall` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → Function.HasFiniteSupport fun z => if z ∈ Metric.ball c R then analyticOrderNatAt f z else 0` | Under disk analyticity and boundary nonvanishing, the function assigning each interior zero its analytic multiplicity and all other points zero has finite support. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.argumentPrinciple_circle` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral f c R = ↑(Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount f c R)` | Argument principle for a circle. For a positive radius `R`, if the function `f` is complex-analytic on a neighborhood of the closed disk of radius `R` centered at `c` and `f` is nonzero on the boundary circle, then the normalized logarithmic-derivative integral of `f` around that circle equals the number of zeros of `f` strictly inside the disk, counted with analytic multiplicity. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral_exists_nat` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → ∃ n, Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral f c R = ↑n` | The normalized logarithmic-derivative integral of a boundary-zero-free analytic function is a nonnegative whole number, viewed as a complex number. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount_pos_of_exists_zero` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → (∃ z ∈ Metric.ball c R, f z = 0) → 0 < Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount f c R` | A boundary-zero-free analytic function that vanishes somewhere strictly inside the disk has a strictly positive multiplicity-weighted interior zero count. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral_re_pos_of_exists_zero` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, f z ≠ 0) → (∃ z ∈ Metric.ball c R, f z = 0) → 0 < (Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral f c R).re` | A boundary-zero-free analytic function with an interior zero has normalized logarithmic-derivative integral with strictly positive real part. |
<!-- /GEN -->

### Homotopy invariance and Rouché's theorem

<!-- GEN:Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.Homotopy -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.ArgumentPrincipleCircle.straightLineHomotopy` | `(ℂ → ℂ) → (ℂ → ℂ) → ℝ → ℂ → ℂ` | For two complex-valued functions, a real interpolation time, and a complex argument, the straight-line homotopy is the value at that argument of the affine combination giving weight $1-t$ to the first function and weight $t$ to the second. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral_eq_of_straightLineHomotopy` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → AnalyticOnNhd ℂ g (Metric.closedBall c R) → (∀ t ∈ Set.Icc 0 1, ∀ z ∈ Metric.sphere c R, Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.straightLineHomotopy f g t z ≠ 0) → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral f c R = Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral g c R` | Two analytic complex functions have equal normalized logarithmic-derivative circle integrals when every function on their straight-line interpolation is nonzero on the boundary circle. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount_eq_of_straightLineHomotopy` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → AnalyticOnNhd ℂ g (Metric.closedBall c R) → (∀ t ∈ Set.Icc 0 1, ∀ z ∈ Metric.sphere c R, Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.straightLineHomotopy f g t z ≠ 0) → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount f c R = Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount g c R` | Two analytic complex functions have the same multiplicity-weighted number of interior zeros when every function on their straight-line interpolation is nonzero on the boundary circle. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.rouche_circle` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → AnalyticOnNhd ℂ g (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, ‖g z - f z‖ < ‖f z‖) → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount f c R = Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.zeroMultiplicityCount g c R` | Rouché's theorem for a circle. For a positive radius `R`, if both `f` and `g` are complex-analytic on a neighborhood of the closed disk of radius `R` centered at `c` and on the boundary circle the discrepancy `‖g z - f z‖` is everywhere strictly smaller than `‖f z‖`, then `f` and `g` have the same multiplicity-weighted number of zeros strictly inside the disk. |
| `Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral_eq_of_rouche` | `0 < R → AnalyticOnNhd ℂ f (Metric.closedBall c R) → AnalyticOnNhd ℂ g (Metric.closedBall c R) → (∀ z ∈ Metric.sphere c R, ‖g z - f z‖ < ‖f z‖) → Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral f c R = Causalean.Mathlib.Analysis.ArgumentPrincipleCircle.normalizedLogDerivCircleIntegral g c R` | Under Rouché's strict boundary inequality, two analytic complex functions have equal normalized logarithmic-derivative circle integrals. |
<!-- /GEN -->

## 10a''a. `Mathlib/Analysis/WeightedCircularTube/` — weighted geometry near the unit circle

Reusable planar geometry for radial power weights near the Euclidean unit circle. The modules define the inward and outward circular tube, prove uniform one-sided mass scaling and annular normalization, and construct reciprocal-scale separated circle nets.

### Basic geometry and weights

<!-- GEN:Causalean.Mathlib.Analysis.WeightedCircularTube.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.WeightedCircularTube.Plane` | `Type` | The Euclidean plane is represented by the complex numbers, equipped with their standard two-dimensional Euclidean metric and Lebesgue measure. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle` | `Set Causalean.Mathlib.Analysis.WeightedCircularTube.Plane` | The unit circle is the set of planar points whose Euclidean distance from the origin is one. |
| `Mathlib.Analysis.WeightedCircularTube.radialOffset` | `ℝ` | For a planar point, its signed radial offset is its Euclidean distance from the origin minus one; it is negative inside the unit circle and positive outside it. |
| `Mathlib.Analysis.WeightedCircularTube.CircleSide` | `Type` | The type of radial sides adjacent to the unit circle has an inside alternative, representing its interior, and an outside alternative, representing its strict exterior. |
| `Mathlib.Analysis.WeightedCircularTube.instDecidableEqCircleSide` | `DecidableEq Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide` | — |
| `Mathlib.Analysis.WeightedCircularTube.CircleSide.region` | `Set Causalean.Mathlib.Analysis.WeightedCircularTube.Plane` | The region associated with a radial side is the closed interior of the unit circle for the inside side, and the strict exterior of the unit circle for the outside side. |
| `Mathlib.Analysis.WeightedCircularTube.powerWeight` | `ℝ → Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → ℝ` | For a real exponent parameter and a planar point, the radial power weight is the absolute radial offset raised to the power $κ-2$. |
| `Mathlib.Analysis.WeightedCircularTube.sideBall` | `Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → ℝ → Set Causalean.Mathlib.Analysis.WeightedCircularTube.Plane` | For a chosen radial side, a planar center, and a real radius, the one-sided ball is the open Euclidean ball with that center and radius, intersected with the region on the chosen side of the unit circle. |
| `Mathlib.Analysis.WeightedCircularTube.isCompact_unitCircle` | `IsCompact Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle` | The unit circle is compact in the Euclidean plane. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_nonempty` | `Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle.Nonempty` | The unit circle contains at least one planar point. |
| `Mathlib.Analysis.WeightedCircularTube.CircleSide.measurableSet_region` | `MeasurableSet side.region` | For a chosen radial side, its radial region is Lebesgue measurable. |
| `Mathlib.Analysis.WeightedCircularTube.measurableSet_sideBall` | `MeasurableSet (Causalean.Mathlib.Analysis.WeightedCircularTube.sideBall side x h)` | For a chosen side, a planar center, and a radius, the corresponding one-sided ball is Lebesgue measurable. |
| `Mathlib.Analysis.WeightedCircularTube.continuous_powerWeight` | `2 < κ → Continuous (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ)` | When the exponent κ exceeds two, the radial power weight is continuous on the entire Euclidean plane, including the unit circle. |
| `Mathlib.Analysis.WeightedCircularTube.measurable_powerWeight` | `2 < κ → Measurable (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ)` | When the exponent κ exceeds two, the radial power weight is Lebesgue measurable. |
| `Mathlib.Analysis.WeightedCircularTube.powerWeight_nonneg` | `0 ≤ Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ z` | For an exponent parameter and a planar point, the radial power weight is nonnegative. |
| `Mathlib.Analysis.WeightedCircularTube.powerWeight_eq_zero_iff` | `2 < κ → ∀ (z : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane), Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ z = 0 ↔ z ∈ Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle` | When the exponent κ exceeds two, the radial power weight at a planar point vanishes exactly when that point is on the unit circle. |
| `Mathlib.Analysis.WeightedCircularTube.integrableOn_powerWeight_sideBall` | `2 < κ → ∀ (side : Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide) (x : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane) (h : ℝ), MeasureTheory.IntegrableOn (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ) (Causalean.Mathlib.Analysis.WeightedCircularTube.sideBall side x h) MeasureTheory.volume` | When the exponent κ exceeds two, the radial power weight is integrable over the one-sided ball determined by the chosen side, center, and radius. |
| `Mathlib.Analysis.WeightedCircularTube.radialOffset_mul_of_mem_unitCircle` | `u ∈ Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle → ∀ (z : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane), Causalean.Mathlib.Analysis.WeightedCircularTube.radialOffset (u * z) = Causalean.Mathlib.Analysis.WeightedCircularTube.radialOffset z` | If a complex multiplier lies on the unit circle, then multiplying any planar point by it preserves signed radial offset. |
| `Mathlib.Analysis.WeightedCircularTube.dist_mul_one_of_mem_unitCircle` | `u ∈ Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle → ∀ (z : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane), dist (u * z) u = dist z 1` | If a complex multiplier lies on the unit circle, then multiplication of a planar point by it preserves its distance from the reference point one. |
<!-- /GEN -->

### Separated nets

<!-- GEN:Causalean.Mathlib.Analysis.WeightedCircularTube.Packing -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.WeightedCircularTube.IsSeparatedAtLeast` | `ℝ → Set Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → Prop` | For a real separation scale and a set of planar points, the property of being separated at least at that scale means that every two distinct points in the set are at distance at least the separation scale. |
| `Mathlib.Analysis.WeightedCircularTube.IsMaximalSeparated` | `Finset Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → ℝ → Prop` | For a finite set of planar sites and a real separation scale, the property of being a maximal separated set on the unit circle means that the sites lie on the unit circle, are separated by at least that scale, and are maximal among all sets with both properties. |
| `Mathlib.Analysis.WeightedCircularTube.CoversUnitCircle` | `Finset Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → ℝ → Prop` | For a finite set of planar sites and a real radius, the property of covering the unit circle means that every point of the unit circle belongs to an open ball of radius $r$ centred at one of the sites. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_maximalSeparated_covers` | `0 < r → Causalean.Mathlib.Analysis.WeightedCircularTube.IsMaximalSeparated sites r → Causalean.Mathlib.Analysis.WeightedCircularTube.CoversUnitCircle sites r` | When the separation radius is positive and a finite site set is inclusion-maximal at that non-strict separation radius, its open balls of that radius cover the unit circle. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_exists_finite_maximalSeparated` | `0 < r → ∃ sites, Causalean.Mathlib.Analysis.WeightedCircularTube.IsMaximalSeparated sites r` | At every positive separation radius, there exists a finite inclusion-maximal separated subset of the unit circle. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_maximalSeparated_card_bounds` | `∃ cpack Cpack hstar, 0 < cpack ∧ cpack ≤ Cpack ∧ 0 < hstar ∧ ∀ (h : ℝ) (sites : Finset Causalean.Mathlib.Analysis.WeightedCircularTube.Plane), 0 < h → h ≤ hstar → Causalean.Mathlib.Analysis.WeightedCircularTube.IsMaximalSeparated sites (3 * h) → Causalean.Mathlib.Analysis.WeightedCircularTube.CoversUnitCircle sites (3 * h) ∧ cpack / h ≤ ↑sites.card ∧ ↑sites.card ≤ Cpack / h` | There are positive universal constants with an ordered small-scale range such that every finite site set maximal at separation three times a positive scale in that range has open three-scale balls covering the unit circle and a number of sites comparable to the reciprocal scale. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_exists_maximalSeparated_with_card_bounds` | `∃ cpack Cpack hstar, 0 < cpack ∧ cpack ≤ Cpack ∧ 0 < hstar ∧ ∀ (h : ℝ), 0 < h → h ≤ hstar → ∃ sites, Causalean.Mathlib.Analysis.WeightedCircularTube.IsMaximalSeparated sites (3 * h) ∧ Causalean.Mathlib.Analysis.WeightedCircularTube.CoversUnitCircle sites (3 * h) ∧ cpack / h ≤ ↑sites.card ∧ ↑sites.card ≤ Cpack / h` | There are positive universal constants with an ordered small-scale range such that at every positive scale in that range one can choose a finite site set maximal at separation three times that scale, whose open three-scale balls cover the unit circle and whose size is comparable to the reciprocal scale. |
<!-- /GEN -->

### One-sided weighted mass

<!-- GEN:Causalean.Mathlib.Analysis.WeightedCircularTube.SideMass -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.WeightedCircularTube.sideBallMass` | `ℝ → Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide → Causalean.Mathlib.Analysis.WeightedCircularTube.Plane → ℝ → ℝ` | For a real exponent, a choice of the inside or outside of the unit circle, a planar centre, and a real radius, the weighted side-ball mass is the two-dimensional Lebesgue integral of the radial power weight with that exponent over the corresponding one-sided open ball. |
| `Mathlib.Analysis.WeightedCircularTube.sideBallMass_integrable` | `2 < κ → ∀ (side : Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide) (x : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane) (h : ℝ), MeasureTheory.IntegrableOn (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ) (Causalean.Mathlib.Analysis.WeightedCircularTube.sideBall side x h) MeasureTheory.volume` | When the exponent κ exceeds two, the radial power weight is integrable over the one-sided ball determined by the chosen side, center, and radius and hence its weighted mass is finite. |
| `Mathlib.Analysis.WeightedCircularTube.sideBallMass_nonneg` | `2 < κ → ∀ (side : Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide) (x : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane) (h : ℝ), 0 ≤ Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side x h` | When the exponent κ exceeds two, the weighted mass of the one-sided ball determined by the chosen side, center, and radius is nonnegative. |
| `Mathlib.Analysis.WeightedCircularTube.sideBallMass_eq_reference` | `x ∈ Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle → Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side x h = Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side 1 h` | For a chosen radial side and a center on the unit circle, rotating that center to the reference point one leaves its weighted side-ball mass unchanged. |
| `Mathlib.Analysis.WeightedCircularTube.reference_powerWeighted_sideBall_bounds` | `2 < κMax → 0 < h0 → h0 < 1 → ∃ c C, 0 < c ∧ c ≤ C ∧ ∀ (κ : ℝ) (side : Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide) (h : ℝ), 2 < κ → κ ≤ κMax → 0 < h → h ≤ h0 → c * h ^ κ ≤ Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side 1 h ∧ Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side 1 h ≤ C * h ^ κ` | When the allowed exponent ceiling exceeds two and the maximum scale is positive and below one, there are positive constants, uniform over both radial sides, all exponents between two and that ceiling, and all positive scales up to that maximum, which bound the reference one-sided weighted mass above and below by constant multiples of the scale to the exponent. |
| `Mathlib.Analysis.WeightedCircularTube.unitCircle_powerWeighted_sideBall_bounds` | `2 < κMax → 0 < h0 → h0 < 1 → ∃ c C, 0 < c ∧ c ≤ C ∧ ∀ (κ : ℝ) (x : Causalean.Mathlib.Analysis.WeightedCircularTube.Plane) (side : Causalean.Mathlib.Analysis.WeightedCircularTube.CircleSide) (h : ℝ), 2 < κ → κ ≤ κMax → x ∈ Causalean.Mathlib.Analysis.WeightedCircularTube.unitCircle → 0 < h → h ≤ h0 → c * h ^ κ ≤ Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side x h ∧ Causalean.Mathlib.Analysis.WeightedCircularTube.sideBallMass κ side x h ≤ C * h ^ κ` | When the allowed exponent ceiling exceeds two and the maximum scale is positive and below one, there are positive constants, uniform over every unit-circle center, either radial side, all exponents between two and that ceiling, and all positive scales up to that maximum, which bound one-sided weighted mass above and below by constant multiples of the scale to the exponent. |
| `Mathlib.Analysis.WeightedCircularTube.annularTube` | `ℝ → Set Causalean.Mathlib.Analysis.WeightedCircularTube.Plane` | For a real tube width, the annular tube is the set of planar points whose absolute radial distance from the unit circle is strictly less than $\delta$. |
| `Mathlib.Analysis.WeightedCircularTube.measurableSet_annularTube` | `MeasurableSet (Causalean.Mathlib.Analysis.WeightedCircularTube.annularTube δ)` | For a tube width, the corresponding annular tube is Lebesgue measurable. |
| `Mathlib.Analysis.WeightedCircularTube.integrableOn_powerWeight_annularTube` | `2 < κ → δ < 1 → MeasureTheory.IntegrableOn (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ) (Causalean.Mathlib.Analysis.WeightedCircularTube.annularTube δ) MeasureTheory.volume` | When the exponent κ exceeds two and the tube width is below one, the radial power weight is integrable over that annular tube. |
| `Mathlib.Analysis.WeightedCircularTube.annularNormalizer` | `ℝ → ℝ → ℝ` | For a real exponent and a real tube width, the weighted annular normalizer is the two-dimensional Lebesgue integral of the radial power weight with that exponent over the annular tube of that width. |
| `Mathlib.Analysis.WeightedCircularTube.annularNormalizer_finite_pos` | `2 < κ → 0 < δ → δ < 1 → MeasureTheory.IntegrableOn (Causalean.Mathlib.Analysis.WeightedCircularTube.powerWeight κ) (Causalean.Mathlib.Analysis.WeightedCircularTube.annularTube δ) MeasureTheory.volume ∧ 0 < Causalean.Mathlib.Analysis.WeightedCircularTube.annularNormalizer κ δ` | When the exponent κ exceeds two and the tube width is positive and below one, the radial power weight is integrable on that annular tube and its weighted normalizer is strictly positive. |
<!-- /GEN -->

## 10a'''. `Mathlib/Analysis/CertifiedContourIntervalArithmetic/` — certified contour interval arithmetic

Certified numerical-analysis infrastructure for turning rational input enclosures
into total, verifiable computations. It combines exact interval arithmetic,
effective real-number refinement, elementary transcendental bounds, finite
Lipschitz mesh enclosures, and measurable deterministic finite selection.

### Exact rational intervals

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | `Type` | A rational closed interval consists of a rational lower endpoint and a rational upper endpoint, with the lower endpoint no greater than the upper endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.ext` | `x.lo = y.lo → x.hi = y.hi → x = y` | — |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.Contains` | `ℝ → Prop` | For a rational interval and a real number, the containment relation holds precisely when the real number is no smaller than the interval's lower endpoint and no larger than its upper endpoint, after the rational endpoints are viewed as real numbers. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width` | `ℚ` | For a rational interval, its width is the upper rational endpoint minus the lower rational endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs` | `ℚ` | For a rational interval, its maximum endpoint magnitude is the larger of the absolute values of its two rational endpoints. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.Subinterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Prop` | For two rational intervals, the subinterval relation holds precisely when the second interval's lower endpoint is no greater than the first interval's lower endpoint and the first interval's upper endpoint is no greater than the second interval's upper endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.point` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational number, the point interval at that number is the rational interval whose lower endpoint is that number, upper endpoint is that number, and endpoints are ordered because they are equal. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.hull` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For two rational intervals, their rational interval hull has the smaller lower endpoint, the larger upper endpoint, and these endpoints in weakly increasing order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.add` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For two rational intervals, their interval sum has lower endpoint equal to the sum of the lower endpoints, upper endpoint equal to the sum of the upper endpoints, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.neg` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval, its interval negation has lower endpoint equal to the negative of the original upper endpoint, upper endpoint equal to the negative of the original lower endpoint, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sub` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For two rational intervals, their interval difference is the interval sum of the first interval and the interval negation of the second. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.mul` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For two rational intervals, their interval product has lower endpoint equal to the least of the four products of one endpoint from each interval, upper endpoint equal to the greatest of those four products, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.AwayFromZero` | `Prop` | For a rational interval, the property of being separated from zero holds precisely when the interval lies strictly below zero or strictly above zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.inv` | `(I : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → I.AwayFromZero → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval that is separated from zero, its interval reciprocal has lower endpoint equal to the reciprocal of the original upper endpoint, upper endpoint equal to the reciprocal of the original lower endpoint, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.div` | `(J : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → J.AwayFromZero → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a numerator rational interval and a denominator rational interval whose denominator interval is separated from zero, their interval quotient is the interval product of the numerator interval and the interval reciprocal of the denominator interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.npow` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval, its natural-power interval sequence assigns the point interval at one to exponent zero and the interval product of the preceding power and the original interval to each positive exponent. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.tighten` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For two rational intervals, their conditional tightening is their exact intersection when its lower endpoint does not exceed its upper endpoint, and the first interval otherwise. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.expand` | `(e : ℚ) → 0 ≤ e → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval, a rational widening amount that is nonnegative, the expanded interval has lower endpoint equal to the original lower endpoint minus the widening amount, upper endpoint equal to the original upper endpoint plus the widening amount, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_nonneg` | `0 ≤ I.width` | Every rational interval has nonnegative width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.contains_point_iff` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.point q).Contains x ↔ x = ↑q` | Containment in a point interval is equality with that rational point. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.subinterval_refl` | `I.Subinterval I` | Subinterval is reflexive. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.subinterval_trans` | `I.Subinterval J → J.Subinterval K → I.Subinterval K` | Subinterval is transitive. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.Contains.mono` | `I.Subinterval J → I.Contains x → J.Contains x` | A real point contained in a subinterval is contained in the enclosing interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_mono` | `I.Subinterval J → I.width ≤ J.width` | Inclusion of rational intervals cannot increase their width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.point_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.point q).Contains ↑q` | The point interval soundly encloses its rational value viewed as a real. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.add_sound` | `I.Contains x → J.Contains y → (I.add J).Contains (x + y)` | Rational interval addition encloses the sum of any enclosed real operands. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.neg_sound` | `I.Contains x → I.neg.Contains (-x)` | Rational interval negation encloses the negation of every enclosed real operand. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sub_sound` | `I.Contains x → J.Contains y → (I.sub J).Contains (x - y)` | Rational interval subtraction encloses the difference of any enclosed real operands. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.mul_sound` | `I.Contains x → J.Contains y → (I.mul J).Contains (x * y)` | For rational intervals `I` and `J`, if `x` is a real number contained in `I` and `y` is a real number contained in `J`, then the interval-multiplication enclosure `mul I J` contains the real product `x * y`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.ne_zero_of_contains` | `I.AwayFromZero → I.Contains x → x ≠ 0` | A real number contained in an interval separated from zero is nonzero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.inv_sound` | `I.Contains x → (I.inv hI).Contains x⁻¹` | Rational interval reciprocal encloses the reciprocal of every enclosed real operand. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.div_sound` | `I.Contains x → J.Contains y → (I.div J hJ).Contains (x / y)` | Rational interval division encloses every quotient whose denominator interval avoids zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.npow_sound` | `I.Contains x → ∀ (n : ℕ), (I.npow n).Contains (x ^ n)` | Repeated interval multiplication encloses every natural power of an enclosed real number. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.tighten_sound` | `I.Contains x → J.Contains x → (I.tighten J).Contains x` | Tightening two intervals that share an enclosed real point returns their intersection and continues to enclose that point. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.tighten_subinterval_left` | `I.Contains x → J.Contains x → (I.tighten J).Subinterval I` | Tightening by another sound interval produces a subinterval of the first interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.add_mono` | `I.Subinterval I' → J.Subinterval J' → (I.add J).Subinterval (I'.add J')` | Addition is inclusion-isotone in both interval arguments. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.neg_mono` | `I.Subinterval J → I.neg.Subinterval J.neg` | Negation preserves interval inclusion. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sub_mono` | `I.Subinterval I' → J.Subinterval J' → (I.sub J).Subinterval (I'.sub J')` | Subtraction is inclusion-isotone in both interval arguments. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.mul_mono` | `I.Subinterval I' → J.Subinterval J' → (I.mul J).Subinterval (I'.mul J')` | Multiplication is inclusion-isotone in both interval arguments. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.npow_mono` | `I.Subinterval J → ∀ (n : ℕ), (I.npow n).Subinterval (J.npow n)` | Natural interval powers preserve interval inclusion. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.inv_mono` | `I.Subinterval J → ∀ (hI : I.AwayFromZero) (hJ : J.AwayFromZero), (I.inv hI).Subinterval (J.inv hJ)` | Reciprocal preserves inclusion when both intervals are certified away from zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.div_mono` | `I.Subinterval I' → J.Subinterval J' → ∀ (hJ0 : J.AwayFromZero) (hJ0' : J'.AwayFromZero), (I.div J hJ0).Subinterval (I'.div J' hJ0')` | Division is inclusion-isotone when both denominator intervals avoid zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_add` | `(I.add J).width = I.width + J.width` | Addition makes widths add exactly. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_neg` | `I.neg.width = I.width` | Negation preserves interval width exactly. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_sub` | `(I.sub J).width = I.width + J.width` | Subtraction makes widths add exactly. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.width_expand` | `(I.expand e he).width = I.width + 2 * e` | Widening an interval by a nonnegative amount on each side increases its width by twice that amount. |
<!-- /GEN -->

### Certified real names

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | `Type` | A positive rational number is a rational number that is strictly greater than zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | `Type` | A certified real name represents a real number by a sequence of rational interval enclosures indexed by precision that is nested — each successive enclosure a subinterval of the one before — and always contains the represented value, together with a computable rule selecting, for any requested positive rational error, a precision level whose enclosure is no wider than that error. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.approx_mono` | `m ≤ n → (x.approx n).Subinterval (x.approx m)` | Nestedness extends from successive precisions to every pair of ordered precisions. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.refine` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a certified real number and a requested positive rational tolerance, its refinement is the rational interval returned at the precision selected by that certificate's modulus for the requested tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.refine_contains` | `(x.refine ε).Contains x.value` | Refinement always encloses the real value named by the certificate. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.refine_width` | `(x.refine ε).width ≤ ↑ε` | For a certified real number `x` and a requested positive rational tolerance `ε`, refining `x` to the precision selected by `ε`'s modulus yields an enclosure whose width is at most `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.exists_refinement` | `0 < ε → ∃ n, (x.approx n).Contains x.value ∧ (x.approx n).width ≤ ε` | Every positive rational tolerance admits an explicitly returned enclosing interval of at most that width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.ofRat` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For a rational number, its certified real name denotes that rational number and uses its degenerate one-point rational interval at every precision. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.neg` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For a certified real number, its certified negation denotes the negative of the real number named by the certificate, negates every rational interval enclosure, and retains the same precision-selection rule. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.add` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For two certified real numbers, their certified sum denotes the sum of their named real numbers, adds their rational interval enclosures at each common precision, and selects a precision sufficient to make each input enclosure no wider than half the requested tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.sub` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For two certified real numbers, their certified difference is their certified sum after negating the second certificate; it therefore denotes the first named real number minus the second. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.sub_value` | `(x.sub y).value = x.value - y.value` | Certified subtraction denotes the difference of the two named real values. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal.value_eq_of_common_approximations` | `(∀ (n : ℕ), x.approx n = y.approx n) → x.value = y.value` | Two certified names whose every approximation is shared denote the same real value. |
<!-- /GEN -->

### Exponential and logarithm names

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Exponential -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScale` | `ℚ → ℕ` | For a rational exponential argument, the exponential scaling factor is the larger of one and the absolute value of the argument's numerator. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expReduced` | `ℚ → ℚ` | For a rational exponential argument, the reduced exponential argument is the argument divided by its exponential scaling factor. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expPartial` | `ℚ → ℕ → ℚ` | For a rational number and a nonnegative polynomial degree, the exponential Taylor partial sum is the sum of the terms $q^k/k!$ for every integer $k$ from zero through $n$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expRemainder` | `ℚ → ℕ → ℚ` | For a rational number and a nonnegative polynomial degree, the exponential remainder radius is $\|q\|^{n+1}(n+2)/((n+1)!(n+1))$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expReducedRaw` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational number and a nonnegative polynomial degree, the reduced exponential enclosure has lower endpoint equal to the Taylor partial sum minus the remainder radius, upper endpoint equal to the Taylor partial sum plus the remainder radius, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expRaw` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational exponential argument and a nonnegative polynomial degree, the raw exponential enclosure is the natural-power interval obtained by raising the reduced exponential enclosure to the exponential scaling factor. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational exponential argument, the scalar exponential enclosure sequence assigns the raw exponential enclosure at degree zero to index zero and the conditional tightening of the preceding enclosure with the next raw enclosure to each positive index. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expPrecision` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | For a rational exponential argument and a strictly positive rational target width, the exponential precision index is the product of one plus the target-width denominator and two copies of one plus the exponential scaling factor. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.abs_expReduced_le_one` | `\|Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expReduced q\| ≤ 1` | Range reduction puts the exponential Taylor argument in the closed unit interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expRaw_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expRaw q n).Contains (Real.exp ↑q)` | The raw rational exponential interval encloses the real exponential of the rational input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar q n).Contains (Real.exp ↑q)` | Every tightened scalar exponential interval encloses the real exponential. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar q (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar q n)` | Scalar exponential enclosures are nested as precision increases. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.tighten_subinterval_right` | `I.Contains x → J.Contains x → (I.tighten J).Subinterval J` | Tightening two enclosures of the same real value is contained in either input enclosure. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.inv_den_le_of_pos` | `0 < u → 1 / ↑u.den ≤ u` | The reciprocal of a positive rational's denominator is no larger than the rational itself. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar q (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expPrecision q ε)).width ≤ ↑ε` | For a rational input `q` and a requested positive rational tolerance `ε`, evaluating the certified scalar exponential at the precision level selected for that tolerance yields an enclosure whose width is no larger than `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expName` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For a rational number, the certified-real representation of its exponential has value equal to the real exponential of that rational number, approximating intervals given by the scalar exponential enclosure sequence, nested approximations, containment of the exact value, the exponential precision index as its modulus, and the corresponding target-width guarantee. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Logarithm -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logCoordinate` | `ℚ → ℚ` | For a rational number, the logarithm coordinate is $(q-1)/(q+1)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logPartial` | `ℚ → ℕ → ℚ` | For a rational number and a nonnegative polynomial degree, the logarithm Taylor partial sum is twice the sum, for every integer $k$ from zero through $n$, of the $(2k+1)$st power of the logarithm coordinate divided by $2k+1$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logRemainder` | `ℚ → ℕ → ℚ` | For a rational number and a nonnegative polynomial degree, the logarithm remainder bound is $2\|z\|^{2n+3}/((2n+3)(1-\|z\|^2))$, where $z=(q-1)/(q+1)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logRaw` | `(q : ℚ) → 0 < q → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational number that is strictly positive and a nonnegative polynomial degree, the raw logarithm enclosure has lower endpoint equal to the logarithm partial sum minus its remainder bound, upper endpoint equal to the logarithm partial sum plus its remainder bound, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar` | `(q : ℚ) → 0 < q → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a strictly positive rational number, the scalar logarithm enclosure sequence assigns the raw logarithm enclosure at degree zero to index zero and the conditional tightening of the preceding enclosure with the next raw enclosure to each positive index. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logPrecision` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | For a rational number and a strictly positive rational target width, the logarithm precision index is the product of one plus the target-width denominator and two copies of one plus the sum of the absolute numerator and denominator of the rational number. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.abs_logCoordinate_lt_one` | `0 < q → \|Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logCoordinate q\| < 1` | A positive rational logarithm argument has atanh coordinate of absolute value strictly below one. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logRaw_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logRaw q hq n).Contains (Real.log ↑q)` | The raw rational logarithm interval encloses the real logarithm of its positive rational input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar q hq n).Contains (Real.log ↑q)` | Every tightened scalar logarithm interval encloses the real logarithm. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar q hq (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar q hq n)` | Scalar logarithm enclosures are nested as precision increases. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logScalar q hq (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logPrecision q ε)).width ≤ ↑ε` | Precision-driven logarithm enclosure width. For a positive rational number and a requested positive rational tolerance, evaluating the scalar logarithm enclosure at the precision level determined from those two inputs produces an interval no wider than the requested tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logName` | `(q : ℚ) → 0 < q → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For a rational number that is strictly positive, the certified-real representation of its logarithm has value equal to the real logarithm of that rational number, approximating intervals given by the scalar logarithm enclosure sequence, nested approximations, containment of the exact value, the logarithm precision index as its modulus, and the corresponding target-width guarantee. |
<!-- /GEN -->

### Transcendental interval extensions

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Operations -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval and a precision index, the exponential interval evaluation has lower endpoint equal to the lower endpoint of the scalar exponential enclosure at the input interval's lower endpoint, upper endpoint equal to the upper endpoint of the scalar exponential enclosure at the input interval's upper endpoint, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval` | `(I : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → 0 < I.lo → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval whose lower endpoint is strictly positive and a precision index, the logarithm interval evaluation has lower endpoint equal to the lower endpoint of the scalar logarithm enclosure at the original lower endpoint, upper endpoint equal to the upper endpoint of the scalar logarithm enclosure at the original upper endpoint, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowRaw` | `(base : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → 0 < base.lo → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a base rational interval and an exponent rational interval whose base interval has a strictly positive lower endpoint, and a precision index, the raw real-power interval is the exponential interval of the product of the exponent interval and the logarithm interval of the base. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval` | `(base : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → 0 < base.lo → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a base rational interval and an exponent rational interval whose base interval has a strictly positive lower endpoint, the real-power interval sequence assigns the raw real-power interval at precision zero to index zero and the conditional tightening of the preceding interval with the next raw real-power interval to each positive index. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval_sound` | `I.Contains x → ∀ (n : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval I n).Contains (Real.exp x)` | Exponential interval evaluation encloses the exponential of every enclosed real input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval_sound` | `I.Contains x → ∀ (n : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval I hI n).Contains (Real.log x)` | Positive logarithm interval evaluation encloses the logarithm of every enclosed real input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval_sound` | `base.Contains x → exponent.Contains y → ∀ (n : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval base exponent hbase n).Contains (x ^ y)` | Certified real-power interval evaluation. For a base rational interval whose lower endpoint is strictly positive that encloses a real base value, and an exponent rational interval enclosing a real exponent value, the real-power interval evaluation of the base and exponent, at any precision level, contains the true value of the base raised to that exponent. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval I (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expInterval I n)` | Exponential interval evaluation is nested in its precision argument. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval I hI (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.logInterval I hI n)` | Positive logarithm interval evaluation is nested in its precision argument. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval base exponent hbase (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.rpowInterval base exponent hbase n)` | Real-power interval evaluation is nested in its precision argument. |
<!-- /GEN -->

### Uniform meshes and contour quadrature

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Mesh -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | `Type` | A rational rectangle in the complex plane consists of a rational interval enclosing the real coordinate and a rational interval enclosing the imaginary coordinate. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.ext` | `x.re = y.re → x.im = y.im → x = y` | — |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.Contains` | `ℂ → Prop` | Given a rational rectangle and a complex number, rectangle membership means that both the real and imaginary coordinates of the number lie in their respective rational intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.Subinterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Prop` | Given two rational rectangles in the complex plane, the refinement relation holds exactly when each coordinate interval of the first is contained in the corresponding coordinate interval of the second. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.add` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For two rational rectangles in the complex plane, their sum rectangle is obtained by adding their real-coordinate intervals and their imaginary-coordinate intervals separately. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.smulRat` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a rational scalar and a rational rectangle in the complex plane, the scalar-multiple rectangle is obtained by multiplying each coordinate interval by that scalar. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.expand` | `(e : ℚ) → 0 ≤ e → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a rational rectangle in the complex plane, a rational error allowance, and the condition that this allowance is nonnegative, the expanded rectangle widens each coordinate interval by that allowance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.zero` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | The zero rectangle is the rational rectangle whose real and imaginary coordinate intervals are both the singleton interval containing zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.add_sound` | `I.Contains z → J.Contains w → (I.add J).Contains (z + w)` | Complex rectangle addition encloses sums of enclosed complex numbers. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.smulRat_sound` | `I.Contains z → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.smulRat q I).Contains (↑q * z)` | Rational scalar multiplication encloses the corresponding complex scalar multiple. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.expand_contains` | `I.Contains z → (I.expand e he).Contains z` | Coordinatewise widening preserves containment. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint` | `ℕ → ℕ → ℝ` | For a mesh size and a node index, the mesh point is the real number $k/n$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleMap` | `ℂ → ℝ → ℝ → ℂ` | For a complex center, a real radius, and a real parameter, the circle point is $c+r\exp(2\pi u i)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleTangent` | `ℝ → ℝ → ℂ` | For a real radius and a real parameter, the circle tangent is $2\pi i r\exp(2\pi u i)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleIntegrand` | `(ℂ → ℂ) → ℂ → ℝ → ℝ → ℂ` | For a complex-valued function, a complex center, a real radius, and a real parameter, the parameterized contour integrand is the value of the function at the corresponding circle point multiplied by the circle tangent there. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleContourIntegral` | `(ℂ → ℂ) → ℂ → ℝ → ℂ` | For a complex-valued function, a complex center, and a real radius, the circle contour integral is the integral from $0$ to $1$ of the parameterized contour integrand. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.minLoUpTo` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → ℕ → ℚ` | Given a sequence of rational intervals, the minimum lower endpoint up to a terminal node index is computed recursively: at index zero it is the lower endpoint of the initial interval, and at each successor index it is the smaller of the preceding result and the new lower endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.minHiUpTo` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → ℕ → ℚ` | Given a sequence of rational intervals, the minimum upper endpoint up to a terminal node index is computed recursively: at index zero it is the upper endpoint of the initial interval, and at each successor index it is the smaller of the preceding result and the new upper endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.maxLoUpTo` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → ℕ → ℚ` | Given a sequence of rational intervals, the maximum lower endpoint up to a terminal node index is computed recursively: at index zero it is the lower endpoint of the initial interval, and at each successor index it is the larger of the preceding result and the new lower endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.maxHiUpTo` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → ℕ → ℚ` | Given a sequence of rational intervals, the maximum upper endpoint up to a terminal node index is computed recursively: at index zero it is the upper endpoint of the initial interval, and at each successor index it is the larger of the preceding result and the new upper endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint_mem` | `0 < n → k ≤ n → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k ∈ Set.Icc 0 1` | A node of a nonempty uniform mesh lies in the unit parameter interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.continuousOn_of_lipschitz_bound` | `0 ≤ C → (∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖g s - g t‖ ≤ C * \|s - t\|) → ContinuousOn g (Set.Icc 0 1)` | A function satisfying a finite Lipschitz bound on the unit interval is continuous there. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.infEnclosure` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → (L : ℚ) → 0 ≤ L → (n : ℕ) → 0 < n → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a sequence of rational node intervals, a rational Lipschitz constant, the condition that the constant is nonnegative, a mesh size, and the condition that the mesh size is positive, the infimum enclosure has lower endpoint equal to the minimum node lower endpoint minus $L/n$ and upper endpoint equal to the minimum node upper endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.supEnclosure` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → (L : ℚ) → 0 ≤ L → (n : ℕ) → 0 < n → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a sequence of rational node intervals, a rational Lipschitz constant, the condition that the constant is nonnegative, a mesh size, and the condition that the mesh size is positive, the supremum enclosure has lower endpoint equal to the maximum node lower endpoint and upper endpoint equal to the maximum node upper endpoint plus $L/n$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.infEnclosure_sound` | `(∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, \|f s - f t\| ≤ ↑L * \|s - t\|) → (∀ k ≤ n, (nodes k).Contains (f (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k))) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.infEnclosure nodes L hL n hn).Contains (sInf (f '' Set.Icc 0 1))` | Certified infimum enclosure on the unit mesh. Fix a nonnegative Lipschitz constant and a positive number of mesh nodes, and suppose the real function is Lipschitz on `[0, 1]` with that constant and each rational node interval contains the function's value at the corresponding mesh point. Then the infimum enclosure built from those node intervals contains the true infimum of the function over `[0, 1]`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.supEnclosure_sound` | `(∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, \|f s - f t\| ≤ ↑L * \|s - t\|) → (∀ k ≤ n, (nodes k).Contains (f (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k))) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.supEnclosure nodes L hL n hn).Contains (sSup (f '' Set.Icc 0 1))` | Rational node enclosures and a Lipschitz bound enclose the supremum of a real function on the unit mesh interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.width_infEnclosure` | `0 ≤ w → ∀ (hL : 0 ≤ L) {n : ℕ} (hn : 0 < n), (∀ k ≤ n, (nodes k).width ≤ w) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.infEnclosure nodes L hL n hn).width ≤ w + L / ↑n` | The infimum enclosure width is at most one mesh error plus the uniform node-enclosure width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.width_supEnclosure` | `0 ≤ w → ∀ (hL : 0 ≤ L) {n : ℕ} (hn : 0 < n), (∀ k ≤ n, (nodes k).width ≤ w) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.supEnclosure nodes L hL n hn).width ≤ w + L / ↑n` | The supremum enclosure width is at most one mesh error plus the uniform node-enclosure width. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Quadrature -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.trapezoidSum` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a sequence of complex rational rectangles, the unscaled trapezoidal rectangle sum at a nonnegative index is computed recursively: at zero it is the zero rectangle, and at each successor it adds the two rectangles at the new cell's endpoints to the preceding sum. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.trapezoidEnclosure` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a sequence of complex rational rectangles and a mesh size, the trapezoidal enclosure is the unscaled trapezoidal rectangle sum multiplied by $1/(2n)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure` | `(ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) → (L : ℚ) → 0 ≤ L → (n : ℕ) → 0 < n → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a sequence of complex rational node rectangles, a rational Lipschitz constant, the condition that this constant is nonnegative, a mesh size, and the condition that the mesh size is positive, the integral enclosure widens the trapezoidal enclosure by $L/(2n)$ in both coordinates. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.norm_integral_sub_trapezoid_le` | `0 ≤ L → ∀ {n : ℕ}, 0 < n → (∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖g s - g t‖ ≤ ↑L * \|s - t\|) → ‖(∫ (u : ℝ) in 0..1, g u) - 1 / ↑(2 * ↑n) * ∑ k ∈ Finset.range n, (g (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k) + g (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n (k + 1)))‖ ≤ ↑L / (2 * ↑n)` | A Lipschitz complex function on the unit parameter interval differs from its deterministic trapezoidal rule by at most half a Lipschitz mesh unit. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure_sound` | `(∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖g s - g t‖ ≤ ↑L * \|s - t\|) → (∀ k ≤ n, (nodes k).Contains (g (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k))) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure nodes L hL n hn).Contains (∫ (u : ℝ) in 0..1, g u)` | Certified trapezoidal enclosure of an integral. Fix a nonnegative Lipschitz constant and a positive node count, and suppose the complex-valued integrand is Lipschitz on `[0, 1]` with that constant and each complex rational rectangle contains the integrand's value at the corresponding mesh point. Then the trapezoidal enclosure built from those rectangles contains the true integral of the integrand over `[0, 1]`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure_width` | `0 ≤ w → ∀ (hL : 0 ≤ L) {n : ℕ} (hn : 0 < n), (∀ k ≤ n, (nodes k).re.width ≤ w ∧ (nodes k).im.width ≤ w) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure nodes L hL n hn).re.width ≤ w + L / ↑n ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure nodes L hL n hn).im.width ≤ w + L / ↑n` | Each coordinate width of the certified integral is bounded by the uniform node width plus one mesh-scale Lipschitz allowance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleContourIntegral_enclosed` | `(∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleIntegrand f c r s - Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleIntegrand f c r t‖ ≤ ↑L * \|s - t\|) → (∀ k ≤ n, (nodes k).Contains (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleIntegrand f c r (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint n k))) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure nodes L hL n hn).Contains (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.circleContourIntegral f c r)` | Applying the complex mesh theorem to the parameterized circle integrand encloses the contour integral. |
<!-- /GEN -->

### Total measurable finite selection

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.instMeasurableSpaceRatInterval` | `MeasurableSpace Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | The measurable structure on rational intervals is the discrete $\sigma$-algebra, so every collection of rational intervals is measurable. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.instMeasurableSpaceComplexRatInterval` | `MeasurableSpace Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | The measurable structure on complex rational rectangles is the discrete $\sigma$-algebra, so every collection of complex rational rectangles is measurable. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue` | `(Fin (n + 1) → Bool) → Fin (n + 1)` | For a nonnegative integer and a Boolean-valued family indexed by the $n+1$ indices from zero through $n, the least-successful-index rule returns the smallest index whose Boolean value is true when such an index exists, and index zero when none exists. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue_accepts` | `(∃ i, accept i = true) → accept (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue accept) = true` | When at least one Boolean test succeeds, the returned index succeeds. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue_le` | `accept i = true → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue accept ≤ i` | Every successful index is no smaller than the successful index returned by the search. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue_eq_zero` | `(∀ (i : Fin (n + 1)), accept i = false) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue accept = 0` | If no Boolean test succeeds, the total search returns index zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.finiteRefine` | `(Fin (n + 1) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a nonnegative integer, a family of $n+1$ rational candidate intervals, and a strictly positive rational width tolerance, the finite-refinement interval is the candidate at the least index whose width is at most the tolerance, or the zeroth candidate when no candidate meets that condition. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.finiteRefine_width` | `(∃ i, (candidates i).width ≤ ↑ε) → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.finiteRefine candidates ε).width ≤ ↑ε` | If a candidate meets the requested width, finite refinement returns the least such candidate and meets the width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_finiteRefine` | `Measurable fun candidates => Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.finiteRefine candidates ε` | Finite refinement is measurable as a function of a finite vector of rational interval candidates. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex` | `(Fin (n + 1) → ℝ) → Fin (n + 1)` | For a nonnegative integer and a real-valued score for each of the $n+1$ indices from zero through $n, the least-score index is obtained by forming the finite set of pairs consisting of each score and its index, ordered first by score and then by index, certifying that this finite set is nonempty, and returning the index in its least pair. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex_minimal` | `score (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex score) ≤ score i` | The selected least-score index has score no larger than every competing score. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex_tie` | `score i = score (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex score) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex score ≤ i` | Equal minimal scores are resolved in favor of the smaller original index. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_leastScoreIndex` | `Measurable Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastScoreIndex` | For a sample-size index `n`, the function that selects, from a vector of `n+1` real scores, the smallest index attaining the least score is measurable with respect to the Borel σ-algebra on the score vector space and the discrete σ-algebra on the finite index set. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_leastTrue` | `(∀ (i : Fin (n + 1)), Measurable (accept i)) → Measurable fun ω => Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.leastTrue fun i => accept i ω` | A finite least-success search is measurable when every individual success event is measurable. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_infEnclosure` | `Measurable fun nodes => Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.infEnclosure (fun k => if hk : k < n + 1 then nodes ⟨k, hk⟩ else nodes 0) L hL n hn` | The rational infimum enclosure is measurable as a function of its finite rational node vector. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_supEnclosure` | `Measurable fun nodes => Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.supEnclosure (fun k => if hk : k < n + 1 then nodes ⟨k, hk⟩ else nodes 0) L hL n hn` | The rational supremum enclosure is measurable as a function of its finite rational node vector. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.FiniteSearch.measurable_integralEnclosure` | `Measurable fun nodes => Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.integralEnclosure (fun k => if hk : k < n + 1 then nodes ⟨k, hk⟩ else nodes 0) L hL n hn` | The rational complex integral enclosure is measurable as a function of its finite node rectangles. |
<!-- /GEN -->

### Packaged interface

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.API -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedIntervalArithmetic` | `Type` | This interface packages sound rational interval arithmetic and total certified-real refinement behind a paper-independent interface. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.certifiedIntervalArithmetic` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedIntervalArithmetic` | The concrete certified interval-arithmetic package provides outwardly sound rational interval operations for addition, subtraction, multiplication, division away from zero, exponentiation, logarithms and real powers on their positive domains, together with certified-real refinement of any requested positive rational precision. |
<!-- /GEN -->

### Certified complex rectangle semantics

Executable certified complex arithmetic builds on the real interval API. It supplies rational rectangles and refining complex names, then composes certified elementary functions, scheduled circle nodes, and finite quadrature without storing exact transcendental or contour-integral values in the executable results.

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sq` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval, its squared interval is the interval from the square of its upper endpoint to the square of its lower endpoint when it lies strictly below zero, the interval from the square of its lower endpoint to the square of its upper endpoint when it lies strictly above zero, and the interval from zero to the larger squared endpoint when it contains zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sq_sound` | `I.Contains x → I.sq.Contains (x ^ 2)` | The sign-aware rational square enclosure contains the square of every real number contained in the input interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sq_mono` | `I.Subinterval J → I.sq.Subinterval J.sq` | Squaring is inclusion-isotone for rational intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtUpper` | `ℚ → ℕ → ℚ` | For a rational number, the Newton upper-iterate sequence assigns the absolute value of the number plus one at iteration zero and one half of the sum of the preceding iterate and the number divided by that iterate at each later iteration. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtLower` | `ℚ → ℕ → ℚ` | For a rational number and an iteration count, the Newton lower square-root bound is the number divided by its Newton upper iterate at that count. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrt_iterates_sound` | `0 ≤ q → ∀ (n : ℕ), ↑(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtLower q n) ≤ √↑q ∧ √↑q ≤ ↑(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtUpper q n)` | For a nonnegative rational number `q` and any fuel count `n`, the Newton-iterate rational lower bound `sqrtLower q n` and upper bound `sqrtUpper q n` both enclose the real square root of `q`: `sqrtLower q n ≤ √q ≤ sqrtUpper q n`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrt_gap_rate` | `0 ≤ q → ∀ (n : ℕ), Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtUpper q n - Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtLower q n ≤ (q + 1) / (↑n + 1)` | The Newton upper-minus-lower gap is bounded by an explicit reciprocal fuel rate. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrt_iterates_converge` | `0 ≤ q → ∀ (ε : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat), ∃ n, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtUpper q n - Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtLower q n ≤ ↑ε` | The gap between the Newton lower and upper square-root bounds converges effectively to zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtInterval` | `(I : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval) → 0 ≤ I.lo → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational interval whose lower endpoint is nonnegative and a Newton iteration count, the square-root interval has lower endpoint equal to the Newton lower bound of the original lower endpoint, upper endpoint equal to the Newton upper bound of the original upper endpoint, and valid endpoint order. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtInterval_sound` | `I.Contains x → ∀ (fuel : ℕ), (I.sqrtInterval hI fuel).Contains √x` | The executable square-root interval encloses the square root of every nonnegative real contained in its input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtInterval_width` | `(I.sqrtInterval hI fuel).width = Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtUpper I.hi fuel - Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.sqrtLower I.lo fuel` | Square-root interval width is exactly the endpoint Newton gap, exposing both input-diameter and iteration-error contributions. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs_le_of_contains_width` | `I.Contains x → \|x\| ≤ ↑C → I.width ≤ w → I.maxAbs ≤ C + w` | Containment of one real value together with interval width controls both endpoint magnitudes. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs_neg` | `I.neg.maxAbs = I.maxAbs` | Negation preserves the maximum endpoint magnitude. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs_add` | `(I.add J).maxAbs ≤ I.maxAbs + J.maxAbs` | Addition increases endpoint magnitude by at most the sum of operand bounds. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs_sub` | `(I.sub J).maxAbs ≤ I.maxAbs + J.maxAbs` | Subtraction increases endpoint magnitude by at most the sum of operand bounds. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval.maxAbs_mul` | `(I.mul J).maxAbs ≤ I.maxAbs * J.maxAbs` | Interval multiplication has the product of operand endpoint magnitudes as an executable magnitude bound. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Rectangles -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.width` | `ℚ` | Given a rational complex rectangle, its width is the larger of the widths of its real and imaginary coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.maxAbs` | `ℚ` | Given a rational complex rectangle, its maximum coordinate endpoint magnitude is the larger of the maximum absolute endpoint magnitudes of its real and imaginary coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.point` | `ℚ → ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a rational real coordinate and a rational imaginary coordinate, the complex point rectangle is the zero-width rectangle containing $x+iy$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.neg` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a rational complex rectangle, its negative rectangle negates both coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.sub` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a first rational complex rectangle and a second rational complex rectangle, their difference rectangle adds the first rectangle to the coordinatewise negative of the second. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.mul` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given two rational complex rectangles, their product rectangle is obtained from the Cartesian complex-product formula using outward rational interval operations on the coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.conj` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a rational complex rectangle, its conjugate rectangle retains its real coordinate interval and negates its imaginary coordinate interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normSq` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational complex rectangle, its squared-modulus interval is the sum of the sign-aware squares of its real and imaginary coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.div` | `(J : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) → J.normSq.AwayFromZero → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a numerator rectangle, a denominator rectangle, and evidence that the denominator's squared-modulus interval excludes zero, the guarded quotient rectangle is the product of the numerator rectangle and the denominator's conjugate, with both resulting coordinate intervals divided by the denominator's squared-modulus interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.tighten` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given two rational complex rectangles, their coordinatewise tightening is formed by tightening the real coordinate intervals and tightening the imaginary coordinate intervals. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.point_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.point x y).Contains (↑↑x + ↑↑y * Complex.I)` | The rational point rectangle contains the corresponding complex number. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.maxAbs_le_of_contains_width` | `I.Contains z → max \|z.re\| \|z.im\| ≤ ↑C → I.width ≤ w → I.maxAbs ≤ C + w` | Semantic coordinate bounds and rectangle width give an executable maximum endpoint-magnitude bound. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.neg_sound` | `I.Contains z → I.neg.Contains (-z)` | Complex rectangle negation encloses negated complex values. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.sub_sound` | `I.Contains z → J.Contains w → (I.sub J).Contains (z - w)` | Complex rectangle subtraction encloses differences of enclosed values. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.mul_sound` | `I.Contains z → J.Contains w → (I.mul J).Contains (z * w)` | Complex rectangle multiplication encloses products of enclosed values. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normSq_sound` | `I.Contains z → I.normSq.Contains (‖z‖ ^ 2)` | The squared-modulus interval contains the squared complex norm. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.div_sound` | `I.Contains z → J.Contains w → (I.div J hJ).Contains (z / w)` | Guarded division of complex rational rectangles. For two complex rational rectangles enclosing complex values `z` and `w` respectively, provided the denominator rectangle's squared-modulus interval is bounded away from zero, the rectangle obtained by dividing the first rectangle by the second under that guard contains the quotient `z / w` . |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.width_conj` | `I.conj.width = I.width` | Conjugation preserves rectangle width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.maxAbs_conj` | `I.conj.maxAbs = I.maxAbs` | Conjugation preserves the maximum coordinate endpoint magnitude. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.tighten_sound_left` | `I.Contains z → J.Contains z → (I.tighten J).Contains z ∧ (I.tighten J).Subinterval I` | Tightening two rectangles containing a common value preserves that value and produces a subrectangle of the first argument. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.width_add` | `(I.add J).width = max (I.re.width + J.re.width) (I.im.width + J.im.width)` | Complex rectangle addition has exactly the maximum of the two summed coordinate widths. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.mul_maxAbs` | `(I.mul J).maxAbs ≤ 2 * I.maxAbs * J.maxAbs` | Complex rectangle multiplication has twice the product of operand coordinate magnitude bounds as an executable magnitude bound. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.rat_sq_width_le` | `K.sq.width ≤ 2 * K.maxAbs * K.width` | Squaring an interval has width at most twice its maximum endpoint magnitude times its width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.rat_sq_hi_le` | `K.sq.hi ≤ K.maxAbs ^ 2` | The upper endpoint of a squared interval is bounded by the square of its maximum endpoint magnitude. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.rat_maxAbs_mono` | `K.Subinterval L → K.maxAbs ≤ L.maxAbs` | Refining an interval cannot increase its maximum endpoint magnitude. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.mul_width` | `(I.mul J).width ≤ 2 * (I.maxAbs * J.width + J.maxAbs * I.width)` | Each coordinate width after complex multiplication is bounded explicitly by the operand widths and endpoint magnitudes. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normSq_width` | `I.normSq.width ≤ 4 * I.maxAbs * I.width` | The squared-modulus interval width is controlled by rectangle magnitude and rectangle width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.div_maxAbs` | `0 < δ → δ ≤ J.normSq.lo → (I.div J hJ).maxAbs ≤ (I.mul J.conj).maxAbs / δ` | If the squared-modulus denominator has positive lower bound δ, guarded division has maximum coordinate magnitude at most the numerator magnitude divided by δ. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.div_width` | `0 < δ → δ ≤ J.normSq.lo → (I.div J hJ).width ≤ (I.mul J.conj).maxAbs * J.normSq.width / δ ^ 2 + (I.mul J.conj).width / δ` | If the squared-modulus denominator has certified lower bound δ, guarded division propagates widths with the usual inverse-square factor. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normSq_lo_nonneg` | `0 ≤ I.normSq.lo` | Given a rational complex rectangle and a natural-number square-root fuel level, the norm interval applies rational square-root bounds to the rectangle's squared-modulus interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normInterval` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Applying rational square-root bounds to the squared-modulus interval produces an executable enclosure of the complex norm. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval.normInterval_sound` | `I.Contains z → ∀ (fuel : ℕ), (I.normInterval fuel).Contains ‖z‖` | The executable modulus interval contains the norm of every enclosed complex value. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Names -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex` | `Type` | A certified complex name represents a complex number by a sequence of rational rectangle enclosures indexed by fuel that is nested — each successive enclosure a subrectangle of the one before — and always contains the represented value, together with a computable rule selecting, for any requested positive rational error, a fuel level whose enclosure meets that coordinate-width tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.refine` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a certified complex name and a requested positive rational tolerance, the refined rational rectangle is the rectangle selected by that name's tolerance-to-fuel rule. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.refine_spec` | `(z.refine ε).Contains z.value ∧ (z.refine ε).width ≤ ↑ε` | Effective refinement preserves containment and meets its requested width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.ofRatPair` | `ℚ → ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex` | For a rational real coordinate and a rational imaginary coordinate, the certified complex name of their complex point has that point as its exact value and the corresponding singleton rectangle at every fuel level. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.add` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex` | For two certified complex names, their certified sum denotes the sum of their exact complex values and uses the coordinatewise sum of their equal-fuel rectangles. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.normApprox` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a certified complex name, the modulus approximation at each nonnegative fuel level is computed recursively: at zero it is the initial rectangle's modulus enclosure, and at each successor it is the intersection of the preceding approximation with the new modulus enclosure. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.normPrecision` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | For a certified complex name and a requested positive rational tolerance, the modulus precision is the maximum of a fuel level that refines the input rectangle to a tolerance determined by its initial magnitude bound and an explicit Newton-iteration fuel bound. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.normApprox_spec` | `(z.normApprox fuel).Contains ‖z.value‖ ∧ (z.normApprox (fuel + 1)).Subinterval (z.normApprox fuel)` | Recursive modulus approximations contain the norm and are adjacent-fuel nested. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.norm_width_at_precision` | `(z.normApprox (z.normPrecision ε)).width ≤ ↑ε` | For a certified complex input and a requested positive rational tolerance `ε`, evaluating the certified-modulus approximation at the precision level selected for that tolerance yields an output enclosure whose width is no larger than `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex.norm` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | For a certified complex name, its certified modulus name denotes the absolute value of its exact complex value and uses the recursive modulus enclosures with the stated precision rule. |
<!-- /GEN -->

### Complex elementary functions

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.atanPartial` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the arctangent Taylor partial sum is the alternating rational series through degree $2f+1$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.atanError` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the arctangent remainder scale is $\|q\|^{2f+3}/(2f+3)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.atanRaw` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational argument and a natural-number fuel level, the raw arctangent interval has endpoints equal to the Taylor partial sum minus and plus the remainder scale, respectively. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piRaw` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a natural-number fuel level, the raw π interval is four times the difference between four times the arctangent interval at $1/5$ and the arctangent interval at $1/239$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a natural-number fuel level, the π interval at that level is the raw π interval at level zero, and at every successor level is the intersection of the preceding π interval and the new raw π interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piPrecision` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | Given a positive rational target width, the π precision is eight times one plus the denominator of that target. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piRaw_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piRaw fuel).Contains Real.pi` | Every raw Machin rectangle contains the mathematical constant π. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval fuel).Contains Real.pi` | Every recursively intersected π rectangle contains π. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval fuel)` | Adjacent π fuel values are nested by construction through finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piInterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piPrecision ε)).width ≤ ↑ε` | For a requested positive rational tolerance `ε`, evaluating the certified π-enclosure at the precision level selected for that tolerance yields an enclosure whose width is no larger than `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.piName` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | The certified real name for π has exact value π, uses the recursively refined π intervals as approximations, and uses the specified π precision to achieve each requested positive rational width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinPartial` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the sine Taylor partial sum is the alternating rational sine series through degree $2f+1$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinError` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the sine remainder scale is $\|q\|^{2f+3}/(2f+3)!$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosPartial` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the cosine Taylor partial sum is the alternating rational cosine series through degree $2f$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosError` | `ℚ → ℕ → ℚ` | Given a rational argument and a natural-number fuel level, the cosine remainder scale is $\|q\|^{2f+2}/(2f+2)!$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinRaw` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational argument and a natural-number fuel level, the raw sine interval has endpoints equal to the sine Taylor partial sum minus and plus the sine remainder scale, respectively. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosRaw` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational argument and a natural-number fuel level, the raw cosine interval has endpoints equal to the cosine Taylor partial sum minus and plus the cosine remainder scale, respectively. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalMid` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℚ` | Given a rational interval, its rational midpoint is the arithmetic mean of its lower and upper endpoints. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalRadius` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℚ` | Given a rational interval, its rational radius is half its width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinIntervalRaw` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational input interval and a natural-number fuel level, the raw sine image interval expands the midpoint sine interval by the input interval's radius. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosIntervalRaw` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational input interval and a natural-number fuel level, the raw cosine image interval expands the midpoint cosine interval by the input interval's radius. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational input interval and a natural-number fuel level, the sine image interval at that level is the raw sine image interval at level zero, and at every successor level is the intersection of the preceding sine image interval and the new raw sine image interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational input interval and a natural-number fuel level, the cosine image interval at that level is the raw cosine image interval at level zero, and at every successor level is the intersection of the preceding cosine image interval and the new raw cosine image interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinRaw_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinRaw q fuel).Contains (Real.sin ↑q)` | Raw rational sine bounds contain the exact sine at every rational center. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosRaw_sound` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosRaw q fuel).Contains (Real.cos ↑q)` | Raw rational cosine bounds contain the exact cosine at every rational center. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.abs_sub_intervalMid_le_radius` | `I.Contains x → \|x - ↑(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalMid I)\| ≤ ↑(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalRadius I)` | Every real point enclosed by a rational interval is at most half its width away from the interval's rational midpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval_sound` | `I.Contains x → ∀ (fuel : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I fuel).Contains (Real.sin x)` | Sine interval evaluation encloses sine throughout the input interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval_sound` | `I.Contains x → ∀ (fuel : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I fuel).Contains (Real.cos x)` | Cosine interval evaluation encloses cosine throughout the input interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I fuel)` | Adjacent sine outputs are nested by recursive finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I fuel)` | Adjacent cosine outputs are nested by recursive finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I fuel).width ≤ I.width + 2 * Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinError (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalMid I) fuel` | Sine output width is the shrinking input diameter plus its explicit Taylor error. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I fuel).width ≤ I.width + 2 * Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosError (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalMid I) fuel` | Cosine output width is the shrinking input diameter plus its explicit Taylor error. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.TrigNames -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a certified real number and a natural-number fuel level, the sine name approximation at that level is the sine interval of the level-zero input enclosure, and at every successor level is the intersection of the preceding sine approximation and the sine interval of the corresponding input enclosure. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a certified real number and a natural-number fuel level, the cosine name approximation at that level is the cosine interval of the level-zero input enclosure, and at every successor level is the intersection of the preceding cosine approximation and the cosine interval of the corresponding input enclosure. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.trigNamePrecision` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | Given a certified real number and a positive rational target width, the trigonometric name precision is half the target width and the larger of the input precision required for that half-width and an explicit Taylor-fuel bound based on the target denominator and the initial enclosure's magnitude. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox_spec` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox x fuel).Contains (Real.sin x.value) ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox x (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox x fuel)` | Recursive sine-name approximations remain sound and adjacent-fuel nested. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox_spec` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox x fuel).Contains (Real.cos x.value) ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox x (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox x fuel)` | Recursive cosine-name approximations remain sound and adjacent-fuel nested. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.power_div_factorial_le` | `\|q\| ≤ ↑A → 1 ≤ A → 1 ≤ d → 1 ≤ r → 32 * d * A ^ 2 ≤ N → \|q\| ^ (2 * N + r) / ↑(2 * N + r).factorial ≤ 1 / ↑(4 * d)` | If the Taylor fuel dominates a rational magnitude and denominator scale, the corresponding rational power-over-factorial remainder is explicitly small. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinName_width_at_precision` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinNameApprox x (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.trigNamePrecision x ε)).width ≤ ↑ε` | For a certified real input and a requested positive rational tolerance `ε`, evaluating the certified sine name-approximation at the precision level selected for that tolerance yields an output enclosure whose width is no larger than `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosName_width_at_precision` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosNameApprox x (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.trigNamePrecision x ε)).width ≤ ↑ε` | Refinement of the input name plus Taylor fuel makes cosine output width effective. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinName` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | Given a certified real number, the certified sine name has exact value the sine of the input value, uses the recursively refined sine intervals as approximations, and uses the trigonometric precision rule for requested positive rational widths. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosName` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CertifiedReal` | Given a certified real number, the certified cosine name has exact value the cosine of the input value, uses the recursively refined cosine intervals as approximations, and uses the trigonometric precision rule for requested positive rational widths. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.IntervalExp -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expIntervalRaw` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | Given a rational input interval and a natural-number fuel level, the raw exponential image interval is the input midpoint, the scalar exponential interval at that midpoint, the upper endpoint of the scalar exponential interval at the input's maximum absolute endpoint magnitude, and the midpoint exponential interval expanded by that upper bound times the input radius. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.RatInterval` | For a rational input interval and a natural-number fuel level, the exponential image interval at that level is the raw exponential image interval at level zero, and at every successor level is the intersection of the preceding exponential image interval and the new raw exponential image interval. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval_sound` | `I.Contains x → ∀ (fuel : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I fuel).Contains (Real.exp x)` | The magnitude-dependent exponential interval contains the exponential of every enclosed real input. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I fuel)` | Adjacent midpoint exponential outputs are nested by finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I fuel).width ≤ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalMid I) fuel).width + 2 * (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Transcendental.expScalar I.maxAbs fuel).hi * Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.intervalRadius I` | For a rational interval `I` and a Taylor fuel/iteration count, the width of the certified interval exponential is bounded by the scalar Taylor truncation error at the interval's midpoint plus twice the interval's radius amplified by a certified bound on the exponential's magnitude over the interval. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ComplexExp -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpRaw` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a rational complex input rectangle and a natural-number fuel level, the raw complex-exponential rectangle multiplies the real-exponential interval of the real coordinate by the cosine and sine intervals of the imaginary coordinate to form its real and imaginary coordinates. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a rational complex input rectangle and a natural-number fuel level, the complex-exponential rectangle at that level is the raw complex-exponential rectangle at level zero, and at every successor level is the intersection of the preceding rectangle and the new raw complex-exponential rectangle. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp_sound` | `I.Contains z → ∀ (fuel : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp I fuel).Contains (Complex.exp z)` | Compositional complex exponential evaluation encloses the exact complex exponential. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp_nested` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp I (fuel + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp I fuel)` | Adjacent complex exponential outputs for one fixed input rectangle are nested by coordinatewise finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp I fuel).width ≤ 2 * (max (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I.re fuel).maxAbs 0 * max (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I.im fuel).width (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I.im fuel).width + max (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.cosInterval I.im fuel).maxAbs (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.sinInterval I.im fuel).maxAbs * max (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.expInterval I.re fuel).width 0)` | Complex exponential width is bounded explicitly by the widths and endpoint magnitudes of its certified exponential and trigonometric factors. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpMagnitude` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℚ` | Given a certified complex number, its complex-exponential magnitude scale is two plus the absolute upper endpoint of the level-zero scalar-exponential interval evaluated at the maximum absolute real-coordinate endpoint of its level-zero enclosure. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStageTolerance` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | Given a natural-number stage index, the complex-exponential stage tolerance is the positive rational number $1/(n+1)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpInnerTolerance` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | Given a certified complex number and a natural-number stage index, the inner complex-exponential tolerance is the stage tolerance divided by sixteen times the complex-exponential magnitude scale. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpInputTolerance` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | Given a certified complex number and a natural-number stage index, the complex-exponential input tolerance is the inner tolerance divided by four times the complex-exponential magnitude scale. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStageInput` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a certified complex number and a natural-number stage index, the complex-exponential stage input rectangle is the input's approximation at the precision required by that stage's input tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStageFuel` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → ℕ` | Given a certified complex number and a natural-number stage index, the complex-exponential stage fuel is the stage input rectangle, the inner tolerance divided by eight, the resulting trigonometric Taylor-fuel bound, and the larger of that bound and the two scalar-exponential precision bounds. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStage` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a certified complex number and a natural-number stage index, the complex-exponential stage rectangle evaluates the complex exponential on that stage's input rectangle using that stage's fuel. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a certified complex number and a natural-number stage index, the complex-exponential name approximation at that level is the stage-zero rectangle, and at every successor level is the intersection of the preceding name approximation and the new stage rectangle. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox_spec` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox z n).Contains (Complex.exp z.value) ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox z (n + 1)).Subinterval (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox z n)` | Recursive complex-exponential name approximations remain sound and adjacent stages are nested by their defining finite intersection. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStage_width` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStage z n).width ≤ ↑(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpStageTolerance n)` | Every scheduled stage has width at most its reciprocal stage tolerance; the proof combines input refinement, scalar Taylor convergence, magnitude amplification, and explicit rectangle-product propagation. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpPrecision` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | Given a certified complex number and a positive rational target width, the complex-exponential precision is the denominator of that target. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp_width_at_precision` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpNameApprox z (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpPrecision z ε)).width ≤ ↑ε` | For a certified complex input and a requested positive rational tolerance `ε`, evaluating the certified complex-exponential approximation at the precision level selected for that tolerance yields an output enclosure whose width is no larger than `ε`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExpName` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplex` | Given a certified complex number, the certified complex-exponential name has exact value the complex exponential of the input value, uses the recursively refined complex-exponential rectangles as approximations, and uses the stated precision rule for requested positive rational widths. |
<!-- /GEN -->

### Scheduled circle nodes and finite programs

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CircleSchedule -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule` | `Type` | A contour schedule packages a requested rational error tolerance, an operation count, an input precision, a mesh size that is nonempty, a fuel budget for transcendental evaluation, and a nonnegative magnitude-amplification bound, together with three nonnegative error budgets — [for node evaluation](hyp:nodeBudget, nodeBudget_nonneg), for the uniform mesh, and for the final quadrature widening — such that the three budgets together fit within the tolerance and the mesh budget absorbs the magnitude-amplified discretization error. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule.canonical` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ → (magnitude : ℚ) → 0 ≤ magnitude → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule` | Given a positive rational tolerance, a natural-number operation count, a rational magnitude bound, and evidence that this bound is nonnegative, the canonical contour schedule allocates one third of the tolerance to each error budget and selects its input precision, mesh size, and fuel from the stated rational data. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.TraceEvent` | `Type` | A trace event retains the schedule object itself, so execution and semantic audit cannot silently disagree about fuel, mesh, or precision. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent` | `ℕ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.TraceEvent` | Given a contour schedule, a mesh-endpoint index, and a primitive-operation index, the circle-node trace event records exactly those three quantities. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleAngle` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a contour schedule and a natural-number endpoint index, the rational rectangle enclosing the corresponding pure-imaginary circle angle has real coordinate zero and imaginary coordinate $2πk/m$, where $m$ is the schedule's mesh size. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | Given a rational radius, a contour schedule, and a natural-number endpoint index, the scheduled circle-node rectangle is the rational-interval evaluation of $r\exp(2πik/m)$ using that schedule's angle enclosure and fuel. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleInnerTolerance` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | Given a rational radius and a positive rational target width, the internal circle tolerance is the smaller of $\mathrm{target}/(256(\|r\|+1))$ and $1/1024$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleInputPrecision` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | Given a rational radius and a positive rational target width, the circle input precision is the π-approximation precision selected for their internal circle tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleExpFuel` | `ℚ → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℕ` | Given a rational radius, a natural-number mesh size, and a positive rational target width, the circle exponential fuel is the larger of the zero-input exponential precision and an explicit trigonometric Taylor-fuel bound computed from the internal circle tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode_width_at_selected_precision` | `0 ≤ radius → ∀ (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) (target : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat), schedule.inputPrecision = Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleInputPrecision radius target → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleExpFuel radius schedule.mesh target ≤ schedule.fuel → ∀ {k : ℕ}, k ≤ schedule.mesh → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius schedule k).width ≤ ↑target` | The explicit circle input precision and exponential fuel make every actually evaluated endpoint rectangle no wider than the requested target. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.exactCircleNode` | `ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule → ℕ → ℂ` | Given a rational radius, a contour schedule, and a natural-number endpoint index, the exact circle node is the complex number $r\exp(2πik/m)$, where $m$ is the schedule's mesh size. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleAngle_sound` | `k ≤ schedule.mesh → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleAngle schedule k).Contains (↑2 * ↑Real.pi * (↑↑k / ↑schedule.mesh) * Complex.I)` | The angle rectangle contains the exact pure-imaginary angle at every endpoint from zero through the terminal mesh endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode_sound` | `k ≤ schedule.mesh → (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius schedule k).Contains (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.exactCircleNode radius schedule k)` | For a rational radius, a quadrature schedule, and a mesh index `k`, if `k` does not exceed the schedule's mesh size, then the computed rational circle-node rectangle at index `k` contains the exact complex circle point `radius · exp(2πi · k / mesh)`. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode_endpoint` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius schedule schedule.mesh).Contains ↑radius ∧ Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.exactCircleNode radius schedule schedule.mesh = ↑radius` | The terminal endpoint `k = mesh`, which is explicitly used by the trapezoidal program, is certified and denotes the same point as endpoint zero. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode_width` | `0 ≤ radius → ∀ (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) (k : ℕ), (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius schedule k).width ≤ radius * (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental.complexExp (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleAngle schedule k) schedule.fuel).width` | Circle-node width propagation exposes radius scaling and the scheduled complex-exponential approximation width. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode_schedule_correspondence` | `(Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent schedule k operation).schedule.operationCount = schedule.operationCount ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent schedule k operation).schedule.inputPrecision = schedule.inputPrecision ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent schedule k operation).schedule.fuel = schedule.fuel ∧ (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent schedule k operation).schedule.mesh = schedule.mesh ∧ Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNodeEvent schedule k operation).schedule k = Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.circleNode radius schedule k` | Reading operation count, input precision, fuel, and mesh from the trace and from execution gives definitionally the same data because both retain the shared schedule object. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Program -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | `Type` | A certified complex map packages an exact complex function with an executable interval-evaluation program over rational input rectangles and a fuel level whose primitive-operation count is recorded, proved sound — every executable result contains the exact value at every enclosed input. Increasing fuel refines the enclosure to a subrectangle of the previous one, and refining the input rectangle cannot enlarge the output enclosure. The map carries [a nonnegative Lipschitz bound on how input width amplifies into output width](hyp:amplification, amplification_nonneg) and a nonincreasing sequence of algorithmic-error bounds in the fuel, such that the evaluated width never exceeds the algorithmic error plus the amplified input width, together with a computable fuel rule meeting any requested positive algorithmic-error target. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.identity` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | The identity certified complex map denotes every complex input itself, returns the input rectangle at every fuel level, has no primitive operations or algorithmic error, and has unit input-width amplification. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.constant` | `ℚ → ℚ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | For a rational real coordinate and a rational imaginary coordinate, the constant certified complex map denotes the corresponding complex constant and returns its singleton rational rectangle for every input rectangle and fuel level. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.add` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | For two certified complex maps, their certified pointwise sum denotes the sum of their exact values, evaluates by adding their output rectangles at a common fuel level, and adds their operation counts, width amplifications, and algorithmic errors. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.sub` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | For two certified complex maps, their certified pointwise difference denotes the difference of their exact values, evaluates by subtracting their output rectangles at a common fuel level, and adds their operation counts, width amplifications, and algorithmic errors. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.mulWithBounds` | `(f g : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap) → (Bf Bg : ℚ) → 0 ≤ Bf → 0 ≤ Bg → (∀ (I : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) (fuel : ℕ), (f.eval I fuel).maxAbs ≤ Bf) → (∀ (I : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval) (fuel : ℕ), (g.eval I fuel).maxAbs ≤ Bg) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap` | Given two certified complex maps, rational magnitude bounds for their executable output rectangles, the condition that both bounds are nonnegative, the condition that every output rectangle of the first map has maximum coordinate magnitude at most its bound, and the analogous condition for the second map, the certified pointwise product denotes the product of their exact values and uses the product-width propagation formula for its error and width amplification. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedComplexMap.algorithmError_le_of_modulus` | `f.errorModulus ε ≤ fuel → f.algorithmError fuel ≤ ↑ε` | Fuel sufficiency bound. For a certified complex-map evaluation algorithm and a requested positive rational error tolerance, if the number of iterations supplied is at least the algorithm's certified error modulus at that tolerance, then running the algorithm for that many iterations yields an approximation whose error is at most the requested tolerance. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram` | `Type` | A finite rational contour program consists of a certified interval extension of the numerator function and of the denominator function, together with a rational circle radius that is strictly positive. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.operationCount` | `ℕ` | For a contour program, the structural operation count of one contour node is the sum of the operation counts of its numerator and denominator maps plus two operations for guarded division and multiplication by the circle point. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.constantUnit` | `ℚ → ℚ → (radius : ℚ) → 0 < radius → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram` | Given a rational real numerator coordinate, a rational imaginary numerator coordinate, a rational circle radius, and the condition that the radius is strictly positive, the constant-over-unit contour program has the indicated constant numerator, unit denominator, and radius. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.nodeBox` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a contour program, a schedule, and a nonnegative node index, the node rectangle is the scheduled rational rectangle for the circle point at that index. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds` | `Type` | Uniform semantic magnitude data for one contour program: a nonnegative bound that covers every real and imaginary coordinate of the exact numerator function everywhere on the circle, and a nonnegative bound that covers the denominator function likewise. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.mapWidthBounds` | `ℚ → ℚ × ℚ` | For a contour program and a rational common error target, the pair of map-width bounds consists of the target plus the target multiplied by the numerator map's width amplification, and the analogous quantity for the denominator map. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.nodePropagationBound` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program → ℚ → ℚ → ℚ` | For a contour program, uniform bounds on its numerator and denominator values, a rational separation bound, and a rational common error target, the node-error propagation bound is obtained by forming the two map-width bounds, adding the numerator bound to its width bound, adding the denominator bound to its width bound, forming a bound for the division numerator, forming its width bound, forming the denominator squared-modulus width bound, forming the quotient width bound, forming the quotient magnitude bound, and combining quotient and circle-point errors. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.nodeScale` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → ℚ` | For a contour program, uniform bounds on its numerator and denominator values, and a positive rational separation bound, the node-error scale is one plus the absolute value of the node-error propagation bound evaluated at target one. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.canonicalNodeTarget` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | For a contour program, uniform bounds on its numerator and denominator values, a positive rational separation bound, and a positive rational tolerance, the canonical node target is the smaller of one and the tolerance divided by three times the node-error scale. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.constantUnitBounds` | `(numeratorRe numeratorIm radius : ℚ) → (hradius : 0 < radius) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.constantUnit numeratorRe numeratorIm radius hradius)` | Given a rational real numerator coordinate, a rational imaginary numerator coordinate, a rational circle radius, and the condition that the radius is strictly positive, the value bounds for the constant-over-unit contour program bound its numerator coordinates by the larger absolute numerator coordinate and its denominator coordinates by one. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule → Type` | A denominator certificate for a contour program under a fixed schedule provides a common squared-modulus separation bound that is strictly positive, together with the guarantees that every mesh endpoint's denominator interval evaluation is executable and bounded away from zero and that the same separation bound lies below every such endpoint's lower squared-modulus bound. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedProgramSchedule` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → Type` | A certified program schedule packages an exact schedule for evaluating a given contour program to a stated separation bound, together with the guarantees that its operation count matches the program's structural operation count, a common positive error target selected canonically from the tolerance, value bounds, and separation, an input precision equal to the uniform circle precision for that target, and that its fuel dominates the circle-exponential fuel requirement, the numerator's algorithmic-error modulus, and the denominator's algorithmic-error modulus, while the compositional arithmetic propagation bound fits within the schedule's node budget. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.nodeTrace` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → {bounds : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program} → {separation : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat} → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedProgramSchedule program bounds separation → ℕ → List Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.TraceEvent` | For a contour program, its value bounds, a positive separation target, a certified schedule, and a nonnegative node index, the node trace lists one scheduled trace event for each structural operation of the program. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.nodeTrace_spec` | `(program.nodeTrace scheduled k).length = program.operationCount ∧ ∀ event ∈ program.nodeTrace scheduled k, event.schedule = scheduled.schedule ∧ event.schedule.operationCount = program.operationCount` | For a contour program, a target error separation, a certified schedule for that program, and a stage index `k`, the executable node trace at stage `k` has exactly one event per structural operation of the program, and every event in it records the full schedule together with that schedule's operation count. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.canonicalScheduled` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → (bounds : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourValueBounds program) → (separation : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat → (magnitude : ℚ) → 0 ≤ magnitude → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CertifiedProgramSchedule program bounds separation` | Given a contour program, uniform bounds on its numerator and denominator values, a positive rational separation bound, a positive rational tolerance, a rational magnitude bound, and the condition that this magnitude bound is nonnegative, the canonical certified schedule derives the operation count, input precision, fuel, mesh, and error budgets from these quantities. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.constantUnitCertificate` | `(numeratorRe numeratorIm radius : ℚ) → (hradius : 0 < radius) → (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.constantUnit numeratorRe numeratorIm radius hradius) schedule` | Given a rational real numerator coordinate, a rational imaginary numerator coordinate, a rational circle radius, the condition that the radius is strictly positive, and a schedule, the constant-over-unit denominator certificate certifies separation one and a squared-modulus lower bound of one at every scheduled endpoint. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.integrandNodeFin` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate program schedule → Fin (schedule.mesh + 1) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a contour program, a schedule, a certificate that its denominator is separated from zero at the scheduled endpoints, and an endpoint index among the mesh plus one endpoints, the finite-index integrand node rectangle is the guarded quotient of the evaluated numerator and denominator rectangles multiplied by the node rectangle. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.integrandNodes` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate program schedule → ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a contour program, a schedule, a certificate that its denominator is separated from zero at the scheduled endpoints, and a nonnegative index, the total integrand-node rectangle is the guarded evaluated quotient times the node rectangle when the index is at most the mesh size, and otherwise is the terminal finite-index node rectangle. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.normalizedIntegrand` | `Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule → ℝ → ℂ` | For a contour program, a schedule, and a real circle parameter, the normalized integrand is the quotient of the exact numerator and denominator at the corresponding circle point, multiplied by that point. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.normalizedContourIntegral` | `ℂ` | For a contour program, the normalized contour integral is its circle contour integral for the exact numerator-to-denominator quotient, divided by $2\pi i$. |
<!-- /GEN -->

### Certified finite contour quadrature

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourEvaluation -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.normalizedContourIntegral_eq` | `(∀ u ∈ Set.Icc 0 1, program.denominator.value (↑program.radius * Complex.exp (↑2 * ↑Real.pi * ↑u * Complex.I)) ≠ 0) → program.normalizedContourIntegral = ∫ (u : ℝ) in 0..1, program.normalizedIntegrand schedule u` | The canceled unit-parameter integral equals the normalized exact circle contour integral whenever the denominator is nonzero along the circle. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.integrandNodes_sound` | `k ≤ schedule.mesh → (program.integrandNodes schedule certificate k).Contains (program.normalizedIntegrand schedule (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.CircleMesh.meshPoint schedule.mesh k))` | Every certified finite endpoint rectangle contains the exact normalized integrand. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.integrandNodes_width_le_propagation` | `↑separation ≤ certificate.separation → ∀ {k : ℕ}, k ≤ scheduled.schedule.mesh → (program.integrandNodes scheduled.schedule certificate k).width ≤ program.nodePropagationBound bounds ↑separation ↑scheduled.target` | The scheduled node-width bound follows compositionally from circle input precision, both map error moduli, their amplification fields, rational magnitude bounds, guarded-division separation, and primitive width lemmas. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram.evaluate` | `(program : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourProgram) → (schedule : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Schedule) → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate program schedule → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.ComplexRatInterval` | For a contour program, a schedule, and a certificate that its denominator is separated from zero at the scheduled endpoints, the evaluated contour rectangle is the deterministic integral enclosure of its integrand-node rectangles, widened symmetrically by half the schedule's quadrature budget. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.denominator_ne_zero_at_nodes` | `k ≤ schedule.mesh → program.denominator.value (Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.exactCircleNode program.radius schedule k) ≠ 0` | Denominator separation at all certified nodes implies nonvanishing of the exact denominator at all mesh endpoints. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.evaluate_contains` | `(∀ u ∈ Set.Icc 0 1, program.denominator.value (↑program.radius * Complex.exp (↑2 * ↑Real.pi * ↑u * Complex.I)) ≠ 0) → (∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖program.normalizedIntegrand schedule s - program.normalizedIntegrand schedule t‖ ≤ ↑schedule.magnitude * \|s - t\|) → (program.evaluate schedule certificate).Contains program.normalizedContourIntegral` | Under a Lipschitz bound and denominator nonvanishing on the full circle, the returned rational rectangle contains the normalized exact contour integral. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.evaluate_width` | `↑separation ≤ certificate.separation → (program.evaluate scheduled.schedule certificate).width ≤ ↑scheduled.schedule.tolerance` | Uniform scheduled node widths propagate through mesh and final widening, so both result coordinates fit within the three split budgets. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.certified_contour_evaluation` | `↑separation ≤ certificate.separation → (∀ u ∈ Set.Icc 0 1, program.denominator.value (↑program.radius * Complex.exp (↑2 * ↑Real.pi * ↑u * Complex.I)) ≠ 0) → (∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖program.normalizedIntegrand scheduled.schedule s - program.normalizedIntegrand scheduled.schedule t‖ ≤ ↑scheduled.schedule.magnitude * \|s - t\|) → (program.evaluate scheduled.schedule certificate).Contains program.normalizedContourIntegral ∧ (program.evaluate scheduled.schedule certificate).re.width ≤ ↑scheduled.schedule.tolerance` | Certified finite-precision evaluation of a contour integral. Fix a contour-integral program, value bounds for it, a target separation, a certified mesh schedule built to meet that target, and a certificate that the denominator stays bounded away from zero, whose guaranteed separation covers the target one; assume also that the program's denominator never vanishes anywhere on the parametrized circle and the normalized integrand is Lipschitz in the circle parameter with the schedule's constant. Then the rational rectangle obtained by evaluating the program both contains the true normalized contour integral and has real-part width at most the schedule's requested tolerance. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.inverseMaxTolerance` | `ℕ → Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.PosRat` | For a nonnegative integer, the reciprocal-maximum tolerance is the positive rational number $1/\max(n,1)$. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.certified_contour_evaluation_inverseMax` | `scheduled.schedule.tolerance = Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.inverseMaxTolerance n → ∀ (certificate : Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.DenominatorCertificate program scheduled.schedule), ↑separation ≤ certificate.separation → (∀ u ∈ Set.Icc 0 1, program.denominator.value (↑program.radius * Complex.exp (↑2 * ↑Real.pi * ↑u * Complex.I)) ≠ 0) → (∀ s ∈ Set.Icc 0 1, ∀ t ∈ Set.Icc 0 1, ‖program.normalizedIntegrand scheduled.schedule s - program.normalizedIntegrand scheduled.schedule t‖ ≤ ↑scheduled.schedule.magnitude * \|s - t\|) → (program.evaluate scheduled.schedule certificate).re.width ≤ 1 / max (↑n) 1` | Specializing the generic contour certificate to reciprocal-max tolerance gives the width required by canonical finite-rational statistics. |
| `Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.midpoint_error_le_half_width` | `I.Contains x → \|↑((I.lo + I.hi) / 2) - x\| ≤ ↑I.width / 2` | Containment in a rational real interval bounds its midpoint error by half the interval width, turning the reciprocal-max width into a statistic error bound. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.API -->
_(no documented declarations in Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.API)_
<!-- /GEN -->

## 10a'. `Mathlib/CondDistribWitness.lean` — Witness-kernel form of a conditional distribution

The measure-theoretic core of a backdoor / Rule-2 argument, stated purely in terms of
Mathlib's `condDistrib` (no SCM machinery).  When the outcome is a measurable function of the
conditioning variable and a residual coordinate, its conditional law is the pushforward of the
residual's conditional law by that function — the "witness kernel".

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `ProbabilityTheory.witnessKernel` | `Kernel (𝒳 × 𝒵) 𝒴`, `p ↦ (condDistrib C Z μ p.2).map (h p.1 p.2)` | Packages the posterior witness kernel `p ↦ (condDistrib C Z μ p.2).map (h p.1 p.2)` as a `Kernel`; Markov when `μ` is finite. | **Proved** |
| `ProbabilityTheory.condDistrib_map_of_condDistrib_fst_eq` | If `Y = h X Z C` pointwise and `C ⊥ X ∣ Z` (as `condDistrib C (X,Z) μ =ᵐ condDistrib C Z μ`), then `condDistrib Y (X,Z) μ =ᵐ (fun p => (condDistrib C Z μ p.2).map (h p.1 p.2))` | The witness-kernel form of a conditional distribution under conditional independence; the SCM-agnostic heart of the backdoor / Rule-2 identification. Proof by uniqueness of disintegration on rectangles. | **Proved** |
| `ProbabilityTheory.condDistrib_map_of_funext` | If `Y = H Z C` pointwise then `condDistrib Y Z μ =ᵐ[μ.map Z] (fun z => (condDistrib C Z μ z).map (H z))` | The **no-treatment** (`X`-free) specialization of the witness lemma: when the conditioning variable `Z` already carries all the information used to split `Y`, no conditional-independence hypothesis is needed. Consumed by the do-side bridge `doSide_M2_pullback_eq_M1_witness`. | **Proved** |

## 10b. `Mathlib/CondIndep.lean` — Graph-independent CI axioms (re-export)

Re-exports the four sub-modules below.  Existing `import Causalean.Mathlib.CondIndep`
statements continue to work unchanged.

### 10b-i. `Mathlib/CondIndep/SemiGraphoid.lean` — Semi-graphoid axioms on `ValuesOn` projections

Weak union and contraction for `CondIndepFun` specialized to `ValuesOn I Ω` projections.
Requires `Causalean.SCM.Model.SCM`.

### 10b-ii. `Mathlib/CondIndep/CondExp.lean` — CondExp + CondIndepFun lemmas

Drop-of-conditioning, product factorization, weak union via `prodMk`, and extension
by an `m`-measurable function.  No SCM import required.

### 10b-iii. `Mathlib/CondIndep/AELift.lean` — A.e.-equality/inequality lifts under overlap

Single-arm a.e.-lift and essential-bound inheritance for potential-outcome variables.

### 10b-iv. `Mathlib/CondIndep/Integrability.lean` — Integrability via condExp indicators

σ-projection of indicator positivity and integrability bound from a condExp product identity.

**SemiGraphoid** (`CondIndep/SemiGraphoid.lean`):

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.condIndep_valuesProjection_weak_union` | `CondIndepFun (σ_Z) (valProjX) (valProjYW) μ → CondIndepFun (σ_ZW) (valProjX) (valProjY) μ` | Weak-union axiom on `ValuesOn` projections; proved by decomposing the union projection into `(Y,W)` and applying the generic `prodMk` weak-union helper | **Proved** |
| `Causalean.condIndep_valuesProjection_contraction` | `CondIndepFun (σ_ZW) X Y μ → CondIndepFun σ_Z X W μ → CondIndepFun σ_Z X (valProjYW) μ` | Contraction axiom on `ValuesOn` projections; proved by `condIndepFun_contraction_of_prodMk` plus projection-union assembly | **Proved** |

**CondExp** (`CondIndep/CondExp.lean`):

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.condExp_sup_comap_eq_of_condIndep_comap` *(private)* | `CondIndepFun m hm g f μ → StronglyMeasurable[σ(g)] Y → Integrable Y μ → μ[Y \| m ⊔ σ(f)] =ᵐ[μ] μ[Y \| m]` | Precise generic drop-of-conditioning helper for `σ(g)`-measurable real integrands; proved by indicator rectangles plus L1 density | **Proved** |
| `Causalean.condExp_sup_comap_eq_of_condIndep` | `CondIndepFun m hm g f μ → Measurable h → Integrable (h ∘ g) μ → μ[h ∘ g \| m ⊔ σ(f)] =ᵐ[μ] μ[h ∘ g \| m]` | Drop-of-conditioning: if `g ⟂ f \| m`, enlarging conditioning by `σ(f)` leaves `h ∘ g`'s condExp unchanged; reduces to the private `σ(g)`-measurable helper | **Proved** |
| `Causalean.condExp_mul_of_condIndep` | `CondIndepFun m hm f g μ → μ[(u∘f)·(v∘g) \| m] =ᵐ μ[u∘f \| m] * μ[v∘g \| m]` | Product factorization of condExp under CI; core step of backdoor ATE proof | **Proved** (depends on `condExp_sup_comap_eq_of_condIndep`) |
| `Causalean.condIndepFun_weak_union_of_prodMk` | `CondIndepFun m hm W (V,A) μ → CondIndepFun (m ⊔ σ_A) (sup_le hm _) W V μ` | Weak union via `prodMk`; proved by CE tower/pullout over the `(V,A)` conditioning algebra and drop-conditioning for the W-indicator | **Proved** (depends on `condExp_sup_comap_eq_of_condIndep`) |
| `Causalean.condIndepFun_prodMk_of_measurable_left` | `CondIndepFun m hm W Z μ → Measurable[m] X → CondIndepFun m hm (W,X) Z μ` | Extension of `CondIndepFun` by an `m`-measurable factor | **Proved** |
| `Causalean.condIndepFun_contraction_of_prodMk` | `CondIndepFun (m ⊔ σ(W)) X Y μ → CondIndepFun m X W μ → CondIndepFun m X (Y,W) μ` | Generic contraction via a product right side; proved by CE tower/pullout and π-system extension over measurable rectangles | **Proved** |

**AELift** (`CondIndep/AELift.lean`):

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.indicator_aeEq_of_aeEq_restrict` | `f =ᵐ[μ.restrict s] g → s.indicator f =ᵐ[μ] s.indicator g` | Pushes a restricted a.e.-equality to a global equality after multiplying by the set indicator | **Proved** |
| `Causalean.aeEq_restrict_of_indicator_aeEq` | `s.indicator f =ᵐ[μ] s.indicator g → f =ᵐ[μ.restrict s] g` | Recovers a restricted a.e.-equality from a global set-indicator equality | **Proved** |
| `Causalean.condExp_indicator_aeEq_zero` | `s.indicator f =ᵐ[μ] 0 → s.indicator (μ[f \| m]) =ᵐ[μ] 0` | Propagates indicator-zero facts through conditional expectation when `s` is `m`-measurable | **Proved** |
| `Causalean.ae_eq_of_ae_eq_restrict_arm` | Two `m`-measurable real functions equal on `μ.restrict {A=a}` are equal μ-a.e., given overlap | Single-arm σ-pushforward lift; used in `Proxy/Helpers/BridgeW.lean` | **Proved** |
| `Causalean.ae_le_YofA_of_ae_le_Y` | `Y ≤ M` a.e. + latent exchangeability + consistency → `Y(a) ≤ M` a.e. | Inherits Y's essential upper bound under overlap | **Proved** |
| `Causalean.ae_le_YofA_of_ae_le_Y_below` | Mirror of above for lower bound | Inherits Y's essential lower bound | **Proved** |

**Integrability** (`CondIndep/Integrability.lean`):

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.ae_pos_condExp_indicator_of_le` | `m₁ ≤ m₂`, overlap on `m₂` ⟹ `μ⟦𝟙_{A=a} \| m₁⟧ > 0` a.e. | σ-projection of indicator positivity; used in `CondExpQ.lean` | **Proved** |
| `Causalean.integrableOn_of_condExp_indicator_mul` | `Measurable[m] g → 0 ≤ᵐ g → μ⟦𝟙_B\|m⟧·g =ᵐ μ⟦𝟙_C\|m⟧ → IntegrableOn g B μ` | MCT-based integrability bound; used in `CondExpQ.lean` | **Proved** |

## 10c. `Mathlib/Indep.lean` — `IndepFun` / `CondIndepFun` plumbing (Block 1)

Generic facts about independence and conditional independence, candidates for
upstream inclusion in Mathlib.  Consumed by `SCM/Do/LocalMarkov.lean` to
discharge the latent-root branch of the local Markov property
(`full_local_markov_latent`).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.indepFun_pi_of_disjoint` | `S T : Finset ι`, `Disjoint S T` → `IndepFun (coord-tuple on S) (coord-tuple on T) (Measure.pi μ)` for a family of probability measures | Coordinate-tuple projections at disjoint `Finset`s are `IndepFun` under `Measure.pi`.  Derived from `iIndepFun_pi` + `iIndepFun.indepFun_finset` | **Proved** |
| `Causalean.finsetCoordProj` / `Causalean.measurable_finsetCoordProj` | `Finset ι → (∀ i, Ω i) → ValuesOn S Ω` and measurability | Generic finite-block tuple projection on a dependent product, packaged separately so product-measure independence lemmas can talk about coordinate blocks without depending on SCM-specific `ValuesOn` infrastructure | **Proved** |
| `Causalean.finsetCoordProjFromCondResidual` / `Causalean.measurable_finsetCoordProjFromCondResidual` / `Causalean.finsetCoordProjFromCondResidual_comp` | Reassemble `π_S` from `(π_U, π_{S \\ U})`, with measurability and composition identity | Residual-coordinate bookkeeping used to reduce overlap-in-conditioning to a disjoint residual product statement | **Proved** |
| `Causalean.condIndepFun_pi_cond_residual_of_disjoint` | pairwise-disjoint residual blocks `S0`, `T0`, `U` → `CondIndepFun σ(π_U) (π_U,π_S0) (π_U,π_T0) (Measure.pi μ)` | Focused finite-product conditional-independence fact: after conditioning on `π_U`, disjoint residual coordinate blocks are conditionally independent; proved from unconditional disjoint-block independence plus weak-union/prodMk lifting | **Proved** |
| `Causalean.condIndepFun_pi_of_inter_subset` | `S ∩ T ⊆ U → CondIndepFun σ(π_U) π_S π_T (Measure.pi μ)` | Raw finite-product conditional-independence fact: coordinate tuple on `S` is CI of tuple on `T` given tuple on `U` whenever the overlap is absorbed by conditioning; reduces to the residual disjoint-block helper | **Proved** (depends on `condIndepFun_pi_cond_residual_of_disjoint`) |
| `Causalean.comap_eq_bot_of_subsingleton` | `Subsingleton β → MeasurableSpace.comap (f : α → β) inferInstance = ⊥` | The comap σ-algebra of a subsingleton-valued function is trivial — every preimage is either `∅` or `univ`.  Used to collapse the `FullCondIndep M X Y ∅ … μ` conditioning σ-algebra to `⊥` | **Proved** |
| `Causalean.condIndepFun_bot_of_indepFun` | `IsProbabilityMeasure μ` + `Measurable f` + `Measurable g` + `IndepFun f g μ` → `CondIndepFun ⊥ bot_le f g μ` | Bridge: `IndepFun` lifts to `CondIndepFun` with trivial (`⊥`) conditioning σ-algebra on a standard Borel space.  Proof via `condIndepFun_iff_condExp_inter_preimage_eq_mul` + `condExp_bot` | **Proved** |
| `Causalean.indepFun_of_map` | `AEMeasurable φ ν` + measurable `X, Y` + `IndepFun (X ∘ φ) (Y ∘ φ) ν` → `IndepFun X Y (ν.map φ)` | Pushforward bridge for `IndepFun`: analogue of `LocalMarkov.condIndepFun_of_map` at the unconditional level.  Proof via `indepFun_iff_measure_inter_preimage_eq_mul` + `Measure.map_apply_of_aemeasurable` | **Proved** |

## 10d. `Mathlib/IndepIntegral.lean` — Drop-of-conditioning integral identity

Upstream candidate.  Contains no Causalean-specific definitions; namespaced
directly under `ProbabilityTheory` so it can be contributed back to Mathlib
verbatim.  Extracted from the IV/LATE first-stage and reduced-form proofs.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `ProbabilityTheory.IndepFun.integral_restrict_preimage_eq_mul` | `IndepFun f g μ`, `Measurable f`, `Measurable g`, `MeasurableSet E`, `Measurable h : β → ℝ` → `∫ ω in f ⁻¹' E, h (g ω) ∂μ = (μ (f ⁻¹' E)).toReal * ∫ ω, h (g ω) ∂μ` | Drop-of-conditioning: under independence of `f` and `g`, conditioning on an event in the `f`-fibre factors out as a scalar multiple.  Proof rewrites the set integral as an indicator-weighted integral, applies `IndepFun.integral_fun_comp_mul_comp`, and evaluates via `integral_indicator_one` | **Proved** (upstream candidate — no Causalean types) |

## 10d''. `Mathlib/ELpNormMeasurable.lean` — Parametric L²-norm measurability

Upstream candidate.  Provides causal-agnostic measurability facts for the
parameter map `ω ↦ ‖g ω‖_{L²(P)}` by rewriting the L² norm as a parametric
lintegral and applying Tonelli measurability.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.measurable_eLpNorm_two_toReal_of_uncurry` | `Measurable (Function.uncurry g)` → `Measurable (fun ω => (eLpNorm (g ω) 2 P).toReal)` | Top-σ-algebra version. Uses `Measurable.lintegral_prod_right'` on `‖g ω x‖ₑ^2`, rewrites `eLpNorm`, and post-processes with `.toReal`. | **Proved** |
| `Causalean.Mathlib.measurable_eLpNorm_two_toReal_of_uncurry_of_factor` | `Measurable[mΩ.prod σ_X] (Function.uncurry g)` → `Measurable[mΩ] (fun ω => (eLpNorm (g ω) 2 P).toReal)` | Sub-σ-algebra product version. The theorem requires joint measurability with respect to the sub-σ-algebra product; coordinate/cylinder measurability alone is not enough to invoke Mathlib's parametric lintegral measurability lemma. | **Proved** |

## 10d'. `Mathlib/IIDCenteredSum.lean` — Conditional 2nd-moment bound for centered i.i.d. sums

Upstream candidate.  Isolates the central probabilistic inequality used by the
fold-B empirical-process bound in `Stat/SampleSplit/FoldBEmpiricalProcess.lean`: for a
finite family of i.i.d. samples independent of a sub-σ-algebra `m_A`, the
expected square of the centered scaled sum is bounded by the expected
`L²(P)`-norm squared of the integrand.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `lintegral_ofReal_inv_sqrt_smul_sum_sq_le` (private) | `0 < s.card`, unscaled square `∫⁻(Σ Y_i)² ≤ s.card · ∫⁻ B` → scaled `∫⁻ ((1/√s.card)·Σ Y_i)² ≤ ∫⁻ B` | Deterministic algebraic step: the `(1/√s.card)²` prefactor cancels the `s.card` factor on the RHS via `lintegral_const_mul'` and `Real.sq_sqrt`. **Proved by Codex.** |
| `eLpNorm_two_sq_toReal_eq_integral_sq` (private) | `MemLp f 2 P` → `ENNReal.ofReal ((eLpNorm f 2 P).toReal ^ 2) = ENNReal.ofReal (∫ f^2 dP)` | Converts the real `L²` norm square back to the square integral, using `MemLp.eLpNorm_eq_integral_rpow_norm` and nonnegativity of `∫ f^2`. **Proved.** |
| `centered_sq_lintegral_le_eLpNorm_two_sq` (private) | `MemLp f 2 P` → `∫⁻ ofReal ((f - ∫f)^2) dP ≤ ofReal (‖f‖²₂)` | Diagonal variance bound: rewrites the centered square lintegral as `variance f P`, applies `variance_le_expectation_sq`, then uses `eLpNorm_two_sq_toReal_eq_integral_sq`. **Proved.** |
| `pi_centered_sum_sq_lintegral_le` (private) | `MemLp f 2 P` → under `Measure.pi (fun _ : s => P)`, `∫⁻ ofReal ((Σ_i (f(v_i)−∫f))²) ≤ |s|·ofReal(‖f‖²₂)` | Fixed-integrand finite-product estimate. Uses `variance_sum_pi` to eliminate cross terms, proves the centered coordinate mean is zero by `integral_map` along `measurePreserving_eval`, and bounds each variance by `variance_le_expectation_sq`. **Proved.** |
| `indep_trim_prod_map_eq` (private) | `Indep m_A (comap Z) μ`, `m_A ≤ mΩ`, measurable `Z` → `map (fun ω => (ω, Zω)) μ = (μ.trim hm_A_le).prod (map Z μ)` | Independent-joining bridge: converts sub-σ-algebra independence to `IndepFun id Z`, applies `indepFun_iff_map_prod_eq_prod_map_map`, and identifies the first marginal as the trimmed measure. **Proved.** |
| `iid_centered_sum_sq_lintegral_unscaled_le_of_joined_law` (private) | Joined product law `(ω, W_s ω) ∼ (μ.trim hm_A_le) × P^s` plus the public measurability/L² hypotheses → unscaled bound | Product-space form: rewrites the original lintegral through the joined law, applies Tonelli, uses `pi_centered_sum_sq_lintegral_le` pointwise in `ω`, pulls out `|s|`, and rewrites the trimmed lintegral back to `μ`. **Proved.** |
| `iid_centered_sum_sq_lintegral_unscaled_le` (private) | Same hypotheses as the public lemma but with conclusion `∫⁻ (Σ Y_i)² ≤ s.card · ∫⁻ ‖g ω‖²₂` (no `1/√s.card` factor) | Reduces the original hypotheses to the joined product law using `indep_trim_prod_map_eq` and `hW_iid_pi`, then calls `iid_centered_sum_sq_lintegral_unscaled_le_of_joined_law`. **Proved.** |
| `Causalean.Mathlib.iid_centered_sum_sq_lintegral_le` | `m_A ≤ mΩ`, `Indep m_A (comap (W on s)) μ`, `μ.map (W on s) = Measure.pi (fun _ => P)`, `Measurable[m_A.prod σ_X] (uncurry g)`, `∀ ω, MemLp (g ω) 2 P` → `∫⁻ ω, ENNReal.ofReal (((1/√s.card) * Σ_{i∈s} (g ω (W i ω) - ∫g ω dP))²) ∂μ ≤ ∫⁻ ω, ENNReal.ofReal (‖g ω‖²_{L²(P)}) ∂μ` | Public theorem. **Proved.** |

## 10e. `Mathlib/ConvergenceInDistribution.lean` — Scalar Slutsky / varying maps

Upstream candidates for convergence in distribution.  Contains no Causalean-specific
definitions; the project-level `Tendsto_dist` wrappers in `Stat/Limit/Convergence.lean`
consume these statements.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `MeasureTheory.tendstoInMeasure_const_of_tendsto_real` | `aᵢ → a₀` → constant random variables `ω ↦ aᵢ` converge in measure to `ω ↦ a₀` | Deterministic topological convergence as convergence in measure for constant real-valued random variables | **Proved** |
| `MeasureTheory.TendstoInDistribution.const_mul_of_tendsto_const` | `Xᵢ ⇒ Z` and `aᵢ → a₀` → `aᵢ·Xᵢ ⇒ a₀·Z` | Same-probability-space deterministic-scalar Slutsky theorem; proved by Mathlib's product Slutsky theorem `continuous_comp_prodMk_of_tendstoInMeasure_const` with multiplication as the continuous map | **Proved** |
| `MeasureTheory.ProbabilityMeasure.tendsto_map_mul_of_tendsto` | `νᵢ → ν` weakly and `aᵢ → a₀` → `(x ↦ aᵢx)_#νᵢ → (x ↦ a₀x)_#ν` | Measure-level deterministic-scalar Slutsky theorem for weak convergence on ℝ. Proved by rewriting `(x ↦ aᵢx)_#νᵢ` as the push-forward of `δ_{aᵢ} × νᵢ` by multiplication, then applying continuity of Dirac measures, product probability measures, and the ordinary continuous-mapping theorem | **Proved** |

## 10f. `Mathlib/FWL.lean` — Frisch–Waugh–Lovell on a finite-dim inner product space

Upstream candidate.  Self-contained algebraic content of the
Frisch–Waugh–Lovell theorem on a real inner-product space `V` with a
nuisance subspace `H` admitting an orthogonal projection.  Causal- and
panel-agnostic; depends only on Mathlib (`Submodule.orthogonalProjection`,
`Matrix.mulVec`, `Matrix.nonsing_inv`).  **Live consumer:**
`Panel/FWLInstanceL2.lean` (§9i0a-FWLInstanceL2)
instantiates this development on `Lp ℝ 2 μ` to give the conditional-mean
estimand papers a shared, measure-theory-native FWL engine.  (The finite
weighted-support tower `Weighted/FWL.lean` does **not** instantiate this file:
its inner product is only positive-semidefinite, so it carries its own FWL
built on `Mathlib/SemiInnerProjection`; the two are connected at the data level
via `Weighted/OfProbabilityMeasure.lean`.)  NL artifact:
`doc/basic_concepts/mathlib/fwl.md`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.FWL.residualize H v` | `Submodule ℝ V → V → V` | Orthogonal residual operator `M_H v = v - P_H v` as a self-map of `V`, defined via `Submodule.orthogonalProjectionFn` (which lands in `V`, not `↥H`) so the result is a `V`-vector. Requires `[H.HasOrthogonalProjection]`. | **Defined** |
| `Causalean.Mathlib.FWL.inner_residualize_of_mem` | `(v : V) {w : V} (hw : w ∈ H) → ⟪residualize H v, w⟫ = 0` | Residual orthogonality: `M_H v ⊥ H`. Direct unfolding of `Submodule.orthogonalProjectionFn_inner_eq_zero`. | **Proved** |
| `Causalean.Mathlib.FWL.residualize_inner_swap_right` | `(v w : V) → ⟪residualize H v, w⟫ = ⟪residualize H v, residualize H w⟫` | Symmetric residualization in the right slot. Splits `w = M_H w + P_H w` and uses orthogonality of the residual to `H`. The key identity used by FWL: residualizing only one side suffices. | **Proved** |
| `Causalean.Mathlib.FWL.gramResid H X` | `Submodule ℝ V → (Fin K → V) → Matrix (Fin K) (Fin K) ℝ` | Residualized Gram matrix `Q_{XX} j k := ⟪M_H (X j), M_H (X k)⟫`. | **Defined** |
| `Causalean.Mathlib.FWL.residInnerVec H X Y` | `… → V → (Fin K → ℝ)` | Residualized inner-product vector `r_j := ⟪M_H (X j), Y⟫`. | **Defined** |
| `Causalean.Mathlib.FWL.fwlCoef H X Y` | `… → V → (Fin K → ℝ)` | The FWL coefficient `θ̂ := Q_{XX}⁻¹ *ᵥ residInnerVec H X Y`. Defined unconditionally; only the normal-equations identity below needs invertibility of `Q_{XX}`. | **Defined** |
| `Causalean.Mathlib.FWL.gramResid_mulVec_fwlCoef` | `(Y : V) (hQ : IsUnit (gramResid H X).det) → (gramResid H X) *ᵥ fwlCoef H X Y = residInnerVec H X Y` | Residualized normal equations for the FWL coefficient. Pure matrix algebra: `A *ᵥ (A⁻¹ *ᵥ b) = b` via `Matrix.mulVec_mulVec`, `Matrix.mul_nonsing_inv`, `Matrix.one_mulVec`. | **Proved** |
| `Causalean.Mathlib.FWL.residInnerVec_eq_residualize_right` | `(Y : V) → residInnerVec H X Y = residInnerVec H X (residualize H Y)` | The FWL right-hand side is invariant under residualizing `Y`. Pointwise consequence of `residualize_inner_swap_right`. | **Proved** |
| `Causalean.Mathlib.FWL.fittedValue X β` | `(Fin K → V) → (Fin K → ℝ) → V` | Finite fitted value `∑ j, β j • X j`; helper lemmas record linearity under coefficient subtraction and commutation with residualization. | **Defined** |
| `Causalean.Mathlib.FWL.residualizedObjective H X Y β` | `… → ℝ` | Residualized least-squares objective `⟪M_H Y − ∑ j β_j • M_H X_j, M_H Y − ∑ j β_j • M_H X_j⟫`. | **Defined** |
| `Causalean.Mathlib.FWL.originalObjective X Y β h` | `… → V → ℝ` | Original least-squares objective with an explicit nuisance term `h`: `⟪Y − Xβ − h, Y − Xβ − h⟫`. | **Defined** |
| `Causalean.Mathlib.FWL.residualizedObjective_eq_fwlCoef_add_error` | `(hQ : IsUnit (gramResid H X).det) → residualizedObjective H X Y β = residualizedObjective H X Y (fwlCoef H X Y) + ⟪X̃(fwlCoef−β), X̃(fwlCoef−β)⟫` | Residualized completing-square identity. The nonnegative error term yields residualized minimization and, with Gram invertibility, coefficient uniqueness. | **Proved** |
| `Causalean.Mathlib.FWL.fwlCoef_residualized_minimizes` | `(hQ : IsUnit (gramResid H X).det) → residualizedObjective H X Y (fwlCoef H X Y) ≤ residualizedObjective H X Y β` | Completing-square theorem: the matrix-defined FWL coefficient minimizes the residualized least-squares objective. Uses the residualized normal equations to kill the cross term. | **Proved** |
| `Causalean.Mathlib.FWL.originalObjective_eq_residualizedObjective_add_projectionError` | `(hh : h ∈ H) → originalObjective X Y β h = residualizedObjective H X Y β + ⟪P_H (Y − Xβ) − h, P_H (Y − Xβ) − h⟫` | Pythagorean decomposition of the original objective into residualized loss plus squared nuisance-projection error. | **Proved** |
| `Causalean.Mathlib.FWL.fwlCoef_original_minimizes` | `(hQ) (hh : h ∈ H) → originalObjective X Y (fwlCoef H X Y) (P_H (Y − X fwlCoef)) ≤ originalObjective X Y β h` | Standard finite-dimensional FWL optimizer theorem: the residualized coefficient paired with the projection of its raw residual minimizes the original least-squares problem over coefficients and nuisance terms in `H`. | **Proved** |
| `Causalean.Mathlib.FWL.fwlCoef_eq_of_original_minimizer` | `original-objective minimizer hypothesis → β = fwlCoef H X Y` | β-block uniqueness theorem: any minimizer of the original least-squares problem has coefficient vector equal to the residualized FWL coefficient. | **Proved** |
| `Causalean.Mathlib.FWL.original_minimizer_eq_fwlCoef_projection` | `original-objective minimizer hypothesis → β = fwlCoef H X Y ∧ h = P_H (Y − X fwlCoef)` | Full standard FWL uniqueness characterization: every original-problem minimizer is exactly the residualized coefficient paired with the nuisance projection of its raw residual. | **Proved** |

## 10h. `Mathlib/TwoStateMarkov.lean` — two-state Markov transition matrix, stationary projection, geometric ergodicity

Project-generic shim (depends only on Mathlib; no causal/panel/Causalean
content) for the two-state Markov chain on `Fin 2` with rows

`(1 - a, a)` and `(b, 1 - b)`. Carries the two transition/stationary
matrix definitions and the substrate for long-T compact-uniform
convergence required by
`CausalSmith/Panel/Q1_SpectralThreshold/LongT.lean`: spectral
decomposition of the transition matrix plus uniform geometric
convergence to the stationary projection on compact subsets of the open
transition-parameter square `(0,1)²`. **Both definitions and all 4
theorems are in place; all theorems proved.**

This file consolidates the two definitions (previously local to
`CausalSmith/CausalSmith/Panel/Q1_SpectralThreshold/Basic.lean`) and the
four ergodicity theorems (previously in
`CausalSmith/CausalSmith/Mathlib/MarkovErgodicity.lean`, now deleted)
into a single neutral Causalean module. `Q1_SpectralThreshold/Basic.lean`
re-exports the two definitions under the short names so existing
downstream code is unchanged.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.TwoStateMarkov.transitionMatrix` | `(a b : ℝ) → Matrix (Fin 2) (Fin 2) ℝ` | The 2×2 row-stochastic transition matrix with rows `(1 - a, a)` and `(b, 1 - b)`. `noncomputable`. | **Defined.** |
| `Causalean.Mathlib.TwoStateMarkov.stationaryProjection` | `(a b : ℝ) → Matrix (Fin 2) (Fin 2) ℝ` | The rank-one projector whose two rows both equal the stationary distribution `(b/(a+b), a/(a+b))`. `noncomputable`. | **Defined.** |
| `Causalean.Mathlib.TwoStateMarkov.transitionMatrix_pow_eq_spectral` | `(a b : ℝ) (ha_pos : 0 < a) (ha_lt_one : a < 1) (hb_pos : 0 < b) (hb_lt_one : b < 1) → ∀ k, M^k = Π + (1 - a - b)^k • (1 - Π)` | Spectral decomposition: powers of the transition matrix split into the stationary projection plus a geometrically-decaying correction along `1 - Π`. **Proved.** |
| `Causalean.Mathlib.TwoStateMarkov.one_minus_a_b_abs_lt_one` | `(0 < a) (0 < b) (a < 1) (b < 1) → |1 - a - b| < 1` | Pointwise spectral gap on the open transition-parameter square. **Proved.** |
| `Causalean.Mathlib.TwoStateMarkov.one_minus_a_b_uniform_gap_on_compact` | `(K : Set (ℝ × ℝ)) (IsCompact K) (K ⊆ (0,1)²) → ∃ ρ < 1, ∀ p ∈ K, |1 - p.1 - p.2| ≤ ρ` | Uniform spectral gap on compact subsets via `IsCompact.exists_isMaxOn` + averaging the maximum with `1`. **Proved.** |
| `Causalean.Mathlib.TwoStateMarkov.transitionMatrix_pow_tendsto_stationary_uniform` | `(K compact ⊆ (0,1)²) → ∀ ε > 0, ∃ N, ∀ k ≥ N, ∀ p ∈ K, ∀ i j, |((M p.1 p.2)^k - Π p.1 p.2) i j| ≤ ε` | Uniform geometric convergence of `M^k` to the stationary projection on compact subsets, combining the spectral decomposition (entries of `1 - Π` bounded by `1`) with the uniform gap (geometric decay rate `ρ < 1`). **Proved.** |

## 10g. `Mathlib/SemiInnerProjection.lean` — semidefinite finite-dimensional projections

Upstream candidate for finite-dimensional projection under a symmetric
positive-semidefinite bilinear form.  This is the generic support-quotient
architecture consumed by `Panel/Subspace.lean`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.exists_orthogonalProjection_of_posSemidef` | finite-dimensional `V`, bilinear form `B`, symmetry, `∀ x, 0 ≤ B x x`, submodule `H` → `∃ P : V →ₗ[ℝ] V, P X ∈ H ∧ B (X - P X) h = 0` | Generic semidefinite projection existence. Proof uses the restricted Gram map `H →ₗ Dual ℝ H`, shows each right-hand side lies in its range via the PSD radical fact and dual-annihilator/range duality, then chooses a linear right inverse of the range restriction. | **Proved.** |

---

## 10i. `Mathlib/StandardGaussian.lean` — standard Gaussian on a finite-dim inner-product space

Constructs the standard Gaussian measure on an arbitrary finite-dimensional
real inner-product space `E` (mean zero, covariance = inner product) and proves
its basic moments. Construction: take `b := stdOrthonormalBasis ℝ E`, build the
product of `n = finrank ℝ E` standard real Gaussians on `Fin n → ℝ`, transport
to `EuclideanSpace ℝ (Fin n)` via `toLp`, then pull back to `E` along the
isometry `b.repr.symm`. Upstream candidate.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Causalean.Mathlib.stdGaussian` | `[FiniteDimensional ℝ E] [BorelSpace E] → Measure E` | The standard Gaussian measure on `E`: `(prod of gaussianReal 0 1).map toLp` pulled back along `b.repr.symm`. | **Proved.** |
| `Causalean.Mathlib.isProbabilityMeasure_stdGaussian` | `IsProbabilityMeasure (stdGaussian E)` | Probability measure (pushforward of a `Measure.pi` of probability measures). | **Proved.** |
| `Causalean.Mathlib.isGaussian_stdGaussian` | `IsGaussian (stdGaussian E)` | Gaussian law; via `iIndepFun.hasGaussianLaw` for the product, then `isGaussian_map_equiv`. | **Proved.** |
| `Causalean.Mathlib.stdGaussian_mean` | `∫ x, x ∂(stdGaussian E) = 0` | Mean is zero; each coordinate `gaussianReal 0 1` has mean 0, pushed through the continuous-linear maps. | **Proved.** |
| `Causalean.Mathlib.covarianceBilin_stdGaussian` | `covarianceBilin (stdGaussian E) u v = ⟪u, v⟫_ℝ` | Covariance bilinear form equals the inner product. Via `covarianceBilin_map` (adjoint of isometry = inverse), `covarianceBilin_apply_pi`, coordinate covariances (Kronecker delta from independence + unit variance), and `OrthonormalBasis.repr` isometry. | **Proved.** |
| `Causalean.Mathlib.stdGaussian_map_normSq_eq_pi` | `(stdGaussian E).map ‖·‖² = (Measure.pi (fun _ : Fin (finrank ℝ E) => gaussianReal 0 1)).map (∑ (w i)²)` | Exposes the product-of-1-D-Gaussians structure of the squared-norm law (rotation-invariance of `‖·‖²` strips the orthonormal-basis transport), used for χ² atomlessness. | **Proved.** |

---

## 10j. `Mathlib/OperatorSqrt.lean` — positive square root of a positive operator

Builds the **operator square root** of a positive symmetric operator on a
finite-dimensional real inner-product space — a result Mathlib lacks (it has the
spectral theorem and `LinearMap.IsPositive`, but no operator sqrt). Construction:
in the orthonormal eigenbasis `b` with real eigenvalues `μ` (both from
`LinearMap.IsSymmetric.eigenvectorBasis`/`eigenvalues`), `posSqrt` acts as
`√(μ i)` on each eigenvector (via `Basis.constr`). Upstream candidate. All in
namespace `LinearMap.IsPositive` (with `hT : T.IsPositive`).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `eigBasis` / `eigVals` | `OrthonormalBasis (Fin (finrank ℝ E)) ℝ E` / `Fin _ → ℝ` | Orthonormal eigenbasis and real eigenvalues of the positive operator `T`. | **Proved.** |
| `eigVals_nonneg` | `0 ≤ hT.eigVals i` | Eigenvalues of a positive operator are nonnegative (`μ i = ⟪T (b i), b i⟫ ≥ 0`). | **Proved.** |
| `posSqrt` | `E →ₗ[ℝ] E` | The square root: `posSqrt (b i) = √(μ i) • b i`, extended by `Basis.constr`. | **Proved.** |
| `posSqrt_mul_self` | `hT.posSqrt ∘ₗ hT.posSqrt = T` | Square law, via `Real.mul_self_sqrt` and `Basis.ext`. | **Proved.** |
| `posSqrt_inner` | `⟪posSqrt x, y⟫ = ∑ i, √(μ i) ⟪b i,x⟫ ⟪b i,y⟫` | Symmetric weighted-sum form of the quadratic data. | **Proved.** |
| `posSqrt_isSymmetric` / `posSqrt_isPositive` | `IsSymmetric` / `IsPositive` | `posSqrt` is itself symmetric and positive. | **Proved.** |
| `posSqrtCLM` | `E →L[ℝ] E` | `posSqrt` packaged as a continuous linear map. | **Proved.** |
| `posSqrtCLM_isSelfAdjoint` / `posSqrtCLM_adjoint` | `IsSelfAdjoint posSqrtCLM` / `adjoint posSqrtCLM = posSqrtCLM` | Self-adjointness of the continuous square root. | **Proved.** |
| `posSqrtCLM_comp_self` | `posSqrtCLM ∘L posSqrtCLM = T.toContinuousLinearMap` | Continuous square law. | **Proved.** |

---

## 10j'. `Mathlib/Optimization/RationalLP.lean` — exact finite rational linear programming

Certified Fourier–Motzkin elimination for finite rational linear programs,
including rational primal attainment and exact primal–dual certificates.

<!-- GEN:Causalean.Mathlib.Optimization.RationalLP -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Optimization.RationalLP.dot` | `(Fin n → ℚ) → (Fin n → ℚ) → ℚ` | Given a common finite number of coordinates and two rational coordinate vectors, the rational dot product is the sum, over all coordinates, of the product of their corresponding entries. |
| `Mathlib.Optimization.RationalLP.AugmentedIneq` | `ℕ → Type` | A linear inequality in ordinary variables `x` and a retained objective coordinate `t`, written `a · x + s * t ≤ b`. |
| `Mathlib.Optimization.RationalLP.AugmentedIneq.Holds` | `(Fin n → ℚ) → ℚ → Prop` | Given a number of ordinary coordinates, an augmented rational inequality, a rational ordinary-variable vector, and a retained rational objective coordinate, the inequality-satisfaction condition holds exactly when the dot product of the ordinary coefficients and vector plus the objective coefficient times the retained coordinate is no greater than the right-hand side. |
| `Mathlib.Optimization.RationalLP.AugmentedIneq.tail` | `Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq n` | Given an augmented rational inequality with one more ordinary coordinate, the tail inequality removes its first ordinary coefficient while retaining its objective coefficient and right-hand side. |
| `Mathlib.Optimization.RationalLP.AugmentedIneq.cancel` | `Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq (n + 1) → Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq n` | Given two augmented rational inequalities with one more ordinary coordinate, the cancellation inequality has each remaining coefficient, its objective coefficient, and its right-hand side equal to the negative first coefficient of the second inequality times the corresponding quantity of the first, plus the first coefficient of the first inequality times the corresponding quantity of the second. |
| `Mathlib.Optimization.RationalLP.eliminateOne` | `List (Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq (n + 1)) → List (Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq n)` | Given a list of augmented rational inequalities with one more ordinary coordinate, the one-step Fourier--Motzkin elimination output contains the tails of all rows whose first coefficient is zero and, for every row with negative first coefficient and every row with positive first coefficient, their cancellation inequality. |
| `Mathlib.Optimization.RationalLP.eliminateOne_iff` | `(∃ z, ∀ r ∈ rows, r.Holds (Fin.cons z x) t) ↔ ∀ r ∈ Causalean.Mathlib.Optimization.RationalLP.eliminateOne rows, r.Holds x t` | One Fourier–Motzkin elimination step preserves exactly the feasible values of the retained objective coordinate. |
| `Mathlib.Optimization.RationalLP.eliminateAll` | `(n : ℕ) → List (Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq n) → List (Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq 0)` | For a nonnegative number of ordinary coordinates and a list of augmented rational inequalities in that many coordinates, the complete Fourier--Motzkin elimination output is the original list when there are no ordinary coordinates and otherwise is obtained by one first-coordinate elimination followed by complete elimination of the remaining coordinates. |
| `Mathlib.Optimization.RationalLP.eliminateAll_iff` | `(∃ x, ∀ r ∈ rows, r.Holds x t) ↔ ∀ r ∈ Causalean.Mathlib.Optimization.RationalLP.eliminateAll n rows, r.Holds Fin.elim0 t` | Eliminating all ordinary variables preserves exactly the feasible values of the retained objective coordinate. |
| `Mathlib.Optimization.RationalLP.Program` | `Type u_1 → ℕ → Type u_1` | A finite rational LP in inequality form: minimize `c · x` subject to `A i · x ≤ b i`. |
| `Mathlib.Optimization.RationalLP.Program.PrimalFeasible` | `(Fin n → ℚ) → Prop` | Given a constraint-label set, a number of ordinary variables, a rational linear program, and a rational vector of those variables, the primal-feasibility condition holds exactly when, for every constraint, the dot product of its coefficient row with the vector is no greater than its right-hand side. |
| `Mathlib.Optimization.RationalLP.Program.objective` | `(Fin n → ℚ) → ℚ` | Given a constraint-label set, a number of ordinary variables, a rational linear program, and a rational vector of those variables, the primal objective value is the dot product of the program's objective coefficient vector with that variable vector. |
| `Mathlib.Optimization.RationalLP.exists_rational_primal_optimizer` | `(∃ x, P.PrimalFeasible x) → (∃ l, ∀ (x : Fin n → ℚ), P.PrimalFeasible x → l ≤ P.objective x) → ∃ xStar, P.PrimalFeasible xStar ∧ ∀ (x : Fin n → ℚ), P.PrimalFeasible x → P.objective xStar ≤ P.objective x` | If a finite rational linear program has a feasible rational point and its objective is bounded below on feasible rational points, then it has a rational primal optimizer. |
| `Mathlib.Optimization.RationalLP.CertifiedIneq` | `(κ → Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq N) → ((Fin N → ℚ) → Fin n → ℚ) → Type u_2` | An augmented inequality together with nonnegative rational provenance from a fixed finite family of original rows. The coefficient identity is stated through a common projection of the original variables; this makes repeated elimination independent of coordinate-reassociation bookkeeping. |
| `Mathlib.Optimization.RationalLP.FinalCertifiedIneq` | `(κ → Causalean.Mathlib.Optimization.RationalLP.AugmentedIneq N) → Type u_2` | A fully eliminated inequality with nonnegative provenance on the original rows and with every original-variable coefficient certified to cancel. |
| `Mathlib.Optimization.RationalLP.Program.DualFeasible` | `(ι → ℚ) → Prop` | Given a finite set of constraint labels and number of ordinary variables, a finite rational linear program, and rational multipliers for its constraints, the dual-feasibility condition holds exactly when every multiplier is nonnegative and, for every ordinary coordinate, the multiplier-weighted sum of constraint coefficients equals the negative objective coefficient. |
| `Mathlib.Optimization.RationalLP.Program.dualObjective` | `(ι → ℚ) → ℚ` | Given a finite set of constraint labels and number of ordinary variables, a finite rational linear program, and rational constraint multipliers, the dual objective value is the negative of the multiplier-weighted sum of the constraint right-hand sides. |
| `Mathlib.Optimization.RationalLP.exists_rational_optimal_primal_dual` | `(∃ x, P.PrimalFeasible x) → (∃ l, ∀ (x : Fin n → ℚ), P.PrimalFeasible x → l ≤ P.objective x) → ∃ xStar yStar, P.PrimalFeasible xStar ∧ P.DualFeasible yStar ∧ P.objective xStar = P.dualObjective yStar ∧ ∀ (x : Fin n → ℚ), P.PrimalFeasible x → P.objective xStar ≤ P.objective x` | Exact rational LP attainment and strong duality. If a finite rational linear program in inequality form has a feasible rational point and its objective is bounded below on feasible rational points, then it has a rational optimal primal point and rational nonnegative dual multipliers with the same objective value. |
<!-- /GEN -->

## 10j''. `Mathlib/Topology/SubsequentialLimits.lean` — compact subsequential-limit sets

General topology bridges between strictly increasing subsequences and mapped
cluster points, with a compactness theorem for eventually compact-valued
sequences.

<!-- GEN:Causalean.Mathlib.Topology.SubsequentialLimits -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Topology.isSubsequentialLimit_iff_mapClusterPt` | `(∃ φ, StrictMono φ ∧ Filter.Tendsto (x ∘ φ) Filter.atTop (nhds a)) ↔ MapClusterPt a Filter.atTop x` | For a sequence in a first-countable space, a point is the limit along a strictly increasing subsequence exactly when it is a mapped cluster point at infinity. |
| `Mathlib.Topology.subsequentialLimitSet_nonempty_compact_of_eventually_mem_compact` | `IsCompact s → (∀ᶠ (n : ℕ) in Filter.atTop, x n ∈ s) → (∃ y φ, StrictMono φ ∧ Filter.Tendsto (x ∘ φ) Filter.atTop (nhds y)) ∧ IsCompact {y \| ∃ φ, StrictMono φ ∧ Filter.Tendsto (x ∘ φ) Filter.atTop (nhds y)}` | If a sequence is eventually contained in a compact set, then its set of limits along strictly increasing subsequences is nonempty and compact. |
<!-- /GEN -->

## 10j. `Mathlib/LinearAlgebra/` — finite Vandermonde systems

Reusable nonsingularity, synthesis-dimension, and block-injectivity certificates
for ordinary and confluent Vandermonde systems.

<!-- GEN:Causalean.Mathlib.LinearAlgebra.ConfluentVandermonde -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.LinearAlgebra.doubledExponent` | `Fin n ⊕ Fin n → ℕ` | Given a nonnegative integer \(n\), the doubled exponent assigns exponent \(i\) to the \(i\)-th index in the first block of \(n\) indices and exponent \(n+i\) to the \(i\)-th index in the second block. The first-block assignment and the second-block assignment together define the encoding. |
| `Mathlib.LinearAlgebra.doubledExponent_injective` | `Function.Injective Causalean.Mathlib.LinearAlgebra.doubledExponent` | The doubled exponent encoding is injective. |
| `Mathlib.LinearAlgebra.doubledExponent_lt` | `Causalean.Mathlib.LinearAlgebra.doubledExponent i < 2 * n` | Every doubled exponent is strictly below `2n`. |
| `Mathlib.LinearAlgebra.doubledCoefficientPolynomial` | `(Fin n ⊕ Fin n → K) → Polynomial K` | Given a nonnegative integer \(n\), a semiring of coefficients, and a coefficient vector indexed by two blocks of \(n\) positions, the doubled coefficient polynomial is the polynomial whose coefficient at each encoded exponent is the corresponding entry of that vector. |
| `Mathlib.LinearAlgebra.coeff_doubledCoefficientPolynomial` | `(Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v).coeff (Causalean.Mathlib.LinearAlgebra.doubledExponent i) = v i` | Reading a doubled coefficient polynomial at an encoded exponent recovers the corresponding coefficient. |
| `Mathlib.LinearAlgebra.doubledCoefficientPolynomial_eq_zero_iff` | `Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v = 0 ↔ v = 0` | A doubled coefficient polynomial vanishes exactly when its coefficient vector vanishes. |
| `Mathlib.LinearAlgebra.natDegree_doubledCoefficientPolynomial_lt` | `1 ≤ n → ∀ {v : Fin n ⊕ Fin n → K}, (Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v).natDegree < 2 * n` | A doubled coefficient polynomial has degree below `2n` when `n` is positive. |
| `Mathlib.LinearAlgebra.confluentVandermonde` | `(Fin n → K) → Matrix (Fin n ⊕ Fin n) (Fin n ⊕ Fin n) K` | Given a nonnegative integer \(n\), a semiring of coefficients, and \(n\) node values, the confluent Vandermonde matrix is the square matrix with rows indexed by monomial degrees from zero through \(2n-1\), value columns at every node, and first-derivative columns at every node. |
| `Mathlib.LinearAlgebra.eval_doubledCoefficientPolynomial` | `Polynomial.eval x (Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v) = ∑ a, x ^ Causalean.Mathlib.LinearAlgebra.doubledExponent a * v a` | Evaluating a polynomial whose coefficients are indexed by two blocks of monomial powers gives the finite sum of those coefficients weighted by the corresponding powers of the evaluation point. |
| `Mathlib.LinearAlgebra.eval_derivative_doubledCoefficientPolynomial` | `Polynomial.eval x (Polynomial.derivative (Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v)) = ∑ a, ↑(Causalean.Mathlib.LinearAlgebra.doubledExponent a) * x ^ (Causalean.Mathlib.LinearAlgebra.doubledExponent a - 1) * v a` | Evaluating the derivative of a polynomial whose coefficients are indexed by two blocks of monomial powers gives the finite sum of coefficients weighted by their monomial exponents and the corresponding reduced powers. |
| `Mathlib.LinearAlgebra.doubledCoefficientPolynomial_eq_zero_of_eval_derivative` | `1 ≤ n → ∀ (s : Fin n → K), Function.Injective s → ∀ (v : Fin n ⊕ Fin n → K), (∀ (i : Fin n), Polynomial.eval (s i) (Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v) = 0) → (∀ (i : Fin n), Polynomial.eval (s i) (Polynomial.derivative (Causalean.Mathlib.LinearAlgebra.doubledCoefficientPolynomial v)) = 0) → v = 0` | A polynomial of degree below `2n` whose value and derivative vanish at `n` distinct points is zero. |
| `Mathlib.LinearAlgebra.det_confluentVandermonde_ne_zero` | `1 ≤ n → ∀ (s : Fin n → K), Function.Injective s → (Causalean.Mathlib.LinearAlgebra.confluentVandermonde s).det ≠ 0` | For a positive number of nodes `n` (`1 ≤ n`), a field `K`, and pairwise distinct nodes `s : Fin n → K`, the confluent Vandermonde determinant at `s` is nonzero. |
| `Mathlib.LinearAlgebra.pinnedExponent` | `Fin n ⊕ Fin (n - 1) → ℕ` | Given a nonnegative integer \(n\), the pinned exponent assigns exponent \(i\) to the \(i\)-th index in its block of \(n\) simple-node positions and exponent \(n+i\) to the \(i\)-th index in its block of \(n-1\) derivative positions. The simple-node assignment and the derivative-position assignment together define the encoding. |
| `Mathlib.LinearAlgebra.pinnedSucc` | `Fin (n - 1) → Fin n` | Given a nonnegative integer \(n\) and an index \(i<n-1\), the pinned successor index is the index \(i+1<n\). |
| `Mathlib.LinearAlgebra.pinnedSucc_injective` | `Function.Injective Causalean.Mathlib.LinearAlgebra.pinnedSucc` | The positive-node embedding is injective. |
| `Mathlib.LinearAlgebra.pinnedSucc_ne_zero` | `Causalean.Mathlib.LinearAlgebra.pinnedSucc i ≠ ⟨0, hn⟩` | A positive-node index never equals the distinguished zero node. |
| `Mathlib.LinearAlgebra.pinnedExponent_injective` | `Function.Injective Causalean.Mathlib.LinearAlgebra.pinnedExponent` | The pinned exponent encoding is injective. |
| `Mathlib.LinearAlgebra.pinnedExponent_lt` | `Causalean.Mathlib.LinearAlgebra.pinnedExponent i < 2 * n - 1` | Every pinned exponent is strictly below `2n - 1`. |
| `Mathlib.LinearAlgebra.pinnedCoefficientPolynomial` | `(Fin n ⊕ Fin (n - 1) → K) → Polynomial K` | Given a nonnegative integer \(n\), a semiring of coefficients, and a coefficient vector indexed by \(n\) value positions and \(n-1\) derivative positions, the pinned coefficient polynomial is the polynomial whose coefficient at each pinned exponent is the corresponding vector entry. |
| `Mathlib.LinearAlgebra.coeff_pinnedCoefficientPolynomial` | `(Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v).coeff (Causalean.Mathlib.LinearAlgebra.pinnedExponent i) = v i` | Reading a pinned coefficient polynomial at an encoded exponent recovers the corresponding coefficient. |
| `Mathlib.LinearAlgebra.pinnedCoefficientPolynomial_eq_zero_iff` | `Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v = 0 ↔ v = 0` | A pinned coefficient polynomial vanishes exactly when its coefficient vector vanishes. |
| `Mathlib.LinearAlgebra.natDegree_pinnedCoefficientPolynomial_lt` | `1 ≤ n → ∀ {v : Fin n ⊕ Fin (n - 1) → K}, (Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v).natDegree < 2 * n - 1` | A pinned coefficient polynomial has degree below `2n - 1`. |
| `Mathlib.LinearAlgebra.pinnedConfluentVandermonde` | `(Fin n → K) → Matrix (Fin n ⊕ Fin (n - 1)) (Fin n ⊕ Fin (n - 1)) K` | Given a nonnegative integer \(n\), a semiring of coefficients, and \(n\) node values, the pinned confluent Vandermonde matrix records values at all nodes and first derivatives at nodes one through \(n-1\), omitting the derivative at node zero. |
| `Mathlib.LinearAlgebra.eval_pinnedCoefficientPolynomial` | `Polynomial.eval x (Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v) = ∑ a, x ^ Causalean.Mathlib.LinearAlgebra.pinnedExponent a * v a` | Evaluating a pinned coefficient polynomial at a point equals the sum of its coefficients, each weighted by that point raised to the coefficient's associated pinned exponent. |
| `Mathlib.LinearAlgebra.eval_derivative_pinnedCoefficientPolynomial` | `Polynomial.eval x (Polynomial.derivative (Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v)) = ∑ a, ↑(Causalean.Mathlib.LinearAlgebra.pinnedExponent a) * x ^ (Causalean.Mathlib.LinearAlgebra.pinnedExponent a - 1) * v a` | Evaluating the derivative of a pinned coefficient polynomial at a point equals the sum of its coefficients weighted by the corresponding derivative monomial values at that point. |
| `Mathlib.LinearAlgebra.pinnedCoefficientPolynomial_eq_zero_of_eval_derivative` | `1 ≤ n → ∀ (s : Fin n → K), Function.Injective s → ∀ (v : Fin n ⊕ Fin (n - 1) → K), (∀ (i : Fin n), Polynomial.eval (s i) (Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v) = 0) → (∀ (i : Fin (n - 1)), Polynomial.eval (s (Causalean.Mathlib.LinearAlgebra.pinnedSucc i)) (Polynomial.derivative (Causalean.Mathlib.LinearAlgebra.pinnedCoefficientPolynomial v)) = 0) → v = 0` | A polynomial in the pinned coefficient model is zero when it vanishes at all nodes and its derivative vanishes at every nondistinguished node. |
| `Mathlib.LinearAlgebra.det_pinnedConfluentVandermonde_ne_zero` | `1 ≤ n → ∀ (s : Fin n → K), Function.Injective s → (Causalean.Mathlib.LinearAlgebra.pinnedConfluentVandermonde s).det ≠ 0` | For a positive number of nodes `n` (`1 ≤ n`), a field `K`, and pairwise distinct nodes `s : Fin n → K`, the pinned confluent Vandermonde determinant at `s` is nonzero: one node contributes only value evaluation and every other node contributes both value and first-derivative evaluation. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.LinearAlgebra.VandermondeSynthesis -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.LinearAlgebra.endpointOrderSynthesis` | `(Fin n → K) → (r : ℕ) → (Fin (n + 1) → K) →ₗ[K] Fin (r + 1) → K` | Given a nonnegative integer \(n\), a commutative semiring of coefficients, \(n\) node values, and a nonnegative integer \(r\), the endpoint-order synthesis map sends \(n+1\) coefficients to the first \(r+1\) moment sums of the node coefficients, with the terminal coefficient added only to the moment of order \(r\). |
| `Mathlib.LinearAlgebra.endpointOrderSynthesis_single` | `(Causalean.Mathlib.LinearAlgebra.endpointOrderSynthesis s r) (Pi.single j.castSucc 1) = fun a => s j ^ ↑a` | Applying the moment-synthesis map to the standard basis vector for a node produces the vector of that node's powers across the requested moment orders. |
| `Mathlib.LinearAlgebra.endpointOrderSynthesis_surjective` | `Function.Injective s → r < n → Function.Surjective ⇑(Causalean.Mathlib.LinearAlgebra.endpointOrderSynthesis s r)` | With distinct node values, the finite moment-synthesis map can reproduce every target vector of moments up through an order smaller than the number of nodes. |
| `Mathlib.LinearAlgebra.endpointOrderSynthesis_injective` | `Function.Injective s → n ≤ r → Function.Injective ⇑(Causalean.Mathlib.LinearAlgebra.endpointOrderSynthesis s r)` | With distinct node values, moments through an order at least as large as the number of nodes uniquely determine every coordinate supplied to the moment-synthesis map. |
| `Mathlib.LinearAlgebra.endpointOrderSynthesis_ker_finrank` | `Function.Injective s → Module.finrank K ↥(Causalean.Mathlib.LinearAlgebra.endpointOrderSynthesis s r).ker = n - r` | For natural numbers `n`, `r`, a field `K`, and pairwise distinct nodes `s : Fin n → K`, the dimension of the kernel of the order-`r` endpoint synthesis map equals `n - r`: below the square threshold (`r < n`) the kernel has dimension `n - r`, and at or above it (`r ≥ n`) the kernel is zero, matching `n - r = 0` under truncated subtraction. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.LinearAlgebra.StackedVandermonde -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.LinearAlgebra.affineBinaryPower` | `K → (k : ℕ) → Fin (k + 1) → K` | Given an element \(z\) of a multiplicative monoid and a nonnegative integer \(k\), the affine power vector is the vector indexed by the integers from zero through \(k\) whose entry at \(r\) is \(z^r\). |
| `Mathlib.LinearAlgebra.stackedContraction` | `(N : ℕ) → (Fin (N + 1) → K) → (Fin (N + 1) → Fin N → K) → (Fin (N + 1) → K) → (k : Fin N) → Fin (↑k + 1) → K` | Given a nonnegative integer \(N\), \(N+1\) slope values, a weight assigned to each slope and each of the \(N\) blocks, and a coefficient attached to each slope, the stacked contraction maps a block index \(k<N\) and an exponent \(r\leq k\) to the sum, over all slopes, of the weight times the coefficient times the \(r\)-th power of that slope. |
| `Mathlib.LinearAlgebra.blockVandermondeWitnessWeights` | `(N : ℕ) → 1 ≤ N → Fin (N + 1) → Fin N → K` | Given a positive integer \(N\), the block-Vandermonde witness weights assign weight one only to index zero in block zero and to every nonzero index in block \(N-1\), and assign weight zero in all other cases. The first block is chosen as block zero, the second as block \(N-1\), and the stated case assignment defines the weights. |
| `Mathlib.LinearAlgebra.stacked_contraction_injective_of_generic_weights` | `2 ≤ N → ∀ (slopes : Fin (N + 1) → K), Function.Injective slopes → ∃ weights, Function.Injective (Causalean.Mathlib.LinearAlgebra.stackedContraction N slopes weights)` | For a commutative integral domain `K`, at least three indices `N + 1` (`2 ≤ N`), and pairwise distinct slope values `slopes : Fin (N + 1) → K`, there exist weights `Fin (N + 1) → Fin N → K` for which the stacked contraction is injective. The first block detects coordinate `0`, while the last block is a square Vandermonde system on coordinates `1,…,N`. |
<!-- /GEN -->

## 10j'. `Mathlib/LinearAlgebra/Cholesky.lean` — real Cholesky factorization (existence + uniqueness)

The **Cholesky factorization** of a real symmetric positive-definite matrix `M : Matrix (Fin d) (Fin d) ℝ`: there is a unique upper-triangular matrix `U` with strictly positive diagonal such that `M = Uᵀ U` (here "upper-triangular" means `j < i → U i j = 0`, all strictly-below-diagonal entries vanish). Existence is derived from Mathlib's LDL decomposition (`LDL.lower_conj_diag`) by absorbing the square roots of the diagonal factor: `U = √D · Lᵀ` where `L = LDL.lower` and `D = LDL.diag`. Uniqueness reduces to the fact that an orthogonal upper-triangular matrix with positive diagonal is the identity. Mathlib has LDL but no Cholesky; upstream candidate. Built for the causal-disentanglement formalization. Namespace `Causalean.Mathlib.LinearAlgebra`. Sorry-free (axioms: `propext`, `Classical.choice`, `Quot.sound`).

| Declaration | Statement | Description | Status |
|---|---|---|---|
| `IsUpperTri` / `isUpperTri_iff_blockTriangular` | `∀ i j, j < i → U i j = 0` / `↔ U.BlockTriangular id` | Strict upper-triangularity predicate and its identification with Mathlib's `BlockTriangular id`. | **Proved.** |
| `ldl_lowerInv_diag_one` | `LDL.lowerInv hM i i = 1` | The Gram-Schmidt lower-inverse matrix is unitriangular (diagonal entries are `1`). | **Proved.** |
| `ldl_diagEntries_pos` | `0 < LDL.diagEntries hM i` | The LDL diagonal entries of a positive-definite matrix are strictly positive (each is `xᵀMx > 0` for the nonzero `x = lowerInv` row). | **Proved.** |
| `ldl_lowerInv_blockTriangular` / `ldl_lower_blockTriangular` | `(…).BlockTriangular toDual` | `LDL.lowerInv` and `LDL.lower` are lower-triangular. | **Proved.** |
| `ldl_lower_diag_one` | `LDL.lower hM i i = 1` | `LDL.lower` is unitriangular (inverse of a unitriangular lower-triangular matrix). | **Proved.** |
| `cholesky_exists` | `∃ U, (∀ i j, j<i → U i j = 0) ∧ (∀ i, 0 < U i i) ∧ M = Uᵀ U` | **Existence.** Every real positive-definite matrix factors as `Uᵀ U` with `U` upper-triangular, positive-diagonal. | **Proved.** |
| `orthogonal_upperTri_pos_diag_eq_one` | `Wᵀ W = 1 → upper-tri → pos-diag → W = 1` | **Key uniqueness lemma.** An orthogonal upper-triangular matrix with positive diagonal is the identity (its transpose is both upper and lower triangular, hence diagonal, hence `±1`, hence `1`). | **Proved.** |
| `cholesky_unique` | `M = Uᵀ U = Vᵀ V → (both upper-tri pos-diag) → U = V` | **Uniqueness.** The factorization is unique: `W = V U⁻¹` is orthogonal upper-triangular with positive diagonal, so `W = 1`. | **Proved.** |

---

## 10k. `Mathlib/MeasureTheory/PartitionIntegral.lean` — finite-partition integral algebra

Decomposing an integral over the fibers of a finite-valued measurable map `H : Ω → ι` (a finite partition of the sample space). Causal-agnostic, Mathlib-shaped measure theory (a Mathlib-PR candidate); relocated here from `Stat/` so any layer (estimation, panel cell algebra, partial-ID) can use it without pulling in `Stat/` or `PO/`. Complements `Causalean.PO.integral_eq_sum_measure_mul_eventCondExp` (the `eventCondExp`/measure-weighted form for an explicit set family): here the form is measurable-map / set-integral, plus the cell-constant pull-out the `PO` lemma does not provide. Index type carries `[Fintype ι] [MeasurableSpace ι] [MeasurableSingletonClass ι]`; no measurability of the cell-weight `c : ι → ℝ` is needed (it is constant on each fiber). Namespace `Causalean.Mathlib.MeasureTheory`.

| Declaration | Statement | Description |
|---|---|---|
| `integral_eq_sum_setIntegral_fiber` | `∫ f = ∑ h, ∫_{H⁻¹{h}} f` | **Fiber decomposition.** For integrable `f` and finite-valued measurable `H`, the integral splits as the sum of set-integrals over the fibers. Wraps `integral_iUnion_fintype` over the fiber cover of `univ`. **Proved.** |
| `integral_cellConst_mul` | `∫ c(H ω)·f ω = ∑ h, c h · ∫_{H⁻¹{h}} f` | **Cell-constant pull-out.** An integrand weighted by a cell-dependent value `c (H ω)` decomposes with `c h` pulled out per cell. Workhorse for panel cell-by-cell regression algebra and cell-weighted-average estimands. No measurability of `c` required. **Proved.** |
| `integral_cellConst` | `∫ c(H ω) = ∑ h, c h · μ(H⁻¹{h})` | **Cell-weight aggregation.** For a finite measure, the integral of a cell-constant function is the cell-weighted sum of cell masses. Special case of `integral_cellConst_mul` with `f ≡ 1`. **Proved.** |

## 10k'. `Mathlib/MeasureTheory/CondExpIndicatorTower.lean` — conditioning ↔ inner-regression weighting

Causal-agnostic conditional-expectation identity (a Mathlib-PR candidate; namespace `MeasureTheory`): the measure-theoretic kernel behind "regression adjustment = inverse-propensity weighting". Reused by `regression_adjustment` (§9g) and earmarked for the ATT/DTR sequential analogs.

| Declaration | Statement | Description |
|---|---|---|
| `condExp_setIndicator_condExp_of_le` | `(hm : m ≤ m') (hm' : m' ≤ m0) [SigmaFinite (μ.trim hm')] (hs : MeasurableSet[m'] s) (hf : Integrable f μ) → μ[s.indicator f \| m] =ᵐ μ[s.indicator (μ[f\|m']) \| m]` | **Masked outcome ↔ inner regression.** Conditioning the masked `1_s·f` on coarse `m` equals conditioning its inner regression `μ[f\|m']` (with `s` `m'`-measurable). Proof: inner `condExp_indicator` + tower `condExp_condExp_of_le`. With `m=σ(X)`, `m'=σ(D,X)`, `s={D=d}`, dividing by `μ[1_s\|m]` turns the LHS into the IPW/adjustment functional, the RHS into the regression. **Proved.** |

## 10k'a. `Mathlib/MeasureTheory/CondExpPreimage.lean` — conditional expectation from design events

The standard conditional-expectation uniqueness theorem in the form used by random-design
arguments: a candidate regression is identified once its integrals agree with the outcome on
every measurable event generated by the design.

<!-- GEN:Causalean.Mathlib.MeasureTheory.CondExpPreimage -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.MeasureTheory.condExp_eq_of_integral_preimage_eq` | `Measurable design → ∀ (Y m : Omega → ℝ), MeasureTheory.Integrable Y mu → MeasureTheory.Integrable m mu → MeasureTheory.AEStronglyMeasurable m mu → (∀ (S : Set D), MeasurableSet S → ∫ (omega : Omega) in design ⁻¹' S, Y omega ∂mu = ∫ (omega : Omega) in design ⁻¹' S, m omega ∂mu) → mu[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[mu] m` | For a finite sampling measure, a measurable design map, an integrable outcome and candidate regression, a candidate regression measurable with respect to the design σ-algebra, and matching outcome and candidate-regression integrals on every measurable design event, the candidate regression is a version of the outcome's conditional expectation given the design. |
<!-- /GEN -->

## 10k''. `Mathlib/MeasureTheory/FiniteAtomicMeasure.lean` — probability measures carried by finitely many atoms

Model-agnostic measure theory for a probability measure whose mass sits on a finite embedded family of points: the decomposition into weighted Dirac measures, transfer of an almost-sure property down to a single positive-mass atom, and the automatic integrability that finite atomic support buys. Promoted out of the transported-LATE frontier run, where the finite-cell model class needs all three.

<!-- GEN:Causalean.Mathlib.MeasureTheory.FiniteAtomicMeasure -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.MeasureTheory.measure_eq_fin_sum_smul_dirac_of_range` | `(∀ (i : ι), MeasurableSet {cell i}) → μ (Set.range ⇑cell) = μ Set.univ → μ = ∑ i, μ {cell i} • MeasureTheory.Measure.dirac (cell i)` | Let `μ` be a finite measure and `cell` an injective family of finitely many points of the sample space, indexed by a finite type `ι`. If every singleton `{cell i}` is measurable and `μ` assigns its full mass to the range of `cell`, i.e. `μ` puts no mass outside these finitely many points, then `μ` equals the sum, over the index `i`, of the point mass `μ {cell i}` scaling the Dirac measure at `cell i`. |
| `Mathlib.MeasureTheory.property_at_of_ae_of_singleton_pos` | `(∀ᵐ (y : 𝒳) ∂μ, p y) → μ {x} ≠ 0 → p x` | An almost-sure property holds at every point to which the measure assigns nonzero mass. |
| `Mathlib.MeasureTheory.integrable_of_finite_atomic_support` | `(∀ (i : ι), MeasurableSet {cell i}) → μ (Set.range ⇑cell) = μ Set.univ → ∀ {E : Type u_3} [inst_3 : NormedAddCommGroup E] (f : 𝒳 → E), MeasureTheory.StronglyMeasurable f → MeasureTheory.Integrable f μ` | Every strongly measurable normed-vector-valued function is integrable under a finite measure concentrated on finitely many measurable points. |
<!-- /GEN -->

## 10k''a. `Mathlib/MeasureTheory/PolynomialZeroLocus.lean` — null loci of real multivariate polynomials

The zero locus of a nonzero real multivariate polynomial over any finite variable type has
Lebesgue measure zero. The numbered-variable proof proceeds by induction and product-measure
slicing; the finite-type form transports the result along a coordinate equivalence.

<!-- GEN:Causalean.Mathlib.MeasureTheory.PolynomialZeroLocus -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.MeasureTheory.volume_mk_zeroLocus_mvPolynomial` | `P ≠ 0 → MeasureTheory.volume {x \| (MvPolynomial.eval x) P = 0} = 0` | For a real polynomial in finitely many numbered variables that is nonzero, its zero locus has Lebesgue measure zero. |
| `Mathlib.MeasureTheory.volume_zeroLocus_mvPolynomial_finite` | `P ≠ 0 → MeasureTheory.volume {x \| (MvPolynomial.eval x) P = 0} = 0` | For a real polynomial indexed by an arbitrary finite variable type that is nonzero, its zero locus has Lebesgue measure zero. |
<!-- /GEN -->

## 10k''b. `Mathlib/MeasureTheory/CompactArgminSelection.lean` — measurable compact argmin selection

This module supplies exact Borel-measurable minimizer rules for a fixed nonempty compact action
set in finite-dimensional Euclidean space. It handles ties without convexity or uniqueness, first
for jointly Borel objectives with continuous action sections and then for nearest-point selection.

<!-- GEN:Causalean.Mathlib.MeasureTheory.CompactArgminSelection -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.MeasureTheory.borelMeasurable_compact_argmin_selector` | `IsCompact K → K.Nonempty → ∀ (f : X × EuclideanSpace ℝ ι → ℝ), Measurable f → (∀ (x : X), ContinuousOn (fun y => f (x, y)) K) → ∃ π, Measurable π ∧ ∀ (x : X), π x ∈ K ∧ ∀ y ∈ K, f (x, π x) ≤ f (x, y)` | A jointly Borel real objective with continuous action sections on a fixed nonempty compact action set in finite-dimensional Euclidean space admits a Borel measurable, feasible rule that minimizes the objective at every parameter. |
| `Mathlib.MeasureTheory.borelMeasurable_nearestPoint_selector` | `IsCompact K → K.Nonempty → ∃ π, Measurable π ∧ ∀ (x : EuclideanSpace ℝ ι), π x ∈ K ∧ dist x (π x) = Metric.infDist x K` | Every nonempty compact subset of a finite-dimensional Euclidean space admits a total Borel measurable nearest-point rule whose chosen point is feasible and realizes the exact distance infimum. |
<!-- /GEN -->

## 10k'''. `Mathlib/MeasureTheory/SetIntegralRecovery.lean` — recovering a measure or a pointwise bound from set integrals

Two dual "know it on every measurable set, conclude it globally" facts: a finite measure whose mass on every event equals the integral of a nonnegative weight over that event *is* the reweighted measure, and a function whose set integrals are dominated by the measure of the set is bounded by one almost everywhere. The second deliberately avoids assuming integrability — the proof truncates to bounded slices, which is what makes it usable when the function is only pinned down by set-integral identities.

<!-- GEN:Causalean.Mathlib.MeasureTheory.SetIntegralRecovery -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.MeasureTheory.measure_eq_withDensity_of_toReal_setIntegral` | `MeasureTheory.Integrable w μ → (∀ (A : Set α), MeasurableSet A → (ν A).toReal = ∫ (x : α) in A, w x ∂μ) → ν = μ.withDensity fun x => ENNReal.ofReal (w x)` | If a weight function `w` is integrable with respect to a reference measure `μ` and a finite measure `ν`'s mass on every measurable event `A` equals, as a real number, the integral of `w` over `A` against `μ`, then `ν` is obtained from `μ` by weighting with the nonnegative part of `w`: `ν = μ.withDensity (fun x => ENNReal.ofReal (w x))`. |
| `Mathlib.MeasureTheory.abs_le_one_ae_of_setIntegral_le_measure` | `Measurable f → (∀ (A : Set Ω), MeasurableSet A → \|∫ (x : Ω) in A, f x ∂mu\| ≤ (mu A).toReal) → ∀ᵐ (x : Ω) ∂mu, \|f x\| ≤ 1` | A measurable function is bounded in absolute value by one almost everywhere if the absolute value of its integral over every measurable event is at most that event's measure. |
<!-- /GEN -->

## 10k''''. `Mathlib/MeasureTheory/AnalyticSetUniversalMeasurability.lean` — universal measurability of analytic events

This roll-up exposes a reusable descriptive-set-theory bridge for statistical arguments on Polish sample spaces: analytic events are measurable after completing any finite Borel sampling measure. It also supplies the minimal function-level corollary needed for upper-semi-analytic losses, whose completed lower integral agrees with their outer integral under the original measure. The results are Mathlib-shaped, paper-agnostic measure theory in the native `MeasureTheory` namespace.

<!-- GEN:Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability -->
_(no documented declarations in Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability)_
<!-- /GEN -->

### `Mathlib/MeasureTheory/AnalyticSetUniversalMeasurability/Capacity.lean` — Choquet capacities

The Choquet-capacity machinery establishes compact approximation for analytic events in Polish spaces. Its representative result says an analytic event has capacity equal to the supremum over its compact subsets.

<!-- GEN:Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability.Capacity -->
| Decl | Signature | Description |
|---|---|---|
| `MeasureTheory.ChoquetCapacity` | `(Ω : Type u_2) → [TopologicalSpace Ω] → Type u_2` | A Choquet capacity on a topological sample space bundles an extended-real-valued set function that is monotone under set inclusion, continuous from below along increasing sequences of sets, finite on every compact set, and right-continuous at compact sets: any strict upper bound on a compact set's value is already an upper bound on some open neighborhood of that compact set. |
| `MeasureTheory.instCoeFunChoquetCapacityForallSetENNReal` | `CoeFun (MeasureTheory.ChoquetCapacity Ω) fun x => Set Ω → ENNReal` | For every topological sample space, the coercion from a Choquet capacity on that space to an extended-nonnegative-valued set function is given by the capacity's underlying set function. |
| `MeasureTheory.ChoquetCapacity.mono` | `s ⊆ t → c.toFun s ≤ c.toFun t` | Capacity values are monotone under inclusion. |
| `MeasureTheory.ChoquetCapacity.isCompact_lt_top` | `IsCompact K → c.toFun K < ⊤` | Capacity values on compact sets are finite. |
| `MeasureTheory.ChoquetCapacity.exists_isOpen_superset_lt` | `IsCompact K → ∀ {a : ENNReal}, c.toFun K < a → ∃ U, K ⊆ U ∧ IsOpen U ∧ c.toFun U < a` | A strict upper bound for the capacity of a compact set also bounds some open neighborhood. |
| `MeasureTheory.ChoquetCapacity.IsCapacitable` | `MeasureTheory.ChoquetCapacity Ω → Set Ω → Prop` | For every topological sample space, Choquet capacity on that space, and subset of the sample space, the property of being capacitable holds exactly when the capacity of the subset equals the supremum of the capacities of all compact subsets contained in it. |
| `MeasureTheory.AnalyticSet.isCapacitable` | `MeasureTheory.AnalyticSet s → c.IsCapacitable s` | Choquet's capacitability theorem. On a Polish sample space, if a set `s` is analytic — the continuous image of a Polish space, or empty, then `s` is capacitable for every Choquet capacity `c`: the capacity of `s` equals the supremum, over the compact subsets of `s`, of their capacities. |
| `MeasureTheory.Measure.toChoquetCapacity` | `(μ : MeasureTheory.Measure Ω) → [MeasureTheory.IsFiniteMeasure μ] → MeasureTheory.ChoquetCapacity Ω` | For every Polish topological sample space equipped with its Borel σ-algebra and finite measure on that space, the Choquet capacity induced by the measure assigns each subset the measure’s value on that subset. |
| `MeasureTheory.Measure.toChoquetCapacity_apply` | `μ.toChoquetCapacity.toFun s = μ s` | The capacity induced by a finite measure assigns every event exactly that measure's outer mass. |
<!-- /GEN -->

### `Mathlib/MeasureTheory/AnalyticSetUniversalMeasurability/UniversalMeasurability.lean` — completion measurability

Capacitability for the outer measure of a finite Borel measure yields the library’s public universal-measurability theorem for analytic events.

<!-- GEN:Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability.UniversalMeasurability -->
| Decl | Signature | Description |
|---|---|---|
| `MeasureTheory.ChoquetCapacity.IsCapacitable.nullMeasurableSet` | `μ.toChoquetCapacity.IsCapacitable s → MeasureTheory.NullMeasurableSet s μ` | A set capacitable for the outer measure of a finite measure is null-measurable for that measure. |
| `MeasureTheory.AnalyticSet.nullMeasurableSet` | `MeasureTheory.AnalyticSet s → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], MeasureTheory.NullMeasurableSet s μ` | On a Polish sample space equipped with its Borel σ-algebra, if a set `s` is analytic, then `s` is null-measurable for every finite Borel measure `μ`: it becomes measurable after completing `μ`, so it differs from an ordinary measurable event only on a `μ`-null set. |
<!-- /GEN -->

### `Mathlib/MeasureTheory/AnalyticSetUniversalMeasurability/UpperSemianalytic.lean` — upper-semi-analytic losses

Strict superlevel sets that are analytic are enough to make an extended-nonnegative loss measurable for the completed sampling measure and to identify its completed lower integral with its outer integral.

<!-- GEN:Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability.UpperSemianalytic -->
| Decl | Signature | Description |
|---|---|---|
| `MeasureTheory.outerLIntegral` | `MeasureTheory.Measure Ω → (Ω → ENNReal) → ENNReal` | The outer integral of an extended-nonnegative-valued function on a measurable sample space, under a measure on that sample space, is the infimum of the lower Lebesgue integrals of all measurable functions that dominate the given function pointwise. |
| `MeasureTheory.outerLIntegral_mono` | `f ≤ g → MeasureTheory.outerLIntegral μ f ≤ MeasureTheory.outerLIntegral μ g` | Outer integration is monotone in its extended-nonnegative integrand. |
| `MeasureTheory.lintegral_le_outerLIntegral` | `∫⁻ (ω : Ω), f ω ∂μ ≤ MeasureTheory.outerLIntegral μ f` | The lower Lebesgue integral is bounded by the outer integral. |
| `MeasureTheory.lintegral_le_outerLIntegral_of_measurable_le` | `f ≤ g → ∫⁻ (ω : Ω), f ω ∂μ ≤ MeasureTheory.outerLIntegral μ g` | A pointwise lower bound may be integrated before comparison with an outer integral, without measurability of either function. |
| `MeasureTheory.outerLIntegral_eq_lintegral_of_measurable` | `Measurable f → MeasureTheory.outerLIntegral μ f = ∫⁻ (ω : Ω), f ω ∂μ` | Outer integration agrees with Lebesgue integration for measurable functions. |
| `MeasureTheory.lintegral_le_lintegral_completion` | `∫⁻ (ω : Ω), f ω ∂μ ≤ ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion` | Completing a measure can only increase the lower integral of an arbitrary extended-nonnegative function. |
| `MeasureTheory.lintegral_completion_eq_of_measurable` | `Measurable f → ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion = ∫⁻ (ω : Ω), f ω ∂μ` | A measurable extended-nonnegative function has the same integral before and after completion of the measure. |
| `MeasureTheory.UpperSemianalytic` | `(Ω → ENNReal) → Prop` | An extended-nonnegative-valued function on a topological sample space is upper-semi-analytic exactly when, for every extended-nonnegative threshold $a$, the set of sample points at which its value is strictly greater than $a$ is analytic. |
| `MeasureTheory.UpperSemianalytic.nullMeasurable` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], MeasureTheory.NullMeasurable f μ` | An upper-semi-analytic extended-nonnegative function is null-measurable for every finite Borel measure. |
| `MeasureTheory.UpperSemianalytic.measurable_completion` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], Measurable f` | An upper-semi-analytic extended-nonnegative loss is measurable once a finite Borel sampling measure is completed, so it is available to ordinary completed-measure integration. |
| `MeasureTheory.UpperSemianalytic.lintegral_completion_eq_outerLIntegral` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion = MeasureTheory.outerLIntegral μ f` | On a Polish sample space equipped with its Borel σ-algebra and a finite measure `μ`, if `f` is upper-semi-analytic — every strict superlevel set `{ω \| a < f ω}` is analytic, then the lower Lebesgue integral of `f` against the completion of `μ` equals the outer integral of `f` with respect to `μ`, i.e. the infimum of the lower integrals of all measurable pointwise majorants of `f`. |
<!-- /GEN -->

## 12. `Stat/` — Causal-agnostic statistical primitives

Foundation layer for the estimation and inference modules: i.i.d. samples, convergence
modes, asymptotic linearity, Neyman orthogonality, and one-shot sample
splits. Mirrors `doc/basic_concepts/po/estimation.tex` Sections 1–5. Files
here are deliberately project-agnostic and are candidates for upstream
contribution to Mathlib.

**Folder layout.** The top level keeps only the foundational `Sample.lean` and
the `SampleSplit.lean` umbrella; everything else is grouped by topic into
subfolders: `Limit/`
(convergence modes, continuous mapping, WLLN), `CLT/` (asymptotic linearity,
multivariate CLT, Gaussian limit, χ²), `Inference/` (Δ-method, variance
estimation, studentization, Wald), `Quantile/` (cdf/quantile inference stack),
`MEstimation/` (Z/M-estimator CLT), `Orthogonality/` (Neyman orthogonality,
conditional-op helpers), `SampleSplit/` (cross-fitting + fold-restricted CLT),
`Minimax/` (Le Cam / Fano / Assouad lower bounds, §12w),
and `Concentration/` (vendored toolbox, §12'). Module paths below reflect this.

### 12np. `Stat/Nonparametric/Approximation/HolderInterpolation.lean` — Multivariate Hölder interpolation

This reusable minimax lower-bound primitive turns a pointwise separation between
Hölder-smooth functions into a local L¹ separation. It supplies the generic
moment-cancelling product kernel and the resulting multidimensional interpolation
inequality used by two-point and Assouad arguments.

<!-- GEN:Causalean.Stat.Nonparametric.Approximation.HolderInterpolation.Defs -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Nonparametric.supBall` | `(Fin d → ℝ) → ℝ → Set (Fin d → ℝ)` | For a nonnegative integer dimension, a centre in that-dimensional real space, and a real radius, the closed coordinatewise neighbourhood is the set of points whose every coordinate differs from the corresponding coordinate of the centre by at most the radius. |
| `Stat.Nonparametric.supBall_eq_pi` | `Causalean.Stat.Nonparametric.supBall x0 r = Set.univ.pi fun i => Set.Icc (x0 i - r) (x0 i + r)` | A closed sup-norm ball — the axis-aligned cube of half-width `r` centred at a point — is exactly the product of the coordinate intervals of radius `r` around the centre's coordinates. |
| `Stat.Nonparametric.isCompact_supBall` | `IsCompact (Causalean.Stat.Nonparametric.supBall x0 r)` | A closed sup-norm ball — the axis-aligned cube, or box, of half-width `r` centred at a point `x0` in `d`-dimensional space — is compact, being a finite product of closed bounded intervals. |
| `Stat.Nonparametric.measurableSet_supBall` | `MeasurableSet (Causalean.Stat.Nonparametric.supBall x0 r)` | A closed sup-norm ball (an axis-aligned cube, or box) is a measurable set. |
| `Stat.Nonparametric.mem_supBall_self` | `0 ≤ r → x0 ∈ Causalean.Stat.Nonparametric.supBall x0 r` | A closed sup-norm ball (axis-aligned cube) of nonnegative half-width contains its own centre. |
| `Stat.Nonparametric.volume_supBall` | `MeasureTheory.volume (Causalean.Stat.Nonparametric.supBall x0 r) = ENNReal.ofReal (2 * r) ^ d` | The Lebesgue volume of a closed sup-norm ball — the axis-aligned cube of side `2r` centred at `x0` in `d` dimensions — equals the `d`-th power of the side length, `(2r)^d`. No sign restriction on `r` is needed: a negative half-width gives an empty cube and a zero right-hand side. |
| `Stat.Nonparametric.mem_cube` | `(x ∈ Set.univ.pi fun x => Set.Icc a b) ↔ ∀ (i : ι), a ≤ x i ∧ x i ≤ b` | Membership in an axis-aligned box: a point lies in it exactly when each of its coordinates lies between `a` and `b`. |
| `Stat.Nonparametric.isCompact_cube` | `IsCompact (Set.univ.pi fun x => Set.Icc a b)` | An axis-aligned box over a finite index type is compact: it is a finite product of closed bounded intervals. |
| `Stat.Nonparametric.HolderBallStd` | `((Fin d → ℝ) → ℝ) → ℝ → ℝ → Set (Fin d → ℝ) → Prop` | For a nonnegative integer dimension, a real-valued function on that-dimensional real space, a real smoothness order, a real radius, and a region of that space, the standard multivariate Hölder ball condition requires that (1) the function has continuous derivatives through order $\lceil\mathrm{order}\rceil-1$ on the region, (2) every derivative of order at most $\lceil\mathrm{order}\rceil-1$ has norm at most the radius there, and (3) the derivative of order $\lceil\mathrm{order}\rceil-1$ changes between any two points in the region by at most the radius times their distance to the power $\mathrm{order}-(\lceil\mathrm{order}\rceil-1)$. |
| `Stat.Nonparametric.prodKernel` | `(ℝ → ℝ) → (d : ℕ) → (Fin d → ℝ) → ℝ` | For a one-dimensional real-valued kernel and a nonnegative integer dimension, the multivariate product kernel assigns to each point the product of the one-dimensional kernel evaluated at all of its coordinates. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Nonparametric.Approximation.HolderInterpolation.Kernel -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Nonparametric.hpkPoly_continuous` | `Continuous (Causalean.Stat.Nonparametric.hpkPoly✝ m c)` | The coefficient polynomial of degree at most `m` built from a coefficient vector is a continuous function on the real line. |
| `Stat.Nonparametric.exists_moment_cancelling_kernel_1d` | `∃ k, Continuous k ∧ (∀ (u : ℝ), 1 < \|u\| → k u = 0) ∧ ∫ (u : ℝ) in Set.Icc (-1) 1, k u = 1 ∧ ∀ (j : ℕ), 1 ≤ j → j ≤ m → ∫ (u : ℝ) in Set.Icc (-1) 1, u ^ j * k u = 0` | For any prescribed finite order `m`, a compactly supported one-dimensional kernel can be chosen to have unit total mass while cancelling every polynomial moment through that order. |
| `Stat.Nonparametric.prodKernel_abs_le` | `(∀ (u : ℝ), \|k u\| ≤ B) → ∀ (u : Fin d → ℝ), \|Causalean.Stat.Nonparametric.prodKernel k d u\| ≤ B ^ d` | A product kernel is uniformly bounded by the coordinatewise bound raised to the dimension, whenever its one-dimensional factor has that bound. |
| `Stat.Nonparametric.prodKernel_integral` | `∫ (u : Fin d → ℝ), Causalean.Stat.Nonparametric.prodKernel k d u = (∫ (t : ℝ), k t) ^ d` | The total mass of a product kernel factorizes into the product of the one-dimensional masses, so a unit-mass factor yields a unit-mass multivariate kernel. |
| `Stat.Nonparametric.prodKernel_moment` | `∫ (u : Fin d → ℝ), (∏ i, u i ^ ν i) * Causalean.Stat.Nonparametric.prodKernel k d u = ∏ i, ∫ (t : ℝ), t ^ ν i * k t` | Each polynomial moment of a product kernel factorizes into the corresponding one-dimensional moments, allowing any cancelled coordinate moment to cancel the entire multivariate moment. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Nonparametric.Approximation.HolderInterpolation -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Nonparametric.l1_lower_of_bias_bound` | `0 < γ → 0 < Ksup → 0 < Δ → 0 < cstar → h = cstar * Δ ^ (1 / γ) → 3 * Δ / 4 ≤ Ksup * h⁻¹ ^ d * Ival → 3 / (4 * Ksup) * cstar ^ d * Δ ^ (1 + ↑d / γ) ≤ Ival` | The bandwidth optimization converts a lower bound on a kernel-smoothed signal into the Hölder interpolation rate: local absolute mass grows at least as the pointwise signal to the power one plus dimension divided by smoothness. |
| `Stat.Nonparametric.prodKernel_continuous` | `Continuous k → Continuous (Causalean.Stat.Nonparametric.prodKernel k d)` | The tensor kernel is continuous when the 1-D kernel is. |
| `Stat.Nonparametric.holder_point_l1_interpolation` | `0 < γ → 0 < M → 0 < r → Causalean.Stat.Nonparametric.supBall x0 r ⊆ S → ∃ cH, 0 < cH ∧ ∀ (g : (Fin d → ℝ) → ℝ), Causalean.Stat.Nonparametric.HolderBallStd g γ M S → cH * \|g x0\| ^ (1 + ↑d / γ) ≤ ∫ (x : Fin d → ℝ) in Causalean.Stat.Nonparametric.supBall x0 r, \|g x\|` | Hölder pointwise ⟹ local `L¹` mass interpolation. Fix a point `x0` in `d`-dimensional Euclidean space. If the Hölder exponent `γ` is positive, the Hölder constant `M` is positive, the neighbourhood radius `r` is positive, and the sup-norm ball of radius `r` around `x0` is contained in the domain `S`, then there is a constant `c_H > 0`, depending only on `γ`, `d`, `M`, `r` and uniform over the Hölder ball, such that every function `g` in the standard Hölder ball of exponent `γ`, constant `M`, and domain `S` satisfies `c_H · \|g(x0)\|^{1 + d/γ} ≤ ∫_{supBall x0 r} \|g\|`. |
<!-- /GEN -->

### 12a0. `Stat/Coupling/ProductLossMonotoneCoupling/` — Product-loss monotone couplings

Coupling-based statistical primitives for extremal dependence with fixed real marginals.
This stack builds explicit comonotone and countermonotone quantile couplings, proves
the sharp Fréchet-Hoeffding cdf envelopes, derives Hoeffding's covariance identity,
and concludes that the product expectation is maximized and minimized by those two
monotone couplings under finite second moments.

<!-- GEN:Causalean.Stat.Coupling -->
_(no documented declarations in Causalean.Stat.Coupling)_
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling -->
_(no documented declarations in Causalean.Stat.Coupling.ProductLossMonotoneCoupling)_
<!-- /GEN -->

#### 12a0.1. `Stat/Coupling/ProductLossMonotoneCoupling/PIT.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.PIT -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.unifOI` | `MeasureTheory.Measure ℝ` | The uniform probability measure on the open unit interval is Lebesgue measure restricted to the interval $(0,1)$. |
| `Stat.instIsProbabilityMeasure_unifOI` | `MeasureTheory.IsProbabilityMeasure Causalean.Stat.unifOI` | The uniform probability measure on the open unit interval is a probability law. |
| `Stat.monotoneOn_quantile` | `MonotoneOn (Causalean.Stat.quantile μ) (Set.Ioo 0 1)` | The quantile function is monotone on the open unit interval `(0,1)` (immediate from `quantile_mono`, whose hypotheses `0 < τ`, `τ' < 1` hold throughout the interior). |
| `Stat.aemeasurable_quantile_unifOI` | `AEMeasurable (Causalean.Stat.quantile μ) Causalean.Stat.unifOI` | The quantile function is a.e.-measurable with respect to the uniform measure on `(0,1)`; it is monotone there, and a monotone function is measurable. |
| `Stat.quantile_map_uniform` | `MeasureTheory.Measure.map (Causalean.Stat.quantile μ) Causalean.Stat.unifOI = μ` | Probability integral transform. For a Borel probability measure `μ` on `ℝ`, the pushforward of the uniform distribution on `(0,1)` under `μ`'s quantile function equals `μ` itself. |
<!-- /GEN -->

#### 12a0.2. `Stat/Coupling/ProductLossMonotoneCoupling/Coupling.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.Coupling -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IsCoupling` | `MeasureTheory.Measure (ℝ × ℝ) → MeasureTheory.Measure ℝ → MeasureTheory.Measure ℝ → Prop` | `π` is a coupling of `μ` and `ν`, i.e. a member of the Fréchet class `Π(μ, ν)`, when it is a probability measure on `ℝ × ℝ` whose first marginal is `μ` and whose second marginal is `ν`. |
| `Stat.comonotoneCoupling` | `MeasureTheory.Measure ℝ → MeasureTheory.Measure ℝ → MeasureTheory.Measure (ℝ × ℝ)` | For two measures on the real line, the comonotone quantile coupling is the image of the uniform measure on the open unit interval under the pair of their quantile functions evaluated at the same uniform draw. Both coordinates are therefore driven by that common draw. |
| `Stat.countermonotoneCoupling` | `MeasureTheory.Measure ℝ → MeasureTheory.Measure ℝ → MeasureTheory.Measure (ℝ × ℝ)` | For two measures on the real line, the countermonotone quantile coupling is the image of the uniform measure on the open unit interval under the first measure's quantile function at a uniform draw and the second measure's quantile function at one minus that draw. |
| `Stat.map_one_sub_unifOI` | `MeasureTheory.Measure.map (fun u => 1 - u) Causalean.Stat.unifOI = Causalean.Stat.unifOI` | The reflection `u ↦ 1 - u` preserves the uniform measure on `(0,1)`: `unifOI.map (fun u => 1 - u) = unifOI`. |
| `Stat.isCoupling_comonotoneCoupling` | `Causalean.Stat.IsCoupling (Causalean.Stat.comonotoneCoupling μ ν) μ ν` | For probability measures `μ` and `ν` on the reals, the comonotone coupling of `μ` and `ν` is indeed a coupling of the pair, i.e. its two marginals recover `μ` and `ν`. |
| `Stat.isCoupling_countermonotoneCoupling` | `Causalean.Stat.IsCoupling (Causalean.Stat.countermonotoneCoupling μ ν) μ ν` | For probability measures `μ` and `ν` on the reals, the countermonotone coupling of `μ` and `ν` is indeed a coupling of the pair, i.e. its two marginals recover `μ` and `ν`. |
<!-- /GEN -->

#### 12a0.3. `Stat/Coupling/ProductLossMonotoneCoupling/FrechetHoeffding.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.FrechetHoeffding -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.jointCdf` | `MeasureTheory.Measure (ℝ × ℝ) → ℝ → ℝ → ℝ` | For a measure on pairs of real-valued quantities and real thresholds, the joint cumulative distribution function is the real-valued mass that the measure assigns to the lower-left quadrant consisting of pairs whose first coordinate is at most the first threshold and whose second coordinate is at most the second threshold. |
| `Stat.frechet_hoeffding_upper` | `Causalean.Stat.IsCoupling π μ ν → ∀ (x y : ℝ), Causalean.Stat.jointCdf π x y ≤ min (↑(ProbabilityTheory.cdf μ) x) (↑(ProbabilityTheory.cdf ν) y)` | Fréchet–Hoeffding upper bound. For a coupling `π` of the probability measures `μ` and `ν` and reals `x` and `y`, the joint cdf of `π` at `(x, y)` is bounded above by the smaller of the marginal cdfs of `μ` at `x` and `ν` at `y`. |
| `Stat.frechet_hoeffding_lower` | `Causalean.Stat.IsCoupling π μ ν → ∀ (x y : ℝ), max (↑(ProbabilityTheory.cdf μ) x + ↑(ProbabilityTheory.cdf ν) y - 1) 0 ≤ Causalean.Stat.jointCdf π x y` | Fréchet–Hoeffding lower bound. For a coupling `π` of the probability measures `μ` and `ν` and reals `x` and `y`, the joint cdf of `π` at `(x, y)` is bounded below by the maximum of `0` and the sum of the marginal cdfs of `μ` at `x` and `ν` at `y`, minus `1`. |
<!-- /GEN -->

#### 12a0.4. `Stat/Coupling/ProductLossMonotoneCoupling/FrechetHoeffdingAttainment.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.FrechetHoeffdingAttainment -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.jointCdf_comonotoneCoupling` | `Causalean.Stat.jointCdf (Causalean.Stat.comonotoneCoupling μ ν) x y = min (↑(ProbabilityTheory.cdf μ) x) (↑(ProbabilityTheory.cdf ν) y)` | Comonotone attainment. The joint cdf of the comonotone coupling of two probability measures `μ` and `ν`, evaluated at a point `(x, y)`, equals the Fréchet–Hoeffding upper bound `min (cdf μ x) (cdf ν y)`. |
| `Stat.jointCdf_countermonotoneCoupling` | `Causalean.Stat.jointCdf (Causalean.Stat.countermonotoneCoupling μ ν) x y = max (↑(ProbabilityTheory.cdf μ) x + ↑(ProbabilityTheory.cdf ν) y - 1) 0` | Countermonotone attainment. The joint cdf of the countermonotone coupling of two probability measures `μ` and `ν`, evaluated at a point `(x, y)`, equals the Fréchet–Hoeffding lower bound `max (cdf μ x + cdf ν y - 1) 0`. |
| `Stat.jointCdf_le_comonotone` | `Causalean.Stat.IsCoupling π μ ν → ∀ [MeasureTheory.IsProbabilityMeasure μ] [MeasureTheory.IsProbabilityMeasure ν] (x y : ℝ), Causalean.Stat.jointCdf π x y ≤ Causalean.Stat.jointCdf (Causalean.Stat.comonotoneCoupling μ ν) x y` | Corollary: for any coupling `π` of `(μ, ν)`, its joint cdf is dominated pointwise by the comonotone joint cdf. This is the pointwise inequality that the covariance identity turns into optimality of `E[XY]`. |
| `Stat.countermonotone_le_jointCdf` | `Causalean.Stat.IsCoupling π μ ν → ∀ [MeasureTheory.IsProbabilityMeasure μ] [MeasureTheory.IsProbabilityMeasure ν] (x y : ℝ), Causalean.Stat.jointCdf (Causalean.Stat.countermonotoneCoupling μ ν) x y ≤ Causalean.Stat.jointCdf π x y` | Corollary: the countermonotone joint cdf is dominated pointwise by any other coupling's joint cdf. |
<!-- /GEN -->

#### 12a0.5. `Stat/Coupling/ProductLossMonotoneCoupling/TailIntegral.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.TailIntegral -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.tailInd` | `ℝ → ℝ → ℝ` | For a real threshold and a real argument, the tail indicator equals one when the argument is strictly below the threshold and zero otherwise. |
| `Stat.signedTail` | `ℝ → ℝ → ℝ` | For a real threshold and a real argument, the signed tail indicator is the indicator that the argument is below the threshold minus the indicator that it is below zero. |
| `Stat.tailInd_apply` | `Causalean.Stat.tailInd a s = if s < a then 1 else 0` | The tail indicator equals one below the threshold and zero at or above it. |
| `Stat.signedTail_eq_indicator_sub` | `Causalean.Stat.signedTail a = ((Set.Ico 0 a).indicator fun x => 1) - (Set.Ico a 0).indicator fun x => 1` | `signedTail a` is, as a function of `s`, the difference of the indicators of the two intervals `Ico 0 a` and `Ico a 0` (at most one of which is nonempty). This is the normal form used to compute its integral and to see it is integrable. |
| `Stat.measurable_signedTail` | `Measurable (Causalean.Stat.signedTail a)` | `signedTail a` is measurable in the tail variable `s`. |
| `Stat.measurable_signedTail_uncurry` | `Measurable fun z => Causalean.Stat.signedTail z.1 z.2` | `signedTail` is jointly measurable in `(a, s)`, since `{z \| z.2 < z.1}` is an open (hence measurable) subset of `ℝ × ℝ`. |
| `Stat.abs_signedTail_le_one` | `\|Causalean.Stat.signedTail a s\| ≤ 1` | The signed tail indicator is bounded by `1` in absolute value. |
| `Stat.integrable_signedTail` | `MeasureTheory.Integrable (Causalean.Stat.signedTail a) MeasureTheory.volume` | `signedTail a` is Lebesgue integrable: it is a bounded function supported on a bounded interval. |
| `Stat.integral_signedTail` | `∫ (s : ℝ), Causalean.Stat.signedTail a s = a` | Signed tail representation. `∫ s, (𝟙{s < a} - 𝟙{s < 0}) ds = a`. |
| `Stat.integral_abs_signedTail` | `∫ (s : ℝ), \|Causalean.Stat.signedTail a s\| = \|a\|` | The `L¹` norm of the signed tail indicator is `\|a\|`: the integrand is `±1` on an interval of length `\|a\|`. |
| `Stat.integrable_signedTail_prod` | `MeasureTheory.Integrable (fun q => Causalean.Stat.signedTail x q.1 * Causalean.Stat.signedTail y q.2) (MeasureTheory.volume.prod MeasureTheory.volume)` | The tensor product `(s, t) ↦ signedTail x s * signedTail y t` is integrable on `ℝ × ℝ` for Lebesgue×Lebesgue. |
| `Stat.integral_signedTail_prod` | `∫ (q : ℝ × ℝ), Causalean.Stat.signedTail x q.1 * Causalean.Stat.signedTail y q.2 ∂MeasureTheory.volume.prod MeasureTheory.volume = x * y` | Product tail representation. For any two reals `x` and `y`, their product `x * y` equals the two-dimensional Lebesgue integral of the product of their signed tail indicators, `∫∫ (𝟙{s<x} - 𝟙{s<0})(𝟙{t<y} - 𝟙{t<0}) ds dt`. |
| `Stat.integral_norm_signedTail_prod` | `∫ (q : ℝ × ℝ), ‖Causalean.Stat.signedTail x q.1 * Causalean.Stat.signedTail y q.2‖ ∂MeasureTheory.volume.prod MeasureTheory.volume = \|x\| * \|y\|` | The `L¹` norm of the product tail representation is `\|x\| * \|y\|`. This is the domination bound that makes the Fubini swap against an `L²` coupling valid: its `π`-integral is `E\|XY\| < ∞` by Cauchy–Schwarz. |
<!-- /GEN -->

#### 12a0.6. `Stat/Coupling/ProductLossMonotoneCoupling/Survival.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.Survival -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.survFst` | `MeasureTheory.Measure (ℝ × ℝ) → ℝ → ℝ` | For a measure on pairs of real-valued quantities and a real threshold, the first marginal survival function is the real-valued mass of pairs whose first coordinate exceeds that threshold. |
| `Stat.survSnd` | `MeasureTheory.Measure (ℝ × ℝ) → ℝ → ℝ` | For a measure on pairs of real-valued quantities and a real threshold, the second marginal survival function is the real-valued mass of pairs whose second coordinate exceeds that threshold. |
| `Stat.jointSurv` | `MeasureTheory.Measure (ℝ × ℝ) → ℝ → ℝ → ℝ` | For a measure on pairs of real-valued quantities and real thresholds, the joint survival function is the real-valued mass of pairs for which the first coordinate exceeds the first threshold and the second coordinate exceeds the second threshold. |
| `Stat.survFst_eq` | `Causalean.Stat.IsCoupling π μ ν → ∀ (s : ℝ), Causalean.Stat.survFst π s = 1 - ↑(ProbabilityTheory.cdf μ) s` | The first marginal survival function of a coupling is `1 - F`, where `F = cdf μ`. Proof: `Prod.fst ⁻¹' Ioi s` is the complement of `Prod.fst ⁻¹' Iic s`, whose `π`-mass is `μ (Iic s) = F s` by `h.map_fst`. |
| `Stat.survSnd_eq` | `Causalean.Stat.IsCoupling π μ ν → ∀ (t : ℝ), Causalean.Stat.survSnd π t = 1 - ↑(ProbabilityTheory.cdf ν) t` | The second marginal survival function of a coupling is `1 - G`, where `G = cdf ν`. |
| `Stat.jointSurv_eq` | `Causalean.Stat.IsCoupling π μ ν → ∀ (s t : ℝ), Causalean.Stat.jointSurv π s t = 1 - ↑(ProbabilityTheory.cdf μ) s - ↑(ProbabilityTheory.cdf ν) t + Causalean.Stat.jointCdf π s t` | Inclusion–exclusion. The joint survival function of a coupling is `S s t = 1 - F s - G t + H_π s t`. Proof: `Ioi s ×ˢ Ioi t` is the complement of `(Prod.fst ⁻¹' Iic s) ∪ (Prod.snd ⁻¹' Iic t)`, and the mass of that union is `F s + G t - H_π s t` by `measureReal_union_add_inter`, the intersection being the lower-left quadrant `Iic s ×ˢ Iic t`. |
| `Stat.surv_gap_eq` | `Causalean.Stat.IsCoupling π μ ν → ∀ (s t : ℝ), Causalean.Stat.jointSurv π s t - Causalean.Stat.survFst π s * Causalean.Stat.survSnd π t = Causalean.Stat.jointCdf π s t - ↑(ProbabilityTheory.cdf μ) s * ↑(ProbabilityTheory.cdf ν) t` | Survival gap = cdf gap. For a coupling `π` of two probability measures `μ` and `ν`, the gap between the joint and product-of-marginals survival functions of `π` at `(s, t)` equals the corresponding gap between the joint and product-of-marginals cumulative distribution functions. |
<!-- /GEN -->

#### 12a0.7. `Stat/Coupling/ProductLossMonotoneCoupling/HoeffdingFubini.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.HoeffdingFubini -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.coupling_fst_memLp` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun p => p.1) 2 π` | The first coordinate is in `L²(π)` when `μ = π.map Prod.fst` has a second moment. |
| `Stat.coupling_snd_memLp` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.MemLp (fun p => p.2) 2 π` | The second coordinate is in `L²(π)` when `ν = π.map Prod.snd` has a second moment. |
| `Stat.coupling_integral_fst` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → ∫ (p : ℝ × ℝ), p.1 ∂π = ∫ (x : ℝ), x ∂μ` | `E_π[X] = E_μ[id]`: the first moment of a coupling is that of its first marginal. |
| `Stat.coupling_integral_snd` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.2 ∂π = ∫ (y : ℝ), y ∂ν` | `E_π[Y] = E_ν[id]`: the first moment of a coupling is that of its second marginal. |
| `Stat.coupling_integrable_mul` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun p => p.1 * p.2) π` | `XY ∈ L¹(π)` by Cauchy–Schwarz from the two `L²` marginals. |
| `Stat.integral_signedTail_fst` | `Causalean.Stat.IsCoupling π μ ν → ∀ (s : ℝ), ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.1 s ∂π = Causalean.Stat.survFst π s - Causalean.Stat.tailInd 0 s` | The `π`-integral of the signed tail indicator of the first coordinate is the marginal survival function minus the constant `𝟙{s<0}`: `∫ p, (𝟙{s < p.1} - 𝟙{s < 0}) ∂π = SX s - 𝟙{s < 0}`. Uses `∫ p, 𝟙{s < p.1} ∂π = π.real (Prod.fst ⁻¹' Ioi s)` and `π univ = 1`. |
| `Stat.integral_signedTail_snd` | `Causalean.Stat.IsCoupling π μ ν → ∀ (t : ℝ), ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.2 t ∂π = Causalean.Stat.survSnd π t - Causalean.Stat.tailInd 0 t` | The `π`-integral of the signed tail indicator of the second coordinate. |
| `Stat.fiber_integral_pi` | `Causalean.Stat.IsCoupling π μ ν → ∀ (s t : ℝ), ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.1 s * Causalean.Stat.signedTail p.2 t ∂π = Causalean.Stat.jointSurv π s t - Causalean.Stat.tailInd 0 t * Causalean.Stat.survFst π s - Causalean.Stat.tailInd 0 s * Causalean.Stat.survSnd π t + Causalean.Stat.tailInd 0 s * Causalean.Stat.tailInd 0 t` | Fibre integral. For a coupling `π` of two probability measures, the `π`-integral of the product of the two signed tail indicators at thresholds `s` and `t` equals the joint survival function of `π` at `(s, t)`, adjusted by cross terms built from the two marginal survival functions and the sign indicators of `s` and `t`. |
<!-- /GEN -->

#### 12a0.8. `Stat/Coupling/ProductLossMonotoneCoupling/HoeffdingFubiniIntegrability.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.HoeffdingFubiniIntegrability -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.integrable_tail_fst_prod` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.Integrable (fun z => Causalean.Stat.signedTail z.2.1 z.1) (MeasureTheory.volume.prod π)` | The single-coordinate tail kernel is integrable on `volume ⊗ π`. Domination: `∫ s, \|signedTail p.1 s\| ds = \|p.1\|`, which is `π`-integrable since `X ∈ L²(π)` and `π` is finite. |
| `Stat.integrable_tail_snd_prod` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun z => Causalean.Stat.signedTail z.2.2 z.1) (MeasureTheory.volume.prod π)` | The single-coordinate tail kernel is integrable on `volume ⊗ π` (second coordinate). |
| `Stat.integrable_bigPhi` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun z => Causalean.Stat.signedTail z.2.1 z.1.1 * Causalean.Stat.signedTail z.2.2 z.1.2) ((MeasureTheory.volume.prod MeasureTheory.volume).prod π)` | The key integrability. Let `π` be a coupling of `μ` and `ν`, where both marginals have finite second moment. Then the product tail kernel `Φ q p = signedTail p.1 q.1 * signedTail p.2 q.2` is integrable for the product of Lebesgue measure on the plane with `π`, i.e. on `(volume ⊗ volume) ⊗ π`. |
| `Stat.integral_prod_eq_integral_fiber` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂π = ∫ (q : ℝ × ℝ), ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.1 q.1 * Causalean.Stat.signedTail p.2 q.2 ∂π ∂MeasureTheory.volume.prod MeasureTheory.volume` | First swap. For a coupling `π` of `μ` and `ν`, where both marginals have finite second moment, the expectation `E_π[XY]` equals the Lebesgue double integral, over the plane, of the fibre integrals `∫ p, Φ q p ∂π`, i.e. `∫ p, p.1 * p.2 ∂π = ∫ q, (∫ p, Φ q p ∂π) dq`. |
| `Stat.mean_fst_tail` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → ∫ (x : ℝ), x ∂μ = ∫ (s : ℝ), Causalean.Stat.survFst π s - Causalean.Stat.tailInd 0 s` | Tail formula for the mean. `E[X] = ∫ s, (SX s - 𝟙{s<0}) ds`, by the same swap on `volume ⊗ π` using `integral_signedTail` pointwise. |
| `Stat.mean_snd_tail` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (y : ℝ), y ∂ν = ∫ (t : ℝ), Causalean.Stat.survSnd π t - Causalean.Stat.tailInd 0 t` | Tail formula for the mean (second coordinate). |
| `Stat.integrable_survFst_sub` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.Integrable (fun s => Causalean.Stat.survFst π s - Causalean.Stat.tailInd 0 s) MeasureTheory.volume` | The centred marginal survival function `s ↦ SX s - 𝟙{s<0}` is Lebesgue integrable (it is `E[signedTail X ·]`, integrable by `Integrable.integral_prod_left` on `integrable_tail_fst_prod`). |
| `Stat.integrable_survSnd_sub` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun t => Causalean.Stat.survSnd π t - Causalean.Stat.tailInd 0 t) MeasureTheory.volume` | The centred marginal survival function `t ↦ SY t - 𝟙{t<0}` is Lebesgue integrable. |
| `Stat.integrable_fiber` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun q => ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.1 q.1 * Causalean.Stat.signedTail p.2 q.2 ∂π) (MeasureTheory.volume.prod MeasureTheory.volume)` | The fibre `q ↦ ∫ p, Φ q p ∂π` is integrable on `ℝ × ℝ`, by `Integrable.integral_prod_left` applied to `integrable_bigPhi`. |
<!-- /GEN -->

#### 12a0.9. `Stat/Coupling/ProductLossMonotoneCoupling/Hoeffding.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.Hoeffding -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.fiber_sub_mean_prod` | `Causalean.Stat.IsCoupling π μ ν → ∀ (q : ℝ × ℝ), ∫ (p : ℝ × ℝ), Causalean.Stat.signedTail p.1 q.1 * Causalean.Stat.signedTail p.2 q.2 ∂π - (Causalean.Stat.survFst π q.1 - Causalean.Stat.tailInd 0 q.1) * (Causalean.Stat.survSnd π q.2 - Causalean.Stat.tailInd 0 q.2) = Causalean.Stat.jointCdf π q.1 q.2 - ↑(ProbabilityTheory.cdf μ) q.1 * ↑(ProbabilityTheory.cdf ν) q.2` | Pointwise cancellation: the fibre integral minus the product of the centred marginal survival functions is exactly the Fréchet gap `H_π - F·G`. |
| `Stat.integrable_frechet_gap` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → MeasureTheory.Integrable (fun p => Causalean.Stat.jointCdf π p.1 p.2 - ↑(ProbabilityTheory.cdf μ) p.1 * ↑(ProbabilityTheory.cdf ν) p.2) (MeasureTheory.volume.prod MeasureTheory.volume)` | The Fréchet gap `H_π - F·G` is integrable on `ℝ × ℝ` for an `L²` coupling; this is the integrability side-condition consumed by `hoeffding_cov_identity` and by the monotone comparison of double integrals in `Optimality.lean`. |
| `Stat.hoeffding_cov_identity_prod` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂π - (∫ (x : ℝ), x ∂μ) * ∫ (y : ℝ), y ∂ν = ∫ (q : ℝ × ℝ), Causalean.Stat.jointCdf π q.1 q.2 - ↑(ProbabilityTheory.cdf μ) q.1 * ↑(ProbabilityTheory.cdf ν) q.2 ∂MeasureTheory.volume.prod MeasureTheory.volume` | Hoeffding's covariance identity, product form. For a coupling `π` of two `L²` probability measures `μ, ν`, |
| `Stat.hoeffding_cov_identity` | `Causalean.Stat.IsCoupling π μ ν → MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂π - (∫ (x : ℝ), x ∂μ) * ∫ (y : ℝ), y ∂ν = ∫ (x : ℝ) (y : ℝ), Causalean.Stat.jointCdf π x y - ↑(ProbabilityTheory.cdf μ) x * ↑(ProbabilityTheory.cdf ν) y` | Hoeffding's covariance identity. For a coupling `π` of `μ` and `ν`, where both `μ` and `ν` have finite second moment (are `L²`), the covariance of the coordinates under `π` equals the double integral, over the plane, of the Fréchet gap between the joint and product cumulative distribution functions, `H_π - F·G`: |
<!-- /GEN -->

#### 12a0.10. `Stat/Coupling/ProductLossMonotoneCoupling/Optimality.lean`

<!-- GEN:Causalean.Stat.Coupling.ProductLossMonotoneCoupling.Optimality -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.product_expectation_le_comonotone` | `Causalean.Stat.IsCoupling π μ ν → ∀ [MeasureTheory.IsProbabilityMeasure μ] [MeasureTheory.IsProbabilityMeasure ν], MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂π ≤ ∫ (p : ℝ × ℝ), p.1 * p.2 ∂Causalean.Stat.comonotoneCoupling μ ν` | Upper optimality. For any coupling `π` of `μ` and `ν`, where both marginals have finite second moment, the expectation of the coordinate product under `π` is at most its expectation under the comonotone (quantile) coupling of `μ` and `ν`: |
| `Stat.countermonotone_le_product_expectation` | `Causalean.Stat.IsCoupling π μ ν → ∀ [MeasureTheory.IsProbabilityMeasure μ] [MeasureTheory.IsProbabilityMeasure ν], MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂Causalean.Stat.countermonotoneCoupling μ ν ≤ ∫ (p : ℝ × ℝ), p.1 * p.2 ∂π` | Lower optimality. For any coupling `π` of `μ` and `ν`, where both marginals have finite second moment, the expectation of the coordinate product under `π` is at least its expectation under the countermonotone coupling of `μ` and `ν`: |
| `Stat.product_expectation_comonotoneCoupling` | `MeasureTheory.MemLp (fun x => x) 2 μ → MeasureTheory.MemLp (fun y => y) 2 ν → ∫ (p : ℝ × ℝ), p.1 * p.2 ∂Causalean.Stat.comonotoneCoupling μ ν = ∫ (u : ℝ) in Set.Ioo 0 1, Causalean.Stat.quantile μ u * Causalean.Stat.quantile ν u` | Closed form of the optimum. For two probability measures `μ` and `ν`, each with finite second moment, the expectation of the coordinate product under the comonotone (quantile) coupling of `μ` and `ν` equals the integral, over `(0,1)`, of the product of their quantile functions: |
<!-- /GEN -->

### 12a. `Stat/Sample.lean`

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample Ω X μ P` | structure | I.i.d. sequence `Z : ℕ → Ω → X` on a single ambient space, with mutual independence (`iIndepFun`), identical distribution (`IdentDistrib`), and law-match (`μ.map (Z 0) = P`). Matches `def:est-iid-sample`. |
| `IIDSample.sampleMean` | `def` | `(1/n) Σ_{i<n} f (Z i ω)` for a real-valued statistic `f`. |

### 12a2. `Stat/Sample/EmpiricalMass.lean`

The empirical frequency of a single point in an i.i.d. sample, with its exact first two moments — the mean recovers the population mass, and the second moment is the multinomial formula `p² + (p − p²)/m`.

<!-- GEN:Causalean.Stat.Sample.EmpiricalMass -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.empiricalMass` | `(Fin N → 𝒳) → 𝒳 → ℝ` | Given an observation space, a nonnegative sample size, a sample indexed by the integers from zero through one less than that size, and a point in the observation space, the empirical mass of that point is the reciprocal of the sample size multiplied by the number of sampled observations equal to that point. |
| `Stat.abs_empiricalMass_le_one` | `\|Causalean.Stat.empiricalMass sample x\| ≤ 1` | The absolute empirical mass of any point is at most one, including for the empty sample. |
| `Stat.integral_empiricalMass` | `0 < m → ∀ (a : 𝒳), MeasurableSet {a} → (∫ (sample : Fin m → 𝒳), Causalean.Stat.empiricalMass sample a ∂MeasureTheory.Measure.pi fun x => μ) = μ.real {a}` | Expected empirical mass. Given a positive sample size $m$ and a measurable singleton `{a}`, the expectation, under the $m$-fold product sampling measure, of the empirical mass of the point `a` — the fraction of sample observations equal to `a` — equals the population probability of `{a}`. |
| `Stat.integral_empiricalMass_sq` | `0 < m → ∀ (a : 𝒳), MeasurableSet {a} → (∫ (sample : Fin m → 𝒳), Causalean.Stat.empiricalMass sample a ^ 2 ∂MeasureTheory.Measure.pi fun x => μ) = μ.real {a} ^ 2 + (↑m)⁻¹ * (μ.real {a} - μ.real {a} ^ 2)` | Second moment of the empirical mass. Given a positive sample size $m$ and a measurable singleton `{a}`, the second moment, under the $m$-fold product sampling measure, of the empirical mass of the point `a` equals the squared population probability of `{a}` plus the usual binomial sampling correction $(1/m)(\mu(\{a\})-\mu(\{a\})^2)$. |
<!-- /GEN -->

### 12a3. `Stat/Sample/CollisionEstimator.lean`

The collision ("birthday") estimator of inverse-frequency functionals from an i.i.d. sample: the collision kernel and its ordered-pair average, plus cell moments and cell-frequency-weighted cross-sample averages. Stated over an arbitrary observation type with an explicit projection to the cell space, so a sample of rich observations can be used directly.

<!-- GEN:Causalean.Stat.Sample.CollisionEstimator -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.collisionKernel` | `(𝒳 → ℝ) → 𝒳 → 𝒳 → ℝ` | Given a finite or infinite cell-label space, a real-valued cell-mass function, and two cell labels, the collision kernel equals the reciprocal mass of the first label when the labels coincide and equals zero otherwise. |
| `Stat.collisionScale` | `(𝒳 → ℝ) → {N : ℕ} → (Fin N → 𝒳) → ℝ` | Given a cell-label space, a real-valued cell-mass function, a nonnegative integer target-sample size, and a target sample of that size, the collision scale is the average collision-kernel value over all ordered pairs of distinct target indices, with the displayed reciprocal convention also applying at sample sizes zero and one. |
| `Stat.cellMoment` | `(𝒳 → ℝ) → (Ω → 𝒳) → {n : ℕ} → (Fin n → Ω) → (Ω → ℝ) → 𝒳 → ℝ` | Given a cell-label space and an observation space, a real-valued cell-mass function, a map assigning each observation to a cell, a nonnegative integer source-sample size, a source sample, a real-valued observation statistic, and a cell label, the cell moment is the inverse source-sample size and inverse cell mass times the sum of the statistic over source observations projected to that cell. |
| `Stat.crossAverage` | `(𝒳 → ℝ) → (Ω → 𝒳) → {n N : ℕ} → (Fin n → Ω) → (Fin N → 𝒳) → (Ω → ℝ) → ℝ` | Given a cell-label space and an observation space, a real-valued cell-mass function, an observation-to-cell map, nonnegative integer source and target sample sizes, a source sample, a target sample of cell labels, and a real-valued observation statistic, the cross-sample average is the average, over target observations, of the corresponding source-sample cell moments. |
<!-- /GEN -->

### 12a4. `Stat/Sample/EffectiveSampleSize.lean`

Kish design effect and effective sample size for a weighted i.i.d. sample. When the weights have population mean one their second moment is the design effect, and dividing the nominal sample size by it gives the effective sample size; this file supplies the "design effect is at least one" fact and the empirical statistic's mean, its variance bound under a fourth-moment envelope, and its lower-tail bound.

<!-- GEN:Causalean.Stat.Sample.EffectiveSampleSize -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.empiricalKishDispersion` | `(Ω → ℝ) → (n : ℕ) → (Fin n → Ω) → ℝ` | Given an observation space, a real-valued weight function on that space, a nonnegative sample size, and a sample indexed by the integers from zero through one less than that size, the empirical Kish dispersion is the reciprocal of the sample size multiplied by the sum of the squared weights of the sampled observations. |
| `Stat.one_le_secondMoment_of_mean_one` | `MeasureTheory.MemLp w 2 μ → ∫ (x : 𝒳), w x ∂μ = 1 → 1 ≤ ∫ (x : 𝒳), w x ^ 2 ∂μ` | A mean-one square-integrable weight has second moment at least one, so its Kish design effect cannot improve on an equally weighted sample. |
| `Stat.empiricalKishDispersion_mean` | `0 < n → MeasureTheory.Integrable (fun o => g o ^ 2) μ → (∫ (sample : Fin n → Ω), Causalean.Stat.empiricalKishDispersion g n sample ∂MeasureTheory.Measure.pi fun x => μ) = ∫ (o : Ω), g o ^ 2 ∂μ` | Expected empirical Kish dispersion. Given a positive sample size $n$ and an integrable squared weight statistic $g^2$ under the population measure, the expectation of the empirical Kish dispersion — the sample average of the squared observation-level weights — under the $n$-fold product sampling measure equals the population second moment $\int g^2\,d\mu$. |
| `Stat.empiricalKishDispersion_variance_le` | `0 < n → MeasureTheory.MemLp (fun o => g o ^ 2) 2 μ → ∫ (o : Ω), g o ^ 2 ∂μ = kappa → (∀ᵐ (o : Ω) ∂μ, g o ^ 4 ≤ 4 * k ^ 2 * g o ^ 2) → ProbabilityTheory.variance (fun sample => Causalean.Stat.empiricalKishDispersion g n sample) (MeasureTheory.Measure.pi fun x => μ) ≤ 4 * k ^ 2 * kappa / ↑n` | If the fourth power of a weight is bounded by four times a squared envelope times its second power, empirical Kish dispersion has variance at most four times the squared envelope and the population second moment, divided by sample size. |
| `Stat.empiricalKishDispersion_lower_tail_le` | `0 < n → 0 < kappa → MeasureTheory.MemLp (Causalean.Stat.empiricalKishDispersion g n) 2 Q → ∫ (sample : Fin n → Ω), Causalean.Stat.empiricalKishDispersion g n sample ∂Q = kappa → ProbabilityTheory.variance (Causalean.Stat.empiricalKishDispersion g n) Q ≤ 4 * k ^ 2 * kappa / ↑n → (Q {sample \| Causalean.Stat.empiricalKishDispersion g n sample < kappa / 2}).toReal ≤ 16 * k ^ 2 / (↑n * kappa)` | Lower-tail bound for empirical Kish dispersion. Given a positive sample size $n$, a positive population Kish dispersion $\kappa$, the empirical Kish dispersion is square-integrable under the sampling measure `Q`, its expectation under `Q` equals $\kappa$, and its variance under `Q` is at most $4k^2\kappa/n$ for a weight envelope $k$, then the probability that the empirical Kish dispersion falls below half its mean $\kappa/2$ is at most $16k^2/(n\kappa)$. |
<!-- /GEN -->

### 12a5. `Stat/Sample/OccupancyWeightedMean/` — occupancy-weighted within-group means

Totalized occupancy-weighted differences of treated and control within-group
sample means, together with a weak-second-moment variance bound under fixed
overlap. The construction covers empty samples, empty group types, zero-mass
groups, and empirical zero-count boundaries without bounded outcomes.

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean -->
_(no documented declarations in Causalean.Stat.Sample.OccupancyWeightedMean)_
<!-- /GEN -->

#### 12a5.1. `Stat/Sample/OccupancyWeightedMean/Basic.lean`

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.armGroupEvent` | `(Omega → kappa) → (Omega → Bool) → Bool → kappa → Set Omega` | Given a sample space, a group-label space, a group-label map, a Boolean arm-assignment map, an arm label, and a group label, the arm--group event is the set of sample-space outcomes whose group and arm labels equal the requested labels. |
| `Stat.groupEvent` | `(Omega → kappa) → kappa → Set Omega` | Given a sample space, a group-label space, a group-label map, and a group label, the group event is the set of sample-space outcomes having that group label, irrespective of arm assignment. |
| `Stat.armGroupResidual` | `(Omega → ℝ) → (Bool → kappa → ℝ) → Bool → kappa → Omega → ℝ` | Given a sample space, a group-label space, a real-valued outcome, a supplied real center for every arm and group, an arm label, a group label, and a sample-space outcome, the arm--group residual is that outcome minus the center supplied for its requested arm and group. |
| `Stat.supportedArmGroupResidual` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → Bool → kappa → Omega → ℝ` | Given a sample space, a group-label space, a group-label map, a Boolean arm-assignment map, a real-valued outcome, arm--group centers, an arm label, and a group label, the supported arm--group residual equals the corresponding residual on the requested arm--group event and zero outside that event. |
| `Stat.sampleDesign` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → Fin n → kappa × Bool` | Given a sample space, a group-label space, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, and a sample of that size, the sample design maps every sample coordinate to its pair of group and arm labels. |
| `Stat.groupArmCount` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → Bool → kappa → ℕ` | Given a sample space, a group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, a sample, an arm label, and a group label, the arm--group count is the number of sample coordinates whose two labels equal those requested. |
| `Stat.groupCount` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → kappa → ℕ` | Given a sample space, a group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, a sample, and a group label, the group count is the sum of the sample counts in that group for the false and true arm labels. |
| `Stat.usableGroup` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → kappa → Prop` | Given a sample space, a group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, a sample, and a group label, the usable-group condition holds exactly when the count in the false arm is positive and the count in the true arm is positive. |
| `Stat.usableGroupTotal` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → ℕ` | Given a sample space, a finite group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, and a sample, the usable-group total is the sum of group counts over precisely those groups represented by both arm labels. |
| `Stat.armResidualSum` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → (Fin n → Omega) → Bool → kappa → ℝ` | Given a sample space, a group-label space, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, a real-valued outcome, arm--group centers, a sample, an arm label, and a group label, the arm--group residual sum adds the supported residuals of all sample coordinates. |
| `Stat.armResidualMean` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → (Fin n → Omega) → Bool → kappa → ℝ` | Given a sample space, a group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, a real-valued outcome, arm--group centers, a sample, an arm label, and a group label, the totalized arm--group residual mean is its residual sum divided by its count when that count is positive, and zero otherwise. |
| `Stat.occupancyWeightedResidual` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → (Fin n → Omega) → ℝ` | Given a measurable sample space, a finite group-label space whose labels can be compared for equality, a nonnegative integer sample size, a measure on the sample space, a group-label map, a Boolean arm-assignment map, a real-valued outcome, arm--group centers, and a sample, the occupancy-weighted residual statistic is the usable-group-occupancy-weighted average of treated-minus-control residual means, totalized to zero when no group is usable. |
| `Stat.inverseUsableGroupTotal` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → ℝ` | Given a sample space, a finite group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, and a sample, the inverse usable-group total is the reciprocal usable-group total when that total is positive and is zero otherwise. |
| `Stat.occupancyDesignVarianceFactor` | `(Omega → kappa) → (Omega → Bool) → (Fin n → Omega) → ℝ` | Given a sample space, a finite group-label space whose labels can be compared for equality, a nonnegative integer sample size, a group-label map, a Boolean arm-assignment map, and a sample, the occupancy design-variance factor is the squared reciprocal usable-group total times the sum, over usable groups, of squared group counts times the two reciprocal arm counts, and is zero when no group is usable. |
| `Stat.measurableSet_groupEvent` | `Measurable group → ∀ (k : kappa), MeasurableSet (Causalean.Stat.groupEvent group k)` | A measurable group label has measurable group fibers. |
| `Stat.measurableSet_armGroupEvent` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), MeasurableSet (Causalean.Stat.armGroupEvent group arm a k)` | Measurable group and arm labels have measurable joint arm/group fibers. |
| `Stat.measurable_armGroupEvent_indicator` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa) {f : Omega → beta}, Measurable f → Measurable ((Causalean.Stat.armGroupEvent group arm a k).indicator f)` | Restricting a measurable function to a joint arm/group cell — keeping its values on the cell and setting it to zero outside — leaves it measurable, whenever the group and arm labels are measurable. |
| `Stat.measurable_sampleDesign` | `Measurable group → Measurable arm → Measurable (Causalean.Stat.sampleDesign group arm)` | Measurable group and arm labels make the coordinatewise finite design measurable. |
| `Stat.measurable_groupArmCount` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.groupArmCount group arm z a k` | Measurable group and arm labels make each fixed arm/group count measurable on a finite product sample. |
| `Stat.measurable_groupCount` | `Measurable group → Measurable arm → ∀ (k : kappa), Measurable fun z => Causalean.Stat.groupCount group arm z k` | Measurable group and arm labels make each fixed group count measurable on the finite product sample space. |
| `Stat.measurableSet_usableGroup` | `Measurable group → Measurable arm → ∀ (k : kappa), MeasurableSet {z \| Causalean.Stat.usableGroup group arm z k}` | Measurable group and arm labels make the event that a fixed empirical group has both arms represented measurable. |
| `Stat.measurable_usableGroup_ite` | `Measurable group → Measurable arm → ∀ (k : kappa) [inst_6 : DecidablePred fun z => Causalean.Stat.usableGroup group arm z k] {f g : (Fin n → Omega) → beta}, Measurable f → Measurable g → Measurable fun z => if Causalean.Stat.usableGroup group arm z k then f z else g z` | A quantity defined by two branches, selected according to whether a fixed empirical group has both arms represented, is measurable whenever both branches are and the group and arm labels are measurable. |
| `Stat.measurable_usableGroupTotal` | `Measurable group → Measurable arm → Measurable fun z => Causalean.Stat.usableGroupTotal group arm z` | Measurable group and arm labels make the total occupancy in empirically usable groups measurable. |
| `Stat.measurable_armGroupResidual` | `Measurable Y → ∀ (a : Bool) (k : kappa), Measurable (Causalean.Stat.armGroupResidual Y center a k)` | A measurable outcome makes each arm/group-centered residual measurable. |
| `Stat.measurable_supportedArmGroupResidual` | `Measurable group → Measurable arm → Measurable Y → ∀ (a : Bool) (k : kappa), Measurable (Causalean.Stat.supportedArmGroupResidual group arm Y center a k)` | Measurable group labels, arm labels, and outcomes make each residual restricted to its own arm/group cell measurable. |
| `Stat.measurable_armResidualSum` | `Measurable group → Measurable arm → Measurable Y → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.armResidualSum group arm Y center z a k` | Measurable group labels, arm labels, and outcomes make each arm/group residual sum measurable on the finite product sample space. |
| `Stat.measurable_armResidualMean` | `Measurable group → Measurable arm → Measurable Y → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.armResidualMean group arm Y center z a k` | Measurable group labels, arm labels, and outcomes make each zero-safe arm/group residual mean measurable on the finite product sample space. |
| `Stat.measurable_occupancyWeightedResidual` | `Measurable group → Measurable arm → Measurable Y → Measurable (Causalean.Stat.occupancyWeightedResidual mu group arm Y center)` | Measurable group labels, arm labels, and outcomes make the zero-safe occupancy-weighted residual measurable on the finite product sample space. |
| `Stat.measurable_inverseUsableGroupTotal` | `Measurable group → Measurable arm → Measurable (Causalean.Stat.inverseUsableGroupTotal group arm)` | Measurable group and arm labels make the zero-safe reciprocal usable occupancy measurable on the finite product sample space. |
| `Stat.measurable_occupancyDesignVarianceFactor` | `Measurable group → Measurable arm → Measurable (Causalean.Stat.occupancyDesignVarianceFactor group arm)` | Measurable group and arm labels make the zero-safe occupancy design variance factor measurable on the finite product sample space. |
<!-- /GEN -->

#### 12a5.2. `Stat/Sample/OccupancyWeightedMean/BinomialDesign.lean`

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean.BinomialDesign -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.inverseTwoCounts` | `ℕ → ℕ → ℝ` | Given a total count and a success count, the two-arm inverse-count contribution is the sum of the reciprocal success and failure counts when both counts are positive, and is zero when either count is zero. |
| `Stat.interiorIndicator` | `ℕ → ℕ → ℝ` | Given a total count and a success count, the binomial interior indicator equals one when both the success and failure counts are positive, and equals zero when either count is zero. |
| `Stat.sum_bernoulli_local_variance_le_share` | `0 < epsilon → epsilon ≤ p → p ≤ 1 - epsilon → ∑ b, (∏ i, if b i = true then p else 1 - p) * (↑(Fintype.card ι) ^ 2 * (↑(r + Fintype.card ι))⁻¹ ^ 2 * Causalean.Stat.inverseTwoCounts (Fintype.card ι) {i \| b i = true}.card) ≤ 4 / (epsilon ^ 2 * (1 - epsilon)) * ∑ b, (∏ i, if b i = true then p else 1 - p) * (↑(Fintype.card ι) * (↑(r + Fintype.card ι))⁻¹ ^ 2 * Causalean.Stat.interiorIndicator (Fintype.card ι) {i \| b i = true}.card)` | If the overlap margin is positive and one group's success probability lies between the margin and one minus the margin, that group's expected inverse-arm variance contribution is controlled by its expected share of reciprocal usable occupancy. |
<!-- /GEN -->

#### 12a5.3. `Stat/Sample/OccupancyWeightedMean/FiniteDesign.lean`

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean.FiniteDesign -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.sum_jointWeight_variance_le_inverse` | `(∀ (k : κ) (a : Bool), 0 ≤ q k a) → (∀ (k : κ), q k false + q k true = g k) → 0 < epsilon → epsilon ≤ 1 / 2 → (∀ (k : κ), 0 < g k → ∀ (a : Bool), epsilon * g k ≤ q k a) → ∑ d, (∏ i, q (d i).1 (d i).2) * Causalean.Stat.occupancyDesignVarianceFactor Prod.fst Prod.snd d ≤ 4 / (epsilon ^ 2 * (1 - epsilon)) * ∑ d, (∏ i, q (d i).1 (d i).2) * Causalean.Stat.inverseUsableGroupTotal Prod.fst Prod.snd d` | If all joint group/arm masses are nonnegative, their two arm masses sum to the group mass, the overlap margin is positive and at most one half, and each positive-mass group gives both arms at least the overlap share, the joint-design average of the occupancy variance factor is bounded by a margin-dependent multiple of average reciprocal usable occupancy. Zero-mass groups contribute zero before any conditional arm probability is formed. |
<!-- /GEN -->

#### 12a5.4. `Stat/Sample/OccupancyWeightedMean/MomentBounds.lean`

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean.MomentBounds -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.supportedArmGroupResidual_mul_eq_zero_of_ne` | `(a, k) ≠ (b, l) → ∀ (omega : Omega), Causalean.Stat.supportedArmGroupResidual group arm Y center a k omega * Causalean.Stat.supportedArmGroupResidual group arm Y center b l omega = 0` | When two arm/group labels differ, their supported residuals have zero pointwise product because their supports are disjoint. |
| `Stat.integral_designWeight_residual_cross_coordinates_eq_zero` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.supportedArmGroupResidual group arm Y center a k) 2 mu) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.armGroupEvent group arm a k, Causalean.Stat.armGroupResidual Y center a k omega ∂mu = 0) → ∀ (W : (Fin n → kappa × Bool) → ℝ) (i j : Fin n), i ≠ j → ∀ (a b : Bool) (k l : kappa), (∫ (z : Fin n → Omega), W (Causalean.Stat.sampleDesign group arm z) * Causalean.Stat.supportedArmGroupResidual group arm Y center a k (z i) * Causalean.Stat.supportedArmGroupResidual group arm Y center b l (z j) ∂MeasureTheory.Measure.pi fun x => mu) = 0` | If group labels, arm labels, and outcomes are measurable, every supported residual has a finite second moment, each residual is centered within its arm/group cell, and the two sample coordinates differ, any finite-design weight times their two supported residuals has product-law integral zero. |
| `Stat.integral_designWeight_residual_sq_le_indicator` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.supportedArmGroupResidual group arm Y center a k) 2 mu) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.armGroupEvent group arm a k, Causalean.Stat.armGroupResidual Y center a k omega ^ 2 ∂mu ≤ (mu (Causalean.Stat.armGroupEvent group arm a k)).toReal * V ^ 2) → ∀ (W : (Fin n → kappa × Bool) → ℝ), (∀ (d : Fin n → kappa × Bool), 0 ≤ W d) → ∀ (i : Fin n) (a : Bool) (k : kappa), (∫ (z : Fin n → Omega), W (Causalean.Stat.sampleDesign group arm z) * Causalean.Stat.supportedArmGroupResidual group arm Y center a k (z i) ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ V ^ 2 * ∫ (z : Fin n → Omega), W (Causalean.Stat.sampleDesign group arm z) * (Causalean.Stat.armGroupEvent group arm a k).indicator (fun x => 1) (z i) ∂MeasureTheory.Measure.pi fun x => mu` | If group labels, arm labels, and outcomes are measurable, every supported residual has a finite second moment, each cell's residual second moment obeys the stated envelope, and the design weight is nonnegative, the weighted residual square at one sample coordinate is bounded in expectation by the corresponding weighted cell indicator envelope. |
| `Stat.integral_occupancyDesignVarianceFactor_le_reciprocal` | `Measurable group → Measurable arm → ∀ (epsilon : ℝ), 0 < epsilon → epsilon ≤ 1 / 2 → (∀ (k : kappa), 0 < (mu (Causalean.Stat.groupEvent group k)).toReal → ∀ (a : Bool), epsilon * (mu (Causalean.Stat.groupEvent group k)).toReal ≤ (mu (Causalean.Stat.armGroupEvent group arm a k)).toReal) → (∫ (z : Fin n → Omega), Causalean.Stat.occupancyDesignVarianceFactor group arm z ∂MeasureTheory.Measure.pi fun x => mu) ≤ 16 / (epsilon ^ 2 * (1 - epsilon)) * ∫ (z : Fin n → Omega), Causalean.Stat.inverseUsableGroupTotal group arm z ∂MeasureTheory.Measure.pi fun x => mu` | If group and arm labels are measurable, the overlap margin is positive and at most one half, and both arms receive at least that share in every positive-mass group, the expected design variance factor is at most sixteen divided by the squared margin times one minus the margin, multiplied by expected reciprocal usable occupancy. This includes zero-mass groups, empty samples, empty group types, and every zero-count boundary. |
<!-- /GEN -->

#### 12a5.5. `Stat/Sample/OccupancyWeightedMean/Variance.lean`

<!-- GEN:Causalean.Stat.Sample.OccupancyWeightedMean.Variance -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.occupancyWeightedResidual_memLp_two` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.supportedArmGroupResidual group arm Y center a k) 2 mu) → MeasureTheory.MemLp (Causalean.Stat.occupancyWeightedResidual mu group arm Y center) 2 (MeasureTheory.Measure.pi fun x => mu)` | If group labels, arm labels, and outcomes are measurable and every arm/group-supported residual has a finite second moment, the zero-safe occupancy-weighted residual has a finite second moment under every finite independent product sample. |
| `Stat.integral_occupancyWeightedResidual_sq_le_reciprocal` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.supportedArmGroupResidual group arm Y center a k) 2 mu) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.armGroupEvent group arm a k, Causalean.Stat.armGroupResidual Y center a k omega ∂mu = 0) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.armGroupEvent group arm a k, Causalean.Stat.armGroupResidual Y center a k omega ^ 2 ∂mu ≤ (mu (Causalean.Stat.armGroupEvent group arm a k)).toReal * V ^ 2) → 0 < epsilon → epsilon ≤ 1 / 2 → (∀ (k : kappa), 0 < (mu (Causalean.Stat.groupEvent group k)).toReal → ∀ (a : Bool), epsilon * (mu (Causalean.Stat.groupEvent group k)).toReal ≤ (mu (Causalean.Stat.armGroupEvent group arm a k)).toReal) → (∫ (z : Fin n → Omega), Causalean.Stat.occupancyWeightedResidual mu group arm Y center z ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ 16 / (epsilon ^ 2 * (1 - epsilon)) * V ^ 2 * ∫ (z : Fin n → Omega), if 0 < Causalean.Stat.usableGroupTotal group arm z then (↑(Causalean.Stat.usableGroupTotal group arm z))⁻¹ else 0 ∂MeasureTheory.Measure.pi fun x => mu` | If group labels, arm labels, and outcomes are measurable, every supported residual has a finite second moment, residuals are centered within every arm/group cell, their cellwise second moments obey the stated envelope, the overlap margin is positive and at most one half, and both arms receive at least that share in every positive-mass group, the squared occupancy-weighted residual has product-law expectation at most sixteen divided by the squared margin times one minus the margin, multiplied by the squared envelope and expected reciprocal usable occupancy. |
<!-- /GEN -->

### 12a6. `Stat/Sample/FiniteStratumMarkedRatioMse/` — fixed-stratum marked ratios

Totalized finite-stratum arm means and treated-minus-control scores for a
deterministic set of categories. The error decomposition keeps centered mark
noise, empirical category-mass fluctuation, and the remainder from empirically
empty arms separate. Exact missing-count moments and overlap bounds combine to
give square-integrability and boundary-safe mean-squared-error guarantees under
cellwise second moments, without requiring bounded outcomes.

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse -->
_(no documented declarations in Causalean.Stat.Sample.FiniteStratumMarkedRatioMse)_
<!-- /GEN -->

#### 12a6.1. `Stat/Sample/FiniteStratumMarkedRatioMse/Basic.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.safeSampleSize` | `ℕ → ℝ` | For a nonnegative integer sample size, the safe real sample size is one when the sample is empty and otherwise equals the ordinary sample size. |
| `Stat.FiniteStratumMarkedRatioMse.categoryArmCount` | `(Omega → kappa) → (Omega → Bool) → (Fin m → Omega) → Bool → kappa → ℕ` | For a category-label function, a Boolean arm-label function, a finite sample, an arm value, and a category, the empirical arm--category count is the number of sample coordinates having both specified labels. |
| `Stat.FiniteStratumMarkedRatioMse.categoryCount` | `(Omega → kappa) → (Omega → Bool) → (Fin m → Omega) → kappa → ℕ` | For a category-label function, a Boolean arm-label function, a finite sample, and a category, the empirical category count is the number of sample coordinates in that category across both arm values. |
| `Stat.FiniteStratumMarkedRatioMse.armCategoryEvent` | `(Omega → kappa) → (Omega → Bool) → Bool → kappa → Set Omega` | For a category-label function, a Boolean arm-label function, an arm value, and a category, the arm--category event is the set of observations with both requested labels. |
| `Stat.FiniteStratumMarkedRatioMse.categoryEvent` | `(Omega → kappa) → kappa → Set Omega` | For a category-label function and a category, the category event is the set of observations assigned that category. |
| `Stat.FiniteStratumMarkedRatioMse.categoryMass` | `MeasureTheory.Measure Omega → (Omega → kappa) → kappa → ℝ` | For a measure on the observation space, a category-label function, and a category, the population category mass is the real-valued measure of the category event. |
| `Stat.FiniteStratumMarkedRatioMse.armCategoryMass` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → Bool → kappa → ℝ` | For a measure on the observation space, a category-label function, a Boolean arm-label function, an arm value, and a category, the population arm--category mass is the real-valued measure of their joint-label event. |
| `Stat.FiniteStratumMarkedRatioMse.supportedArmMark` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Bool → kappa → Omega → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, an arm value, and a category, the supported arm mark equals the mark on the corresponding arm--category event and zero outside it. |
| `Stat.FiniteStratumMarkedRatioMse.armMarkSum` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Fin m → Omega) → Bool → kappa → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, a finite sample, an arm value, and a category, the empirical arm--category mark sum is the sum of supported marks over all sample coordinates. |
| `Stat.FiniteStratumMarkedRatioMse.totalizedArmMean` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Fin m → Omega) → Bool → kappa → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, a finite sample, an arm value, and a category, the totalized empirical arm mean is the arm--category mark sum divided by its count when that count is positive, and zero otherwise. |
| `Stat.FiniteStratumMarkedRatioMse.populationArmMean` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Bool → kappa → ℝ` | For a measure on the observation space, a category-label function, a Boolean arm-label function, a real-valued mark, an arm value, and a category, the population arm--category mean is the mark integral over that cell divided by its positive mass, and zero when its mass is zero. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmScore` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Finset kappa → Bool → (Fin m → Omega) → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, a finite set of categories, an arm value, and a finite sample, the fixed-stratum arm score is the sum of totalized arm means weighted by empirical category occupancy divided by the nominal sample size. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedRatio` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Finset kappa → (Fin m → Omega) → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, a finite set of categories, and a finite sample, the fixed-stratum marked ratio is the fixed-stratum arm score for the true arm minus that for the false arm. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmTarget` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Finset kappa → Bool → ℝ` | For a measure on the observation space, a category-label function, a Boolean arm-label function, a real-valued mark, a finite set of categories, and an arm value, the fixed-stratum arm target is the sum of population arm--category means weighted by population category masses. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedTarget` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → Finset kappa → ℝ` | For a measure on the observation space, a category-label function, a Boolean arm-label function, a real-valued mark, and a finite set of categories, the fixed-stratum marked target is the true-arm target minus the false-arm target. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenterTarget` | `MeasureTheory.Measure Omega → (Omega → kappa) → Finset kappa → (Bool → kappa → ℝ) → Bool → ℝ` | For a measure on the observation space, a category-label function, a finite set of categories, an arm-and-category center function, and an arm value, the fixed-stratum arm center target is the sum of category masses multiplied by the corresponding supplied centers. |
| `Stat.FiniteStratumMarkedRatioMse.supportedArmResidual` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → Bool → kappa → Omega → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, an arm-and-category center function, an arm value, and a category, the supported arm residual is the mark minus its supplied cell center on that cell and zero outside it. |
| `Stat.FiniteStratumMarkedRatioMse.totalizedArmResidualMean` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → (Fin m → Omega) → Bool → kappa → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, an arm-and-category center function, a finite sample, an arm value, and a category, the totalized empirical residual mean is zero for an empty arm--category cell and otherwise averages its supported centered residuals. |
| `Stat.FiniteStratumMarkedRatioMse.missingArmCount` | `(Omega → kappa) → (Omega → Bool) → (Fin m → Omega) → Bool → kappa → ℕ` | For a category-label function, a Boolean arm-label function, a finite sample, an arm value, and a category, the missing-arm count is the category occupancy when the requested arm--category count is zero, and zero otherwise. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenteredNoise` | `(Omega → kappa) → (Omega → Bool) → (Omega → ℝ) → (Bool → kappa → ℝ) → Finset kappa → Bool → (Fin m → Omega) → ℝ` | For a category-label function, a Boolean arm-label function, a real-valued mark, an arm-and-category center function, a finite set of categories, an arm value, and a finite sample, the fixed-stratum arm centered noise is the occupancy-weighted sum of totalized cell residual means. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMissingRemainder` | `(Omega → kappa) → (Omega → Bool) → (Bool → kappa → ℝ) → Finset kappa → Bool → (Fin m → Omega) → ℝ` | For a category-label function, a Boolean arm-label function, an arm-and-category center function, a finite set of categories, an arm value, and a finite sample, the fixed-stratum arm missing remainder is the nominal-sample-size-normalized sum of cell centers times category occupancies whose requested arm is absent. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMassFluctuation` | `MeasureTheory.Measure Omega → (Omega → kappa) → (Omega → Bool) → (Bool → kappa → ℝ) → Finset kappa → Bool → (Fin m → Omega) → ℝ` | For a measure on the observation space, a category-label function, a Boolean arm-label function, an arm-and-category center function, a finite set of categories, an arm value, and a finite sample, the fixed-stratum arm mass fluctuation is the sum of each center multiplied by empirical category occupancy divided by nominal sample size minus population category mass. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_categoryArmCount` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.FiniteStratumMarkedRatioMse.categoryArmCount group arm z a k` | Measurable group and arm labels make every fixed empirical arm/category count measurable on the product sample space. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_categoryCount` | `Measurable group → Measurable arm → ∀ (k : kappa), Measurable fun z => Causalean.Stat.FiniteStratumMarkedRatioMse.categoryCount group arm z k` | Measurable group and arm labels make every fixed empirical category count measurable on the product sample space. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_armMarkSum` | `Measurable group → Measurable arm → Measurable Y → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.FiniteStratumMarkedRatioMse.armMarkSum group arm Y z a k` | Measurable group and arm labels and a measurable mark make every empirical arm/category mark sum measurable. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_totalizedArmMean` | `Measurable group → Measurable arm → Measurable Y → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.FiniteStratumMarkedRatioMse.totalizedArmMean group arm Y z a k` | Measurable group and arm labels and a measurable mark make every totalized empirical arm/category mark mean measurable, including at zero count. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_fixedStratumMarkedRatio` | `Measurable group → Measurable arm → Measurable Y → Measurable (Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedRatio group arm Y H)` | Measurable group and arm labels and a measurable mark make the fixed-set marked ratio score measurable. |
| `Stat.FiniteStratumMarkedRatioMse.measurable_missingArmCount` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), Measurable fun z => Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a k` | Measurable group and arm labels make each missing-arm category count measurable. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmScore_sub_centerTarget_decomposition` | `Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmScore group arm Y H a z - Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenterTarget mu group H center a = Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenteredNoise group arm Y center H a z + Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMassFluctuation mu group arm center H a z - Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMissingRemainder group arm center H a z` | The single-arm empirical score error around a supplied center target is exactly centered ratio noise plus empirical-mass fluctuation minus the explicit missing-arm remainder. |
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenterTarget_eq_target` | `Measurable group → Measurable arm → (∀ (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, Y omega - center a k ∂mu = 0) → (∀ k ∈ H, 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenterTarget mu group H center a = Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmTarget mu group arm Y H a` | Measurable group and arm labels, square-integrable supported residuals, cellwise residual centering, and positive arm mass in every selected positive-mass category ensure that the center-weighted target equals the population arm target. |
<!-- /GEN -->

#### 12a6.2. `Stat/Sample/FiniteStratumMarkedRatioMse/NestedCountBound.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.NestedCountBound -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.sampleIndexSet` | `(I → Omega) → Set Omega → Finset I` | Given a finite index set, an observation space, an observation assigned to each index, and a set of observations, the sample index set is the finite set of precisely those indices whose assigned observation belongs to that set. |
| `Stat.FiniteStratumMarkedRatioMse.integral_nested_count_sq_mul_totalized_inverse_le` | `MeasurableSet C → MeasurableSet R → R ⊆ C → ∀ (epsilon : ℝ), 0 < epsilon → epsilon * (mu C).toReal ≤ (mu R).toReal → (∫ (z : Fin m → Omega), ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.sampleIndexSet z C).card ^ 2 * if 0 < (Causalean.Stat.FiniteStratumMarkedRatioMse.sampleIndexSet z R).card then (↑(Causalean.Stat.FiniteStratumMarkedRatioMse.sampleIndexSet z R).card)⁻¹ else 0 ∂MeasureTheory.Measure.pi fun x => mu) ≤ 2 * ↑m * (mu C).toReal / epsilon` | Measurable outer and inner events, nesting of the inner event inside the outer event, a positive overlap margin, and inner-event mass at least that margin times outer-event mass imply that the expected squared outer count times the zero-safe inverse inner count is at most twice sample size times outer-event mass divided by the margin. |
<!-- /GEN -->

#### 12a6.3. `Stat/Sample/FiniteStratumMarkedRatioMse/MissingMoments.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.MissingMoments -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.integral_missingArmCount_eq` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), (∫ (z : Fin m → Omega), ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a k) ∂MeasureTheory.Measure.pi fun x => mu) = ↑m * (Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) * (1 - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) ^ (m - 1)` | Measurable group and arm labels imply that the expected category occupancy retained only when one arm is absent equals the sample size times the off-arm category mass times the empty-arm probability for the other coordinates. |
| `Stat.FiniteStratumMarkedRatioMse.integral_missingArmCount_sq_eq` | `Measurable group → Measurable arm → ∀ (a : Bool) (k : kappa), (∫ (z : Fin m → Omega), ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a k) ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) = ↑m * (Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) * (1 - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) ^ (m - 1) + ↑(m.descFactorial 2) * (Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) ^ 2 * (1 - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) ^ (m - 2)` | Measurable group and arm labels imply that the exact second moment of one missing-arm category count is the sum of its one-coordinate diagonal and ordered two-coordinate contributions. |
| `Stat.FiniteStratumMarkedRatioMse.integral_missingArmCount_mul_eq` | `Measurable group → Measurable arm → ∀ (a : Bool) {k l : kappa}, k ≠ l → (∫ (z : Fin m → Omega), ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a k) * ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a l) ∂MeasureTheory.Measure.pi fun x => mu) = ↑(m.descFactorial 2) * (Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) * (Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group l - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a l) * (1 - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k - Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a l) ^ (m - 2)` | Measurable group and arm labels and distinct categories imply that their missing-arm counts have the exact ordered-pair cross moment obtained by excluding the union of the two arm/category cells. |
| `Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope` | `MeasureTheory.Measure Omega → (Omega → kappa) → ℕ → ℝ → Finset kappa → ℝ` | For a measure on the observation space, a category-label function, a nonnegative integer sample size, a real overlap margin, and a finite set of categories, the missing-arm exponential envelope is the sum, over selected categories, of category mass times the exponential decay determined by the sample size, overlap margin, and that mass. |
| `Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope` | `MeasureTheory.Measure Omega → (Omega → kappa) → ℕ → ℝ → ℝ → Finset kappa → ℝ` | For a measure on the observation space, a category-label function, a nonnegative integer sample size, a real overlap margin, a real mass lower bound, and a finite set of categories, the lower-mass missing envelope first sets its squared overlap denominator to the squared half-adjusted sample-size overlap margin times the mass lower bound, and then uses the number of selected categories divided by that denominator when it is positive, and their total category mass otherwise. This totalization keeps the envelope defined for small samples, zero overlap, and a zero mass lower bound. |
| `Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope_le_lowerMass` | `Measurable group → ∀ (m : ℕ) (epsilon B : ℝ) (H : Finset kappa), 0 < epsilon → (∀ k ∈ H, B ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) → Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope mu group m epsilon H ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope mu group m epsilon B H` | A measurable group label, a positive overlap margin, and a deterministic lower bound on every selected category mass ensure that the exponential missing-arm envelope is at most the boundary-safe lower-mass envelope. |
| `Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope_eq_of_pos` | `3 ≤ m → 0 < epsilon → 0 < B → Causalean.Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope mu group m epsilon B H = ↑H.card / ((↑(m - 2) / 2 * epsilon) ^ 2 * B)` | A sample size of at least three, a positive overlap margin, and a positive category-mass lower bound make the boundary-safe lower-mass envelope equal its inverse-polynomial expression. |
<!-- /GEN -->

#### 12a6.4. `Stat/Sample/FiniteStratumMarkedRatioMse/MissingBound.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.MissingBound -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.integrable_missingArmCount_mul` | `Measurable group → Measurable arm → ∀ (a : Bool) (k l : kappa), MeasureTheory.Integrable (fun z => ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a k) * ↑(Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmCount group arm z a l)) (MeasureTheory.Measure.pi fun x => mu)` | When the stratum label and the arm assignment are measurable, the product of the counts of missing arm/category cells for two categories is integrable over the product sample, because the two counts are bounded by the sample size. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArmMissingRemainder_sq_le_aux` | `Measurable group → Measurable arm → (∀ (k : kappa), \|center a k\| ≤ M) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∫ (z : Fin m → Omega), Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMissingRemainder group arm center H a z ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ M ^ 2 * (1 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m + Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope mu group m epsilon H ^ 2)` | Measurable group and arm labels, cell centers bounded in absolute value by the envelope, a positive overlap margin, and arm mass at least that margin times category mass imply that the missing-arm remainder has a boundary-safe diagonal-plus-exponential second-moment bound. |
<!-- /GEN -->

#### 12a6.5. `Stat/Sample/FiniteStratumMarkedRatioMse/CenteredNoiseBound.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.CenteredNoiseBound -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArmCenteredNoise_sq_le` | `Measurable group → Measurable arm → Measurable Y → (∀ (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, Y omega - center a k ∂mu = 0) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, (Y omega - center a k) ^ 2 ∂mu ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k * M ^ 2) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∫ (z : Fin m → Omega), Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmCenteredNoise group arm Y center H a z ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ (2 * M ^ 2 * ∑ k ∈ H, Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) / (Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m * epsilon)` | Measurable group and arm labels and a measurable mark, square-integrable supported residuals, cellwise residual centering, the stated cellwise second-moment envelope, a positive overlap margin, and arm mass at least that margin times category mass imply that the occupancy-weighted centered ratio noise has second moment at most twice the squared envelope times selected mass, divided by safe sample size and overlap. |
<!-- /GEN -->

#### 12a6.6. `Stat/Sample/FiniteStratumMarkedRatioMse/Main.lean`

<!-- GEN:Causalean.Stat.Sample.FiniteStratumMarkedRatioMse.Main -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedRatio_memLp_two` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedRatio group arm Y H) 2 (MeasureTheory.Measure.pi fun x => mu)` | Measurable group and arm labels and a measurable mark together with finite second moments for every supported residual cell imply finite second moments for the treated-minus-control fixed-set score. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArmMassFluctuation_sq_le` | `Measurable group → Measurable arm → (∀ (k : kappa), \|center a k\| ≤ M) → (∫ (z : Fin m → Omega), Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMassFluctuation mu group arm center H a z ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ M ^ 2 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m` | Measurable group and arm labels and cell centers bounded in absolute value by the envelope imply that the empirical category-mass fluctuation has second moment at most the squared envelope divided by the safe sample size, including for an empty sample. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArmMissingRemainder_sq_le` | `Measurable group → Measurable arm → (∀ (k : kappa), \|center a k\| ≤ M) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∫ (z : Fin m → Omega), Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmMissingRemainder group arm center H a z ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ M ^ 2 * (1 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m + Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope mu group m epsilon H ^ 2)` | Measurable group and arm labels, cell centers bounded in absolute value by the envelope, a positive overlap margin, and arm mass at least that margin times category mass imply that the normalized aggregate empty-arm remainder has second moment bounded by a parametric diagonal term plus the squared exponentially damped missing-arm envelope. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArm_error_sq_le_exponential` | `Measurable group → Measurable arm → Measurable Y → (∀ (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, Y omega - center a k ∂mu = 0) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, (Y omega - center a k) ^ 2 ∂mu ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k * M ^ 2) → (∀ (k : kappa), \|center a k\| ≤ M) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∫ (z : Fin m → Omega), (Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmScore group arm Y H a z - Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmTarget mu group arm Y H a) ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ M ^ 2 * ((8 * ∑ k ∈ H, Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) / (Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m * epsilon) + 6 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m + 4 * Causalean.Stat.FiniteStratumMarkedRatioMse.missingArmExponentialEnvelope mu group m epsilon H ^ 2)` | Measurable group and arm labels and a measurable mark, square-integrable supported residuals, cellwise residual centering, the stated residual second-moment envelope, bounded cell centers, a positive overlap margin, and arm mass at least that margin times category mass imply a uniform one-arm fixed-set mean-squared-error bound with an explicit exponentially damped missing-arm remainder. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumArm_error_sq_le` | `Measurable group → Measurable arm → Measurable Y → (∀ (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, Y omega - center a k ∂mu = 0) → (∀ (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, (Y omega - center a k) ^ 2 ∂mu ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k * M ^ 2) → (∀ (k : kappa), \|center a k\| ≤ M) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∀ k ∈ H, B ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) → (∫ (z : Fin m → Omega), (Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmScore group arm Y H a z - Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumArmTarget mu group arm Y H a) ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ M ^ 2 * ((8 * ∑ k ∈ H, Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) / (Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m * epsilon) + 6 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m + 4 * Causalean.Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope mu group m epsilon B H ^ 2)` | Measurable group and arm labels and a measurable mark, square-integrable supported residuals, cellwise residual centering, the stated residual second-moment envelope, bounded cell centers, a positive overlap margin, arm mass at least that margin times category mass, and a deterministic lower bound on every selected category mass imply the boundary-safe one-arm mean-squared-error bound with an inverse-polynomial missing-arm envelope. |
| `Stat.FiniteStratumMarkedRatioMse.integral_fixedStratumMarkedRatio_error_sq_le` | `Measurable group → Measurable arm → Measurable Y → (∀ (a : Bool) (k : kappa), MeasureTheory.MemLp (Causalean.Stat.FiniteStratumMarkedRatioMse.supportedArmResidual group arm Y center a k) 2 mu) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, Y omega - center a k ∂mu = 0) → (∀ (a : Bool) (k : kappa), ∫ (omega : Omega) in Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryEvent group arm a k, (Y omega - center a k) ^ 2 ∂mu ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k * M ^ 2) → (∀ (a : Bool) (k : kappa), \|center a k\| ≤ M) → 0 < epsilon → (∀ (k : kappa), 0 < Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k → ∀ (a : Bool), epsilon * Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.armCategoryMass mu group arm a k) → (∀ k ∈ H, B ≤ Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) → (∫ (z : Fin m → Omega), (Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedRatio group arm Y H z - Causalean.Stat.FiniteStratumMarkedRatioMse.fixedStratumMarkedTarget mu group arm Y H) ^ 2 ∂MeasureTheory.Measure.pi fun x => mu) ≤ 4 * M ^ 2 * ((8 * ∑ k ∈ H, Causalean.Stat.FiniteStratumMarkedRatioMse.categoryMass mu group k) / (Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m * epsilon) + 6 / Causalean.Stat.FiniteStratumMarkedRatioMse.safeSampleSize m + 4 * Causalean.Stat.FiniteStratumMarkedRatioMse.lowerMassMissingEnvelope mu group m epsilon B H ^ 2)` | Measurable group and arm labels and a measurable mark, square-integrable supported residuals in both arms, cellwise residual centering, the stated residual second-moment envelope, bounded cell centers, a positive overlap margin, both arm masses at least that margin times category mass, and a deterministic lower bound on every selected category mass imply that the treated-minus-control fixed-set score obeys the boundary-safe mean-squared-error bound at the same parametric and missing-arm scale, up to the universal two-arm factor. |
<!-- /GEN -->

### 12a'. `Stat/Sample/PiTransport.lean`

Canonical product-space construction and finite-product transport for i.i.d.
samples.

<!-- GEN:Causalean.Stat.Sample.PiTransport -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.iidSample_infinitePi` | `(P : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure P] → Causalean.Stat.IIDSample (ℕ → X) X (MeasureTheory.Measure.infinitePi fun x => P) P` | Given a measurable observation space and a probability measure on it, the independent, identically distributed sample on the infinite product space is formed by coordinate projections, and every coordinate has the given probability measure as its marginal law. |
| `Stat.iidSample_finN_pushforward` | `MeasureTheory.Measure.map (fun ω k => S.Z (↑k) ω) μ = MeasureTheory.Measure.pi fun x => P` | For an i.i.d. sample `S` and a fixed horizon `n`, the joint map of the first `n` sample points pushes `μ` forward to the product measure on `Fin n → X`. |
| `Stat.iidSample_finN_measurable` | `Measurable fun ω k => S.Z (↑k) ω` | The joint observable `Ψ ω k = S.Z k ω` is measurable `Ω → (Fin n → X)`. |
| `Stat.event_pullback_along_iidSample` | `MeasurableSet E → ∀ {δ : ℝ}, (MeasureTheory.Measure.pi fun x => P) E ≥ 1 - ENNReal.ofReal δ → have Ψ := fun ω k => S.Z (↑k) ω; have E' := Ψ ⁻¹' E; MeasurableSet E' ∧ μ E' ≥ 1 - ENNReal.ofReal δ` | Event transport along an IID sample (`Fin n` version). Given a measurable event `E` in the space of length-`n` outcome tuples whose product-measure probability under `n` independent copies of the population law is at least $1-\delta$, the pullback of `E` along the joint observable built from the first `n` coordinates of the i.i.d. sample is a measurable event on the underlying sample space, with probability at least $1-\delta$. |
| `Stat.measurePreserving_pi_restrict_dep` | `MeasureTheory.MeasurePreserving (fun s i => s ↑i) (MeasureTheory.Measure.pi μ) (MeasureTheory.Measure.pi fun i => μ ↑i)` | Dropping the coordinates outside a decidable sub-index of a product of probability measures leaves the product measure over that sub-index: the retained coordinates carry exactly their own product law, with no trace of the discarded ones. |
| `Stat.measurePreserving_pi_restrict` | `MeasureTheory.MeasurePreserving (fun s i => s ↑i) (MeasureTheory.Measure.pi fun x => μ) (MeasureTheory.Measure.pi fun x => μ)` | Dropping the coordinates outside a decidable sub-index of an i.i.d. product sample leaves the i.i.d. product sample over that sub-index. |
| `Stat.map_pi_restrict` | `MeasureTheory.Measure.map (fun s i => s ↑i) (MeasureTheory.Measure.pi fun x => μ) = MeasureTheory.Measure.pi fun x => μ` | The law of the sub-index coordinates of an i.i.d. product sample is the product law over that sub-index; this is the pushforward packaging of the measure-preserving statement. |
| `Stat.integral_comp_pi_restrict` | `(∫ (s : ι → X), g fun i => s ↑i ∂MeasureTheory.Measure.pi fun x => μ) = ∫ (z : Subtype p → X), g z ∂MeasureTheory.Measure.pi fun x => μ` | Averaging a function of the sub-index coordinates over the whole i.i.d. product sample is the same as averaging it over an i.i.d. product sample indexed by the sub-index alone. |
| `Stat.measurePreserving_pi_restrict_finset` | `MeasureTheory.MeasurePreserving (fun s i => s ↑i) (MeasureTheory.Measure.pi fun x => μ) (MeasureTheory.Measure.pi fun x => μ)` | Dropping the coordinates outside a finite set of indices of an i.i.d. product sample leaves the i.i.d. product sample indexed by that finite set. |
| `Stat.map_pi_restrict_finset` | `MeasureTheory.Measure.map (fun s i => s ↑i) (MeasureTheory.Measure.pi fun x => μ) = MeasureTheory.Measure.pi fun x => μ` | The law of the coordinates in a finite index set of an i.i.d. product sample is the product law over that finite set; this is the pushforward packaging of the measure-preserving statement. |
| `Stat.integral_comp_pi_restrict_finset` | `(∫ (s : ι → X), g fun i => s ↑i ∂MeasureTheory.Measure.pi fun x => μ) = ∫ (z : ↥S → X), g z ∂MeasureTheory.Measure.pi fun x => μ` | Averaging a function of the coordinates in a finite index set over the whole i.i.d. product sample is the same as averaging it over an i.i.d. product sample indexed by that finite set. |
| `Stat.HasIIDSample` | `MeasureTheory.Measure X → Prop` | Given a measurable observation space and a measure on it, the existence-of-an-independent, identically distributed-sample assertion states that there is a sample space with a measurable structure and a measure carrying a nonempty collection of independent, identically distributed samples whose common marginal law is the given measure. |
| `Stat.hasIIDSample_of_isProbabilityMeasure` | `Causalean.Stat.HasIIDSample P` | An i.i.d. sample with a given law always exists: every probability measure is the common marginal of some independent, identically distributed sample, realised on the infinite product space by the coordinate projections. |
| `Stat.hasIIDSample_iff_isProbabilityMeasure` | `Causalean.Stat.HasIIDSample P ↔ MeasureTheory.IsProbabilityMeasure P` | The existence of an i.i.d. sample with a prescribed common marginal is equivalent to that marginal being a probability measure. |
| `Stat.measurable_finCoordinatewise` | `Measurable phi → Measurable fun z i => phi (z i)` | Applying a measurable observation rule separately to every position of a finite sample produces a measurable recoded sample. |
| `Stat.map_pi_finCoordinatewise` | `Measurable phi → MeasureTheory.Measure.map (fun z i => phi (z i)) (MeasureTheory.Measure.pi fun x => mu) = MeasureTheory.Measure.pi fun x => MeasureTheory.Measure.map phi mu` | Under a common probability law, applying a measurable observation rule coordinate by coordinate turns the finite product law into the finite product of the recoded marginal law. |
| `Stat.integral_comp_finCoordinatewise` | `Measurable phi → ∀ (g : (Fin n → Y) → ℝ), Measurable g → (∫ (z : Fin n → X), g fun i => phi (z i) ∂MeasureTheory.Measure.pi fun x => mu) = ∫ (y : Fin n → Y), g y ∂MeasureTheory.Measure.pi fun x => MeasureTheory.Measure.map phi mu` | Under a common probability law, if the observation rule is measurable and the real-valued statistic of the recoded sample is measurable, then its expectation after coordinatewise recoding equals its expectation under the product of the recoded marginal law. |
<!-- /GEN -->

### 12b. `Stat/Limit/Convergence.lean`

| Declaration | Statement | Description |
|---|---|---|
| `Tendsto_inProb`, `Tendsto_L2`, `Tendsto_dist` | `def`s | Aliases for convergence in probability / `L²` / distribution (Lévy–Prokhorov-style at the pushforward-measure level). |
| `Tendsto_dist.const_mul_tendsto` | `Xn ⇒ Q` and `aₙ → a₀` ⇒ `aₙ·Xn ⇒ (x ↦ a₀x)_#Q` | Project wrapper around `MeasureTheory.ProbabilityMeasure.tendsto_map_mul_of_tendsto`; handles the pushforward-map bookkeeping for `Tendsto_dist`. **Proved** (depends on Mathlib stub). |
| `IsBigOp Xn rn μ`, `IsLittleOp Xn rn μ` | `def`s | `O_p(rn)` and `o_p(rn)` per `def:est-stoch-order`. |
| `IsLittleOp.mul_isBigOp` | `o_p(rn)·O_p(sn) = o_p(rn·sn)` | Requires `0 < rn n`, `0 < sn n` for all `n`. **Proved.** |
| `IsBigOp.add` | `O_p(rn) + O_p(rn) = O_p(rn)` | **Proved.** |
| `IsBigOp.mul_isLittleOp_one_isLittleOp` | Slutsky-style: `O_p(1)·o_p(1) = o_p(1)` | **Proved.** |
| `Tendsto_dist.tightness` | `Xn ⇒ Q` ⇒ `Xn = O_p(1)` | Tightness of a single distributional limit on ℝ: pick a tail set `{|x| ≥ N}` with small `Q`-mass via continuity from above on the antitone family `{|x| ≥ n}`, then portmanteau on the closed tail to bound `limsup μ.map(Xn n) {|x| > N} ≤ Q {|x| ≥ N}`. **Proved by Codex.** Δ-method helper. |
| `IsBigOp.const_mul_tendsto_zero` | `Xn = O_p(1)` ∧ `aₙ → 0` ⇒ `aₙ·Xn = o_p(1)` | Degenerate Slutsky: bounds the bad event `{ε < |aₙ Xn|}` by `{M < |Xn|}` once `|aₙ| < ε/M`, using only the deterministic shrinkage `|aₙ| → 0`. **Proved by Codex.** Δ-method helper. |

### 12b'. `Stat/Limit/ConvergenceVec.lean`

Vector analogue of `Convergence.lean`'s `Tendsto_dist` plus the Slutsky lemmas needed by the multivariate Δ-method (`Stat/Inference/DeltaMethod.lean`) and the vector asymptotic-normality corollary (`Stat/CLT/AsymptoticLinearityVec.lean`).

| Declaration | Statement | Description |
|---|---|---|
| `Tendsto_dist_vec` | `def` | Vector convergence in distribution: `μ.map (Xn n)` converges weakly to `Q` in `ProbabilityMeasure E` for `E` a pseudo-metric space with `OpensMeasurableSpace`. Direct port of scalar `Tendsto_dist`. |
| `Tendsto_dist_vec.add_isLittleOp_one` | vector Slutsky absorption | `Xn ⇒ Q` and `‖Yn − Xn‖ = o_p(1)` ⇒ `Yn ⇒ Q`, on `[NormedAddCommGroup E] [BorelSpace E]`. **Proved by Codex** as a metric-space port of the scalar proof using the bounded-Lipschitz portmanteau characterization (`tendsto_iff_forall_lipschitz_integral_tendsto`); integrand `F : E → ℝ` is split over `{‖F (Yn) − F (Xn)‖ < L·ε/2}` and its complement, with the complement controlled via Lipschitz pullback through `‖Yn − Xn‖`. |
| `Tendsto_dist_vec.congr_ae` | eventual a.e. congruence | Vector analogue of `Tendsto_dist.congr_ae`. **Proved.** |
| `Tendsto_dist_vec.map_continuous` | continuous-map pushforward | `g : E → F` continuous ⇒ `Tendsto_dist_vec Xn Q μ` ⇒ `Tendsto_dist_vec (g ∘ Xn) (Q.map g) μ`. **Proved by Codex** via `MeasureTheory.ProbabilityMeasure.tendsto_map_of_tendsto_of_continuous`. Used by the multivariate Δ-method to push forward by the Fréchet derivative `Dg`. |

### 12c0. `Stat/CLT/GaussianTail.lean`

| Declaration | Statement | Description | Status |
|---|---|---|---|
| `gaussian_tail_small` | `0<v → 0<ε → ∃ R>0, N(0,v)((-∞,-R]) ≤ ε ∧ N(0,v)([R,∞)) ≤ ε` | Gaussian half-line tail control: both one-sided tails of `N(0,v)` go below any `ε` for a large cutoff (continuity-from-above on half-line families shrinking to `∅`). Factored out of the quantile-Bahadur rate proof; reused by `PartialID/Inference/ImbensManski.lean` | **Proved** |

### 12c. `Stat/CLT/AsymptoticLinearity.lean`

| Declaration | Statement | Description |
|---|---|---|
| `gaussianMeasure m σ²` | `def` | Real-typed wrapper around `ProbabilityTheory.gaussianReal m σ².toNNReal`; negative variances clip to `0` (Dirac at `m`). **Proved.** |
| `instIsProbabilityMeasureGaussianMeasure` | instance | `IsProbabilityMeasure (gaussianMeasure m v)` for every real `m, v`, inherited from Mathlib's `gaussianReal`. **Proved.** |
| `IsAsymLinear θn θ₀ ψ S I` | structure | Asymptotic linearity per `def:est-asym-linear`: `mean_zero`, `finite_var` (encoded as `Integrable (ψ²)`), `remainder = o_p(1)` over an explicit index family `I` (classically `I n = {0, ..., n-1}`). |
| `IsAsymLinear.normalizedSum S ψ I n` | `def` | The normalised partial sum `(1/√|I n|) Σ_{i∈I(n)} ψ(Z_i)` along an i.i.d. sample and an index family `I`. |
| `IsAsymLinear.rescaledEstimator θn θ₀ I n` | `def` | The rescaled estimator `√|I n| (θn n − θ₀)` for index family `I`. |
| `IIDSample.clt_normalized_sum` | `(1/√n) Σ ψ(Zᵢ) ⇒ N(0, ∫ψ² dP)` | CLT contact point: needs `ψ` measurable, mean-zero, square-integrable. **Proved** via `ProbabilityTheory.central_limit` from the upstream `RemyDegenne/CLT` package, with variance normalisation and a zero-variance degenerate branch. |
| `Tendsto_dist.add_isLittleOp_one` | Slutsky absorption | If `Xn ⇒ Q` in distribution and `Yn − Xn = o_p(1)`, then `Yn ⇒ Q`. **Proved** at the `Tendsto_dist` (measure-level) wrapper via the bounded-Lipschitz characterisation of weak convergence. |
| `Tendsto_dist.congr_ae` | eventual a.e. congruence | If `Xn ⇒ Q` and `Xn n =ᵐ Yn n` eventually, then `Yn ⇒ Q`. **Proved.** |
| `Tendsto_dist.const_mul_tendsto_gaussian` | deterministic scalar Slutsky for Gaussian targets | If `Xn ⇒ N(0, v)` and deterministic `a n → a₀`, then `a n · Xn ⇒ N(0, a₀²v)`. **Proved** (depends on `MeasureTheory.ProbabilityMeasure.tendsto_map_mul_of_tendsto`) by `Tendsto_dist.const_mul_tendsto` plus `gaussianReal_map_const_mul`. |
| `IsAsymLinear.tendsto_normal` | `√|I n|(θn − θ₀) ⇒ N(0, ∫ψ² dP)` | Headline corollary `prop:est-al-implies-an`: combines `clt_normalized_sum` with `Tendsto_dist.add_isLittleOp_one` to absorb the asymptotically-linear remainder (classical corollary uses `I n = Finset.range n`). **Proved.** |

### 12c'. `Stat/CLT/AsymptoticLinearityVec.lean`

Vector-θ companion to `AsymptoticLinearity.lean`. Adds the vector-valued asymptotic-linearity predicate `IsAsymLinearVec` parametric in a Euclidean space `E`, plus a bridge to the scalar form when `E = ℝ`, and the headline multivariate asymptotic-normality corollary.

| Declaration | Statement | Description |
|---|---|---|
| `IsAsymLinearVec θn θ₀ ψ S I` | structure | Vector-valued asymptotic linearity at `θ₀ : E` with vector IF `ψ : X → E`. Fields: `mean_zero` (Bochner integral), `finite_var` (`‖ψ‖² ∈ L¹`), `remainder` (norm `o_p(1)`). |
| `IsAsymLinearVec.normalizedSum`, `rescaledEstimator` | `def`s | Vector versions of the scalar accessors. |
| `IsAsymLinearVec.toScalar` | bridge | When `E = ℝ`, `IsAsymLinearVec` reduces to scalar `IsAsymLinear`. **Proved**: field-by-field rewrite using `‖x‖ = |x|` and `r • x = r * x` on `ℝ`. |
| `IsAsymLinear.toVec` | bridge | Scalar-to-vector lift. **Proved** (mirror of `toScalar`). |
| `IsAsymLinearVec.tendsto_normal_vec` | `theorem` | Multivariate analogue of `tendsto_normal`: given a vector CLT contact `_hCLT` (the partial-sum pushforward converges to `Q : ProbabilityMeasure E`), the rescaled estimator pushforward also converges to `Q`. **Proved by Codex** by absorbing `_h.remainder` into `_hCLT` via `Tendsto_dist_vec.add_isLittleOp_one`. The multivariate CLT itself is left to the caller. |

### 12d. `Stat/Orthogonality/Orthogonality.lean`

| Declaration | Statement | Description |
|---|---|---|
| `NeymanOrthogonal m η₀ θ₀ P` | `def` | Moment condition `∫ m η₀ x θ₀ dP = 0` plus vanishing Gâteaux derivative along every direction `η ∈ H`. Restricted realization sets are imposed at instantiation (cf. `H_ε` in `Estimation/ATE/InfluenceFunction.lean`). |

### 12e. `Stat/SampleSplit.lean` (umbrella)

Re-export umbrella over per-strategy split modules:
- `Stat/SampleSplit/OneShot.lean` — `OneShotSplit` (the original API, unchanged).
- `Stat/SampleSplit/KFold.lean` — `KFoldSplit` (K-fold cross-fitting).

Existing consumers that import `Causalean.Stat.SampleSplit` continue to work.

#### 12e.OneShot

| Declaration | Statement | Description |
|---|---|---|
| `OneShotSplit S` | structure | Fold-size schedule `n₁ : ℕ → ℕ` with `bound`, `grow`, `cogrow` per `def:est-one-shot-split`. |
| `OneShotSplit.foldA n` | `def` | `{0, …, n₁ n − 1}`. |
| `OneShotSplit.foldB n` | `def` | `{n₁ n, …, n − 1}` (filtered range). |
| `OneShotSplit.foldA_disjoint_foldB` | `Disjoint (foldA n) (foldB n)` | Pure combinatorics. **Proved.** |
| `OneShotSplit.folds_indep` | `IndepFun (Z\|foldA) (Z\|foldB) μ` | Direct corollary of `iIndepFun.indepFun_finset` plus disjointness. **Proved.** |
| `OneShotSplit.foldB_card` | `(foldB n).card = n - n₁ n` | Cardinality of the suffix-shaped fold-B (filter on `range n`). **Proved.** Used to derive eventual positivity of `(foldB n).card` from `cogrow`. |
| `OneShotSplit.foldB_card_tendsto` | `Tendsto (fun n => (foldB n).card) atTop atTop` | Direct corollary of `cogrow` + `foldB_card`. **Proved.** |

#### 12e.2. `Stat/SampleSplit/KFold.lean`

K-fold sample split (cross-fitting). Sibling to `OneShot.lean`. Used by `Estimation/OrthogonalMoments/DMLCrossFit.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `KFoldSplit S K` | structure | K disjoint folds with `partition`, `cover`, `grow`, `ratio` (`fold(n,k).card/n → 1/K`). |
| `KFoldSplit.trainComplement n k` | `def` | `{0,…,n-1} \ fold(n,k)`. |
| `KFoldSplit.fold_disjoint_trainComplement` | `lemma` | Disjointness via `Finset.mem_sdiff`. **Proved.** |
| `KFoldSplit.folds_indep` | `theorem` | `(Z i)_{i ∈ fold(n,k)}` independent of `(Z i)_{i ∈ trainComplement(n,k)}` under `μ`. **Proved** (one-line `S.indep.indepFun_finset` mirroring `OneShotSplit.folds_indep`). |

#### 12e.3. `Stat/SampleSplit/FiniteSelector.lean`

Finite pilot-selected L² transfer: a measurable selector chooses one of
finitely many branches from a pilot coordinate independent of the tail
coordinate. Uniform fixed-branch squared-risk bounds transfer to the selected
branch on a good pilot event, with a bounded bad-event extension and a thin
one-shot iid split bridge.

<!-- GEN:Causalean.Stat.SampleSplit.FiniteSelector -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.selectorCell` | `(Alpha → Iota) → Set Alpha → Iota → Set Alpha` | For a selection rule from pilot samples to branch labels, a designated set of pilot samples, and a branch label, the selector cell for that branch is the subset of the designated set on which the selection rule chooses that label. |
| `Stat.measurableSet_selectorCell` | `Measurable select → ∀ {good : Set Alpha}, MeasurableSet good → ∀ (i : Iota), MeasurableSet (Causalean.Stat.selectorCell select good i)` | When the selection rule is measurable, the designated pilot event is measurable, and a branch is fixed, its selector cell is measurable. |
| `Stat.measurable_finiteSelector_apply` | `Measurable select → ∀ {err : Iota → Beta → ℝ}, (∀ (i : Iota), Measurable (err i)) → Measurable fun z => err (select z.1) z.2` | When finite branch selection is measurable and each branch-specific error is measurable, evaluating the selected error on a pilot-tail pair is measurable. |
| `Stat.measurable_finiteSelector_sq` | `Measurable pilot → Measurable tail → Measurable select → (∀ (i : Iota), Measurable (err i)) → Measurable fun omega => err (select (pilot omega)) (tail omega) ^ 2` | If the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, and every branch error is measurable, the selected squared error is measurable. |
| `Stat.IndepFun.integrable_finiteSelector_sq` | `ProbabilityTheory.IndepFun pilot tail mu → Measurable pilot → Measurable tail → Measurable select → (∀ (i : Iota), MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map tail mu)) → MeasureTheory.Integrable (fun omega => err (select (pilot omega)) (tail omega) ^ 2) mu` | If pilot and tail coordinates are independent, the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, and every fixed branch has integrable squared error under the tail law, the pilot-selected squared error is integrable under the ambient probability law. |
| `Stat.IndepFun.integral_finiteSelector_sq_eq_sum` | `ProbabilityTheory.IndepFun pilot tail mu → Measurable pilot → Measurable tail → Measurable select → ∀ {good : Set Alpha}, MeasurableSet good → (∀ (i : Iota), MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map tail mu)) → ∫ (omega : Omega) in pilot ⁻¹' good, err (select (pilot omega)) (tail omega) ^ 2 ∂mu = ∑ i, (mu (pilot ⁻¹' Causalean.Stat.selectorCell select good i)).toReal * ∫ (b : Beta), err i b ^ 2 ∂MeasureTheory.Measure.map tail mu` | If pilot and tail coordinates are independent, the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, the designated pilot event is measurable, and every fixed branch has integrable squared error under the tail law, the selected risk on that event equals the sum of each selector-cell probability times its fixed-branch tail risk. |
| `Stat.integrableOn_finiteSelector_sq_of_eligible` | `Measurable pilot → Measurable tail → Measurable select → ∀ {good : Set Alpha}, MeasurableSet good → (∀ a ∈ good, Eligible (select a)) → (∀ (i : Iota), Eligible i → MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map tail mu)) → MeasureTheory.IntegrableOn (fun omega => err (select (pilot omega)) (tail omega) ^ 2) (pilot ⁻¹' good) mu` | If the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, the designated pilot event is measurable, every branch selected there is eligible, and eligible branches have integrable squared error under the tail law, the selected squared error is integrable on that pilot event. |
| `Stat.IndepFun.integral_finiteSelector_sq_le` | `ProbabilityTheory.IndepFun pilot tail mu → Measurable pilot → Measurable tail → Measurable select → ∀ {good : Set Alpha}, MeasurableSet good → 0 ≤ V → (∀ a ∈ good, Eligible (select a)) → (∀ (i : Iota), Eligible i → MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map tail mu)) → (∀ (i : Iota), Eligible i → ∫ (b : Beta), err i b ^ 2 ∂MeasureTheory.Measure.map tail mu ≤ V) → ∫ (omega : Omega) in pilot ⁻¹' good, err (select (pilot omega)) (tail omega) ^ 2 ∂mu ≤ V` | If pilot and tail coordinates are independent, the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, the designated pilot event is measurable, the common risk bound is nonnegative, every branch selected on that event is eligible, eligible branches have integrable squared tail error, and their fixed-branch risks obey the common bound, the pilot-selected risk on the event obeys that same bound. |
| `Stat.IndepFun.integral_finiteSelector_sq_le_add_bad` | `ProbabilityTheory.IndepFun pilot tail mu → Measurable pilot → Measurable tail → Measurable select → (∀ (i : Iota), Measurable (err i)) → ∀ {good : Set Alpha}, MeasurableSet good → 0 ≤ V → 0 ≤ H → (∀ a ∈ good, Eligible (select a)) → (∀ (i : Iota), Eligible i → MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map tail mu)) → (∀ (i : Iota), Eligible i → ∫ (b : Beta), err i b ^ 2 ∂MeasureTheory.Measure.map tail mu ≤ V) → (∀ (omega : Omega), err (select (pilot omega)) (tail omega) ^ 2 ≤ H) → mu.real (pilot ⁻¹' goodᶜ) ≤ delta → ∫ (omega : Omega), err (select (pilot omega)) (tail omega) ^ 2 ∂mu ≤ V + H * delta` | If pilot and tail coordinates are independent, the pilot coordinate is measurable, the tail coordinate is measurable, the finite selection rule is measurable, all branch errors are measurable, the designated pilot event is measurable, the good-event risk bound is nonnegative, the global squared-error envelope is nonnegative, every branch selected on the good event is eligible, eligible branches have integrable squared tail error, their fixed-branch risks obey the good-event bound, the selected squared error obeys the global envelope, and the bad pilot event has probability at most the given tolerance, the full risk is at most the good-event bound plus the envelope times that tolerance. |
| `Stat.OneShotSplit.integral_finiteSelector_sq_le` | `Measurable select → ∀ {good : Set (↥(split.foldA n) → X)}, MeasurableSet good → 0 ≤ V → (∀ a ∈ good, Eligible (select a)) → (∀ (i : Iota), Eligible i → MeasureTheory.Integrable (fun b => err i b ^ 2) (MeasureTheory.Measure.map (fun omega j => S.Z (↑j) omega) mu)) → (∀ (i : Iota), Eligible i → ∫ (b : ↥(split.foldB n) → X), err i b ^ 2 ∂MeasureTheory.Measure.map (fun omega j => S.Z (↑j) omega) mu ≤ V) → ∫ (omega : Omega) in (fun omega j => S.Z (↑j) omega) ⁻¹' good, (err (select fun j => S.Z (↑j) omega) fun j => S.Z (↑j) omega) ^ 2 ∂mu ≤ V` | Given an iid one-shot sample split, a sample horizon, a measurable finite branch selector on the pilot fold, a measurable good pilot event, a nonnegative common risk bound, eligibility of every branch selected on the good event, integrability of every eligible branch's squared error under the tail-fold law, and the common fixed-branch risk bound, the selected squared risk on the good event is at most that common bound. |
<!-- /GEN -->

#### 12e.4. `Stat/SampleSplit/FiniteCategoryPilot.lean`

Finite-category pilot thresholding on arbitrary finite iid coordinate blocks:
measurable category counts, cellwise tilted Chernoff bounds, and a simultaneous
mass sandwich with an explicit category-cardinality factor.

<!-- GEN:Causalean.Stat.SampleSplit.FiniteCategoryPilot -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.categoryMass` | `MeasureTheory.Measure X → (X → Iota) → Iota → ℝ` | Given a measurable observation space, a category-label space, a measure on the observation space, a category-label map, and a category, the category mass is the measure of observations assigned to that category, represented as a real number. |
| `Stat.categoryIndicator` | `(X → Iota) → Iota → X → ℝ` | Given an observation space, a category-label space whose labels can be compared for equality, a category-label map, a category, and an observation, the category indicator is one when that observation has the designated category and zero otherwise. |
| `Stat.measurable_categoryIndicator` | `Measurable label → ∀ (k : Iota), Measurable (Causalean.Stat.categoryIndicator label k)` | Given a measurable category label and a fixed category, the corresponding zero-one category indicator is measurable. |
| `Stat.integral_categoryIndicator` | `Measurable label → ∀ (k : Iota), ∫ (x : X), Causalean.Stat.categoryIndicator label k x ∂P = Causalean.Stat.categoryMass P label k` | Given a measurable category label and a fixed category, the mean of its zero-one indicator under a probability law equals the category's population mass. |
| `Stat.pilotCategoryCount` | `[DecidableEq Iota] → (X → Iota) → Finset ℕ → Iota → Omega → ℕ` | Given a measurable sample space and observation space, a category-label space whose labels can be compared for equality, a sample-space measure and population observation measure, an independent and identically distributed sample, a category-label map, a finite set of sample coordinates, a category, and a sample-space outcome, the pilot category count is the number of selected coordinates whose observed label equals that category. |
| `Stat.measurable_pilotCategoryCount` | `Measurable label → ∀ (block : Finset ℕ) (k : Iota), Measurable (Causalean.Stat.pilotCategoryCount S label block k)` | Given an iid sample, a measurable category label, a finite coordinate block, and a fixed category, the pilot count of that category is measurable. |
| `Stat.pilotCategoryCount_cast_eq_sum` | `↑(Causalean.Stat.pilotCategoryCount S label block k omega) = ∑ j ∈ block, Causalean.Stat.categoryIndicator label k (S.Z j omega)` | Given an iid sample, a category label, a finite coordinate block, a category, and a sample outcome, the real-valued pilot count equals the sum of category indicators over the block. |
| `Stat.pilotCategoryCount_upper_tail_of_tilt` | `Measurable label → ∀ (block : Finset ℕ) (k : Iota) {p a : ℝ}, Causalean.Stat.categoryMass P label k ≤ p → ∀ (s : ℝ), 0 ≤ s → mu.real {omega \| a < ↑(Causalean.Stat.pilotCategoryCount S label block k omega)} ≤ Real.exp (-s * a + ↑block.card * (p * (Real.exp s - 1)))` | Given an iid sample, a measurable category label, a finite pilot block, a fixed category, an upper bound on that category's population mass, an exponential tilt, and nonnegativity of the tilt, the probability that the category count exceeds the specified level is bounded by the corresponding Chernoff exponent. |
| `Stat.pilotCategoryCount_lower_tail_of_tilt` | `Measurable label → ∀ (block : Finset ℕ) (k : Iota) {p a : ℝ}, p ≤ Causalean.Stat.categoryMass P label k → ∀ s ≤ 0, mu.real {omega \| ↑(Causalean.Stat.pilotCategoryCount S label block k omega) ≤ a} ≤ Real.exp (-s * a + ↑block.card * (p * (Real.exp s - 1)))` | Given an iid sample, a measurable category label, a finite pilot block, a fixed category, a lower bound on that category's population mass, an exponential tilt, and nonpositivity of the tilt, the probability that the category count is at most the specified level is bounded by the corresponding Chernoff exponent. |
| `Stat.pilotSelected` | `(X → Iota) → Finset ℕ → ℝ → Omega → Finset Iota` | Given a measurable sample space and observation space, a finite category-label space whose labels can be compared for equality, a sample-space measure and population observation measure, an independent and identically distributed sample, a category-label map, a finite pilot-coordinate block, a real threshold, and a sample-space outcome, the selected pilot categories are exactly the categories whose pilot counts strictly exceed the threshold. |
| `Stat.finiteCategoryPilotGood` | `(X → Iota) → Finset ℕ → ℝ → ℝ → ℝ → Set Omega` | Given a measurable sample space and observation space, a finite category-label space whose labels can be compared for equality, a sample-space measure and population observation measure, an independent and identically distributed sample, a category-label map, a finite pilot-coordinate block, and real threshold, lower-band, and upper-band values, the finite-category pilot good event consists exactly of outcomes for which each selected category has population mass at least the lower band and each unselected category has population mass at most the upper band. |
| `Stat.measurableSet_finiteCategoryPilotGood` | `Measurable label → ∀ (block : Finset ℕ) (t lowerBand upperBand : ℝ), MeasurableSet (Causalean.Stat.finiteCategoryPilotGood S label block t lowerBand upperBand)` | Given an iid sample, a measurable category label, a finite pilot block, a selection threshold, a lower mass band, and an upper mass band, the simultaneous finite-category pilot good event is measurable. |
| `Stat.finiteCategoryPilot_bad_probability` | `Measurable label → ∀ (block : Finset ℕ) {t : ℝ}, 0 < t → ∀ (lowerBand upperBand sUpper sLower : ℝ), 0 ≤ sUpper → sLower ≤ 0 → mu.real (Causalean.Stat.finiteCategoryPilotGood S label block t lowerBand upperBand)ᶜ ≤ ↑(Fintype.card Iota) * (Real.exp (-sUpper * t + ↑block.card * (lowerBand * (Real.exp sUpper - 1))) + Real.exp (-sLower * t + ↑block.card * (upperBand * (Real.exp sLower - 1))))` | Given an iid sample, a measurable category label, a finite pilot block, a positive selection threshold, a lower population-mass band, an upper population-mass band, an upper-tail exponential tilt, a lower-tail exponential tilt, nonnegativity of the upper-tail tilt, and nonpositivity of the lower-tail tilt, failure of the simultaneous category-mass sandwich has probability at most the number of categories times the sum of the two explicit Chernoff tails. The bound also covers empty blocks and empty category types. |
| `Stat.finiteCategoryPilot_bad_probability_log_two` | `Measurable label → ∀ (block : Finset ℕ) {t : ℝ}, 0 < t → ∀ (lowerBand upperBand : ℝ), mu.real (Causalean.Stat.finiteCategoryPilotGood S label block t lowerBand upperBand)ᶜ ≤ ↑(Fintype.card Iota) * (Real.exp (-Real.log 2 * t + ↑block.card * lowerBand) + Real.exp (Real.log 2 * t - ↑block.card * upperBand / 2))` | Given an iid sample, a measurable category label, a finite pilot block, a positive selection threshold, a lower population-mass band, and an upper population-mass band, the simultaneous category-mass sandwich failure probability obeys the explicit bound obtained from opposite logarithmic tilts of magnitude log two. |
<!-- /GEN -->

### 12f. `Stat/Orthogonality/ConditionalOp.lean`

Envelope-based stochastic-order helpers + Cauchy–Schwarz packaging. Consumed by `PlugIn.lean` (single-factor bias) and `Remainder.lean` (product-rate AIPW bias). Causal-agnostic; candidate for upstream Mathlib contribution once stable.

| Declaration | Statement | Description |
|---|---|---|
| `IsBigOp.of_sqEnvelope` | `(Xn n)² ≤ cn n a.s.` ∧ `cn = O_p(rn²)` ⇒ `Xn = O_p(rn)` | Square-envelope ⇒ `O_p`, using a.e. event inclusion and limsup monotonicity. **Proved.** |
| `IsLittleOp.of_sqEnvelope` | `(Xn n)² ≤ cn n a.s.` ∧ `cn = o_p(rn²)` ⇒ `Xn = o_p(rn)` | Square-envelope ⇒ `o_p`, using a.e. event inclusion and convergence-to-zero monotonicity. **Proved.** |
| `abs_integral_le_eLpNorm_two` | `IsProbabilityMeasure ν` ⇒ `\|∫ f dν\| ≤ ‖f‖_{L²(ν)}` | Constant-case Cauchy–Schwarz via the L¹/L² comparison on probability spaces. **Proved.** |
| `integral_abs_mul_le_eLpNorm_mul_eLpNorm` | `∫ \|f·g\| dν ≤ ‖f‖₂ · ‖g‖₂` | Cauchy–Schwarz for L²·L² via Mathlib Hölder. **Proved.** |
| `integral_op_of_eLpNorm_op` | `eLpNorm (fn) 2 ν = o_p(rn)` ⇒ `∫ fn dν = o_p(rn)` | Lifts L²-rate to integrated bias by pointwise domination. **Proved.** |
| `integral_abs_mul_op_of_eLpNorm_op` | `eLpNorm (fn) 2 ν = o_p(sn)` ∧ fixed `g ∈ L²` ⇒ `∫ \|fn·g\| dν = o_p(sn · ‖g‖₂)` | Lifts L²-rate to integrated absolute product, splitting the zero/nonzero fixed-norm cases. **Proved.** |

### 12g. `Stat/SampleSplit/PartialFoldCLT.lean`

Fold-restricted CLT for one-shot sample splits. Provides the CLT contact and asymptotic-normality corollary at rate `√|B(n)|` for split estimators (PlugIn / DML), plus a √n-rate conversion under a fixed split ratio `|B(n)|/n → c ∈ (0,1)` with the standard variance inflation `σ²/c`.

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.clt_normalizedFoldB` | `(1/√\|B(n)\|) Σ_{i∈B(n)} ψ(Z_i) ⇒ N(0, ∫ψ²dP)` | Fold-B CLT. **Proved** by reindexing fold-B through a finite-set characteristic-function bridge and composing the existing `IIDSample.clt_normalized_sum` CLT wrapper along `\|B(n)\| → ∞`. |
| `IsAsymLinear.tendsto_normal_foldB` | AL on fold B ⇒ `√\|B(n)\|·(θn − θ₀) ⇒ N(0, σ²)` | Mirror of `tendsto_normal` for `I = split.foldB`. **Proved by Codex** (4-line composition of `clt_normalizedFoldB` + `Tendsto_dist.add_isLittleOp_one`). |
| `IsAsymLinear.tendsto_normal_foldB_sqrt_n` | `\|B(n)\|/n → c ∈ (0,1)` ∧ AL on fold B ⇒ `√n·(θn − θ₀) ⇒ N(0, σ²/c)` | √n-rate conversion under fixed split ratio. **Proved** (depends on the Mathlib varying-map stub); handles fold-B measurability, deterministic `√n/√\|B(n)\| → 1/√c`, eventual a.e. equality, and variance algebra. |

### 12h. `Stat/SampleSplit/FoldBEmpiricalProcess.lean`

Empirical-process bounds on a centered evaluation-fold sum of a training-fold-measurable random function. Consumed by `DMLChernozhukov.lean` (one-shot) and `DMLCrossFit.lean` (K-fold). The file is structured in three layers:

1. **Abstract layer (private)** — parameterised over an arbitrary evaluation-fold schedule `eval : ℕ → Finset ℕ`, training σ-algebra `m_train : ℕ → MeasurableSpace Ω`, plus independence and iid-product-law hypotheses.
2. **OneShot wrappers** — preserved public signatures `foldB_centered_sum_isLittleOp_one` and `sqrtFoldB_integral_isLittleOp_one`, now thin instantiations of the abstract layer.
3. **K-fold corollaries** — new public lemmas under `KFoldSplit.*` namespace.

#### Abstract layer (private)

| Declaration | Statement | Description |
|---|---|---|
| `centeredEvalSum_sq_lintegral_le_core` / `centeredEvalSum_sq_lintegral_le` | `∫⁻ ω, ((1/√\|eval n\|) Σ_{eval n} (g(Z_i) − ∫g dP))² ∂μ ≤ ∫⁻ ω, ‖g(n,ω)‖²_{L²(P)} ∂μ` | Second-moment bound on the centered eval-sum, parametric in `(eval, m_train, h_train_le, h_indep, h_iid)`. **Proved** via the generic `iid_centered_sum_sq_lintegral_le` helper in `Causalean/Mathlib/IIDCenteredSum.lean`. |
| `evalSum_truncated_isLittleOp_one` | bounded `g` with `∫⁻ ‖g‖²₂ → 0` ⇒ centered eval-sum is `o_p(1)` | Markov / Chebyshev in `ENNReal` plus bounded convergence. **Proved by Codex (A0)** via `MeasureTheory.meas_ge_le_lintegral_div`. Eventual `0 < (eval n).card` from a separate `h_eval_card_grow` hypothesis. |
| `evalSum_isLittleOp_one` | `‖g(n,ω)‖_{L²(P)} = o_p(1)` (random-norm rate) ⇒ centered eval-sum is `o_p(1)` | Truncation at L²-norm ≤ 1, then `evalSum_truncated_isLittleOp_one` + `IsLittleOp.of_eq_on_asymptotic`. **Proved.** |
| `evalSum_bias_isLittleOp_one` | `(eval n).card / n → c ∈ (0,1)` ∧ `‖f‖_{L²(P)} = o_p(n^{-1/2})` ⇒ `√\|eval n\|·∫f dP = o_p(1)` | Bias term, fully parametric. **Proved.** |

#### OneShot wrappers (public; signatures preserved for backward compat)

| Declaration | Statement | Description |
|---|---|---|
| `foldB_centered_sum_isLittleOp_one` | fold-A-measurable `f` with `‖f‖_{L²(P)} = o_p(1)` ⇒ `(1/√\|B(n)\|) Σ_{i ∈ B(n)} (f(n,ω,Z_i ω) − ∫f dP) = o_p(1)` | Thin wrapper around the abstract layer, instantiating `eval n := split.foldB n`, `m_train n := comap (foldA n) Z`, `h_indep := split.folds_indep n` (definitional `IndepFun`-to-`Indep` bridge), `h_iid := S.indep.precomp + IdentDistrib.map_eq + S.law`, `h_eval_card_grow := split.foldB_card_tendsto`. **Proved.** |
| `sqrtFoldB_integral_isLittleOp_one` | `\|B(n)\|/n → c ∈ (0,1)` ∧ `‖f‖_{L²(P)} = o_p(n^{-1/2})` ⇒ `√\|B(n)\|·∫f dP = o_p(1)` | OneShot bias wrapper. **Proved.** |

#### KFold corollaries (public; new)

| Declaration | Statement | Description |
|---|---|---|
| `KFoldSplit.fold_centered_sum_isLittleOp_one` | per `k : Fin K`, training-complement-measurable `f` with `‖f‖_{L²(P)} = o_p(1)` ⇒ `(1/√\|fold n k\|) Σ_{fold n k} (f(Z_i) − ∫f dP) = o_p(1)` | K-fold per-evaluation-fold centered sum. Instantiates the abstract layer with `eval n := split.fold n k`, `m_train n := comap (trainComplement n k) Z`, `h_indep` via `split.folds_indep n k` (with `.symm` since the K-fold version is stated `IndepFun fold trainComplement`), `h_eval_card_grow := split.grow k`. **Proved.** |
| `KFoldSplit.sqrtFold_integral_isLittleOp_one` | `1 < K` ∧ `‖f‖_{L²(P)} = o_p(n^{-1/2})` ⇒ per `k`, `√\|fold n k\|·∫f dP = o_p(1)` | K-fold per-fold bias. Uses `c := (K:ℝ)⁻¹` ∈ (0, 1) (whence the `1 < K` requirement) and `split.ratio k`. **Proved.** |

### 12i. `Stat/MEstimation/InfluenceFunction.lean`

Light data structure packaging the three properties an influence function must satisfy: measurability, mean-zero, and finite second moment. Mirrors `def:sp-pathwise-diff` from `doc/basic_concepts/Semi-parametric Inference/semi_parametric_inference.tex` at the *assumption* level — pathwise differentiability is deferred.

| Declaration | Statement | Description |
|---|---|---|
| `InfluenceFunction P ψ` | `Prop`-valued structure | An influence function for parameter `θ : E` under law `P : Measure X` is a measurable, mean-zero, square-integrable function `ψ : X → E`. Fields: `measurable`, `mean_zero`, `finite_var`. |

### 12i'. `Stat/Inference/AffineInversion.lean`

Finite-volume geometry and expected-volume control for confidence sets obtained
by inverting a scalar affine inequality. The module is model-free: applications
supply only a parameter region, random affine coefficients, a mean-radius
proxy, and a bad-slope probability bound.

<!-- GEN:Causalean.Stat.Inference.AffineInversion -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.affineInversionSet` | `Set ℝ → ℝ → ℝ → ℝ → Set ℝ` | Given a real parameter region, real-valued intercept, slope, and radius parameters, the affine-inversion acceptance set consists exactly of those parameter values in the region whose affine discrepancy $\|A-\theta B\|$ is at most $r$. |
| `Stat.affineInversionSet_restrictedVolume_le` | `MeasureTheory.volume region ≠ ⊤ → ∀ (A B r : ℝ), B ≠ 0 → 0 ≤ r → Causalean.Stat.restrictedSetVolume region (Causalean.Stat.affineInversionSet region A B r) ≤ min (MeasureTheory.volume region).toReal (2 * r / \|B\|)` | Affine inversion has restricted volume at most the parameter-region volume and at most twice the radius divided by the nonzero slope. |
| `Stat.affineInversionSet_restrictedVolume_le_region` | `MeasureTheory.volume region ≠ ⊤ → ∀ (A B r : ℝ), Causalean.Stat.restrictedSetVolume region (Causalean.Stat.affineInversionSet region A B r) ≤ (MeasureTheory.volume region).toReal` | Affine inversion is always bounded by the volume of its parameter region. |
| `Stat.expectedRestrictedVolume_affineInversion_le` | `MeasureTheory.volume region ≠ ⊤ → ∀ (A B K : Ω → ℝ) (n : ℕ) (L mu Kbar q : ℝ), 0 ≤ L → 0 < mu → 0 < n → (∀ (w : Ω), 0 ≤ K w) → MeasureTheory.Integrable K Q → ∫ (w : Ω), K w ∂Q ≤ Kbar → (Q {w \| mu / 2 < \|B w - mu\|}).toReal ≤ q → ∫ (w : Ω), Causalean.Stat.restrictedSetVolume region (Causalean.Stat.affineInversionSet region (A w) (B w) (L * √(K w / ↑n))) ∂Q ≤ 4 * L * √(Kbar / ↑n) / mu + (MeasureTheory.volume region).toReal * q` | Expected restricted volume for affine inversion is controlled by the mean radius and by the probability that the random slope is less than half its positive target value. |
| `Stat.inverseStrength_to_frontier` | `0 ≤ B → 0 < t → min cap (A / √t + B / t) ≤ max cap (A + B) * min 1 (t ^ (-1 / 2))` | An inverse-root plus inverse-strength bound, capped by a nonnegative region volume, is bounded by the compact inverse-square-root frontier form. |
| `Stat.expectedRestrictedVolume_affineInversion_frontier_le` | `MeasureTheory.volume region ≠ ⊤ → ∀ (A B K : Ω → ℝ) (n : ℕ) (L mu Kbar q kappa inflation Y t : ℝ), 0 ≤ L → 0 < mu → 0 < n → (∀ (w : Ω), 0 ≤ K w) → MeasureTheory.Integrable K Q → ∫ (w : Ω), K w ∂Q ≤ Kbar → (Q {w \| mu / 2 < \|B w - mu\|}).toReal ≤ q → 0 < kappa → 0 ≤ inflation → 0 ≤ Y → Kbar ≤ inflation * kappa → (MeasureTheory.volume region).toReal * q ≤ Y / t → t = ↑n * mu ^ 2 / kappa → ∫ (w : Ω), Causalean.Stat.restrictedSetVolume region (Causalean.Stat.affineInversionSet region (A w) (B w) (L * √(K w / ↑n))) ∂Q ≤ max (MeasureTheory.volume region).toReal (4 * √inflation * L + Y) * min 1 (t ^ (-1 / 2))` | Affine-inversion frontier bound over a finite-volume region. Fix a probability space `(Ω, Q)`, real-valued functions `A`, `B`, `K` on it, and a region of the line with finite Lebesgue measure. Suppose a nonnegative radius scale `L`, a positive centering level `mu` and a positive sample size `n`; suppose `K` is pointwise nonnegative and `Q`-integrable with `Q`-mean at most `Kbar`, and that the `Q`-probability that `B` deviates from `mu` by more than `mu`/2 is at most `q`. Suppose further a positive scale `kappa`, a nonnegative inflation factor `inflation` and nonnegative slack `Y`, with `Kbar` controlled by `inflation · kappa` and the bad-event contribution `(vol region) · q` controlled by `Y / t`, where `t` is defined as `n · mu² / kappa`. Then the `Q`-expected restricted volume of the affine-inversion set built from `A`, `B` and the shrinking radius `L · √(K/n)` is at most `max(vol region, 4·√inflation·L + Y) · min(1, t^(-1/2))`. |
<!-- /GEN -->

### 12j. `Stat/Inference/DeltaMethod.lean`

Smooth-function CLT. Two forms; both proved.

| Declaration | Statement | Description |
|---|---|---|
| `deltaMethod_scalar` | `theorem` | If `√n (T_n − t₀) ⇒ N(0, σ²)` and `g : ℝ → ℝ` differentiable at `t₀` with derivative `g'`, then `√n (g T_n − g t₀) ⇒ N(0, g'² σ²)`. **Proved by Codex.** Pipeline: tightness of `√n (T_n − t₀)` via `Tendsto_dist.tightness`; consistency `T_n − t₀ →_p 0` via `IsBigOp.const_mul_tendsto_zero` against `(√n)⁻¹ → 0`; remainder `Rn := √n · (g(T_n) − g t₀ − g' (T_n − t₀))` is `o_p(1)` from `HasDerivAt.isLittleO_sub` on the localizing event `{|T_n − t₀| < ρ}`; finally `Tendsto_dist.const_mul_tendsto_gaussian` for `g' · √n (T_n − t₀)` and `Tendsto_dist.add_isLittleOp_one` to absorb `Rn`. |
| `deltaMethod` | `theorem` | Multivariate form: `√n • (T_n − t₀) ⇒ Q` (vector limit) and `g : E → F` Fréchet-differentiable at `t₀` with derivative `Dg : E →L[ℝ] F` ⇒ `√n • (g T_n − g t₀) ⇒ Q.map Dg`. **Proved by Codex.** Vector port of the scalar pipeline: `‖√n • (T_n − t₀)‖` is `O_p(1)` via `Tendsto_dist_vec.map_continuous` along `‖·‖ : E → ℝ` plus `Tendsto_dist.tightness`; consistency `T_n − t₀ →_p 0` in norm; remainder controlled by `HasFDerivAt.isLittleO_sub`; linear pushforward `Dg` via `Tendsto_dist_vec.map_continuous` again; vector Slutsky absorption via `Tendsto_dist_vec.add_isLittleOp_one`. |

### 12j'. `Stat/Inference/HadamardDeriv.lean` + `Stat/Inference/FunctionalDelta.lean`

**Directional (Hadamard) functional delta method.** The Fréchet delta method (12j) cannot reach lattice functionals: an interval-identified parameter's sharp endpoints are `sup'`/`inf'` of separately estimable quantities (`PartialID/.../IntersectionBound.lean` `iInter_Icc_eq_Icc : ⋂ⱼ [Lⱼ,Uⱼ] = [sup' L, inf' U]`; Manski MIV envelopes; combined MTR+MTS bounds), and `max`/`min` are only *Hadamard directionally* differentiable — with a **nonlinear** derivative at a tie, which is exactly the binding/kink regime where bound inference is delicate (Hirano–Porter 2012; Fang–Santos 2019). References: van der Vaart (1998) §20.2; Shapiro (1991).

`HadamardDeriv.lean` is the analysis core, with no probability assumptions. Besides the
base derivative and the lattice derivatives, it provides a continuously directionally
differentiable closure calculus for composition, products, arithmetic, and finite
sums, suprema, and infima.

_Generated from docstrings — do not hand-edit; run `npm run doc:gen` (see CLAUDE.md). Edit the Lean docstring to change a description._

<!-- GEN:Causalean.Stat.Inference.HadamardDeriv -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.HasHadamardDirDerivAt` | `(E → F) → (E → F) → E → Prop` | Given normed real vector spaces, a map between them, a proposed directional derivative, and a point, the Hadamard directional differentiability property at that point holds exactly when, for every direction, every sequence of directions converging to it, and every strictly positive real step-size sequence converging to zero, |
| `Stat.HasFDerivAt.hasHadamardDirDerivAt` | `HasFDerivAt φ L θ → Causalean.Stat.HasHadamardDirDerivAt φ (fun x => L x) θ` | Fréchet implies Hadamard. If the map `φ` is Fréchet-differentiable at the point `θ`, with continuous linear derivative `L`, then `φ` is Hadamard directionally differentiable at `θ`, with the same map `L` serving as its Hadamard directional derivative. |
| `Stat.maxDirDeriv` | `ℝ → ℝ → ℝ × ℝ → ℝ` | Given a first real coordinate and a second real coordinate, the directional derivative of the maximum functional at their pair sends a direction $(u,v)$ to $u$ when $b<a$, to $v$ when $a<b$, and to $\max(u,v)$ when $a=b$. |
| `Stat.minDirDeriv` | `ℝ → ℝ → ℝ × ℝ → ℝ` | Given a first real coordinate and a second real coordinate, the directional derivative of the minimum functional at their pair sends a direction $(u,v)$ to $u$ when $a<b$, to $v$ when $b<a$, and to $\min(u,v)$ when $a=b$. |
| `Stat.continuous_maxDirDeriv` | `Continuous (Causalean.Stat.maxDirDeriv a b)` | The directional derivative of the maximum functional is continuous. |
| `Stat.continuous_minDirDeriv` | `Continuous (Causalean.Stat.minDirDeriv a b)` | The directional derivative of the minimum functional is continuous. |
| `Stat.hasHadamardDirDerivAt_max` | `Causalean.Stat.HasHadamardDirDerivAt (fun z => max z.1 z.2) (Causalean.Stat.maxDirDeriv a b) (a, b)` | `max` is Hadamard directionally differentiable. At `(a, b)` the derivative is `maxDirDeriv a b` (linear off the diagonal, sublinear at a tie). |
| `Stat.hasHadamardDirDerivAt_min` | `Causalean.Stat.HasHadamardDirDerivAt (fun z => min z.1 z.2) (Causalean.Stat.minDirDeriv a b) (a, b)` | `min` is Hadamard directionally differentiable. At `(a, b)` the derivative is `minDirDeriv a b`. |
| `Stat.HasContinuousHadamardDirDerivAt` | `(E → F) → E → Prop` | Given normed real vector spaces, a map between them and a point, the continuous Hadamard directional differentiability property at that point holds exactly when there exists a continuous directional derivative at that point and that derivative satisfies Hadamard directional differentiability. |
| `Stat.HasHadamardDirDerivAt.comp` | `Causalean.Stat.HasHadamardDirDerivAt g g' (f x) → Causalean.Stat.HasHadamardDirDerivAt f f' x → Causalean.Stat.HasHadamardDirDerivAt (g ∘ f) (g' ∘ f') x` | If the outer map has a Hadamard directional derivative and the inner map has a Hadamard directional derivative, then their composition has the composed directional derivative. |
| `Stat.HasContinuousHadamardDirDerivAt.comp` | `Causalean.Stat.HasContinuousHadamardDirDerivAt g (f x) → Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt (g ∘ f) x` | If the outer map and the inner map are continuously Hadamard directionally differentiable, then their composition is too. |
| `Stat.HasContinuousHadamardDirDerivAt.of_hasFDerivAt` | `HasFDerivAt f L x → Causalean.Stat.HasContinuousHadamardDirDerivAt f x` | If a map has a Fréchet derivative, then it is continuously Hadamard directionally differentiable. |
| `Stat.HasContinuousHadamardDirDerivAt.of_differentiableAt` | `DifferentiableAt ℝ f x → Causalean.Stat.HasContinuousHadamardDirDerivAt f x` | If a map is differentiable at a point, then it is continuously Hadamard directionally differentiable there. |
| `Stat.HasContinuousHadamardDirDerivAt.congr_of_eventuallyEq` | `f =ᶠ[nhds x] g → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt f x` | If two maps agree near a point and one is continuously Hadamard directionally differentiable there, then the other has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.prod` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => (f y, g y)) x` | If each component map is continuously Hadamard directionally differentiable, then their product-valued pairing has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.add` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => f y + g y) x` | If two real-valued maps are continuously Hadamard directionally differentiable, then their pointwise sum has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.sub` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => f y - g y) x` | If two real-valued maps are continuously Hadamard directionally differentiable, then their pointwise difference has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.mul` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => f y * g y) x` | If two real-valued maps are continuously Hadamard directionally differentiable, then their pointwise product has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.div` | `g x ≠ 0 → Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => f y / g y) x` | If the denominator is nonzero and the numerator and denominator maps are continuously Hadamard directionally differentiable, then their pointwise quotient has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.finset_sum` | `(∀ i ∈ s, Causalean.Stat.HasContinuousHadamardDirDerivAt (f i) x) → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => ∑ i ∈ s, f i y) x` | If every map indexed by a finite set is continuously Hadamard directionally differentiable, then their pointwise finite sum has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.max` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => max (f y) (g y)) x` | If two real-valued maps are continuously Hadamard directionally differentiable, then their pointwise maximum has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.min` | `Causalean.Stat.HasContinuousHadamardDirDerivAt f x → Causalean.Stat.HasContinuousHadamardDirDerivAt g x → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => min (f y) (g y)) x` | If two real-valued maps are continuously Hadamard directionally differentiable, then their pointwise minimum has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.finset_sup'` | `(∀ i ∈ s, Causalean.Stat.HasContinuousHadamardDirDerivAt (f i) x) → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => s.sup' hs fun i => f i y) x` | If a finite index set is nonempty and every indexed map is continuously Hadamard directionally differentiable, then their pointwise finite supremum has the same property. |
| `Stat.HasContinuousHadamardDirDerivAt.finset_inf'` | `(∀ i ∈ s, Causalean.Stat.HasContinuousHadamardDirDerivAt (f i) x) → Causalean.Stat.HasContinuousHadamardDirDerivAt (fun y => s.inf' hs fun i => f i y) x` | If a finite index set is nonempty and every indexed map is continuously Hadamard directionally differentiable, then their pointwise finite infimum has the same property. |
<!-- /GEN -->

`FunctionalDelta.lean` (the delta theorem; consumes the existing `Tendsto_dist_vec` weak-convergence infra):

| Declaration | Statement | Description |
|---|---|---|
| `isLittleOp_one_of_measure_ne_tendsto_zero` | theorem | Reusable Slutsky device: if `μ{Yₙ ≠ Xₙ} → 0` then `Yₙ − Xₙ = o_p(1)`. The ingredient for the *off-diagonal* case (lattice functional selects one coordinate except when the estimated ordering is wrong, an event of vanishing probability). **Proved.** |
| `instIsProbabilityMeasure_map_max` / `_min` | instance | The lattice pushforward `Q.map (z ↦ max/min z.1 z.2)` of a probability measure `Q` is a probability measure. Global instance, so the `Tendsto_dist` conclusions below need *not* assume it. |
| `sqrt_mul_max_sub` / `sqrt_mul_min_sub` | lemma | **The exact tie identity** `√n·(max/min âₙ b̂ₙ − a) = max/min(√n(âₙ−a), √n(b̂ₙ−a))` — the rescaled image equals the lattice functional applied to the joint rescaled deviation, with no remainder. |
| `maxStat_aemeasurable` / `minStat_aemeasurable` | lemma | The image statistic `√n·(max/min âₙ b̂ₙ − a)` is `AEMeasurable` for every `n`, *derived* from the joint deviation's measurability via the tie identity — so the delta theorems need *not* assume it. |
| `deltaMethod_max_tie` | theorem | **Directional delta method for `max` at a tie.** If `√n • ((âₙ,b̂ₙ) − (a,a)) ⇒ Q` on `ℝ×ℝ`, then `√n·(max âₙ b̂ₙ − a) ⇒ Q.map (z ↦ max z.1 z.2)` (generally *non-Gaussian* — the law of `max(Z₁,Z₂)`). The binding/kink case the Fréchet method cannot reach. **Sorry-free, axiom-clean.** Proof: the exact identity `√n(max âₙ b̂ₙ − a) = max(√n(âₙ−a), √n(b̂ₙ−a)) = max∘Sₙ` (`sqrt_mul_max_sub`), then continuous mapping (`Tendsto_dist_vec.map_continuous`) + `congr_ae`; no linearization remainder. Hypotheses: only the joint-deviation measurability `hSn_meas` and the joint CLT `hCLT` — the pushforward-probability instance and the image-statistic measurability are now discharged automatically (via the instance / `maxStat_aemeasurable`). |
| `deltaMethod_min_tie` | theorem | Companion for `min`. **Sorry-free, axiom-clean.** Same discharged-assumption shape. |

*Off-diagonal (`a ≠ b`) and the abstract `sup'`/`inf'`-over-finite-families forms are the natural follow-ups; the tie case is the novel/hard one and the reusable `isLittleOp_one_of_measure_ne_tendsto_zero` is the device for the off-diagonal reduction.*

### 12k. `Stat/MEstimation/ZEstimator.lean`

Parametric-inference regularity bundle (structure layer). Houses only `ZEstimatorRegularity`; the headline theorem `zEstimator_clt` lives downstream in `Stat/MEstimation/ZEstimatorCLT.lean` to keep the import DAG acyclic (the proof needs `Stat/MEstimation/EmpiricalExpansion.lean`, which itself imports this file for the structure).

| Declaration | Statement | Description |
|---|---|---|
| `ZEstimatorRegularity ψ θ₀ P` | structure | Identification (`∫ ψ(·;θ₀) dP = 0`), Jacobian `J₀ : E →L[ℝ] E` + invertibility witness, Fréchet-derivative spec, finite var, measurability, plus three smoothness fields: `jacobian_continuity` (`ContinuousAt` of `θ ↦ ∫ ψ(·;θ) dP` at `θ₀`; redundant with `J₀_spec` but exposed for downstream convenience), `psi_int_neighborhood` (`ψ(·;θ)` is `P`-integrable on a neighborhood of `θ₀`), and `score_envelope` (integrable `L²` derivative envelope: ∃ `δ>0`, ∃ nonneg measurable `F : X → ℝ` with `F²∈L¹(P)` such that `‖ψθz−ψθ₀z‖ ≤ ‖θ−θ₀‖·F z` for all `z : X` and all `‖θ−θ₀‖<δ` (pointwise — produced by MVT on smooth scores; needed to transfer the bound to random sample-dependent `θn`) — this is the LaTeX-spec form of `def:par-smoothness` and is what discharges `score_diff_L2_isLittleOp_sqrt` via Markov + iid LLN on `F²`). Reference: `def:par-smoothness` (van der Vaart 1998 §5.6). |

### 12l. `Stat/MEstimation/EmpiricalExpansion.lean`

Empirical-process / linearization layer feeding `zEstimator_clt`. References: `def:par-smoothness`, `thm:par-z-clt` in `doc/basic_concepts/Semi-parametric Inference/parametric_inference.tex`; van der Vaart (1998) §5.6 Thm 5.41; Newey-McFadden (1994) §7.

| Declaration | Statement | Description |
|---|---|---|
| `StochEquicontAt ψ θ₀ P μ S θn` | `def` | Asymptotic-equicontinuity predicate at `θ₀` along `θn`: `∀ ε>0, ∃ δ>0` such that the empirical-process gap `(√n)⁻¹ Σ (ψ(θn,Z_i)−ψ(θ₀,Z_i)) − √n ∫ (ψ(θn,·)−ψ(θ₀,·)) dP` exceeding `ε` on `{‖θn−θ₀‖<δ}` has vanishing probability as `n→∞`. Standard Donsker-class hypothesis (van der Vaart 1998 Lemma 19.24 / §19.4); exposed as a hypothesis pending Mathlib's empirical-process layer. |
| `score_diff_L2_isLittleOp_sqrt` | `theorem` | Under `ZEstimatorRegularity` (in particular `score_envelope`) + consistency `θn →_p θ₀`, the sample mean square `(1/n) Σ_{i<n} ‖ψ(θn,Z_i) − ψ(θ₀,Z_i)‖²` is `o_p(1)`. Proof: dominated by `‖θn−θ₀‖² · (1/n) Σ F(Z_i)²` on the `δ`-good event (pointwise envelope bound), where the second factor satisfies a Markov bound `μ{Mn>M} ≤ K/M` with `K := ∫F² dP` (using `IIDSample.identDist + S.law` to push `∫ω F(S.Z i ω)² dμ = ∫z F z² dP`); the `δ`-bad event has probability → 0 by `hConsistent (τ/2)`. **Proved.** |
| `empiricalScoreDiff_isLittleOp_sqrt` | `theorem` | Centered empirical-process control: under `ZEstimatorRegularity`, consistency, and `hStochEquicont : StochEquicontAt ψ θ₀ P μ S θn`, `‖(1/√n) Σ (ψ(θn,Z_i) − ψ(θ₀,Z_i)) − √n · ∫ (ψ(θn,·) − ψ(θ₀,·)) dP‖ = o_p(1)`. (Subtracting `√n · ∫` is the iid bias correction.) Proof: combine `hStochEquicont(ε)` with `hConsistent(δ/2)` to remove the conditioning on `{‖θn−θ₀‖<δ}` via measure-union bound. **Proved.** |
| `populationScoreDiff_eq_jacobian_plus_remainder` | `theorem` | Population Fréchet expansion: `∫ (ψ(θ,·) − ψ(θ₀,·)) dP − J₀ (θ − θ₀) =o[𝓝 θ₀] ‖θ − θ₀‖`. Rephrasing of `J₀_spec` as `Asymptotics.IsLittleO`. **Proved.** |
| `localStochasticExpansion` | `theorem` | Headline expansion: under `ZEstimatorRegularity`, consistency, `hStochEquicont`, and the rate `hRate : ‖θn − θ₀‖ = O_p(1/√n)`, the norm `‖(1/√n) Σ (ψ(θn,Z_i) − ψ(θ₀,Z_i)) − √n · J₀ (θn − θ₀)‖` is `o_p(1)`. Combines (b)+(c)+`hRate` via triangle. Plugged directly into `zEstimator_clt`. **Proved.** |

### 12l'. `Stat/MEstimation/ZEstimatorCLT.lean`

Headline theorem layer for the parametric-inference workhorse. Z- / M-estimator CLT for vector-valued estimating equations. OLS, MLE, IV-2SLS instantiate it. Imports `Stat/MEstimation/EmpiricalExpansion.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `zEstimator_clt` | `theorem` | Under `ZEstimatorRegularity`, consistency, asymptotic equicontinuity (`hStochEquicont : StochEquicontAt ψ θ₀ P μ S θn`), the rate hypothesis `hRate : ‖θn − θ₀‖ = O_p(1/√n)`, and empirical-moment vanishing, `θn` is asymptotically linear at `θ₀` with IF `−J₀⁻¹ · ψ(·; θ₀)` (Chernozhukov form). Conclusion is `IsAsymLinearVec`. Reference: van der Vaart 1998 Thm 5.41. **Proved.** All three `IsAsymLinearVec` fields discharged: `mean_zero` (push `−J₀⁻¹` through Bochner integral via `identification`), `finite_var` (operator-norm bound on `J₀_inv` + `finite_var`), and `remainder` (combine `localStochasticExpansion` + `hMoment`, then bound the rescaled remainder by `J₀_inv` and the finite-dim left-inverse derived from `J₀_inverse`). The Donsker-step content is encapsulated in the `hStochEquicont` hypothesis, to be discharged per-instance until Mathlib's empirical-process layer lands. |

### 12l''. `Stat/MEstimation/ExtremumConsistency.lean` — discharging the CLT's consistency and equicontinuity hypotheses

Additive bridge: re-exposes the headline CLTs (`zEstimator_clt`, `gmm_asymptotically_linear`) with their two *opaque* hypotheses derived from clean primitives. **Consistency** `hConsistent` is derived from the primitive extremum conditions via the empirical-process layer's `mEstimator_consistent_of_glivenkoCantelli` (§12ep.c) — classical reduction (Newey–McFadden 1994 Thm 2.1; van der Vaart 1998 Thm 5.7): consistency follows from a Glivenko–Cantelli criterion plus a well-separated population maximum. **Equicontinuity** `hStochEquicont : StochEquicontAt` is derived from the *class-level* Donsker property `AsymptoticEquicont` (§12ep.d) via `stochEquicontAt_of_asymptoticEquicont` — the estimator-indexed modulus is reconstructed from the function-class property plus consistency. The original `hConsistent`/`hStochEquicont`-taking theorems are unchanged and remain available for callers who establish those by other means. Imports `Stat/GMM/AsymptoticNormality.lean` + `Stat/EmpiricalProcess/MEstimatorConsistency.lean` + `Stat/EmpiricalProcess/Equicontinuity/Modulus.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `consistent_lt_norm_of_le_dist` | `(∀ ε>0, μ{ε ≤ dist (θn n) θ₀} → 0) ⇒ (∀ ε>0, μ{ε < ‖θn n − θ₀‖} → 0)` | **Format bridge** between the consistency conclusion of the empirical-process layer (`≤ dist`) and the form the CLT layer consumes (`< ‖·‖`); on a normed group `dist = ‖·−·‖` and `{ε<‖·‖} ⊆ {ε≤dist}`, so the measures squeeze to 0. **Proved, sorry-free.** |
| `zEstimator_clt_of_extremum` | same `IsAsymLinearVec` conclusion as `zEstimator_clt` | **Z-estimator CLT from extremum primitives.** Replaces `hConsistent` by `(m, hGC : GlivenkoCantelli S m, hArgmax : θn maximizes Pₙ m over θ₀, hSep : well-separated population max at θ₀)`; consistency is derived and fed to `zEstimator_clt`. The Donsker modulus `hStochEquicont`, rate `hRate`, and score FOC `hMoment` are unchanged. Typically `ψ = ∂m/∂θ`, so one estimator satisfies both `hMoment` and `hArgmax`; `m`/`ψ` kept independent to avoid differentiability bookkeeping. **Proved, sorry-free.** |
| `gmm_asymptotically_linear_of_extremum` | same `IsAsymLinearVec θn θ₀ prob.influence …` conclusion as `gmm_asymptotically_linear` | **GMM analogue.** Discharges `hConsistent` from a Glivenko–Cantelli GMM criterion `m` (classically `m θ = −ḡₙ(θ)ᵀ W ḡₙ(θ)`) with well-separated population max of which `θn` is a sample maximiser. **Proved, sorry-free.** |
| `zEstimator_clt_of_donsker` | same `IsAsymLinearVec` conclusion as `zEstimator_clt` | **Z-estimator CLT with the equicontinuity hypothesis discharged.** Replaces `hStochEquicont` by the class-level `hAEC : AsymptoticEquicont ψ θ₀ P μ S` (§12ep.d); `StochEquicontAt` is reconstructed via `stochEquicontAt_of_asymptoticEquicont` from `hAEC` + `hConsistent`. Keeps `hConsistent`, `hRate`, `hMoment`. **Proved, sorry-free, axiom-clean.** |
| `zEstimator_clt_of_extremum_donsker` | same `IsAsymLinearVec` conclusion as `zEstimator_clt` | **Both opaque hypotheses discharged.** Assumes neither `hConsistent` nor `hStochEquicont`: takes the extremum package `(m, hGC, hArgmax, hSep)` and the Donsker property `hAEC`. The single derived consistency conclusion is threaded into *both* the linearisation and the `StochEquicontAt` reduction. Fully primitive Z-estimator CLT. **Proved, sorry-free, axiom-clean.** |
| `gmm_asymptotically_linear_of_donsker` | same `IsAsymLinearVec θn θ₀ prob.influence …` conclusion as `gmm_asymptotically_linear` | **GMM analogue of `zEstimator_clt_of_donsker`.** `hStochEquicont` replaced by the class-level `hAEC : AsymptoticEquicont prob.score prob.θ₀ P μ S`. **Proved, sorry-free, axiom-clean.** |
| `gmm_asymptotically_linear_of_extremum_donsker` | same `IsAsymLinearVec θn θ₀ prob.influence …` conclusion as `gmm_asymptotically_linear` | **GMM analogue of `zEstimator_clt_of_extremum_donsker`.** Neither `hConsistent` nor `hStochEquicont` assumed: consistency from the GC GMM criterion, equicontinuity from `hAEC`. Fully primitive GMM asymptotic-linearity theorem. **Proved, sorry-free, axiom-clean.** |

### 12l'''. Finite Poisson M-estimation substrate

Finite-dimensional Poisson projection tools: a total maximizer selector,
existence and uniqueness under positive weights and an injective linear design,
deterministic argmax stability and finite-table consistency, continuity under
mean perturbations, a one-cell implicit derivative, and the sign of a selected
scalar coefficient from a conditional nuisance fit.

<!-- GEN:Causalean.Stat.MEstimation.ArgmaxStability -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.tendsto_argmax_of_eventually_mem_compact` | `IsCompact K → limitArgmax ∈ K → ContinuousOn limitCriterion K → (∀ y ∈ K, limitCriterion y ≤ limitCriterion limitArgmax) → TendstoUniformlyOn criterion limitCriterion Filter.atTop K → (∀ᶠ (N : ℕ) in Filter.atTop, argmax N ∈ K) → (∀ᶠ (N : ℕ) in Filter.atTop, ∀ y ∈ K, criterion N y ≤ criterion N (argmax N)) → (∀ y ∈ K, limitCriterion y = limitCriterion limitArgmax → y = limitArgmax) → Filter.Tendsto argmax Filter.atTop (nhds limitArgmax)` | Deterministic argmax stability. Let `criterion : ℕ → E → ℝ` be a sequence of objective functions with pointwise limit `limitCriterion`, over a compact set `K`. Suppose the limiting maximizer `limitArgmax` lies in `K`, `limitCriterion` is continuous on `K`, `limitArgmax` maximizes `limitCriterion` over `K`, and it is the unique maximizer over `K`, up to equal criterion values. If `criterion` converges to `limitCriterion` uniformly on `K`, each `argmax N` eventually lies in `K`, and `argmax N` eventually maximizes `criterion N` over `K`, then the sequence `argmax` converges to `limitArgmax`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.MEstimation.FinitePoisson -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IsUniqueGlobalMax` | `(A → ℝ) → A → Prop` | Given a real-valued criterion on a domain and a point in that domain, the assertion that the point is its unique global maximizer means that every domain point has criterion value no larger than this point's value, and that equality can occur only at this point. |
| `Stat.maximizerOrZero` | `(A → ℝ) → A` | Given a domain equipped with a distinguished zero and a real-valued criterion on that domain, the selected maximizer or zero is a global maximizer when one exists and is the distinguished zero otherwise. |
| `Stat.poissonCell_linear_coercive_bound` | `0 < m → m * x - Real.exp x ≤ (m + 1) ^ 2 / 2 - min m 1 * \|x\|` | A positive-mean Poisson cell log likelihood is bounded above by a linearly coercive function. This is the elementary tail estimate used in the finite Poisson maximizer existence argument. |
| `Stat.poissonCell_strictConcave_midpoint` | `0 < q → x ≠ y → (q * (m * x - Real.exp x) + q * (m * y - Real.exp y)) / 2 < q * (m * ((x + y) / 2) - Real.exp ((x + y) / 2))` | Strict midpoint concavity of a positive-weight Poisson cell whenever the two linear predictors differ. |
| `Stat.finitePoissonObjective` | `(I → ℝ) → (I → ℝ) → (E →ₗ[ℝ] I → ℝ) → E → ℝ` | Given a finite index set, cell weights, cell means, a real-linear map from the parameter space to cell predictors, and a parameter value, the finite Poisson objective is the sum over cells of each weight times its mean times its predictor minus its predictor's exponential. |
| `Stat.finitePoissonObjective_continuous` | `Continuous (Causalean.Stat.finitePoissonObjective q m A)` | The finite Poisson criterion is continuous in its parameter whenever the linear design acts on a finite-dimensional normed space. |
| `Stat.finitePoissonObjective_exists_unique_max` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m i) → Function.Injective ⇑A → ∃! x, ∀ (y : E), Causalean.Stat.finitePoissonObjective q m A y ≤ Causalean.Stat.finitePoissonObjective q m A x` | Existence and uniqueness of the finite Poisson pseudo-true parameter. If every cell weight is strictly positive, every cell mean is strictly positive, and the linear design map is injective, then the finite Poisson criterion attains its supremum over the parameter space at a unique point. |
| `Stat.finitePoissonObjective_score` | `(∀ (y : E), Causalean.Stat.finitePoissonObjective q m A y ≤ Causalean.Stat.finitePoissonObjective q m A x) → ∑ i, q i * A d i * (m i - Real.exp (A x i)) = 0` | Every directional score vanishes at a global maximizer. |
| `Stat.finitePoissonObjective_isMax_of_score` | `(∀ (i : I), 0 ≤ q i) → (∀ (d : E), ∑ i, q i * A d i * (m i - Real.exp (A x i)) = 0) → ∀ (y : E), Causalean.Stat.finitePoissonObjective q m A y ≤ Causalean.Stat.finitePoissonObjective q m A x` | For a nonnegative weighted Poisson objective, vanishing of every directional score is sufficient for global maximality. |
| `Stat.uniqueGlobalMax_maximizerOrZero` | `(∃! x, ∀ (y : E), f y ≤ f x) → Causalean.Stat.IsUniqueGlobalMax f (Causalean.Stat.maximizerOrZero f)` | A criterion with a unique global maximum is maximized uniquely by its total maximizer selector. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.MEstimation.FinitePoissonConsistency -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.finitePoissonObjective_tendstoUniformlyOn` | `IsCompact K → (∀ (i : I), Filter.Tendsto (fun N => qN N i) Filter.atTop (nhds (q i))) → (∀ (i : I), Filter.Tendsto (fun N => mN N i) Filter.atTop (nhds (m i))) → TendstoUniformlyOn (fun N => Causalean.Stat.finitePoissonObjective (qN N) (mN N) A) (Causalean.Stat.finitePoissonObjective q m A) Filter.atTop K` | Pointwise convergence of the finitely many weights and means gives uniform convergence of the finite Poisson objective on every compact parameter set. |
| `Stat.finitePoissonObjective_eventually_common_compact` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m i) → Function.Injective ⇑A → (∀ (i : I), Filter.Tendsto (fun N => qN N i) Filter.atTop (nhds (q i))) → (∀ (i : I), Filter.Tendsto (fun N => mN N i) Filter.atTop (nhds (m i))) → (∀ᶠ (N : ℕ) in Filter.atTop, ∀ (y : E), Causalean.Stat.finitePoissonObjective (qN N) (mN N) A y ≤ Causalean.Stat.finitePoissonObjective (qN N) (mN N) A (argmax N)) → ∃ K, IsCompact K ∧ ∀ᶠ (N : ℕ) in Filter.atTop, argmax N ∈ K` | Convergent positive coefficients and a fixed injective design put any eventual sequence of global maximizers in one common compact ball. |
| `Stat.finitePoissonObjective_argmax_tendsto` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m i) → Function.Injective ⇑A → (∀ (i : I), Filter.Tendsto (fun N => qN N i) Filter.atTop (nhds (q i))) → (∀ (i : I), Filter.Tendsto (fun N => mN N i) Filter.atTop (nhds (m i))) → (∀ᶠ (N : ℕ) in Filter.atTop, ∀ (y : E), Causalean.Stat.finitePoissonObjective (qN N) (mN N) A y ≤ Causalean.Stat.finitePoissonObjective (qN N) (mN N) A (argmax N)) → Causalean.Stat.IsUniqueGlobalMax (Causalean.Stat.finitePoissonObjective q m A) limitArgmax → Filter.Tendsto argmax Filter.atTop (nhds limitArgmax)` | Stability of finite Poisson maximizers under convergence of weights and means. Given a sequence of finite Poisson criteria with weights `qN N` and means `mN N`, suppose the limiting weights are strictly positive, the limiting means are strictly positive, the linear design map is injective, the weights `qN N` converge cellwise to the limiting weights `q`, and the means `mN N` converge cellwise to the limiting means `m`. If `argmax N` maximizes the `N`-th criterion, eventually in `N`, and `limitArgmax` is the unique global maximizer of the limiting criterion, then the maximizer sequence `argmax` converges to `limitArgmax`. |
| `Stat.finitePoissonObjective_argmax_continuousAt_mean` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m₀ i) → Function.Injective ⇑A → ContinuousAt (fun m => Causalean.Stat.maximizerOrZero (Causalean.Stat.finitePoissonObjective q m A)) m₀` | Continuity of the finite Poisson maximizer in the means. If every cell weight is strictly positive and the base mean vector `m₀` has every entry strictly positive, and the linear design map is injective, then the selected maximizer of the finite Poisson criterion is continuous, as a function of the mean vector, at `m₀`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.MEstimation.FinitePoissonDerivative -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.expCellUpdatedMean` | `(I → ℝ) → I → ℝ → ℝ → I → ℝ` | Given a finite index set with decidable equality, a real-valued vector of cell means, one cell index, a real scale, and a real path coordinate, the exponentially updated cell-mean vector agrees with the original vector at every cell other than the selected one and assigns the selected cell the value $B\exp(x)$. |
| `Stat.finitePoissonObjective_expCell_argmax_snd_hasDerivAt` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m i) → 0 < B → Function.Injective ⇑A → (∀ (v : U × ℝ), (∀ (d : U × ℝ), ∑ i, q i * A d i * ((if i = j then B * Real.exp x₀ else 0) - Real.exp (A (Causalean.Stat.maximizerOrZero (Causalean.Stat.finitePoissonObjective q (Causalean.Stat.expCellUpdatedMean m j B x₀) A)) i) * A v i) = 0) → v.2 = betaDot) → HasDerivAt (fun x => (Causalean.Stat.maximizerOrZero (Causalean.Stat.finitePoissonObjective q (Causalean.Stat.expCellUpdatedMean m j B x) A)).2) betaDot x₀` | Derivative of the finite Poisson maximizer under one exponentially perturbed cell mean. Suppose every cell weight is strictly positive, every base cell mean is strictly positive, the perturbation scale `B` is strictly positive, and the linear design map is injective; consider replacing cell `j`'s mean by the exponential path `x ↦ B · exp x`. If `betaDot` is the unique value solving the score equation linearized at the maximizer for base point `x₀`, for every perturbation direction, then the second (scalar) coordinate of the maximizer, as a function of `x`, has derivative `betaDot` at `x₀`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.MEstimation.FinitePoissonSign -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.finitePoissonObjective_snd_sign_of_nuisance_score` | `(∀ (i : I), 0 < q i) → (∀ (i : I), 0 < m i) → Function.Injective ⇑A → (∀ (u : U), ∑ i, q i * A (u, 0) i * (m i - Real.exp (A (u₀, 0) i)) = 0) → have beta := (Causalean.Stat.maximizerOrZero (Causalean.Stat.finitePoissonObjective q m A)).2; have scalarScore := ∑ i, q i * A (0, 1) i * (m i - Real.exp (A (u₀, 0) i)); (beta < 0 ↔ scalarScore < 0) ∧ (beta = 0 ↔ scalarScore = 0) ∧ (0 < beta ↔ 0 < scalarScore)` | Sign of the selected scalar coefficient from the nuisance score. Suppose every cell weight is strictly positive, every cell mean is strictly positive, the linear design map is injective, and the conditional nuisance fit `u₀`, with the scalar coordinate held at zero, clears every nuisance-direction score. Then the selected scalar coefficient of the finite Poisson maximizer has exactly the sign of the remaining scalar score: it is negative, zero, or positive exactly when the scalar score is. |
<!-- /GEN -->

### 12m. `Stat/Limit/ContinuousMapping.lean`

Estimator-agnostic convergence-in-probability primitives. Extracted from the (formerly `private`) helpers in `Estimation/NPIV/DR/AsymptoticNormal.lean` so any estimator can reuse them. Consumed by `Stat/Inference/Studentize.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `Tendsto_inProb.comp_continuousAt` | `Xn →_p c` ∧ `ContinuousAt g c` ⇒ `g∘Xn →_p g c` | Continuous-mapping theorem in probability at a **constant** limit `c`. ε-δ pullback of `ContinuousAt` through `tendstoInMeasure_iff_dist`. **Proved.** |
| `Tendsto_inProb.pi_comp_continuousAt` | finite `ι`, coordinatewise `Yₙᵢ →_p cᵢ`, `ContinuousAt g c` ⇒ `g(Yₙ) →_p g(c)` | Finite-Pi continuous-mapping theorem in probability. Converts coordinate tails to a finite union bound, then applies continuity in the product metric. **Proved.** |
| `Tendsto_inProb.matrix_comp_continuousAt` | entrywise `Mₙᵢⱼ →_p M₀ᵢⱼ`, `ContinuousAt g M₀` ⇒ `g(Mₙ) →_p g(M₀)` | Matrix-valued corollary of the finite-Pi theorem, reindexing entries by `K×K`. **Proved.** |
| `Tendsto_inProb.inv` | `Yn →_p Y₀` ∧ `Y₀ ≠ 0` ⇒ `1/Yn →_p 1/Y₀` | Reciprocal continuity (special case of `comp_continuousAt` via `continuousAt_inv₀`). **Proved.** |
| `Tendsto_inProb.sub_const` | `Yn →_p Y₀` ⇒ `Yn − Y₀ →_p 0` | Center a convergence-in-probability statement at its constant limit. **Proved.** |
| `Tendsto_inProb.sub` | `Xn →_p a` ∧ `Yn →_p b` ⇒ `Xn − Yn →_p a − b` | Difference of two convergent-in-probability sequences (constant limits); ε/2 union bound (Mathlib lacks `TendstoInMeasure.sub`). **Proved.** |
| `Tendsto_inProb.isLittleOp_one` | `Yn →_p 0` ⇒ `Yn = o_p(1)` | Convergence in probability to zero is `o_p(1)`. **Proved.** |
| `Tendsto_inProb.isBigOp_one` | `Xn →_p c` ⇒ `Xn = O_p(1)` | In-probability tightness at a constant limit. **Proved.** |

### 12n. `Stat/Limit/WLLN.lean`

Generic weak law of large numbers for `IIDSample`, plus the consistent-variance corollary. Generalizes the Panel-specific `iidPanel_sampleMean_ae` (boundedness weakened to integrability; a.s. limit converted to in-probability). Supplies the `σ̂ₙ →_p σ²` consistency input that the studentized/Wald layer takes as a hypothesis.

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.measurable_sampleMean` | `Measurable g` ⇒ `Measurable (S.sampleMean g N)` | Measurability of the sample mean (finite sum of `g ∘ Z i`, scaled). **Proved.** |
| `IIDSample.sampleMean_tendsto_inProb` | `Integrable (g∘Z₀)` ⇒ `S.sampleMean g →_p ∫ g dP` | WLLN: i.i.d. sample mean converges in probability to the population mean. Wraps `strong_law_ae_real` (a.s.) then `tendstoInMeasure_of_tendsto_ae` on the finite measure `μ`; law transfer via `integral_map` + `S.law`. Requires `[IsProbabilityMeasure P]`. **Proved.** |
| `IIDSample.sampleSecondMoment_tendsto_inProb` | `Integrable (ψ²∘Z₀)` ⇒ `S.sampleMean ψ² →_p ∫ ψ² dP` | Consistent variance for a known influence function `ψ`: `(1/n)Σψ(Zᵢ)² →_p σ²`. Direct corollary with `g := ψ²`. **Proved.** |

### 12n.a. `Stat/Limit/ObservationDependentVanTrees/` — observation-dependent Bayesian Cramér–Rao bounds

Reusable van Trees infrastructure for σ-finite dominated observation models in which the estimand may depend jointly on the parameter and the observation. The final theorem combines guarded density-zero score algebra, absolute-continuity integration by parts, product-measure Fubini, and weighted L² Cauchy–Schwarz.

#### `Stat/Limit/ObservationDependentVanTrees/Basic.lean`

<!-- GEN:Causalean.Stat.Limit.ObservationDependentVanTrees.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Limit.ObservationDependentVanTrees.parameterMeasure` | `ℝ → ℝ → MeasureTheory.Measure ℝ` | Given a lower endpoint and an upper endpoint, the parameter reference measure is Lebesgue measure restricted to the closed interval from the lower to the upper endpoint. |
| `Stat.Limit.ObservationDependentVanTrees.jointDensity` | `(ℝ → ℝ) → (ℝ → X → ℝ) → ℝ × X → ℝ` | Given a prior density on the real parameter, a conditional observation density given that parameter, and a parameter--observation pair, the joint density is the product of the prior density and the conditional likelihood density at that pair. |
| `Stat.Limit.ObservationDependentVanTrees.priorScore` | `(ℝ → ℝ) → (ℝ → ℝ) → ℝ → ℝ` | Given a prior density, its parameter derivative, and a parameter value, the guarded prior score is the derivative divided by the density when the density is positive, and is zero otherwise. |
| `Stat.Limit.ObservationDependentVanTrees.likelihoodScore` | `(ℝ → X → ℝ) → (ℝ → X → ℝ) → ℝ → X → ℝ` | Given a conditional likelihood density, its parameter derivative, a parameter value, and an observation, the guarded likelihood score is the derivative divided by the likelihood when that likelihood is positive, and is zero otherwise. |
| `Stat.Limit.ObservationDependentVanTrees.jointScore` | `(ℝ → ℝ) → (ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → ℝ × X → ℝ` | Given a prior density, its derivative, a conditional likelihood density, its derivative, and a parameter--observation pair, the guarded joint score is the derivative of the prior--likelihood product divided by that product when the product is positive, and is zero otherwise. |
| `Stat.Limit.ObservationDependentVanTrees.priorInformation` | `ℝ → ℝ → (ℝ → ℝ) → (ℝ → ℝ) → ℝ` | Given a lower endpoint, an upper endpoint, a prior density, and its derivative, the prior Fisher information is the integral over the closed parameter interval of the prior density times the squared guarded prior score. |
| `Stat.Limit.ObservationDependentVanTrees.fisherInformation` | `MeasureTheory.Measure X → (ℝ → X → ℝ) → (ℝ → X → ℝ) → ℝ → ℝ` | Given a measure on a measurable observation space, a conditional likelihood density, its parameter derivative, and a parameter value, the conditional Fisher information is the observation integral of the likelihood times the squared guarded likelihood score at that parameter. |
| `Stat.Limit.ObservationDependentVanTrees.errorScoreField` | `(ℝ → ℝ) → (ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (X → ℝ) → ℝ × X → ℝ` | Given a prior density, its derivative, a conditional likelihood density, its derivative, a target function, an estimator, and a parameter--observation pair, the signed error--joint-score field is estimator error times guarded joint score, weighted by joint density. |
| `Stat.Limit.ObservationDependentVanTrees.sensitivityField` | `(ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → ℝ × X → ℝ` | Given a prior density, a conditional likelihood density, the parameter derivative of a target function, and a parameter--observation pair, the target-sensitivity field is that derivative weighted by the joint density. |
| `Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField` | `(ℝ → ℝ) → (ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (X → ℝ) → ℝ × X → ℝ` | Given a prior density, its derivative, a conditional likelihood density, its derivative, a target function, its parameter derivative, an estimator, and a parameter--observation pair, the derivative-balance field is the parameter derivative of the joint-density-weighted estimation error. |
| `Stat.Limit.ObservationDependentVanTrees.errorSqField` | `(ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (X → ℝ) → ℝ × X → ℝ` | Given a prior density, a conditional likelihood density, a target function, an estimator, and a parameter--observation pair, the squared-error field is the squared estimator error weighted by the joint density. |
| `Stat.Limit.ObservationDependentVanTrees.scoreSqField` | `(ℝ → ℝ) → (ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → ℝ × X → ℝ` | Given a prior density, its derivative, a conditional likelihood density, its derivative, and a parameter--observation pair, the squared-score field is the squared guarded joint score weighted by the joint density. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior` | `ℝ → ℝ → ℝ → ℝ` | Given a center, a radius, and a parameter value, the smooth quartic prior is $(15/(16a))\,[1-((θ-c)/a)^2]^2$ when $\|θ-c\|<a$, and is zero otherwise. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv` | `ℝ → ℝ → ℝ → ℝ` | Given a center, a radius, and a parameter value, the smooth quartic prior derivative is $-(15/(4a^3))(θ-c)[1-((θ-c)/a)^2]$ when $\|θ-c\|<a$, and is zero otherwise. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_nonneg` | `0 < a → ∀ (θ : ℝ), 0 ≤ Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ` | A positive bandwidth makes the smooth quartic prior nonnegative at every parameter value. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_pos_iff` | `0 < a → (0 < Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ ↔ \|θ - c\| < a)` | Under a positive bandwidth, the smooth quartic prior is positive exactly inside the open interval defined by its center and bandwidth. |
| `Stat.Limit.ObservationDependentVanTrees.support_smoothPrior` | `0 < a → Function.support (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) = Set.Ioo (c - a) (c + a)` | Under a positive bandwidth, the nonzero set of the smooth quartic prior is exactly its open support interval. |
| `Stat.Limit.ObservationDependentVanTrees.tsupport_smoothPrior` | `0 < a → tsupport (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) = Set.Icc (c - a) (c + a)` | Under a positive bandwidth, the topological support of the smooth quartic prior is exactly the corresponding closed interval. |
| `Stat.Limit.ObservationDependentVanTrees.support_smoothPrior_subset_Icc` | `0 < a → ell < c - a → c + a < u → Function.support (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) ⊆ Set.Icc ell u` | If the bandwidth is positive and the prior's left and right support endpoints lie strictly inside an ambient interval, then every point where the prior is nonzero lies in the ambient closed interval. |
| `Stat.Limit.ObservationDependentVanTrees.tsupport_smoothPrior_subset_Ioo` | `0 < a → ell < c - a → c + a < u → tsupport (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) ⊆ Set.Ioo ell u` | If the bandwidth is positive and the prior's left and right support endpoints lie strictly inside an ambient interval, then the prior's topological support lies in the ambient open interval. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_ambient_endpoints` | `0 < a → ell < c - a → c + a < u → Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a ell = 0 ∧ Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a u = 0` | If the bandwidth is positive and the prior's left and right support endpoints lie strictly inside an ambient interval, then the prior vanishes at both ambient endpoints. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_contDiff` | `0 < a → ContDiff ℝ 1 (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a)` | A positive bandwidth makes the smooth quartic prior continuously differentiable. |
| `Stat.Limit.ObservationDependentVanTrees.hasDerivAt_smoothPrior` | `0 < a → ∀ (θ : ℝ), HasDerivAt (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a θ) θ` | Under a positive bandwidth, the explicit derivative representative is the derivative of the smooth quartic prior at every parameter value. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_absolutelyContinuousOnInterval` | `0 < a → ell ≤ u → AbsolutelyContinuousOnInterval (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) ell u` | A positive bandwidth and ordered endpoints ensure that the smooth quartic prior is absolutely continuous on the interval. |
| `Stat.Limit.ObservationDependentVanTrees.integral_smoothPrior_volume` | `0 < a → ∫ (θ : ℝ), Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ = 1` | Under a positive bandwidth, the smooth quartic density has total Lebesgue integral one. |
| `Stat.Limit.ObservationDependentVanTrees.integral_smoothPrior_parameterMeasure` | `0 < a → ell ≤ c - a → c + a ≤ u → ∫ (θ : ℝ), Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u = 1` | If the bandwidth is positive and the prior's left and right support endpoints lie in an ambient interval, then the prior has mass one under Lebesgue measure restricted to that interval. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_integrable_parameterMeasure` | `0 < a → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u)` | A positive bandwidth makes the smooth quartic prior integrable on every restricted parameter interval. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_scoreSq_aestronglyMeasurable` | `0 < a → MeasureTheory.AEStronglyMeasurable (fun θ => Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u)` | A positive bandwidth makes the prior-weighted squared guarded score strongly measurable on every restricted parameter interval. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_scoreSq_integrable` | `0 < a → MeasureTheory.Integrable (fun θ => Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u)` | A positive bandwidth makes the prior-weighted squared guarded score integrable on every restricted parameter interval. |
| `Stat.Limit.ObservationDependentVanTrees.priorInformation_smoothPrior` | `0 < a → ell ≤ c - a → c + a ≤ u → Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) = 10 / a ^ 2` | If the bandwidth is positive and the prior's left and right support endpoints lie in an ambient interval, then the prior Fisher information equals ten divided by the squared bandwidth. |
| `Stat.Limit.ObservationDependentVanTrees.priorInformation_smoothPrior_le` | `0 < a → ell ≤ c - a → c + a ≤ u → Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) ≤ 40 / a ^ 2` | If the bandwidth is positive and the prior's left and right support endpoints lie in an ambient interval, then the prior Fisher information is at most forty divided by the squared bandwidth. |
<!-- /GEN -->

#### `Stat/Limit/ObservationDependentVanTrees/GuardedInformation.lean`

<!-- GEN:Causalean.Stat.Limit.ObservationDependentVanTrees.GuardedInformation -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Limit.ObservationDependentVanTrees.derivative_eq_zero_of_nonnegative_of_eq_zero` | `(∀ (t : ℝ), 0 ≤ f t) → HasDerivAt f df θ → f θ = 0 → df = 0` | A differentiable nonnegative function has zero derivative at any point where it attains value zero, so the supplied derivative is zero. |
| `Stat.Limit.ObservationDependentVanTrees.guarded_score_mul_density` | `0 ≤ q → (q = 0 → dq = 0) → (if 0 < q then dq / q else 0) * q = dq` | A nonnegative density with a derivative numerator that vanishes whenever the density vanishes has its guarded score times its density equal to that derivative numerator. |
| `Stat.Limit.ObservationDependentVanTrees.jointScore_eq_add` | `0 < w θ → 0 < p θ x → Causalean.Stat.Limit.ObservationDependentVanTrees.jointScore w dw p dp (θ, x) = Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ + Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x` | At a point where both the prior and likelihood densities are positive, the guarded joint score equals the sum of their guarded scores. |
| `Stat.Limit.ObservationDependentVanTrees.jointScore_mul_jointDensity` | `0 ≤ w θ → 0 ≤ p θ x → (w θ = 0 → dw θ = 0) → (p θ x = 0 → dp θ x = 0) → Causalean.Stat.Limit.ObservationDependentVanTrees.jointScore w dw p dp (θ, x) * Causalean.Stat.Limit.ObservationDependentVanTrees.jointDensity w p (θ, x) = dw θ * p θ x + w θ * dp θ x` | For nonnegative prior and likelihood densities whose derivative numerators vanish on their respective zero-density sets, joint density times guarded joint score equals the product-rule derivative numerator. |
| `Stat.Limit.ObservationDependentVanTrees.errorScoreField_eq_numerator` | `0 ≤ w θ → 0 ≤ p θ x → (w θ = 0 → dw θ = 0) → (p θ x = 0 → dp θ x = 0) → Causalean.Stat.Limit.ObservationDependentVanTrees.errorScoreField w dw p dp g T (θ, x) = (T x - g θ x) * (dw θ * p θ x + w θ * dp θ x)` | For nonnegative prior and likelihood densities whose derivative numerators vanish wherever the corresponding density is zero, the weighted error--score field equals estimation error times the unguarded product-rule numerator. |
| `Stat.Limit.ObservationDependentVanTrees.likelihoodScore_integral_eq_zero_of_normalization` | `θ ∈ Set.Ioo a b → (∀ t ∈ Set.Icc a b, ∫ (x : X), p t x ∂μ = 1) → HasDerivAt (fun t => ∫ (x : X), p t x ∂μ) (∫ (x : X), dp θ x ∂μ) θ → (∀ (x : X), 0 ≤ p θ x) → (∀ᵐ (x : X) ∂μ, p θ x = 0 → dp θ x = 0) → MeasureTheory.Integrable (fun x => p θ x) μ → MeasureTheory.Integrable (fun x => dp θ x) μ → ∫ (x : X), Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x * p θ x ∂μ = 0` | At an interior parameter value, a normalized likelihood whose integral may be differentiated using the supplied derivative, with nonnegative density, zero-set derivative control, and both fields integrable has conditional mean-zero guarded likelihood score. |
| `Stat.Limit.ObservationDependentVanTrees.joint_score_information_decomposition` | `(∀ (θ : ℝ), 0 ≤ w θ) → (∀ (θ : ℝ) (x : X), 0 ≤ p θ x) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, w θ = 0 → dw θ = 0) → (∀ᵐ (z : ℝ × X) ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ, p z.1 z.2 = 0 → dp z.1 z.2 = 0) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, ∫ (x : X), p θ x ∂μ = 1) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, ∫ (x : X), Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x * p θ x ∂μ = 0) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.scoreSqField w dw p dp) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.scoreSqField w dw p dp) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.AEStronglyMeasurable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b) → MeasureTheory.Integrable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * (Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2)) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * (Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2)) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.scoreSqField w dw p dp z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ = Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation a b w dw + ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b` | For nonnegative prior and likelihood densities with zero-set derivative control, likelihood normalization, centered conditional scores, and the stated measurable integrability of all square and cross fields, joint-score information equals prior information plus average conditional Fisher information. |
| `Stat.Limit.ObservationDependentVanTrees.finite_likelihood_normalization` | `∑ x, p θ x = 1 → ∫ (x : X), p θ x ∂MeasureTheory.Measure.count = 1` | If the masses of a finite likelihood sum to one, then its integral against counting measure is one. |
| `Stat.Limit.ObservationDependentVanTrees.hasDerivAt_finite_likelihood_sum` | `(∀ (x : X), HasDerivAt (fun t => p t x) (dp θ x) θ) → HasDerivAt (fun t => ∑ x, p t x) (∑ x, dp θ x) θ` | If each cell probability has the supplied parameter derivative, then the derivative of their finite sum is the sum of those derivatives. |
| `Stat.Limit.ObservationDependentVanTrees.finite_derivative_centering` | `(∀ (x : X), HasDerivAt (fun t => p t x) (dp θ x) θ) → (∀ᶠ (t : ℝ) in nhds θ, ∑ x, p t x = 1) → ∑ x, dp θ x = 0` | If each cell probability has the supplied derivative and the finite likelihood is locally normalized, then the derivative masses sum to zero. |
| `Stat.Limit.ObservationDependentVanTrees.finite_likelihoodScore_mul` | `(∀ (x : X), 0 ≤ p θ x) → (∀ (x : X), p θ x = 0 → dp θ x = 0) → ∀ (x : X), Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x * p θ x = dp θ x` | If finite likelihood masses are nonnegative and their derivatives vanish on zero-mass cells, then likelihood mass times guarded score equals the cell derivative. |
| `Stat.Limit.ObservationDependentVanTrees.finite_likelihoodScore_centered` | `(∀ (x : X), 0 ≤ p θ x) → (∀ (x : X), p θ x = 0 → dp θ x = 0) → ∑ x, dp θ x = 0 → ∑ x, Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x * p θ x = 0` | If finite likelihood masses are nonnegative, derivatives vanish on zero-mass cells, and the derivative masses are centered, then the likelihood-weighted guarded scores sum to zero. |
| `Stat.Limit.ObservationDependentVanTrees.fisherInformation_count_eq_sum` | `Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation MeasureTheory.Measure.count p dp θ = ∑ x, p θ x * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp θ x ^ 2` | Conditional Fisher information under counting measure equals the finite sum of likelihood mass times squared guarded score. |
| `Stat.Limit.ObservationDependentVanTrees.average_fisherInformation_le` | `(∀ (θ : ℝ), 0 ≤ w θ) → ∫ (θ : ℝ), w θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u = 1 → MeasureTheory.Integrable w (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → MeasureTheory.Integrable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → (∀ (θ : ℝ), 0 < w θ → Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ ≤ I) → ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u ≤ I` | If the prior is nonnegative, normalized, and integrable, prior-weighted Fisher information is integrable, and conditional Fisher information is pointwise bounded wherever the prior is positive, then average Fisher information is bounded by the same constant. |
<!-- /GEN -->

#### `Stat/Limit/ObservationDependentVanTrees/IntegrationByParts.lean`

<!-- GEN:Causalean.Stat.Limit.ObservationDependentVanTrees.IntegrationByParts -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Limit.ObservationDependentVanTrees.ac_product_integral_eq_boundary` | `a ≤ b → AbsolutelyContinuousOnInterval f a b → AbsolutelyContinuousOnInterval g a b → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt f (df θ) θ) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt g (dg θ) θ) → IntervalIntegrable (fun θ => df θ * g θ + f θ * dg θ) MeasureTheory.volume a b → ∫ (θ : ℝ) in a..b, df θ * g θ + f θ * dg θ = f b * g b - f a * g a` | Two functions on an ordered interval with absolute continuity, almost-everywhere derivative representatives, and an integrable product-rule field satisfy the product-rule integral equals the endpoint change in their product. |
| `Stat.Limit.ObservationDependentVanTrees.ac_weighted_error_integral_eq_boundary` | `a ≤ b → AbsolutelyContinuousOnInterval w a b → AbsolutelyContinuousOnInterval p a b → AbsolutelyContinuousOnInterval g a b → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt w (dw θ) θ) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt p (dp θ) θ) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt g (dg θ) θ) → IntervalIntegrable (fun θ => (dw θ * p θ + w θ * dp θ) * (c - g θ) - w θ * p θ * dg θ) MeasureTheory.volume a b → ∫ (θ : ℝ) in a..b, (dw θ * p θ + w θ * dp θ) * (c - g θ) - w θ * p θ * dg θ = w b * p b * (c - g b) - w a * p a * (c - g a)` | A prior weight, likelihood section, and observation-dependent target with absolute continuity, almost-everywhere derivative representatives, and an integrable weighted-error derivative field satisfy the weighted-error derivative integral equals the weighted endpoint difference. |
| `Stat.Limit.ObservationDependentVanTrees.product_integral_derivativeBalance_eq_zero` | `a ≤ b → ∀ {w dw : ℝ → ℝ} {p dp g dg : ℝ → X → ℝ} {T : X → ℝ}, AbsolutelyContinuousOnInterval w a b → (∀ᵐ (x : X) ∂μ, AbsolutelyContinuousOnInterval (fun θ => p θ x) a b) → (∀ᵐ (x : X) ∂μ, AbsolutelyContinuousOnInterval (fun θ => g θ x) a b) → (∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt w (dw θ) θ) → (∀ᵐ (x : X) ∂μ, ∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt (fun t => p t x) (dp θ x) θ) → (∀ᵐ (x : X) ∂μ, ∀ᵐ (θ : ℝ) ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b, HasDerivAt (fun t => g t x) (dg θ x) θ) → (∀ᵐ (x : X) ∂μ, w b * p b x * (T x - g b x) = 0 ∧ w a * p a x * (T x - g a x) = 0) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField w dw p dp g dg T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField w dw p dp g dg T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ) → ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField w dw p dp g dg T z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure a b).prod μ = 0` | In a σ-finite observation model, if the prior weight and almost every likelihood and target section are absolutely continuous with the stated derivative representatives, endpoint error products vanish, and the derivative-balance field is measurable and integrable, the joint integral of the derivative-balance field is zero. |
<!-- /GEN -->

#### `Stat/Limit/ObservationDependentVanTrees/WeightedL2.lean`

<!-- GEN:Causalean.Stat.Limit.ObservationDependentVanTrees.WeightedL2 -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Limit.ObservationDependentVanTrees.product_integral_eq_iterated` | `MeasureTheory.AEStronglyMeasurable F (μ.prod ν) → MeasureTheory.Integrable F (μ.prod ν) → ∫ (z : A × B), F z ∂μ.prod ν = ∫ (a : A), ∫ (b : B), F (a, b) ∂ν ∂μ ∧ ∫ (z : A × B), F z ∂μ.prod ν = ∫ (b : B), ∫ (a : A), F (a, b) ∂μ ∂ν` | A real-valued product field that is almost-everywhere strongly measurable and integrable under two σ-finite measures has both iterated integrals equal to its product-measure integral. |
| `Stat.Limit.ObservationDependentVanTrees.weighted_integral_mul_sq_le` | `(∀ᵐ (a : A) ∂μ, 0 ≤ q a) → MeasureTheory.AEStronglyMeasurable (fun a => f a ^ 2 * q a) μ → MeasureTheory.AEStronglyMeasurable (fun a => s a ^ 2 * q a) μ → MeasureTheory.AEStronglyMeasurable (fun a => f a * s a * q a) μ → MeasureTheory.Integrable (fun a => f a ^ 2 * q a) μ → MeasureTheory.Integrable (fun a => s a ^ 2 * q a) μ → MeasureTheory.Integrable (fun a => f a * s a * q a) μ → (∫ (a : A), f a * s a * q a ∂μ) ^ 2 ≤ (∫ (a : A), f a ^ 2 * q a ∂μ) * ∫ (a : A), s a ^ 2 * q a ∂μ` | Under a nonnegative weight and measurable, integrable weighted square and cross-product fields, the squared weighted pairing is bounded by the product of the two weighted second moments. |
<!-- /GEN -->

#### `Stat/Limit/ObservationDependentVanTrees/Main.lean`

<!-- GEN:Causalean.Stat.Limit.ObservationDependentVanTrees.Main -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Limit.ObservationDependentVanTrees.observation_dependent_van_trees` | `ell < u → ∀ (w dw : ℝ → ℝ) (p dp g dg : ℝ → X → ℝ) (T : X → ℝ), ContDiff ℝ 1 w → Function.support w ⊆ Set.Icc ell u → (∀ (θ : ℝ), HasDerivAt w (dw θ) θ) → (∀ (θ : ℝ), 0 ≤ w θ) → ∫ (θ : ℝ), w θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u = 1 → (∀ (θ : ℝ) (x : X), 0 ≤ p θ x) → (∀ θ ∈ Set.Icc ell u, ∫ (x : X), p θ x ∂μ = 1) → (∀ θ ∈ Set.Icc ell u, MeasureTheory.Integrable (fun x => p θ x) μ) → (∀ θ ∈ Set.Icc ell u, MeasureTheory.Integrable (fun x => dp θ x) μ) → (∀ θ ∈ Set.Ioo ell u, HasDerivAt (fun t => ∫ (x : X), p t x ∂μ) (∫ (x : X), dp θ x ∂μ) θ) → (∀ᵐ (x : X) ∂μ, AbsolutelyContinuousOnInterval (fun θ => p θ x) ell u) → (∀ᵐ (x : X) ∂μ, AbsolutelyContinuousOnInterval (fun θ => g θ x) ell u) → (∀ᵐ (z : ℝ × X) ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ, HasDerivAt (fun t => p t z.2) (dp z.1 z.2) z.1) → (∀ᵐ (z : ℝ × X) ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ, HasDerivAt (fun t => g t z.2) (dg z.1 z.2) z.1) → (∀ᵐ (x : X) ∂μ, w u * p u x * (T x - g u x) = 0 ∧ w ell * p ell x * (T x - g ell x) = 0) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField w dw p dp g dg T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.derivativeBalanceField w dw p dp g dg T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.errorScoreField w dw p dp g T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.errorScoreField w dw p dp g T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.sensitivityField w p dg) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.sensitivityField w p dg) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.errorSqField w p g T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.errorSqField w p g T) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (Causalean.Stat.Limit.ObservationDependentVanTrees.scoreSqField w dw p dp) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (Causalean.Stat.Limit.ObservationDependentVanTrees.scoreSqField w dw p dp) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → MeasureTheory.Integrable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2 ^ 2) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.AEStronglyMeasurable (fun z => w z.1 * p z.1 z.2 * (Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2)) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → MeasureTheory.Integrable (fun z => w z.1 * p z.1 z.2 * (Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw z.1 * Causalean.Stat.Limit.ObservationDependentVanTrees.likelihoodScore p dp z.1 z.2)) ((Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) → 0 < Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u w dw + ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u → (∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.sensitivityField w p dg z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ) ^ 2 / (Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u w dw + ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation μ p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) ≤ ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.errorSqField w p g T z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod μ` | A nondegenerate parameter interval with a continuously differentiable, compactly supported, normalized nonnegative prior; a normalized nonnegative dominated likelihood; the stated differentiation-under-the-integral, sectionwise absolute-continuity, derivative, boundary, product-measurability, and integrability conditions; and strictly positive finite total information ensures that Bayes mean-squared error for an estimator of an observation-dependent target is at least the squared joint mean of the target's parameter derivative divided by prior information plus average Fisher information. |
| `Stat.Limit.ObservationDependentVanTrees.FiniteVanTreesModelRegularity` | `(X : Type u_1) → [Fintype X] → [inst : MeasurableSpace X] → [MeasurableSingletonClass X] → ℝ → ℝ → (ℝ → ℝ) → (ℝ → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (ℝ → X → ℝ) → (X → ℝ) → Prop` | Finite-experiment van Trees regularity collects the model-specific normalization, absolute-continuity, derivative, measurability, and integrability conditions for a fixed prior and counting-measure observation model. |
| `Stat.Limit.ObservationDependentVanTrees.finite_vanTrees_lower_bound` | `ell < u → Causalean.Stat.Limit.ObservationDependentVanTrees.FiniteVanTreesModelRegularity X ell u w dw p dp g dg T → ContDiff ℝ 1 w → Function.support w ⊆ Set.Icc ell u → (∀ (θ : ℝ), HasDerivAt w (dw θ) θ) → (∀ (θ : ℝ), 0 ≤ w θ) → ∫ (θ : ℝ), w θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u = 1 → (∀ᵐ (x : X) ∂MeasureTheory.Measure.count, w u * p u x * (T x - g u x) = 0 ∧ w ell * p ell x * (T x - g ell x) = 0) → MeasureTheory.AEStronglyMeasurable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → MeasureTheory.Integrable (fun θ => w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.priorScore w dw θ ^ 2) (Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u) → 0 ≤ s → s ≤ ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.sensitivityField w p dg z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod MeasureTheory.Measure.count → ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation MeasureTheory.Measure.count p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u ≤ I → Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u w dw ≤ P → 0 < Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u w dw + ∫ (θ : ℝ), w θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation MeasureTheory.Measure.count p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u → s ^ 2 / (I + P) ≤ ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.errorSqField w p g T z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod MeasureTheory.Measure.count` | A nondegenerate parameter interval, [finite-model regularity] (hyp:M), continuous differentiability, compact support, a valid prior derivative, nonnegativity, normalization, vanishing boundary errors, measurability and integrability of prior information, a nonnegative sensitivity bound that lies below average sensitivity, upper bounds on average likelihood information and prior information, and positive total information imply a native-real Bayes squared-risk lower bound equal to squared sensitivity divided by the sum of the two information bounds. |
| `Stat.Limit.ObservationDependentVanTrees.smoothPrior_finite_vanTrees_lower_bound` | `0 < a → ell < c - a → c + a < u → Causalean.Stat.Limit.ObservationDependentVanTrees.FiniteVanTreesModelRegularity X ell u (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) p dp g dg T → 0 ≤ s → s ≤ ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.sensitivityField (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) p dg z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod MeasureTheory.Measure.count → (∀ (θ : ℝ), 0 < Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ → Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation MeasureTheory.Measure.count p dp θ ≤ I) → 0 < Causalean.Stat.Limit.ObservationDependentVanTrees.priorInformation ell u (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPriorDeriv c a) + ∫ (θ : ℝ), Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a θ * Causalean.Stat.Limit.ObservationDependentVanTrees.fisherInformation MeasureTheory.Measure.count p dp θ ∂Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u → s ^ 2 / (I + 40 / a ^ 2) ≤ ∫ (z : ℝ × X), Causalean.Stat.Limit.ObservationDependentVanTrees.errorSqField (Causalean.Stat.Limit.ObservationDependentVanTrees.smoothPrior c a) p g T z ∂(Causalean.Stat.Limit.ObservationDependentVanTrees.parameterMeasure ell u).prod MeasureTheory.Measure.count` | A positive bandwidth, strict containment of the left and right prior-support endpoints, finite-model regularity, a nonnegative sensitivity bound that lies below average sensitivity, a pointwise likelihood-information bound, and positive total information imply a native-real Bayes squared-risk lower bound equal to squared sensitivity divided by likelihood information plus forty over the squared bandwidth. |
<!-- /GEN -->

### 12n'. `Stat/SampleSplit/FoldBWLLN.lean`

Fold-B weak law of large numbers: the analogue of `sampleMean_tendsto_inProb` averaged over the evaluation fold `B(n)` of a `OneShotSplit` (a growing index subset) instead of the full sample. Unlike the full-sample WLLN (an SLLN/a.e. argument) this is a finite-variance Chebyshev argument over the fold, so it restricts cleanly to `B(n)`. Supplies the missing substrate for fold-B sample-mean consistency (e.g. the Jacobian-consistency input of the partially linear feasible-DML estimator).

| Declaration | Statement | Description |
|---|---|---|
| `OneShotSplit.foldB_sampleMean_tendsto_inProb` | `MemLp g 2 P` ⇒ `(1/\|B(n)\|)Σ_{i∈B(n)} g(Zᵢ) →_p ∫ g dP` | **Fold-B WLLN.** For a fixed finite-variance `g`, the fold-B sample mean converges in probability to the population mean. Proof: per-index variance/mean transport via `IdentDistrib.variance_eq`/`map`, `IndepFun.variance_sum` over the finset `B(n)` gives `Var = Var(g)/\|B(n)\|`, Chebyshev (`meas_ge_le_variance_div_sq`) and `\|B(n)\|→∞` (`foldB_card_tendsto`) close it (empty folds handled via the eventual filter). **Proved.** |

### 12n''. `Stat/Inference/VarianceEstimation.lean`

Consistent variance/covariance estimation for the i.i.d. sample type — the `σ̂ →ₚ σ₀` input the studentized/Wald layer takes as a hypothesis. Builds directly on the WLLN.

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.sampleMean_mul_tendsto_inProb` | `(1/N)Σ g₁(Zᵢ)g₂(Zᵢ) →ₚ ∫ g₁g₂ dP` | Empirical mean of a product (WLLN with `g := g₁·g₂`). **Proved.** |
| `IIDSample.abs_apply_mul_le_norm_sq` | `\|φ(ψ x)·φ'(ψ x)\| ≤ (‖φ‖‖φ'‖)·‖ψ x‖²` | Operator-norm domination helper. **Proved.** |
| `IIDSample.sampleCov_entry_tendsto_inProb` | `(1/N)Σ φ(ψ(Zᵢ))φ'(ψ(Zᵢ)) →ₚ ∫ φ(ψ)φ'(ψ) dP` | **Covariance consistency** for `ψ : X → E` via continuous coordinate functionals `φ φ' : E →L[ℝ] ℝ` (specializes to entrywise Σ̂ₖₗ via `EuclideanSpace.proj`). **Proved.** |
| `Tendsto_inProb.sqrt` | `Vn →ₚ v₀ ⇒ √Vn →ₚ √v₀` | Continuous-mapping for `Real.sqrt`. **Proved.** |
| `sqrt_var_tendsto_inProb` | `σ̂² →ₚ σ₀² ∧ σ₀>0 ⇒ √(σ̂²) →ₚ σ₀` | **Studentization-ready** standard-error consistency; feeds `div_tendsto_inProb_gaussian`. **Proved.** |

### 12n'''. `Stat/CLT/MultivariateCLT.lean`

Multivariate CLT via the **Cramér–Wold device**, the vector analogue of `clt_normalized_sum`. Discharges the multivariate CLT contact `_hCLT` taken as a hypothesis by `IsAsymLinearVec.tendsto_normal_vec` and `deltaMethod` (vector Δ-method). Weak convergence on a finite-dim real inner-product space `E` is reduced to pointwise charFun convergence (Lévy continuity, `ProbabilityMeasure.tendsto_iff_tendsto_charFun` from the `Clt` package); each direction's charFun is the scalar charFun of the projected sum `⟪t, vecNS⟫`, handled by the scalar CLT.

**Limit-law status:** the Cramér–Wold reduction and per-direction charFun limit `→ exp(−½∫⟪t,ψ⟫² dP)` are fully proven, sorry-free. The theorems here keep the target `Q` abstract, parameterized by `charFun Q t = exp(−½∫⟪t,ψ⟫² dP)`. That abstract hypothesis is **discharged concretely** in `Stat/CLT/GaussianLimit.lean` (see 12n'''c): `gaussianLimit ψ` is the covariance-`Σ` Gaussian built explicitly, and `IIDSample.clt_normalizedSum_vec` is the multivariate CLT against it with no abstract `Q`/charFun hypothesis remaining.

| Declaration | Statement | Description |
|---|---|---|
| `inner_normalizedSum` | `⟪t, vecNS n ω⟫ = scalarNS_{⟪t,ψ⟫} n ω` | Projection identity. **Proved.** |
| `IIDSample.normalizedSum_vec_charFun_tendsto` | `charFun(μ.map vecNS)(t) → exp(−½∫⟪t,ψ⟫² dP)` | **Per-direction charFun limit** from the scalar CLT via `integral_map`. **Proved.** |
| `Tendsto_dist_vec.of_charFun_tendsto` | pointwise charFun convergence ⇒ `Tendsto_dist_vec` | **Cramér–Wold wrapper**. **Proved.** |
| `IIDSample.clt_normalizedSum_vec_of_charFun` | Gaussian-charFun `Q` ⇒ `vecNS ⇒ Q` | **Multivariate CLT contact**; discharges `_hCLT`. **Proved.** |
| `IsAsymLinearVec.tendsto_normal_vec_clt` | vector asym-linearity + Gaussian-charFun `Q` ⇒ rescaled estimator ⇒ Q | **End-to-end vector asymptotic normality** (no CLT hypothesis). **Proved.** |

### 12n'''b. `Stat/CLT/GaussianCharFunBridge.lean`

Verified glue converting a *concrete* centered Gaussian into the abstract `hQ` characteristic-function target taken as a hypothesis by `MultivariateCLT`/`WaldVec`/`RatioDeltaMethod`. The multivariate-Gaussian charFun formula is `ProbabilityTheory.IsGaussian.charFun_eq'` (`Mathlib/Probability/Distributions/Gaussian/CharFun.lean`), proven at the current pin. The concrete `Q` is now constructed in `Stat/CLT/GaussianLimit.lean` (12n'''c), completing de-abstraction.

| Declaration | Statement | Description |
|---|---|---|
| `charFun_isGaussian_centered` | `IsGaussian Q` ∧ `∫ x ∂Q = 0` ⇒ `charFun Q t = exp(−½·covarianceBilin Q t t)` | centered-Gaussian charFun; specialises Mathlib `IsGaussian.charFun_eq'`. **Proved** (axioms: propext/choice/Quot). |
| `charFun_isGaussian_of_cov_eq` | + `∀ t, covarianceBilin Q t t = ∫⟪t,ψ⟫²dP` ⇒ `charFun Q t = exp(−½∫⟪t,ψ⟫²dP)` | matches the exact `hQ` shape of the multivariate CLT contact, ready to discharge it once `Q` is built. **Proved.** |

### 12n'''c. `Stat/CLT/SecondMomentOperator.lean` + `Stat/CLT/GaussianLimit.lean`

**De-abstraction of the multivariate CLT.** Together with `Mathlib/OperatorSqrt.lean` (operator √, 10j) and `Mathlib/StandardGaussian.lean` (10i), these build the concrete covariance-`Σ` Gaussian limit and discharge the abstract `hQ` of `MultivariateCLT`. `SecondMomentOperator` is the operator `Σ t = ∫ ⟪t,ψ x⟫ • ψ x ∂P`; `GaussianLimit` sets `gaussianLimit ψ := stdGaussian.map √Σ` and proves its charFun is the CLT target.

| Declaration | Statement | Description |
|---|---|---|
| `secondMomentLM` | `E →ₗ[ℝ] E` | Second-moment operator `Σ t = ∫⟪t,ψ⟫•ψ ∂P` (Bochner integral); linearity via `integral_add`/`integral_smul`. **Proved.** |
| `secondMomentLM_inner` | `⟪Σ t, s⟫ = ∫⟪t,ψ⟫⟪s,ψ⟫ ∂P` | Quadratic-form identity, via `integral_inner`. **Proved.** |
| `secondMomentLM_isPositive` | `(secondMomentLM ψ).IsPositive` | `Σ` is positive (symmetric + `⟪Σt,t⟫ = ∫⟪t,ψ⟫² ≥ 0`). **Proved.** |
| `gaussianLimit` | `Measure E` | The CLT limit: `(stdGaussian E).map √Σ`, centered Gaussian with covariance `Σ`. `IsGaussian`/`IsProbabilityMeasure` instances. **Proved.** |
| `gaussianLimit_mean` | `∫ x, x ∂(gaussianLimit ψ) = 0` | Centered (mean pushes through the continuous-linear √Σ; `stdGaussian_mean`). **Proved.** |
| `gaussianLimit_covarianceBilin` | `covarianceBilin (gaussianLimit ψ) t t = ∫⟪t,ψ⟫⟪t,ψ⟫ ∂P` | Covariance recovers the asymptotic-variance integral, via `covarianceBilin_map`, `posSqrtCLM_adjoint` (self-adjoint), `posSqrtCLM_comp_self` (√Σ∘√Σ=Σ), `covarianceBilin_stdGaussian`. **Proved.** |
| `gaussianLimit_charFun` | `charFun (gaussianLimit ψ) t = exp(−½∫⟪t,ψ⟫² dP)` | Exactly the abstract `hQ`; via `charFun_isGaussian_of_cov_eq`. **Proved.** |
| `IIDSample.clt_normalizedSum_vec` | centered sq-int `ψ` ⇒ `vecNS ⇒ gaussianLimit ψ` | **Multivariate CLT against the concrete Gaussian limit** — no abstract `Q`/charFun hypothesis. **Proved** (axiom-clean). |

### 12n''''. `Stat/Inference/WaldVec.lean`

Multivariate Wald / confidence-ellipsoid asymptotic coverage — the vector analogue of the scalar `Tendsto_dist.wald_coverage`. The Wald statistic `Wₙ = (√n(θ̂ₙ−θ₀))ᵀ Σ̂ₙ⁻¹ (√n(θ̂ₙ−θ₀))` is already ℝ-valued, so an ellipsoid `{θ : Wald ≤ c}` has coverage `χ(Iic c)` via one-sided portmanteau. The construction of `Wₙ` and the proof `Wₙ ⇒ χ` (continuous mapping of the joint limit through `(t,S) ↦ tᵀS⁻¹t`) is the caller's input, exactly as the scalar `wald_coverage` takes `Sₙ ⇒ N(0,1)`. The limit `χ` is kept abstract here, but identifying the image of the concrete Gaussian `gaussianLimit` (12n'''c) under the quadratic form `S ↦ SᵀΣ⁻¹S` as `χ²_d` is now **closed** in `Stat/Inference/ChiSquaredWald.lean` (12n'''d).

| Declaration | Statement | Description |
|---|---|---|
| `Tendsto_dist.wald_coverage_Iic` | `Wₙ ⇒ χ` ∧ `χ(∂Iic c)=0` ∧ bridge ⇒ `coverProb → χ(Iic c)` | **Ellipsoid coverage** via one-sided portmanteau on `Set.Iic c`. **Proved.** |
| `Tendsto_dist.wald_coverage_Iic_of_noAtoms` | `Wₙ ⇒ χ` (`[NoAtoms χ]`) ∧ bridge ⇒ `coverProb → χ(Iic c)` | Same, boundary-null discharged automatically for any atomless limit (e.g. non-degenerate χ²). **Proved.** |

### 12n'''d. `Stat/CLT/ChiSquared.lean` + `Stat/Inference/ChiSquaredWald.lean`

**χ² identification of the Wald limit.** `ChiSquared` *defines* the χ²_d law and `ChiSquaredWald` whitens `gaussianLimit` through the Wald quadratic form to recognise its image as χ²_d, closing the open step flagged in `WaldVec`. χ²_d is defined as the law of `‖W‖²` for `W ~ stdGaussian` (textbook squared-norm definition; the `Gamma(d/2,1/2)` density form is not developed — Mathlib has no χ²). The whitening: `gaussianLimit = stdGaussian.map √Σ`, and with `Σ` non-degenerate (injective ⇒ invertible in finite dim), `⟪√Σ w, Σ⁻¹(√Σ w)⟫ = ‖w‖²` by self-adjointness of `√Σ`.

| Declaration | Statement | Description |
|---|---|---|
| `chiSqDist` | `ℕ → Measure ℝ` | The χ²_d law: `(stdGaussian (EuclideanSpace ℝ (Fin d))).map ‖·‖²`. `IsProbabilityMeasure` instance. **Proved.** |
| `stdGaussian_map_linearIsometryEquiv` | `(stdGaussian E).map ι = stdGaussian F` for `ι : E ≃ₗᵢ F` | `stdGaussian` is isometry-invariant; matched characteristic functions (`charFun_isGaussian_centered` + `Measure.ext_of_charFun`, covariance preserved by `ι`). **Proved.** |
| `stdGaussian_map_normSq` | `(stdGaussian E).map ‖·‖² = chiSqDist (finrank ℝ E)` | The squared-norm law depends only on dimension. **Proved.** |
| `noAtoms_pi_normSq` / `noAtoms_chiSqDist` | `1 ≤ d ⇒ NoAtoms (chiSqDist d)` | χ²_d is atomless for `d ≥ 1` (squared first coordinate atomless, Fubini over the independent remainder; reduced to a product of 1-D Gaussians via `stdGaussian_map_normSq_eq_pi`). **Proved.** |
| `secondMomentInv` | `E →L[ℝ] E` | `Σ⁻¹ = (√Σ)⁻¹∘(√Σ)⁻¹` from injectivity of `Σ`; `secondMomentInv_secondMomentLM` / `secondMomentLM_secondMomentInv` certify it is the two-sided inverse. **Proved.** |
| `gaussianLimit_waldForm_map` | `(gaussianLimit ψ).map (S ↦ ⟪S, Σ⁻¹ S⟫) = chiSqDist (finrank ℝ E)` | **The χ² identification** (whitening through `√Σ`). **Proved.** |
| `Tendsto_dist.wald_coverage_chiSq` | `Wₙ ⇒ χ²_d` (`1≤d`) ∧ bridge ⇒ `coverProb → χ²_d(Iic c)` | WaldVec ellipsoid coverage specialised to the identified atomless limit `chiSqDist d`. **Proved.** |

### 12n'''e. `Stat/CLT/ChiSquaredProjection.lean` — squared-norm of a projected Gaussian is χ²

The law of `‖P z‖²` for an **orthogonal projection** `P` (self-adjoint idempotent) and `z ~ stdGaussian` is `χ²_{rank P}`. Reusable beyond GMM (F-tests, score/LM tests, rank-deficient Wald). Proof: corestrict `P` to its range `S`, transport by the orthonormal-basis isometry `S ≃ₗᵢ EuclideanSpace ℝ (Fin r)`; the pushforward of `stdGaussian E` is `stdGaussian S` (centered, covariance = inner product, via `Measure.ext_of_charFun` — the crux is that the corestriction's adjoint is the inclusion because `P` fixes its range), then `stdGaussian_map_normSq`.

| Declaration | Statement | Description |
|---|---|---|
| `stdGaussian_map_normSq_orthogonalProjection` | `IsSelfAdjoint P` ∧ `P ∘L P = P` ⇒ `(stdGaussian E).map (‖P·‖²) = chiSqDist (finrank ℝ (range P))` | **Projected-Gaussian χ².** The squared norm of an orthogonally-projected standard Gaussian is `χ²` with rank-of-projection degrees of freedom. **Proved, axiom-clean.** |

### 12o. `Stat/Inference/Studentize.lean`

Generic studentized-CLT and Wald-coverage engine, abstracted off the concrete TRAE-DR estimator (the NPIV/DR theorems now instantiate these). Portmanteau / Gaussian-boundary helpers are made public here.

| Declaration | Statement | Description |
|---|---|---|
| `Tendsto_dist.tendsto_measure_of_null_frontier` | `Xn ⇒ Q` ∧ `Q(∂E)=0` ⇒ `(μ.map Xn)(E) → Q(E)` | Continuity-set portmanteau for the project's `Tendsto_dist` wrapper. **Proved.** |
| `gaussianMeasure_zero_one_singleton` | `N(0,1)({x}) = 0` | Standard normal has no atoms. **Proved.** |
| `gaussianMeasure_zero_one_frontier_Icc` | `0 < z` ⇒ `N(0,1)(∂[−z,z]) = 0` | Zero boundary mass for a symmetric interval. **Proved.** |
| `Tendsto_dist.div_tendsto_inProb_gaussian` | `Xn ⇒ N(0,σ₀²)` ∧ `σ̂ →_p σ₀` ∧ `σ₀>0` ⇒ `Xn/σ̂ ⇒ N(0,1)` | **Studentized CLT** (random-denominator Slutsky). Scales `Xn` by `1/σ₀` (`const_mul_tendsto_gaussian`); the remainder `Xn·(1/σ̂ − 1/σ₀)` is `O_p(1)·o_p(1)` via `tightness` + `comp_continuousAt`; absorbs with `add_isLittleOp_one`. **Proved.** |
| `Tendsto_dist.wald_coverage` | `Sn ⇒ N(0,1)` ∧ `coverProb − μ{Sn∈[−z,z]} → 0` ⇒ `coverProb → N(0,1)([−z,z])` | **Wald coverage** (generic): portmanteau on the closed interval plus the caller-supplied event-equivalence bridge. Specialize at `z = z_{1−α/2}` for `1 − α` coverage. **Proved.** |

### 12o'. `Stat/Bootstrap/` — Nonparametric bootstrap standard error

The econometric workhorse use of the bootstrap (`vce(bootstrap)`): estimate the standard error of an asymptotically-linear estimator by the bootstrap, then form a studentized / Wald interval. The key fact is that the nonparametric (multinomial / Efron) bootstrap variance of `√n(X̄* − X̄)` is **exactly** the empirical variance `(1/n)Σ(ψ(Zᵢ) − ψ̄ₙ)²` — not an asymptotic approximation — so a *consistent* bootstrap SE plugs straight into the generic studentized CLT / Wald coverage of `Stat/Inference/Studentize.lean`. The nonparametric percentile / percentile-t refinements (which need conditional weak convergence) remain out of scope, but the **Gaussian multiplier (wild) bootstrap** below now supplies an *exact* distributional law on the conditioned sample. Three files: `Variance.lean` (the exact variance identity + consistency), `CI.lean` (studentized CLT + Wald coverage), and `GaussianMultiplier.lean` (exact multiplier-bootstrap law).

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.bootstrapVar` | `noncomputable def`, `ℕ → Ω → ℝ` | Bootstrap variance: the conditional variance `E*[ψ²] − (E*ψ)² = sampleMean(ψ²) − (sampleMean ψ)²` of a single multinomial-bootstrap draw `ψ(Z*₁)` over the empirical distribution. |
| `IIDSample.bootstrapVar_eq_centered` | `bootstrapVar = (1/n)Σ(ψ(Zᵢ) − ψ̄ₙ)²` | **Exact variance identity** (`E*[(ψ−E*ψ)²]`), the centered empirical second moment; holds for every `n` (`field_simp`/`ring` after the centered-sum expansion). **Proved, axiom-clean.** |
| `IIDSample.bootstrapVar_nonneg` | `0 ≤ bootstrapVar S ψ n ω` | Nonnegativity — it is a sum of squares (`bootstrapVar_eq_centered`). **Proved.** |
| `IIDSample.bootstrapVar_tendsto_inProb` | meas/int/sq-int/mean-zero `ψ` ⇒ `bootstrapVar →ₚ ∫ ψ² dP` | **Consistency of the bootstrap variance**: `sampleMean(ψ²) →ₚ ∫ψ²` and `(sampleMean ψ)² →ₚ (∫ψ)²=0`, subtracted via `Tendsto_inProb.sub`. **Proved, axiom-clean.** |
| `IIDSample.bootstrapSE` | `noncomputable def`, `√(bootstrapVar)` | Bootstrap standard error of `√n θ̂` — the bootstrap estimate of the asymptotic SD `√(∫ψ²)`. |
| `IIDSample.bootstrapSE_tendsto_inProb` | same hyps ⇒ `bootstrapSE →ₚ √(∫ ψ² dP)` | **SE consistency**; `Tendsto_inProb.sqrt` of the variance consistency. Supplies the `σ̂ →ₚ σ₀` input of `div_tendsto_inProb_gaussian`. **Proved.** |
| `IIDSample.bootstrapStudentized` | `noncomputable def`, `√n(θ̂−θ₀)/σ̂ₙ` | The bootstrap studentized statistic (full-sample index family). |
| `bootstrap_studentized_tendsto` | `IsAsymLinear θn θ₀ ψ S range` ∧ `0<∫ψ²` ⇒ `√n(θ̂−θ₀)/σ̂ₙ ⇒ N(0,1)` | **Bootstrap studentized CLT**: `IsAsymLinear.tendsto_normal` (numerator `⇒ N(0,∫ψ²)`) + bootstrap-SE consistency through `Tendsto_dist.div_tendsto_inProb_gaussian`. **Proved, axiom-clean.** |
| `bootstrap_wald_coverage` | + `z>0` ∧ coverage-event bridge ⇒ `coverProb → N(0,1)([−z,z])` | **Bootstrap Wald asymptotic coverage**; instantiates `Tendsto_dist.wald_coverage` on the bootstrap studentized statistic. `z = z_{1−α/2}` gives the `1−α` interval `θ̂ₙ ± z_{1−α/2}·σ̂ₙ/√n`. **Proved, axiom-clean.** |

#### 12o''. `Stat/Bootstrap/GaussianMultiplier.lean` — exact Gaussian multiplier (wild) bootstrap law

The multiplier/wild bootstrap (van der Vaart §23; Mammen) attaches i.i.d. mean-zero multipliers `ξᵢ` to the recentred data `aᵢ = xᵢ − x̄ₙ`. With **standard Gaussian** multipliers the perturbed statistic is, on a *fixed* data vector, **exactly** a centred Gaussian — no CLT and no conditional-distribution machinery, because a linear combination of independent Gaussians is Gaussian with variances adding. Both declarations **proved, sorry-free, axiom-clean**.

| Declaration | Statement | Description |
|---|---|---|
| `map_weighted_sum_gaussian` | `iIndepFun ξ`, each `μ.map (ξ i) = gaussianReal 0 1` ⇒ `μ.map (ω ↦ ∑ i, a i · ξ i ω) = gaussianReal 0 ⟨∑ i, (a i)², _⟩` | **Core fact**: a weighted sum of i.i.d. standard Gaussians has law `N(0, ∑ aᵢ²)`. `Finset` induction peeling one summand: each `aᵢ ξᵢ ~ N(0, aᵢ²)` (`gaussianReal_map_const_mul`), the partial sum is independent of the new term (`iIndepFun.indepFun_finset_sum_of_notMem`), and their laws convolve (`gaussianReal_add_gaussianReal_of_indepFun`). |
| `multiplierBootstrap_law` | as above with `aᵢ = n^{-1/2}(xᵢ − x̄)` ⇒ law of `Tₙ* = n^{-1/2} ∑ i, (xᵢ − x̄) ξᵢ` is `gaussianReal 0 ⟨sₙ², _⟩` | **Exact law of the studentized multiplier-bootstrap mean**: the bootstrap statistic is exactly `N(0, sₙ²)` with `sₙ² = (1/n) ∑ (xᵢ − x̄)²` the fixed sample variance. Since `sₙ² →ₚ σ²`, this is the distributional bootstrap counterpart of the CLT limit `N(0, σ²)`. |

---

#### 12p. `Stat/Quantile/Quantile.lean` — Quantile function (generalized inverse of a cdf)

Causal-agnostic statistical primitive: the lower quantile function of a real
probability measure, `quantile μ τ := sInf {x | τ ≤ cdf μ x}`, the
left-continuous generalized inverse of Mathlib's `ProbabilityTheory.cdf`.
Mathlib has the cdf but no generalized inverse; this file supplies it plus the
order (Galois) characterisation. Project-agnostic; upstream-candidate.

| Declaration | Statement | Description |
|---|---|---|
| `quantileSet μ τ` | `def`, `Set ℝ` | The super-level set `{x | τ ≤ cdf μ x}` defining the quantile. |
| `quantile μ τ` | `noncomputable def`, `ℝ` | `sInf (quantileSet μ τ)` — the `τ`-quantile (left-continuous generalized inverse of the cdf). |
| `quantileSet_up_closed` | `lemma` | `x ∈ quantileSet μ τ → x ≤ x' → x' ∈ quantileSet μ τ` (cdf monotone). **Proved.** |
| `bddBelow_quantileSet` | `lemma` | For `0 < τ`, the super-level set is bounded below (cdf → 0 at −∞). **Proved.** |
| `nonempty_quantileSet` | `lemma` | For `τ < 1`, the super-level set is nonempty (cdf → 1 at +∞). **Proved.** |
| `le_cdf_quantile` | `lemma` | For `τ < 1`, `τ ≤ cdf μ (quantile μ τ)`. Uses right-continuity of the cdf. **Proved.** |
| `le_cdf_of_quantile_le` | `lemma` | For `τ < 1`, `quantile μ τ ≤ x → τ ≤ cdf μ x`. **Proved.** |
| `quantile_le_of_le_cdf` | `lemma` | For `0 < τ`, `τ ≤ cdf μ x → quantile μ τ ≤ x` (`csInf_le`). **Proved.** |
| `quantile_le_iff` | `theorem` | **Galois connection** on `(0,1)`: `quantile μ τ ≤ x ↔ τ ≤ cdf μ x`. **Proved.** |
| `quantile_mono` | `lemma` | Quantile monotone in `τ` on `(0,1)`. **Proved.** |

#### 12q. `Stat/Quantile/EmpiricalCDF.lean` — Empirical cumulative distribution function

Sample analogue of `cdf P`, `F̂ₙ(y) = (1/n) Σ 1{Z_i ≤ y}` (the `sampleMean` of the indicator `cdfStat y`), with its fixed-`y` influence function `cdfIF P y z = 1{z ≤ y} − F(y)`. Because `√n (F̂ₙ(y) − F(y))` *is* the normalized influence-function sum, the empirical cdf is asymptotically linear with an identically-zero remainder, giving the fixed-`y` CLT directly. Causal-agnostic base layer for `Stat/Quantile/SampleQuantile.lean`; upstream-candidate.

| Declaration | Statement | Description |
|---|---|---|
| `cdfStat y` | `noncomputable def`, `ℝ → ℝ` | The lower-ray indicator `1{· ≤ y}` (= `Set.indicator (Iic y) 1`). |
| `measurable_cdfStat`, `cdfStat_nonneg`, `cdfStat_le_one`, `cdfStat_sq` | `lemma`s | Measurability; `0 ≤ 1{·≤y} ≤ 1`; idempotence `(1{·≤y})² = 1{·≤y}`. **Proved.** |
| `integrable_cdfStat` | `lemma` | `cdfStat y` is integrable (bounded on a finite measure). **Proved.** |
| `integral_cdfStat` | `lemma` | `∫ 1{·≤y} dP = cdf P y` (= `P.real (Iic y)`). **Proved.** |
| `IIDSample.empiricalCDF S y` | `noncomputable def`, `ℕ → Ω → ℝ` | The empirical cdf `F̂ₙ(y)`, the `sampleMean` of `cdfStat y`. |
| `cdfIF P y` | `noncomputable def`, `ℝ → ℝ` | Fixed-`y` influence function `1{z ≤ y} − F(y)`. |
| `measurable_cdfIF`, `cdfIF_mean_zero`, `abs_cdfIF_le_one`, `cdfIF_sq_integrable` | `lemma`s | Measurability; `∫ cdfIF dP = 0`; `|cdfIF| ≤ 1`; square-integrability. **Proved.** |
| `cdfIF_variance` | `lemma` | `∫ cdfIF² dP = F(y)(1 − F(y))`, the Bernoulli variance of `{Z ≤ y}`. **Proved.** |
| `isLittleOp_zero` | `lemma` | The zero sequence is `o_p(1)`. **Proved.** |
| `rescaledEmpiricalCDF_eq_normalizedSum` | `lemma` | **Key identity** `√n (F̂ₙ(y) − F(y)) = (1/√n) Σ cdfIF(Z_i)`. **Proved.** |
| `empiricalCDF_isAsymLinear` | `lemma` | `F̂ₙ(y)` is `IsAsymLinear` at `F(y)` with IF `cdfIF P y` (remainder ≡ 0). **Proved.** |
| `empiricalCDF_tendsto_normal` | `theorem` | **Empirical-cdf CLT** `√n (F̂ₙ(y) − F(y)) ⇒ N(0, F(y)(1−F(y)))`. **Proved** (instance of `IsAsymLinear.tendsto_normal`). |
| `empiricalCDF_tendsto_inProb` | `theorem` | **Consistency** (WLLN) `F̂ₙ(y) →ₚ F(y)`. **Proved.** |

#### 12q'. `Stat/Quantile/ConditionalMarkedSubsampleDkw/` — Empirical-CDF tails after Boolean selection

Reusable finite-sample conditioning infrastructure for treatment-arm and other Boolean-marked
subsamples.  It conditions on the complete mark vector, reindexes the retained outcome
coordinates, identifies their conditional iid product law, and averages a fixed-size empirical-CDF
tail bound across the finite partition.  The final theorem therefore turns any supplied
fixed-size DKW-style tail inequality into the corresponding random-selected-arm guarantee;
`dkwRadius` supplies the usual `sqrt(log(4/α)/(2m))` radius interface.

<!-- GEN:Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndices` | `(Fin n → Bool) → Bool → Finset (Fin n)` | For a Boolean word and a requested mark, the selected positions are the finite set of coordinates carrying that mark, as given by filtering all coordinates. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount` | `(Fin n → Bool) → Bool → ℕ` | For a Boolean word and a requested mark, the selected count is the number of its selected positions, given by their cardinality. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndex` | `(w : Fin n → Bool) → (a : Bool) → Fin (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a) ↪o Fin n` | For a Boolean word and a requested mark, the selected-coordinate embedding enumerates the selected positions in their original order, using the ordered finite-set enumeration. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndex_mark` | `w ((Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndex w a) j) = a` | For a Boolean word, a requested mark, and a selected-coordinate index, the coordinate selected by the embedding carries the requested mark. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.sum_selectedIndex` | `∑ j, f ((Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndex w a) j) = ∑ i ∈ Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedIndices w a, f i` | For a Boolean word, a requested mark, and a summand function, summing over the reindexed selected coordinates equals summing over all requested-mark positions. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent` | `(Fin n → Ω → Bool × ℝ) → (Fin n → Bool) → Set Ω` | For a marked sample and a Boolean word, the exact-word event is the set of outcomes whose complete mark vector equals that word. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measurableSet_wordEvent` | `(∀ (i : Fin n), Measurable (Z i)) → ∀ (w : Fin n → Bool), MeasurableSet (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w)` | Given a marked sample, measurability of every marked observation, and a Boolean word, the corresponding exact-word event is measurable. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.disjoint_wordEvent` | `w ≠ v → Disjoint (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w) (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z v)` | Given a marked sample and two distinct Boolean words, their exact-word events are disjoint. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.iUnion_wordEvent` | `⋃ w, Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w = Set.univ` | For a marked sample, the union of all exact-word events is the whole sample space. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCount` | `(Fin n → Ω → Bool × ℝ) → Bool → Ω → ℕ` | For a marked sample, a requested mark, and a sample outcome, the selected count is the number of observed coordinates carrying that mark. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCount_eq_on_wordEvent` | `ω ∈ Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w → Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCount Z a ω = Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a` | Given a marked sample, a requested mark, a Boolean word, and an outcome in that word's event, the observed selected count equals the word's deterministic selected count. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes` | `(Fin n → Ω → Bool × ℝ) → (w : Fin n → Bool) → (a : Bool) → Ω → Fin (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a) → ℝ` | For a marked sample, a Boolean word, and a requested mark, the selected outcome vector takes the outcome component at each reindexed selected coordinate. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.empiricalCDFVec` | `(Fin m → ℝ) → ℝ → ℝ` | For a finite real vector and a threshold, the vector empirical CDF is the normalized sum of lower-ray indicators. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedEmpiricalCDF` | `(Fin n → Ω → Bool × ℝ) → Bool → Ω → ℝ → ℝ` | For a marked sample, a requested mark, a sample outcome, and a threshold, the selected empirical CDF is the lower-ray count in the selected arm divided by its observed size. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.empiricalCDFVec_selectedOutcomes_eq` | `ω ∈ Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w → ∀ (y : ℝ), Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.empiricalCDFVec (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes Z w a ω) y = Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedEmpiricalCDF Z a ω y` | Given a marked sample, a requested mark, a Boolean word, an outcome in its exact-word event, and a threshold, the reindexed-vector empirical CDF equals the directly selected empirical CDF. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.uniformCDFDeviation` | `MeasureTheory.Measure ℝ → (Fin m → ℝ) → ℝ` | For a population law and a finite real vector, the uniform empirical-CDF deviation is the supremum over thresholds of the absolute empirical-minus-population CDF difference. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.fixedCDFBadSet` | `MeasureTheory.Measure ℝ → ℝ → Set (Fin m → ℝ)` | For a population law and a deviation radius, the fixed-size bad set is the set of vectors whose uniform empirical-CDF deviation exceeds that radius. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent` | `(Fin n → Ω → Bool × ℝ) → Bool → MeasureTheory.Measure ℝ → (ℕ → ℝ) → Set Ω` | For a marked sample, a requested mark, a population law, and a count-indexed radius, the selected-sample bad event is the event that the selected arm is nonempty and its uniform deviation exceeds the radius at its observed size. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent_inter_wordEvent` | `0 < Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a → Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent Z a ρ radius ∩ Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w = Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes Z w a ⁻¹' Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.fixedCDFBadSet ρ (radius (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a)) ∩ Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent Z w` | Given a marked sample, a requested mark, a population law, a count-indexed radius, a Boolean word, and a positive selected count for that word, intersecting the selected bad event with its exact-word event equals the reindexed fixed-size bad event intersected with that word event. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.ConditionalLaw -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID` | `(Ω : Type u_1) → [inst : MeasurableSpace Ω] → ℕ → MeasureTheory.Measure Ω → MeasureTheory.Measure (Bool × ℝ) → Type u_1` | A finite marked iid experiment packages the marked observation family, its coordinatewise measurability, mutual independence, and the common marginal law. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization` | `MeasureTheory.Measure (Bool × ℝ) → Bool → ENNReal → MeasureTheory.Measure ℝ → Prop` | A Boolean mark factorization packages a positive requested-mark mass and the identity expressing each marked outcome-set mass as that mass times the outcome law. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.measure_wordEvent_eq_prod` | `μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w) = ∏ i, ν {z \| z.1 = w i}` | Given a finite marked iid experiment and a Boolean word, the probability of its exact-word event equals the product of its coordinatewise mark probabilities. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.map_snd_cond_mark_eq` | `Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization ν a e ρ → ∀ (i : Fin n), MeasureTheory.Measure.map (fun ω => (S.Z i ω).2) μ[\|{ω \| (S.Z i ω).1 = a}] = ρ` | Given a finite marked iid experiment, a requested mark, its mass, a probability law for selected outcomes, a Boolean mark factorization, and a coordinate, conditioning that coordinate's outcome on its requested mark gives the selected-outcome law. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.outcomes_iIndep_cond_word` | `μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w) ≠ 0 → ProbabilityTheory.iIndepFun (fun i ω => (S.Z i ω).2) μ[\|Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w]` | Given a finite marked iid experiment, a Boolean word, and positive probability for its exact-word event, all outcome coordinates remain mutually independent after conditioning on that event. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.selectedOutcome_map_cond_word_eq` | `Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization ν a e ρ → ∀ (w : Fin n → Bool), μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w) ≠ 0 → ∀ (j : Fin (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedWordCount w a)), MeasureTheory.Measure.map (fun ω => Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes S.Z w a ω j) μ[\|Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w] = ρ` | Given a finite marked iid experiment, a requested mark, its mass, a selected-outcome law, a Boolean mark factorization, a Boolean word, positive probability for that word, and a selected-coordinate index, that reindexed selected outcome has law ρ after conditioning on the complete word. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.selectedOutcomes_iIndep_cond_word` | `μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w) ≠ 0 → ProbabilityTheory.iIndepFun (fun j ω => Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes S.Z w a ω j) μ[\|Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w]` | Given a finite marked iid experiment, a requested mark, a Boolean word, and positive probability for its exact-word event, the reindexed selected outcomes remain mutually independent after conditioning on that event. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measurable_selectedOutcomes` | `Measurable (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes S.Z w a)` | Given a finite marked iid experiment, a requested mark, and a Boolean word, the map from a sample outcome to its reindexed selected outcome vector is measurable. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.MarkedIID.selectedOutcomes_conditionalLaw` | `Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization ν a e ρ → ∀ (w : Fin n → Bool), μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w) ≠ 0 → MeasureTheory.Measure.map (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedOutcomes S.Z w a) μ[\|Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.wordEvent S.Z w] = MeasureTheory.Measure.pi fun x => ρ` | Given a finite marked iid experiment, a requested mark, its mass, a selected-outcome law, a Boolean mark factorization, a Boolean word, and positive probability for that word, the reindexed selected outcome vector conditioned on the word has the finite iid product law with marginal ρ. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.FiniteAggregation -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measure_eq_sum_mul_cond_of_finite_partition` | `(∀ (i : ι), MeasurableSet (C i)) → (∀ (i j : ι), i ≠ j → Disjoint (C i) (C j)) → ⋃ i, C i = Set.univ → ∀ (E : Set Ω), MeasurableSet E → μ E = ∑ i, μ (C i) * μ[E \| C i]` | Given a probability measure, a finite collection of cells, measurability of every cell, pairwise disjointness of distinct cells, coverage of the whole sample space, an event, and measurability of that event, its probability equals the finite sum of cell mass times conditional event probability. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measure_le_of_cond_le_on_finite_partition` | `(∀ (i : ι), MeasurableSet (C i)) → (∀ (i j : ι), i ≠ j → Disjoint (C i) (C j)) → ⋃ i, C i = Set.univ → ∀ (E : Set Ω), MeasurableSet E → ∀ (β : ENNReal), (∀ (i : ι), μ (C i) ≠ 0 → μ[E \| C i] ≤ β) → μ E ≤ β` | Given a probability measure, a finite collection of cells, measurability of every cell, pairwise disjointness of distinct cells, coverage of the whole sample space, an event, measurability of that event, a probability bound, and that bound conditional on each positive-mass cell, the unconditional event probability is at most the same bound. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.Measurability -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.empiricalCDFVec_continuousWithinAt_Ici` | `ContinuousWithinAt (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.empiricalCDFVec x) (Set.Ici y) y` | For a finite real vector and a threshold, its empirical CDF is continuous from the right at that threshold. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measurable_uniformCDFDeviation` | `Measurable (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.uniformCDFDeviation ρ)` | For a probability law, the uniform empirical-CDF deviation of a finite real vector from that law is measurable. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measurableSet_fixedCDFBadSet` | `MeasurableSet (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.fixedCDFBadSet ρ radius)` | For a probability law and a deviation radius, the fixed-size uniform empirical-CDF bad set is measurable. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.TailLift -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.measurableSet_selectedCDFBadEvent` | `MeasurableSet (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent S.Z a ρ radius)` | Given a finite marked iid experiment, a requested mark, a selected-outcome probability law, and a count-indexed radius, the selected-subsample empirical-CDF bad event is measurable. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.conditionalMarkedSubsample_empiricalCDF_tail` | `Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization ν a e ρ → ∀ (radius : ℕ → ℝ) (β : ENNReal), (∀ (m : ℕ), 0 < m → (MeasureTheory.Measure.pi fun x => ρ) (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.fixedCDFBadSet ρ (radius m)) ≤ β) → μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent S.Z a ρ radius) ≤ β` | Given a finite marked iid experiment, a requested mark, its mass, a selected-outcome law, a Boolean mark factorization, a count-indexed radius, a probability bound, and the corresponding fixed-size iid empirical-CDF bound at every positive sample size, the probability that the selected arm is nonempty and its uniform empirical-CDF deviation exceeds its count-indexed radius is at most β. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.dkwRadius` | `ℝ → ℕ → ℝ` | For a confidence parameter and a selected count, the DKW confidence radius is the square root of log(4/α) divided by twice that count. |
| `Stat.Quantile.ConditionalMarkedSubsampleDkw.conditionalMarkedSubsample_dkwRadius` | `Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.BooleanMarkFactorization ν a e ρ → ∀ (α : ℝ), 0 < α → α ≤ 1 → (∀ (m : ℕ), 0 < m → (MeasureTheory.Measure.pi fun x => ρ) (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.fixedCDFBadSet ρ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.dkwRadius α m)) ≤ ENNReal.ofReal (α / 2)) → μ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.selectedCDFBadEvent S.Z a ρ (Causalean.Stat.Quantile.ConditionalMarkedSubsampleDkw.dkwRadius α)) ≤ ENNReal.ofReal (α / 2)` | Given a finite marked iid experiment, a requested mark, its mass, a selected-outcome law, a Boolean mark factorization, a confidence parameter between zero and one, and the fixed-size DKW--Massart bound at every positive sample size, the selected-subsample empirical-CDF deviation at the DKW radius has tail probability at most α/2. |
<!-- /GEN -->

#### 12r. `Stat/Quantile/SampleQuantile.lean` — Sample-quantile asymptotics (Bahadur representation)

`√n`-asymptotic normality of a quantile-estimator sequence `q̂ₙ` for the `τ`-quantile of `P`, via the Bahadur representation with influence function `ψ_τ(z) = (τ − 1{z ≤ q₀})/f₀` and asymptotic variance `τ(1−τ)/f₀²`. The hard empirical-process (Donsker / asymptotic-equicontinuity) content — the `o_p(n^{-1/2})` Bahadur remainder — is **exposed as a hypothesis** in the `QuantileRegularity` bundle, mirroring the `StochEquicontAt` pattern of `Stat/MEstimation/EmpiricalExpansion.lean`; everything downstream is proved. Reusable by any quantile estimator (plain sample quantile, IPW-weighted QTE quantile, …) satisfying the expansion. References: Bahadur (1966); van der Vaart (1998) §21.

| Declaration | Statement | Description |
|---|---|---|
| `quantileIF τ q₀ f₀` | `noncomputable def`, `ℝ → ℝ` | Sample-quantile influence function `(τ − 1{z ≤ q₀})/f₀`. |
| `measurable_quantileIF` | `lemma` | Measurability of `quantileIF`. **Proved.** |
| `quantileIF_mean_zero` | `lemma` | `∫ ψ_τ dP = 0`, given `cdf P q₀ = τ`. **Proved.** |
| `quantileIF_sq_integrable` | `lemma` | `ψ_τ` is square-integrable (bounded by `(|τ|+1)/|f₀|`). **Proved.** |
| `quantileIF_variance` | `lemma` | `∫ ψ_τ² dP = τ(1−τ)/f₀²`, the classical sample-quantile asymptotic variance. **Proved.** |
| `QuantileRegularity S qn τ q₀ f₀` | `structure` (`Prop`) | Regularity bundle: `τ ∈ (0,1)`, density `f₀ > 0`, `cdf P q₀ = τ`, `HasDerivAt (cdf P) f₀ q₀`, and `bahadur` — the exposed Donsker remainder (= `IsAsymLinear.remainder` for `(q̂ₙ, q₀, ψ_τ)`). |
| `QuantileRegularity.isAsymLinear` | `lemma` | The bundle is an `IsAsymLinear` witness for `q̂ₙ` at `q₀` with IF `ψ_τ`. **Proved.** |
| `QuantileRegularity.tendsto_normal` | `theorem` | **Sample-quantile CLT** `√n (q̂ₙ − q₀) ⇒ N(0, τ(1−τ)/f₀²)`. **Proved** (specializes `IsAsymLinear.tendsto_normal` with the closed-form variance). |

#### 12s. `Stat/Quantile/EmpiricalQuantile.lean` — Sample quantile (empirical-cdf inverse)

The `τ`-sample quantile `q̂ₙ(τ) = quantile νₙ τ`, the generalized inverse of the empirical cdf, where `νₙ = (1/n) Σ_{i<n} δ_{Zᵢ}` is the empirical measure. Supplies the deterministic/structural facts (switching relation, monotonicity, atom bound) feeding the Bahadur derivation. **Fully proved, sorry-free, axiom-clean.**

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.empiricalMeasure S n ω` | `noncomputable def` | Empirical measure `(1/n) Σ_{i<n} δ_{Zᵢ}`. |
| `IIDSample.empiricalMeasure_isProbabilityMeasure` | `lemma` | For `0 < n`, `νₙ` is a probability measure. **Proved.** |
| `IIDSample.empiricalMeasure_real_Iic` | `lemma` | `νₙ.real (Iic y) = F̂ₙ(y)`. **Proved.** |
| `IIDSample.empiricalMeasure_cdf` | `lemma` | **cdf bridge** `cdf νₙ = F̂ₙ`. **Proved.** |
| `IIDSample.sampleQuantile S τ` | `noncomputable def` | Sample quantile `q̂ₙ(τ) = quantile νₙ τ`. |
| `IIDSample.sampleQuantile_le_iff` | `lemma` | **Switching relation** `q̂ₙ(τ) ≤ x ↔ τ ≤ F̂ₙ(x)` (`0<τ<1`, `0<n`). **Proved** (from `quantile_le_iff` + cdf bridge). |
| `IIDSample.empiricalCDF_monotone` | `lemma` | `y ↦ F̂ₙ(y)` is monotone. **Proved**. |
| `IIDSample.sampleQuantile_atom_bound` | `lemma` | **Atom bound** `\|F̂ₙ(q̂ₙ) − τ\| ≤ 1/n` a.s., under atomless `P` (`Continuous (cdf P)`); needs `[IsProbabilityMeasure μ]`. **Proved** (no-atoms from continuous cdf via `measure_singleton`/`leftLim`; a.s. tie-free from `IndepFun`+`NoAtoms`; discrete count `card(Cle q)=card(Clt)+card(Ceq)`, ties `≤1`, strict part `≤ nτ` via threshold below `q`). |

#### 12t. `Stat/Quantile/SampleQuantileBahadur.lean` — Bahadur representation (DERIVED)

Derives the Bahadur remainder of the **sample** quantile from elementary tools (Chebyshev + monotone-grid oscillation) — no Donsker/empirical-process hypothesis. Turns `SampleQuantile.QuantileRegularity.bahadur` from a hypothesis into a theorem for `q̂ₙ`. **Fully proved, sorry-free, axiom-clean.** This file is now a thin re-export umbrella; the derivation is split by layer under `Stat/Quantile/SampleQuantileBahadur/`: `Oscillation.lean` (L2 Chebyshev increment + L3 monotone-grid oscillation, incl. `SampleQuantileReg`/`empProcess`), `Rate.lean` (L4 root-`n` rate), `Linearity.lean` (L5 inversion + L6 assembly). Existing imports of `Causalean.Stat.Quantile.SampleQuantileBahadur` are unaffected. The declarations below are listed by their owning layer.

| Declaration | Statement | Description |
|---|---|---|
| `SampleQuantileReg P τ q₀ f₀` | `structure` (`Prop`) | Regularity: `τ∈(0,1)`, `f₀>0`, `cdf P q₀=τ`, `HasDerivAt (cdf P) f₀ q₀`, **and** `cont` (atomless `P`). |
| `IIDSample.empProcess S n ω y` | `noncomputable def` | Centered scaled empirical process `Gₙ(y) = √n (F̂ₙ(y) − F(y))`. |
| `IIDSample.empProcess_increment_tendsto_zero` | `lemma` (L2) | `Gₙ(q₀+u/√n) − Gₙ(q₀) →ₚ 0` (Chebyshev second-moment via `iid_centered_sum_sq_lintegral_le`). **Proved.** |
| `IIDSample.empProcess_cell_sandwich`, `empProcess_node_max_tendsto_zero`, `exists_grid_bracket` | `lemma`s | L3 helpers: per-cell monotone sandwich; finite-grid node-max `→ₚ 0`; uniform-mesh bracketing. **Proved.** |
| `IIDSample.empProcess_oscillation` | `lemma` (L3, **crux**) | At random `Uₙ=O_p(1)`: `Gₙ(q₀+Uₙ/√n) − Gₙ(q₀) →ₚ 0` (monotone-grid sandwich). **Proved.** |
| `cdf_increment_sqrt_tendsto` (in `.Oscillation`), `Tendsto_dist.limsup_measure_closed_le`, `gaussian_tail_small`, `IIDSample.empProcess_q0_tendsto_normal`/`_bigO` | `lemma`s | L4 helpers: Taylor limit `√n(F(q₀+a/√n)−F(q₀))→f₀a` (single canonical lemma defined in `Oscillation.lean`, reused by `Rate.lean` — the former duplicate `_pre`/`Rate` copies are consolidated); closed-set portmanteau; Gaussian-tail smallness; `Gₙ(q₀) ⇒ N(0,τ(1−τ))` + its `O_p(1)`. **Proved.** |
| `IIDSample.sampleQuantile_rate` | `lemma` (L4) | `√n(q̂ₙ−q₀) = O_p(1)` (two-sided tail via switching + portmanteau). **Proved.** |
| `IIDSample.sampleQuantile_atom_term_tendsto_zero`, `sampleQuantile_taylor_remainder_tendsto_zero`, `Tendsto_inProb.add_zero_zero`/`of_isLittleOp_one`/`const_mul_zero` | `lemma`s | L5/L6 helpers: `√n(F̂ₙ(q̂ₙ)−τ)→ₚ0`; `√n·R(q̂ₙ)→ₚ0`; `Tendsto_inProb` `+`/bridge/scalar-mul algebra. **Proved.** |
| `IIDSample.sampleQuantile_inversion` | `lemma` (L5) | `Gₙ(q̂ₙ) = −f₀·√n(q̂ₙ−q₀) + o_p(1)` (switching + atom bound + Taylor). **Proved.** |
| `IIDSample.normalizedSum_quantileIF_eq` | `lemma` | `(1/√n) Σ ψ_τ(Zᵢ) = −Gₙ(q₀)/f₀`. **Proved.** |
| `IIDSample.sampleQuantile_isAsymLinear` | `theorem` (L6) | **Derived** `IsAsymLinear` for `q̂ₙ` (Bahadur remainder proved `o_p(1)`: `f₀·R̃ₙ = L5 − L3`). **Proved, axiom-clean.** |
| `IIDSample.sampleQuantile_quantileRegularity` | `theorem` | `q̂ₙ` satisfies `QuantileRegularity` with `bahadur` derived ⇒ scalar CLT `√n(q̂ₙ−q₀)⇒N(0,τ(1−τ)/f₀²)` for free. **Proved.** |

#### 12u. `Stat/Quantile/SampleQuantileJoint.lean` — Joint normality of a quantile vector

Joint asymptotic normality of `(q̂ₙ(τ₁),…,q̂ₙ(τ_k))` via the per-coordinate derived Bahadur + the project multivariate CLT (Cramér–Wold). Target `Q` kept abstract via Gaussian charFun, as in the rest of the multivariate-CLT stack. **Fully proved, sorry-free, axiom-clean.**

| Declaration | Statement | Description |
|---|---|---|
| `eucl v` | `noncomputable abbrev` | Pack `Fin k → ℝ` into `EuclideanSpace ℝ (Fin k)` (the `PiLp 2` equiv). |
| `eucl_apply`, `norm_sq_eucl`, `norm_eucl_le_sum_abs` | `lemma`s | `(eucl v) j = v j`; `‖eucl v‖² = ∑ vⱼ²`; `‖eucl v‖ ≤ ∑ \|vⱼ\|`. **Proved.** |
| `quantileIFVec τ q f` | `noncomputable def` | Joint IF `ψ(z)_j = (τⱼ−1{z≤qⱼ})/fⱼ`. |
| `IIDSample.sampleQuantileVec S τ` | `noncomputable def` | Sample-quantile vector `(q̂ₙ(τⱼ))_j`. |
| `measurable_quantileIFVec` | `lemma` | Measurability of `ψ`. **Proved.** |
| `quantileIF_cross` | `lemma` | **Covariance entry** `∫ ψ_{τⱼ}ψ_{τₗ} dP = (min(τⱼ,τₗ)−τⱼτₗ)/(fⱼfₗ)` (`1{≤qⱼ}1{≤qₗ}=1{≤min}`, `F(min)=min(τ)` by monotonicity). **Proved.** |
| `IIDSample.sampleQuantileVec_isAsymLinearVec` | `theorem` | Vector asymptotic linearity (coordinatewise from scalar Bahadur; `eucl` commutes with `•`/`−`/`∑`; norm `≤ ∑\|Rₙⱼ\|`, each `o_p(1)`). **Proved.** |
| `IIDSample.sampleQuantileVec_tendsto_normal` | `theorem` | **Headline** `√n(q̂ₙ(τⱼ)−qⱼ)_j ⇒ N(0,Σ)`, `Σ_{jl}=(min(τⱼ,τₗ)−τⱼτₗ)/(fⱼfₗ)` (`Q` abstract Gaussian-charFun via `tendsto_normal_vec_clt`). **Proved, axiom-clean.** |

### 12v. `Stat/GMM/` — Generalized Method of Moments (Hansen 1982)

Abstract GMM stack over a moment model `g : E → X → F` (parameter `θ : E`,
moment vector valued in `F`, over-identified `dim E ≤ dim F`).  Three layers:
the pure operator-algebra **optimal-weighting theorem** (`VarianceAlgebra`), the
statistical **problem bundle + efficiency** (`Setup`), and **asymptotic
normality** as a corollary of the Z-estimator CLT (`AsymptoticNormality`).  All
declarations are namespace `Causalean.Stat`.  Spec: Hansen (1982); Newey &
McFadden (1994), §3.  All **sorry-free and axiom-clean**.

#### 12v.1. `Stat/GMM/VarianceAlgebra.lean` — sandwich/efficient variance algebra

Pure finite-dimensional operator algebra (no measure theory) on continuous
linear maps between real inner-product spaces.  Inverses are carried as *data
with two-sided witnesses* (mirroring `ZEstimatorRegularity.J₀_inv`), so no
operator inverse is constructed.

| Declaration | Statement | Description |
|---|---|---|
| `gmmBread G W` | `(adjoint G) ∘L W ∘L G : E →L E` | The **bread** `GᵀWG` of Jacobian `G : E →L F` and weighting `W : F →L F`; symmetric when `W` is, invertible at full-rank `G`, `W ≻ 0`. |
| `gmmSandwich G W Cov breadInv` | `breadInv ∘L Gᵀ ∘L W ∘L Cov ∘L W ∘L G ∘L breadInv` | The **sandwich asymptotic variance** `(GᵀWG)⁻¹ GᵀW Σ WG (GᵀWG)⁻¹`, given a two-sided inverse `breadInv` of the bread. (`Cov` = covariance `Σ`, since `Σ` is a reserved Lean token.) |
| `adjoint_inv_self` | `adjoint M = M` ∧ `M ∘L N = id` ⇒ `adjoint N = N` | Self-adjointness of a right inverse of a self-adjoint operator (left-inverse uniqueness). Single shared copy in `VarianceAlgebra.lean` (imported by `OverID.lean`; the former duplicate in `OverID.lean` is removed). |
| `adjoint_gmmBread` | `adjoint W = W` ⇒ `adjoint (gmmBread G W) = gmmBread G W` | (private) The bread is self-adjoint when `W` is. |
| `gmm_efficiency` | `(gmmSandwich G W Cov breadInv − effInv).IsPositive` | **GMM optimal-weighting theorem (Hansen 1982, Thm 3.2).** The sandwich variance dominates the efficient variance `effInv = (GᵀCov⁻¹G)⁻¹` in the Löwner order, with equality at `W = Cov⁻¹`. Proof: `V(W) − V★ = (A−B)Σ(A−B)ᵀ ⪰ 0` with `A = (GᵀWG)⁻¹GᵀW`, `B = (GᵀΣ⁻¹G)⁻¹GᵀΣ⁻¹`; the cross terms collapse to `V★` because `Σ`/`Σ⁻¹` cancel against the bread, and positivity is `IsPositive.conj_adjoint` on `Σ ⪰ 0`. **Proved, axiom-clean.** |

#### 12v.2. `Stat/GMM/Setup.lean` — GMM problem, influence function, efficiency

| Declaration | Statement | Description |
|---|---|---|
| `gmmScore G W g` | `fun θ x => adjoint G (W (g θ x))` | The **combined GMM score** `ψ(θ,x) = GᵀW g(θ,x)`; its empirical zero is the (linearized) GMM estimator (FOC of `ḡᵀWḡ`). |
| `gmmIF G W breadInv g θ₀` | `fun x => −(breadInv (adjoint G (W (g θ₀ x))))` | The **GMM influence function** `−(GᵀWG)⁻¹GᵀW g(θ₀,·)`. |
| `GMMProblem P` | structure | Moment model bundle: `g`, truth `θ₀`, weighting `W` (`hWsa : adjoint W = W`), Jacobian `G`, identification `∫ g(θ₀) dP = 0`, measurability/finite-variance of `g(θ₀)`, Fréchet-derivative spec `jac_spec`, covariance `Cov` with second-moment characterization `hCov`, and two-sided inverse witnesses for `Cov`, `GᵀWG`, `GᵀCov⁻¹G`. |
| `GMMProblem.score` / `influence` | `noncomputable def`s | The problem's combined score / influence function. |
| `GMMProblem.asympVar` | `gmmSandwich G W Cov breadInv` | The **sandwich asymptotic variance** of the problem. |
| `GMMProblem.effVar` | `effInv` | The **efficient variance** `(GᵀCov⁻¹G)⁻¹`. |
| `GMMProblem.cov_isPositive` | `prob.Cov.IsPositive` | The covariance is a positive operator — it is a second moment (`⟪Cov t,t⟫ = ∫ ⟪t,g⟫² ≥ 0`, symmetric). **Proved.** |
| `GMMProblem.efficiency` | `(prob.asympVar − prob.effVar).IsPositive` | **Hansen optimal-weighting theorem, statistical form**: the sandwich variance dominates the efficient variance in the Löwner order; the efficient GMM weighting is `W = Cov⁻¹`. Instantiates `gmm_efficiency`. **Proved, axiom-clean.** |

#### 12v.3. `Stat/GMM/AsymptoticNormality.lean` — GMM CLT via the combined-score Z-estimator

| Declaration | Statement | Description |
|---|---|---|
| `gmm_asymptotically_linear` | `IsAsymLinearVec θn θ₀ prob.influence S (range n)` | **GMM asymptotic linearity.** If `θn` solves the combined empirical moment `(1/n) Σ GᵀW g(Z_i;θn) = 0`, is consistent at rate `O_p(1/√n)`, and the combined score `prob.score` satisfies `ZEstimatorRegularity` (with `reg.J₀_inv = breadInv`), then `θn` is asymptotically linear with IF `−(GᵀWG)⁻¹GᵀW g(θ₀,·)`. Hence `√n(θn−θ₀) ⇒ N(0, asympVar)` via `IsAsymLinearVec.tendsto_normal_vec`. Reduces GMM to `zEstimator_clt` on `gmmScore`. **Proved, axiom-clean.** |

#### 12v.4. `Stat/GMM/OverID.lean` — over-identification (J / Hansen–Sargan) test

The whitened J-test: with the moment covariance normalised to the identity (the change of variables the `Σ̂⁻¹`-weighting performs), the residual maker `M = I − G(GᵀG)⁻¹Gᵀ` is an orthogonal projection of rank `m − k`, so the J-statistic limit `‖Mw‖²` is `χ²_{m−k}` via `stdGaussian_map_normSq_orthogonalProjection`. The general-covariance case is the change of variables `z ↦ Σ^{-1/2} z`. Inverses carried as data+witnesses (`heL`, `heR`).

**Deferred (known gap).** This file establishes only the *limit law* `‖Mw‖² ⇒ χ²_{m−k}` for a standard Gaussian `w` on the moment space. Wiring the **empirical** J-statistic `n·ḡₙ(θ̂)ᵀ Σ̂⁻¹ ḡₙ(θ̂)` to this limit — i.e. proving the empirical moments converge to `Mw` under the efficient weighting and applying continuous mapping — is left as future work (needs the moment-CLT contact point + the whitening change-of-variables at the estimator). The χ² target law itself is fully proved.

| Declaration | Statement | Description |
|---|---|---|
| `gmmHatMatrix G effInv` | `G ∘L effInv ∘L adjoint G` | The **hat matrix** `H = G(GᵀG)⁻¹Gᵀ`, orthogonal projection onto `range G`. |
| `gmmResidualMaker G effInv` | `id − gmmHatMatrix G effInv` | The **residual maker** `M = I − H`, orthogonal projection onto `(range G)^⊥`. |
| `gmmResidualMaker_isSelfAdjoint` / `_idempotent` | `IsSelfAdjoint M` / `M ∘L M = M` | `M` is an orthogonal projection (uses `effInv` self-adjoint + the inverse witnesses). **Proved.** |
| `gmmResidualMaker_finrank_range` | `finrank (range M) = finrank F − finrank E` | **Hansen–Sargan dof** `m − k`. Proof: `range M = ker H`, `range H = range G`, `G` injective (from `heL`); rank–nullity (`LinearMap.finrank_range_add_finrank_ker`) + `LinearMap.finrank_range_of_inj`. **Proved.** |
| `jStatistic_chiSq` | `(stdGaussian F).map (‖M·‖²) = chiSqDist (finrank F − finrank E)` | **Headline: the whitened GMM J-statistic limit law is `χ²_{m−k}`.** Applies the projected-Gaussian χ² lemma to `M`. **Proved, axiom-clean.** |

### 12w. `Stat/Minimax/` — Minimax lower bounds (Le Cam two-point)

The converse direction of the inference stack: machinery to certify that *no*
estimator can do better than a given rate (optimality / sharpness), as opposed
to the achievability results in §12c–§12v. The whole program reduces to one
chain: *risk lower bound ← reduction to testing ← testing error ≥ 1 − TV ← TV ≤ f(KL)*.
The first three links are elementary (this folder); the KL bridge (Pinsker)
lives in `Pinsker.lean`. Project-agnostic; candidates for upstream Mathlib.

Three reduction tiers share the same testing core: **Le Cam** (`LeCam.lean`, §12w.2)
packs two hypotheses; **Fano** (`Fano.lean`, §12w.7) packs many pairwise-separated
ones (rates from `log`-cardinality); **Assouad** (`Assouad.lean`, §12w.8) packs a
`d`-cube of coordinate-wise tests (rates that grow with dimension). Fano and Assouad
are both proven *unconditionally* from the elementary testing bound — no entropy or
product-measure machinery — so the divergence input enters only through `tvDist`.

#### 12w.1. `Stat/Minimax/TotalVariation.lean` — statistical TV distance

Mathlib only carries `SignedMeasure.totalVariation` (Jordan norm); this file develops the testing-oriented statistical TV between two measures.

| Declaration | Statement | Description |
|---|---|---|
| `tvDist μ ν` | `⨆ A measurable, \|μ.real A − ν.real A\|` | **Total variation distance**: sup over measurable sets of the probability gap. |
| `abs_measureReal_sub_le_one` | `\|μ.real A − ν.real A\| ≤ 1` | Each defining term is bounded by 1 (probability measures). **Proved.** |
| `bddAbove_tvDist_range` | `BddAbove (range …)` | The defining family is bounded above by 1. **Proved.** |
| `abs_measureReal_sub_le_tvDist` | `\|μ.real A − ν.real A\| ≤ tvDist μ ν` | Each measurable set's gap is `≤ tvDist` (via `le_ciSup`). **Proved.** |
| `measureReal_sub_le_tvDist` | `ν.real A − μ.real A ≤ tvDist μ ν` | Signed-gap form. **Proved.** |
| `tvDist_nonneg`, `tvDist_le_one` | `0 ≤ tvDist μ ν ≤ 1` | Range. **Proved.** |
| `tvDist_symm` | `tvDist μ ν = tvDist ν μ` | Symmetry. **Proved.** |
| `one_sub_tvDist_le_test` | `1 − tvDist μ ν ≤ μ.real A + ν.real Aᶜ` | **The testing bound**: the total error of any test (rejection region `A`) is at least `1 − tvDist`. The single inequality driving the two-point method. **Proved, axiom-clean.** |

#### 12w.2. `Stat/Minimax/LeCam.lean` — Le Cam two-point method

The reduction from estimation to testing. Parameters live in a `[PseudoMetricSpace Θ] [OpensMeasurableSpace Θ]`; estimators are arbitrary measurable `est : Ω → Θ`.

| Declaration | Statement | Description |
|---|---|---|
| `measurableSet_error` | `MeasurableSet {ω \| s ≤ dist (est ω) θ}` | The error region of a measurable estimator is measurable (closed-set preimage). **Proved.** |
| `one_sub_tvDist_le_error_sum` | `1 − tvDist P₀ P₁ ≤ P₀.real {s ≤ dist(est,θ₀)} + P₁.real {s ≤ dist(est,θ₁)}` | **Summed form.** Under `2s`-separation `2s ≤ dist θ₀ θ₁`, the two error probabilities of any estimator sum to at least `1 − tvDist`. Proof = testing bound + triangle inequality + monotonicity. **Proved.** |
| `half_one_sub_tvDist_le_max_error` | `(1 − tvDist P₀ P₁)/2 ≤ max (P₀.real {…}) (P₁.real {…})` | **Headline (max form).** Every estimator has worst-case error probability at least `½(1 − tvDist P₀ P₁)`: the canonical minimax lower bound. **Proved, axiom-clean.** |

#### 12w.3. `Stat/Minimax/Pinsker.lean` — KL bridge

Bridges `tvDist` to Mathlib's `InformationTheory.klDiv`, making the two-point bound usable for concrete families (KL is computable for Gaussians etc.). **Pinsker's inequality is now proven unconditionally** — both the scalar core (`klFun_lower_bound`) and the measure-theoretic half (Scheffé `≤` + weighted Cauchy–Schwarz). `PinskerBound` remains as a reusable `Prop` packaging but is no longer a substrate gate.

| Declaration | Statement | Description |
|---|---|---|
| `klFun_mul_lower_aux` | `0 ≤ (x+2)·klFun x − (3/2)(x−1)²` for `x ≥ 0` | Cleared-denominator Pinsker scalar bound. Proved by full two-level calculus on `φ` (`HasDerivAt` for φ, φ′; `φ″ = 2(log x + 1/x − 1) ≥ 0` via `log_le_sub_one`; monotonicity ⇒ `φ ≥ φ(1)=0`). **Proved, axiom-clean.** |
| `klFun_lower_bound` | `(3/2)(x−1)²/(x+2) ≤ klFun x` for `x ≥ 0` | Division-form Pinsker scalar engine. **Proved.** |
| `PinskerBound μ ν` | `Prop`: `tvDist μ ν ≤ √((klDiv μ ν).toReal/2)` | Reusable `Prop` packaging of the Pinsker bound for a pair. **Proven** by `pinskerBound_of_ac_of_ne_top`. |
| `pinskerBound_of_ac_of_ne_top` | `μ ≪ ν` + `klDiv μ ν ≠ ⊤` ⇒ `PinskerBound μ ν` | **Pinsker's inequality, proven unconditionally**: Scheffé `≤` (`tvDist_le_half_integral_abs_rnDeriv`) + weighted Cauchy–Schwarz (`√(p+2)` weight) + `klFun_lower_bound`. Both hyps are genuinely needed (`.toReal` collapses `klDiv = ⊤` to `0`). **Proved, axiom-clean.** |
| `pinskerBound_pi_iid` | one-sample `μ ≪ ν` + `Integrable (llr μ ν) μ` ⇒ `PinskerBound (μ^{⊗n}) (ν^{⊗n})` | i.i.d.-product Pinsker: product AC + `klDiv ≠ ⊤` discharged from marginals via `ProductKLLeCam.pi_iid_*`, then the unconditional inequality. **Proved.** |
| `klForm_two_point_lower_bound_of_pinsker` | from `PinskerBound P₀ P₁` + 2s-separation: worst-case error `≥ (1 − √(KL/2))/2` | KL-form minimax lower bound from a supplied `PinskerBound` term; composes with `half_one_sub_tvDist_le_max_error`. **Proved from hypothesis.** |
| `klForm_two_point_lower_bound` | `P₀ ≪ P₁` + `klDiv P₀ P₁ ≠ ⊤` + 2s-separation ⇒ worst-case error `≥ (1 − √(KL/2))/2` | **Unconditional** KL-form minimax lower bound: derives the Pinsker bridge via `pinskerBound_of_ac_of_ne_top` (no `PinskerBound` hypothesis). **Proved.** |

#### 12w.4. `Stat/Minimax/Scheffe.lean` — variational TV identity (≤ direction)

The Scheffé `≤` bound — the analytic workhorse for both the χ² route (`TV ≤ ½√χ²`) and discharging `PinskerBound`. Proved via the symmetric splitting `∫_A f = −∫_{Aᶜ} f` (sidesteps posPart/negPart machinery). The reverse `≥` (full equality) was deliberately skipped — the `≤` suffices downstream.

| Declaration | Statement | Description |
|---|---|---|
| `abs_setIntegral_le_half_integral_abs_of_integral_eq_zero` | for integrable `f`, `∫ f ∂ν = 0`, measurable `A`: `\|∫ x in A, f ∂ν\| ≤ ½∫\|f\| ∂ν` | Generic, probability-free; clean Mathlib-contribution candidate. **Proved.** |
| `measureReal_sub_eq_setIntegral_rnDeriv_sub_one` | under `μ ≪ ν`: `μ.real A − ν.real A = ∫ x in A, ((μ.rnDeriv ν x).toReal − 1) ∂ν` | Signed-gap as a density integral. **Proved.** |
| `tvDist_le_half_integral_abs_rnDeriv` | `tvDist μ ν ≤ ½∫\|(μ.rnDeriv ν x).toReal − 1\| ∂ν` (prob measures, `μ ≪ ν`) | **Scheffé `≤`.** **Proved, axiom-clean.** |

#### 12w.5. `Stat/Minimax/ChiSquared.lean` — χ² divergence link

`chiSqDiv` and the χ² control of total variation — the divergence that makes two-point indistinguishability computable (and tensorizes over iid samples). Named `chiSqDiv` to avoid colliding with the χ² *distribution* `chiSqDist` (§12n'''d).

| Declaration | Statement | Description |
|---|---|---|
| `chiSqDiv μ ν` | `∫ ((μ.rnDeriv ν x).toReal − 1)² ∂ν` | χ²-divergence of μ relative to ν. |
| `chiSqDiv_nonneg` | `0 ≤ chiSqDiv μ ν` | Integrand is a square. **Proved.** |
| `tvDist_le_half_sqrt_chiSqDiv` | `tvDist μ ν ≤ ½·√(chiSqDiv μ ν)` (prob, `μ≪ν`, sq-deviation integrable) | **Cauchy–Schwarz on Scheffé** (Hölder `2,2` with `g≡1`, `∫1²∂ν=1`). **Proved, axiom-clean.** |
| `chiSqDiv_eq` | `chiSqDiv μ ν = (∫ (μ.rnDeriv ν x).toReal² ∂ν) − 1` (`μ≪ν`) | Expanded form via `∫ dμ/dν ∂ν = 1`. **Proved.** |

| `rnDeriv_prod_eq` | `(μ₁.prod μ₂).rnDeriv (ν₁.prod ν₂) =ᵐ fun z ↦ rnDeriv μ₁ ν₁ z.1 · rnDeriv μ₂ ν₂ z.2` | Product-measure RN-derivative factorization (assembled from `withDensity_rnDeriv_eq` + `prod_withDensity₀` + `rnDeriv_withDensity₀`; Mathlib has no packaged version). Reusable. **Proved.** |
| `chiSqDiv_prod` | `1 + chiSqDiv (μ₁.prod μ₂) (ν₁.prod ν₂) = (1+chiSqDiv μ₁ ν₁)(1+chiSqDiv μ₂ ν₂)` | **χ² tensorization** (binary). Via `rnDeriv_prod_eq` + `chiSqDiv_eq` + Fubini (`integral_prod_mul`). **Proved, axiom-clean.** |
| `chiSqDiv_map_measurableEquiv` | `chiSqDiv (μ.map e) (ν.map e) = chiSqDiv μ ν` for `e : Ω ≃ᵐ Ω'` (σ-finite) | **χ² is invariant under a measurable equivalence** (relabelling the sample space). Via `integral_map_equiv` + `MeasurableEmbedding.rnDeriv_map`. Reusable. **Proved, axiom-clean.** |
| `chiSqDiv_self` | `chiSqDiv ρ ρ = 0` (σ-finite) | Self-divergence vanishes (`rnDeriv_self =ᵐ 1`); the `n=0` base case. **Proved, axiom-clean.** |
| `pi_iid_absolutelyContinuous` | `μ ≪ ν ⇒ Measure.pi (fun _:Fin n ↦ μ) ≪ Measure.pi (fun _:Fin n ↦ ν)` | i.i.d. product a.c. (Mathlib has no `pi`-level a.c. lemma); induction via `piFinSuccAbove` + `AbsolutelyContinuous.prod`/`.map`. **Proved, axiom-clean.** |
| `one_add_chiSqDiv_pi_iid` | `1 + chiSqDiv (Measure.pi (fun _:Fin n ↦ μ)) (Measure.pi (fun _:Fin n ↦ ν)) = (1 + chiSqDiv μ ν)ⁿ` (finite `Ω`, prob, `μ≪ν`) | **n-fold i.i.d. χ² tensorization** — the iid scaling consumed by `n`-sample two-point/Le Cam bounds. Induction on `Fin n` via `measurePreserving_piFinSuccAbove` + `chiSqDiv_map_measurableEquiv` + binary `chiSqDiv_prod`; integrability discharged for free by `Integrable.of_finite` (restricted to `[Fintype Ω] [MeasurableSingletonClass Ω]`). **Proved, axiom-clean.** |
| `pi_iid_integrable_sq_dev` | `Integrable ((d(μ^⊗n)/d(ν^⊗n) − 1)²) (ν^⊗n)` from the one-sample integrability `hint` (general `[MeasurableSpace S]`, probs, `μ≪ν`) | **Integrability propagation** to the i.i.d. product: the squared density deviation stays integrable under `n`-fold products (L²(ν)-tensorization of the density). Supplies the side-condition that `one_add_chiSqDiv_pi_iid` got for free on a finite space. **Proved, axiom-clean.** |
| `one_add_chiSqDiv_pi_iid_general` | `1 + chiSqDiv (Measure.pi (fun _:Fin n ↦ μ)) (Measure.pi (fun _:Fin n ↦ ν)) = (1 + chiSqDiv μ ν)ⁿ` (general `[MeasurableSpace S]`, probs, `μ≪ν`, sq-deviation integrable) | **n-fold i.i.d. χ² tensorization on a GENERAL (possibly continuous) measurable space** — the continuum analogue of `one_add_chiSqDiv_pi_iid` (whose `[Fintype]` only discharged integrability for free). Same `chiSqDiv_prod` induction, with integrability supplied by `hint` + `pi_iid_integrable_sq_dev`. Needed by minimax two-point lower bounds whose sample space is a continuum (e.g. a covariate space). **Proved, axiom-clean.** |
| `chiSqDiv_prod_ancillary` | `chiSqDiv (μ.prod ρ) (ν.prod ρ) = chiSqDiv μ ν` (probs, `μ≪ν`, `hint` for `μ,ν`) | **Ancillary factor invariance**: a common product factor `ρ` cancels (`1 + χ²(ρ‖ρ) = 1`). The formal "an ancillary observation carries no information"; the bridge from finite-cell to continuous-`X` (§13sa.6). **Proved, axiom-clean.** |

#### 12w.5a. `Stat/Minimax/ChiSquaredKernel.lean` — χ² disintegration for common-design marked laws

Common-design marked laws retain their design coordinate and draw a mark from a kernel. This
module identifies their χ² divergence as the design-average of the fibre divergences.

<!-- GEN:Causalean.Stat.Minimax.ChiSquaredKernel -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.attachKernel` | `MeasureTheory.Measure D → ProbabilityTheory.Kernel D M → MeasureTheory.Measure (D × M)` | Given a measurable design space, a measurable mark space, a measure on the design space, and a probability kernel from design points to mark laws, the attached marked-observation law draws a design point from the measure and then a mark from the corresponding kernel, retaining both coordinates. |
| `Stat.one_add_chiSqDiv_attachKernel` | `(∀ (x : D), (kappa x).AbsolutelyContinuous (eta x)) → MeasureTheory.Integrable (fun p => ((kappa.rnDeriv eta p.1 p.2).toReal - 1) ^ 2) (Causalean.Stat.attachKernel m eta) → 1 + Causalean.Stat.chiSqDiv (Causalean.Stat.attachKernel m kappa) (Causalean.Stat.attachKernel m eta) = ∫ (x : D), 1 + Causalean.Stat.chiSqDiv (kappa x) (eta x) ∂m` | For a probability design law, two measurable probability marking kernels, pointwise absolute continuity of the first mark law with respect to the second, and integrability of their jointly measurable squared density deviation, one plus the χ²-divergence of the resulting marked laws equals the design-average of one plus the conditional χ²-divergences. |
<!-- /GEN -->

#### 12w.5aa. `Stat/Minimax/ChiSquaredTwoPoint.lean` — centered Bernoulli χ² formula

The explicit χ² calculation for the centered Bernoulli law and its signed two-point image,
ready to combine with common-design disintegration and i.i.d. tensorization.

<!-- GEN:Causalean.Stat.Minimax.ChiSquaredTwoPoint -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.chiSqDiv_bernoulliLaw_centerHalf` | `\|u\| < 1 / 2 → Causalean.Stat.chiSqDiv (Causalean.Mathlib.Probability.bernoulliLaw (1 / 2 + u)) (Causalean.Mathlib.Probability.bernoulliLaw (1 / 2)) = 4 * u ^ 2` | For a perturbation whose magnitude is strictly below one half, the χ²-divergence of the Bernoulli law with success probability one half plus that perturbation, relative to the centered Bernoulli law, is four times the squared perturbation. |
| `Stat.chiSqDiv_twoPointMean_centerHalf` | `\|u\| < 1 / 2 → Causalean.Stat.chiSqDiv (Causalean.Mathlib.Probability.twoPointMean (1 / 2) u) (Causalean.Mathlib.Probability.twoPointMean (1 / 2) 0) = 4 * u ^ 2` | For a mean perturbation whose magnitude is strictly below one half, the χ²-divergence of the symmetric two-point law with that mean, relative to the centered symmetric two-point law, is four times the squared perturbation. |
<!-- /GEN -->

#### 12w.5b. `Stat/Minimax/ChiSquaredFinite.lean` — finite / product specializations of χ²

| Declaration | Statement | Description |
|---|---|---|
| `rnDeriv_mul_measure_singleton` | `ν {x} · (dμ/dν)(x) = μ {x}` (`μ≪ν`, measurable singletons) | **Discrete Radon–Nikodym bridge** (a genuine Mathlib gap): pointwise density inversion on atoms, via `setLIntegral_rnDeriv` + `restrict_singleton`/`lintegral_dirac'`. **Proved, axiom-clean.** |
| `finite_one_add_chiSqDiv` | `1 + chiSqDiv μ ν = ∑ x, (μ.real {x})² / (ν.real {x})` (finite `Ω`, probs, `μ≪ν`) | **Finite χ² formula**: turns χ² into a finite sum of squared point-mass ratios (`chiSqDiv_eq` + `integral_fintype` + the bridge). The enabling step for the Ingster bound. **Proved, axiom-clean.** |
| `pi_real_singleton` | `(Measure.pi μ).real {ω} = ∏ i, (μ i).real {ω i}` (finite index, probs) | **Product point mass** (real-valued): `{ω} = univ.pi {ωᵢ}` + `Measure.pi_pi` + `ENNReal.toReal_prod`. **Proved, axiom-clean.** |

#### 12w.6. `Stat/Minimax/MinimaxRisk.lean` — real-functional / n-sample packaging

Causal-agnostic specialization of Le Cam to a real-valued functional estimated from `n` i.i.d. samples — the shape consumed by concrete minimax lower bounds (the causal structure-agnostic ATE theorem lives in `Estimation/`, not here).

The file also carries the **Bayes-risk step** (`integral_le_sSup_range_of_isProbabilityMeasure`): averaging a risk function against any prior probability measure is `≤` its worst case, which is how a minimax lower bound is certified by exhibiting one prior and bounding *its* average risk from below. Use it when the hard instance is a continuum prior rather than a finite hypothesis set (Le Cam/Fano); see its docstring for the statement.

| Declaration | Statement | Description |
|---|---|---|
| `real_two_point_lower_bound` | `2s ≤ \|θ₀−θ₁\|` ⇒ `(1−tvDist P₀ P₁)/2 ≤ max (P₀.real{s≤\|est−θ₀\|}) (P₁.real{s≤\|est−θ₁\|})` | Le Cam at `Θ=ℝ` (`dist a b = \|a−b\|`). **Proved.** |
| `two_point_lower_bound_of_tvDist_le` | same with explicit `tvDist P₀ P₁ ≤ c` ⇒ bound `(1−c)/2` | Convenience for plugging a TV upper bound. **Proved.** |
| `iid_two_point_lower_bound` | for laws `P₀,P₁` on `S`, functional `τ`, `n` samples (`Measure.pi (fun _:Fin n ↦ Pⱼ)`), `2s ≤ \|τ P₀ − τ P₁\|` ⇒ worst-case miss prob `≥ ½(1 − tvDist)` between the `n`-fold laws | **The n-sample minimax form.** **Proved.** |
| `two_point_lower_bound_of_chiSqDiv_le` | `chiSqDiv P₀ P₁ ≤ c` (+ `≪`, integrability) ⇒ worst-case miss prob `≥ (1 − ½√c)/2` | **χ²-form bound** (via `tvDist ≤ ½√χ²`); the directly-certifiable form since χ² is computable and tensorizes. **Proved, axiom-clean.** |

#### 12w.6a. `Stat/Minimax/FiniteKernelBayes.lean` — finite-state Bayes-to-minimax bridge

A continuous prior can be pushed through a Markov kernel into a finite decision-state
space without leaving Causalean's real-valued minimax API. This module supplies the
average-to-worst-case and Bayes-to-minimax inequalities, with deterministic-map and
`FiniteDesign` views of the same finite distribution.

<!-- GEN:Causalean.Stat.Minimax.FiniteKernelBayes -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.finite_range_bddAbove` | `BddAbove (Set.range f)` | A real-valued function on a finite state space has a bounded range. |
| `Stat.finite_range_bddBelow` | `BddBelow (Set.range f)` | A real-valued function on a finite state space has a range bounded below. |
| `Stat.finite_integrable` | `MeasureTheory.Integrable f μ` | A real-valued statistic on a finite state space is integrable under every finite measure. |
| `Stat.loss_integrable_kernel` | `MeasureTheory.Integrable (loss a) (K θ)` | The loss from one action is integrable under every probability distribution emitted by a Markov kernel into a finite state space. |
| `Stat.kernelAverageLoss` | `ProbabilityTheory.Kernel Θ S → (A → S → ℝ) → A → Θ → ℝ` | Given a measurable parameter space, a finite measurable state space, an action space, a probability kernel from parameters to states, a real-valued loss for each action and state, an action, and a parameter value, the kernel-average loss is the expected loss of that action under the state distribution selected by the kernel at that parameter value. |
| `Stat.stronglyMeasurable_kernelAverageLoss` | `MeasureTheory.StronglyMeasurable (Causalean.Stat.kernelAverageLoss K loss a)` | The kernel-averaged loss of a finite-state action is measurable as a function of the parameter. |
| `Stat.integrable_kernelAverageLoss` | `MeasureTheory.Integrable (Causalean.Stat.kernelAverageLoss K loss a) π` | Under a probability prior, the kernel-averaged loss of a finite-state action is integrable. |
| `Stat.kernelAverageLoss_eq_sum` | `Causalean.Stat.kernelAverageLoss K loss a θ = ∑ s, (K θ).real {s} * loss a s` | Kernel averaging on a finite state space equals the sum of statewise losses weighted by the kernel's singleton probabilities. |
| `Stat.measurable_kernel_singletonReal` | `Measurable fun θ => (K θ).real {s}` | A finite-state Markov kernel's probability of a fixed state is measurable in its source parameter. |
| `Stat.integrable_kernel_singletonReal` | `MeasureTheory.Integrable (fun θ => (K θ).real {s}) π` | A finite-state Markov kernel's probability of a fixed state is integrable under every probability prior. |
| `Stat.mixedKernelLoss` | `MeasureTheory.Measure Θ → ProbabilityTheory.Kernel Θ S → (A → S → ℝ) → A → ℝ` | Given a measurable parameter space, a finite measurable state space, an action space, a prior measure on the parameter space, a probability kernel from parameters to states, a real-valued loss for each action and state, and an action, the mixed kernel loss is that action's kernel-average loss integrated with respect to the prior. |
| `Stat.mixedKernelLoss_eq_sum_integral` | `Causalean.Stat.mixedKernelLoss π K loss a = ∑ s, (∫ (θ : Θ), (K θ).real {s} ∂π) * loss a s` | A continuous-prior mixture through a finite-state kernel equals the sum of statewise losses weighted by the prior-averaged singleton probabilities. |
| `Stat.mixedKernelLoss_nonneg` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → ∀ (a : A), 0 ≤ Causalean.Stat.mixedKernelLoss π K loss a` | If statewise loss is nonnegative, then its prior-and-kernel mixture is nonnegative. |
| `Stat.mixedKernelLoss_le_worstCaseRisk` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → ∀ (a : A), ∫ (θ : Θ), ∫ (s : S), loss a s ∂K θ ∂π ≤ Causalean.Stat.worstCaseRisk loss a` | If loss is nonnegative in every state, then the continuous-prior average of each action's finite-state kernel loss is no greater than that action's finite worst-case risk. |
| `Stat.realBayesRisk` | `MeasureTheory.Measure Θ → ProbabilityTheory.Kernel Θ S → (A → S → ℝ) → ℝ` | Given a measurable parameter space, a finite measurable state space, an action space, a prior measure on the parameter space, a probability kernel from parameters to states, and a real-valued loss for each action and state, the real Bayes risk is the infimum, over all actions, of their mixed kernel losses. |
| `Stat.realBayesRisk_nonneg` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → 0 ≤ Causalean.Stat.realBayesRisk π K loss` | If statewise loss is nonnegative, then the real Bayes risk is nonnegative. |
| `Stat.realBayesRisk_le_minimaxValue` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → Causalean.Stat.realBayesRisk π K loss ≤ Causalean.Stat.minimaxValue loss` | With at least one available action and nonnegative statewise loss, the real Bayes risk under a continuous prior and finite-state Markov kernel is at most the finite-state minimax value. |
| `Stat.integrable_loss_comp` | `Measurable f → ∀ (loss : A → S → ℝ) (a : A), MeasureTheory.Integrable (fun θ => loss a (f θ)) π` | A finite-state loss composed with a measurable deterministic state map is integrable under every probability prior. |
| `Stat.mixedKernelLoss_deterministic` | `Causalean.Stat.mixedKernelLoss π (ProbabilityTheory.Kernel.deterministic f hf) loss a = ∫ (θ : Θ), loss a (f θ) ∂π` | A measurable deterministic state map has the same mixed kernel loss as the ordinary prior integral of its composed loss. |
| `Stat.integral_loss_comp_le_worstCaseRisk` | `Measurable f → ∀ (loss : A → S → ℝ), (∀ (a : A) (s : S), 0 ≤ loss a s) → ∀ (a : A), ∫ (θ : Θ), loss a (f θ) ∂π ≤ Causalean.Stat.worstCaseRisk loss a` | If the state map is measurable and the loss is nonnegative in every state, then the prior average of the composed loss is no greater than its finite worst-case risk. |
| `Stat.deterministicBayesRisk` | `MeasureTheory.Measure Θ → (Θ → S) → (A → S → ℝ) → ℝ` | Given a measurable parameter space, a finite measurable state space, an action space, a prior measure on the parameter space, a map assigning each parameter a state, and a real-valued loss for each action and state, the deterministic Bayes risk is the infimum, over all actions, of the prior-integrated loss evaluated at the state assigned to each parameter. |
| `Stat.deterministicBayesRisk_eq_realBayesRisk` | `Causalean.Stat.deterministicBayesRisk π f loss = Causalean.Stat.realBayesRisk π (ProbabilityTheory.Kernel.deterministic f hf) loss` | A measurable deterministic state map gives the same Bayes risk whether viewed directly or as its deterministic Markov kernel. |
| `Stat.deterministicBayesRisk_le_minimaxValue` | `Measurable f → ∀ (loss : A → S → ℝ), (∀ (a : A) (s : S), 0 ≤ loss a s) → Causalean.Stat.deterministicBayesRisk π f loss ≤ Causalean.Stat.minimaxValue loss` | With at least one available action, a measurable deterministic state map, and nonnegative statewise loss, the deterministic Bayes risk is at most the finite-state minimax value. |
| `Stat.finiteDesign_integrable` | `MeasureTheory.Integrable g D.toMeasure` | A real-valued statistic on a finite design space is integrable under the design's induced probability measure. |
| `Stat.finiteDesign_expectedLoss_eq_sum_eq_integral` | `D.E (loss a) = ∑ s, D.p s * loss a s ∧ D.E (loss a) = ∫ (s : S), loss a s ∂D.toMeasure` | A finite design's expected action loss equals both its finite weighted sum and its induced probability-measure integral. |
| `Stat.finiteDesign_expectedLoss_le_worstCaseRisk` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → ∀ (a : A), D.E (loss a) ≤ Causalean.Stat.worstCaseRisk loss a` | If the statewise loss is nonnegative, then a finite design's expected loss for each action is no greater than that action's finite worst-case risk. |
| `Stat.finiteDesignBayesRisk` | `Causalean.Experimentation.DesignBased.FiniteDesign S → (A → S → ℝ) → ℝ` | Given a finite state space, an action space, a finite randomization design on the state space, and a real-valued loss for each action and state, the finite-design Bayes risk is the infimum, over all actions, of their expected losses under that design. |
| `Stat.finiteDesignBayesRisk_le_minimaxValue` | `(∀ (a : A) (s : S), 0 ≤ loss a s) → Causalean.Stat.finiteDesignBayesRisk D loss ≤ Causalean.Stat.minimaxValue loss` | With at least one available action and nonnegative statewise loss, a finite design's Bayes risk is at most the finite-state minimax value. |
| `Stat.inducedFiniteDesign` | `(π : MeasureTheory.Measure Θ) → [MeasureTheory.IsProbabilityMeasure π] → (K : ProbabilityTheory.Kernel Θ S) → [ProbabilityTheory.IsMarkovKernel K] → Causalean.Experimentation.DesignBased.FiniteDesign S` | Given a measurable parameter space, a nonempty finite measurable state space with measurable singletons, a probability prior on the parameter space, and a Markov kernel from parameters to states, the induced finite design is the design whose probability assigned to each state is the prior average of the kernel's probability of that state, these probabilities are nonnegative, and their sum over all states is one. |
| `Stat.inducedFiniteDesign_p` | `(Causalean.Stat.inducedFiniteDesign π K).p s = ∫ (θ : Θ), (K θ).real {s} ∂π` | The induced finite design's mass at each state is the prior integral of the kernel's singleton probability. |
| `Stat.inducedFiniteDesign_expectedLoss_eq_mixedKernelLoss` | `(Causalean.Stat.inducedFiniteDesign π K).E (loss a) = Causalean.Stat.mixedKernelLoss π K loss a` | Expected loss under the induced finite design equals the continuous-prior, kernel-averaged loss integral. |
| `Stat.realBayesRisk_eq_inducedFiniteDesignBayesRisk` | `Causalean.Stat.realBayesRisk π K loss = Causalean.Stat.finiteDesignBayesRisk (Causalean.Stat.inducedFiniteDesign π K) loss` | A continuous prior mixed through a finite-state Markov kernel has the same real Bayes risk as its induced finite design. |
<!-- /GEN -->

#### 12w.6a.i. `Stat/FiniteRaoBlackwell/` — guarded finite Rao–Blackwell reductions

Uniform finite allocation experiments often create statistic fibers that have zero probability
at some latent states. This subtree supplies a guarded finite conditional law, exact
disintegration, finite-prior posteriors, a factorization criterion for a shared conditional law,
and squared-loss Rao–Blackwell risk reductions. `KernelBridge` exposes the same constructions as
Markov kernels and connects their real-valued finite sums to `FiniteKernelBayes`.

`DesignPushforward` works directly with any finite randomization design, conditioning source values along a deterministic coarsening without passing through the uniform-experiment wrapper. Its caller-supplied default affects only null fibers, while disintegration, range preservation, and squared-loss contraction remain exact.

##### `Stat/FiniteRaoBlackwell/DesignPushforward.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.DesignPushforward -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.fiberMass` | `Causalean.Experimentation.DesignBased.FiniteDesign Ω → (Ω → B) → B → ℝ` | For a finite probability design on source points, a deterministic coarsening map, and a coarsened value, the fiber mass is the total design probability of source points mapped to that value. |
| `Stat.FiniteRaoBlackwell.fiberMass_eq_map_p` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b = (D.map φ).p b` | A coarsening fiber's total design mass equals the probability of its value under the pushforward design. |
| `Stat.FiniteRaoBlackwell.fiberMass_nonneg` | `0 ≤ Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b` | Every coarsening fiber has nonnegative design mass. |
| `Stat.FiniteRaoBlackwell.fiberMass_sum` | `∑ b, Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b = 1` | The masses of all coarsening fibers sum to one. |
| `Stat.FiniteRaoBlackwell.fiberNumerator` | `Causalean.Experimentation.DesignBased.FiniteDesign Ω → (Ω → B) → (Ω → V → ℝ) → B → V → ℝ` | For a finite probability design on source points, a deterministic coarsening map, a real-valued source function indexed by values, a coarsened value, and an index value, the fiber numerator is the sum of the design probability times the source-function value over source points mapped to that coarsened value. |
| `Stat.FiniteRaoBlackwell.conditionalFiberWeight` | `Causalean.Experimentation.DesignBased.FiniteDesign Ω → (Ω → B) → B → Ω → ℝ` | For a finite probability design on source points, a deterministic coarsening map, a coarsened value, and a source point, the conditional fiber weight is its design probability divided by the fiber mass when that mass is nonzero and the point maps to the value, and zero otherwise. |
| `Stat.FiniteRaoBlackwell.conditionalFiberWeight_nonneg` | `0 ≤ Causalean.Stat.FiniteRaoBlackwell.conditionalFiberWeight D φ b ω` | Conditional fiber weights are nonnegative on both positive-mass and null fibers. |
| `Stat.FiniteRaoBlackwell.sum_conditionalFiberWeight_eq_one` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b ≠ 0 → ∑ ω, Causalean.Stat.FiniteRaoBlackwell.conditionalFiberWeight D φ b ω = 1` | When the requested fiber has nonzero mass, its conditional weights sum to one. |
| `Stat.FiniteRaoBlackwell.p_eq_zero_of_mem_fiberMass_eq_zero` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b = 0 → ∀ (ω : Ω), φ ω = b → D.p ω = 0` | If a fiber has zero design mass and a source point maps into that fiber, the source point itself has zero design probability. |
| `Stat.FiniteRaoBlackwell.fiberNumerator_eq_zero_of_fiberMass_eq_zero` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b = 0 → Causalean.Stat.FiniteRaoBlackwell.fiberNumerator D φ h b v = 0` | If a fiber has zero design mass, the design-weighted numerator of every real-valued source function vanishes on that fiber. |
| `Stat.FiniteRaoBlackwell.conditionalMeanAlongMap` | `Causalean.Experimentation.DesignBased.FiniteDesign Ω → (Ω → B) → ℝ → (Ω → V → ℝ) → B → V → ℝ` | For a finite probability design on source points, a deterministic coarsening map, a real-valued default, a real-valued source function indexed by values, a coarsened value, and an index value, the conditional mean along the map is the design-weighted fiber mean when the fiber has nonzero mass and the supplied default when it has zero mass. |
| `Stat.FiniteRaoBlackwell.conditionalMeanAlongMap_of_fiberMass_eq_zero` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b = 0 → Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b v = d` | On a zero-mass fiber, the guarded conditional mean equals its supplied default. |
| `Stat.FiniteRaoBlackwell.conditionalMeanAlongMap_eq_sum_conditionalFiberWeight` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b ≠ 0 → Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b v = ∑ ω, Causalean.Stat.FiniteRaoBlackwell.conditionalFiberWeight D φ b ω * h ω v` | On a nonzero-mass fiber, the guarded conditional mean equals the source-value average under the normalized conditional fiber weights. |
| `Stat.FiniteRaoBlackwell.sum_fiberMass_mul_conditionalMeanAlongMap` | `∑ b, Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b * Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b (v b) = ∑ ω, D.p ω * h ω (v (φ ω))` | The original design-weighted sum equals the sum of each fiber mass times its guarded conditional mean, with null fibers contributing zero regardless of the default. |
| `Stat.FiniteRaoBlackwell.E_map_conditionalMeanAlongMap` | `((D.map φ).E fun b => Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b (v b)) = D.E fun ω => h ω (v (φ ω))` | The pushforward expectation of the guarded conditional mean equals the original-design expectation of the source value evaluated at the covariate selected by its coarsening. |
| `Stat.FiniteRaoBlackwell.E_conditionalMeanAlongMap_comp` | `(D.E fun ω => Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h (φ ω) (v (φ ω))) = D.E fun ω => h ω (v (φ ω))` | Composing the guarded conditional mean with the coarsening has the same expectation under the original design as the corresponding source value. |
| `Stat.FiniteRaoBlackwell.conditionalMeanAlongMap_mem_Icc` | `d ∈ Set.Icc l u → (∀ (ω : Ω) (v : V), h ω v ∈ Set.Icc l u) → Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b v ∈ Set.Icc l u` | If the default lies in a fixed closed interval and every source value lies in that interval, the guarded conditional mean also lies in the interval. |
| `Stat.FiniteRaoBlackwell.conditionalMeanAlongMap_sq_le` | `Causalean.Stat.FiniteRaoBlackwell.fiberMass D φ b ≠ 0 → (Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b v - t) ^ 2 ≤ ∑ ω, Causalean.Stat.FiniteRaoBlackwell.conditionalFiberWeight D φ b ω * (h ω v - t) ^ 2` | On a nonzero-mass fiber, the squared loss of the conditional mean is at most the conditional fiber average of the source squared losses. |
| `Stat.FiniteRaoBlackwell.E_map_conditionalMeanAlongMap_sq_le` | `((D.map φ).E fun b => (Causalean.Stat.FiniteRaoBlackwell.conditionalMeanAlongMap D φ d h b (v b) - t) ^ 2) ≤ D.E fun ω => (h ω (v (φ ω)) - t) ^ 2` | Conditioning over the fibers of any deterministic pushforward weakly contracts covariate-parametric squared loss under an arbitrary finite design. |
<!-- /GEN -->

##### `Stat/FiniteRaoBlackwell/Core.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.Core -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment` | `(Latent : Type u_5) → (Allocation : Type u_6) → (Observation : Type u_7) → (Statistic : Type u_8) → [Fintype Latent] → [Fintype Allocation] → [Fintype Observation] → [Fintype Statistic] → Type (max (max (max u_5 u_6) u_7) u_8)` | A finite uniform-allocation experiment consists of a nonempty admissible allocation set, a normalized nonnegative observation mass for every state and allocation, a finite statistic, and a fallback observation used only to totalize conditioning on null fibers. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fallbackAllocation` | `Allocation` | For a finite uniform-allocation experiment, the fallback allocation is a chosen member of its nonempty set of admissible allocations. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fallbackSample` | `Allocation × Observation` | For a finite uniform-allocation experiment, the fallback full-data point pairs its fallback allocation with its fallback observation. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.sampleStatistic` | `Allocation × Observation → Statistic` | For a finite uniform-allocation experiment and a full-data point, the sample statistic is the statistic computed from that point's allocation and observation. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.uniformAllocationMass` | `Allocation → ℝ` | For a finite uniform-allocation experiment and an allocation, the uniform allocation mass is the reciprocal of the number of admissible allocations when that allocation is admissible, and zero otherwise. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.jointMass` | `Latent → Allocation × Observation → ℝ` | For a finite uniform-allocation experiment, a latent state, and a full-data point, the joint mass is the uniform allocation mass of its allocation multiplied by the conditional observation mass of its observation at that state and allocation. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.uniformAllocationMass_nonneg` | `0 ≤ E.uniformAllocationMass a` | The uniform allocation mass is nonnegative at every allocation. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.jointMass_nonneg` | `0 ≤ E.jointMass θ z` | The full-data joint mass is nonnegative at every state and sample point. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.jointMass_sum` | `∑ z, E.jointMass θ z = 1` | At every latent state, the full-data joint mass sums to one. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticMass` | `Latent → Statistic → ℝ` | For a finite uniform-allocation experiment, a latent state, and a statistic value, the statistic mass is the sum of joint masses of all full-data points whose statistic equals that value. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticMass_nonneg` | `0 ≤ E.statisticMass θ s` | Every statistic mass is nonnegative. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticMass_sum` | `∑ s, E.statisticMass θ s = 1` | At every latent state, the statistic masses sum to one. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalWeight` | `Latent → Statistic → Allocation × Observation → ℝ` | For a finite uniform-allocation experiment, a latent state, a statistic value, and a full-data point, the guarded conditional weight is the joint mass divided by the statistic mass when that mass is positive and the point has the requested statistic, is zero for other points, and is instead a point mass at the fallback sample when the statistic mass is zero. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalWeight_of_pos` | `0 < E.statisticMass θ s → ∀ (z : Allocation × Observation), E.conditionalWeight θ s z = if E.sampleStatistic z = s then E.jointMass θ z / E.statisticMass θ s else 0` | On a positive fiber, the guarded conditional weight is the usual fiber-restricted Bayes ratio. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalWeight_of_eq_zero` | `E.statisticMass θ s = 0 → ∀ (z : Allocation × Observation), E.conditionalWeight θ s z = if z = E.fallbackSample then 1 else 0` | On a zero-mass fiber, the guarded conditional weight is the fallback point mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalWeight_nonneg` | `0 ≤ E.conditionalWeight θ s z` | Every guarded conditional weight is nonnegative, including on null fibers. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalWeight_sum` | `∑ z, E.conditionalWeight θ s z = 1` | The guarded conditional weights sum to one on every fiber, with positive fibers normalized by Bayes' formula and null fibers normalized by the fallback point mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalDesign` | `Latent → Statistic → Causalean.Experimentation.DesignBased.FiniteDesign (Allocation × Observation)` | For a finite uniform-allocation experiment, a latent state, and a statistic value, the guarded conditional full-data design is the finite probability design whose probabilities are the guarded conditional weights. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticDesign` | `Latent → Causalean.Experimentation.DesignBased.FiniteDesign Statistic` | For a finite uniform-allocation experiment and a latent state, the statistic design is the finite probability design whose probabilities are the statistic masses. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticMass_mul_conditionalWeight` | `E.statisticMass θ s * E.conditionalWeight θ s z = if E.sampleStatistic z = s then E.jointMass θ z else 0` | Multiplying a statistic marginal by its guarded conditional weight recovers the joint mass on that fiber and zero away from it, including when the marginal is zero. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.jointMass_eq_statisticMass_mul_conditionalWeight` | `E.jointMass θ z = E.statisticMass θ (E.sampleStatistic z) * E.conditionalWeight θ (E.sampleStatistic z) z` | Every full-data atom factors into its statistic marginal and guarded conditional weight. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.disintegrate_sum` | `∑ z, E.jointMass θ z * f z = ∑ s, E.statisticMass θ s * ∑ z, E.conditionalWeight θ s z * f z` | Every real test function has the same joint expectation as its statistic-marginal expectation of the guarded conditional expectation; null fibers contribute exactly zero. |
<!-- /GEN -->

##### `Stat/FiniteRaoBlackwell/Posterior.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.Posterior -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorJointStatisticMass` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → Latent → Statistic → ℝ` | For a finite uniform-allocation experiment, a finite prior design on latent states, a latent state, and a statistic value, the prior joint state--statistic mass is the prior probability of the state multiplied by that state's statistic mass at the specified value. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorJointStatisticMass_nonneg` | `0 ≤ E.priorJointStatisticMass prior θ s` | Every prior state--statistic joint mass is nonnegative. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorJointStatisticMass_sum` | `∑ p, E.priorJointStatisticMass prior p.1 p.2 = 1` | The prior state--statistic joint masses sum to one. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorStatisticMass` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → Statistic → ℝ` | For a finite uniform-allocation experiment, a finite prior design on latent states, and a statistic value, the prior-predictive statistic mass is the sum of the prior joint state--statistic masses over all latent states. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorStatisticMass_nonneg` | `0 ≤ E.priorStatisticMass prior s` | Every prior-predictive statistic mass is nonnegative. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorStatisticMass_sum` | `∑ s, E.priorStatisticMass prior s = 1` | The prior-predictive statistic masses sum to one. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorWeight` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → Statistic → Latent → ℝ` | For a finite uniform-allocation experiment, a finite prior design on latent states, a statistic value, and a latent state, the guarded posterior weight is the prior joint state--statistic mass divided by the prior-predictive statistic mass when the latter is positive, and is the original prior probability of the state when it is zero. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorWeight_of_pos` | `0 < E.priorStatisticMass prior s → ∀ (θ : Latent), E.posteriorWeight prior s θ = E.priorJointStatisticMass prior θ s / E.priorStatisticMass prior s` | On a positive prior-predictive fiber, the guarded posterior is the usual Bayes ratio. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorWeight_of_eq_zero` | `E.priorStatisticMass prior s = 0 → ∀ (θ : Latent), E.posteriorWeight prior s θ = prior.p θ` | On a null prior-predictive fiber, the guarded posterior is the original prior. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorWeight_nonneg` | `0 ≤ E.posteriorWeight prior s θ` | Every guarded posterior weight is nonnegative. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorWeight_sum` | `∑ θ, E.posteriorWeight prior s θ = 1` | Guarded posterior weights sum to one on positive and null prior-predictive fibers. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorStatisticMass_mul_posteriorWeight` | `E.priorStatisticMass prior s * E.posteriorWeight prior s θ = E.priorJointStatisticMass prior θ s` | Multiplying the statistic marginal by its guarded posterior weight recovers each state--statistic joint atom, including on null fibers. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorDesign` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → Statistic → Causalean.Experimentation.DesignBased.FiniteDesign Latent` | For a finite uniform-allocation experiment, a finite prior design on latent states, and a statistic value, the guarded posterior design is the finite probability design on latent states whose probabilities are the guarded posterior weights. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorStatisticDesign` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → Causalean.Experimentation.DesignBased.FiniteDesign Statistic` | For a finite uniform-allocation experiment and a finite prior design on latent states, the prior-predictive statistic design is the finite probability design whose probabilities are the prior-predictive statistic masses. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posterior_disintegrate_sum` | `∑ θ, ∑ s, E.priorJointStatisticMass prior θ s * f θ s = ∑ s, E.priorStatisticMass prior s * ∑ θ, E.posteriorWeight prior s θ * f θ s` | Every real test function of latent state and statistic has the same prior-joint expectation as prior-predictive statistic expectation of posterior conditional expectation; null fibers contribute exactly zero. |
<!-- /GEN -->

##### `Stat/FiniteRaoBlackwell/Sufficiency.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.Sufficiency -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.CommonConditionalKernel` | `Type (max (max u_2 u_3) u_4)` | A common finite conditional law assigns normalized nonnegative full-data weights to every statistic value and agrees with each statewise Bayes conditional on every positive-mass fiber. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization` | `Type (max (max (max u_1 u_2) u_3) u_4)` | A sufficient factorization writes every full-data mass as a nonnegative factor depending on the state and data only through the statistic, times a nonnegative state-independent carrier weight on the full data. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.fiberCarrierMass` | `E.SufficientFactorization → Statistic → ℝ` | For a finite uniform experiment, a sufficient factorization of that experiment's full-data masses, and a statistic value, the carrier mass of that statistic fiber is the sum of the factorization's state-independent carrier weights over all allocation--observation pairs having that statistic value. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.fiberCarrierMass_nonneg` | `0 ≤ F.fiberCarrierMass s` | Every carrier fiber mass is nonnegative. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.statisticMass_eq_factor_mul_fiberCarrierMass` | `E.statisticMass θ s = F.statisticFactor θ s * F.fiberCarrierMass s` | The statistic mass factors as the state-and-statistic factor times the carrier fiber mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.fiberCarrierMass_pos_of_statisticMass_pos` | `0 < E.statisticMass θ s → 0 < F.fiberCarrierMass s` | Positive statistic mass forces the state-independent carrier fiber to have positive mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.commonConditionalWeight` | `E.SufficientFactorization → Statistic → Allocation × Observation → ℝ` | For a finite uniform experiment, a sufficient factorization of its full-data masses, a statistic value, and an allocation--observation pair, the common conditional weight equals the carrier weight divided by the carrier mass when that mass is positive and the pair has the stated statistic, equals zero when the mass is positive but the pair has another statistic, and otherwise is a unit mass at the experiment's designated fallback pair. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.commonConditionalWeight_nonneg` | `0 ≤ F.commonConditionalWeight s z` | Factorization-induced common conditional weights are nonnegative on every fiber. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.commonConditionalWeight_sum` | `∑ z, F.commonConditionalWeight s z = 1` | Factorization-induced common conditional weights sum to one on every fiber. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.commonConditionalWeight_eq_conditionalWeight` | `0 < E.statisticMass θ s → ∀ (z : Allocation × Observation), F.commonConditionalWeight s z = E.conditionalWeight θ s z` | On every positive statewise statistic fiber, factorization identifies the common conditional weight with the guarded Bayes conditional weight. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.SufficientFactorization.toCommonConditionalKernel` | `E.SufficientFactorization → E.CommonConditionalKernel` | For a finite uniform experiment and a sufficient factorization of its full-data masses, the common conditional kernel induced by that factorization assigns the factorization-induced common conditional weights to every statistic value and allocation--observation pair. |
<!-- /GEN -->

##### `Stat/FiniteRaoBlackwell/RaoBlackwell.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.RaoBlackwell -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalMean` | `(Allocation × Observation → ℝ) → Latent → Statistic → ℝ` | For a finite uniform-allocation experiment, a real-valued full-data estimator, a latent state, and a statistic value, the conditional mean is the estimator's finite expectation under the guarded conditional design given that state and statistic value. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.measurable_conditionalMean` | `Measurable (E.conditionalMean est θ)` | On a finite statistic space, the state-indexed conditional mean is measurable for the discrete sigma algebra. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fullRisk` | `(Latent → ℝ) → (Allocation × Observation → ℝ) → Latent → ℝ` | For a finite uniform-allocation experiment, a real-valued target indexed by latent state, a real-valued full-data estimator, and a latent state, the full-data squared-error risk is the joint expected value of the estimator's squared error relative to the target at that state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticRisk` | `(Latent → ℝ) → (Statistic → ℝ) → Latent → ℝ` | For a finite uniform-allocation experiment, a real-valued target indexed by latent state, a real-valued statistic-only estimator, and a latent state, the statistic-only squared-error risk is the joint expected squared error after applying the estimator to the sample statistic. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fullRisk_nonneg` | `0 ≤ E.fullRisk target est θ` | Full-data squared-error risk is nonnegative at every latent state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticRisk_nonneg` | `0 ≤ E.statisticRisk target est θ` | Statistic-only squared-error risk is nonnegative at every latent state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalMean_sq_le` | `(E.conditionalMean est θ s - target) ^ 2 ≤ ∑ z, E.conditionalWeight θ s z * (est z - target) ^ 2` | Conditional Jensen for squared loss: the squared error of the conditional mean is at most the conditional mean squared error on every guarded fiber. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticRisk_conditionalMean_le_fullRisk` | `E.statisticRisk target (E.conditionalMean est θ) θ ≤ E.fullRisk target est θ` | At any fixed state, composing that state's conditional mean with the statistic has no larger squared-error risk than the original estimator. This is a fiberwise analytic inequality; the conditional mean in this statement is not asserted to be one estimator shared across states. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.commonRaoBlackwellEstimator` | `(E : Causalean.Stat.FiniteRaoBlackwell.FiniteUniformExperiment Latent Allocation Observation Statistic) → E.CommonConditionalKernel → (Allocation × Observation → ℝ) → Statistic → ℝ` | For a finite uniform-allocation experiment, a common conditional kernel, and a real-valued full-data estimator, the common Rao--Blackwell estimator assigns to each statistic value the finite weighted average of the full-data estimator under that kernel. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalMean_eq_commonRaoBlackwellEstimator` | `0 < E.statisticMass θ s → E.conditionalMean est θ s = E.commonRaoBlackwellEstimator K est s` | On every positive statewise fiber, the state-indexed conditional mean equals the common Rao--Blackwell estimator supplied by the sufficient conditional kernel. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.measurable_commonRaoBlackwellEstimator` | `Measurable (E.commonRaoBlackwellEstimator K est)` | On finite statistic spaces, the common Rao--Blackwell estimator is measurable for the discrete sigma algebra. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticRisk_commonRaoBlackwellEstimator_le_fullRisk` | `E.statisticRisk target (E.commonRaoBlackwellEstimator K est) θ ≤ E.fullRisk target est θ` | A common sufficient conditional kernel produces one statistic-only Rao--Blackwell estimator whose squared-error risk is no larger at every latent state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorRisk_commonRaoBlackwellEstimator_le` | `prior.E (E.statisticRisk target (E.commonRaoBlackwellEstimator K est)) ≤ prior.E (E.fullRisk target est)` | Under any finite prior on latent states, the common Rao--Blackwell estimator has no larger prior-averaged squared-error risk than the original estimator. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.worstCaseRisk_commonRaoBlackwellEstimator_le` | `Causalean.Stat.worstCaseRisk (E.statisticRisk target) (E.commonRaoBlackwellEstimator K est) ≤ Causalean.Stat.worstCaseRisk (E.fullRisk target) est` | Under state-independent conditional laws, Rao--Blackwellization weakly decreases the finite worst-case squared risk, in the real-valued `worstCaseRisk` API. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.minimaxValue_statistic_le_full_of_commonConditionalKernel` | `Causalean.Stat.minimaxValue (E.statisticRisk target) ≤ Causalean.Stat.minimaxValue (E.fullRisk target)` | If every full-data estimator is Rao--Blackwellized through state-independent conditionals, the minimax value over statistic-only estimators is no larger than the full-data minimax value. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.raoBlackwellEstimator` | `(E : Causalean.Stat.FiniteRaoBlackwell.FiniteUniformExperiment Latent Allocation Observation Statistic) → E.SufficientFactorization → (Allocation × Observation → ℝ) → Statistic → ℝ` | For a finite uniform-allocation experiment, a sufficient factorization, and a real-valued full-data estimator, the Rao--Blackwell estimator is the statistic-only estimator obtained by averaging the full-data estimator with the factorization's derived common conditional law. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.measurable_raoBlackwellEstimator` | `Measurable (E.raoBlackwellEstimator F est)` | The factorization-based Rao--Blackwell estimator is measurable on a finite discrete statistic space. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticRisk_raoBlackwellEstimator_le_fullRisk` | `E.statisticRisk target (E.raoBlackwellEstimator F est) θ ≤ E.fullRisk target est θ` | A sufficient factorization produces one statistic-only Rao--Blackwell estimator whose squared-error risk is no larger than the full-data estimator at every latent state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.priorRisk_raoBlackwellEstimator_le` | `prior.E (E.statisticRisk target (E.raoBlackwellEstimator F est)) ≤ prior.E (E.fullRisk target est)` | Under any finite prior, the one statistic-only estimator derived from a sufficient factorization has no larger prior-averaged squared-error risk than the full-data estimator. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.worstCaseRisk_raoBlackwellEstimator_le` | `Causalean.Stat.worstCaseRisk (E.statisticRisk target) (E.raoBlackwellEstimator F est) ≤ Causalean.Stat.worstCaseRisk (E.fullRisk target) est` | Under a sufficient factorization, the one statistic-only Rao--Blackwell estimator has no larger finite worst-case squared risk than the full-data estimator. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.minimaxValue_statistic_le_full` | `Causalean.Stat.minimaxValue (E.statisticRisk target) ≤ Causalean.Stat.minimaxValue (E.fullRisk target)` | If the statistic satisfies the finite factorization criterion, its estimator class has minimax squared-risk value no larger than the full-data estimator class. |
<!-- /GEN -->

##### `Stat/FiniteRaoBlackwell/KernelBridge.lean`

<!-- GEN:Causalean.Stat.FiniteRaoBlackwell.KernelBridge -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fullKernel` | `ProbabilityTheory.Kernel Latent (Allocation × Observation)` | Given a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with σ-algebras on the latent, allocation, and observation spaces and measurable singleton latent states, the full-data Markov kernel assigns to each latent state the experiment's finite joint probability law of the allocation and observation at that state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fullKernel_isMarkovKernel` | `ProbabilityTheory.IsMarkovKernel E.fullKernel` | For a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with distinguishable allocation, observation, and statistic values, σ-algebras on the latent, allocation, and observation spaces, and measurable singleton latent states, the full-data kernel is a Markov kernel: at every latent state, its output law is a probability measure. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticKernel` | `ProbabilityTheory.Kernel Latent Statistic` | Given a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with σ-algebras on the latent and statistic spaces and measurable singleton latent states, the statistic Markov kernel assigns to each latent state the experiment's finite marginal probability law of the statistic. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticKernel_isMarkovKernel` | `ProbabilityTheory.IsMarkovKernel E.statisticKernel` | For a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with distinguishable allocation, observation, and statistic values, σ-algebras on the latent and statistic spaces, and measurable singleton latent states, the statistic kernel is a Markov kernel: at every latent state, its output law is a probability measure. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalKernel` | `Latent → ProbabilityTheory.Kernel Statistic (Allocation × Observation)` | Given a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with σ-algebras on the allocation, observation, and statistic spaces and measurable singleton statistic values, and a latent state, the guarded conditional Markov kernel assigns to each statistic value the experiment's conditional probability law of the allocation--observation pair at that state, using the fallback law when the statistic fiber has zero probability. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalKernel_isMarkovKernel` | `ProbabilityTheory.IsMarkovKernel (E.conditionalKernel θ)` | For a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with distinguishable allocation, observation, and statistic values, σ-algebras on the allocation, observation, and statistic spaces, and measurable singleton statistic values and a latent state, the guarded conditional kernel is a Markov kernel: at every statistic value, including a zero-probability fibre, its output law is a probability measure. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.commonConditionalMarkovKernel` | `(E : Causalean.Stat.FiniteRaoBlackwell.FiniteUniformExperiment Latent Allocation Observation Statistic) → E.SufficientFactorization → ProbabilityTheory.Kernel Statistic (Allocation × Observation)` | Given a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with σ-algebras on the allocation, observation, and statistic spaces and measurable singleton statistic values, and a sufficient factorization of its full-data masses, the common conditional Markov kernel assigns to each statistic value the factorization-induced state-independent conditional probability law of the allocation--observation pair. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.commonConditionalMarkovKernel_isMarkovKernel` | `ProbabilityTheory.IsMarkovKernel (E.commonConditionalMarkovKernel F)` | For a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with distinguishable allocation, observation, and statistic values, σ-algebras on the allocation, observation, and statistic spaces, and measurable singleton statistic values and a sufficient factorization of its full-data probability masses, the factorization-derived common conditional kernel is a Markov kernel: at every statistic value, including a zero-probability fibre, its output law is a probability measure. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorKernel` | `Causalean.Experimentation.DesignBased.FiniteDesign Latent → ProbabilityTheory.Kernel Statistic Latent` | Given a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with σ-algebras on the latent and statistic spaces and measurable singleton statistic values, and a finite prior probability distribution on latent states, the posterior Markov kernel assigns to each statistic value the guarded posterior probability law of the latent state, using the designated fallback law on prior-predictive statistic fibers of zero probability. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorKernel_isMarkovKernel` | `ProbabilityTheory.IsMarkovKernel (E.posteriorKernel prior)` | For a finite uniform experiment on finite latent, allocation, observation, and statistic spaces, with distinguishable allocation, observation, and statistic values, σ-algebras on the latent and statistic spaces, and measurable singleton statistic values and a finite prior probability distribution on latent states, the guarded posterior kernel is a Markov kernel: at every statistic value, including a prior-predictive zero-probability fibre, its output law is a probability measure. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.fullKernel_singletonReal` | `(E.fullKernel θ).real {z} = E.jointMass θ z` | The real singleton probability of the full-data kernel equals the finite joint mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.statisticKernel_singletonReal` | `(E.statisticKernel θ).real {s} = E.statisticMass θ s` | The real singleton probability of the statistic kernel equals the finite statistic mass. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.conditionalKernel_singletonReal` | `((E.conditionalKernel θ) s).real {z} = E.conditionalWeight θ s z` | The real singleton probability of the guarded conditional kernel equals its finite conditional weight. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.commonConditionalMarkovKernel_singletonReal` | `((E.commonConditionalMarkovKernel F) s).real {z} = F.toCommonConditionalKernel.weight s z` | The real singleton probability of the factorization-derived common conditional Markov kernel equals its finite common conditional weight. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.posteriorKernel_singletonReal` | `((E.posteriorKernel prior) s).real {θ} = E.posteriorWeight prior s θ` | The real singleton probability of the finite-prior posterior kernel equals the guarded posterior weight of that latent state. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.kernelAverageLoss_statisticKernel_eq_sum` | `Causalean.Stat.kernelAverageLoss E.statisticKernel loss a θ = ∑ s, E.statisticMass θ s * loss a s` | Kernel averaging through the statistic kernel is exactly the real finite sum of statistic losses weighted by their statistic masses, matching `FiniteKernelBayes`. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.kernelAverageLoss_posteriorKernel_eq_sum` | `Causalean.Stat.kernelAverageLoss (E.posteriorKernel prior) loss a s = ∑ θ, E.posteriorWeight prior s θ * loss a θ` | Kernel averaging a real latent-state loss through the finite-prior posterior kernel is exactly the finite posterior-weighted sum, matching the real `FiniteKernelBayes` interface. |
| `Stat.FiniteRaoBlackwell.FiniteUniformExperiment.kernelMean_conditionalKernel_eq_conditionalMean` | `∫ (z : Allocation × Observation), est z ∂(E.conditionalKernel θ) s = E.conditionalMean est θ s` | The measure-theoretic conditional-kernel mean of a full-data estimator equals the finite conditional mean, so no `ENNReal` conversion appears in Rao--Blackwell calculations. |
<!-- /GEN -->

#### 12w.6a.ii. `Stat/Minimax/FinitePosteriorBayesRisk.lean` — finite posterior squared-loss Bayes risk

A finite prior and finite observation kernel determine a guarded posterior mean, an exact
squared-loss decomposition, and the resulting real-valued Bayes-risk infimum. Continuous priors
enter through the existing induced finite design, which also transfers estimator-wise integral
lower bounds to the corresponding finite and continuous-mixture Bayes risks.

<!-- GEN:Causalean.Stat.Minimax.FinitePosteriorBayesRisk -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.kernelMass` | `ProbabilityTheory.Kernel S X → S → X → ℝ` | Given a measurable latent-state space, a measurable observation space, a Markov observation kernel from latent states to observations, a latent state, and an observation, the real-valued singleton observation mass is the kernel's mass at that observation conditional on that latent state. |
| `Stat.kernelMass_nonneg` | `0 ≤ Causalean.Stat.kernelMass L s x` | Every singleton observation mass emitted by a Markov kernel is nonnegative. |
| `Stat.kernelMass_sum` | `∑ x, Causalean.Stat.kernelMass L s x = 1` | The singleton observation masses emitted in each latent state sum to one. |
| `Stat.jointMass` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → S → X → ℝ` | Given a finite measurable latent-state space, a measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, a latent state, and an observation, the joint mass of that state and observation is the prior mass of the state times the conditional singleton mass of the observation. |
| `Stat.jointMass_nonneg` | `0 ≤ Causalean.Stat.jointMass ν L s x` | Every latent-state--observation joint mass is nonnegative. |
| `Stat.jointMass_sum` | `∑ s, ∑ x, Causalean.Stat.jointMass ν L s x = 1` | The joint masses of a normalized finite prior and Markov observation kernel sum to one. |
| `Stat.observationMass` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → X → ℝ` | Given a finite measurable latent-state space, a measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, and an observation, the observation marginal mass is the sum, over all latent states, of their joint masses with that observation. |
| `Stat.observationMass_nonneg` | `0 ≤ Causalean.Stat.observationMass ν L x` | Every observation marginal mass is nonnegative. |
| `Stat.observationMass_sum` | `∑ x, Causalean.Stat.observationMass ν L x = 1` | The observation marginal masses sum to one. |
| `Stat.posteriorWeight` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → X → S → ℝ` | Given a finite measurable latent-state space, a measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, an observation, and a latent state, the guarded posterior weight is zero when the observation has zero marginal mass and is otherwise the joint mass of the state and observation divided by that marginal mass. |
| `Stat.posteriorWeight_of_ne_zero` | `Causalean.Stat.observationMass ν L x ≠ 0 → ∀ (s : S), Causalean.Stat.posteriorWeight ν L x s = Causalean.Stat.jointMass ν L s x / Causalean.Stat.observationMass ν L x` | On a nonzero observation fiber, the guarded posterior weight is the usual Bayes ratio. |
| `Stat.posteriorWeight_of_eq_zero` | `Causalean.Stat.observationMass ν L x = 0 → ∀ (s : S), Causalean.Stat.posteriorWeight ν L x s = 0` | On a null observation fiber, every guarded posterior weight is zero. |
| `Stat.posteriorWeight_nonneg` | `0 ≤ Causalean.Stat.posteriorWeight ν L x s` | Every guarded posterior weight is nonnegative. |
| `Stat.posteriorWeight_sum_of_pos` | `0 < Causalean.Stat.observationMass ν L x → ∑ s, Causalean.Stat.posteriorWeight ν L x s = 1` | On a positive-mass observation fiber, the guarded posterior weights sum to one. |
| `Stat.posteriorWeight_sum_of_eq_zero` | `Causalean.Stat.observationMass ν L x = 0 → ∑ s, Causalean.Stat.posteriorWeight ν L x s = 0` | On a null observation fiber, the guarded posterior weights sum to zero. |
| `Stat.observationMass_mul_posteriorWeight` | `Causalean.Stat.observationMass ν L x * Causalean.Stat.posteriorWeight ν L x s = Causalean.Stat.jointMass ν L s x` | Multiplying an observation marginal by its guarded posterior weight recovers the joint atom, including on null fibers. |
| `Stat.disintegrate_sum` | `∑ s, ∑ x, Causalean.Stat.jointMass ν L s x * f s x = ∑ x, Causalean.Stat.observationMass ν L x * ∑ s, Causalean.Stat.posteriorWeight ν L x s * f s x` | Every real test function has the same joint expectation as the marginally weighted guarded posterior expectation. |
| `Stat.posteriorMean` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → (S → X → ℝ) → X → ℝ` | Given a finite measurable latent-state space, a measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, an observation-dependent real target, and an observation, the guarded posterior mean is the sum over latent states of the guarded posterior weight times that target at the state and observation. |
| `Stat.observationMass_mul_posteriorMean` | `Causalean.Stat.observationMass ν L x * Causalean.Stat.posteriorMean ν L t x = ∑ s, Causalean.Stat.jointMass ν L s x * t s x` | Observation mass times the guarded posterior mean equals the corresponding joint weighted target numerator, including on null fibers. |
| `Stat.posterior_centered_sum_of_pos` | `0 < Causalean.Stat.observationMass ν L x → ∑ s, Causalean.Stat.posteriorWeight ν L x s * (t s x - Causalean.Stat.posteriorMean ν L t x) = 0` | On a positive-mass observation fiber, posterior target residuals have weighted mean zero. |
| `Stat.squaredRisk` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → (S → X → ℝ) → (X → ℝ) → ℝ` | Given a finite measurable latent-state space, a finite measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, an observation-dependent real target, and a real-valued estimator based on the observation, the squared risk is the sum over all latent states and observations of their joint mass times the estimator's squared error relative to the target. |
| `Stat.posteriorResidual` | `Causalean.Experimentation.DesignBased.FiniteDesign S → ProbabilityTheory.Kernel S X → (S → X → ℝ) → ℝ` | Given a finite measurable latent-state space, a finite measurable observation space, a finite probability design on the latent states, a Markov observation kernel from latent states to observations, and an observation-dependent real target, the posterior residual risk is the sum over observations of their marginal mass times the posterior-weighted squared deviation of the target from its guarded posterior mean. |
| `Stat.squaredRisk_eq_posteriorResidual_add` | `Causalean.Stat.squaredRisk ν L t T = Causalean.Stat.posteriorResidual ν L t + ∑ x, Causalean.Stat.observationMass ν L x * (T x - Causalean.Stat.posteriorMean ν L t x) ^ 2` | Every estimator's squared risk equals posterior residual risk plus its marginally weighted squared distance from the posterior mean. |
| `Stat.squaredRisk_posteriorMean` | `Causalean.Stat.squaredRisk ν L t (Causalean.Stat.posteriorMean ν L t) = Causalean.Stat.posteriorResidual ν L t` | The guarded posterior-mean estimator has squared risk exactly equal to posterior residual risk. |
| `Stat.posteriorMean_minimizes` | `Causalean.Stat.squaredRisk ν L t (Causalean.Stat.posteriorMean ν L t) ≤ Causalean.Stat.squaredRisk ν L t T` | The guarded posterior mean minimizes squared risk among all real estimators. |
| `Stat.iInf_squaredRisk_eq_posteriorResidual` | `⨅ T, Causalean.Stat.squaredRisk ν L t T = Causalean.Stat.posteriorResidual ν L t` | The infimum over all real estimators of finite squared risk is exactly the posterior residual Bayes risk. |
| `Stat.statewiseSquaredLoss` | `ProbabilityTheory.Kernel S X → (S → X → ℝ) → (X → ℝ) → S → ℝ` | Given a measurable latent-state space, a finite measurable observation space, a Markov observation kernel from latent states to observations, an observation-dependent real target, a real-valued estimator based on the observation, and a latent state, the statewise squared loss is the sum over observations of their conditional singleton masses times the estimator's squared error relative to the target. |
| `Stat.finiteDesign_expectedLoss_statewiseSquaredLoss` | `ν.E (Causalean.Stat.statewiseSquaredLoss L t T) = Causalean.Stat.squaredRisk ν L t T` | Finite-design expected statewise squared loss is the joint-law squared risk. |
| `Stat.finiteDesignBayesRisk_statewiseSquaredLoss` | `Causalean.Stat.finiteDesignBayesRisk ν (Causalean.Stat.statewiseSquaredLoss L t) = Causalean.Stat.posteriorResidual ν L t` | The real-valued finite-design Bayes-risk infimum for observation-dependent squared loss is exactly the posterior residual risk. |
| `Stat.mixedKernelLoss_statewiseSquaredLoss_eq_inducedSquaredRisk` | `Causalean.Stat.mixedKernelLoss π K (Causalean.Stat.statewiseSquaredLoss L t) T = Causalean.Stat.squaredRisk (Causalean.Stat.inducedFiniteDesign π K) L t T` | The continuous-prior mixture of an estimator's finite-observation squared loss is the real-valued squared risk under the induced finite design. |
| `Stat.realBayesRisk_statewiseSquaredLoss_eq_posteriorResidual` | `Causalean.Stat.realBayesRisk π K (Causalean.Stat.statewiseSquaredLoss L t) = Causalean.Stat.posteriorResidual (Causalean.Stat.inducedFiniteDesign π K) L t` | The real Bayes risk of continuously mixed finite-state squared loss equals the posterior residual risk computed from the induced finite design. |
| `Stat.integratedLowerBound_le_inducedFiniteDesignBayesRisk` | `(∀ (T : X → ℝ), integratedRisk T = Causalean.Stat.squaredRisk (Causalean.Stat.inducedFiniteDesign π K) L t T) → (∀ (T : X → ℝ), B ≤ integratedRisk T) → B ≤ Causalean.Stat.finiteDesignBayesRisk (Causalean.Stat.inducedFiniteDesign π K) (Causalean.Stat.statewiseSquaredLoss L t)` | If an integrated continuous-prior risk agrees estimator by estimator with the induced finite squared risk and has a common lower bound, that constant lower-bounds the real-valued finite-design Bayes-risk infimum. |
| `Stat.integratedLowerBound_le_inducedPosteriorResidual` | `(∀ (T : X → ℝ), integratedRisk T = Causalean.Stat.squaredRisk (Causalean.Stat.inducedFiniteDesign π K) L t T) → (∀ (T : X → ℝ), B ≤ integratedRisk T) → B ≤ Causalean.Stat.posteriorResidual (Causalean.Stat.inducedFiniteDesign π K) L t` | If an integrated continuous-prior risk agrees estimator by estimator with the induced finite squared risk and has a common lower bound, that bound is at most the posterior residual risk of the induced finite experiment. |
| `Stat.mixedIntegratedLowerBound_le_inducedFiniteDesignBayesRisk` | `(∀ (T : X → ℝ), integratedRisk T = Causalean.Stat.mixedKernelLoss π K (Causalean.Stat.statewiseSquaredLoss L t) T) → (∀ (T : X → ℝ), B ≤ integratedRisk T) → B ≤ Causalean.Stat.finiteDesignBayesRisk (Causalean.Stat.inducedFiniteDesign π K) (Causalean.Stat.statewiseSquaredLoss L t)` | If an integrated risk agrees estimator by estimator with the generic continuous mixture and has a common lower bound, that bound transfers to the corresponding real-valued induced finite-design Bayes risk. |
| `Stat.mixedIntegratedLowerBound_le_realBayesRisk` | `(∀ (T : X → ℝ), integratedRisk T = Causalean.Stat.mixedKernelLoss π K (Causalean.Stat.statewiseSquaredLoss L t) T) → (∀ (T : X → ℝ), B ≤ integratedRisk T) → B ≤ Causalean.Stat.realBayesRisk π K (Causalean.Stat.statewiseSquaredLoss L t)` | If an integrated risk agrees estimator by estimator with the generic continuous mixture and has a common lower bound, that bound transfers to the real-valued continuous-mixture Bayes risk. |
<!-- /GEN -->

#### 12w.6b. `Stat/Minimax/MarkovKernelTransport.lean` — randomized experiment transport

Blackwell garbling cannot improve squared-error estimation risk. A bounded target-experiment
estimator is averaged over the Markov kernel to produce its Rao--Blackwell pullback on the
source experiment; Jensen's inequality compares their risks, while an affine target change
rescales the lower bound by the squared slope. The finite-product construction applies the
kernel independently across coordinates and includes the empty sample.

<!-- GEN:Causalean.Stat.Minimax.MarkovKernelTransport -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.UniformlyBounded` | `(A → ℝ) → Prop` | For an arbitrary domain and a real-valued function on that domain, the uniform boundedness property holds exactly when there exists a nonnegative real constant that bounds the function's absolute value at every input. |
| `Stat.kernelMean` | `ProbabilityTheory.Kernel X Y → (Y → ℝ) → X → ℝ` | For an input measurable space, an output measurable space, and a Markov kernel from the input space to the output space and a real-valued function on the output space, the kernel mean assigns to each input the expectation of that function under the output distribution selected by the kernel. |
| `Stat.measurable_kernelMean` | `Measurable T → Measurable (Causalean.Stat.kernelMean K T)` | Measurability of a real-valued estimator ensures that its expectation under each kernel output distribution varies measurably with the kernel input. |
| `Stat.abs_kernelMean_le` | `0 ≤ M → (∀ (y : Y), \|T y\| ≤ M) → ∀ (x : X), \|Causalean.Stat.kernelMean K T x\| ≤ M` | If a proposed bound is nonnegative and bounds the estimator in absolute value at every output, then the kernel mean obeys the same absolute bound at every input when each kernel output is a probability distribution. |
| `Stat.uniformlyBounded_kernelMean` | `Causalean.Stat.UniformlyBounded T → Causalean.Stat.UniformlyBounded (Causalean.Stat.kernelMean K T)` | Uniform boundedness of an estimator implies that averaging it against a Markov kernel is uniformly bounded by the same witness. |
| `Stat.kernelAffinePullback` | `ProbabilityTheory.Kernel X Y → ℝ → ℝ → (Y → ℝ) → X → ℝ` | For an input measurable space, an output measurable space, and a Markov kernel from the input space to the output space, a real slope, a real offset, and a real-valued estimator on the output space, the affine kernel pullback assigns to each input the kernel mean of the estimator, minus the offset, divided by the slope. |
| `Stat.measurable_kernelAffinePullback` | `Measurable targetEst → Measurable (Causalean.Stat.kernelAffinePullback K a b targetEst)` | Measurability of a target estimator ensures that its affine kernel pullback is measurable on the source experiment. |
| `Stat.uniformlyBounded_kernelAffinePullback` | `a ≠ 0 → ∀ {targetEst : Y → ℝ}, Causalean.Stat.UniformlyBounded targetEst → Causalean.Stat.UniformlyBounded (Causalean.Stat.kernelAffinePullback K a b targetEst)` | If the affine slope is nonzero and the target estimator is uniformly bounded, then the affine kernel pullback is uniformly bounded on the source experiment. |
| `Stat.integral_kernelMean_eq_integral_comp` | `Measurable T → Causalean.Stat.UniformlyBounded T → ∫ (x : X), Causalean.Stat.kernelMean K T x ∂P = ∫ (y : Y), T y ∂P.bind ⇑K` | If the target estimator is measurable and uniformly bounded, then its expectation after taking the kernel mean under a source probability law equals its expectation under the garbled law. |
| `Stat.sqLoss_kernelMean_le` | `Measurable T → Causalean.Stat.UniformlyBounded T → ∀ (c : ℝ) (x : X), (Causalean.Stat.kernelMean K T x - c) ^ 2 ≤ ∫ (y : Y), (T y - c) ^ 2 ∂K x` | If the target estimator is measurable and uniformly bounded, then the squared error of its kernel mean is no greater than the kernel average of its squared error at each source observation. |
| `Stat.sqRisk_kernelMean_le_comp` | `Measurable T → Causalean.Stat.UniformlyBounded T → ∀ (c : ℝ), Causalean.Stat.sqRisk P (Causalean.Stat.kernelMean K T) c ≤ Causalean.Stat.sqRisk (P.bind ⇑K) T c` | If the target estimator is measurable and uniformly bounded, then Rao--Blackwellizing it through a Markov kernel cannot increase squared risk: the source-law risk of the kernel mean is at most the target-law risk after garbling. |
| `Stat.sqRisk_kernelAffinePullback_le_comp` | `a ≠ 0 → ∀ {targetEst : Y → ℝ}, Measurable targetEst → Causalean.Stat.UniformlyBounded targetEst → a ^ 2 * Causalean.Stat.sqRisk P (Causalean.Stat.kernelAffinePullback K a b targetEst) theta ≤ Causalean.Stat.sqRisk (P.bind ⇑K) targetEst (a * theta + b)` | If the affine slope is nonzero, the target estimator is measurable, and the target estimator is uniformly bounded, then the source risk of the affine kernel pullback, multiplied by the squared slope, is at most the target risk under the garbled law. |
| `Stat.forall_estimator_exists_sqRisk_ge_of_kernel_affine_transport` | `a ≠ 0 → (∀ (j : Iota), Q j = (P j).bind ⇑K) → (∀ (sourceEst : X → ℝ), Measurable sourceEst → Causalean.Stat.UniformlyBounded sourceEst → ∃ j, L ≤ Causalean.Stat.sqRisk (P j) sourceEst (theta j)) → ∀ (targetEst : Y → ℝ), Measurable targetEst → Causalean.Stat.UniformlyBounded targetEst → ∃ j, a ^ 2 * L ≤ Causalean.Stat.sqRisk (Q j) targetEst (a * theta j + b)` | Suppose the affine slope is nonzero, each target law is obtained by passing its source law through the common Markov kernel, and every measurable uniformly bounded source estimator incurs squared risk at least a fixed level for some parameter index. Then every measurable uniformly bounded target estimator incurs at least the source level multiplied by the squared slope for some parameter index, with the target parameter transformed by the same affine map. |
| `Stat.finProductKernel` | `(n : ℕ) → ProbabilityTheory.Kernel X Y → ProbabilityTheory.Kernel (Fin n → X) (Fin n → Y)` | For an input measurable space, an output measurable space, a nonnegative integer number of coordinates, and a Markov kernel from the input space to the output space, the finite product kernel maps an input vector to the unique point-mass kernel at zero coordinates, and otherwise to independent kernel outputs coordinate by coordinate. |
| `Stat.finProductKernel_apply` | `(Causalean.Stat.finProductKernel n K) x = MeasureTheory.Measure.pi fun i => K (x i)` | Each fibre of the finite product kernel equals the independent product of its coordinate output laws, including the unique empty product fibre. |
| `Stat.instIsMarkovKernelFinProductKernel` | `ProbabilityTheory.IsMarkovKernel (Causalean.Stat.finProductKernel n K)` | For every nonnegative integer number of coordinates and every Markov kernel between measurable input and output spaces, the independent product kernel over that many coordinates is a Markov kernel, including when there are zero coordinates. |
| `Stat.finProductKernel_comp_pi` | `(MeasureTheory.Measure.pi fun x => P).bind ⇑(Causalean.Stat.finProductKernel n K) = MeasureTheory.Measure.pi fun x => P.bind ⇑K` | Passing an independent finite product law through the coordinatewise product kernel produces the product of the one-coordinate garbled law, including when there are no coordinates. |
| `Stat.sqRisk_finProductKernel_affinePullback_le` | `a ≠ 0 → ∀ {targetEst : (Fin n → Y) → ℝ}, Measurable targetEst → Causalean.Stat.UniformlyBounded targetEst → a ^ 2 * Causalean.Stat.sqRisk (MeasureTheory.Measure.pi fun x => P) (Causalean.Stat.kernelAffinePullback (Causalean.Stat.finProductKernel n K) a b targetEst) theta ≤ Causalean.Stat.sqRisk (MeasureTheory.Measure.pi fun x => P.bind ⇑K) targetEst (a * theta + b)` | If the affine slope is nonzero, a finite-sample target estimator is measurable, and that estimator is uniformly bounded, then the source-product risk of its affine product-kernel pullback, multiplied by the squared slope, is at most its target-product risk. |
| `Stat.forall_estimator_exists_sqRisk_ge_of_kernel_affine_transport_pi` | `a ≠ 0 → (∀ (j : Iota), Q j = (P j).bind ⇑K) → (∀ (sourceEst : (Fin n → X) → ℝ), Measurable sourceEst → Causalean.Stat.UniformlyBounded sourceEst → ∃ j, L ≤ Causalean.Stat.sqRisk (MeasureTheory.Measure.pi fun x => P j) sourceEst (theta j)) → ∀ (targetEst : (Fin n → Y) → ℝ), Measurable targetEst → Causalean.Stat.UniformlyBounded targetEst → ∃ j, a ^ 2 * L ≤ Causalean.Stat.sqRisk (MeasureTheory.Measure.pi fun x => Q j) targetEst (a * theta j + b)` | Suppose the affine slope is nonzero, each one-coordinate target law is obtained by applying the common Markov kernel to its source law, and every measurable uniformly bounded estimator on the finite source product incurs squared risk at least a fixed level for some parameter index. Then every measurable uniformly bounded estimator on the target product incurs at least the source level multiplied by the squared slope for some parameter index, including when the sample has no coordinates. |
<!-- /GEN -->

#### 12w.7. `Stat/Minimax/Fano.lean` — multiple-hypothesis (M-ary) lower bound

Generalizes Le Cam from *two* hypotheses to *many* pairwise-`2s`-separated ones — the testing/TV form of Fano's lemma (Tsybakov §2.6, Le Cam–Birgé). An estimator induces disjoint acceptance regions `A i = {ω | dist(est ω, θ i) < s}`; comparing each `Pᵢ(A i)` to a *reference* hypothesis `i₀` costs one `tvDist`, bounding the total correct-recovery mass. Proven **unconditionally** from `measureReal_sub_le_tvDist` — no entropy/mutual-information machinery; divergence enters only through `tvDist` (bound it by KL/χ² via §12w.3/§12w.5). Uses a reference index `i₀` rather than the centroid mixture, so it does **not** depend on `Mixture.lean` (the symmetric mixture-centroid form is a strict sharpening that can be layered on).

| Declaration | Statement | Description |
|---|---|---|
| `acceptanceRegion est θ s` | `{ω \| dist (est ω) θ < s}` | Points the estimator places strictly within `s` of `θ`. |
| `measurableSet_acceptanceRegion` | `MeasurableSet (acceptanceRegion est θ s)` | Complement of the (closed) error region. **Proved.** |
| `acceptanceRegion_disjoint` / `_pairwiseDisjoint` | `2s ≤ dist θ₀ θ₁ ⇒ Disjoint (A θ₀) (A θ₁)`; pairwise version over a family | A point within `s` of both targets would force them within `2s`. **Proved.** |
| `sum_refReal_acceptance_le` | `∑ᵢ (P i₀).real (A i) ≤ 1` | Reference mass over disjoint regions is `≤ 1` (finite additivity + `≤ univ`). **Proved.** |
| `sum_correct_le` | `∑ᵢ (P i).real (A i) ≤ 1 + ∑ᵢ tvDist (P i₀) (P i)` | **Heart of Fano**: each `Pᵢ(A i)` vs reference costs one `tvDist`. **Proved.** |
| `fano_average_error` | `1 − (1 + ∑ᵢ tvDist (P i₀) (P i))/N ≤ (∑ᵢ (P i).real {s ≤ dist(est,θ i)})/N` (`N = card ι`) | **Average-error minimax lower bound** over the family. **Proved, axiom-clean.** |
| `fano_exists_error` | under `tvDist (P i₀) (P i) ≤ β`: `∃ i, 1 − 1/N − β ≤ (P i).real {s ≤ dist(est,θ i)}` | **Usable form**: large `N` + small `β` ⇒ some hypothesis forces error. **Proved, axiom-clean.** |

#### 12w.8. `Stat/Minimax/Assouad.lean` — hypercube lower bound (Assouad's lemma)

Packs a `d`-cube `{P τ : τ ∈ (Fin d → Bool)}` in which each coordinate is an independent two-point test — converting a `d`-fold product into a `d`-fold *sum* of testing bounds (rates that grow with dimension). A cube estimator `est : Ω → (Fin d → Bool)` decodes each bit; its expected **Hamming risk** counts mis-decoded coordinates. Proof = coordinate-flip averaging: pairing each vertex `τ` with its `j`-flip via an involution (`flipPerm`), the two coordinate-`j` masses are `(P τ).real S + (P (flip j τ)).real Sᶜ`, lower-bounded by `1 − tvDist` via `one_sub_tvDist_le_test`. **Unconditional**, no product-measure machinery.

| Declaration | Statement | Description |
|---|---|---|
| `flipBit j τ` / `flipPerm j` | `Function.update τ j (!τ j)`; as an `Equiv.Perm` | Flip the `j`-th cube coordinate (an involution; the perm reindexes cube-sums). **Proved involutive.** |
| `hammingRisk P est τ` | `∑ⱼ (P τ).real {ω \| est ω j ≠ τ j}` | Expected number of coordinates the estimator decodes incorrectly at vertex `τ`. |
| `measurableSet_decode_ne` | `MeasurableSet {ω \| est ω j ≠ b}` from `MeasurableSet {ω \| est ω j = b}` | Decoding-error sets are complements of level sets. **Proved.** |
| `decode_ne_flip_compl` | `{ω \| est ω j ≠ (flip j τ) j} = {ω \| est ω j ≠ τ j}ᶜ` | The `j`-th error set at the flipped vertex is the complement at `τ` (`a ≠ !b ↔ a = b`). **Proved.** |
| `hammingRisk_pair_ge` | `1 − tvDist (P τ) (P (flip j τ)) ≤ (P τ).real Sⱼτ + (P (flip j τ)).real Sⱼ(flip)` | **Per-vertex pair bound** = two-point testing bound on the `j`-th decoded bit. **Proved.** |
| `sum_decode_ge` | `∑_τ (1 − tvDist (P τ)(P (flip j τ))) ≤ 2·∑_τ (P τ).real {est j ≠ τ j}` | Per-coordinate cube-sum bound via the flip involution (`Equiv.sum_comp`). **Proved.** |
| `assouad_average` | under `tvDist (P τ)(P (flip j τ)) ≤ β`: `(d/2)(1 − β) ≤ (∑_τ hammingRisk P est τ)/card` | **Assouad (average form).** Large `d` + small `β` ⇒ many mis-decoded coordinates. **Proved, axiom-clean.** |
| `assouad_exists` | same hypotheses: `∃ τ, (d/2)(1 − β) ≤ hammingRisk P est τ` | **Existence form**: some vertex forces large Hamming risk. **Proved, axiom-clean.** |

#### 12w.9. `Stat/Minimax/Mixture.lean` — finite mixtures of measures

Generic finite mixture `∑ᵢ wᵢ • Pᵢ` and the **domination lemma** behind the fuzzy-hypothesis / second-moment method: a mixture over in-class hypotheses with a common functional value never assigns more mass than the worst single hypothesis, so an averaged risk is `≤` the supremum. Consumed by the structure-agnostic ATE lower bound (§13sa) for the `dominated` (realizability) obligation of `TwoPointWitness`.

| Declaration | Statement | Description |
|---|---|---|
| `mixture w P` | `∑ i, w i • P i` (`w : Fin K → ℝ≥0∞`) | Finite mixture of measures with `ℝ≥0∞` weights. |
| `mixture_apply` | `mixture w P A = ∑ i, w i · P i A` | Mass on a set is the weighted sum of the parts' masses. **Proved.** |
| `mixture_isProbabilityMeasure` | `∑ i, w i = 1 ⇒ IsProbabilityMeasure (mixture w P)` (parts prob) | A convex combination of probability measures is a probability measure. **Proved.** |
| `mixtureReal_le` | `∑ i, w i = 1, (∀ i, (P i).real A ≤ B) ⇒ (mixture w P).real A ≤ B` (parts prob) | **Domination**: a weighted average of per-part masses is `≤` a common bound. The averaging step in the fuzzy-hypothesis reduction. **Proved, axiom-clean.** |
| `exists_real_ge_mixture` | `∑ i, w i = 1 ⇒ ∃ i, (mixture w P).real A ≤ (P i).real A` (parts prob, `[Nonempty ι]`) | **Witness extraction**: a finite average is `≤` its maximal component, so some part carries at least the mixture's mass. Pulls a single hard DGP out of a mixture hypothesis. **Proved, axiom-clean.** |

#### 12w.10. `Stat/Minimax/HellingerAffinity.lean` — affinity of two common-measure densities

The *third* route from a hypothesis pair to a TV bound, alongside Pinsker (§12w.3) and
χ² (§12w.5). It applies when the two candidate laws are given as densities `f, g`
against **one** common dominating measure — the situation in every explicit two-tilt
construction — so no Radon–Nikodym derivative and no mutual absolute-continuity premise
is needed. The chain is: compute the coordinate *affinity* `∫ √(f·g)`, tensorize it over
the product (`densityAffinity_pi`), convert the product defect into a *sum* of coordinate
defects (`one_sub_prod_le_sum`), and finally read it as a TV bound
(`tvDist_le_sqrt_two_mul_one_sub_affinity`). This is what makes an `n`-fold product prior
tractable: only one coordinate integral has to be computed by hand.

Complementary to `BretagnolleHuber.lean`, which bounds the *same* affinity number **from
below** by `½·exp(−KL)` but works through `rnDeriv`; use this file when you own the
densities and that file when you own the KL divergence.

<!-- GEN:Causalean.Stat.Minimax.HellingerAffinity -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.densityAffinity` | `MeasureTheory.Measure α → (α → ℝ) → (α → ℝ) → ℝ` | Given a measurable sample space, a measure on that space, and two real-valued functions on it, the Hellinger--Bhattacharyya affinity of the functions relative to the measure is the integral of the square root of their pointwise product. |
| `Stat.hellingerSqDensity` | `MeasureTheory.Measure α → (α → ℝ) → (α → ℝ) → ℝ` | Given a measurable sample space, a measure on that space, and two real-valued functions on it, the unhalved squared Hellinger discrepancy relative to the measure is the integral of the squared difference between their pointwise square roots. |
| `Stat.hellingerSqDensity_eq_two_mul_one_sub_affinity` | `MeasureTheory.Integrable f μ → MeasureTheory.Integrable g μ → 0 ≤ f → 0 ≤ g → ∫ (x : α), f x ∂μ = 1 → ∫ (x : α), g x ∂μ = 1 → Causalean.Stat.hellingerSqDensity μ f g = 2 * (1 - Causalean.Stat.densityAffinity μ f g)` | For two integrable nonnegative densities that each integrate to one, the unhalved squared Hellinger distance is exactly twice the affinity defect, i.e. twice one minus the affinity. This is the algebraic identity that lets an affinity computation be read as a Hellinger distance and back. |
| `Stat.tvDist_le_sqrt_two_mul_one_sub_affinity` | `MeasureTheory.Integrable f μ → MeasureTheory.Integrable g μ → 0 ≤ f → 0 ≤ g → ∫ (x : α), f x ∂μ = 1 → ∫ (x : α), g x ∂μ = 1 → Causalean.Stat.tvDist (μ.withDensity fun x => ENNReal.ofReal (f x)) (μ.withDensity fun x => ENNReal.ofReal (g x)) ≤ √(2 * (1 - Causalean.Stat.densityAffinity μ f g))` | Cauchy–Schwarz on the Hellinger affinity. For a dominating measure `μ` and functions `f`, `g` such that `f` is `μ`-integrable, `g` is `μ`-integrable, `f` is pointwise nonnegative, `g` is pointwise nonnegative, `f` integrates to `1` against `μ`, and `g` integrates to `1` against `μ` — so that `f dμ` and `g dμ` are probability densities — the total variation distance between the two weighted laws is at most the square root of twice the affinity defect, `√(2(1 − densityAffinity μ f g))`. This is the Cauchy–Schwarz half of the standard total-variation–Hellinger comparison, and it is what converts an affinity computation into a Le Cam two-point bound. |
| `Stat.one_sub_prod_le_sum` | `(∀ (i : ι), 0 ≤ a i) → (∀ (i : ι), a i ≤ 1) → 1 - ∏ i, a i ≤ ∑ i, (1 - a i)` | For finitely many numbers in the unit interval, the amount by which their product falls short of one is at most the total shortfall of the individual factors. Applied to coordinate affinities this is the union-bound step that turns a tensorized affinity into a sum of coordinate defects. |
| `Stat.densityAffinity_pi` | `(∀ (i : ι) (u : E i), 0 ≤ f i u) → (∀ (i : ι) (u : E i), 0 ≤ g i u) → (Causalean.Stat.densityAffinity (MeasureTheory.Measure.pi μ) (fun x => ∏ i, f i (x i)) fun x => ∏ i, g i (x i)) = ∏ i, ∫ (u : E i), √(f i u * g i u) ∂μ i` | Affinity tensorizes. On a finite product of σ-finite measure spaces, the affinity of two densities that each factor coordinatewise is the product of the coordinate affinities. This is what makes an `n`-fold product construction tractable: a single coordinate defect computation is enough. |
<!-- /GEN -->

#### 12w.11. `Stat/Minimax/HonestConfidenceSet.lean` — honest-set frontier tools

Finite-volume random-set and order lemmas for honest confidence sets: a Tonelli
identity for expected restricted volume, total-variation coverage transfer, and
generic liminf/limsup frontier-risk packaging over arbitrary model classes.

<!-- GEN:Causalean.Stat.Minimax.HonestConfidenceSet -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.restrictedSetVolume` | `Set ℝ → Set ℝ → ℝ` | For a parameter region on the real line and a set on the real line, the restricted set volume is the real-valued Lebesgue volume of their intersection, with infinite volume represented by zero. |
| `Stat.expected_restrictedSetVolume_eq_integral_inclusion` | `MeasurableSet {p \| p.2 ∈ C p.1} → MeasurableSet region → MeasureTheory.volume region ≠ ⊤ → ∫ (ω : Ω), Causalean.Stat.restrictedSetVolume region (C ω) ∂Q = ∫ (u : ℝ) in region, (Q {ω \| u ∈ C ω}).toReal` | The expected restricted volume of a jointly measurable random set equals the integral, over the parameter region, of its pointwise inclusion probabilities. |
| `Stat.coverage_tv_expectedRestrictedVolume_lower` | `(∀ (u : ℝ), MeasureTheory.IsProbabilityMeasure (Q u)) → (∀ u ∈ I, coverage ≤ ((Q u) {ω \| u ∈ C ω}).toReal) → (∀ u ∈ I, Causalean.Stat.tvDist (Q u) (Q reference) ≤ tv) → MeasurableSet {p \| p.2 ∈ C p.1} → MeasurableSet region → MeasureTheory.volume region ≠ ⊤ → MeasurableSet I → I ⊆ region → (MeasureTheory.volume I).toReal * (coverage - tv) ≤ ∫ (ω : Ω), Causalean.Stat.restrictedSetVolume region (C ω) ∂Q reference` | Coverage-to-expected-restricted-volume bound. For a family of laws `Q u` indexed by `u : ℝ`, a random set `C`, a subset `I` of a parameter region `region`, and a reference point `reference`, suppose every `Q u` is a probability measure, `C` covers `u` with probability at least `coverage`, for every `u` in `I`, `Q u` is within total variation `tv` of the reference law `Q reference`, for every `u` in `I`, the graph `{(ω, u) \| u ∈ C ω}` is measurable, `region` is measurable, `region` has finite Lebesgue volume, `I` is measurable, and `I` is contained in `region`. Then the expected restricted volume of `C` under the reference law `Q reference` is at least `(volume I) · (coverage − tv)`. |
| `Stat.classCoverage_liminf` | `Filter.Tendsto delta Filter.atTop (nhds 0) → (∀ᶠ (n : ℕ) in Filter.atTop, ∃ P, cls n P) → (∀ (n : ℕ) (P : Model), cls n P → 0 ≤ coverage n P ∧ coverage n P ≤ 1) → (∀ (n : ℕ) (P : Model), cls n P → 1 - alpha - delta n ≤ coverage n P) → 1 - alpha ≤ Filter.liminf (fun n => ⨅ P, coverage n ↑P) Filter.atTop` | Pointwise coverage rows with a vanishing uniform error imply asymptotic uniform coverage over any eventually inhabited sequence of model classes. |
| `Stat.inverseSqrtCap_anti` | `0 < t0 → t0 ≤ t → min 1 (t ^ (-1 / 2)) ≤ min 1 (t0 ^ (-1 / 2))` | The capped inverse-square-root rate is antitone on positive strengths. |
| `Stat.classFrontierRisk` | `(ℕ → Model → Prop) → (ℕ → Model → ℝ) → (ℕ → Model → ℝ) → ℝ → ℝ` | For a sequence of model classes, a real-valued strength and a real-valued expected-length criterion for each sample size and model, and a real strength threshold, the class frontier risk is the limit superior, across sample sizes, of the supremum expected length over the models in that class whose strength is at least the threshold. |
| `Stat.classFrontierRisk_le` | `0 ≤ C0 → 0 < t0 → (∀ (n : ℕ) (P : Model), cls n P → 0 ≤ expectedLength n P) → (∀ (n : ℕ) (P : Model), cls n P → expectedLength n P ≤ C0 * min 1 (strength n P ^ (-1 / 2))) → Causalean.Stat.classFrontierRisk cls strength expectedLength t0 ≤ C0 * min 1 (t0 ^ (-1 / 2))` | A pointwise capped inverse-square-root expected-length bound passes through both the class supremum and asymptotic limsup at the threshold value. |
| `Stat.coverageInfOrOne` | `(ι → ℝ) → ℝ` | For a real-valued criterion indexed by an arbitrary collection, the worst-case criterion with the empty-collection convention is its infimum when the collection is nonempty and is one when it is empty. |
| `Stat.coverageInfOrOne_of_nonempty` | `Causalean.Stat.coverageInfOrOne f = ⨅ i, f i` | On a nonempty model class, worst-case coverage with the empty-class convention is the ordinary infimum of coverage across the class. |
| `Stat.coverageInfOrOne_of_isEmpty` | `Causalean.Stat.coverageInfOrOne f = 1` | On an empty model class, worst-case coverage with the empty-class convention is one, expressing that the coverage requirement is vacuously satisfied. |
<!-- /GEN -->

#### 12w.12. `Stat/Minimax/FiniteSquaredLoss/` — finite bounded squared-loss minimax attainment

This proof chain gives a finite decision-problem minimax theorem for dependent finite
observation spaces. It uses the existing `FiniteDesign` expectation and `minimaxValue`
interfaces, derives the conditional Jensen mixing step for squared loss, and returns an
ordinary randomized procedure together with an attaining least-favorable finite prior.

##### 12w.12a. `Stat/Minimax/FiniteSquaredLoss/PosteriorBarycenter.lean` — dependent posterior and barycenter bounds

This module gives finite Bayesian lower certificates and conditional-barycenter upper
certificates when each design has its own observation type. Guarded posterior means and
barycenters make both certificates valid without full-support assumptions.

<!-- GEN:Causalean.Stat.Minimax.FiniteSquaredLoss.PosteriorBarycenter -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Minimax.FiniteSquaredLoss.Model` | `(Theta : Type u_1) → (B : Type u_2) → [Fintype Theta] → [Fintype B] → (X : B → Type u_3) → [(b : B) → Fintype (X b)] → Type (max (max u_1 u_2) u_3)` | A dependent finite experiment assigns, at every state and design index, a nonnegative normalized mass function on that design's observation type, together with a real target. |
| `Stat.Minimax.FiniteSquaredLoss.Model.predictiveMass` | `Causalean.Experimentation.DesignBased.FiniteDesign Theta → (b : B) → X b → ℝ` | Given a dependent finite experiment, a prior probability design over states, a design index, and an observation in that design's observation space, the prior predictive mass is the sum over states of prior probability times the conditional probability of that observation. |
| `Stat.Minimax.FiniteSquaredLoss.Model.sum_predictiveMass` | `∑ x, M.predictiveMass nu b x = 1` | The prior predictive probabilities within a fixed design sum to one. |
| `Stat.Minimax.FiniteSquaredLoss.Model.predictiveMass_nonneg` | `0 ≤ M.predictiveMass nu b x` | Every prior predictive probability is nonnegative. |
| `Stat.Minimax.FiniteSquaredLoss.Model.predictiveTarget` | `Causalean.Experimentation.DesignBased.FiniteDesign Theta → (b : B) → X b → ℝ` | Given a dependent finite experiment, a prior probability design over states, a design index, and an observation in that design's observation space, the predictive target numerator is the sum over states of prior probability times observation likelihood times the state's target. |
| `Stat.Minimax.FiniteSquaredLoss.Model.posteriorMean` | `Causalean.Experimentation.DesignBased.FiniteDesign Theta → (b : B) → X b → ℝ` | Given a dependent finite experiment, a prior probability design over states, a design index, and an observation in that design's observation space, the guarded posterior mean is the predictive target numerator divided by predictive mass when that mass is nonzero, and is zero when it is zero. |
| `Stat.Minimax.FiniteSquaredLoss.Model.joint_eq_zero_of_predictiveMass_eq_zero` | `M.predictiveMass nu b x = 0 → ∀ (theta : Theta), nu.p theta * M.P theta b x = 0` | A zero-mass predictive observation has zero joint probability in every state. |
| `Stat.Minimax.FiniteSquaredLoss.Model.predictiveTarget_eq_zero_of_predictiveMass_eq_zero` | `M.predictiveMass nu b x = 0 → M.predictiveTarget nu b x = 0` | A zero-mass predictive observation has zero target-weighted joint probability. |
| `Stat.Minimax.FiniteSquaredLoss.Model.predictiveMass_mul_posteriorMean` | `M.predictiveMass nu b x * M.posteriorMean nu b x = M.predictiveTarget nu b x` | Predictive probability times the guarded posterior mean equals the target-weighted joint probability, including for null observations. |
| `Stat.Minimax.FiniteSquaredLoss.Model.posteriorResidual` | `Causalean.Experimentation.DesignBased.FiniteDesign Theta → B → ℝ` | Given a dependent finite experiment, a prior probability design over states, and a design index, the posterior residual is the sum, over states and observations, of prior probability times observation likelihood times squared deviation of the guarded posterior mean from the state's target. |
| `Stat.Minimax.FiniteSquaredLoss.Model.posteriorResidual_nonneg` | `0 ≤ M.posteriorResidual nu b` | Every design-specific posterior residual risk is nonnegative. |
| `Stat.Minimax.FiniteSquaredLoss.Model.squaredRisk_eq_posteriorResidual_add` | `∑ theta, nu.p theta * ∑ x, M.P theta b x * (delta x - M.tau theta) ^ 2 = M.posteriorResidual nu b + ∑ x, M.predictiveMass nu b x * (delta x - M.posteriorMean nu b x) ^ 2` | The prior risk of any decision rule equals posterior residual risk plus its predictive-probability-weighted squared distance from the posterior mean. |
| `Stat.Minimax.FiniteSquaredLoss.Model.squaredRisk_posteriorMean` | `∑ theta, nu.p theta * ∑ x, M.P theta b x * (M.posteriorMean nu b x - M.tau theta) ^ 2 = M.posteriorResidual nu b` | The guarded posterior mean attains exactly the design-specific posterior residual risk. |
| `Stat.Minimax.FiniteSquaredLoss.Model.posteriorResidual_le_squaredRisk` | `M.posteriorResidual nu b ≤ ∑ theta, nu.p theta * ∑ x, M.P theta b x * (delta x - M.tau theta) ^ 2` | The posterior residual risk is no greater than the prior squared-loss risk of any real decision rule for the same design. |
| `Stat.Minimax.FiniteSquaredLoss.Model.iInf_squaredRisk_eq_posteriorResidual` | `⨅ delta, ∑ theta, nu.p theta * ∑ x, M.P theta b x * (delta x - M.tau theta) ^ 2 = M.posteriorResidual nu b` | The smallest prior squared-loss risk over all real decision rules for a fixed design equals its posterior residual risk. |
| `Stat.Minimax.FiniteSquaredLoss.Model.sInf_posteriorResidual_le_priorRisk` | `sInf (Set.range (M.posteriorResidual nu)) ≤ ∑ theta, nu.p theta * Causalean.Stat.Minimax.FiniteSquaredLoss.risk M.P M.tau q theta` | The least posterior residual across designs is no greater than the prior-average risk of any bounded dependent procedure. |
| `Stat.Minimax.FiniteSquaredLoss.Model.sInf_posteriorResidual_le_minimaxValue` | `l ≤ u → sInf (Set.range (M.posteriorResidual nu)) ≤ Causalean.Stat.minimaxValue (Causalean.Stat.Minimax.FiniteSquaredLoss.risk M.P M.tau)` | An ordered action interval makes the least design-specific posterior residual a lower bound on the dependent experiment's bounded squared-loss minimax value. |
| `Stat.Minimax.FiniteSquaredLoss.conditionalBarycenter` | `Causalean.Experimentation.DesignBased.FiniteDesign B → ((b : B) → X b → G → ℝ) → (G → ℝ) → ℝ → (b : B) → X b → ℝ` | Given a probability design over design indices, grid weights for each design index, observation, and grid action, real grid actions, a default action, a design index, and an observation in that design's observation space, the guarded conditional barycenter is the weighted average of grid actions divided by that design's probability when it is positive, and is the default action when it is zero. |
| `Stat.Minimax.FiniteSquaredLoss.weight_eq_zero_of_designMass_eq_zero` | `(∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → ∀ {b : B}, pi.p b = 0 → ∀ (x : X b) (g : G), w b x g = 0` | Nonnegative grid weights that sum to the design probability are zero individually whenever that design has zero probability. |
| `Stat.Minimax.FiniteSquaredLoss.conditionalBarycenter_mem_Icc` | `(∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → ∀ {l u : ℝ}, d ∈ Set.Icc l u → (∀ (g : G), gamma g ∈ Set.Icc l u) → ∀ (b : B) (x : X b), Causalean.Stat.Minimax.FiniteSquaredLoss.conditionalBarycenter pi w gamma d b x ∈ Set.Icc l u` | Nonnegative grid weights that sum to the design probability, with the null-fiber default inside a closed interval and all grid actions inside that interval, produce a guarded conditional barycenter in the same interval. |
| `Stat.Minimax.FiniteSquaredLoss.designMass_mul_conditionalBarycenter_sq_le` | `(∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → ∀ (b : B) (x : X b), pi.p b * (Causalean.Stat.Minimax.FiniteSquaredLoss.conditionalBarycenter pi w gamma d b x - t) ^ 2 ≤ ∑ g, w b x g * (gamma g - t) ^ 2` | Nonnegative grid weights that sum to the design probability make design probability times the guarded barycenter's squared error no greater than the corresponding weighted grid-action squared error. |
| `Stat.Minimax.FiniteSquaredLoss.Model.conditionalBarycenter_risk_le` | `(∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → ∀ (theta : Theta), ∑ b, pi.p b * ∑ x, M.P theta b x * (Causalean.Stat.Minimax.FiniteSquaredLoss.conditionalBarycenter pi w gamma d b x - M.tau theta) ^ 2 ≤ ∑ b, ∑ x, ∑ g, M.P theta b x * w b x g * (gamma g - M.tau theta) ^ 2` | Nonnegative grid weights that sum to each design probability make the statewise risk of the guarded conditional barycenter no greater than the randomized grid-action risk. |
| `Stat.Minimax.FiniteSquaredLoss.barycenterProcedure` | `(pi : Causalean.Experimentation.DesignBased.FiniteDesign B) → [inst_2 : (b : B) → Fintype (X b)] → (w : (b : B) → X b → G → ℝ) → (gamma : G → ℝ) → (d : ℝ) → (∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → {l u : ℝ} → d ∈ Set.Icc l u → (∀ (g : G), gamma g ∈ Set.Icc l u) → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u` | Given a probability design over design indices, grid weights, real grid actions, a default action, nonnegative grid weights, weights that sum to the corresponding design probability, a default action in the closed interval from $l$ to $u$, and grid actions all in that interval, the barycenter procedure is the bounded finite procedure using that design and the guarded conditional barycenter as its decision rule. |
| `Stat.Minimax.FiniteSquaredLoss.Model.risk_barycenterProcedure_le` | `Causalean.Stat.Minimax.FiniteSquaredLoss.risk M.P M.tau (Causalean.Stat.Minimax.FiniteSquaredLoss.barycenterProcedure pi w gamma d hw hocc hd hgamma) theta ≤ ∑ b, ∑ x, ∑ g, M.P theta b x * w b x g * (gamma g - M.tau theta) ^ 2` | Nonnegative grid weights that sum to each design probability, with the null-fiber default inside the action interval and all grid actions inside that interval, make the bounded barycenter procedure's statewise risk no greater than the randomized grid-action risk. |
| `Stat.Minimax.FiniteSquaredLoss.Model.minimaxValue_le_of_randomizedGridRisk` | `(∀ (b : B) (x : X b) (g : G), 0 ≤ w b x g) → (∀ (b : B) (x : X b), ∑ g, w b x g = pi.p b) → ∀ {l u c : ℝ}, d ∈ Set.Icc l u → (∀ (g : G), gamma g ∈ Set.Icc l u) → (∀ (theta : Theta), ∑ b, ∑ x, ∑ g, M.P theta b x * w b x g * (gamma g - M.tau theta) ^ 2 ≤ c) → Causalean.Stat.minimaxValue (Causalean.Stat.Minimax.FiniteSquaredLoss.risk M.P M.tau) ≤ c` | Nonnegative grid weights that sum to each design probability, with the null-fiber default inside the action interval, all grid actions inside that interval, and randomized grid-action risk uniformly bounded across states, imply the dependent experiment's bounded squared-loss minimax value is no greater than that bound. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Minimax.FiniteSquaredLoss.Core -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Minimax.FiniteSquaredLoss.Procedure` | `(X : R → Type u_4) → [(r : R) → Fintype (X r)] → ℝ → ℝ → Type (max u_2 u_4)` | A procedure consists of a finite randomized design and, for every selected design and dependent observation, an action in the prescribed closed interval. |
| `Stat.Minimax.FiniteSquaredLoss.risk` | `(Theta → (r : R) → X r → ℝ) → (Theta → ℝ) → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u → Theta → ℝ` | Given state-specific observation likelihoods, a real target for each state, a bounded randomized procedure, and a state, the statewise squared-loss risk is the procedure-design-weighted sum of likelihood-weighted squared differences between the selected action and that state's target. |
| `Stat.Minimax.FiniteSquaredLoss.risk_nonneg` | `(∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∀ (q : Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u) (theta : Theta), 0 ≤ Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau q theta` | When the likelihood coefficients are nonnegative, every bounded finite procedure has nonnegative squared-loss risk in each state. |
| `Stat.Minimax.FiniteSquaredLoss.procedure_nonempty` | `l ≤ u → Nonempty (Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u)` | A nonempty design space and a nonempty action interval have a bounded finite randomized procedure, even when some observation spaces are empty. |
| `Stat.Minimax.FiniteSquaredLoss.Ambient` | `(R : Type u_4) → (R → Type u_5) → Type (max u_4 u_4 u_5)` | Given a set of design indices and an observation space for each design index, the ambient coordinate space consists of one real design-weight vector and one real action coordinate for every design-index and observation pair. |
| `Stat.Minimax.FiniteSquaredLoss.procedureSet` | `(X : R → Type u_4) → ℝ → ℝ → Set (Causalean.Stat.Minimax.FiniteSquaredLoss.Ambient R X)` | Given a family of finite observation spaces indexed by design, a lower action bound, and an upper action bound, the feasible procedure-coordinate set contains exactly those ambient coordinates whose design weights form a probability distribution and whose action coordinates all lie in the closed interval from the lower to the upper bound. |
| `Stat.Minimax.FiniteSquaredLoss.Procedure.toAmbient` | `Causalean.Stat.Minimax.FiniteSquaredLoss.Ambient R X` | Given a bounded finite randomized procedure, its ambient Euclidean coordinates are its design probabilities together with its action at every design-index and observation pair. |
| `Stat.Minimax.FiniteSquaredLoss.Procedure.toAmbient_mem` | `q.toAmbient ∈ Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u` | The Euclidean coordinates of every bounded finite procedure belong to the feasible coordinate set. |
| `Stat.Minimax.FiniteSquaredLoss.Procedure.ofAmbient` | `z ∈ Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u` | Given an ambient coordinate point that belongs to the feasible procedure-coordinate set, the corresponding bounded finite procedure uses its design-weight coordinates as design probabilities and its action coordinates as bounded decisions. |
| `Stat.Minimax.FiniteSquaredLoss.Procedure.toAmbient_ofAmbient` | `(Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure.ofAmbient hz).toAmbient = z` | Turning a feasible coordinate point into a procedure and back recovers that coordinate point. |
| `Stat.Minimax.FiniteSquaredLoss.rawRisk` | `(Theta → (r : R) → X r → ℝ) → (Theta → ℝ) → Causalean.Stat.Minimax.FiniteSquaredLoss.Ambient R X → Theta → ℝ` | Given state-specific observation likelihoods, a real target for each state, an ambient coordinate point, and a state, the raw squared-loss risk is the design-coordinate-weighted sum of likelihood-weighted squared differences between action coordinates and that state's target. |
| `Stat.Minimax.FiniteSquaredLoss.rawRisk_toAmbient` | `Causalean.Stat.Minimax.FiniteSquaredLoss.rawRisk P tau q.toAmbient theta = Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau q theta` | For given likelihood coefficients, the ambient polynomial representation of a procedure's risk equals its public squared-loss risk. |
| `Stat.Minimax.FiniteSquaredLoss.procedureSet_nonempty` | `l ≤ u → (Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u).Nonempty` | A nonempty design space and a nonempty action interval make the feasible Euclidean procedure set nonempty. |
| `Stat.Minimax.FiniteSquaredLoss.convex_procedureSet` | `l ≤ u → Convex ℝ (Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u)` | An action interval with ordered endpoints makes the feasible Euclidean procedure set convex. |
| `Stat.Minimax.FiniteSquaredLoss.isCompact_procedureSet` | `IsCompact (Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u)` | The feasible Euclidean procedure set is compact: it is the product of a finite probability simplex and finitely many closed action intervals. |
| `Stat.Minimax.FiniteSquaredLoss.continuous_rawRisk` | `Continuous fun z => Causalean.Stat.Minimax.FiniteSquaredLoss.rawRisk P tau z theta` | For fixed likelihood coefficients and targets, the ambient squared-loss risk in any state varies continuously with the procedure coordinates. |
| `Stat.Minimax.FiniteSquaredLoss.riskVector` | `(Theta → (r : R) → X r → ℝ) → (Theta → ℝ) → Causalean.Stat.Minimax.FiniteSquaredLoss.Ambient R X → Theta → ℝ` | Given state-specific observation likelihoods, a real target for each state, and an ambient coordinate point, the risk vector assigns to every state its raw squared-loss risk at that coordinate point. |
| `Stat.Minimax.FiniteSquaredLoss.continuous_riskVector` | `Continuous (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau)` | For fixed likelihood coefficients, the vector collecting each state's ambient squared-loss risk varies continuously with the procedure coordinates. |
| `Stat.Minimax.FiniteSquaredLoss.isCompact_riskVector_image` | `IsCompact (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u)` | Under given likelihood coefficients, the set of risk vectors attainable by feasible finite procedures is compact. |
| `Stat.Minimax.FiniteSquaredLoss.finiteDesign_mem_stdSimplex` | `nu.p ∈ stdSimplex ℝ Theta` | Every finite randomized design has a mass function in the standard probability simplex. |
| `Stat.Minimax.FiniteSquaredLoss.finiteDesignOfSimplex` | `w ∈ stdSimplex ℝ Theta → Causalean.Experimentation.DesignBased.FiniteDesign Theta` | Given a vector of weights over the finite state space that belongs to the standard probability simplex, the corresponding finite design is the probability design whose mass function is that vector. |
| `Stat.Minimax.FiniteSquaredLoss.bayesPayoff` | `(Theta → ℝ) → (Theta → ℝ) → ℝ` | Given a real-valued risk vector over states and a vector of prior weights, the finite Bayes payoff is the sum over states of each prior weight times its risk-vector value. |
| `Stat.Minimax.FiniteSquaredLoss.bayesPayoff_eq_E` | `Causalean.Stat.Minimax.FiniteSquaredLoss.bayesPayoff z nu.p = nu.E z` | Pairing a risk vector with a finite prior equals that prior's existing finite-design expectation. |
| `Stat.Minimax.FiniteSquaredLoss.continuous_bayesPayoff` | `Continuous fun p => Causalean.Stat.Minimax.FiniteSquaredLoss.bayesPayoff p.1 p.2` | The finite Bayes payoff varies continuously with both the risk vector and the prior weights. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Minimax.FiniteSquaredLoss.Mixing -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Minimax.FiniteSquaredLoss.mixProcedure` | `l ≤ u → 0 ≤ t → t ≤ 1 → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u → Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u` | Given a finite set of design points, a finite observation set at each design point, ordered action bounds, a mixing weight no smaller than zero, the same weight no larger than one, and two feasible randomized procedures, the mixed feasible randomized procedure assigns the convex combination of their design masses at each design point. At a point with positive mixed mass it averages their actions using the corresponding posterior mixture weights, and at a point with zero mixed mass it uses the lower action bound. |
| `Stat.Minimax.FiniteSquaredLoss.risk_mixProcedure_le` | `(∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∀ {l u t : ℝ} (hlu : l ≤ u) (ht0 : 0 ≤ t) (ht1 : t ≤ 1) (q₀ q₁ : Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u) (theta : Theta), Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau (Causalean.Stat.Minimax.FiniteSquaredLoss.mixProcedure hlu ht0 ht1 q₀ q₁) theta ≤ t * Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau q₀ theta + (1 - t) * Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau q₁ theta` | With nonnegative likelihood coefficients, an ordered action interval, and a mixing weight between zero and one, conditional mixing produces a procedure whose statewise squared-loss risk is no greater than the corresponding mixture of the two original risks. No normalization of the coefficients is required. |
| `Stat.Minimax.FiniteSquaredLoss.dominatedRiskVectors` | `(Theta → (r : R) → X r → ℝ) → (Theta → ℝ) → ℝ → ℝ → Set (Theta → ℝ)` | Given a parameter space, a finite set of design points, a finite observation set at each design point, likelihood coefficients, a target value for each parameter, and two action bounds, the set of dominated risk vectors consists of exactly those real-valued functions on the parameter space for which there exists a feasible finite randomized procedure whose squared-loss risk is no greater at every parameter. |
| `Stat.Minimax.FiniteSquaredLoss.convex_dominatedRiskVectors` | `(∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∀ {l u : ℝ}, l ≤ u → Convex ℝ (Causalean.Stat.Minimax.FiniteSquaredLoss.dominatedRiskVectors P tau l u)` | With nonnegative likelihood coefficients and an ordered action interval, the set of risk vectors dominated by an ordinary finite procedure is convex. |
| `Stat.Minimax.FiniteSquaredLoss.riskVector_image_subset_dominated` | `Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u ⊆ Causalean.Stat.Minimax.FiniteSquaredLoss.dominatedRiskVectors P tau l u` | Under given likelihood coefficients, every risk vector attained by feasible ambient coordinates is dominated by the risk of the associated ordinary finite procedure. |
| `Stat.Minimax.FiniteSquaredLoss.exists_procedure_risk_le_of_mem_convexHull` | `(∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∀ {l u : ℝ}, l ≤ u → ∀ {z : Theta → ℝ}, z ∈ (convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u) → ∃ q, ∀ (theta : Theta), Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau q theta ≤ z theta` | With nonnegative likelihood coefficients and an ordered action interval, every convex combination of attainable risk vectors is coordinatewise dominated by the risk vector of an ordinary finite procedure. |
| `Stat.Minimax.FiniteSquaredLoss.isCompact_convexHull_riskVectors` | `IsCompact ((convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u))` | Under given likelihood coefficients, the convex hull of the attainable finite risk vectors is compact. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Minimax.FiniteSquaredLoss.Saddle -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Minimax.FiniteSquaredLoss.convexified_risk_game_has_saddle` | `l ≤ u → ∃ zstar ∈ (convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u), ∃ nustar ∈ stdSimplex ℝ Theta, IsSaddlePointOn ((convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u)) (stdSimplex ℝ Theta) Causalean.Stat.Minimax.FiniteSquaredLoss.bayesPayoff zstar nustar` | For nonempty finite state and design spaces, given likelihood coefficients and an ordered action interval, the convexified risk game has an attaining saddle point consisting of a risk vector and a finite prior. |
| `Stat.Minimax.FiniteSquaredLoss.saddle_value_eq_minimaxValue` | `(∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∀ {l u : ℝ}, l ≤ u → ∀ {zstar nustar : Theta → ℝ}, zstar ∈ (convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u) → nustar ∈ stdSimplex ℝ Theta → IsSaddlePointOn ((convexHull ℝ) (Causalean.Stat.Minimax.FiniteSquaredLoss.riskVector P tau '' Causalean.Stat.Minimax.FiniteSquaredLoss.procedureSet X l u)) (stdSimplex ℝ Theta) Causalean.Stat.Minimax.FiniteSquaredLoss.bayesPayoff zstar nustar → Causalean.Stat.Minimax.FiniteSquaredLoss.bayesPayoff zstar nustar = Causalean.Stat.minimaxValue (Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau)` | Given nonnegative likelihood coefficients, an ordered action interval, and a saddle point in the feasible convexified game, its Bayes payoff equals Causalean's minimax value over ordinary bounded finite procedures. |
| `Stat.Minimax.FiniteSquaredLoss.finite_bounded_squared_loss_has_saddle` | `l ≤ u → (∀ (theta : Theta) (r : R) (x : X r), 0 ≤ P theta r x) → ∃ qstar deltastar nu, (∀ (theta : Theta), Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau { design := qstar, decision := deltastar } theta ≤ Causalean.Stat.minimaxValue (Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau)) ∧ ∀ (qprime : Causalean.Stat.Minimax.FiniteSquaredLoss.Procedure X l u), Causalean.Stat.minimaxValue (Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau) ≤ nu.E (Causalean.Stat.Minimax.FiniteSquaredLoss.risk P tau qprime)` | Finite bounded squared-loss minimax theorem. In nonempty finite state and design spaces, an ordered real action interval and nonnegative likelihood coefficients guarantee an ordinary randomized design, bounded decision rule, and finite least-favorable prior attaining both minimax saddle inequalities. No likelihood normalization, observation-space nonemptiness, or target-in-interval assumption is required. |
<!-- /GEN -->

## 12ep. `Stat/EmpiricalProcess/` — Uniform LLN (Glivenko–Cantelli) & extremum-estimator consistency

Empirical-process layer written for the econometrics workflow (van der Vaart
1998, Ch. 19; Newey & McFadden 1994, §2). A *function class* is an indexed family
`f : ι → X → ℝ`; the empirical process measures how far the sample means `Pₙ f_i`
sit from the population means `P f_i = ∫ f_i dP`, **uniformly over the class**.
This is the uniform law of large numbers that the M-estimation CLT stack
(`Stat/MEstimation/`) previously had to assume. All declarations **proved,
sorry-free**.

### 12ep.a `Stat/EmpiricalProcess/Basic.lean` — vocabulary

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.empiricalProcess f i n` | `def`, `Ω → ℝ` | The centred-scaled process `√n · (Pₙ f_i − P f_i)` at class member `i` — the object whose weak limit (a Gaussian process) is the subject of Donsker theory. |
| `IIDSample.supDeviation f n` | `def`, `Ω → ℝ` | Finite-class sup deviation `⨆ i, \|Pₙ f_i − P f_i\|` (meaningful as a statistic when `ι` is finite). |
| `GlivenkoCantelli S f` | `def`, `Prop` | **(Weak) uniform LLN**: for every `ε > 0`, `μ {ω | ∃ i, ε ≤ \|Pₙ f_i − P f_i\|} → 0`. Stated existentially (not via `⨆`) so it is meaningful for infinite/uncountable classes without lattice-junk artefacts. |
| `L1Bracketing f P ε` | `structure` | A finite `L¹(P)` `ε`-bracketing: `m` brackets `[lo j, hi j]` (`j : Fin m`) with integrable endpoints, `P`-mean width `∫ (hi j − lo j) dP ≤ ε`, and an `assign : ι → Fin m` sandwiching each member `lo (assign i) ≤ f i ≤ hi (assign i)`. |
| `HasL1Bracketing f P` | `def`, `Prop` | Finite `L¹(P)` bracketing numbers: an `ε`-bracketing exists for every `ε > 0`. The classical sufficient condition for Glivenko–Cantelli. |

### 12ep.b `Stat/EmpiricalProcess/GlivenkoCantelli.lean` — uniform LLN theorems

| Declaration | Statement | Description |
|---|---|---|
| `glivenkoCantelli_of_fintype` | `[Finite ι]`, each `f i` measurable & `P`-integrable ⇒ `GlivenkoCantelli S f` | A **finite** class of integrable functions is Glivenko–Cantelli. Union bound over coordinates, each vanishing by the pointwise WLLN (`IIDSample.sampleMean_tendsto_inProb`). **Proved.** |
| `glivenkoCantelli_of_hasL1Bracketing` | each `f i` measurable, `HasL1Bracketing f P` ⇒ `GlivenkoCantelli S f` | A class with **finite `L¹(P)` bracketing numbers** is Glivenko–Cantelli (van der Vaart 1998, Thm 19.4; Newey–McFadden Lemma 2.4). Each member is sandwiched in a bracket of mean-width `≤ ε/2`, so its deviation is controlled by the `2m` bracket-endpoint deviations plus `ε/2`; the endpoints vanish by the finite case. **Proved.** |

Private helpers: `tendsto_measure_zero_of_le` (measure squeeze), `integrable_pullback` (transfer `P`-integrability to `μ`), `dev_tendsto_zero` (single-statistic WLLN rephrased from the `edist`/`ℝ≥0∞` `TendstoInMeasure` to the real `\|·\|` event), `sampleMean_mono`.

### 12ep.c `Stat/EmpiricalProcess/MEstimatorConsistency.lean` — extremum-estimator consistency

| Declaration | Statement | Description |
|---|---|---|
| `mEstimator_consistent_of_glivenkoCantelli` | `GlivenkoCantelli S m` + sample-argmax domination over `θ₀` + well-separated population max ⇒ `∀ ε>0, μ {ω | ε ≤ dist (θ̂ₙ ω) θ₀} → 0` | **Consistency of extremum / M-estimators** (Newey–McFadden 1994, Thm 2.1; van der Vaart 1998, Thm 5.7). `θ̂ₙ` maximizing the sample criterion `θ ↦ Pₙ m(θ,·)` is consistent for the population maximizer `θ₀`. **Consumes** the `GlivenkoCantelli` engine: the uniform-convergence hypothesis is exactly that predicate. Proof is the classical `M(θ₀) − M(θ̂ₙ) ≤ 2·sup_θ\|Mₙ−M\|` sandwich versus the separation gap `η`. **Proved, sorry-free.** |
| `mEstimator_consistent_of_bracketing` | finite `L¹(P)` bracketing of `m` + argmax domination + well-separated max ⇒ consistency | **Bracketing headline corollary**: composes `glivenkoCantelli_of_hasL1Bracketing` with the consistency theorem, so a verifiable bracketing condition (instead of an assumed uniform LLN) yields consistency directly. **Proved, sorry-free.** |

### 12ep.d `Stat/EmpiricalProcess/Equicontinuity/` — stochastic equicontinuity & the `StochEquicontAt` reduction

The empirical-process / Donsker layer at the **parametric** scale, discharging the
`StochEquicontAt` hypothesis consumed by `zEstimator_clt` and
`gmm_asymptotically_linear`. **Key structural finding:** `StochEquicontAt`
(fixed `δ`, inner limit `0`, gap evaluated at the *random* estimator `θn`) is only
true when `θn →_p θ₀` — it secretly bundles consistency. The honest decomposition
is **class-level asymptotic equicontinuity (`AsymptoticEquicont`, independent of
`θn`) + consistency ⟹ `StochEquicontAt`**. The uniform-in-`n` control of the
empirical process comes from the variance identity `Var(√n(Pₙ−P)f) = Var_P f` via
Chebyshev — no CLT and no chaining for the base cases. All declarations below are
**proved, sorry-free, axiom-clean**. `E` is a finite-dimensional real inner-product
space (the `Z`-estimator parameter/value space).

`Process.lean` — vocabulary:

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.empProcVec S f n` | `def`, `Ω → E` | **Vector-valued centered empirical process** `Gₙ(f)(ω) = (√n)⁻¹ • Σ_{i<n} f(Zᵢ ω) − √n • ∫ f dP`; the `E`-valued analogue of `empiricalProcess`. |
| `IIDSample.empProcVec_eq_stochEquicont_gap` | `rfl` bridge | `empProcVec` of the score difference `ψ(θ,·)−ψ(θ₀,·)` equals the centered gap `R_n` of `StochEquicontAt`. |

`SecondMoment.lean` — the uniform-in-`n` Chebyshev workhorse:

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.measurable_empProcVec` | `Measurable (fun ω => empProcVec f n ω)` | Measurability of the process in `ω`. |
| `empProcVec_sq_lintegral_le` | `f ∈ L²(P)` ⇒ `∫⁻ ‖Gₙ(f)‖² dμ ≤ ENNReal.ofReal (∫ ‖f‖² dP)` | **Second-moment / variance identity at the empirical scale**, uniformly in `n`. Proved coordinate-wise: Parseval (`OrthonormalBasis.sum_sq_inner_right`) reduces `‖Gₙ(f)‖²` to a sum of scalar centered i.i.d. sums, each bounded by `Causalean.Mathlib.iid_centered_sum_sq_lintegral_le`. No CLT, no chaining. |
| `empProcVec_chebyshev` | `0<ε` ⇒ `μ {‖Gₙ(f)‖ > ε} ≤ ENNReal.ofReal (∫ ‖f‖² dP) / ENNReal.ofReal (ε²)` | **Chebyshev bound** (second moment + Markov, `meas_ge_le_lintegral_div`). The uniform-in-`n` modulus that drives equicontinuity: as `‖f‖_{L²(P)}` shrinks, the tail shrinks at a rate independent of sample size. |

`Modulus.lean` — the class-level predicate and the reduction:

| Declaration | Statement | Description |
|---|---|---|
| `AsymptoticEquicont ψ θ₀ P μ S` | `def`, `Prop` | **Class-level asymptotic equicontinuity** (the Donsker content, independent of any estimator): `∀ ε>0, ∀ η>0, ∃ δ>0, ∀ᶠ n, μ {ω | ∃ θ, ‖θ−θ₀‖<δ ∧ ε < ‖Gₙ(ψ(θ,·)−ψ(θ₀,·))(ω)‖} ≤ η`. Existential over `θ` (not a non-measurable `sup`) so outer-measure bookkeeping is automatic. |
| `empProcVec_atEstimator_tendsto_zero` | `AsymptoticEquicont` + `θn →_p θ₀` ⇒ `Gₙ(ψ(θn,·)−ψ(θ₀,·)) →_p 0` (unconditional) | The empirical process at the estimator vanishes — textbook union bound (split on `{‖θn−θ₀‖≥δ}` via consistency; on the complement the gap is witnessed by `θ=θn`). |
| `stochEquicontAt_of_asymptoticEquicont` | `AsymptoticEquicont` + `θn →_p θ₀` ⇒ `StochEquicontAt ψ θ₀ P μ S θn` | **The reduction.** Discharges the estimator-indexed `StochEquicontAt` hypothesis of `zEstimator_clt`/`gmm_asymptotically_linear` from the clean class-level Donsker property plus consistency. |
| `empProcVec_isLittleOp_of_L2` | deterministic `θn` with `∫ ‖ψ(θn,·)−ψ(θ₀,·)‖² dP → 0` ⇒ gap is `o_p(1)` | **Deterministic-curve equicontinuity** (non-vacuousness witness): direct from `empProcVec_chebyshev`, no chaining, no consistency. The base case certifying the `AsymptoticEquicont` machinery is satisfiable. |

`MaximalInequality.lean` — sub-exponential maximal inequality (chaining primitive):

| Declaration | Statement | Description |
|---|---|---|
| `HasSubexponentialMGF.measure_abs_ge_le` | `μ.real {ε ≤ \|X\|} ≤ 2 exp(−ε²/(2(v+bε)))` | Two-sided sub-exponential tail, from the one-sided `measure_ge_le` applied to `X` and `−X`. |
| `measure_exists_abs_ge_le` | finite family ⇒ `μ.real {∃ i∈t, ε ≤ \|Yᵢ\|} ≤ t.card · 2 exp(−ε²/(2(v+bε)))` | **Maximal inequality** for the max of finitely many mean-zero sub-exponential variables (union bound). The reusable primitive a bracketing/chaining maximal inequality is built from. |
| `measure_sup'_ge_le` | same bound for `μ.real {ε ≤ t.sup' (fun i => \|Yᵢ\|)}` | `sup'` form of the maximal inequality. |

**Remaining frontier.** Discharging `AsymptoticEquicont` for an *infinite* class
over a continuum of `θ` (genuine bracketing-entropy / Ossiander chaining, van der
Vaart 1998 Lemma 19.34) is the real Mathlib gap — the `√n` bracketing bias is
intrinsic and cannot be tamed without chaining (finite classes are trivially
equicontinuous). `MaximalInequality.lean` provides the single-scale primitive for
that future chaining argument.

### 12ep.e `Stat/EmpiricalProcess/CrossFitRate.lean` — conditional / cross-fit `O_p` rate

Paper-agnostic workhorse behind every cross-fit / DML "remainder-is-negligible"
step: the centered empirical mean of a (possibly cross-fit / conditional) score is
`O_p(n^{-1/2} · (second-moment)^{1/2})`, stated over an arbitrary score and
σ-field. Reuses the existing `O_p` predicate `Causalean.Stat.IsBigOp` (no second
predicate) and the conditional second-moment engine
`Causalean.Mathlib.iid_centered_sum_sq_lintegral_le`. The Markov "L²-envelope ⇒ `O_p`"
step (previously inlined privately in `Estimation/OrthogonalMoments/DMLCrossFit.lean`)
is extracted here as the reusable primitive. All declarations **proved, sorry-free,
axiom-clean**.

| Declaration | Statement | Description |
|---|---|---|
| `IsBigOp.mono_rate` | `0 ≤ rₙ ≤ sₙ`, `O_p(rₙ)` ⇒ `O_p(sₙ)` | **Weaken to a larger rate** — a larger envelope is a weaker statement. |
| `IsBigOp.scale_rate` | `c>0`, `O_p(c·rₙ)` ⇒ `O_p(rₙ)` | **Absorb a positive constant rate factor** into the witness `M`. |
| `IsBigOp.const_rate_collapse` | `0 ≤ N`, `O_p(fun _ => N)` ⇒ `O_p(fun _ => 1)` | **Collapse a fixed constant rate to the unit rate** (normalizes fold-sum `O_p(1)` conclusions). |
| `IsBigOp.const_mul` | `O_p(rₙ)` ⇒ `c·Xₙ = O_p(rₙ)` | **Constant multiple** of the random sequence keeps the rate. |
| `IsBigOp.add'` | `O_p(rₙ) + O_p(sₙ) = O_p(rₙ+sₙ)` (nonneg rates) | **Additivity at the sum rate** (`IsBigOp.add` is the `rₙ=sₙ` special case). |
| `IsBigOp.of_abs_le` | pointwise `\|Xₙ\| ≤ \|Yₙ\|`, `Yₙ = O_p(rₙ)` ⇒ `Xₙ = O_p(rₙ)` | **Domination rule** for stochastic big-O. |
| `IsBigOp.zero` | `0 = O_p(rₙ)` | Constant-zero sequence is stochastically bounded at any rate. |
| `IsBigOp.finset_sum` | finite sum of terms each `O_p(rₙ)` ⇒ sum is `O_p(rₙ)` | Finite-sum bookkeeping for stochastic big-O; constants absorb the finite index set. |
| `IsBigOp.mul` | `0≤rₙ`, `0≤sₙ`, `Xₙ=O_p(rₙ)`, `Yₙ=O_p(sₙ)` ⇒ `XₙYₙ=O_p(rₙsₙ)` | **Product rule** for stochastic big-O at two nonnegative rates. |
| `IsBigOp.of_sq_lintegral_le` | `Xₙ` a.e.-meas., `0 ≤ Vₙ`, `∫⁻ (Xₙ)² dμ ≤ Vₙ` ⇒ `Xₙ = O_p(√Vₙ)` | **The Markov primitive.** A deterministic second-moment envelope yields the matching `O_p` rate (Chebyshev: `μ{M√Vₙ<\|Xₙ\|} ≤ 1/M²`). The reusable core every cross-fit rate proof consumes. |
| `isBigOp_centered_crossFit_sum` | eval set `s n` ⊥ sub-σ-field `m_A n`, conditionally iid; score `g n` `(m_A n ⊗ σ_X)`-measurable & `L²(P)`; deterministic `Vₙ ≥ ∫_Ω ‖g n ω‖²_{L²(P)} dμ` ⇒ centered fold sum `= O_p(√Vₙ)` | **Lemma B — the conditional / cross-fit lift** (the deliverable). `∫ g n ω dP = 𝔼[g\|m_A n](ω)` is the conditional mean (eval fold ⊥ `m_A n`), so the sum is `√\|s n\|·(ℙₙg − 𝔼[g\|m_A n])`. Second moment via the engine, rate via `of_sq_lintegral_le`. Estimand-agnostic; weak-overlap clipped-AIPW is the first consumer. |
| `IIDSample.sampleMean_sub_sq_lintegral_le` | iid sample, `f ∈ L²(P)`, `0<n` ⇒ `∫⁻ (ℙₙf − ∫f dP)² dμ ≤ E_P[f²]/n` | **Lemma A second-moment bound.** `(1/√n)Σ(f(Zᵢ)−m) = √n(ℙₙf−m)`, so the engine bound divides by `n`. |
| `IIDSample.sampleMean_sub_meas_ge_le` | `0<n`, `t>0` ⇒ `μ{t ≤ \|ℙₙf − ∫f dP\|} ≤ E_P[f²]/(n·t²)` | **Chebyshev tail** for the centered sample mean (second-moment bound + Markov). |
| `IIDSample.sampleMean_sub_isBigOp` | iid sample, `f ∈ L²(P)` ⇒ `(ℙₙf − ∫f dP) = O_p(√(E_P[f²]/n))` | **Lemma A — the unconditional `O_p` rate.** Feeding the centered statistic `f−∫f` (whose `E_P[(·)²]=Var_P f`) gives the sharp `O_p(√(Var_P f/n))` form. The `(ℙₙ−P)φ^bd` consumer. |

## 12x. `Stat/UStatistic/` — U-statistics: Hoeffding decomposition, exact variance, and the CLT

The reusable statistical primitive behind variance estimators, rank/sign statistics,
and the higher-order influence-function estimators (§HOIF). The development has two
layers with a one-way bridge:

- **Order-`m` (the general theory).** `OrderM/` builds the fixed-order U-statistic on
  ordered injective `m`-tuples, its Hájek expansion, the exact order-`m` second moment,
  and — the **headline** — the fixed-order central limit theorem
  `uStatisticOrder_clt_of_regular` (`OrderM/CLT.lean`): a regular order-`m` U-statistic
  is `√n`-asymptotically normal with the Hájek variance `∫ ψ² dP`. Its one genuinely
  probabilistic input, negligibility of the higher-order remainder, is discharged from
  a first-order-degeneracy `L² ≤ C/n` keystone (`OrderM/FirstDegenKernel.lean` →
  `OrderM/RemainderSecondMoment.lean` → `OrderM/RemainderNegligible.lean`).
- **Order-2 (a thin `m = 2` face).** `Basic.lean`/`Variance.lean` keep the natural
  pairwise-kernel API (`h : X → X → ℝ`, off-diagonal `uStatistic`, `DegenKernel`, `ζ`)
  that the HOIF estimators consume. The order-2 *variance* now routes through the
  order-`m` `ExactVariance` internally (via `pairKernel`); the order-2 CLT is recovered
  as the `m = 2` specialization `uStatistic_clt_of_symmetric_via_orderM`
  (`OrderM/OrderTwo.lean`).

_Per-declaration tables below are generated from docstrings — do not hand-edit; run `npm run doc:gen`. Edit the Lean docstring to change a description._

### 12x.1. `Stat/UStatistic/Basic.lean` — order-2 Hoeffding decomposition

<!-- GEN:Causalean.Stat.UStatistic.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.uStatistic` | `(X → X → ℝ) → ℕ → Ω → ℝ` | For a sample space and an outcome space, each equipped with a σ-algebra, an independent and identically distributed sample with probability law $\mu$ and marginal law $P$, a real-valued two-argument kernel, and a nonnegative integer $n$, the order-two U-statistic is the random variable that, at each sample outcome, equals $(n(n-1))^{-1}$ times the sum of the kernel over all ordered distinct pairs among the first $n$ observations. |
| `Stat.uMean` | `(X → X → ℝ) → MeasureTheory.Measure X → ℝ` | For an outcome space equipped with a σ-algebra, a real-valued two-argument kernel, and a measure $P$ on that space, the population mean of the kernel is its iterated integral with respect to $P$ in both arguments. |
| `Stat.uProj` | `(X → X → ℝ) → MeasureTheory.Measure X → X → ℝ` | For an outcome space equipped with a σ-algebra, a real-valued two-argument kernel, and a measure $P$ on that space, the first centered Hoeffding projection assigns to each outcome $x$ the integral of the kernel with first argument $x$ under $P$, minus the population mean of the kernel. |
| `Stat.uDegen` | `(X → X → ℝ) → MeasureTheory.Measure X → X → X → ℝ` | For an outcome space equipped with a σ-algebra, a real-valued two-argument kernel, and a measure $P$ on that space, the degenerate second-order kernel assigns to outcomes $x$ and $y$ the original kernel value minus its population mean and minus its first centered Hoeffding projections at $x$ and $y$. |
| `Stat.hoeffding_decomp` | `h x y = Causalean.Stat.uMean h P + Causalean.Stat.uProj h P x + Causalean.Stat.uProj h P y + Causalean.Stat.uDegen h P x y` | Hoeffding decomposition (pointwise identity). For a kernel `h`, population law `P`, and points `x` and `y`, the kernel value decomposes as `h(x, y) = θ + h₁(x) + h₁(y) + g(x, y)`, the population mean plus the two first-order Hoeffding projections plus the degenerate second-order residual. |
| `Stat.uProj_integral_eq_zero` | `MeasureTheory.Integrable (fun x => ∫ (y : X), h x y ∂P) P → ∫ (x : X), Causalean.Stat.uProj h P x ∂P = 0` | The first projection integrates to zero: `∫ h₁ dP = 0`. |
| `Stat.uProj_integrable` | `MeasureTheory.Integrable (fun x => ∫ (y : X), h x y ∂P) P → MeasureTheory.Integrable (Causalean.Stat.uProj h P) P` | The first projection is integrable whenever `x ↦ ∫ h(x, ·) dP` is. |
| `Stat.uDegen_integral_right_eq_zero` | `MeasureTheory.Integrable (fun x => ∫ (y : X), h x y ∂P) P → ∀ (x : X), MeasureTheory.Integrable (fun y => h x y) P → ∫ (y : X), Causalean.Stat.uDegen h P x y ∂P = 0` | Degeneracy of the second-order kernel. For every fixed `x`, `∫ g(x, ·) dP = 0`. This is the defining property that makes the U-statistic's quadratic remainder negligible at the `√n` scale. |
<!-- /GEN -->

### 12x.2. `Stat/UStatistic/Hajek.lean` — generic `o_p` transfer helper

<!-- GEN:Causalean.Stat.UStatistic.Hajek -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.isLittleOp_of_eventuallyEq` | `Causalean.Stat.IsLittleOp g r μ → (∀ᶠ (n : ℕ) in Filter.atTop, f n = g n) → Causalean.Stat.IsLittleOp f r μ` | Transfer of `o_p` along eventual equality. For sequences of random variables `f, g : ℕ → Ω → ℝ` and a rate `r : ℕ → ℝ`, if `g` is `o_p(r)` under `μ` and `f n = g n` for all sufficiently large `n`, then `f` is also `o_p(r)` under `μ`. |
<!-- /GEN -->

### 12x.3. `Stat/UStatistic/Variance.lean` — order-2 exact variance (thin `m = 2` face for HOIF)

<!-- GEN:Causalean.Stat.UStatistic.Variance -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.injectiveTupleCount_two_eq_offDiag_card` | `Causalean.Stat.injectiveTupleCount 2 n = ↑(Finset.range n).offDiag.card` | The number of ordered injective pairs from a sample of size `n` equals the number of off-diagonal ordered pairs drawn from that sample. |
| `Stat.injectiveTupleCount_two_eq_mul_sub_one` | `2 ≤ n → Causalean.Stat.injectiveTupleCount 2 n = ↑n * (↑n - 1)` | For a sample of size at least two, the number of ordered injective pairs is the sample size times one less than the sample size. |
| `Stat.DegenKernel` | `MeasureTheory.Measure X → (X → X → ℝ) → Prop` | Degenerate order-2 kernel. A kernel `g : X → X → ℝ` is doubly degenerate under the measure `P` when `g` is measurable as a function on $X \times X$, `g` is symmetric, $g(x,y) = g(y,x)$, integrating `g` over its second argument against `P` gives zero for every fixed first argument, and `g` is square-integrable under the product measure $P \times P$. |
| `Stat.DegenKernel.integrable` | `Causalean.Stat.DegenKernel P g → MeasureTheory.Integrable (fun p => g p.1 p.2) (P.prod P)` | `g` is L¹ on a finite product measure, from L². |
| `Stat.DegenKernel.deg'` | `Causalean.Stat.DegenKernel P g → ∀ (y : X), ∫ (x : X), g x y ∂P = 0` | Left degeneracy, from symmetry and right degeneracy. |
| `Stat.DegenKernel.toOrderDegenKernel` | `Causalean.Stat.DegenKernel P g → Causalean.Stat.OrderDegenKernel P (Causalean.Stat.pairKernel g)` | Bridge to the order-`m` theory (`m = 2`). If the two-argument kernel `g : X → X → ℝ` is degenerate: measurable, symmetric, square-integrable under `P × P`, and with zero conditional mean in each coordinate, then the paired order-2 kernel `pairKernel g`, defined by `z ↦ g(z 0, z 1)`, is completely degenerate in the order-`m` sense. |
| `Stat.IIDSample.zeta` | `MeasureTheory.Measure X → (X → X → ℝ) → ℝ` | For a measurable observation space, a measure on that space, and a real-valued kernel of two observations, the kernel second moment is $\iint g(x,y)^2\,dP(x)\,dP(y)$, evaluated under two independent draws from the given measure. |
| `Stat.IIDSample.zeta_eq_zetaOrder` | `Causalean.Stat.IIDSample.zeta P g = Causalean.Stat.IIDSample.zetaOrder P (Causalean.Stat.pairKernel g)` | The order-2 second moment `ζ` equals the order-`m` second moment `ζ_m` of the paired kernel: `∬ g² dP dP = ∫ (pairKernel g)² dP²`. A change of variables along the `Fin 2 → X` ≃ `X × X` measure equivalence. |
| `Stat.IIDSample.zeta_nonneg` | `0 ≤ Causalean.Stat.IIDSample.zeta P g` | `ζ ≥ 0`. |
| `Stat.IIDSample.integral_offDiag_sum_sq` | `Causalean.Stat.DegenKernel P g → ∀ (n : ℕ), ∫ (ω : Ω), (∑ p ∈ (Finset.range n).offDiag, g (S.Z p.1 ω) (S.Z p.2 ω)) ^ 2 ∂μ = 2 * ↑(Finset.range n).offDiag.card * Causalean.Stat.IIDSample.zeta P g` | Second moment of the off-diagonal sum. For an i.i.d. sample `S` and sample size `n`, if the two-argument kernel `g` is degenerate, then the second moment of the sum of `g(Z_i, Z_j)` over all ordered off-diagonal index pairs `i ≠ j` below `n` equals `2 · \|offDiag\| · ζ`, where `ζ` is the kernel's second moment under `P × P`. The `m = 2` case of `integral_injectiveTuples_sum_sq_degen`, using `sum_injectiveTuples_two_eq_offDiag`, `toOrderDegenKernel`, and `zeta_eq_zetaOrder` (`2! = 2`, `injectiveTupleCount 2 n = \|offDiag\| = n(n−1)`). |
| `Stat.IIDSample.integral_rescaled_sq` | `Causalean.Stat.DegenKernel P g → ∀ {n : ℕ}, 2 ≤ n → ∫ (ω : Ω), (√↑n * Causalean.Stat.uStatistic S g n ω) ^ 2 ∂μ = 2 * Causalean.Stat.IIDSample.zeta P g / (↑n - 1)` | L² bound on the rescaled degenerate U-statistic. For an i.i.d. sample `S`, if the two-argument kernel `g` is degenerate and the sample size is at least two, then the second moment of the `√n`-rescaled degenerate U-statistic of `g` equals `2ζ/(n−1)`, where `ζ` is the kernel's second moment under `P × P`. The `m = 2` case of `integral_rescaled_order_sq_degen` (`n · 2! · ζ / n^{(2)} = 2ζ/(n−1)`), via `uStatisticOrder_two_eq_uStatistic` and `zeta_eq_zetaOrder`. |
| `Stat.IIDSample.memLp_rescaled` | `Causalean.Stat.DegenKernel P g → ∀ (n : ℕ), MeasureTheory.MemLp (fun ω => √↑n * Causalean.Stat.uStatistic S g n ω) 2 μ` | The rescaled degenerate U-statistic is in `L²`. |
| `Stat.IIDSample.integral_rescaled_eq_zero` | `Causalean.Stat.DegenKernel P g → ∀ (n : ℕ), ∫ (ω : Ω), √↑n * Causalean.Stat.uStatistic S g n ω ∂μ = 0` | The rescaled degenerate U-statistic has mean zero. |
| `Stat.variance_offDiag_kernel_le` | `Measurable (Function.uncurry kernel) → ∀ (M : ℝ), (∀ (x y : X), \|kernel x y\| ≤ M) → ProbabilityTheory.variance (fun target => (↑N * ↑(N - 1))⁻¹ * ∑ i, ∑ j, if i ≠ j then kernel (target i) (target j) else 0) (MeasureTheory.Measure.pi fun x => μ) ≤ 32 * M ^ 2 / ↑N` | A bounded kernel's off-diagonal average over an independent sample has variance at most thirty-two times the squared kernel bound divided by the sample size. |
<!-- /GEN -->

### 12x.4. `Stat/UStatistic/OrderM/Basic.lean` — fixed-order U-statistic on injective tuples

<!-- GEN:Causalean.Stat.UStatistic.OrderM.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.injectiveTuples` | `(m n : ℕ) → Finset (Fin m → Fin n)` | For a tuple length and a number of available sample positions, the collection of ordered injective tuples consists of all ordered selections of the specified length whose entries are distinct positions among the first specified number of observations. |
| `Stat.injectiveTupleCount` | `ℕ → ℕ → ℝ` | For a tuple length and a number of available sample positions, the ordered injective-tuple count is the real-valued number of ordered selections of that length with distinct entries among the first specified number of observations. |
| `Stat.injectiveSubtypeEquivEmbedding` | `(m n : ℕ) → { t // Function.Injective t } ≃ (Fin m ↪ Fin n)` | For a domain size and a codomain size, the equivalence between injective maps and embeddings identifies every injective map from an $m$-element index set to an $n$-element index set with the corresponding embedding, and conversely. |
| `Stat.injectiveTuples_card_eq_descFactorial` | `(Causalean.Stat.injectiveTuples m n).card = n.descFactorial m` | The ordered injective tuple count is the falling factorial `n (n-1) ...`. |
| `Stat.injectiveTupleCount_eq_descFactorial` | `Causalean.Stat.injectiveTupleCount m n = ↑(n.descFactorial m)` | The real-valued ordered injective tuple count is the falling factorial. |
| `Stat.uStatisticOrder` | `{m : ℕ} → ((Fin m → X) → ℝ) → ℕ → Ω → ℝ` | Given an independent and identically distributed sample on a measurable sample space, with a specified sample-space measure and observation-space measure, an order-$m$ real-valued kernel, and a sample size, the fixed-order U-statistic maps each sample outcome to the average of the kernel over all ordered $m$-tuples of distinct observations among its first $n$ observations. |
| `Stat.uMeanOrder` | `((Fin m → X) → ℝ) → MeasureTheory.Measure X → ℝ` | Given a measurable observation space and an order-$m$ real-valued kernel on it and a measure on that space, the population mean of the kernel is its integral under the $m$-fold product of that measure. |
| `Stat.insertCoord` | `(j : Fin m) → X → ({ k // k ≠ j } → X) → Fin m → X` | Given an order $m$, a distinguished coordinate, a value in the observation space, and values for every remaining coordinate, the completed coordinate tuple assigns the given value to the distinguished coordinate and the supplied remaining values to all other coordinates. |
| `Stat.uProjOrderAt` | `Fin m → ((Fin m → X) → ℝ) → MeasureTheory.Measure X → X → ℝ` | Given an order-$m$ real-valued kernel, a distinguished coordinate, and a measure on the observation space, the coordinate-specific first Hoeffding projection maps a proposed value at that coordinate to the kernel integrated over the product measure for all other coordinates, minus the kernel's population mean. |
| `Stat.uProjOrder` | `((Fin m → X) → ℝ) → MeasureTheory.Measure X → X → ℝ` | Given a positive order $m$, an order-$m$ real-valued kernel, and a measure on the observation space, the first Hoeffding projection is the coordinate-specific first projection obtained by treating the first coordinate as distinguished. |
| `Stat.uDegenOrder` | `((Fin m → X) → ℝ) → MeasureTheory.Measure X → (Fin m → X) → ℝ` | Given a positive order $m$, an order-$m$ real-valued kernel, and a measure on the observation space, the higher-order residual kernel maps each $m$-tuple to the original kernel value minus its population mean and minus the sum of all coordinate-specific first Hoeffding projections at that tuple. |
| `Stat.hoeffding_decomp_order` | `h z = Causalean.Stat.uMeanOrder h P + ∑ j, Causalean.Stat.uProjOrderAt j h P (z j) + Causalean.Stat.uDegenOrder h P z` | For an order-`m` kernel `h`, population law `P`, and an `m`-tuple of points `z`, the kernel value decomposes as the population mean plus the sum of the `m` coordinatewise first Hoeffding projections plus the degenerate higher-order residual kernel evaluated at `z`. |
| `Stat.uProjOrderAt_integrable` | `MeasureTheory.Integrable (fun x => ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) P → MeasureTheory.Integrable (Causalean.Stat.uProjOrderAt j h P) P` | The coordinatewise first projection is integrable whenever the corresponding slice-averaged kernel is integrable. |
| `Stat.uProjOrderAt_integral_eq_zero` | `MeasureTheory.Integrable (fun x => ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) P → ∫ (x : X), ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P ∂P = Causalean.Stat.uMeanOrder h P → ∫ (x : X), Causalean.Stat.uProjOrderAt j h P x ∂P = 0` | The coordinatewise first projection integrates to zero once the slice-averaged representation of the population mean is available. The additional equality is the standard finite-product/Fubini identity for the chosen coordinate. |
| `Stat.uInfluenceOrder_integrable` | `(∀ (j : Fin m), MeasureTheory.Integrable (Causalean.Stat.uProjOrderAt j h P) P) → MeasureTheory.Integrable (fun x => ∑ j, Causalean.Stat.uProjOrderAt j h P x) P` | The summed first-order influence function is integrable if every coordinatewise first projection is integrable. |
| `Stat.uInfluenceOrder_integral_eq_zero` | `(∀ (j : Fin m), MeasureTheory.Integrable (Causalean.Stat.uProjOrderAt j h P) P) → (∀ (j : Fin m), ∫ (x : X), Causalean.Stat.uProjOrderAt j h P x ∂P = 0) → ∫ (x : X), ∑ j, Causalean.Stat.uProjOrderAt j h P x ∂P = 0` | The summed first-order influence function is centered if every coordinatewise first projection is centered. |
| `Stat.integral_pi_eval_eq` | `MeasureTheory.Integrable f P → (∫ (z : ι → X), f (z i) ∂MeasureTheory.Measure.pi fun x => P) = ∫ (x : X), f x ∂P` | Integrating a function of one coordinate under a finite product law recovers the one-dimensional integral. |
| `Stat.uDegenOrder_integral_tail_eq_zero` | `(∀ (j : Fin m), MeasureTheory.Integrable (fun x => ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) P) → (∀ (j : Fin m), ∫ (x : X), ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P ∂P = Causalean.Stat.uMeanOrder h P) → (∀ (j : Fin m) (x : X), MeasureTheory.Integrable (fun tail => h (Causalean.Stat.insertCoord j x tail)) (MeasureTheory.Measure.pi fun x => P)) → ∀ (j : Fin m) (x : X), (∫ (tail : { k // k ≠ j } → X), Causalean.Stat.uDegenOrder h P (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) = 0` | The first-order Hoeffding residual has zero conditional mean in each coordinate after integrating over all other coordinates, provided the usual finite-product/Fubini identities and slice integrability assumptions hold. |
| `Stat.pairKernel` | `(X → X → ℝ) → (Fin 2 → X) → ℝ` | Given an observation space and a real-valued binary kernel, the corresponding two-coordinate kernel maps an ordered pair to the binary kernel evaluated at its first and second entries. |
| `Stat.sum_injectiveTuples_two_eq_offDiag` | `(∑ t ∈ Causalean.Stat.injectiveTuples 2 n, Causalean.Stat.pairKernel h fun j => S.Z (↑(t j)) ω) = ∑ p ∈ (Finset.range n).offDiag, h (S.Z p.1 ω) (S.Z p.2 ω)` | Ordered injective `Fin 2` tuples are the same data as off-diagonal ordered pairs. |
| `Stat.uStatisticOrder_two_eq_uStatistic` | `Causalean.Stat.uStatisticOrder S (Causalean.Stat.pairKernel h) n = Causalean.Stat.uStatistic S h n` | The order-2 fixed-order statistic agrees with the existing ordered off-diagonal U-statistic for the corresponding pair kernel. |
| `Stat.finiteInjectiveTuples` | `(ι : Type u_3) → [Fintype ι] → (n : ℕ) → Finset (ι → Fin n)` | For a finite coordinate family and a sample size, the collection of injective sample assignments consists of all assignments sending distinct coordinates to distinct positions among the first $n$ observations. |
| `Stat.finiteInjectiveTuples_card` | `(Causalean.Stat.finiteInjectiveTuples ι n).card = n.descFactorial (Fintype.card ι)` | For a finite coordinate family and sample size `n`, the number of injective sample assignments is the falling factorial of `n` with length equal to the number of coordinates. |
| `Stat.normalizedFiniteKernelStatistic` | `{ι : Type u_3} → [Fintype ι] → ((ι → X) → ℝ) → ℕ → Ω → ℝ` | Given an independent and identically distributed sample on a measurable sample space, with a specified sample-space measure and observation-space measure, a finite coordinate family, a real-valued kernel indexed by that family, and a sample size, the normalized finite-kernel statistic maps each sample outcome to the average kernel value over every injective assignment of the coordinate family to the first $n$ sample positions. |
| `Stat.orderedProductKernel` | `(Fin r → X → ℝ) → (Fin r → X) → ℝ` | Given an observation space, an order, and one real-valued function of an observation for each coordinate, the ordered-product kernel maps an ordered $r$-tuple to the product of its coordinate-specific function values. |
| `Stat.normalizedOrderedProductStatistic` | `{r : ℕ} → (Fin r → X → ℝ) → ℕ → Ω → ℝ` | Given an independent and identically distributed sample on a measurable sample space, with a specified sample-space measure and observation-space measure, an order, one real-valued function of an observation for each coordinate, and a sample size, the normalized ordered-product statistic maps each sample outcome to the average, over all injective ordered $r$-tuples from its first $n$ observations, of the product of the corresponding coordinate-specific function values. |
| `Stat.normalizedFiniteKernelStatistic_fin_eq_uStatisticOrder` | `Causalean.Stat.normalizedFiniteKernelStatistic S k n = Causalean.Stat.uStatisticOrder S k n` | For an i.i.d. sample, an order-`r` kernel, and sample size `n`, the normalized finite-kernel statistic agrees with the existing fixed-order U-statistic. |
| `Stat.normalizedOrderedProductStatistic_eq_uStatisticOrder` | `Causalean.Stat.normalizedOrderedProductStatistic S f n = Causalean.Stat.uStatisticOrder S (Causalean.Stat.orderedProductKernel f) n` | For an i.i.d. sample, a family of order-`r` coordinate functions, and sample size `n`, the normalized ordered-product statistic is the existing fixed-order U-statistic applied to their product kernel. |
<!-- /GEN -->

### 12x.5. `Stat/UStatistic/OrderM/Hajek.lean` — Hájek expansion + asymptotic linearity

<!-- GEN:Causalean.Stat.UStatistic.OrderM.Hajek -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.uInfluenceOrder` | `((Fin m → X) → ℝ) → MeasureTheory.Measure X → X → ℝ` | For a measurable observation space, an order $m$, a real-valued kernel of that order, and an observation distribution, the first-order influence function maps each observation to the sum of the kernel's first Hoeffding projections over all $m$ coordinate positions. |
| `Stat.uInfluenceOrder_eq_card_mul_of_common_projection` | `(∀ (j : Fin m) (x : X), Causalean.Stat.uProjOrderAt j h P x = φ x) → Causalean.Stat.uInfluenceOrder h P = fun x => ↑m * φ x` | If all coordinatewise first projections agree with a common projection, then the order-`m` influence function is `m` times that projection. |
| `Stat.uRemainderOrder` | `{m : ℕ} → [NeZero m] → ((Fin m → X) → ℝ) → ℕ → Ω → ℝ` | For an i.i.d. sample on a measurable sample space, with observations in a measurable observation space and their probability distribution, a positive integer order, a real-valued kernel of that order, and a sample size, the higher-order Hájek remainder statistic is the order-$m$ U-statistic of the kernel remaining after all of its first-order Hoeffding projections have been removed. |
| `Stat.OrderDegenKernel` | `MeasureTheory.Measure X → {m : ℕ} → [NeZero m] → ((Fin m → X) → ℝ) → Prop` | Fully degenerate order-`m` kernel. A kernel `g` on `m`-tuples over `X` is completely degenerate under the product measure $P^{\otimes m}$ when `g` is measurable, `g` is invariant under permuting its `m` coordinates, integrating `g` over any one coordinate against `P` gives zero, whichever coordinate and values are held fixed for the rest, and `g` is square-integrable under $P^{\otimes m}$. |
| `Stat.OrderDegenerateNegligible` | `{m : ℕ} → [NeZero m] → ((Fin m → X) → ℝ) → Prop` | For an i.i.d. sample on a measurable sample space, with observations in a measurable observation space and their probability distribution, a positive integer order, and a real-valued kernel of that order, the order-$m$ degeneracy-negligibility condition asserts that the kernel's higher-order Hájek remainder, multiplied by $\sqrt n$, converges in probability to zero as the sample size tends to infinity. |
| `Stat.injectiveTupleCount_ne_zero` | `m ≤ n → Causalean.Stat.injectiveTupleCount m n ≠ 0` | If `m ≤ n`, there is at least one ordered injective `m`-tuple in `Fin n`. |
| `Stat.injectiveTuples_fiber_card_eq` | `{t ∈ Causalean.Stat.injectiveTuples m n \| t j = y}.card = {t ∈ Causalean.Stat.injectiveTuples m n \| t j = y'}.card` | Fibres of a fixed coordinate map on ordered injective tuples have the same cardinality. The bijection composes tuples with the codomain transposition swapping the two fibre values. |
| `Stat.sum_injectiveTuples_apply_eq_range` | `m ≤ n → ∀ (j : Fin m) (f : ℕ → ℝ), ∑ t ∈ Causalean.Stat.injectiveTuples m n, f ↑(t j) = Causalean.Stat.injectiveTupleCount m n / ↑n * ∑ i ∈ Finset.range n, f i` | For each coordinate of an ordered injective tuple, every sample index appears equally often. |
| `Stat.uStatisticOrder_sub_uMean_eq` | `m ≤ n → ∀ (ω : Ω), Causalean.Stat.uStatisticOrder S h n ω - Causalean.Stat.uMeanOrder h P = (↑n)⁻¹ * ∑ i ∈ Finset.range n, Causalean.Stat.uInfluenceOrder h P (S.Z i ω) + Causalean.Stat.uRemainderOrder S h n ω` | Hájek decomposition for a fixed-order U-statistic. For an i.i.d. sample `S`, order-`m` kernel `h`, and sample outcome `ω`, if the sample size is at least `m`, then the order-`m` U-statistic centered at its population mean decomposes exactly as the average of the first-order influence function over the first `n` sample points plus the higher-order Hájek remainder statistic. |
| `Stat.uStatisticOrder_remainder_eq` | `m ≤ n → (fun ω => √↑(Finset.range n).card * (Causalean.Stat.uStatisticOrder S h n ω - Causalean.Stat.uMeanOrder h P) - (√↑(Finset.range n).card)⁻¹ * ∑ i ∈ Finset.range n, Causalean.Stat.uInfluenceOrder h P (S.Z i ω)) = fun ω => √↑n * Causalean.Stat.uRemainderOrder S h n ω` | The Hájek remainder in `IsAsymLinear` form is the rescaled higher-order residual U-statistic. |
| `Stat.uStatisticOrder_isAsymLinear` | `∫ (x : X), Causalean.Stat.uInfluenceOrder h P x ∂P = 0 → MeasureTheory.Integrable (fun x => Causalean.Stat.uInfluenceOrder h P x ^ 2) P → Causalean.Stat.OrderDegenerateNegligible S h → Causalean.Stat.IsAsymLinear (Causalean.Stat.uStatisticOrder S h) (Causalean.Stat.uMeanOrder h P) (Causalean.Stat.uInfluenceOrder h P) S fun r => Finset.range r` | Fixed-order U-statistic asymptotic linearity. For an i.i.d. sample `S` and an order-`m` kernel `h`, write `ψ` for the summed coordinatewise first Hoeffding projection of `h`. If `ψ` has population mean zero and is square-integrable, and if the higher-order Hájek remainder of the order-`m` U-statistic is negligible at the `√n` scale, then the order-`m` U-statistic is asymptotically linear toward its population mean `uMeanOrder h P`, with influence function `ψ`. |
<!-- /GEN -->

### 12x.6. `Stat/UStatistic/OrderM/Variance.lean` — completely-degenerate transport

<!-- GEN:Causalean.Stat.UStatistic.OrderM.Variance -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IIDSample.map_fintype_tuple_eq` | `Function.Injective r → MeasureTheory.Measure.map (fun ω i => S.Z (↑(r i)) ω) μ = MeasureTheory.Measure.pi fun x => P` | The joint law of any finite collection of distinct sample coordinates is the corresponding product law. |
| `Stat.IIDSample.map_tuple_eq` | `Function.Injective t → MeasureTheory.Measure.map (fun ω j => S.Z (↑(t j)) ω) μ = MeasureTheory.Measure.pi fun x => P` | For an i.i.d. sample `S`, sample size `n`, and order `m`, if the index map `t : Fin m → Fin n` is injective, then the joint law of the sample coordinates selected by `t` is the `m`-fold product measure `P^m`. |
| `Stat.OrderDegenKernel.integrable` | `Causalean.Stat.OrderDegenKernel P g → MeasureTheory.Integrable g (MeasureTheory.Measure.pi fun x => P)` | A square-integrable order-`m` degenerate kernel is integrable under the product law. |
| `Stat.OrderDegenKernel.integral_eq_zero` | `Causalean.Stat.OrderDegenKernel P g → Causalean.Stat.uMeanOrder g P = 0` | A fully degenerate order-`m` kernel has zero product-law mean. |
| `Stat.IIDSample.integrable_orderKernelTerm` | `Measurable h → MeasureTheory.Integrable h (MeasureTheory.Measure.pi fun x => P) → ∀ {t : Fin m → Fin n}, Function.Injective t → MeasureTheory.Integrable (fun ω => h fun j => S.Z (↑(t j)) ω) μ` | An order-`m` kernel term along an injective tuple is integrable whenever the kernel is integrable under the product law. |
| `Stat.IIDSample.integral_orderKernelTerm_eq` | `Measurable h → ∀ {t : Fin m → Fin n}, Function.Injective t → ∫ (ω : Ω), h fun j => S.Z (↑(t j)) ω ∂μ = ∫ (z : Fin m → X), h z ∂MeasureTheory.Measure.pi fun x => P` | The expectation of an injectively indexed order-`m` kernel term equals the kernel's product-law mean. |
| `Stat.IIDSample.integral_orderKernelTerm_eq_zero_of_uMean_zero` | `Measurable h → ∀ {t : Fin m → Fin n}, Function.Injective t → Causalean.Stat.uMeanOrder h P = 0 → ∫ (ω : Ω), h fun j => S.Z (↑(t j)) ω ∂μ = 0` | An injectively indexed order-`m` kernel term has mean zero whenever the kernel has zero product-law mean. |
| `Stat.IIDSample.integral_uStatisticOrder_eq_uMean` | `Measurable h → MeasureTheory.Integrable h (MeasureTheory.Measure.pi fun x => P) → m ≤ n → ∫ (ω : Ω), Causalean.Stat.uStatisticOrder S h n ω ∂μ = Causalean.Stat.uMeanOrder h P` | The fixed-order U-statistic is unbiased: its expectation is the product-law kernel mean. |
| `Stat.IIDSample.integral_uStatisticOrder_eq_zero_of_uMean_zero` | `Measurable h → MeasureTheory.Integrable h (MeasureTheory.Measure.pi fun x => P) → m ≤ n → Causalean.Stat.uMeanOrder h P = 0 → ∫ (ω : Ω), Causalean.Stat.uStatisticOrder S h n ω ∂μ = 0` | A fixed-order U-statistic with product-law mean zero has expectation zero. |
| `Stat.IIDSample.integral_rescaled_uStatisticOrder_eq_sqrt_mul_uMean` | `Measurable h → MeasureTheory.Integrable h (MeasureTheory.Measure.pi fun x => P) → m ≤ n → ∫ (ω : Ω), √↑n * Causalean.Stat.uStatisticOrder S h n ω ∂μ = √↑n * Causalean.Stat.uMeanOrder h P` | The rescaled fixed-order U-statistic has mean equal to the same rescaling of the product-law kernel mean. |
| `Stat.IIDSample.integral_rescaled_uStatisticOrder_eq_zero_of_uMean_zero` | `Measurable h → MeasureTheory.Integrable h (MeasureTheory.Measure.pi fun x => P) → m ≤ n → Causalean.Stat.uMeanOrder h P = 0 → ∫ (ω : Ω), √↑n * Causalean.Stat.uStatisticOrder S h n ω ∂μ = 0` | If an order-`m` kernel has product-law mean zero, then the rescaled fixed-order U-statistic has mean zero. |
| `Stat.IIDSample.integral_uStatisticOrder_eq_zero_of_degenKernel_uMean_zero` | `Causalean.Stat.OrderDegenKernel P g → m ≤ n → Causalean.Stat.uMeanOrder g P = 0 → ∫ (ω : Ω), Causalean.Stat.uStatisticOrder S g n ω ∂μ = 0` | A fully degenerate order-`m` kernel whose product-law mean is zero gives a mean-zero fixed-order U-statistic. The product-law mean-zero assumption is kept explicit here rather than inferred from coordinatewise degeneracy. |
| `Stat.IIDSample.integral_uStatisticOrder_eq_zero_of_degenKernel` | `Causalean.Stat.OrderDegenKernel P g → m ≤ n → ∫ (ω : Ω), Causalean.Stat.uStatisticOrder S g n ω ∂μ = 0` | A fully degenerate order-`m` kernel gives a mean-zero fixed-order U-statistic in the nonempty sampling regime `m ≤ n`. |
| `Stat.IIDSample.integral_orderTerm_eq_zero_of_degenKernel_uMean_zero` | `Causalean.Stat.OrderDegenKernel P g → ∀ {t : Fin m → Fin n}, Function.Injective t → Causalean.Stat.uMeanOrder g P = 0 → ∫ (ω : Ω), g fun j => S.Z (↑(t j)) ω ∂μ = 0` | An injectively indexed fully degenerate order-`m` kernel term has mean zero when its product-law mean is zero. |
| `Stat.IIDSample.integral_orderTerm_eq_zero` | `Causalean.Stat.OrderDegenKernel P g → ∀ {t : Fin m → Fin n}, Function.Injective t → ∫ (ω : Ω), g fun j => S.Z (↑(t j)) ω ∂μ = 0` | An injectively indexed fully degenerate order-`m` kernel term has mean zero. |
| `Stat.IIDSample.integral_rescaled_uStatisticOrder_eq_zero_of_degenKernel_uMean_zero` | `Causalean.Stat.OrderDegenKernel P g → m ≤ n → Causalean.Stat.uMeanOrder g P = 0 → ∫ (ω : Ω), √↑n * Causalean.Stat.uStatisticOrder S g n ω ∂μ = 0` | A fully degenerate order-`m` kernel whose product-law mean is zero gives a mean-zero rescaled fixed-order U-statistic. This is a mean statement only; it does not assert the variance bound or negligibility. |
| `Stat.IIDSample.integral_rescaled_uStatisticOrder_eq_zero_of_degenKernel` | `Causalean.Stat.OrderDegenKernel P g → m ≤ n → ∫ (ω : Ω), √↑n * Causalean.Stat.uStatisticOrder S g n ω ∂μ = 0` | A fully degenerate order-`m` kernel gives a mean-zero rescaled fixed-order U-statistic in the nonempty sampling regime `m ≤ n`. This is a mean statement only; it does not assert the variance bound or negligibility. |
| `Stat.IIDSample.zetaOrder` | `MeasureTheory.Measure X → {m : ℕ} → ((Fin m → X) → ℝ) → ℝ` | For a measurable observation space, a measure on that space, a nonnegative integer kernel order $m$, and a real-valued kernel of $m$ observations, the order-$m$ kernel second moment is $\int g(z)^2\,dP^m(z)$, where $P^m$ is the product measure of $m$ independent draws from the given measure. |
| `Stat.IIDSample.integrable_orderTerm` | `Measurable g → MeasureTheory.Integrable g (MeasureTheory.Measure.pi fun x => P) → ∀ {t : Fin m → Fin n}, Function.Injective t → MeasureTheory.Integrable (fun ω => g fun j => S.Z (↑(t j)) ω) μ` | An order-`m` kernel term along an injective tuple is integrable. |
| `Stat.IIDSample.integrable_orderTerm_sq` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ {t : Fin m → Fin n}, Function.Injective t → MeasureTheory.Integrable (fun ω => (g fun j => S.Z (↑(t j)) ω) ^ 2) μ` | The square of an order-`m` kernel term along an injective tuple is integrable. |
| `Stat.IIDSample.orderTerm_diag` | `Measurable g → ∀ {t : Fin m → Fin n}, Function.Injective t → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) ^ 2 ∂μ = Causalean.Stat.IIDSample.zetaOrder P g` | For an i.i.d. sample `S` and order-`m` kernel `g` that is measurable, if the index map `t` is injective, then the second moment of the kernel term evaluated along the sample coordinates selected by `t` equals the kernel's second moment `ζ_m` under the `m`-fold product law. |
| `Stat.IIDSample.memLp_orderTerm` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ {t : Fin m → Fin n}, Function.Injective t → MeasureTheory.MemLp (fun ω => g fun j => S.Z (↑(t j)) ω) 2 μ` | Each injective order-`m` kernel term is in `L²`. |
| `Stat.IIDSample.integrable_orderTerm_mul` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → MeasureTheory.Integrable (fun ω => (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω) μ` | The product of two injective order-`m` kernel terms is integrable. |
| `Stat.IIDSample.integrable_injectiveTuples_sum` | `Measurable g → MeasureTheory.Integrable g (MeasureTheory.Measure.pi fun x => P) → ∀ (n : ℕ), MeasureTheory.Integrable (fun ω => ∑ t ∈ Causalean.Stat.injectiveTuples m n, g fun j => S.Z (↑(t j)) ω) μ` | The injective-tuple sum of an order-`m` kernel is integrable. |
| `Stat.IIDSample.memLp_injectiveTuples_sum` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ (n : ℕ), MeasureTheory.MemLp (fun ω => ∑ t ∈ Causalean.Stat.injectiveTuples m n, g fun j => S.Z (↑(t j)) ω) 2 μ` | The injective-tuple sum of an order-`m` kernel is in `L²`. |
| `Stat.IIDSample.memLp_rescaled_order` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ (n : ℕ), MeasureTheory.MemLp (fun ω => √↑n * Causalean.Stat.uStatisticOrder S g n ω) 2 μ` | For an i.i.d. sample `S`, order-`m` kernel `g` that is measurable and square-integrable under the `m`-fold product law, and sample size `n`, the `√n`-rescaled order-`m` U-statistic of `g` is square-integrable. |
| `Stat.IIDSample.zetaOrder_nonneg` | `0 ≤ Causalean.Stat.IIDSample.zetaOrder P g` | `ζ_m` is nonnegative. |
| `Stat.integral_normalizedFiniteKernelStatistic` | `Fintype.card ι ≤ n → Measurable k → MeasureTheory.Integrable k (MeasureTheory.Measure.pi fun x => P) → ∫ (ω : Ω), Causalean.Stat.normalizedFiniteKernelStatistic S k n ω ∂μ = ∫ (z : ι → X), k z ∂MeasureTheory.Measure.pi fun x => P` | For an i.i.d. sample, a finite coordinate family, a kernel, and sample size `n`, if the number of coordinates does not exceed the sample size, the kernel is measurable, and the kernel is integrable under the product law, the expected normalized statistic equals the kernel's product-law mean. |
<!-- /GEN -->

### 12x.6a. `Stat/UStatistic/OrderM/PartialMatching.lean` — finite overlap matchings

Partial matchings encode which coordinates of two ordered tuples reuse the same
sample observations. Their fixed-size count supplies the overlap enumeration
used by mixed-order U-statistic products.

<!-- GEN:Causalean.Stat.UStatistic.OrderM.PartialMatching -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.PartialMatching` | `ℕ → ℕ → Type` | A partial matching between ordered coordinate sets of sizes `r` and `s` selects a subset from each side and pairs the selected coordinates bijectively. |
| `Stat.PartialMatching.size` | `ℕ` | For a partial matching, its matching size is the number of selected left coordinates, equivalently the number of paired coordinates. |
| `Stat.PartialMatching.right_card` | `M.right.card = M.size` | Given a partial matching, its selected right subset has the same number of coordinates as the matching. |
| `Stat.PartialMatching.empty` | `(r s : ℕ) → Causalean.Stat.PartialMatching r s` | For two nonnegative coordinate-set sizes, the empty partial matching selects no coordinate on either side and therefore contains no pairs. |
| `Stat.PartialMatching.empty_size` | `(Causalean.Stat.PartialMatching.empty r s).size = 0` | For coordinate-set sizes `r` and `s`, the empty partial matching has no pairs. |
| `Stat.PartialMatching.eq_empty_of_size_eq_zero` | `M.size = 0 → M = Causalean.Stat.PartialMatching.empty r s` | A partial matching with no pairs is the empty partial matching. |
| `Stat.PartialMatching.MergedIndex` | `Type` | For a partial matching between coordinate sets of sizes $r$ and $s$, the merged coordinate set contains every left coordinate together with precisely those right coordinates that are not selected by the matching. |
| `Stat.PartialMatching.leftInjection` | `(M : Causalean.Stat.PartialMatching r s) → Fin r → M.MergedIndex` | For a partial matching and a left coordinate, the left-coordinate injection assigns that coordinate its own position in the merged coordinate set. |
| `Stat.PartialMatching.rightInjection` | `(M : Causalean.Stat.PartialMatching r s) → Fin s → M.MergedIndex` | For a partial matching and a right coordinate, the right-coordinate injection assigns the coordinate to its matched left-coordinate position when it is matched, and otherwise to its own separate position in the merged coordinate set. |
| `Stat.PartialMatching.mergedIndex_card` | `Fintype.card M.MergedIndex = r + s - M.size` | For a partial matching between coordinate sets of sizes `r` and `s`, the merged coordinate set contains `r + s` minus the matching size coordinates. |
| `Stat.partialMatchingEquivSigma` | `(r s : ℕ) → Causalean.Stat.PartialMatching r s ≃ (left : Finset (Fin r)) × (right : Finset (Fin s)) × (↑↑left ≃ ↑↑right)` | For two nonnegative coordinate-set sizes, the partial-matching representation equivalence bijects partial matchings with a selected subset of each coordinate set and a bijection between the two selected subsets. |
| `Stat.equivFintype` | `Fintype (α ≃ β)` | For two finite sets, the collection of bijections between them is itself a finite collection. |
| `Stat.finsetFintype` | `Fintype (Finset α)` | For a finite set, the collection of all its finite subsets is itself a finite collection. |
| `Stat.partialMatchingFintype` | `(r s : ℕ) → Fintype (Causalean.Stat.PartialMatching r s)` | For every pair of nonnegative integer coordinate-set sizes, the collection of partial matchings between coordinate sets of those sizes is finite. |
| `Stat.partialMatchingsOfSize` | `(r s : ℕ) → ℕ → Finset (Causalean.Stat.PartialMatching r s)` | For two nonnegative coordinate-set sizes and a prescribed number of pairs, the fixed-size partial-matching family is the finite set of all partial matchings having exactly that prescribed number of paired coordinates. |
| `Stat.mem_partialMatchingsOfSize` | `M ∈ Causalean.Stat.partialMatchingsOfSize r s h ↔ M.size = h` | A partial matching belongs to the family with `h` pairs exactly when its matching size is `h`. |
| `Stat.card_partialMatchingsOfSize` | `(Causalean.Stat.partialMatchingsOfSize r s h).card = r.choose h * s.choose h * h.factorial` | For coordinate-set sizes `r` and `s` and matching size `h`, the number of partial matchings is the product of the two subset counts and the number of permutations of `h` objects. |
| `Stat.PartialMatching.size_le_min` | `M.size ≤ min r s` | A partial matching has at most the smaller coordinate-set size many pairs. |
| `Stat.sum_partialMatchingsOfSize` | `∑ M, F M = ∑ h ∈ Finset.range (min r s + 1), ∑ M ∈ Causalean.Stat.partialMatchingsOfSize r s h, F M` | Given a quantity assigned to each partial matching, summing it over all matchings equals summing first by matching size through the smaller coordinate-set size. |
<!-- /GEN -->

### 12x.6b. `Stat/UStatistic/OrderM/MixedOrderCovariance.lean` — unequal-order product and covariance expansions

The partial-matching expansion rewrites a product of normalized ordered-product
statistics as merged product-law moments. Its centered form isolates the
disjoint normalization correction from all positive-overlap contributions.

<!-- GEN:Causalean.Stat.UStatistic.OrderM.MixedOrderCovariance -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.mergedProductKernel` | `(Fin r → X → ℝ) → (Fin s → X → ℝ) → (M : Causalean.Stat.PartialMatching r s) → (M.MergedIndex → X) → ℝ` | For two nonnegative orders, a family of real-valued left factors indexed by the first order, a family of real-valued right factors indexed by the second order, and a partial matching between their indices, the merged product kernel assigns to each collection of observations indexed by the merged coordinates the product of all left and right factors, evaluating matched factors at their common observation and unmatched factors at separate observations. |
| `Stat.mergedProductMoment` | `MeasureTheory.Measure X → (Fin r → X → ℝ) → (Fin s → X → ℝ) → Causalean.Stat.PartialMatching r s → ℝ` | For two nonnegative orders, a measure on a measurable observation space, families of real-valued left and right factors, and a partial matching between their indices, the merged product moment is the integral of the associated merged product kernel under independent draws from that measure, one draw for each merged coordinate. |
| `Stat.matchingNormalization` | `ℕ → {r s : ℕ} → Causalean.Stat.PartialMatching r s → ℝ` | For a nonnegative sample size, two nonnegative factor orders, and a partial matching between their indices, the matching normalization is the falling factorial of the sample size at the number of distinct observations induced by the matching, divided by the product of the two marginal falling factorials. |
| `Stat.matchingNormalization_of_mem` | `M ∈ Causalean.Stat.partialMatchingsOfSize r s h → Causalean.Stat.matchingNormalization n M = ↑(n.descFactorial (r + s - h)) / (↑(n.descFactorial r) * ↑(n.descFactorial s))` | Given sample size `n`, statistic orders `r` and `s`, and overlap size `h`, a partial matching belonging to the size-`h` family, its normalization has the falling factorial of length `r + s - h` in the numerator. |
| `Stat.normalizedOrderedProductStatistic_mul_expansion` | `r ≤ n → s ≤ n → ∀ (f : Fin r → X → ℝ) (g : Fin s → X → ℝ), (fun ω => Causalean.Stat.normalizedOrderedProductStatistic S f n ω * Causalean.Stat.normalizedOrderedProductStatistic S g n ω) = fun ω => ∑ h ∈ Finset.range (min r s + 1), ∑ M ∈ Causalean.Stat.partialMatchingsOfSize r s h, Causalean.Stat.matchingNormalization n M * Causalean.Stat.normalizedFiniteKernelStatistic S (Causalean.Stat.mergedProductKernel f g M) n ω` | For an i.i.d. sample, statistic orders and a sample size, if the first order does not exceed the sample size and the second order does not exceed the sample size, then the pointwise product of the statistics built from two coordinate-function families equals the sum of normalized merged-kernel statistics over all partial matchings, grouped by overlap size. |
| `Stat.integrable_normalizedFiniteKernelStatistic` | `Measurable k → MeasureTheory.Integrable k (MeasureTheory.Measure.pi fun x => P) → MeasureTheory.Integrable (Causalean.Stat.normalizedFiniteKernelStatistic S k n) μ` | If a finite-arity kernel is measurable and integrable under the product of the population law, then the normalized statistic that averages it over all injective index tuples of the sample is integrable. |
| `Stat.integral_normalizedOrderedProductStatistic_mul` | `r ≤ n → s ≤ n → ∀ (f : Fin r → X → ℝ) (g : Fin s → X → ℝ), (∀ (M : Causalean.Stat.PartialMatching r s), Measurable (Causalean.Stat.mergedProductKernel f g M)) → (∀ (M : Causalean.Stat.PartialMatching r s), MeasureTheory.Integrable (Causalean.Stat.mergedProductKernel f g M) (MeasureTheory.Measure.pi fun x => P)) → ∫ (ω : Ω), Causalean.Stat.normalizedOrderedProductStatistic S f n ω * Causalean.Stat.normalizedOrderedProductStatistic S g n ω ∂μ = ∑ h ∈ Finset.range (min r s + 1), ∑ M ∈ Causalean.Stat.partialMatchingsOfSize r s h, Causalean.Stat.matchingNormalization n M * Causalean.Stat.mergedProductMoment P f g M` | For an i.i.d. sample, statistic orders and a sample size, if the first order and the second order do not exceed the sample size, then for two coordinate-function families, when every merged kernel is measurable and integrable under its product law, the expected product of their normalized statistics is the partial-matching sum of merged product-law moments. |
| `Stat.orderedProductMean` | `MeasureTheory.Measure X → (Fin r → X → ℝ) → ℝ` | For a nonnegative order, a measure on a measurable observation space, and a family of real-valued coordinate factors, the ordered-product mean is the integral of their coordinatewise product under independent draws from that measure, one draw for every coordinate. |
| `Stat.centeredCrossMoment` | `MeasureTheory.Measure Ω → (Ω → ℝ) → (Ω → ℝ) → ℝ` | For a measure on a measurable sample space and two real-valued random variables on that sample space, the centered cross moment is the integral of their product minus the product of their separate integrals. |
| `Stat.mergedProductMoment_empty` | `Measurable (Causalean.Stat.orderedProductKernel f) → Measurable (Causalean.Stat.orderedProductKernel g) → MeasureTheory.Integrable (Causalean.Stat.orderedProductKernel f) (MeasureTheory.Measure.pi fun x => P) → MeasureTheory.Integrable (Causalean.Stat.orderedProductKernel g) (MeasureTheory.Measure.pi fun x => P) → Causalean.Stat.mergedProductMoment P f g (Causalean.Stat.PartialMatching.empty r s) = Causalean.Stat.orderedProductMean P f * Causalean.Stat.orderedProductMean P g` | Under a probability law, for two coordinate-function families of orders `r` and `s`, if the first product kernel is measurable, the second product kernel is measurable, the first product kernel is integrable, and the second product kernel is integrable, the empty matching's merged moment factors into the two separate product-law means. |
| `Stat.centeredCrossMoment_normalizedOrderedProductStatistic` | `r ≤ n → s ≤ n → ∀ (f : Fin r → X → ℝ) (g : Fin s → X → ℝ), Measurable (Causalean.Stat.orderedProductKernel f) → Measurable (Causalean.Stat.orderedProductKernel g) → MeasureTheory.Integrable (Causalean.Stat.orderedProductKernel f) (MeasureTheory.Measure.pi fun x => P) → MeasureTheory.Integrable (Causalean.Stat.orderedProductKernel g) (MeasureTheory.Measure.pi fun x => P) → (∀ (M : Causalean.Stat.PartialMatching r s), Measurable (Causalean.Stat.mergedProductKernel f g M)) → (∀ (M : Causalean.Stat.PartialMatching r s), MeasureTheory.Integrable (Causalean.Stat.mergedProductKernel f g M) (MeasureTheory.Measure.pi fun x => P)) → Causalean.Stat.centeredCrossMoment μ (Causalean.Stat.normalizedOrderedProductStatistic S f n) (Causalean.Stat.normalizedOrderedProductStatistic S g n) = (Causalean.Stat.matchingNormalization n (Causalean.Stat.PartialMatching.empty r s) - 1) * Causalean.Stat.orderedProductMean P f * Causalean.Stat.orderedProductMean P g + ∑ h ∈ Finset.range (min r s + 1) with 0 < h, ∑ M ∈ Causalean.Stat.partialMatchingsOfSize r s h, Causalean.Stat.matchingNormalization n M * Causalean.Stat.mergedProductMoment P f g M` | Under a probability population law, for an i.i.d. sample, statistic orders and a sample size, if the first order and the second order do not exceed the sample size, then for two coordinate-function families, when their product kernels are measurable, their product kernels are integrable, every merged kernel is measurable, and every merged kernel is integrable, their centered cross moment equals an explicit disjoint normalization correction plus the merged moments from every positive-size partial matching. |
<!-- /GEN -->

### 12x.6c. `Stat/UStatistic/OrderM/MixedOrderBounds.lean` — falling-factorial normalization bounds

Uniform bounds control each positive-overlap normalization by an inverse power
of sample size and control the disjoint correction at order `R²/n`.

<!-- GEN:Causalean.Stat.UStatistic.OrderM.MixedOrderBounds -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.factorialMatchingRatio_le` | `r ≤ R → s ≤ R → 4 * R ^ 2 ≤ n → h ≤ min r s → ↑(n.descFactorial (r + s - h)) / (↑(n.descFactorial r) * ↑(n.descFactorial s)) ≤ Real.exp 1 / ↑n ^ h` | For sample size `n`, orders `r` and `s`, common order bound `R`, and overlap size `h`, if the first order is at most `R`, the second order is at most `R`, the sample size is at least four times `R` squared, and the overlap fits inside both orders, the mixed falling-factorial ratio is at most `exp(1)` divided by `n` to the overlap size. |
| `Stat.matchingNormalization_le` | `r ≤ R → s ≤ R → 4 * R ^ 2 ≤ n → M ∈ Causalean.Stat.partialMatchingsOfSize r s h → Causalean.Stat.matchingNormalization n M ≤ Real.exp 1 / ↑n ^ h` | For sample size `n`, orders `r` and `s`, common order bound `R`, and overlap size `h`, a partial matching has normalization at most `exp(1)` divided by `n` to the overlap size when the first order is at most `R`, the second order is at most `R`, the sample size is at least four times `R` squared, and the matching has size `h` as claimed. |
| `Stat.factorialDisjointCorrection_le` | `r ≤ R → s ≤ R → 4 * R ^ 2 ≤ n → \|↑(n.descFactorial (r + s)) / (↑(n.descFactorial r) * ↑(n.descFactorial s)) - 1\| ≤ 2 * ↑R ^ 2 / ↑n` | For sample size `n`, orders `r` and `s`, and common order bound `R`, if the first order is at most `R`, the second order is at most `R`, and the sample size is at least four times `R` squared, the absolute disjoint normalization correction is at most twice `R` squared divided by `n`. |
| `Stat.emptyMatchingNormalization_sub_one_le` | `r ≤ R → s ≤ R → 4 * R ^ 2 ≤ n → \|Causalean.Stat.matchingNormalization n (Causalean.Stat.PartialMatching.empty r s) - 1\| ≤ 2 * ↑R ^ 2 / ↑n` | For sample size `n`, orders `r` and `s`, and common order bound `R`, if the first order is at most `R`, the second order is at most `R`, and the sample size is at least four times `R` squared, the empty partial matching's normalization differs from one by at most twice `R` squared divided by `n`. |
<!-- /GEN -->

### 12x.7. `Stat/UStatistic/OrderM/ExactVariance.lean` — exact order-`m` second moment

<!-- GEN:Causalean.Stat.UStatistic.OrderM.ExactVariance -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IIDSample.permOfImageEq` | `Function.Injective t → Function.Injective q → Finset.image t Finset.univ = Finset.image q Finset.univ → Equiv.Perm (Fin m)` | Given a nonnegative integer $m$ specifying the size of a source index set, a nonnegative integer $n$ specifying the size of a target index set, two maps from the source index set into the target index set, the assumption that both maps are injective, and the assumption that their images coincide, the selected permutation of the source index set reorders the first map into the second. It first selects, for each target value of the second map, a source index having the same first-map value, and then selects, for each target value of the first map, a source index having the same second-map value. |
| `Stat.IIDSample.crossterm_eq_zeta_of_image_eq` | `Measurable g → (∀ (σ : Equiv.Perm (Fin m)) (z : Fin m → X), g (z ∘ ⇑σ) = g z) → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → Finset.image t Finset.univ = Finset.image q Finset.univ → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ = Causalean.Stat.IIDSample.zetaOrder P g` | Equal-image cross term. If two ordered injective `m`-tuples have the same image, then `q` is a reordering of `t`, so by symmetry `g(Z_q) = g(Z_t)` and the cross expectation is `ζ_m = zetaOrder P g`. |
| `Stat.IIDSample.crossterm_eq_zero_of_image_ne` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → (∀ (j : Fin m) (tail : { k // k ≠ j } → X), ∫ (x : X), g (Causalean.Stat.insertCoord j x tail) ∂P = 0) → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → Finset.image t Finset.univ ≠ Finset.image q Finset.univ → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ = 0` | Distinct-image cross term. If two ordered injective `m`-tuples have different images, complete degeneracy kills the cross expectation: some index of `t` is absent from `q`, and integrating that coordinate out gives `0`. |
| `Stat.IIDSample.card_injectiveTuples_image_eq` | `Function.Injective t → {q ∈ Causalean.Stat.injectiveTuples m n \| Finset.image q Finset.univ = Finset.image t Finset.univ}.card = m.factorial` | For an ordered injective `m`-tuple `t`, the ordered injective tuples with the same image are exactly its `m!` reorderings. |
| `Stat.IIDSample.integral_injectiveTuples_sum_sq_degen` | `Causalean.Stat.OrderDegenKernel P g → ∀ (n : ℕ), ∫ (ω : Ω), (∑ t ∈ Causalean.Stat.injectiveTuples m n, g fun j => S.Z (↑(t j)) ω) ^ 2 ∂μ = ↑m.factorial * Causalean.Stat.injectiveTupleCount m n * Causalean.Stat.IIDSample.zetaOrder P g` | Exact second moment of the injective-tuple sum. For an i.i.d. sample `S` and sample size `n`, if the order-`m` kernel `g` is completely degenerate: symmetric, square-integrable, and with zero conditional mean after integrating out any single coordinate, then the second moment of the sum of `g` over all ordered injective `m`-tuples drawn from the first `n` sample indices equals `m! · n^{(m)} · ζ_m`, where `n^{(m)}` is the number of such tuples and `ζ_m` is the kernel's second moment under the `m`-fold product law. |
| `Stat.IIDSample.integral_rescaled_order_sq_degen` | `Causalean.Stat.OrderDegenKernel P g → ∀ {n : ℕ}, m ≤ n → ∫ (ω : Ω), (√↑n * Causalean.Stat.uStatisticOrder S g n ω) ^ 2 ∂μ = ↑n * ↑m.factorial * Causalean.Stat.IIDSample.zetaOrder P g / Causalean.Stat.injectiveTupleCount m n` | Exact variance of the rescaled degenerate fixed-order U-statistic. For an i.i.d. sample `S`, if the order-`m` kernel `g` is completely degenerate and the sample size is at least `m`, then the second moment of the `√n`-rescaled order-`m` U-statistic of `g` equals `n · m! · ζ_m / n^{(m)}`, which specializes to `2ζ/(n−1)` when `m = 2`. |
<!-- /GEN -->

### 12x.8. `Stat/UStatistic/OrderM/FirstDegenKernel.lean` — first-order degenerate kernel

<!-- GEN:Causalean.Stat.UStatistic.OrderM.FirstDegenKernel -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.OrderFirstDegenKernel` | `MeasureTheory.Measure X → {m : ℕ} → [NeZero m] → ((Fin m → X) → ℝ) → Prop` | First-order degenerate order-`m` kernel. A kernel `g` on `m`-tuples over `X`, together with the population measure `P`, is first-order degenerate when `g` is measurable, its first Hoeffding projection vanishes in every coordinate — integrating `g` over the other $m-1$ coordinates against the product measure leaves zero, whichever coordinate and value are held fixed, and `g` is square-integrable under the product measure $P^{\otimes m}$. |
| `Stat.OrderFirstDegenKernel.integrable` | `Causalean.Stat.OrderFirstDegenKernel P g → MeasureTheory.Integrable g (MeasureTheory.Measure.pi fun x => P)` | A first-order degenerate square-integrable kernel is integrable under the product law. |
| `Stat.OrderFirstDegenKernel.integral_eq_zero` | `Causalean.Stat.OrderFirstDegenKernel P g → Causalean.Stat.uMeanOrder g P = 0` | Population mean of a first-order degenerate kernel is zero. If the order-`m` kernel `g` is first-order degenerate: measurable, square-integrable under the `m`-fold product law, and with zero mean after integrating out all but any single coordinate, then the population mean of `g` under the `m`-fold product law is zero. |
<!-- /GEN -->

### 12x.9. `Stat/UStatistic/OrderM/RemainderSecondMoment.lean` — keystone `L² ≤ C/n` bound

<!-- GEN:Causalean.Stat.UStatistic.OrderM.RemainderSecondMoment -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IIDSample.crossterm_zero_of_disjoint` | `Causalean.Stat.OrderFirstDegenKernel P g → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → Disjoint (Finset.image t Finset.univ) (Finset.image q Finset.univ) → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ = 0` | For a first-order-degenerate kernel, the expected product of terms evaluated on two injective sample-index tuples with disjoint index sets is zero. |
| `Stat.IIDSample.crossterm_zero_of_shared_one` | `Causalean.Stat.OrderFirstDegenKernel P g → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → ∀ {a : Fin n}, Finset.image t Finset.univ ∩ Finset.image q Finset.univ = {a} → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ = 0` | For a first-order-degenerate kernel, the expected product of terms evaluated on two injective sample-index tuples sharing exactly one index is zero. |
| `Stat.IIDSample.crossterm_eq_zero_of_shared_le_one` | `Causalean.Stat.OrderFirstDegenKernel P g → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → (Finset.image t Finset.univ ∩ Finset.image q Finset.univ).card ≤ 1 → ∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ = 0` | Cross-term vanishing. For an i.i.d. sample `S`, sample size `n`, and order-`m` kernel `g` that is first-order degenerate, let `t` and `q` be ordered `m`-tuples of sample indices that are each injective, and suppose the images of `t` and `q` share at most one sample index. Then the expected product of the kernel evaluated along `t` and along `q` is zero: zero shared indices give independence with mean zero on each factor, while one shared index reduces, after conditioning on it, to first-order degeneracy of each factor. |
| `Stat.IIDSample.crossterm_abs_le_zeta` | `Measurable g → MeasureTheory.Integrable (fun z => g z ^ 2) (MeasureTheory.Measure.pi fun x => P) → ∀ {n : ℕ} {t q : Fin m → Fin n}, Function.Injective t → Function.Injective q → \|∫ (ω : Ω), (g fun j => S.Z (↑(t j)) ω) * g fun j => S.Z (↑(q j)) ω ∂μ\| ≤ Causalean.Stat.IIDSample.zetaOrder P g` | The absolute expected product of two kernel evaluations on distinct sample tuples is no larger than the kernel's second moment under the product distribution. |
| `Stat.IIDSample.sharedPositions_card_eq` | `Function.Injective q → {j \| q j ∈ Finset.image t Finset.univ}.card = (Finset.image t Finset.univ ∩ Finset.image q Finset.univ).card` | For an injective tuple map, the number of positions whose values occur in a second tuple equals the number of values shared by the two tuple images. |
| `Stat.IIDSample.card_share_ge_two_le` | `{tq ∈ Causalean.Stat.injectiveTuples m n ×ˢ Causalean.Stat.injectiveTuples m n \| 2 ≤ (Finset.image tq.1 Finset.univ ∩ Finset.image tq.2 Finset.univ).card}.card ≤ m.choose 2 * m ^ 2 * n ^ (2 * m - 2)` | The number of ordered pairs of injective tuples of length m drawn from n observations that share at least two sample indices is bounded by a polynomial in n determined by the tuple length. |
| `Stat.IIDSample.descFactorial_ge` | `m ≤ n → ↑n ^ m / ↑m ^ m ≤ ↑(n.descFactorial m)` | When the sample size is at least the order, its falling factorial is at least the sample-size power divided by the order power. |
| `Stat.IIDSample.integral_injectiveTuples_sum_sq_le_shared_count` | `Causalean.Stat.OrderFirstDegenKernel P g → ∀ {n : ℕ}, ∫ (ω : Ω), (∑ t ∈ Causalean.Stat.injectiveTuples m n, g fun j => S.Z (↑(t j)) ω) ^ 2 ∂μ ≤ ↑{tq ∈ Causalean.Stat.injectiveTuples m n ×ˢ Causalean.Stat.injectiveTuples m n \| 2 ≤ (Finset.image tq.1 Finset.univ ∩ Finset.image tq.2 Finset.univ).card}.card * Causalean.Stat.IIDSample.zetaOrder P g` | For a first-order-degenerate kernel, the second moment of the unnormalised sum over ordered injective sample tuples is bounded by the number of tuple pairs sharing at least two observations times the kernel's second moment. |
| `Stat.IIDSample.rescaled_order_normalization_le` | `m ≤ n → ∀ {ζ : ℝ}, 0 ≤ ζ → ↑n * (Causalean.Stat.injectiveTupleCount m n)⁻¹ ^ 2 * (↑(m.choose 2) * ↑m ^ 2 * ↑n ^ (2 * m - 2) * ζ) ≤ ↑(m.choose 2) * ↑m ^ (2 * m + 2) * ζ / ↑n` | For a nonnegative second-moment bound and a sample size at least the kernel order, the falling-factorial normalization term is bounded by a constant divided by the sample size. |
| `Stat.IIDSample.integral_rescaled_order_sq_le` | `Causalean.Stat.OrderFirstDegenKernel P g → ∃ C, 0 ≤ C ∧ ∀ {n : ℕ}, m ≤ n → ∫ (ω : Ω), (√↑n * Causalean.Stat.uStatisticOrder S g n ω) ^ 2 ∂μ ≤ C / ↑n` | `L²` bound on the rescaled higher-order remainder. For an i.i.d. sample `S`, if the order-`m` kernel `g` is first-order degenerate, then there is a nonnegative constant `C`, depending only on the order `m` and the kernel's second moment `ζ_m = E[g²]`, such that the second moment of the `√n`-rescaled order-`m` U-statistic of `g` is at most `C/n` for every sample size `n ≥ m`. This is the keystone estimate; it packages cross-term vanishing, the Cauchy–Schwarz bound `\|E[g_t g_q]\| ≤ ζ_m`, the `O(n^{2m-2})` count of surviving tuple pairs, and the `n · (n^{(m)})⁻²` normalization. |
<!-- /GEN -->

### 12x.10. `Stat/UStatistic/OrderM/RemainderNegligible.lean` — negligibility discharge

<!-- GEN:Causalean.Stat.UStatistic.OrderM.RemainderNegligible -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.IIDSample.integral_rescaled_order_eq_zero` | `Causalean.Stat.OrderFirstDegenKernel P g → ∀ {n : ℕ}, m ≤ n → ∫ (ω : Ω), √↑n * Causalean.Stat.uStatisticOrder S g n ω ∂μ = 0` | The rescaled higher-order remainder has mean zero. |
| `Stat.IIDSample.orderDegenerateNegligible_of_firstDegen` | `Causalean.Stat.OrderFirstDegenKernel P g → Causalean.Stat.IsLittleOp (fun n ω => √↑n * Causalean.Stat.uStatisticOrder S g n ω) (fun x => 1) μ` | Negligibility of the higher-order remainder. For an i.i.d. sample `S`, if the order-`m` kernel `g` is first-order degenerate: measurable, square-integrable, and mean zero after integrating out any single coordinate, then the `√n`-rescaled order-`m` U-statistic of `g` converges to zero in probability, i.e. it is `o_p(1)`. |
| `Stat.orderDegenerateNegligible_of_residual` | `Measurable (Causalean.Stat.uDegenOrder h P) → MeasureTheory.Integrable (fun z => Causalean.Stat.uDegenOrder h P z ^ 2) (MeasureTheory.Measure.pi fun x => P) → (∀ (j : Fin m), MeasureTheory.Integrable (fun x => ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) P) → (∀ (j : Fin m), ∫ (x : X), ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P ∂P = Causalean.Stat.uMeanOrder h P) → (∀ (j : Fin m) (x : X), MeasureTheory.Integrable (fun tail => h (Causalean.Stat.insertCoord j x tail)) (MeasureTheory.Measure.pi fun x => P)) → Causalean.Stat.OrderDegenerateNegligible S h` | The higher-order remainder of a fixed-order U-statistic is negligible. For an i.i.d. sample `S` and order-`m` kernel `h`, write `g` for the higher-order Hájek residual of `h`. If `g` is measurable and square-integrable under the `m`-fold product law, and if for every coordinate integrating `h` over the remaining `m − 1` coordinates yields an integrable function of that coordinate with the same population mean `uMeanOrder h P` in every coordinate and `h` remains integrable in the remaining coordinates for every fixed value of that coordinate, then the `√n`-rescaled higher-order residual U-statistic `√n · Gₙ` converges to zero in probability, i.e. it is `o_p(1)`. This discharges the `OrderDegenerateNegligible` hypothesis consumed by the order-`m` CLT `uStatisticOrder_clt` (`Causalean.Stat.UStatistic.OrderM.CLT`). |
<!-- /GEN -->

### 12x.11. `Stat/UStatistic/OrderM/CLT.lean` — headline: fixed-order U-statistic CLT

<!-- GEN:Causalean.Stat.UStatistic.OrderM.CLT -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.uStatisticOrder_clt` | `Measurable (Causalean.Stat.uInfluenceOrder h P) → ∫ (x : X), Causalean.Stat.uInfluenceOrder h P x ∂P = 0 → MeasureTheory.Integrable (fun x => Causalean.Stat.uInfluenceOrder h P x ^ 2) P → Causalean.Stat.OrderDegenerateNegligible S h → ∀ (hθn_meas : ∀ (n : ℕ), AEMeasurable (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatisticOrder S h) (Causalean.Stat.uMeanOrder h P) (fun r => Finset.range r) n) μ), Causalean.Stat.Tendsto_dist (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatisticOrder S h) (Causalean.Stat.uMeanOrder h P) fun r => Finset.range r) (Causalean.Stat.gaussianMeasure 0 (∫ (x : X), Causalean.Stat.uInfluenceOrder h P x ^ 2 ∂P)) μ hθn_meas` | Fixed-order U-statistic CLT. For an i.i.d. sample `S` and an order-`m` kernel `h`, write `ψ` for the summed coordinatewise first Hoeffding projection of `h`. If `ψ` is measurable, has population mean zero, and is square-integrable, if the higher-order Hájek remainder of the order-`m` U-statistic is negligible at the `√n` scale, and if the `√n`-rescaled U-statistic is almost-everywhere measurable at every sample size, then the `√n`-rescaled U-statistic converges in distribution to the centered Gaussian law with variance `∫ψ²dP`. |
| `Stat.uStatisticOrder_clt_of_regular` | `Measurable (Causalean.Stat.uDegenOrder h P) → MeasureTheory.Integrable (fun z => Causalean.Stat.uDegenOrder h P z ^ 2) (MeasureTheory.Measure.pi fun x => P) → (∀ (j : Fin m), MeasureTheory.Integrable (fun x => ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P) P) → (∀ (j : Fin m), ∫ (x : X), ∫ (tail : { k // k ≠ j } → X), h (Causalean.Stat.insertCoord j x tail) ∂MeasureTheory.Measure.pi fun x => P ∂P = Causalean.Stat.uMeanOrder h P) → (∀ (j : Fin m) (x : X), MeasureTheory.Integrable (fun tail => h (Causalean.Stat.insertCoord j x tail)) (MeasureTheory.Measure.pi fun x => P)) → Measurable (Causalean.Stat.uInfluenceOrder h P) → ∫ (x : X), Causalean.Stat.uInfluenceOrder h P x ∂P = 0 → MeasureTheory.Integrable (fun x => Causalean.Stat.uInfluenceOrder h P x ^ 2) P → ∀ (hθn_meas : ∀ (n : ℕ), AEMeasurable (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatisticOrder S h) (Causalean.Stat.uMeanOrder h P) (fun r => Finset.range r) n) μ), Causalean.Stat.Tendsto_dist (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatisticOrder S h) (Causalean.Stat.uMeanOrder h P) fun r => Finset.range r) (Causalean.Stat.gaussianMeasure 0 (∫ (x : X), Causalean.Stat.uInfluenceOrder h P x ^ 2 ∂P)) μ hθn_meas` | Fixed-order U-statistic CLT (end-to-end). For an i.i.d. sample `S` and an order-`m` kernel `h`, write `g` for the higher-order Hájek residual of `h` and `ψ` for the summed coordinatewise first Hoeffding projection of `h`. If the residual `g` is measurable and square-integrable under the `m`-fold product law, if for every coordinate integrating `h` over the remaining `m − 1` coordinates yields an integrable function of that coordinate with the same population mean `uMeanOrder h P` in every coordinate and `h` remains integrable in the remaining coordinates for every fixed value of that coordinate, and if `ψ` is measurable, mean zero, square-integrable, and the `√n`-rescaled U-statistic is almost-everywhere measurable at every sample size, then the `√n`-rescaled order-`m` U-statistic converges in distribution to the centered Gaussian law with variance `∫ψ²dP`. |
<!-- /GEN -->

### 12x.12. `Stat/UStatistic/OrderM/OrderTwo.lean` — `m = 2` bridge to the order-2 CLT

<!-- GEN:Causalean.Stat.UStatistic.OrderM.OrderTwo -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.uMeanOrder_pairKernel` | `MeasureTheory.Integrable (fun p => h p.1 p.2) (P.prod P) → Causalean.Stat.uMeanOrder (Causalean.Stat.pairKernel h) P = Causalean.Stat.uMean h P` | The order-`2` population mean of `pairKernel h` is the order-2 mean of `h`. Requires product-integrability of `h` so that the joint integral over `Fin 2 → X` agrees (via Fubini) with the iterated integral defining `uMean`. |
| `Stat.uProjOrderAt_pairKernel_of_symm` | `(∀ (x y : X), h x y = h y x) → MeasureTheory.Integrable (fun p => h p.1 p.2) (P.prod P) → (∀ (x : X), MeasureTheory.Integrable (fun y => h x y) P) → ∀ (j : Fin 2), Causalean.Stat.uProjOrderAt j (Causalean.Stat.pairKernel h) P = Causalean.Stat.uProj h P` | Each coordinate first projection of `pairKernel h` equals the order-2 first projection of `h`, for a symmetric kernel. (For `j = 0` no symmetry is needed; for `j = 1` it is used to swap the integration coordinate.) |
| `Stat.uInfluenceOrder_pairKernel_of_symm` | `(∀ (x y : X), h x y = h y x) → MeasureTheory.Integrable (fun p => h p.1 p.2) (P.prod P) → (∀ (x : X), MeasureTheory.Integrable (fun y => h x y) P) → Causalean.Stat.uInfluenceOrder (Causalean.Stat.pairKernel h) P = fun x => 2 * Causalean.Stat.uProj h P x` | The order-`2` influence function of `pairKernel h` is `2 · h₁`, matching the order-2 CLT influence function `ψ = 2 · uProj h P`. |
| `Stat.uDegenOrder_pairKernel_of_symm` | `(∀ (x y : X), h x y = h y x) → MeasureTheory.Integrable (fun p => h p.1 p.2) (P.prod P) → (∀ (x : X), MeasureTheory.Integrable (fun y => h x y) P) → Causalean.Stat.uDegenOrder (Causalean.Stat.pairKernel h) P = fun z => Causalean.Stat.uDegen h P (z 0) (z 1)` | The order-`2` degenerate residual of `pairKernel h` is the order-2 degenerate kernel `uDegen h P`, evaluated at the two coordinates. |
| `Stat.uStatistic_clt_of_symmetric_via_orderM` | `(∀ (x y : X), h x y = h y x) → (Measurable fun p => h p.1 p.2) → MeasureTheory.Integrable (fun p => Causalean.Stat.uDegen h P p.1 p.2 ^ 2) (P.prod P) → MeasureTheory.Integrable (fun x => ∫ (y : X), h x y ∂P) P → (∀ (x : X), MeasureTheory.Integrable (fun y => h x y) P) → MeasureTheory.Integrable (fun x => Causalean.Stat.uProj h P x ^ 2) P → ∀ (hθn_meas : ∀ (n : ℕ), AEMeasurable (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatistic S h) (Causalean.Stat.uMean h P) (fun m => Finset.range m) n) μ), Causalean.Stat.Tendsto_dist (Causalean.Stat.IsAsymLinear.rescaledEstimator (Causalean.Stat.uStatistic S h) (Causalean.Stat.uMean h P) fun m => Finset.range m) (Causalean.Stat.gaussianMeasure 0 (∫ (x : X), (fun x => 2 * Causalean.Stat.uProj h P x) x ^ 2 ∂P)) μ hθn_meas` | Order-2 U-statistic CLT, derived from the fixed-order-`m` CLT. For an i.i.d. sample `S` and a two-argument kernel `h : X → X → ℝ` that is symmetric and jointly measurable, suppose the order-2 Hájek residual of `h` is square-integrable under the product law `P × P`, the row integral `x ↦ ∫h(x,y)dP(y)` is integrable and each row `y ↦ h(x,y)` is itself integrable for every `x`, the first Hoeffding projection of `h` is square-integrable, and the `√n`-rescaled U-statistic is almost-everywhere measurable at every sample size. Then the `√n`-rescaled order-2 U-statistic converges in distribution to the centered Gaussian law with variance `4ζ₁`, where `ζ₁` is the variance of the first Hoeffding projection. |
<!-- /GEN -->

## 12wo. `Stat/PolynomialTail/` — Polynomial lower-tail inverse-moment asymptotics

Causal-agnostic real-analysis / measure-theory infrastructure for the small-value
(lower-tail) asymptotics of a bounded positive random variable.  This is the moment
calculus underlying *any* inverse-weighted estimand whose weight is floored at `1/λ` —
not tied to one paper.  It specializes to (and quantitatively refines) the qualitative
`Overlap`/positivity assumption in `Identification/`: taking `U = min(e(X), 1−e(X))`
recovers the variance/rate ingredients of IPW, AIPW, and Hájek estimators under thin
overlap, trimming/truncation bias, and weak-overlap minimax rates.  Fix a probability
measure `P` and a measurable `U : Ω → ℝ` with `0 < U ≤ 1` a.s.  The two-sided
polynomial lower-tail hypothesis `PolyTail P U κ t₀ cm cp` says
`cm·tᵏ ≤ P{U ≤ t} ≤ cp·tᵏ` on `(0,t₀]` (`0 < κ`, `0 < t₀ < 1`, `0 < cm < cp`).  The
library bounds the two truncated inverse moments (`λ`-truncation, `0 < λ ≤ t₀`)

    I P U λ = ∫ U/(max U λ)²   (= 1/U on {U≥λ}, U/λ² on {U<λ}),
    J P U λ = ∫ (max U λ)⁻¹    (= 1/U on {U≥λ}, 1/λ  on {U<λ}),

and the leftover trimming weight `wλ = 1 − U/(max U λ) ∈ [0,1]` (supported on `{U<λ}`).
The key trichotomy is in the tail exponent `κ`: `λ^{κ-1}` (κ<1), `log(1/λ)` (κ=1),
bounded (κ>1).  The lower bounds genuinely use **both** tail constants (the gap
`cm < cp` breaks any one-sided argument).  Method: Mathlib's layer-cake theorem
`MeasureTheory.Integrable.integral_eq_integral_Ioc_meas_le` reduces `J` to a
threshold integral of `P{U ≤ t⁻¹}`, evaluated against `∫ t^{-κ}` (`integral_rpow` /
`integral_inv`).  All declarations **sorry-free and axiom-clean**.

### 12wo.1. `Stat/PolynomialTail/Basic.lean` — setup, integrands, basic facts

| Declaration | Statement | Description |
|---|---|---|
| `PolyTail P U κ t₀ cm cp` | `structure : Prop` | The polynomial lower-tail hypothesis: `cm·tᵏ ≤ P{U≤t} ≤ cp·tᵏ` on `(0,t₀]`, with `0<κ`, `0<t₀<1`, `0<cm<cp`. Fields `kappa_pos/t0_pos/t0_lt_one/cm_pos/cm_lt_cp/tail_lower/tail_upper`. |
| `PolyTail.cp_pos` | `0 < cp` | Upper constant positive. |
| `TailSetup P U` | `structure : Prop` | Structural hypotheses on `U`: `Measurable U`, `0 < U` a.s., `U ≤ 1` a.s. |
| `truncDen U λ ω` | `def := max (U ω) λ` | Truncated denominator. |
| `invMomentI P U λ` | `def := ∫ U/(max U λ)²` | Truncated inverse second moment. |
| `invMomentJ P U λ` | `def := ∫ (max U λ)⁻¹` | Truncated inverse first moment. |
| `trimWeight U λ ω` | `def := 1 − U/(max U λ)` | Leftover weight `∈[0,1]`, supported on `{U<λ}`. |
| `measurable_*` | measurability | The three integrands and the denominator are measurable. |
| `measureReal_lt_le` | `P{U<λ} ≤ cp·λᵏ` | Mass of the open lower level set (monotonicity + upper tail). |
| `invMomentI_integrand_mem` / `invMomentJ_integrand_mem` / `trimWeight_mem` | pointwise `∈[0,1/λ²]` / `[0,1/λ]` / `[0,1]` | Pointwise integrand sandwiches. |
| `integrable_invMomentI_integrand` / `…J…` / `integrable_trimWeight` | `Integrable … P` | Integrability (bounded a.e. on a probability space). |

### 12wo.2. `Stat/PolynomialTail/LayerCakeReduction.lean` — layer-cake reduction

| Declaration | Statement | Description |
|---|---|---|
| `tailIntegral P U λ` | `def := ∫ t in (1,λ⁻¹], P{U ≤ t⁻¹}` | The threshold ("tail") integral. |
| `monotone_cdf` | `Monotone (s ↦ P{U≤s})` | The lower CDF is monotone. |
| `measurable_tailIntegrand`, `tailIntegrand_nonneg`, `tailIntegrand_le_one`, `integrableOn_tailIntegrand` | regularity | Measurability/`∈[0,1]`/integrability of `t ↦ P{U≤t⁻¹}`. |
| `invMomentJ_eq_one_add_tailIntegral` | `J P U λ = 1 + tailIntegral P U λ` (`0<λ<1`) | **Master layer-cake reduction.** Levels `t≤1` contribute exactly `1` (`(max U λ)⁻¹ ≥ 1` a.s.); levels `(1,λ⁻¹]` reproduce `P{U≤t⁻¹}`. |
| `tailIntegral_nonneg` | `0 ≤ tailIntegral P U λ` | Nonnegativity. |

### 12wo.3. `Stat/PolynomialTail/PowerIntegral.lean` — power-law interval integrals

| Declaration | Statement | Description |
|---|---|---|
| `zero_notMem_uIcc` | `0 ∉ [[a,b]]` (`0<a≤b`) | Side condition for `integral_rpow`/`integral_inv`. |
| `integral_rpow_neg_Ioc` | `∫ t in (a,b], t^{-κ} = (b^{1-κ}−a^{1-κ})/(1−κ)` (`κ≠1`, `0<a≤b`) | Power integral via `integral_rpow`. |
| `integral_inv_neg_Ioc` | `∫ t in (a,b], t^{-1} = log b − log a` (`0<a≤b`) | `κ=1` case via `integral_inv`. |

### 12wo.4. `Stat/PolynomialTail/TailIntegralBounds.lean` — tail-integral sandwich

| Declaration | Statement | Description |
|---|---|---|
| `inv_rpow_eq_rpow_neg` | `(t⁻¹)^κ = t^{-κ}` (`t>0`) | rpow identity. |
| `tailIntegrand_upper_window` / `tailIntegrand_lower_window` | `cm·t^{-κ} ≤ P{U≤t⁻¹} ≤ cp·t^{-κ}` on `(t₀⁻¹,λ⁻¹]` | Polynomial-window integrand sandwich. |
| `integrableOn_rpow_neg_window` | `IntegrableOn (t^{-κ}) (t₀⁻¹,λ⁻¹]` | Integrability of the power integrand. |
| `tailIntegral_le` | `tailIntegral ≤ (t₀⁻¹−1) + cp·∫_{(t₀⁻¹,λ⁻¹]} t^{-κ}` | **Upper** sandwich (bounded shell `(1,t₀⁻¹]` + polynomial window). |
| `tailIntegral_ge` | `cm·∫_{(t₀⁻¹,λ⁻¹]} t^{-κ} ≤ tailIntegral` | **Lower** sandwich. |

### 12wo.5. `Stat/PolynomialTail/MomentJBounds.lean` — three-regime bounds for `J`

| Declaration | Statement | Description |
|---|---|---|
| `powInt_ne_one` | `∫_{(t₀⁻¹,λ⁻¹]} t^{-κ} = (λ^{κ-1}−t₀^{κ-1})/(1−κ)` (`κ≠1`) | Regime power-integral value. |
| `powInt_eq_one` | `… = log(1/λ) − log(1/t₀)` (`κ=1`) | Regime power-integral value. |
| `invMomentJ_bounds_lt` | `∃ 0<a≤A, ∀λ∈(0,t₀], a·λ^{κ-1} ≤ J ≤ A·λ^{κ-1}` (`κ<1`) | **κ<1.** Constants depend only on `(κ,cm,cp,t₀)`: `A=t₀⁻¹+cp/(1−κ)`, `a=min(cm/(1−κ), t₀^{1−κ})`. |
| `invMomentJ_bounds_eq` | `∃ 0<a≤A, a·log(1/λ) ≤ J ≤ A·log(1/λ)+A` (`κ=1`) | **κ=1.** |
| `invMomentJ_bounds_gt` | `∃ 0<a≤A, a ≤ J ≤ A` (`κ>1`) | **κ>1.** — inverse first moment is *bounded* (`a=1` since `J≥1`). |

### 12wo.6. `Stat/PolynomialTail/MomentIBounds.lean` — bounds for `I` + IsTheta

| Declaration | Statement | Description |
|---|---|---|
| `invMomentI_le_invMomentJ` | `I P U λ ≤ J P U λ` | Pointwise `U/(max U λ)² ≤ (max U λ)⁻¹`. Gives the **upper** half of the `I` bounds from the `J` bounds. |
| `invMomentJ_sub_invMomentI_le` | `J P U λ − I P U λ ≤ cp·λ^{κ-1}` | Gap controlled by tail mass: pointwise `= wλ/(max U λ) ≤ λ⁻¹·wλ`, and `∫wλ ≤ P{U<λ} ≤ cp·λᵏ`. The bridge for the `κ=1` `I`-lower bound. |
| `invMomentI_antitone` | `I P U λ₂ ≤ I P U λ₁` (`λ₁≤λ₂`) | `I` decreases as `λ` grows (`max U λ` grows). Bridge for the `κ>1` constant `I`-floor. |
| `invMomentI_ge_shell` | `(cm/2)·(cm/(2cp))^{1/κ}·λ^{κ-1} ≤ I P U λ` | **Shell lower bound.** The shell `(ρλ,λ]`, `ρ=(cm/(2cp))^{1/κ}`, has mass `≥(cm/2)λᵏ` (uses **both** tails) on which `U/λ² ≥ ρ/λ`. |
| `invMomentI_bounds_lt` | `∃ 0<a≤A, ∀λ∈(0,t₀], a·λ^{κ-1} ≤ I ≤ A·λ^{κ-1}` (`κ<1`) | Lower = shell, upper = `I ≤ J`. |
| `invMomentI_bounds_eq` | `∃ 0<a≤A, a·log(1/λ) ≤ I ≤ A·log(1/λ)+A` (`κ=1`) | Lower = `I ≥ J − cp` (relation) uniformized with the shell floor; upper = `I ≤ J`. |
| `invMomentI_bounds_gt` | `∃ 0<a≤A, a ≤ I ≤ A` (`κ>1`) | Bounded inverse second moment: lower `I(λ)≥I(t₀)≥` shell-at-`t₀` (antitone); upper = `I ≤ J`. |
| `isTheta_of_two_sided` | two-sided bound ⟹ `f =Θ[𝓝[>]0] g` | Generic `IsTheta` packager. |
| `invMomentJ_isTheta_lt` / `invMomentI_isTheta_lt` | `J ≍ λ^{κ-1}`, `I ≍ λ^{κ-1}` (`κ<1`) | **IsTheta** on `𝓝[>] 0`. |
| `invMomentJ_isTheta_gt` / `invMomentI_isTheta_gt` | `J ≍ 1`, `I ≍ 1` (`κ>1`) | **IsTheta** (bounded regime). |

### 12wo.7. `Stat/PolynomialTail/TrimBias.lean` — general bias bound

| Declaration | Statement | Description |
|---|---|---|
| `trimWeight_bias_bound` | `\|∫ wλ·g ∂P\| ≤ Cβ·cp·λ^{κ+β}` | For `g` with Hölder envelope `\|g\|≤Cβ·λ^β` on `{U<λ}`: tail-mass (`≤cp·λᵏ`) × envelope. Elementary (no layer cake). |

## 12'. `Stat/Concentration/` — Vendored concentration toolbox (Phase-4a)

Selectively vendored from
[auto-res/lean-rademacher](https://github.com/auto-res/lean-rademacher)
(commit `72d28921dc960f47691640fb973303a1be9d13ca`, MIT License). Audit
trail and porting policy live in
[Causalean/Stat/Concentration/UPSTREAM.md](../Causalean/Stat/Concentration/UPSTREAM.md).
Phase 4a covers definitions and the headline statements only; lower-level
proof chains are scaffolded with `sorry` and filled by Codex in a
separate pass.

#### 12'.0 `Stat/Concentration/Chebyshev.lean`

Plain single-measure Chebyshev in the form call sites want: a *variance bound* and a supplied mean give a real-valued probability bound on the deviation event. Distinct from `iid_sum_chebyshev` (§12'.f'-adjacent, in `Matrix/IidSums.lean`), which is the i.i.d.-*sum* form with an exact variance and an `ENNReal` conclusion.

<!-- GEN:Causalean.Stat.Concentration.Chebyshev -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.probability_abs_sub_mean_gt_le` | `MeasureTheory.MemLp F 2 Q → 0 < a → ∫ (x : Ω), F x ∂Q = mean → ProbabilityTheory.variance F Q ≤ v → (Q {x \| a < \|F x - mean\|}).toReal ≤ v / a ^ 2` | For a finite measure `Q` and a statistic `F` on the underlying space, if `F` is square-integrable under `Q`, the deviation threshold `a` is positive, `F` has mean equal to `mean` under `Q`, and the variance of `F` under `Q` is at most the envelope `v`, then the probability that `F` deviates from `mean` by more than `a` in absolute value is at most `v/a²`. |
<!-- /GEN -->

#### 12'.0a `Stat/Concentration/RandomDesignWeightedHoeffding/Basic.lean`

Conditional Hoeffding exponential-moment bounds for bounded outcomes conditionally centered by
their observed designs. The coefficient array may depend measurably on the whole finite design
vector; the module derives the requisite product conditional law instead of assuming it.

<!-- GEN:Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy` | `((Fin N → D) → Fin N → ℝ) → (Fin N → D) → ℝ` | For a coefficient array determined by a complete design vector and a complete design vector, the realized weight energy is the sum of the squares of the coefficients selected by that design vector. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum` | `(D → ℝ) → ((Fin N → D) → Fin N → ℝ) → (Fin N → D) → (Fin N → ℝ) → ℝ` | For a real-valued conditional mean function of a design value, a coefficient array determined by a complete design vector, a complete design vector, and a vector of real marks, the weighted centered mark sum is the sum over sample positions of the selected coefficient times the mark minus its conditional mean at the corresponding design value. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.selfNormalizedWeightedCenteredSum` | `(Omega → D) → (Omega → ℝ) → (D → ℝ) → ((Fin N → D) → Fin N → ℝ) → (Fin N → Omega) → ℝ` | For a design map, a real-valued outcome map, a real-valued conditional mean function of the design, a coefficient array determined by a complete design vector, and a finite vector of sampled units, the self-normalized weighted centered sum is the weighted centered sum divided by the square root of the realized weight energy of the sampled design vector; when that energy is zero, this value is zero. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.measurable_realizedWeightEnergy` | `Measurable w → Measurable (Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w)` | The sum of squared coefficients selected by a measurable complete-design weight array is itself measurable. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.measurable_weightedCenteredMarkSum` | `Measurable mD → ∀ {w : (Fin N → D) → Fin N → ℝ}, Measurable w → Measurable fun p => Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum mD w p.1 p.2` | A measurable regression and measurable complete-design weights make the weighted centered mark sum jointly measurable. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.measurable_weightedCenteredSum` | `Measurable design → ∀ {Y : Omega → ℝ}, Measurable Y → ∀ {mD : D → ℝ}, Measurable mD → ∀ {w : (Fin N → D) → Fin N → ℝ}, Measurable w → Measurable (Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w)` | A measurable design, measurable outcome, measurable regression, and measurable complete-design weights make the finite-sample weighted centered sum measurable. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.measurable_selfNormalizedWeightedCenteredSum` | `Measurable design → ∀ {Y : Omega → ℝ}, Measurable Y → ∀ {mD : D → ℝ}, Measurable mD → ∀ {w : (Fin N → D) → Fin N → ℝ}, Measurable w → Measurable (Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.selfNormalizedWeightedCenteredSum design Y mD w)` | A measurable design, measurable outcome, measurable regression, and measurable complete-design weights make the self-normalized weighted centered sum measurable, including at zero energy. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.measurableSet_selfNormalizedDeviation` | `Measurable design → ∀ {Y : Omega → ℝ}, Measurable Y → ∀ {mD : D → ℝ}, Measurable mD → ∀ {w : (Fin N → D) → Fin N → ℝ}, Measurable w → ∀ (t : ℝ), MeasurableSet {z \| t ≤ \|Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.selfNormalizedWeightedCenteredSum design Y mD w z\|}` | A measurable design, measurable outcome, measurable regression, and measurable complete-design weights make the event that the absolute self-normalized sum exceeds the fixed threshold measurable. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.centered_condDistrib_mgf_le` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → (∀ᵐ (omega : Omega) ∂P, 0 ≤ Y omega) → (∀ᵐ (omega : Omega) ∂P, Y omega ≤ 1) → ∀ (mD : D → ℝ), Measurable mD → P[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[P] mD ∘ design → ∀ᵐ (d : D) ∂MeasureTheory.Measure.map design P, ∀ (s : ℝ), ∫ (y : ℝ), Real.exp (s * (y - mD d)) ∂(ProbabilityTheory.condDistrib Y design P) d ≤ Real.exp (s ^ 2 / 8)` | A probability law P, measurable design, and measurable outcome that is almost surely nonnegative and at most one, together with a measurable regression equal almost surely to its conditional expectation given the design, yield the conditional Hoeffding exponential-moment bound on almost every design fibre. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_weighted_centered_attachKernel_hasSubgaussianMGF` | `Measurable mD → (∀ᵐ (d : D) ∂Q, ∫ (y : ℝ), y ∂K d = mD d) → (∀ᵐ (d : D) ∂Q, ∀ᵐ (y : ℝ) ∂K d, y ∈ Set.Icc 0 1) → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → ∀ᵐ (d : Fin N → D) ∂MeasureTheory.Measure.pi fun x => Q, ProbabilityTheory.HasSubgaussianMGF (Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum mD w d) (Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w d / 4).toNNReal ((Causalean.Stat.finProductKernel N K) d)` | Under a probability design law and Markov marking kernel whose conditional mean is the measurable regression, whose marks lie in the unit interval almost surely on almost every fibre, and whose complete-design weight array is measurable, the weighted centered mark sum is sub-Gaussian on almost every complete-design fibre with one quarter of its realized weight energy as variance proxy. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_weighted_centered_attachKernel_conditional_mgf_le` | `Measurable mD → (∀ᵐ (d : D) ∂Q, ∫ (y : ℝ), y ∂K d = mD d) → (∀ᵐ (d : D) ∂Q, ∀ᵐ (y : ℝ) ∂K d, y ∈ Set.Icc 0 1) → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → ∀ᵐ (d : Fin N → D) ∂MeasureTheory.Measure.pi fun x => Q, ∀ (s : ℝ), ∫ (y : Fin N → ℝ), Real.exp (s * Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum mD w d y) ∂(Causalean.Stat.finProductKernel N K) d ≤ Real.exp (s ^ 2 * Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w d / 8)` | Under a probability design law and Markov marking kernel whose conditional mean is the measurable regression, whose marks lie in the unit interval almost surely on almost every fibre, and whose complete-design weight array is measurable, the weighted centered sum has the conditional Hoeffding exponential-moment bound on almost every complete-design fibre. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_weighted_centered_conditional_mgf_le` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → (∀ᵐ (omega : Omega) ∂P, 0 ≤ Y omega) → (∀ᵐ (omega : Omega) ∂P, Y omega ≤ 1) → ∀ (mD : D → ℝ), Measurable mD → P[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[P] mD ∘ design → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → ∀ᵐ (d : Fin N → D) ∂MeasureTheory.Measure.pi fun x => MeasureTheory.Measure.map design P, ∀ (s : ℝ), ∫ (y : Fin N → ℝ), Real.exp (s * Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum mD w d y) ∂(Causalean.Stat.finProductKernel N (ProbabilityTheory.condDistrib Y design P)) d ≤ Real.exp (s ^ 2 * Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w d / 8)` | A probability law P, measurable design, and measurable outcome that is almost surely in the unit interval, together with a measurable regression equal almost surely to the conditional expectation given the design and measurable complete-design weights, give the conditional Hoeffding exponential-moment bound on almost every complete design vector. |
<!-- /GEN -->

#### 12'.0b `Stat/Concentration/RandomDesignWeightedHoeffding/Tail.lean`

Self-normalized two-sided Hoeffding tails for the random-design weighted sums in `Basic`, with a
kernel-native retained-design result and an i.i.d. observation-product corollary.

<!-- GEN:Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.Tail -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_observation_law_map_design_outcome` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → MeasureTheory.Measure.map (fun z => (Causalean.Mathlib.Probability.designVector design z, fun i => Y (z i))) (MeasureTheory.Measure.pi fun x => P) = Causalean.Stat.attachKernel (MeasureTheory.Measure.pi fun x => MeasureTheory.Measure.map design P) (Causalean.Stat.finProductKernel N (ProbabilityTheory.condDistrib Y design P))` | A probability law P, measurable design, and measurable outcome map an i.i.d. observation product to the retained-design law with the finite product of its one-observation conditional mark kernels. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_weighted_centered_attachKernel_tail_le` | `Measurable mD → (∀ᵐ (d : D) ∂Q, ∫ (y : ℝ), y ∂K d = mD d) → (∀ᵐ (d : D) ∂Q, ∀ᵐ (y : ℝ) ∂K d, y ∈ Set.Icc 0 1) → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → (∀ᵐ (d : Fin N → D) ∂MeasureTheory.Measure.pi fun x => Q, 0 < Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w d) → ∀ {t : ℝ}, 0 ≤ t → (Causalean.Stat.attachKernel (MeasureTheory.Measure.pi fun x => Q) (Causalean.Stat.finProductKernel N K)).real {p \| t * √(Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w p.1) ≤ \|Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.weightedCenteredMarkSum mD w p.1 p.2\|} ≤ 2 * Real.exp (-2 * t ^ 2)` | Under a probability design law and Markov marking kernel whose conditional mean is the measurable regression, whose marks lie in the unit interval almost surely on almost every fibre, with measurable complete-design weights of positive realized energy almost surely, every nonnegative threshold obeys the two-sided self-normalized Hoeffding tail bound for the retained-design law. |
| `Stat.Concentration.RandomDesignWeightedHoeffding.product_weighted_centered_tail_le` | `Measurable design → ∀ (Y : Omega → ℝ), Measurable Y → (∀ᵐ (omega : Omega) ∂P, 0 ≤ Y omega) → (∀ᵐ (omega : Omega) ∂P, Y omega ≤ 1) → ∀ (mD : D → ℝ), Measurable mD → P[Y \| MeasurableSpace.comap design inferInstance] =ᵐ[P] mD ∘ design → ∀ (w : (Fin N → D) → Fin N → ℝ), Measurable w → (∀ᵐ (z : Fin N → Omega) ∂MeasureTheory.Measure.pi fun x => P, 0 < Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w (Causalean.Mathlib.Probability.designVector design z)) → ∀ {t : ℝ}, 0 ≤ t → (MeasureTheory.Measure.pi fun x => P).real {z \| t * √(Causalean.Stat.Concentration.RandomDesignWeightedHoeffding.realizedWeightEnergy w (Causalean.Mathlib.Probability.designVector design z)) ≤ \|Causalean.Mathlib.Probability.weightedCenteredSum design Y mD w z\|} ≤ 2 * Real.exp (-2 * t ^ 2)` | A probability law P, measurable design, and measurable outcome that is almost surely in the unit interval, together with a measurable regression equal almost surely to the conditional expectation given the design, measurable complete-design weights of positive realized energy almost surely, and a nonnegative threshold, yield the two-sided self-normalized Hoeffding tail bound for the finite i.i.d. sample. |
<!-- /GEN -->

#### 12'.a `Stat/Concentration/Rademacher.lean`

| Declaration | Statement | Description |
|---|---|---|
| `Signs n` | `def`, `Type` | `{−1, +1}`-valued sign vectors of length `n`. |
| `empiricalRademacherComplexity n f S` | `noncomputable def`, `ℝ` | `(1 / 2ⁿ) Σ_σ supᵢ |(1/n) Σₖ σ k · f i (S k)|`. |
| `rademacherComplexity n f μ X` | `noncomputable def`, `ℝ` | Population version: `E[empiricalRademacherComplexity n f (X ∘ ω)]` under `ω ∼ μⁿ`. |
| `empiricalRademacherComplexity_without_abs` | `noncomputable def`, `ℝ` | Variant without the outer absolute value (used in some symmetrization steps). |
| `uniformDeviation n f μ X S` | `noncomputable def`, `ℝ` | `supᵢ |(1/n) Σₖ f i (S k) − E[f i ∘ X]|`. |
| `uniformDeviation_measurable` | `theorem` | Measurability of `uniformDeviation` for countable function classes with each `f i` measurable. **Proved.** |

#### 12'.a' `Stat/Concentration/ERMOracle.lean` — generic ERM oracle inequality

The method-agnostic learning-theory rate: empirical risk minimization over a countable
hypothesis class with bounded loss has excess risk controlled by the Rademacher complexity of
the loss class plus a McDiarmid tail. The engine that turns a complexity bound for a specific
method's class into a concrete excess-risk rate.

| Declaration | Statement | Description |
|---|---|---|
| `erm_excess_le_two_uniformDeviation` | `theorem` | **Deterministic ERM basic inequality:** an empirical minimizer beating a comparator on the sample has excess population risk `≤ 2·uniformDeviation`. **Proved.** |
| `erm_oracle_inequality` | `theorem` | **Oracle inequality:** `μⁿ{ 4·𝔯ₙ + 2ε < R(ĥ) − R(h⋆) } ≤ exp(−ε² t n)`, chaining the basic inequality with FoML's symmetrization + McDiarmid tail (`𝔯ₙ` = Rademacher complexity of the loss class). **Proved.** |
| `erm_oracle_inequality_separable` | `theorem` | Separable-class version (`SeparableSpace`/`FirstCountableTopology ι`, loss continuous in the parameter) — the form that applies to the uncountable-but-separable L²/L¹-ball linear classes (kernel, lasso). **Proved.** |

#### 12'.b `Stat/Concentration/BoundedDifference.lean`

| Declaration | Statement | Description |
|---|---|---|
| `uniformDeviation_bounded_difference` | `theorem` (`sorry`) | `uniformDeviation n f μ X` has bounded differences with constant `(1/n)·2b` in each coordinate, when each `f i` is bounded by `b`. |
| `bounded_difference_of_bounded` | `theorem` (`sorry`) | The map `ω ↦ supᵢ |Σₖ f i (X (ω k)) − n·E[f i ∘ X]|` has bounded differences with constant `2b`. |

#### 12'.c `Stat/Concentration/McDiarmid.lean`

| Declaration | Statement | Description |
|---|---|---|
| `mcdiarmid_inequality_pos` | `theorem` (`sorry`) | McDiarmid upper-tail: bounded-difference family `(X i)` ⇒ `P(f − E f ≥ ε) ≤ exp(−2ε² t)` whenever `t Σ cᵢ² ≤ 1`. |
| `mcdiarmid_inequality_neg` | `theorem` (`sorry`) | Symmetric lower-tail: `P(f − E f ≤ −ε) ≤ exp(−2ε² t)`. |
| `mcdiarmid_inequality_pos'` | `theorem` (`sorry`) | Single-`X` form on the product measure `μⁿ` — the version actually consumed by `OrthogonalLearning/LocalEmpProcess/Rademacher.lean`. |

#### 12'.d `Stat/Concentration/Symmetrization.lean`

| Declaration | Statement | Description |
|---|---|---|
| `expectation_le_rademacher` | `theorem` (`sorry`) | `E[supᵢ |Σₖ f i (X (ω k)) − n·E f i|] ≤ 2 n · rademacherComplexity n f μ X`. Wainwright Thm 4.10. |

#### 12'.e `Stat/Concentration/Separable.lean`

| Declaration | Statement | Description |
|---|---|---|
| `separableSpaceSup_eq` | `theorem` | For continuous bounded `f : X → E` with `X` separable, `⨆ x, f x = ⨆ i, f (denseSeq X i)`. **Proved.** |
| `separableSpaceSup_eq_real` | `theorem` | Real-valued specialisation handling the unbounded case. **Proved.** |

#### 12'.e2 `Stat/Concentration/CoveringNumber.lean`

Metric covering numbers for totally bounded sets, ported from
`auto-res/lean-rademacher` for the Dudley entropy-integral chain.

| Declaration | Statement | Description |
|---|---|---|
| `coveringNumber_exists` | `TotallyBounded A`, `ε > 0` → `∃ n t, t.card = n ∧ A ⊆ ⋃ y ∈ t, Metric.ball y ε` | Extracts a finite ε-net for a totally bounded set and records its cardinality. |
| `coveringNumber` | `TotallyBounded A → ℝ → ℕ` | Minimal finite ε-cover cardinality for positive ε, and zero for nonpositive ε. |
| `coveringNumber_eq` | `ε > 0` → `coveringNumber ha ε = Nat.find …` | Unfolds the positive-radius branch of `coveringNumber`. |
| `converingNumber_antitone` | `AntitoneOn (coveringNumber ha) (Set.Ioi 0)` | Covering number is antitone in the radius on positive radii. |
| `coveringNumber_nonzero` | `A.Nonempty`, `ε > 0` → `0 < coveringNumber ha ε` | Nonempty totally bounded sets have positive covering number at positive radius. |
| `converingNumber_aemeasurable` | `AEMeasurable (coveringNumber ha) volume` | Measurability of the covering-number function on the real radius line. |
| `coveringFinset` | `TotallyBounded A`, `ε > 0` → `Finset X` | Chooses a finite ε-net attaining `coveringNumber`. |
| `coveringFinset_cover` | `A ⊆ ⋃ y ∈ coveringFinset ha hε, Metric.ball y ε` | The chosen finset covers the target set by ε-balls. |
| `coveringFinset_card` | `(coveringFinset ha hε).card = coveringNumber ha ε` | The chosen finset has cardinality equal to the covering number. |

#### 12'.e3 `Stat/Concentration/DudleyEntropy.lean`

The Dudley entropy-integral chaining bound controls empirical Rademacher
complexity by the covering-number entropy integral of the empirical
pseudometric function space.

| Declaration | Statement | Description |
|---|---|---|
| `dudley_entropy_integral'` | `ε > 0`, total boundedness, positive sample size, empirical-norm radius `c`, and `ε < c/2` → empirical Rademacher complexity without absolute value is bounded by `4ε` plus the entropy integral from `ε` to `c/2` | Core Dudley entropy-integral bound for a fixed sample and function class. |
| `dudley_entropy_integral_bound` | Same bound with the public sample-size parameter name `n` | Headline wrapper ported from FoML `Main.lean`. |

#### 12'.e3b `Stat/Concentration/SqrtLogIntegral.lean`

Elementary real-analysis evaluation used to simplify Dudley entropy-integral
upper bounds with logarithmic kernels.

| Declaration | Statement | Description |
|---|---|---|
| `sqrt_le_add_one_div_two` | `0 ≤ t → sqrt t ≤ (t + 1) / 2` | AM-GM bound for the square root, proved from nonnegativity of `(sqrt t - 1)^2`. |
| `log_div_nonneg_of_mem_Icc` | `0 < ε`, `x ∈ [ε, δ]` → `0 ≤ log (δ / x)` | Positivity of the logarithmic ratio kernel on the integration interval. |
| `integral_log_div_eq` | `0 < ε`, `ε ≤ δ` → `∫ x in ε..δ, log (δ / x) = δ - ε - ε * log (δ / ε)` | Exact closed-form evaluation of the logarithmic ratio integral. |
| `integral_log_div_le` | `0 < ε`, `ε ≤ δ` → `∫ x in ε..δ, log (δ / x) ≤ δ - ε` | Interval-length upper bound obtained from `log (δ / ε) ≥ 0`. |
| `sqrtLog_integral_le` | `0 < ε`, `ε ≤ δ` → `∫ x in ε..δ, sqrt (log (δ / x)) ≤ δ` | Dudley entropy-integral evaluation using the AM-GM pointwise bound and the logarithmic-ratio integral estimate. |

#### 12'.e4 `Stat/Concentration/VCCovering.lean`

VC-dimension control of empirical covering numbers for real-valued classes
whose sample evaluations factor through a Boolean class.

| Declaration | Statement | Description |
|---|---|---|
| `restrictionPattern` | `(𝒳 → Bool) → (Fin n → 𝒳) → Finset (Fin n)` | Records the sample coordinates at which a Boolean classifier is true. |
| `growthFamily` | `(ι → 𝒳 → Bool) → (Fin n → 𝒳) → Finset (Finset (Fin n))` | The finite family of Boolean restriction patterns realized on the sample. |
| `card_growthFamily_le_sum_choose` | `𝒜.vcDim ≤ d → 𝒜.card ≤ ∑ k ∈ Finset.Iic d, n.choose k` | Sauer-Shelah cardinality bound for a finite family on an `n`-point sample. |
| `vc_coveringNumber_le_growth` | Binary factorization of `F` through `π` on the sample and `ε > 0` → `coveringNumber h' ε ≤ (growthFamily π S).card` | Bounds the empirical covering number by choosing one representative per realized Boolean pattern. |
| `vc_coveringNumber_le_sum_choose` | Same hypotheses plus `(growthFamily π S).vcDim ≤ d` → `coveringNumber h' ε ≤ ∑ k ∈ Finset.Iic d, n.choose k` | Combines the finite self-cover with Sauer-Shelah. |
| `sum_choose_le_exp_mul_div_pow` | `0 < d`, `d ≤ n` → `∑_{k≤d} (n choose k) ≤ (e n / d)^d` | Standard binomial-sum estimate; currently the only intentionally scaffolded arithmetic lemma in this file. |
| `log_coveringNumber_le` | Nonempty class, binary factorization, `ε > 0`, `0 < d`, `d ≤ n`, and VC dimension at most `d` → logarithmic covering bound | Converts the VC covering bound into the usual entropy display using the standard binomial-sum estimate. |

#### 12'.e4b `Stat/Concentration/HausslerPacking.lean`

Haussler-style finite-VC packing bounds for weighted Boolean Hamming metrics,
isolating the random-coordinate extraction core from the deterministic
Sauer-Shelah cardinality step.

| Declaration | Statement | Description |
|---|---|---|
| `weightedHammingSq` | Coordinate weights and two Boolean vectors on `Fin n` → weighted Hamming pseudo-distance squared | Sums the weights of coordinates where the two Boolean vectors differ. |
| `subsamplePattern` | Coordinate map `Fin m → Fin n` and Boolean vector → `Finset (Fin m)` | Records the true coordinates after restricting a Boolean vector to a sampled coordinate list. |
| `weightedHammingSq_nonneg` | Nonnegative weights → `0 ≤ weightedHammingSq w a b` | Basic nonnegativity of the weighted Hamming pseudo-distance squared. |
| `card_subsample_family_le_succ_pow` | Restricted trace VC dimension at most `d` → restricted trace cardinality at most `(m + 1)^d` | Applies Sauer-Shelah and a binomial-sum bound to a sampled coordinate family. |
| `subsample_image_vcDim_le` | Original true-set family VC dimension at most `d` → every coordinate restriction has VC dimension at most `d` | Shows restriction along any coordinate map cannot increase VC dimension. |
| `finite_averaging_exists_separating_subsample` | Total weight at most `r²`, `ε²`-separation, `2 ≤ #P`, and `0 < ε ≤ r` → a separating restricted trace with `m ≤ 1 + (2*r²/ε²)*log #P` | Isolates the finite product-sum averaging step for the random-coordinate extraction; currently the remaining `sorry` in this module. |
| `exists_separating_subsample` | VC control, total weight at most `r²`, `ε²`-separation, `2 ≤ #P`, and `0 < ε ≤ r` → a separating restricted trace with `m ≤ 1 + (2*r²/ε²)*log #P` and restricted VC dimension at most `d` | Combines finite averaging with VC-dimension preservation for sampled coordinate families. |
| `self_log_solve` | `L ≤ d*log(2 + a*L)` with `d ≥ 1`, `a ≥ 2`, and `L ≥ 0` → an explicit `1 + 2*d*log(2*a*d)` bound | Scalar self-logarithm inequality used after Sauer-Shelah. |
| `vc_weightedHamming_packing_card_le` | VC control, total weight at most `r²`, and `ε²`-separation → `log #P ≤ 1 + 2*d*log(4*d*r²/ε²)` | Logarithmic Haussler ε-packing bound with no dependence on the ambient coordinate count, modulo the isolated extraction lemma. |
| `vc_weightedHamming_log_packing_le` | Same hypotheses → `log #P ≤ 1 + 2*d*log(4*d*r²/ε²)` | Alias for the clean logarithmic weighted VC packing bound. |

#### 12'.e4ba `Stat/Concentration/Covering/RealValuedVCSubgraph/Basic.lean`

Uniform polynomial L² covering bounds for bounded real-valued VC-subgraph classes under arbitrary probability measures.

<!-- GEN:Causalean.Stat.Concentration.Covering.RealValuedVCSubgraph.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.subgraphClassifier` | `(ι → 𝒳 → ℝ) → ι → 𝒳 × ℝ → Bool` | Given a real-valued function class on an observation space, a class index, and an observation-threshold pair, the strict subgraph classifier returns true exactly when the threshold is strictly below the selected function's value at the observation. |
| `Stat.Concentration.HasPseudoDimAtMost` | `(ι → 𝒳 → ℝ) → ℕ → Prop` | Given a real-valued function class on an observation space and a nonnegative integer $d$, the pseudo-dimension-at-most-$d$ property holds exactly when, for every finite collection of observation-threshold pairs, the VC dimension of the strict-subgraph labelings induced by the class on that collection is at most $d$. |
| `Stat.Concentration.hasPseudoDimAtMost_iff_growthFamily` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d ↔ ∀ (n : ℕ) (T : Fin n → 𝒳 × ℝ), (Causalean.Stat.Concentration.growthFamily (Causalean.Stat.Concentration.subgraphClassifier F) T).vcDim ≤ d` | The pseudo-dimension certificate unfolds to the existing finite Boolean growth-family VC certificate on every thresholded sample. |
| `Stat.Concentration.measureL2Dist` | `MeasureTheory.Measure 𝒳 → (𝒳 → ℝ) → (𝒳 → ℝ) → ℝ` | Given a measure $Q$ on an observation space and two real-valued functions on that space, their measure-based $L^2$ semidistance is $\sqrt{\int (f-g)^2\,dQ}$. |
| `Stat.Concentration.IsL2Cover` | `MeasureTheory.Measure 𝒳 → (ι → 𝒳 → ℝ) → ℝ → Finset ι → Prop` | Given a measure $Q$ on an observation space, a real-valued function class, a radius $r$, and a finite set of class indices, the open $L^2(Q)$ cover property holds exactly when every class member lies at strictly less than distance $r$ from a member indexed by that finite set. |
| `Stat.Concentration.L2CoveringNumberLe` | `MeasureTheory.Measure 𝒳 → (ι → 𝒳 → ℝ) → ℝ → ℕ → Prop` | Given a measure $Q$ on an observation space, a real-valued function class, a radius $r$, and a nonnegative integer $N$, the $L^2(Q)$ covering-number-at-most-$N$ property holds exactly when there is a finite set of at most $N$ class indices that forms an open $L^2(Q)$ cover at radius $r$. |
| `Stat.Concentration.vcSubgraphCoverBound` | `ℕ → ℝ → ℕ` | Given a nonnegative integer $d$ and a real number $\varepsilon$, the explicit VC-subgraph cover bound is the least integer no smaller than $(16/\varepsilon)^{8(d+1)}$. |
| `Stat.Concentration.sharp_vc_weightedHamming_packing_card_le` | `(∀ (j : Fin n), 0 ≤ w j) → ∀ (r ε : ℝ), 0 < ε → ε ≤ r → ∑ j, w j ≤ r ^ 2 → ∀ (P : Finset (Fin n → Bool)), (Finset.image (fun a => {j \| a j = true}) P).vcDim ≤ d → (∀ a ∈ P, ∀ b ∈ P, a ≠ b → ε ^ 2 ≤ Causalean.Stat.Concentration.weightedHammingSq w a b) → P.card ≤ ⌈(16 * r ^ 2 / ε ^ 2) ^ (2 * (d + 1))⌉₊` | A finite Boolean VC class with nonnegative coordinate weights has a dimension-free-base polynomial packing bound in weighted Hamming distance. |
| `Stat.Concentration.real_vcSubgraph_l2_covering` | `(∀ (i : ι), Measurable (F i)) → Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ {U ε : ℝ}, 0 < U → 0 < ε → ε ≤ 1 → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → ∀ (Q : MeasureTheory.Measure 𝒳) [MeasureTheory.IsProbabilityMeasure Q], Causalean.Stat.Concentration.L2CoveringNumberLe Q F (ε * U) (Causalean.Stat.Concentration.vcSubgraphCoverBound d ε)` | Polynomial `L²(Q)` covering number from a pseudo-dimension bound. For a family of measurable real-valued functions of pseudo-dimension at most d, uniformly bounded by a positive envelope U, and a relative radius ε strictly between 0 and 1, the `L²(Q)` covering number at radius ε·U is at most `vcSubgraphCoverBound d ε`, uniformly over every probability measure Q on the domain. |
| `Stat.Concentration.pseudoDim_gives_finite_subgraph_vc` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ (T : Fin n → 𝒳 × ℝ), (Causalean.Stat.Concentration.growthFamily (Causalean.Stat.Concentration.subgraphClassifier F) T).vcDim ≤ d` | The real-valued theorem genuinely reuses the existing finite VC combinatorics: its hypothesis gives the exact threshold-trace certificate needed by `VCCovering` and `HausslerPacking`. |
<!-- /GEN -->

#### 12'.e4bb `Stat/Concentration/Covering/RealValuedVCSubgraph/Algebra.lean`

Closure certificates for finite sums and products of uniformly polynomial-entropy classes.

<!-- GEN:Causalean.Stat.Concentration.Covering.RealValuedVCSubgraph.Algebra -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.HasPolynomialL2Cover` | `(ι → 𝒳 → ℝ) → ℝ → Prop` | A class of functions has uniform polynomial `L²` entropy with envelope `U` when `U` is positive, every function in the class is measurable and pointwise bounded in absolute value by `U`, and the class admits one polynomial covering-number bound, in the relative radius, holding simultaneously for every probability measure and every relative radius in `(0,1]` . |
| `Stat.Concentration.HasPseudoDimAtMost.hasPolynomialL2Cover` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d → (∀ (i : ι), Measurable (F i)) → 0 < U → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → Causalean.Stat.Concentration.HasPolynomialL2Cover F U` | Bounded finite-pseudo-dimension classes admit a polynomial `L²` cover. If a real-valued function class has pseudo-dimension at most d, every member is measurable, and the class is uniformly bounded by a positive envelope U, then the class carries a uniform polynomial `L²` covering certificate at envelope U. |
| `Stat.Concentration.HasPolynomialL2Cover.neg` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun i x => -F i x) U` | Negating every member of a polynomial-entropy class preserves its envelope and uniform polynomial `L²` entropy. |
| `Stat.Concentration.HasPolynomialL2Cover.add` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → Causalean.Stat.Concentration.HasPolynomialL2Cover G V → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun p x => F p.1 x + G p.2 x) (U + V)` | Pointwise addition of two independently indexed polynomial-entropy classes has uniform polynomial `L²` entropy with summed envelope. |
| `Stat.Concentration.HasPolynomialL2Cover.mul` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → Causalean.Stat.Concentration.HasPolynomialL2Cover G V → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun p x => F p.1 x * G p.2 x) (U * V)` | Pointwise multiplication of two independently indexed bounded polynomial-entropy classes has uniform polynomial `L²` entropy with product envelope. |
| `Stat.Concentration.HasPolynomialL2Cover.finSum` | `(∀ (k : K), Causalean.Stat.Concentration.HasPolynomialL2Cover (F k) (U k)) → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun θ x => ∑ k, F k (θ k) x) (∑ k, U k)` | A fixed finite sum of independently indexed polynomial-entropy classes again has uniform polynomial `L²` entropy, with the sum of the envelopes. |
| `Stat.Concentration.HasPolynomialL2Cover.finProd` | `(∀ (k : K), Causalean.Stat.Concentration.HasPolynomialL2Cover (F k) (U k)) → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun θ x => ∏ k, F k (θ k) x) (∏ k, U k)` | A fixed finite product of independently indexed polynomial-entropy classes again has uniform polynomial `L²` entropy, with the product envelope. |
| `Stat.Concentration.HasPolynomialL2CoverWith` | `(ι → 𝒳 → ℝ) → ℝ → ℝ → ℕ → Prop` | A polynomial `L²` covering certificate with named entropy witnesses. Unlike `HasPolynomialL2Cover`, this form retains the particular base and integer exponent, so a family of constructions can share witnesses before its observation-specific parameters are introduced. |
| `Stat.Concentration.HasPolynomialL2CoverWith.forget` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → Causalean.Stat.Concentration.HasPolynomialL2Cover F U` | Forgetting the named witnesses gives the ordinary polynomial-cover certificate. |
| `Stat.Concentration.HasPolynomialL2CoverWith.enlargeEnvelope` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → U ≤ V → Causalean.Stat.Concentration.HasPolynomialL2CoverWith F V A p` | Enlarging a named envelope preserves its entropy witnesses. |
| `Stat.Concentration.HasPseudoDimAtMost.hasPolynomialL2CoverWith` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d → (∀ (i : ι), Measurable (F i)) → 0 < U → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U 16 (8 * (d + 1))` | A bounded measurable pseudo-dimension class has the canonical named entropy witnesses used by the VC-subgraph covering theorem. |
| `Stat.Concentration.HasPolynomialL2CoverWith.neg` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → Causalean.Stat.Concentration.HasPolynomialL2CoverWith (fun i x => -F i x) U A p` | Negation preserves named polynomial-cover witnesses. |
| `Stat.Concentration.HasPolynomialL2CoverWith.add` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → Causalean.Stat.Concentration.HasPolynomialL2CoverWith G V B q → Causalean.Stat.Concentration.HasPolynomialL2CoverWith (fun z x => F z.1 x + G z.2 x) (U + V) (8 * A * B) (p + q + 2)` | Addition combines named polynomial-cover witnesses by the same explicit formula as the ordinary cover algebra. |
| `Stat.Concentration.HasPolynomialL2CoverWith.mul` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → Causalean.Stat.Concentration.HasPolynomialL2CoverWith G V B q → Causalean.Stat.Concentration.HasPolynomialL2CoverWith (fun z x => F z.1 x * G z.2 x) (U * V) (8 * A * B) (p + q + 2)` | Multiplication combines named polynomial-cover witnesses by the same explicit formula as the ordinary cover algebra. |
| `Stat.Concentration.HasPolynomialL2CoverWith.tightenEnvelopeBy` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F (c * V) A p → 1 ≤ c → 0 < V → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ V) → Causalean.Stat.Concentration.HasPolynomialL2CoverWith F V (c * A) p` | If an assembled class has envelope `c * V` but a direct pointwise bound by `V`, shrinking the envelope by the fixed factor `c` costs the same factor in the named entropy base. |
| `Stat.Concentration.HasUniformPolynomialL2CoverOver` | `(S : Type u_1) → {ι : S → Type v} → ((s : S) → ι s → 𝒳 → ℝ) → (S → ℝ) → Prop` | Given an observation space, an auxiliary parameter set $S$, an index set depending on the parameter, a parameter-indexed family of real-valued function classes, and a parameter-indexed envelope, the uniform polynomial $L^2$ covering property over $S$ holds exactly when there are one real entropy base and one nonnegative-integer exponent such that every parameter's class has the corresponding polynomial $L^2$ covering certificate with its designated envelope. |
| `Stat.Concentration.HasUniformPolynomialL2CoverOver.neg` | `Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F U → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s i x => -F s i x) U` | Uniform named covers are stable under pointwise negation. |
| `Stat.Concentration.HasUniformPolynomialL2CoverOver.add` | `Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F U → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S G V → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s z x => F s z.1 x + G s z.2 x) fun s => U s + V s` | Uniform named covers are stable under pointwise addition. |
| `Stat.Concentration.HasUniformPolynomialL2CoverOver.mul` | `Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F U → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S G V → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s z x => F s z.1 x * G s z.2 x) fun s => U s * V s` | Uniform named covers are stable under pointwise multiplication. |
| `Stat.Concentration.HasUniformPolynomialL2CoverOver.finSum` | `(∀ (k : K), Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s => F s k) fun s => U s k) → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s θ x => ∑ k, F s k (θ k) x) fun s => ∑ k, U s k` | A nonempty finite sum preserves entropy witnesses uniformly over all auxiliary parameters. |
<!-- /GEN -->

#### 12'.e4bc `Stat/Concentration/Covering/RealValuedVCSubgraph/Parametric.lean`

Finite-dimensional bounded linear classes and finite-VC indicator compositions for the real-valued covering API.

<!-- GEN:Causalean.Stat.Concentration.Covering.RealValuedVCSubgraph.Parametric -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.CoeffBox` | `Type w → ℝ → Type (max 0 w)` | Given an index set of coefficients and a real bound, the coefficient box is the set of real coefficient vectors indexed by that set for which every coordinate has absolute value at most $B$. |
| `Stat.Concentration.linearParameterClass` | `(K → 𝒳 → ℝ) → (B : ℝ) → Causalean.Stat.Concentration.CoeffBox K B → 𝒳 → ℝ` | Given a finite index set, a real-valued feature family, a coefficient bound, a coefficient vector in the corresponding box, and an evaluation point, the linear parameter class evaluation is the sum of each coefficient times its corresponding feature value at that point. |
| `Stat.Concentration.linearParameterClass_hasPseudoDimAtMost` | `Causalean.Stat.Concentration.HasPseudoDimAtMost (Causalean.Stat.Concentration.linearParameterClass φ B) (Fintype.card K + 1)` | A finite-dimensional linear class has pseudo-dimension bounded by the number of coordinates (the stated `+1` leaves room for the affine threshold). |
| `Stat.Concentration.linearParameterClass_hasPolynomialL2Cover` | `0 < B → 0 < M → (∀ (k : K), Measurable (φ k)) → (∀ (k : K) (x : 𝒳), \|φ k x\| ≤ M) → Causalean.Stat.Concentration.HasPolynomialL2Cover (Causalean.Stat.Concentration.linearParameterClass φ B) (↑(Fintype.card K) * B * M)` | Covering certificate for a bounded-coefficient linear class. Given a finite family of measurable real-valued features, each bounded in absolute value by M, where B is a positive coefficient bound and M is positive, the class of linear combinations of the features with each coefficient constrained to `[-B,B]` carries a uniform polynomial `L²` covering certificate at envelope `\|K\|·B·M`. |
| `Stat.Concentration.HasVCAtMost` | `(κ → 𝒳 → Bool) → ℕ → Prop` | Given a Boolean classifier family and a natural number, the property of having VC dimension at most that number means that, for every finite sample, the VC dimension of the label patterns realized on that sample is at most $d$. |
| `Stat.Concentration.HasVCAtMost.indicatorClass_hasPseudoDimAtMost` | `Causalean.Stat.Concentration.HasVCAtMost π d → Causalean.Stat.Concentration.HasPseudoDimAtMost (fun j x => if π j x = true then 1 else 0) d` | A Boolean VC class, viewed as a real-valued zero-one indicator class, has the same pseudo-dimension bound. |
| `Stat.Concentration.HasPolynomialL2Cover.mulIndicator` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → (∀ (j : κ), MeasurableSet {x \| π j x = true}) → Causalean.Stat.Concentration.HasVCAtMost π d → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun p x => F p.1 x * if π p.2 x = true then 1 else 0) U` | Multiplying a polynomial-entropy real class by a measurable finite-VC family of indicators preserves uniform polynomial `L²` entropy. |
<!-- /GEN -->

#### 12'.e4bd `Stat/Concentration/Covering/RealValuedVCSubgraph/Empirical.lean`

Empirical-measure specialization and the direct bridge from real-valued VC-subgraph entropy to Dudley chaining.

<!-- GEN:Causalean.Stat.Concentration.Covering.RealValuedVCSubgraph.Empirical -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.finiteSampleMeasure` | `(Fin n → 𝒳) → MeasureTheory.Measure 𝒳` | For a sample of $n$ observations in a measurable space, the finite-sample measure assigns equal mass $1/n$ to each observation, equivalently forming the normalized sum of point masses at the observations (with the displayed formula also determining the value when $n=0$). |
| `Stat.Concentration.finiteSampleMeasure_isProbabilityMeasure` | `0 < n → MeasureTheory.IsProbabilityMeasure (Causalean.Stat.Concentration.finiteSampleMeasure S)` | The empirical law of a positive-size finite sample is a probability measure. |
| `Stat.Concentration.integral_finiteSampleMeasure` | `0 < n → ∀ {f : 𝒳 → ℝ}, Measurable f → ∫ (x : 𝒳), f x ∂Causalean.Stat.Concentration.finiteSampleMeasure S = 1 / ↑n * ∑ i, f (S i)` | Integration against the empirical law is the arithmetic average of a measurable real-valued function over the sample. |
| `Stat.Concentration.measureL2Dist_finiteSampleMeasure_eq_empiricalDist` | `0 < n → ∀ {f g : 𝒳 → ℝ}, Measurable f → Measurable g → Causalean.Stat.Concentration.measureL2Dist (Causalean.Stat.Concentration.finiteSampleMeasure S) f g = Causalean.Stat.Concentration.empiricalDist S f g` | `L²` distance under the empirical law agrees exactly with the empirical root-mean-square distance used by the existing Dudley API. |
| `Stat.Concentration.real_vcSubgraph_empirical_l2_covering` | `(∀ (i : ι), Measurable (F i)) → Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ {U ε : ℝ}, 0 < U → 0 < ε → ε ≤ 1 → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → ∀ (S : Fin n → 𝒳), 0 < n → Causalean.Stat.Concentration.L2CoveringNumberLe (Causalean.Stat.Concentration.finiteSampleMeasure S) F (ε * U) (Causalean.Stat.Concentration.vcSubgraphCoverBound d ε)` | The arbitrary-measure VC-subgraph theorem specializes to the empirical law of every positive-size finite sample. |
| `Stat.Concentration.real_vcSubgraph_empirical_totallyBounded` | `(∀ (i : ι), Measurable (F i)) → Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ {U : ℝ}, 0 < U → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → ∀ (S : Fin n → 𝒳), 0 < n → TotallyBounded Set.univ` | A pseudo-dimension certificate makes the existing empirical function space totally bounded on every positive-size finite sample. |
| `Stat.Concentration.real_vcSubgraph_empirical_coveringNumber_le` | `0 < ε → ε ≤ 1 → ∀ (henvelope : ∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) (S : Fin n → 𝒳) (hn : 0 < n), have htot := Causalean.Stat.Concentration.real_vcSubgraph_empirical_totallyBounded hmeas hpdim hU henvelope S hn; Causalean.Stat.Concentration.coveringNumber htot (ε * U) ≤ Causalean.Stat.Concentration.vcSubgraphCoverBound d ε` | The empirical covering number used by Dudley is polynomially bounded at every envelope-relative radius, with the same arbitrary-measure constants. |
| `Stat.Concentration.real_vcSubgraph_dudley_example` | `(∀ (i : ι), Measurable (F i)) → Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ {U δ : ℝ}, 0 < U → 0 < δ → δ < U / 2 → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → ∀ (S : Fin n → 𝒳), 0 < n → ∃ (htot : TotallyBounded Set.univ), (∀ (ε : ℝ), 0 < ε → ε ≤ 1 → Causalean.Stat.Concentration.coveringNumber htot (ε * U) ≤ Causalean.Stat.Concentration.vcSubgraphCoverBound d ε) ∧ empiricalRademacherComplexity_without_abs n F S ≤ 4 * δ + 12 / √↑n * ∫ (x : ℝ) in δ..U / 2, √(Real.log ↑(Causalean.Stat.Concentration.coveringNumber htot x))` | Direct application to the fixed-sample Dudley bound. For a measurable class of pseudo-dimension at most d, uniformly bounded by a positive envelope U, and a positive scale δ strictly less than U/2, evaluated on a sample S of positive size n, the class's empirical restriction is totally bounded, its covering number at every relative radius ε in `(0,1]` obeys the same polynomial bound `vcSubgraphCoverBound d ε`, and consequently its empirical Rademacher complexity without the outer absolute value is at most the Dudley entropy-integral bound `4δ + (12/√n) ∫_δ^(U/2) √(log(coveringNumber x)) dx`. |
<!-- /GEN -->

#### 12'.e4be `Stat/Concentration/Covering/EuclideanRadialPolynomial/` — moving-center radial classes

Finite-trace semialgebraic certificates for compactly supported Euclidean
radial monomials, including odd powers, atomic boundary mass, finite signed
arms, shared-center boxed polynomials, and bounded residual scores.  The
resulting polynomial `L²(Q)` covers are uniform over every probability law.

<!-- GEN:Causalean.Stat.Concentration.Covering.EuclideanRadialPolynomial.Trace -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.EuclideanRadialPolynomial.booleanCombinationVCBound` | `ℕ → ℕ → ℕ` | Given a number of Boolean component classes and a common VC-dimension bound, the Boolean-combination VC bound is the explicit natural number $2^{m(d+1)+1}$. |
| `Stat.Concentration.EuclideanRadialPolynomial.finiteUnionVCBound` | `ℕ → ℕ → ℕ` | Given a number of component classes and a common VC-dimension bound, the finite-union VC bound is the explicit natural number $2^{m(d+1)+1}$. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasVCAtMost.reindex` | `Causalean.Stat.Concentration.HasVCAtMost π d → ∀ (e : κ → ι), Causalean.Stat.Concentration.HasVCAtMost (fun k => π (e k)) d` | Reparameterizing a Boolean class by an arbitrary map cannot increase its finite-trace VC dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasVCAtMost.compDomain` | `Causalean.Stat.Concentration.HasVCAtMost π d → ∀ (g : 𝒴 → 𝒳), Causalean.Stat.Concentration.HasVCAtMost (fun i y => π i (g y)) d` | Precomposing the observation argument of a Boolean class cannot increase its finite-trace VC dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPseudoDimAtMost.reindex` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ (e : κ → ι), Causalean.Stat.Concentration.HasPseudoDimAtMost (fun k => F (e k)) d` | Reparameterizing a real-valued class by an arbitrary map cannot increase its pseudo-dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPseudoDimAtMost.compDomain` | `Causalean.Stat.Concentration.HasPseudoDimAtMost F d → ∀ (g : 𝒴 → 𝒳), Causalean.Stat.Concentration.HasPseudoDimAtMost (fun i y => F i (g y)) d` | Precomposing every function in a real-valued class with a fixed map cannot increase its pseudo-dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.linearSignClass` | `(K → 𝒳 → ℝ) → (K → ℝ) → 𝒳 → Bool` | Given a finite feature index set, a real-valued feature family, a real coefficient vector, and an evaluation point, the homogeneous linear-sign classifier returns true exactly when the coefficient-feature inner product at that point is strictly positive. |
| `Stat.Concentration.EuclideanRadialPolynomial.linearSignClass_hasVCAtMost` | `Causalean.Stat.Concentration.HasVCAtMost (Causalean.Stat.Concentration.EuclideanRadialPolynomial.linearSignClass φ) (Fintype.card K)` | Homogeneous linear threshold classifiers in `K` real coordinates have VC dimension at most the number of coordinates. |
| `Stat.Concentration.EuclideanRadialPolynomial.booleanCombination_hasVCAtMost` | `(∀ (j : Fin m), Causalean.Stat.Concentration.HasVCAtMost (π j) d) → ∀ (combine : 𝒳 → (Fin m → Bool) → Bool), Causalean.Stat.Concentration.HasVCAtMost (fun θ x => combine x fun j => π j (θ j) x) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.booleanCombinationVCBound m d)` | VC bound for a point-dependent Boolean combination of classes. Given m independently parameterized Boolean classifier families, each of VC dimension at most d, applying to them any combining rule that may itself depend on the sampled point still yields a Boolean class of VC dimension at most `booleanCombinationVCBound m d`. |
| `Stat.Concentration.EuclideanRadialPolynomial.finiteUnion_hasVCAtMost` | `(∀ (k : K), Causalean.Stat.Concentration.HasVCAtMost (π k) d) → Causalean.Stat.Concentration.HasVCAtMost (fun θ => π θ.fst θ.snd) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.finiteUnionVCBound (Fintype.card K) d)` | A finite union of `m` Boolean classes of VC dimension at most `d` has finite VC dimension bounded by `finiteUnionVCBound m d`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.Covering.EuclideanRadialPolynomial.Geometry -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint` | `ℕ → Type` | Given a natural dimension, the Euclidean point space is the real Euclidean space of that dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.RadiusPoint` | `ℕ → Type` | Given a natural dimension, the point-radius space is the set of pairs consisting of a point in the corresponding real Euclidean space and an arbitrary real radius threshold. |
| `Stat.Concentration.EuclideanRadialPolynomial.movingCenterClosedBallClassifier` | `(d : ℕ) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadiusPoint d → Bool` | Given a natural dimension, a center point, and a point together with a radius threshold, the moving-center closed-ball classifier returns true exactly when the point lies at distance no greater than the threshold from the center. |
| `Stat.Concentration.EuclideanRadialPolynomial.movingCenterBallExteriorClassifier` | `(d : ℕ) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadiusPoint d → Bool` | Given a natural dimension, a center point, and a point together with a radius threshold, the moving-center exterior classifier returns true exactly when the point lies at distance strictly greater than the threshold from the center. |
| `Stat.Concentration.EuclideanRadialPolynomial.movingCenterClosedBall_hasVCAtMost` | `Causalean.Stat.Concentration.HasVCAtMost (Causalean.Stat.Concentration.EuclideanRadialPolynomial.movingCenterClosedBallClassifier d) (d + 2)` | Variable-radius closed-ball tests indexed by a moving Euclidean center have VC dimension at most `d + 2`. |
| `Stat.Concentration.EuclideanRadialPolynomial.movingCenterBallExterior_hasVCAtMost` | `Causalean.Stat.Concentration.HasVCAtMost (Causalean.Stat.Concentration.EuclideanRadialPolynomial.movingCenterBallExteriorClassifier d) (d + 2)` | Variable-radius strict-exterior tests indexed by a moving Euclidean center have VC dimension at most `d + 2`. |
| `Stat.Concentration.EuclideanRadialPolynomial.ClosedBallParam` | `ℕ → Type` | Given a natural dimension, the closed-ball parameter space is the set of pairs consisting of a point in that real Euclidean space and a nonnegative real radius. |
| `Stat.Concentration.EuclideanRadialPolynomial.euclideanClosedBallClassifier` | `(d : ℕ) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.ClosedBallParam d → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → Bool` | Given a natural dimension, a closed-ball parameter, and a point in the corresponding Euclidean space, the closed-ball classifier returns true exactly when the point lies in the closed ball specified by that parameter. |
| `Stat.Concentration.EuclideanRadialPolynomial.euclideanClosedBall_hasVCAtMost` | `Causalean.Stat.Concentration.HasVCAtMost (Causalean.Stat.Concentration.EuclideanRadialPolynomial.euclideanClosedBallClassifier d) (d + 2)` | Closed balls in `d`-dimensional Euclidean space have VC dimension at most `d + 2`. This neutral Causalean-only statement replaces the paper-local planar ball lemma with a finite-dimensional bound. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial` | `(d : ℕ) → ℝ → ℝ → ℝ → ℕ → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → ℝ` | Given a natural dimension, a bandwidth, two relative annulus endpoints, a natural-number degree, a center point, and an evaluation point, the radial annulus monomial equals $(\operatorname{dist}(z,x)/q)^k$ when the distance from the evaluation point to the center lies between $aq$ and $bq$, inclusive, and equals zero otherwise. |
| `Stat.Concentration.EuclideanRadialPolynomial.RadialMonomialParam` | `ℕ → ℕ → Type` | Given a natural dimension and a maximal natural-number degree, the radial-monomial parameter space consists of a center in that Euclidean space paired with a degree from zero through $p$. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialClass` | `(d p : ℕ) → ℝ → ℝ → ℝ → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadialMonomialParam d p → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → ℝ` | Given a natural dimension, a maximal degree, a bandwidth and two relative annulus endpoints, a center-and-degree parameter, and a Euclidean evaluation point, the radial-monomial class maps that parameter and point to the corresponding radial annulus monomial. |
| `Stat.Concentration.EuclideanRadialPolynomial.fixedRadialPseudoDimBound` | `ℕ → ℕ` | Given a natural dimension, the fixed-degree radial pseudo-dimension bound is the explicit Boolean-combination bound $2^{3(d+3)+1}$. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialPseudoDimBound` | `ℕ → ℕ → ℕ` | Given a natural dimension and a maximal degree, the radial pseudo-dimension bound is the explicit finite-union bound $2^{(p+1)(2^{3(d+3)+1}+1)+1}$ obtained by combining $p+1$ fixed-degree bounds. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial_hasPseudoDimAtMost` | `0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPseudoDimAtMost (fun x => Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial d q a b k x) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.fixedRadialPseudoDimBound d)` | For every positive bandwidth and ordered nonnegative annulus, the class of fixed-degree radial monomials with moving center has pseudo-dimension bounded solely by the Euclidean dimension. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialClass_hasPseudoDimAtMost` | `0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPseudoDimAtMost (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialClass d p q a b) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialPseudoDimBound d p)` | Pseudo-dimension bound for the moving-center radial-monomial class. For a positive bandwidth q, a nonnegative annulus inner radius a, and inner radius at most outer radius b, allowing both the Euclidean center and the monomial degree — ranging from zero through p — to vary gives the radial-monomial class a pseudo-dimension of at most `radialPseudoDimBound d p`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.Covering.EuclideanRadialPolynomial.Cover -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope` | `ℝ → ℕ → ℝ` | For an upper relative radius $b$ and a nonnegative integer $p$, the radial-monomial envelope is $(\max\{1,b\})^p$. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial_measurable` | `Measurable (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial d q a b k x)` | A compactly supported Euclidean radial monomial is Borel measurable in the observation for every fixed center and degree. |
| `Stat.Concentration.EuclideanRadialPolynomial.abs_radialAnnulusMonomial_le` | `0 < q → 0 ≤ a → a ≤ b → k ≤ p → ∀ (x z : Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d), \|Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial d q a b k x z\| ≤ Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p` | Every degree `k ≤ p` radial monomial on an ordered nonnegative annulus is bounded in absolute value by `radialMonomialEnvelope b p`. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPolynomialL2Cover.pullback` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → ∀ (e : κ → ι), Causalean.Stat.Concentration.HasPolynomialL2Cover (fun k => F (e k)) U` | Pulling a polynomial-cover class back along an arbitrary parameter map preserves a polynomial `L²` covering certificate when the new parameter type is nonempty. Cover centers are replaced by occupied-class representatives, which costs only a factor two in radius. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPolynomialL2CoverWith.pullback` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → ∀ (e : κ → ι), Causalean.Stat.Concentration.HasPolynomialL2CoverWith (fun k => F (e k)) U (2 * A) p` | Pullback preserves named entropy witnesses, with the explicit factor-two radius cost used by the ordinary pullback construction. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasUniformPolynomialL2CoverOver.pullback` | `Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F U → (∀ (s : S), Nonempty (κ s)) → ∀ (e : (s : S) → κ s → ι s), Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S (fun s k => F s (e s k)) U` | A parameterwise pullback preserves entropy witnesses uniformly over the outer parameter family. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPolynomialL2Cover.monoEnvelope` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → U ≤ V → Causalean.Stat.Concentration.HasPolynomialL2Cover F V` | Enlarging the positive envelope of a polynomial `L²` covering certificate preserves the certificate and its polynomial constants. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasPolynomialL2CoverWith.monoEnvelope` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A p → U ≤ V → Causalean.Stat.Concentration.HasPolynomialL2CoverWith F V A p` | Enlarging an envelope preserves named entropy witnesses. |
| `Stat.Concentration.EuclideanRadialPolynomial.HasUniformPolynomialL2CoverOver.monoEnvelope` | `Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F U → (∀ (s : S), U s ≤ V s) → Causalean.Stat.Concentration.HasUniformPolynomialL2CoverOver S F V` | A parameterwise envelope enlargement preserves uniform named witnesses. |
| `Stat.Concentration.EuclideanRadialPolynomial.finiteClass_hasPolynomialL2Cover` | `0 < U → (∀ (i : ι), Measurable (F i)) → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → Causalean.Stat.Concentration.HasPolynomialL2Cover F U` | Every finite family of measurable functions bounded by a positive common envelope has a uniform polynomial `L²` covering certificate. |
| `Stat.Concentration.EuclideanRadialPolynomial.finiteClass_hasPolynomialL2CoverWith` | `0 < U → (∀ (i : ι), Measurable (F i)) → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U (max 1 ↑(Fintype.card ι)) 1` | A finite bounded measurable class has named entropy witnesses depending only on its cardinality. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialClass_hasPolynomialL2Cover` | `0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPolynomialL2Cover (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialClass d p q a b) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p)` | Moving-center radial monomials of every degree from zero through `p` have a polynomial `L²(Q)` cover with a positive constant envelope, uniformly over every probability measure `Q`. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial_hasPolynomialL2Cover` | `0 < q → 0 ≤ a → a ≤ b → k ≤ p → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun x => Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialAnnulusMonomial d q a b k x) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p)` | A fixed degree `k ≤ p` of the moving-center radial class has the same uniform polynomial cover and envelope as the full degree vector. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn` | `(d p : ℕ) → (Ω → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d) → ℝ → ℝ → ℝ → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadialMonomialParam d p → Ω → ℝ` | Given an observation space $\Omega$, a Euclidean dimension $d$, a maximum degree $p$, a location map from observations to $d$-dimensional Euclidean space, a bandwidth $q$, annulus radii $a$ and $b$, a radial-monomial parameter θ, and an observation $\omega$, the radial monomial evaluated at that observation is the corresponding radial-monomial class function evaluated at the mapped location. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn_hasPolynomialL2Cover` | `Measurable loc → 0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPolynomialL2Cover (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn d p loc q a b) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p)` | Covering certificate transported through a location map. For a measurable map from the underlying observation space into `d`-dimensional Euclidean space, given a positive bandwidth q, a nonnegative annulus inner radius a, and inner radius at most outer radius b, composing the moving-center radial-monomial class of degree at most p with the location map still carries a uniform polynomial `L²` covering certificate at envelope `radialMonomialEnvelope b p`. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn_hasPolynomialL2CoverWith` | `Measurable loc → 0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPolynomialL2CoverWith (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn d p loc q a b) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p) 16 (8 * (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialPseudoDimBound d p + 1))` | The moving-center radial class after a measurable location map has the canonical named VC-subgraph entropy witnesses. |
| `Stat.Concentration.EuclideanRadialPolynomial.finiteSignedArmRadial_hasPolynomialL2Cover` | `Measurable loc → (∀ (s : A), Measurable (arm s)) → (∀ (s : A) (ω : Ω), \|arm s ω\| ≤ 1) → 0 < q → 0 ≤ a → a ≤ b → Causalean.Stat.Concentration.HasPolynomialL2Cover (fun θ ω => Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialOn d p loc q a b θ.1 ω * arm θ.2 ω) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialMonomialEnvelope b p)` | Multiplying a moving-center radial monomial by any bounded measurable finite family of signed arms preserves a uniform polynomial cover. Boolean arms and the two signs are obtained by taking a finite arm type and values in `{0,1}` or `{-1,1}`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.Covering.EuclideanRadialPolynomial.Score -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.EuclideanRadialPolynomial.boundedRadialPolynomial` | `(d p : ℕ) → ℝ → ℝ → ℝ → (B : ℝ) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → Causalean.Stat.Concentration.CoeffBox (Fin (p + 1)) B → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d → ℝ` | Given a natural dimension, a maximal degree, a bandwidth and two relative annulus endpoints, a coefficient bound, a center, a boxed coefficient vector, and an evaluation point, the bounded radial polynomial is the sum, over degrees zero through $p$, of each coefficient times the corresponding radial annulus monomial about the shared center. |
| `Stat.Concentration.EuclideanRadialPolynomial.RadialPolynomialParam` | `ℕ → ℕ → ℝ → Type` | Given a natural dimension, a maximal degree, and a coefficient bound, the radial-polynomial parameter space consists of a Euclidean center paired with a coefficient vector whose every coordinate has absolute value at most $B$. |
| `Stat.Concentration.EuclideanRadialPolynomial.boundedRadialPolynomialOn` | `(d p : ℕ) → (Ω → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d) → ℝ → ℝ → ℝ → (B : ℝ) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadialPolynomialParam d p B → Ω → ℝ` | Given an observation space equipped with a σ-algebra, a natural dimension, a maximal degree, a Euclidean location map, a bandwidth and two relative annulus endpoints, a coefficient bound, a radial-polynomial parameter, and an observation, the observation-indexed bounded radial polynomial is the bounded radial polynomial evaluated at the Euclidean location of that observation. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialPolynomialEnvelope` | `ℝ → ℕ → ℝ → ℝ` | Given an outer relative radius, a maximal degree, and a coefficient bound, the radial-polynomial envelope is $(p+1)B$ times the radial-monomial envelope at that outer radius and degree. |
| `Stat.Concentration.EuclideanRadialPolynomial.abs_boundedRadialPolynomial_le` | `0 < q → 0 ≤ a → a ≤ b → 0 ≤ B → ∀ (x : Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d) (β : Causalean.Stat.Concentration.CoeffBox (Fin (p + 1)) B) (z : Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d), \|Causalean.Stat.Concentration.EuclideanRadialPolynomial.boundedRadialPolynomial d p q a b B x β z\| ≤ Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialPolynomialEnvelope b p B` | Every boxed shared-center radial polynomial is bounded by the number of basis functions times the coefficient bound times the monomial envelope. |
| `Stat.Concentration.EuclideanRadialPolynomial.boundedRadialPolynomialOn_hasPolynomialL2Cover` | `Measurable loc → 0 < q → 0 ≤ a → a ≤ b → 0 < B → Causalean.Stat.Concentration.HasPolynomialL2Cover (Causalean.Stat.Concentration.EuclideanRadialPolynomial.boundedRadialPolynomialOn d p loc q a b B) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialPolynomialEnvelope b p B)` | Boxed finite radial polynomials with one moving center shared by all degrees have a uniform polynomial `L²` covering certificate. |
| `Stat.Concentration.EuclideanRadialPolynomial.RadialResidualScoreParam` | `ℕ → ℕ → ℝ → Type v → Type (max 0 v)` | Given a natural dimension, a maximal degree, a coefficient bound, and an arm index set, the radial residual-score parameter space consists of a radial-polynomial parameter paired with one arm index. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialResidualScore` | `(d p : ℕ) → (Ω → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d) → (A → Ω → ℝ) → (Ω → ℝ) → ℝ → ℝ → ℝ → (B : ℝ) → Fin (p + 1) → Causalean.Stat.Concentration.EuclideanRadialPolynomial.RadialResidualScoreParam d p B A → Ω → ℝ` | Given an observation space equipped with a σ-algebra, an arm index set, a natural dimension, a maximal degree, a Euclidean location map, an arm-valued function, a response function, a bandwidth and two relative annulus endpoints, a coefficient bound, a degree index, a residual-score parameter, and an observation, the radial residual score is the product of the arm value, the selected radial annulus monomial, and the residual obtained by subtracting the shared-center boxed radial-polynomial fit from the response. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialResidualScoreEnvelope` | `ℝ → ℕ → ℝ → ℝ → ℝ` | Given an outer relative radius, a maximal degree, a coefficient bound, and a response bound, the radial residual-score envelope is the radial-monomial envelope times the sum of $R$ and the radial-polynomial envelope. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialResidualScore_hasPolynomialL2Cover` | `Measurable loc → (∀ (s : A), Measurable (arm s)) → (∀ (s : A) (ω : Ω), \|arm s ω\| ≤ 1) → Measurable response → (∀ (ω : Ω), \|response ω\| ≤ R) → 0 < q → 0 ≤ a → a ≤ b → 0 < B → 0 < R → Causalean.Stat.Concentration.HasPolynomialL2Cover (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialResidualScore d p loc arm response q a b B j) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialResidualScoreEnvelope b p B R)` | Covering certificate for a bounded finite-arm residual score. Given a measurable Euclidean location map, a finite family of arms, each measurable and bounded in absolute value by 1, a measurable response variable that is bounded in absolute value by R, together with a positive bandwidth q, a nonnegative annulus inner radius a, inner radius at most outer radius b, a positive polynomial-coefficient bound B, and a positive response bound R, then the score formed by multiplying an arm indicator, a radial monomial, and the residual of the response against a boxed radial-polynomial fit carries a positive-envelope uniform polynomial `L²(Q)` covering certificate — with envelope `radialResidualScoreEnvelope b p B R` — over every probability measure Q, including atomic ones. |
| `Stat.Concentration.EuclideanRadialPolynomial.radialResidualScore_hasUniformPolynomialL2CoverWith` | `∃ C n, ∀ (loc : Ω → Causalean.Stat.Concentration.EuclideanRadialPolynomial.EuclideanPoint d) (arm : A → Ω → ℝ) (response : Ω → ℝ) {q a b B R : ℝ}, Measurable loc → (∀ (s : A), Measurable (arm s)) → (∀ (s : A) (ω : Ω), \|arm s ω\| ≤ 1) → Measurable response → (∀ (ω : Ω), \|response ω\| ≤ R) → 0 < q → 0 ≤ a → a ≤ b → 0 < B → 0 < R → Causalean.Stat.Concentration.HasPolynomialL2CoverWith (fun θ => Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialResidualScore d p loc arm response q a b B θ.2 θ.1) (Causalean.Stat.Concentration.EuclideanRadialPolynomial.radialResidualScoreEnvelope b p B R) C n` | The radial residual-score construction admits entropy witnesses depending only on the Euclidean dimension, polynomial degree, and finite arm type. In particular, the witnesses precede all location maps, arm functions, responses, bandwidths, annuli, and envelope radii. |
<!-- /GEN -->

#### 12'.e4bf `Stat/Concentration/VarianceAdaptiveVCExpectedMaximal/` — variance-adaptive VC-type expected suprema

For a countable measurable real-valued class, this module combines empirical
L² covers, Dudley chaining, localization, and symmetrization.  Its main bound
retains the population L² radius in the leading term and needs no separate
measurability assumption for a continuum supremum.

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover` | `(ι → 𝒳 → ℝ) → ℝ → ℝ → ℝ → Prop` | Given a real-valued function class on an observation space, an envelope $U$, an entropy base $A$, and an exponent $v$, the polynomial empirical $L^2$ covering property holds exactly when, for every positive sample size and every sample of that size, and for every radius $\varepsilon$ with $0<\varepsilon\le 1$, there is a finite index set forming an $L^2$ cover under the empirical measure at radius $\varepsilon U$ and its cardinality is at most $(A/\varepsilon)^v$. |
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover.enlargeEnvelope` | `Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → U ≤ V → Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F V A v` | Enlarging the envelope of an empirical polynomial cover preserves its entropy witnesses. |
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover.pullback` | `Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → (∀ (i : ι), Measurable (F i)) → ∀ (e : κ → ι), Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover (fun k => F (e k)) U (2 * A) v` | Restricting a measurable polynomial empirical-cover class along a nonempty parameter map preserves its exponent and costs only a factor two in the entropy base. The replacement centers are chosen inside the restricted class. |
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover.monoEnvelope` | `Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → U ≤ V → Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F V A v` | Enlarging the envelope in a polynomial empirical-cover certificate keeps the same centers, entropy base, and exponent. |
| `Stat.Concentration.vcMaximalLog` | `ℝ → ℝ → ℝ → ℝ` | Given an entropy base $A$, an envelope $U$, and a variance scale $\sigma$, the normalized logarithmic complexity is $\log(\max\{e,AU/\sigma\})$. This normalization makes the logarithm at least one. |
| `Stat.Concentration.vcExpectedMaximalRate` | `ℝ → ℝ → ℝ → ℝ → ℕ → ℝ` | Given an envelope $U$, a variance scale $\sigma$, an entropy base $A$, an exponent $v$, and a sample size $n$, the variance-adaptive VC-type rate is $\sigma\sqrt{vL/n}+vUL/n$, where $L=\log(\max\{e,AU/\sigma\})$. |
| `Stat.Concentration.varianceAdaptiveVCConstant` | `ℝ` | The fixed numerical constant for the variance-adaptive VC-type expected maximal inequality is $16384$. |
| `Stat.Concentration.countableEmpiricalSup` | `MeasureTheory.Measure 𝒳 → (ι → 𝒳 → ℝ) → {n : ℕ} → (Fin n → 𝒳) → ℝ` | Given a measure $P$ on an observation space, a real-valued function class indexed by a set $\iota$, and a sample of $n$ observations, the countable empirical supremum is the supremum, over the class, of the absolute difference between the sample average and the population mean. |
| `Stat.Concentration.HasPolynomialL2Cover.hasPolynomialEmpiricalL2Cover` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → ∃ A v, Real.exp 1 ≤ A ∧ 1 ≤ v ∧ Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v` | Empirical covering from population covering. If a family `F` of real-valued functions with envelope `U` admits a uniform polynomial $L^2$ covering-number bound over every probability measure, then there exist a base `A` at least Euler's number and an exponent `v` at least one such that `F` has polynomial empirical $L^2$ covering numbers with envelope `U`, base `A`, and exponent `v`. |
| `Stat.Concentration.HasPolynomialL2CoverWith.hasPolynomialEmpiricalL2Cover` | `Causalean.Stat.Concentration.HasPolynomialL2CoverWith F U A₀ p → Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U (max (Real.exp 1) (2 * A₀)) ↑(p + 1)` | Named arbitrary-law entropy witnesses give correspondingly named empirical witnesses after the canonical Euler-base and positive-exponent normalization. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.EmpiricalCover -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover.totallyBounded` | `Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → (∀ (i : ι), Measurable (F i)) → 0 < U → ∀ {n : ℕ} (S : Fin n → 𝒳), 0 < n → TotallyBounded Set.univ` | A positive-envelope polynomial empirical covering certificate makes the class image totally bounded in the empirical root-mean-square pseudometric on every nonempty finite sample. |
| `Stat.Concentration.HasPolynomialEmpiricalL2Cover.coveringNumber_le` | `0 < ε → ε ≤ 1 → have htot := Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover.totallyBounded hcover hmeas hU S hn; ↑(Causalean.Stat.Concentration.coveringNumber htot (ε * U)) ≤ (A / ε).rpow v` | Covering-number bound from an empirical cover. Suppose `F` has polynomial empirical $L^2$ covering numbers with envelope `U`, base `A`, and exponent `v`, every member of `F` is measurable, the envelope `U` is strictly positive, the sample `S` has positive size `n`, and the relative scale `ε` lies in the interval $(0,1]$ ; then Dudley's empirical covering number of the class at radius `ε * U`, taken in the totally bounded empirical pseudometric space this cover furnishes, is at most the real power `(A / ε) ^ v`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.Rademacher -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.empiricalRademacherComplexity_measurable_countable` | `(∀ (i : ι), Measurable (F i)) → ∀ (n : ℕ), Measurable fun S => empiricalRademacherComplexity n F S` | For a countably indexed family of measurable functions, the empirical Rademacher complexity of the family is a measurable function of the sample. |
| `Stat.Concentration.empiricalL2Radius_measurable` | `(∀ (i : ι), Measurable (F i)) → ∀ (n : ℕ), Measurable fun S => Causalean.Stat.Concentration.empiricalL2Radius✝ F S` | For a countably indexed family of measurable functions, the largest empirical L² norm attained over the family is a measurable function of the sample. |
| `Stat.Concentration.varianceAdaptiveRademacherComplexity_le` | `0 < σ → σ < U → Real.exp 1 ≤ A → 1 ≤ v → (∀ (i : ι), Measurable (F i)) → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → (∀ (i : ι), (Causalean.Stat.Concentration.measureL2Dist P (F i) fun x => 0) ≤ σ) → Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → ∀ (n : ℕ), 0 < n → rademacherComplexity n F P id ≤ Causalean.Stat.Concentration.varianceAdaptiveVCConstant / 2 * Causalean.Stat.Concentration.vcExpectedMaximalRate U σ A v n` | Variance-adaptive Rademacher complexity bound. Let `P` be a probability measure on the sample space and `F` a countable family of real-valued functions on it. Suppose the population $L^2$ radius `σ` is strictly positive and strictly less than the envelope `U`, the covering-entropy base `A` is at least Euler's number and the exponent `v` is at least one, every function in `F` is measurable and bounded in absolute value by `U`, each function's population $L^2$ distance from the zero function is at most `σ`, `F` has polynomial empirical $L^2$ covering numbers with envelope `U`, base `A`, and exponent `v`, and the sample size `n` is positive. Then the Rademacher complexity of `F` under `P` with `n` i.i.d. observations is at most half the universal constant `varianceAdaptiveVCConstant` times the variance-adaptive rate `σ √(v log(AU/σ)/n) + v U log(AU/σ)/n`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.ExpectedMaximal -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.varianceAdaptiveExpectedMaximal_le` | `0 < σ → σ < U → Real.exp 1 ≤ A → 1 ≤ v → (∀ (i : ι), Measurable (F i)) → (∀ (i : ι) (x : 𝒳), \|F i x\| ≤ U) → (∀ (i : ι), (Causalean.Stat.Concentration.measureL2Dist P (F i) fun x => 0) ≤ σ) → Causalean.Stat.Concentration.HasPolynomialEmpiricalL2Cover F U A v → ∀ (n : ℕ), 0 < n → (∫ (S : Fin n → 𝒳), Causalean.Stat.Concentration.countableEmpiricalSup P F S ∂MeasureTheory.Measure.pi fun x => P) ≤ Causalean.Stat.Concentration.varianceAdaptiveVCConstant * Causalean.Stat.Concentration.vcExpectedMaximalRate U σ A v n` | Variance-adaptive expected maximal inequality. Let `P` be a probability measure on the sample space and `F` a countable family of real-valued functions on it. Suppose the population $L^2$ radius `σ` is strictly positive and strictly less than the envelope `U`, the covering-entropy base `A` is at least Euler's number and the exponent `v` is at least one, every function in `F` is measurable and bounded in absolute value by `U`, each function's population $L^2$ distance from the zero function is at most `σ`, `F` has polynomial empirical $L^2$ covering numbers with envelope `U`, base `A`, and exponent `v`, and the sample size `n` is positive. Then the expected empirical supremum of `F` over an `n`-point i.i.d. sample drawn from `P` is at most the universal constant `varianceAdaptiveVCConstant` times the variance-adaptive rate `σ √(v log(AU/σ)/n) + v U log(AU/σ)/n`. |
| `Stat.Concentration.HasPolynomialL2Cover.varianceAdaptiveExpectedMaximal_le` | `Causalean.Stat.Concentration.HasPolynomialL2Cover F U → 0 < σ → σ < U → (∀ (i : ι), (Causalean.Stat.Concentration.measureL2Dist P (F i) fun x => 0) ≤ σ) → ∃ A v, Real.exp 1 ≤ A ∧ 1 ≤ v ∧ ∀ (n : ℕ), 0 < n → (∫ (S : Fin n → 𝒳), Causalean.Stat.Concentration.countableEmpiricalSup P F S ∂MeasureTheory.Measure.pi fun x => P) ≤ Causalean.Stat.Concentration.varianceAdaptiveVCConstant * Causalean.Stat.Concentration.vcExpectedMaximalRate U σ A v n` | An existing `HasPolynomialL2Cover` certificate yields some explicit polynomial constants for which the countable class satisfies the variance-adaptive expected maximal inequality at every positive sample size. |
<!-- /GEN -->

#### 12'.e4c `Stat/Concentration/VCLocalizedCovering.lean`

Metric bridges connecting binary-factored real classes with the weighted
Boolean Hamming geometry used in localized finite-VC covering arguments.

| Declaration | Statement | Description |
|---|---|---|
| `empiricalDist_sq_eq_weightedHammingSq` | Binary factorization `F i (S j) = φ j (π i (S j))` → squared empirical L² distance equals weighted Hamming squared distance with weights `(φ j true − φ j false)²/n` | Converts empirical metric separation for a binary-factored real class into weighted Boolean trace separation on the same sample. |

#### 12'.e5 `Stat/Concentration/VCLocalizedRegime.lean`

Localized finite-VC Rademacher regime used by the sharp localized uniform
deviation layer.  The elementary sub-root and critical-radius algebra is proved
directly.  The remaining Massart finite-realization construction is isolated as
an explicit analytic sublemma rather than packaged as a hypothesis field.

| Declaration | Statement | Description |
|---|---|---|
| `BinaryFactoredVCClass` | Real-valued class `F` factors through a Boolean class whose sample growth family has VC dimension at most `d` | Packages the binary trace substrate used by VC covering arguments. |
| `BinaryTraceEntropyControl` | Either samplewise VC-dimension control or direct `(m + 1)^d` growth-cardinality control | Common entropy input for the localized star-hull theorem. |
| `LocalizedVCDudleyHypotheses` | Empirical radius and total boundedness fields | Deterministic hypothesis bundle for the sample-path localized Dudley step. |
| `localizedVCDudleyHypotheses_of_empiricalRadius` | Bounded binary-factored finite-VC class plus an explicit samplewise empirical-radius bridge → `LocalizedVCDudleyHypotheses` | Constructor for the localized Dudley hypothesis bundle. For population L² localization norms, the empirical-radius bridge remains a real extra obligation; boundedness alone does not give deterministic empirical-radius control on every sample. |
| `starHullZeroOut_empirical_rademacher_massart_vc` | Binary trace entropy + localized hypotheses → Massart finite-realization bound with rate `r * sqrt((2 d log(n+1)+2)/n)` | Isolates the remaining analytic finite-pattern Massart construction. |
| `absolute_dudley_vc_starHullZeroOut_linear_residual_shared` | Same localized star-hull bound with either VC-dimension or direct growth-cardinality trace control, assuming `K ≥ 1` | Shared residual used by the downstream VC localized bounds. |
| `vcLocalizedRademacherUpperBound` | Binary-factored finite-VC class + localized hypotheses → local Rademacher upper bound and critical-radius rate | Headline localized finite-VC Rademacher envelope. |
| `vcLocalizedEnvelope` | Finite-VC localized assumptions → `localized_uniform_deviation_sharp` envelope | Connects the finite-VC Rademacher regime to the localized uniform-deviation theorem. |

#### 12'.f `Stat/Concentration/Hoeffding.lean`

Causalean-facing corollaries of Mathlib's sub-Gaussian framework
(`Mathlib.Probability.Moments.SubGaussian`) specialised to the `IIDSample`
model. All declarations **proved, sorry-free, axiom-clean**.

| Declaration | Statement | Description |
|---|---|---|
| `IIDSample.map_eq` | `lemma` | Each sample point has law `P`: `μ.map (S.Z i) = P`. |
| `IIDSample.integral_comp_eq` | `lemma` | Population-mean identity `∫ f (S.Z i) ∂μ = ∫ f ∂P` for measurable `f`. |
| `IIDSample.iIndepFun_comp` | `lemma` | Independence of the composed family `fun i ↦ f ∘ S.Z i` from `S.indep`. |
| `hoeffding_ge` | `theorem` | One-sided Hoeffding: for `f` valued in `[a,b]` (`a<b`) `P`-a.e., `P(X̄ₙ − E[f] ≥ ε) ≤ exp(−2nε²/(b−a)²)`. |
| `Concentration.measureReal_abs_dev_le_two_sided` | `lemma` | Generic two-sided assembly: from one-sided bounds `μ.real{ε ≤ T−m} ≤ B` and `μ.real{ε ≤ −T+m} ≤ B`, concludes `μ.real{ε ≤ |T−m|} ≤ 2B`. Shared by `hoeffding_abs_ge` and `bernstein_abs_ge` (the former duplicated union+calc boilerplate). |
| `hoeffding_abs_ge` | `theorem` | Two-sided Hoeffding: `P(|X̄ₙ − E[f]| ≥ ε) ≤ 2·exp(−2nε²/(b−a)²)`. Two-sided assembly via `measureReal_abs_dev_le_two_sided`. |

#### 12'.f' `Stat/Concentration/TailBounds/BinomialCount.lean`

Multiplicative one-sided tail bounds for Bernoulli counts observed through the
common `IIDSample` interface.

<!-- GEN:Causalean.Stat.Concentration.TailBounds.BinomialCount -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.bernoulliCount` | `Causalean.Stat.IIDSample Ω 𝒳 μ P → (𝒳 → ℝ) → ℕ → Ω → ℝ` | For an independent and identically distributed sample with a specified sample-space law and population law, a real-valued statistic on the observation space, and a nonnegative integer $m$, the Bernoulli count is the function that maps every sample-space outcome to the sum of the statistic over its first $m$ sampled observations. |
| `Stat.Concentration.bernoulliCount_measurable` | `Measurable f → ∀ (m : ℕ), Measurable (Causalean.Stat.Concentration.bernoulliCount S f m)` | — |
| `Stat.Concentration.bernoulliCount_nonneg` | `(∀ (x : 𝒳), 0 ≤ f x) → ∀ (m : ℕ) (ω : Ω), 0 ≤ Causalean.Stat.Concentration.bernoulliCount S f m ω` | A count built from a nonnegative statistic is nonnegative. |
| `Stat.Concentration.bernoulliCount_le` | `(∀ (x : 𝒳), f x ≤ 1) → ∀ (m : ℕ) (ω : Ω), Causalean.Stat.Concentration.bernoulliCount S f m ω ≤ ↑m` | A count of `m` observations of a statistic bounded by one never exceeds `m`. |
| `Stat.Concentration.exp_mul_le_secant` | `x ∈ Set.Icc 0 1 → Real.exp (s * x) ≤ 1 + x * (Real.exp s - 1)` | On the unit interval the exponential function stays below the chord joining its values at the two endpoints: for a number `x` between zero and one and any tilt `s`, `exp (s * x)` is at most `1 + x * (exp s - 1)`. This is the convexity step behind every Bernoulli-type Chernoff bound. |
| `Stat.Concentration.mgf_le_of_mem_Icc_zero_one` | `AEMeasurable f P → (∀ᵐ (x : 𝒳) ∂P, f x ∈ Set.Icc 0 1) → ∀ (s : ℝ), ProbabilityTheory.mgf f P s ≤ Real.exp ((∫ (x : 𝒳), f x ∂P) * (Real.exp s - 1))` | The moment generating function of a measurable statistic taking values in the unit interval is at most `exp (mean * (exp s - 1))`, where `mean` is the statistic's population mean. This holds at every tilt `s`, positive or negative. |
| `Stat.Concentration.mgf_eq_of_mem_zero_one` | `AEMeasurable f P → (∀ᵐ (x : 𝒳) ∂P, f x = 0 ∨ f x = 1) → ∀ (p s : ℝ), ∫ (x : 𝒳), f x ∂P = p → ProbabilityTheory.mgf f P s = 1 + p * (Real.exp s - 1)` | For a statistic that only takes the values zero and one, the moment generating function is exactly `1 + p * (exp s - 1)`, where `p` is the probability that the statistic equals one. |
| `Stat.Concentration.boundedCount_mgf_le_integral` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ (m : ℕ) (s : ℝ), ProbabilityTheory.mgf (Causalean.Stat.Concentration.bernoulliCount S f m) μ s ≤ Real.exp (↑m * ((∫ (x : 𝒳), f x ∂P) * (Real.exp s - 1)))` | The moment generating function of the count of the first `m` observations of a measurable `[0,1]`-valued statistic is at most `exp (m * mean * (exp s - 1))`, where `mean` is the statistic's population mean. This is the i.i.d. tensorisation of the one-observation bound and holds at every tilt `s`. |
| `Stat.Concentration.boundedCount_mgf_le` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ {p : ℝ}, ∫ (x : 𝒳), f x ∂P ≤ p → ∀ (m : ℕ) (s : ℝ), 0 ≤ s → ProbabilityTheory.mgf (Causalean.Stat.Concentration.bernoulliCount S f m) μ s ≤ Real.exp (↑m * (p * (Real.exp s - 1)))` | If the population mean of a measurable `[0,1]`-valued statistic is at most `p`, then at every nonnegative tilt `s` the moment generating function of the count of the first `m` observations is at most `exp (m * p * (exp s - 1))`. |
| `Stat.Concentration.boundedCount_upper_tail_of_tilt` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ {p a : ℝ}, ∫ (x : 𝒳), f x ∂P ≤ p → ∀ (m : ℕ) (s : ℝ), 0 ≤ s → μ.real {ω \| a < Causalean.Stat.Concentration.bernoulliCount S f m ω} ≤ Real.exp (-s * a + ↑m * (p * (Real.exp s - 1)))` | Chernoff upper tail at an arbitrary nonnegative tilt. For a measurable `[0,1]`-valued statistic with population mean at most `p`, the probability that the count of the first `m` observations exceeds a level `a` is at most `exp (-s * a + m * p * (exp s - 1))`, for every nonnegative `s`. Optimising over `s` recovers the usual multiplicative Chernoff bounds. |
| `Stat.Concentration.boundedCount_lower_tail_of_tilt` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ {p a : ℝ}, p ≤ ∫ (x : 𝒳), f x ∂P → ∀ (m : ℕ), ∀ s ≤ 0, μ.real {ω \| Causalean.Stat.Concentration.bernoulliCount S f m ω ≤ a} ≤ Real.exp (-s * a + ↑m * (p * (Real.exp s - 1)))` | Chernoff lower tail at an arbitrary nonpositive tilt. For a measurable `[0,1]`-valued statistic with population mean at least `p`, the probability that the count of the first `m` observations falls at or below a level `a` is at most `exp (-s * a + m * p * (exp s - 1))`, for every nonpositive `s`. Optimising over `s` recovers the usual multiplicative Chernoff bounds. |
| `Stat.Concentration.bernoulliCount_upper_tail` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ {p a : ℝ}, ∫ (x : 𝒳), f x ∂P ≤ p → ∀ {m : ℕ}, ↑m * p < a / 2 → μ.real {ω \| a < Causalean.Stat.Concentration.bernoulliCount S f m ω} ≤ Real.exp (-a * (Real.log 2 - 1 / 2))` | Upper multiplicative tail for the count of an i.i.d. `[0,1]`-valued statistic. Let `S` be an i.i.d. sample and let `f` be a measurable statistic taking values in the unit interval. If the population mean of `f` is at most `p` and `m` times `p` is less than half the threshold `a`, then the probability that the sum of `f` over the first `m` draws exceeds `a` is at most $\exp(-a(\log 2 - 1/2))$. |
| `Stat.Concentration.bernoulliCount_lower_tail` | `Measurable f → (∀ (x : 𝒳), f x ∈ Set.Icc 0 1) → ∀ {p a : ℝ}, 0 ≤ p → p ≤ ∫ (x : 𝒳), f x ∂P → ∀ {m : ℕ}, 2 * a < ↑m * p → μ.real {ω \| Causalean.Stat.Concentration.bernoulliCount S f m ω ≤ a} ≤ Real.exp (-(↑m * p) / 8)` | Lower multiplicative tail for the count of an i.i.d. `[0,1]`-valued statistic. Let `S` be an i.i.d. sample and let `f` be a measurable statistic taking values in the unit interval. If `p` is nonnegative, the population mean of `f` is at least `p`, and twice the threshold `a` is less than `m` times `p`, then the probability that the sum of `f` over the first `m` draws is at most `a` is at most $\exp(-mp/8)$. |
<!-- /GEN -->

#### 12'.g `Stat/Concentration/SubExponential.lean`

Sub-exponential MGF framework mirroring Mathlib's sub-Gaussian design, in the
`(v, b)` parameterization (validity interval encoded as `b·|t| < 1`, so `b = 0`
recovers the sub-Gaussian/all-`t` case). All declarations **proved, sorry-free,
axiom-clean**.

| Declaration | Statement | Description |
|---|---|---|
| `HasSubexponentialMGF X v b μ` | `structure` | `exp(t·X)` integrable and `mgf X μ t ≤ exp(v t²/2)` for every `t` with `b·|t| < 1`. |
| `HasSubexponentialMGF.isFiniteMeasure` | `lemma` | A sub-exponential variable lives on a finite measure. |
| `HasSubexponentialMGF.neg` | `lemma` | Closure under negation (same `(v,b)`). |
| `HasSubexponentialMGF.congr` | `lemma` | Transfer along an a.e.-equality of variables. |
| `HasSubexponentialMGF.const_mul` | `lemma` | Scaling `r·X` has parameters `(r²v, |r|b)`. |
| `HasSubexponentialMGF.add_of_indepFun` | `lemma` | Independent variables add: `X+Y` has parameters `(vX+vY, max bX bY)`. |
| `HasSubexponentialMGF.zero` | `lemma` | The constant `0` is sub-exponential with parameters `(0, b)`. |
| `HasSubexponentialMGF.measure_ge_le` | `theorem` | Chernoff tail `P(X ≥ ε) ≤ exp(−ε²/(2(v + bε)))` (single explicit minimizer `t = ε/(v+2bε)`). |
| `HasSubexponentialMGF.sum_range_of_iIndepFun` | `lemma` | A sum of `n` independent variables with common `(v,b)` is sub-exponential with `(n•v, b)`. |

#### 12'.h Mathlib sub-Gaussian re-use index

The sub-Gaussian half of the toolbox is consumed directly from Mathlib's
`ProbabilityTheory` namespace (no Causalean duplication); the Causalean wrappers above
build on it. Key upstream names:

| Mathlib declaration | Description |
|---|---|
| `HasSubgaussianMGF X c μ` | Sub-Gaussian MGF predicate `mgf X μ t ≤ exp(c t²/2)` for all `t`. |
| `hasSubgaussianMGF_of_mem_Icc` | Hoeffding's lemma: a `[a,b]`-valued RV is sub-Gaussian with proxy `((b−a)/2)²` after centering. |
| `HasSubgaussianMGF.measure_ge_le` | Sub-Gaussian Chernoff right tail. |
| `HasSubgaussianMGF.measure_sum_range_ge_le_of_iIndepFun` | Hoeffding's inequality for sums of independent sub-Gaussians (used by `hoeffding_ge`). |
| `HasSubgaussianMGF.add_of_indepFun` / `sum_of_iIndepFun` | Independent sums (parameters add). |

#### 12'.i `Stat/Concentration/Bernstein.lean`

Bernstein's inequality routed through the constant-`v` sub-exponential framework.
**Fully proved and `sorry`-free** (all four declarations are axiom-clean:
`propext`/`Classical.choice`/`Quot.sound` only). The earlier power-series blocker
is avoided entirely: the sub-exponential packaging only needs the MGF bound on
the validity interval `c|t| < 1`, where `|tX| ≤ 1` a.e. lets the elementary
pointwise bound `exp u ≤ 1 + u + u²` integrate term-by-term.

| Declaration | Statement | Description |
|---|---|---|
| `exp_le_one_add_add_sq` | `lemma` | Pointwise `exp u ≤ 1 + u + u²` for `|u| ≤ 1`; a real corollary of `Real.norm_exp_sub_one_sub_id_le`. **Proved.** |
| `bounded_mgf_le_exp_sq` | `lemma` | For mean-zero `X`, `|X| ≤ c`, `E[X²] ≤ σ²`: `mgf X μ t ≤ exp(σ²t²)` when `c|t| ≤ 1`. Integrates the pointwise bound using `E[X]=0`, `E[X²]≤σ²`. **Proved.** |
| `bounded_hasSubexponentialMGF` | `lemma` | A bounded mean-zero variable is sub-exponential with parameters `(2σ², c)` (note `2σ²·t²/2 = σ²t²`). **Proved.** |
| `bernstein_ge` | `theorem` | One-sided Bernstein: `P(X̄ₙ − E[f] ≥ ε) ≤ exp(−n ε² / (2(2σ² + cε)))`. From `bounded_hasSubexponentialMGF` + independent-sum + Chernoff. **Proved.** |
| `bernstein_abs_ge` | `theorem` | Two-sided Bernstein: `P(|X̄ₙ − E[f]| ≥ ε) ≤ 2·exp(−n ε² / (2(2σ² + cε)))`. **Proved.** |

> **Constant note.** The variance enters with proxy `2σ²` and the scale is `c`
> (so the tail is `exp(−nε²/(2(2σ²+cε)))`). The sharper textbook constant
> `exp(−nε²/(2(σ²+cε/3)))` is *not* attainable through the constant-`v`
> sub-exponential structure — it needs a Bernstein-specific Chernoff optimisation
> on the denominator-form MGF — and was deliberately traded for this cleaner,
> looser constant. The `c|t| < 1` validity interval forces the scale `b ≥ c`, so
> `b = c` is optimal for this route.

---

#### 12'.i.1 `Stat/Concentration/Matrix/LocalizedGramBasic.lean`

Deterministic and one-observation substrate for localized random-design Gram
matrices: it defines the local count and weighted Gram quadratic form, turns
entrywise error into a quadratic-form error, and establishes the bounded local
weight moment estimates that make the variance scale with local mass.

<!-- GEN:Causalean.Stat.Concentration.Matrix.LocalizedGramBasic -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.localWeight_integrable` | `Measurable q → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → MeasureTheory.Integrable q P` | A measurable local weight between zero and one almost everywhere is integrable under the probability law. |
| `Stat.Concentration.localWeight_centered_envelope` | `(∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → ∀ {p : ℝ}, ∫ (x : X), q x ∂P = p → ∀ᵐ (x : X) ∂P, \|q x - p\| ≤ 1` | A local weight between zero and one almost everywhere, with the stated population mean, differs from that mean by at most one almost everywhere. |
| `Stat.Concentration.localWeight_centered_secondMoment_le` | `Measurable q → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → ∀ {p : ℝ}, ∫ (x : X), q x ∂P = p → ∫ (x : X), (q x - ∫ (y : X), q y ∂P) ^ 2 ∂P ≤ p` | A measurable local weight between zero and one almost everywhere, with the stated population mean, has centered second moment at most that local mass. |
| `Stat.Concentration.localGram_centered_envelope` | `(∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → ∀ {B : ℝ}, 0 ≤ B → (∀ (j : κ), ∀ᵐ (x : X) ∂P, \|phi j x\| ≤ B) → ∀ (j k : κ), ∀ᵐ (x : X) ∂P, \|q x * phi j x * phi k x - ∫ (y : X), q y * phi j y * phi k y ∂P\| ≤ 2 * B ^ 2` | A localized Gram coordinate with a local weight between zero and one and features bounded by a nonnegative envelope differs from its population mean by at most twice the squared envelope almost everywhere. |
| `Stat.Concentration.localGram_centered_secondMoment_le` | `Measurable q → (∀ (j : κ), Measurable (phi j)) → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → ∀ {B p : ℝ}, 0 ≤ B → (∀ (j : κ), ∀ᵐ (x : X) ∂P, \|phi j x\| ≤ B) → ∫ (x : X), q x ∂P = p → ∀ (j k : κ), ∫ (x : X), (q x * phi j x * phi k x - ∫ (y : X), q y * phi j y * phi k y ∂P) ^ 2 ∂P ≤ B ^ 4 * p` | A localized Gram coordinate from measurable functions, a unit-interval local weight, bounded features, and the stated local mass has centered second moment bounded by envelope-to-the-fourth times local mass. |
| `Stat.Concentration.localGram_integrable` | `Measurable q → (∀ (j : κ), Measurable (phi j)) → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → ∀ {B : ℝ}, 0 ≤ B → (∀ (j : κ), ∀ᵐ (x : X) ∂P, \|phi j x\| ≤ B) → ∀ (j k : κ), MeasureTheory.Integrable (fun x => q x * phi j x * phi k x) P` | A localized Gram coordinate from measurable functions, a unit-interval local weight, and bounded features is integrable under the population law. |
| `Stat.Concentration.localCount` | `(X → ℝ) → (Fin N → X) → ℝ` | For a real-valued observation-weight function and a finite sample, the unnormalised local count is the sum of the weights assigned to all observations in the sample. |
| `Stat.Concentration.localGramEntry` | `(X → ℝ) → (κ → X → ℝ) → (Fin N → X) → κ → κ → ℝ` | For a real-valued observation-weight function, a finite indexed family of real-valued features, a finite sample, and two feature indices, the corresponding entry of the unnormalised weighted empirical Gram matrix is the sum, over observations, of the weight times the two selected feature values. |
| `Stat.Concentration.localGramQuadratic` | `(X → ℝ) → (κ → X → ℝ) → (Fin N → X) → (κ → ℝ) → ℝ` | For a real-valued observation-weight function, a finite indexed family of real-valued features, a finite sample, and a real coefficient vector indexed by the features, the quadratic form of the unnormalised weighted empirical Gram matrix is the double sum of the product of two coefficients and their weighted empirical Gram-matrix entry. |
| `Stat.Concentration.LocalizedGramGood` | `ℝ → (X → ℝ) → (κ → X → ℝ) → (Fin N → X) → Prop` | For a real lower-eigenvalue threshold, a real-valued observation-weight function, a finite indexed family of real-valued features, and a finite sample, the localized empirical Gram matrix is good exactly when (1) the local count is positive and (2) for every real coefficient vector indexed by the features, its quadratic form is at least one half of the threshold times the local count times the squared Euclidean norm of that vector. |
| `Stat.Concentration.entrywise_to_quadratic` | `0 ≤ epsilon → (∀ (j k : κ), \|E j k\| ≤ epsilon) → \|∑ j, ∑ k, v j * v k * E j k\| ≤ ↑(Fintype.card κ) * epsilon * ∑ j, v j ^ 2` | If finite-matrix entries are bounded by a nonnegative tolerance, the absolute quadratic-form error is at most dimension times tolerance times squared vector norm. |
| `Stat.Concentration.entrywise_gram_lower_bound` | `0 ≤ epsilon → (∀ (j k : κ), \|G j k - scale * M j k\| ≤ epsilon) → scale * ∑ j, ∑ k, v j * v k * M j k - ↑(Fintype.card κ) * epsilon * ∑ j, v j ^ 2 ≤ ∑ j, ∑ k, v j * v k * G j k` | If an empirical matrix is entrywise close to a scaled population matrix with nonnegative tolerance, each empirical quadratic form is bounded below by the population form minus the dimension-scaled error. |
| `Stat.Concentration.localizedGram_good_of_entrywise` | `0 < N → 0 < p → 0 < lambda → (∀ (v : κ → ℝ), lambda * p * ∑ j, v j ^ 2 ≤ ∑ j, ∑ k, v j * v k * M j k) → ∀ (omega : Fin N → X), \|Causalean.Stat.Concentration.localCount q omega - ↑N * p\| < ↑N * p / 2 → (∀ (j k : κ), \|Causalean.Stat.Concentration.localGramEntry q phi omega j k - ↑N * M j k\| < lambda * ↑N * p / (4 * ↑(Fintype.card κ))) → Causalean.Stat.Concentration.LocalizedGramGood lambda q phi omega` | Given positive sample size, mass, and coercivity, population coercivity, a small count error, and small entrywise Gram errors, the realised local count is positive and the empirical Gram is coercive relative to it. |
| `Stat.Concentration.localizedGram_failure_subset_deviations` | `0 < N → 0 < p → 0 < lambda → (∀ (v : κ → ℝ), lambda * p * ∑ j, v j ^ 2 ≤ ∑ j, ∑ k, v j * v k * M j k) → {omega \| ¬Causalean.Stat.Concentration.LocalizedGramGood lambda q phi omega} ⊆ {omega \| ↑N * p / 2 ≤ \|Causalean.Stat.Concentration.localCount q omega - ↑N * p\| ∨ ∃ j k, lambda * ↑N * p / (4 * ↑(Fintype.card κ)) ≤ \|Causalean.Stat.Concentration.localGramEntry q phi omega j k - ↑N * M j k\|}` | Under positive sample size, mass, and coercivity and population coercivity, failure of empirical local-Gram coercivity implies either a large count error or a large entrywise Gram error. |
<!-- /GEN -->

#### 12'.i.2 `Stat/Concentration/Matrix/LocalizedGram.lean`

Finite-product Bernstein control for a bounded local weight and finite feature
vector. The headline coercivity result combines simultaneous local-count and
Gram-entry deviations with the deterministic perturbation lemma, giving an
explicit failure probability with exponent proportional to the effective local
sample size `N·p`.

<!-- GEN:Causalean.Stat.Concentration.Matrix.LocalizedGram -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.localizedGramRate` | `(κ : Type u_1) → [Fintype κ] → ℝ → ℝ → ℝ` | For a finite feature index set, a real coercivity level, and a real envelope bound, the localized Gram-matrix exponential rate is the smaller of $1/20$ and $\lambda^2/[16d(4dB^4+B^2\lambda)]$, where $d$ is the number of feature indices. |
| `Stat.Concentration.localized_empiricalGram_coercive_of_pos` | `Measurable q → (∀ (j : κ), Measurable (phi j)) → ∀ {p lambda B : ℝ}, 0 < N → 0 < p → 0 < lambda → 0 ≤ B → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → (∀ (j : κ), ∀ᵐ (x : X) ∂P, \|phi j x\| ≤ B) → ∫ (x : X), q x ∂P = p → (∀ (v : κ → ℝ), lambda * p * ∑ j, v j ^ 2 ≤ ∑ j, ∑ k, v j * v k * ∫ (x : X), q x * phi j x * phi k x ∂P) → (MeasureTheory.Measure.pi fun x => P).real {omega \| ¬Causalean.Stat.Concentration.LocalizedGramGood lambda q phi omega} ≤ 2 * (1 + ↑(Fintype.card κ) * ↑(Fintype.card κ)) * Real.exp (-↑N * p * Causalean.Stat.Concentration.localizedGramRate κ lambda B)` | Given measurable local weights and features, positive sample size, local mass, and population coercivity, a nonnegative feature envelope, a unit-interval local weight, features bounded by that envelope, the stated local mass, and population Gram coercivity, failure of positive local count or realised-count-relative empirical Gram coercivity has an explicit Bernstein bound with exponent proportional to sample size times local mass. |
| `Stat.Concentration.localized_empiricalGram_coercive` | `Measurable q → (∀ (j : κ), Measurable (phi j)) → ∀ {p lambda B : ℝ}, 0 < N → 0 ≤ p → 0 < lambda → 0 ≤ B → (∀ᵐ (x : X) ∂P, 0 ≤ q x ∧ q x ≤ 1) → (∀ (j : κ), ∀ᵐ (x : X) ∂P, \|phi j x\| ≤ B) → ∫ (x : X), q x ∂P = p → (∀ (v : κ → ℝ), lambda * p * ∑ j, v j ^ 2 ≤ ∑ j, ∑ k, v j * v k * ∫ (x : X), q x * phi j x * phi k x ∂P) → (MeasureTheory.Measure.pi fun x => P).real {omega \| ¬Causalean.Stat.Concentration.LocalizedGramGood lambda q phi omega} ≤ 2 * (1 + ↑(Fintype.card κ) * ↑(Fintype.card κ)) * Real.exp (-↑N * p * Causalean.Stat.Concentration.localizedGramRate κ lambda B)` | Given measurable local weights and features, positive sample size, nonnegative local mass, and positive population coercivity, a nonnegative feature envelope, a unit-interval local weight, features bounded by that envelope, the stated local mass, and population Gram coercivity, failure of positive local count or realised-count-relative empirical Gram coercivity has an explicit Bernstein bound with exponent proportional to sample size times local mass. |
<!-- /GEN -->

---

#### 12'.j `Stat/Concentration/ConfidenceInterval.lean`

Inverts the two-sided Hoeffding and Bernstein tails (12'.f, 12'.i) into explicit
*finite-sample* confidence half-widths. For a confidence level `δ ∈ (0,1]` and
sample size `n`, each result gives a half-width `w(n,δ)` such that the population
mean `m = ∫ f ∂P` is covered by the random interval `[X̄ₙ − w, X̄ₙ + w]` with
probability `≥ 1 − δ`. Both a miss-probability form (`P(w ≤ |X̄ₙ − m|) ≤ δ`) and
its coverage complement are provided. Holds at every fixed `n`, unlike the
asymptotic Wald interval. **Fully proved and `sorry`-free.**

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `hoeffdingCIHalfWidth` | `(a b : ℝ) (n : ℕ) (δ : ℝ) : ℝ` | Hoeffding half-width `(b−a)·√(log(2/δ)/(2n))` for an `[a,b]`-valued statistic. | **Proved** (def) |
| `bernsteinCIHalfWidth` | `(c σ : ℝ) (n : ℕ) (δ : ℝ) : ℝ` | Variance-adaptive Bernstein half-width `2σ·√(log(2/δ)/n) + 2c·(log(2/δ)/n)`; leading term scales with `σ`. | **Proved** (def) |
| `hoeffding_ci_miss` | `… : μ.real {ω | hoeffdingCIHalfWidth a b n δ ≤ |X̄ₙ − m|} ≤ δ` | Miss bound: the mean falls outside the Hoeffding interval w.p. `≤ δ`. Inverts `hoeffding_abs_ge`. | **Proved** |
| `bernstein_ci_miss` | `… : μ.real {ω | bernsteinCIHalfWidth c σ n δ ≤ |X̄ₙ − m|} ≤ δ` | Miss bound for the variance-adaptive Bernstein interval; exact quadratic-slack inversion of `bernstein_abs_ge`. | **Proved** |
| `hoeffding_ci_cover` | `… : 1 − δ ≤ μ.real {ω | |X̄ₙ − m| < hoeffdingCIHalfWidth a b n δ}` | Coverage complement of `hoeffding_ci_miss`. | **Proved** |
| `bernstein_ci_cover` | `… : 1 − δ ≤ μ.real {ω | |X̄ₙ − m| < bernsteinCIHalfWidth c σ n δ}` | Coverage complement of `bernstein_ci_miss`. | **Proved** |

---

#### 12'.k `Stat/Concentration/EmpiricalBernstein.lean`

Sample-variance infrastructure plus an **elementary, fully rigorous** data-driven
("empirical") Bernstein confidence interval. The full Maurer–Pontil bound is out
of scope; instead the population variance `σ² = ∫(f−m)²` is bounded above by the
*observed* sample variance `V̂ₙ` plus a deterministic slack `τ` (via two Hoeffding
deviations — one for `f`, one for `f²` — at level `δ/2` each), and `σ ≤ √(V̂ₙ+τ)`
is fed into the Bernstein half-width using monotonicity in `σ`. Constants are
looser than Maurer–Pontil's (the second-moment Hoeffding step is wasteful) but
every step is elementary. **Fully proved and `sorry`-free** (axiom-clean:
`propext`/`Classical.choice`/`Quot.sound` only).

The explicit slack (with `M := max |a| |b|`, `d := √(log(4/δ)/(2n))`) is
`τ = M²·d + 2 M (b−a)·d`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `IIDSample.sampleVariance` | `(S : IIDSample Ω X μ P) (f : X → ℝ) (n : ℕ) : Ω → ℝ` | Biased (`1/n`-normalised) empirical variance `(1/n) ∑_{i<n} (f(Zᵢ ω) − X̄ₙ(ω))²` (in namespace `Causalean.Stat` for dot-notation). | **Proved** (def) |
| `IIDSample.sampleVariance_nonneg` | `… (ω) : 0 ≤ S.sampleVariance f n ω` | The sample variance is nonnegative (a scaled sum of squares). | **Proved** |
| `IIDSample.measurable_sampleVariance` | `(hf : Measurable f) (n) : Measurable (S.sampleVariance f n)` | The sample variance is measurable in the sample point `ω`. | **Proved** |
| `IIDSample.sampleVariance_eq` | `… (ω) : S.sampleVariance f n ω = (n:ℝ)⁻¹ * ∑ i ∈ range n, (f (Z i ω))^2 − (X̄ₙ ω)^2` | Computational form: empirical second moment minus squared empirical mean. | **Proved** |
| `empiricalVarianceSlack` | `(a b : ℝ) (n : ℕ) (δ : ℝ) : ℝ` | The deterministic slack `τ = M²·d + 2M(b−a)·d` (`M = max|a||b|`, `d = √(log(4/δ)/(2n))`). | **Proved** (def) |
| `empirical_variance_concentration` | `(hf) (hab : a<b) (hbound : f∈[a,b] a.e.) (n) (hn) (hδ0) (hδ1) : μ.real {ω | V̂ₙ + τ < σ²} ≤ δ` | Data-driven variance bound: w.p. `≥ 1−δ`, `σ² ≤ V̂ₙ + empiricalVarianceSlack a b n δ`. Union of two level-`δ/2` Hoeffding events (for `f` and `f²`) plus a null set for the a.e. `|X̄ₙ| ≤ M` bound. | **Proved** |
| `empiricalBernsteinCIHalfWidth` | `(S) (f) (a b c : ℝ) (n) (δ) : Ω → ℝ` | Data-driven half-width: `bernsteinCIHalfWidth` with oracle `σ` replaced by the observed `√(V̂ₙ(ω) + τ)`. | **Proved** (def) |
| `empirical_bernstein_ci_miss` | `(hf) (hab) (hbound_ab) (hc : 0≤c) (hbound : |f−m|≤c a.e.) (hposvar : 0<σ²) (n) (hn) (hδ0) (hδ1) : μ.real {ω | empiricalBernsteinCIHalfWidth S f a b c n δ ω ≤ |X̄ₙ − m|} ≤ 2δ` | Data-driven Bernstein CI miss bound: the mean falls outside the observed-variance interval w.p. `≤ 2δ`. Unions the oracle Bernstein miss (`σ=√σ²`) with the variance-bound failure, using monotonicity of the width in `σ` on the good event. | **Proved** |

---

## 12g. `Estimation/OrthogonalMoments/` — Abstract DML estimation and inference framework

Generic moment-functional layer that consolidates the DML asymptotic-linearity
machinery so any concrete instance (currently AIPW) reduces to instantiating
the abstract structure. Mirrors `doc/basic_concepts/po/estimation/dml_ate.tex`
generalised away from AIPW-specific algebra.

### 12g.1 `Estimation/OrthogonalMoments/MomentFunctional.lean`

| Declaration | Statement | Description |
|---|---|---|
| `GeneralMoment Ω μ Z P_Z H` | structure | Abstract bundle: moment functional `m : H → Z → ℝ → ℝ`, truth `η₀ : H`, target `θ₀ : ℝ`, perturbation set `H_ε ⊆ H`, bilinear seminorms `(ρ₁, ρ₂) : H → H → NNReal`, joint measurability of `m _ · _`, and `η₀ ∈ H_ε`. |
| `GeneralMoment.J₀` | field, `ℝ` | Jacobian `∂_θ ∫ m(η₀, z, θ) dP_Z |_{θ=θ₀}` of the population moment. Scalar form (vector-θ generalisation deferred). For AIPW (linear score `m = ψ − θ`), `J₀ = −1`. |
| `GeneralMoment.J₀_ne_zero` | field, `Prop` | Non-singularity witness; ensures `J₀_inv` is well-defined. |
| `GeneralMoment.J₀_inv` | `noncomputable def` | `J₀⁻¹`. **Proved.** |
| `GeneralMoment.J₀_mul_J₀_inv` | `lemma`, `@[simp]` | `J₀ * J₀_inv = 1`. **Proved.** |
| `MeanZero M` | `Prop` | `∫ z, M.m M.η₀ z M.θ₀ ∂P_Z = 0`. |
| `H_ε_PerturbClosed M` | `Prop` | `M.H_ε` is closed under convex combinations between `η₀` and `η ∈ H_ε`. |

##### `LinearMoment` (linear-score extension)

Extension of `GeneralMoment` carrying the decomposition `m η z θ = m_a η z * θ + m_b η z` (Chernozhukov 2018 Thm 3.1 "linear score" form). AIPW is the canonical instance with `m_a η z = -1` and `m_b η z = ψ_AIPW(η, z)`, giving `J₀ = -1`.

| Declaration | Statement | Description |
|---|---|---|
| `LinearMoment` | structure (extends `GeneralMoment`) | Adds `m_a`, `m_b`, measurability, decomposition `m_decomp`, and consistency `J₀_eq : J₀ = ∫ m_a η₀ z dP_Z`. |

### 12g.2 `Estimation/OrthogonalMoments/DirectionalDeriv.lean`

| Declaration | Statement | Description |
|---|---|---|
| `HasDirDeriv M` | structure | Pointwise directional derivative data: function `dM : H → Z → ℝ`, pointwise `Tendsto` witness for the difference quotient along the segment `η₀ → η`, and measurability of `dM η`. |

### 12g.3 `Estimation/OrthogonalMoments/NeymanOrthogonal.lean`

| Declaration | Statement | Description |
|---|---|---|
| `NeymanOrthogonal M D` | `Prop` | `∀ η ∈ M.H_ε, ∫ z, D.dM η z ∂P_Z = 0`. |
| `DiffQuotientEnvelope M` | `Prop` | Existence of an L¹(P_Z) envelope dominating the difference quotient near `t = 0`. |
| `integratedMoment_diffQuotient_tendsto_zero` | `theorem` | DCT bridge: `NeymanOrthogonal` + `DiffQuotientEnvelope` ⇒ integrated difference quotient → 0 along `𝓝[≠] 0`. **Proved** (forward-compat; not on AIPW path). |

### 12g.4 `Estimation/OrthogonalMoments/RemainderBound.lean`

| Declaration | Statement | Description |
|---|---|---|
| `BilinearRemainder M C` | `Prop` | `∀ η ∈ M.H_ε, |∫ z, M.m η z M.θ₀ ∂P_Z| ≤ C · ρ₁(η, η₀) · ρ₂(η, η₀)`. |
| `bilinear_remainder_of_smoothness` | `theorem` | NeymanOrthogonal + uniform second-order envelope on the linearization residual ⇒ `∃ C, BilinearRemainder M C`. **Proved** (after signature strengthening: uniform `K, g`). |

### 12g.5. `Estimation/OrthogonalMoments/DMLChernozhukov.lean`

Classical Chernozhukov-form DML theorem; conclusion uses IF `−J₀⁻¹ · m(η₀, ·, θ₀)` and asymptotic variance `J₀⁻¹ Σ J₀⁻ᵀ`. Matches Chernozhukov et al. 2018 Thm 3.1 / 3.2. The earlier zero-centered `dml_asymptoticLinear` (`Estimation/OrthogonalMoments/DML.lean`) was deleted on 2026-05-08 since AIPW migrated and no other instance used it.

| Declaration | Statement | Description |
|---|---|---|
| `dmlChernozhukovEstimator M sample split η_hat` | `noncomputable def` | One-step Chernozhukov estimator: `θ̂_n := θ₀ − J₀⁻¹ · Pₙ_B m(η̂, ·, θ₀)`. Reduces to `(1/|B|) Σ ψ(η̂, Z_i)` for AIPW (J₀ = −1). |
| `dml_chernozhukov_asymptoticLinear` | `theorem` | Asymptotic linearity of `dmlChernozhukovEstimator` at `M.θ₀` with IF `−J₀⁻¹ · m(η₀, ·, θ₀)`, indexed over `split.foldB`. **Proved by Codex** (G+B decomposition with the Chernozhukov-form score `m(η̂, ·, θ₀)` and a `−J₀⁻¹` constant-multiplication wrapper; bundled an inline `const_mul_isLittleOp_one` helper). **API refactored 2026-05-06:** dropped the `D : HasDirDeriv M`, `_hNO : NeymanOrthogonal M D`, and `_h_in_Hε : ∀ n ω, η_hat n ω ∈ M.H_ε` parameters; replaced the ∀-quantified `_hBR : BilinearRemainder M Crem` with the per-η̂_n bound `_hBR_at : ∀ n ω, |∫ z, M.m (η_hat n ω) z M.θ₀ ∂P_Z| ≤ Crem · ρ₁(η̂_n, η₀) · ρ₂(η̂_n, η₀)`. Net effect: instances supply only what they actually need (orthogonality is implicit in the per-η̂_n bound), removing the artificial ∀-over-`H_ε` hypothesis. **Further trimmed 2026-05-09:** dropped the unused `_hc_lt : c < 1`, `_h_indiv_rate_ρ₁`, `_h_indiv_rate_ρ₂` parameters (all discarded in the body — the abstract proof needs only the product rate); callers (`AIPWInstance`, `AutoDebias.DML`) keep them at their own level when needed locally. |

### 12g.5'. `Estimation/OrthogonalMoments/DMLCrossFit.lean`

K-fold cross-fitted version of `dml_chernozhukov_asymptoticLinear`. Same Chernozhukov form, K folds, score evaluated independently of nuisance training fold per Chernozhukov et al. 2018 §3.2. Concludes asymptotic linearity at the **full-sample** index `Finset.range n` (the K fold-k sub-aggregations are reweighted into a `(1/√n)`-normalised sum modulo `o_p(1)`).

| Declaration | Statement | Description |
|---|---|---|
| `dmlCrossFitEstimator M sample split η_hat` | `noncomputable def` | K-fold averaged Chernozhukov estimator: `θ̂ := M.θ₀ − J₀⁻¹ · K⁻¹ · Σ_k \|fold n k\|⁻¹ · Σ_{i ∈ fold n k} m(η̂^{(-k)}, Z_i, θ₀)`. |
| `foldNormalizedSum_isBigOp` | private lemma | `(1/√\|fold n k\|) Σ_{i ∈ fold n k} ψ(Z_i) = O_p(1)` whenever `ψ` is mean-zero and `L²(P_Z)`. Markov + the abstract `iid_centered_sum_sq_lintegral_le` with trivial training σ-algebra `m_A = ⊥` (independence is automatic via `ProbabilityTheory.indep_bot_left`); mean-zero collapses centered = uncentered. **Proved.** |
| `foldNormalizedSumOverN_isBigOp` | private lemma | `(1/√n) Σ_{i ∈ fold n k} ψ(Z_i) = O_p(1)`. Bound by `foldNormalizedSum_isBigOp` × `√(\|fold n k\|/n)` (the latter is bounded eventually via `split.cover` + `split.partition` giving `(fold n k).card ≤ n`). **Proved.** |
| `dml_crossFit_asymptoticLinear` | `theorem` | Asymptotic linearity of `dmlCrossFitEstimator` at `M.θ₀` with IF `−J₀⁻¹ · m(η₀, ·, θ₀)`, indexed over `fun n => Finset.range n`. Hypotheses: per-fold measurability/integrability (`_h_m_meas`, `_h_m_train`, `_h_m_train_uncurry`, `_h_m_int`, `_h_m_sq_int`), per-fold score-difference rate (`_h_score_diff_rate k`), per-(n,k,ω) bilinear remainder bound `_hBR_at`, individual ρ-rates, product rate, and `_hK_pos : 1 < K` (tightened from `0 < K` because the K-fold bias step needs `(K:ℝ)⁻¹ ∈ (0, 1)`; `K = 1` reduces to the one-shot estimator and is out of scope). Proof structure: `mean_zero` and `finite_var` by constant-rescaling of `MeanZero M` / `_hFV`; the `remainder` decomposes into `h_fold_centered` + `h_fold_bias` (per-fold, lifted from the OneShot proof in `DMLChernozhukov.lean` via `KFoldSplit.fold_centered_sum_isLittleOp_one` and an inline bias derivation analogous to OneShot `hB`) and `h_full_reweighting` (the cover/partition + ratio rescaling). The reweighting step uses `IsBigOp.const_mul_tendsto_zero` for the weight-discrepancy "ratio piece" and `IsBigOp.mul_isLittleOp_one_isLittleOp` for the "score piece"; finite K-fold sum closed via a small inline `IsLittleOp` finite-Finset additivity helper. **Proved by Codex (A5).** **API refactored 2026-05-09:** mirrors the OneShot 2026-05-06 refactor — dropped the `D : HasDirDeriv M`, `_hNO : NeymanOrthogonal M D`, `_h_in_Hε`, and ∀-quantified `_hBR : BilinearRemainder M Crem` parameters; replaced by the per-(n,k,ω) bound `_hBR_at`. |

### 12g.6 `Estimation/OrthogonalMoments/AIPWInstance.lean`

| Declaration | Statement | Description |
|---|---|---|
| `aipwGeneralMoment S h_e_pointwise` | `noncomputable def` | AIPW realization of `GeneralMoment` with `ρ₁ η η' = ‖Δμ_T‖_{L²(P_X)} + ‖Δμ_F‖_{L²(P_X)}`, `ρ₂ η η' = ‖Δe‖_{L²(P_X)}`, `H_ε = ATE.H_ε ε`. |
| `aipwGeneralMoment.J₀`, `J₀_ne_zero` | fields | AIPW Jacobian `J₀ = −1` (linear score `m = ψ − θ`). |
| `aipw_meanZero` | `theorem` | Wraps `aipw_mean_zero`. **Proved.** |
| `aipw_bilinearRem` | `theorem` | `aipw_remainder_bound` reshaped into `BilinearRemainder` via `Fintype.sum_bool` distribution. **Proved.** |
| `aipw_dml_isAsymLinear` | `theorem` | Headline AIPW DML AL, retargeted to use `dml_chernozhukov_asymptoticLinear`. Conclusion: `IsAsymLinear (dmlChernozhukovEstimator (aipwGeneralMoment S _) _ _ _) S.θ₀ (fun z => −J₀_inv · aipwMomentFunctional S.η₀ z S.θ₀)`. **Refactored 2026-05-06:** dropped the ∀-quantified `h_L2 : ∀ η ∈ H_ε ε, …` hypothesis (and the `hε_pos` / `hε_le` extracts that only fed the now-removed `aipw_neymanOrthog` / `aipwHasDirDeriv` calls). Now takes per-η̂_n L²: `h_mu_diff_memLp : ∀ n ω a, MemLp (μ̂(n,ω,a) − μ_val a) 2 P_X` and `h_e_diff_memLp : ∀ n ω, MemLp (ê(n,ω) − e_val) 2 P_X`. Body builds the per-η̂_n bilinear remainder bound directly via `aipw_remainder_bound` (followed by `Fintype.sum_bool` to align with the `ρ₁ · ρ₂ = (‖Δμ_T‖ + ‖Δμ_F‖) · ‖Δe‖` shape) and feeds it as `_hBR_at` to the refactored abstract theorem. The Gâteaux-derivative `aipwHasDirDeriv` / `aipw_neymanOrthog` wrappers (and the underlying `Neyman.lean` / `NeymanAux/` machinery) are no longer on the AL critical path and were archived to `Estimation/ATE/Archived/` on 2026-05-06. **Proved.** |

### 12g.7. `Estimation/OrthogonalMoments/Riesz.lean`

Riesz representer pattern for linear-in-γ functionals (ATE, policy value, average derivative). Light skeleton; concrete instances live next to their estimators.

| Declaration | Statement | Description |
|---|---|---|
| `RieszRepresentation H_γ γ_target L P_X` | structure | A Riesz representer `α₀ : X → ℝ` with `representation : ∀ γ, L γ = ∫ x, α₀ x · γ x ∂P_X`, plus measurability and L¹ witnesses. |
| `rieszScore γ_target L proj_X Y_obs γ α θ z` | `noncomputable def` | The Riesz orthogonal score `m(z; θ, γ, α) := L γ + α(proj_X z) · (Y_obs z − γ_target γ (proj_X z)) − θ`. |
| `rieszScore_meanZero` | `theorem` | Under the regression-orthogonality assumption `∫ z, α₀(proj_X z)·(Y_obs z − γ_target γ₀ (proj_X z)) ∂P_Z = 0`, the population score at the truth `(γ₀, α₀, L γ₀)` integrates to zero. **Proved.** |
| `rieszScore_bilinearRem` | `theorem` | Bilinear remainder identity: `∫ z, m(z; L γ₀, γ, α) ∂P_Z − ∫ z, m(z; L γ₀, γ₀, α₀) ∂P_Z = − ∫ x, (α x − α₀ x)·(γ_target γ x − γ_target γ₀ x) ∂P_X` under pushforward `P_X = P_Z.map proj_X`, dual regression-orthogonality, integrability of the perturbed residual, and integrability of the four product terms. **Proved.** |

### 12g.8. `Estimation/OrthogonalMoments/Parametric.lean`

Parametric (no-nuisance) specialisation of `GeneralMoment` (`H = Unit`). With `H = Unit`, the abstract `dmlChernozhukovEstimator` collapses to the classical parametric one-step estimator and the rescaled remainder is identically zero. OLS, IV-2SLS, MLE instantiate this pattern.

| Declaration | Statement | Description |
|---|---|---|
| `parametricMoment m_par θ₀ J₀ hJ m_meas` | `noncomputable def` | Constructs `GeneralMoment Ω μ Z P_Z Unit` from a user-supplied moment `m_par : ℝ → Z → ℝ`, target `θ₀`, and Jacobian `J₀ ≠ 0`. Bilinear seminorms are zero (no nuisance to perturb). |
| `parametric_asymptoticLinear` | `theorem` | `IsAsymLinear (dmlChernozhukovEstimator (parametricMoment …) sample split (fun _ _ => ())) θ₀ (fun z => −J₀⁻¹ · m_par θ₀ z) sample split.foldB` from population mean-zero `∫ m_par(θ₀, ·) dP_Z = 0` and L²-finite `Integrable ((m_par θ₀)²) P_Z`. The remainder collapses identically: the parametric one-step estimator equals the influence-function partial sum exactly. **Proved.** |

### 12g.9. `Estimation/OrthogonalMoments/SecondStageOperator.lean`

Abstract second-stage regression operator and stability framework for the DR-Learner CATE estimator (Kennedy 2023). Operator-level / target-agnostic content: takes a real-valued pseudo-outcome `f : γ × Bool × ℝ → ℝ` and returns a real-valued estimate at a query point `x : γ`. No AIPW algebra, no causal content. Mirrors `def:est-cate-second-stage` and `def:est-cate-stability` in `doc/basic_concepts/po/estimation/dr_learner_cate.tex`.

| Declaration | Statement | Description |
|---|---|---|
| `SecondStageOperator Ω μ γ` | structure | Abstract bundle: `evalAt : ℕ → Ω → (γ × Bool × ℝ → ℝ) → γ → ℝ` taking sample size, randomness scope, pseudo-outcome, and query point; plus a minimal joint-measurability stub `meas_evalAt_const`. Linearity in input is left as a separate predicate `IsLinearInInput`. |
| `SecondStageOperator.IsLinearInInput op` | `Prop` | `evalAt n ω (f + g) x = evalAt n ω f x + evalAt n ω g x`. Linear smoothers satisfy this; tree mean estimators with random splits typically do not. |
| `SecondStageOperator.oracleEstimator op f` | `def` | Operator applied to a fixed "true" pseudo-outcome `f` (Kennedy's `\tilde\tau_n`). |
| `SecondStageOperator.oracleRiskScale op f target x n` | `noncomputable def`, `ℝ` | `R^*_n(x) := √(∫ ω, (op.evalAt n ω f x − target x)² ∂μ)`. Pointwise oracle risk scale. |
| `IsConditionalBias` | removed | The old placeholder `Prop` alias (`True`) was removed. Conditional-bias identification now enters through a caller-supplied `BiasIdent` predicate parameter on `Stable` and `oracle_expansion`. |
| `Stable op target d_n x BiasIdent` | `Prop` | Def 4 stability: for any `(fHat_n, f, bHat_n)` with `BiasIdent fHat_n f bHat_n`, `d_n →_p 0` implies the operator-level discrepancy `op.evalAt(fHat_n) − op.evalAt(f) − op.evalAt(bHat_n)` is `o_p(R^*_n(x))`. |
| `oracle_expansion` | `theorem` | Headline: under `Stable op target d_n x BiasIdent`, `Tendsto_inProb d_n 0`, and a `BiasIdent fHat_n f bHat_n` witness, the operator-level oracle expansion `op.evalAt(fHat_n) − op.evalAt(f) − op.evalAt(bHat_n) = o_p(R^*_n(x))` holds. **Proved by Codex** (one-line application of `Stable`). |

**Refactored 2026-05-09:** the placeholder `IsConditionalBias := True` was replaced by an explicit Prop predicate parameter `BiasIdent` that callers supply, restoring semantic content.

### 12g.10. `Estimation/OrthogonalMoments/LinearSmoother.lean`

Abstract weighted-Hölder bound for linear-smoother second-stage operators. Mirrors `prop:est-cate-linear-smoother-bound` in `doc/basic_concepts/po/estimation/dr_learner_cate.tex`.

| Declaration | Statement | Description |
|---|---|---|
| `LinearSmootherOp Ω μ γ` | structure (extends `SecondStageOperator`) | Adds `weights : ℕ → Ω → γ → (γ × Bool × ℝ) → ℝ`. The actual weighted-sum identity `evalAt n ω f x = Σ w · f(z_i)` is supplied as a separate predicate `IsLinearSmoother` rather than baked in. |
| `LinearSmootherOp.IsLinearSmoother op n ω x B w xs` | `Prop` | Predicate witnessing the weighted-sum identity `∀ f, op.evalAt n ω f x = Σ_{i ∈ B} w i · f (xs i)` for explicit weights `w : ι → ℝ` and data points `xs : ι → γ × Bool × ℝ`. |
| `WeightedNorm B w g p` | `noncomputable def`, `ℝ` | Weighted Lᵖ norm `(Σ_i ∈ B, |w i| / (Σ_j |w j|) · |g i|^p)^(1/p)`. Index type `ι` abstract; uses `Real.rpow`. |
| `WeightedNorm_nonneg` | `lemma` | Non-negativity of `WeightedNorm`. **Proved.** |
| `smoother_bias_holder` | `theorem` | `|op.evalAt n ω (fun z => g z.1) x| ≤ c_n · WeightedNorm B w (g ∘ xs.1) 1` under `IsLinearSmoother op n ω x B w xs` and `Σ |w| ≤ c_n`. **Proved by Codex** via triangle inequality + factor-out-`Σ|w|` rearrangement of the weighted-L¹ norm. |
| `smoother_bias_product_holder` | `theorem` | Hölder for `g = g₁·g₂`: `|op.evalAt n ω (fun z => g₁(z.1) · g₂(z.1)) x| ≤ c_n · ‖g₁ ∘ xs.1‖_{w,p} · ‖g₂ ∘ xs.1‖_{w,q}` under `IsLinearSmoother`, `Σ |w| ≤ c_n`, and `Real.HolderConjugate p q`. **Proved by Codex** via `Real.inner_le_Lp_mul_Lq_of_nonneg` applied to `f i := |w i|^{1/p} · |g₁ (xs i).1|`, `g i := |w i|^{1/q} · |g₂ (xs i).1|` with `1/p + 1/q = 1`. |

### 12g.11. `Estimation/OrthogonalMoments/AutoDebias/` — Automatic debiasing module

Reusable construction that converts any **linear-in-regression target** (or, more generally, any **regression-nuisance moment**) into a Neyman-orthogonal score by adding the Riesz-representer correction `α(X)·(Y − g(X))`. Mirrors `doc/basic_concepts/po/estimation/automatic_debiasing.tex`. The module contains an algebraic core (linear functional case), a general-moment layer, and a one-shot Auto-DML estimator with its asymptotic-linearity theorem.

#### 12g.11.a `Estimation/OrthogonalMoments/AutoDebias/Linear.lean` — Linear functional core

Defs 1, 3, 5 and Props 4, 6 of the tex.

| Declaration | Statement | Description |
|---|---|---|
| `LinRegFnSys` | structure | Linear regression-functional system: types `Z, X, H_γ` with measure/AddCommGroup/Module instances, `proj_X, Y_obs, γ_target` (linear in `H_γ`), observation-level linear-in-γ functional `m_lin : Z → H_γ → ℝ`, regression truth `g₀`, pushforward `P_X = P_Z.map proj_X`, and the integrated dual-orthogonality `regression_resid_orthog`. |
| `L_of_m S γ` | `noncomputable def` | Population linear functional `γ ↦ ∫ m_lin(z, γ) dP_Z`. |
| `L_of_m_add`, `L_of_m_smul` | `theorem`, `theorem` | Linearity of `L_of_m` in `γ` (under integrability). **Proved.** |
| `linRieszScore S γ α θ z` | `noncomputable def` | Alias of `rieszScore` from `Estimation.OrthogonalMoments.Riesz` specialised to a `LinRegFnSys`. |
| `linRieszScore_meanZero` | `theorem` | Population score at the truth `(g₀, α₀, L g₀)` integrates to zero (Prop 6, mean-zero). Wraps `rieszScore_meanZero` via `regression_resid_orthog`. **Proved.** |
| `linRieszScore_directional_g_zero` | `theorem` | Directional zero in the regression direction: `(∫ m_lin z ν_g dP_Z) − ∫ rep.α₀ · γ_target ν_g dP_X = 0` (Prop 6, part b). Direct from `rep.representation`. **Proved.** |
| `linRieszScore_directional_α_zero` | `theorem` | Directional zero in the representer direction: residual orthogonality of `ν_α` against `Y_obs − γ_target g₀` (Prop 6, part c). Direct call to `regression_resid_orthog`. **Proved.** |
| `linRieszLoss S α` | `noncomputable def` | Linear Riesz loss `∫ (γ_target α x)² dP_X − 2 · L_of_m S α` (Def 3). |
| `linRieszLoss_excess_eq_l2dist` | `theorem` | Prop 4, second half: `linRieszLoss α − linRieszLoss α₀_idx = ∫ (γ_target α − γ_target α₀_idx)² dP_X` when `rep.α₀ = γ_target α₀_idx` pointwise. Complete-the-square via `rep.representation`. **Proved.** |
| `linRieszLoss_FOC_iff_representer` | `theorem` | Prop 4, first half: `α₀_idx` is a directional minimizer of `linRieszLoss` along every line `α₀_idx + t • ν` iff `L(ν) = ∫ γ_target α₀_idx · γ_target ν dP_X` for every `ν`. Parabolic-identity FOC argument with quadratic-coefficient case split on `a = ∫ (γ_target ν)² dP_X`. **Proved.** |

#### 12g.11.b `Estimation/OrthogonalMoments/AutoDebias/General.lean` — General-moment layer

Defs 8, 9, 10, 12, 13 and Prop 11 of the tex.

| Declaration | Statement | Description |
|---|---|---|
| `RegNuisanceMomentSys` | structure | Regression-nuisance moment system. Same data layer as `LinRegFnSys`; replaces the linear functional `m_lin` with a general moment `M : ℝ → H → ℝ` (kernel `m : H → Z → ℝ → ℝ`, `M_truth : M θ₀ g₀ = 0`, `m_population : M θ g = ∫ m g z θ dP_Z`) and a precomputed Gateaux derivative `D_g_M : H → ℝ` with linearity laws. |
| `AutoDebiasRepresenter S` | structure | The representer `α₀ : X → ℝ` characterised by `D_g_M ν = ∫ α₀ · γ_target ν dP_X` (Def 9). |
| `autoDebiasedScore S g α θ z` | `noncomputable def` | Observation-level form: `m g z θ + α(proj_X z) · (Y_obs z − γ_target g (proj_X z))`. |
| `autoDebiasedMoment S g α θ` | `noncomputable def` | `∫ z, autoDebiasedScore S g α θ z ∂P_Z` (Def 10). |
| `autoDebiasedMoment_meanZero_at_truth` | `theorem` | Prop 11, part a: `autoDebiasedMoment S g₀ rep.α₀ θ₀ = 0` from `M_truth` + `regression_resid_orthog` + `m_population`. **Proved.** |
| `autoDebiasedMoment_directional_g_zero` | `theorem` | Prop 11, part b: `D_g_M ν_g − ∫ rep.α₀ · γ_target ν_g dP_X = 0`. Direct from `rep.representation`. **Proved.** |
| `autoDebiasedMoment_directional_α_zero` | `theorem` | Prop 11, part c: residual orthogonality of arbitrary measurable `ν_α`. Direct call to `regression_resid_orthog`. **Proved.** |
| `genRieszLoss S α` | `noncomputable def` | General Riesz loss `∫ (γ_target α x)² dP_X − 2 · D_g_M α` (Def 12). FOC ↔ representer characterisation is structurally the same as `linRieszLoss_FOC_iff_representer`; not re-proved here. |
| `finDiffRieszLoss S Z_data θ_hat g_hat ε α C n ω` | `noncomputable def` | Finite-difference representer loss (Def 13): empirical second moment minus a centred finite-difference approximation of `D_g M(θ̂_n, ĝ_n)[α]` with perturbation scale `ε n`. Pure definition; intended target of future RieszNet-style analysis. |

#### 12g.11.c `Estimation/OrthogonalMoments/AutoDebias/DML.lean` — Auto-DML estimator and AL theorem

Def 6 and Thm 7 of the tex.

| Declaration | Statement | Description |
|---|---|---|
| `linAutoNuisance S` | `def` | Joint nuisance type `S.H_γ × (S.X → ℝ)` with componentwise `AddCommGroup` (`Prod.instAddCommGroup`) and `Module ℝ` (`Prod.instModule`). |
| `linAutoGeneralMoment μ S rep ε hε_nn h_score_meas` | `noncomputable def` | `GeneralMoment` instance over `linAutoNuisance S`. `m (γ, α) z θ := linRieszScore S γ α θ z`; `η₀ := (S.g₀, rep.α₀)`; `θ₀ := L_of_m S S.g₀`; `H_ε := { (γ, α) | ‖γ_target γ − γ_target g₀‖_{L²(P_X)} ≤ ε ∧ ‖α − α₀‖_{L²(P_X)} ≤ ε }`; `ρ₁`/`ρ₂` are the corresponding L² norms; `J₀ = −1`. The measurability witness `h_score_meas : ∀ η θ, Measurable (fun z => linRieszScore S η.1 η.2 θ z)` and non-negativity `hε_nn : 0 ≤ ε` are passed as parameters because `linAutoNuisance` does not internally constrain `α : X → ℝ` to be measurable. |
| `linAuto_meanZero` | `theorem` | `MeanZero` of the linear Auto-DML moment at the truth. Direct call to `linRieszScore_meanZero`. **Proved.** |
| `linAuto_int_pred S rep η` | `private def` | Per-η integrability bundle (10 components) exposing the exact API shape required by `rieszScore_bilinearRem`. Used as a hypothesis in `linAuto_bilinearRem`. |
| `linAuto_bilinearRem` | `theorem` | Bilinear remainder bound: `\|∫ z, score η z θ₀ ∂P_Z\| ≤ C · ρ₁(η, η₀) · ρ₂(η, η₀)` for `η ∈ H_ε` with `linAuto_int_pred S rep η` and explicit `MemLp` witnesses for the two L²(P_X) differences. Proof factors via `rieszScore_bilinearRem` then applies Hölder-2 (`integral_mul_norm_le_Lp_mul_Lq`). **Proved.** |
| `linAutoDMLEstimator S rep ε hε_nn h_score_meas sample split η_hat` | `noncomputable def` | One-shot linear Auto-DML estimator (Def 6), built as `dmlChernozhukovEstimator (linAutoGeneralMoment μ S rep ε hε_nn h_score_meas) sample split η_hat`. Equivalently: `(1/\|B(n)\|) Σ_{i ∈ B(n)} [m_lin(Z_i; ĝ_n) + α̂_n(X_i)·(Y_i − ĝ_n(X_i))]`. |
| `linAutoDML_asymptoticLinear` | `theorem` | Thm 7 headline: `IsAsymLinear (linAutoDMLEstimator …) (L_of_m S S.g₀) (fun z => −J₀_inv · linRieszScore S S.g₀ rep.α₀ θ₀ z) sample split.foldB` under `MeanZero`, finite L² of the oracle score, per-η bilinear remainder, the standard fold-A measurability/integrability hypotheses, and the score-difference / individual / product rate hypotheses inherited from `dml_chernozhukov_asymptoticLinear`. Proof: direct composition. **Proved.** |

The module skips the ATE-equivalence remark from the tex (Remark 7) per design.

## 13'''. `Estimation/CATE/` — DR-Learner CATE estimation (Kennedy 2023)

Specific-system layer instantiating `BackdoorEstimationSystem` for the function-valued target `τ₀(x) = μ_val(true, x) − μ_val(false, x)`. Mirrors `doc/basic_concepts/po/estimation/dr_learner_cate.tex`. The DR-Learner is built on top of the same AIPW pseudo-outcome already used for ATE; the new content is (1) the function-valued target with `condDistrib`-style pointwise interpretation, (2) the DR conditional-bias identity (Prop 2 / `prop:est-cate-dr-bias-identity`, the milestone), and (3) the second-stage regression operator with stability hypothesis driving the oracle expansion (Theorem 5).

### 13'''.a `Estimation/CATE/Setup.lean`

| Declaration | Statement | Description |
|---|---|---|
| `CATEEstimationSystem P γ` | structure (extends `BackdoorEstimationSystem`) | Empty extension — the CATE target `τ_val` is purely derived. All back-door substrate (consistency, exchangeability, overlap, integrability, `μ_val` / `e_val`, σ(X)-compatibility) is inherited. |
| `CATEEstimationSystem.τ_val S x` | `def`, `ℝ` | Value-space CATE: `τ_val x := μ_val true x − μ_val false x`. Definition `def:est-cate-system`. |
| `CATEEstimationSystem.measurable_τ_val` | `lemma` | Measurability of `τ_val`. **Proved.** |
| `CATEEstimationSystem.tau_val_eq_CATE` | `theorem` | `(fun ω => τ_val (factualX ω)) =ᵐ μ[Y(true) − Y(false) | σ(X)]` under the back-door assumptions. **Proved by Codex** (combine `μ_compat true/false` with `condExp_sub`). |

### 13'''.b `Estimation/CATE/PseudoOutcome.lean`

| Declaration | Statement | Description |
|---|---|---|
| `phi_eta z η` | `noncomputable def` | Uncentered AIPW pseudo-outcome (Kennedy's `φ_η`): `aipwMoment z η.μ_fn η.e_fn 0`. Definition `def:est-cate-dr-pseudo-outcome`. |
| `phi₀ S z` | `noncomputable def` | True pseudo-outcome `φ_0 := φ_{η_0}`. |
| `measurable_phi_eta`, `measurable_phi₀` | `lemma` | Measurability in the data argument. **Proved by Codex** (mirror `measurable_ψ_AIPW`). |

### 13'''.c `Estimation/CATE/PseudoOutcomeMean.lean` (Prop 1)

| Declaration | Statement | Description |
|---|---|---|
| `phi₀_factualZ_cond_exp` | `theorem` | σ(X)-conditional form of `prop:est-cate-dr-cond-mean`: `μ[φ₀(factualZ) | σ(X)] =ᵐ τ_val ∘ factualX` under back-door + strict overlap. **Proved by Codex** via local `weighted_residual_cond_exp_zero` helper + `cond_exp_residual_zero` + `condExp_mul_of_stronglyMeasurable_left`. |
| `phi_eta_cond_exp_eq_tau` | `theorem` | Value-space `P_X`-a.e. form: `∀ᵐ x ∂P_X, ∫ z, φ₀(z) ∂condDistrib factualZ factualX P.μ x = τ_val x`. Hypotheses add `[StandardBorelSpace γ] [Nonempty γ]` (for `condDistrib`) and `Integrable (φ₀ ∘ factualZ) P.μ`. **Proved by Codex** by transporting `phi₀_factualZ_cond_exp` through `condExp_ae_eq_integral_condDistrib` and pushing forward to `P_X` via `MeasureTheory.ae_map_iff` (measurability of the equality set comes from `StronglyMeasurable.integral_condDistrib` for the LHS and `measurable_τ_val` for the RHS). |

### 13'''.d `Estimation/CATE/ConditionalBias.lean` (Prop 2 — milestone)

The DR conditional-bias identity. Pure σ(X)-conditional algebra; the closed-form cross-product remainder driving the double-robustness corollary (`rem:est-cate-double-robust`).

| Declaration | Statement | Description |
|---|---|---|
| `condBias η η₀ x` | `noncomputable def`, `ℝ` | Closed form: `Σ_{a∈Bool} ((η.e_fn x − η₀.e_fn x)·(η.μ_fn a x − η₀.μ_fn a x)) / (if a then η.e_fn x else 1 − η.e_fn x)`. |
| `measurable_condBias` | `lemma` | Measurability in `x`. **Proved by Codex** via `Finset.measurable_sum` + product-of-measurables. |
| `cond_exp_residual_at_h` | `private lemma` | Generalized form of `cond_exp_residual_zero` (parameterised over arbitrary measurable `h : γ → ℝ`): `μ[1{D=d}·(Y − h(X)) | σ(X)] =ᵐ propScore d · (μ_val d (factualX) − h (factualX))`. **Proved by Codex** mirroring `MeanZero.lean` lines 109–196. |
| `phi_eta_minus_phi₀_cond_exp` | `theorem` | σ(X)-conditional bias identity (Prop 2 form on Ω): `μ[φ_η(factualZ) − φ_0(factualZ) | σ(X)] =ᵐ condBias η η₀ ∘ factualX` under per-arm outcome-regression integrability and strict overlap. **Proved by Codex** with a 100-line expansion: 4 calls to `cond_exp_residual_at_h` + `e_compat` / `propScore_false_ae` substitution + `field_simp` + `ring`. **Hypothesis additions vs. plan**: `(h_μ_η_int : ∀ a, Integrable (η.μ_fn a ∘ factualX) P.μ)` and `(hε_pos : 0 < ε)` were added so the algebra is well-defined. |
| `phi_eta_minus_phi₀_at_x` | `theorem` | Value-space `P_X`-a.e. form: `∀ᵐ x ∂P_X, ∫ z, (phi_eta z η − phi₀ S z) ∂condDistrib factualZ factualX P.μ x = condBias η η₀ x`. Hypotheses add `[StandardBorelSpace γ] [Nonempty γ]`, `0 < ε`, and per-arm η-integrability. **Proved by Codex** by transporting `phi_eta_minus_phi₀_cond_exp` through `condExp_ae_eq_integral_condDistrib` and pushing forward to `P_X` via `MeasureTheory.ae_map_iff` (measurability of the equality set from `StronglyMeasurable.integral_condDistrib` and `measurable_condBias`). |
| `condBias_zero_of_propensity_match`, `condBias_zero_of_outcome_match` | `lemma` | Double-robustness corollaries (`rem:est-cate-double-robust`): `condBias = 0` if propensity matches truth, or if both outcome arms match. **Proved by Codex** via `Finset.sum_eq_zero` + `sub_eq_zero` substitution. |
| `cond_exp_phi_eta_dir_deriv_at_truth_zero` | `theorem` | Differentiated companion of `phi_eta_minus_phi₀_cond_exp`: σ(X)-conditional of `phi_eta_dir_deriv η₀ v (factualZ)` is zero a.e. under strict overlap on `η₀` and bounded `v` (uniform bounds on `v.μ_fn b` and `v.e_fn`). **Proved by Codex** by 5-summand decomposition of the closed form; the two `e_fn`-residual terms vanish via `cond_exp_residual_zero` (d=true / d=false) and the two `μ_fn`-pullouts cancel via `S.e_compat` (propScore = e_val a.e.) plus the derived `propScore false =ᵐ 1 − e_val ∘ factualX`. |

### 13'''.d′ `Estimation/CATE/PhiEtaDeriv.lean`

Closed-form directional derivative of the DR pseudo-outcome `phi_eta` in its nuisance argument `η`, evaluated at an anchor `g₀ : NuisanceVec γ` along a direction `v : NuisanceVec γ`. Because `phi_eta` is reciprocal in `η.e_fn`, the value-difference `phi_eta z η − phi_eta z g₀` is **not** the directional derivative; this file computes the genuine derivative and proves the pointwise difference-quotient tendsto under strict overlap.

| Declaration | Statement | Description |
|---|---|---|
| `phi_eta_dir_deriv g₀ v z` | `noncomputable def`, `ℝ` | Closed-form directional derivative `D_g phi_eta(z, g₀)[v] = (v.μ_fn true X − v.μ_fn false X) + indA(z)·(−v.e_fn(X)/g₀.e²·(Y − g₀.μ_fn true X) − v.μ_fn true X / g₀.e_fn X) − (1 − indA(z))·(v.e_fn(X)/(1 − g₀.e_fn X)²·(Y − g₀.μ_fn false X) − v.μ_fn false X / (1 − g₀.e_fn X))`. Used by `Estimation/CATE/OrthogonalLearning/DRLearner/Analytic.lean` to identify the opaque `NuisanceDirDeriv.dPhi` field via uniqueness of limits. |
| `measurable_phi_eta_dir_deriv` | `lemma` | Measurability in `z`. Proved by composing the measurabilities of the five summands. |
| `phi_eta_dir_deriv_tendsto` | `theorem` | Pointwise tendsto: `(phi_eta z (g₀ + t • v) − phi_eta z g₀) / t → phi_eta_dir_deriv g₀ v z` as `t → 0` along `𝓝[≠] 0`, under strict overlap (`g₀ ∈ H_ε ε`, `0 < ε`). **Proved by Codex** via a private `phi_eta_real_line_hasDerivAt` HasDerivAt helper on ℝ + standard real-analysis combinators; the strict-overlap hypothesis ensures the denominators `g₀.e_fn X` and `1 − g₀.e_fn X` are nonzero. |

### 13'''.e `Estimation/CATE/Kennedy/DRLearner.lean`

Thin instantiation of `SecondStageOperator` to the AIPW pseudo-outcome.

| Declaration | Statement | Description |
|---|---|---|
| `drLearnerEstimator S op η_hat n ω x` | `noncomputable def`, `ℝ` | `op.evalAt n ω (fun z => phi_eta z (η_hat n ω)) x`. Kennedy's `\hat\tau^{DR}_n(x)`. |
| `drOracleEstimator S op n ω x` | `noncomputable def`, `ℝ` | `op.evalAt n ω (fun z => phi₀ S z) x`. Kennedy's `\tilde\tau_n(x)`. |
| `drOracleRiskScale S op x n` | `noncomputable def`, `ℝ` | Specialisation of `oracleRiskScale` to `(phi₀ S, S.τ_val, x)`. |
| `drOracleEstimator_eq_oracleEstimator` | `lemma` | Sanity unfolding: `drOracleEstimator S op = op.oracleEstimator (phi₀ S)`. **Proved** by `rfl`. |

### 13'''.f `Estimation/CATE/Kennedy/OracleExpansion.lean` (Theorem 5)

| Declaration | Statement | Description |
|---|---|---|
| `dr_oracle_expansion` | `theorem` | AIPW specialisation of `General.oracle_expansion`: under `CATEEstimationSystem`, `Stable op S.τ_val d_n x BiasIdent`, `Tendsto_inProb d_n 0`, and a `BiasIdent` witness for `phi_eta`, `phi₀`, and `condBias`, `(drLearner − drOracle − op.evalAt(condBias)) = o_p(R^*_n(x))`. **Proved by Codex** as a direct call to `General.oracle_expansion`. |
| `dr_oracle_efficient` | `theorem` | Corollary: if additionally the smoothed-bias term is `o_p(R^*_n(x))`, then `(drLearner − drOracle) = o_p(R^*_n(x))`. **Proved by Codex** via `IsLittleOp.add_eventually_nonneg_rate` (newly exposed from `Estimation/ATE/RemainderIdentity.lean`) + algebraic cancellation via `convert ... ; funext ; ring`. |

### 13'''.g `Estimation/CATE/Kennedy/LinearSmootherCATE.lean` (Prop 6)

| Declaration | Statement | Description |
|---|---|---|
| `cate_linear_smoother_bias_bound` | `theorem` | Hölder bound for the smoothed DR-bias term: `\|op.evalAt n ω (condBias η_hat η₀ ∘ z.1) x\| ≤ aipw_rem_const ε · c_n · ‖Δπ‖_{w,p} · Σ_a ‖Δμ_a‖_{w,q}` under `IsLinearSmoother op n ω x B w xs`, `Σ \|w\| ≤ c_n`, `Real.HolderConjugate p q`, and `η_hat n ω ∈ H_ε ε`. **Proved by Codex (filled)** — codex left 4 mechanical errors (unknown `Finset.sum_bool` instead of `Fintype.sum_bool`; summand-order mismatch; sum-distributivity rearrangements) which were patched inline. The proof: bound `\|condBias y\| ≤ aipw_rem_const ε · Σ_a \|Δπ y\| · \|Δμ_a y\|` pointwise (mirrors `aipw_remainder_bound` in `RemainderBound.lean`), then apply `smoother_bias_product_holder` arm-by-arm to a constructed `absOp` with absolute weights. |
| `cate_dr_oracle_efficient_linear` | `theorem` | Corollary plugging a linear-smoother `LinearSmootherOp` into `dr_oracle_efficient` (uses `op.toSecondStageOperator` projection). **Proved** by direct application of `dr_oracle_efficient`. |

### 13'''.h `Estimation/CATE/SecondOrderBias.lean` (honest second-order product bias — reusable core)

The genuine double-robustness content: the DR-Learner nuisance bias weighted by an arbitrary bounded σ(X)-test function `w` is a **product** of the two nuisance L²-errors. Generalises `aipw_remainder_bound` (the `w ≡ 1` / ATE case) to a test function. All sorry-free + axiom-clean.

| Declaration | Statement | Description |
|---|---|---|
| `integral_phiDiff_mul_eq_condBias` | `theorem` | Conditioning identity: `∫ z, (phi_eta z η − phi₀ S z)·w z.1 ∂P_Z = ∫ x, condBias η η₀ x · w x ∂P_X` for measurable `w`. **Proved** by pulling the σ(X)-measurable factor `w(X)` through `condExp_mul_of_stronglyMeasurable_right` and substituting `phi_eta_minus_phi₀_cond_exp` (the `dr_scoreZero` pull-out idiom). |
| `abs_integral_phiDiff_mul_le_product` | `theorem` | Second-order product bound: `\|∫ z, (phi_eta z η − phi₀ S z)·w z.1 ∂P_Z\| ≤ (B/ε)·Σ_a ‖η.μ_fn a − μ_val a‖_{L²(P_X)}·‖η.e_fn − e_val‖_{L²(P_X)}` for `\|w\| ≤ B`, `η ∈ H_ε`. **Proved** by the identity + pointwise `\|condBias·w\| ≤ (B/ε)(\|Δμ_T·Δe\| + \|Δμ_F·Δe\|)` (strict overlap) + `integral_abs_mul_le_eLpNorm_mul_eLpNorm` per arm — the Cauchy–Schwarz route of `aipw_remainder_bound`. |

### 13'''.i `Estimation/CATE/OrthogonalLearning/SecondOrderBias.lean` (DR-Learner `Bias_n` is second-order)

| Declaration | Statement | Description |
|---|---|---|
| `OSL.drBias_le_product` | `theorem` | `\|Bias_n (drOSLSystem) ((drMixedDirDeriv).Dθ_at η₀) ((drMixedDirDeriv).Dθ_at h) θ̂\| ≤ (2B/ε)·Σ_a ‖h.μ_fn a − μ_val a‖₂·‖h.e_fn − e_val‖₂` for `\|D.dEval θ̂\| ≤ B`, `h ∈ H_ε`. **Proved**: both `drMixedDirDeriv` bundles have `dℓ_θ = -2·(phi_eta z η − eval θ₀ z.1)·D.dEval θ z.1` (literal field), so `Bias_n` telescopes (the `eval θ₀` term cancels) to `2·∫(phi_eta·h − phi₀)·D.dEval θ̂`, then `abs_integral_phiDiff_mul_le_product` with `w := D.dEval θ̂`. **Replaces the trivial `OSL.Bias_taylor_form` existential placeholder.** Axiom-clean. |

### 13'''.j `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/DRLearnerHonest.lean` (honest fixed-nuisance Kennedy oracle inequality)

| Declaration | Statement | Description |
|---|---|---|
| `OSL.oracle_inequality_drLearner_highProb_honest` | `theorem` | Same setup as `oracle_inequality_drLearner_highProb`, with the DD bundles fixed to `drMixedDirDeriv` and a uniform bound `\|D.dEval θ\| ≤ B` on `Θ_set`. Conclusion replaces the symbolic `Bias_n` term with the **explicit** product bias: `‖τ̂ − θ₀‖² ≤ (4(1+σ)/σ²)·ρ²_{n,δ} + (4/σ)·(2B/ε)·Σ_a ‖Δμ_a‖₂‖Δe‖₂ + (4/σ)·r_opt n` w.p. ≥ 1−δ. **Proved** by composing the existing end-to-end with `drBias_le_product` (`(4/σ)·Bias_n ≤ (4/σ)·\|Bias_n\| ≤ …`). Axiom-clean. |

### 13'''.k `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/DRLearnerRandomNuisance.lean` (un-pinned random cross-fitted nuisance)

| Declaration | Statement | Description |
|---|---|---|
| `OSL.oracle_inequality_drLearner_random_nuisance` | `theorem` | Un-pins the nuisance from fixed `h` to a random fold-A estimate `ĥ : ℕ → Ω → NuisanceVec γ` (each `ĥ n ω ∈ H_ε`). Conclusion carries the **random** product bias `(4/σ)·(2B/ε)·Σ_a ‖ĥ n ω.μ_fn a − μ_val a‖₂·‖ĥ n ω.e_fn − e_val‖₂` on a high-prob set per `n`. **Proved** by the deterministic `oracle_inequality_plugin_ERM` (already accepts random `ghat`) + `drBias_le_product` at `ĥ n ω` (already holds ∀ `h ∈ H_ε`). Takes the realised empirical-process modulus event `hMod` as a hypothesis — discharging it is the cross-fitting frontier (keystone now proved, see `RandomParam.lean`). Axiom-clean. |

### 13'''.l `Estimation/OrthogonalLearning/LocalEmpProcess/RandomParam.lean` (cross-fit conditioning keystone)

| Declaration | Statement | Description |
|---|---|---|
| `OSL.randomParam_event_le` | `theorem` | **Cross-fit conditioning keystone.** Given a fold-A sub-σ-algebra `m_A`, the fold-B coordinate map `Y : Ω → β` with `μ.map Y = ν` and `Indep m_A σ(Y)`, a fold-A-measurable bad-set family `Bad : Ω → Set β` (joint-measurable in `m_A.prod mβ`) with each section `ν (Bad ω) ≤ δ`, then the *random* event `μ {ω : Y ω ∈ Bad ω} ≤ δ` — plugging a fold-A-measurable random parameter into fold-B events of uniform mass ≤ δ preserves the bound. **Proved** via `indep_trim_prod_map_eq` (product factorisation `μ.map (ω ↦ (ω,Yω)) = (μ.trim m_A).prod ν`) + `Measure.prod_apply` (Tonelli on the ν-section) + `∫(≤δ)=≤δ`. Axiom-clean. The measure-theoretic heart of lifting the fixed-nuisance modulus to a random cross-fitted nuisance — now consumed by `RandomNuisance.lean` below. |

### 13'''.l.2 `Estimation/OrthogonalLearning/LocalEmpProcess/RandomNuisance.lean` (cross-fit lift of the modulus to a random nuisance)

Closes the OSL/Kennedy G2 frontier: the fixed-`g` bridge `localEmpProcessModulus_of_bounded_rademacher` is lifted to a *random fold-A-measurable nuisance* `ĥ : ℕ → Ω → G` via the keystone. All declarations sorry-free and axiom-clean.

| Declaration | Statement | Description |
|---|---|---|
| `OSL.LocalEmpProcessModulusRandom S S_iid split ρ δ ĥ` | `def`, `Prop` | Random-`g` analogue of `LocalEmpProcessModulus`: for each `n` a high-prob (`≥ 1−δ`) event on which the centred excess risk at the random nuisance `ĥ n ω` obeys the modulus inequality uniformly in `θ`. Exactly the `hMod` shape consumed by `oracle_inequality_drLearner_random_nuisance`. |
| `OSL.foldBCoord S_iid split n` | `noncomputable def`, `Ω → Fin (foldB n).card → Z` | Fold-B coordinate map (reindex of the fold-B subsample by `orderIsoOfFin`). |
| `OSL.badDataSet S S_iid split n b δ R g` | `noncomputable def`, `Set (Fin (foldB n).card → Z)` | Product-space bad set for nuisance `g`: `{s : 2•rademacherComplexity + 2b√(2 log(1/δ)/m) ≤ uniformDeviation (centred-loss class at g) s}`. |
| `OSL.modulusRadius split b δ R` | `noncomputable def`, `ℕ → ℝ` | Deterministic radius `ρ n = √(if card=0 then 2b else 2 R n + 2b√(2 log(1/δ)/card))`. |
| `OSL.foldBCoord_law` | `theorem` | `μ.map (foldBCoord S_iid split n) = Measure.pi P_Z` — the fold-B coordinate law (mirrors the monolith `hY_law`, via `foldB_pi_law` + `piCongrLeft`). |
| `OSL.foldBCoord_meas` | `theorem` | `foldBCoord` is measurable. |
| `OSL.badDataSet_mass_le` | `theorem` | `Measure.pi P_Z (badDataSet … g) ≤ δ` (McDiarmid tail bound `uniform_deviation_tail_bound_separable_of_pos` at the centred class; needs `0 < b`, `0 < card`). |
| `OSL.modulus_of_not_badData` | `theorem` | Off the bad set (`foldBCoord ω ∉ badDataSet … g`), the modulus inequality holds uniformly over `Θ_set` (mirrors the monolith on-event branch; re-derives the dense-range Rademacher bridge inline since the monolith's bridge lemmas are `private`). |
| `OSL.localEmpProcessModulus_random_of_bounded_rademacher` | `theorem` | **Cross-fit lift (frontier capstone).** Given a random nuisance `ĥ` with uniform bound `b>0`, continuity, and Rademacher bound `R` *at every realisation* `ĥ n ω`, plus the cross-fit structural facts `hindep n` (fold A ⊥ fold-B coords) and `hbad_joint n` (the bad set depends jointly measurably on `(ω,s)` w.r.t. `m_A n ⊗ σ(foldB)`), concludes `LocalEmpProcessModulusRandom S S_iid split (modulusRadius …) δ ĥ`. **Proved** by `badDataSet_mass_le` (per-section mass) + `randomParam_event_le` (condition on fold A) for the high-prob event, `modulus_of_not_badData` on the complement, with an `card=0` trivial branch. Axiom-clean. `hindep`/`hbad_joint` are named structural hypotheses (true by the cross-fit construction; same spirit as taking `RademacherBound` as a hypothesis). |

## 13''''. `Estimation/OrthogonalLearning/` — Orthogonal Statistical Learning (function-valued targets)

Sibling submodule to `Estimation/OrthogonalMoments/` and `Estimation/CATE/`: when the target is a function (e.g. CATE) instead of a scalar functional, "orthogonality" is Neyman-orthogonality of the *loss*, and the headline guarantee is a deterministic **oracle inequality** in the target-space norm rather than a CLT. Mirrors `doc/basic_concepts/po/estimation/orthogonal_statistical_learning.tex`. Phase-1 + Phase-2 + Phase-3 land the deterministic theorems and the DR-Learner orthogonality witness (abstract — concrete discharge of the mixed-DD bundle / DCT bridge / Riesz cancellation deferred to `Estimation/CATE/OrthogonalLearning/DRLearner/Analytic.lean`); concentration / modulus realisation (Phase 4) is deferred.

### 13''''.a `Estimation/OrthogonalLearning/Setup.lean`

| Declaration | Statement | Description |
|---|---|---|
| `OSLSystem Ω μ Z P_Z Θ G` | `structure` | Bundle: convex `Θ_set ⊆ Θ` (Hilbert-space-like), nuisance set `G_set ⊆ G`, loss `ℓ : Z → Θ → G → ℝ`, joint measurability, truth `(θ₀, g₀)`. Statistical-learning analogue of `GeneralMoment`. Definition `def:est-osl-system`. |
| `OSLSystem.L S θ g` | `noncomputable def`, `ℝ` | Population risk `∫ ℓ z θ g ∂P_Z`. |
| `Θ_PerturbClosed`, `G_PerturbClosed` | `def`, `Prop` | Closure of `Θ_set` (resp. `G_set`) under line segments from the truth. |

### 13''''.b `Estimation/OrthogonalLearning/Population/DirectionalDeriv.lean`

| Declaration | Statement | Description |
|---|---|---|
| `HasDirDerivTheta S g` | `structure` | Pointwise dir derivative of `θ ↦ ℓ z θ g` along `θ₀ → θ`, with measurability and tendsto witnesses. Anchored at any nuisance `g`. |
| `HasDirDerivG S` | `structure` | Pointwise dir derivative of `g ↦ ℓ z θ₀ g` along `g₀ → g`. |
| `HasMixedDirDeriv S` | `structure` | Family `Dθ_at : ∀ g, HasDirDerivTheta S g` plus mixed-derivative value `dℓ_θg θ g z` with tendsto + measurability. Captures the natural-language `D_g D_θ ℓ` at `(θ₀, g₀)`. |
| `FirstOrderInequality S Dθ` | `def`, `Prop` | `∀ θ ∈ Θ_set, 0 ≤ ∫ z, Dθ.dℓ_θ θ z ∂P_Z` — population KKT inequality at the truth. |

### 13''''.c `Estimation/OrthogonalLearning/Population/NeymanOrthogonal.lean`

| Declaration | Statement | Description |
|---|---|---|
| `NeymanOrthogLoss S M` | `def`, `Prop` | Integrated mixed dir derivative vanishes: `∀ θ ∈ Θ_set, ∀ g ∈ G_set, ∫ M.dℓ_θg θ g z ∂P_Z = 0`. Definition `def:est-osl-neyman-loss`. |
| `DiffQuotientEnvelopeTheta`, `DiffQuotientEnvelopeG` | `def`, `Prop` | L¹(P_Z) envelopes for the target / nuisance difference quotients (DCT bridge inputs). |
| `neymanOrthog_iff_score_deriv_zero` | `theorem` (`sorry`) | Score reformulation: `NeymanOrthogLoss` ↔ score map `g ↦ ∫ Dθ.dℓ_θ θ z` has zero derivative at `g₀`. **Proof deferred to Phase 4 (DCT bridge).** |

### 13''''.d `Estimation/OrthogonalLearning/PluginERM.lean`

| Declaration | Statement | Description |
|---|---|---|
| `empRiskFoldB S S_iid split n ω θ g` | `noncomputable def`, `ℝ` | Estimation-fold empirical risk `(1/|B(n)|) Σ_{i∈B(n)} ℓ (Z_i ω) θ g`. |
| `SampleSplitPluginERM S S_iid split θhat ghat r_opt` | `structure` | Predicate: `θhat n ω ∈ Θ_set` and ε-minimizer of fold-B empirical risk against `ghat`, with non-negative slack `r_opt`. Definition `def:est-osl-plugin-erm`. |

### 13''''.e `Estimation/OrthogonalLearning/LocalEmpProcess/Local.lean`

| Declaration | Statement | Description |
|---|---|---|
| `LocalEmpProcessModulus S S_iid split ρ δ g` | `def`, `Prop` | Hypothesis-form modulus: per-`n`, an event of measure `≥ 1 - δ` carrying the centred-process bound `(L θ g − L θ₀ g) − (L_emp θ − L_emp θ₀) ≤ ρ_n ‖θ − θ₀‖ + ρ_n²` uniformly over `Θ_set`. Definition `def:est-osl-local-modulus`. |

### 13''''.f `Estimation/OrthogonalLearning/Population/SecondOrderBias.lean`

| Declaration | Statement | Description |
|---|---|---|
| `Bias_n S Dθ_truth Dθ_at_ghat θhat` | `noncomputable def`, `ℝ` | Loss-gradient nuisance bias `∫ Dθ_truth.dℓ_θ θhat − ∫ Dθ_at_ghat.dℓ_θ θhat`. Definition `def:est-osl-second-order-bias`. |
| `Bias_taylor_form` | `theorem` (`sorry`) | Second-order Taylor representation placeholder; full formalisation needs a `D²_g` bundle (Phase 4). |

### 13''''.g `Estimation/OrthogonalLearning/OracleInequality.lean` (Phase-1 milestone)

| Declaration | Statement | Description |
|---|---|---|
| `oracle_inequality_plugin_ERM` | `theorem` | Deterministic oracle inequality: under σ-strong convexity of `L(·, ghat n ω)`, FOI at the truth, plug-in ERM, and the modulus inequality at the sample point, `‖θhat n ω − θ₀‖² ≤ 4(1+σ)/σ² · (ρ n)² + (4/σ)·Bias_n + (4/σ)·r_opt n`. Theorem `thm:est-osl-plugin-erm-oracle`. **Proved by Codex.** Constant on `r_opt` is `4/σ` rather than the note's `2/σ` (the AM–GM proof actually yields `4/σ` — see file docstring). |
| `oracle_inequality_plugin_ERM_highProb` | `theorem` (`sorry`) | **High-probability** oracle inequality: consumes `LocalEmpProcessModulus S S_iid split ρ δ g` (the real probability statement) and concludes that for every `n`, with `μ`-probability ≥ `1 − δ` over the ambient sample, `‖θhat n ω − θ₀‖² ≤ 4(1+σ)/σ²·(ρ n)² + (4/σ)·Bias_n + (4/σ)·r_opt n`. Pinned to a fixed nuisance value `g` (`ghat ≡ g`); fold-A randomisation deferred. Phase-1 ↔ Phase-4a contact point. **Sorry**, awaiting Codex fill. |

### 13''''.h `Estimation/OrthogonalLearning/Sparse/Setup.lean`

Finite-dimensional specialisation (Phase 2). `Θ := EuclideanSpace ℝ (Fin p)`.

| Declaration | Statement | Description |
|---|---|---|
| `l1Norm ν T`, `l1Full ν` | `noncomputable def`, `ℝ` | Coordinate ℓ¹ norm restricted to a `Finset T`, and the full sum. |
| `linftyDev hp ν` | `noncomputable def`, `ℝ` | Coordinate ℓ∞ deviation `max_i |ν i|` via `Finset.sup'` on a non-empty index set. |
| `RestrictedCone S₀` | `noncomputable def`, `Set` | Cone `{ν : ‖ν_{S₀ᶜ}‖₁ ≤ 3 ‖ν_{S₀}‖₁}`. Definition `def:est-osl-sparse-plugin-erm` (cone). |
| `mem_RestrictedCone_iff` | `lemma` | Unfolding lemma. **Proved by Codex.** |
| `l1Full_eq` | `lemma` | `l1Full ν = l1Norm ν S₀ + l1Norm ν (univ \ S₀)`. **Proved by Codex.** |
| `l1Norm_supp_le_card_sqrt_mul_l2norm` | `lemma` | Cauchy–Schwarz on the support: `l1Norm ν S₀ ≤ √S₀.card · ‖ν‖`. **Proved by Codex.** |
| `SparsePluginERM empRiskFn θhat lambda` | `structure` | Exact minimiser of `empRiskFn θ + λ‖θ‖₁`. Definition `def:est-osl-sparse-plugin-erm`. |

### 13''''.i `Estimation/OrthogonalLearning/Sparse/RSC.lean`

| Declaration | Statement | Description |
|---|---|---|
| `RestrictedStrongConvexity empRiskFn gradEmp θ₀ S₀ σn` | `def`, `Prop` | σₙ-RSC at `θ₀` over `S₀`: for ν ∈ cone, `empRiskFn(θ₀+ν) − empRiskFn θ₀ − ⟪gradEmp θ₀, ν⟫ ≥ (σn/2)‖ν‖²`. Definition `def:est-osl-rsc`. |

### 13''''.j `Estimation/OrthogonalLearning/Sparse/PluginGuarantee.lean` (Phase-2 milestone)

| Declaration | Statement | Description |
|---|---|---|
| `sparse_plugin_guarantee` | `theorem` | Deterministic sparse plug-in guarantee: under RSC, `λ ≥ 2 ‖∇emp θ₀ − popGrad‖_∞`, FOC at the truth, and a subgradient inequality, the sparse plug-in ERM `θhat` satisfies `θhat − θ₀ ∈ RestrictedCone S₀` and `‖θhat − θ₀‖ ≤ 12 λ √s / σn`. Theorem `thm:est-osl-sparse-plugin-guarantee`. **Proved by Codex.** Signature carries an explicit `h_subgrad : ∀ θ, empRiskFn θ − empRiskFn θ₀ ≥ ⟪gradEmp θ₀, θ − θ₀⟫` because the basic-inequality step requires `gradEmp θ₀` to act as a subgradient at `θ₀` (the natural-language note implicitly uses this; we make it explicit). |
| `LinftyDevTailBound hp dev ρ δ` | `def`, `Prop` | Predicate: there is a measurable event `E ⊆ Ω` with `μ E ≥ 1 − δ` on which `linftyDev hp (dev ω) ≤ ρ`. The gradient-deviation analogue of `LocalEmpProcessModulus`. |
| `sparse_plugin_guarantee_highProb` | `theorem` (`sorry`) | **High-probability** sparse plug-in ERM guarantee: under `LinftyDevTailBound` on the empirical-gradient deviation and `λ ≥ 2 ρ`, with probability ≥ `1 − δ`, `θhat ω − θ₀ ∈ RestrictedCone S₀` and `‖θhat ω − θ₀‖ ≤ 12 λ √s / σn`. Phase-2 ↔ concentration contact point. **Sorry**, awaiting Codex fill. |

### 13''''.k `Estimation/CATE/OrthogonalLearning/DRLearner.lean` (Phase-3 milestone)

| Declaration | Statement | Description |
|---|---|---|
| `BoundedNuisanceDirs η₀` | `def`, `Set (NuisanceVec γ)` | Bounded-direction slice anchored at `η₀`: the set of `η` whose deviation `η − η₀` has uniformly bounded outcome-regression and propensity components (`∃ Cμ, ∀ b x, |(η − η₀).μ_fn b x| ≤ Cμ` and analogous for `e_fn`). Used as the nuisance slice `G_set` of `drOSLSystem` so that the score-zero obligation can be discharged constructively via `dr_scoreZero_of_bounded`. |
| `anchor_mem_boundedNuisanceDirs η₀` | `lemma` | `η₀ ∈ BoundedNuisanceDirs η₀`, witnessed by `Cμ = Ce = 0`. |
| `drOSLSystem S Θ Θ_set Θ_convex θ₀ θ₀_mem eval eval_meas eval_θ₀` | `noncomputable def`, `OSLSystem` | DR-Learner OSL system: data law from `S.toBackdoorEstimationSystem.P_Z`; loss `(z, θ, η) ↦ (phi_eta z η − eval θ z.1)²`; nuisance `G := NuisanceVec γ` with `G_set := BoundedNuisanceDirs η₀` (uniformly bounded perturbation directions); truth-witness `eval θ₀ = τ_val`. |
| `drNeymanOrthog_witness` | `theorem` | DR-Learner squared loss is Neyman-orthogonal at `(τ₀, η₀)`, consuming a concrete `M : HasMixedDirDeriv` and the analytic inputs `hBridge : MixedScoreDCTBridge` and `hScoreFlat` (the score-form orthogonality tendsto, quantified over `BoundedNuisanceDirs η₀` to match `drOSLSystem.G_set`). Proved as a thin wrapper around `neymanOrthog_iff_score_deriv_zero`. Proposition `prop:est-osl-dr-loss-orthogonal`. The concrete instantiation of `M`, with closed-form `Dθ_at`/`dℓ_θg` and the pointwise tendsto obligations discharged, lives in `Estimation/CATE/OrthogonalLearning/DRLearner/Analytic.lean`. |

### 13''''.k' `Estimation/CATE/OrthogonalLearning/DRLearner/Analytic.lean` (Phase-3 analytic discharge)

Refines `drNeymanOrthog_witness` by *constructing* the closed-form `HasMixedDirDeriv` for the DR-Learner squared loss and discharging the pointwise tendsto obligations. The DCT swap and integrated orthogonality remain as hypotheses — see footnote.

| Declaration | Statement | Description |
|---|---|---|
| `EvalDirDeriv Θ_set θ₀ eval` | `structure` | Directional-derivative bundle for the candidate-evaluation map `eval : Θ → γ → ℝ`: carries `dEval θ x` (the dir-deriv value at `θ₀` in direction `θ − θ₀`), pointwise tendsto, measurability, and a uniform `L∞` bound on `dEval θ` (needed for DCT). For the linear function-class case `eval θ x = θ x`, `dEval θ x = θ x − θ₀ x`. |
| `NuisanceDirDeriv g₀` | `structure` | Directional-derivative bundle for `phi_eta` in the *nuisance* direction at `g₀`: carries `dPhi : NuisanceVec γ → (γ × Bool × ℝ) → ℝ` with pointwise tendsto and measurability. Required because `phi_eta` is reciprocal in `η.e_fn`, hence non-linear in `η`, and the value-difference `phi_eta z η − phi_eta z g₀` is **not** the directional derivative. |
| `drMixedDirDeriv S hA hε_pos h_overlap_η₀ Θ Θ_set Θ_convex θ₀ θ₀_mem eval eval_meas eval_θ₀ D ND` | `noncomputable def`, `HasMixedDirDeriv (drOSLSystem ...)` | Closed-form mixed directional-derivative bundle for the DR-Learner loss: `(Dθ_at η).dℓ_θ θ z = −2·(phi_eta z η − eval θ₀ z.1)·D.dEval θ z.1` and `dℓ_θg θ η z = −2·ND.dPhi η z·D.dEval θ z.1`. Pointwise tendsto for both fields **proved by Codex** via the chain rule `a² − b² = (a−b)(a+b)` on `(phi_eta z η − eval θ z.1)²` for the target field, and via direct substitution + `ND.pointwise` for the mixed field. |
| `dr_scoreZero_of_bounded` | `lemma` | **Honest discharge of the integrated-orthogonality obligation.** For any candidate target `θ` and any nuisance perturbation `η : NuisanceVec γ` whose direction `η − η₀` has uniformly bounded `μ_fn`/`e_fn`, `∫ z, ND.dPhi η z · D.dEval θ z.1 ∂P_Z = 0`. **Proved by Codex** by (i) identifying `ND.dPhi η z = phi_eta_dir_deriv η₀ (η − η₀) z` via uniqueness of `nhdsWithin` limits applied to `ND.pointwise` and `phi_eta_dir_deriv_tendsto`, (ii) converting the `P_Z`-integral to a `P.μ`-integral via `MeasureTheory.integral_map`, (iii) applying the conditional-expectation tower + σ(X)-measurable pull-out of `D.dEval ∘ factualX`, and (iv) plugging in `cond_exp_phi_eta_dir_deriv_at_truth_zero` to get the inner conditional is `0` a.e. Replaces the tautological `dr_mixedScoreDCTBridge` passthrough that previously sat in this file. |
| `dr_scoreFlat ... D ND hBridge` | `theorem` | Score-flatness on `BoundedNuisanceDirs η₀`: for every `θ ∈ Θ_set` and every `η ∈ BoundedNuisanceDirs η₀`, the integrated score difference quotient tends to zero as `t → 0`. The single remaining analytic hypothesis is `hBridge` (the DCT swap); the integrated-orthogonality obligation is discharged inside the proof by `dr_scoreZero_of_bounded`, applied to the boundedness witnesses extracted from `η ∈ BoundedNuisanceDirs η₀`. The closing computation is `∫ M.dℓ_θg = −2 · ∫ ND.dPhi · D.dEval = −2 · 0 = 0`. |
| `drNeymanOrthog ... D ND hBridge` | `theorem` | `NeymanOrthogLoss` for the DR-Learner loss at `(τ₀, η₀)` over `BoundedNuisanceDirs η₀`, parameterised by `D : EvalDirDeriv` and `ND : NuisanceDirDeriv`. The score-zero obligation is fully internalised: the only remaining analytic hypothesis is `hBridge` (the DCT swap). One-line proof: feed `hBridge` and `dr_scoreFlat` into `neymanOrthog_iff_score_deriv_zero`. Restricting `drOSLSystem.G_set` to `BoundedNuisanceDirs η₀` is the standard semiparametric move that lets `dr_scoreZero_of_bounded` discharge the integrated-orthogonality obligation directly without integral-defaults-to-zero conventions for unbounded directions. |

### 13''''.l `Estimation/OrthogonalLearning/LocalEmpProcess/Rademacher.lean` (Phase-4a bridge)

Bridge between the vendored Rademacher / McDiarmid / Symmetrization
chain in `Causalean/Stat/Concentration/` and the OSL `LocalEmpProcessModulus`
predicate.  Pure consumption of FoML headlines plus
`Stat/FoldBEmpiricalProcess.foldB_pi_law` for the
`μⁿ`-on-`Fin m → Ω` ↔ `μ`-on-`Ω` transport, and a generic-`DenseRange`
analog of `separableSpaceSup_eq_real` for the sup-lifting step.

**Why not `[Countable S.Θ_set]`?**  Combined with `S.Θ_convex`,
countability forces `S.Θ_set.Subsingleton` (a non-trivial convex subset
of a real vector space contains a line segment, which is uncountable).
The bridge therefore takes an **explicit countable enumeration**
`idx : ℕ → S.Θ_set` with `DenseRange idx` plus a continuity hypothesis
`LossContinuousOnΘset`, and uses the dense-range analog of FoML's
`separableSpaceSup_eq_real` to lift sup-over-ℕ to sup-over-`↥S.Θ_set`.

| Declaration | Statement | Description |
|---|---|---|
| `foldB_pi_law` | `lemma` | Public alias of the (private) `oneShot_iid` from `Stat/SampleSplit/FoldBEmpiricalProcess.lean`: `μ.map (ω ↦ (i ↦ S.Z i ω)) = Measure.pi (fun _ ↦ P)` over `i ∈ split.foldB n`. The bridge consumed by the McDiarmid/symmetrization chain to transport `μⁿ`-conclusions to fold B on `μ`. |
| `RademacherBound S S_iid split g idx R` | `def`, `Prop` | Hypothesis: for every `n`, `0 ≤ R n` and `rademacherComplexity (split.foldB n).card (k ↦ ℓ z (idx k).val g − ℓ z θ₀ g) μ (S_iid.Z 0) ≤ R n`. Indexed over the *countable* index `ℕ` via the user-supplied `idx : ℕ → S.Θ_set`. |
| `UniformlyBoundedLoss S g b` | `def`, `Prop` | `∀ z, ∀ θ ∈ Θ_set, |ℓ z θ g| ≤ b`. |
| `LossContinuousOnΘset S g` | `def`, `Prop` | `∀ z, Continuous (fun (θ : ↥S.Θ_set) => S.ℓ z θ.val g)`. Needed by the dense-range lifting. |
| `separableSup_via_denseRange` | `private lemma` | Generic `DenseRange` analog of `FoML.separableSpaceSup_eq_real`: for continuous `f : X → ℝ` on nonempty topological `X` with `DenseRange idx`, `⨆ x : X, f x = ⨆ k : ℕ, f (idx k)`. Proved. |
| `empiricalRademacherComplexity_eq_denseRange`, `rademacherComplexity_eq_denseRange` | `private lemma` | Empirical / population Rademacher complexity is preserved under re-indexing from `X` to `ℕ` via dense range, given continuity in `θ`. Proved. |
| `rademacherComplexity_map_id` | `private lemma` | `rademacherComplexity n f (μ.map sample) id = rademacherComplexity n f μ sample`. Pushforward simplification used in the transport step. Proved. |
| `populationRisk_abs_le_of_uniform`, `populationRisk_sub_le_two_mul_bound` | `private lemma` | `|S.L θ g| ≤ b` and `S.L θ g − S.L θ₀ g ≤ 2 b` for `θ ∈ Θ_set`, derived from `UniformlyBoundedLoss` + `[IsProbabilityMeasure P_Z]`. The boundedness fallback used in the empty-fold (`m = 0`) case. Proved. |
| `localEmpProcessModulus_of_bounded_rademacher` | `theorem` | **Phase-4a headline (proved).** Under `UniformlyBoundedLoss S g b`, `LossContinuousOnΘset S g`, an explicit countable dense `idx : ℕ → S.Θ_set`, and `RademacherBound` against `idx`, conclude `LocalEmpProcessModulus` with `ρ n := √(if (split.foldB n).card = 0 then 2b else 2 R n + 2b · √(2 log(1/δ) / (split.foldB n).card))`. The nonempty-fold branch is the textbook `√(2 R_n + ε)` rate with `ε = 2b · √(2 log(1/δ)/|B(n)|)`; the `m = 0` branch uses `√(2b)` to dominate the boundedness fallback `|L θ g − L θ₀ g| ≤ 2b`. Proof: case split on `m = 0` vs `m ≥ 1`, then on `b = 0` vs `b > 0`; for `m ≥ 1, b > 0` chains `expectation_le_rademacher` + `mcdiarmid_inequality_pos'` + `uniformDeviation_bounded_difference` on the ℕ-indexed centred class, transports via `foldB_pi_law` + `Finset.orderIsoOfFin`, and lifts to sup-over-`↥S.Θ_set` via `separableSup_via_denseRange`. |
| `localEmpProcessModulus_singleton` | `theorem` | Smoke test: `Θ_set = {θ₀}` ⇒ modulus holds with `ρ ≡ 0`. Proved. |

### 13''''.m `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/DRLearner.lean` (Phase-4a end-to-end)

Apply the Phase-4a bridge to the DR-Learner OSL system.  Real success
criterion for Phase 4a: yields `LocalEmpProcessModulus` for `(τ̂_n, ĥ_n)`
under bounded eval / outcomes / nuisance L∞ / overlap.

| Declaration | Statement | Description |
|---|---|---|
| `DREvalBounded Θ_set eval M_Θ` | `def`, `Prop` | Uniform `|eval θ x| ≤ M_Θ` over `Θ_set × γ`. |
| `DROutcomeBounded S M_Y` | `def`, `Prop` | Bounded outcomes type-level: `0 ≤ M_Y ∧ ∀ z : γ × Bool × ℝ, |z.2.2| ≤ M_Y`. The `∀ z` form (rather than `∀ ω`) is needed because `UniformlyBoundedLoss` quantifies over all `z`. |
| `DRNuisanceOverlap S Θ_set h ε` | `def`, `Prop` | Strict-overlap predicate: `0 < ε ∧ ε ≤ 1/2 ∧ ∀ x, ε ≤ h.e_fn x ∧ h.e_fn x ≤ 1 − ε`. Carries the propensity-bracketing slice condition (no longer a `True`-stub). |
| `DRNuisanceMuBounded h M_μ` | `def`, `Prop` | Uniform L∞ bound on the outcome-regression nuisance: `0 ≤ M_μ ∧ ∀ b x, |h.μ_fn b x| ≤ M_μ`. Required for boundedness of the AIPW pseudo-outcome. |
| `dr_loss_uniformly_bounded` | `theorem` | Bounded eval + outcomes + nuisance L∞ + overlap ⇒ DR squared loss is uniformly bounded over `Θ_set` at `h` by the **explicit** value `b := (M_Θ + 2·M_μ + 2·(M_Y + M_μ)/ε)²`. Proved by Codex via the AIPW pseudo-outcome triangle bound `\|phi_eta z h\| ≤ 2 M_μ + 2 (M_Y + M_μ) / ε`. (Returns `UniformlyBoundedLoss ... b` directly — no existential — so callers can write FS-style hypotheses against the explicit value.) |
| `localEmpProcessModulus_drLearner` | `theorem` | End-to-end: instantiates `localEmpProcessModulus_of_bounded_rademacher` for the DR-Learner `OSLSystem`. Takes `idx : ℕ → ↥(drOSLSystem ...).Θ_set`, `idx_dense : DenseRange idx`, and `LossContinuousOnΘset` for the DR loss in addition to the `(M_Θ, M_Y, M_μ, ε)` quartet. Concludes the tightened piecewise rate, with textbook `√(2 R_n + ε)` on nonempty folds and `√(2b)` when the fold is empty. Proved as a thin wrapper. |

### 13''''.n `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/DRLearnerEndToEnd.lean` (Phase-3 ↔ Phase-4a chain)

| Declaration | Statement | Description |
|---|---|---|
| `oracle_inequality_drLearner_highProb` | `theorem` | **Phase-3 ↔ Phase-4a end-to-end chain (proved).** Composes `localEmpProcessModulus_drLearner` (Phase-4a DR modulus discharge) with `oracle_inequality_plugin_ERM_highProb` (Phase-1 high-probability oracle inequality) to derive a closed-form high-probability rate `‖τhat n ω − τ₀‖² ≤ (4(1+σ)/σ²)·(√(if (split.foldB n).card = 0 then 2b else 2 R n + 2b·√(2 log(1/δ)/(split.foldB n).card)))² + (4/σ)·Bias_n + (4/σ)·r_opt n` with `μ`-probability ≥ `1 − δ` for the DR-Learner OSL system. Threads `idx`, `idx_dense`, `LossContinuousOnΘset`, and `DRNuisanceMuBounded` through. Phase 4a verification step 3 — **complete and sorry-free**. |

### 13''''.o `Stat/Concentration/{StarHull,Contraction,CriticalRadius,LocalRademacher,UniformDeviationLocalized}.lean` (Phase-4b localized chain)

Path-B (McDiarmid + symmetrization + contraction) localized empirical-process toolbox. Sibling to Phase-4a `Stat/Concentration/{Rademacher,...}` shims, but contains *new* formalization (not vendored). Headline: `localized_uniform_deviation` with shape `r·δ_n + δ_n²` matching Foster–Syrgkanis 2019 Lemma 29 up to log factors. Plan reference: `.claude/plans/i-just-added-a-rosy-hennessy.md`, Phase 4b.

#### 13''''.o.1 `Stat/Concentration/StarHull.lean`

| Declaration | Statement | Description |
|---|---|---|
| `starHull F` | `def`, `Set (𝒳 → ℝ)` | `{α • F i : i ∈ ι, α ∈ [0,1]}`. Bartlett–Bousquet–Mendelson 2005 star-hull around 0. |
| `starHullIndex F` | `noncomputable def` | Subtype re-index `{f // f ∈ starHull F}` for downstream Rademacher arguments. |
| `mem_starHull_self`, `zero_mem_starHull`, `starHull_mono`, `starHull_smul_mem` | `lemma`s (`sorry`) | Algebra: `F i ∈ starHull F`, `0 ∈ starHull F`, monotonicity, scalar closure on `[0,1]`. |

#### 13''''.o.2 `Stat/Concentration/Contraction.lean`

| Declaration | Statement | Description |
|---|---|---|
| `LipschitzAt0 φ L` | `def`, `Prop` | `φ 0 = 0 ∧ ∀ x y, |φ x − φ y| ≤ L |x − y|`. The Lipschitz-with-`φ(0)=0` regime needed by Ledoux–Talagrand. |
| `rademacher_contraction` | `theorem` | **Ledoux–Talagrand 1991 Thm 4.12 (signed form).** `R̂_n^{noabs}(φ ∘ F, S) ≤ L · R̂_n^{noabs}(F, S)` for L-Lipschitz `φ` with `φ 0 = 0`. **Proved by Codex** via the hybrid-induction argument: a sign-pair Lipschitz inequality (`sup_pair_lipschitz_scaled`) bounds the two-sign sum at one coordinate, then a hybrid average `hybridAverage` interpolating between `φ ∘ F` and `L · F` is shown monotone in the number of swapped coordinates (`hybridAverage_mono_step`); telescoping recovers the headline. |
| `rademacher_contraction_abs` | `theorem` | Absolute-value form: `R̂_n(φ ∘ F, S) ≤ 2·L·R̂_n(F, S)`. **Proved by Codex** by reducing to the signed form applied to `φ` and `−φ` (via `lipschitzAt0_neg`), bounding `|x| ≤ x⁺ + x⁻` over the Rademacher sup. |
| `empiricalRademacherComplexity_contraction_abs_of_bddAbove` | `theorem` | **Infinite-index contraction.** Same `R̂_n(φ ∘ F, S) ≤ 2L·R̂_n(F, S)` as `rademacher_contraction_abs` but for an ARBITRARY `[Nonempty ι]` (not just `Fintype`), assuming a uniform bound `|F i x| ≤ M`. Reduces the infinite-index case to the Fintype lemma by choosing, for each of the finitely many sign vectors, an `ε`-approximate maximizer; their finite collection is a finite subindex. This is what lets the squared-loss kernel/lasso ERM (whose weight ball is infinite) use the contraction. **Proved.** |
| `empiricalRademacherComplexity_sub_le` | `theorem` | **Sub-additivity over class differences.** `R̂_n(F − G, S) ≤ R̂_n(F, S) + R̂_n(G, S)` (uniform bounds on `F`, `G`). Splits the centered squared loss `⟪w,x⟩² − 2y⟪w,x⟩` into its quadratic and cross parts. **Proved.** |
| `empiricalRademacherComplexity_smul_class`, `empiricalRademacherComplexity_without_abs_smul_class` | `theorem` | Smoke test: `R̂_n(c•F) = |c|·R̂_n(F)` (resp. signed analogue). **Proved.** |
| `rademacher_swap_step`, `rademacher_telescope` | `private lemma` | Sign-swap proof scaffolds; **proved** as wrappers around the headline `rademacher_contraction` after the hybrid-induction proof landed. Kept private to flag the proof outline. |
| `SignAtom`, `flipSign`, `flipSignEquiv` | `private def` / `Equiv` | `Signs n` infrastructure: `SignAtom = {-1, 1}` finset element, single-coordinate sign flip and the sign-flip equivalence used to re-index `∑_σ` after a coordinate swap. **Proved.** |
| `sup_pair_lipschitz_scaled` | `private lemma` | Sign-pair Lipschitz inequality on `iSup`: `(⨆ a + c·s·φ b) + (⨆ a − c·s·φ b) ≤ (⨆ a + c·s·L·b) + (⨆ a − c·s·L·b)` for `s ∈ {−1, +1}`, `c ≥ 0`. The single-coordinate engine of the contraction proof. **Proved.** |
| `hybridInner`, `hybridAverage`, `hybridBase` | `private noncomputable def` | The hybrid signed-Rademacher quantity: at parameter `m`, replaces `φ(F i (S k))` by `L·F i (S k)` for `k < m` and keeps `φ` for `k ≥ m`. `hybridInner` is the per-`σ`-per-`i` summand; `hybridAverage` is `(1/2ⁿ) Σ_σ ⨆_i hybridInner`; `hybridBase` factors out the `k`-th coordinate for the swap step. |
| `hybridAverage_mono_step` | `private lemma` | Monotonicity of the hybrid average in `m`: `hybridAverage m ≤ hybridAverage (m+1)` for `m < n`, by applying `sup_pair_lipschitz_scaled` to the two-sign sum at coordinate `m` after re-indexing via `flipSignEquiv`. **Proved.** |
| `rademacher_contraction_core` | `private lemma` | The actual contraction inequality on the without-abs form, before the `L`-scaling re-arrangement. Iterates `hybridAverage_mono_step` from `m = 0` to `m = n`, then closes by the without-abs scaling lemma. **Proved.** |
| `lipschitzAt0_neg` | `private lemma` | `LipschitzAt0 φ L → LipschitzAt0 (fun x => −φ x) L`. **Proved.** |
| `iSup_abs_le_iSup_add_iSup_neg_of_exists_zero` | `private lemma` | `⨆ |x i| ≤ (⨆ x i) + (⨆ (−x i))` when `0` lies in the family (i.e. for any class containing the zero function). **Proved.** |
| `withZero F` | `private def` | The class `F` augmented with the zero function, used to ensure both sup's are non-negative for the abs-form bound. |

#### 13''''.o.3 `Stat/Concentration/CriticalRadius.lean`

| Declaration | Statement | Description |
|---|---|---|
| `starHullBall F norm r` | `def`, `Set (𝒳 → ℝ)` | `starHull F ∩ {f : norm f ≤ r}`. |
| `starHullZeroOut F norm r` | `noncomputable def`, `starHullParam ι → 𝒳 → ℝ` | Radius-localized star-hull family: active rays evaluate as `α·F_i`, inactive rays are zero. |
| `starHullZeroOutScaleCoeff`, `starHullZeroOut_inner_term_eq`, `starHullZeroOut_inner_sup_eq` | scale coefficient and Rademacher inner-term identities | Public star-hull scale-contraction helpers used by finite-VC measurability and Massart arguments. |
| `RademacherUpperBound F norm μ X n ψ` | `def`, `Prop` | Uniform Rademacher-complexity envelope on `starHullBall` (deterministic / population — Risk #7 option (a)). |
| `criticalRadius ψ` | `noncomputable def`, `ℝ` | `sInf {δ > 0 : ψ δ ≤ δ²}` — Bartlett–Bousquet–Mendelson 2005 critical radius. |
| `SubRoot ψ` | `def`, `Prop` | Sub-root: `ψ ≥ 0`, `ψ r / r` non-increasing, `ψ` non-decreasing. |
| `criticalRadius_nonneg`, `criticalRadius_le` | `lemma`s | Basic structural lemmas. **Proved by Codex.** (Earlier scaffold included `criticalRadius_pos_of_subRoot`; removed because it is false for `ψ ≡ 0` under the displayed sub-root hypotheses alone — needs an additional non-degeneracy / continuity assumption that no downstream consumer in Phase 4b needs.) |
| `subRoot_homogeneity` | `lemma` | **Sub-root inequality.** For sub-root `ψ` and `δ* := criticalRadius ψ`, if `r ≥ δ*` then `ψ r ≤ r·δ*`. The single line everything downstream cites. **Proved by Codex.** |

#### 13''''.o.4 `Stat/Concentration/LocalRademacher.lean`

| Declaration | Statement | Description |
|---|---|---|
| `localRademacherComplexity F norm μ X n r` | `noncomputable def`, `ℝ` | Localized Rademacher complexity, **zero-out form**: the FoML `rademacherComplexity` of the family `fun g x => if norm g.val ≤ r then g.val x else 0` over `starHullIndex F`. The earlier `sInf`-over-upper-bounds scaffold was anti-monotone in `r`; the zero-out form fixes the direction (more `r` ⇒ more non-zero entries ⇒ larger sup). |
| `localRademacherComplexity_nonneg` | `lemma` | Non-negativity via `integral_nonneg` + `Real.iSup_nonneg` + `abs_nonneg` (each summand of `empiricalRademacherComplexity` is an absolute value). **Proved.** |
| `localRademacherComplexity_mono_r` | `lemma` (`sorry`) | Monotonicity in radius. The pointwise comparison is straightforward; lifting through the integral needs an integrability witness on the upper function. **Phase-4b TODO.** |
| `localRademacherComplexity_le_starHull` | `lemma` (`sorry`) | **Star-hull lifting** (BBM 2005 step 2). Same integrability TODO as `_mono_r`. |
| `localRademacher_le_critical_radius` | `theorem` (`sorry`) | **Sub-root inequality, headline form.** For `r ≥ criticalRadius ψ`, `localRademacherComplexity F norm μ X n r ≤ r · criticalRadius ψ`. Combines `_le_starHull` with `subRoot_homogeneity`. **Phase-4b TODO.** |

#### 13''''.o.5 `Stat/Concentration/UniformDeviationLocalized.lean`

| Declaration | Statement | Description |
|---|---|---|
| `LocalizedRegime Ω ι 𝒳 F norm μ X` | `structure` | Input bundle (Phase 4b redesign): uniform bound `b` on `|F i (X ω)|`, and a sub-root Rademacher envelope `ψ` on `starHullZeroOut F norm r` (uniform in `n`). The Lipschitz `φ` slot has been **dropped** — the envelope `ψ` lives directly on the centred loss class. Callers wanting a parameter-class envelope + Lipschitz `φ` link can layer a contraction wrapper (deferred — see `Contraction.lean`). |
| `localized_uniform_deviation` | `theorem` | **Path-B fixed-`n` headline.** Under `LocalizedRegime`, with probability `≥ 1 − δ` over `μⁿ`, uniformly over `i` of `norm`-radius `≤ r`, the centred deviation is bounded by `4·r·δ_n + b·√(2 log(1/δ)/n)` where `δ_n := criticalRadius ψ`. The `L_φ` factor is **gone** (absorbed into `ψ`). Quarantines the McDiarmid step inside the proof so a future Talagrand upgrade is a drop-in (Risk #9). **Proved.** |
| `localized_uniform_deviation_sharp` | `theorem` | Reusable Foster–Syrgkanis Lemma 29 peeling layer over `localized_uniform_deviation`: for any public upper radius `ρ ≥ criticalRadius (ψ n)`, diameter cap `Rmax`, radius-uniform Rademacher prerequisites, and peeling slack `b·√(2 log(2(K+1)/δ)/n) ≤ ρ²`, produces a single `μⁿ`-event on which each class element satisfying `0 ≤ norm(F_i) ≤ Rmax` obeys `|(P_n − P)F_i| ≤ 8ρ·norm(F_i) + 5ρ²`. This is the generic abstraction intended for OSL, NPIV, and future localized EP users. **Proved.** |
| `localized_uniform_deviation_singleton` | `theorem` | Singleton smoke-test stub. **Proved.** |

#### 13''''.o.6 `Stat/Concentration/LocalizedEnvelopeExpectation.lean`

In-expectation (and self-localizing offset) forms of the localized deviation bound, for downstream rate arguments that need an expectation rather than a high-probability envelope. Both are general empirical-process facts over a `LocalizedRegime`; the margin-coupled / VC specializations (turning the `norm`-radius into a problem-specific regret radius) are supplied by the caller. Built to discharge the localized finite-VC envelope gates of `stat_policy_regret_margin_overlap`.

| Declaration | Statement | Description |
|---|---|---|
| `localized_uniform_deviation_expectation` | `theorem` | **Fixed-radius in-expectation envelope.** Steps 1–2 of the Path-B argument (symmetrization via FoML `uniform_deviation_expectation_le_two_smul_rademacher_complexity` + critical radius via `localRademacher_le_critical_radius`), stopping before the McDiarmid tail. For the radius-`r` zero-out localization, `𝔼[uniformDeviation over {norm(F i) ≤ r}] ≤ 2·r·criticalRadius(ψ n)`. The in-expectation analogue of `localized_uniform_deviation`. **Proved (Codex), axiom-clean.** |
| `localized_offset_expectation` | `theorem` | **Self-localizing offset positive-part envelope.** Given a margin coupling `norm(F i) ≤ A·(Δ i)^κ` (the caller's regret radius `Δ`, `0<κ<1`), `𝔼[⨆ i, {2·|(Pₙ−P)F i| − Δ i/4}_+] ≤ offsetPeelingConstantC (1/8) κ · (16ρA)^{1/(1−κ)} + 10ρ² + 4·R.b·δ`. Assembled from the high-probability `localized_uniform_deviation_sharp`, the deterministic Young/AM-GM optimisation `OffsetPeeling.offset_peeling_coeff`, and a bounded bad-event tail split. Caller picks `ρ` = VC critical-radius rate and `δ` polynomially small for a clean `(B²/n)^{A_α}(log n)^p` rate. **Proved (Codex), axiom-clean.** |

### 13''''.p `Estimation/OrthogonalLearning/LocalEmpProcess/Localized.lean` (Phase-4b bridge)

Sibling to `OrthogonalLearning/LocalEmpProcess/Rademacher.lean` (Phase 4a). Discharges `LocalEmpProcessModulus` from `localized_uniform_deviation` with the localized rate `ρ_n ≈ criticalRadius ψ`.

| Declaration | Statement | Description |
|---|---|---|
| `OSLLocalizedRegime S S_iid split g idx norm ψ b` | `def`, `Prop` | Phase 4b redesigned predicate: `0 ≤ b`, centred-loss boundedness `|ℓ z θ g − ℓ z θ₀ g| ≤ b`, `SubRoot ψ`, and `RademacherUpperBound` for the centred ℕ-indexed loss class (re-indexed via dense `idx : ℕ → S.Θ_set`) on a fold-B-sized population sample. The Lipschitz-link slot `L_φ` is **dropped** — the envelope `ψ` lives directly on the centred loss class. |
| `empRiskFoldB_centered_abs_le` | `private lemma` | Pointwise centred-loss bound `\|ℓ z θ g − ℓ z θ₀ g\| ≤ b` averages to `\|empRiskFoldB θ − empRiskFoldB θ₀\| ≤ b`. **Proved.** |
| `localEmpProcessModulus_of_localized_bounded` | `theorem` | **Uniform-bound fallback bridge (legitimate low-hypothesis option).** Under `OSLLocalizedRegime` + a population centred-risk bound `hpop_center : ∀ θ ∈ Θ_set, \|S.L θ g − S.L θ₀ g\| ≤ b`, concludes `LocalEmpProcessModulus` with the *constant* envelope `ρ n := √(2b)`. **Proved.** Does NOT use `ψ`, critical-radius, or any Lipschitz / Rademacher-integrability machinery — purely bounded-loss. Useful when those inputs are unavailable; rate does not vanish in `n`. |
| `localEmpProcessModulus_of_localized_sharp` | `theorem` | **Phase-4b sharp headline — Foster–Syrgkanis Lemma 29 form (proved).** Under `OSLLocalizedRegime` plus an explicit Lipschitz constant `(L : ℝ) (hL_nonneg) (hF_lip : norm (z↦ℓ z θ g − ℓ z θ₀ g) ≤ L · ‖θ−θ₀‖)`, diameter `Rmax`, the n-dependent critical-radius hypotheses, Rademacher prerequisites, and the **peeling-aware** FS critical-radius lower bound `hδ_dom : ∀ n K, 0 < (split.foldB n).card → Rmax ≤ δ_n · 2^K → b · √(2 log(2(K+1)/δ)/m) ≤ δ_n²`, concludes `LocalEmpProcessModulus` with the FS Lemma 29 rate `ρ n := if (split.foldB n).card = 0 then √(2b) else (8L+3) · criticalRadius (ψ (split.foldB n).card)`. **Proved (Codex)** via dyadic peeling + fold-B transport (mirroring `OrthogonalLearning/LocalEmpProcess/Rademacher.lean`) + FS-absorption. Axiom-clean. |
| `localEmpProcessModulus_of_localized_singleton` | `theorem` | Singleton smoke-test (Θ_set = {θ₀} ⇒ ρ ≡ 0). **Proved.** |

### 13''''.q `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/LocalizedDRLearner.lean` (Phase-4b end-to-end)

Apply `localEmpProcessModulus_of_localized` to the DR-Learner. Verification step 6 of Phase 4b.

| Declaration | Statement | Description |
|---|---|---|
| `DRCriticalRadius S Θ_set eval idx h norm ψ` | `def`, `Prop` | Phase 4b redesigned predicate: `SubRoot ψ ∧ ∀ n, RademacherUpperBound (centred quadratic DR-loss class indexed by idx) norm S.P_Z id n ψ`. The DR centred class is `(φ_h(z) − eval (idx k) z.1)² − (φ_h(z) − S.τ_val z.1)²`. (Deterministic / population — Risk #10.) |
| `localEmpProcessModulus_localized_drLearner` | `theorem` | **Phase-4b success criterion (sharp Foster–Syrgkanis Lemma 29 form, proved).** End-to-end: instantiates `localEmpProcessModulus_of_localized_sharp` for the DR-Learner OSL system. Takes `(M_Θ, M_Y, M_μ, ε)` boundedness/overlap, `[Countable Θ_set]`, dense `idx`, `DRCriticalRadius`, an explicit Lipschitz constant `(L, hL_nonneg, hF_lip)` for the centred DR-loss class, the diameter `Rmax`, the n-dependent critical-radius facts, the Rademacher prerequisites, and the **peeling-aware** FS lower-bound `hδ_dom : ∀ n K, 0 < |B(n)| → Rmax ≤ δ_n · 2^K → 2·b_loss·√(2 log(2(K+1)/δ)/m) ≤ δ_n²` (stated against `b_loss = (M_Θ + 2·M_μ + 2·(M_Y+M_μ)/ε)²`). Concludes `LocalEmpProcessModulus` with the FS Lemma 29 rate `ρ n := if (split.foldB n).card = 0 then √(2b) else (8L+3) · criticalRadius (ψ (split.foldB n).card)`. When chained with `OracleInequality`, gives the Foster–Syrgkanis `O_p(δ_n² + nuisance²)` rate. |
| `localEmpProcessModulus_localized_drLearner_bounded` | `theorem` | **Phase-4b end-to-end (uniform-bound fallback variant).** Working chain that calls `localEmpProcessModulus_of_localized_bounded` with `b := 2·dr_loss_bound`. Concludes `LocalEmpProcessModulus` with the *constant* rate `ρ n := √(2 · 2b)` — does NOT use the critical radius. **Proved.** Useful as a low-hypothesis option when the sharp hypotheses (Lipschitz, Rademacher integrability, FS lower bound) are not available. |

### 13''''.r `Estimation/CATE/OrthogonalLearning/LocalEmpProcess/LocalizedDRLearnerEndToEnd.lean` (Phase-4b localized oracle chain)

Composes the sharp localized DR-Learner modulus with the generic OSL oracle inequality.

| Declaration | Statement | Description |
|---|---|---|
| `oracle_inequality_localized_drLearner_highProb` | `theorem` | Chains `localEmpProcessModulus_localized_drLearner` with `oracle_inequality_plugin_ERM_highProb` to obtain the DR-Learner high-probability oracle inequality with localized rate `ρ n := if (split.foldB n).card = 0 then √(2b) else (8L+3) · criticalRadius (ψ (split.foldB n).card)`. |

## 13gm. `Estimation/GaussMarkov/` — Finite Gauss-Markov / BLUE layer

Reusable finite Gauss-Markov theory: the variance of a linear estimator, spherical/general covariance, the OLS weight via the normal-equations pseudoinverse, and the minimum-variance (BLUE) ordering theorems. Built on Mathlib `Matrix` (`mulVec`/`vecMul`/`dotProduct`, `nonsing_inv`, `PosSemidef`) and `ProbabilityTheory.covariance`/`variance`. Files: `QuadForm.lean` (variance object), `LeastNorm.lean` (algebraic ordering), `OLS.lean` (OLS weight), `Variance.lean` (probability bridge), `BLUE.lean` (variance-form BLUE). Consumed by the imputation event-study efficiency theorem (§9i0a-ImputationEventStudyEfficiency). No `sorry`/`admit`/`axiom`.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `GaussMarkov.quadVar` | `Matrix Obs Obs ℝ → (Obs → ℝ) → ℝ` | Variance of a linear estimator with weight `w` under covariance `Σ`: the quadratic form `w ⬝ᵥ Σ *ᵥ w` (`= wᵀΣw`) | **Defined** |
| `GaussMarkov.SphericalErrors` | `Matrix Obs Obs ℝ → ℝ → Prop` | Spherical (homoskedastic, uncorrelated) covariance: `Σ = σ² • I` | **Defined** |
| `GaussMarkov.quadVar_spherical` | `SphericalErrors S σ → quadVar S w = σ² * (w ⬝ᵥ w)` | Under spherical errors the variance form reduces to `σ²‖w‖²` | **Proved** |
| `GaussMarkov.quadVar_nonneg` | `S.PosSemidef → 0 ≤ quadVar S w` | Variance form of a PSD covariance is nonnegative | **Proved** |
| `GaussMarkov.colSpan_dotProduct_leftNull` | `z ᵥ* X = 0 → (X *ᵥ g) ⬝ᵥ z = 0` | Column span ⟂ left null space (the orthogonality at the heart of Gauss-Markov) | **Proved** |
| `GaussMarkov.gauss_markov_spherical` | `SphericalErrors S σ → wStar = X *ᵥ g → wStar ᵥ* X = c → w ᵥ* X = c → quadVar S wStar ≤ quadVar S w` | **Finite Gauss-Markov, spherical:** among weights with the same unbiasedness image, an OLS weight (in the column span of `X`) minimizes variance (Pythagoras) | **Proved** |
| `GaussMarkov.gauss_markov_gls` | `S.PosSemidef → S *ᵥ wStar = X *ᵥ g → wStar ᵥ* X = c → w ᵥ* X = c → quadVar S wStar ≤ quadVar S w` | **Finite Gauss-Markov, general (GLS):** for any PSD covariance, a GLS weight (`Σ wStar` in column span) minimizes variance | **Proved** |
| `GaussMarkov.olsWeight` | `Matrix Obs Param ℝ → (Param → ℝ) → (Obs → ℝ)` | OLS weight via the normal-equations pseudoinverse: `X (XᵀX)⁻¹ c` | **Defined** |
| `GaussMarkov.olsWeight_unbiased` | `IsUnit (Xᵀ * X).det → olsWeight X c ᵥ* X = c` | The OLS weight is unbiased when `XᵀX` is invertible (full column rank) | **Proved** |
| `GaussMarkov.olsWeight_mem_colSpan` | `olsWeight X c = X *ᵥ ((Xᵀ * X)⁻¹ *ᵥ c)` | The OLS weight lies in the column span of `X` | **Proved** |
| `GaussMarkov.olsWeight_blue_spherical` | `IsUnit (Xᵀ*X).det → SphericalErrors S σ → w ᵥ* X = c → quadVar S (olsWeight X c) ≤ quadVar S w` | OLS is BLUE (variance-form) under spherical errors | **Proved** |
| `GaussMarkov.covMatrix` | `(Obs → Ω → ℝ) → Measure Ω → Matrix Obs Obs ℝ` | Covariance matrix of a finite random family, `Σ i j = cov(Yᵢ, Yⱼ)` | **Defined** |
| `GaussMarkov.variance_linearCombination` | `(∀ i, MemLp (Y i) 2 μ) → Var[∑ i, w i * Y i; μ] = quadVar (covMatrix Y μ) w` | **Probability bridge:** the variance of a random linear estimator equals the covariance quadratic form | **Proved** |
| `GaussMarkov.SphericalFamily` | `(Obs → Ω → ℝ) → Measure Ω → ℝ → Prop` | Random family with spherical errors: each cell variance `σ²`, distinct cells uncorrelated | **Defined** |
| `GaussMarkov.sphericalFamily_covMatrix` | `SphericalFamily Y μ σ → SphericalErrors (covMatrix Y μ) σ` | A spherical family has scalar-identity covariance matrix | **Proved** |
| `GaussMarkov.covMatrix_posSemidef` | `(∀ i, MemLp (Y i) 2 μ) → (covMatrix Y μ).PosSemidef` | A covariance matrix is positive semidefinite | **Proved** |
| `GaussMarkov.variance_blue_spherical` | `(∀ i, MemLp (Y i) 2 μ) → SphericalFamily Y μ σ → wStar = X *ᵥ g → wStar ᵥ* X = c → w ᵥ* X = c → Var[∑ wStar·Y] ≤ Var[∑ w·Y]` | **BLUE (variance form), spherical:** OLS minimizes actual `ProbabilityTheory.variance` in the linear-unbiased class | **Proved** |
| `GaussMarkov.variance_blue_gls` | `(∀ i, MemLp (Y i) 2 μ) → (covMatrix Y μ) *ᵥ wStar = X *ᵥ g → wStar ᵥ* X = c → w ᵥ* X = c → Var[∑ wStar·Y] ≤ Var[∑ w·Y]` | **BLUE (variance form), general:** GLS minimizes variance for the family's own covariance | **Proved** |

---

## 13. `Estimation/ATE/` — Back-door ATE estimation and inference

Specific-system layer instantiating `POBackdoorSystem`. Mirrors
`doc/basic_concepts/po/estimation/{aipw_if,plug_in_ate,dml_ate}.tex`. The
concrete `dml_ATE_isAsymLinear` in `Estimation/ATE/DML.lean` remains the
production AIPW asymptotic-linearity headline; the abstract framework in
`Estimation/OrthogonalMoments/*` provides the abstractions, and `aipw_dml_isAsymLinear`
is **proved** (the θ₀-shift gap was closed by the 2026-05-06 per-η̂ bilinear-remainder
refactor; see §12g.6).

### 13a. `Estimation/ATE/InfluenceFunction.lean` (umbrella)

Single-import aggregator that re-exports the five sub-modules below.
`PlugIn.lean` and `DML.lean` import this file. Refactored from the original
~900-line monolithic file.

#### 13a.1 `Estimation/ATE/Setup.lean`

`BackdoorEstimationSystem` structure, data law, value-space estimand.

| Declaration | Statement | Description |
|---|---|---|
| `BackdoorEstimationSystem P γ` | structure | Extends `POBackdoorSystem` with value-space `μ_val : Bool × γ → ℝ`, `e_val : γ → (0,1)`, and the **observable** compat fields `μ_reg_compat : μ_val d ∘ factualX =ᵐ adjustedCE d` (the ML/regression target — NO assumptions; see `regression_adjustment`), `e_compat : propScore true =ᵐ e_val ∘ factualX`. **The counterfactual reading `μ[Y(d)\|σX] =ᵐ μ_val d ∘ factualX` is no longer a field** — it is the derived lemma `μ_compat` below. |
| `BackdoorEstimationSystem.μ_compat` | `(hA : Assumptions) (d) → μ[Y(d)\|σX] =ᵐ μ_val d ∘ factualX` | **Derived counterfactual compatibility** (was a structure field): `cate_backdoor hA d` composed with `μ_reg_compat`. The counterfactual binding now correctly requires identification, not structural data. **Proved.** |
| `POBackdoorSystem.toBackdoorEstimationSystem` | `(S : POBackdoorSystem) (hov : two-sided overlap a.e.) (hY : Integrable factualY) → BackdoorEstimationSystem P γ` | **Derivability constructor.** Certifies the structure adds no assumptions beyond `POBackdoorSystem` + overlap + integrability: `μ_val := regFn`, `e_val := eLift` clamped into (0,1) off-support; all fields discharged (`μ_reg_compat` from `regression_adjustment`, `e_pos`/`e_lt_one` from the clamp, `e_compat` from overlap). **Proved.** |
| `StrictOverlap S ε` | `Prop` | `0 < ε`, `ε ≤ 1/2`, and `ε ≤ propScore true ω ≤ 1−ε` a.s. |
| `P_X`, `P_Z`, `factualZ` | `def`s | Covariate marginal `μ.map factualX`, joint data law `μ.map (X,A,Y)`, and the data triple. |
| `measurable_factualZ` | `Measurable S.factualZ` | Product-measurability. **Proved.** |
| `θ₀ S` | `def` | Value-space estimand `∫ x, μ_val(1,x) − μ_val(0,x) ∂P_X`. |
| `θ₀_eq_ATE` | `S.θ₀ = S.ATE` | Reduces to `integral_map` + `μ_compat hA` + `condExp_sub` + `integral_condExp`. **Proved.** |

#### 13a.2 `Estimation/ATE/AIPWMoment.lean`

AIPW moment, influence function, nuisance space.

| Declaration | Statement | Description |
|---|---|---|
| `projX`, `projA`, `projY`, `indA` | `def`s | Projections from `(γ × Bool × ℝ)` and the real-valued indicator of `{a = true}`. |
| `aipwMoment z μ_fn e_fn θ` | `def` | The AIPW moment `m_AIPW` from `def:est-aipw-moment`. |
| `ψ_AIPW S z` | `def` | Influence function at the truth `m_AIPW(η₀, z, θ₀)`. |
| `NuisanceVec γ` | structure | Nuisance pair `(μ_fn, e_fn)` with measurability witnesses. Componentwise `Zero`/`Add`/`Neg`/`Sub`/`SMul ℝ`/`AddCommGroup`/`Module ℝ` instances. **Proved.** |
| `η₀ S` | `def` | The truth as a `NuisanceVec`. |
| `H_ε ε` | `def` | Overlap-bounded realization set `{η : ε ≤ η.e_fn ≤ 1−ε on γ}`, the Lean instantiation of `def:est-aipw-nuisance-space`. |
| `aipwMomentFunctional` | `def` | The AIPW moment as a `NuisanceVec γ → (γ × Bool × ℝ) → ℝ → ℝ` family, suitable for `NeymanOrthogonal`. |

#### 13a.3 `Estimation/ATE/MeanZero.lean`

Helpers and the headline mean-zero lemma.

| Declaration | Statement | Description |
|---|---|---|
| `measurable_ψ_AIPW` | `Measurable S.ψ_AIPW` | **Proved.** |
| `cond_exp_residual_zero` | `μ[indD d · (Y − μ_val d (X)) \| σX] =ᵐ 0` | Reusable backdoor-factorization lemma; cites `cate_backdoor`, `μ_compat`, `condExp_sub`. **Proved.** |
| `theta_zero_factualX_integral` | `S.θ₀ = ∫ ω, (μ_val 1 X(ω) − μ_val 0 X(ω)) ∂P.μ` | One-line `integral_map` pull-back. **Proved.** |
| `aipw_factualZ_integral_zero` | `∫ ω, ψ_AIPW(factualZ ω) ∂P.μ = 0` | Pointwise decomposition into 4 pieces; main + constant cancel, both IPW corrections vanish via `weighted_residual_integral_zero`. **Proved.** |
| `aipw_mean_zero` | `∫ ψ_AIPW dP_Z = 0` | One-line `integral_map` reduction to `aipw_factualZ_integral_zero`. **Proved.** |

#### 13a.4 `Estimation/ATE/ScorePullout.lean`

Shared score pull-out lemmas used by `MeanZero.lean` and `Remainder*.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `e_val_label` | `e_val_label true = e_val`, `e_val_label false = 1 - e_val` | Value-space propensity conversion for labels. **Proved.** |
| `measurable_e_val_label` | `Measurable (S.e_val_label d)` | Measurability of value-space propensity label. **Proved.** |
| `propScore_false_ae` | `propScore false =ᵐ 1 - propScore true` | Linearity of conditional expectation + `indicator_add_indicator_not`. **Proved.** |
| `propScore_eq_e_val_label_ae` | `propScore d =ᵐ e_val_label d (X)` | `σ(X)`-conditional expectation of the treatment indicator. **Proved.** |
| `weighted_residual_integral_zero` | `∫ g(X) · indD d · (Y − μ_val d X) ∂P.μ = 0` for measurable `g` and integrable product | σ(X)-pull-out + `h_residual_ce_zero` + `integral_condExp`. **Proved.** |
| `indicator_to_propScore_integral` | `∫ f(X) · indD d ∂P.μ = ∫ f(X) · e_val_label d (X) ∂P.μ` | Rewrites indicators inside integrals to value-space scores. **Proved.** |

#### 13a.5 `Estimation/ATE/Archived/` — Gâteaux-derivative Neyman witness (archived 2026-05-06)

The textbook Gâteaux-derivative Neyman orthogonality witness for AIPW
(`aipw_neyman` and the supporting `NeymanAux/*` helpers) was archived after
the 2026-05-06 refactor of `aipw_dml_isAsymLinear`, which closes the DML
asymptotic-linearity proof through the per-η̂ bilinear-remainder bound
(`aipw_remainder_bound`) directly without going through a directional
derivative.  The archived files are retained for reference; they no longer
participate in the live build.

* `Archived/Neyman.lean` — `aipw_neyman` (Sorry; Gâteaux-form Neyman orthogonality on `H_ε`).
* `Archived/NeymanAux.lean` — umbrella import.
* `Archived/NeymanAux/Overlap.lean` — perturbation algebra and overlap bounds.
* `Archived/NeymanAux/Score.lean` — assignment-residual pull-out.
* `Archived/NeymanAux/{MuTrueResidual,MuFalseResidual,ETrueResidual,EFalseResidual}.lean` — four directional residual-zero integrals.
* `Archived/NeymanAux/Residual.lean` — residual umbrella.
* `Archived/NeymanAux/Directional.lean` — `aipwDirDeriv`, `measurable_aipwDirDeriv`, `aipwMoment_diff_quotient_tendsto`, `integral_aipwDirDeriv_eq_zero` (Sorry), `integral_aipwMoment_diff_quotient_tendsto` (Sorry).

#### 13a.6 `Estimation/ATE/FiniteVar.lean`

| Declaration | Statement | Description |
|---|---|---|
| `aipw_finite_var` | `Integrable (ψ_AIPW²) P_Z` | Pushforward to Ω + `MemLp` arithmetic + conditional Jensen on `μ_val(d, X)`. Requires `Integrable (factualY²)` and `Integrable ((YofD d)²)`. **Proved.** |

#### 13a.7 `Estimation/ATE/AIPWScoreL2.lean`

L²(P_Z) continuity of `m_AIPW(η, ·, θ₀)` in `η` on `H_ε`. Consumed by the DML `R₂` empirical-process step.

| Declaration | Statement | Description |
|---|---|---|
| `K_AIPW ε` | `def` | Lipschitz constant `1 + 2/ε + 2/ε²`. |
| `aipw_score_diff_pointwise_bound` | `∀ᵐ z ∂P_Z, \|m_AIPW(η,z,θ₀) − m_AIPW(η₀,z,θ₀)\| ≤ K_AIPW ε · (Σ_a \|Δμ_a\| + (\|y−μ₁\|+\|y−μ₀\|)·\|Δe\|)` | `P_Z`-a.e. Lipschitz on `H_ε`. Statement is a.e. (not pointwise) so `S.StrictOverlap ε` (`P.μ`-a.s.) suffices: it transfers via `e_compat` and pushforward to `ε ≤ e_val ∘ projX ≤ 1−ε` `P_Z`-a.s., then the algebraic Lipschitz step closes. **Proved by Codex.** |
| `YMuVal_residual_sq` | `def` | `(\|y − μ_val(1,x)\| + \|y − μ_val(0,x)\|)²` — fixed L¹(P_Z) cross-term integrand. |
| `yMuVal_residual_sq_integrable` | `Integrable YMuVal_residual_sq P_Z` | Pushforward `h_y2 + h_yd2` along `factualZ`, plus conditional-Jensen for `μ_val(d,X)² ∈ L²(P_X)`. **Proved by Codex.** |
| `aipw_score_diff_isLittleOp_one` | individual rates + `MemLp` of nuisance differences ⇒ `‖m_AIPW(η̂,·,θ₀) − m_AIPW(η₀,·,θ₀)‖_{L²(P_Z)} = o_p(1)` | Uses the a.e. Lipschitz bound, `P_Z.map projX = P_X`, and a truncation argument for the residual-weighted propensity-error cross term. **Proved.** |

### 13b. `Estimation/ATE/PlugIn.lean`

| Declaration | Statement | Description |
|---|---|---|
| `plugInEstimator` | `def` | `(1/\|B(n)\|) Σ_{i ∈ B(n)} (μ̂(n)(1, X_i) − μ̂(n)(0, X_i))`. |
| `ψ_plugin` | `def` | Plug-in IF `ψ_plugin(z) = μ_val(1, x) − μ_val(0, x) − θ₀`. |
| `plugIn_isAsymLinear` | Anchor (C) | Asymptotic linearity at `θ₀` with IF `ψ_plugin`, **indexed over `split.foldB`**. Hypotheses now include joint measurability `h_mu_meas : ∀ n a, Measurable (fun (p : P.Ω × γ) => μ_hat n p.1 a p.2)`, the new joint fold-A measurability `h_mu_uncurry_foldA : ∀ n a, Measurable[m_A.prod σ_γ] (fun p => μ_hat n p.1 a p.2)` (consumed by the new `iid_centered_sum_sq_lintegral_le` bridge), and `MemLp` `h_mu_memLp : ∀ n ω a, MemLp (fun x => μ_hat n ω a x) 2 S.P_X` (classical implicit assumptions). All three bullets **proved**: `mean_zero`, `finite_var` inline; `remainder` decomposes into `G + B` (centered fold-B sum + bias) via `foldB_centered_sum_isLittleOp_one` and `sqrtFoldB_integral_isLittleOp_one` plus the `S.P_Z.map projX = S.P_X` bridge. **Modulo the single probability sorry in `IIDCenteredSum.iid_centered_sum_sq_lintegral_unscaled_le` + the pre-existing Markov + DCT sorry in `foldB_centered_sum_truncated_isLittleOp_one`**. |
| `plugIn_tendstoNormal` | `√\|B(n)\| (θ̂ⁿ − θ₀) ⇒ N(0, ∫ψ²_plugin dP_Z)` | "In particular ..." clause of `thm:est-plug-in-ate-al`: composes `plugIn_isAsymLinear` with `IsAsymLinear.tendsto_normal_foldB` from `Stat/SampleSplit/PartialFoldCLT.lean`. **Proved** modulo the existing AL `sorry`. The `√n`-rate form `√n (θ̂ⁿ − θ₀) ⇒ N(0, σ²/c)` is one further Slutsky scaling step (cf. `IsAsymLinear.tendsto_normal_foldB_sqrt_n`). |

### 13c. `Estimation/ATE/DML.lean`

| Declaration | Statement | Description |
|---|---|---|
| `dmlEstimator` | `def` | `(1/\|B(n)\|) Σ_{i ∈ B(n)} m_AIPW(η̂(n), Z_i, _)`. |
| `dml_ATE_isAsymLinear` | Anchor (B) | Asymptotic linearity at `θ₀` with IF `ψ_AIPW`, **indexed over `split.foldB`**. **Migrated 2026-05-06** to a thin wrapper over the abstract `aipw_dml_isAsymLinear` (Estimation/OrthogonalMoments/AIPWInstance.lean): builds `η_hat n ω : NuisanceVec γ` from `(μ_hat n ω, e_hat n ω)`, derives per-η̂_n L²-of-differences from `h_mu_memLp` / `h_e_memLp` (plus inline derivations of `MemLp (S.μ_val a)` via condExp and `MemLp S.e_val` via boundedness), translates the production rate hypotheses into the abstract `ρ₁ = ‖Δμ_T‖₂ + ‖Δμ_F‖₂` / `ρ₂ = ‖Δe‖₂` shape (sum-of-`o_p` arguments), passes the score measurability/integrability bundle, applies the abstract theorem, then transports the conclusion field-by-field via two pointwise equalities: (i) IF equality `−J₀_inv · aipwMomentFunctional S.η₀ z S.θ₀ = S.ψ_AIPW z` (`J₀ = −1` ⇒ `−J₀_inv = 1`); (ii) rescaled-error equality `√\|B(n)\| · (dmlChern − θ₀) = √\|B(n)\| · (dmlEstimator − θ₀)` proved by case-split on `\|B(n)\| = 0` (both sides vanish via `√0 = 0`) vs `\|B(n)\| > 0` (algebraic identity using `aipwMoment(z, μ̂, ê, 0) = aipwMoment(z, μ̂, ê, S.θ₀) + S.θ₀`). The two-step transport correctly handles the empty-fold edge case where the underlying estimators differ pointwise but their rescaled errors agree. Net effect: the original ~615-line proof body (centered fold-B sum + bias decomposition expanded inline) is replaced by ~150 lines of structural translation, with all heavy lifting delegated to the OrthogonalMoments/ abstractions. New hypothesis vs the old version: `h_e_val_pointwise : ∀ x, ε ≤ S.e_val x ∧ S.e_val x ≤ 1 − ε` (needed to construct `aipwGeneralMoment`'s `η₀_mem` field). The `h_L2 : ∀ η ∈ H_ε ε, …` hypothesis was NOT added — it's eliminated by the parallel (2b) refactor of `aipw_dml_isAsymLinear`. **Proved** (no sorries). |
| `dml_ATE_tendstoNormal` | `√\|B(n)\| (θ̂ⁿ − θ₀) ⇒ N(0, ∫ψ²_AIPW dP_Z)` | "In particular ..." clause of `thm:est-dml-ate-al`: composes `dml_ATE_isAsymLinear` with `IsAsymLinear.tendsto_normal_foldB`. **Proved.** Signature gained one extra hypothesis `h_e_val_pointwise` (forwarded to `dml_ATE_isAsymLinear` after the 2026-05-06 migration). The `√n`-rate / `σ²/c` form is the same downstream Slutsky scaling. |

### 13d. `Estimation/ATE/Remainder.lean`

AIPW second-order remainder bound (split into `RemainderIdentity.lean` and
`RemainderBound.lean`).  Combines the algebraic remainder identity with the
overlap-bounded Cauchy–Schwarz step to convert the L²-product rate hypothesis
from `DML.lean` into the `o_p(n^{-1/2})` cross-term that closes `R₁` of the
three-remainder DML decomposition.  After the 2026-05-06 refactor this is the
sole route to Neyman orthogonality on the live AL critical path; the
Gâteaux-derivative witness `aipw_neyman` was archived to
`Estimation/ATE/Archived/Neyman.lean`.

| Declaration | Statement | Description |
|---|---|---|
| `aipw_rem_const ε` | `def` | The constant `2 / (ε(1−ε))`, the uniform weight bound on `H_ε`. |
| `plugin_bias_le_eLpNorm` | `\|∫ (μ̂ − μ_val) dP_X\| ≤ ‖μ̂ − μ_val‖_{L²(P_X)}` | Direct corollary of `abs_integral_le_eLpNorm_two` since `P_X` is a probability measure. **Proved.** (`RemainderIdentity.lean`) |
| `aipw_remainder_identity` | `∫ aipwMomentFunctional η z θ₀ dP_Z = ∫x ...` | Exact identity rewriting the remainder as cross terms before Cauchy–Schwarz. **Proved.** (`RemainderIdentity.lean`) |
| `aipw_remainder_bound` | `\|∫ aipwMomentFunctional η z θ₀ dP_Z\| ≤ aipw_rem_const ε · Σ_a ‖Δμ_a‖₂ · ‖Δe‖₂` for `η ∈ H_ε ε` | Algebraic expansion of `aipw_remainder_identity` + weight bound + Cauchy–Schwarz. **Proved.** (`RemainderBound.lean`) |
| `aipw_remainder_op` | `(η̂(n) ∈ H_ε ε)` ∧ `‖Δμ_a‖₂ · ‖Δe‖₂ = o_p(n^{-1/2})` ⇒ `∫ aipwMomentFunctional η̂ z θ₀ dP_Z = o_p(n^{-1/2})` | Stochastic-order corollary consumed at `DML.lean:152` (the `R₁` cross-term). **Proved.** (`RemainderBound.lean`) |

---

## 13plr. `Estimation/PLR/{Nuisance,Moment,Setup,MeanZero,RemainderBound,DML,ScoreL2,Feasible,JacobianConsistency}.lean` — Partially Linear DML

Instantiates the abstract DML `GeneralMoment` framework (§OrthogonalMoments) with
the Robinson partialling-out score for the partially linear model, built on the PO
causal layer §9g'''. Yields the doubly-robust moment whose product-rate remainder
drives √n-asymptotic normality of the DML estimator of the structural slope `θ`.
The score is a *linear score* with `m_a = −(D−m(X))²`, so the Jacobian
`J₀ = −E[(D−m_val(X))²]` is DGP-dependent (unlike AIPW's `J₀ = −1`).

| Declaration | Statement | Description |
|---|---|---|
| `PLRNuisance γ` | structure | Bundled nuisance `(ℓ, m)` of two measurable covariate functions, with `AddCommGroup`/`Module ℝ` instances — the DML nuisance space `H`. |
| `plrResidual η z` | `ℝ` | The treatment residual `d − m(x)` of observation `z=(x,d,y)`. |
| `plrMomentFunctional η z θ` | `ℝ` | The Robinson partialling-out score `(y−ℓ(x)−θ(d−m(x)))·(d−m(x))`. |
| `plrMoment_decomp` | `ψ = m_a·θ + m_b` | Linear-in-parameter decomposition, `m_a = −(d−m)²`, `m_b = (y−ℓ)(d−m)`. |
| `measurable_plrMomentFunctional` | measurability | The score is measurable in the data (the nuisance carries its own measurability). |
| `PLRSystem` | structure (extends model) | Value-space estimation system: a `POPartialLinearModel` plus regression representatives `ℓ_val, m_val`, their compatibility with `E[Y∣σX]`/`E[D∣σX]`, and nondegeneracy `E[(D−m_val(X))²]≠0`. |
| `PLRSystem.P_Z` / `P_X` | `Measure …` | Joint observed-data law `(X,D,Y)` and covariate marginal. |
| `PLRSystem.integral_P_Z` | `∫ f dP_Z = ∫ f∘(X,D,Y) dμ` | Change-of-variables bridge between `P_Z`-level and `μ`-level statements. |
| `PLRSystem.plrGeneralMoment` | `GeneralMoment …` | **The DML moment instance**: score, truth `η₀=(ℓ_val,m_val)`, target `θ₀=θ`, L²(P_X) bilinear seminorms, Jacobian `J₀=−E[(D−m_val(X))²]`. |
| `PLRSystem.plr_meanZero` | `MeanZero plrGeneralMoment` | The score has zero population mean at the truth (reduces, via CoV + compat, to `integral_U_resid`). |
| `PLRSystem.plr_remainder_bound` | `\|E[ψ(η,·,θ₀)]\| ≤ (1+\|θ₀\|)·ρ₁·ρ₂` | **Doubly-robust bilinear remainder**: the population bias at an estimated nuisance is the *product* of the two L²(P_X) nuisance errors; the framework's `o_p(n^{-1/2})` product-rate is exactly the textbook "both nuisances at `o_p(n^{-1/4})`." |
| `PLRSystem.plr_finite_var` | `Integrable ψ(η₀,·,θ₀)²` | Finite variance of the influence function, `E[U²·(D−m₀(X))²]<∞`. |
| `PLRSystem.plr_dml_isAsymLinear` | `IsAsymLinear (dmlChernozhukovEstimator …) θ₀ ψ …` | **Asymptotic linearity** of the one-step partially linear DML estimator: builds mean-zero/finite-var/doubly-robust-remainder from the PLR facts and invokes the proven `dml_chernozhukov_asymptoticLinear` engine (measurability + nuisance-rate conditions taken as hypotheses, as in the AIPW headline). |
| `PLRSystem.plr_dml_tendstoNormal` | `√\|B(n)\|·(θ̂ⁿ−θ₀) ⇒ N(0, ∫ψ² dP_Z)` | **√n-asymptotic normality** of the one-step DML estimator of the structural slope, via the generic `IsAsymLinear.tendsto_normal_foldB` CLT bridge; asymptotic variance `E[U²(D−m₀)²]/E[(D−m₀)²]²`. |
| `PLRSystem.plr_score_diff_isLittleOp_one` | `‖ψ(η̂,·,θ₀)−ψ(η₀,·,θ₀)‖_{L²(P_Z)} = o_p(1)` | Score-difference L²-consistency (`ScoreL2.lean`): from bounded truth residuals + a uniform sup-bound on the treatment-regression error + the two L²(P_X) nuisance rates. Discharges the `h_score_diff_rate` hypothesis of `plr_dml_isAsymLinear` (so it need not be assumed). Proof: the purely algebraic 5-term doubly-robust expansion + eLpNorm triangle/L∞·L² bounds. |
| `PLRSystem.plrFeasibleEstimator` | `Pₙ[(Y−ℓ̂)(D−m̂)] / Pₙ[(D−m̂)²]` | The **feasible solved** Robinson DML estimator (`Feasible.lean`) — the ratio actually computed in practice, obtained by solving the empirical orthogonal moment for `θ`. |
| `PLRSystem.plr_dml_feasible_tendstoNormal` | `√\|B(n)\|·(θ̂_feas−θ₀) ⇒ N(0, ∫ψ² dP_Z)` | **√n-asymptotic normality of the feasible solved estimator**, with the same limiting variance as the one-step estimator. Reduces to `plr_dml_tendstoNormal` via the asymptotic equivalence `√\|B\|(θ̂_feas−θ̂_os) = (J₀⁻¹−(Pₙmₐ)⁻¹)·(√\|B\|·Pₙψ) = o_p(1)·O_p(1)`, taking the fold-B Jacobian consistency `Pₙmₐ(η̂) →ₚ J₀` as a hypothesis. |
| `PLRSystem.integral_plrMomentA_η₀_eq_J₀` | `∫ mₐ(η₀,·) dP_Z = J₀` | The population integral of the partialling-out moment at the truth equals the Jacobian `J₀=−E[(D−m_val(X))²]` (`JacobianConsistency.lean`). Change of variables + the `mₐ=−(residual)²`/`residSecondMoment` defs. |
| `PLRSystem.integral_plrMomentA_diff_eq` | `∫ (mₐ(η̂)−mₐ(η₀)) dP_Z = −‖Δm‖²₂` | **Bias of the empirical-Jacobian increment**: its population integral is minus the squared L²(P_X) treatment-regression error; the cross term vanishes by orthogonality of the residual (`integral_condExpZero_mul_comp_factualX` + `condExp_resid_sigmaX`). |
| `PLRSystem.plr_jacobian_consistency` | `Pₙmₐ(η̂) →ₚ J₀` | **Fold-B Jacobian consistency** (`JacobianConsistency.lean`): the empirical partialling-out Jacobian at the estimated nuisance, averaged over fold B, converges in probability to its population value `J₀`. This is exactly the `hJ_consist` hypothesis of `plr_dml_feasible_tendstoNormal`, so feasible-estimator normality no longer needs it assumed. Proof: fold-B WLLN on `mₐ(η₀,·)` for the truth average; the bias of the increment is `−‖Δm‖²₂ = o_p(1)` by the L²(P_X) rate; the centered fluctuation is `o_p(1)` from the fold-B centered empirical-process bound renormalized by `(√\|B\|)⁻¹→0`. |

## 13'. `Estimation/ATT/` — Back-door ATT estimation and inference (parallel to §13)

Specific-system layer instantiating `POBackdoorSystem` for the **Average Treatment effect on the Treated** (ATT). Mirrors the ATE pipeline of §13 but uses the Hahn (1998) ATT AIPW form: nuisance is `(μ₀, e)` (no `μ₁`); one-sided overlap (`e ≤ 1 − ε`); rescaled moment `m_AIPW^ATT(η, z, θ) = A·(Y − μ₀(X)) − (1−A)·(e/(1−e))·(Y − μ₀(X)) − A·θ`; Jacobian `J₀ = −π_T` (not `−1`); influence function `ψ_ATT(z) = (1/π_T) · m_AIPW^ATT(η₀, z, θ_ATT)`.

Imports the Phase-1 identification result `PO/ID/Exact/ATT.lean` (§9g').

### 13'a. `Estimation/ATT/InfluenceFunction.lean` (umbrella)

Single-import aggregator that re-exports Setup, AIPWMoment, ScorePullout, MeanZero, FiniteVar.

#### 13'a.1 `Estimation/ATT/Setup.lean` — `TreatedEstimationSystem`, data law, value-space estimand

| Declaration | Statement | Description |
|---|---|---|
| `TreatedEstimationSystem P γ` | structure | Extends `POBackdoorSystem` with value-space `μ₀_val : γ → ℝ`, `e_val : γ → ℝ`, `e_lt_one`, and the **observable** compat fields `μ₀_reg_compat : μ₀_val ∘ factualX =ᵐ adjustedCE false` (ML/regression target — NO assumptions), `e_compat : propScore true =ᵐ e_val ∘ factualX`. The counterfactual `μ[Y(0)\|σX] =ᵐ μ₀_val ∘ factualX` is the derived lemma `μ₀_compat` below. Only the control-arm μ₀ enters the nuisance. |
| `TreatedEstimationSystem.μ₀_compat` | `(hA : Assumptions) → μ[Y(0)\|σX] =ᵐ μ₀_val ∘ factualX` | **Derived counterfactual compatibility** (was a field): `cate_backdoor hA false` composed with `μ₀_reg_compat`. **Proved.** |
| `POBackdoorSystem.toTreatedEstimationSystem` | `(S : POBackdoorSystem) (hov : two-sided overlap a.e.) (hY : Integrable factualY) → TreatedEstimationSystem P γ` | **Derivability constructor.** `μ₀_val := regFn false`, `e_val := eLift` clamped below 1; fields discharged (`μ₀_reg_compat` from `regression_adjustment false`, `e_lt_one` from clamp, `e_compat` from overlap). **Proved.** |
| `OneSidedOverlap S ε` | `Prop` | `0 < ε ∧ ε ≤ 1/2 ∧ (∀ᵐ ω ∂P.μ, propScore true ω ≤ 1 − ε)`. One-sided variant of `StrictOverlap`. |
| `P_X`, `P_Z`, `factualZ` | `def`s | Same as ATE; covariate marginal, joint data law, data triple. |
| `measurable_factualZ` | `Measurable S.factualZ` | **Proved.** |
| `P_Z_map_projX_eq_P_X` | `S.P_Z.map projX = S.P_X` | **Proved.** |
| `π_val S` | `def := S.toPOBackdoorSystem.propTreated` | Marginal treatment probability `π_T = E[A]`. |
| `θ₀ S` | `def := S.toPOBackdoorSystem.adjustedATT` | Value-space ATT estimand. |
| `θ₀_eq_ATT` | `S.θ₀ = S.toPOBackdoorSystem.ATT` | One-line delegation to Phase-1 `ATT_eq_adjustedATT`. **Proved.** |

#### 13'a.2 `Estimation/ATT/AIPWMoment.lean` — ATT AIPW moment, IF, nuisance

| Declaration | Statement | Description |
|---|---|---|
| `aipwMomentATT z μ₀_fn e_fn θ` | `def` | `A·(Y − μ₀(X)) − (1−A)·(e/(1−e))·(Y − μ₀(X)) − A·θ`. Reuses ATE's `projX, projA, projY, indA`. |
| `ψ_ATT S z` | `def := (1/π_val) · aipwMomentATT z μ₀_val e_val 0 − θ₀` | Centered ATT influence function for the population-`π_T` one-shot estimator. |
| `TreatedNuisanceVec γ` | structure | Nuisance pair `(μ₀_fn, e_fn)` with measurability witnesses; componentwise `Zero/Add/Neg/Sub/SMul ℝ/AddCommGroup/Module ℝ` instances. **Proved** (mechanical componentwise). |
| `η₀ S` | `def` | The truth as a `TreatedNuisanceVec`. |
| `H_ε ε` | `def := { η | ∀ x, η.e_fn x ≤ 1 − ε }` | One-sided overlap-bounded realization set. |
| `aipwMomentATTFunctional` | `def` | The ATT AIPW moment as `TreatedNuisanceVec γ → (γ × Bool × ℝ) → ℝ → ℝ`. |
| `measurable_aipwMomentATTFunctional` | `Measurable (fun z => aipwMomentATTFunctional η z θ)` | **Proved.** |

#### 13'a.3 `Estimation/ATT/ScorePullout.lean` — score pull-out helpers (one-sided)

| Declaration | Statement | Description |
|---|---|---|
| `ipwWeight_false` | `def := e_val(x) / (1 − e_val(x))` | Value-space IPW weight for the control arm. |
| `measurable_ipwWeight_false` | `Measurable S.ipwWeight_false` | **Proved.** |
| `propScore_false_ae` | `propScore false =ᵐ 1 − propScore true` | Mirrors ATE counterpart. **Proved.** |
| `propScore_eq_e_val_ae` | `propScore true =ᵐ e_val ∘ factualX` | Direct delegation to `S.e_compat`. **Proved.** |
| `propScore_false_eq_one_minus_e_val_ae` | `propScore false =ᵐ 1 − e_val ∘ factualX` | Combine `propScore_false_ae` + `e_compat`. **Proved.** |
| `residual_false_condExp_zero` | `μ[indD false · (Y − μ₀_val(X)) \| σX] =ᵐ 0` | Mirrors `cond_exp_residual_zero` for `d=false`. Uses `μ₀_compat`, consistency, `condExp_mul_of_condIndep`. **Proved.** |
| `weighted_residual_false_integral_zero` | `∫ g(X)·indD false·(Y − μ₀_val(X)) ∂P.μ = 0` | Mirrors ATE `weighted_residual_integral_zero` for control arm. **Proved.** |
| `indicator_to_propScore_integral` | `∫ f(X)·indD d ∂P.μ = ∫ f(X)·(if d=true then e_val(X) else 1−e_val(X)) ∂P.μ` | Same shape as ATE counterpart. **Proved.** |

#### 13'a.4 `Estimation/ATT/MeanZero.lean`

| Declaration | Statement | Description |
|---|---|---|
| `measurable_ψ_ATT` | `Measurable S.ψ_ATT` | **Proved.** |
| `measurable_aipwMomentATT_at_θ₀` | `Measurable (fun z => aipwMomentATT z μ₀_val e_val θ₀)` | **Proved.** |
| `aipw_mean_zero_ATT` | `∫ z, aipwMomentATT z μ₀_val e_val θ₀ ∂P_Z = 0` under `OneSidedOverlap`, `Assumptions`, `0 < π_val`, `Y² ∈ L¹`, `Y(0)² ∈ L¹`, `hIPW` integrability | Reduces to Phase-1 `adjustedATT_eq_aipwForm` via `μ₀_compat + cate_backdoor` ae-rewrite of the σ(X) representatives, then arithmetic cancellation `π_T·θ₀ − θ₀·π_T = 0`. **Proved.** |

#### 13'a.5 `Estimation/ATT/FiniteVar.lean`

| Declaration | Statement | Description |
|---|---|---|
| `aipw_finite_var_ATT` | `Integrable (aipwMomentATT z μ₀_val e_val θ₀)² P_Z` | Pushforward to Ω + `(a+b+c)² ≤ 3(a²+b²+c²)` + each summand bounded via overlap and `Y², Y(0)² ∈ L¹`. **Proved.** |
| `ipw_estimated_integrable` | for any `η` with `0 ≤ e ≤ 1−ε` `P_X`-a.e. and `μ₀ ∈ L²(P_X)`: `Integrable ((1−A)·(e/(1−e))·(Y − μ₀)) P_Z` | **Derives the per-learner `h_IPW_at` gate** from one-sided overlap + L² learners. Weight `(1−A)·e/(1−e)` is `P_Z`-a.e. bounded by `ε⁻¹` (`L^∞`), residual `Y − μ₀ ∈ L²(P_Z)`, product in `L² ⊆ L¹`. Bounds taken **a.e.** (not pointwise). **Proved.** |
| `ipw_truth_integrable` | `Integrable ((1−A)·(propScore/(1−propScore))·(Y − adjustedCE false)) P.μ` under `OneSidedOverlap`, `Assumptions`, `Y², Y(0)² ∈ L¹` | **Derives the truth-nuisance `hIPW` gate** (consumed by `aipw_mean_zero_ATT` / `att_dml_isAsymLinear`) from overlap + L² outcomes alone — no pointwise bounds. Applies `ipw_estimated_integrable` at `η₀`, transfers `P_Z → P.μ` along `factualZ`, then rewrites to PO representatives via `e_compat` / `μ₀_reg_compat`. **Proved.** |

### 13'b. `Estimation/ATT/Remainder.lean` — second-order remainder (split file)

| Declaration | Statement | Description |
|---|---|---|
| `aipw_rem_const_ATT ε` | `def := 1/ε` | One-sided overlap weight bound (`(1−ê)⁻¹ ≤ 1/ε`). Single product (no Σ over arms). |
| `IsLittleOp.add_eventually_nonneg_rate`, `IsLittleOp.of_abs_le_const_mul` | private helpers | Local copies of the general utilities from ATE/RemainderBound. **Proved.** |
| `aipw_remainder_identity_ATT` | `∫ aipwMomentATTFunctional η z θ₀ ∂P_Z = ∫ x, ((ê − e)/(1−ê)) · (μ̂₀ − μ₀_val) ∂P_X` under `OneSidedOverlap`, `Assumptions`, `MemLp` of differences, IPW integrability | Hahn (1998) Theorem 1 eq. 7 second-order form. ATT version is one cross-product (no sum over arms). | **One sorry remaining** — the algebraic decomposition into σ(X)-residual zero-means is documented but the final `(ê·(1−e_val) − e_val·(1−ê))/(1−ê) = (ê − e_val)/(1−ê)` recombination step left as `sorry` after 3 codex iterations. (`RemainderIdentity.lean`) |
| `aipw_remainder_bound_ATT` | `\|∫ aipwMomentATTFunctional η z θ₀ ∂P_Z\| ≤ (1/ε) · ‖μ̂₀ − μ₀_val‖₂ · ‖ê − e_val‖₂` for `η ∈ H_ε ε` | Cauchy–Schwarz on `aipw_remainder_identity_ATT`. **Proved** modulo the identity sorry above. (`RemainderBound.lean`) |
| `aipw_remainder_op_ATT` | Stochastic-order corollary `o_p(n^{-1/2})` under product rate | **Proved** modulo the identity sorry. (`RemainderBound.lean`) |

### 13'c. `Estimation/ATT/AIPWScoreL2.lean`

L²(P_Z) continuity of `m_AIPW^ATT(η, ·, θ₀)` in `η` on `H_ε`. ATT version drops the d=true arm.

| Declaration | Statement | Description |
|---|---|---|
| `K_AIPW_ATT ε` | `def := 1 + 2/ε + 2/ε²` | Lipschitz constant. |
| `K_AIPW_ATT_one_le, K_AIPW_ATT_mu_le, K_AIPW_ATT_inv_sq_le` | private monotonicity | **Proved.** |
| `aipw_score_lipschitz_ATT` | `\|m_AIPW^ATT(η,z,θ₀) − m_AIPW^ATT(η₀,z,θ₀)\| ≤ K_ATT(ε)·\|Δμ₀(x)\| + K_ATT(ε)·\|y − μ₀(x)\|·\|Δe(x)\|` | Pointwise Lipschitz on `H_ε`; ATT is two summands (no sum over arms). **Proved.** Hypothesis list extended with `h_e_pointwise : ∀ x, S.e_val x ≤ 1 − ε`. |
| `aipw_score_diff_isLittleOp_one_ATT` | individual rates + `MemLp` ⇒ `‖m_AIPW^ATT(η̂,·,θ₀) − m_AIPW^ATT(η₀,·,θ₀)‖_{L²(P_Z)} = o_p(1)` | **One sorry remaining** — the truncation step for the residual·Δe cross term needs `\|Δe\| ≤ 1`, which the current one-sided `H_ε` (`ê ≤ 1 − ε`, no lower bound) does not directly provide. Documented in a `--` comment. |

### 13'd. `Estimation/ATT/ATTInstance.lean` — abstract `GeneralMoment` instance + headline DML

| Declaration | Statement | Description |
|---|---|---|
| `attGeneralMoment` | `def : GeneralMoment …` | Instantiates the abstract framework. `ρ₁ = ‖Δμ₀‖_{L²(P_X)}` (single arm), `ρ₂ = ‖Δe‖_{L²(P_X)}`, `J₀ = −S.π_val`, `J₀_ne_zero` from `0 < S.π_val`. |
| `att_meanZero` | `MeanZero (attGeneralMoment …)` | One-line delegation to `aipw_mean_zero_ATT`. **Proved.** |
| `att_bilinearRem` | `∃ C, BilinearRemainder (attGeneralMoment …) C` | One-line delegation to `aipw_remainder_bound_ATT`; no Σ_a sum to unfold. **Proved.** (Conditional on the RemainderIdentity sorry.) |
| `att_dml_isAsymLinear` | `IsAsymLinear (dmlChernozhukovEstimator (attGeneralMoment …) sample split η_hat) S.θ₀ (fun z => -J₀_inv · aipwMomentATTFunctional S.η₀ z S.θ₀) sample split.foldB` | Headline composition: `att_meanZero` + `aipw_finite_var_ATT` + per-η̂_n bilinear remainder + `aipw_score_diff_isLittleOp_one_ATT`, plumbed into `dml_chernozhukov_asymptoticLinear`. **Proved.** (Conditional on the AIPWScoreL2 + RemainderIdentity sorries.) |

### 13'e. `Estimation/ATT/DML.lean` — user-facing wrapper

| Declaration | Statement | Description |
|---|---|---|
| `dmlEstimator_ATT` | `def := (1/π_val) · (1/\|B(n)\|) Σ_{i ∈ B(n)} aipwMomentATT(Z_i, μ̂₀(n,ω), ê(n,ω), 0)` | Empirical Hahn-form ATT estimator. |
| `ψ_ATT_integral_zero` | `∫ ψ_ATT dP_Z = 0` under `OneSidedOverlap`, `Assumptions`, `0 < π_T`, `Y², Y(0)² ∈ L¹`, truth IPW integrability | Mean-zero of the centered population-`π_T` IF. Derived from `aipw_mean_zero_ATT` + `∫ A dP_Z = π_T` and `m(η₀,z,0) = m(η₀,z,θ₀) + A·θ₀`. **Proved.** |
| `ψ_ATT_finite_var` | `Integrable (ψ_ATT)² P_Z` under `OneSidedOverlap`, `Assumptions`, `Y², Y(0)² ∈ L¹` | Finite variance of the centered IF. Derived from `aipw_finite_var_ATT` (moment-at-`θ₀` in `L²`) + boundedness of `A`, via `ψ_ATT = (1/π_T)·(m(η₀,·,θ₀) + θ₀·A) − θ₀ ∈ L²(P_Z)`. **Proved.** |
| `dml_ATT_isAsymLinear` | Asymptotic linearity at `θ₀` with IF `ψ_ATT`, indexed over `split.foldB`, under `OneSidedOverlap`, `Assumptions`, true/estimated propensity bounds, `0 < π_T`, `Y², Y(0)² ∈ L¹`, joint/fold-A measurability, per-ω L² nuisance hypotheses, score integrability, and individual + product L² rates | Wrapper over `att_dml_isAsymLinear`. Builds `η_hat` from `(μ̂₀, ê)`, forwards the abstract score regularity bundle, and transports the Chernozhukov remainder to the population-`π_T` estimator with the centered `ψ_ATT`. The `IsAsymLinear` mean-zero / finite-variance fields are **derived** (via `ψ_ATT_integral_zero` / `ψ_ATT_finite_var`), and the truth / per-learner IPW integrability gates are **derived** (via `ipw_truth_integrable` / `ipw_estimated_integrable`) — none taken as hypotheses. **Proved in `DML.lean`.** |

### Phase-2 status summary

Twelve files written; ten with full proofs, two with one documented sorry each. Two sorries remain in the production chain (RemainderIdentity and AIPWScoreL2 isLittleOp), both stemming from the genuinely simpler statement requirements of the ATT setting that don't directly mirror ATE. Headline mathematical content (mean-zero, finite variance, Lipschitz bound, OR/AIPW remainder identity at the population level, abstract DML asymptotic linearity composition, DML headline transport) is proved except for the documented upstream gaps.

---

## 13''. `Estimation/DTR/` — Sequential AIPW (DTR, n = 2) DML estimation and inference

Dynamic-treatment-regime counterpart of the ATE DML pipeline.  The target is the fixed-regime mean `θ₀ = E[Y(d̄)]` for `d̄ : Fin 2 → δ`; the score is the explicit two-stage sequential AIPW moment with stagewise IPW weights, centered by `θ₀`.  The population remainder is a cross-product identity over the stage-history marginals `P_H₀` and `P_H₁`, with `J₀ = -1` in the abstract `GeneralMoment` instance.

Phase A scope: this module is intentionally specialized to horizon `n = 2`; Phase B generalizes to arbitrary finite horizons.  The treatment space `δ` is discrete enough for equality indicators, via `[MeasurableSingletonClass δ]`.

### 13''a.1 `Estimation/DTR/Setup.lean`

| Declaration | Statement | Description |
|---|---|---|
| `DTREstimationSystem P δ γ` | `structure extends PODTRSystem P 2 δ γ` | Adds fixed regime `dbar`, value-space representatives `μ₀_val/e₀_val/μ₁_val/e₁_val`, pointwise propensity bounds in `(0,1)`, and the **observable** regression-compat fields. `μ₀_reg_compat : μ₀_val ∘ factualS 0 =ᵐ innerReg dbar 1` (the outer nested regression `f₁`); `μ₁_reg_compat : μ₁_val ∘ (S₁,D₀,S₀) =ᵐ stageOneReg` (the last-stage regression `f₂`); `e₀_compat`/`e₁_compat` (observable propensities). The counterfactual `μ[Y(dbar)\|σ(histₖ)]` readings are NO LONGER fields. |
| `DTREstimationSystem.μ₀_compat` | `(hA : Assumptions) → μ[Y(dbar)\|σ(hist₀)] =ᵐ μ₀_val ∘ factualS 0` | **Derived** stage-0 counterfactual compat, via `cdtr_backdoor` (`μ[Y(dbar)\|σ(hist₀)] =ᵐ innerReg dbar (n-1)`) + `μ₀_reg_compat`. **Proved.** |
| `DTREstimationSystem.stageOneReg` | `def : P.Ω → ℝ := condExpRatio_{hist₁}(Y·1_{D₁=d̄₁}, 1_{D₁=d̄₁})` | **Observable stage-1 regression** `f₂ = E[Y\|hist₁,D₁=d̄₁]` (the paper CNSS nested-regression base case); the ML/regression target `μ₁_val` represents. |
| `DTREstimationSystem.μ₁_val_comp_eq_stageOneReg` | `μ₁_val ∘ (S₁,D₀,S₀) =ᵐ stageOneReg` | `μ₁_reg_compat` via the `stageOneReg` abbreviation. **Proved.** |
| `DTREstimationSystem.stageOneReg_indD_eq` | `(hA) → stageOneReg · indD dbar 1 =ᵐ indD dbar 1 · μ[Y(dbar)\|σ(hist₁)]` | **Stage-1 `·indD` identification** (regime-local `{D₀=d̄₀}`): the observable `f₂` agrees with the counterfactual conditional on the regime path. Via `cdtr_base` + `stageOneReg = innerReg 0` on `{D₀=d̄₀}`. Foundation for the AIPW-score consumers. **Proved.** |
| `DTREstimationSystem.stageOneReg_memLp` | `(hov : StrictOverlap ε) (h_y2 : Integrable factualY²) → MemLp stageOneReg 2 P.μ` | `f₂ ∈ L²` from strict overlap (denominator `≥ ε`) + factual second moment. Foundation for the L²-membership consumers (replaces the old counterfactual route). **Proved.** |
| `StrictOverlap S ε` | `0 < ε ∧ ε ≤ 1/2 ∧` stage-0 and stage-1 conditional propensities lie in `[ε, 1−ε]` a.e. | Two-stage overlap predicate for the target regime. **Consumer note:** the DTR estimation theorems (`MeanZero`/`ScorePullout`/`FiniteVar`/`DML`/`RemainderIdentity`/`ScoreL2`) now thread `StrictOverlap`/`Integrable factualY²`/`Assumptions` where the old proofs used the (removed) counterfactual compat fields. |
| `PODTRSystem.toDTREstimationSystem` (`Estimation/DTR/Constructor.lean`) | `(S : PODTRSystem P 2 δ γ) (dbar) (two-stage overlap a.e.) → DTREstimationSystem P δ γ` | **DTR derivability constructor.** Builds all four nuisances from Doob–Dynkin lifts — `μ₀_val` from `innerReg dbar 1`, `μ₁_val` from `stageOneReg`, `e₀_val`/`e₁_val` from the stagewise propensity lifts clamped into (0,1) — and discharges all ten structure fields. Certifies `DTREstimationSystem` adds no assumptions beyond a bare DTR system + overlap. Lift machinery: `exists_stage0_lift`/`exists_stage1_lift` (`σ(histₖ) ≤ comap (explicit factual tuple)` + `Measurable.exists_eq_measurable_comp`). **Proved.** |
| `P_H₀`, `P_H₁`, `P_Z` | `Measure (γ 0)`, `Measure (γ 1 × δ × γ 0)`, `Measure (γ 0 × δ × γ 1 × δ × ℝ)` | Stage-history marginals and the factual data law. |
| `factualZ`, `measurable_factualZ` | `Ω → γ 0 × δ × γ 1 × δ × ℝ`; `Measurable S.factualZ` | Observed tuple `(S₀,D₀,S₁,D₁,Y)` and its measurability. |
| `θ₀` | `def := S.toPODTRSystem.dtrEffect S.dbar` | Value-space target estimand `E[Y(dbar)]`. |
| `θ₀_eq_dtrEffect` | `S.θ₀ = S.toPODTRSystem.dtrEffect S.dbar` | Definitional agreement with the PO-level DTR effect; `rfl`. |

### 13''a.2 `Estimation/DTR/SeqDRMoment.lean`

| Declaration | Statement | Description |
|---|---|---|
| `projS₀`, `projD₀`, `projS₁`, `projD₁`, `projY`, `histH₁` | coordinate projections from `(s₀,d₀,s₁,d₁,y)`; `histH₁ z : γ 1 × δ × γ 0` | Data-tuple projections and cons-ordered stage-1 history. |
| `indEq` | `def (d d' : δ) : ℝ := if d = d' then 1 else 0` | Real equality indicator for discrete treatments. |
| `DTRNuisanceVec₂ δ γ` | structure | Bundles `μ₀_fn/e₀_fn : γ 0 → ℝ` and `μ₁_fn/e₁_fn : γ 1 × δ × γ 0 → ℝ` with measurability witnesses; componentwise algebra instances and extensionality are provided. |
| `seqDRMoment dbar z η θ` | `μ₀ + 1{D₀=d₀}/e₀·(μ₁−μ₀) + 1{D₀=d₀}1{D₁=d₁}/(e₀e₁)·(Y−μ₁) − θ` | Explicit two-stage sequential DR/AIPW moment. |
| `η₀`, `S.seqDRMoment`, `ψ_seqDR` | truth nuisance; system-specialized moment; truth score | Extracts the true nuisance from `S` and defines `ψ_seqDR(z) = S.seqDRMoment z S.η₀ S.θ₀`. |
| `H_ε` | pointwise `ε ≤ e₀,e₁ ≤ 1−ε` on both history spaces | Realization set for overlap-bounded nuisance candidates. |
| `seqDRMomentFunctional`, `measurable_seqDRMomentFunctional` | `DTRNuisanceVec₂ δ γ → Z → ℝ → ℝ`; measurable in `z` | Functional form consumed by the abstract DML layer. **Proved.** |
| `measurable_projS₀`, `measurable_histH₁` | `Measurable (projS₀)`, `Measurable (histH₁)` | Shared projection measurability lemmas. **Proved.** |
| `P_Z_map_projS₀_eq_P_H₀`, `P_Z_map_histH₁_eq_P_H₁` | `P_Z.map projS₀ = P_H₀`, `P_Z.map histH₁ = P_H₁` | Shared pushforward identities used by `RemainderIdentity` and `ScoreL2`. **Proved.** |

### 13''a.3 `Estimation/DTR/ScorePullout.lean`

| Declaration | Statement | Description |
|---|---|---|
| `weighted_residual_integral_zero_stage0` | weighted integral of `1{D₀=dbar 0}·(μ₁_val(history₁) − μ₀_val(S₀))` is zero | Stage-0 score pull-out. Corrected stage-0 residual uses `μ₁_val(history₁) − μ₀_val(s₀)`. **Proved.** |
| `indicator_to_propScore_integral_stage0` | `∫ f(S₀)·1{D₀=dbar 0} dμ = ∫ f(S₀)·e₀_val(S₀) dμ` | Stage-0 indicator-to-propensity rewrite. **Proved.** |
| `weighted_residual_integral_zero_stage1` | weighted integral of `1{D₀=dbar 0}·1{D₁=dbar 1}·(factualY − μ₁_val(history₁))` is zero | Stage-1 score pull-out with the joint regime indicator. **Proved.** |
| `indicator_to_propScore_integral_stage1` | `∫ f(history₁)·1{D₁=dbar 1} dμ = ∫ f(history₁)·e₁_val(history₁) dμ` | Stage-1 indicator-to-propensity rewrite. **Proved.** |

### 13''a.4 `Estimation/DTR/MeanZero.lean`

| Declaration | Statement | Description |
|---|---|---|
| `measurable_ψ_seqDR` | `Measurable S.ψ_seqDR` | Score measurability. **Proved.** |
| `propScore_ne_zero_stage0`, `propScore_ne_zero_stage1` | stagewise conditional propensities are nonzero a.e. | Nondegeneracy from DTR overlap assumptions. **Proved.** |
| `cond_exp_residual_zero_stage0`, `cond_exp_residual_zero_stage1` | corrected stage-0 and stage-1 residual conditional expectations are zero a.e. | Conditional-mean-zero core for the sequential score. **Proved.** |
| `theta_zero_factualS₀_integral` | `S.θ₀ = ∫ μ₀_val(factualS 0) dP.μ` | Identifies the estimand with the stage-0 regression integral. **Proved.** |
| `seqDR_factualZ_integral_zero` | private Ω-side integral of `ψ_seqDR(factualZ)` is zero | Unmapped mean-zero helper. **Proved.** |
| `seqDR_mean_zero` | `∫ z, S.ψ_seqDR z ∂S.P_Z = 0` | Headline mean-zero theorem under DTR assumptions, strict overlap, `Y²`, and all `Y(dbar)²` integrability. **Proved.** |

### 13''a.5 `Estimation/DTR/FiniteVar.lean`

| Declaration | Statement | Description |
|---|---|---|
| `measurable_ψ_seqDR_squared` | `Measurable (fun z => (S.ψ_seqDR z)^2)` | Squared-score measurability. **Proved.** |
| `seqDR_finite_var` | `Integrable (fun z => (S.ψ_seqDR z)^2) S.P_Z` | Finite variance from strict overlap and second moments of `Y` and all `Y(dbar)`. **Proved.** |

### 13''b. `Estimation/DTR/RemainderIdentity/Helpers.lean`

Proof helpers for the headline remainder identity; split out to keep `RemainderIdentity.lean` to the headline declaration only.

| Declaration | Statement | Description |
|---|---|---|
| `indEq_factualD0_eq_indicator`, `indEq_factualD1_eq_indicator` | `indEq (factualD k ω) (dbar k) = dVar k .indicator (dbar k) ω` | Rewrites stage-0/1 equality indicators as `{0,1}`-valued indicator functions. |
| `eta_e0_pos_of_mem_Hε`, `eta_e1_pos_of_mem_Hε` | `0 < η.e₀_fn s₀`, `0 < η.e₁_fn h` | Positivity of nuisance propensities from `H_ε` membership. |
| `indicator_weighted_delta_mu0_integrable` | `Integrable (1{D₀=dbar 0}/ê₀·(μ̂₀−μ₀)) P.μ` | Stage-0 indicator-weighted μ-error integrability via L²·L∞. |
| `indicator_weighted_delta_mu1_stage0_integrable` | `Integrable (1{D₀=dbar 0}/ê₀·(μ̂₁−μ₁)) P.μ` | Stage-0 weight applied to μ₁ error. |
| `indicator_weighted_delta_mu1_stage1_integrable` | `Integrable (1{D₀=dbar 0}·1{D₁=dbar 1}/(ê₀·ê₁)·(μ̂₁−μ₁)) P.μ` | Stage-1 double-indicator-weighted μ₁-error integrability via `ε²`-bounded weight. |
| `split_stage_history_integral` | `∫ z, f₀(projS₀ z)+f₁(histH₁ z) dP_Z = ∫ f₀ dP_H₀ + ∫ f₁ dP_H₁` | Splits a P_Z integral over the two stage-history marginals. |

### 13''b'. `Estimation/DTR/RemainderIdentity.lean`

| Declaration | Statement | Description |
|---|---|---|
| `seqDR_remainder_identity_stage0` | placeholder stage-0 identity slot, currently `True` | Reserved for independent unrolling of the `P_H₀` cross product. |
| `seqDR_remainder_identity_stage1` | placeholder stage-1 identity slot, currently `True` | Reserved for independent unrolling of the `P_H₁` cross product. |
| `seqDR_remainder_identity` | `∫ m_seqDR(η,z,θ₀) dP_Z = ∫_{P_H₀} (ê₀−e₀)·(1/ê₀)·(μ̂₀−μ₀) + ∫_{P_H₁} 1{D₀=dbar 0}·(ê₁−e₁)·(1/(ê₀·ê₁))·(μ̂₁−μ₁)` | Headline n=2 algebraic remainder identity. Stage-1 RHS includes `indEq h.2.1 (S.dbar 0)` because `P_H₁` is unconditional. Stage weights are `w₀ = 1/η.e₀_fn` and `w₁ = 1/(η.e₀_fn(s₀)·η.e₁_fn(history₁))`. **One documented sorry**: full n=2 algebraic unrolling, the module's second-hardest proof per the plan. |

### 13''c. `Estimation/DTR/RemainderBound.lean`

| Declaration | Statement | Description |
|---|---|---|
| `seqDR_rem_const ε` | `def := 2 / (ε^2 · (1 − ε))` | Uniform strict-overlap constant for the two-stage IPW weights. |
| `IsLittleOp.add_eventually_nonneg_rate`, `IsLittleOp.of_abs_le_const_mul` | private helpers | Local stochastic-order utilities copied from ATE. **Proved.** |
| `seqDR_remainder_bound` | absolute population remainder ≤ `seqDR_rem_const ε · (‖Δμ₀‖₂+‖Δμ₁‖₂) · (‖Δe₀‖₂+‖Δe₁‖₂)` | Cauchy-Schwarz bound over `P_H₀` and `P_H₁`. **Proved modulo upstream `seqDR_remainder_identity`.** |
| `seqDR_remainder_op` | four product rates imply the population remainder is `o_p(n^{-1/2})` | Stochastic-order corollary using the 00, 11, 01, and 10 cross-products. **Proved modulo upstream identity.** |

### 13''d. `Estimation/DTR/ScoreL2/Helpers.lean`

Proof helpers for the headline L²(P_Z) continuity theorem; split out to keep `ScoreL2.lean` to the headline declaration only.

| Declaration | Statement | Description |
|---|---|---|
| `K_seqDR ε` | `def := 1 + 2/ε + 4/ε^2 + 4/ε^3` | Scaffolded pointwise Lipschitz constant for sequential DR score continuity. |
| `K_seqDR_one_le`, `K_seqDR_mu0_le`, `K_seqDR_mu1_le`, `K_seqDR_inv_sq_le`, `K_seqDR_inv_cubed_le` | bounds `1 ≤ K`, `1+1/ε ≤ K`, etc. | Sub-term domination by `K_seqDR`. |
| `indEq_nonneg`, `indEq_le_one` | `0 ≤ indEq d d' ≤ 1` | Indicator range lemmas. |
| `IsLittleOp.add_one`, `IsLittleOp.of_abs_le_const_mul_one` | composition rules for `o_p(1)` | Add and domination lemmas for stochastic little-o. |
| `eLpNorm_comp_projS₀_eq`, `eLpNorm_comp_histH₁_eq` | `eLpNorm (f ∘ projS₀) 2 P_Z = eLpNorm f 2 P_H₀`, etc. | L² norm transport from P_Z to stage marginals via pushforward. |
| `seqDR_stage0_ratio_bound`, `seqDR_stage1_ratio_bound`, `seqDR_real_bound` | pointwise Lipschitz estimates on `H_ε` | Algebraic bounds for the |Δm_seqDR| estimate. |
| `μ₀_val_memLp`, `μ₁_val_memLp` | `MemLp (μ₀_val ∘ projS₀) 2 P_Z`, etc. | MemLp of true regression values under P_Z. |

### 13''d'. `Estimation/DTR/ScoreL2.lean`

| Declaration | Statement | Description |
|---|---|---|
| `seqDR_score_diff_isLittleOp_one` | `‖m_seqDR(η̂_n,·,θ₀) − m_seqDR(η₀,·,θ₀)‖_{L²(P_Z)} = o_p(1)` | Headline score L² continuity from the four individual stagewise rates. **One documented sorry**: needs DTR analogs of the ATE pointwise Lipschitz, `P_Z → P_H` projection, and residual-truncation lemmas, mirroring about 500 lines of `ATE/AIPWScoreL2.lean` infrastructure. |

### 13''e. `Estimation/DTR/DTRInstance.lean`

| Declaration | Statement | Description |
|---|---|---|
| `seqDRGeneralMoment` | `def : GeneralMoment ...` | Abstract instance with `ρ₁ = ‖Δμ₀‖_{P_H₀,2}+‖Δμ₁‖_{P_H₁,2}`, `ρ₂ = ‖Δe₀‖_{P_H₀,2}+‖Δe₁‖_{P_H₁,2}`, and `J₀ = -1`. |
| `seqDR_meanZero` | `MeanZero (seqDRGeneralMoment S h_e_pointwise)` | Delegates to `seqDR_mean_zero`. **Proved.** |
| `seqDR_bilinearRem` | `∃ C, BilinearRemainder (seqDRGeneralMoment S h_e_pointwise) C` | Delegates to `seqDR_remainder_bound`. **Proved modulo upstream identity.** |
| `seqDR_dml_isAsymLinear` | Chernozhukov-form asymptotic linearity with IF `fun z => -J₀_inv · S.seqDRMomentFunctional S.η₀ z S.θ₀` | Abstract DTR asymptotic-linearity headline. **Proved modulo upstream RemainderIdentity + ScoreL2 sorries.** |

### 13''f. `Estimation/DTR/DML.lean`

| Declaration | Statement | Description |
|---|---|---|
| `dml_DTR_estimator` | empirical fold-B mean of `seqDRMoment S.dbar (Z_i) (η_hat n ω) 0` | Production one-shot sequential DR estimator for the DTR effect. |
| `dml_DTR_isAsymLinear` | `IsAsymLinear (dml_DTR_estimator ...) S.θ₀ S.ψ_seqDR sample split.foldB` | User-facing wrapper over `seqDR_dml_isAsymLinear`, bundling four stagewise hats and translating rates/measurability. **One documented sorry**: needs reusable measurability lemma for `indEq` over `MeasurableSingletonClass δ` histories plus inverse-weight bound normalization for the bundled DTR score. |

### Phase A status summary

Ten files implement the `n = 2` DTR sequential-AIPW DML pipeline.  Three documented sorries remain: `seqDR_remainder_identity`, `seqDR_score_diff_isLittleOp_one`, and `dml_DTR_isAsymLinear`.  All three are large algebraic / infrastructure proofs that mirror the corresponding ATE files at larger stagewise scale; downstream typechecking succeeds because the sorries are inside lemma bodies, not statements.  Phase B (general horizon `n`) is deferred.

---

## 13'''''. `Estimation/NPIV/` — TRAE / NPIV doubly-robust estimation and inference track

Formalizes the non-iterated Tikhonov regularized adversarial estimator (TRAE) of
Bennett et al. (2023) for linear inverse-problem functionals — the canonical
NPIV setup.  Mirrors `doc/basic_concepts/po/estimation/trae_inverse_problems.tex`.
**The entire primal one-step Tikhonov track is fully proved (zero sorries).**
Phase A (mixed-bias identity), Phase B (TRAE-DR asymptotic linearity +
asymptotic normality), Phase C-lite + Phase D (operator layer `T` + primal rate
theorem), and Phase E (adjoint `T†`, real-CFC spectral calculus, Tikhonov bias
discharge, and the localized empirical-process event) are all complete.  The
dual TRAE (`def:est-trae-dual`) and its symmetric rate theorem are *not*
formalized — only the `DualSolution` structure the DR score needs is.  See the
per-phase Status section at the end.

### Phase A files

#### `Estimation/NPIV/Setup.lean`

| Declaration | Statement | Description |
|---|---|---|
| `InverseProblemSystem Ω μ` | structure | Linear inverse-problem functional system (`def:est-trae-system`).  Carries observation type `𝒲` with projections `xOf : 𝒲 → 𝒳`, `zOf : 𝒲 → 𝒵`; random observation `W : Ω → 𝒲`; candidate sets `Hbar ⊆ (𝒳 → ℝ)`, `Qbar ⊆ (𝒵 → ℝ)` (closure under subtraction; no Hilbert structure in Phase A); observation-level moment maps `m, m_e` with linearity-over-subtraction; per-class measurability + integrability witnesses; primal nuisance `h₀ ∈ Hbar`; primal moment identity `E[m(W; q)] = E[h₀(X) q(Z)]` for `q ∈ Qbar`. |
| `InverseProblemSystem.X`, `.Z` | `def`s | Derived random variables `X := xOf ∘ W`, `Z := zOf ∘ W`. |
| `InverseProblemSystem.θ₀` | `def` | Scalar target `θ₀ := E[m_e(W; h₀)]`. |
| `InverseProblemSystem.phiVal h q w` | `def` | Pointwise pseudo-outcome `m_e(w; h) + m(w; q) − q(zOf w) h(xOf w)`. Reusable for empirical fold averages. |
| `InverseProblemSystem.phi h q ω` | `def` | `phiVal h q (W ω)` — the ω-indexed score. |
| `InverseProblemSystem.Θ h q` | `def` | `Θ(h, q) := E[φ_{h, q}(W)]` (`def:est-trae-dr-functional`). |
| `InverseProblemSystem.DualSolution S q₀` | structure | `q₀ ∈ Qbar` plus the dual moment identity `E[m_e(W; h)] = E[q₀(Z) h(X)]` for `h ∈ Hbar` (`def:est-trae-dual-solution`). |

#### `Estimation/NPIV/MixedBias.lean`

| Declaration | Statement | Description |
|---|---|---|
| `mixed_bias_identity` | `Θ(h, q) − θ₀ = E[(q₀(Z) − q(Z))(h(X) − h₀(X))]` for `h ∈ Hbar, q ∈ Qbar` and a dual solution `q₀` | The **mixed-bias / DR identity** (`prop:est-trae-mixed-bias`).  First NPIV milestone.  Pure integral-linearity proof using `m_e_sub`, the dual identity on direction `h − h₀ ∈ Hbar`, and the primal moment on `q ∈ Qbar`. |
| `Θ_h₀_eq_θ₀` | `Θ(h₀, q) = θ₀` | DR cancellation at the truth on the primal side. Specialization of `mixed_bias_identity` at `h = h₀`. |
| `Θ_q₀_eq_θ₀` | `Θ(h, q₀) = θ₀` | DR cancellation at the truth on the dual side. Specialization at `q = q₀`. |

### Phase B files

#### `Estimation/NPIV/DR/Estimator.lean`

| Declaration | Statement | Description |
|---|---|---|
| `instMeasurableSpace_𝒲/𝒳/𝒵` | scoped `instance` | Lift `S.inst𝒲/𝒳/𝒵` to typeclass-resolvable instances inside the `DR` namespace so downstream signatures use plain `Measure S.𝒲`, `IIDSample`, etc. without `@`-qualification. |
| `trae_dr_estimator S sample split h_hat q_hat n ω` | `def` | One-shot TRAE-DR estimator (`def:est-trae-dr-estimator`): empirical fold-B mean of `phiVal (h_hat n ω) (q_hat n ω) (sample.Z i ω)`. Nuisances `h_hat, q_hat` are random functions trained (in applications) on fold A. |
| `trae_dr_estimator_eq_avg_phi` | rfl-lemma exposing the fold-B average form | Reusable helper for the asymptotic-linearity decomposition. |

#### `Estimation/NPIV/DR/AsymptoticLinear.lean`

| Declaration | Statement | Description |
|---|---|---|
| `ρ₀ S q₀ w` | `def` | Oracle score `φ_{h₀, q₀}(w) − θ₀`. Influence function for the asymptotic-linearity theorem. |
| `mean_zero_of_DualSolution_mu`, `mean_zero_of_DualSolution` | `∫ ρ₀ = 0` | Derive oracle-score centering from the primal/dual moment identities. The `P_W` version transports the structural-law identity along the observation-law bridge `μ.map S.W = P_W` and consumes oracle-score measurability. |
| `TRAEDRRemainderHyps S hq₀ sample split h_hat q_hat` | structure | Bundles (i)–(iv) of `thm:est-trae-dr-al-criterion`: (i) `range` (h_hat / q_hat live in Hbar / Qbar); (ii) `mixed_bias` (`√|B(n)|` × mixed-bias integral is `o_p(1)`); (iii) `ep_remainder` (centered fold-B sum of `φ̂_n − φ_0` is `o_p(1)` after `√|B(n)|` scaling); (iv) `finite_var` (`E[ρ₀²] < ∞` under `P_W`). Mean-zero is derived by `mean_zero_of_DualSolution`, not bundled as a hypothesis. |
| `trae_dr_isAsymLinear` | `IsAsymLinear (trae_dr_estimator …) S.θ₀ (ρ₀ S q₀) sample split.foldB` | **TRAE-DR asymptotic linearity** (`thm:est-trae-dr-al-criterion`): three-line decomposition into oracle-IF main term + conditional bias (closed by `mixed_bias_identity`) + empirical-process remainder (closed by `ep_remainder`). The signature includes the standard observation-law bridge `μ.map S.W = P_W` and oracle-score measurability so the IF centering is derived rather than assumed. |
| `mixed_bias_sufficient` | dominated `bnd n ω` + `√|B(n)|·bnd = o_p(1)` ⇒ `mixed_bias` field | Forward implication abstracting over a deterministic upper bound on the conditional mixed-bias integral; the `min{‖q̂ − q₀‖·‖T(ĥ−h₀)‖, ‖T*(q̂−q₀)‖·‖ĥ−h₀‖}` form will instantiate `bnd` once the operator layer (Phase C) is in place. |

#### `Estimation/NPIV/DR/AsymptoticNormal.lean`

| Declaration | Statement | Description |
|---|---|---|
| `trae_dr_asymp_normal` | `Tendsto_dist (rescaledEstimator …) (gaussianMeasure 0 σ₀²) μ` | **TRAE-DR asymptotic normality** (`thm:est-trae-dr-asymp-normal`): direct composition of `trae_dr_isAsymLinear` with `IsAsymLinear.tendsto_normal_foldB` from `Stat/SampleSplit/PartialFoldCLT.lean`. Variance is `σ₀² := ∫ ρ₀² dP_W`, indexed at the fold-B rate `√|B(n)|`. |
| `trae_dr_studentized` | `Tendsto_dist (… / σ_hat_n) (gaussianMeasure 0 1) μ` | Studentized convergence under `σ̂_n →_p σ₀`, `σ₀ > 0`, `σ₀² = ∫ ρ₀² dP_W`. Now a thin instantiation of the generic `Tendsto_dist.div_tendsto_inProb_gaussian` (`Stat/Inference/Studentize.lean`). |
| `trae_dr_wald_coverage` | `Tendsto (μ {ω : |θ̂ − θ₀| ≤ z·σ̂/√|B|})_{n} → (gaussianMeasure 0 1)(Icc -z z)` | Wald asymptotic coverage; specialize at `z = z_{1-α/2}` for the Wald `1 − α` interval. Now a thin instantiation of the generic `Tendsto_dist.wald_coverage` (`Stat/Inference/Studentize.lean`). |

### Phase C-lite + Phase D files (rate theorem track)

#### `Estimation/NPIV/Operator.lean`

Realises the projected conditional-expectation operator `T = Π_{Q̄} ∘ E[· | σ(Z)]` concretely on the ambient `Lp ℝ 2 μ` (no pushforward boilerplate).  The single population identity `T_inner_eq_integral` is what the rate theorem consumes; `T_add` / `T_sub` are linearity helpers used by the weak-rate triangle step.  `T*` (adjoint) is **not** scaffolded — the simplified rate proof (note lines 250–384) does not need it.

| Declaration | Statement | Description |
|---|---|---|
| `OperatorSystem Ω μ` | `structure ... extends InverseProblemSystem Ω μ` | `InverseProblemSystem` plus the L² realisation: `m_Z` (σ(Z) σ-algebra coarser than `m₀`), `IsFiniteMeasure μ`, closed submodules `Hbar_L2, Qbar_L2 : Submodule ℝ (Lp ℝ 2 μ)`, an `HasOrthogonalProjection` instance for `Qbar_L2`, `MemLp 2`-witnesses for `h ∘ X` and `q ∘ Z`, and coherence (the `Lp` class lies in the closed subspace). |
| `OperatorSystem.hL2 hh`, `.qL2 hq` | `def`s | Lift `h ∈ Hbar` (resp. `q ∈ Qbar`) to its `Lp ℝ 2 μ` class using the `MemLp` witness. |
| `OperatorSystem.condExpStep f` | `def` | Conditional-expectation step `E[· | σ(Z)]` as a self-map of `Lp ℝ 2 μ`. |
| `OperatorSystem.T f` | `def` | The concrete operator `Π_{Q̄} ∘ condExpStep f` (`def:est-trae-system` step iii of the operator triple). |
| `OperatorSystem.condExpStep_sub`, `T_add`, `T_sub` | `lemma`s | ℝ-linearity helpers; `T_sub` is the workhorse for the weak-rate triangle inequality. |
| `OperatorSystem.QbarAEMeasurableMZ` | `def` (predicate) | `q ∘ Z` is `m_Z`-AEStronglyMeasurable for every `q ∈ Qbar`.  Kept as a `def` for backwards compatibility with downstream consumers; **derived** by `qbar_aestrongly_measurable_mZ` from `m_Z_eq + meas_of_Qbar`, so it is not a separate hypothesis on the structure. |
| `OperatorSystem.qbar_aestrongly_measurable_mZ` | `(S : OperatorSystem Ω μ) : S.QbarAEMeasurableMZ` | **Proved** (no extra hypothesis): combines `S.m_Z_eq : m_Z = comap (zOf ∘ W) inst𝒵` with `comap_measurable` and `meas_of_Qbar` to get `m_Z`-strong measurability of `q ∘ zOf ∘ W` for every `q ∈ Qbar`. |
| `OperatorSystem.T_inner_eq_integral` | `⟪T (hL2 hh − hL2 hh'), qL2 hf⟫_{L²(μ)} = ∫ (h(X) − h'(X)) · f(Z) dμ` | **Population identity** — the only fact about `T` the rate proof uses.  Proved via projection invisibility against `Q̄`-elements, `condExpL2` self-adjointness (consuming `qbar_aestrongly_measurable_mZ` internally — no longer takes the measurability hypothesis as an explicit argument), and `Lp` inner-product unfolding. |
| `OperatorSystem.strongNorm`, `.weakNorm` | `def`s | `strongNorm f := ‖f‖`, `weakNorm f := ‖S.T f‖`.  Sugar around the standard `Lp` norm. |

#### `Estimation/NPIV/SourceCondition.lean`

| Declaration | Statement | Description |
|---|---|---|
| `SourceCondition S β` | structure | β-source condition at `h₀` (`def:est-trae-source-condition`).  Carries witness `w₀ ∈ Hbar` and the actual spectral identity `S.hL2 S.h₀_mem = Complexification.realCFC S.Tstar_T (fun x => Real.rpow (max x 0) (β/2)) (S.hL2 w₀_mem)` (uses the real-CFC built in `Operator/Complexification.lean`). |
| `TikhonovBiasBound S β λ sc` | structure | Tikhonov bias hypothesis at level `λ` (proof sketch lines 276–303).  Bundles a Tikhonov solution `h*_λ ∈ Hbar` and a constant `C ≥ 0` with three inequalities: `strong_bias` (`‖h*_λ − h₀‖² ≤ C·‖w₀‖·λ^{min β 2}`), `weak_bias` (`‖T(h*_λ − h₀)‖² ≤ C·‖w₀‖·λ^{min(β+1) 2}`), and `strong_convexity` (the population-level inequality `λ‖ĥ − h*_λ‖² + ‖T(ĥ − h*_λ)‖² ≤ ‖T(ĥ − h₀)‖² − ‖T(h*_λ − h₀)‖² + λ(‖ĥ‖² − ‖h*_λ‖²)` for every `ĥ ∈ Hbar`).  Phase E discharges all three from `SourceCondition` via spectral calculus. |


#### `Estimation/NPIV/Primal/Estimator.lean`

| Declaration | Statement | Description |
|---|---|---|
| `TRAEClasses S` | structure | Candidate class `H ⊆ Hbar` and critic class `F ⊆ Qbar` for the primal TRAE optimisation (`def:est-trae-population-criterion`). Closure / non-emptiness fields included. |
| `innerIntegrand`, `innerObjective`, `supObjective` | `noncomputable def`s | Pointwise integrand `2{m(W;f) − h(X)f(Z)} − f(Z)² + λ h(X)²`, its fold-A empirical average, and the `iSup` over `f ∈ F` (`def:est-trae-primal`). |
| `IsTRAEPrimalEstimator S TC sample split lambda h_hat` | structure | Post-hoc property characterising the primal TRAE: `mem_H : h_hat n ω ∈ TC.H`, an optimality-style inequality `opt`, and a measurability witness.  No `argmin-sup` formalism — the rate theorem only needs the inequality form. |

#### `Estimation/NPIV/Primal/Rate.lean`

| Declaration | Statement | Description |
|---|---|---|
| `TRAERatePrimalHyps S TC sample split lambda β ζ delta h_hat is_estimator` | structure | Deterministic, paper-facing hypotheses for `thm:est-trae-rate-theorem`: source condition, Tikhonov bias bound, realisability `h*_λ ∈ H`, closedness of the critic class for residuals, mean-square continuity of `m`, almost-sure boundedness, and the paper-scale δ-floor for the fixed confidence level `ζ`, with positive constant `c > 0`: `δ_n ≥ c·√((log log(n+1) + log(1/ζ)) / n)`.  Making `ζ` a parameter avoids forcing one fixed `delta` sequence to satisfy every confidence level at once. |
| `TRAERatePrimalAbstractHyps S TC sample split lambda β ζ delta h_hat is_estimator` | structure | Extends `TRAERatePrimalHyps` with the packaged **`empirical_process_event`** field — the post-substitution-and-CR form of the localized empirical-process inequality (`SC_RHS ≤ K_ep · (R_b² + δ‖TΔ‖ + δ² + λδ‖Δ‖ + λδ²)`) on a high-probability event of mass `≥ 1 − ζ`.  This is the modular deterministic theorem input, not the paper-facing rate assumption bundle. |
| `combined_metric_algebra` | private real-arithmetic helper | AM-GM absorption: `lambda·x² + y² ≤ K_ep·(R² + δy + δ² + λδx + λδ²) → lambda·x² + y² ≤ 4(K_ep² + K_ep + 1)·(R² + δ²)`. |
| `combined_metric_rate` | `λ·Δ_strong² + Δ_weak² ≤ K · (R_b² + δ_n²)` on `Aζ` | **Combined-metric rate** (helper, deterministic).  Chains `tikhonov_bias.strong_convexity` with `empirical_process_event` and applies `combined_metric_algebra` for AM-GM absorption. |
| `strong_rate_from_combined` | `‖ĥ − h₀‖² ≤ C_strong · (δ²/λ + ‖w₀‖·λ^{min β 1})` | **Strong-metric rate** (helper, deterministic).  Triangle inequality `‖a − c‖ ≤ ‖a − b‖ + ‖b − c‖` plus drop of `Δ_weak²` from the combined inequality, plus `weak_bias` (gives `R_b²/λ ≤ C·‖w₀‖·λ^{min β 1}`) and `strong_bias` (absorbed using `λ^{min β 2} ≤ 2 · λ^{min β 1}` for `λ < 2`). |
| `weak_rate_from_combined` | `‖T(ĥ − h₀)‖² ≤ C_weak · (δ² + ‖w₀‖·λ^{min(β+1) 2})` | **Weak-metric rate** (helper, deterministic).  Triangle inequality on the weak metric using `T_add` / `norm_add_le`, plus drop of `λ·Δ_strong²` from the combined inequality, plus `weak_bias`.  `C_weak := 2K + (2K + 2)·C`. |
| `trae_primal_rate_from_empirical_process` | both rates simultaneously on a single `Aζ` of mass `≥ 1 − ζ` | Abstract TRAE primal rate theorem.  Consumes `TRAERatePrimalAbstractHyps`, so the empirical-process event is already packaged.  Stitches `combined_metric_rate` → `strong_rate_from_combined` → `weak_rate_from_combined` on a common high-probability event. |
| `trae_primal_rate` | both rates simultaneously on a single `Aζ` of mass `≥ 1 − ζ` | **Paper-facing TRAE primal rate theorem.**  Lives in `Rate.lean` for discoverability.  Consumes deterministic `TRAERatePrimalHyps`, localized regimes, and explicit absorption/small-`n` obligations; internally calls `empirical_process_event_of_absorption` to build `TRAERatePrimalAbstractHyps`, then invokes `trae_primal_rate_from_empirical_process`. |

### Status

- Phase A: **fully proved** (3/3). `mixed_bias_identity`, `Θ_h₀_eq_θ₀`, `Θ_q₀_eq_θ₀`.
- Phase B: **fully proved** (8/8). `trae_dr_isAsymLinear`, `mixed_bias_sufficient`, `trae_dr_asymp_normal`, `trae_dr_studentized`, `trae_dr_wald_coverage`.
- **Phase C-lite + Phase D**: **fully proved** (5/5).
  - `T_inner_eq_integral` (population identity for `T`): **proved** (Codex via `condExpL2` self-adjointness + projection invisibility + `Lp` inner-product unfolding).
  - `combined_metric_rate`: **proved** (Codex with hand-decomposed AM-GM helper `combined_metric_algebra`).
  - `strong_rate_from_combined`: **proved** (Codex; uses triangle in `Lp ℝ 2 μ` plus `λ^{min β 2} ≤ 2·λ^{min β 1}` case-split).
  - `weak_rate_from_combined`: **proved** (Codex; uses pre-emptively scaffolded `T_sub` / `T_add` helpers in `Operator.lean` for the weak-metric triangle inequality).
  - `trae_primal_rate` (headline): **proved** (Codex stitch).
- **Phase E** — **spectral discharge fully closed**.  Adjoint layer
  fully proved.  The pure-Hilbert-space Tikhonov layer
  (`Operator/Tikhonov.lean`) discharges the existence of the population
  minimiser and population strong convexity via Lax–Milgram on the
  closed subspace `Hbar_L2`.  The real-CFC layer
  (`Operator/Complexification.lean`) is fully closed; it provides the
  spectral API the Tikhonov bias discharge consumes.  `SourceCondition`
  now carries the *actual* real-CFC spectral identity, and
  `SpectralSourceCondition` reduces to two structural fields
  (`beta_nonneg`, `Hbar_L2_eq_top`); the closedness witness and
  nonnegative spectrum facts are derived.  The resolvent identification
  of the Lax–Milgram minimiser (`tikhonovMinimiserL2_eq_resolvent`) and
  the two Tikhonov bias bounds (`strong_bias`, `weak_bias`) are
  **proved** theorems on
  `SpectralSourceCondition` (Codex Phase E2 fill, using
  `Complexification.realCFC_*`).  `tikhonov_bias_from_spectral`
  packages these plus `tikhonovMinimiserL2_strong_convexity` into a
  fully-derived `TikhonovBiasBound`.  Discharge of
  `empirical_process_event` is **fully closed** (no sorries): the
  localized-empirical-process bookkeeping is supplied by the
  `LocalizedRegimeBundle` hypotheses (law / realizability / boundedness /
  diameter / peeling-slack / critical-radius), exactly as the paper
  assumes `δ_n` upper-bounds the critical radii, and the master
  concentration inequality `localized_uniform_deviation_sharp`
  (`Stat/Concentration/UniformDeviationLocalized.lean`) is itself proved.
  See files below.

### Phase E files

#### `Estimation/NPIV/Operator/Adjoint.lean` (~110 lines, fully proved)

Bundles the bare-function operator `OperatorSystem.T : Lp ℝ 2 μ → Lp ℝ 2 μ`
as a continuous linear map and exposes the adjoint `T†` and the self-adjoint
composite `T†T`.

| Declaration | Statement | Description |
|---|---|---|
| `OperatorSystem.condExpStepL` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` | Conditional-expectation step `E[· \| σ(Z)]` as a CLM, via `(lpMeas).subtypeL.comp (condExpL2 ℝ ℝ S.m_Z_le)`. |
| `OperatorSystem.Tlin` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` | Bundled CLM version of `S.T`: `Qbar_L2.starProjection.comp condExpStepL`. |
| `OperatorSystem.Tlin_apply` | `S.Tlin f = S.T f` | Coherence: bundled CLM agrees with the bare-function `T`. **Proved (`rfl`).** |
| `OperatorSystem.Tadjoint` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` | Adjoint `T† := ContinuousLinearMap.adjoint S.Tlin`. |
| `OperatorSystem.Tstar_T` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` | Self-adjoint, PSD composite `T† T = Tadjoint.comp Tlin`. The operator the β-source condition refers to. |
| `OperatorSystem.Tstar_T_isSelfAdjoint` | `IsSelfAdjoint S.Tstar_T` | Self-adjointness of `T† T` via `IsSelfAdjoint.star_mul_self` after identifying adjoint with `star`. **Proved.** |

#### `Estimation/NPIV/Operator/Tikhonov.lean` (~200 lines, sorry-only scaffold)

Pure-real-Hilbert-space layer for the Tikhonov minimisation problem on
the closed subspace `Hbar_L2`.  Discharges the existence of the
population minimiser and the population strong-convexity inequality via
Mathlib's Lax–Milgram theorem (`IsCoercive.continuousLinearEquivOfBilin`)
— **no continuous functional calculus needed**.  These were previously
baked fields of `SpectralSourceCondition`.

| Declaration | Statement | Description |
|---|---|---|
| `OperatorSystem.tikhonovBilin S λ` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ →L[ℝ] ℝ` | Bilinear form `(u, v) ↦ ⟨T u, T v⟩ + λ ⟨u, v⟩` on the ambient `Lp ℝ 2 μ`. |
| `tikhonovBilin_apply`, `tikhonovBilin_symm` | unfold + symmetry | Pointwise formula and symmetry of the bilinear form. |
| `OperatorSystem.tikhonovBilinSub S λ` | `S.Hbar_L2 →L[ℝ] S.Hbar_L2 →L[ℝ] ℝ` | The bilinear form restricted to the closed subspace `Hbar_L2` (via `Submodule.subtypeL` pre/post-composition).  Argument fed into Lax–Milgram. |
| `tikhonovBilinSub_apply` | unfold | Pointwise formula linking the restricted form back to `tikhonovBilin`. |
| `tikhonovBilinSub_isCoercive` | `IsCoercive (S.tikhonovBilinSub λ)` for `0 < λ` | Coercivity with constant `λ`, since `bilin u u = ‖T u‖² + λ ‖u‖² ≥ λ ‖u‖²`. |
| `OperatorSystem.tikhonovTargetSub S` | `S.Hbar_L2 →L[ℝ] ℝ` | Target functional `v ↦ ⟨T h₀, T v⟩` on `Hbar_L2`, used in the variational identity. |
| `tikhonovTargetSub_apply` | unfold | Pointwise formula for the target functional. |
| `OperatorSystem.tikhonovMinimiserL2 S λ` | `Lp ℝ 2 μ` | Population Tikhonov minimiser at level `λ`, defined for `0 < λ` by Lax–Milgram applied to `tikhonovBilinSub` and `tikhonovTargetSub`; arbitrary value (`0`) for `λ ≤ 0`. |
| `tikhonovMinimiserL2_mem` | `∈ S.Hbar_L2` | The minimiser lies in the closed candidate subspace. |
| `tikhonovMinimiserL2_optimality` | `⟨T h*, T v⟩ + λ⟨h*, v⟩ = ⟨T h₀, T v⟩` for `v ∈ Hbar_L2` | Variational identity (Lax–Milgram statement, restated using the L²-level inner product). |
| `tikhonovMinimiserL2_strong_convexity` | `λ‖ĥ−h*‖² + ‖T(ĥ−h*)‖² ≤ ‖T(ĥ−h₀)‖² − ‖T(h*−h₀)‖² + λ(‖ĥ‖² − ‖h*‖²)` | **Population strong convexity at the minimiser.**  Pure Hilbert-space Taylor-expansion identity at the minimiser; the first-order term cancels via `tikhonovMinimiserL2_optimality`. |

#### `Estimation/NPIV/Operator/Complexification.lean` (~1090 lines, fully proved)

Real continuous functional calculus on `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` via
complexification.  Embeds the real `L²` isometrically into the complex
`L²` (`ιLp` / `reLp` / `imLp`), lifts any real CLM `A` to a complex CLM
`complexLift A` whose action on the real subspace recovers `A`, and
defines `realCFC A f := reLp ∘ cfc (f ∘ Complex.re) (complexLift A) ∘ ιLp`.
Provides the full API the spectral discharge consumes (`realCFC_apply`,
`realCFC_id`, `realCFC_mul`, `realCFC_resolvent_mul_self`,
`realCFC_isSelfAdjoint`, `realCFC_norm_le`).

#### `Estimation/NPIV/Operator/SpectralCalculus.lean` (~1230 lines, fully proved)

Spectral source condition + Tikhonov bias discharge using the real-CFC
API from `Complexification.lean`.  `SourceCondition` (in `../SourceCondition.lean`)
carries the actual spectral identity, so `SpectralSourceCondition`
only adds the structural assumptions the spectral argument needs.
The two bias bounds and the resolvent identification of the Lax–Milgram
minimiser are now theorems on `SpectralSourceCondition`, all proved.

| Declaration | Statement | Description |
|---|---|---|
| `SpectralSourceCondition S β` | `structure ... extends SourceCondition S β` | Strengthens `SourceCondition` with: `beta_nonneg : 0 ≤ β` and `Hbar_L2_eq_top : S.Hbar_L2 = ⊤` (full-space candidate class).  The closedness witness `Hbar_L2_hasProj` is derived from `Hbar_L2_eq_top`, and `Tstar_T_spectrum_nonneg` is derived from positivity of `T†T`. |
| `SpectralSourceCondition.sourceSymbol β` / `continuous_sourceSymbol` | `ℝ → ℝ` / `Continuous` | The continuous extension `x ↦ Real.rpow (max x 0) (β/2)` of `x^{β/2}` to all of ℝ. |
| `SpectralSourceCondition.spectralPower sc` | `Lp ℝ 2 μ →L[ℝ] Lp ℝ 2 μ` | Defined as `Complexification.realCFC S.Tstar_T (sourceSymbol β)`; the operator `(T†T)^{β/2}` consumed by the spectral identity. |
| `SpectralSourceCondition.biasConst sc` | `ℝ` | Uniform constant `Real.rpow (max 1 (‖S.Tstar_T‖+1)) β` absorbing both the small-β and large-β regimes of the sup-on-spectrum analysis. |
| `tikhonovMinimiserL2_eq_resolvent` | `S.tikhonovMinimiserL2 λ = realCFC S.Tstar_T (x ↦ x/(λ+x)) (S.hL2 S.h₀_mem)` | Identifies the Lax–Milgram minimiser with the resolvent expression `(T†T+λI)⁻¹ T†T h₀`.  Uses `Hbar_L2_eq_top` to lift the variational identity to the full ambient space.  **Proved** (Codex; uses private helpers `realCFC_const_add` and `resolvent_left_inverse` built on `realCFC_resolvent_mul_self`). |
| `SpectralSourceCondition.strong_bias` | `‖h*_λ − h₀‖² ≤ biasConst · ‖w₀‖² · λ^{min β 2}` | Strong-metric Tikhonov bias, derived spectrally from the resolvent identity + `realCFC_norm_le` + sup-on-spectrum lemmas (`residual_symbol_bound_small/_large` private helpers handle β≤2 / β>2 cases).  **Proved** (Codex). |
| `SpectralSourceCondition.weak_bias` | `‖T(h*_λ − h₀)‖² ≤ biasConst · ‖w₀‖² · λ^{min(β+1) 2}` | Weak-metric Tikhonov bias.  Reduces `‖T u‖² = ⟨T†T u, u⟩` (via `Tstar_T`, `adjoint_inner_left`), expresses it as `⟨realCFC A (x · f²) w, w⟩` via `realCFC_mul` + self-adjointness, then bounds via Cauchy–Schwarz + `realCFC_norm_le` with the tight sup `weak_residual_bound` (β≤1 / β>1 case split).  **Proved** (Codex). |
| `TikhonovPullback S β λ sc` | `structure` | Function-level pullback (unchanged):  L² class of the function-level minimiser equals `tikhonovMinimiserL2 S λ`. |
| `tikhonov_bias_from_spectral S β λ λ_pos sc pb` | `TikhonovBiasBound S β λ sc.toSourceCondition` | Packages the spectral `strong_bias` / `weak_bias` theorems plus `tikhonovMinimiserL2_strong_convexity` into a `TikhonovBiasBound` with `C := sc.biasConst · ‖w₀‖`.  **Fully proved** (no sorries). |

#### `Estimation/NPIV/Primal/EmpiricalProcessEvent/` — localized EP discharge

Split implementation of the discharge of
`TRAERatePrimalAbstractHyps.empirical_process_event` from four localized-regime
instances on `star(H · F), star(m ∘ F), star(F), star(H)`.  The umbrella file
`Primal/EmpiricalProcessEvent.lean` re-exports the final discharge module.

| File | Contents | Description |
|---|---|---|
| `Algebra.lean` | `young_cross_envelope` | Deterministic Young / AM-GM envelope converting the localized additive cross terms into the `R² + δ·w + δ² + κ²` shape used by the EP RHS. |
| `Regime.lean` | `LocalizedRegimeBundle`, `LocalizedRegimes` | Bundles one localized deviation regime per abstract function class, including nonnegativity of the abstract localization norm, plus law, realizability, boundedness, closedness, diameter, peeling slack, and interpretation fields connecting the abstract classes to the concrete NPIV losses. |
| `Core.lean` | imports `Regime` | Small compatibility umbrella for the algebra and regime declarations. |
| `LocalizedEventsBase.lean` | `integral_comp_law_W`, `localized_omega_event_sharp_for_bundle` | Shared law-change helper for rewriting integrals under `P_W` back to integrals over `Ω`, plus the NPIV-facing wrapper around `localized_uniform_deviation_sharp` for a single `LocalizedRegimeBundle`. The wrapper pulls the product-space sharp event back along the IID sample, uses the bundle's localization-norm nonnegativity, keeps the diameter obligation per concrete index, and rewrites the population mean through `regime.law_W`. |
| `LocalizedEventH.lean` | `localized_omega_event_for_H` | Fixed-diameter Ω-side localized deviation event for squared candidate differences, used by the centred regulariser bound. |
| `LocalizedEventHF.lean` | `localized_omega_event_for_HF`, `localized_omega_event_for_HF_pair`, `localized_omega_event_for_HF_pair_peeled` | Ω-side localized deviation events for the product class `H · F`, including the peeled pair-gap Foster interpretation.  The peeled helper now delegates the dyadic peeling to `localized_omega_event_sharp_for_bundle`. |
| `LocalizedEventMF.lean` | `localized_omega_event_for_mF`, `localized_omega_event_for_mF_pair_peeled` | Ω-side localized deviation events for the moment class `m ∘ F`, including the peeled critic-pair form.  The peeled helper now delegates the dyadic peeling to `localized_omega_event_sharp_for_bundle`. |
| `LocalizedEventF.lean` | `localized_omega_event_for_F`, `localized_omega_event_for_F_pair_peeled` | Ω-side localized deviation events for the squared critic class `F`, including the peeled critic-pair form.  The peeled helper now delegates to `localized_omega_event_sharp_for_bundle` and states the leading term at the public `δ_n` scale. |
| `LocalizedEvents.lean` | imports the localized event files | Small compatibility umbrella for all Ω-side localized deviation events. |
| `EPMasterEvent.lean` | `population_closedness_critic_gap_eq`, `empirical_critic_argmax_localized`, `empirical_critic_argmax_localized_from_components`, `ep_master_event_from_localized` | Objective-level EP master event plus the deterministic Foster argmax bridge. `population_closedness_critic_gap_eq` proves the population quadratic-curvature identity at a closedness witness; `empirical_critic_argmax_localized` turns empirical critic optimality plus a one-sided loss-deviation bound into an L² localization bound for the empirical sup-attainer; `empirical_critic_argmax_localized_from_components` packages that bridge from the existing single-critic `m∘F`, `H·F`, and `F²` deviations without adding critic linearity assumptions. `ep_master_event_from_localized` exposes the raw objective-level localized EP modulus before final δ-schedule absorption. |
| `EPPerN.lean` | `ep_pop_inner_at_closedness_witness`, `ep_per_n_inequality_from_deviations`, `ep_inequality_from_localized` | Per-horizon deterministic EP bridge and public EP inequality. `ep_pop_inner_at_closedness_witness` is the population-side identity `2·E[m·f] − 2·E[h·f] − E[f²] = weak(h - h₀)²` at a closedness witness. `ep_per_n_inequality_from_deviations` consumes the raw objective-level localized modulus and removes the empirical sup-objective excess using estimator optimality. `ep_inequality_from_localized` extracts the witness payload from `ep_master_event_from_localized` and feeds it through `ep_per_n_inequality_from_deviations`; `Discharge.lean` then absorbs the explicit rate into the final TRAE population shape. |
| `EPInequality.lean` | imports `EPPerN` | Small compatibility umbrella for the explicit empirical-process inequality. |
| `Regulariser.lean` | `measure_iInter_nat_ge_one_sub_tsum_of_ge`, `centred_regulariser_bound_from_localized` | All-`n` centred empirical regulariser bound obtained from the H-side localized event. |
| `Discharge.lean` | `empirical_process_event_from_localized`, `explicitRate`, `populationShape`, `empirical_process_event_of_absorption` | Combines the EP inequality and regulariser bound, then exposes the `Rate.lean`-shaped empirical-process event conditional on the explicit-rate absorption hypothesis.  The paper-facing theorem using it is `trae_primal_rate` in `Rate.lean`. |

---

## 13W. `Causalean/Panel/Weighted/` — Weighted-Finite-Index Substrate (FWL / WLS Core)

**FWL/WLS-centric framing.** Estimand-characterization papers (Sloczynski,
Goodman-Bacon, Sun-Abraham, BJS, DCDH, MTW, Angrist-Imbens, Wooldridge, …)
all express their estimators as `θ̂ = ∑_r ω_r · τ_r`; those `ω_r` are the
implicit FWL / WLS regression weights this substrate makes explicit. The
eight files below build, in order, a finite weighted index (`WeightedSupport
R`), its weighted inner product, the nuisance-subspace projection, the WLS
optimality theorem, the FWL identity, plus reusable indicator-span /
normalized-weight / additive-span subspace constructions. Both `Panel/`
(via `Cells I T := WeightedSupport (I × T)`) and
`Panel/EstimandCharacterization/` (via partition-cell, ordered-IV, response-type,
or cohort-period instantiations of `R`) are thin facades over this layer.

### 13W.a `Weighted/Support.lean`

The substrate: a finite index `R` carrying weights `ω_r ≥ 0` (strictly
positive on the observed subset) summing to 1. Generalizes `Panel/Cells.lean`.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport R` | `structure` over `[Fintype R] [DecidableEq R]` | Bundles `observed : Finset R` (nonempty), `weight : R → ℝ`, `weight_pos`, `weight_zero_off`, `weight_sum_one`. |
| `WeightedSupport.weight_nonneg` | `0 ≤ c.weight r` | Pointwise nonnegativity. **Proved.** |
| `WeightedSupport.sum_weight_univ` | `∑ r, c.weight r = ∑ r ∈ c.observed, c.weight r` | Sum over `Finset.univ` reduces to sum over `observed`. **Proved.** |
| `WeightedSupport.sum_weight` | `∑ r ∈ c.observed, c.weight r = 1` | Restated normalization. **Proved.** |
| `WeightedSupport.sum_weight_univ_eq_one` | `∑ r, c.weight r = 1` | Normalization over `Finset.univ`. **Proved.** |

### 13W.b `Weighted/InnerProduct.lean`

Weighted inner product `⟨A, B⟩_ω := ∑_{r∈observed} ω_r · A_r · B_r` and its
matrix-valued lift to vector arrays `Fin K → (R → ℝ)`.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.ip` | `(c : WeightedSupport R) → (R → ℝ) → (R → ℝ) → ℝ` | Scalar weighted inner product `c.ip A B = ∑ r ∈ observed, ω_r · A r · B r`. |
| `WeightedSupport.ip_symm` | `c.ip A B = c.ip B A` | Symmetry. **Proved.** |
| `WeightedSupport.ip_add_left` / `ip_add_right` | bilinearity | Additivity in each argument. **Proved.** |
| `WeightedSupport.ip_smul_left` / `ip_smul_right` | `c.ip (s • A) B = s · c.ip A B` (and sym.) | Scalar homogeneity. **Proved.** |
| `WeightedSupport.ip_self_summand_nonneg` | `∀ r ∈ observed, 0 ≤ ω_r · A r · A r` | Per-summand nonnegativity. **Proved.** |
| `WeightedSupport.ip_self_nonneg` | `0 ≤ c.ip A A` | Positivity (semi-definite). **Proved.** |
| `WeightedSupport.ip_self_eq_zero_iff` | `c.ip A A = 0 ↔ ∀ r ∈ observed, A r = 0` | Definiteness on the observed support. **Proved.** |
| `WeightedSupport.ipMat` | `(Fin K → R → ℝ) → (Fin K → R → ℝ) → Matrix (Fin K) (Fin K) ℝ` | Entrywise inner product `(c.ipMat A B) j k = c.ip (A j) (B k)`. |
| `WeightedSupport.ipMat_transpose` | `(c.ipMat A B).transpose = c.ipMat B A` | Transposed symmetry. **Proved.** |
| `WeightedSupport.ipMat_add_left` / `ipMat_add_right` | entrywise bilinearity | Additivity in each tuple argument. **Proved.** |
| `WeightedSupport.ipMat_smul_left` / `ipMat_smul_right` | `c.ipMat (s • A) B = s • c.ipMat A B` (and sym.) | Scalar homogeneity entrywise. **Proved.** |

### 13W.c `Weighted/Subspace.lean`

Weighted orthogonal projection onto a nuisance subspace `H ⊆ R → ℝ`, and the
residual maker `M_H := id − P_H`. Existence is via
`Causalean.Mathlib.SemiInnerProjection.exists_orthogonalProjection_of_posSemidef`
(since `c.ip` is only positive-semidefinite, not definite, off the observed
support).

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.weighted_orthogonal_projection_exists` | `∃ P : (R→ℝ) →ₗ[ℝ] (R→ℝ), …` | Existence of the weighted projection onto `H`. **Proved.** |
| `WeightedSupport.proj` | `(c : WeightedSupport R) → Submodule ℝ (R → ℝ) → (R → ℝ) →ₗ[ℝ] (R → ℝ)` | Chosen weighted projection `P_H`. `noncomputable`. |
| `WeightedSupport.proj_mem` / `proj_orthogonal` | membership / orthogonality | Defining properties of `proj`. **Proved.** |
| `WeightedSupport.proj_apply_of_mem` | `X ∈ H → proj X = X` on observed | Projection is identity on its image, on the observed support. **Proved.** |
| `WeightedSupport.proj_apply_eq_of_mem_orthogonal` | any orthogonal nuisance candidate matches `proj X` on observed | Uniqueness of the projection on the observed support. **Proved.** |
| `WeightedSupport.proj_idem_apply` | `proj (proj X) = proj X` on observed | Idempotence on observed cells. **Proved.** |
| `WeightedSupport.residualize` | `Submodule ℝ (R → ℝ) → (R → ℝ) →ₗ[ℝ] (R → ℝ)` | Residual maker `M_H = id − P_H`. `noncomputable`. |
| `WeightedSupport.tildeX` | scalar residualization | `X̃ := M_H X`. `noncomputable`. |
| `WeightedSupport.tildeXVec` | column-wise residualization | `Fin K → (R → ℝ)` tuple of residualized columns. `noncomputable`. |
| `WeightedSupport.residualize_in_orthogonal` | `c.ip (c.tildeX H X) h = 0` for `h ∈ H` | Residuals are `H`-orthogonal. **Proved.** |
| `WeightedSupport.residualize_self_of_mem` | `X ∈ H → tildeX X = 0` on observed | Nuisance-space arrays residualize to zero on observed cells. **Proved.** |
| `WeightedSupport.residualize_idem_apply` | `residualize (residualize X) = residualize X` on observed | Residual maker idempotent on observed cells. **Proved.** |

### 13W.d `Weighted/WLS.lean`

Headline: **weighted projections ARE the WLS solutions.** Characterizes
`proj` / `residualize` as the unique-modulo-off-support solutions to the
weighted least-squares minimization problem.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.proj_eq_argmin` | `∀ h ∈ H, c.ip (X − proj X) (X − proj X) ≤ c.ip (X − h) (X − h)` | The weighted projection minimizes the WLS objective `⟨X − h, X − h⟩_ω` over `h ∈ H`. **Proved.** |
| `WeightedSupport.residualize_orth_iff_argmin` | residual-orthogonality at `p ∈ H` ↔ WLS-optimality at `p` | The WLS first-order condition: orthogonality of `X − p` to all of `H` is equivalent to `p` being the WLS minimizer. **Proved.** |

### 13W.e `Weighted/FWL.lean`

The **headline of the substrate**: the Frisch–Waugh–Lovell identity. This is
the **finite-cell** FWL; the parallel measure-theoretic / L² FWL lives at
`Causalean/Panel/Analysis/Residualization.lean`.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.Q_XX` | `WeightedSupport R → (Fin K → R → ℝ) → Submodule ℝ (R → ℝ) → Matrix (Fin K) (Fin K) ℝ` | Residualized Gram matrix `⟨X̃, X̃⟩_ω`. `noncomputable`. |
| `WeightedSupport.rhsVec` | `… → (R → ℝ) → Fin K → ℝ` | Residualized RHS / score vector `⟨X̃, Y⟩_ω`. `noncomputable`. |
| `WeightedSupport.thetaHat` | `… → (R → ℝ) → Fin K → ℝ` | FWL coefficient `Q_XX⁻¹ *ᵥ rhsVec`. `noncomputable`. |
| `WeightedSupport.RankCondition` | `IsUnit (Q_XX c X H).det` | Nonsingularity of `Q_XX` (finite algebraic form of `Q_XX ≻ 0`). |
| `WeightedSupport.Q_XX_mulVec_thetaHat` | `RankCondition → Q_XX *ᵥ thetaHat = rhsVec` | `thetaHat` solves the residualized normal equations. **Proved.** |
| `WeightedSupport.fwl_identity` | long-regression β = residualized-short-regression θ̂ | The joint minimizer `β` of the long WLS objective over `(β, α ∈ H)` equals `thetaHat` of the H-residualized regressors. **Proved.** |

### 13W.f `Weighted/IndicatorSpan.lean`

Finite-cell partition algebra: singleton-cell indicators on a classifier
`G : R → 𝒢`, the linear span they generate, and partition-cell statistics
(cell mass, cell mean). Two-axis variant for cohort+period nuisance classes.
This file ports the **finite-cell** part of the archived
`Panel/CellBridge.lean`; the L² / measure-theoretic part
lives in `Causalean/Panel/Analysis/Residualization.lean`.

| Declaration | Signature | Description |
|---|---|---|
| `Weighted.cellIndicator` | `(G : R → 𝒢) → 𝒢 → R → ℝ` | Real-valued singleton-cell indicator `r ↦ if G r = g then 1 else 0`. |
| `Weighted.indicatorSpan` | `(G : R → 𝒢) → Submodule ℝ (R → ℝ)` | Linear span of the singleton indicators `{cellIndicator G g}_{g : 𝒢}`. |
| `Weighted.mem_indicatorSpan_iff` | `h ∈ indicatorSpan G ↔ ∃ c : 𝒢 → ℝ, h = ∑ g, c g • cellIndicator G g` | Characterization of membership. **Proved.** |
| `Weighted.cellIndicator_mem_indicatorSpan` | `cellIndicator G g ∈ indicatorSpan G` | Generators belong to the span. **Proved.** |
| `Weighted.twoAxisIndicatorSpan` | `(G : R → 𝒢) → (T : R → 𝒯) → Submodule ℝ (R → ℝ)` | Two-axis indicator span: sup of `indicatorSpan G` and `indicatorSpan T`. Used for cohort+period nuisance classes (Goodman-Bacon, Sun-Abraham). |
| `Weighted.indicatorSpan_le_twoAxisIndicatorSpan` / `_right` | left and right injection | Each one-axis span sits inside the two-axis span. **Proved.** |
| `WeightedSupport.cellMass` | `(c : WeightedSupport R) → (G : R → 𝒢) → 𝒢 → ℝ` | Cell mass `∑_{r ∈ observed, G r = g} ω_r`. |
| `WeightedSupport.cellMass_nonneg` | `0 ≤ c.cellMass G g` | Nonnegativity. **Proved.** |
| `WeightedSupport.cellMean` | `(c : WeightedSupport R) → (F : R → ℝ) → (G : R → 𝒢) → 𝒢 → ℝ` | Totalized cell mean of `F` on cell `g`. `noncomputable`. |
| `WeightedSupport.jointCellMass` | `(c : WeightedSupport R) → (G : R → 𝒢) → (T : R → 𝒯) → 𝒢 → 𝒯 → ℝ` | Joint cell mass `∑_{r : G r = g, T r = k} ω_r`. |

### 13W.g `Weighted/NormalizedWeights.lean`

Paper-agnostic finite weighted-sum algebra: normalized weights `a_i / ∑ a_j`
and weighted centered covariance / variance identities. Consumed by
estimand-characterization papers when re-expressing a regression coefficient
`θ̂` as `∑_r ω_r τ_r`.

| Declaration | Signature | Description |
|---|---|---|
| `NormalizedWeights.normalizedWeight` | `(ι → ℝ) → ι → ℝ` | Generic normalized weight `a_i / ∑ k, a k`. `noncomputable`. |
| `NormalizedWeights.normalizedWeight_nonneg` | `(∀ i, 0 ≤ a i) → 0 < ∑ a → 0 ≤ normalizedWeight a i` | Nonnegativity from nonneg raw weights and positive total mass. **Proved.** |
| `NormalizedWeights.sum_normalizedWeight_eq_one` | `0 < ∑ a → ∑ i, normalizedWeight a i = 1` | Normalized weights sum to one. **Proved.** |
| `NormalizedWeights.weighted_center_cov` | `(∑ p = 1) → ∑ p (x − x̄)(y − ȳ) = ½ ∑∑ p p' (x − x')(y − y')` | Weighted centered covariance equals one half the ordered pairwise cross-product average. **Proved.** |
| `NormalizedWeights.weighted_center_var` | `(∑ p = 1) → ∑ p (x − x̄)² = ½ ∑∑ p p' (x − x')²` | Weighted centered variance equals one half the ordered pairwise squared-gap average. **Proved.** |

### 13W.h `Weighted/AdditiveSpan.lean`

Generic two-axis additive subspace: for a generic `R` with two projections
`f₁ : R → A` and `f₂ : R → B`, the additive span is `{ h | ∃ a b, h r = a (f₁
r) + b (f₂ r) }`. Specialized to `R = I × T`, `f₁ = Prod.fst`, `f₂ =
Prod.snd`, this is the two-way fixed-effect subspace `H_twfe` used in
`Panel/FixedEffect.lean` and across the estimand-characterization layer
(Goodman-Bacon, Sun-Abraham, DCDH, BJS, …).

| Declaration | Signature | Description |
|---|---|---|
| `AdditiveSpan` | `(f₁ : R → A) → (f₂ : R → B) → Submodule ℝ (R → ℝ)` | Generic two-axis additive span. |
| `AdditiveSpan.mem_iff` | `h ∈ AdditiveSpan f₁ f₂ ↔ ∃ a b, ∀ r, h r = a (f₁ r) + b (f₂ r)` | Characterization of membership. **Proved.** |
| `AdditiveSpan.const_mem` | constants lie in the additive span | `∀ c₀ : ℝ, (fun _ => c₀) ∈ AdditiveSpan f₁ f₂`. **Proved.** |
| `AdditiveSpan.finiteDimensional` | `instance [Finite R] : Module.Finite ℝ (AdditiveSpan f₁ f₂)` | Finite-dimensionality. **Proved.** |
| `twoAxisAdditiveSpan` | `(I T : Type*) → Submodule ℝ ((I × T) → ℝ) := AdditiveSpan Prod.fst Prod.snd` | Specialization to `R = I × T`. |
| `IsUnitTimeAdditive` | `(Unit → Time → ℝ) → Prop` | Back-compat membership predicate matching the original `EstimandCharacterization.AdditiveSpan` API. |

### 13W.i `Weighted/OfProbabilityMeasure.lean`

Bridge (audit helper **M1**) connecting the finite weighted-support tower to a
probability measure on a finite measurable space. For a probability measure `μ`
on a `Fintype` `R` with measurable singletons, the point masses
`ω_r := (μ {r}).toReal` form a `WeightedSupport R`, and its weighted inner
product equals the `L²(μ)` integral pairing. Lets the measure-theoretic
cell-bridge layer (`Residualization`/`CellBridge`) and the finite FWL tower be
used interchangeably on a finite probability space.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.ofProbabilityMeasure` | `(μ : Measure R) [IsProbabilityMeasure μ] → WeightedSupport R` | Weighted support induced by `μ`: weight `r ↦ (μ {r}).toReal`, observed set = positive-mass support. `noncomputable`. **Proved** (all structure fields discharged). |
| `WeightedSupport.ofProbabilityMeasure_weight` | `(ofProbabilityMeasure μ).weight r = (μ {r}).toReal` | Weight unfolds to the point mass. `@[simp]`. **Proved.** |
| `WeightedSupport.ip_eq_integral` | `(ofProbabilityMeasure μ).ip A B = ∫ r, A r * B r ∂μ` | **M1 bridge**: the induced weighted inner product is the `L²(μ)` integral pairing. **Proved.** |

### 13W.j `Weighted/ScalarFWL.lean`

Scalar (`K = 1`) Frisch–Waugh–Lovell from the weighted **normal equations**
(audit helper **M2**) — the finite weighted-support counterpart of the
measure-theoretic `Causalean.PO.residualizedCoefficient_eq_of_normalEqs`. Bridges
the normal-equation inputs estimand papers produce to the residualized-coefficient
ratio, without going through the joint-minimization `fwl_identity`.

| Declaration | Signature | Description |
|---|---|---|
| `WeightedSupport.ip_tildeX_self` | `c.ip (c.tildeX H X) X = c.ip (c.tildeX H X) (c.tildeX H X)` | The residualized regressor pairs identically with `X` and its residual (projection part is orthogonal). **Proved.** |
| `WeightedSupport.scalar_fwl_of_normalEqs` | `α ∈ H → 0 < ⟨X̃,X̃⟩_ω → ⟨Y−βX−α, X⟩_ω = 0 → (∀ h ∈ H, ⟨Y−βX−α, h⟩_ω = 0) → β = ⟨X̃,Y⟩_ω / ⟨X̃,X̃⟩_ω` | **M2**: scalar FWL from the two weighted normal equations; derives the coefficient ratio (does not assume it). **Proved.** |

---

## 9i0a-FWLInstanceL2. `Panel/FWLInstanceL2.lean` — L²(μ) instance of the abstract FWL

Instantiates the abstract, panel-agnostic FWL development of `Mathlib/FWL.lean`
(§10f) on the genuine inner-product space `Lp ℝ 2 μ`, with a finite-dimensional
nuisance subspace `H`. This is the **measure-theoretic instance** of the canonical
projection layer (see
`doc/basic_concepts/po/estimand_characterization/INTERFACE_canonical_projection.md`):
the cell-indicator nuisance classes of the conditional-mean estimand papers
(Goodman-Bacon, Sun-Abraham, MTW, Słoczyński) are finite-dimensional subspaces of
`Lp ℝ 2 μ`, so the abstract FWL coefficient, residualized normal equations, and
least-squares optimality apply directly. **This file is what makes `Mathlib/FWL.lean`
a live dependency rather than an orphan.** (Re-proving the raw-function
`Causalean.PO.residualizedCoefficient_eq_of_normalEqs` *through* this instance — the
full de-duplication of the measure-theoretic FWL tower — is a documented follow-up
requiring the raw-function ↔ `Lp`-quotient coercion bridge.)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `FWLInstanceL2.hasOrthogonalProjection_of_finiteDimensional` | `instance (H : Submodule ℝ (Lp ℝ 2 μ)) [FiniteDimensional ℝ H] : H.HasOrthogonalProjection` | Finite-dim subspaces of `Lp ℝ 2 μ` are complete, hence admit an orthogonal projection (free, via `HasOrthogonalProjection.ofCompleteSpace`). | **Proved** |
| `FWLInstanceL2.inner_eq_integral` | `inner ℝ f g = ∫ a, f a * g a ∂μ` | L² inner product = population covariance: bridges the abstract FWL inner products to the integrals the papers manipulate. | **Proved** |
| `FWLInstanceL2.gramResid_mulVec_fwlCoef` | `IsUnit (gramResid H X).det → gramResid H X *ᵥ fwlCoef H X Y = residInnerVec H X Y` | Residualized normal equations on `L²(μ)` (specialization of the abstract lemma). | **Proved** |
| `FWLInstanceL2.fwlCoef_original_minimizes` | `IsUnit … → h ∈ H → originalObjective … (fwlCoef …) (P_H …) ≤ originalObjective X Y β h` | FWL least-squares optimality on `L²(μ)`. | **Proved** |
| `FWLInstanceL2.fwlCoef_eq_of_original_minimizer` | minimizer hypothesis `→ β = fwlCoef H X Y` | β-block uniqueness on `L²(μ)`. | **Proved** |

---

## 13sa. `Estimation/MinimaxATE/` — Structure-agnostic ATE optimality (Jin–Syrgkanis 2024)

> **Entry point:** `Estimation/MinimaxATE.lean` (the roll-up beside the folder) imports the
> whole development and carries a docstring index of the headline theorems. Lower bounds:
> `minimax_lower_bound` (base, `ChiSquaredCore.lean`), `…_gen`/`…_var`/`…_continuous`,
> `parametric_lower_bound`. Achievability + capstone: `aipw_minimaxMiss_le`,
> `aipw_attains_minimax_rate` (both `Optimality.lean`).

Causal layer for the **converse** direction of doubly-robust ATE estimation: the
structure-agnostic minimax lower bound showing the DR/AIPW product-bias rate
`√εg·√εm` cannot be beaten (Jin–Syrgkanis 2024, *Structure-agnostic Optimality of
Doubly Robust Learning for Treatment Effect Estimation*, arXiv:2402.14264). Built
on the causal-agnostic `Stat/Minimax/` Le Cam layer (§12w). The paper's
construction makes nuisances piecewise-constant on `M = 2^m` cells of `[0,1]^K`,
so we collapse each cell to a point and use a **finite covariate** `X : Fin M`
(uniform), `D Y : Bool` — all measures finite PMFs.

**Scope of the finite-`X` model (read before citing as "the paper's theorem").**
The paper's covariate is *continuous* (`X` uniform on `[0,1]^K`); only the
nuisances are cell-constant, so within a cell the actual position of `X` is still
observed. Our `X : Fin M` observes *only the cell index* — a strictly more
collapsed object. This loses nothing of the **research content**: the
perturbation construction, the `√εg·√εm` ATE gap, and the Ingster χ² second-moment
bound never touch the within-cell continuum (the within-cell position is
*ancillary* — its conditional law is cell-uniform for every DGP in the class). So
the finite-`X` and continuous-`X`-with-cell-constant-nuisances models are
statistically equivalent for this functional, and a finite-`X` lower bound implies
the continuous-`X` lower bound by a **soft ancillarity reduction** (tensor both
hypotheses with the common within-cell law; the ancillary factor cancels in the
χ²). That reduction is now **formalized** in `ContinuousX.lean` (§13sa.6):
`two_point_lower_bound_continuous` proves the same `1/4` two-point bound
for an arbitrary within-cell ancillary space `(W, ρ)`.

The model covariate is kept as a general finite type `C` (so `Model/Witness/Bump`
read `C`, instantiated to `C = Fin K × Bool` by the explicit construction).

**What the headline matches in the paper, and what it does not.** The paper's
Theorem 1 lower-bounds the **minimax `(1−γ)`-quantile risk** of the *squared* error,
`𝔐ₙ,γ(ℱ) = inf_est sup_{(m,g)∈ℱ} 𝒬_{1−γ}(|est − θ|²)` (eq. for `𝔐ₙ,γ`), for
`γ ∈ (1/2,1)`. A quantile bound `𝔐ₙ,γ ≥ ρ` is *by definition* the statement that
every estimator misses the truth by `√ρ` with probability `≥ 1−γ` on some in-class
DGP. So our `minimax_lower_bound` (`1/4 ≤ minimaxMiss … s`) **is** the
paper's headline theorem at `γ = 3/4`, `ρ = s² ≍ εg·εm` — the *stronger*
(probability/quantile) form, not the weaker expected-risk form. The paper itself
notes (and we formalize in §13sa.7) that the quantile bound *implies* the
expected-`𝔼`-risk bound `sup E[(est−θ)²] ≥ (1−γ)ρ`. On the remaining differences from
the paper's full generality: (i) the **constant general center** `(m₀,g₀,g₁) ∈ (0,1)³`
(the paper's Assumption 2 in the constant-nuisance case, incl. the known-baseline
remark `ĝ(0)=g₀`) **is now formalized** in §13sa.8 (`minimax_lower_bound_gen`),
generalizing the `(m̂,ĝ)≡1/2` headline; the exact `λ`-independent ATE gap survives because
the center is constant *within* each Rademacher pair. (i') A **cell-varying center** —
nuisances that are arbitrary *functions* of the pair index (genuinely non-constant over the
covariate space), bounded away from `{0,1}` — **is now formalized** in §13sa.9
(`minimax_lower_bound_var`), using the new non-uniform `ingster_bound_general`
(§13sa.9) for the per-pair χ² coefficient `Γⱼ`. The earlier note that this "needs a
Taylor-remainder Fano argument" was **wrong**: the paper keeps the two-point/Le Cam
structure, the first-order bump term cancels (`E[ŵΔ]=0`), and the ATE separation is a
`λ`-independent *lower bound* — packaged abstractly as the **one-sided mixture two-point
lemma** `mixture_two_point_lower_bound` (§13sa.2b). The following are still **not**
formalized: (i'') the *within-pair-varying* center (the two cells of a pair carry different
nuisance values — the only residual of the fully-functional Case-1 construction; needs the
one-sided lemma + the 4-distinct-values-per-pair overlap algebra); (ii) the weight function
is `w ≡ 1` (no `‖w‖∞²`/`‖w‖₂²` factors); (iii) the additive `1/n` parametric-variance
term **is now formalized** (unweighted, `‖w‖₂²=1`) in §13sa.10
(`parametric_lower_bound`); (iv) only Case 1 (`εg ≳ εm`) of the bias construction is
built (Case 2 `εm > εg` would recover `√εgεm` in that regime).

### 13sa.1 `Estimation/MinimaxATE/Model.lean` — finite observed-data model

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Obs M` | `abbrev := Fin M × Bool × Bool` | One observation `(X, D, Y)` with finite covariate. | — |
| `ValidDGP m g` | `structure`, `Prop` | Propensity `m : Fin M → ℝ` and both outcome arms `g : Bool → Fin M → ℝ` take values in `[0,1]`. | — |
| `obsReal m g z` | `noncomputable def`, `ℝ` | Real mass `(1/M)·P(D=d\|x)·P(Y=y\|d,x)` of observation `z`. | — |
| `obsReal_sum` | `∑ z, obsReal m g z = 1` (needs `[NeZero M]`) | Total mass is 1 (sum over `Y`, then `D`, then `X`). **Proved.** | **Proved** |
| `obsReal_nonneg` | `ValidDGP → 0 ≤ obsReal m g z` | Nonnegativity from validity. | **Proved** |
| `obsPMF hv`, `obsLaw hv` | `noncomputable def`, `PMF (Obs M)` / `Measure (Obs M)` | Single-observation law as a PMF / probability measure (via `PMF.ofFintype`). `obsLaw_isProb` instance. | **Proved** |
| `ate g` | `noncomputable def`, `ℝ := (1/M) Σ_x (g true x − g false x)` | The ATE functional; depends only on `g`. | — |
| `l2sq a b` | `noncomputable def`, `ℝ := (1/M) Σ_x (a x − b x)²` | Squared `L²(P_X)` distance. | — |
| `InClass mhat ghat εg εm m g` | `structure`, `Prop` | Structure-agnostic nuisance class `ℱ`: valid + each outcome arm within `εg` and propensity within `εm` of fixed estimates `(mhat, ghat)`. | — |
| `InClassDGP …` | `def`, subtype | A DGP packaged with its `InClass` proof. | — |
| `productLaw hv n` | `noncomputable def`, `Measure (Fin n → Obs M) := Measure.pi …` | `n`-sample i.i.d. product law; `productLaw_isProb` instance. | **Proved** |
| `nMiss hv n est s` | `noncomputable def`, `ℝ` | `(productLaw hv n).real {x \| s ≤ \|est x − ate g\|}`: probability `est` misses the true ATE by `s`. | — |
| `nMSE hv n est` | `noncomputable def`, `ℝ := ∫ x, (est x − ate g)² ∂(productLaw hv n)` | Mean-squared error of `est`: the expected-risk functional (the paper's weaker `𝔼`-risk form). | — |
| `nMiss_sq_le_nMSE` | `(hv) (n) (hs : 0 ≤ s) → s²·nMiss hv n est s ≤ nMSE hv n est` (needs `[MeasurableSingletonClass C]`) | **Chebyshev/Markov bridge**: probability-of-miss controls MSE. Quantitative form of "quantile bound ⟹ expected-risk bound". Via `mul_meas_ge_le_integral_of_nonneg`; integrability free on the finite sample space. | **Proved, axiom-clean** |
| `minimaxMiss mhat ghat εg εm n est s` | `noncomputable def`, `ℝ := ⨆ p : InClassDGP …, nMiss …` | Worst-case-over-class miss probability; a lower bound on it is a minimax lower bound. | — |
| `bddAbove_nMiss_range`, `nMiss_le_minimaxMiss` | bddAbove / `nMiss ≤ minimaxMiss` | Each in-class miss `≤ 1`, so the sup is well-defined and dominates each member. | **Proved** |

### 13sa.2 `Estimation/MinimaxATE/Reduction/Witness.lean` — two-point reduction

Abstract Le Cam reduction (over a general finite covariate `C`). The explicit
witness is now built (§13sa.4); this layer is correct regardless of how it is built.

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `TwoPointWitness C n mhat ghat εg εm` | `structure` (needs `[Fintype C] [Nonempty C] [MeasurableSpace C]`) | Le Cam witness: separation `s`, TV budget `c`, two `n`-sample laws `Q : Bool → Measure …` (`prob`), true ATEs `θ : Bool → ℝ` with `sep : 2s ≤ \|θ true − θ false\|`, `tvBound : tvDist (Q false) (Q true) ≤ c`, and the realizability field `dominated` (each `Q j`-miss `≤ minimaxMiss`, discharged by a mixture-of-in-class-DGPs construction). | — |
| `twoPointWitness_lower_bound` | `(W) (hest : Measurable est) → (1 − W.c)/2 ≤ minimaxMiss … est W.s` | **Headline.** Le Cam two-point bound (`Stat.two_point_lower_bound_of_tvDist_le`) + realizability domination. **Proved, axiom-clean.** | **Proved** |
| `twoPointWitness_quarter` | `(W) (hc : W.c ≤ 1/2) (hest) → 1/4 ≤ minimaxMiss … est W.s` | A witness with TV budget `≤ 1/2` forces every estimator to miss the truth by `s` with prob `≥ 1/4` somewhere in the class. **Proved, axiom-clean.** | **Proved** |

### 13sa.3 `Estimation/MinimaxATE/Reduction/Bump.lean` — `l2sq` / Rademacher-bump algebra

Small reusable algebra for the squared `L²(P_X)` distance, used to verify
class-membership of the perturbed nuisances (a constant-magnitude bump lands
exactly on the budget boundary).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `l2sq_self` | `l2sq a a = 0` | Distance to self is zero. | **Proved** |
| `l2sq_comm` | `l2sq a b = l2sq b a` | Symmetry. | **Proved** |
| `l2sq_nonneg` | `0 ≤ l2sq a b` | Nonnegativity (no `[NeZero M]` needed). | **Proved** |
| `l2sq_bump` | `(∀ x, (σ x)² = 1) ⇒ l2sq (fun x ↦ b x + δ·σ x) b = δ²` (needs `[Nonempty C]`) | **Constant-magnitude (Rademacher) bump**: a `±δ` perturbation has `l2sq = δ²`, so `δ = √ε` lands the perturbation on the boundary of `ℱ(ε,·)`. | **Proved, axiom-clean** |

### 13sa.4 Explicit construction (Jin–Syrgkanis §4.4, Case 1) — `ConstCenterHalf/` (`Construction/Membership/Gap/ExplicitWitness.lean`)

The explicit lower-bound witness, on the **paired-cell covariate** `C = Fin K × Bool`
(pair index × position) with centered estimates `m̂ = ĝ ≡ 1/2` and unweighted ATE.
A Rademacher sign vector `λ : Fin K → Bool` drives the bump
`Δ λ (j,b) = signOf b · signOf (λ j) ∈ {±1}` (`Δ² = 1`, balanced per pair). Every
declaration **sorry-free + axiom-clean**.

| Declaration | File | Statement | Description |
|---|---|---|---|
| `Δ`, `Δ_sq`, `Δ_mem` | Construction | `Δ λ x = signOf x.2·signOf (λ x.1)`; `Δ² = 1`; `Δ ∈ {±1}` | The Rademacher bump and its sign properties. |
| `mPerturbed`, `gPerturbed` | Construction | `mλ = 1/2 − β·Δ`; `gλ(1) = (1/2+α·Δ)/(1−2β·Δ)`, `gλ(0)=1/2` | The asymmetric (non-linear-in-`λ`) perturbed DGP (eq. 17, `m̂=ĝ=1/2`). |
| `validDGP_perturbed` | Construction | `0≤α, 0≤β, α+2β≤1/2 ⇒ ValidDGP (mPerturbed β λ) (gPerturbed α β λ)` | The perturbed nuisances stay in `[0,1]`. |
| `inClass_perturbed` | Membership | `β²≤εm, (α+β)²/(1−2β)²≤εg ⇒ InClass m̂ ĝ εg εm (mλ) (gλ)` | Class membership: `l2sq(mλ,m̂)=β²` exactly, `l2sq(gλ(1),ĝ(1))≤(α+β)²/(1−2β)²`. |
| `ate_gPerturbed` | Gap | `ate (gPerturbed α β λ) = 2β(α+β)/(1−4β²)` | **Exact ATE — independent of `λ`** (the per-pair sum is sign-invariant). The √(εg·εm) gap. |
| `Qfalse`, `Qtrue`, `signWeight` | ExplicitWitness | `Qfalse = P̂^⊗n`; `Qtrue = (1/2^K)Σ_λ Qλ^⊗n` (uniform `mixture`) | The two `n`-sample data laws (null vs. uniform mixture of perturbations). |
| `explicitWitness` | ExplicitWitness | `… (htv : tvDist Qfalse Qtrue ≤ 1/2) → TwoPointWitness (Fin K×Bool) n m̂ ĝ εg εm` | Assembled witness; `dominated` discharged by `mixtureReal_le` (all `Qλ` in-class, common ATE). |
| `explicit_minimax_lower_bound` | ExplicitWitness | budgets + `htv : tvDist Qfalse Qtrue ≤ 1/2` ⇒ `1/4 ≤ minimaxMiss m̂ ĝ εg εm n est (β(α+β)/(1−4β²))` | Conditional form (carries `htv`); `minimax_lower_bound` (§13sa.5) discharges it. **Proved, axiom-clean.** |

### 13sa.5 χ² indistinguishability core — `ConstCenterHalf/` (`ChiSqOverlap/Ingster/ChiSquaredCore.lean`)

The Ingster χ² second-moment bound that discharges `htv`, making the lower bound
**unconditional** (only the natural regime hypotheses remain). All sorry-free +
axiom-clean. Builds on the finite χ² formula `finite_one_add_chiSqDiv` (§12w).

| Declaration | File | Statement | Description |
|---|---|---|---|
| `obsLaw_real_singleton` | ChiSqOverlap | `(obsLaw hv).real {z} = obsReal m g z` | The single-observation PMF point mass (generic `C`). |
| `chiSqOverlap`, `chiSqOverlap_eq` | ChiSqOverlap | `chiSqOverlap α β λ λ' := ∑_z qλ(z)qλ'(z)/p̂(z) = 1 + (2γ/K)·Σ_j signOf(λ j)signOf(λ' j)`, `γ = α²+2αβ+3β²` | **Exact closed form** of the single-obs χ²-overlap (finite algebra; per-pair sign-balanced cancellation). |
| `ingster_bound` | Ingster | `0≤γ, 2γ≤1, 2n²γ² ≤ K·log 2 ⇒ Σ_{λ,λ'} (2^K)⁻²·(1+(2γ/K)Σ_j s_j s'_j)ⁿ ≤ 2` | **The Ingster bound** (standalone real analysis): `(1+x)≤eˣ` + Rademacher independence (factorize to `cosh(nc)^K`) + `cosh y ≤ e^{y²/2}`. |
| `one_add_chiSqDiv_Qtrue_Qfalse` | ChiSquaredCore | `1 + χ²(Qtrue‖Qfalse) = Σ_{λ,λ'} (2^K)⁻²·chiSqOverlap(λ,λ')ⁿ` | **Mixture 2nd-moment identity**: finite χ² formula + product point mass (`pi_real_singleton`) + `Fintype.sum_pow`. |
| `chiSqDiv_Qtrue_Qfalse_le_one` | ChiSquaredCore | regime ⇒ `χ²(Qtrue‖Qfalse) ≤ 1` | Combine the identity with `chiSqOverlap_eq` + `ingster_bound`. |
| `tvDist_Qfalse_Qtrue_le_half` | ChiSquaredCore | regime ⇒ `tvDist Qfalse Qtrue ≤ 1/2` | `tvDist ≤ ½√χ² ≤ 1/2` (discharges `htv`). |
| `minimax_lower_bound` | ChiSquaredCore | budgets + `2γ≤1` + `2n²γ² ≤ K·log 2` + `Measurable est` ⇒ `1/4 ≤ minimaxMiss m̂ ĝ εg εm n est (β(α+β)/(1−4β²))` | **Headline (unconditional).** No estimator is within `s ≍ √(εg·εm)` of the true ATE w.p. `> 3/4` over the class, in the regime `n²·(εg+εm)² ≲ K`. **Proved, axiom-clean.** |

Helpers: `absolutelyContinuous_of_singleton_pos` (full support ⇒ AC), `productLaw_real_singleton`, `Qfalse_singleton_ne_zero`, `signWeight_toReal` (`= (2^K)⁻¹`).

### 13sa.6 Continuous covariate — `ContinuousX.lean`

The ancillarity reduction to the paper's continuous-`X` statement. The within-cell
position is modelled by an **arbitrary** probability space `(W, ρ)`; the continuous
`n`-sample laws are the finite-cell laws tensored with `n` ancillary `ρ`-draws.
Tensoring both hypotheses with the common factor leaves the χ² unchanged
(`chiSqDiv_prod_ancillary`, §12w), so the bound transfers verbatim. Sorry-free +
axiom-clean.

| Declaration | Statement | Description |
|---|---|---|
| `ancillaryLaw ρ n`, `QfalseCont`, `QtrueCont` | `Measure.pi (fun _ => ρ)`; `(Qfalse).prod (ancillaryLaw ρ n)`; `(Qtrue …).prod (ancillaryLaw ρ n)` | The `n` within-cell ancillary draws and the continuous null / alternative laws on `(Fin n → Obs (Fin K×Bool)) × (Fin n → W)`. |
| `chiSqDiv_QtrueCont_QfalseCont_le_one` | regime ⇒ `χ²(QtrueCont‖QfalseCont) ≤ 1` | The ancillary factor cancels (`chiSqDiv_prod_ancillary`), reducing to the finite-cell `chiSqDiv_Qtrue_Qfalse_le_one`. |
| `tvDist_QfalseCont_QtrueCont_le_half` | regime ⇒ `tvDist QfalseCont QtrueCont ≤ 1/2` | Continuous-`X` indistinguishability. |
| `two_point_lower_bound_continuous` | budgets + regime + `Measurable est` ⇒ `1/4 ≤ max (QtrueCont.real {miss 2s}) (QfalseCont.real {miss 0})` | **Headline (continuous covariate).** Two-point (Le Cam) obstruction on the richer continuous experiment; the ancillary within-cell position cannot help. Bounds the two-hypothesis max-miss, not `minimaxMiss`. **Proved, axiom-clean.** |

### 13sa.7 Expected-risk (MSE) form — `MSE.lean`

The paper's quantile lower bound (§13sa.5) *implies* the weaker expected-squared-error
bound used by Balakrishnan et al. (`𝔐ₙ,γ ≥ ρ ⟹ minimax `𝔼`-risk `≥ (1−γ)ρ`). This
file makes that implication explicit and **unconditional** via the Chebyshev bridge
`nMiss_sq_le_nMSE` (§13sa.1). The witness DGP is pulled out of the two-point bound
`1/4 ≤ max(…)`: either the null `(m̂,ĝ)` (left branch) or a single Rademacher-perturbed
DGP extracted from the uniform mixture `Qtrue` by `exists_real_ge_mixture` (a finite
average is `≤` its maximal component). Sorry-free + axiom-clean.

| Declaration | Statement | Description |
|---|---|---|
| `minimax_lower_bound_mse` | budgets + regime + `Measurable est` ⇒ `∃ p : InClassDGP m̂ ĝ εg εm, (β(α+β)/(1−4β²))²/4 ≤ nMSE p.2.valid n est` | **Expected-risk headline.** Every estimator has MSE `≥ s²/4 ≍ εg·εm` on *some* in-class DGP — the product rate is an unbeatable `𝔼`-squared-error floor. The `(1−γ)`-factored (`γ=3/4`) consequence of the quantile bound. **Proved, axiom-clean.** |

(No continuous-covariate MSE form: on the non-finite continuous sample space the
squared error of an arbitrary measurable estimator need not be integrable, so an
unconditional Chebyshev step is unavailable — the continuous result stays in its
native quantile/two-point form, which is already the paper's risk notion.)

### 13sa.8 General constant center — `ConstCenterGeneral/` (`Construction/Gap/Membership/ChiSqOverlap/LowerBound/MSE.lean`, `namespace GenConstr`)

Generalizes the `(m̂,ĝ)≡1/2` development (§13sa.4–7) to an **arbitrary constant
nuisance center** `(m₀,g₀,g₁) ∈ (0,1)³` bounded away from `{0,1}` — the paper's
Assumption 2 in the constant case, including the known-baseline remark. The center
is held constant *within* each Rademacher pair, so the ATE gap stays **exactly
`λ`-independent** (no Taylor/Fano needed) and the abstract `ingster_bound` (§13sa.5)
is reused verbatim with `γ := Γ/2`. The `1/2` headline is the instance
`m₀=g₁=1/2`. All declarations **sorry-free + axiom-clean**; live in `namespace GenConstr`.

| Declaration | File | Statement | Description |
|---|---|---|---|
| `GenConstr` | Construction | `structure` (fields `α β m₀ g₀ g₁ : ℝ` + the bounded-away / smallness inequalities `hβg₁ : β<g₁`, `hαg₁ : α≤g₁`, `hgU : g₁+α+β/g₁≤1`, `hmU : m₀(1+β/g₁)≤1`) | Bundles the bump scalars, the constant center, and exactly the inequalities keeping every perturbed probability in `[0,1]`. |
| `mhatG`, `ghatG`, `mPertG`, `gPertG` | Construction | `m̂≡m₀`; `ĝ(1)≡g₁,ĝ(0)≡g₀`; `mλ=m₀(1−(β/g₁)·Δ)`; `gλ(1)=(g₁+α·Δ)/(1−(β/g₁)·Δ)`, `gλ(0)=g₀` | The general-center perturbed DGP (collapses to §13sa.4 at `m₀=g₁=1/2`, `β/g₁=2β`). |
| `denomG_pos`, `validDGP_hatG`, `validDGP_pertG` | Construction | `0<1−(β/g₁)·Δ`; null & perturbed are `ValidDGP` | Positivity of the denominator and `[0,1]`-validity. |
| `gPertG_true_eq` | Gap | `gλ(1,x) = (g₁²+α·g₁·Δ)/(g₁−β·Δ)` | **Cleared treated arm** (clears the inner `β/g₁` fraction); used throughout. |
| `ate_ghatG`, `ate_gPertG` | Gap | `ate ĝ = g₁−g₀`; `ate gλ = (g₁−g₀) + g₁β(α+β)/(g₁²−β²)` (needs `[NeZero K]`) | **Exact ATE — independent of `λ`** (per-pair sum sign-invariant). |
| `ate_gapG` | Gap | `ate gλ − ate ĝ = g₁β(α+β)/(g₁²−β²)` | The `√(εg·εm)` gap (collapses to `2β(α+β)/(1−4β²)` at `g₁=1/2`). |
| `l2sq_mPertG`, `l2sq_gPertG_true_le`, `inClassG` | Membership | `l2sq(mλ,m̂)=(m₀β/g₁)²` exactly; `l2sq(gλ(1),ĝ(1))≤g₁²(α+β)²/(g₁−β)²`; budgets `(m₀β/g₁)²≤εm`, `g₁²(α+β)²/(g₁−β)²≤εg ⇒ InClass m̂ ĝ εg εm mλ gλ` | Class membership. |
| `Γ`, `chiSqOverlapG`, `chiSqOverlap_eqG` | ChiSqOverlap | `Γ = m₀α²/g₁ + m₀(α+β/g₁)²/(1−g₁) + m₀²β²/(g₁²(1−m₀))`; `chiSqOverlapG λ λ' = 1 + (Γ/K)·Σ_j s_j s'_j` | **Exact single-obs χ²-overlap closed form** (linear-`Δ` terms cancel by construction; `Γ=2(α²+2αβ+3β²)` at `m₀=g₁=1/2`). |
| `QfalseG`, `QpertG`, `QtrueG`, `Γ_nonneg` | LowerBound | `P̂^⊗n`; `Qλ^⊗n`; `(1/2^K)Σ_λ Qλ^⊗n`; `0≤Γ` | The two `n`-sample laws and nonnegativity of the overlap coefficient. |
| `one_add_chiSqDiv_QtrueG_QfalseG`, `chiSqDiv_QtrueG_QfalseG_le_one`, `tvDist_QfalseG_QtrueG_le_half` | LowerBound | mixture 2nd-moment identity; `Γ≤1`, `2n²(Γ/2)²≤K·log 2 ⇒ χ²≤1`; ⇒ `tvDist≤1/2` | χ² indistinguishability via `ingster_bound` at `γ=Γ/2`. |
| `minimax_lower_bound_gen` | LowerBound | budgets + `Γ≤1` + `2n²(Γ/2)²≤K·log 2` + `Measurable est` ⇒ `1/4 ≤ minimaxMiss m̂ ĝ εg εm n est (g₁β(α+β)/(2(g₁²−β²)))` | **Headline (general constant center, unconditional).** No estimator is within `s ≍ √(εg·εm)` of the true ATE w.p. `>3/4` over the class, for any constant bounded-away `(m₀,g₀,g₁)`. **Proved, axiom-clean.** |
| `minimax_lower_bound_mse_gen` | MSE | same hyps ⇒ `∃ p : InClassDGP m̂ ĝ εg εm, (g₁β(α+β)/(2(g₁²−β²)))²/4 ≤ nMSE p.2.valid n est` | **Expected-risk form (general center).** MSE `≥ s²/4` on some in-class DGP; the `(1−γ)`-factored consequence. **Proved, axiom-clean.** |

### 13sa.9 Cell-varying center — `VaryingCenterCase1/` (`Construction/Gap/Membership/ChiSqOverlap/LowerBound.lean`, `namespace VarConstr`) + `Reduction/WitnessMixture.lean` + `VaryingCenterCase1/Ingster.lean` (non-uniform Ingster)

Generalizes §13sa.8 from a *constant* center to a **cell-varying center**: the
nuisance estimates are arbitrary *functions* of the pair index `j : Fin K` (bounded
away from `{0,1}`), genuinely non-constant over the covariate space — the finite-model
analogue of the paper's functional center, for centers constant *within* each
Rademacher pair (the residual within-pair variation is gap (i'') of the §13sa scope
note). Because the center is shared by a pair's two positions, the per-pair ATE gap is
still **exactly `λ`-independent** (no Taylor remainder), but the χ²-overlap coefficient
`Γⱼ` now **varies per pair**, so the bound needs the non-uniform Ingster inequality.
Two reusable pieces of infrastructure were added; everything **sorry-free + axiom-clean**.

| Declaration | File | Statement | Description |
|---|---|---|---|
| `mixture_two_point_lower_bound` | Reduction/WitnessMixture | null in-class DGP + finite weighted family of in-class alternatives with **per-component** separation `2s ≤ \|ate g₀ − ate(gλ i)\|` + `tvDist(Q₀, mixture) ≤ c` ⇒ `(1−c)/2 ≤ minimaxMiss` | **One-sided mixture two-point lemma.** Sharper than `TwoPointWitness`: the alternatives need not share one ATE — a per-component *lower* bound suffices (the asymmetric testing argument measures each component at its own ATE). The abstract key enabling fully-functional centers. **Proved, axiom-clean.** |
| `ingster_bound_general` | Ingster | `(∀j,0≤d j)`, `Σⱼ d j ≤ 1`, `(n²/2)Σⱼ(d j)² ≤ log 2` ⇒ `Σ_{λ,λ'}(2^K)⁻²(1+Σⱼ d j·s_j s'_j)^n ≤ 2` | **Non-uniform Ingster χ² bound.** Per-pair coefficients `d j` (vs the constant `2γ/K` of `ingster_bound`); factorizes to `∏ⱼ cosh(n·d j)` via `Finset.prod_univ_sum`. **Proved, axiom-clean.** |
| `VarConstr` | Construction | `structure` (fields `α β : ℝ`, `m₀ g₀ g₁ : Fin K → ℝ` + per-pair bounded-away/smallness inequalities) | Bundles the bump scalars and the **cell-varying** center (one bounded-away value per pair). |
| `mhatV`, `ghatV`, `mPertV`, `gPertV`, `validDGP_hatV`, `validDGP_pertV` | Construction | `m̂ x=m₀ x.1`; `ĝ(1,x)=g₁ x.1, ĝ(0,x)=g₀ x.1`; `mλ=m₀ x.1(1−(β/g₁ x.1)Δ)`; `gλ(1,x)=(g₁ x.1+αΔ)/(1−(β/g₁ x.1)Δ)` ; both `ValidDGP` | The cell-varying perturbed DGP, indexed through the pair index `x.1`. |
| `ate_ghatV`, `ate_gPertV`, `ate_gapV`, `ate_gap_nonneg` | Gap | `ate gλ` is a `λ`-independent sum over pairs; `ate gλ − ate ĝ = (card)⁻¹·2β(α+β)·Σⱼ g₁ⱼ/(g₁ⱼ²−β²) ≥ 0` | **Exact per-pair ATE gap** (sign-invariant within each pair), nonnegative. |
| `l2sq_mPertV_le`, `l2sq_gPertV_true_le`, `inClassV` | Membership | per-pair budgets `(m₀ⱼ·β/g₁ⱼ)²≤εm`, `g₁ⱼ²(α+β)²/(g₁ⱼ−β)²≤εg` (all `j`) ⇒ `InClass m̂ ĝ εg εm mλ gλ` | Class membership via average-of-bounded-terms (per-pair sup bounds are *sufficient*). |
| `ΓV`, `ΓV_nonneg`, `chiSqOverlapV`, `chiSqOverlap_eqV` | ChiSqOverlap | `Γⱼ = m₀ⱼα²/g₁ⱼ + m₀ⱼ(α+β/g₁ⱼ)²/(1−g₁ⱼ) + m₀ⱼ²β²/(g₁ⱼ²(1−m₀ⱼ))`; `chiSqOverlapV λ λ' = 1 + Σⱼ (Γⱼ/K)·s_j s'_j` | **Per-pair χ²-overlap closed form** (linear-`Δ` terms cancel; coefficient varies per pair → feeds `ingster_bound_general` with `d j = Γⱼ/K`). |
| `chiSqDiv_QtrueV_QfalseV_le_one`, `tvDist_QfalseV_QtrueV_le_half` | LowerBound | `Σⱼ Γⱼ/K ≤ 1`, `(n²/2)Σⱼ(Γⱼ/K)² ≤ log 2` ⇒ `χ²≤1` ⇒ `tvDist≤1/2` | χ² indistinguishability via the non-uniform Ingster bound. |
| `minimax_lower_bound_var` | LowerBound | per-pair budgets + `Σⱼ Γⱼ/K ≤ 1` + `(n²/2)Σⱼ(Γⱼ/K)² ≤ log 2` + `Measurable est` ⇒ `1/4 ≤ minimaxMiss m̂ ĝ εg εm n est (½(ate gλ − ate ĝ))` | **Headline (cell-varying center, Case 1 `εg ≳ εm`).** No estimator is within `s` of the true ATE w.p. `>3/4`, for a nuisance center varying arbitrarily across pairs. **Proved, axiom-clean.** |

### 13sa.9b Cell-varying center, Case 2 (`εm > εg`) — `VaryingCenterCase2/Construction/Gap/Membership/ChiSqOverlap/LowerBound.lean`

The **symmetric second construction** (Jin–Syrgkanis 2024 eq. (18), `f_n > e_n'`).
`VaryingCenterCase1`'s Case-1 construction attains the product rate `s ≍ √(εg·εm)` only when the
outcome budget dominates (`εg ≳ εm`); this file covers the opposite regime by
**swapping the two nuisances' roles**: the propensity deviation is the large one
(`O(α+β)`, carrying `√εm`) and the outcome deviation the small one (`O(β)`, carrying
`√εg`). In the finite-cell model the construction is `gλ(0,·)=g₀`,
`gλ(1,x)=g₁ⱼ/D`, `mλ=m₀ⱼ(1+αg₁ⱼΔ)D` with `D=1+(β/g₁ⱼ)Δ−αβ`. Although non-linear in
`λ`, the observed masses are polynomial in `Δ` (the `D` cancels in `mλ·gλ(1)`; the
propensity collapses to `mλ=m₀ⱼ(1+κⱼΔ)`, `κⱼ=β/g₁ⱼ+αg₁ⱼ−α²βg₁ⱼ`). Reuses
`VaryingCenterCase1/Ingster`'s `ingster_bound_general`. **All proved, axiom-clean.**

| Declaration | File | Statement | Description |
|---|---|---|---|
| `VarConstr2` | VaryingCenterCase2/Construction | `structure` (fields `α β : ℝ`, `m₀ g₀ g₁ : Fin K → ℝ` + per-pair `hgU`/`hκ`/`hmU` inequalities) | Bundles the bump scalars and cell-varying center for the propensity-dominant construction; `hgU` forces `D>0` and `gλ(1)≤1`, `hκ` keeps `mλ≥0`, `hmU` keeps `mλ≤1`. |
| `κ`, `D2`, `mhat2`, `ghat2`, `mPert2`, `gPert2`, `mPert2_eq`, `validDGP_hat2`, `validDGP_pert2` | VaryingCenterCase2/Construction | `κⱼ=β/g₁ⱼ+αg₁ⱼ−α²βg₁ⱼ`; `D=1+(β/g₁)Δ−αβ`; `mλ=m₀(1+αg₁Δ)D`, `gλ(1)=g₁/D`; `mPert2_eq : mλ=m₀(1+κΔ)`; both `ValidDGP` | The Case-2 perturbed DGP and its affine `Δ`-collapse (via `Δ²=1`), proved valid. |
| `ate_ghat2`, `ate_gPert2`, `ate_gap2`, `ate_gap2_nonneg`, `gPert2_true_eq2`, `clearedDenom_pos`, `denomE_pos` | VaryingCenterCase2/Gap | `ate gλ` is `λ`-independent; `ate gλ − ate ĝ = (card)⁻¹·2β·Σⱼ g₁ⱼ(αg₁ⱼ²(1−αβ)+β)/Eⱼ ≥ 0`, `Eⱼ=g₁ⱼ²(1−αβ)²−β²` | **Exact per-pair ATE gap** (cleared treated arm `g₁²/(g₁(1−αβ)+βΔ)`; the two-position sum collapses `σ²`), nonnegative. |
| `l2sq_mPert2_le`, `l2sq_gPert2_true_le`, `gPert2_true_sub`, `inClass2` | VaryingCenterCase2/Membership | per-pair budgets `(m₀ⱼκⱼ)²≤εm` (exact, propensity), `β²(αg₁ⱼ+1)²/(1−β/g₁ⱼ−αβ)²≤εg` (worst-case, outcome) ⇒ `InClass m̂ ĝ εg εm mλ gλ` | Class membership in the swapped regime: `mλ−m̂=m₀κΔ` is **exact** (`O(α+β)`→`√εm`), `gλ(1)−ĝ(1)=β(αg₁−Δ)/D` is `O(β)`→`√εg`. |
| `ΓV2`, `ΓV2_nonneg`, `chiSqOverlapV2`, `obsReal_pert2_eq`, `chiSqOverlap_eq2` | VaryingCenterCase2/ChiSqOverlap | `Γⱼ = m₀ⱼα²g₁ⱼ³ + m₀ⱼ(β/g₁ⱼ+αg₁ⱼ(1−g₁ⱼ)−α²βg₁ⱼ)²/(1−g₁ⱼ) + m₀ⱼ²κⱼ²/(1−m₀ⱼ)`; `chiSqOverlapV2 λ λ' = 1 + Σⱼ(Γⱼ/K)·s_j s'_j` | **Per-pair χ²-overlap closed form** (constant part `=1`, the finite analogue of `E_λ qλ=p̂`; only the `λ·λ'` cross term survives → feeds `ingster_bound_general` with `d j=Γⱼ/K`). |
| `chiSqDiv_Qtrue2_Qfalse2_le_one`, `tvDist_Qfalse2_Qtrue2_le_half` | VaryingCenterCase2/LowerBound | `Σⱼ Γⱼ/K ≤ 1`, `(n²/2)Σⱼ(Γⱼ/K)² ≤ log 2` ⇒ `χ²≤1` ⇒ `tvDist≤1/2` | χ² indistinguishability via the non-uniform Ingster bound. |
| `minimax_lower_bound_var2` | VaryingCenterCase2/LowerBound | propensity-dominant per-pair budgets + `Σⱼ Γⱼ/K ≤ 1` + `(n²/2)Σⱼ(Γⱼ/K)² ≤ log 2` + `Measurable est` ⇒ `1/4 ≤ minimaxMiss m̂ ĝ εg εm n est (½(ate gλ − ate ĝ))` | **Headline (cell-varying center, Case 2 `εm > εg`).** The symmetric construction covering the regime `minimax_lower_bound_var` cannot; together they give the full product rate `√(εg·εm)` in both regimes. **Proved, axiom-clean.** |

### 13sa.10 Parametric `1/n` term — `Parametric.lean`

The additive `1/n` (parametric-variance) term of the paper's `Ω(εg εm + 1/n)` bound,
via a **separate** two-point construction independent of the Rademacher/Ingster bias
machinery. Both hypotheses are single product laws (no mixture): a constant center
`(m₀,g₀,g₁)` and a constant **outcome shift** `δ` on the treated arm only. The single-
observation χ² has the exact closed form `χ²₁ = m₀δ²/(g₁(1−g₁))`, so tensorizing
(`one_add_chiSqDiv_pi_iid`) and bounding `(1+χ²₁)^n ≤ e^{nχ²₁} ≤ 2` keeps `tvDist ≤ ½`
once `n·χ²₁ ≤ log 2` — i.e. `δ ≍ 1/√n`. The ATE gap is exactly `δ`. **Axiom-clean.**

| Declaration | Statement | Description |
|---|---|---|
| `one_add_chiSqDiv_obsPert_obsNull` | `1 + chiSqDiv (obsLaw pert) (obsLaw null) = 1 + m₀δ²/(g₁(1−g₁))` | **Exact single-obs χ²** for a constant treated-arm shift `δ` (the `d=0` block cancels; `d=1` block gives `m₀(1+δ²/(g₁(1−g₁)))`). |
| `parametric_lower_bound` | `0≤δ`, `g₁+δ≤1`, `δ²≤εg`, `n·m₀δ²/(g₁(1−g₁)) ≤ log 2`, `Measurable est` ⇒ `1/4 ≤ minimaxMiss (fun _=>m₀) (fun d _=>if d then g₁ else g₀) εg εm n est (δ/2)` | **Headline (parametric term).** With `δ ≍ 1/√n` (so `s = δ/2 ≍ 1/√n`, `s² ≍ 1/n`), no estimator is within `s` of the ATE w.p. `>3/4` — the `1/n` minimax floor even with a perfectly-known nuisance class. **Proved, axiom-clean.** |

### 13sa.11 Achievability — `AIPWEstimator.lean`

The **matching upper bound**: the fixed-center AIPW estimator attains the `√(εg·εm)` rate.
Because the structure-agnostic class is centered at *fixed* nuisances `(mhat, ghat)` (not
data-estimated), the natural estimator is the plug-in AIPW **sample average**
`θ̂(sample) = (1/n) Σ_i ψ_AIPW(Z_i; mhat, ghat)`. Everything stays in the finite `Obs C` /
`productLaw` world (no potential-outcome lift, no asymptotics), so the pairing with the
`minimaxMiss` converse is direct. This is the finite specialization of the general AIPW DML
asymptotic-normality theorem `Estimation/ATE/DML.lean` (`dml_ATE_tendstoNormal`). **All proved,
axiom-clean.**

| Declaration | Statement | Description |
|---|---|---|
| `aipwScoreFin mhat ghat z` | `def`, `ℝ` | Fixed-center AIPW score `(ĝ₁ x − ĝ₀ x) + D·(Y−ĝ₁ x)/m̂ x − (1−D)·(Y−ĝ₀ x)/(1−m̂ x)` at one observation `z=(x,D,Y)` (`yReal` reads `Y:Bool` as `{0,1}`). |
| `estAIPW mhat ghat n` | `def`, `(Fin n → Obs C) → ℝ` | The estimator: sample average `(n)⁻¹ Σ_i aipwScoreFin mhat ghat (sample i)`. |
| `aipw_pop_mean` | `∫ aipwScoreFin … ∂(obsLaw hv) = Σ_z obsReal m g z · aipwScoreFin …` | Population mean of the score is the finite PMF-weighted sum. **Proved.** |
| `aipw_bias_identity` | `(Σ_z obsReal·score) − ate g = (1/\|C\|) Σ_x [(1−m/m̂)(ĝ₁−g₁) − (1−(1−m)/(1−m̂))(ĝ₀−g₀)]` | **Doubly-robust remainder identity** (the analytic heart): the plug-in bias factors into products of nuisance errors. **Proved.** |
| `exists_center_overlap` | `(∀x, 0<m̂ x<1) ⇒ ∃ ε>0, ∀x, ε ≤ m̂ x ∧ ε ≤ 1−m̂ x` | Uniform center overlap from the finite `min` (so the score weights `1/m̂`, `1/(1−m̂)` are bounded). **Proved.** |
| `aipw_bias_bound` | `\|bias\| ≤ ε⁻¹·((√l2sq(g₁,ĝ₁)+√l2sq(g₀,ĝ₀))·√l2sq(m,m̂))` | Finite Cauchy–Schwarz over uniform `P_X`: the bias is bounded by the product of `L²` nuisance errors. **Proved.** |
| `aipwScore_bound` | `\|aipwScoreFin mhat ghat z\| ≤ 1 + 2/ε` | The score is bounded (center valid + `m̂` `ε`-off `{0,1}`). **Proved.** |
| `aipw_var_bound` | `variance (estAIPW mhat ghat n) (productLaw hv n) ≤ (1+2/ε)²/n` | Variance of the i.i.d. average (via `variance_sum_pi` + bounded score). **Proved.** |

### 13sa.12 Capstone — `Optimality.lean` (minimax rate is `Θ(√(εg·εm))`, attained by AIPW)

Pairs the converse (`minimax_lower_bound_var`) with the AIPW achievability into the
optimality statement. **All proved, axiom-clean** (`aipw_attains_minimax_rate` uses
only `propext`/`Classical.choice`/`Quot.sound`).

| Declaration | Statement | Description |
|---|---|---|
| `aipw_mean_eq` | `0<n ⇒ ∫ estAIPW … ∂(productLaw hv n) = Σ_z obsReal·score` | Mean of the i.i.d. average equals the single-obs population mean (via `measurePreserving_eval` + `aipw_pop_mean`). **Proved.** |
| `aipw_nMiss_le` | bias `≤ b`, `variance ≤ V`, `b<s` ⇒ `nMiss hv n (estAIPW …) s ≤ V/(s−b)²` | **Bias-centered Chebyshev** miss bound (`meas_ge_le_variance_div_sq`; `MemLp` free on the finite domain via `eLpNorm_lt_top_of_finite`). **Proved.** |
| `aipw_inclass_bias_bound` | `InClass mhat ghat εg εm m g ⇒ \|bias\| ≤ ε⁻¹·(2√εg·√εm)` | Uniform-over-class bias bound: `aipw_bias_bound` + `l2sq ≤ εg/εm` via `Real.sqrt` monotonicity. **Proved.** |
| `aipw_minimaxMiss_le` | `s > ε⁻¹·2√εg√εm ⇒ minimaxMiss mhat ghat εg εm n (estAIPW …) s ≤ ((1+2/ε)²/n)/(s − ε⁻¹·2√εg√εm)²` | **Worst-case AIPW miss bound**, uniform over the class (`ciSup_le` + the three pieces), `→ 0` as `n·εg·εm → ∞`. **Proved.** |
| `MinimaxRateThreshold mhat ghat εg εm n` | `structure` | Reusable "rate attained + unbeatable" vocabulary: a single `estimator` with a converse miss-lower-bound (`probLower` at `sepLower`) and an achievability miss-upper-bound (`missUpper` at `sepUpper`). |
| `aipw_attains_minimax_rate` | `noncomputable def`, `MinimaxRateThreshold P.mhatV P.ghatV εg εm n` | **Capstone.** For the cell-varying construction `P` with the lower-bound budgets/regularity: every estimator misses by `s_P = gap/2 ≍ √(εg·εm)` w.p. `≥ 1/4` (`converse` ← `minimax_lower_bound_var`), while the AIPW estimator's worst-case miss at any `sepUpper > ε⁻¹·2√εg√εm` (also `Θ(√(εg·εm))`) is `≤ ((1+2/ε)²/n)/(sepUpper−…)²`. Hence the minimax ATE rate is `Θ(√(εg·εm))` and DR/AIPW attains it (constant-factor, not constant-sharp). **Axiom-clean.** |

### 13sa.C Causal grounding — `Causal/Construction.lean`, `Causal/Bridge.lean`, `Causal/Minimax.lean`

Grounds the observed-data contrast `ate g` in the potential-outcome framework: the minimax lower bound is a bound on the genuine causal ATE `E[Y(1) − Y(0)]`, identified by backdoor adjustment, not merely a regression contrast. `Construction.lean` builds — from a finite DGP `(m, g)` on a finite covariate `C` — the first concrete `POBackdoorSystem` in the library: a backdoor SCM `Un → Xc → A → Y`, `Xc → Y` with latent noise roots `Ea → A`, `Ey → Y` (`A = 1{Ea ≤ m Xc}`, `Y = 1{Ey ≤ g A Xc}`), lifted through `POSystem.ofSCM`. `Bridge.lean` defines the causal estimand and proves it equals the contrast.

| Declaration | Statement | Description | Status |
|---|---|---|---|
| `dgpSCM` / `dgpPO` / `dgpBackdoor` | the witness gSCM, its `ofSCM` PO system, and the extracted `POBackdoorSystem (dgpPO m g) C` | The concrete finite backdoor construction (treatment `A`, outcome `Y`, covariate `Xc`); `structFun_measurable`/`isProbability_latent` discharged | **Defined** |
| `causalATE m g` | `(dgpBackdoor m g).ATE` | The causal estimand `E[Y(1) − Y(0)]` of the constructed system | **Defined** |
| `dgp_consistency` / `dgp_unconfoundedness` / `dgp_propScore_eq_m` / `dgp_adjustedCE_eq_g` / `dgp_overlap` | the backdoor `Assumptions` fields | Consistency (free from `ofSCM_consistency`); unconfoundedness `A ⟂ (Y(1),Y(0))∣X` (via `ofSCM_condIndepCF_of_dSep` + `condIndepCF_congr_cond`); `P[A=1∣X]=m(X)` and `E[Y∣A=d,X]=g(d,X)` (uniform-coordinate conditional expectations); two-sided overlap from strict `m ∈ (0,1)` | **Proved** |
| `causalATE_eq_ate` | `(hv : ValidDGP m g) (hso : ∀ x, m x ∈ Ioo 0 1) → causalATE m g = ate g` | **Estimand bridge.** The causal ATE equals the observed-data contrast `ate g`, via `BackdoorEstimationSystem.θ₀_eq_ATE` + uniform `P_X`. So every minimax lower bound transfers to `E[Y(1) − Y(0)]`. Axiom-clean (standard axioms + `native_decide` for the graph checks) | **Proved** |
| `minimaxMissCausal mhat ghat εg εm n est s` | `⨆ p : InClassDGP mhat ghat εg εm, (productLaw p.valid n).real {x | s ≤ \|est x − causalATE p.m p.g\|}` | Causal-centered analogue of `minimaxMiss`, with each miss event centered at the concrete backdoor causal ATE instead of the observed-data contrast. | **Defined** |
| `minimax_lower_bound_var_causal` | `hm`, `hg`, strict `∀j, P.m₀ j * (1 + P.β / P.g₁ j) < 1`, budgets/regularity ⇒ `1/4 ≤ minimaxMissCausal P.mhatV P.ghatV … gap/2` | Causal version of the cell-varying minimax lower bound. The additional strict-overlap hypothesis keeps the perturbed Var witnesses inside the overlap region, allowing `causalATE_eq_ate` to transfer the observed-data lower bound to `E[Y(1) − Y(0)]`. | **Proved** |

---

## 13eff. `Estimation/Efficiency/` — Hahn (1998) semiparametric efficiency bound

The **third vertex** of the ATE efficiency triangle, joining achievability
(`dml_ATE_isAsymLinear`, §13c) and the minimax lower-bound tower (§13sa): the
*semiparametric efficiency bound* `= Var(efficient influence function)`, the
theorem that the AIPW influence function `ψ_AIPW` (§13a.2) **is** Hahn's efficient
influence function, and the "role of the propensity score" (the bound does not
depend on whether the propensity score is known). Reference: Hahn (1998),
*On the Role of the Propensity Score in Efficient Semiparametric Estimation of
Average Treatment Effects*, Econometrica 66(2):315–331. Plan: `doc/hahn_efficiency_plan.md`.

### 13eff.1 `Estimation/Efficiency/TangentProjection.lean` — abstract Hilbert efficiency machine

Pure Hilbert-space geometry over a real inner-product space `H` with a tangent
subspace `T` (`[T.HasOrthogonalProjection]`). The reusable abstraction every
future estimator-efficiency theorem cites. All **sorry-free, axiom-clean**.

| Declaration | Statement | Description |
|---|---|---|
| `IsGradient T g ψ` | `Prop := ∀ s ∈ T, ⟪ψ, s⟫_ℝ = ⟪g, s⟫_ℝ` | `ψ` is an influence function / gradient agreeing with reference `g` on every tangent direction. |
| `efficientIF T g` | `noncomputable def := ↑(orthogonalProjection T g)` | The efficient influence function = orthogonal projection of any gradient onto the tangent space. |
| `effBound T g` | `noncomputable def := ‖efficientIF T g‖^2` | The semiparametric efficiency bound. |
| `efficientIF_mem` | `efficientIF T g ∈ T` | The EIF lies in the tangent space. |
| `isGradient_iff_sub_mem_orthogonal` | `IsGradient T g ψ ↔ ψ − g ∈ Tᗮ` | Gradients form the affine coset `g + Tᗮ`. |
| `efficientIF_isGradient` | `IsGradient T g (efficientIF T g)` | The EIF is itself a gradient. |
| `orthogonalProjection_eq_of_isGradient` | `IsGradient T g ψ ⇒ orthogonalProjection T ψ = efficientIF T g` | The projection onto `T` is the same for every gradient. |
| `normSq_gradient_decomp` | `IsGradient T g ψ ⇒ ‖ψ‖² = ‖efficientIF T g‖² + ‖ψ − efficientIF T g‖²` | Pythagoras decomposition along the tangent space. |
| `effBound_le_normSq` | `IsGradient T g ψ ⇒ effBound T g ≤ ‖ψ‖²` | **The efficiency lower bound**: no gradient beats the EIF's norm. |
| `norm_eq_iff_eq_efficientIF` | `IsGradient T g ψ ⇒ (‖ψ‖² = effBound T g ↔ ψ = efficientIF T g)` | Sharpness: equality only at the EIF. |
| `efficientIF_unique` | `IsGradient T g ψ → ψ ∈ T → ψ = efficientIF T g` | A gradient lying in `T` is the EIF. |
| `efficientIF_eq_self_of_mem` | `g ∈ T ⇒ efficientIF T g = g` | A tangent reference is its own EIF. |
| `effBound_eq_of_mem_sub` | `T' ≤ T → g ∈ T' ⇒ efficientIF T' g = g ∧ efficientIF T g = g ∧ effBound T' g = effBound T g` | **Tangent-shrinking** corollary: removing tangent directions orthogonal to `g` leaves the bound unchanged. |

### 13eff.2 `Estimation/Efficiency/Submodel.lean` — regular parametric submodels via exponential tilting

The analytic engine for pathwise derivatives. For a base law `P` and a bounded
mean-zero score `s`, the exponential tilt `P_t = (e^{t·s}/c(t))·P` realizes `s`
as a regular submodel. All **sorry-free**.

| Declaration | Statement | Description |
|---|---|---|
| `tiltNorm P s t` | `noncomputable def := ∫ e^{t·s} dP` | Normalizing constant `c(t)`. |
| `tiltExp P s φ t` | `noncomputable def := (∫ φ·e^{t·s} dP)/tiltNorm` | Tilted expectation `E_{P_t}[φ]`. |
| `tiltMeasure P s t` | `noncomputable def := (ofReal c(t))⁻¹ • P.withDensity (ofReal e^{t·s})` | The tilted probability measure `P_t`. |
| `tiltNorm_zero` | `tiltNorm P s 0 = 1` | At `t=0` the tilt is `P`. |
| `hasDerivAt_tilt_numerator` | `HasDerivAt (fun t => ∫ φ·e^{t·s} dP) (∫ φ·s dP) 0` | Differentiation under the integral sign (Mathlib `hasDerivAt_integral_of_dominated_loc_of_deriv_le`), bounded score + integrable `φ`. |
| `hasDerivAt_tiltExp` | bounded mean-zero `s`, integrable `φ` ⇒ `HasDerivAt (tiltExp P s φ) (∫ φ·s dP) 0` | **The tilt-derivative lemma**: `d/dt E_{P_t}[φ]|₀ = Cov_P(φ,s) = ∫ φ·s dP`. The analytic core of every pathwise-derivative argument. |
| `tilt_exp_integrable` | `Integrable (e^{t·s}) P` | Bounded score ⇒ density integrable (comparison with constant). |
| `tiltNorm_pos` | `0 < tiltNorm P s t` | Positivity of the normalizer (lower-bounded by `e^{−|t|M}`). |
| `isProbabilityMeasure_tiltMeasure` | `IsProbabilityMeasure (tiltMeasure P s t)` | The tilt has total mass 1 (`ofReal_integral_eq_lintegral_ofReal` + `ENNReal.inv_mul_cancel`). |
| `integral_tiltMeasure` | `∫ h ∂(tiltMeasure P s t) = tiltExp P s h t` | Bridge: expectations under `P_t` are tilted expectations (`integral_withDensity_eq_integral_smul₀`). |

### 13eff.3 `Estimation/Efficiency/ATEVariance.lean` — the Hahn variance decomposition

| Declaration | Statement | Description |
|---|---|---|
| `hahn_weight_true_Linf`, `hahn_weight_false_Linf` | `MemLp (a/e)`/`((1−a)/(1−e)) ⊤ P.μ` | Overlap ⇒ the IPW weights are bounded (private helpers). |
| `hahn_mu_L2` | `MemLp (μ_val d ∘ X) 2 P.μ` | Conditional-Jensen `L²` bound on the regression (private helper). |
| `aipw_variance_hahn_decomposition` | `∫ ψ_AIPW² dP_Z = ∫ (μ₁−μ₀−τ)² dP_X + ∫ (a/e²)(Y−μ₁)² dP_Z + ∫ ((1−a)/(1−e)²)(Y−μ₀)² dP_Z` | **The Hahn efficiency-bound formula.** `ψ = A+B−C`, `BC=0` (`a∈{0,1}`), the two cross-terms vanish via `weighted_residual_integral_zero`. The last two terms are `E[σ²₁/e]`, `E[σ²₀/(1−e)]`. **Proved.** |
| `dml_ATE_attains_hahn_bound` | `√\|B(n)\|·(θ̂ⁿ − θ₀) ⇒ N(0, V_H)` | AIPW/DML attains the Hahn bound: `dml_ATE_tendstoNormal` with the limit variance rewritten as the 3-term `V_H`. **Proved.** |

### 13eff.4 `Estimation/Efficiency/ATETangent.lean` — ψ_AIPW is the efficient influence function

Instantiates the abstract machine (§13eff.1) in `H = Lp ℝ 2 P_Z`, where the
mean-zero tangent space is `Tfull = (ℝ∙1)ᗮ`. All **sorry-free**.

| Declaration | Statement | Description |
|---|---|---|
| `propensity_score_residual_integral_zero` | `∫ h(X)·(1_{D=1} − e_val(X)) dμ = 0` | The propensity residual is mean-zero given `σ(X)` (private helper). |
| `aipw_orthogonal_propensity_score` | `∫ ψ_AIPW·(α(X)·(indA − e_val(X))) dP_Z = 0` | **Role of the propensity score**: `ψ_AIPW` is orthogonal to every propensity-score direction (B/C terms via `weighted_residual_integral_zero`, A term via the residual helper). **Proved.** |
| `instIsProbabilityMeasure_P_Z` | `IsProbabilityMeasure S.P_Z` | The data law is a probability measure. |
| `aipw_memLp` | `MemLp ψ_AIPW 2 P_Z` | `ψ_AIPW ∈ L²(P_Z)` (from `aipw_finite_var`). |
| `aipwLp`, `oneLp`, `Tfull` | `Lp ℝ 2 P_Z` elements / `(ℝ∙oneLp)ᗮ` | `ψ_AIPW` and constant `1` as `L²` classes; the mean-zero tangent space. |
| `inner_oneLp` | `⟪f, oneLp⟫ = ∫ f dP_Z` | The `L²` inner product against `1` is the mean. |
| `aipwLp_mem_tangent` | `aipwLp ∈ Tfull` | `ψ_AIPW` is mean-zero (`aipw_mean_zero`), so lies in the tangent space. |
| `aipw_is_efficientIF` | `efficientIF Tfull aipwLp = aipwLp` | **ψ_AIPW is its own efficient influence function** (it lies in the tangent space). |
| `effBound_eq_variance`, `effBound_eq_hahn` | `effBound Tfull aipwLp = ∫ ψ_AIPW² dP_Z = V_H` | The efficiency bound equals `Var(ψ_AIPW)` equals the Hahn formula. |
| `efficiency_bound_optimal` | `IsGradient Tfull aipwLp ψ ⇒ effBound Tfull aipwLp ≤ ‖ψ‖²` | **The semiparametric efficiency bound**: every influence function of the ATE has variance `≥ V_H`; `ψ_AIPW` attains it. |
| `effBound_eq_of_known_propensity` | `T' ≤ Tfull → aipwLp ∈ T' ⇒ effBound T' aipwLp = effBound Tfull aipwLp` | **Propensity-invariance** (Hahn's headline): the bound is unchanged when the tangent space shrinks to the propensity-known model (`ψ_AIPW` is still in it by `aipw_orthogonal_propensity_score`), so knowing the propensity score does not improve efficiency. |

### 13eff.5 `Estimation/Efficiency/ATEEfficientIF.lean` — pathwise ATE efficient influence function

This capstone module connects Hahn's pathwise derivative calculation to the
abstract pathwise-gradient and projection machinery. It records the bridge from
the AIPW covariance formula to the `L²(P_Z)` inner product, identifies the full
mean-zero space as the nonparametric tangent space under the supplied density
hypothesis, and states the conditional Hahn theorem that `ψ_AIPW` is the
canonical efficient influence function for the backdoor ATE.

<!-- GEN:Causalean.Estimation.Efficiency.ATEEfficientIF -->
| Decl | Signature | Description |
|---|---|---|
| `Estimation.ATE.BackdoorEstimationSystem.inner_aipwLp_eq_integral` | `inner ℝ (S.aipwLp h_overlap hA h_y2 h_yd2) f = ∫ (z : γ × Bool × ℝ), S.ψ_AIPW z * ↑↑f z ∂S.P_Z` | The covariance pairing between the AIPW influence function and any square-integrable score equals the Hilbert-space inner product against the AIPW element in the observed-data square-integrable space. |
| `Estimation.ATE.BackdoorEstimationSystem.score_mem_Tfull` | `m.score ∈ S.Tfull` | Every regular submodel score is mean-zero, so every such score belongs to the full nonparametric tangent space for the observed-data law. |
| `Estimation.ATE.BackdoorEstimationSystem.isTangentSpace_Tfull` | `S.Tfull ≤ Causalean.Estimation.Efficiency.tangentSpace S.oneLp S.P_Z → Causalean.Estimation.Efficiency.IsTangentSpace S.oneLp S.P_Z S.Tfull` | The full mean-zero space is a genuine tangent space once the supplied nonparametric-model hypothesis says it is contained in the closed span of regular-submodel scores. |
| `Estimation.ATE.BackdoorEstimationSystem.aipw_isPathwiseGradient_ATE` | `(∀ (m : Causalean.Estimation.Efficiency.RegularSubmodel S.oneLp S.P_Z), HasDerivAt (fun t => ψ (m.path t)) (∫ (z : γ × Bool × ℝ), S.ψ_AIPW z * ↑↑m.score z ∂S.P_Z) 0) → Causalean.Estimation.Efficiency.IsPathwiseGradient S.oneLp S.P_Z ψ (S.aipwLp h_overlap hA h_y2 h_yd2)` | Hahn's pathwise-derivative identity makes the AIPW score a pathwise gradient of the backdoor ATE functional along every regular submodel. |
| `Estimation.ATE.BackdoorEstimationSystem.aipw_is_efficientInfluenceFunction` | `(∀ (m : Causalean.Estimation.Efficiency.RegularSubmodel S.oneLp S.P_Z), HasDerivAt (fun t => ψ (m.path t)) (∫ (z : γ × Bool × ℝ), S.ψ_AIPW z * ↑↑m.score z ∂S.P_Z) 0) → S.Tfull ≤ Causalean.Estimation.Efficiency.tangentSpace S.oneLp S.P_Z → Causalean.Estimation.Efficiency.IsPathwiseGradient S.oneLp S.P_Z ψ (S.aipwLp h_overlap hA h_y2 h_yd2) ∧ S.aipwLp h_overlap hA h_y2 h_yd2 ∈ S.Tfull ∧ ∀ (g : ↥(MeasureTheory.Lp ℝ 2 S.P_Z)), Causalean.Estimation.Efficiency.IsPathwiseGradient S.oneLp S.P_Z ψ g → Causalean.Estimation.Efficiency.efficientIF S.Tfull g = S.aipwLp h_overlap hA h_y2 h_yd2` | AIPW is the efficient influence function for the backdoor ATE. Let `S` be a backdoor average-treatment-effect estimation system with strict overlap at level ε, satisfying the system's core identification assumptions, in which the observed outcome has finite second moment and each potential outcome under treatment level `d` has finite second moment. Suppose a functional ψ of the observed-data law satisfies Hahn's pathwise-derivative identity — along every regular submodel, ψ's derivative at the true law equals the covariance of the AIPW score with the submodel's score, and that the full mean-zero space is a genuine nonparametric tangent space, i.e. it is contained in the closed span of regular-submodel scores. Then the AIPW influence function is a pathwise gradient of ψ, lies in the full mean-zero tangent space, and is the canonical efficient influence function: for every other pathwise gradient `g` of ψ, projecting `g` onto the tangent space returns the AIPW influence function. |
<!-- /GEN -->

---

## 14. `Panel/` — Panel regression algebra

> **PARTIALLY ARCHIVED 2026-06-12.** The dead CausalSmith research *question grammar* and its
> population machinery (`Regression/`, `PathLimit/`, `Probability/`, `DGP/`,
> `Baseline/`, `Assignment/`, `Target/`, `Question.lean`, `Bridge.lean`) were moved
> to `archive/Causalean/Panel/` and their API descriptions removed from this file
> (CausalSmith research now proposes free-form questions; no live consumer; `CausalSmith`
> imports nothing from `Causalean.Panel`). See `archive/README.md` and
> `docs/superpowers/specs/2026-06-12-panel-substrate-unification-design.md`.

The cell / inner-product / projection / FWL machinery lives in
`Causalean/Panel/Weighted/` (see §13W). The live `Panel/` layer provides the
`R = I × T` instantiation (`Cells I T := WeightedSupport (I × T)`) plus the
panel-specific apparatus: balanced panels (`Cells.balanced`), the two-way
fixed-effect subspace (`H_twfe` via `Weighted.twoAxisAdditiveSpan I T`), the
cell-level potential-outcome layer (`PO/`), the share-weighted two-way panel
(`WeightedTwoWayPanel`, §14c-WTWP), and the TWFE indicator closed forms
(`FixedEffect/IndicatorClosedForms`, §14d-IndicatorClosedForms). It is
**algebraic, not estimation-theoretic**: finite-sample statements are identities
conditional on the treatment path.

Live subsections: §14a (`Cells` + `InnerProduct`), §14b (`Subspace` +
`FixedEffect`), §14c (`PO/`), §14c-WTWP (`WeightedTwoWayPanel`),
§14d-IndicatorClosedForms (`FixedEffect/IndicatorClosedForms`).

### 14a. `Panel/Cells.lean` + `Panel/InnerProduct.lean`

#### 14a.1 `Panel/Cells.lean`

Observed unit-period cells `R ⊆ I × T` with strictly positive weights summing
to 1.  Mirrors Definition 2.1 of the projection note.

| Declaration | Signature | Description |
|---|---|---|
| `Cells I T` | `structure` (over `[Fintype I] [Fintype T] [DecidableEq I] [DecidableEq T]`) | Bundles `observed : Finset (I × T)` (nonempty), `weight : I × T → ℝ`, `weight_pos`, `weight_zero_off`, `weight_sum_one`. |
| `Cells.weight_nonneg` | `0 ≤ c.weight r` for all `r` | Pointwise nonnegativity (positive on `observed`, zero off it). **Proved.** |
| `Cells.sum_weight` | `∑ r ∈ c.observed, c.weight r = 1` | Restated normalization. **Proved.** |
| `Cells.sum_weight_univ` | `∑ r, c.weight r = ∑ r ∈ c.observed, c.weight r` | Sum over `Finset.univ` agrees with sum over `observed`. **Proved.** |
| `Cells.sum_weight_univ_eq_one` | `∑ r, c.weight r = 1` | Normalization restated over `Finset.univ`. **Proved.** |
| `Cells.balanced` | `Cells I T` (under `[Nonempty I] [Nonempty T]`) | Balanced panel: `observed = univ`, `weight = 1/(\|I\|·\|T\|)`. `noncomputable`. |
| `Cells.balanced_observed`, `balanced_weight` | `@[simp]` | Definitional unfoldings of `balanced`. |

#### 14a.2 `Panel/InnerProduct.lean`

Weighted inner product on scalar arrays and its matrix-valued lift to vector
arrays modeled as `Fin K → ((I × T) → ℝ)`.

| Declaration | Signature | Description |
|---|---|---|
| `Cells.ip` | `(c : Cells I T) → (I × T → ℝ) → (I × T → ℝ) → ℝ` | `c.ip A B = ∑ r ∈ c.observed, c.weight r · A r · B r`. |
| `Cells.ip_def` | `@[simp]` unfolding | Direct unfolding of `c.ip A B`. **Proved.** |
| `Cells.ip_symm` | `c.ip A B = c.ip B A` | Symmetry. **Proved.** |
| `Cells.ip_add_left` / `ip_add_right` | bilinearity | Additivity in left / right argument. **Proved.** |
| `Cells.ip_smul_left` / `ip_smul_right` | `c.ip (s • A) B = s · c.ip A B` (and symmetrically) | Scalar homogeneity. **Proved.** |
| `Cells.ip_self_summand_nonneg` | `∀ r ∈ c.observed, 0 ≤ c.weight r * A r * A r` | Per-summand nonnegativity, used by both positivity and definiteness. **Proved.** |
| `Cells.ip_self_nonneg` | `0 ≤ c.ip A A` | Positivity. **Proved.** |
| `Cells.ip_self_eq_zero_iff` | `c.ip A A = 0 ↔ ∀ r ∈ c.observed, A r = 0` | Definiteness on the support `R`. **Proved.** |
| `Cells.ipMat` | `(Fin K → I × T → ℝ) → (Fin K → I × T → ℝ) → Matrix (Fin K) (Fin K) ℝ` | Entrywise inner product `(c.ipMat A B) j k = c.ip (A j) (B k)`. |
| `Cells.ipMat_apply` | `@[simp]` unfolding | Direct unfolding. **Proved.** |
| `Cells.ipMat_transpose` | `(c.ipMat A B).transpose = c.ipMat B A` | Transposed symmetry. **Proved.** |
| `Cells.ipMat_add_left` / `ipMat_add_right` | bilinearity | Additivity in each tuple argument (entrywise). **Proved.** |
| `Cells.ipMat_smul_left` / `ipMat_smul_right` | `c.ipMat (s • A) B = s • c.ipMat A B` (and symmetrically) | Scalar homogeneity entrywise. **Proved.** |

### 14b. `Panel/Subspace.lean` + `Panel/FixedEffect.lean`

Nuisance spaces, weighted residualization, and the two-way fixed-effect
subspace from Definition 2.2.

| Declaration | Signature | Description |
|---|---|---|
| `Cells.V` | `(I T : Type*) → Type _ := (I × T) → ℝ` | Scalar cell-array space used throughout `Panel/`. |
| `Cells.weighted_orthogonal_projection_exists` | `∃ P : V →ₗ[ℝ] V, ...` | Existence of a weighted orthogonal projection onto a subspace. Proved by packaging `c.ip` as a positive-semidefinite bilinear form and applying the generic semidefinite-projection helper. |
| `Cells.proj` | `Cells I T → Submodule ℝ (V I T) → V I T →ₗ[ℝ] V I T` | Chosen weighted projection onto a nuisance subspace. |
| `Cells.proj_mem` / `proj_orthogonal` | membership and orthogonality lemmas | Defining properties of `proj`. **Proved.** |
| `Cells.proj_apply_of_mem` / `proj_apply_eq_of_mem_orthogonal` / `proj_idem_apply` | supportwise projection identities | Projection agrees with members of `H` on observed cells; any orthogonal candidate nuisance component agrees with the chosen projection on observed cells; projection is idempotent there. |
| `Cells.residualize` | `V I T →ₗ[ℝ] V I T` | Residual maker `M_H = id - P_H`. |
| `Cells.tildeX` / `tildeXVec` | scalar and column-wise residualization | Residualized scalar array and `Fin K` tuple of arrays. |
| `Cells.residualize_in_orthogonal` | `c.ip (c.tildeX H X) h = 0` for `h ∈ H` | Key residual orthogonality. **Proved.** |
| `Cells.residualize_self_of_mem` / `residualize_idem_apply` | supportwise residual identities | Residuals of nuisance-space arrays vanish on observed cells; `M_H` is idempotent on observed cells. |
| `Cells.H_twfe` | `Submodule ℝ (V I T)` | Two-way fixed-effect subspace `{h : h(i,t)=a_i+b_t}`. |
| `Cells.const_mem_H_twfe` | constants are in `H_twfe` | Fixed-effect space contains constant arrays. **Proved.** |
| `Cells.H_twfe_finiteDimensional` | `Module.Finite ℝ (c.H_twfe)` | Finite-dimensionality inherited from the finite ambient cell-array space. |

### 14c. `Panel/PO/` — cell-level potential outcomes

Statement-level scaffolding for the panel potential-outcomes layer.
Mirrors Definitions 2.3–2.4 and Assumption 2.1 of
`CausalSmith/doc/general_projection_carryover_note.tex`.  Treatment paths,
exposure mappings, the bundled `PanelPOSystem`, and the (definitional)
consistency lemma.  Conditional means `m_r^0(D)` and exposure responses
`τ_r(e | D)` are defined via `MeasureTheory.condExp` against the
σ-algebra `σ(D)`.

#### 14c.1 `Panel/PO/TreatmentPath.lean`

Realized treatment paths and finite-memory histories.

| Declaration | Signature | Description |
|---|---|---|
| `TreatmentPath` | `(I T A : Type*) → Type _ := I → T → A` | A unit's treatment trajectory. Mirrors `D = (D_{it})` of Def. 2.3. |
| `TreatmentPath.History` | `[Inhabited A] (p : ℕ) (D : TreatmentPath I (Fin T₀) A) (i : I) (t : Fin T₀) : Fin (p+1) → A` | Length-`p+1` history `(D_{it}, …, D_{i,t-p})` clamped to `default` for out-of-range lags. Implements the `D_{is} = 0` boundary convention. |
| `TreatmentPath.History_zero` | `History p D i t 0 = D i t` | Lag-0 unfolding (contemporaneous treatment). `@[simp]`. **Proved.** |

#### 14c.2 `Panel/PO/Exposure.lean`

Exposure mappings and baseline exposures.

| Declaration | Signature | Description |
|---|---|---|
| `Exposure` | `(I T A : Type*) (E : I × T → Type*) → Type _ := (r : I × T) → TreatmentPath I T A → E r` | Cell-indexed known function `E_r = e_r(D)` of the realized treatment paths. Mirrors Def. 2.3. |
| `BaselineExposure` | `{I T : Type*} (E : I × T → Type*) → Type _ := (r : I × T) → E r` | Baseline `e_r^0 ∈ E_r` for each cell. Mirrors `e_r^0` of Def. 2.4. |

A binary finite-memory builder is intentionally not shipped (awkward dependent-type instantiation); downstream files build the `Exposure` inline against `TreatmentPath.History`.

#### 14c.3 `Panel/PO/CellPO.lean`

Bundled panel potential-outcomes system and its derived objects.

| Declaration | Signature | Description |
|---|---|---|
| `PanelPOSystem` | `structure` | Bundles `I, T, A, E, e, e0, Ω, μ, D, Y` together with the typeclass instances (`Fintype`, `DecidableEq`, `MeasurableSpace`, `IsProbabilityMeasure`) and measurability/integrability hypotheses (`D_meas`, `Y_meas`, `Y_integrable`). Mirrors Defs. 2.3–2.4. |
| `PanelPOSystem.observedExposure` | `(r : I × T) → Ω → E r := e r (D ω)` | Realized exposure at cell `r`. |
| `PanelPOSystem.observedY` | `(r : I × T) → Ω → ℝ := Y r (observedExposure r ω) ω` | Realized outcome at cell `r`. |
| `PanelPOSystem.DSigmaAlg` | `MeasurableSpace Ω` | σ-algebra `σ(D)` generated by the treatment-path random variable, defined as the supremum over `(i, t)` of the comap of `(fun ω => D ω i t)`. |
| `PanelPOSystem.DSigmaAlg_le` | `DSigmaAlg ≤ ambient σ-alg` | `σ(D)` is a sub-σ-algebra, proved from the cellwise measurability hypotheses `D_meas` via `measurable_iff_comap_le` and `iSup_le`. |
| `PanelPOSystem.m0` | `(r : I × T) → Ω → ℝ` | Baseline conditional mean `m_r^0(D) := E[Y_r(e_r^0) | σ(D)]`. `noncomputable`. Mirrors `m_r^0` of Def. 2.4. |
| `PanelPOSystem.tau` | `(r : I × T) → E r → Ω → ℝ` | Cell-level exposure response `τ_r(e | D) := E[Y_r(e) − Y_r(e_r^0) | σ(D)]`. `noncomputable`. Mirrors `τ_r(e | D)` of Def. 2.4. |
| `PanelPOSystem.realizedTau` | `(r : I × T) → Ω → ℝ := tau r (observedExposure r ω) ω` | Realized exposure response `τ_r(E_r | D)`. `noncomputable`. |

#### 14c.4 `Panel/PO/Assumptions/Consistency.lean`

Panel consistency (Assumption 2.1).  Definitional under the structure setup.

| Declaration | Signature | Description |
|---|---|---|
| `PanelPOSystem.observedY_eq_potentialOutcome` | `observedY r ω = Y r (observedExposure r ω) ω` | Pointwise consistency, by `rfl`. **Proved.** |
| `PanelConsistency` | `(P : PanelPOSystem) → Prop` | Universally-quantified consistency statement: `∀ r ω, observedY r ω = Y r (observedExposure r ω) ω`. Named alias for Assumption 2.1. |
| `panelConsistency_holds` | `PanelConsistency P` | Consistency holds by `rfl` under the structure setup. **Proved.** |

#### 14c.5 `Panel/PO/Mobius.lean`

Boolean-cube inclusion-exclusion for Lemma 3.1.

| Declaration | Signature | Description |
|---|---|---|
| `PO.Mobius.indicator` | `Finset (Fin (p+1)) → Fin (p+1) → Fin 2` | Binary history with ones on the selected subset. |
| `PO.Mobius.indicator_empty` | `indicator ∅ = fun _ => 0` | Empty-subset indicator is the zero history. **Proved.** |
| `PO.Mobius.delta` | `((Fin (p+1) → Fin 2) → ℝ) → Finset (Fin (p+1)) → ℝ` | Boolean-cube interaction coefficient `δ_S` by alternating sums. |
| `PO.Mobius.prod_indicator_eq` | product over `S` equals `1` iff `S` is contained in active history support | Converts binary products to subset tests. **Proved.** |
| `PO.Mobius.coeff_sum` | alternating coefficient sum is `1` on `A=B`, else `0` | Inclusion-exclusion coefficient identity. **Proved.** |
| `PO.Mobius.mobius_expansion` | `τ h = ∑_{nonempty S} δ τ S * ∏_{k∈S} h k` when `τ 0 = 0` | Lemma 3.1 Boolean-cube expansion. **Proved.** |

### 14c-WTWP. `Panel/WeightedTwoWayPanel.lean` — Share-weighted two-way panel (L1 unification target)

Share-weighted generalization of `Panel/UniformTwoWayPanel`
(audit `shared_core.md` item **M5**): two-way demeaning under a general unit pmf
`p` (Goodman-Bacon cohort shares; `p ≡ 1/|Unit|` recovers the uniform panel) with
a uniform period measure. Intended as the single L1 demeaning/FWL layer the
EstimandCharacterization papers instantiate (migration is a separate, review-gated
step).

A **bridge** section connects these concrete closed forms to the generic
`WeightedSupport` FWL substrate (`Panel/Weighted/`, §13W): the panel is the cell
support `R = Unit × Time` with weight `ω_{(i,t)} = p_i/|Time|`, the `p`-weighted
inner product is `|Time|` times `WeightedSupport.ip`, and `ddot` is the generic
residual maker against the two-axis additive span. The abstract FWL coefficient
lemma `Weighted.WeightedSupport.scalar_fwl_of_normalEqs` is thus reusable through
the bridge, so the former bespoke (and consumer-less)
`finite_residualized_coefficient_eq_of_normalEqs` and its helper
`finiteResidualizedCoefficient` were removed. (The same-named live helpers in
`UniformTwoWayPanel`, §9i0a-UniformTwoWayPanel, are unaffected.)

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `WeightedTwoWayPanel.UnitWeights` | `structure { p, pos, sum_one }` | Unit weights as a probability vector (`0 < p i`, `∑ p = 1`) | **Defined** |
| `unitMean` / `timeMean` / `grandMean` | `… → ℝ` | Uniform-time unit mean, `p`-weighted time mean `∑_i p_i V_{it}`, `p`-weighted grand mean | **Defined** |
| `ddot` | `(w) (V) (i) (t) → ℝ` | Double-demeaned residual `V − unitMean − timeMean + grandMean` | **Defined** |
| `inner` | `(w) (V W) → ℝ` | `p`-weighted finite inner product `∑_i ∑_t p_i · V_{it} W_{it}` | **Defined** |
| `IsUnitTimeAdditive` | `abbrev` | Shared additive-class predicate `h_it = a_i + b_t` | **Defined** |
| `ddot_reconstruct` / `sub_ddot_eq_unitTimeProjection` / `unitTimeProjection_additive` | identities | Reconstruction + residual/projection identities | **Proved** |
| `ddot_orthogonal_unit` | `(hT : 0 < card Time) → ∑_i ∑_t p_i (ddot·a_i) = 0` | `ddot ⟂` unit-only functions (period sum of `ddot` vanishes per unit) | **Proved** |
| `ddot_orthogonal_time` | `∑_i ∑_t p_i (ddot·b_t) = 0` | `ddot ⟂` time-only functions (`p`-weighted unit sum vanishes per period) | **Proved** |
| `ddot_orthogonal_unit_time` | `IsUnitTimeAdditive h → inner w (ddot V) h = 0` | `ddot ⟂` the whole additive class | **Proved** |
| `cellSupport` *(bridge)* | `(w) → WeightedSupport (Unit × Time)` | The panel as a cell-indexed weighted support, weight `ω_{(i,t)} = p_i/|Time|` (every cell observed); needs `[Nonempty Unit] [Nonempty Time]` | **Defined** |
| `mem_twoAxisAdditiveSpan_iff` *(bridge)* | `h ∈ twoAxisAdditiveSpan ↔ IsUnitTimeAdditive (uncurry h)` | Two-axis additive span membership = the panel additive-class predicate | **Proved** |
| `inner_eq_card_smul_ip` *(bridge)* | `inner w V W = |Time| · (cellSupport w).ip V W` | The `p`-weighted panel inner product is `|Time|` times the generic `WeightedSupport.ip` | **Proved** |
| `ddot_eq_residualize` *(bridge)* | `(cellSupport w).residualize (twoAxisAdditiveSpan) V (i,t) = ddot w V i t` | Concrete↔abstract: `ddot` is the generic residual maker against the two-axis additive span (pointwise, every cell observed) | **Proved** |

### 14d-IndicatorClosedForms. `Panel/FixedEffect/IndicatorClosedForms.lean` — TWFE residual closed forms

Reusable substrate for `Cells.tildeX c.H_twfe A` when `A` is a structured
indicator (cohort-period, switch-on diagonal, triangular). Bridges the gap
identified in `Q1_StaggeredMinimalBasis/Helpers.lean` (L3, L4) and is the
cleanest place to put any future "closed-form residual under TWFE" lemma.
Includes a self-contained `cohortPeriodCells` construction (mirrors
`positiveCohortPeriodCells` from the staggered Q1 attempt; no Q-instance
import).

| Declaration | Signature | Description | Status |
|---|---|---|---|
| `Cells.tildeX_const` | `c.tildeX c.H_twfe (fun _ => k) r = 0` on observed cells | Constants are absorbed by `H_twfe` (one-line via `residualize_self_of_mem`). **Proved.** |
| `Cells.tildeX_unit_indicator` | unit-only indicator → 0 | `ind(i = i₀)` is a member of `H_twfe` via `a(i) := ind(i = i₀)`, `b(t) := 0`. **Proved.** |
| `Cells.tildeX_period_indicator` | period-only indicator → 0 | Symmetric: `ind(t = t₀)` ∈ `H_twfe`. **Proved.** |
| `Cells.tildeX_product_indicator_balanced` | `tildeX (1_{S_I}(i)·1_{S_T}(t)) = (ind − |S_I|/|I|)·(ind − |S_T|/|T|)` on observed | Centered-product residual under balanced weights. Proof: candidate `g ∈ H_twfe` plus `H_twfe_orthogonal_iff` plus balanced row/column centered-sum lemmas, then `proj_apply_eq_of_mem_orthogonal`. **Proved.** |
| `Cells.cohortPeriodCells` | `(law : CohortLaw C) → Cells (Fin C) (Fin S)` with weights `π(g)/S` | Generic positive-cohort cells construction. **Defined.** |
| `Cells.tildeX_cell_indicator_cohortPeriod` | cell `ind(g = g₀ ∧ t = t₀)` → four-term factorization | Centered-cell residual under cohort-period weights. **Proved.** |
| `Cells.tildeX_diagonal_indicator_cohortPeriod` | staggered diagonal `ind(g.val = t.val)` | Closed form with the explicit grand-mean correction `(1/S)·∑_{g.val<S} π(g)`. Proof uses row/column normal equations plus the diagonal-hit Finset reindexing. **Proved.** |
| `Cells.tildeX_triangular_indicator_cohortPeriod` | staggered triangular `ind(g.val < t.val)` | Closed form with the explicit grand-mean correction. Proof uses row/column normal equations plus finite-sum Fubini for the triangular predicate. **Proved.** |
| `Cells.H_twfe_orthogonal_iff` (private) | unit/period basis test suffices for `H_twfe`-orthogonality | Helper used by the substantive proofs. **Proved**: forward by specialization to unit/period indicators; backward by decomposing arbitrary `h(p) = a(p.1) + b(p.2)` into the `e_{i₀}`/`f_{t₀}` basis via Fintype sum manipulation. |

## 15. `ML/` — Standalone machine-learning spine

A causal-free supervised-learning library: basic regression methods as first-class
objects with their defining-optimization / closed-form-or-structure / population-target
properties. Does **not** import OSL/SCM/PO/Estimation. Design + plan:
`docs/superpowers/specs/2026-06-15-causalean-ml-module-design.md`,
`docs/superpowers/plans/2026-06-15-causalean-ml-module.md`.

### 15a. `ML/Core/` — the spine (Phase 0, complete)

Dual-view: a parametric `Predictor` (optimization lives here) and an extensional
`HypothesisClass` (population targets live here), joined by one `Bridge`.

| Declaration | File | Description |
| --- | --- | --- |
| `squaredLoss`, `bool01`, `sigmoid`, `softplus`, `logisticScoreLoss` | `Core/Losses.lean` | Pointwise losses + logistic link; with `sigmoid_pos/lt_one`, `softplus_pos`, `squaredLoss_nonneg`. |
| `Predictor` | `Core/Hypothesis.lean` | Parametrized predictor family: admissible `paramSet` + `predict : Θ → X → Y`. |
| `HypothesisClass` | `Core/Hypothesis.lean` | Extensional class: a set of measurable functions `X → Y`. |
| `FeatureMap` | `Core/Hypothesis.lean` | Feature transform `X → (K → ℝ)`; identity gives linear regression, other choices give polynomial/spline/Fourier (sieve) regression. |
| `Loss`, `empiricalRisk(P)`, `populationRisk(P)`, `iidEmpiricalRisk`, `HasFinitePopulationRisk` | `Core/Risk.lean` | Risk functionals in both views; `iidEmpiricalRisk` via `Stat.IIDSample.sampleMean`. |
| `IsERMP`, `IsApproxERMP`, `RegularizedERM`, `IsERM`, `IsPopulationRiskMinimizer` | `Core/ERM.lean` | Candidate-wise minimizer predicates via `IsMinOn` (parametric + extensional). |
| `imageClass`, `isERMP_to_extensional`, `populationTarget_pushforward` | `Core/Bridge.lean` | The realized class of a predictor; parametric minimizers push forward to extensional minimizers. |
| `empiricalRiskP_convexOn_of_loss_convex`, `convexOn_comp_inner`, `exists_isMinOn_of_isCompact`, `exists_isMinOn_univ_of_coercive`, `fderiv_eq_zero_of_isMinOn_univ`, `SubgradientAt`, `subgradientAt_zero_iff_isMinOn` | `Core/Convex.lean` | Convex-analysis substrate: convexity of empirical risk, linear-composition convexity, compact/coercive existence, Fermat's rule, subgradient↔minimizer. |
| `populationRisk_eq_of_both_minimizers`, `populationRisk_minimizer_eq_target`, `IsL2Projection`, `square_loss_population_target_of_isL2Projection` | `Core/PopulationTarget.lean` | P3 scaffold + squared-loss keystone: a residual orthogonal to all functions of the covariate (the L²-projection / regression function) minimizes squared population risk. |

### 15b. `ML/Linear/`, `ML/Ridge/` — linear least squares and ridge (Phase 1, complete)

Linear-in-features regression: predictor `x ↦ ∑ₖ βₖ (φ x)ₖ`; the `FeatureMap` makes
polynomial/spline/Fourier (sieve) regression instances of the same theorems.

| Declaration | File | Description |
| --- | --- | --- |
| `linearPredict`, `olsObjective`, `ols_is_squaredLoss_ERM_of_normalEq`, `empiricalRisk_squaredLoss_linear` | `Linear/Finite.lean` | OLS objective; P1: a normal-equation solution `XᵀX β̂ = Xᵀy` minimizes SSE; bridge to the spine's `empiricalRisk`. |
| `olsCoef`, `ols_normalEq_of_minimizer`, `olsCoef_normalEq`, `olsCoef_unique` | `Linear/ClosedForm.lean` | P2: minimizer ⇒ normal equations (converse); closed form `(XᵀX)⁻¹Xᵀy` and uniqueness under `IsUnit (XᵀX).det`. |
| `IsPopulationOLS`, `bestLinearPredictor_minimizes_populationRisk` | `Linear/Population.lean` | P3: a coefficient with residual uncorrelated to every feature minimizes squared population risk over the linear class (best linear predictor). |
| `ridgeObjective`, `ridge_is_regularized_squaredLoss_ERM_of_normalEq` | `Ridge/Finite.lean` | Ridge objective `SSE + λ‖β‖²`; P1: a ridge normal-equation solution `(XᵀX+λI)β̂ = Xᵀy` minimizes it (λ ≥ 0). |
| `ridgeCoef`, `ridgeGram_posDef`, `ridgeCoef_normalEq`, `ridgeCoef_unique` | `Ridge/ClosedForm.lean` | P2: for λ>0 the ridge Gram is positive definite ⇒ closed form `(XᵀX+λI)⁻¹Xᵀy`, unique without a full-rank assumption. |
| `populationRidgeObjective`, `IsPopulationRidge`, `populationRidge_minimizes` | `Ridge/Population.lean` | P3: the regularized population normal equations `E[(Y−⟪β,φ⟫)φₖ] = λβₖ` characterize the (shrunk) population ridge minimizer. |

### 15b'. `ML/Ridge/Rate.lean` — ridge root-n estimation rate (learning theory)

The causal-free learning-theory result `ML/Causal/RateAssembly` consumes to discharge the
DML `o_p(n^{-1/4})` nuisance condition: the sample ridge predictor converges to the population
ridge minimizer at the L²(P_X) root-n rate. Built from the i.i.d.-sample second-moment engine
`Stat.IIDSample.sampleMean_sub_isBigOp` and the in-probability continuous-mapping /
stochastic-order substrate in `Stat/Limit/ContinuousMapping.lean` and
`Stat/EmpiricalProcess/CrossFitRate.lean`.

| Declaration | File | Description |
| --- | --- | --- |
| `empiricalGram`, `empiricalCross`, `sampleRidgeCoef`, `sampleRidgePredictor`, `populationGram` | `Ridge/Rate.lean` | The sample ridge estimator over the first `n` i.i.d. points: empirical feature Gram `Ĝₙ`, cross moment `Ĉₙ`, coefficient `(Ĝₙ+λI)⁻¹Ĉₙ`, the induced predictor, and the entrywise population Gram `Gₖₗ = ∫ φₖφₗ`. |
| `empiricalGram_posSemidef`, `sampleRidgeCoef_sub_eq` | `Ridge/Rate.lean` | `Ĝₙ` is PSD (average of rank-one `φφᵀ`); the algebraic crux factoring the coefficient error `β̂ₙ−β⋆ = (Ĝₙ+λI)⁻¹(Ĉₙ−Ĝₙβ⋆−λβ⋆)`. |
| `centered_score_mean_isBigOp` | `Ridge/Rate.lean` | The centered empirical score mean `Ĉₙ−Ĝₙβ⋆−λβ⋆` is `O_p(n^{-1/2})` (centered i.i.d. vector mean with square-integrable score coordinates; population mean `λβ⋆` from `IsPopulationRidge`). |
| `sampleRidgeCoef_isBigOp` | `Ridge/Rate.lean` | The ridge coefficient error `‖β̂ₙ−β⋆‖` is `O_p(n^{-1/2})`: the regularized empirical-Gram inverse is `O_p(1)` (entrywise matrix LLN + inverse continuity at the PosDef limit) times the centered score mean. |
| `continuousAt_matrixInv_entry` | `Ridge/Rate.lean` | Matrix-topology helper: at any nonsingular finite matrix, each inverse entry `M ↦ (M⁻¹)ₖₗ` is continuous. |
| `eLpNorm_predictor_sub_le` | `Ridge/Rate.lean` | Deterministic: the prediction L²(P_X) error is bounded by a finite constant times the coefficient error, `‖∑ₖ δₖφ·ₖ‖_{L²} ≤ C‖δ‖`. |
| `ridge_achievesL2Rate` | `Ridge/Rate.lean` | **Headline.** Under the population ridge normal equation, `λ>0`, the L⁴ feature moment and square-integrable score, the sample ridge predictor attains the root-n L² rate toward the population ridge minimizer. |

### 15c. `ML/Lasso/` — L1-regularized least squares (Phase 2)

| Declaration | File | Description |
| --- | --- | --- |
| `l1penalty`, `lassoObjective`, `softThreshold`, `l1penalty_nonneg`, `convexOn_l1penalty`, `convexOn_olsObjective`, `convexOn_lassoObjective` | `Lasso/Finite.lean` | L1 penalty and lasso objective; convexity of the (non-smooth) objective. |
| `softThreshold_isMinOn` | `Lasso/Optimality.lean` | P2: the soft-threshold `S_λ(a)` is the exact minimizer of the scalar prox objective `(u−a)² + 2λ|u|`. |
| `rademacherComplexity_l1_ball_le` | `Lasso/Rate.lean` | **Rademacher rate (high-dim).** Over `L∞`-bounded features, the L¹-ball linear class has `rademacherComplexity ≤ (X∞·W/√n)·√(2 log 2d)` — the `√(log d / n)` lasso rate. |
| `lasso_erm_excess_rate` | `Lasso/Rate.lean` | **Lasso ERM excess-risk rate (linear functional).** Instantiates the separable ERM oracle inequality for the L¹-ball linear class on the L∞-bounded feature domain, giving a tail bound at threshold `4·(X∞·W/√n)·√(2 log 2d) + 2ε`. Loss is the bare linear functional. |
| `LassoFeat`, `LassoWeight`, `lasso_erm_squaredLoss_excess_rate` | `Lasso/SquaredLoss.lean` | **Lasso ERM squared-loss excess-risk rate.** Genuine regression loss `(∑ⱼ wⱼxⱼ−y)²` (L∞-bounded features, responses in `[−Yb,Yb]`). Same centered-loss decomposition as the kernel version (quadratic part via the infinite-index contraction + clamped square, cross part `2y·x` as a rescaled lasso linear class), carrying the `√(2 log 2d)` factor: tail bound at threshold `4·((4(X∞W)²+2·Yb·X∞·W)/√n)·√(2 log 2d) + 2ε`. |
| `margin_erm_surrogate_excess_rate` | `Margin/Rate.lean` | **Margin classification surrogate excess-risk rate.** For ANY `L`-Lipschitz surrogate `φ` (hinge, logistic, smoothed 0–1), the margin loss `φ(y·⟪w,x⟩)` over the `W`-ball of linear classifiers (features in `Xb`-ball, labels in `[−Yb,Yb]`). Single contraction: the margin `y·⟪w,x⟩=⟪w,y·x⟩` is a linear class over rescaled features and `φ̃=φ−φ(0)` is `LipschitzAt0`, so `RC ≤ 2L·Yb·Xb·W/√n`; tail bound at threshold `8·L·Yb·Xb·W/√n + 2ε`. |
| `effectiveDimension`, `effectiveDimension_nonneg`, `summable_effectiveDimension_term`, `effectiveDimension_le_trace_div`, `effectiveDimension_le_card`, `effectiveDimension_antitone` | `Kernel/EffectiveDimension.lean` | **Effective dimension** `N(λ)=∑ᵢ μᵢ/(μᵢ+λ)=tr(T(T+λ)⁻¹)` of a trace-class operator (summable nonneg eigenvalues `μ`), the quantity the Caponnetto–De Vito kernel-ridge optimal rate is stated in terms of. Definitional foundation + structural properties: nonneg, summand-summability, the dimension-free bound `N(λ)≤(tr T)/λ`, the finite bound `N(λ)≤#ι`, and antitonicity in `λ`. The downstream optimal-rate theorem additionally needs Hilbert-space operator-concentration (operator Bernstein), a separate substrate — see the file docstring. |

Full subgradient/KKT optimality for general designs and the population L1 target are deferred.

### 15d. `ML/Binary/`, `ML/Surrogate/` — logistic regression and convex ERM (Phase 2)

| Declaration | File | Description |
| --- | --- | --- |
| `logisticEmpRisk`, `convexOn_softplus`, `convexOn_logisticScoreLoss`, `convexOn_logisticEmpRisk`, `continuous_logisticEmpRisk`, `logistic_exists_minimizer_on_compact` | `Binary/Logistic.lean` | P1/P2: logistic log-loss is convex (softplus convex via monotone sigmoid derivative); a minimizer exists on any compact parameter set. |
| `bernoulliCE`, `bernoulliCE_isMinOn` | `Binary/FisherConsistency.lean` | P3: cross-entropy is minimized at the true probability on the interior `(0,1)` (Gibbs inequality). The interior is essential — the closed-interval version is false under `Real.log 0 = 0`. |
| `ProperBinaryLoss`, `properLoss_population_minimizer_recovers_eta` | `Surrogate/GenericERM.lean` | A proper loss is Fisher consistent: the population-risk minimizer recovers the conditional probability `η` (via `integral_mono`). |

### 15d'. `ML/Binary/Rate.lean` — L²-regularized logistic estimation rate (learning theory)

Binary-response analogue of the ridge rate: the `λ‖β‖²` penalty makes the logistic objective
globally `2λ`-strongly convex, so the coefficient error is controlled by a strong-convexity basic
inequality (the role ridge's `λI` played) rather than a closed-form inverse. Reuses the same
centered-score `O_p(n^{-1/2})` machinery and the shared linear-predictor L² bound.

| Declaration | File | Description |
| --- | --- | --- |
| `regLogisticGrad`, `IsPopulationRegLogistic`, `logisticPredictor` | `Binary/Rate.lean` | The empirical penalized-logistic gradient `n⁻¹Σ(σ⟨β,φᵢ⟩−yᵢ)φᵢ + 2λβ`, the penalized population first-order condition `E[(σ⟨β⋆,φ⟩−Y)φₖ] + 2λβ⋆ₖ = 0`, and the fitted-probability predictor `x ↦ σ(⟨β,φ(x)⟩)`. |
| `regLogistic_basic_inequality` | `Binary/Rate.lean` | Deterministic strong-convexity crux: a sample point solving the empirical FOC `∇ₙ(β̂)=0` satisfies `2λ‖β̂−β⋆‖ ≤ ∑ₖ\|∇ₙ(β⋆)ₖ\|` (gradient strong monotonicity: σ monotone ⇒ logistic part ≥ 0, penalty gives `2λ·id`). |
| `regLogisticGrad_coord_isBigOp` | `Binary/Rate.lean` | Each coordinate of the empirical gradient at `β⋆` is `O_p(n^{-1/2})` — a centered i.i.d. mean (population mean `0` by `IsPopulationRegLogistic`), via `sampleMean_sub_isBigOp`. |
| `regLogisticCoef_isBigOp` | `Binary/Rate.lean` | The coefficient error `‖β̂ₙ−β⋆‖` is `O_p(n^{-1/2})`: the basic inequality times the centered gradient. |
| `regLogistic_achievesL2Rate` | `Binary/Rate.lean` | **Headline.** The fitted probability `σ(⟨β̂ₙ,φ⟩)` attains the root-n L² rate toward `σ(⟨β⋆,φ⟩)`, via the `1/4`-Lipschitz `σ` and `eLpNorm_predictor_sub_le`. |

### 15e. `ML/Tree/`, `ML/NeuralNet/`, `ML/Kernel/` — trees, networks, kernels (Phase 3)

| Declaration | File | Description |
| --- | --- | --- |
| `FinitePartitionPredictor`, `eval`, `eval_eqOn_cell` | `Tree/FinitePartitionPredictor.lean` | Piecewise-constant tree predictor; constant on each cell. |
| `RandomForest`, `eval`, `eval_mem_Icc` | `Tree/Forest.lean` | Forest = average of trees; averaging preserves pointwise `[a,b]` bounds. |
| `DenseLayer`, `Activation`, `layerMap`, `evalLayers`, `evalLayers_append`, `evalLayers_lipschitz` | `NeuralNet/Layer.lean`, `NeuralNet/FeedForward.lean` | Feedforward composition class; evaluation respects concatenation; Lipschitz with the product of per-layer constants. (No universal approximation / training.) |
| `IsRKHS` | `Kernel/RKHS.lean` | Abstract reproducing-kernel Hilbert space interface (`f x = ⟪f, kₓ⟫`); Mathlib has no RKHS hierarchy. |
| `empiricalRademacherComplexity_innerBall_le`, `rkhs_ball_empiricalRademacher_le` | `Kernel/RKHSRademacher.lean` | **Intrinsic RKHS-ball Rademacher rate.** The empirical complexity of a Hilbert norm-ball linear class, and hence of an RKHS ball with bounded representers, is bounded by `κ·r/√n`. |
| `gram`, `IsPSDkernel`, `gram_add_lambda_posDef` | `Kernel/Gram.lean` | Kernel Gram matrix; `K + λI ≻ 0` for `λ > 0`. |
| `krrRisk`, `representer_theorem` | `Kernel/Ridge.lean` | P2: any kernel-ridge minimizer lies in the span of the sample representers (orthogonal-decomposition proof). |
| `rademacherComplexity_l2_ball_le` | `Kernel/Rate.lean` | **Rademacher rate.** Over `L²`-bounded features, the `W`-ball linear class (dual/RKHS-ball view of kernel ridge) has `rademacherComplexity ≤ Xb·W/√n`. |
| `kernel_erm_excess_rate` | `Kernel/Rate.lean` | **Kernel ERM excess-risk rate (linear functional).** Instantiates the separable ERM oracle inequality for the closed `W`-ball of linear predictors on the closed `Xb`-ball feature domain, giving a tail bound at threshold `4·Xb·W/√n + 2ε`. Loss is the bare linear functional `⟪w,x⟩`. |
| `clampedSq`, `clampedSq_eq_sq`, `clampedSq_le_sq`, `lipschitzAt0_clampedSq`, `continuous_clampedSq` | `Surrogate/ClampedSquare.lean` | Globally `2c`-Lipschitz surrogate `(clamp_{[-c,c]} t)²` for `t ↦ t²`, agreeing with the square on `[-c,c]`; the Lipschitz function fed to the contraction principle so the (non-Lipschitz) squared loss can be pulled out of the Rademacher complexity on a bounded prediction range. |
| `KFeat`, `KWeight`, `kernel_erm_squaredLoss_excess_rate` | `Kernel/SquaredLoss.lean` | **Kernel ERM squared-loss excess-risk rate.** Genuine regression loss `(⟪w,x⟩−y)²` (responses in `[−Yb,Yb]`). RC of the centered squared-loss class is bounded by decomposing `⟪w,x⟩²−2y⟪w,x⟩`: quadratic part via the infinite-index Ledoux–Talagrand contraction with the clamped square, cross part `2y⟪w,x⟩=⟪w,2y·x⟩` as a linear class over rescaled features. Tail bound at threshold `4·(4(XbW)²+2·Yb·Xb·W)/√n + 2ε`. |

### 15f. `ML/Causal/` — ML learners as causal nuisance estimators (causal-coupling corner)

The **only** ML files permitted to import the causal layers (`PO`/`Estimation`); the
`Causalean.ML` core stays causal-free. Connects the ML population targets to the
AIPW/DML nuisances via Mathlib `condExp`.

| Declaration | File | Description |
| --- | --- | --- |
| `covarSigma`, `covarSigma_le`, `isL2Projection_of_condExp`, `condExp_of_isL2Projection` | `Causal/RegressionBridge.lean` | The conditional mean `E[Y∣X]` (Mathlib `condExp`) **is** an `IsL2Projection`, and conversely — full equivalence linking the ML squared-loss keystone to the regression-function nuisance. |
| `mlOutcomeRegression_ae_eq`, `mlPropensity_ae_eq`, `mlNuisanceVec` | `Causal/Nuisance.lean` | The squared-loss ML target on arm `d` is the outcome regression `E[Y∣X,D=d]`; on the treatment indicator it is the propensity `P(D=1∣X)`; `mlNuisanceVec` packages two ML targets into `Estimation.ATE.NuisanceVec`. |
| `aipw_mlNuisance_meanZero_of_wellSpecified` | `Causal/AIPWExample.lean` | End-to-end: well-specified ML nuisances make the AIPW estimating equation mean-zero at the ATE `θ₀` (composes `mlNuisanceVec` with `Estimation.ATE.aipw_mean_zero`). |
| `AchievesL2Rate`, `isLittleOp_one_of_le_one`, `isLittleOp_mul_quarter`, `isLittleOp_quarter_of_isBigOp_sqrt` | `Core/Rate.lean` | Learning-rate abstraction (estimator attains L²-rate toward its population target) + generic rate algebra: per-nuisance `o_p(n^{-1/4})` rates give DML's individual `o_p(1)` and product `o_p(n^{-1/2})` conditions; a root-n `O_p(n^{-1/2})` rate is `o_p(n^{-1/4})`. (Moved here from `Causal/DMLRate`; causal-free common currency between method rate-proofs and the `Causal` assembly.) |

Note: `ML/Core/PopulationTarget.IsL2Projection` quantifies over **measurable** test functions
(required for the `condExp` bridge); the keystone threads this measurability.

Deferred follow-ups: complexity/generalization bounds, sieve approximation theory, raw-kernel→RKHS
construction, NN universal approximation, RF consistency; and (causal-ML trajectory) an end-to-end
AIPW-ATE example with ML nuisances, then the fast-rate layer discharging DML's `o(n^{-1/4})`
nuisance-rate condition.

## 15g. `Mathlib/Probability/FiniteCellConditionalMomentBridge.lean` — finite-cell conditional moments and support transfer

Generic finite-cell conditioning infrastructure: normalize a measure after restricting it to a
measurable positive-mass cell, derive finite-coordinate first and cross-moment factorization from
bounded-test factorization, and transfer an observed-arm absolute bound to an independent potential
outcome across the entire cell. It does not depend on a particular causal model, outcome radius, or
finite partition representation.

<!-- GEN:Causalean.Mathlib.Probability.FiniteCellConditionalMomentBridge -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.normalizedRestrict` | `MeasureTheory.Measure Ω → Set Ω → MeasureTheory.Measure Ω` | Given a measurable sample space, a measure on it, and a cell in that sample space, the normalized restricted measure is the measure restricted to the cell and scaled by the reciprocal of the measure of that cell. |
| `Mathlib.Probability.normalizedRestrictedIntegral` | `MeasureTheory.Measure Ω → Set Ω → (Ω → E) → E` | Given a measurable sample space, a real normed vector-valued outcome space, a measure, a cell, and a function on the sample space, the normalized restricted integral is the integral of the function with respect to the normalized restricted measure of that cell. |
| `Mathlib.Probability.normalizedRestrict_isProbabilityMeasure` | `MeasurableSet C → 0 < P C → MeasureTheory.IsProbabilityMeasure (Causalean.Mathlib.Probability.normalizedRestrict P C)` | For a finite sampling measure, a measurable cell with strictly positive mass has a normalized restricted law that is a probability measure. |
| `Mathlib.Probability.normalizedRestrict_apply` | `0 < P C → MeasurableSet A → (Causalean.Mathlib.Probability.normalizedRestrict P C) A = (P C)⁻¹ * P (A ∩ C)` | For a positive-mass cell and a measurable event, the normalized cell law of that event is its intersection mass divided by the cell mass. |
| `Mathlib.Probability.normalizedRestrictedIntegral_eq` | `0 < P C → ∀ (f : Ω → E), Causalean.Mathlib.Probability.normalizedRestrictedIntegral P C f = (P C).toReal⁻¹ • ∫ (ω : Ω) in C, f ω ∂P` | On a positive-mass cell, integrating a function under the normalized cell law is the restricted integral rescaled by the reciprocal cell mass. |
| `Mathlib.Probability.ae_normalizedRestrict_iff` | `0 < P C → ∀ {p : Ω → Prop}, (∀ᵐ (ω : Ω) ∂Causalean.Mathlib.Probability.normalizedRestrict P C, p ω) ↔ ∀ᵐ (ω : Ω) ∂P.restrict C, p ω` | On a positive-mass cell, an almost-sure assertion holds under the normalized cell law exactly when it holds under the unnormalized restricted measure. |
| `Mathlib.Probability.BoundedTestFactorization` | `MeasureTheory.Measure Ω → (Ω → S) → (Ω → T) → Prop` | Given a measurable sample space, two measurable value spaces, a measure, and two random elements, the bounded-test factorization condition holds exactly when, for every pair of measurable bounded real-valued test functions on the two value spaces, the integral of their product after applying the two random elements equals the product of their separate integrals. |
| `Mathlib.Probability.NormalizedRestrictedBoundedTestFactorization` | `MeasureTheory.Measure Ω → Set Ω → (Ω → S) → (Ω → T) → Prop` | Given a measurable sample space, two measurable value spaces, a measure, a cell, and two random elements, the normalized restricted bounded-test factorization condition is bounded-test factorization of those random elements under the normalized restriction of the measure to the cell. |
| `Mathlib.Probability.indepFun_of_boundedTestFactorization` | `Measurable X → Measurable Y → Causalean.Mathlib.Probability.BoundedTestFactorization μ X Y → ProbabilityTheory.IndepFun X Y μ` | Under a probability law, measurable random elements whose all bounded measurable real-valued tests factor are independent. |
| `Mathlib.Probability.firstMomentVector` | `MeasureTheory.Measure Ω → (Ω → Fin n → ℝ) → Fin n → ℝ` | Given a measurable sample space, a finite vector dimension, a measure, and a finite-dimensional real random vector, the first-moment vector has at each coordinate the integral of the corresponding coordinate of the random vector. |
| `Mathlib.Probability.crossMomentMatrix` | `MeasureTheory.Measure Ω → (Ω → Fin m → ℝ) → (Ω → Fin n → ℝ) → Matrix (Fin m) (Fin n) ℝ` | Given a measurable sample space, two finite vector dimensions, a measure, and two finite-dimensional real random vectors, the cross-moment matrix has at each ordered pair of coordinates the integral of the product of the corresponding coordinates of the two random vectors. |
| `Mathlib.Probability.normalizedRestricted_coordinate_factorization` | `MeasurableSet C → 0 < P C → ∀ {m n : ℕ} {X : Ω → Fin m → ℝ} {Y : Ω → Fin n → ℝ}, Measurable X → Measurable Y → (∀ (i : Fin m), MeasureTheory.Integrable (fun ω => X ω i) (Causalean.Mathlib.Probability.normalizedRestrict P C)) → (∀ (j : Fin n), MeasureTheory.Integrable (fun ω => Y ω j) (Causalean.Mathlib.Probability.normalizedRestrict P C)) → Causalean.Mathlib.Probability.NormalizedRestrictedBoundedTestFactorization P C X Y → ∀ (i : Fin m) (j : Fin n), MeasureTheory.Integrable (fun ω => X ω i * Y ω j) (Causalean.Mathlib.Probability.normalizedRestrict P C) ∧ (Causalean.Mathlib.Probability.normalizedRestrictedIntegral P C fun ω => X ω i * Y ω j) = (Causalean.Mathlib.Probability.normalizedRestrictedIntegral P C fun ω => X ω i) * Causalean.Mathlib.Probability.normalizedRestrictedIntegral P C fun ω => Y ω j` | For a measurable positive-mass cell, measurable finite-coordinate random vectors, integrable individual coordinates, and factorization of every bounded measurable test under the normalized cell law, each coordinate product is integrable and its cell cross moment factors into the two cell first moments. |
| `Mathlib.Probability.normalizedRestricted_crossMomentMatrix_eq_outer` | `MeasurableSet C → 0 < P C → ∀ {m n : ℕ} {X : Ω → Fin m → ℝ} {Y : Ω → Fin n → ℝ}, Measurable X → Measurable Y → (∀ (i : Fin m), MeasureTheory.Integrable (fun ω => X ω i) (Causalean.Mathlib.Probability.normalizedRestrict P C)) → (∀ (j : Fin n), MeasureTheory.Integrable (fun ω => Y ω j) (Causalean.Mathlib.Probability.normalizedRestrict P C)) → Causalean.Mathlib.Probability.NormalizedRestrictedBoundedTestFactorization P C X Y → Causalean.Mathlib.Probability.crossMomentMatrix (Causalean.Mathlib.Probability.normalizedRestrict P C) X Y = Matrix.vecMulVec (Causalean.Mathlib.Probability.firstMomentVector (Causalean.Mathlib.Probability.normalizedRestrict P C) X) (Causalean.Mathlib.Probability.firstMomentVector (Causalean.Mathlib.Probability.normalizedRestrict P C) Y)` | For a measurable positive-mass cell, measurable finite-coordinate random vectors, integrable individual coordinates, and factorization of every bounded measurable test under the normalized cell law, the complete normalized cross-moment matrix is the outer product of the two normalized first-moment vectors. |
| `Mathlib.Probability.armIndicator` | `Set Ω → Ω → ℝ` | Given a sample space and an event in it, the real-valued arm indicator equals one for sample points in the event and zero for all other sample points. |
| `Mathlib.Probability.ae_abs_potential_le_of_indep_positive_arm` | `MeasurableSet C → 0 < P C → MeasurableSet A → ∀ {Ypot Yobs : Ω → ℝ}, Measurable Ypot → Measurable Yobs → MeasureTheory.Integrable Ypot (Causalean.Mathlib.Probability.normalizedRestrict P C) → MeasureTheory.Integrable Yobs ((Causalean.Mathlib.Probability.normalizedRestrict P C).restrict A) → 0 < (Causalean.Mathlib.Probability.normalizedRestrict P C) A → ProbabilityTheory.IndepFun Ypot (Causalean.Mathlib.Probability.armIndicator A) (Causalean.Mathlib.Probability.normalizedRestrict P C) → Yobs =ᵐ[(Causalean.Mathlib.Probability.normalizedRestrict P C).restrict A] Ypot → ∀ {R : ℝ}, (∀ᵐ (ω : Ω) ∂(Causalean.Mathlib.Probability.normalizedRestrict P C).restrict A, \|Yobs ω\| ≤ R) → ∀ᵐ (ω : Ω) ∂P.restrict C, \|Ypot ω\| ≤ R` | Within a measurable positive-mass cell, for a measurable arm event, if the potential and observed outcomes are measurable, the potential outcome and observed outcome are integrable under their respective cell and observed-arm laws, the arm has positive normalized cell probability, the potential outcome is independent of the arm indicator under the normalized cell law, the observed and potential outcomes agree almost surely on that arm, and the observed outcome obeys an absolute bound there, then the potential outcome obeys the same absolute bound almost surely throughout the cell. |
<!-- /GEN -->

## 16. `Mathlib/Probability/DarmoisSkitovich.lean` — Bernstein's theorem (finite-variance Darmois–Skitovich)

Mathlib-staging probability result: the analytic engine behind linear non-Gaussian
causal discovery (LiNGAM, Shimizu et al. 2006). Proves that non-Gaussianity of
disturbances breaks the rotational symmetry the Gaussian case suffers from. Built
entirely on Mathlib's `charFun` infrastructure (`contDiff_charFun`,
`iIndepFun.charFun_map_sum_eq_prod`, `iteratedDeriv_charFun_zero`,
`Measure.ext_of_charFun`, `charFun_gaussianReal`); none of this result is in Mathlib.
Sorry-free; axioms `[propext, Classical.choice, Quot.sound]` only.

| Declaration | Statement | Description |
| --- | --- | --- |
| `bernstein_charFun_funeq` | `f(u+v)·g(u−v) = f u·g u·(f v·g(−v))` for `f = charFun (P.map X)`, `g = charFun (P.map Y)` | Functional equation: joint characteristic function of `(X,Y)` computed two ways, using `X ⟂ Y` and `(X+Y) ⟂ (X−Y)`. Plumbing. **Proved** |
| `charFun_eventually_ne_zero` | `∀ᶠ t in 𝓝 0, charFun μ t ≠ 0` (probability `μ` on `ℝ`) | `charFun` is continuous and `1` at `0`, so nonzero near `0`; this is what makes the logarithmic derivative `(charFun μ)'/charFun μ` well defined. **Proved** |
| `charFun_contDiff_two` | `ContDiff ℝ 2 (charFun (P.map Z))` given `MemLp Z 2 P` | `C²`-regularity of the characteristic function under a finite second moment, via `contDiff_charFun`. **Proved** |
| `gaussianForm_of_funeq` | functional equation + `C²` + value `1` at `0` ⟹ `∃ c`, locally `f t = exp(f'(0)·t + c·t²/2)` and `g t = exp(g'(0)·t + c·t²/2)` (shared `c`) | The hard pure-analysis core (no probability). Log-derivative argument with no `Complex.log`: differentiate the functional equation once in each variable to force the second logarithmic derivative constant, then solve the resulting ODE. **Proved** |
| `bernstein_charFun_gaussian_nhds_zero` | `∃ mf mg σ² ≥ 0`, locally `charFun (P.map X) t = exp(mf·t·I − σ²·t²/2)` and likewise `Y` with shared `σ²` | Wires the analytic core to characteristic-function facts: identifies the constants as moments (`mf = E[X]`, `σ² = Var X`) via `iteratedDeriv_charFun_zero`; the shared `σ²` (= `Var X = Var Y`) feeds the doubling bootstrap. **Proved** |
| `bernstein` | `X ⟂ Y`, `MemLp · 2`, `(X+Y) ⟂ (X−Y)` ⟹ `IsGaussian (P.map X) ∧ IsGaussian (P.map Y)` | **Bernstein's theorem** (smallest non-degenerate Darmois–Skitovich instance; the 2-variable LiNGAM identification core). Doubling bootstrap `f(2t) = f(t)²·g(t)·g(−t)` propagates the local Gaussian form to all of `ℝ`, then `Measure.ext_of_charFun` against `gaussianReal`. **Proved** |

## 17. `Discovery/LiNGAM/` — Linear Non-Gaussian Acyclic Model (causal discovery)

Formalization of Shimizu, Hoyer, Hyvärinen & Kerminen (JMLR 2006). The non-Gaussianity
engine used here lives in `Mathlib/Probability/DarmoisSkitovich.lean` (§16), and the
proved headline theorem uses the kurtosis route rather than the abandoned general
Darmois-Skitovich attempt.

| Declaration | Statement | Description |
| --- | --- | --- |
| `perm_uniqueness` (`PermutationUniqueness.lean`) | for lower-triangular `M` with non-zero diagonal and permutations `σ, τ`, `(∀ i, M (σ i) (τ i) ≠ 0) ↔ σ = τ` | **Appendix A, Lemma 1.** Uniqueness of the zero-free-diagonal row permutation; this is what pins the disturbance↔variable correspondence (resolving ICA's column-permutation indeterminacy). Slick proof: nonzero diagonal + lower-triangularity ⟹ `τ i ≤ σ i` ∀i, and equal permutation index-sums force equality. **Proved** |
| `lingam_reduction` (`Identifiability.lean`) | for unit-diagonal `C, C'` with `C` lower-triangular in order `σ`, if `C' i j = d i · C (τ i) j` then `C = C'` | **Structural identifiability core (ICA → matrix).** Given the ICA gen-permutation relation between the `I − B` matrices, acyclicity + unit-diagonal normalization force equality. Reuses the Appendix-A sum argument. **Proved** |
| `lingam_identifiable` (`Identifiability.lean`) | for `B, B'` with zero diagonal, `B` acyclic in order `σ`, if `(1 − B') i j = d i · (1 − B) (τ i) j` then `B = B'` | **LiNGAM coefficient identifiability (modulo ICA).** `B`-level packaging of `lingam_reduction` for `C = I − B`. **Proved** |
| `affine_of_continuous_of_second_diff_zero` (`Mathlib/Analysis/FrechetFunctionalEquation.lean`) | continuous `f : ℝ → E` with `f (x+s+s) + f x = f (x+s) + f (x+s)` ⟹ `∃ a b, f x = a + x • b` | **Fréchet functional equation, degree one.** A standalone finite-difference helper: midpoint-affine ⟹ additive ⟹ `ℝ`-linear (`AddMonoidHom.toRealLinearMap`). **Proved** |
| `genPerm_of_invertible_of_colSupport` (`GenPerm.lean`) | invertible `W` with ≤1 non-zero entry per column ⟹ `∃ τ d, (d i ≠ 0) ∧ W i j = if j = τ i then d i else 0` | Generalized-permutation extraction: invertibility upgrades "≤1 per column" to "exactly one per row". **Proved** |
| `kurt` / `cross_fourth_cumulant_eq_sum` / `colSupport_of_kurtosis` (`Kurtosis.lean`) | excess kurtosis `κ₄`; `cum(yᵢ,yᵢ,yₖ,yₖ) = Σⱼ Wᵢⱼ²Wₖⱼ²κ₄(eⱼ)`; and: independent centered finite-4th-moment sources with non-zero same-sign kurtosis + `yᵢ ⟂ yₖ` ⟹ `Wᵢⱼ·Wₖⱼ = 0` | **Marcinkiewicz-free column support.** The 4th-cumulant route to the `genPerm` input, avoiding Darmois–Skitovich entirely. Identity proved by the full Isserlis 4-index expansion under independence; column support by sum-of-same-sign-is-zero. Sorry-free, axiom-clean. **Proved** |
| `ica_genPerm_relation` / `lingam_identifiability_kurtosis` (`LiNGAMKurtosis.lean`) | column support + invertibility ⟹ gen-permutation relation; and the headline: invertible acyclic unit-diagonal mixing + independent centered finite-4th-moment same-sign-kurtosis disturbances + equal observed law ⟹ `A⁻¹ = A'⁻¹` (`B = B'`) | **Complete, fully sorry-free LiNGAM identification theorem (kurtosis route).** Chains `colSupport_of_kurtosis` → `ica_genPerm_relation` → `lingam_reduction`; no Darmois–Skitovich, no Marcinkiewicz, no Fréchet ladder. `#print axioms` = `[propext, Classical.choice, Quot.sound]`. **Proved** |

## 17b. `Discovery/LinearDisentanglement/` — Linear causal disentanglement via interventions (Squires–Seigal–Bhate–Uhler 2023)

Formalization of Squires, Seigal, Bhate & Uhler, *Linear Causal Disentanglement via
Interventions* (ICML 2023, `arXiv:2211.16467`). Latent variables `Z` follow a linear SEM
`BₖZ = ε` per context, observed only through a full-rank mixing `X = GZ`; a single perfect
intervention per latent node identifies the latent DAG and parameters **up to `S(𝒢)`**
(order-preserving relabelings). This is the interventional/second-moment counterpart of
LiNGAM: it **reuses** LiNGAM's structural pinning (`lingam_reduction` / `perm_uniqueness`)
but replaces the non-Gaussianity engine with interventions. Taken at the **algebraic
(matrix-level)** view of the paper's Theorem 2: a model is the tuple `(H, {Bₖ}, {iₖ}, {λₖ})`
and identifiability compares two tuples producing the same precision matrices `{Θₖ}` (no
Moore–Penrose pseudoinverse needed). Currently a **complete compiling scaffold** with the
algebraic backbone proven and the deep linear-algebra steps staged (see status column).

| Declaration | Statement | Description |
| --- | --- | --- |
| `Solution` / `permMat` / `Theta0` / `Theta` / `InSG` (`Model.lean`) | model tuple (full-row-rank `H`, edge relation with `j→i ⟹ i<j`, upper-triangular positive-diagonal `B₀` with support = edges, perfect single-node interventions `Bₖ=B₀+e_{iₖ}cₖᵀ`, `cₖ=λₖe_{iₖ}−B₀ᵀe_{iₖ}`); `Θₖ=HᵀBₖᵀBₖH`; `S(𝒢)` order-preserving perms; permutation matrix `(P_σ)_{ij}=⟦i=σj⟧` | Model layer + the two `permMat` orthogonality lemmas (`permMat σ·(permMat σ)ᵀ=1` and transpose). **Proved (defs + orthogonality, sorry-free, axiom-clean)** |
| `fact_transpose_mul` / `key_identity` (`KeyIdentity.lean`) | `BᵀB=Σᵢ(Bᵀeᵢ)⊗²`; and `Θₖ−Θ₀=(HᵀBₖᵀe_{iₖ})⊗²−(HᵀB₀ᵀe_{iₖ})⊗²` | **Algebraic backbone (their key Proposition).** A single-node intervention changes only one row of `B`, so the rank-one decompositions cancel except in the target row; the precision difference is a difference of two outer products (rank ≤ 2). **Proved (sorry-free, axiom-clean)** |
| `sigma_solutions` (`SigmaSolutions.lean`) | conjugating `B₀,Bₖ` by `permMat σ` and permuting rows of `H` leaves every `Θ₀,Θₖ` unchanged | **(⊇) direction of Theorem 2** (`prop:sigma_solutions`): each `σ∈S(𝒢)` gives a solution. Pure orthogonality of `permMat σ`. **Proved (sorry-free)** |
| `pa`/`Pa`/`anc`/`An`/`prec`/`preceq`, `IsPORQ`, `prec_lt`, `inner_toLp_eq_dotProduct`, `dotProduct_self_pos`, `eq_of_mul_eq_mul_row_indep`, `row_indep_of_mul_row_indep` (`PartialOrderRQ.lean`) | parents/ancestors and the partial order `i≺j ↔ j∈an(i)`; **Definition 1** (partial order RQ decomposition `H=RQ`, `R` `⪯`-supported with `Rᵢᵢ≥0`, rows of `Q` unit-norm and orthogonal along `≺`); + the `⬝ᵥ`↔`EuclideanSpace` inner-product bridge, positive-definiteness, and full-row-rank cancellation helpers | Graph order + Def 1 + reusable helpers. The helpers (bridge, cancellation, rank propagation) are **proved, axiom-clean**. **Proved (helpers)** |
| `porq_exists` / `porq_unique` (`PartialOrderRQ.lean`) | existence and uniqueness of the partial order RQ decomposition of a full-row-rank `H` | **Existence** by a bespoke partial-order Gram–Schmidt recursion (`porqResidual`, projecting onto the ancestor-row span along `≺`, normalized via the `⬝ᵥ`↔`EuclideanSpace` bridge `inner_toLp_eq_dotProduct`); **uniqueness** by downward induction pinning each `qᵢ` (`Q=Q'`) then full-row-rank cancellation (`eq_of_mul_eq_mul_row_indep`) for `R=R'`. **Proved (sorry-free, axiom-clean)** |
| `rowspan_inclusion_a` / `rowspan_inclusion_b` / `rowspan_inclusion_c` (`Rowspan.lean`) | (a) `rowspan(Θₖ−Θ₀)⊆⟨hᵢ:i∈𝓘⟩ ↔ Pa(iₖ)⊆𝓘`; (b) `⊆⟨qᵢ:i∈An(iₖ)⟩`; (c) `Pa(iₖ)⊄𝓘 ⟹ ⊄⟨qᵢ:i∈𝓘⟩` (all given `Θₖ≠Θ₀`) | **Lemma 1** linking observable precision differences to the latent graph. (a),(b),(c) **all proved sorry-free, axiom-clean.** Two faithfulness fixes the formalization forced: (a)/(c) need the paper's non-degeneracy `Θₖ≠Θ₀` (Assumption 1(b) genericity — absent, a degenerate source intervention `λₖ=(B₀)_{iₖiₖ}` makes them false); (c) additionally needs `𝓘` **ancestor-closed** (the algorithm's actual usage — a "diamond" graph with non-closed `𝓘` is a genuine counterexample to (c) as the paper states it). Key helpers: `vec_mem_rowSpan_vecMulVec_sub` (Gram extraction), `hSpan_An_eq_qSpan_An` (inverse-PORQ support, by dimension count), `Q_row_dotProduct_H_row_eq_zero` (PORQ orthogonality). **Proved (sorry-free, axiom-clean)** |
| `disentanglement_uniqueness` (`Uniqueness.lean`) | one intervention per node (`target` **bijective**) + non-degenerate (`Θₖ≠Θ₀`) + equal `{Θₖ}` ⟹ the two solutions differ by a single `σ∈S(𝒢)` **up to nonzero signed diagonal scaling**: `H'=diag(μ)·Pσ·H` (μ nonzero, any sign), `B'ₖ·(diag μ·Pσ)=diag(ν)·Pσ·Bₖ` (ν=±1), targets `i'ₖ=σ iₖ` | **(⊆) direction of Theorem 2.** The clean orthogonal-matrix route: change-of-basis recovery `H'=MH` (L1), Gram congruence (L2), `gram_to_orthogonal` (L3), the **sum trick** `Mᵀ·diag(d')·M=diag(d)` → orthogonal `O`, the row-collapse induction → `M` signed monomial, signed-Cholesky read-off → per-context relations, `InSG σ` and target read-off. Reuses `cholesky_unique`. **Proved (sorry-free, axiom-clean)** |
| `orthogonal_upperTri_signed_diag` / `signed_cholesky` / `permMat_transpose_eq` / `permMat_conj_apply'` / `permMat_mul_diagonal` (`Uniqueness.lean`, private) | orthogonal upper-triangular matrix is a `±1` diagonal; `PᵀP=BᵀB` with `B` upper-tri pos-diag ⟹ `P=diag(s)·B` with `s i=±1` (signed Cholesky); `(permMat σ)ᵀ=permMat σ⁻¹`; `(permMat σ)ᵀ·X·permMat σ` relabels by `σ`; `permMat σ·diag s=diag(s∘σ⁻¹)·permMat σ` | Reusable signed-Cholesky / permutation-conjugation helpers feeding the read-off step (F). The signed Cholesky generalizes `cholesky_unique` to a single sign-fixed factor. **Proved (sorry-free, axiom-clean)** |
| `monomial_relations_from_orthogonal_core` (`Uniqueness.lean`, private) | from the orthogonal `O = diag(√d')·M·diag(√d⁻¹)` of the sum trick: `M=diag μ·permMat σ` (nonzero `μ`) and the per-context signed relations `B'ₖ·M=diag ν·permMat σ·Bₖ` with shared `ν=sign∘μ` | The geometric core (`prop:orthogonal-correctness`). **(D)** decreasing parent-elimination induction on `O`'s rows (via `central_rank2_eq`, the `e_c`-support argument + orthogonality) ⟹ row `σn` of `O` is `±eₙ`; **(E)** monomial form of `M`; **(F)** signed-Cholesky read-off (`signed_cholesky`/`bRel`) with shared sign `sign∘μ`, whose σ-triangularity input `S'.B₀(σa)(σb)=0` for `b<a` is read off directly from the `(b,a)` entry of `central_rank2_eq` (no extra induction). **Proved (sorry-free, axiom-clean)** |
| `disentanglement_identifiability` (`Identifiability.lean`) | **flagship Theorem 2 (algebraic, up to S(𝒢) and signed scaling):** one intervention per node + non-degenerate + equal `{Θₖ}` ⟹ solutions related by a single `σ∈S(𝒢)` and a nonzero signed diagonal scaling of latent directions; with `sigma_solutions` (⊇) this characterizes the solution set | **Headline, fully proved.** Reduces to `disentanglement_uniqueness`. `#print axioms` = `[propext, Classical.choice, Quot.sound]`. **Proved (sorry-free, axiom-clean)** |

### Quantitative simultaneous-congruence stability

These paper-independent matrix tools package the perturbation layer behind robust
second-moment identification: a finite family of approximate congruence equations, an
affine-minor separation condition for their diagonal shifts, an explicit local
operator-norm modulus, and the compactness exclusion radius outside a local chart.

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.Definitions -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.SqMatrix` | `ℕ → Type` | For a nonnegative integer dimension, a square matrix is a real-valued matrix with that many rows and that many columns. |
| `Discovery.LinearDisentanglement.Quantitative.congruenceDefect` | `(Fin d → ℝ) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a dimension, an observed matrix, a prescribed diagonal vector, and a candidate change-of-coordinates matrix, the congruence defect is $BAB^\mathsf{T}$ minus the diagonal matrix formed from the prescribed vector. |
| `Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual` | `(E → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d) → (E → Fin d → ℝ) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → ℝ` | For a dimension, a nonempty finite environment collection, an observed matrix for each environment, a prescribed diagonal vector for each environment, and a candidate change-of-coordinates matrix, the simultaneous congruence residual is the largest Euclidean operator norm of the individual congruence defects across environments. |
| `Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual_nonneg` | `0 ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual A s B` | For a finite real matrix family, its prescribed diagonal shifts, and a candidate change of coordinates, the simultaneous-congruence residual is nonnegative. |
| `Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual_le_iff` | `Causalean.Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual A s B ≤ ε ↔ ∀ (e : E), ‖Causalean.Discovery.LinearDisentanglement.Quantitative.congruenceDefect (A e) (s e) B‖ ≤ ε` | For a finite real matrix family, its prescribed diagonal shifts, a candidate change of coordinates, and a tolerance, the simultaneous residual is at most that tolerance exactly when every individual congruence defect is. |
| `Discovery.LinearDisentanglement.Quantitative.AffineMinorSeparated` | `(E → Fin d → ℝ) → ℝ → Prop` | For a dimension, a finite environment collection, a diagonal shift vector for each environment, and a real margin, affine minor separation holds when there exist one base environment and one selected environment for each coordinate such that the absolute determinant of their shift-difference matrix is at least the margin. |
| `Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound` | `(E → Fin d → ℝ) → ℝ → Prop` | For a dimension, a finite environment collection, a diagonal shift vector for each environment, and a real bound, the shift-scale bound holds exactly when every coordinate of every prescribed diagonal shift has absolute value at most the bound. |
| `Discovery.LinearDisentanglement.Quantitative.UnitDiagonal` | `Prop` | For a dimension and a square matrix, unit-diagonal normalization holds exactly when every diagonal entry of the matrix equals one. |
| `Discovery.LinearDisentanglement.Quantitative.operatorConditionNumber` | `ℝ` | For a dimension and a square matrix, its Euclidean operator-norm condition number is the product of its Euclidean operator norm and the Euclidean operator norm of its inverse. |
| `Discovery.LinearDisentanglement.Quantitative.WellConditioned` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Prop` | For a dimension, a real condition bound, and a square matrix, bounded conditioning holds exactly when the matrix is nonsingular and its Euclidean operator-norm condition number is at most the bound. |
| `Discovery.LinearDisentanglement.Quantitative.PairConditionBound` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Prop` | For a dimension, a real condition bound, and a reference and candidate matrix, the pair condition bound holds exactly when the reference matrix is boundedly conditioned by that bound and the candidate matrix is boundedly conditioned by that bound. |
| `Discovery.LinearDisentanglement.Quantitative.PairMatrixScaleBound` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Prop` | For a dimension, a real scale bound, and a reference and candidate matrix, the pair matrix-scale bound holds exactly when the reference matrix's Euclidean operator norm is at most the scale bound and the candidate matrix's Euclidean operator norm is at most the scale bound. |
| `Discovery.LinearDisentanglement.Quantitative.transition` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a dimension and a reference and candidate matrix, the transition matrix is the candidate matrix multiplied by the inverse of the reference matrix. |
| `Discovery.LinearDisentanglement.Quantitative.ExactCongruence` | `(E → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d) → (E → Fin d → ℝ) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Prop` | For a dimension, a finite environment collection, an observed matrix for each environment, a prescribed diagonal vector for each environment, and a reference matrix, exact congruence holds exactly when, in every environment, the reference matrix transforms the observed matrix into the diagonal matrix prescribed there. |
| `Discovery.LinearDisentanglement.Quantitative.ApproximateCongruence` | `(E → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d) → (E → Fin d → ℝ) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → ℝ → Prop` | For a dimension, a nonempty finite environment collection, an observed matrix for each environment, a prescribed diagonal vector for each environment, a candidate matrix, and a real tolerance, approximate congruence holds exactly when the simultaneous congruence residual is at most the tolerance. |
| `Discovery.LinearDisentanglement.Quantitative.affineSolveFactor` | `ℕ → ℝ → ℝ → ℝ` | For a dimension, a shift scale, and an affine-separation margin, the affine solve factor is $d(d-1)!(2L)^{d-1}/\delta$. |
| `Discovery.LinearDisentanglement.Quantitative.productControlFactor` | `ℕ → ℝ → ℝ → ℝ` | For a dimension, a shift scale, and an affine-separation margin, the product-control factor is twice the affine solve factor. |
| `Discovery.LinearDisentanglement.Quantitative.stabilityConstant` | `ℕ → ℝ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a shift scale, an affine-separation margin, a matrix scale, and a condition-number envelope, the stability constant is $2dR\max(1,\kappa)$ times the product-control factor. |
| `Discovery.LinearDisentanglement.Quantitative.admissibleRadius` | `ℕ → ℝ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a shift scale, an affine-separation margin, a matrix scale, and a condition-number envelope, the admissible residual radius is the smaller of the two displayed reciprocal bounds determined by those quantities. |
| `Discovery.LinearDisentanglement.Quantitative.productControlFactor_pos` | `0 < d → 0 < L → 0 < δ → 0 < Causalean.Discovery.LinearDisentanglement.Quantitative.productControlFactor d L δ` | When the matrix dimension is positive, the shift scale is positive, and the affine separation margin is positive, the coordinate-product amplification factor is strictly positive. |
| `Discovery.LinearDisentanglement.Quantitative.admissibleRadius_pos` | `0 < d → 0 < L → 0 < δ → 0 < R → 0 < Causalean.Discovery.LinearDisentanglement.Quantitative.admissibleRadius d L δ R κ` | When the matrix dimension is positive, the shift scale is positive, the affine separation margin is positive, and the matrix scale is positive, the declared small-residual radius is strictly positive. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.Quantitative -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.abs_entry_le_opNorm` | `\|M i j\| ≤ ‖M‖` | For a real square matrix and a selected row and column, the absolute value of that entry is bounded by the Euclidean operator norm. |
| `Discovery.LinearDisentanglement.Quantitative.opNorm_le_dimension_mul_of_entry_bound` | `0 ≤ c → (∀ (i j : Fin d), \|M i j\| ≤ c) → ‖M‖ ≤ ↑d * c` | For a real square matrix, if the proposed entry bound is nonnegative and every entry obeys that bound, its Euclidean operator norm is at most the dimension times the bound. |
| `Discovery.LinearDisentanglement.Quantitative.coordinate_le_affineSolveFactor` | `0 < δ → 0 ≤ L → (∀ (i j : Fin d), \|V i j\| ≤ 2 * L) → δ ≤ \|Matrix.det V\| → (∀ (i : Fin d), \|Matrix.mulVec V x i\| ≤ η) → ∀ (j : Fin d), \|x j\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.affineSolveFactor d L δ * η` | For a real square coefficient matrix and a solution vector, if the determinant margin is positive, the scale bound is nonnegative, every coefficient is bounded, the determinant has the stated margin, and the linear-system residual is uniformly bounded, every solution coordinate is bounded by the explicit Cramer's-rule factor times the residual bound. |
| `Discovery.LinearDisentanglement.Quantitative.transition_diagonal_congruence` | `IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → ∀ (e : E), Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B * Matrix.diagonal (s e) * Matrix.transpose (Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B) = B * A e * Matrix.transpose B` | For a real matrix family, its diagonal shifts, an exact reference matrix, a candidate matrix, if the reference is invertible and it exactly realizes every prescribed congruence, then the transition matrix conjugates each reference diagonal into the candidate congruence. |
| `Discovery.LinearDisentanglement.Quantitative.transition_coordinate_product_control` | `0 ≤ L → 0 < δ → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s L → Causalean.Discovery.LinearDisentanglement.Quantitative.AffineMinorSeparated s δ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.ApproximateCongruence A s B ε → (∀ (i a : Fin d), \|Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B i a ^ 2 - if i = a then 1 else 0\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.productControlFactor d L δ * ε) ∧ ∀ (i j a : Fin d), i ≠ j → \|Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B i a * Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B j a\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.productControlFactor d L δ * ε` | For a finite real matrix family, its prescribed diagonal shifts, an exact reference, and a candidate, if the shift scale is nonnegative, the affine separation margin is positive, the shifts obey their scale bound, a separated affine minor exists, the reference is invertible, the reference realizes the congruences exactly, and the candidate has the stated approximate residual, then squared transition entries and cross-row products have the explicit coordinatewise residual bounds. |
| `Discovery.LinearDisentanglement.Quantitative.transition_diagonal_ge_half` | `0 < d → 0 < L → 0 < δ → 0 < R → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s L → Causalean.Discovery.LinearDisentanglement.Quantitative.AffineMinorSeparated s δ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairMatrixScaleBound R B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairConditionBound κ B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → 0 ≤ ε → ε ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.admissibleRadius d L δ R κ → Causalean.Discovery.LinearDisentanglement.Quantitative.ApproximateCongruence A s B ε → ∀ (i : Fin d), 1 / 2 ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.transition B₀ B i i` | For positive matrix dimension, positive shift scale, positive affine separation, and positive matrix scale, if the shifts have the declared scale, their affine minor is separated, the reference is unit-diagonal, the candidate is unit-diagonal, both matrices obey the scale bound, both matrices obey the condition bound, the reference congruences are exact, the residual tolerance is nonnegative, it is below the admissible radius, and the candidate is approximately congruent, then every transition diagonal entry is at least one half. |
| `Discovery.LinearDisentanglement.Quantitative.opNorm_sub_le_of_approximate_simultaneous_congruence` | `0 < d → 0 < L → 0 < δ → 0 < R → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s L → Causalean.Discovery.LinearDisentanglement.Quantitative.AffineMinorSeparated s δ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairMatrixScaleBound R B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairConditionBound κ B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → 0 ≤ ε → ε ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.admissibleRadius d L δ R κ → Causalean.Discovery.LinearDisentanglement.Quantitative.ApproximateCongruence A s B ε → ‖B - B₀‖ ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.stabilityConstant d L δ R κ * ε` | Explicit simultaneous-congruence stability. For positive matrix dimension, positive shift scale, positive affine separation, and positive matrix scale, if the diagonal shifts obey their scale bound, the shift family has a separated affine minor, the reference is unit-diagonal, the candidate is unit-diagonal, the pair obeys the matrix scale bound, the pair obeys the condition-number bound, the reference realizes every congruence exactly, the residual tolerance is nonnegative, the tolerance is admissibly small, and the candidate realizes the congruences up to that tolerance, then the candidate is within the explicit linear modulus times the tolerance of the reference in Euclidean operator norm. |
<!-- /GEN -->

#### Pairwise-affine stability

These modules extend the quantitative layer to the case in which each coordinate pair may
select its own three affinely separating environments. They retain the existing transition,
congruence, and norm vocabulary while deriving pairwise control, local branch selection, and
the explicit operator-norm bound separately.

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Definitions -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairAffineDet` | `(E → Fin p → ℝ) → Fin p → Fin p → E → E → E → ℝ` | For a dimension, an environment collection, a diagonal shift vector for each environment, two selected coordinates, and three environments, the pairwise affine determinant is the signed area determinant of the three corresponding two-coordinate shift points. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated` | `(E → Fin p → ℝ) → ℝ → Prop` | For a dimension, a finite environment collection, a diagonal shift vector for each environment, and a real margin, pairwise affine separation holds exactly when every two distinct coordinates admit three, possibly pair-specific, environments whose affine determinant has absolute value at least the margin. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence` | `(E → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → ℝ → Prop` | For a dimension, a finite environment collection, an observed matrix for each environment, a candidate matrix, and a real tolerance, off-diagonal approximate congruence holds exactly when every off-diagonal entry of every transformed matrix has absolute value at most the tolerance. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p` | For a dimension and a reference and candidate matrix, the transition error is the transition matrix minus the identity matrix. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → ℝ` | For a dimension and a square matrix, its entrywise Euclidean size is the square root of the sum of squared absolute values of all its entries. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor` | `ℝ → ℝ → ℝ` | For a matrix scale and a pairwise affine-separation margin, the pairwise solve factor is $6M/\delta$. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseAggregateFactor` | `ℕ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a matrix scale, a pairwise affine-separation margin, and a shift scale, the pairwise aggregate factor is the pairwise solve factor times the square root of $p(p-1)(1+L^2)$. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseStabilityConstant` | `ℕ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a matrix scale, a pairwise affine-separation margin, and a shift scale, the pairwise stability constant is sixteen times the pairwise aggregate factor times $L^3$. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius` | `ℕ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a matrix scale, a pairwise affine-separation margin, and a shift scale, the pairwise residual radius is the reciprocal of thirty-two times the squared maximum of one and the pairwise aggregate factor, times the matrix scale. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius` | `ℕ → ℝ → ℝ → ℝ → ℝ → ℝ` | For a dimension, a matrix scale, a pairwise affine-separation margin, a shift scale, and an inverse-norm envelope, the pairwise local radius is the stated minimum-based reference-neighborhood radius divided by the dimension and the inverse-norm envelope. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot` | `ℕ → ℝ → ℝ` | For a dimension and a condition-number envelope, the condition root is $((p!)\kappa^{p-1})^{1/p}$. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairMatrixNormBound` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Prop` | For a dimension, a real scale bound, and a reference and candidate matrix, the pair matrix-norm bound holds exactly when both Euclidean operator norms are at most the scale bound: the reference matrix's norm is at most the bound and the candidate matrix's norm is at most the bound. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InReferenceNeighborhood` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Prop` | For a dimension, a real neighborhood radius, and a reference and candidate matrix, membership in the reference neighborhood holds exactly when their entrywise Euclidean distance is at most the radius. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.DetConditionEnvelope` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Prop` | For a dimension, a condition-number envelope, and a square matrix, the determinant-condition envelope holds exactly when the matrix is nonsingular, its determinant has absolute value at most $p!$, and its Euclidean operator-norm condition number is at most the envelope. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairDetConditionEnvelope` | `ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Prop` | For a dimension, a condition-number envelope, and a reference and candidate matrix, the pair determinant-condition envelope holds exactly when the reference matrix satisfies that determinant-condition envelope and the candidate matrix satisfies that determinant-condition envelope. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InIdentityBranch` | `ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix p → Prop` | For a dimension, a matrix scale, a pairwise affine-separation margin, a shift scale, and a reference and candidate matrix, membership in the identity branch holds exactly when the transition error has entrywise Euclidean size at most one and twice the pairwise aggregate factor times the matrix scale times that size is at most one half. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius_pos` | `0 < M → 0 < δ → 1 ≤ L → 0 < Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius p M δ L` | For positive shift scale and pairwise margin and a matrix scale at least one, the explicit residual threshold is strictly positive. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius_pos` | `0 < p → 0 < M → 0 < δ → 1 ≤ L → 0 < J → 0 < Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius p M δ L J` | For positive dimension, shift scale, pairwise margin, and inverse envelope and a matrix scale at least one, the explicit local reference-neighborhood radius is strictly positive. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.one_le_conditionRoot` | `0 < p → 1 ≤ κ → 1 ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ` | For positive dimension and a condition envelope at least one, the determinant/condition root is at least one. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.opNorm_le_conditionRoot` | `0 < p → 1 ≤ κ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.DetConditionEnvelope κ B → ‖B‖ ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ` | For positive dimension, condition envelope at least one, a unit-diagonal matrix, and its determinant/condition envelope, the matrix operator norm is bounded by the determinant/condition root. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairMatrixNormBound_conditionRoot` | `0 < p → 1 ≤ κ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairDetConditionEnvelope κ B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairMatrixNormBound (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ) B₀ B` | For positive dimension, condition envelope at least one, unit diagonals, and a common determinant/condition envelope, both matrix norms obey the common `conditionRoot` bound. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseControl -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.coordinateProductRemainder_le` | `0 ≤ M → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → ∀ (e₁ e₀ : E) (i j : Fin p), \|∑ k, R i k * R j k * (s e₁ k - s e₀ k)\| ≤ 2 * M * Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 R ^ 2` | For a real square matrix, two rows, and two environments, if the shifts have scale at most `M` and `M` is nonnegative, then the weighted coordinate-product remainder is bounded by `2 M` times the squared entrywise L² size. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pair_cramer_control` | `0 ≤ M → 0 < δ → 0 ≤ η → \|a\| ≤ 2 * M ∧ \|b\| ≤ 2 * M ∧ \|c\| ≤ 2 * M ∧ \|d\| ≤ 2 * M → δ ≤ \|a * d - c * b\| → a * x + b * y = r₁ → c * x + d * y = r₂ → \|r₁\| ≤ η → \|r₂\| ≤ η → \|x\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor M δ * η ∧ \|y\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor M δ * η` | If the scale is nonnegative, the determinant margin is positive, and the residual bound is nonnegative, all four coefficients have scale at most `2M`, the two-by-two coefficient determinant is separated, the two displayed equations hold, and both equation residuals are at most `η`, then both unknowns obey the conservative explicit Cramer bound. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.selectedTriple_offDiagonal_control` | `i ≠ j → ∀ (e₀ e₁ e₂ : E), 0 ≤ M → 0 < δ → 0 ≤ ε → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → δ ≤ \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairAffineDet s i j e₀ e₁ e₂\| → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → have R := Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B; have η := 2 * M * Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 R ^ 2 + 2 * ε; \|R i j\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor M δ * η ∧ \|R j i\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor M δ * η` | For distinct selected coordinates, nonnegative scale and residual parameters with a positive affine margin, an exact invertible reference, an off-diagonally approximate candidate, bounded shifts, and one selected triple whose affine determinant separates the coordinates, both off-diagonal transition errors for that coordinate pair are bounded by the selected triple's affine-system estimate. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwise_offDiagonal_control` | `0 ≤ M → 0 < δ → 0 ≤ ε → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated s δ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → ∀ (i j : Fin p), i ≠ j → \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B i j\| ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseSolveFactor M δ * (2 * M * Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B) ^ 2 + 2 * ε)` | For nonnegative scale and residual parameters with a positive affine margin, pairwise affine-separated bounded shifts, an exact invertible reference, and an off-diagonally approximate candidate, every ordered off-diagonal transition error obeys the common pairwise estimate. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.diagonalBranch_control` | `IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → 0 ≤ c → (∀ (i j : Fin p), i ≠ j → \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B i j\| ≤ c) → ∀ (i : Fin p), \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B i i\| ^ 2 ≤ ‖B₀‖ ^ 2 * ∑ k ∈ Finset.univ.erase i, \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B i k\| ^ 2` | For an invertible reference, unit-diagonal reference and candidate matrices, a nonnegative entry bound, and an off-diagonal transition-entry bound, each diagonal transition error is controlled by the same row's off-diagonal entries and the corresponding reference column. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2_transitionError_le` | `0 < p → 0 ≤ L → 0 ≤ c → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → ‖B₀‖ ≤ L → (∀ (i j : Fin p), i ≠ j → \|Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B i j\| ≤ c) → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B) ≤ c * √(↑p * ↑(p - 1) * (1 + L ^ 2))` | For positive dimension and nonnegative matrix and entry bounds, an invertible unit-diagonal reference and a unit-diagonal candidate, a reference norm bound, and a common off-diagonal transition-entry bound, the full transition error's entrywise L² size is bounded by the ordered-pair aggregation factor. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.LocalBranch -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2_mul_le` | `Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 (X * Y) ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 X * Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 Y` | For two real square matrices, the entrywise-L² size of their product is at most the product of their entrywise-L² sizes. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2_le_dimension_mul_opNorm` | `0 < p → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 X ≤ ↑p * ‖X‖` | For positive dimension and a real square matrix, its entrywise-L² size is at most the dimension times its Euclidean operator norm. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError_eq_sub_mul_inv` | `IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B = (B - B₀) * B₀⁻¹` | For an invertible reference matrix, the transition error equals the candidate-reference difference multiplied by the reference inverse. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError_entryL2_le_of_neighborhood` | `0 < p → 0 ≤ J → IsUnit (Matrix.det B₀) → ‖B₀⁻¹‖ ≤ J → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InReferenceNeighborhood ρ B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B) ≤ ↑p * J * ρ` | For positive dimension and inverse envelope, an invertible reference, a bound on its inverse operator norm, and a local entrywise-L² neighborhood, the transition error is bounded by dimension times inverse scale times neighborhood radius. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.invOpNorm_le_conditionEnvelope` | `0 < p → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.DetConditionEnvelope κ B → ‖B⁻¹‖ ≤ κ` | For positive dimension, a unit-diagonal matrix, and its determinant/condition envelope, the reference inverse operator norm is at most the declared condition envelope. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.inIdentityBranch_of_small_residual` | `0 < p → 0 < M → 0 < δ → 1 ≤ L → 0 < J → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated s δ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairMatrixNormBound L B₀ B → ‖B₀⁻¹‖ ≤ J → 0 ≤ ε → ε ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius p M δ L → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InReferenceNeighborhood (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius p M δ L J) B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InIdentityBranch M δ L B₀ B` | Small-residual local identity-branch selection. For positive dimension, shift scale, affine margin, matrix scale, and inverse envelope, bounded pairwise-separated shifts, an exact invertible unit-diagonal reference, a unit-diagonal norm-bounded candidate, an inverse bound, nonnegative approximate residual, the explicit residual smallness condition, and membership in the explicit ordinary reference neighborhood, the transition lies on the identity branch needed by the linear stability estimate. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.Stability -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.opNorm_le_entryL2` | `‖R‖ ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.entryL2 R` | For a real square matrix, its Euclidean operator norm is at most its entrywise L² size. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.sub_eq_transitionError_mul` | `IsUnit (Matrix.det B₀) → B - B₀ = Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.transitionError B₀ B * B₀` | For an invertible reference, the candidate-reference difference factors through the transition error. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.opNorm_sub_le_of_pairwise_affine_in_identity_branch` | `0 < p → 0 < M → 0 < δ → 1 ≤ L → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated s δ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairMatrixNormBound L B₀ B → 0 ≤ ε → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InIdentityBranch M δ L B₀ B → ‖B - B₀‖ ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseStabilityConstant p M δ L * ε` | Local pairwise-affine simultaneous-congruence stability. For positive dimension, shift scale, and affine margin, a matrix scale at least one, bounded pairwise-separated shifts, an exact invertible unit-diagonal reference, a unit-diagonal candidate with the declared matrix scale, nonnegative off-diagonal residual tolerance, off-diagonal approximate feasibility, and the explicit local identity-branch condition, the candidate-reference Euclidean operator distance is linear in the residual. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.opNorm_sub_le_of_pairwise_affine` | `0 < p → 0 < M → 0 < δ → 1 ≤ L → 0 < J → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated s δ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairMatrixNormBound L B₀ B → ‖B₀⁻¹‖ ≤ J → 0 ≤ ε → ε ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius p M δ L → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InReferenceNeighborhood (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius p M δ L J) B₀ B → ‖B - B₀‖ ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseStabilityConstant p M δ L * ε` | Small-residual local pairwise-affine simultaneous-congruence stability. For positive dimension, shift scale, affine margin, matrix scale, and inverse envelope, bounded pairwise-separated shifts, an exact invertible unit-diagonal reference, a unit-diagonal norm-bounded candidate, a reference inverse bound, nonnegative off-diagonal residual below the explicit threshold, approximate feasibility, and membership in the explicit entrywise-L² neighborhood of the reference, the candidate-reference Euclidean operator distance is linear in the residual. |
| `Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.opNorm_sub_le_condition_specialization` | `0 < p → 0 < M → 0 < γ → 1 ≤ κ → Causalean.Discovery.LinearDisentanglement.Quantitative.ShiftScaleBound s M → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairwiseAffineSeparated s γ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.PairDetConditionEnvelope κ B₀ B → Causalean.Discovery.LinearDisentanglement.Quantitative.ExactCongruence A s B₀ → 0 ≤ ε → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.OffDiagonalApproximateCongruence A B ε → ε ≤ Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseResidualRadius p M γ (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ) → Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.InReferenceNeighborhood (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.pairwiseLocalRadius p M γ (Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ) κ) B₀ B → ‖B - B₀‖ ≤ 16 * (6 * M / γ) * √(↑p * ↑(p - 1) * (1 + Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ ^ 2)) * Causalean.Discovery.LinearDisentanglement.Quantitative.PairwiseAffine.conditionRoot p κ ^ 3 * ε` | Under positive dimension, positive shift scale and pairwise margin, condition envelope at least one, bounded pairwise-separated shifts, exact and approximate unit-diagonal feasibility, a nonnegative residual tolerance, the determinant/condition envelopes, and the explicit residual threshold and ordinary local reference neighborhood, the operator error is bounded by the requested displayed constant with `L = ((p!) κ^(p-1))^(1/p)`. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.Quantitative.CompactExclusion -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.Quantitative.PositiveExclusionRadius` | `Set X → Set X → (X → ℝ) → Type` | A positive exclusion radius is a positive lower bound for a residual on the part of `K` outside `U`, together with a candidate where that lower bound is attained. |
| `Discovery.LinearDisentanglement.Quantitative.exists_positiveExclusionRadius` | `IsCompact K → IsOpen U → x₀ ∈ K → x₀ ∈ U → (K \ U).Nonempty → Continuous r → (∀ x ∈ K, 0 ≤ r x) → (∀ x ∈ K, r x = 0 → x = x₀) → Nonempty (Causalean.Discovery.LinearDisentanglement.Quantitative.PositiveExclusionRadius K U r)` | For a compact candidate set, an open local neighborhood, a continuous residual, and a reference point, if the candidate set is compact, the neighborhood is open, the reference belongs to the candidate set, the reference belongs to the neighborhood, the far set is nonempty, the residual is continuous, the residual is nonnegative on candidates, and its only zero among candidates is the reference, then the far set has a strictly positive attained residual minimum. |
| `Discovery.LinearDisentanglement.Quantitative.continuous_simultaneousCongruenceResidual` | `Continuous (Causalean.Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual A s)` | For a finite real matrix family and its prescribed diagonal shifts, the simultaneous-congruence residual is continuous as a function of the candidate matrix. |
| `Discovery.LinearDisentanglement.Quantitative.exists_simultaneousCongruence_exclusionRadius` | `IsCompact K → IsOpen U → B₀ ∈ K → B₀ ∈ U → (K \ U).Nonempty → (∀ B ∈ K, Causalean.Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual A s B = 0 → B = B₀) → Nonempty (Causalean.Discovery.LinearDisentanglement.Quantitative.PositiveExclusionRadius K U (Causalean.Discovery.LinearDisentanglement.Quantitative.simultaneousCongruenceResidual A s))` | For a finite real matrix family, its diagonal shifts, a compact normalized candidate set, an open local chart, and a reference matrix, if the candidate set is compact, the chart is open, the reference is a candidate, the reference lies in the chart, the far candidates are nonempty, and only the reference has zero residual among candidates, then the far candidates have a strictly positive attained simultaneous-congruence residual minimum. |
| `Discovery.LinearDisentanglement.Quantitative.farFeasibleSet` | `Set (P × X) → (P → X) → (P → ℝ) → Set (P × X)` | For a parameter space, a candidate pseudometric space, a set of feasible parameter--candidate pairs, a reference candidate assigned to each parameter, and a real-valued radius assigned to each parameter, the far feasible set consists exactly of the feasible pairs whose candidate is at least that parameter's assigned radius from its reference candidate. |
| `Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius` | `Set (P × X) → (P → X) → (P → ℝ) → (P × X → ℝ) → Type (max u_1 u_2)` | A uniform positive exclusion radius records a far feasible pair where the residual reaches a strictly positive value that lower-bounds its value at every far feasible pair. |
| `Discovery.LinearDisentanglement.Quantitative.isClosed_farFeasibleSet` | `IsCompact K → Continuous x₀ → Continuous ρ → IsClosed (Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ)` | Given a feasible correspondence, a reference section, and a radius function, if the correspondence is compact, the reference section is continuous, and the radius function is continuous, then the parameter-dependent far feasible set is closed. |
| `Discovery.LinearDisentanglement.Quantitative.isCompact_farFeasibleSet_of_isClosed` | `IsCompact K → IsClosed (Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ) → IsCompact (Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ)` | Given a feasible correspondence, a reference section, and a radius function, if the correspondence is compact and its far feasible set is closed, then the far feasible set is compact. |
| `Discovery.LinearDisentanglement.Quantitative.exists_uniformPositiveExclusionRadius_of_isClosed` | `IsCompact K → IsClosed (Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ) → (Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ).Nonempty → Continuous r → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) = 0 → x = x₀ p) → (∀ (p : P), 0 < ρ p) → Nonempty (Causalean.Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius K x₀ ρ r)` | Given a feasible correspondence, a reference section, a residual, and a positive-radius function, if the correspondence is compact, the far feasible set is closed, the far feasible set is nonempty, the residual is continuous, the residual is nonnegative on feasible pairs, only the reference candidate has zero residual among feasible pairs, and every radius is strictly positive, then some far pair attains a strictly positive residual minimum on the far feasible set. |
| `Discovery.LinearDisentanglement.Quantitative.uniformCompactCorrespondence_dichotomy` | `IsCompact K → Continuous x₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) = 0 → x = x₀ p) → Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ = ∅ ∨ Nonempty (Causalean.Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius K x₀ ρ r)` | Given a feasible correspondence, a reference section, a residual, and a radius function, if the correspondence is compact, the reference section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference candidate has zero residual among feasible pairs, then either no far feasible pair exists or one attains a strictly positive uniform residual minimum. |
| `Discovery.LinearDisentanglement.Quantitative.uniformCompactCorrespondence_dichotomy_explicit` | `IsCompact K → Continuous x₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) = 0 → x = x₀ p) → Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ = ∅ ∨ ∃ pStar xStar ε₀, (pStar, xStar) ∈ Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ ∧ 0 < ε₀ ∧ r (pStar, xStar) = ε₀ ∧ ∀ z ∈ Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K x₀ ρ, ε₀ ≤ r z` | Given a feasible correspondence, a reference section, a residual, and a radius function, if the correspondence is compact, the reference section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference candidate has zero residual among feasible pairs, then either the far feasible set is empty or a far pair and its strictly positive attained residual minimum can be exhibited explicitly. |
| `Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius.dist_lt_of_residual_lt` | `(p, x) ∈ K → r (p, x) < h.radius → dist x (x₀ p) < ρ p` | Given a uniform positive exclusion radius, a feasible pair, and a residual strictly below that radius, then the candidate lies strictly inside its parameter-dependent reference neighborhood. |
| `Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius.dist_lt_of_residual_le_half` | `(p, x) ∈ K → r (p, x) ≤ h.radius / 2 → dist x (x₀ p) < ρ p` | Given a uniform positive exclusion radius, a feasible pair, and a residual at most half that radius, then the candidate lies strictly inside its parameter-dependent reference neighborhood. |
| `Discovery.LinearDisentanglement.Quantitative.exists_uniformExclusionTolerance_lt` | `IsCompact K → Continuous x₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) = 0 → x = x₀ p) → ∃ ε₀, 0 < ε₀ ∧ ∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) < ε₀ → dist x (x₀ p) < ρ p` | Given a feasible correspondence, a reference section, a residual, and a radius function, if the correspondence is compact, the reference section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference candidate has zero residual among feasible pairs, then one strictly positive tolerance sends every feasible pair with smaller residual inside its own reference neighborhood. |
| `Discovery.LinearDisentanglement.Quantitative.exists_uniformExclusionTolerance_le` | `IsCompact K → Continuous x₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) = 0 → x = x₀ p) → ∃ ε₀, 0 < ε₀ ∧ ∀ (p : P) (x : X), (p, x) ∈ K → r (p, x) ≤ ε₀ → dist x (x₀ p) < ρ p` | Given a feasible correspondence, a reference section, a residual, and a radius function, if the correspondence is compact, the reference section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference candidate has zero residual among feasible pairs, then one strictly positive tolerance sends every feasible pair with residual at most that tolerance inside its own reference neighborhood. |
| `Discovery.LinearDisentanglement.Quantitative.sqMatrix_uniformCompactCorrespondence_dichotomy` | `IsCompact K → Continuous B₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (B : Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d), (p, B) ∈ K → r (p, B) = 0 → B = B₀ p) → Causalean.Discovery.LinearDisentanglement.Quantitative.farFeasibleSet K B₀ ρ = ∅ ∨ Nonempty (Causalean.Discovery.LinearDisentanglement.Quantitative.UniformPositiveExclusionRadius K B₀ ρ r)` | Given a parameter-matrix correspondence, a reference matrix section, a residual, and a radius function, if the correspondence is compact, the reference matrix section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference matrix has zero residual among feasible pairs, then either no operator-norm-far pair exists or one attains a strictly positive uniform residual minimum. |
| `Discovery.LinearDisentanglement.Quantitative.exists_sqMatrix_uniformExclusionTolerance` | `IsCompact K → Continuous B₀ → Continuous r → Continuous ρ → (∀ (p : P), 0 < ρ p) → (∀ z ∈ K, 0 ≤ r z) → (∀ (p : P) (B : Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d), (p, B) ∈ K → r (p, B) = 0 → B = B₀ p) → ∃ ε₀, 0 < ε₀ ∧ ∀ (p : P) (B : Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d), (p, B) ∈ K → r (p, B) ≤ ε₀ → ‖B - B₀ p‖ < ρ p` | Given a parameter-matrix correspondence, a reference matrix section, a residual, and a radius function, if the correspondence is compact, the reference matrix section is continuous, the residual is continuous, the radius function is continuous, every radius is strictly positive, the residual is nonnegative on feasible pairs, and only the reference matrix has zero residual among feasible pairs, then one positive residual tolerance puts every feasible pair at or below it strictly inside its parameter-dependent Euclidean operator-norm reference ball. |
<!-- /GEN -->

### Collinear simultaneous-congruence ambiguity

These reusable finite-dimensional tools give a constructive converse to affine separation:
when two selected diagonal shifts are affine-collinear, a small normalized two-row
deformation preserves the whole covariance family while producing a distinct admissible
diagonalizer.  They are paper-independent and use only real matrix algebra, positivity,
and the finite-family small-parameter argument.

<!-- GEN:Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.Definitions -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate` | `(E → Fin d → ℝ) → Fin d → Fin d → Type` | A concrete certificate that the selected two-coordinate shift cloud is contained in the affine line `u x + v y = c`, with a nonzero normal vector. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairShear` | `Fin d → Fin d → ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d$-dimensional coordinate system, two selected coordinates, and three real numbers specifying a line normal and deformation magnitude, the elementary two-row shear is the identity matrix except for selected off-diagonal entries $tv$ and $tu$. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ` | For a $d\u2011by\u2011$d reference matrix, two selected coordinates, and a shear coefficient and magnitude, the first normalization denominator is $1+t v B_{ji}$. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ` | For a $d\u2011by\u2011$d reference matrix, two selected coordinates, and a shear coefficient and magnitude, the second normalization denominator is $1+t u B_{ij}$. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairRowNormalizer` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d\u2011by\u2011$d reference matrix, two selected coordinates, and a line normal and deformation magnitude, the pair row normalizer is the diagonal matrix that rescales the two selected rows by the reciprocals of their normalization denominators and leaves all other rows unchanged. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d\u2011by\u2011$d reference matrix, two selected coordinates, and a line normal and deformation magnitude, the normalized two-coordinate deformation is the pair row normalizer multiplied by the elementary pair shear. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d\u2011by\u2011$d reference diagonalizer, two selected coordinates, and a line normal and deformation magnitude, the deformed diagonalizer is the normalized two-coordinate deformation applied to the rows of the reference diagonalizer. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → Prop` | For a $d\u2011by\u2011$d matrix and two selected coordinates, pair-cycle admissibility holds exactly when the product of the two opposite selected off-diagonal entries is not one. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.commonShiftCrossTerm` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → ℝ → ℝ` | For a $d\u2011by\u2011$d reference matrix, two selected coordinates, and a line normal, offset, and deformation magnitude, the common shift cross term is the line offset times the deformation magnitude, divided by both selected normalization denominators. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairSymmetricOffDiagonal` | `Fin d → Fin d → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d$-dimensional coordinate system, two selected coordinates, and a real entry value, the symmetric selected off-diagonal matrix has that value in both selected off-diagonal positions and zero elsewhere. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → ℝ → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d\u2011by\u2011$d reference diagonalizer and invariant matrix, two selected coordinates, and a line normal, offset, and deformation magnitude, the deformed invariant matrix is obtained by first forming the normalized two-coordinate deformation, then taking its congruence transform of the invariant matrix and adding the symmetric common shift cross term. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Fin d → Fin d → ℝ → ℝ → ℝ → (Fin d → ℝ) → Fin d → ℝ` | For a $d\u2011by\u2011$d reference diagonalizer, two selected coordinates, a line normal and deformation magnitude, an original diagonal shift vector, and a coordinate, the deformed shift at that coordinate is the corresponding diagonal entry obtained after forming the normalized two-coordinate deformation and taking the congruence transform of the original diagonal shift matrix. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance` | `Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → (Fin d → ℝ) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d` | For a $d\u2011by\u2011$d diagonalizer and invariant matrix and a diagonal shift vector, the represented covariance matrix is the inverse congruence transform of the invariant matrix plus the diagonal shift matrix. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.RepresentsCovarianceFamily` | `(E → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d) → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → Causalean.Discovery.LinearDisentanglement.Quantitative.SqMatrix d → (E → Fin d → ℝ) → Prop` | For a dimension, an environment collection, a covariance matrix for each environment, a diagonalizer and invariant matrix, and a diagonal shift vector for each environment, the covariance family is represented exactly when every environment's covariance matrix equals the covariance represented by those common matrices and that environment's shift vector. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.Algebra -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation_zero` | `Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j u v 0 = 1` | At zero deformation parameter, the normalized pair deformation is the identity matrix. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer_zero` | `Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v 0 = B` | At zero deformation parameter, the deformed diagonalizer equals the reference diagonalizer. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant_zero` | `Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j u v c 0 = Ω` | At zero deformation parameter, the transformed invariant is the original invariant matrix. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift_zero` | `Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j u v 0 s = s` | At zero deformation parameter, the transformed diagonal shift is the original shift. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairShear_isUnit_det` | `i ≠ j → ∀ {u v t : ℝ}, 1 - t ^ 2 * u * v ≠ 0 → IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.pairShear i j u v t))` | When the selected coordinates are distinct and the selected shear-block determinant is nonzero, the elementary pair shear is invertible. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairRowNormalizer_isUnit_det` | `i ≠ j → ∀ {u v t : ℝ}, Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.pairRowNormalizer B i j u v t))` | When the selected coordinates are distinct and both row-normalization denominators are nonzero, the diagonal row normalizer is invertible. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation_isUnit_det` | `i ≠ j → ∀ {u v t : ℝ}, Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → 1 - t ^ 2 * u * v ≠ 0 → IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j u v t))` | When the selected coordinates are distinct, both normalization denominators are nonzero, and the shear determinant is nonzero, the normalized pair deformation is invertible. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer_unitDiagonal` | `i ≠ j → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → ∀ {u v t : ℝ}, Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v t)` | When the selected coordinates are distinct, the reference diagonal is unit-normalized, and both normalization denominators are nonzero, row normalization restores unit diagonal. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer_cycle_identity` | `i ≠ j → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → ∀ {u v t : ℝ}, Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t * Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t * (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v t i j * Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v t j i - 1) = (B i j * B j i - 1) * (1 - t ^ 2 * u * v)` | When the selected coordinates are distinct, the reference diagonal is unit-normalized, and both normalization denominators are nonzero, the selected cycle-product defect obeys the stated shear identity. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer_pairCycleAdmissible` | `i ≠ j → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B i j → ∀ {u v t : ℝ}, Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → 1 - t ^ 2 * u * v ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v t) i j` | When the selected coordinates are distinct, the reference diagonal is unit-normalized, its selected two-cycle is admissible, both normalization denominators are nonzero, and the shear determinant is nonzero, the deformed selected two-cycle remains admissible. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer_ne` | `i ≠ j → IsUnit (Matrix.det B) → ∀ {u v t : ℝ}, u ≠ 0 ∨ v ≠ 0 → t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j u v t ≠ B` | When the selected coordinates are distinct, the reference diagonalizer is invertible, the line normal is nonzero, the deformation parameter is nonzero, and both normalization denominators are nonzero, the deformed diagonalizer differs from the reference. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift_apply_first` | `i ≠ j → ∀ (u v t : ℝ) (s : Fin d → ℝ), Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j u v t s i = (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t)⁻¹ ^ 2 * (s i + t ^ 2 * v ^ 2 * s j)` | When the selected coordinates are distinct, the first transformed shift is its stated nonnegative weighted sum. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift_apply_second` | `i ≠ j → ∀ (u v t : ℝ) (s : Fin d → ℝ), Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j u v t s j = (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t)⁻¹ ^ 2 * (t ^ 2 * u ^ 2 * s i + s j)` | When the selected coordinates are distinct, the second transformed shift is its stated nonnegative weighted sum. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift_apply_of_ne` | `k ≠ i → k ≠ j → ∀ (u v t : ℝ) (s : Fin d → ℝ), Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j u v t s k = s k` | When the coordinate is neither selected coordinate, its transformed shift is unchanged. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift_nonnegative` | `i ≠ j → ∀ (u v t : ℝ) (s : Fin d → ℝ), (∀ (k : Fin d), 0 ≤ s k) → ∀ (k : Fin d), 0 ≤ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j u v t s k` | When the selected coordinates are distinct and the original shift is coordinatewise nonnegative, the transformed shift is coordinatewise nonnegative. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.pairShear_diagonal_congruence_decomposition` | `i ≠ j → ∀ (s : E → Fin d → ℝ) (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j) (t : ℝ) (e : E), have T := Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j cert.u cert.v t; T * Matrix.diagonal (s e) * Matrix.transpose T = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.pairSymmetricOffDiagonal i j (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.commonShiftCrossTerm B i j cert.u cert.v cert.c t) + Matrix.diagonal (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j cert.u cert.v t (s e))` | When the selected coordinates are distinct, the transformed diagonal shift matrix splits into the common symmetric off-diagonal term and its new diagonal shift. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.total_congruence_decomposition` | `i ≠ j → ∀ (s : E → Fin d → ℝ) (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j) (t : ℝ) (e : E), have T := Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j cert.u cert.v t; T * (Ω + Matrix.diagonal (s e)) * Matrix.transpose T = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j cert.u cert.v cert.c t + Matrix.diagonal (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j cert.u cert.v t (s e))` | When the selected coordinates are distinct, the latent congruence is exactly the deformed invariant plus the transformed diagonal shift. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance_deformation_eq` | `i ≠ j → ∀ (s : E → Fin d → ℝ) (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j) (t : ℝ) (e : E), IsUnit (Matrix.det B) → IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j cert.u cert.v t)) → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j cert.u cert.v t) (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j cert.u cert.v cert.c t) (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j cert.u cert.v t (s e)) = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance B Ω (s e)` | With two distinct selected coordinates, an invertible reference diagonalizer, and an invertible normalized deformation, the transformed representation gives exactly the same covariance matrix in each environment. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance_posDef` | `IsUnit (Matrix.det B) → Matrix.PosDef Ω → (∀ (k : Fin d), 0 ≤ s k) → Matrix.PosDef (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance B Ω s)` | Given an invertible diagonalizer, a positive-definite invariant, and a coordinatewise nonnegative shift, the represented covariance matrix is positive definite. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant_isSymm` | `i ≠ j → Matrix.IsSymm Ω → ∀ (u v c t : ℝ), Matrix.IsSymm (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j u v c t)` | When the selected coordinates are distinct and the original invariant is symmetric, the deformed invariant is symmetric. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.SmallParameter -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.CollinearAmbiguity.exists_deformedInvariant_posDef_radius` | `i ≠ j → Matrix.PosDef Ω → ∀ (u v c : ℝ), ∃ ρ > 0, ∀ (t : ℝ), \|t\| < ρ → Matrix.PosDef (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j u v c t)` | When the selected coordinates are distinct and the original invariant is positive definite, some positive radius keeps every transformed invariant positive definite. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.exists_algebraic_admissibility_radius` | `∃ ρ > 0, ∀ (t : ℝ), \|t\| < ρ → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j v t ≠ 0 ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j u t ≠ 0 ∧ 1 - t ^ 2 * u * v ≠ 0` | Some positive radius makes both row-normalization denominators and the selected shear determinant nonzero. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.exists_small_admissible_parameter` | `i ≠ j → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B → IsUnit (Matrix.det B) → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B i j → Matrix.PosDef Ω → ∀ (s : E → Fin d → ℝ) (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j), (∀ (e : E) (k : Fin d), 0 ≤ s e k) → ∀ {r : ℝ}, 0 < r → ∃ t, 0 < \|t\| ∧ \|t\| < r ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.firstNormalizationDenom B i j cert.v t ≠ 0 ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.secondNormalizationDenom B i j cert.u t ≠ 0 ∧ 1 - t ^ 2 * cert.u * cert.v ≠ 0 ∧ IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.normalizedPairDeformation B i j cert.u cert.v t)) ∧ IsUnit (Matrix.det (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j cert.u cert.v t)) ∧ Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j cert.u cert.v t) ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j cert.u cert.v t) i j ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B i j cert.u cert.v t ≠ B ∧ Matrix.PosDef (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B Ω i j cert.u cert.v cert.c t) ∧ ∀ (e : E) (k : Fin d), 0 ≤ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B i j cert.u cert.v t (s e) k` | Given two distinct selected coordinates, a unit-diagonal reference matrix, an invertible reference matrix, an admissible selected two-cycle, a positive-definite invariant, coordinatewise nonnegative shifts, and a positive requested bound, there is a nonzero deformation parameter below that bound preserving all stated admissibility, positivity, and nonnegativity properties. |
<!-- /GEN -->

<!-- GEN:Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.Main -->
| Decl | Signature | Description |
|---|---|---|
| `Discovery.LinearDisentanglement.CollinearAmbiguity.exists_collinear_simultaneous_congruence_ambiguity` | `i ≠ j → Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ → IsUnit (Matrix.det B₀) → Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B₀ i j → Matrix.PosDef Ω₀ → (∀ (e : E) (k : Fin d), 0 ≤ s e k) → ∀ (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j) {r : ℝ}, 0 < r → ∃ t B₁ Ω₁ s₁, 0 < \|t\| ∧ \|t\| < r ∧ B₁ = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedDiagonalizer B₀ i j cert.u cert.v t ∧ Ω₁ = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedInvariant B₀ Ω₀ i j cert.u cert.v cert.c t ∧ (s₁ = fun e => Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.deformedShift B₀ i j cert.u cert.v t (s e)) ∧ Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₁ ∧ IsUnit (Matrix.det B₁) ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B₁ i j ∧ B₁ ≠ B₀ ∧ Matrix.IsSymm Ω₁ ∧ Matrix.PosDef Ω₁ ∧ (∀ (e : E) (k : Fin d), 0 ≤ s₁ e k) ∧ (∀ (e : E), Matrix.PosDef (Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance B₀ Ω₀ (s e))) ∧ ∀ (e : E), Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance B₁ Ω₁ (s₁ e) = Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.representedCovariance B₀ Ω₀ (s e)` | Given two distinct selected coordinates, a unit-diagonal reference diagonalizer, its invertibility, an admissible selected two-cycle, a positive-definite invariant, coordinatewise nonnegative shifts, and a positive parameter bound, an affine-collinearity certificate produces a distinct normalized invertible diagonalizer, a symmetric positive-definite invariant, and nonnegative shifts representing the identical finite covariance family. |
| `Discovery.LinearDisentanglement.CollinearAmbiguity.exists_interior_collinear_ambiguity_example` | `i ≠ j → (∀ (e : E) (k : Fin d), 0 ≤ s e k) → ∀ (cert : Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.AffineLineCertificate s i j), ∃ Sigma B₀ B₁ Ω₀ Ω₁ s₁, B₀ = 1 ∧ Ω₀ = 1 ∧ Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₀ ∧ IsUnit (Matrix.det B₀) ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B₀ i j ∧ Causalean.Discovery.LinearDisentanglement.Quantitative.UnitDiagonal B₁ ∧ IsUnit (Matrix.det B₁) ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.PairCycleAdmissible B₁ i j ∧ B₁ ≠ B₀ ∧ Matrix.PosDef Ω₀ ∧ Matrix.IsSymm Ω₁ ∧ Matrix.PosDef Ω₁ ∧ (∀ (e : E) (k : Fin d), 0 ≤ s₁ e k) ∧ (∀ (e : E), Matrix.PosDef (Sigma e)) ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.RepresentsCovarianceFamily Sigma B₀ Ω₀ s ∧ Causalean.Discovery.LinearDisentanglement.CollinearAmbiguity.RepresentsCovarianceFamily Sigma B₁ Ω₁ s₁` | Given two distinct selected coordinates and coordinatewise nonnegative shifts, the prescribed affine-collinear shift family has a positive-definite interior covariance example with two distinct compatible normalized diagonalizers. |
<!-- /GEN -->

## 17c. `Discovery/InvariantPrediction/` — Invariant Causal Prediction (Peters–Bühlmann–Meinshausen 2016)

Formalization of the setup and soundness of Peters, Bühlmann & Meinshausen, *Causal inference
using invariant prediction* (JRSS-B 2016, `arXiv:1501.01332`). An **environment family**
`EnvFamily` is a `Fintype`-indexed family of generalized SCMs `{M i}` over common observed/latent
node sets, sharing the target's parent set (E4 `hParents`), its structural mechanism (E2 `hStruct`,
as `HEq`), the latent-noise law (E3 `hLatent`, as `HEq`), and carrying an **exogeneity** field
`hExo` (the target's latent parents are independent of its observed parents under each environment's
joint law — the SEM assumption `εᵉ ⊥ Xᵉ_{S*}` that rules out hidden confounding into `Y`).
A predictor set `S` is **invariant** when the conditional law of the target given `X_S` is one fixed
kernel across all environments (an a.e. statement w.r.t. each environment's predictor marginal).

| Declaration | Statement | Description |
| --- | --- | --- |
| `EnvFamily` / `paObs` / `paLat` / `Invariant` (`Model.lean`, `Invariance.lean`, `MechanismFactor.lean`) | environment-family structure with shared parents/mechanism/latent law + exogeneity `hExo`; observed/latent parents of the target; the invariance predicate (one shared conditional kernel a.e. per environment) | Model + grammar layer. **Defs** |
| `mechanism_invariant` (`Invariance.lean`) | the target's observed parents `paObs` form an invariant set: in every environment the conditional law of the target given its observed parents equals one fixed structural factor | **Headline (the engine of soundness), proved.** Witness = law of `structFun Y` driven by the latent-parent noise (reference environment); per-environment disintegration via exogeneity (`hExo`), cross-environment agreement via shared E2/E3/E4. `#print axioms` = `[propext, sorryAx, Classical.choice, Quot.sound]`, where `sorryAx` is **only** the isolated `s_eq_on_fixed_parents` regularity gap below. **Proved (modulo one regularity gap)** |
| `icp_sound` / `idSet` / `invariantSets` (`Soundness.lean`, `IdentifiedSet.lean`) | **Theorem 1 (soundness):** the identified set `S(E) = ⋂` (invariant sets) is contained in the target's observed parents — ICP never selects a non-cause | Immediate from `mechanism_invariant`. Same axiom profile (reduces to `s_eq_on_fixed_parents`). **Proved (modulo the same gap)** |
| `mechanismFun` / `condDistrib_target_eq_mechanismKernel` / `mechanismKernel_env_eq` / `jointKernel_map_paLat_eq_latentProduct_map` / `structFun_yNode_apply_eq` / `latentProduct_heq` / `map_heq_transport` (`MechanismFactor.lean`) | the structural mechanism as a measurable map of observed+latent parents; per-environment factorization of the target conditional through the mechanism kernel; cross-environment equality of that kernel; latent-parent marginal = latent-product marginal; cross-environment `structFun Y` agreement; HEq transport of latent product measures / push-forwards | Supporting machinery for `mechanism_invariant` (Steps A/B/C). **Proved (sorry-free)** |
| `s_eq_on_fixed_parents` (`MechanismFactor.lean`) | environments assign the same value to any **fixed** (intervened) parent of the target | **The one residual `sorry`.** Holds vacuously when the target has no fixed parents; not derivable from the bare `EnvFamily` fields (the `EnvFamily` shares the parent set and forbids changing it, but does not pin the intervention *value* on a fixed parent), so it is isolated here as the structural regularity gap (ICP's "no intervention on a direct cause of `Y` with a shifting value"). **`sorry` (isolated gap)** |
| `parent_omitted_not_invariant` / `icp_complete` / `RichEnv` (`Completeness.lean`) | **Theorem 2 (completeness):** under richness/faithfulness primitives `S(E)` equals the parents | Completeness direction; the hard propagation lemma `parent_omitted_not_invariant` is a pre-existing escalated `sorry` (unchanged). **Partial (`sorry`)** |

### `LinearGaussian/` — completeness in the linear-Gaussian model (`prop:1`(i), sorry-free)

A self-contained linear-Gaussian sub-development (random-variable encoding, NOT built on the
nonparametric SWIG/kernel layer above) that proves the **converse** `S(E) ⊇ PA(Y)` — the only setting
in which the paper establishes it — via the do-intervention **mean-shift** argument. The observational
SEM `ObsSEM` carries the coefficient matrix `β`, an acyclic `DAG` (`edge k j ↔ βⱼₖ ≠ 0`), Gaussian
noises `εⱼ ~ N(0,σⱼ²)`, and the paper's **Assumption-1 exogeneity** `hYexo` (`ε₀ ⊥ Xₖ` for each parent
`k ∈ PA(Y)`); `Env` is a single do-intervention environment on the same probability space (with its own
`hExo`); `EnvFamily` bundles the observational SEM with finitely many environments. The regression layer
(`Regression.lean`) defines the residual `R = Y − Σ γₖ Xₖ`, the regression-invariance null `H_{0,S}`
(`InvarianceNull`: a shared coefficient `γ` and residual law `Fε`, residual ⊥ predictors and same law
across environments), and the identified set `S(E) = ⋂{S : H_{0,S}}`.

| Declaration | Statement | Description |
| --- | --- | --- |
| `ObsSEM` / `Env` / `EnvFamily` / `InvarianceNull` / `identifiedSet` (`Model.lean`, `Regression.lean`) | observational linear-Gaussian SEM with exogeneity field `hYexo`, do-intervention environment with `hExo`, environment family; the regression residual, invariance null `H_{0,S}`, and identified set `S(E)` | Model + observable layer. **Defs** |
| `eps_integrable` / `eps_integral_zero` (`Helpers/Moments.lean`) | each noise `εⱼ` is integrable with `E[εⱼ] = 0` (Gaussian first moment) | Moment helpers. **Proved (sorry-free)** |
| `obsResidual_eq_eps` / `envResidual_eq_eps` (`Helpers/Residual.lean`) | with the causal coefficient `γ* = β₀,·`, the residual equals the target noise `ε₀` a.e. in both the observational and every interventional environment | Algebraic helpers (target never intervened, `β₀₀ = 0`). **Proved (sorry-free)** |
| `nonDescendant_invariance` (`Helpers/Invariance.lean`) | under the single do-intervention `do(X_{k₀}=a)`, every coordinate that is neither `k₀` nor a descendant of `k₀` keeps its observational value a.e. | Structural backbone of the mean-shift (strong induction on topological order). **Proved (sorry-free)** |
| `icp_sound_linearGaussian` (`Completeness.lean`) | **Soundness:** `S(E) ⊆ PA(Y)` — `PA(Y)` satisfies the invariance null with `γ* = β₀,·` and residual law `N(0,σ₀²)` (residual `=ᵐ ε₀`, exogeneity gives independence, `hGauss` gives the shared law) | Reuses the exogeneity fields `hYexo`/`hExo`. **Proved (sorry-free)** |
| `exists_youngest_nonzero` (`Completeness.lean`) | among a nonempty index set there is a youngest one (largest topological order), no directed path to any other element | Youngest-node selection. **Proved (sorry-free)** |
| `residual_mean_shift_of_doIntervention` (`Completeness.lean`) | under `do(X_{k₀}=a)` with `a ≠ E[X¹_{k₀}]`, `α_{k₀} ≠ 0`, and the youngest-node property `hyoung`, the interventional and observational residuals have different means (gap `α_{k₀}·(a − E[X¹_{k₀}])`), so they are not `IdentDistrib` | The eq:help1/help2 mean-shift; `hyoung` kills all non-`k₀` terms. **Proved (sorry-free)** |
| `icp_complete_linearGaussian` (`Completeness.lean`) | **Theorem `prop:1`(i) (completeness):** with shifted single do-interventions on every predictor, `S(E) = PA(Y)` | Combines soundness with the mean-shift: a missing parent forces a youngest support index whose intervention breaks the shared residual law. `#print axioms` = `[propext, Classical.choice, Quot.sound]`. **Proved (sorry-free)** |

## 18. `Experimentation/` — design-based (randomization) inference under interference

The experimentation cluster: a shared, paper-agnostic **design-based substrate**
(`Experimentation/DesignBased/`, namespace `Causalean.Experimentation.DesignBased`) plus one
folder per formalized paper that consumes it (`Experimentation/ExposureMappingInterference/` and
`Experimentation/TwoStageInterference/`, namespaces `Causalean.Experimentation.{ExposureMappingInterference,TwoStageInterference}`).
The substrate is the finite-population,
fixed-potential-outcome flavor of the potential-outcomes framework — probability comes from the
experimenter's randomization over a *finite* assignment space `Ω` (a sibling of the
measure-theoretic superpopulation `PO/`, which it never imports), with a deliberately lightweight
finite-sum layer (`E X = ∑ z, p z · X z`) so all algebraic identities are `Finset` algebra.
Further experimentation papers (two-stage experiments, …) slot in as sibling folders under
`Experimentation/`, reusing `DesignBased`. First paper: Aronow & Samii (2017, AOAS), "Estimating
Average Causal Effects Under General Interference" (arXiv:1305.6156; plan `doc/aronow_samii_plan.md`).
Second paper: Hudgens & Halloran (2008, JASA), "Toward Causal Inference With Interference" — the
two-stage / partial-interference estimand zoo (direct/indirect/total/overall effects), which forced
the reusable `Product`/`TwoStage` substrate combinators (plan `doc/hudgens_halloran_plan.md`).

**Main results** (the paper's named theorems; flagged as headlines in
`doc/library_review/Experimentation.json` — everything else is supporting substrate):
`Experimentation.DesignBased.E_htEffect` (Lemma 4.1, HT effect unbiasedness — substrate),
`Experimentation.DesignBased.Var_htTotal` (Prop 4.4, HT variance — substrate),
`Experimentation.ExposureMappingInterference.htMean_consistent_of_var` (Prop 6.4, consistency),
`…ExposureMappingInterference.wald_coverage_of_conditions` (Prop 6.5, oracle-variance Wald coverage from
primitive conditions), `…ExposureMappingInterference.wald_coverage_feasible_of_conditions` (Prop 6.5/6.6, the
feasible estimated-V̂ interval — the flagship), and
`…ExposureMappingInterference.var_NsqVhat_tendsto_zero_of_conditions` (Prop 6.6, variance-estimator consistency).
Hudgens–Halloran main results: `…TwoStageInterference.E_estDirect` / `E_estIndirect` / `E_estTotal`
(Theorems 1–3, unbiasedness of the direct/indirect/total effect estimators), `…TwoStageInterference.CE_total_decomp`
(total = direct + indirect), `…TwoStageInterference.Var_tauHat` (Theorem 5, the within-group Neyman variance), and
`…TwoStageInterference.Var_popEst` (Theorem 4, the two-stage variance decomposition), and
`…TwoStageInterference.Var_estDirect` (Theorem 6, the direct-effect-estimator variance).
The substrate (`DesignBased/`: finite-sum `E`/`Var`/`Cov`, exposure, HT, Chebyshev, product/compound
designs, the abstract `var_edge_sum_le`, normal-CDF facts, the measure bridge) is reusable
architecture, not main results.

### `Experimentation/DesignBased/Design.lean` — finite randomization design + `E`/`Var`/`Cov` algebra

| Declaration | Statement | Description |
|---|---|---|
| `FiniteDesign` | structure: `p : Ω → ℝ`, `p_nonneg`, `p_sum : ∑ p = 1` | A randomization design = a probability mass function on a finite assignment space `Ω`. |
| `FiniteDesign.E` / `Var` / `Cov` | `E X = ∑ z, p z · X z`; `Var X = E[(X−E X)²]`; `Cov X Y = E[(X−E X)(Y−E Y)]` | Finite-sum expectation, variance, covariance of real random variables under the design. |
| `ind` / `Pr` | `ind A = fun z => if A z then 1 else 0`; `Pr A = E (ind A)` | Indicator of an event and its design probability. |
| `E_const`/`E_add`/`E_sub`/`E_const_mul`/`E_mul_const`/`E_neg`/`E_sum`/`E_congr` | linearity of `E` | Expectation is linear and respects finite sums and pointwise equality. |
| `Var_eq` / `Cov_eq` | `Var X = E[X²] − (E X)²`; `Cov X Y = E[X·Y] − E X · E Y` | The computational forms of variance and covariance. |
| `Cov_self` / `Cov_comm` / `Var_congr` / `Cov_congr` | `Cov X X = Var X`; symmetry; congruence | Basic covariance/variance identities. |
| `Cov_const_mul_left/right` / `Cov_sub_left/right` / `Cov_sum_left/right` | bilinearity of `Cov` | Covariance is bilinear over scalar multiples, differences, and finite sums. |
| `Cov_linear_comb` / `Var_linear_comb` | `Cov (∑ cᵢXᵢ) (∑ eⱼYⱼ) = ∑∑ cᵢeⱼ Cov(Xᵢ,Yⱼ)`; variance specialization | Covariance/variance of finite linear combinations as double sums — the workhorse for HT variance. |
| `Var_sub` / `Var_const_mul` | `Var(X−Y)=Var X+Var Y−2Cov(X,Y)`; `Var(c·X)=c²·Var X` | Variance of a difference and under scaling. |
| `E_ind` / `ind_sq` / `Var_ind` | `E(ind A)=Pr A`; `(ind A)²=ind A`; `Var(ind A)=Pr A·(1−Pr A)` | Indicator expectation/idempotence/variance. |
| `E_nonneg` / `E_le_one` / `ind_nonneg` / `ind_le_one` / `Pr_nonneg` / `Pr_le_one` | `[0,1]` bounds | Elementary bounds: expectations of `[0,1]`-valued variables and probabilities lie in `[0,1]`. |

### `Experimentation/DesignBased/Exposure.lean` — exposure mappings and generalized probability of exposure

| Declaration | Statement | Description |
|---|---|---|
| `expo` / `expoInd` | `expo f θ i z = f z (θ i)`; `expoInd f θ i d = ind (expo i = d)` | The exposure unit `i` receives under assignment `z`, and its `{0,1}` indicator. |
| `prop` / `propPairSame` / `propPairCross` | `π_i(d)=Pr(expo i=d)`; `π_{ij}(d)=E[1ᵢ1ⱼ]`; `π_{ij}(d,d')=E[1ᵢ(d)1ⱼ(d')]` | Generalized probability of exposure and joint exposure probabilities. |
| `E_expoInd` / `sum_prop_eq_one` | `E(expoInd i d)=π_i(d)`; `∑_d π_i(d)=1` | Propensity as an expectation; propensities sum to one over exposures. |
| `Cov_expoInd_same` / `Cov_expoInd_cross` | `Cov(1ᵢ(d),1ⱼ(d))=π_{ij}(d)−π_iπ_j` (and cross) | Covariance of exposure indicators in terms of joint/marginal propensities. |
| `expoInd_mul_self_of_ne` / `propPairCross_self_of_ne` | for `d≠d'`: `1ᵢ(d)·1ᵢ(d')=0`; `π_{ii}(d,d')=0` | A unit cannot occupy two distinct exposures at once. |

### `Experimentation/DesignBased/PotentialOutcome.lean` — properly-specified mapping (Cond 1) and consistency (Cond 2)

| Declaration | Statement | Description |
|---|---|---|
| `ProperlySpecified` | `∀ i z, yr i z = y i (expo i z)` | Condition 1: interference acts only through the exposure (potential outcome factors through it). |
| `Yobs` | `Yobs y f θ i z = y i (expo i z)` | Observed outcome of unit `i` under assignment `z`. |
| `Yobs_eq_sum` | `Yobs i z = ∑_d 1ᵢ(d)·y i d` | Condition 2 (consistency), as a derived lemma. |
| `expoInd_mul_Yobs` / `expoInd_mul_Yobs_sq` / `expoInd₂_mul_Yobs` | on-event substitutions `1·Yobs = 1·y` (and squared / two-indicator forms) | The exposure indicator forces the observed outcome to its potential-outcome value. |

### `Experimentation/DesignBased/HT/{Estimator,Unbiased,Variance}.lean` — Horvitz–Thompson estimators

| Declaration | Statement | Description |
|---|---|---|
| `htTotal` / `htMean` / `htEffect` | `ŷᵀ(d)=∑ᵢ 1ᵢ(d)Yᵢ/π_i(d)`; `μ̂=ŷᵀ/N`; `τ̂=μ̂(dk)−μ̂(dl)` | Inverse-probability-weighted total, mean, and effect estimators. |
| `muTrue` / `tauTrue` / `htTotal_eq` | `μ(d)=(1/N)∑ y i d`; `τ=μ(dk)−μ(dl)`; HT total in `y i d` form | Estimands and the indicator-forced rewrite of the estimator. |
| `E_htTotal` / `E_htMean` / `E_htEffect` | unbiasedness (Lemma 4.1, Prop 4.4) | The HT total/mean/effect estimators are unbiased under positive propensities. **Proved.** |
| `Var_htTotal_cov` / `Var_htTotal` | variance as double-sum-of-covariances and expanded form `eq:total_variance` | Randomization variance of `ŷᵀ(d)`: `∑ π_i(1−π_i)(y/π)² + ∑_{i≠j}(π_{ij}−π_iπ_j)(y/π)(y/π)`. **Proved.** |
| `Cov_htTotal_cov` / `Cov_htTotal` | covariance forms `eq:totals_covariance` | `Cov[ŷᵀ(dk),ŷᵀ(dl)]`; the diagonal yields the famously unidentified `−∑ᵢ yᵢ(dk)yᵢ(dl)` term (needs `dk≠dl`). **Proved.** |

### `Experimentation/ExposureMappingInterference/Variance/Conservative.lean` — conservative variance estimators (§5, positive-joint + zero-pairwise)

| Declaration | Statement | Description |
|---|---|---|
| `htVarEst` / `htCovEst` / `htEffectVarEst` | the HT variance/covariance/effect-variance estimators | Design-based estimators of `Var[ŷᵀ]`, `Cov[ŷᵀ,ŷᵀ]`, `Var[τ̂]` (positive-joint regime). |
| `E_htVarEst` | `E[V̂[ŷᵀ(d)]] = Var[ŷᵀ(d)]` (Lemma 5.1) | The variance estimator is exactly unbiased in the positive-joint regime. **Proved.** |
| `E_htCovEst_le` | `E[Ĉov] ≤ Cov` (Prop 5.4) | The covariance estimator is nonpositively biased (Young's inequality on the diagonal). **Proved.** |
| `E_htEffectVarEst_ge` | `Var[τ̂] ≤ E[V̂[τ̂]]` (Prop 5.7) | The assembled effect-variance estimator is conservative (nonnegative bias). **Proved.** |
| `E_htVarEst_eq_addBias` | `E[V̂[ŷᵀ(d)]] = Var[ŷᵀ(d)] + A` (Prop 5.2) | Bias of the variance estimator when some joint probabilities vanish, `A = ∑_{π_{ij}(d)=0} y_i(d)y_j(d)`. **Proved.** |
| `htA2` / `E_htVarEst_add_htA2_ge` | `Var[ŷᵀ(d)] ≤ E[V̂ + Â₂]` (Prop 5.3) | Young correction `Â₂` over zero-joint pairs makes the variance estimator conservative without the positive-joint assumption. **Proved.** |
| `E_htCovEst_eq_of_noEffect` | `E[Ĉov] = Cov` under `y_i(dk)=y_i(dl)` (Prop 5.5) | The covariance estimator is exactly unbiased when there is no effect (the Young diagonal correction is exact). **Proved.** |
| `htCovEstA` / `E_htCovEstA_le` | `E[Ĉov_A] ≤ Cov` (Prop 5.6) | General covariance estimator handling zero cross-joints; Young correction over all `j` with `π_{ij}(dk,dl)=0` (incl. the diagonal `j=i`, subsuming the unidentified `−∑ᵢ y_i(dk)y_i(dl)` term). Nonpositively biased with no positive-cross-joint assumption. **Proved.** |
| `htEffectVarEstA` / `E_htEffectVarEstA_ge` | `Var[τ̂] ≤ E[V̂_A[τ̂]]` (general Prop 5.7) | General conservative effect-variance estimator assembling `Â₂` and `Ĉov_A`; nonnegative bias with no joint-positivity hypotheses. **Proved.** |

### `Experimentation/DesignBased/{Chebyshev,GaussianCDF,EdgeVarianceBound,FiniteDesignMeasure}.lean` (substrate) + `Experimentation/ExposureMappingInterference/Asymptotics/{Consistency,SteinCLT,SteinInstance,Intervals,VarianceConsistency,VarEstQuadBound,VarEstConsistencyConditions}.lean` (paper) — large-sample theory

| Declaration | Statement | Description |
|---|---|---|
| `FiniteDesign.chebyshev` | `Pr[|X−E X|≥ε] ≤ Var X/ε²` | Finite Chebyshev inequality (the convergence-in-probability engine). **Proved.** |
| `Experiment` / `gdep` / `N` | sequence-of-experiments bundle; pairwise dependency indicator; population size | Substrate for asymptotics over a sequence of nested finite-population experiments. |
| `Var_htMean_le` | `Var[μ̂(d)] ≤ c²(N+∑g)/N²` | Variance bound under bounded weights and pairwise dependence. **Proved.** |
| `htMean_consistent_of_var` | `Var→0 ⇒ Pr[|μ̂−μ|≥ε]→0` (Prop 6.4) | Consistency of the HT estimator via Chebyshev + unbiasedness. **Proved.** |
| `stdNormalCdf` / `stdNormalCdf_neg` / `monotone_stdNormalCdf` | `Φ(t)=(𝒩(0,1)).real(Iic t)`; `Φ(−t)=1−Φ(t)`; monotone | Standard-normal CDF (`GaussianCDF.lean`), agrees with Mathlib `cdf`; symmetry from `gaussianReal_map_neg` + atomlessness. **Proved.** |
| `Experiment.studentizedEffect` | `(τ̂−τ)/√Var[τ̂]` | The studentized HT effect statistic. |
| `LocalDependenceCLT` | interface premise: studentized statistic `→d 𝒩(0,1)` (Chen–Shao 2004 Thm 2.7, bounded-neighborhood case) | **DISCHARGED** by `localDependenceCLT_of_stein` (`SteinInstance.lean`) via the from-scratch Stein CLT below — no longer an open gate. |
| `wald_coverage` | `1−α ≤ liminf` of Wald-interval coverage (Prop 6.5, "at least `1−α`") | **Proved** from `LocalDependenceCLT` + nonzero variance + CDF symmetry. |
| `GaussianCDF`/`FiniteDesignMeasure`/`SteinInstance` | `stdNormalCdf`+symmetry; `D.toMeasure := ∑ p(z)·δ_z` bridge (`∫=E`,`.real=Pr`,`variance=Var`); `localDependenceCLT_of_stein`, `wald_coverage_of_stein` | Bridge from the lightweight design layer to the measure-theoretic Stein CLT + the discharge. `wald_coverage_of_stein` is **end-to-end** A–S asymptotic coverage from primitive conditions (no CLT premise). **All axiom-clean.** |
| `continuous_stdNormalCdf` | `Φ` is continuous (Gaussian atomless) | Used by the *feasible* interval (`GaussianCDF.lean`). **Proved.** |
| `wald_coverage_feasible` | `1−α ≤ liminf` coverage of `τ̂ ± z·√V̂` (Prop 6.5, the paper's **estimated**-variance interval) | The faithful interval, from CLT + a single variance-estimator-consistency input `hVhat` (`Intervals.lean`). **Proved.** |
| `htEffectVarEst_undershoot_tendsto_zero` / `relVar_of_NsqVar_tendsto` / `wald_coverage_feasible_of_relVar` | reduce `hVhat` to `Var[V̂]/Var²→0`; bridge `Var[N·V̂]→0`+Cond4 ⇒ that; capstone | Chebyshev reduction + conservativeness `E[V̂]≥Var` isolate the variance-consistency input to one limit (`VarianceConsistency.lean`). **Proved, axiom-clean.** |
| `FiniteDesign.var_edge_sum_le` | `Var[∑ᵢⱼ bᵢⱼ] ≤ 8M²m³N` for bounded edge-functions vanishing off a degree-≤`m` graph with unlinked-edge `Cov=0` | Abstract pure-FiniteDesign formalization of the appendix `O(N⁻¹)` quadruple-sum count (`EdgeVarianceBound.lean`). Reusable. **Proved, axiom-clean.** |
| `var_htEdgeStat_le` | `Var[ŷVar(dk)+ŷVar(dl)−2Ĉov] ≤ 8·vbBound²·m³·N` | Per-population bound: `V̂_raw` written as an edge-sum, then `var_edge_sum_le` under Conditions 1, 1′ (joint overlap `1/π_{ij}≤c₃`), 3 (`VarEstQuadBound.lean`). **Proved, axiom-clean.** |
| `var_NsqVhat_tendsto_zero_of_conditions` / `wald_coverage_feasible_of_conditions` | `N²·Var[V̂]→0` from uniform Conditions 1/1′/3 + `N→∞`; end-to-end feasible coverage `≥1−α` from the primitive conditions | Discharges the variance-consistency input to primitives (`VarEstConsistencyConditions.lean`). The only assumption beyond the literal Conditions 1/3/4 is the explicit joint-overlap bound `1/π_{ij}≤c₃` that the paper's appendix uses **implicitly**. **Proved, axiom-clean.** |

**Faithfulness note (Prop 6.6).** The paper's appendix derivation of `Var[N·V̂]→0` treats each
variance-estimator summand `aᵢⱼ ∝ 1/π_{ij}` as `O(1)`, but Condition 1 bounds only `1/π_i`, never
the joint `1/π_{ij}`; the bound therefore needs a joint-overlap lower bound on dependent pairs that
the literal Conditions 1/3/4 omit. We make this explicit as **Condition 1′** (`1/π_{ij}(d) ≤ c₃`)
and recover the paper's claim under it. See `doc/aronow_samii_varest_consistency_plan.md`.

The Chen–Shao dependency-graph CLT is built from scratch in
`Causalean/Mathlib/Probability/SteinMethod/{Solution,Bounds,DependencyCLT,CLT}.lean` (Mathlib has no
Stein's method or Wasserstein metric): the Stein equation `steinSol_hasDerivAt`, the three
Chen–Goldstein–Shao solution bounds `‖f_h‖,‖f_h'‖,‖f_h''‖≤2L` (incl. a from-scratch Mills-ratio
bound), the local-dependence Stein bound `stein_local_dependence_bound`, and the abstract CLT
`stein_cdf_clt` (via the `clt` package's Lévy continuity + portmanteau). Reusable / promotable.

### `Experimentation/DesignBased/{Product,TwoStage}.lean` — product & compound (two-stage) designs (substrate)

The reusable two-stage substrate that Hudgens–Halloran (paper #2) forced. Paper-agnostic.

| Declaration | Statement | Description |
|---|---|---|
| `prodDesign` | `(prodDesign D).p w = ∏ i, (D i).p (w i)` | Product of a finite family of designs `D i : FiniteDesign (α i)` on the dependent product `∀ i, α i`. Makes cross-coordinate (cross-group) independence a structural fact, not an assumption. |
| `FiniteDesign.E_prod_prod` / `E_prod_apply` | `E(∏ᵢ gᵢ(wᵢ))=∏ᵢ E gᵢ`; `E(g(wⱼ))=(D j).E g` | Expectation of a product of single-coordinate functions factors; a function of one coordinate has that coordinate's marginal expectation. **Proved.** |
| `compound` | `(compound D₁ D₂).p (s,w) = D₁.p s · ∏ᵢ (D₂ s i).p (wᵢ)` | Compound (two-stage) design: stage-1 design `D₁` on `Ω₁`, then conditionally on `s`, independent within-coordinate designs `D₂ s i` (which may depend on `s`). |
| `FiniteDesign.E_compound` / `E_compound_factor` | `E[h(s)·g(wⱼ)] = E_s[h(s)·(D₂ s j).E g]` | The stage-2 collapse engine: a stage-1 quantity times a single-group within-function reduces the inner randomization to that group's conditional marginal. **Proved.** |
| `FiniteDesign.E_compound_tower` | `(compound D₁ D₂).E F = E_s[(prodDesign (D₂ s)).E (w ↦ F(s,w))]` | Tower property: the joint expectation is the stage-1 expectation of the stage-2 conditional expectation (`CompoundVariance.lean`). **Proved.** |
| `FiniteDesign.Var_compound_eq_tower` | `Var X = E_s[Var_{w∣s} X] + Var_s(E_{w∣s} X)` | **Law of total variance** for the two-stage design: total variance = expected within-stage variance + variance of the stage-2 conditional mean. The gateway to the between-group / within-group variance decomposition (HH Thm 4/6). **Proved, axiom-clean.** |
| `FiniteDesign.E_prod_apply₂` / `Cov_prod_apply_of_ne` / `Var_prod_apply` (`ProductVariance.lean`) | `E[g(wᵢ)h(wⱼ)]=Eg·Eh` (`i≠j`); `Cov=0` (`i≠j`); `Var(g(wⱼ))=(D j).Var g` | Cross-coordinate independence of the product design: functions of distinct coordinates are uncorrelated. **Proved.** |
| `FiniteDesign.Var_prod_linear_comb` | `(prodDesign D).Var (∑ᵢ cᵢ gᵢ(wᵢ)) = ∑ᵢ cᵢ²·(D i).Var gᵢ` | Variance of a linear combination of single-coordinate functions = sum of coordinate variances (the within-group variance term). **Proved, axiom-clean.** |

### `Experimentation/TwoStageInterference/{Basic,Stratified,Unbiased,Effects}.lean` — two-stage estimands, stratified interference, unbiasedness

Hudgens & Halloran (2008). Groups `ι`, group `i` of size `n i`; within-group assignment
`WAssign n i = Fin (n i) → Bool`; two-stage `jointDesign D₁ ψ φ = compound D₁ (fun s i => if s i then ψ i else φ i)`.
Partial interference is structural (the product over groups); the only modeling restriction is
*within*-group stratified interference, needed only for the variance theory (Phase 1 unbiasedness
holds without it). Stage-1 and within-group design propensities (`C/N`, `mᵢ/nᵢ`) enter as
hypotheses — the design-based known-propensity stance; Assumption 1's mixed strategy satisfies them.
Plan: `doc/hudgens_halloran_plan.md`. Namespace `Causalean.Experimentation.TwoStageInterference`.

| Declaration | Statement | Description |
|---|---|---|
| `WAssign` / `StratAssign` / `jointDesign` | `Fin (n i)→Bool`; `ι→Bool`; `compound D₁ (…)` | Within-group and stage-1 assignment spaces and the joint two-stage design. |
| `indMean` / `groupMean` / `popMean` / `indMarg` / `popMarg` | `ȳ_ij(z;ρ)` conditional mean; group/population averages; marginal versions | The average-potential-outcome estimands (conditional on own treatment `z`, and the marginal forms). |
| `CE_direct` / `CE_indirect` / `CE_total` / `CE_overall` | `ȳ(0;ψ)−ȳ(1;ψ)`; `ȳ(0;φ)−ȳ(0;ψ)`; `ȳ(0;φ)−ȳ(1;ψ)`; `ȳ(φ)−ȳ(ψ)` | The four population causal-effect contrasts (direct, indirect/spillover, total, overall). |
| `groupEst` / `popEst` / `estDirect` / `estIndirect` / `estTotal` | within-group Hájek mean; population average over the selected groups; the effect contrasts | The estimators (fixed-count Hájek form, hence linear in the treatment indicators). |
| `numTreatedOthers` / `stratExpo` / `StratifiedInterference` / `exists_strat_factor` | count of other treated; `(wⱼ, #others)`; Assumption 2; factorization `Y i j w = g (stratExpo i j w)` | Stratified interference (`Stratified.lean`): outcomes depend on the assignment only through own treatment and the count of other treated units; hence factor through the exposure. **Proved.** |
| `E_groupEst` | `E[Ŷ_i(z;ρ)] = ȳ_i(z;ρ)` | Within-group unbiasedness under constant treatment propensity `mᵢ/nᵢ`. **Proved.** |
| `E_popEst` / `E_popEst_pick` | `E[Ŷ(z;ψ)] = ȳ(z;ψ)` and the strategy-agnostic generalization | **Theorem 1**: population unbiasedness, via the stage-2 collapse engine; `E_popEst_pick` covers either allocation strategy. **Proved.** |
| `E_estDirect` / `E_estIndirect` / `E_estTotal` | `E[effect estimator] = CE_{D,I,T}` | **Theorems 1–3**: the direct/indirect/total effect estimators are exactly unbiased for their estimands. **Proved.** |
| `CE_total_decomp` | `CE^T = CE^D + CE^I` | The total effect decomposes into the direct plus indirect effects. **Proved.** |

### `Experimentation/TwoStageInterference/{Variance,VarianceMoments,VarianceConservative}.lean` — Theorem 5 (within-group Neyman variance) + conservative estimator (conservativeness proved)

Hudgens & Halloran (2008, JASA), "Toward Causal Inference With Interference". Within one group under
a completely randomized experiment that always treats exactly `K` of `n` units, and under stratified
interference (Assumption 2), each unit's outcome on the design support is two-valued — `a j` when
treated, `b j` when in control — so the difference-in-means estimator is linear in the treatment
indicators and its randomization variance is the classical Neyman completely-randomized variance.
The two-valued outcomes `a, b` are taken as data; the link to the stratified factorization
(`exists_strat_factor`) is upstream. The completely-randomized first/second-order propensities enter
as hypotheses `hmean` (`E[Tⱼ]=K/n`) and `hpair` (`E[TⱼTₖ]=K(K−1)/(n(n−1))`, `j≠k`). Namespace
`Causalean.Experimentation.TwoStageInterference`.

| Declaration | Statement | Description |
|---|---|---|
| `T` | `T j = ind (fun w => w j = true)` | Treatment indicator of unit `j` on the assignment space `Fin n → Bool` (design-independent). |
| `tauHat` | `(∑ⱼ bⱼ(1−Tⱼ))/(n−K) − (∑ⱼ aⱼTⱼ)/K` | Difference-in-means estimator `ȳ(0)−ȳ(1)`: control mean minus treated mean as a function of the assignment. |
| `popMeanV` / `S1` / `S0` / `Stau` | `x̄=(∑x)/n`; `S₁=∑(aⱼ−ā)²/(n−1)`; `S₀=∑(bⱼ−b̄)²/(n−1)`; `Sτ=∑((aⱼ−bⱼ)−(ā−b̄))²/(n−1)` | Population sample variances (Neyman, `n−1` denominator) of treated-state, control-state outcomes and unit effects. |
| `FiniteDesign.Var_add_const` | `Var(X+c)=Var X` | Variance is shift-invariant (substrate helper). |
| `sum_sum_ite_quadratic` | `∑ⱼ∑ₖ cⱼcₖ(if j=k then vd else vo) = vo(∑c)²+(vd−vo)∑c²` | The double-sum-to-split-form algebraic core turning `Var_linear_comb` into the Neyman split. |
| `sum_sub_mean_sq` | `∑ⱼ(xⱼ−x̄)² = ∑ⱼxⱼ² − (∑x)²/m` | Sum of squared deviations equals raw second moment minus squared first moment over `m`. |
| `cov_diag` / `cov_offdiag` | `Cov(Tⱼ,Tⱼ)=(K/n)(1−K/n)`; `Cov(Tⱼ,Tₖ)=K(K−1)/(n(n−1))−(K/n)²` (`j≠k`) | Complete-randomization indicator covariances from `hmean`/`hpair`. **Proved.** |
| `Var_tauHat` | `ρ.Var(tauHat) = S₁/K + S₀/(n−K) − Sτ/n` | **Theorem 5 (Neyman form).** Derived (not assumed) from the indicator covariances via the linear-comb double sum and moment algebra. **Proved, sorry-free.** |
| `obsMeanTreated` / `obsMeanControl` | `(∑ⱼTⱼaⱼ)/K`; `(∑ⱼ(1−Tⱼ)bⱼ)/(n−K)` | Empirical means among observed treated / control units (`VarianceConservative.lean`). |
| `ShatTreated` / `ShatControl` | `∑ⱼTⱼ(aⱼ−ā_obs)²/(K−1)`; `∑ⱼ(1−Tⱼ)(bⱼ−b̄_obs)²/(n−K−1)` | Observed sample variances among treated / control units. |
| `varHat` | `Ŝ₁/K + Ŝ₀/(n−K)` | Conservative variance estimator (empirical analogue of the first two Neyman terms), computable from one realized assignment. |
| `varHat_nonneg` | `0 ≤ varHat` (for `1≤K`, `K+1≤n`) | Pointwise nonnegativity: sum of two nonnegative sample variances over positive counts. **Proved.** |
| `E_congr_supp` / `E_Shat` (`VarianceMoments.lean`) | support-congruence for `E`; `E[(1/(M−1))∑ Uⱼ(xⱼ−x̄_U)²] = S_x` | Generic unbiasedness of the observed sample variance under a `{0,1}` selection family `U` with the CRE moments (first `M/n`, pairwise `M(M−1)/(n(n−1))`, deterministic support total `M`). **Proved, axiom-clean.** |
| `E_ShatTreated` / `E_ShatControl` | `ρ.E Ŝ₁ = S₁`; `ρ.E Ŝ₀ = S₀` | The observed treated/control sample variances are unbiased for the population variances under sampling without replacement (`E_Shat` with `U=T`,`M=K` and `U=1−T`,`M=n−K`; control moments derived from `hmean`/`hpair`/`hsupp`). **Proved.** |
| `E_varHat_conservative` | `ρ.Var(tauHat) ≤ ρ.E(varHat)` | **Conservativeness (HH Eq. 9).** `E[v̂ar] = S₁/K + S₀/(n−K) = Var + Sτ/n ≥ Var`. **Proved, axiom-clean.** Hypotheses: CRE moments `hmean`/`hpair`, deterministic treated count on the support `hsupp` (`∑ⱼTⱼ=K`), and non-degeneracy `2≤K`, `K+2≤n` (so both sample variances are well-defined). |

### `Experimentation/TwoStageInterference/{StageOne,BetweenGroup}.lean` — Theorem 4 (two-stage variance decomposition)

The between-group / within-group variance decomposition of the population estimator `Ŷ(z;ψ)` under
the two-stage mixed-strategy design, assembled from the law of total variance
(`Var_compound_eq_tower`), the product-design within-group variance (`Var_prod_linear_comb`), and the
stage-1 SRS sample-mean variance. Namespace `Causalean.Experimentation.TwoStageInterference`.

| Declaration | Statement | Description |
|---|---|---|
| `SmuVar` / `Var_srs_mean` (`StageOne.lean`) | `SmuVar μ = ∑(μᵢ−μ̄)²/(N−1)`; `D₁.Var((1/m)∑ᵢ Uᵢμᵢ) = (1−m/N)/m · SmuVar μ` | The variance of a simple-random-sample mean of `m` of `N` fixed numbers is the finite-population correction `(1−m/N)/m` times their population variance — the between-group variance term. Given the SRS selection moments (first `m/N`, pairwise `m(m−1)/(N(N−1))`, diagonal `(m/N)(1−m/N)`). **Proved, derived constant.** |
| `Var_groupAgg` (`BetweenGroup.lean`) | `Var((∑ᵢ 1(Sᵢ=ψ)·gᵢ(wᵢ))/C) = (1−C/N)/C·SmuVar((ψ·).E g·) + (1/(CN))∑ᵢ (ψ i).Var gᵢ` | The abstract two-stage variance decomposition for an arbitrary per-group statistic `g`; Theorems 4 and 6 are corollaries (instantiating `g` as a single-treatment mean / a per-group effect difference). Assembled via the law of total variance. **Proved, axiom-clean.** |
| `Var_popEst` (`BetweenGroup.lean`) | `Var(Ŷ(z;ψ)) = (1−C/N)/C · σ²_D + (1/(CN))∑ᵢ Var(Ŷ_i(z;ψ)∣Sᵢ=ψ)` | **Theorem 4**: the two-stage randomization variance of the population mean estimator splits into a between-group SRS term (finite-population correction `(1−C/N)/C` on the group-level means `ȳ_i(z;ψ)`) plus a within-group term averaging the per-group conditional variances. A corollary of `Var_groupAgg`. Hypotheses: within propensities `mᵢ/nᵢ`, stage-1 marginal `C/N` and pair `C(C−1)/(N(N−1))`. **Proved, axiom-clean.** |
| `Var_estDirect` (`BetweenGroupEffect.lean`) | `Var(ĈE^D(ψ)) = (1−C/N)/C · σ²_D + (1/(CN))∑ᵢ Var(ĈE^D_i(ψ)∣Sᵢ=ψ)` | **Theorem 6**: the two-stage variance of the direct-*effect* estimator `Ŷ(0;ψ)−Ŷ(1;ψ)` — between-group SRS term over the group-level direct effects `ȳ_i(0;ψ)−ȳ_i(1;ψ)` plus a within-group term averaging the per-group direct-effect-estimator variances. Corollary of `Var_groupAgg` with `gᵢ` the per-group direct-effect estimator (`Ŷ(0;ψ)−Ŷ(1;ψ) = (∑ᵢ 1(Sᵢ=ψ)·dᵢ(wᵢ))/C`). **Proved, axiom-clean.** |

### `Experimentation/TwoStageInterference/Asymptotic/{Setup,Consistency,CLT,CLTDischarge,CLTDischargeMain,Wald}.lean` — Liu–Hudgens (2014) large-sample inference (minimal)

Liu & Hudgens (2014, JASA), "Large-Sample Randomization Inference of Causal Effects in the Presence of
Interference" — the asymptotic theory for the two-stage design, minimal direct-effect slice. Regime:
many groups (`m → ∞`), exact permutation first stage, **homogeneity**. The Prop 5.1 CLT is proved the
paper's own way — condition on the selection (groups are then independent), conditional CLT, lift to
unconditional via homogeneity — so **no finite-population (Hájek) CLT is needed**. The conditional CLT
is now discharged from primitives (the independent-summands CLT `prodDesign_clt`), so Prop 5.1 rests
on explicit homogeneity + regularity, not a CLT black box. Plan: `doc/liu_hudgens_plan.md`. Namespace
`Causalean.Experimentation.TwoStageInterference`.

| Declaration | Statement | Description |
|---|---|---|
| `LHExperiment` / `E_estD` / `var_estD` (`Setup.lean`) | the sequence-of-experiments bundle; `jointD.E estD = DEbar`; `jointD.Var estD = directVar` | The bundle carrying the two-stage design + its known propensities; the unbiasedness and Theorem-6 variance bridges (from `E_estDirect` / `Var_estDirect`). |
| `estDirect_consistent` (`Consistency.lean`) | `Var → 0 ⟹ D̂E(α₁) − DE̅ →ₚ 0` | Consistency of the direct-effect estimator via Chebyshev + the vanishing design variance. **Proved, axiom-clean.** |
| `tendsto_E_of_uniformBound` / `Pr_compound_eq_E_condPr` (`CLT.lean`) | mixture lifting (`E F_n → L` from uniform bounds); `(compound D₁ D₂).Pr P = D₁.E (conditional Pr)` | Reusable substrate: the unconditional law is the design-average of the conditional laws (from `E_compound_tower`), and uniform conditional convergence lifts through the average. **Proved.** |
| `directEffect_clt` (`CLT.lean`) | studentized `(D̂E − DE̅)/√Var →_d N(0,1)` | **Proposition 5.1** (direct effect), modulo the uniform conditional-CLT regularity `hcond` (the paper's Lindeberg + homogeneity). Proved by the tower bridge + mixture lifting. The `hcond`-free primitive form is `directEffect_clt_homogeneous` below. **Proved, axiom-clean.** |
| `tendsto_E_of_uniformBound_ae` / `Var_sub_const` (`CLTDischarge.lean`) | support-restricted mixture lifting (`E F_n → L` from bounds only where `D₁.p s ≠ 0`); `Var(X−c) = Var X` | The "a.e." variant of `tendsto_E_of_uniformBound`: the stage-1 average only weights support points, so a uniform bound on the support suffices to lift the conditional limit — needed because homogeneity (`hhom`) only relates conditional CDFs across the support. Plus the constant-shift variance invariance. **Proved.** |
| `Homogeneous` / `DEbar_eq_of_homogeneous` / `directVar_eq_of_homogeneous` / `stud_eq_sum_of_homogeneous` (`CLTDischarge.lean`) | the homogeneity + regularity bundle; `DEbar = δ`; `directVar = v/C`; `stud(s,w) = ∑ᵢ gₛ,ᵢ(wᵢ)` on the support | The faithful encoding of Prop 5.1's hypotheses (constant group direct effects `δ`, constant within-group variance `v n > 0`, bounded centered per-group estimator `M`, exactly-`C` selections on the support, the `B → 0` / `card·B³ → 0` rates, and selection-independence `hhom`), with the reductions that turn the conditional studentized statistic into a normalized independent sum. **Proved.** |
| `condCLT_ref` (`CLTDischargeMain.lean`) | conditional CDF at the reference selection `→ Φ(t)` | The conditional CLT, obtained by feeding the homogeneity-reduced per-coordinate summands `gₛ₀,ᵢ` (mean-zero, unit total variance via `Var_prod_linear_comb`, uniformly bounded) into the independent-summands CLT `prodDesign_clt`. **Proved, axiom-clean.** |
| `directEffect_clt_homogeneous` (`CLTDischargeMain.lean`) | studentized `(D̂E − DE̅)/√Var →_d N(0,1)` | **Proposition 5.1, fully primitive.** Identical conclusion to `directEffect_clt` but `hcond`-free: it rests on the explicit `Homogeneous` bundle. The conditional CLT is `condCLT_ref` (from `prodDesign_clt`); `hhom` lifts the uniform bound across the stage-1 support; `tendsto_E_of_uniformBound_ae` averages it. **Proved, axiom-clean.** |
| `prodDesign_Pr_reindex` (`DesignBased/ProductReindex.lean`) / `hhom_of_identical` / `directEffect_clt_identical` (`Asymptotic/Identical.lean`) | `(prodDesign D).Pr(P∘(·∘σ)) = (prodDesign (D∘σ)).Pr P`; `hhom` for identical groups; the CLT under identical groups | **Prop 5.1 on literal identical groups** — `hhom` is *derived*, not assumed. The reindex lemma (a pure finite-sum permutation invariance) shows the conditional CDF is selection-independent when all groups are identical (a permutation `σ` matching two equal-size selections, built via `Equiv.sumCompl` + `Fintype.equivOfCardEq`, relabels one conditional law to the other). `directEffect_clt_identical` then rests only on identical groups + bounded outcomes + the many-groups rate. **Proved, axiom-clean.** |
| `wald_coverage_oracle` (`Wald.lean`) | `1 − γ ≤ liminf Pr(\|D̂E − DE̅\| ≤ z·√Var)` | Asymptotic coverage of the oracle Wald interval `D̂E ± z·√Var`, from the CLT + standard-normal CDF symmetry (transplant of the A-S oracle `wald_coverage`). **Proved, axiom-clean.** |
| `wald_coverage_feasible` (`WaldFeasible.lean`) | `1 − γ ≤ liminf Pr(\|D̂E − DE̅\| ≤ z·√V̂)` | Asymptotic coverage of the **feasible** Wald interval `D̂E ± z·√V̂` using an *estimated* variance, from the per-threshold CLT + a single conservative-consistency input `hVhat` (`Pr(V̂ < (1−ε)·Var) → 0`), with the estimator `Vh` abstract — transplant of the A-S feasible `wald_coverage_feasible`. Discharging `hVhat` to a concrete two-stage estimator (the A-S `_of_conditions` analogue) is future work. **Proved, axiom-clean.** |

### `UnknownInterference/` — Sävje–Aronow–Hudgens (2021), EATE under unknown interference (arXiv:1711.06399)

Bernoulli-design core of "Average treatment effects in the presence of unknown interference." Units `U`, treatment vector `z : U → Bool`, potential outcomes `y i z` depending on the *whole* assignment (interference), of unknown form. Scope: Bernoulli design with the Horvitz–Thompson **and Hájek** estimators (the paper's conceptual heart). Deferred (see `doc/savje_aronow_hudgens_plan.md`): complete/paired/arbitrary designs (α-mixing), the variance-estimator results, external validity. No CLT — the paper proves Chebyshev is sharp and Gaussian approximations generally fail under these conditions.

| Declaration (file) | Statement | Description |
|---|---|---|
| `Interferes` / `InterfDep` / `dbar` (`Basic.lean`) | interference indicator `I ℓ i`; interference dependence (some `ℓ` interferes with both `i`,`j`); average dependence `d̄ = n⁻¹∑ᵢ∑ⱼ 1[InterfDep i j]` | The interference structure; `d̄` is the basis for "restricted interference" `d̄ = o(n)`. **Defs** |
| `tau` / `ACATE` / `EATE` / `htEst` (`Basic.lean`) | `τ_i(z₋ᵢ)=y_i(1;z₋ᵢ)−y_i(0;z₋ᵢ)`; `ACATE(z)=n⁻¹∑ᵢτ_i`; `EATE = E[ACATE(Z)]`; the Horvitz–Thompson estimator | The estimand (assignment-conditional ATE marginalized over the design; generalizes ATE) + the HT estimator. **Defs** |
| `y_eq_of_agree_on_interferers` (`Basic.lean`) | if `z`,`z'` agree on every unit interfering with `i` then `y i z = y i z'` | A unit's outcome depends only on its interferers — the bridge to disjoint-block independence. **Proved, axiom-clean.** |
| `bernoulliDesign` (`Bernoulli.lean`) | `prodDesign` of per-unit coin flips with `Pr(Z_i=1)=p_i` | The Bernoulli design; cross-unit independence is structural. **Def + marginal lemmas** |
| `htEst_unbiased` (`Unbiased.lean`) | `E[ĤT] = EATE` under the Bernoulli design | HT is **exactly unbiased** for EATE (`Z_i ⊥ Z₋ᵢ` ⇒ `E[Z_iY_i/p_i]=E[y_i(1;Z₋ᵢ)]`). **Proved, axiom-clean.** |
| `var_htEst_le` (`VarianceBound.lean`) | `Var(ĤT) ≤ k⁴·d̄/n` | The quantitative heart (paper's body bound): covariances vanish off the interference-dependence graph (disjoint-block independence `Cov_prod_disjoint_zero`), surviving ones bounded by `k⁴`, counted by `dbarCount = n·d̄`. **Proved, axiom-clean.** |
| `htEst_consistent_eate` (`Consistency.lean`) | along a sequence with `k⁴·d̄/n → 0`, `Pr(\|ĤT − EATE\| ≥ ε) → 0` | **Flagship (Prop, Bernoulli rate).** HT is consistent for EATE under restricted interference, no structural knowledge of the interference — Chebyshev on the variance bound. The paper's `O_p(√(d̄/n))` rate is exactly this Chebyshev reading. **Proved, axiom-clean.** |
| `root_n_var` (`Consistency.lean`) | `d̄ ≤ C ⇒ n·Var(ĤT) ≤ k⁴·C` | **Root-n consistency (Corollary)** under bounded interference: the variance is `O(1/n)`. **Proved, axiom-clean.** |
| `hajekEst` / `hajek_consistent_eate` (`Hajek.lean`) | the Hájek (ratio/IPW) estimator `Â₁/B̂₁ − Â₀/B̂₀`; along a sequence with `k⁴·d̄/n → 0`, uniformly bounded `k`, and bounded potential-outcome moments, `ĤA →ₚ EATE` | **Hájek consistency.** Each numerator `Âₜ →ₚ ȳ(t)` (vanishing variance) and each realized-weight normalizer `B̂ₜ →ₚ 1` (`E[B̂ₜ]=1`); the Slutsky ratio step gives `ĤA →ₚ ȳ(1)−ȳ(0) = EATE`. Adds the uniform-`k` bound and Assumption-C potential-outcome moments beyond the HT hypotheses (disclosed). **Proved, axiom-clean.** |
| `VhatBer` / `E_VhatBer_bias` (`Confidence.lean`) | conventional variance estimator `V̂_Ber = n⁻²∑ĤTᵢ²`; `E[V̂_Ber] − Var(ĤT) = n⁻²(∑(E ĤTᵢ)² − ∑_{i≠j}Cov(ĤTᵢ,ĤTⱼ))` | **Anti-conservativeness mechanism** (the warning): the off-diagonal covariances (nonzero only on the interference graph, either sign) are the bias making `V̂_Ber` understate uncertainty. **Proved, axiom-clean.** |
| `var_htEst_le_inflated` (`Confidence.lean`) | `(∀ i, degDep i ≤ D) ⇒ Var(ĤT) ≤ (1+D)·E[V̂_Ber]` | **Conservative inflation** (the fix, in expectation): inflating by an interference-degree bound restores conservativeness — the cleaner cousin of the paper's `d_max`/spectral-radius estimators. **Proved, axiom-clean.** |
| `chebyshev_ci_eate` / `eate_ci_kbound` (`Confidence.lean`) | `Var(ĤT) ≤ V ⇒ Pr(\|ĤT − EATE\| ≤ √(V/α)) ≥ 1−α`; concretely the interval `ĤT ± √(k⁴·d̄/(n·α))` covers EATE `≥ 1−α` | **Finite-sample confidence interval for EATE** under unknown interference, Chebyshev-based (the paper proves a CLT fails). Exact, from HT unbiasedness + the proven variance bound. **Proved, axiom-clean.** |
| `FiniteDesign.E_prod_block_mul` / `Cov_prod_disjoint_zero` (`DesignBased/ProductBlock.lean`) | functions of disjoint coordinate blocks of a product design factor in expectation / are uncorrelated | Substrate (block generalization of `E_prod_apply₂`/`Cov_prod_apply_of_ne`), proved via the existing product-measure independence bridge; requires `MeasurableSpace`/`MeasurableSingletonClass` on the coordinate types. **Proved, axiom-clean.** |
| `FiniteDesign.TendstoInProb` / `tendstoInProb_of_var` / `tendstoInProb_div_one` / `.sub` / `.add` / `.abs` / `.const_mul` · `BoundedInProb` / `boundedInProb_of_var_bound` / `TendstoInProb.mul_boundedInProb` (`DesignBased/InProb.lean`) | convergence in probability for finite designs; the Chebyshev consistency engine; the Slutsky ratio step (`X→ₚa`, `Y→ₚ1`, `|a|≤M` ⇒ `X/Y→ₚa`); closure under sums/differences/absolute value/scaling; uniform tightness (`BoundedInProb`) with a variance-bound criterion and the product-tightness engine `o_p × O_p = o_p` | Reusable in-probability substrate for ratio estimators (Hájek); `mul_boundedInProb` is the delta-method-remainder engine behind the bipartite denominator-tightness discharge. **Proved, axiom-clean.** |

## 10z. Reusable minimax and nonparametric reduction substrate

These modules package the experiment-level tools used by sharp lower-bound
arguments: maximal couplings and coordinatewise overlap, marked-Poisson
depoissonization, outer integration, countable empirical-process reductions,
Bernoulli common-statistic kernels, local-polynomial coercivity, and planar
polar-integration identities.  They are independent of any particular paper
construction and can be imported directly from the Causalean hierarchy.

<!-- GEN:Causalean.Stat.Minimax.MaximalCoupling -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.measurableEqOfStandardBorel` | `MeasurableEq X` | For a standard Borel measurable space, the measurability of its equality relation is defined. |
| `Stat.tvDist_eq_half_integral_abs_rnDeriv_sub` | `mu.AbsolutelyContinuous xi → nu.AbsolutelyContinuous xi → Causalean.Stat.tvDist mu nu = 1 / 2 * ∫ (x : X), \|(mu.rnDeriv xi x).toReal - (nu.rnDeriv xi x).toReal\| ∂xi` | Scheffé's identity with both probability laws dominated by an arbitrary finite reference measure. |
| `Stat.rnCommonPart` | `MeasureTheory.Measure X → MeasureTheory.Measure X → MeasureTheory.Measure X → MeasureTheory.Measure X` | Given a measurable sample space and three measures on it, consisting of two target measures and a reference measure, the common Radon--Nikodym submeasure is the reference measure weighted by the pointwise minimum of the two target measures' Radon--Nikodym densities relative to that reference measure. |
| `Stat.rnCommonPart_le_left` | `mu.AbsolutelyContinuous xi → Causalean.Stat.rnCommonPart mu nu xi ≤ mu` | The RN common part is dominated by its first law. |
| `Stat.rnCommonPart_le_right` | `nu.AbsolutelyContinuous xi → Causalean.Stat.rnCommonPart mu nu xi ≤ nu` | The RN common part is dominated by its second law. |
| `Stat.rnCommonPart_mass_eq_one_sub_tvDist` | `mu.AbsolutelyContinuous xi → nu.AbsolutelyContinuous xi → (Causalean.Stat.rnCommonPart mu nu xi) Set.univ = ENNReal.ofReal (1 - Causalean.Stat.tvDist mu nu)` | The mass of the RN common part is exactly one minus total variation. |
| `Stat.measure_eq_of_tvDist_eq_zero` | `Causalean.Stat.tvDist mu nu = 0 → mu = nu` | Probability measures at total-variation distance zero are equal. |
| `Stat.maximalCoupling` | `(mu nu : MeasureTheory.Measure X) → [MeasureTheory.IsProbabilityMeasure mu] → [MeasureTheory.IsProbabilityMeasure nu] → MeasureTheory.Measure (X × X)` | Given a measurable sample space and two probability measures on it, the maximal coupling is the measure on pairs whose first branch is the diagonal coupling when their total-variation distance is zero, and whose second branch otherwise combines their common part on the diagonal with the normalized product of their residual measures. |
| `Stat.maximalCoupling_map_fst` | `MeasureTheory.Measure.map Prod.fst (Causalean.Stat.maximalCoupling mu nu) = mu` | The first marginal of the maximal coupling is the first law. |
| `Stat.maximalCoupling_map_snd` | `MeasureTheory.Measure.map Prod.snd (Causalean.Stat.maximalCoupling mu nu) = nu` | The second marginal of the maximal coupling is the second law. |
| `Stat.maximalCoupling_eq_mass_ge` | `ENNReal.ofReal (1 - Causalean.Stat.tvDist mu nu) ≤ (Causalean.Stat.maximalCoupling mu nu) {p \| p.1 = p.2}` | For two probability measures `mu` and `nu` on a standard Borel space `X`, the two coordinates of their maximal coupling agree with probability at least one minus their total variation distance. |
| `Stat.compressionCoupling` | `(Q0 Q1 : MeasureTheory.Measure Z) → [MeasureTheory.IsProbabilityMeasure Q0] → [MeasureTheory.IsProbabilityMeasure Q1] → (compress : Z → S) → Measurable compress → MeasureTheory.Measure (Z × Z)` | Given two standard Borel measurable spaces, an observation space and a compressed-state space, two probability measures on the observation space, and a measurable compression map from observations to compressed states, the compression coupling is the joint law obtained by maximally coupling the two compressed laws and then, conditional on each coupled compressed state, drawing each observation from its corresponding regular conditional distribution. |
| `Stat.compressionCoupling_map_fst` | `MeasureTheory.Measure.map Prod.fst (Causalean.Stat.compressionCoupling Q0 Q1 compress hcompress) = Q0` | The first marginal of the lifted compression coupling is the first raw law. |
| `Stat.compressionCoupling_map_snd` | `MeasureTheory.Measure.map Prod.snd (Causalean.Stat.compressionCoupling Q0 Q1 compress hcompress) = Q1` | The second marginal of the lifted compression coupling is the second raw law. |
| `Stat.compressionCoupling_map_compress_pair` | `MeasureTheory.Measure.map (Prod.map compress compress) (Causalean.Stat.compressionCoupling Q0 Q1 compress hcompress) = Causalean.Stat.maximalCoupling (MeasureTheory.Measure.map compress Q0) (MeasureTheory.Measure.map compress Q1)` | Compressing both coordinates of the lifted coupling recovers the maximal coupling of the compressed laws. |
| `Stat.compressionCoupling_equal_compression_mass_ge` | `ENNReal.ofReal (1 - Causalean.Stat.tvDist (MeasureTheory.Measure.map compress Q0) (MeasureTheory.Measure.map compress Q1)) ≤ (Causalean.Stat.compressionCoupling Q0 Q1 compress hcompress) {p \| compress p.1 = compress p.2}` | In the lifted coupling, the compressed observations agree with probability at least one minus the total variation of their compressed laws. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Minimax.CoordinatewiseOverlap -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.compressedCoordinateLaw` | `(Z → S) → MeasureTheory.Measure Z → MeasureTheory.Measure S` | For a compression from a measurable observation space to a measurable summary space and a measure on the observation space, the compressed-coordinate law is the image measure of the summary obtained by applying the compression to an observation governed by that measure. |
| `Stat.compressedCoordinateLaw_klDiv_le` | `Measurable compress → ∀ (μ ν : MeasureTheory.Measure Z) [MeasureTheory.IsFiniteMeasure μ] [MeasureTheory.IsFiniteMeasure ν], InformationTheory.klDiv (Causalean.Stat.compressedCoordinateLaw compress μ) (Causalean.Stat.compressedCoordinateLaw compress ν) ≤ InformationTheory.klDiv μ ν` | Measurable finite-coordinate compression cannot increase KL divergence. |
| `Stat.coordinatewiseSuccessProbability` | `(Q : (j : Fin M) → Bool → MeasureTheory.Measure (Z j)) → (R : MeasureTheory.Measure A) → [∀ (j : Fin M) (b : Bool), MeasureTheory.IsProbabilityMeasure (Q j b)] → [MeasureTheory.IsProbabilityMeasure R] → ((j : Fin M) → Z j → S j) → ((j : Fin M) → S j → ((k : Fin M) → Z k) → A → Bool) → ENNReal` | For a nonnegative number of coordinates, measurable raw-observation and summary spaces at every coordinate, a measurable ancillary space, two probability laws for each coordinate, indexed by its binary state, a probability law for a common ancillary variable, one compression for each coordinate, and a decoder for each coordinate that uses its compressed observation, the full raw observation vector, and the ancillary variable, the coordinatewise success probability is the average, over all binary state vectors, of the probability that every decoder recovers its corresponding state under the associated independent product experiment. |
| `Stat.coordinateOverlap` | `((j : Fin M) → Bool → MeasureTheory.Measure (Z j)) → ((j : Fin M) → Z j → S j) → Fin M → ℝ` | For two probability laws at each coordinate, indexed by its binary state, a compression at each coordinate, and one coordinate, the common-part overlap at that coordinate is one minus the total-variation distance between the two compressed laws of that coordinate. |
| `Stat.selectCoupledRaw` | `(Fin M → Bool) → ((j : Fin M) → Z j × Z j) → (j : Fin M) → Z j` | For a binary hypercube vertex and a pair of raw observations at every coordinate, the selected raw observation vector takes the first member of each pair when the corresponding vertex bit is false and the second member when it is true. |
| `Stat.coupledDecoderGood` | `((j : Fin M) → Z j → S j) → ((j : Fin M) → S j → ((k : Fin M) → Z k) → A → Bool) → (Fin M → Bool) → ((j : Fin M) → Z j × Z j) → A → Prop` | For a nonnegative number of coordinates, one compression for each coordinate, one decoder for each coordinate, a binary hypercube vertex, a pair of raw observations at every coordinate, and an ancillary-variable value, the coupled decoder-good condition holds exactly when every decoder, applied to its compressed selected observation together with the full selected raw vector and the ancillary value, returns its corresponding vertex bit. |
| `Stat.coupledGoodIndicator` | `((j : Fin M) → Z j → S j) → ((j : Fin M) → S j → ((k : Fin M) → Z k) → A → Bool) → (Fin M → Bool) → ((j : Fin M) → Z j × Z j) → A → ENNReal` | For a nonnegative number of coordinates, one compression for each coordinate, one decoder for each coordinate, a binary hypercube vertex, a pair of raw observations at every coordinate, and an ancillary-variable value, the coupled decoder-good condition holds exactly when every decoder, applied to its compressed selected observation together with the full selected raw vector and the ancillary value, returns its corresponding vertex bit. |
| `Stat.coupledDecoderGood_flip_exclusive` | `(∀ (j : Fin M) (s : S j) (z z' : (k : Fin M) → Z k) (a : A), (∀ (k : Fin M), k ≠ j → z k = z' k) → decoder j s z a = decoder j s z' a) → ∀ (omega : Fin M → Bool) (z : (j : Fin M) → Z j × Z j) (a : A) (j : Fin M), compress j (z j).1 = compress j (z j).2 → ¬(Causalean.Stat.coupledDecoderGood compress decoder omega z a ∧ Causalean.Stat.coupledDecoderGood compress decoder (Causalean.Stat.flipBit j omega) z a)` | If the two compressed versions agree at coordinate `j`, simultaneous correctness is impossible at both endpoints of the corresponding cube edge. |
| `Stat.coupledDecoderGood_count_le_half` | `(∀ (j : Fin M) (s : S j) (z z' : (k : Fin M) → Z k) (a : A), (∀ (k : Fin M), k ≠ j → z k = z' k) → decoder j s z a = decoder j s z' a) → ∀ (z : (j : Fin M) → Z j × Z j) (a : A) (j : Fin M), compress j (z j).1 = compress j (z j).2 → ∑ omega, Causalean.Stat.coupledGoodIndicator compress decoder omega z a ≤ 2 ^ M / 2` | Once one coupled coordinate has equal compressions, at most half of the hypercube vertices can be simultaneously decoded correctly. |
| `Stat.half_integral_abs_rnDeriv_sub_le_tvDist` | `μ.AbsolutelyContinuous ξ → ν.AbsolutelyContinuous ξ → 1 / 2 * ∫ (x : Ω), \|(μ.rnDeriv ξ x).toReal - (ν.rnDeriv ξ x).toReal\| ∂ξ ≤ Causalean.Stat.tvDist μ ν` | For two probability measures dominated by a common finite measure, half the `L¹` distance between their Radon--Nikodym densities is bounded by total variation. This is the reverse Scheffé inequality needed to construct the common submeasure in the maximal-coupling argument. |
| `Stat.overlap_ge_exp_neg_klBudget` | `0 ≤ B → InformationTheory.klDiv μ ν ≤ ENNReal.ofReal B → 1 / 2 * Real.exp (-B) ≤ 1 - Causalean.Stat.tvDist μ ν` | A nonnegative finite KL budget yields the corresponding Bretagnolle--Huber lower bound on testing overlap. |
| `Stat.prod_one_sub_le_exp_neg_sum` | `(∀ (j : Fin M), ρ j ≤ 1) → ∏ j, (1 - ρ j) ≤ Real.exp (-∑ j, ρ j)` | A product of complementary overlap probabilities is bounded by the exponential of minus their sum. |
| `Stat.prod_one_sub_le_exp_neg_card_mul` | `(∀ (j : Fin M), ρ j ≤ 1) → (∀ (j : Fin M), c ≤ ρ j) → ∏ j, (1 - ρ j) ≤ Real.exp (-↑M * c)` | A common coordinatewise overlap floor `c` bounds the complementary product by `exp (-M * c)`. |
| `Stat.tvDist_eq_zero_of_klBudget_nonpos` | `B ≤ 0 → InformationTheory.klDiv μ ν ≤ ENNReal.ofReal B → Causalean.Stat.tvDist μ ν = 0` | A nonpositive real KL budget forces two probability measures to coincide, and hence forces their total variation distance to vanish. |
| `Stat.card_mul_exp_neg_log_eq_rpow` | `1 ≤ M → ∀ (κ : ℝ), ↑M * (1 / 2 * Real.exp (-(κ * Real.log ↑M))) = ↑M ^ (1 - κ) / 2` | The exponential KL-overlap floor has the expected power-law scaling after multiplication by the number of coordinates. |
| `Stat.coordinateOverlap_product_le_of_nonnegative_kl` | `1 ≤ M → ∀ {Z : Fin M → Type u_1} {S : Fin M → Type u_2} [inst : (j : Fin M) → MeasurableSpace (Z j)] [inst_1 : (j : Fin M) → MeasurableSpace (S j)] (Q : (j : Fin M) → Bool → MeasureTheory.Measure (Z j)) [∀ (j : Fin M) (b : Bool), MeasureTheory.IsProbabilityMeasure (Q j b)] (compress : (j : Fin M) → Z j → S j), (∀ (j : Fin M), Measurable (compress j)) → ∀ {κ : ℝ}, 0 ≤ κ → (∀ (j : Fin M), InformationTheory.klDiv (Causalean.Stat.compressedCoordinateLaw (compress j) (Q j false)) (Causalean.Stat.compressedCoordinateLaw (compress j) (Q j true)) ≤ ENNReal.ofReal (κ * Real.log ↑M)) → ∏ j, (1 - Causalean.Stat.coordinateOverlap Q compress j) ≤ Real.exp (-↑M ^ (1 - κ) / 2)` | Under a nonnegative logarithmic KL budget, the product of coordinatewise total-variation factors has the finite-`M` exponential bound. |
| `Stat.ennreal_error_lower_bound_of_success_upper_bound` | `0 ≤ p → e ≤ 1 → p ≤ e → s ≤ ENNReal.ofReal (1 / 2 * (1 + p)) → ENNReal.ofReal (1 / 2 * (1 - e)) ≤ 1 - s` | A real-valued product bound below one converts an ENNReal simultaneous success upper bound into the complementary error lower bound. |
| `Stat.coordinatewise_overlap_direct_product` | `1 ≤ M → ∀ {Z : Fin M → Type u_1} {S : Fin M → Type u_2} {A : Type u_3} [inst : (j : Fin M) → MeasurableSpace (Z j)] [∀ (j : Fin M), StandardBorelSpace (Z j)] [inst_2 : (j : Fin M) → MeasurableSpace (S j)] [∀ (j : Fin M), StandardBorelSpace (S j)] [inst_4 : MeasurableSpace A] [StandardBorelSpace A] (Q : (j : Fin M) → Bool → MeasureTheory.Measure (Z j)) (R : MeasureTheory.Measure A) [inst_6 : ∀ (j : Fin M) (b : Bool), MeasureTheory.IsProbabilityMeasure (Q j b)] [inst_7 : MeasureTheory.IsProbabilityMeasure R] (compress : (j : Fin M) → Z j → S j), (∀ (j : Fin M), Measurable (compress j)) → ∀ (decoder : (j : Fin M) → S j → ((k : Fin M) → Z k) → A → Bool), (∀ (j : Fin M), Measurable fun p => decoder j p.1 p.2.1 p.2.2) → (∀ (j : Fin M) (s : S j) (z z' : (k : Fin M) → Z k) (a : A), (∀ (k : Fin M), k ≠ j → z k = z' k) → decoder j s z a = decoder j s z' a) → Causalean.Stat.coordinatewiseSuccessProbability Q R compress decoder ≤ ENNReal.ofReal (1 / 2 * (1 + ∏ j, (1 - Causalean.Stat.coordinateOverlap Q compress j))) ∧ ∀ κ < 1, (∀ (j : Fin M), InformationTheory.klDiv (Causalean.Stat.compressedCoordinateLaw (compress j) (Q j false)) (Causalean.Stat.compressedCoordinateLaw (compress j) (Q j true)) ≤ ENNReal.ofReal (κ * Real.log ↑M)) → ENNReal.ofReal (1 / 2 * (1 - Real.exp (-↑M ^ (1 - κ) / 2))) ≤ 1 - Causalean.Stat.coordinatewiseSuccessProbability Q R compress decoder` | Coordinatewise-overlap direct-product bound. In a conditionally independent hypercube experiment with at least one coordinate, per-coordinate candidate laws `Q j`, indexed by a bit, a per-coordinate compression map `compress` that is measurable at every coordinate, and per-coordinate decoders `decoder` built from the compressed local summary, the other coordinates' raw data, and shared randomness that are jointly measurable and depend on the raw sample at coordinate `j` only through its compressed summary, not directly on the raw value at `j`, then decentralized coordinate decoders cannot on average be correct more often than the common-part product bound built from the per-coordinate total-variation overlaps; and if every compressed adjacent KL divergence is at most `κ log M`, the displayed finite-`M` simultaneous-error certificate follows. |
| `Stat.coordinatewise_overlap_direct_product_asymptotic` | `∀ κ < 1, Filter.Tendsto Mseq Filter.atTop Filter.atTop → Filter.Tendsto (fun n => 1 / 2 * (1 - Real.exp (-↑(Mseq n) ^ (1 - κ) / 2))) Filter.atTop (nhds (1 / 2))` | Along any sequence `M_n → ∞` and for fixed `κ < 1`, the finite direct-product certificate tends to one half. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.Depoissonization -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSampleMap` | `(X → Y) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample Y` | Given a map from one observation space to another and a finite sample in the first space, the mapped finite sample has the same size and applies the map to every observation. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_finiteSampleMap` | `Measurable f → Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSampleMap f)` | Pointwise mapping of dependent finite samples is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSampleMap_fixedSizeEmbed` | `Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSampleMap f (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n x) = Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.fixedSizeEmbed n fun i => f (x i)` | Mapping commutes with fixed-size embedding. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.cellObservationLaw_eq_of_restrict_eq` | `μ (p.cellSet j) ≠ 0 → μ (p.cellSet j) = ν (p.cellSet j) → μ.restrict (p.cellSet j) = ν.restrict (p.cellSet j) → p.cellObservationLaw μ j = p.cellObservationLaw ν j` | Equal restrictions and equal cell masses give equal normalized cell laws. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.markedPoissonKL_le_two_mul_of_piKL` | `1 ≤ n → ∀ {B : ℝ}, 0 ≤ B → InformationTheory.klDiv (MeasureTheory.Measure.pi fun x => P) (MeasureTheory.Measure.pi fun x => Q) ≤ ENNReal.ofReal B → InformationTheory.klDiv (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw P P R (2 * ↑n)) (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw Q P R (2 * ↑n)) ≤ ENNReal.ofReal (2 * B)` | Consider a sample size `n` that is at least `1` and a nonnegative KL budget `B`, and suppose the KL divergence between `n` independent identically distributed draws from `P` and from `Q` is at most `B`. Then the KL divergence between the marked Poisson experiments with mean count `2n`, mark law `R`, and intensity measures `P` and `Q` respectively (both built over the same baseline `P`) is at most `2B`. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalPrefixObservations` | `X → (n : ℕ) → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample (X × ℝ) → Fin n → X` | Given a fallback observation, a nonnegative integer prefix length, and a finite sample of observation--real-mark pairs, the canonical prefix observations are the first $n$ observations when the sample has at least $n$ pairs, and otherwise are the constant $n$-tuple of the fallback observation. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.measurable_canonicalPrefixObservations` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalPrefixObservations x₀ n)` | Reading a fixed prefix from a canonical finite configuration is measurable. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.map_canonicalPrefixObservations_restrict_count_ge` | `MeasureTheory.Measure.map (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalPrefixObservations x₀ n) ((Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam).restrict (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count ⁻¹' Set.Ici n)) = (ProbabilityTheory.poissonMeasure lam) (Set.Ici n) • MeasureTheory.Measure.pi fun x => P` | On the successful-count event, the canonical marked-Poisson configuration's first `n` observations have the unnormalised product law. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.poisson_two_n_lower_tail` | `(ProbabilityTheory.poissonMeasure (2 * ↑n)) {k \| k < n} ≤ ENNReal.ofReal (Real.exp (-↑n * (1 - Real.log 2)))` | The lower tail used in de-Poissonization is exponentially small. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream` | `X → Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X → ℕ × (ℕ → X)` | Given a fallback observation and a finite sample, the padded stream representation is the pair consisting of its size and an infinite stream that agrees with the sample at positions below that size and equals the fallback observation thereafter. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream_measurable` | `Measurable (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream x0)` | — |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.streamToFiniteSample_paddedStream` | `Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.streamToFiniteSample (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream x0 s) = s` | — |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream_range` | `Set.range (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSamplePaddedStream x0) = {z \| ∀ (k : ℕ), z.1 ≤ k → z.2 k = x0}` | — |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteSample_standardBorelSpace` | `StandardBorelSpace (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample X)` | For every nonempty standard Borel observation space equipped with its measurable structure, the space of finite samples from that observation space is a standard Borel space. |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw_map_count` | `MeasureTheory.Measure.map Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.FiniteSample.count (Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.canonicalMarkedPoissonSampleLaw P R lam) = ProbabilityTheory.poissonMeasure lam` | — |
| `Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw_probability_eq` | `Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMeasureMarkedPoissonLaw P P0 R lam = Causalean.Mathlib.Probability.FiniteMarkedPoissonPartition.finiteMarkedPoissonSampleLaw P R lam` | When the intensity measure is already a probability law, the finite-measure Poisson wrapper agrees with the ordinary marked-Poisson sample law. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.MeasureTheory.AnalyticSetUniversalMeasurability.UpperSemianalytic -->
| Decl | Signature | Description |
|---|---|---|
| `MeasureTheory.outerLIntegral` | `MeasureTheory.Measure Ω → (Ω → ENNReal) → ENNReal` | The outer integral of an extended-nonnegative-valued function on a measurable sample space, under a measure on that sample space, is the infimum of the lower Lebesgue integrals of all measurable functions that dominate the given function pointwise. |
| `MeasureTheory.outerLIntegral_mono` | `f ≤ g → MeasureTheory.outerLIntegral μ f ≤ MeasureTheory.outerLIntegral μ g` | Outer integration is monotone in its extended-nonnegative integrand. |
| `MeasureTheory.lintegral_le_outerLIntegral` | `∫⁻ (ω : Ω), f ω ∂μ ≤ MeasureTheory.outerLIntegral μ f` | The lower Lebesgue integral is bounded by the outer integral. |
| `MeasureTheory.lintegral_le_outerLIntegral_of_measurable_le` | `f ≤ g → ∫⁻ (ω : Ω), f ω ∂μ ≤ MeasureTheory.outerLIntegral μ g` | A pointwise lower bound may be integrated before comparison with an outer integral, without measurability of either function. |
| `MeasureTheory.outerLIntegral_eq_lintegral_of_measurable` | `Measurable f → MeasureTheory.outerLIntegral μ f = ∫⁻ (ω : Ω), f ω ∂μ` | Outer integration agrees with Lebesgue integration for measurable functions. |
| `MeasureTheory.lintegral_le_lintegral_completion` | `∫⁻ (ω : Ω), f ω ∂μ ≤ ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion` | Completing a measure can only increase the lower integral of an arbitrary extended-nonnegative function. |
| `MeasureTheory.lintegral_completion_eq_of_measurable` | `Measurable f → ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion = ∫⁻ (ω : Ω), f ω ∂μ` | A measurable extended-nonnegative function has the same integral before and after completion of the measure. |
| `MeasureTheory.UpperSemianalytic` | `(Ω → ENNReal) → Prop` | An extended-nonnegative-valued function on a topological sample space is upper-semi-analytic exactly when, for every extended-nonnegative threshold $a$, the set of sample points at which its value is strictly greater than $a$ is analytic. |
| `MeasureTheory.UpperSemianalytic.nullMeasurable` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], MeasureTheory.NullMeasurable f μ` | An upper-semi-analytic extended-nonnegative function is null-measurable for every finite Borel measure. |
| `MeasureTheory.UpperSemianalytic.measurable_completion` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], Measurable f` | An upper-semi-analytic extended-nonnegative loss is measurable once a finite Borel sampling measure is completed, so it is available to ordinary completed-measure integration. |
| `MeasureTheory.UpperSemianalytic.lintegral_completion_eq_outerLIntegral` | `MeasureTheory.UpperSemianalytic f → ∀ (μ : MeasureTheory.Measure Ω) [MeasureTheory.IsFiniteMeasure μ], ∫⁻ (ω : MeasureTheory.NullMeasurableSpace Ω μ), f ω ∂μ.completion = MeasureTheory.outerLIntegral μ f` | On a Polish sample space equipped with its Borel σ-algebra and a finite measure `μ`, if `f` is upper-semi-analytic — every strict superlevel set `{ω \| a < f ω}` is analytic, then the lower Lebesgue integral of `f` against the completion of `μ` equals the outer integral of `f` with respect to `μ`, i.e. the infimum of the lower integrals of all measurable pointwise majorants of `f`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.Separability -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.centeredEmpiricalAverage` | `MeasureTheory.Measure Ω → {n : ℕ} → (Fin n → Ω) → (Ω → ℝ) → ℝ` | Given a measure $\mu$ on an observation space, a sample of $n$ observations, and a real-valued function, the centered empirical average is its sample average minus its integral with respect to $\mu$. |
| `Stat.Concentration.HasCountableEmpiricalSupReduction` | `MeasureTheory.Measure Ω → (ι → Ω → ℝ) → Prop` | Given a measure $\mu$ on an observation space and a family of real-valued functions indexed by a set $\iota$, the countable empirical-supremum reduction property holds exactly when every member of the family is measurable and there is a sequence of indices whose associated countable subfamily has, for every sample size, the same supremum of absolute centered empirical averages as the full family almost surely under the corresponding product measure. |
| `Stat.Concentration.hasCountableEmpiricalSupReduction_of_pointwise_dense` | `(∀ᵐ (z : Ω) ∂μ, z ∈ S) → (∀ (i : ι), ∃ kseq, ∀ z ∈ S, Filter.Tendsto (fun m => g (g0 (kseq m)) z) Filter.atTop (nhds (g i z))) → (∀ (i : ι), Measurable (g i)) → (∃ G, MeasureTheory.Integrable G μ ∧ ∀ (i : ι) (z : Ω), \|g i z\| ≤ G z) → Causalean.Stat.Concentration.HasCountableEmpiricalSupReduction μ g` | Countable supremum reduction from pointwise density. Let `μ` be a σ-finite measure on `Ω`, `g : ι → Ω → ℝ` a family of functions, and `g0 : ℕ → ι` a countable subfamily. Suppose `S` is a `μ`-conull subset of `Ω`, on `S`, every `g i` is the pointwise limit, along some subsequence, of the countable subfamily `g ∘ g0`, each `g i` is measurable, and there is a single `μ`-integrable envelope `G` dominating `\|g i\|` uniformly in `i`. Then the countable subfamily indexed by `g0` realizes the full continuum empirical-process supremum of `g` almost surely under every finite product law of `μ`. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Concentration.VarianceAdaptiveVCExpectedMaximal.EntropyChaining -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Concentration.HasVCUniformEntropy` | `MeasureTheory.Measure Ω → (ι → Ω → ℝ) → ℝ → ℝ → ℝ → ℝ → Prop` | For a measure on a measurable sample space, a class of real-valued functions indexed by a set, an envelope bound, a radius, a covering constant, and an entropy exponent, the class has uniform VC-type entropy exactly when (1) the radius is positive, (2) the radius is strictly smaller than the envelope bound, (3) the covering constant is at least $e$, (4) the entropy exponent is at least one, (5) every function in the class is measurable, (6) every function is bounded in absolute value by the envelope bound at every sample point, (7) every function has population $L^2$ distance at most the radius from the zero function, and (8) every countable enumeration of the class has the stipulated polynomial empirical $L^2$ covering property. |
| `Stat.Concentration.countableEmpiricalProcessSup` | `MeasureTheory.Measure Ω → (ι → Ω → ℝ) → (ℕ → ι) → {n : ℕ} → (Fin n → Ω) → ENNReal` | For a measure on a measurable sample space, a class of real-valued functions, a countable enumeration of that class, and a finite sample, the countable empirical-process supremum is the extended nonnegative real supremum, over the enumerated functions, of the absolute centered empirical average. |
| `Stat.Concentration.vcEntropy_chaining_bound` | `Causalean.Stat.Concentration.HasVCUniformEntropy μ g U σ A v → ∃ C, 0 < C ∧ ∀ (n : ℕ), 1 ≤ n → (∫⁻ (w : Fin n → Ω), Causalean.Stat.Concentration.countableEmpiricalProcessSup μ g g0 w ∂MeasureTheory.Measure.pi fun x => μ) ≤ ENNReal.ofReal (C * (σ * √(Real.log (U / σ) / ↑n) + U * Real.log (U / σ) / ↑n))` | Dudley chaining bound for VC-type entropy. Let `μ` be a probability measure on `Ω`, `g : ι → Ω → ℝ` a family of functions, and `g0 : ℕ → ι` a countable enumeration of the index set. If `g` has uniform VC-type entropy relative to `μ`, with envelope `U`, population $L^2$ radius `σ`, covering-entropy base `A`, and exponent `v`, then there is a universal constant `C > 0` such that, for every sample size `n ≥ 1`, the expectation of the countable empirical-process supremum along the enumeration `g0` over the `n`-fold product of `μ` is at most `C · (σ √(log(U/σ)/n) + U log(U/σ)/n)`. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.InformationTheory.CommonStatisticBernoulli -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.InformationTheory.one_add_mul_one_sub_mem_Icc` | `p ∈ Set.Icc (1 / 4) (3 / 4) → 1 + p * (1 - p) ∈ Set.Icc 1 (5 / 4)` | A Bernoulli parameter in the middle half of the unit interval has variance between zero and one quarter, so adding unit noise gives variance between one and five quarters. |
| `Mathlib.InformationTheory.commonStatisticBernoulliKernel` | `(p : S → ℝ) → Measurable p → ProbabilityTheory.Kernel S ℝ` | For a measurable input space, a real-valued function of the input, and the hypothesis that this function is measurable, the common-statistic Bernoulli kernel assigns to every input the Bernoulli probability measure on the real line with success probability given by that function at the input. |
| `Mathlib.InformationTheory.commonStatisticBernoulliKernel_isMarkovKernel` | `(∀ (r : S), 0 ≤ p r) → (∀ (r : S), p r ≤ 1) → ProbabilityTheory.IsMarkovKernel (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp)` | Pointwise unit-interval parameters make the common-statistic Bernoulli kernel Markov. |
| `Mathlib.InformationTheory.commonStatisticBernoulli_klDiv_le_of_localized_parameter` | `(∀ (r : ℝ), 1 / 4 ≤ p r) → (∀ (r : ℝ), p r ≤ 3 / 4) → (∀ (r : ℝ), 1 / 4 ≤ q r) → (∀ (r : ℝ), q r ≤ 3 / 4) → ∀ {D : ℝ}, 0 ≤ D → ∀ {E : Set ℝ}, MeasurableSet E → (∀ᵐ (r : ℝ) ∂m, \|p r - q r\| ≤ E.indicator (fun x => D) r) → InformationTheory.klDiv (m.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp)) (m.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel q hq)) ≤ ENNReal.ofReal (4 * D ^ 2) * m E` | A common statistic with conditionally Bernoulli outcomes has KL bounded by the squared change in its success parameter, integrated only over the statistic region where that parameter can change. This is the generic disintegration step used by the signed hard-cell comparison. |
| `Mathlib.InformationTheory.commonStatisticBernoulliOutcome_klDiv_le_of_localized_parameter` | `(∀ (r : ℝ), 1 / 4 ≤ p r) → (∀ (r : ℝ), p r ≤ 3 / 4) → (∀ (r : ℝ), 1 / 4 ≤ q r) → (∀ (r : ℝ), q r ≤ 3 / 4) → ∀ {D : ℝ}, 0 ≤ D → ∀ {E : Set ℝ}, MeasurableSet E → (∀ᵐ (r : ℝ) ∂m, \|p r - q r\| ≤ E.indicator (fun x => D) r) → InformationTheory.klDiv (MeasureTheory.Measure.map Prod.swap (m.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp))) (MeasureTheory.Measure.map Prod.swap (m.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel q hq))) ≤ ENNReal.ofReal (4 * D ^ 2) * m E` | Swapping a common statistic behind its Bernoulli outcome preserves the localized KL estimate, giving the `(outcome, statistic)` coordinate order used by signed observations. |
| `Mathlib.InformationTheory.statisticSuccessMeasure` | `MeasureTheory.Measure A → (A → ℝ) → (A → ℝ) → MeasureTheory.Measure ℝ` | For a measurable sample space, a measure on that space, a real-valued success-weight function, and a real-valued statistic, the success-weighted statistic law is the pushforward along the statistic of the base measure weighted by $\max\{p(x),0\}$. |
| `Mathlib.InformationTheory.statisticSuccessParameter` | `MeasureTheory.Measure A → (A → ℝ) → (A → ℝ) → ℝ → ℝ` | For a measurable sample space, a measure on that space, a real-valued success-weight function, and a real-valued statistic, the statistic-level success parameter is the Radon--Nikodym derivative of the success-weighted statistic law with respect to the statistic's pushforward law under the base measure, converted to a real number. |
| `Mathlib.InformationTheory.statisticSuccessMeasure_absolutelyContinuous` | `Measurable stat → (∀ (x : A), p x ≤ 1) → (Causalean.Mathlib.InformationTheory.statisticSuccessMeasure nu p stat).AbsolutelyContinuous (MeasureTheory.Measure.map stat nu)` | The success-weighted statistic law is dominated by the statistic marginal when the pointwise success probability is at most one. |
| `Mathlib.InformationTheory.statisticSuccessParameter_setIntegral` | `Measurable p → Measurable stat → (∀ (x : A), 0 ≤ p x) → (∀ (x : A), p x ≤ 1) → ∀ (B : Set ℝ), MeasurableSet B → ∫ (r : ℝ) in B, Causalean.Mathlib.InformationTheory.statisticSuccessParameter nu p stat r ∂MeasureTheory.Measure.map stat nu = ∫ (x : A) in {x \| stat x ∈ B}, p x ∂nu` | Set integrals of the conditional parameter recover success-weighted integrals on statistic preimages. |
| `Mathlib.InformationTheory.statisticSuccessParameter_mem_Icc_ae` | `Measurable p → Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → ∀ᵐ (r : ℝ) ∂MeasureTheory.Measure.map stat nu, Causalean.Mathlib.InformationTheory.statisticSuccessParameter nu p stat r ∈ Set.Icc (1 / 4) (3 / 4)` | Middle-half pointwise bounds pass to the conditional statistic parameter almost everywhere. |
| `Mathlib.InformationTheory.clippedStatisticSuccessParameter` | `MeasureTheory.Measure A → (A → ℝ) → (A → ℝ) → ℝ → ℝ` | For a measurable sample space, a measure on that space, a real-valued success-weight function, and a real-valued statistic, the clipped statistic-level success parameter is the statistic-level success parameter truncated below at $1/4$ and above at $3/4$. |
| `Mathlib.InformationTheory.clippedStatisticSuccessParameter_measurable` | `Measurable (Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu p stat)` | — |
| `Mathlib.InformationTheory.clippedStatisticSuccessParameter_mem_Icc` | `Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu p stat r ∈ Set.Icc (1 / 4) (3 / 4)` | — |
| `Mathlib.InformationTheory.clippedStatisticSuccessParameter_ae_eq` | `Measurable p → Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu p stat =ᵐ[MeasureTheory.Measure.map stat nu] Causalean.Mathlib.InformationTheory.statisticSuccessParameter nu p stat` | — |
| `Mathlib.InformationTheory.commonStatisticBernoulliKernel_setLIntegral_eq` | `(∀ (x : A), 0 ≤ p x) → (∀ (x : A), p x ≤ 1) → (∀ (x : B), 0 ≤ p' x) → (∀ (x : B), p' x ≤ 1) → ∀ {D : Set A} {D' : Set B}, nu D = nu' D' → ∫ (x : A) in D, p x ∂nu = ∫ (x : B) in D', p' x ∂nu' → ∀ (E : Set ℝ), ∫⁻ (x : A) in D, ((Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp) x) E ∂nu = ∫⁻ (x : B) in D', ((Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p' hp') x) E ∂nu'` | Integrating Bernoulli kernels over two base sets gives the same outcome measure when the base masses and success-weighted masses agree. |
| `Mathlib.InformationTheory.statisticBernoulliOutcomeLaw_eq_map_swap_compProd` | `Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → MeasureTheory.Measure.map (fun z => (z.2, stat z.1)) (nu.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp)) = MeasureTheory.Measure.map Prod.swap ((MeasureTheory.Measure.map stat nu).compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel (Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu p stat) (Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter_measurable nu p stat)))` | Compressing the base coordinate to a statistic turns a Bernoulli mixture into a Bernoulli composition product over the statistic marginal. |
| `Mathlib.InformationTheory.clippedStatisticSuccessParameter_abs_sub_le_ae` | `Measurable p → Measurable p' → Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → (∀ (x : A), 1 / 4 ≤ p' x) → (∀ (x : A), p' x ≤ 3 / 4) → MeasureTheory.Measure.map stat nu = MeasureTheory.Measure.map stat nu' → ∀ {D : ℝ}, 0 ≤ D → ∀ {E : Set ℝ}, MeasurableSet E → (∀ (B : Set ℝ), MeasurableSet B → \|∫ (x : A) in {x \| stat x ∈ B}, p x ∂nu - ∫ (x : A) in {x \| stat x ∈ B}, p' x ∂nu'\| ≤ D * ((MeasureTheory.Measure.map stat nu) (B ∩ E)).toReal) → ∀ᵐ (r : ℝ) ∂MeasureTheory.Measure.map stat nu, \|Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu p stat r - Causalean.Mathlib.InformationTheory.clippedStatisticSuccessParameter nu' p' stat r\| ≤ E.indicator (fun x => D) r` | A localized setwise bound on success-weighted statistic masses yields the corresponding almost-everywhere bound on conditional Bernoulli parameters. |
| `Mathlib.InformationTheory.statisticBernoulliOutcome_klDiv_le_of_localized_success_bound` | `Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → (∀ (x : A), 1 / 4 ≤ p' x) → (∀ (x : A), p' x ≤ 3 / 4) → MeasureTheory.Measure.map stat nu = MeasureTheory.Measure.map stat nu' → ∀ {D : ℝ}, 0 ≤ D → ∀ {E : Set ℝ}, MeasurableSet E → (∀ (B : Set ℝ), MeasurableSet B → \|∫ (x : A) in {x \| stat x ∈ B}, p x ∂nu - ∫ (x : A) in {x \| stat x ∈ B}, p' x ∂nu'\| ≤ D * ((MeasureTheory.Measure.map stat nu) (B ∩ E)).toReal) → InformationTheory.klDiv (MeasureTheory.Measure.map (fun z => (z.2, stat z.1)) (nu.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp))) (MeasureTheory.Measure.map (fun z => (z.2, stat z.1)) (nu'.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p' hp'))) ≤ ENNReal.ofReal (4 * D ^ 2) * (MeasureTheory.Measure.map stat nu) E` | For a measurable space `A`, two finite measures `nu` and `nu'` on it, and success-probability functions `p`, `p'` on `A`, suppose `p`, `p'`, and a statistic `stat` are all measurable, `p` takes values in `[1/4, 3/4]` and `p'` likewise takes values in `[1/4, 3/4]`, and `stat` pushes `nu` and `nu'` forward to the same marginal law. Given a nonnegative discrepancy bound `D` and a measurable exceptional set `E` such that for every measurable set `B` of statistic values, the setwise success-mass discrepancy `\|∫_{stat∈B} p dnu − ∫_{stat∈B} p' dnu'\|` is at most `D` times the `stat`-pushforward mass of `nu` on `B ∩ E`, then the Kullback–Leibler divergence between the compressed Bernoulli-outcome laws obtained by pairing the outcome with `stat` under `nu` and under `nu'` is at most `4·D²` times the `stat`-pushforward mass of `E` under `nu`. |
| `Mathlib.InformationTheory.statisticBernoulliOutcome_restrict_compl_eq_of_localized_success_bound` | `Measurable stat → (∀ (x : A), 1 / 4 ≤ p x) → (∀ (x : A), p x ≤ 3 / 4) → (∀ (x : A), 1 / 4 ≤ p' x) → (∀ (x : A), p' x ≤ 3 / 4) → MeasureTheory.Measure.map stat nu = MeasureTheory.Measure.map stat nu' → ∀ {D : ℝ}, 0 ≤ D → ∀ {E : Set ℝ}, MeasurableSet E → (∀ (B : Set ℝ), MeasurableSet B → \|∫ (x : A) in {x \| stat x ∈ B}, p x ∂nu - ∫ (x : A) in {x \| stat x ∈ B}, p' x ∂nu'\| ≤ D * ((MeasureTheory.Measure.map stat nu) (B ∩ E)).toReal) → (MeasureTheory.Measure.map (fun z => (z.2, stat z.1)) (nu.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p hp))).restrict {z \| z.2 ∉ E} = (MeasureTheory.Measure.map (fun z => (z.2, stat z.1)) (nu'.compProd (Causalean.Mathlib.InformationTheory.commonStatisticBernoulliKernel p' hp'))).restrict {z \| z.2 ∉ E}` | Common statistic marginals and a localized setwise success-mass bound also imply exact agreement of the compressed outcome laws away from the exceptional statistic set. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Nonparametric.LocalPolynomial.GramCoercivity -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Nonparametric.LocalPolynomial.localPolynomial` | `(p : ℕ) → (Fin (p + 1) → ℝ) → Polynomial ℝ` | For a nonnegative polynomial degree and real coefficients indexed from zero through that degree, the local-polynomial coefficient polynomial is $\sum_i v_i u^i$. |
| `Stat.Nonparametric.LocalPolynomial.localPolynomial_eval` | `Polynomial.eval u (Causalean.Stat.Nonparametric.LocalPolynomial.localPolynomial p v) = ∑ i, v i * u ^ ↑i` | Evaluation of the coefficient polynomial is the dot product with the monomial basis. |
| `Stat.Nonparametric.LocalPolynomial.localPolynomial_eq_zero_iff` | `Causalean.Stat.Nonparametric.LocalPolynomial.localPolynomial p v = 0 ↔ v = 0` | The coefficient polynomial vanishes only when every coefficient does. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy` | `(p : ℕ) → (Fin (p + 1) → ℝ) → ℝ` | For a nonnegative polynomial degree and real coefficients indexed from zero through that degree, the radial polynomial energy is the double sum of $v_i v_j$ divided by $i+j+2$, over all coefficient indices $i,j$ from zero through that degree. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy_eq_integral` | `Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p v = ∫ (u : ℝ) in 0..1, (∑ i, v i * u ^ ↑i) ^ 2 * u` | The explicit moment matrix is exactly the weighted squared-polynomial integral on the unit interval. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy_pos` | `v ≠ 0 → 0 < Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p v` | A nonzero coefficient vector has strictly positive radial energy. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy_continuous` | `Continuous (Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p)` | The radial energy is a continuous quadratic function of its coefficient vector. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy_smul` | `Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p (a • v) = a ^ 2 * Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p v` | Radial energy is homogeneous of degree two in the coefficient vector. |
| `Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy_coercive` | `∃ c, 0 < c ∧ ∀ (v : Fin (p + 1) → ℝ), c * ∑ i, v i ^ 2 ≤ Causalean.Stat.Nonparametric.LocalPolynomial.radialPolynomialEnergy p v` | For a polynomial degree bound `p`, there is a positive constant such that the radial polynomial energy of any coefficient vector is bounded below by that constant times the sum of the squared coefficients: on the Euclidean unit sphere, radial polynomial energy has a positive minimum, and homogeneity packages this as a coercive lower bound for all coefficient vectors. |
| `Stat.Nonparametric.LocalPolynomial.signedRadialPolynomialEnergy_coercive` | `∃ c, 0 < c ∧ ∀ (t : Bool) (v : Fin (p + 1) → ℝ), c * ∑ i, v i ^ 2 ≤ ∫ (u : ℝ) in 0..1, (∑ i, v i * (if t = true then u else -u) ^ ↑i) ^ 2 * u` | The same coercivity constant works in both signed-distance orientations. The negative orientation merely changes coefficient `i` by the sign `(-1)^i`, which preserves the sum of coefficient squares. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.HalfDiscPolar -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.planarRadius` | `ℝ × ℝ → ℝ` | For a point in the real coordinate plane, the planar radius is its Euclidean distance from the origin, namely $\sqrt{x^2+y^2}$ for coordinates $(x,y)$. |
| `Mathlib.Analysis.planarRadius_measurable` | `Measurable Causalean.Mathlib.Analysis.planarRadius` | The Euclidean radius on the coordinate plane is Borel measurable. |
| `Mathlib.Analysis.planarAngle` | `ℝ × ℝ → ℝ` | For a point in the real coordinate plane, the planar angle is the angular coordinate assigned by the polar-coordinate chart. |
| `Mathlib.Analysis.integral_cos_zero_to_pi` | `∫ (θ : ℝ) in Set.Ioc 0 Real.pi, Real.cos θ = 0` | The cosine has zero integral on a half-circle. |
| `Mathlib.Analysis.integral_cos_sq_zero_to_pi` | `∫ (θ : ℝ) in Set.Ioc 0 Real.pi, Real.cos θ ^ 2 = Real.pi / 2` | The quadratic cosine moment on a half-circle is `π / 2`. |
| `Mathlib.Analysis.halfDisc_weighted_polar_integral` | `∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) * h (Causalean.Mathlib.Analysis.planarAngle z) = (∫ (s : ℝ) in Set.Ioc 0 r, s * g s) * ∫ (θ : ℝ) in Set.Ioo 0 Real.pi, h θ` | For radial and angular weight functions `g` and `h` and a radius `r`, the integral of the product `g(radius)·h(angle)` over the open upper half-disc of radius `r` factors as the product of the radial integral `∫ s·g(s) ds` over `(0, r]` and the angular integral `∫ h(θ) dθ` over `(0, π)`. |
| `Mathlib.Analysis.halfDisc_weighted_cos_cancellation` | `∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) * Real.cos (Causalean.Mathlib.Analysis.planarAngle z) = 0` | A cosine angular tilt has zero integral against every radial weight on an upper half-disc. |
| `Mathlib.Analysis.halfDisc_radialSet_weighted_cos_cancellation` | `MeasurableSet A → ∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r} ∩ Causalean.Mathlib.Analysis.planarRadius ⁻¹' A, g (Causalean.Mathlib.Analysis.planarRadius z) * Real.cos (Causalean.Mathlib.Analysis.planarAngle z) = 0` | A cosine angular tilt has zero mass on every measurable radial subset of an upper half-disc. This is the setwise interface used to identify radial pushforwards, rather than merely their total masses. |
| `Mathlib.Analysis.planarFirst_div_radius_eq_cos` | `0 < z.2 → z.1 / Causalean.Mathlib.Analysis.planarRadius z = Real.cos (Causalean.Mathlib.Analysis.planarAngle z)` | On the open upper half-plane, the first coordinate divided by the radius is the cosine of the polar angle. |
| `Mathlib.Analysis.halfDisc_weighted_first_div_radius_cancellation` | `∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) * (z.1 / Causalean.Mathlib.Analysis.planarRadius z) = 0` | A radial weight times the Cartesian direction cosine has zero integral on an open upper half-disc. |
| `Mathlib.Analysis.closedHalfDisc_weighted_first_div_radius_cancellation` | `∫ (z : ℝ × ℝ) in {z \| 0 ≤ z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) * (z.1 / Causalean.Mathlib.Analysis.planarRadius z) = 0` | Replacing the open diameter of an upper half-disc by the closed diameter does not affect the Cartesian cosine integral. |
| `Mathlib.Analysis.translatedClosedHalfDisc_weighted_first_div_radius_cancellation` | `∫ (z : ℝ × ℝ) in {z \| 0 ≤ (z - c).2 ∧ Causalean.Mathlib.Analysis.planarRadius (z - c) ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius (z - c)) * ((z - c).1 / Causalean.Mathlib.Analysis.planarRadius (z - c)) = 0` | Translation preserves the zero Cartesian-cosine integral over a closed upper half-disc. |
| `Mathlib.Analysis.halfDisc_weighted_cos_sq` | `∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) * Real.cos (Causalean.Mathlib.Analysis.planarAngle z) ^ 2 = (∫ (s : ℝ) in Set.Ioc 0 r, s * g s) * (Real.pi / 2)` | The cosine-squared angular moment converts a radial weight into the nonzero `π/2` factor used to cancel the affine regression term. |
| `Mathlib.Analysis.halfDisc_radial_integral` | `∫ (z : ℝ × ℝ) in {z \| 0 < z.2 ∧ Causalean.Mathlib.Analysis.planarRadius z ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius z) = ∫ (s : ℝ) in Set.Ioc 0 r, Real.pi * s * g s` | A radial integrand on a half-disc admits the expected polar-coordinate decomposition. |
| `Mathlib.Analysis.halfDisc_cos_radial_cancellation` | `∫ (s : ℝ) in Set.Ioc 0 r, s * g s * ∫ (θ : ℝ) in Set.Ioc 0 Real.pi, Real.cos θ = 0` | Multiplication by cosine contributes zero after angular integration on every radial shell. |
| `Mathlib.Analysis.translatedHalfDisc_weighted_cos_cancellation` | `∫ (z : ℝ × ℝ) in (fun u => c + u) '' {u \| 0 < u.2 ∧ Causalean.Mathlib.Analysis.planarRadius u ≤ r}, g (Causalean.Mathlib.Analysis.planarRadius (z - c)) * Real.cos (Causalean.Mathlib.Analysis.planarAngle (z - c)) = 0` | Translating an upper half-disc does not change the cosine cancellation. |
| `Mathlib.Analysis.translatedHalfDisc_radialSet_weighted_cos_cancellation` | `MeasurableSet A → ∫ (z : ℝ × ℝ) in (fun u => c + u) '' {u \| 0 < u.2 ∧ Causalean.Mathlib.Analysis.planarRadius u ≤ r} ∩ {z \| Causalean.Mathlib.Analysis.planarRadius (z - c) ∈ A}, g (Causalean.Mathlib.Analysis.planarRadius (z - c)) * Real.cos (Causalean.Mathlib.Analysis.planarAngle (z - c)) = 0` | Translation preserves cosine cancellation on every measurable radial subset of a half-disc. |
<!-- /GEN -->

<!-- GEN:Causalean.Stat.Nonparametric.LocalPolynomial.CoordinateDerivative -->
| Decl | Signature | Description |
|---|---|---|
| `Stat.Nonparametric.LocalPolynomial.coordinateMultiOrder` | `(Fin 2 → ℕ) → ℕ` | For a bivariate multi-index, the total order of that multi-index is the sum of its two coordinate orders. |
| `Stat.Nonparametric.LocalPolynomial.coordinateDirections` | `(alpha : Fin 2 → ℕ) → Fin (Causalean.Stat.Nonparametric.LocalPolynomial.coordinateMultiOrder alpha) → EuclideanSpace ℝ (Fin 2)` | For a bivariate multi-index and a position in a list whose length is its total order, the associated ordered standard-coordinate direction is the first standard coordinate direction when the position is smaller than the first coordinate order, and the second standard coordinate direction otherwise. |
| `Stat.Nonparametric.LocalPolynomial.coordinatePartial` | `(EuclideanSpace ℝ (Fin 2) → ℝ) → (Fin 2 → ℕ) → EuclideanSpace ℝ (Fin 2) → ℝ` | For a real-valued function on two-dimensional Euclidean space, a bivariate multi-index, and a point in that space, the scalar coordinate partial derivative is the iterated derivative of total order given by the multi-index, evaluated at the point along the associated ordered standard-coordinate directions. |
| `Stat.Nonparametric.LocalPolynomial.coordinatePartial_abs_le_iteratedFDeriv_norm` | `\|Causalean.Stat.Nonparametric.LocalPolynomial.coordinatePartial f alpha x\| ≤ ‖iteratedFDeriv ℝ (Causalean.Stat.Nonparametric.LocalPolynomial.coordinateMultiOrder alpha) f x‖` | Evaluating the iterated Fréchet derivative of a function `f` of a bivariate multi-index `alpha` at a point `x`, along the standard coordinate directions, cannot increase its operator norm — the resulting scalar coordinate partial is bounded in absolute value by the operator norm of the full iterated derivative. |
| `Stat.Nonparametric.LocalPolynomial.coordinatePartial_sub_abs_le_iteratedFDeriv_sub_norm` | `\|Causalean.Stat.Nonparametric.LocalPolynomial.coordinatePartial f alpha x - Causalean.Stat.Nonparametric.LocalPolynomial.coordinatePartial f alpha z\| ≤ ‖iteratedFDeriv ℝ (Causalean.Stat.Nonparametric.LocalPolynomial.coordinateMultiOrder alpha) f x - iteratedFDeriv ℝ (Causalean.Stat.Nonparametric.LocalPolynomial.coordinateMultiOrder alpha) f z‖` | Differences of scalar coordinate partials are bounded by the operator norm of the corresponding Fréchet-derivative difference. |
<!-- /GEN -->

## 11. `SCM/ID/` — Backdoor / Frontdoor do-calculus assembly theorems

The do-calculus Backdoor and Frontdoor identification theorems live in
`SCM/ID/Backdoor.lean`, `SCM/ID/BackdoorCriterion.lean`, and
`SCM/ID/Frontdoor.lean` (sorry-free), with the adjustment functionals in
`SCM/ID/Adjustment.lean`. See the SCM/ID sections above for the
per-declaration tables.

---

## Proof strategies for concrete DAGs

| Goal type | Recommended tactic | Notes |
|---|---|---|
| `v ∈ G.parents w` | `by decide` | Finite membership |
| `G.parents v = ∅` | `by native_decide` | Finset equality |
| `G.isRoot v` / `¬G.isRoot v` | `by native_decide` | Unfolds to `parents = ∅` |
| `G.dSep X Y Z` / `¬G.dSep X Y Z` | `by decide` | Bayes Ball is computable |
| `directlyConfounded v w` | `by decide` | Finite search |
| `v ∈ cComponentOf w` | `by native_decide` | BFS computation |
| `cComponentOf v = S` | `by native_decide` | Finset equality |
| `G.isAncestor u v` | Term-mode `.edge`/`.trans` | More informative than `decide` |
| `¬G.isAncestor v v` | `G.isAncestor_irrefl v` | Direct theorem application |
| `edgeType u v = t` | `rfl` | Definitional equality |


## 10a''''''''. `Mathlib/Analysis/AbsoluteValueMomentPriorDuality/` — absolute-value approximation and moment-prior duality

Reusable approximation-theory infrastructure for the nonsmooth function x ↦ |x| on the unit interval. It defines the intrinsic best uniform polynomial error, proves the exact dual construction of symmetric probability measures with matched polynomial moments, and establishes universal inverse-degree upper and lower bounds. The Fourier certificate is kept in a separate support module; the API module is the consumer-facing import.

### Basic approximation facts

<!-- GEN:Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.Basic -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.unitInterval` | `Set ℝ` | The unit interval is the closed set of real numbers from $-1$ through $1$, inclusive. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs` | `Polynomial ℝ → ℝ` | For a real polynomial, its uniform absolute-value approximation error is the supremum, over every real number in the closed interval from $-1$ through $1$, of the absolute difference between the polynomial's value and that number's absolute value. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs` | `ℕ → ℝ` | For a nonnegative integer degree bound, the best uniform absolute-value approximation error is the infimum of the uniform errors of all real polynomials whose degree is at most that bound. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs_eq_sSup` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs p = sSup ((fun x => \|\|x\| - Polynomial.eval x p\|) '' Set.Icc (-1) 1)` | For a real polynomial, its uniform absolute-value approximation error is the supremum of its pointwise residual on the unit interval. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs_le_iff` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs p ≤ e ↔ ∀ x ∈ Set.Icc (-1) 1, \|\|x\| - Polynomial.eval x p\| ≤ e` | For a real polynomial and a proposed error bound, the bound holds exactly when it bounds every residual on the unit interval. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_eq_sInf` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K = sInf {e \| ∃ p, p.natDegree ≤ K ∧ e = Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs p}` | For a polynomial degree limit, the best absolute-value approximation error is the infimum over all admissible polynomial errors. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_nonneg` | `0 ≤ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For a polynomial degree limit, the best approximation error cannot be negative. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_antitone` | `K ≤ L → Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs L ≤ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For two degree limits with the first no larger than the second, allowing the larger degree cannot increase the best error. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.exists_bestPolynomialAbs` | `∃ p, p.natDegree ≤ K ∧ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.uniformApproxErrorAbs p = Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For a polynomial degree limit, some admissible polynomial attains the best absolute-value approximation error. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.exists_bestPolynomialAbs_interval` | `∃ p, p.natDegree ≤ K ∧ ∀ x ∈ Set.Icc (-1) 1, \|\|x\| - Polynomial.eval x p\| ≤ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For a polynomial degree limit, some admissible polynomial bounds every absolute-value residual by the best error on the unit interval. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_two_mul_add_one` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs (2 * m + 1) = Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs (2 * m)` | For a nonnegative integer, allowing the odd degree 2m+1 gives the same best error as degree 2m. |
<!-- /GEN -->

### Fourier certificate

<!-- GEN:Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.Fejer -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.instFactLtRealOfNatPi_causalean` | `Fact (0 < Real.pi)` | — |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.oneSidedFourierSum` | `ℕ → C(AddCircle Real.pi, ℂ)` | For a nonnegative integer order, the one-sided Fourier sum is the continuous complex-valued function on the circle obtained by summing the Fourier characters with integer frequencies from zero through one less than that order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerKernel` | `ℕ → AddCircle Real.pi → ℝ` | For a nonnegative integer order and a point on the circle of period π, the normalized Fejér kernel is the squared complex modulus of the one-sided Fourier sum at that point, divided by the order; at order zero, this quotient is defined to be zero. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.continuous_fejerKernel` | `Continuous (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerKernel n)` | For a Fejér order, the normalized Fejér kernel is continuous. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean` | `ℕ → C(AddCircle Real.pi, ℂ) → ℂ` | For a nonnegative integer order and a continuous complex-valued function on the circle of period π, the Fejér mean is the Haar integral of the function multiplied by the normalized Fejér kernel of that order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.integral_fourier` | `∫ (x : AddCircle Real.pi), (fourier k) x ∂AddCircle.haarAddCircle = if k = 0 then 1 else 0` | For a Fourier frequency, its Haar integral is one at frequency zero and zero otherwise. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejer_integrand_expand` | `↑(Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerKernel n t) * (fourier ↑k) t = (↑n)⁻¹ * ∑ r ∈ Finset.range n, ∑ s ∈ Finset.range n, (fourier (↑r - ↑s + ↑k)) t` | For a Fejér order, Fourier frequency, and circle point, the kernel-weighted character has the stated finite Fourier expansion. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean_fourier_nat` | `0 < n → Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean n (fourier ↑k) = ↑(if k < n then ↑(n - k) / ↑n else 0)` | For a positive Fejér order and a nonnegative frequency, the Fejér mean has the stated triangular multiplier. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean_fourier_neg_nat` | `0 < n → Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean n (fourier (-↑k)) = ↑(if k < n then ↑(n - k) / ↑n else 0)` | For a positive Fejér order and a negative frequency magnitude, the Fejér mean has the same triangular multiplier. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.integral_fejerKernel` | `0 < n → ∫ (t : AddCircle Real.pi), Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerKernel n t ∂AddCircle.haarAddCircle = 1` | For a positive Fejér order, the normalized kernel integrates to one. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean_norm_le` | `0 < n → ∀ (f : C(AddCircle Real.pi, ℂ)) (E : ℝ), (∀ (t : AddCircle Real.pi), ‖f t‖ ≤ E) → ‖Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean n f‖ ≤ E` | For a positive Fejér order, a continuous input, and a uniform norm bound, the Fejér mean obeys that bound. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMeanCLM` | `(n : ℕ) → 0 < n → C(AddCircle Real.pi, ℂ) →L[ℂ] ℂ` | For a positive integer order, the continuous-linear Fejér mean functional maps each continuous complex-valued function on the circle of period π to its Fejér mean of that order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMeanCLM_apply` | `(Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMeanCLM n hn) f = Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fejerMean n f` | For a positive Fejér order and a continuous input, the packaged linear map equals the integral definition. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.valleePoussinMeanCLM` | `(n : ℕ) → 0 < n → C(AddCircle Real.pi, ℂ) →L[ℂ] ℂ` | For a positive integer order, the continuous-linear de la Vallée--Poussin mean maps a continuous complex-valued function on the circle of period π to twice its Fejér mean of order twice the given order, minus its Fejér mean of the given order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.valleePoussinMean_fourier_nat` | `k ≤ n → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.valleePoussinMeanCLM n hn) (fourier ↑k) = 1` | For a positive order and a frequency at most that order, the de la Vallée--Poussin mean preserves the nonnegative Fourier character. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.valleePoussinMean_fourier_neg_nat` | `k ≤ n → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.valleePoussinMeanCLM n hn) (fourier (-↑k)) = 1` | For a positive order and a frequency magnitude at most that order, the de la Vallée--Poussin mean preserves the negative Fourier character. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM` | `(n : ℕ) → 0 < n → C(AddCircle Real.pi, ℂ) →L[ℂ] ℂ` | For a positive integer order, the continuous-linear cusp functional maps a continuous complex-valued function on the circle of period π to its value at zero minus its de la Vallée--Poussin mean of that order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_nat` | `k ≤ n → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier ↑k) = 0` | For a positive order and a nonnegative frequency at most that order, the cusp functional annihilates the Fourier character. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_neg_nat` | `k ≤ n → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier (-↑k)) = 0` | For a positive order and a negative frequency magnitude at most that order, the cusp functional annihilates the Fourier character. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_nat_nonneg` | `0 ≤ ((Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier ↑k)).re` | For a positive order and a nonnegative frequency, the real part of the cusp multiplier is nonnegative. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_neg_nat_nonneg` | `0 ≤ ((Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier (-↑k))).re` | For a positive order and a negative frequency magnitude, the real part of the cusp multiplier is nonnegative. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_nat_high` | `2 * n ≤ k → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier ↑k) = 1` | For a positive order and a nonnegative frequency at least twice that order, the cusp functional has multiplier one. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_fourier_neg_nat_high` | `2 * n ≤ k → (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) (fourier (-↑k)) = 1` | For a positive order and a negative frequency magnitude at least twice that order, the cusp functional has multiplier one. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctional_norm_apply_le` | `‖(Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) f‖ ≤ 4 * ‖f‖` | For a positive order and a continuous input, the cusp functional is bounded by four times the uniform norm. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fourier_pair_eq` | `(↑A - ↑B * Complex.I) / 2 * (fourier ↑k) ↑t + (↑A + ↑B * Complex.I) / 2 * (fourier (-↑k)) ↑t = ↑(A * Real.cos (2 * ↑k * t) + B * Real.sin (2 * ↑k * t))` | For two real coefficients, a frequency, and a circle coordinate, the conjugate Fourier pair equals the corresponding real sine--cosine mode. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.exists_fourierPoly_sinDouble` | `p.natDegree ≤ n → ∃ q, (∀ (t : ℝ), q ↑t = ↑(Polynomial.eval (Real.sin (2 * t)) p)) ∧ (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.cuspFunctionalCLM n hn) q = 0` | For a polynomial of degree at most a positive order, there is a corresponding Fourier polynomial that agrees after sine composition and is annihilated by the cusp functional. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.circleDouble` | `C(AddCircle Real.pi, AddCircle Real.pi)` | The circle-doubling map is the continuous map from the additive circle of period π to itself that sends each point to twice that point. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.fourier_comp_circleDouble` | `(fourier j).comp Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.FejerCertificate.circleDouble = fourier (2 * j)` | For a Fourier frequency, composition with circle doubling doubles that frequency. |
<!-- /GEN -->

### Measure duality and prior projections

<!-- GEN:Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.Duality -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSymmetric` | `MeasureTheory.Measure ℝ → Prop` | For a Borel measure on the real line, symmetry about zero means that reflecting every point through zero leaves the measure unchanged. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSupportedOnUnitInterval` | `MeasureTheory.Measure ℝ → Prop` | For a measure on the real line, support on the unit interval means that the complement of the closed interval from −1 to 1 has measure zero. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsExtremalDecomposition` | `ℕ → Type` | Extremal signed-measure data represented by its positive and negative parts. Each part has mass `1/2`, is symmetric and supported on `[-1,1]`; their degree-`K` moments agree, while their absolute first moments differ by `E_K`. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.exists_absExtremalDecomposition` | `Nonempty (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsExtremalDecomposition K)` | For a polynomial degree limit, a normalized extremal signed-measure decomposition exists. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsMomentMatchedPriors` | `ℕ → Type` | Two symmetric probability measures on `[-1,1]` whose moments match through degree `K` and whose absolute first moments have the oriented gap `2 E_K`. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsExtremalDecomposition.toMomentMatchedPriors` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsMomentMatchedPriors K` | For a nonnegative moment-degree limit and an absolute-value extremal decomposition at that limit, the associated pair of moment-matched priors has first prior equal to twice the decomposition's negative measure and second prior equal to twice its positive measure. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.exists_symmetric_momentMatched_absGap` | `0 < K → Even K → Nonempty (Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.AbsMomentMatchedPriors K)` | For a positive even degree, a symmetric moment-matched probability-prior pair with the exact absolute-moment gap exists. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorZero_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure P.ν₀` | For a packaged prior pair, its first prior is a probability measure. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorOne_isProbabilityMeasure` | `MeasureTheory.IsProbabilityMeasure P.ν₁` | For a packaged prior pair, its second prior is a probability measure. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorZero_mass` | `P.ν₀ Set.univ = 1` | For a packaged prior pair, its first prior has total mass one. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorOne_mass` | `P.ν₁ Set.univ = 1` | For a packaged prior pair, its second prior has total mass one. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorZero_supported` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSupportedOnUnitInterval P.ν₀` | For a packaged prior pair, its first prior is supported on the unit interval. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorOne_supported` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSupportedOnUnitInterval P.ν₁` | For a packaged prior pair, its second prior is supported on the unit interval. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorZero_symmetric` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSymmetric P.ν₀` | For a packaged prior pair, its first prior is symmetric about zero. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.priorOne_symmetric` | `Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.IsSymmetric P.ν₁` | For a packaged prior pair, its second prior is symmetric about zero. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.prior_moments_eq` | `j ≤ K → ∫ (x : ℝ), x ^ j ∂P.ν₀ = ∫ (x : ℝ), x ^ j ∂P.ν₁` | For a packaged prior pair and a moment order no larger than the degree limit, the two priors have equal moments of that order. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.prior_absMoment_gap` | `∫ (x : ℝ), \|x\| ∂P.ν₁ - ∫ (x : ℝ), \|x\| ∂P.ν₀ = 2 * Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For a packaged prior pair, the oriented difference in absolute first moments is twice the best approximation error. |
<!-- /GEN -->

### Inverse-degree approximation rate

<!-- GEN:Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.Rate -->
| Decl | Signature | Description |
|---|---|---|
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_upper` | `0 < K → Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K ≤ 1 / ↑K` | For a positive polynomial degree, the best absolute-value approximation error is at most one divided by that degree. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_lower` | `0 < K → 1 / 100 / ↑K ≤ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K` | For a positive polynomial degree, the cusp of absolute value forces the stated inverse-degree lower bound. |
| `Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs_order` | `∃ c C, 0 < c ∧ c ≤ C ∧ ∀ (K : ℕ), 0 < K → c / ↑K ≤ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K ∧ Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality.bestUniformApproxErrorAbs K ≤ C / ↑K` | There are universal positive constants that sandwich the best absolute-value approximation error between constant multiples of the reciprocal degree for every positive degree. |
<!-- /GEN -->

<!-- GEN:Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality -->
_(no documented declarations in Causalean.Mathlib.Analysis.AbsoluteValueMomentPriorDuality)_
<!-- /GEN -->
