# Shell scripts and the toolchain pin must stay LF on every platform: a Windows clone with
# core.autocrlf=true would otherwise check them out CRLF, and Git Bash cannot run a CRLF script
# (`$'\r': command not found`). Everything else follows the client's line-ending setting.
*.sh text eol=lf
lean-toolchain text eol=lf
